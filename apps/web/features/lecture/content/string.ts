import type { TutorialData } from "@/types";

export const string = {
  id: 1,
  title: "字串（KMP/Z函式/Manacher/字串雜湊/AC自動機/後綴陣列/子序列自動機）",
  src: "https://leetcode.cn/circle/discuss/SJFwQI/",
  last_update: "2025-11-07T23:47:26.098808+00:00",
  summary:
    "本題單系統梳理字串演算法核心內容，涵蓋 KMP、Z 函式、Manacher、字串雜湊、最小表示法、字典樹、AC 自動機、後綴陣列/自動機、子序列自動機等十大模組。\n\n**本題單涵蓋**：\n- KMP\n- Z函式\n- Manacher\n- 字串雜湊\n- AC自動機\n- 後綴陣列\n- 子序列自動機\n\n:::example KMP：計算 π 陣列（最長 border / 字首函式）\n**C++ 模板（KMP）**：\n\n```cpp\n// KMP：計算 π 陣列（最長 border / 字首函式）\nvector<int> kmpBuild(const string& s) {\n    int n = s.size();\n    vector<int> pi(n, 0);\n    for (int i = 1; i < n; ++i) {\n        int j = pi[i - 1];\n        while (j > 0 && s[i] != s[j]) {\n            j = pi[j - 1];\n        }\n        if (s[i] == s[j]) {\n            ++j;\n        }\n        pi[i] = j;\n    }\n    return pi;\n}\n```\n:::\n\n\n\n字串算法的總覽與模板索引：從匹配（KMP／Z）、迴文（Manacher）、雜湊到 AC 自動機與後綴結構。\n\n**路線**：先穩定寫出 KMP 與雙模雜湊，再視題目深度補 Manacher 與自動機。\n",
  children: [
    {
      id: 3,
      title: "1. KMP（字首的字尾）",
      summary:
        "KMP 演算法透過預處理模式串的 π 陣列（最長 border 長度），實現文字串中模式串的線性時間匹配。適用於子串匹配、旋轉字串、重複子串等場景。模板題為 28. 找出字串中第一個匹配項的下標。\n\n:::example 建立 KMP 前綴陣列（pi 陣列）\n**C++ 模板**：\n\n```cpp\n// 建立 KMP 前綴陣列（pi 陣列）\nvector<int> kmpBuild(const string& pattern) {\n    int n = pattern.size();\n    vector<int> pi(n);\n    for (int i = 1; i < n; ++i) {\n        int j = pi[i - 1];\n        while (j > 0 && pattern[i] != pattern[j]) {\n            j = pi[j - 1];\n        }\n        if (pattern[i] == pattern[j]) {\n            ++j;\n        }\n        pi[i] = j;\n    }\n    return pi;\n}\n\n// KMP 匹配：在 text 中找 pattern 的所有出現位置\nvector<int> kmpSearch(const string& text, const string& pattern) {\n    vector<int> pi = kmpBuild(pattern);\n    vector<int> res;\n    int j = 0;\n    for (int i = 0; i < (int)text.size(); ++i) {\n        while (j > 0 && text[i] != pattern[j]) {\n            j = pi[j - 1];\n        }\n        if (text[i] == pattern[j]) {\n            ++j;\n        }\n        if (j == (int)pattern.size()) {\n            res.push_back(i - j + 1);\n            j = pi[j - 1];\n        }\n    }\n    return res;\n}\n```\n:::",
      children: [
        {
          id: 4,
          title: "1.1 基礎",
          summary:
            '## 這個技術解決什麼問題\n\n在長字串裡找模式串第一次（或所有）出現的位置。\n\n暴力做法把模式串貼到每個起點逐字比對，失敗就整個往右挪一格從頭再比——**已經比對成功的資訊全部丟掉**，最壞 `O(nm)`。\n\nKMP 的槓桿是：比對失敗時，模式串已配對成功的那段前綴本身是有結構的。如果它有一個「前綴等於後綴」的段（稱為 **border**），就可以只退到那個 border 的長度，繼續往下比，不必回到起點。主串指標因此**永不回退**，總時間降到 `O(n + m)`。\n\nborder 的用途遠不只字串匹配——週期判定（459）、最短補成迴文（214）本質都是 border 問題。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往 KMP / border 想：\n\n- **要在字串裡找子串、判斷某段是否為週期性重複、或處理「前綴等於後綴」的結構。** 459、214 都是 border 的直接應用。\n- **暴力比對會反覆從頭重比同一段前綴而超時。** 一旦發現「失敗後想保留已比對成功的資訊」，就是 KMP 的味道。\n- **題目問「最短可以補成迴文 / 週期」這類字面看似無關，但其實本質是 border。** 例如把字串反轉後接起來找最長 border。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要找的「前綴等於後綴」結構是什麼？`pi` 的每一格代表哪一段前綴？\n2. 比對失敗時，我的 `j` 該退回哪裡？為什麼主串指標不用退？\n3. 答案是「位置」「週期長度」還是「border 長度」？它從 `pi` 的哪一格讀出？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 28 | 找出字串中第一個匹配項的下標 | N/A | 重點是重用已比對資訊；練習把 border、LCP、hash 或自動機狀態寫成不變式。 |\n| 214 | 最短迴文串 | N/A | 要利用左右對稱；練習同時處理奇偶中心、半徑或區間狀態。 |\n| 459 | 重複的子字串 | N/A | 重點是重用已比對資訊；練習把 border、LCP、hash 或自動機狀態寫成不變式。 |\n| 686 | 重複疊加字串匹配 | N/A | 重點是重用已比對資訊；練習把 border、LCP、hash 或自動機狀態寫成不變式。 |\n| 796 | 旋轉字串 | N/A | 重點是重用已比對資訊；練習把 border、LCP、hash 或自動機狀態寫成不變式。 |\n\n## 核心想法與直覺\n\n本節要解決的場景是：在一段長字串裡找另一段短字串（叫做「模式串」）第一次出現在哪。例如在 `"aabaabaaf"` 裡找 `"aabaaf"`，這就是 LeetCode 28「找出字串中第一個匹配項的下標」。\n\n最直覺的暴力做法是：把模式串貼到每一個起點，逐字比對；只要中途有一個字元不合，就把模式串整個往右挪一格，從頭再比一次。問題是「從頭再比」浪費了已經比對成功的資訊——前面那串明明已經比對相同了，挪一格卻全部丟掉重來，最壞會是 `O(nm)`。\n\nKMP 的核心想法是：**比對失敗時，不要把模式串退回起點，而是利用「已經配對成功的那段前綴」本身的結構，只退到一個剛好還能接續的位置。**\n\n:::example border 的意思：既是前綴、又是後綴的最長那段\n關鍵概念叫 border（中文常稱「最長相等前後綴」）：對模式串的某個前綴，找出「既是它的前綴、又是它的後綴」的最長一段（不能是整段自己）。例如 `"aabaa"` 的 border 是 `"aa"`，長度 2。KMP 先把每個前綴的 border 長度算成一個陣列 `pi`（也叫 next 陣列）。\n\n```cpp\n// border 的意思：既是前綴、又是後綴的最長那段\n// "aabaa" -> 前綴 "aa" == 後綴 "aa"，所以 pi = 2\n// 比對到第 5 個字失敗時，不必退回 0，退回到長度 2 即可繼續\n```\n:::\n\n\n\n有了 `pi`，比對失敗時就「跳」到 `pi` 指示的位置，已比對好的字元不再重看，整體變成 `O(n + m)`。\n\n## 狀態／資料結構定義\n\n**前綴函數 `pi`（也叫 next 陣列、失配陣列）**：`pi[i]` = 子串 `s[0..i]` 的**最長真 border** 長度，也就是「既是它的前綴、又是它的後綴，且不等於自己」的最長那一段的長度。\n\n例：`s = "aabaab"`，`pi = [0, 1, 0, 1, 2, 3]`。`pi[5] = 3` 表示 `"aab"` 同時是 `"aabaab"` 的前綴與後綴。\n\n**長度與下標要分清**：`pi[i]` 是**長度**，所以下一個要比對的模式串字元下標就是 `pi[i]`（0-based 下，長度 `k` 的前綴的下一個字元下標正好是 `k`）。這是 KMP 最容易寫錯的一點。\n\n**匹配時的指標**：`i` 走主串（只增不減），`j` 走模式串（失配時退回 `pi[j-1]`）。`j == m` 時找到一次匹配，接著把 `j` 退到 `pi[m-1]` 繼續找下一個。\n\n**週期公式**：長度 `n` 的字串，若 `n % (n - pi[n-1]) == 0` 且 `pi[n-1] > 0`，則它由長度 `n - pi[n-1]` 的子串重複而成（459）。\n\n## 不變量或正確性證明\n\n**建表的不變量**：計算 `pi[i]` 時，`j` 始終是「`s[0..i-1]` 的某個 border 長度」，而且是遞減枚舉的。\n\n流程是：從 `j = pi[i-1]` 開始，若 `s[i] == s[j]` 則 `pi[i] = j + 1`；否則 `j = pi[j-1]` 退到下一個更短的 border，重複。\n\n**為什麼退回 `pi[j-1]` 就能枚舉所有 border**：關鍵引理是「`s[0..i]` 的所有 border 長度，恰好是 `pi[i]`、`pi[pi[i]-1]`、`pi[pi[pi[i]-1]-1]`…這條鏈」。證明：若 `k` 是一個 border 長度且 `k < pi[i]`，則長度 `k` 的前綴既是 `s[0..i]` 的前綴也是後綴；因為它也在長度 `pi[i]` 的那個 border 內部同時當前綴與後綴，所以 `k` 必是 `s[0..pi[i]-1]` 的 border，即 `k <= pi[pi[i]-1]`。反覆套用即得整條鏈，且鏈上沒有遺漏。因此**退回必須用 `while` 而不是 `if`**——可能連退好幾次。\n\n**匹配的正確性**：不變量是「任一時刻，`j` 等於已匹配的模式串前綴長度，且 `s[i-j..i-1] == p[0..j-1]`」。失配時退回 `pi[j-1]`，由 border 定義，`p[0..pi[j-1]-1]` 仍等於剛才匹配上的那段後綴，所以不變量維持，而且**沒有跳過任何可能的起點**——若某個更長的重疊也能匹配，它對應的長度就會是一個更大的 border，早就被枚舉到了。\n\n**均攤 `O(n + m)`**：`j` 每次只增加 1（每輪最多一次），而退回只會減少 `j`。因為 `j >= 0`，退回的總次數不超過增加的總次數，即 `O(n)`。這與單調堆疊的攤還論證是同一套。\n\n## 逐步演算法\n\n:::example 計算 pi（next）陣列：pi[i] = 前綴 p[0..i] 的最長 border…\n**1. 計算模式串的 `pi` 陣列。** `pi[i]` 表示「模式串前 `i + 1` 個字元」這段前綴的 border 長度。用兩個指標自我比對：`i` 往前掃，`len` 記目前 border 長度，失敗就退回 `pi[len - 1]`。\n\n```cpp\n// 計算 pi（next）陣列：pi[i] = 前綴 p[0..i] 的最長 border 長度\nvector<int> buildPi(const string& p) {\n    vector<int> pi(p.size(), 0);\n    for (int i = 1, len = 0; i < (int)p.size();) {\n        if (p[i] == p[len]) {\n            pi[i++] = ++len;  // 延長 border\n        } else if (len) {\n            len = pi[len - 1];  // 退回更短的 border\n        } else {\n            pi[i++] = 0;  // 退無可退\n        }\n    }\n    return pi;\n}\n```\n:::\n\n\n\n:::example 用 pi 在主串 s 裡找模式串 p，回傳第一個起點下標\n**2. 用 `pi` 掃主串比對。** 用一個指標走主串 `s`，`j` 記目前在模式串配對到哪。配對失敗時，`j` 退回 `pi[j - 1]`，主串指標不回頭。\n\n```cpp\n// 用 pi 在主串 s 裡找模式串 p，回傳第一個起點下標\nint strStr(const string& s, const string& p) {\n    auto pi = buildPi(p);\n    for (int i = 0, j = 0; i < (int)s.size(); ++i) {\n        while (j && s[i] != p[j]) {\n            j = pi[j - 1];  // 失敗就跳\n        }\n        if (s[i] == p[j]) {\n            ++j;\n        }\n        if (j == (int)p.size()) {\n            return i - j + 1;  // 完全配對\n        }\n    }\n    return -1;\n}\n```\n:::\n\n\n\n**3. 邊界與複雜度。**\n\n- `pi` 的退回是「鏈式」的：可能連退好幾次，所以用 `while`，不是 `if`。\n- 主串指標永遠只前進，`j` 退回的總次數不超過前進次數，所以是均攤 `O(n + m)`。\n- 應用變體：LeetCode 459「重複的子字串」可由 `n % (n - pi[n - 1]) == 0` 判斷；686「重複疊加」與 214「最短迴文串」也都靠 border 的性質。\n\n## C++17 模板\n\n以下用本節代表題 28. 找出字串中第一個匹配項的下標 展示核心寫法。\n\n**完整題目**：在字串 `haystack` 中尋找字串 `needle` 第一次出現的位置，若不存在則回傳 `-1`。\n\n:::example KMP 字串匹配：先建立 needle 的前綴函數 pi，再在 haystack 上做線…\n**線索**：題目需要大量子串匹配、前後綴或相等比較；重用已比較資訊就是 KMP/Z/雜湊/自動機的線索。\n\n```cpp\nclass Solution {\npublic:\n    // KMP 字串匹配：先建立 needle 的前綴函數 pi，再在 haystack 上做線性搜尋\n    int strStr(string haystack, string needle) {\n            int m = needle.size();\n            vector<int> pi(m);  // pi[i]：needle[0..i] 的最長相等真前後綴長度\n            // 建構 pi 陣列\n            for (int i = 1; i < m; ++i) {\n                    int j = pi[i - 1];\n                    // 若失配，利用 pi 回退到上一個可能匹配的位置\n                    while (j && needle[i] != needle[j]) {\n                            j = pi[j - 1];  // 失配時回退，不從頭重新匹配\n                    }\n                    if (needle[i] == needle[j]) {\n                            ++j;\n                    }\n                    pi[i] = j;\n            }\n            // 在 haystack 中用 pi 輔助匹配\n            for (int i = 0, j = 0; i < (int)haystack.size(); ++i) {\n                    while (j && haystack[i] != needle[j]) {\n                            j = pi[j - 1];\n                    }\n                    if (haystack[i] == needle[j]) {\n                            ++j;\n                    }\n                    if (j == m) {  // 完整匹配，返回起始位置\n                            return i - m + 1;\n                    }\n            }\n            return -1;\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- `pi[i]` 表示前綴的最長 border；失配時跳到下一個可能 border，而不是回頭重比。\n- 預處理陣列或狀態機保存已比對資訊，主迴圈失配時不用回到原點重比。\n- 下標與長度的換算要固定，尤其是 border、LCP、半徑與回傳起點。\n\n## 時間與空間複雜度\n\n**建表 `O(m)`**、**匹配 `O(n)`**，合計 **`O(n + m)`**。空間 `O(m)`（只需 `pi` 陣列）。\n\n對比暴力的 `O(nm)`——`n = m = 1e5` 時是 `1e10` 對 `2e5`。\n\n**均攤論證是關鍵**：內層的 `while` 看起來讓複雜度變成巢狀，但如上所述，`j` 的總減少量受總增加量（`<= n`）限制，所以 while 的總迭代次數是 `O(n)` 而非 `O(nm)`。\n\n**應用的成本**：\n- 459 重複的子字串：建一次 `pi` 後 `O(1)` 判斷，總計 `O(n)`。\n- 214 最短迴文串：把 `s + \'#\' + reverse(s)` 建 `pi`，最後一格就是 `s` 的最長迴文前綴長度，`O(n)`。分隔符 `#` 必須是兩邊都不出現的字元，否則 border 會跨界而算錯。\n- 686 重複疊加：在重複若干次的主串上跑 KMP，重複次數的上界要先推導出來。\n\n## 常見錯誤與邊界條件\n\n- 索引從 0 開始時 border 長度與下標要分清。\n- 雜湊建議雙模或 unsigned long long，並理解碰撞風險。\n- 字元集大時不要硬開 26。\n- 空模式或長度 1 要特判。\n\n## 常見變形\n\n- KMP 處理模式串 border。\n- Z 函式處理每個後綴和整串的 LCP。\n- 雜湊適合快速判等，AC 自動機適合多模式。\n\n## 與相似技巧的比較\n\n- **對比 Z 函式**（下一節）：兩者資訊等價、都是 `O(n)`，可以互相轉換。KMP 的 `pi[i]` 講「前綴的後綴」，Z 的 `z[i]` 講「後綴的前綴」。做匹配時 Z 函式的寫法更直白（接起來算一次就好），但 KMP 的 `pi` 在週期、border 鏈相關問題上更順手。\n- **對比字串雜湊**：雜湊做匹配是 `O(n + m)` 期望時間、程式更短，但有碰撞機率，且回答不了「border 鏈」這類結構問題。要求確定性正確就用 KMP。\n- **對比 `std::string::find`**：多數實作是 `O(nm)` 最壞（樸素或 Boyer-Moore-Horspool），實務上很快但沒有最壞保證。競賽中資料刻意構造時會被卡。\n- **對比 AC 自動機**：要同時匹配**多個**模式串時，AC 自動機是 KMP 的推廣（把 `pi` 換成 trie 上的 fail 指標）。單一模式串用 KMP 就夠。\n- **對比後綴自動機／後綴陣列**：能回答更廣的子串查詢，但建構與理解成本高得多。單純匹配別用大砲。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 28 | [找出字串中第一個匹配項的下標](https://leetcode.cn/problems//find-the-index-of-the-first-occurrence-in-a-string/) |  |\n| 214 | [最短迴文串](https://leetcode.cn/problems//shortest-palindrome/) |  |\n| 459 | [重複的子字串](https://leetcode.cn/problems//repeated-substring-pattern/) |  |\n| 686 | [重複疊加字串匹配](https://leetcode.cn/problems//repeated-string-match/) |  |\n| 796 | [旋轉字串](https://leetcode.cn/problems//rotate-string/) |  |\n| 1392 | [最長快樂字首](https://leetcode.cn/problems//longest-happy-prefix/) | 1876 |\n| 1397 | [找到所有好字串](https://leetcode.cn/problems//find-all-good-strings/) | 2667 |\n| 1668 | [最大重複子字串](https://leetcode.cn/problems//maximum-repeating-substring/) | 1396 |\n| 1764 | [透過連線另一個陣列的子陣列得到一個陣列](https://leetcode.cn/problems//form-array-by-concatenating-subarrays-of-another-array/) | 1588 |\n| 2800 | [包含三個字串的最短字串](https://leetcode.cn/problems//shortest-string-that-contains-three-strings/) | 1856 |\n| 3008 | [找出陣列中的美麗下標 II](https://leetcode.cn/problems//find-beautiful-indices-in-the-given-array-ii/) | 2016 |\n| 3036 | [匹配模式陣列的子陣列數目 II](https://leetcode.cn/problems//number-of-subarrays-that-match-a-pattern-ii/) | 1895 |\n| 3037 | [在無限流中尋找模式 II](https://leetcode.cn/problems//find-pattern-in-infinite-stream-ii/) |  |\n| 3455 | [最短匹配子字串](https://leetcode.cn/problems//shortest-matching-substring/) | 2303 |\n| 3529 | [統計水平子串和垂直子串重疊格子的數目](https://leetcode.cn/problems//count-cells-in-overlapping-horizontal-and-vertical-substrings/) | 2105 |\n| 3571 | [最短超級串 II](https://leetcode.cn/problems//find-the-shortest-superstring-ii/) |  |\n\n## 本節重點速查\n\n- `pi[i]` = `s[0..i]` 的**最長真 border 長度**（既是前綴又是後綴）。\n- 建表時從 `j = pi[i-1]` 開始，相等就 `j+1`，否則 `j = pi[j-1]` 繼續退——**必須用 `while`，因為 border 鏈可能連退好幾次**。\n- 匹配時主串指標**永不回退**，`j` 失配才退，均攤 `O(n + m)`。\n- `pi[i]` 是**長度**，下一個要比的模式串下標正好也是它（0-based），別混淆長度與下標。\n- **週期判定**：`n % (n - pi[n-1]) == 0` 且 `pi[n-1] > 0` ⟹ 由長度 `n - pi[n-1]` 的子串重複而成（459）。\n- 拼接字串時分隔符要用兩邊都不出現的字元，否則 border 會跨界。\n- 空模式與長度 1 要特判。',
        },
      ],
    },
    {
      id: 5,
      title: "2. Z 函式（字尾的字首）",
      summary:
        "Z 函式計算字尾與原串的最長公共字首（LCP），可用於子串匹配。構造 `pattern + separator + text`（分隔符不可出現在模式串或文字串中）；若文字起點 `i` 對應的 $z[m+1+i] \\geq m$，則匹配成功。它可替代 KMP 處理單模式匹配，也適用於 2223 等前綴與後綴比較題。\n\n:::example Z 函式：z[i] = s[i..] 與 s 的最長公共字首長度\n**C++ 模板**：\n\n```cpp\n// Z 函式：z[i] = s[i..] 與 s 的最長公共字首長度\nvector<int> zFunction(const string& s) {\n    int n = s.size();\n    vector<int> z(n, 0);\n    int l = 0, r = 0;\n    for (int i = 1; i < n; ++i) {\n        if (i < r) {\n            z[i] = min(r - i, z[i - l]);\n        }\n        while (i + z[i] < n && s[z[i]] == s[i + z[i]]) {\n            z[i]++;\n        }\n        if (i + z[i] > r) {\n            l = i;\n            r = i + z[i];\n        }\n    }\n    return z;\n}\n```\n:::",
      children: [
        {
          id: 6,
          title: "2.1 基礎",
          summary:
            "## 這個技術解決什麼問題\n\n一次求出「對每一個起點 `i`，從 `i` 開始的後綴與整個字串的最長共同前綴有多長」。\n\n這正是 `z[i]` 的定義。它回答的是一類反覆出現的問題——「某個後綴和開頭前綴相同多少」（2223 構造字串的總得分和直接就是 `Σ z[i]`）。\n\n逐位重比是 `O(n^2)`。Z 函式用一個「已知的最右迴文式匹配區間」把已算過的結果借過來用，壓到 `O(n)`。\n\n它也能做字串匹配：把 `模式串 + 分隔符 + 主串` 接起來算一次 Z，`z[i] == m` 的位置就是一次匹配。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往 Z 函式想：\n\n- **題目反覆問「某個後綴 / 某個位置開始的子串，和開頭前綴相同多少」。** 2223、3045（前後綴下標對）都是這類「和前綴比」的問題。\n- **需要一次求出「對每個起點」的共同前綴長度，而不是只比一次。** 暴力逐位重比會 `O(n^2)`，Z 函式壓到 `O(n)`。\n- **匹配 / 週期 / 恢復初始狀態類問題**（如 3031）常可化成「前綴與後綴對齊」，Z 函式與 KMP 在此常可互換。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要比較的「基準前綴」是哪一段？`z[i]` 對我代表什麼長度？\n2. 我有沒有正確維護 Z-box `[l, r]`，並在借用時加上 `min`？\n3. 答案是某個 `z[i]`、它們的總和、還是「`z[i]` 等於某長度」的位置數？\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n本節要算的是字串的「Z 函式」。給一個字串 `s`，`z[i]` 定義為：**從位置 `i` 開始的後綴，和 `s` 整體（從頭開始）最長能相同多少個字元**。例如 `s = \"aabxaab\"`，`z[4] = 3`，因為從第 4 位開始的 `\"aab\"` 和開頭的 `\"aab\"` 相同三個字。（習慣上 `z[0]` 不定義或設為整個長度。）\n\n為什麼有用？因為 `z[i]` 等於「開頭這段前綴」和「位置 `i` 這段」的最長共同前綴長度。把「模式串 + 分隔符 + 主串」接成一個字串算 Z 函式，就能一次找出模式串在主串中所有出現位置。\n\n:::example Z-box 的概念：[l, r] 是已知「s[l..r] == s[0..r-l]」的最…\n暴力算每個 `z[i]` 都從頭逐字比，是 `O(n^2)`。Z 函式的核心想法是：**維護一個「目前已知、最靠右、且確定和開頭相同」的區間 `[l, r]`（叫 Z-box），新位置可以借用這個區間裡已經算過的結果，避免重比。**\n\n```cpp\n// Z-box 的概念：[l, r] 是已知「s[l..r] == s[0..r-l]」的最右區間\n// 算 z[i] 時，若 i 落在 [l, r] 內，s[i] 對應到 s[i-l]\n// 於是可以先抄 z[i-l] 當作起點，再往後嘗試延伸\n```\n:::\n\n\n\n借用已算結果，使得每個字元最多被「往右延伸」掃過一次，總複雜度降為 `O(n)`。\n\n## 狀態／資料結構定義\n\n**`z[i]`**：`s[i..]` 與 `s[0..]` 的最長共同前綴長度。習慣上 `z[0]` 設為 `n`（或不定義）。\n\n**Z-box `[l, r]`**：目前已知的、右端最靠右的一段「與前綴相同」的區間，即 `s[l..r] == s[0..r-l]`。這是整個演算法的記憶體——所有加速都來自它。\n\n**核心轉移**：處理 `i` 時，若 `i <= r`，則 `i` 落在已知的 Z-box 內，它在前綴中的對應位置是 `i - l`，於是可以先取\n\n`z[i] = min(r - i + 1, z[i - l])`\n\n再從這個長度往後親自比對延伸。若 `i > r` 則沒有可借用的資訊，從 0 開始暴力延伸。\n\n延伸完後若 `i + z[i] - 1 > r`，就更新 `[l, r] = [i, i + z[i] - 1]`。\n\n**分隔符**（做匹配時）：`'#'` 必須是**兩串都不會出現**的字元，否則匹配長度會越界而算錯。\n\n## 不變量或正確性證明\n\n**Z-box 不變量**：任一時刻 `s[l..r] == s[0..r-l]`，且 `r` 是所有已處理位置中能達到的最右端。\n\n**`min` 為什麼不能少**：設 `i <= r`，令 `k = i - l`。因為 `s[l..r]` 與前綴逐字相同，所以 `s[i..r]` 與 `s[k..r-l]` 逐字相同。\n\n- 若 `z[k] < r - i + 1`：`z[k]` 在 Z-box 內部就已經結束（第 `k + z[k]` 位與前綴不符），而這一位仍在已驗證的範圍內，所以 `z[i] = z[k]` **確定**，不必再比。\n- 若 `z[k] >= r - i + 1`：`z[i]` 至少是 `r - i + 1`（這一段已驗證），但**超出 `r` 的部分從未被驗證過**，必須親自逐字比。若直接寫 `z[i] = z[k]` 就會用上未驗證的資訊而出錯。\n\n這正是 `min(r - i + 1, z[i - l])` 中 `min` 的意義——**只信任已驗證的範圍**。\n\n**`O(n)` 的攤還論證**：`while` 迴圈每成功延伸一步，`r` 至少增加 1；而 `r` 從 0 單調遞增到最多 `n - 1`，所以所有延伸的總步數不超過 `n`。每個 `i` 的失敗比對只有一次。因此總時間 `O(n)`。\n\n**匹配的正確性**：在 `p + '#' + s` 上，若 `z[i] == m`（`m = |p|`），代表從 `i` 開始有 `m` 個字元與 `p` 相同。因為 `'#'` 不在任一串中出現，`z[i]` 不可能超過 `m`，所以 `z[i] == m` 恰好對應一次完整匹配。\n\n## 逐步演算法\n\n:::example 計算 Z 函式：z[i] = s 從 i 起與 s 整體的最長共同前綴\n**1. 維護 Z-box 並計算 `z`。** 對每個 `i`：若 `i` 在 `[l, r]` 內，先用 `z[i - l]` 當作可信賴的下界；接著用 `while` 嘗試把這段往後延伸；若延伸超過 `r`，就更新 Z-box。\n\n```cpp\n// 計算 Z 函式：z[i] = s 從 i 起與 s 整體的最長共同前綴\nvector<int> zFunction(const string& s) {\n    int n = s.size();\n    vector<int> z(n, 0);\n    for (int i = 1, l = 0, r = 0; i < n; ++i) {\n        if (i < r) {\n            z[i] = min(r - i, z[i - l]);  // 借用已算結果\n        }\n        while (i + z[i] < n && s[z[i]] == s[i + z[i]]) {\n            z[i]++;  // 嘗試延伸\n        }\n        if (i + z[i] > r) {\n            l = i;\n            r = i + z[i];\n        }  // 更新最右 Z-box\n    }\n    return z;\n}\n```\n:::\n\n\n\n:::example 用 Z 函式找 pattern 在 text 的所有出現\n**2. 用 Z 函式做字串匹配。** 把 `pattern + '#' + text` 接起來算 `z`；凡是 `z[i]` 等於 `pattern` 長度的位置，就代表 `pattern` 在 `text` 的對應處完整出現。\n\n```cpp\n// 用 Z 函式找 pattern 在 text 的所有出現\nstring combined = pattern + '#' + text;  // '#' 不在字母表中\nauto z = zFunction(combined);\nint m = pattern.size();\nfor (int i = m + 1; i < (int)combined.size(); ++i) {\n    if (z[i] == m) { /* text 位置 i - m - 1 命中 */\n    }\n}\n```\n:::\n\n\n\n**3. 邊界與複雜度。**\n\n- 分隔符 `'#'` 必須是兩串都不會出現的字元，否則匹配長度會錯。\n- `z[i] = min(r - i, z[i - l])` 的 `min` 不能少：若 `z[i - l]` 想延伸超過 `r`，超出的部分尚未驗證過，必須親自比。\n- 複雜度 `O(n)`：`r` 只增不減，`while` 延伸的總步數受 `r` 增量限制。例如 2223「構造字串的總得分和」就是把每個 `z[i]` 直接加總。\n\n## C++17 模板\n\n以下用同型延伸示範題 28. 找出字串中第一個匹配項的下標 展示核心寫法。\n\n**完整題目**：在字串 `haystack` 中尋找字串 `needle` 第一次出現的位置，若不存在則回傳 `-1`。\n\n:::example KMP 字串匹配：先建立 needle 的前綴函數 pi，再在 haystack 上做線…\n**線索**：題目需要大量子串匹配、前後綴或相等比較；重用已比較資訊就是 KMP/Z/雜湊/自動機的線索。\n\n```cpp\nclass Solution {\npublic:\n    // KMP 字串匹配：先建立 needle 的前綴函數 pi，再在 haystack 上做線性搜尋\n    int strStr(string haystack, string needle) {\n            int m = needle.size();\n            vector<int> pi(m);  // pi[i]：needle[0..i] 的最長相等真前後綴長度\n            // 建構 pi 陣列\n            for (int i = 1; i < m; ++i) {\n                    int j = pi[i - 1];\n                    // 若失配，利用 pi 回退到上一個可能匹配的位置\n                    while (j && needle[i] != needle[j]) {\n                            j = pi[j - 1];  // 失配時回退，不從頭重新匹配\n                    }\n                    if (needle[i] == needle[j]) {\n                            ++j;\n                    }\n                    pi[i] = j;\n            }\n            // 在 haystack 中用 pi 輔助匹配\n            for (int i = 0, j = 0; i < (int)haystack.size(); ++i) {\n                    while (j && haystack[i] != needle[j]) {\n                            j = pi[j - 1];\n                    }\n                    if (haystack[i] == needle[j]) {\n                            ++j;\n                    }\n                    if (j == m) {  // 完整匹配，返回起始位置\n                            return i - m + 1;\n                    }\n            }\n            return -1;\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- `pi[i]` 表示前綴的最長 border；失配時跳到下一個可能 border，而不是回頭重比。\n- 預處理陣列或狀態機保存已比對資訊，主迴圈失配時不用回到原點重比。\n- 下標與長度的換算要固定，尤其是 border、LCP、半徑與回傳起點。\n\n## 時間與空間複雜度\n\n**時間 `O(n)`**：由上述攤還論證，`r` 單調不減且上界 `n`。\n\n**空間 `O(n)`**：`z` 陣列。做匹配時還要 `O(n + m)` 存拼接後的字串。\n\n對比暴力逐位比較的 `O(n^2)`——`n = 1e5` 時是 `1e10` 對 `1e5`。\n\n**與 KMP 的常數比較**：兩者都是線性，Z 函式的內層迴圈更單純（只有往右延伸，沒有 border 鏈的多次退回），實測常數略小。但 KMP 只需 `O(m)` 額外空間（不必拼接主串），做超長主串的串流匹配時更省。\n\n**應用成本**：\n- 2223 構造字串的總得分和：算一次 Z 再加總，`O(n)`；注意得分和會超過 `int`，用 `long long`。\n- 3045 前後綴下標對：對每個字串算 Z 或用 KMP 的 border，仍是線性。\n\n## 常見錯誤與邊界條件\n\n- 索引從 0 開始時 border 長度與下標要分清。\n- 雜湊建議雙模或 unsigned long long，並理解碰撞風險。\n- 字元集大時不要硬開 26。\n- 空模式或長度 1 要特判。\n\n## 常見變形\n\n- KMP 處理模式串 border。\n- Z 函式處理每個後綴和整串的 LCP。\n- 雜湊適合快速判等，AC 自動機適合多模式。\n\n## 與相似技巧的比較\n\n- **對比 KMP**：資訊等價、都是 `O(n)`，可互相轉換。Z 講「後綴的前綴」，`pi` 講「前綴的後綴」。**做匹配時 Z 更直白**（拼接後算一次即可）；**處理週期與 border 鏈時 `pi` 更順手**（459 用 `pi` 一行就判完）。\n- **對比字串雜湊**：雜湊能 `O(1)` 比較任意兩段，比 Z 更靈活；但有碰撞機率，且無法直接給出「最長共同前綴長度」（要再二分，多一個 `log`）。\n- **對比 LCP 陣列**（下一節）：`lcp[i][j]` 回答任意兩個起點的共同前綴，`O(n^2)` 空間；Z 只回答「與開頭前綴」的共同前綴，`O(n)` 空間。需求更窄，代價更低。\n- **對比後綴陣列 + LCP**：能回答任意兩後綴的 LCP，`O(n log n)` 建構。Z 函式是它在「其中一個後綴固定為整串」時的特例。\n- **對比 Manacher**：結構高度相似——都維護一個「最右已知區間」並借用對稱位置的結果。學會其中一個，另一個的論證幾乎照抄。\n\n## 代表例題\n\n**題號**：2223、3031、3045、3292、3303\n\n重點是重用已比對資訊；練習把 border、LCP、hash 或自動機狀態寫成不變式。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 2223 | [構造字串的總得分和](https://leetcode.cn/problems/sum-of-scores-of-built-strings/) | 2220 |\n| 3031 | [將單詞恢復初始狀態所需的最短時間 II](https://leetcode.cn/problems/minimum-time-to-revert-word-to-initial-state-ii/)（Premium） | 2278 |\n| 3045 | [統計前字尾下標對 II](https://leetcode.cn/problems/count-prefix-and-suffix-pairs-ii/)（Premium） | 2328 |\n| 3292 | [形成目標字串需要的最少字串數 II](https://leetcode.cn/problems/minimum-number-of-valid-strings-to-form-target-ii/)（Premium） | 2662 |\n| 3303 | [第一個幾乎相等子字串的下標](https://leetcode.cn/problems/find-the-occurrence-of-first-almost-equal-substring/)（Premium） | 2509 |\n| 3474 | [字典序最小的生成字串](https://leetcode.cn/problems/lexicographically-smallest-generated-string/) | 2605 |\n\n## 本節重點速查\n\n- `z[i]` = `s[i..]` 與整串前綴的最長共同前綴長度。\n- 靠 **Z-box `[l, r]`**（目前最右的「與前綴相同」區間）加速：`i <= r` 時先取 `z[i] = min(r - i + 1, z[i - l])`，再親自往後延伸。\n- **`min` 不能少**——超出 `r` 的部分從未驗證過，直接用 `z[i-l]` 會錯。\n- 延伸後若右端超過 `r` 就更新 Z-box。\n- `O(n)` 來自 `r` 單調不減的攤還論證。\n- 做匹配就把 `模式 + '#' + 主串` 接起來，`z[i] == m` 即一次匹配；\n- **分隔符必須是兩串都不出現的字元**。\n- 與 KMP 資訊等價：匹配用 Z 更直白，週期／border 鏈用 `pi` 更順手。",
        },
        {
          id: 7,
          title: "2.2 LCP 陣列",
          summary:
            '## 這個技術解決什麼問題\n\nDP 轉移裡需要**反覆比較兩個不同起點的子串**是否相等、相同多長、或誰的字典序較小。\n\n每次逐字比是 `O(n)`，問很多次就爆掉。這裡的解法很直接：先花 `O(n^2)` 把「任意兩個起點的最長共同前綴」全部預處理成一張表，之後每次查詢 `O(1)`。\n\n它的定位很明確——**當 `n` 只有幾千、但比較次數是 `O(n^2)` 甚至更多時，用空間換時間**。1977（劃分數字方案數）、2430、3388 都是這個形狀。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往 LCP 陣列想：\n\n- **DP 轉移裡需要「反覆比較兩個不同起點的子串是否相等 / 相同多長」。** 1977（劃分數字方案數）、2430、3388 都是 DP + 大量子串比較。\n- **字串長度不大（多半 `n ≤ 幾千`），但比較次數很多。** `O(n^2)` 建表可接受，換來每次比較 `O(1)`。\n- **題目自然出現「切成兩段並比較大小 / 相等」的劃分結構。** 比較大小時也可先用 LCP 找到第一個不同的位置，再比那一位。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我在 DP 裡到底要「比較哪兩個起點」的子串？查 `lcp[i][j]` 對應誰？\n2. 我要的是「相等」（`lcp >= L`）還是「比大小」（找第一個不同位）？\n3. `n^2` 的時間與空間在這題的資料範圍下吃得消嗎？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 1977 | [劃分數字的方案數（Premium）](https://leetcode.cn/problems/number-of-ways-to-separate-numbers/) | 2817 | 重點是重用已比對資訊；練習把 border、LCP、hash 或自動機狀態寫成不變式。 |\n| 2430 | [對字母串可執行的最大刪除數](https://leetcode.cn/problems/maximum-deletions-on-a-string/) | 2102 | 重點是重用已比對資訊；練習把 border、LCP、hash 或自動機狀態寫成不變式。 |\n| 3388 | [統計陣列中的美麗分割（Premium）](https://leetcode.cn/problems/count-beautiful-splits-in-an-array/) | 2365 | 重點是重用已比對資訊；練習把 border、LCP、hash 或自動機狀態寫成不變式。 |\n\n## 核心想法與直覺\n\n本節談 LCP 陣列。LCP 是 Longest Common Prefix（最長共同前綴）的縮寫：兩段字串從頭開始最多能相同幾個字元。例如 `"abcde"` 與 `"abcxy"` 的 LCP 是 3（`"abc"`）。\n\n很多字串題的瓶頸在於「反覆問兩個位置開始的子串相不相等 / 相同多長」。如果每次都逐字比，單次就 `O(n)`，問很多次就爆掉。這個技巧的核心想法是：**先用一張二維表，把「字串裡任意兩個起點的 LCP 長度」全部預處理好，之後每次查詢就是 `O(1)` 查表。**\n\n:::example LCP 遞推的直覺\n關鍵是這條遞推關係，從後往前填：\n\n```cpp\n// LCP 遞推的直覺：\n// 若 s[i] == s[j]，那 i、j 開始的共同前綴 = 1 + (i+1、j+1 的共同前綴)\n// 若 s[i] != s[j]，第一個字就不同，共同前綴 = 0\n// lcp[i][j] = (s[i]==s[j]) ? lcp[i+1][j+1] + 1 : 0;\n```\n:::\n\n\n\n因為 `lcp[i][j]` 只依賴右下角的 `lcp[i + 1][j + 1]`，所以只要從字串尾端往前、由內往外填，每一格都是 `O(1)`，總共 `O(n^2)` 建表、`O(n^2)` 空間。雖然不便宜，但換來「之後任意兩段比較都 `O(1)`」，在需要大量比較的 DP 題裡非常划算。\n\n## 狀態／資料結構定義\n\n**`lcp[i][j]`**：`s[i..]` 與 `s[j..]` 的最長共同前綴長度。\n\n**遞推式**（從右下往左上填）：\n\n```\nlcp[i][j] = (s[i] == s[j]) ? lcp[i+1][j+1] + 1 : 0\n```\n\n**多開一圈邊界**：宣告成 `vector<vector<int>> lcp(n + 1, vector<int>(n + 1, 0))`，`lcp[n][*]` 與 `lcp[*][n]` 天然是 0，免去越界判斷。\n\n**兩個常用查詢**：\n\n- **子串相等**：`s[i..i+len-1] == s[j..j+len-1]` ⟺ `lcp[i][j] >= len`。\n- **子串比大小**：令 `k = lcp[i][j]`；若 `k >= len` 則相等，否則比較 `s[i+k]` 與 `s[j+k]` 這一個字元即可定勝負。\n\n填表順序必須是 `i` 從 `n-1` 遞減、`j` 從 `n-1` 遞減，因為每格依賴右下角那一格。\n\n## 不變量或正確性證明\n\n**遞推式的正確性**：\n\n- 若 `s[i] != s[j]`，兩個後綴第一個字元就不同，共同前綴長度為 0。\n- 若 `s[i] == s[j]`，共同前綴至少有這一個字元；扣掉它之後，剩下要比的是 `s[i+1..]` 與 `s[j+1..]`，其最長共同前綴正是 `lcp[i+1][j+1]`。所以 `lcp[i][j] = lcp[i+1][j+1] + 1`。\n\n這是一個直接的結構性歸納——**任意兩後綴的 LCP，扣掉首字元後仍是兩後綴的 LCP**。\n\n**填表順序的必要性**：`lcp[i][j]` 依賴 `lcp[i+1][j+1]`（右下角），所以兩層迴圈都必須遞減。寫成遞增會讀到還沒填的格子（值為 0），答案全錯而且不會報錯——這是本節最容易犯又最難查的錯。\n\n**「比大小只要看一個字元」的正確性**：字典序比較的定義是「找第一個相異位」。`lcp[i][j] = k` 恰好說明前 `k` 位相同、第 `k` 位相異（或其中一方已結束）。所以比 `s[i+k]` 與 `s[j+k]` 就足夠，不必再往後看。這把 `O(n)` 的字典序比較降到 `O(1)`。\n\n## 逐步演算法\n\n:::example 建 LCP 表：lcp[i][j] = s 從 i 與從 j 開始的最長共同前綴\n**1. 建立 LCP 表。** 開一個 `(n + 1) × (n + 1)` 的表（多一圈當邊界 0），從 `i = n - 1`、`j = n - 1` 往左上填。\n\n```cpp\n// 建 LCP 表：lcp[i][j] = s 從 i 與從 j 開始的最長共同前綴\nint n = s.size();\nvector<vector<int>> lcp(n + 1, vector<int>(n + 1, 0));\nfor (int i = n - 1; i >= 0; i--) {\n    for (int j = n - 1; j >= 0; j--) {\n        if (s[i] == s[j]) {\n            lcp[i][j] = lcp[i + 1][j + 1] + 1;\n        }\n    }\n}\n```\n:::\n\n\n\n:::example 判斷 s[i..i+L) 與 s[j..j+L) 是否相等：一次查表\n**2. 用 `O(1)` 查詢判斷子串相等。** 想知道「從 `i` 起的長度 `L` 子串」是否等於「從 `j` 起的長度 `L` 子串」，只要看 `lcp[i][j] >= L`。\n\n```cpp\n// 判斷 s[i..i+L) 與 s[j..j+L) 是否相等：一次查表\nbool equalSub(int i, int j, int L, vector<vector<int>>& lcp) {\n    return lcp[i][j] >= L;  // 共同前綴夠長 => 兩段完全相同\n}\n```\n:::\n\n\n\n:::example 例：dp[i] 依賴「s[i..i+len) 是否等於 s[i+len..)」\n**3. 接到 DP 上。** 像 2430「對字母串可執行的最大刪除數」，狀態轉移時要判斷「前綴是否等於緊接其後的同長度段」，這個判斷正好用 `lcp` 直接 `O(1)` 取得，把整體從 `O(n^3)` 降到 `O(n^2)`。\n\n```cpp\n// 例：dp[i] 依賴「s[i..i+len) 是否等於 s[i+len..)」\n// 用 lcp[i][i+len] >= len 在 O(1) 判斷，避免重複逐字比較\nif (lcp[i][i + len] >= len) {\n    dp[i] = max(dp[i], dp[i + len] + 1);\n}\n```\n:::\n\n\n\n**邊界與複雜度。** 多開一圈邊界（值為 0）可免去越界判斷；建表與空間都是 `O(n^2)`，當 `n` 很大時要改用字串雜湊或後綴結構替代。\n\n## C++17 模板\n\n以下用同型延伸示範題 28. 找出字串中第一個匹配項的下標 展示核心寫法。\n\n**完整題目**：在字串 `haystack` 中尋找字串 `needle` 第一次出現的位置，若不存在則回傳 `-1`。\n\n:::example KMP 字串匹配：先建立 needle 的前綴函數 pi，再在 haystack 上做線…\n**線索**：題目需要大量子串匹配、前後綴或相等比較；重用已比較資訊就是 KMP/Z/雜湊/自動機的線索。\n\n```cpp\nclass Solution {\npublic:\n    // KMP 字串匹配：先建立 needle 的前綴函數 pi，再在 haystack 上做線性搜尋\n    int strStr(string haystack, string needle) {\n            int m = needle.size();\n            vector<int> pi(m);  // pi[i]：needle[0..i] 的最長相等真前後綴長度\n            // 建構 pi 陣列\n            for (int i = 1; i < m; ++i) {\n                    int j = pi[i - 1];\n                    // 若失配，利用 pi 回退到上一個可能匹配的位置\n                    while (j && needle[i] != needle[j]) {\n                            j = pi[j - 1];  // 失配時回退，不從頭重新匹配\n                    }\n                    if (needle[i] == needle[j]) {\n                            ++j;\n                    }\n                    pi[i] = j;\n            }\n            // 在 haystack 中用 pi 輔助匹配\n            for (int i = 0, j = 0; i < (int)haystack.size(); ++i) {\n                    while (j && haystack[i] != needle[j]) {\n                            j = pi[j - 1];\n                    }\n                    if (haystack[i] == needle[j]) {\n                            ++j;\n                    }\n                    if (j == m) {  // 完整匹配，返回起始位置\n                            return i - m + 1;\n                    }\n            }\n            return -1;\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- `pi[i]` 表示前綴的最長 border；失配時跳到下一個可能 border，而不是回頭重比。\n- 預處理陣列或狀態機保存已比對資訊，主迴圈失配時不用回到原點重比。\n- 下標與長度的換算要固定，尤其是 border、LCP、半徑與回傳起點。\n\n## 時間與空間複雜度\n\n**建表 `O(n^2)` 時間、`O(n^2)` 空間**；**每次查詢 `O(1)`**。\n\n適用門檻很清楚：`n = 3000` 時表有 `9e6` 格，用 `int` 是 36 MB——已經接近多數題目的記憶體上限。`n = 5000` 就會 MLE，得改用 `short` 或換方法。\n\n**什麼時候划算**：查詢次數 `Q` 遠大於 `n` 時。`Q` 次逐字比較是 `O(Qn)`；建表後是 `O(n^2 + Q)`。`Q = O(n^2)`（DP 的每個狀態轉移都要比一次）時，`O(n^2)` 完勝 `O(n^3)`。\n\n**`n` 很大時的替代**：\n- **字串雜湊**：`O(n)` 預處理、`O(1)` 比較相等（但要二分才能求 LCP 長度，多一個 `log`），空間 `O(n)`。\n- **後綴陣列 + LCP + 稀疏表**：`O(n log n)` 預處理、`O(1)` 求任意兩後綴的 LCP，空間 `O(n log n)`。正確且省空間，但實作長得多。\n\n## 常見錯誤與邊界條件\n\n- 索引從 0 開始時 border 長度與下標要分清。\n- 雜湊建議雙模或 unsigned long long，並理解碰撞風險。\n- 字元集大時不要硬開 26。\n- 空模式或長度 1 要特判。\n\n## 常見變形\n\n- KMP 處理模式串 border。\n- Z 函式處理每個後綴和整串的 LCP。\n- 雜湊適合快速判等，AC 自動機適合多模式。\n\n## 與相似技巧的比較\n\n- **對比 Z 函式**：Z 只回答「與整串前綴」的共同前綴，`O(n)` 時間與空間；LCP 表回答**任意兩個起點**，代價是 `O(n^2)`。需求更廣，成本更高。\n- **對比字串雜湊**：雜湊的空間是 `O(n)`、比較相等 `O(1)`，`n` 大時是首選；但求「相同多長」要二分（多一個 `log`），而且有碰撞機率。LCP 表是確定性的，且直接給出長度。\n- **對比後綴陣列 + 稀疏表**：能在 `O(n log n)` 空間內回答任意兩後綴的 LCP，是 `n` 大時的正解。實作成本高，`n <= 3000` 時沒必要。\n- **對比每次逐字比較**：程式最短，但 `O(Qn)`。只有查詢次數少時才划算。\n- **選型一句話**：`n <= 3000` 且查詢密集用本節的二維表；`n` 更大就換雜湊（可接受碰撞）或後綴陣列（要求確定性）。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1977 | [劃分數字的方案數](https://leetcode.cn/problems//number-of-ways-to-separate-numbers/) | 2817 |\n| 2430 | [對字母串可執行的最大刪除數](https://leetcode.cn/problems//maximum-deletions-on-a-string/) | 2102 |\n| 3388 | [統計陣列中的美麗分割](https://leetcode.cn/problems//count-beautiful-splits-in-an-array/) | 2365 |\n\n## 本節重點速查\n\n- `lcp[i][j]` = `s[i..]` 與 `s[j..]` 的最長共同前綴長度，遞推式 `lcp[i][j] = (s[i]==s[j]) ? lcp[i+1][j+1] + 1 : 0`。\n- **兩層迴圈都要遞減**（依賴右下角），寫成遞增會讀到未填的 0 而全錯且不報錯。\n- 多開一圈邊界省去越界判斷。\n- 查詢：**相等**用 `lcp[i][j] >= len`；\n- **比大小**取 `k = lcp[i][j]` 後只比 `s[i+k]` 與 `s[j+k]` 一個字元。\n- 建表 `O(n^2)` 時間與空間，`n = 3000` 約 36 MB 已近上限。\n- `n` 更大就改用字串雜湊（`O(n)` 空間、可接受碰撞）或後綴陣列 + 稀疏表（確定性但實作長）。',
        },
      ],
    },
    {
      id: 8,
      title: "3. Manacher 演算法（迴文串）",
      summary:
        "Manacher 演算法線上性時間內求出所有迴文中心的最長迴文串。可用於判斷子串是否為迴文、求最長迴文子串、最短迴文串構造等。模板題為 5. 最長迴文子串。\n\n:::example Manacher：求每個中心的最長迴文半徑\n**C++ 模板**：\n\n```cpp\n// Manacher：求每個中心的最長迴文半徑\nvector<int> manacher(const string& s) {\n    string t = \"#\";\n            for (char c : s) {\n        t += c;\n        t += '#';\n    }\n    int n = t.size();\n    vector<int> p(n, 0);\n    int c = 0, r = 0;\n    for (int i = 0; i < n; ++i) {\n        // 如果 i 在當前回文右邊界內，利用對稱性初始化 p[i]\n        if (i < r) {\n            p[i] = min(r - i, p[2 * c - i]);\n        }\n        // 中心擴展：嘗試擴展以 i 為中心的迴文半徑\n        while (i - p[i] - 1 >= 0 && i + p[i] + 1 < n &&\n                      t[i - p[i] - 1] == t[i + p[i] + 1]) {\n            p[i]++;\n        }\n        // 更新迴文右邊界 r 和對應的中心 c\n        if (i + p[i] > r) {\n            c = i;\n            r = i + p[i];\n        }\n    }\n    return p;\n}\n```\n:::",
      children: [
        {
          id: 9,
          title: "3.1 基礎",
          summary:
            '## 這個技術解決什麼問題\n\n在 `O(n)` 時間內求出**每一個中心**的最長迴文半徑，據此回答「最長迴文子串」（5）、「有幾個迴文子串」（647）等問題。\n\n中心擴展法是 `O(n^2)`——每個中心都從頭往外比。Manacher 的觀察是：**迴文是對稱的，所以在一個已知的大迴文內部，左半邊的資訊可以直接鏡射給右半邊用**。加上「維護最右已知迴文邊界」的攤還技巧，總時間降到線性。\n\n它與 Z 函式的結構幾乎一模一樣——都靠一個「最右已知區間」借用對稱位置的結果。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往 Manacher 想：\n\n- **要找「最長迴文子串」或「數所有迴文子串」，且字串很長需要 `O(n)`。** 5、647、1745、1960 皆屬此類。\n- **需要對「每一個中心」都知道迴文能延伸多遠**，而不是只判斷某一段是否為迴文。\n- **暴力中心擴展 `O(n^2)` 會超時**，又不想動用更重的後綴結構時，Manacher 是首選。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我是否需要「每個中心的最長迴文半徑」，而不只是單一判斷？\n2. 我有沒有用 `#` 統一奇偶、用哨兵避免越界？\n3. 改造後的半徑要怎麼換算回原字串的長度與起點？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 5 | 最長迴文子串 | N/A | 要利用左右對稱；練習同時處理奇偶中心、半徑或區間狀態。 |\n| 214 | 最短迴文串 | N/A | 要利用左右對稱；練習同時處理奇偶中心、半徑或區間狀態。 |\n| 647 | 迴文子串 | N/A | 要利用左右對稱；練習同時處理奇偶中心、半徑或區間狀態。 |\n| 1745 | [分割回文串 IV](https://leetcode.cn/problems/palindrome-partitioning-iv/) | 1925 | 要利用左右對稱；練習同時處理奇偶中心、半徑或區間狀態。 |\n| 1960 | [兩個迴文子字串長度的最大乘積（Premium）](https://leetcode.cn/problems/maximum-product-of-the-length-of-two-palindromic-substrings/) | 2691 | 要利用左右對稱；練習同時處理奇偶中心、半徑或區間狀態。 |\n\n## 核心想法與直覺\n\n本節解決「迴文」相關問題。迴文就是正著讀、反著讀都一樣的字串，例如 `"aba"`、`"abba"`。常見任務是找最長迴文子串（LeetCode 5）或數出有幾個迴文子串（647）。\n\n迴文有奇數長度（中心是一個字元，如 `"aba"`）和偶數長度（中心在兩字元之間，如 `"abba"`）兩種，處理起來很煩。Manacher 的第一個技巧是**在每個字元之間插入分隔符 `#`，把所有迴文統一成奇數長度**，例如 `"abba"` 變成 `"#a#b#b#a#"`，中心一律落在某個位置上。\n\n:::example 對稱借用的直覺：c 是目前最右迴文的中心，i 在它的右半\n接著對新字串算「迴文半徑」`p[i]`：以 `i` 為中心，最多能向左右各延伸多少個字元仍保持迴文。暴力是每個中心都向外擴展，最壞 `O(n^2)`。Manacher 的核心想法是：**維護目前「最靠右」的那個迴文覆蓋區間 `[l, r]`，新中心 `i` 若落在這區間內，就可以用它對稱位置 `2 * c - i` 已經算好的半徑當起點，省去重複擴展。**\n\n```cpp\n// 對稱借用的直覺：c 是目前最右迴文的中心，i 在它的右半\n// i 關於 c 的鏡像位置是 mirror = 2*c - i\n// 在不超過右邊界 r 的前提下，p[i] 至少和 p[mirror] 一樣大\n// p[i] = min(r - i, p[mirror]); 再從這裡繼續往外擴\n```\n:::\n\n\n\n每個位置的「往外擴展」總次數受右邊界 `r` 的增量限制，因此整體是 `O(n)`。\n\n## 狀態／資料結構定義\n\n**統一奇偶**：在每個字元之間與首尾插入分隔符 `#`，`"abba"` 變成 `"#a#b#b#a#"`。改造後**所有迴文都是奇數長度**，中心一律落在某個位置上，不必分奇偶討論。\n\n**哨兵**：改造串頭尾再各加一個絕不出現的字元（如 `^` 與 `$`），`while` 往外擴時自然會停下，不必寫越界判斷。\n\n**`p[i]`**：改造串中以 `i` 為中心的最長迴文**半徑**（不含中心，或含中心視實作而定——選一種並固定）。\n\n**關鍵換算**：若 `p[i]` 定義為「不含中心的半徑」，則原串中對應的迴文**長度恰為 `p[i]`**，起點是 `(i - p[i]) / 2`。這個換算務必推一次並寫在註解裡，是本節最容易出錯的地方。\n\n**最右邊界 `(c, r)`**：`c` 是目前右端最靠右的迴文的中心，`r = c + p[c]` 是它的右端。\n\n## 不變量或正確性證明\n\n**邊界不變量**：`r` 是所有已處理中心所能達到的最右迴文端點，`c` 是達到它的那個中心。`s[c-p[c] .. c+p[c]]` 是迴文。\n\n**鏡射轉移**：處理 `i`（`i < r`）時，令 `mirror = 2*c - i`（`i` 關於 `c` 的對稱點）。因為 `[c-p[c], r]` 是迴文，`i` 附近的結構與 `mirror` 附近**在 `r` 以內**是鏡像相同的。所以\n\n`p[i] = min(r - i, p[mirror])`\n\n再從這個半徑往外親自擴展。\n\n**`min` 為什麼不能少**（與 Z 函式同一個道理）：\n\n- 若 `p[mirror] < r - i`：`mirror` 的迴文完全落在大迴文內部，鏡射資訊完整可信，`p[i] = p[mirror]` **確定**。\n- 若 `p[mirror] >= r - i`：`i` 的迴文至少延伸到 `r`，但**超出 `r` 的部分不在大迴文內，從未被驗證**，必須親自逐字比。直接令 `p[i] = p[mirror]` 會用上未驗證的資訊而出錯。\n\n**`O(n)` 的攤還論證**：`while` 每成功擴展一步，`r` 至少增加 1；`r` 單調不減且上界是改造串長度 `2n+1`。因此所有擴展的總步數是 `O(n)`。每個 `i` 的失敗比對只有一次。\n\n**分隔符為何不影響答案**：`#` 兩兩之間互相對稱，插入後迴文性質完全保留；且改造串中任一迴文的中心若是 `#` 就對應原串的偶數迴文，若是原字元就對應奇數迴文——兩種情形都被涵蓋，不重不漏。\n\n## 逐步演算法\n\n:::example 改造：abba -> `^#a#b#b#a#$`（`^`、`$` 為兩端哨兵，避免邊界判斷）\n**1. 改造字串。** 在頭尾與每個字元間插入 `#`（有時頭部再放一個哨兵字元避免越界）。\n\n```cpp\n// 改造：abba -> ^#a#b#b#a#$（^、$ 為兩端哨兵，避免邊界判斷）\nstring t = "^";\nfor (char c : s) {\n    t += \'#\'; t += c;\n}\nt += "#$";\n```\n:::\n\n\n\n:::example 計算迴文半徑 p[i]（在改造後字串 t 上）\n**2. 計算半徑陣列。** 維護目前最右迴文的中心 `c` 與右界 `r`；對每個 `i` 先用對稱位置借值，再嘗試向外擴展，最後更新 `c`、`r`。\n\n```cpp\n// 計算迴文半徑 p[i]（在改造後字串 t 上）\nvector<int> p(t.size(), 0);\nfor (int i = 1, c = 0, r = 0; i + 1 < (int)t.size(); ++i) {\n    if (i < r) {\n        p[i] = min(r - i, p[2 * c - i]);  // 對稱借用\n    }\n    while (t[i - p[i] - 1] == t[i + p[i] + 1]) {\n        p[i]++;  // 往外擴展\n    }\n    if (i + p[i] > r) {\n        c = i;\n        r = i + p[i];\n    }  // 更新最右\n}\n```\n:::\n\n\n\n:::example 還原：p[i] 即原字串對應迴文長度；起點 = (i - p[i]) / 2\n**3. 還原答案。** 改造後的半徑 `p[i]` 恰好等於原字串中以該處為中心的迴文長度。最長迴文就是 `max(p[i])`，其起點可由 `i`、`p[i]` 反推。\n\n```cpp\n// 還原：p[i] 即原字串對應迴文長度；起點 = (i - p[i]) / 2\nint best = 0, center = 0;\nfor (int i = 1; i + 1 < (int)p.size(); ++i) {\n    if (p[i] > best) {\n        best = p[i];\n        center = i;\n    }\n}\nint start = (center - best) / 2;  // 在原字串中的起點\n```\n:::\n\n\n\n**邊界與複雜度。** 哨兵 `^`、`$` 讓 `while` 不會越界；半徑與原長度的換算（插入 `#` 後）要記牢；時間與空間 `O(n)`。\n\n## C++17 模板\n\n以下用本節代表題 5. 最長迴文子串 展示核心寫法。\n\n**完整題目**：給定字串，回傳其中最長的迴文子字串。\n\n:::example 從中心向外展開，找以 [l,r] 為初始中心的最長回文\n**線索**：題目圍繞迴文的左右對稱；中心、半徑或區間兩端是最自然的狀態。\n\n```cpp\nclass Solution {\npublic:\n    string longestPalindrome(string s) {\n            int bestL = 0, bestLen = 1;\n            // 從中心向外展開，找以 [l,r] 為初始中心的最長回文\n            auto expand = [&](int l, int r) {\n                    while (l >= 0 && r < (int)s.size() && s[l] == s[r]) {\n                            l--, r++;  // 兩端字元相同則繼續擴張\n                    }\n                    // 迴圈結束後 [l+1, r-1] 才是回文範圍，長度為 r-l-1\n                    if (r - l - 1 > bestLen) {\n                            bestLen = r - l - 1;\n                            bestL = l + 1;\n                    }\n            };\n            for (int i = 0; i < (int)s.size(); ++i) {\n                    expand(i, i);      // 奇數長度回文，以單一字元為中心\n                    expand(i, i + 1);  // 偶數長度回文，以相鄰兩字元為中心\n            }\n            return s.substr(bestL, bestLen);\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- 中心擴展枚舉奇偶中心；Manacher 則把已知迴文半徑復用到下一個中心以降到 O(n)。\n- 預處理陣列或狀態機保存已比對資訊，主迴圈失配時不用回到原點重比。\n- 下標與長度的換算要固定，尤其是 border、LCP、半徑與回傳起點。\n\n## 時間與空間複雜度\n\n**時間 `O(n)`**、**空間 `O(n)`**（改造串與 `p` 陣列各一份）。\n\n對比中心擴展法的 `O(n^2)`——`n = 1e5` 時是 `1e10` 對 `1e5`。5（最長迴文子串）在 `n <= 1000` 時中心擴展就夠；1745、1960 這類 `n` 到 `1e5` 的題就必須用 Manacher。\n\n**647 數迴文子串**：以 `i` 為中心的迴文子串個數是 `(p[i] + 1) / 2`（改造串半徑換算回原串的個數），全部加總即得，仍是 `O(n)`。\n\n**空間常數**：改造串長度是 `2n + 3`（含分隔符與哨兵），所以實際記憶體約是原串的兩倍多。`n = 1e6` 時仍在可接受範圍。\n\n**與 Z 函式的關係**：兩者的攤還論證一字不差，只是 Manacher 維護的是「最右迴文」而 Z 維護的是「最右前綴匹配」。\n\n## 常見錯誤與邊界條件\n\n- 偶數中心是 `(i, i + 1)`，不要漏。\n- Manacher 可插入分隔符統一奇偶。\n- 回傳子串時注意起點與長度。\n- 子串與子序列是不同問題。\n\n## 常見變形\n\n- 中心擴展 O(n^2) 適合中小資料。\n- Manacher O(n) 適合只看迴文半徑。\n- 區間 DP 適合切分、最少修改或子序列。\n\n## 與相似技巧的比較\n\n- **對比中心擴展法**（下一節）：中心擴展 `O(n^2)`、五行寫完、幾乎不會錯；Manacher `O(n)`、但換算與 `min` 容易寫錯。`n <= 2000` 用中心擴展，`n` 上萬才用 Manacher。\n- **對比字串雜湊 + 二分**：對每個中心二分迴文半徑，用正反兩份雜湊 `O(1)` 驗證，總計 `O(n log n)`。比 Manacher 慢一個 `log` 但好寫，且雜湊本來就常備著。有碰撞風險。\n- **對比 DP**（`dp[i][j]` 表示 `s[i..j]` 是否迴文）：`O(n^2)` 時間與空間，但能直接支援「最少切幾刀全是迴文」這類後續 DP。需求是「迴文分割」時 DP 更順手。\n- **對比回文自動機（PAM）**：能處理「本質不同的迴文子串個數」「每個迴文的出現次數」等更細的問題，`O(n)`。實作比 Manacher 長得多，競賽中少見。\n- **對比 Z 函式**：結構同源。學會其中一個之後，另一個的不變量與攤還論證幾乎可以照抄。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 5 | [最長迴文子串](https://leetcode.cn/problems//longest-palindromic-substring/) |  |\n| 214 | [最短迴文串](https://leetcode.cn/problems//shortest-palindrome/) |  |\n| 647 | [迴文子串](https://leetcode.cn/problems//palindromic-substrings/) |  |\n| 1745 | [分割回文串 IV](https://leetcode.cn/problems//palindrome-partitioning-iv/) | 1925 |\n| 1960 | [兩個迴文子字串長度的最大乘積](https://leetcode.cn/problems//maximum-product-of-the-length-of-two-palindromic-substrings/) | 2691 |\n| 3327 | [判斷 DFS 字串是否是迴文串](https://leetcode.cn/problems//check-if-dfs-strings-are-palindromes/) | 2454 |\n| 3504 | [子字串連線後的最長迴文串 II](https://leetcode.cn/problems//longest-palindrome-after-substring-concatenation-ii/) | 2398 |\n\n## 本節重點速查\n\n- 插入 `#` 把所有迴文統一成**奇數長度**，首尾加哨兵免去越界判斷。\n- 維護「最右迴文」`(c, r)`，處理 `i` 時先取 `p[i] = min(r - i, p[2*c - i])` 再往外擴展——**`min` 不能少**，超出 `r` 的部分從未驗證過。\n- 擴展後若右端超過 `r` 就更新 `(c, r)`。\n- `O(n)` 來自 `r` 單調不減的攤還論證。\n- **半徑與原串長度的換算務必推一次並寫進註解**（`p[i]` 若定義為不含中心的半徑，原串迴文長度恰為 `p[i]`，起點 `(i - p[i]) / 2`）。\n- 647 數迴文子串用 `Σ (p[i] + 1) / 2`。\n- `n <= 2000` 時中心擴展就夠，別為難自己。',
        },
        {
          id: 10,
          title: "3.2 中心擴展法",
          summary:
            '## 這個技術解決什麼問題\n\n同樣是迴文問題——最長迴文子串、數迴文子串、判斷某段是否迴文——但用最直覺、最不容易寫錯的方式。\n\n核心性質只有一句：**每個迴文都有一個中心，從中心往兩側看是對稱的**。所以枚舉所有中心，從中心同時向左右擴展，只要字元相等就繼續。\n\n它的價值不在複雜度（`O(n^2)`），而在**可靠**。`n` 只有幾千時，五行程式就解決，不必冒 Manacher 換算寫錯的風險。它也常被當成預處理步驟——先把「每個中心的半徑」或「`(i,j)` 是否迴文」算出來，再餵給後續的 DP。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往中心擴展想：\n\n- **要找最長迴文 / 數迴文 / 判斷某段是否迴文，且字串規模中等（`n` 約幾千以內）。** 5、647、2472 都適用。\n- **題目強調「左右對稱」**，而你想要一個好寫、不易出錯的解法。\n- **後續還要在迴文資訊上做 DP**（如「最多切幾段都是迴文」），可先用中心擴展把「`(i, j)` 是否為迴文」或每個中心的半徑預處理出來。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我有沒有同時處理奇數中心 `(i, i)` 與偶數中心 `(i, i + 1)`？\n2. 我要的是「最長」（取 max 並回推起點）還是「計數」（累加成功次數）？\n3. 資料範圍允許 `O(n^2)` 嗎？若不行是否該改用 Manacher？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 5 | 最長迴文子串 | N/A | 要利用左右對稱；練習同時處理奇偶中心、半徑或區間狀態。 |\n| 2472 | [不重疊迴文子字串的最大數目](https://leetcode.cn/problems/maximum-number-of-non-overlapping-palindrome-substrings/) | 2013 | 要利用左右對稱；練習同時處理奇偶中心、半徑或區間狀態。 |\n| 3504 | [子字串連線後的最長迴文串 II（Premium）](https://leetcode.cn/problems/longest-palindrome-after-substring-concatenation-ii/) | 2398 | 要利用左右對稱；練習同時處理奇偶中心、半徑或區間狀態。 |\n| 3579 | [字串轉換需要的最小運算元（Premium）](https://leetcode.cn/problems/minimum-steps-to-convert-string-with-operations/) | 2493 | 重點是重用已比對資訊；練習把 border、LCP、hash 或自動機狀態寫成不變式。 |\n| 3615 | [圖中的最長迴文路徑（Premium）](https://leetcode.cn/problems/longest-palindromic-path-in-graph/) | 2463 | 要利用左右對稱；練習同時處理奇偶中心、半徑或區間狀態。 |\n\n## 核心想法與直覺\n\n本節談「中心擴展法」，它是處理迴文最直覺、最好寫的方法。迴文的定義是正著反著讀都一樣（如 `"aba"`、`"abba"`），而它有一個關鍵性質：**每個迴文都有一個中心，從中心往兩側看是左右對稱的。**\n\n核心想法就是：**枚舉每一個可能的中心，從中心同時向左、向右擴展，只要左右字元相等就繼續，碰到不等或越界就停。** 停下時走過的範圍就是以該中心為對稱軸的最長迴文。\n\n迴文有兩種中心，要分別處理：\n\n- 奇數長度迴文：中心是「一個字元」，例如 `"aba"` 的中心是 `b`，從 `(i, i)` 開始擴。\n- 偶數長度迴文：中心是「兩字元之間的縫隙」，例如 `"abba"` 的中心在兩個 `b` 之間，從 `(i, i + 1)` 開始擴。\n\n:::example 中心擴展：從 (l, r) 往兩側擴，回傳這個中心的最長迴文長度\n```cpp\n// 中心擴展：從 (l, r) 往兩側擴，回傳這個中心的最長迴文長度\nint expand(const string& s, int l, int r) {\n    while (l >= 0 && r < (int)s.size() && s[l] == s[r]) {\n        l--;\n        ++r;\n    }\n    return r - l - 1;  // 跳出迴圈時多走了一步，故 -1\n}\n```\n:::\n\n\n\n對長度 `n` 的字串，共有 `2n - 1` 個中心（`n` 個字元中心 + `n - 1` 個縫隙中心），每個中心最多擴 `O(n)`，所以整體 `O(n^2)`。它比 Manacher 慢，但程式碼短、好懂、不易寫錯，是大多數中等規模迴文題的首選。\n\n## 狀態／資料結構定義\n\n**兩種中心**必須都處理：\n\n- **奇數長度**：中心是單一字元，起點 `(i, i)`。\n- **偶數長度**：中心在兩字元之間，起點 `(i, i + 1)`。\n\n漏掉偶數中心是本節最常見的錯——`"abba"` 這種測資會直接暴露。\n\n**擴展函數**：`while (l >= 0 && r < n && s[l] == s[r]) { --l; ++r; }`。跳出時 `[l+1, r-1]` 是以該中心為軸的最長迴文，**長度是 `r - l - 1`**，起點是 `l + 1`。\n\n**兩種收尾**：\n- **求最長**：比較 `r - l - 1` 與目前最佳，更新時記下起點。\n- **計數**（647）：每成功擴展一次就 `++ans`——因為每一次成功擴展都對應一個不同的迴文子串。\n\n**當預處理用**：開 `bool isPal[n][n]`，擴展時把每個成功的 `(l, r)` 標記起來，供後續 DP `O(1)` 查詢。\n\n## 不變量或正確性證明\n\n**擴展迴圈的不變量**：進入每一輪迴圈前，`s[l+1..r-1]` 是迴文。\n\n初始時 `(l, r) = (i, i)` 給出空區間或單字元（都是迴文）；`(i, i+1)` 給出空區間。每輪若 `s[l] == s[r]`，在兩端各加一個相同字元後仍是迴文——這正是迴文的遞迴定義，不變量維持。\n\n**最大性**：跳出時要嘛越界、要嘛 `s[l] != s[r]`。兩種情形都表示不能再往外擴，所以 `[l+1, r-1]` 是以該中心為軸的**最長**迴文。\n\n**枚舉的完整性**：任何迴文子串都有唯一的中心——長度為奇數時是正中間那個字元（中心 `(i,i)`），長度為偶數時是正中間兩字元之間（中心 `(i,i+1)`）。`n` 個奇中心加 `n-1` 個偶中心共 `2n-1` 個，涵蓋所有可能。因此枚舉全部中心不會遺漏任何迴文。\n\n**計數不重複的理由**（647）：每個迴文子串由「中心 + 半徑」唯一決定。擴展過程中每成功一步就對應一個不同的半徑，因此每個迴文恰被數一次。\n\n**為什麼是 `O(n^2)` 而非 `O(n)`**：與 Manacher 不同，這裡沒有借用已知資訊——每個中心都從零開始擴展。最壞情形（如 `"aaaa…a"`）每個中心都擴展 `O(n)` 步，總計 `O(n^2)`。\n\n## 逐步演算法\n\n:::example 枚舉中心找最長迴文子串（LeetCode 5）\n**1. 枚舉所有中心，奇偶各擴一次。** 對每個 `i` 都試「以 `i` 為單字元中心」和「以 `i, i + 1` 為縫隙中心」。\n\n```cpp\n// 枚舉中心找最長迴文子串（LeetCode 5）\nstring longestPalindrome(const string& s) {\n    int start = 0, len = 0;\n    for (int i = 0; i < (int)s.size(); ++i) {\n        int a = expand(s, i, i);      // 奇數中心\n        int b = expand(s, i, i + 1);  // 偶數中心\n        int cur = max(a, b);\n        if (cur > len) {\n            len = cur;\n            start = i - (cur - 1) / 2;\n        }\n    }\n    return s.substr(start, len);\n}\n```\n:::\n\n\n\n:::example 計數：每次 s[l]==s[r] 成功就 ++ans\n**2. 計數版本：直接累加。** 若要「數迴文子串個數」（647），就把每次擴展成功的次數加總——每擴展成功一格，就代表多出一個迴文。\n\n```cpp\n// 計數：每次 s[l]==s[r] 成功就 ++ans\nint countAt(const string& s, int l, int r, int& ans) {\n    while (l >= 0 && r < (int)s.size() && s[l] == s[r]) {\n        l--;\n        ++r;\n        ++ans;\n    }\n    return ans;\n}\n```\n:::\n\n\n\n**3. 邊界與複雜度。**\n\n- 偶數中心 `(i, i + 1)` 要確保 `i + 1` 不越界；上面的 `expand` 已用條件判斷涵蓋。\n- 由長度回推起點時，奇偶都適用 `start = i - (len - 1) / 2`。\n- 時間 `O(n^2)`、空間 `O(1)`；若 `n` 大到 `O(n^2)` 會超時，再換 Manacher。像 2472、3504 這類「在迴文上再做 DP」的題，常先用中心擴展預處理出每個位置的迴文資訊。\n\n## C++17 模板\n\n以下用本節代表題 5. 最長迴文子串 展示核心寫法。\n\n**完整題目**：給定字串，回傳其中最長的迴文子字串。\n\n:::example 從中心向外展開，找以 [l,r] 為初始中心的最長回文\n**線索**：題目圍繞迴文的左右對稱；中心、半徑或區間兩端是最自然的狀態。\n\n```cpp\nclass Solution {\npublic:\n    string longestPalindrome(string s) {\n            int bestL = 0, bestLen = 1;\n            // 從中心向外展開，找以 [l,r] 為初始中心的最長回文\n            auto expand = [&](int l, int r) {\n                    while (l >= 0 && r < (int)s.size() && s[l] == s[r]) {\n                            l--, r++;  // 兩端字元相同則繼續擴張\n                    }\n                    // 迴圈結束後 [l+1, r-1] 才是回文範圍，長度為 r-l-1\n                    if (r - l - 1 > bestLen) {\n                            bestLen = r - l - 1;\n                            bestL = l + 1;\n                    }\n            };\n            for (int i = 0; i < (int)s.size(); ++i) {\n                    expand(i, i);      // 奇數長度回文，以單一字元為中心\n                    expand(i, i + 1);  // 偶數長度回文，以相鄰兩字元為中心\n            }\n            return s.substr(bestL, bestLen);\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- 中心擴展枚舉奇偶中心；Manacher 則把已知迴文半徑復用到下一個中心以降到 O(n)。\n- 預處理陣列或狀態機保存已比對資訊，主迴圈失配時不用回到原點重比。\n- 下標與長度的換算要固定，尤其是 border、LCP、半徑與回傳起點。\n\n## 時間與空間複雜度\n\n**時間 `O(n^2)`**：`2n - 1` 個中心，每個最壞擴展 `O(n)` 步。最壞測資是全部字元相同。\n\n**空間 `O(1)`**（只求最長或計數）或 **`O(n^2)`**（要建 `isPal` 表供 DP 用）。\n\n實務門檻：`n <= 2000` 時 `n^2 = 4e6`，完全沒問題；`n = 1e4` 是 `1e8`，勉強；`n = 1e5` 必然 TLE，要換 Manacher。\n\n**與 Manacher 的取捨**：Manacher 是 `O(n)`，但改造串、半徑換算、`min` 的邊界都容易寫錯。中心擴展五行寫完、幾乎不會錯。**先看 `n`，再決定要不要冒險**——這是本節存在的全部理由。\n\n**當預處理用時**：`O(n^2)` 建表換來後續 DP 的 `O(1)` 查詢。像「最少切幾刀讓每段都是迴文」（132）就是先建 `isPal` 再跑 `O(n^2)` DP，總計仍是 `O(n^2)`。\n\n## 常見錯誤與邊界條件\n\n- 偶數中心是 `(i, i + 1)`，不要漏。\n- Manacher 可插入分隔符統一奇偶。\n- 回傳子串時注意起點與長度。\n- 子串與子序列是不同問題。\n\n## 常見變形\n\n- 中心擴展 O(n^2) 適合中小資料。\n- Manacher O(n) 適合只看迴文半徑。\n- 區間 DP 適合切分、最少修改或子序列。\n\n## 與相似技巧的比較\n\n- **對比 Manacher**：`O(n^2)` 對 `O(n)`。中心擴展好寫可靠，Manacher 快但易錯。`n <= 2000` 選前者，`n` 上萬選後者。兩者的核心觀察相同（迴文有中心且對稱），Manacher 多的只是「借用已知資訊」的攤還技巧。\n- **對比 DP `isPal[i][j]`**：遞推式 `isPal[i][j] = (s[i]==s[j]) && isPal[i+1][j-1]`，同樣 `O(n^2)`，但空間也是 `O(n^2)`。要反覆查詢任意 `(i,j)` 是否迴文時用 DP；只求最長或計數時中心擴展的空間是 `O(1)`。\n- **對比字串雜湊 + 二分**：`O(n log n)`，對每個中心二分半徑。比中心擴展快，比 Manacher 好寫，代價是碰撞風險與正反兩份雜湊的實作。\n- **對比暴力枚舉所有子串**：`O(n^3)`（`n^2` 個子串各驗證 `O(n)`）。中心擴展本質上就是把驗證的成本攤進枚舉裡。\n- **對比子序列類迴文題**（如 516 最長迴文子序列）：那是完全不同的問題，要用區間 DP。**子串連續、子序列可跳**——別把兩者的解法搞混。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 5 | [最長迴文子串](https://leetcode.cn/problems//longest-palindromic-substring/) |  |\n| 2472 | [不重疊迴文子字串的最大數目](https://leetcode.cn/problems//maximum-number-of-non-overlapping-palindrome-substrings/) | 2013 |\n| 3504 | [子字串連線後的最長迴文串 II](https://leetcode.cn/problems//longest-palindrome-after-substring-concatenation-ii/) | 2398 |\n| 3579 | [字串轉換需要的最小運算元](https://leetcode.cn/problems//minimum-steps-to-convert-string-with-operations/) | 2493 |\n| 3615 | [圖中的最長迴文路徑](https://leetcode.cn/problems//longest-palindromic-path-in-graph/) | 2463 |\n\n## 本節重點速查\n\n- 枚舉 `2n-1` 個中心（**奇數中心 `(i,i)` 與偶數中心 `(i,i+1)` 都要**，漏掉偶數是最常見的錯），從中心往兩側擴展到不等或越界。\n- 跳出時 `[l+1, r-1]` 是最長迴文，**長度 `r - l - 1`、起點 `l + 1`**。\n- 求最長就取 max 並回推起點；\n- **計數（647）就每成功擴展一次 `++ans`**——每次成功對應一個不同的迴文。\n- 正確性靠「每個迴文有唯一中心」，所以枚舉全部中心不會遺漏。\n- `O(n^2)` 時間、`O(1)` 空間。\n- `n <= 2000` 用它就好，別為難自己去寫 Manacher；\n- `n` 上萬才換。\n- 注意**子串（連續）與子序列（可跳）是不同問題**，後者要用區間 DP。',
        },
      ],
    },
    {
      id: 11,
      title: "4. 字串雜湊",
      summary:
        "使用多項式雜湊（單模/雙模）快速比較子串。適用於子串匹配、重複子串、最長公共子串等。推薦先做 2156. 查詢給定雜湊值的子串。可二分比較字典序。\n\n:::example 字串雜湊（多項式雜湊）\n**C++ 模板**：\n\n```cpp\n// 字串雜湊（多項式雜湊）\nstruct StringHash {\n    static const long long kMod = 1e9 + 7, kBase = 131;\n    vector<long long> h, pw;\n    StringHash(const string& s) {\n        int n = s.size();\n        h.resize(n + 1);\n        pw.resize(n + 1);\n        h[0] = 0;\n        pw[0] = 1;\n        for (int i = 0; i < n; ++i) {\n            h[i + 1] = (h[i] * kBase + s[i]) % kMod;\n            pw[i + 1] = pw[i] * kBase % kMod;\n        }\n    }\n    long long get(int l, int r) {\n        return (h[r + 1] - h[l] * pw[r - l + 1] % kMod + kMod * 2) % kMod;\n    }\n};\n```\n:::",
      children: [
        {
          id: 12,
          title: "4.1 基礎",
          summary:
            "## 這個技術解決什麼問題\n\n把一段字串壓成一個數字，讓「比較兩段子串是否相等」從逐字比對 `O(長度)` 變成比兩個數字 `O(1)`。\n\n做法是**多項式滾動雜湊**：把字串看成 `base` 進位的數。先算好每個前綴的雜湊，任意子串的雜湊就能用「前綴相減、配合 `base` 的次方對齊」在 `O(1)` 取得——結構與前綴和完全一樣，只是進了位。\n\n它是最泛用的字串工具：搭配二分答案能求最長重複子串（1062）、最長重複子陣列（718）；也能取代 KMP 做匹配、取代 LCP 表做子串比較。代價是**有碰撞機率**。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往字串雜湊想：\n\n- **需要大量、快速地判斷「兩段子串是否相等」或「某子串是否出現過」。** 28、187、1062、1554 都是這類比較密集的題。\n- **要對「子串長度」二分搜尋**（最長重複子串、最長重複子陣列），每次驗證都需 `O(1)` 比較。\n- **允許極小機率出錯；雙雜湊能進一步降低碰撞機率，但仍不是確定性保證**，且想要比後綴結構更短的程式碼。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要比較的是哪些子串？前綴雜湊讓我能 `O(1)` 取得它們嗎？\n2. 我有沒有處理好取模負數，必要時是否該用雙雜湊防碰撞？\n3. 是否搭配二分搜尋長度，把整體壓到 `O(n log n)`？\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n本節談「字串雜湊」（string hashing），更精確地說是「多項式滾動雜湊」。它的目標是：**把一段字串壓成一個數字，使得「比較兩段子串是否相等」從逐字比對（`O(長度)`）變成比兩個數字（`O(1)`）。**\n\n:::example 把字串當成 base 進位數\n做法是把字串看成一個 base 進位的數。給定底數 `base`（如 131）和模數 `mod`（一個大質數），字串 `s` 的雜湊定義為：\n\n```cpp\n// 把字串當成 base 進位數：\n// hash(\"abc\") = (a*base^2 + b*base^1 + c*base^0) mod mod\n// 例 base=131：'a'*131^2 + 'b'*131 + 'c'，再取模避免溢位\n```\n:::\n\n\n\n關鍵在「前綴雜湊」：先算出每個前綴的雜湊 `h[i]`（前 `i` 個字元的雜湊），那麼任意子串 `s[l..r]` 的雜湊就能用前綴相減、配合 `base` 的次方在 `O(1)` 取得。這就像前綴和，只是進了位。\n\n因為兩段相等的子串雜湊必相等，我們就能用一次比較取代逐字比對。要注意這是**機率性**方法：不同字串理論上可能算出相同雜湊（碰撞）。選大質數模數、或同時用兩組 `(base, mod)` 做「雙雜湊」，可把碰撞機率壓到可忽略。例如 187「重複的 DNA 序列」、1062「最長重複子串」都靠它快速判斷子串是否出現過。\n\n## 狀態／資料結構定義\n\n**前綴雜湊**：`h[0] = 0`，`h[i+1] = h[i] * base + s[i]`（對 `mod` 取模）。\n\n**次方表**：`pw[0] = 1`，`pw[i+1] = pw[i] * base`。預先算好，否則每次查詢要多一個 `log`（快速冪）。\n\n**子串雜湊**：`s[l..r]`（0-based，閉區間）的雜湊是\n\n`(h[r+1] - h[l] * pw[r-l+1] % mod + mod) % mod`\n\n**`+ mod` 不能省**——相減可能是負數，C++ 的 `%` 對負數回傳負值。\n\n**參數選擇**：\n- `base` 取一個大於字元集大小的質數，如 131 或 13331。\n- `mod` 取大質數，如 `1e9+7`、`998244353`。\n- **雙雜湊**：用兩組不同的 `(base, mod)`，把兩個雜湊值合成 `pair` 或打包成一個 `long long` 比較。碰撞機率從 `1/mod` 降到約 `1/mod^2`。\n- **自然溢位**：用 `unsigned long long` 讓它自動對 `2^64` 取模，速度最快，但存在針對性的反雜湊構造（Thue-Morse 串），競賽中可能被卡。\n\n所有中間值用 `long long`，`h[i] * base` 在 `mod = 1e9+7` 時可達 `1e12`。\n\n## 不變量或正確性證明\n\n**前綴雜湊的定義展開**：`h[i]` = `Σ_{k=0}^{i-1} s[k] * base^(i-1-k) (mod m)`，即把 `s[0..i-1]` 當成 `base` 進位數。\n\n**子串公式的推導**：\n\n`h[r+1] = Σ_{k=0}^{r} s[k] * base^(r-k)`\n`h[l] * base^(r-l+1) = Σ_{k=0}^{l-1} s[k] * base^(l-1-k) * base^(r-l+1) = Σ_{k=0}^{l-1} s[k] * base^(r-k)`\n\n兩者相減，`k < l` 的項完全抵消，剩下 `Σ_{k=l}^{r} s[k] * base^(r-k)`——正是子串 `s[l..r]` 當成 `base` 進位數的值。這就是公式的正確性。\n\n**單向保證**：**相等的子串必定雜湊相同**（同一個函數，同樣的輸入）。反過來不成立——**雜湊相同不保證子串相等**，這就是碰撞。\n\n**碰撞機率的估計**：把雜湊視為 `[0, mod)` 上的均勻隨機值，兩個不同子串碰撞的機率約 `1/mod`。若程式做了 `Q` 次比較，出錯機率約 `Q/mod`；`Q = 1e6`、`mod = 1e9+7` 時約 `1e-3`——**不算小**。這正是雙雜湊的理由：機率降到 `Q/mod^2 ≈ 1e-12`，實務上可視為不會發生。\n\n**生日悖論的陷阱**：若你把 `n` 個子串的雜湊丟進集合比對彼此，碰撞機率是 `O(n^2/mod)` 而非 `O(n/mod)`。`n = 1e5` 時 `n^2 = 1e10 > mod`，**單雜湊幾乎必定碰撞**。這類「兩兩比對」的用法一定要雙雜湊。\n\n## 逐步演算法\n\n:::example 預處理：h[i] 為前 i 個字的雜湊，pw[i] 為 base^i\n**1. 預處理前綴雜湊與冪次。** 掃一遍字串，邊累積前綴雜湊，邊預存 `base` 的各次冪。\n\n```cpp\n// 預處理：h[i] 為前 i 個字的雜湊，pw[i] 為 base^i\nconst long long base = 131, mod = 1e9 + 7;\nint n = s.size();\nvector<long long> h(n + 1, 0), pw(n + 1, 1);\nfor (int i = 0; i < n; ++i) {\n    h[i + 1] = (h[i] * base + s[i]) % mod;\n    pw[i + 1] = pw[i] * base % mod;\n}\n```\n:::\n\n\n\n:::example 取子串 s[l..r]（含兩端）的雜湊，O(1)\n**2. `O(1)` 取任意子串雜湊。** 用前綴相減扣掉高位部分（記得補模避免負數）。\n\n```cpp\n// 取子串 s[l..r]（含兩端）的雜湊，O(1)\nlong long sub(int l, int r) {\n    return ((h[r + 1] - h[l] * pw[r - l + 1]) % mod + mod) % mod;\n}\n```\n:::\n\n\n\n:::example 找是否存在長度 L 的重複子串\n**3. 應用：找重複子串。** 把每個長度 `L` 的子串雜湊丟進 `unordered_set`，重複出現代表找到相等子串（187 的固定長度版；1062 再對 `L` 二分搜尋）。\n\n```cpp\n// 找是否存在長度 L 的重複子串\nbool hasDup(int L) {\n    unordered_set<long long> seen;\n    for (int i = 0; i + L <= n; ++i) {\n        if (!seen.insert(sub(i, i + L - 1)).second) {\n            return true;\n        }\n    }\n    return false;\n}\n```\n:::\n\n\n\n**邊界與複雜度。** 取模後相減可能變負，務必 `+mod` 再取模；想更穩就用雙雜湊。預處理 `O(n)`，之後每次子串比較 `O(1)`。像 718「最長重複子陣列」可雜湊 + 二分達 `O(n log n)`。\n\n## C++17 模板\n\n以下用本節代表題 28. 找出字串中第一個匹配項的下標 展示核心寫法。\n\n**完整題目**：在字串 `haystack` 中尋找字串 `needle` 第一次出現的位置，若不存在則回傳 `-1`。\n\n:::example KMP 字串匹配：先建立 needle 的前綴函數 pi，再在 haystack 上做線…\n**線索**：題目需要大量子串匹配、前後綴或相等比較；重用已比較資訊就是 KMP/Z/雜湊/自動機的線索。\n\n```cpp\nclass Solution {\npublic:\n    // KMP 字串匹配：先建立 needle 的前綴函數 pi，再在 haystack 上做線性搜尋\n    int strStr(string haystack, string needle) {\n            int m = needle.size();\n            vector<int> pi(m);  // pi[i]：needle[0..i] 的最長相等真前後綴長度\n            // 建構 pi 陣列\n            for (int i = 1; i < m; ++i) {\n                    int j = pi[i - 1];\n                    // 若失配，利用 pi 回退到上一個可能匹配的位置\n                    while (j && needle[i] != needle[j]) {\n                            j = pi[j - 1];  // 失配時回退，不從頭重新匹配\n                    }\n                    if (needle[i] == needle[j]) {\n                            ++j;\n                    }\n                    pi[i] = j;\n            }\n            // 在 haystack 中用 pi 輔助匹配\n            for (int i = 0, j = 0; i < (int)haystack.size(); ++i) {\n                    while (j && haystack[i] != needle[j]) {\n                            j = pi[j - 1];\n                    }\n                    if (haystack[i] == needle[j]) {\n                            ++j;\n                    }\n                    if (j == m) {  // 完整匹配，返回起始位置\n                            return i - m + 1;\n                    }\n            }\n            return -1;\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- `pi[i]` 表示前綴的最長 border；失配時跳到下一個可能 border，而不是回頭重比。\n- 預處理陣列或狀態機保存已比對資訊，主迴圈失配時不用回到原點重比。\n- 下標與長度的換算要固定，尤其是 border、LCP、半徑與回傳起點。\n\n## 時間與空間複雜度\n\n**預處理 `O(n)`**（前綴雜湊與次方表各掃一遍）、**每次子串比較 `O(1)`**、**空間 `O(n)`**。\n\n**典型組合**：\n- **二分答案 + 雜湊**（718 最長重複子陣列、1062 最長重複子串）：二分長度 `L`，用雜湊把所有長度 `L` 的子串丟進 `unordered_set` 檢查是否有重複，單次 `O(n)`，總計 **`O(n log n)`**。\n- **取代 KMP 做匹配**：`O(n + m)` 期望時間，程式短很多。\n- **求兩段的 LCP 長度**：對長度二分 + `O(1)` 驗證，`O(log n)` 一次；比 Z 函式或 LCP 表多一個 `log`。\n\n**雙雜湊的成本**：時間與空間都乘 2，常數變大但漸進不變。除非題目卡得很緊，否則一律用雙雜湊。\n\n**與確定性方法的比較**：KMP、Z、Manacher 都是確定性 `O(n)`；雜湊是機率性但適用面廣得多。追求絕對正確且問題形狀吻合時用前者，需要靈活比較任意兩段時用後者。\n\n## 常見錯誤與邊界條件\n\n- **相減後沒 `+ mod`**。C++ 的 `%` 對負數回傳負值，`h[r+1] - h[l] * pw[...]` 為負時直接取模會得到負的雜湊，比較全錯。一律寫 `(a - b % mod + mod) % mod`。\n- **單雜湊用在「兩兩比對」上**。生日悖論讓碰撞機率變成 `O(n^2/mod)`，`n = 1e5` 時幾乎必碰。這類用法一定要雙雜湊。\n- **`base` 小於字元集大小**。`base` 必須大於最大字元值，否則不同字串會有相同的多項式表示（例如 `base = 26` 配上 `'a'..'z'` 映射到 `0..25` 時，前導的 `'a'` 會被吃掉）。慣例是把字元映射成 `s[i] - 'a' + 1`（從 1 開始）並取 `base >= 27`。\n- **忘記預先算 `pw` 表**。每次用快速冪會多一個 `log`，在 `O(n log n)` 的迴圈裡就變成 `O(n log^2 n)`。\n- **溢位**。`h[i] * base` 與 `h[l] * pw[...]` 在 `mod = 1e9+7` 時乘積達 `1e18`，剛好在 `long long` 邊緣。用 `unsigned long long` 或確認 `mod < 2^31`。\n- **自然溢位被卡**。`unsigned long long` 的自動模 `2^64` 有已知的反雜湊構造。競賽題用雙模更安全。\n- **空串與長度 1**。`l > r` 的情形要特判；`h[0] = 0` 代表空串的雜湊。\n- 驗證方式：對拍一份逐字比較的暴力，用隨機小測資跑幾千組。\n\n## 常見變形\n\n- KMP 處理模式串 border。\n- Z 函式處理每個後綴和整串的 LCP。\n- 雜湊適合快速判等，AC 自動機適合多模式。\n\n## 與相似技巧的比較\n\n- **對比 KMP / Z 函式**：那些是確定性的 `O(n)`，但只回答特定形狀的問題（單模式匹配、與前綴的 LCP）。雜湊能比較**任意兩段**，靈活度高得多，代價是碰撞機率。\n- **對比 LCP 二維表**：LCP 表 `O(n^2)` 空間、`O(1)` 查 LCP 長度、確定性；雜湊 `O(n)` 空間、`O(log n)` 查 LCP 長度、機率性。`n <= 3000` 用表，更大用雜湊。\n- **對比後綴陣列**：後綴陣列 `O(n log n)` 建構、確定性、能回答排名與 LCP；雜湊 + 二分能做到類似效果且好寫太多。競賽中「雜湊 + 二分」常是後綴陣列的務實替代。\n- **對比 `std::hash<string>`**：標準庫的雜湊只能對整個字串算，無法 `O(1)` 取子串雜湊——滾動雜湊的價值就在這裡。\n- **對比 Rabin-Karp**：Rabin-Karp 就是滾動雜湊用於單模式匹配的經典名稱，本節是它的一般化（前綴雜湊支援任意子串）。\n\n## 代表例題\n\n**題號**：28、187、718、1062、1554\n\n重點是重用已比對資訊；練習把 border、LCP、hash 或自動機狀態寫成不變式。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 28 | [找出字串中第一個匹配項的下標](https://leetcode.cn/problems/find-the-index-of-the-first-occurrence-in-a-string/) |  |\n| 187 | [重複的 DNA 序列](https://leetcode.cn/problems/repeated-dna-sequences/) |  |\n| 718 | [最長重複子陣列](https://leetcode.cn/problems/maximum-length-of-repeated-subarray/) |  |\n| 1062 | [最長重複子串](https://leetcode.cn/problems/longest-repeating-substring/)（Premium） |  |\n| 1554 | [只有一個不同字元的字串](https://leetcode.cn/problems/strings-differ-by-one-character/)（Premium） |  |\n| 2168 | [每個數字的頻率都相同的獨特子字串的數量](https://leetcode.cn/problems/unique-substrings-with-equal-digit-frequency/) |  |\n| 3722 | [反轉後字典序最小的字串](https://leetcode.cn/problems/lexicographically-smallest-string-after-reverse/) | 1414 |\n| 1367 | [二叉樹中的連結串列](https://leetcode.cn/problems/linked-list-in-binary-tree/) | 1650 |\n| 2261 | [含最多 K 個可整除元素的子陣列](https://leetcode.cn/problems/k-divisible-elements-subarrays/) | 1724 |\n| 1297 | [子串的最大出現次數](https://leetcode.cn/problems/maximum-number-of-occurrences-of-a-substring/) | 1748 |\n| 1316 | [不同的迴圈子字串](https://leetcode.cn/problems/distinct-echo-substrings/) | 1837 |\n| 3756 | [連線非零數字並乘以其數字和 II](https://leetcode.cn/problems/concatenate-non-zero-digits-and-multiply-by-sum-ii/) | 1968 |\n| 2156 | [查詢給定雜湊值的子串](https://leetcode.cn/problems/find-substring-with-given-hash-value/) | 2063 |\n| 3213 | [最小代價構造字串](https://leetcode.cn/problems/construct-string-with-minimum-cost/) | 2171 |\n| 1044 | [最長重複子串](https://leetcode.cn/problems/longest-duplicate-substring/) | 2429 |\n| 1923 | [最長公共子路徑](https://leetcode.cn/problems/longest-common-subpath/) | 2661 |\n| 3292 | [形成目標字串需要的最少字串數 II](https://leetcode.cn/problems/minimum-number-of-valid-strings-to-form-target-ii/) | 2662 |\n\n## 本節重點速查\n\n- 前綴雜湊 `h[i+1] = h[i] * base + s[i]`，子串雜湊 `(h[r+1] - h[l] * pw[r-l+1] % mod + mod) % mod`——**`+ mod` 不能省**，C++ 的 `%` 對負數回傳負值。\n- `pw` 表要預先算好。\n- **相等必同雜湊，同雜湊不必相等**：碰撞機率單雜湊約 `Q/mod`，而「兩兩比對」因生日悖論是 `O(n^2/mod)`，`n = 1e5` 時幾乎必碰——**這類用法一定要雙雜湊**。\n- `base` 要大於字元集（字元映射從 1 開始）。\n- 所有乘法用 `long long`／`unsigned long long`。\n- 經典組合是**二分長度 + 雜湊驗證**，`O(n log n)` 解最長重複子串（718、1062）。\n- 自然溢位最快但有反雜湊構造，競賽用雙模更安全。",
        },
      ],
    },
    {
      id: 13,
      title: "5. 最小表示法",
      summary:
        "求字串迴圈同構串中字典序最小者。透過雙指標維護候選起點，每次排除 k+1 個位置，總複雜度 $O(n)$。模板題為 899. 有序佇列（k=1）。\n\n:::example 最小表示法：找到字串旋轉中字典序最小的起始位置\n**C++ 模板**：\n\n```cpp\n// 最小表示法：找到字串旋轉中字典序最小的起始位置\nint minRepresentation(const string& s) {\n    int n = s.size(), i = 0, j = 1, k = 0;\n    while (i < n && j < n && k < n) {\n        int diff = s[(i + k) % n] - s[(j + k) % n];\n        // 字元相同，繼續比較下一個位置\n        if (diff == 0) {\n            ++k;\n        }\n        // i 位置的字元較大，移動 i 指標跳過這段\n        else if (diff > 0) {\n            i += k + 1;\n            k = 0;\n        }\n        // j 位置的字元較大，移動 j 指標跳過這段\n        else {\n            j += k + 1;\n            k = 0;\n        }\n        // 避免 i 和 j 指向同一位置\n        if (i == j) {\n            ++j;\n        }\n    }\n    return min(i, j);\n}\n```\n:::",
      children: [
        {
          id: 14,
          title: "5.1 基礎",
          summary:
            '## 這個技術解決什麼問題\n\n把字串首尾相接成環，從不同位置開始讀會得到不同的旋轉。**最小表示法**在 `O(n)` 時間內找出字典序最小的那個旋轉。\n\n最大的用途是**判斷兩字串是否互為旋轉**——只要它們的最小表示相同，就是同一個環。相較於「拼接後找子串」的做法（`s + s` 裡找 `t`），最小表示法還能直接給出「應該從哪裡開始讀」，這在 1625、3403、3406 這類「操作後求字典序極值」的題目裡是直接需要的。\n\n暴力比較所有 `n` 個旋轉是 `O(n^2)`；雙指標的巧妙之處在於每次失配都能一次跳過一大段候選。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往最小表示法（或其雙指標思路）想：\n\n- **題目把字串看成「環」，要從某起點開始讀，求字典序最小 / 最大的旋轉。** 1625、3403、3406 都是「操作後求字典序極值」。\n- **要判斷兩字串是否為彼此的旋轉**，又不想用「拼接後找子串」那套。\n- **允許任意起點重排 / 旋轉**，本質是在所有旋轉中挑極值，暴力 `O(n^2)` 太慢時。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的字串是不是被當成「環」，答案是某個旋轉？\n2. 我求的是最小還是最大表示？比較方向對嗎？\n3. 索引有沒有 `% n` 處理環、有沒有防止兩指標重合？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 899 | [有序佇列](https://leetcode.cn/problems/orderly-queue/) | 2097 | 重點是重用已比對資訊；練習把 border、LCP、hash 或自動機狀態寫成不變式。 |\n| 1625 | [執行操作後字典序最小的字串](https://leetcode.cn/problems/lexicographically-smallest-string-after-applying-operations/) | 1992 | 重點是重用已比對資訊；練習把 border、LCP、hash 或自動機狀態寫成不變式。 |\n| 1708 | 長度為 K 的最大子陣列（Premium） | N/A | 重點是重用已比對資訊；練習把 border、LCP、hash 或自動機狀態寫成不變式。 |\n| 3403 | [從盒子中找出字典序最大的字串 I（Premium）](https://leetcode.cn/problems/find-the-lexicographically-largest-string-from-the-box-i/) | 1762 | 重點是重用已比對資訊；練習把 border、LCP、hash 或自動機狀態寫成不變式。 |\n| 3406 | 從盒子中找出字典序最大的字串 II（Premium） | N/A | 重點是重用已比對資訊；練習把 border、LCP、hash 或自動機狀態寫成不變式。 |\n\n## 核心想法與直覺\n\n本節談「最小表示法」。先解釋情境：把一個字串首尾相接成一個「環」，從不同位置開始讀，會得到不同的「旋轉」。例如 `"baca"` 的所有旋轉是 `baca`、`acab`、`caba`、`abac`。其中字典序最小的那個 `abac` 就叫這個字串的「最小表示」。\n\n它最大的用途是**判斷兩個字串是否為彼此的旋轉**：只要它們的最小表示相同，就是同一個環。例如要判斷 `"abcde"` 與 `"cdeab"` 是不是旋轉關係，各自求最小表示再比一次即可。\n\n:::example 雙指標比較的直覺：i、j 是兩個候選起點，k 是已比對相同的長度\n暴力做法是把 `n` 個旋轉全部生成再比大小，`O(n^2)`。最小表示法的核心想法是：**用兩個候選起點 `i`、`j` 同時往後逐字比較，一旦分出大小，就能一口氣排除「失敗者開頭那一整段」的所有起點，把比較壓到 `O(n)`。**\n\n```cpp\n// 雙指標比較的直覺：i、j 是兩個候選起點，k 是已比對相同的長度\n// 比較 s[i+k] 與 s[j+k]：\n//   相等 -> k++ 繼續比\n//   s[i+k] > s[j+k] -> 以 i..i+k 為起點都不可能更小，i 跳到 i+k+1\n//   反之 -> j 跳過 j..j+k\n// 為了處理「環」，索引用 % n 取回到開頭\n```\n:::\n\n\n\n每次「跳過」都讓某個指標前進至少一格且不回頭，所以總比較次數 `O(n)`。像 899「有序佇列」、1625、3403/3406「找字典序最大字串」這類「在旋轉 / 重排後求字典序極值」的題，都是它的變體。\n\n## 狀態／資料結構定義\n\n**兩個候選指標** `i`、`j`：分別代表兩個尚未被排除的起點，初值 `i = 0`、`j = 1`。\n\n**偏移量 `k`**：目前這兩個起點已經比對相同的長度。\n\n**環的模擬**：一律用 `s[(i + k) % n]` 存取，不要真的把字串接兩份（雖然那樣也對，但多了 `O(n)` 空間）。\n\n**主迴圈**：當 `i < n && j < n && k < n` 時比較 `a = s[(i+k)%n]`、`b = s[(j+k)%n]`——\n\n- `a == b`：`++k`，繼續比。\n- `a > b`：起點 `i` 較差，令 `i = i + k + 1`。\n- `a < b`：起點 `j` 較差，令 `j = j + k + 1`。\n- 後兩種情形都要重設 `k = 0`，並且**若 `i == j` 就 `++j`**（兩指標不能重合，否則永遠比自己而空轉）。\n\n**答案**：迴圈結束後 `min(i, j)` 就是最小表示的起點。求**最大**表示只要把 `>` 與 `<` 的處理對調。\n\n## 不變量或正確性證明\n\n**不變量**：任一時刻，最小表示的起點必定在「尚未被排除的候選」之中；而 `i`、`j` 是目前最靠前的兩個候選。\n\n**跳躍的正確性**（為什麼失配時能一次跳 `k + 1` 格）：設在偏移 `k` 處首次不同，且 `s[(i+k)%n] > s[(j+k)%n]`，即起點 `i` 比起點 `j` 差。要證的是——**起點 `i`、`i+1`、…、`i+k` 全部都可以排除**。\n\n對任意 `0 <= t <= k`，考慮起點 `i + t` 與起點 `j + t`。因為 `i` 與 `j` 的前 `k` 個字元相同，所以 `s[i+t .. i+k-1]` 與 `s[j+t .. j+k-1]` 逐字相同，而第 `k - t` 個字元處 `s[(i+k)%n] > s[(j+k)%n]`。因此起點 `i + t` 的旋轉在第一個相異位上**嚴格大於**起點 `j + t` 的旋轉，`i + t` 不可能是最小表示的起點。\n\n對 `t = 0..k` 都成立，所以可以一口氣令 `i = i + k + 1`。這正是把 `O(n^2)` 壓成 `O(n)` 的關鍵一步。\n\n**`if (i == j) ++j` 的必要性**：跳躍後兩個指標可能撞在一起，此時比較的是同一個起點，`a == b` 恆成立，`k` 會一路加到 `n` 而白跑一遍。往前挪一格即可。\n\n**`O(n)` 的攤還論證**：每輪迴圈要嘛 `k` 增加 1，要嘛 `i` 或 `j` 前進至少 `k + 1` 格並重設 `k = 0`。`i + j` 的總前進量不超過 `2n`，而每次前進 `k+1` 格對應之前累積的 `k` 次比較，因此總比較次數是 `O(n)`。\n\n## 逐步演算法\n\n:::example 回傳最小表示的起始下標（s 視為環）\n**1. 雙指標求最小表示的起點。** `i` 從 0、`j` 從 1 出發，`k` 記已比對相同長度；分出大小就讓較大者跳過已比對段。\n\n```cpp\n// 回傳最小表示的起始下標（s 視為環）\nint minRotation(const string& s) {\n    int n = s.size(), i = 0, j = 1, k = 0;\n    while (i < n && j < n && k < n) {\n        char a = s[(i + k) % n], b = s[(j + k) % n];\n        if (a == b) {\n            ++k;\n            continue;\n        }\n        if (a > b) {\n            i += k + 1;\n        } else {\n            j += k + 1;  // 跳過失敗段\n        }\n        if (i == j) {\n            ++j;  // 兩指標不可重合\n        }\n        k = 0;\n    }\n    return min(i, j);  // 較小者即最小表示起點\n}\n```\n:::\n\n\n\n:::example 由起點還原最小表示字串\n**2. 還原最小表示字串。** 拿到起點後，用 `(start + t) % n` 把環從該處讀一圈。\n\n```cpp\n// 由起點還原最小表示字串\nstring canonical(const string& s) {\n    int n = s.size(), st = minRotation(s);\n    string res;\n    for (int t = 0; t < n; ++t) {\n        res += s[(st + t) % n];\n    }\n    return res;\n}\n```\n:::\n\n\n\n:::example 判斷 a、b 是否互為旋轉\n**3. 應用：判斷旋轉同構。** 兩字串長度相同，且最小表示相同，即互為旋轉。\n\n```cpp\n// 判斷 a、b 是否互為旋轉\nbool sameRing(const string& a, const string& b) {\n    return a.size() == b.size() && canonical(a) == canonical(b);\n}\n```\n:::\n\n\n\n**邊界與複雜度。** 索引務必 `% n` 回到開頭以模擬環；`if (i == j) j++` 不可漏，否則兩指標重合會空轉。時間 `O(n)`、空間 `O(1)`（還原字串才多花 `O(n)`）。求「最大表示」只要把比較方向反過來即可。\n\n## C++17 模板\n\n```cpp\n// 最小表示法：回傳字典序最小旋轉的起點下標，O(n) 時間、O(1) 空間\nint minimalRepresentation(const string& s) {\n    const int n = s.size();\n    int i = 0, j = 1, k = 0;\n\n    while (i < n && j < n && k < n) {\n        char a = s[(i + k) % n], b = s[(j + k) % n];  // % n 模擬環\n        if (a == b) { ++k; continue; }\n\n        if (a > b) i += k + 1;  // 起點 i 較差，i..i+k 全可排除（見證明）\n        else       j += k + 1;  // 起點 j 較差\n\n        if (i == j) ++j;        // 兩指標不可重合，否則會空轉\n        k = 0;\n    }\n    return min(i, j);\n}\n\n// 還原最小表示的字串\nstring minimalRotation(const string& s) {\n    int p = minimalRepresentation(s);\n    return s.substr(p) + s.substr(0, p);\n}\n\n// 判斷 a 與 b 是否互為旋轉\nbool isRotation(const string& a, const string& b) {\n    return a.size() == b.size() && minimalRotation(a) == minimalRotation(b);\n}\n```\n\n求**最大**表示只要把 `if (a > b)` 與 `else` 的處理對調。\n\n## 程式碼拆解\n\n- `pi[i]` 表示前綴的最長 border；失配時跳到下一個可能 border，而不是回頭重比。\n- 預處理陣列或狀態機保存已比對資訊，主迴圈失配時不用回到原點重比。\n- 下標與長度的換算要固定，尤其是 border、LCP、半徑與回傳起點。\n\n## 時間與空間複雜度\n\n**時間 `O(n)`**（由上述攤還論證），**空間 `O(1)`**——只用三個整數，不必複製字串。還原最小表示的字串才需要額外 `O(n)`。\n\n對比暴力枚舉所有 `n` 個旋轉並兩兩比較的 `O(n^2)`。\n\n**與「拼接後找子串」的比較**：判斷 `a`、`b` 是否互為旋轉，也可以檢查 `b` 是否為 `a + a` 的子串（用 KMP，`O(n)`）。兩者複雜度相同，但拼接法要 `O(n)` 額外空間，而且**答不出「應該從哪裡開始讀」**——需要極值旋轉本身時就只能用最小表示法。\n\n**多字串分組**：要把一堆字串按「是否互為旋轉」分組時，對每個字串求最小表示再當作 key 丟進 `unordered_map`，總計 `O(Σ|s|)`。這比兩兩比對的 `O(n^2)` 好得多。\n\n## 常見錯誤與邊界條件\n\n- **漏掉 `if (i == j) ++j`**。兩指標重合後會一直比自己、`k` 加到 `n` 才跳出，雖然多數情況仍給出正確答案，但邏輯是錯的且會退化。務必加上。\n- **忘記 `% n`**。不模就會越界。若改用 `s + s` 的拼接寫法，則要注意上界是 `n` 而非 `2n`。\n- **失配後沒重設 `k = 0`**。跳躍後兩個新起點的已比對長度歸零，不重設會直接跳過前面的字元。\n- **跳躍只走一格**。寫成 `++i` 而非 `i += k + 1`，答案仍對但退化成 `O(n^2)`——這是「對但超時」的典型。\n- **求最大表示時只改了一邊的比較**。`>` 與 `<` 的兩個分支都要對調。\n- **回傳 `i` 而非 `min(i, j)`**。迴圈可能因 `j >= n` 而結束，此時答案在 `i`；也可能反過來。一律取 `min`。\n- **邊界**：`n == 1` 直接回傳 0；空字串要特判；全部字元相同時 `k` 會一路加到 `n` 然後跳出，答案是 0，正確。\n- 驗證方式：對拍一份「枚舉所有旋轉取最小」的暴力，用隨機小字串跑幾千組。\n\n## 常見變形\n\n- KMP 處理模式串 border。\n- Z 函式處理每個後綴和整串的 LCP。\n- 雜湊適合快速判等，AC 自動機適合多模式。\n\n## 與相似技巧的比較\n\n- **對比「拼接後找子串」**（`b` 是否為 `a + a` 的子串）：同樣 `O(n)`，但只能回答「是不是旋轉」，答不出「最小旋轉是哪一個」。需要極值本身就用最小表示法。\n- **對比後綴陣列**：對 `s + s` 建後綴陣列後，排名最前且起點 `< n` 的後綴就是最小表示。正確但 `O(n log n)` 且實作長得多。\n- **對比字串雜湊**：能 `O(1)` 比較兩個旋轉是否相同，但要找**最小**的仍需 `O(n log n)`（對每個起點二分 LCP 再比一位）。最小表示法更直接。\n- **對比 Booth 演算法**：另一個 `O(n)` 求最小旋轉的經典演算法，基於 KMP 的失配函數。與本節的雙指標法複雜度相同，雙指標版更短好記。\n- **對比一般的雙指標**：本節的雙指標維護的是「候選起點」而非「區間端點」，跳躍量由已比對長度決定——這種「失配後大步跳」的模式與 KMP 的 border 跳躍同源。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 899 | [有序佇列](https://leetcode.cn/problems//orderly-queue/) | 2097 |\n| 1625 | [執行操作後字典序最小的字串](https://leetcode.cn/problems//lexicographically-smallest-string-after-applying-operations/) | 1992 |\n| 1708 | [長度為 K 的最大子陣列](https://leetcode.cn/problems//largest-subarray-length-k/) |  |\n| 3403 | [從盒子中找出字典序最大的字串 I](https://leetcode.cn/problems//find-the-lexicographically-largest-string-from-the-box-i/) | 1762 |\n| 3406 | [從盒子中找出字典序最大的字串 II](https://leetcode.cn/problems//find-the-lexicographically-largest-string-from-the-box-ii/) |  |\n\n## 本節重點速查\n\n- 雙指標 `i = 0`、`j = 1`，偏移 `k`，一律用 `(i + k) % n` 模擬環。\n- 相同就 `++k`；\n- `s[(i+k)%n] > s[(j+k)%n]` 就 `i += k + 1`（反之 `j += k + 1`），然後 **`if (i == j) ++j`**（不可重合，否則空轉）並 **`k = 0`**。\n- 答案是 `min(i, j)`。\n- **跳 `k+1` 格的正確性**：起點 `i..i+k` 都能被對應的 `j..j+k` 嚴格擊敗，所以可一次全部排除——這是把 `O(n^2)` 壓成 `O(n)` 的關鍵，寫成 `++i` 答案仍對但會超時。\n- 求**最大**表示把兩個比較分支對調。\n- 時間 `O(n)`、空間 `O(1)`。\n- 判斷互為旋轉也可用「`b` 是否為 `a + a` 的子串」，但那答不出「從哪開始讀」。',
        },
      ],
    },
    {
      id: 15,
      title: "6. 字典樹",
      summary:
        "## 這個技術解決什麼問題\n\n手上有一大堆字串（或一堆數字的二進位形式），要反覆問「有沒有某個前綴」「哪個字串和我共用最長前綴」「跟某個數字 XOR 最大的是誰」。\n\n每次查詢都掃過全部字串是 `O(字串數 × 長度)`。字典樹的觀察是：**很多字串開頭長得一樣**——`cat`、`car`、`card` 都以 `ca` 開頭。把共同開頭只存一份、像樹一樣分岔出去，查詢就只要沿樹走一條路，成本變成 `O(查詢字串長度)`，**與字典裡有幾個字串完全無關**。\n\n把整數看成固定長度的二進位字串插進 **01-trie**，就能對「XOR 最大／最小」做逐位貪心——這是它在演算法題裡最常見的變形。\n\n## 辨識題型的訊號\n\n看到以下味道，就往字典樹想：\n\n- **反覆查「前綴」相關問題**：以某字串開頭的有幾個、是否存在某前綴、字元流即時匹配（如〔字元流〕Stream of Characters 1032）。\n- **在一堆字串裡找共同開頭或互相匹配**：例如〔陣列中的字串匹配〕1408、〔多次搜尋〕1001 這種「哪些字串是別人的子串／前綴」。\n- **題目出現「XOR 最大／最小」且要在一堆數字裡選**：把數字當二進位字串插進 01-trie 做逐位貪心。\n- **暴力是「每次查詢都重掃全部字串逐字比較」**，總時間爆炸；trie 把共同前綴合併，查詢只與單個字串長度有關。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的「邊」代表什麼？是 26 個字母，還是 0/1 兩個位元？\n2. 每個節點除了子節點指標，還需要存什麼（結尾標記、經過次數）？\n3. 查詢時我是「沿目標前綴走」，還是「每一步刻意走相反分支」做貪心？\n\n**代表例題**\n\n- 本節題單尚未配置代表題；建議先用章節標題關鍵字到題庫搜尋同型題，並記錄暴力解與正解差異。\n\n## 核心想法與直覺\n\n本節要解決的場景是：手上有一大堆字串（或一堆數字的二進位形式），你要反覆問「有沒有某個前綴」「哪個字串和我共用最長前綴」「跟某個數字 XOR 最大的是誰」。\n\n最直覺的做法是每次查詢都把全部字串重掃一遍逐字元比較，很慢。觀察到關鍵：**很多字串開頭都長得一樣**，例如 `cat`、`car`、`card` 都以 `ca` 開頭。如果把這些共同的開頭只存一份、像樹一樣分岔出去，查前綴就只要沿著樹走，不必重複比較。這棵樹就叫「字典樹（trie）」。\n\n:::example 字典樹節點：26 條邊（小寫字母）+ 是否為某個單字的結尾\n每個節點代表「走到這裡所拼出的前綴」，每條邊代表「再接一個字元」。從根往下走 `c → a → t`，就走到了 `cat`。\n\n```cpp\n// 字典樹節點：26 條邊（小寫字母）+ 是否為某個單字的結尾\nstruct Node {\n    Node* child[26] = {};  // child[i] 代表接上字母 'a'+i\n    bool isEnd = false;    // 走到這裡是否剛好是一個完整單字\n};\n```\n:::\n\n\n\n同樣的想法可以搬到數字上：把每個數字寫成固定位數的二進位（例如 30 位），由高位到低位插進一棵只有兩條邊（0 與 1）的 trie，這叫「01-trie」，常用來做「找出和某數 XOR 最大的另一個數」這類逐位貪心。\n\n## 狀態／資料結構定義\n\n**節點**：每個節點代表一個前綴。用陣列池比指標好寫也更快：\n\n```\nint ch[MAXN][SIGMA];  // ch[u][c] = 從節點 u 走字元 c 到達的節點，0 表示不存在\nint cnt[MAXN];        // 以此節點為結尾的字串數（支援重複插入與刪除）\nint pass[MAXN];       // 經過此節點的字串數（回答前綴計數、支援刪除）\nint tot = 0;          // 已配置的節點數，根是 0\n```\n\n**`SIGMA`**：字元集大小。小寫字母是 26，01-trie 是 2。**字元集大時不要硬開 26**——含大小寫、數字或任意 ASCII 時要開 62 或 128，否則會寫壞記憶體。字元集非常大時改用 `unordered_map<char,int>` 存邊，省空間但常數變大。\n\n**插入**：沿字元走，節點不存在就 `ch[u][c] = ++tot`，每步 `++pass[u]`，結尾 `++cnt[u]`。\n\n**刪除**：沿路 `--pass`，結尾 `--cnt`。`pass` 歸零即代表該分支已無字串，可視為不存在——這是支援刪除的關鍵，光有 `cnt` 做不到。\n\n**01-trie 的位序**：**必須從最高位插到最低位**，否則逐位貪心的高位優先性質不成立。\n\n## 不變量或正確性證明\n\n**路徑不變量**：從根走到節點 `u` 的路徑上的字元序列，恰好是 `u` 所代表的前綴；而且**這條路徑是唯一的**——每個節點只有一個父節點，每條邊標一個字元。\n\n因此「查詢前綴 `p` 是否存在」等價於「從根沿 `p` 走得通」，「以 `p` 開頭的字串有幾個」等價於 `pass[走到的節點]`。這兩件事都只要 `O(|p|)`。\n\n**`pass` 與 `cnt` 的區別**：`cnt[u]` 是「恰好等於這個前綴」的字串數，`pass[u]` 是「以這個前綴開頭」的字串數。查完整字串用 `cnt`，查前綴用 `pass`。混用是最常見的邏輯錯誤。\n\n**01-trie 逐位貪心的正確性**：要找與 `x` XOR 最大的數。從最高位往下，在每一層優先走「與 `x` 當前位相反」的分支（那樣該位的 XOR 是 1），走不通才走同向。\n\n正確性來自位權重：`2^b > 2^(b-1) + … + 2^0`。只要第 `b` 位能取到 1，不論低位如何，結果都大於「第 `b` 位為 0 而低位全為 1」的最大值。所以每層貪心取 1 永不吃虧——這與「試填法」是同一套交換論證。\n\n**為什麼必須從最高位插**：貪心是由高位往低位決定的，樹的深度順序必須與位權重順序一致。從低位插的話，第一層分岔對應的是最低位，貪心就完全失去意義。\n\n## 逐步演算法\n\n:::example 節點：pass 記錄有幾個單字經過此節點（支援前綴計數 / 刪除）\n**1. 定義節點。** 字元集多大，每個節點就有多少條邊；再加上需要的計數或結尾標記。\n\n```cpp\n// 節點：pass 記錄有幾個單字經過此節點（支援前綴計數 / 刪除）\nstruct Node {\n    Node* child[26] = {};\n    int pass = 0;  // 經過次數\n    bool isEnd = false;\n};\n```\n:::\n\n\n\n:::example 插入單字：沿字元建立路徑，終點標記 isEnd\n**2. 插入。** 從根開始，沿字元逐層往下；該分支不存在就新建節點，最後在終點標記結尾。\n\n```cpp\n// 插入單字：沿字元建立路徑，終點標記 isEnd\nvoid insert(Node* root, const string& s) {\n    Node* cur = root;\n    for (char c : s) {\n        int i = c - 'a';\n        if (!cur->child[i]) {\n            cur->child[i] = new Node();\n        }\n        cur = cur->child[i];\n        cur->pass++;  // 這個前綴又多一個單字經過\n    }\n    cur->isEnd = true;\n}\n```\n:::\n\n\n\n:::example 計算有多少單字以 prefix 為前綴\n**3. 查詢前綴。** 同樣沿字元走；中途斷掉就代表不存在，走到底再看 `pass` 或 `isEnd`。\n\n```cpp\n// 計算有多少單字以 prefix 為前綴\nint countPrefix(Node* root, const string& prefix) {\n    Node* cur = root;\n    for (char c : prefix) {\n        int i = c - 'a';\n        if (!cur->child[i]) {\n            return 0;  // 路斷了 → 沒有\n        }\n        cur = cur->child[i];\n    }\n    return cur->pass;\n}\n```\n:::\n\n\n\n:::example 由高位到低位，能走相反位就走，湊出最大 XOR\n**4. 01-trie 的貪心查詢。** 求最大 XOR 時，每一位都「優先走相反位元」，因為相反位 XOR 出 1，能讓高位儘量大。\n\n```cpp\n// 由高位到低位，能走相反位就走，湊出最大 XOR\nfor (int b = 29; b >= 0; b--) {\n    int bit = (x >> b) & 1;\n    if (cur->child[bit ^ 1]) {\n        ans |= (1 << b);\n        cur = cur->child[bit ^ 1];\n    } else {\n        cur = cur->child[bit];\n    }\n}\n```\n:::\n\n\n\n**5. 邊界與複雜度。** 插入 / 查詢一個長度 `L` 的字串是 `O(L)`，與字典中字串數量無關。空間最壞 `O(總字元數 × 字元集大小)`，字元集越大越耗記憶體。要支援刪除就靠 `pass` 計數歸零判斷。\n\n## C++17 模板\n\n```cpp\n// 字典樹（陣列池版），支援插入、刪除、前綴計數\nstruct Trie {\n    static constexpr int SIGMA = 26;\n    vector<array<int, SIGMA>> ch{1};   // ch[0] 是根\n    vector<int> cnt{0}, pass{0};\n\n    int newNode() {\n        ch.push_back({});\n        cnt.push_back(0);\n        pass.push_back(0);\n        return (int)ch.size() - 1;\n    }\n\n    void insert(const string& s) {\n        int u = 0;\n        for (char c : s) {\n            int k = c - 'a';\n            if (!ch[u][k]) ch[u][k] = newNode();\n            u = ch[u][k];\n            ++pass[u];\n        }\n        ++cnt[u];\n    }\n\n    // 以 p 為前綴的字串數；p 本身出現幾次要看 cnt\n    int countPrefix(const string& p) const {\n        int u = 0;\n        for (char c : p) {\n            int k = c - 'a';\n            if (!ch[u][k]) return 0;\n            u = ch[u][k];\n        }\n        return pass[u];\n    }\n\n    // 刪除一次 s（呼叫前需確定 s 存在）\n    void erase(const string& s) {\n        int u = 0;\n        for (char c : s) {\n            u = ch[u][c - 'a'];\n            --pass[u];\n        }\n        --cnt[u];\n    }\n};\n\n// 01-trie：求與 x 的 XOR 最大值。務必從最高位插入\nstruct XorTrie {\n    static constexpr int BITS = 30;\n    vector<array<int, 2>> ch{1};\n\n    void insert(int x) {\n        int u = 0;\n        for (int b = BITS; b >= 0; --b) {  // 高位優先\n            int k = (x >> b) & 1;\n            if (!ch[u][k]) { ch.push_back({}); ch[u][k] = (int)ch.size() - 1; }\n            u = ch[u][k];\n        }\n    }\n\n    int maxXor(int x) const {\n        int u = 0, ans = 0;\n        for (int b = BITS; b >= 0; --b) {\n            int k = (x >> b) & 1;\n            if (ch[u][k ^ 1]) { ans |= 1 << b; u = ch[u][k ^ 1]; }  // 走相反位 → 該位 XOR 為 1\n            else u = ch[u][k];\n        }\n        return ans;\n    }\n};\n```\n\n## 時間與空間複雜度\n\n**插入／查詢／刪除 `O(L)`**，`L` 是該字串長度——**與字典裡有幾個字串無關**。這是字典樹最核心的性質。\n\n**空間最壞 `O(總字元數 × SIGMA)`**：每個節點都要開一條 `SIGMA` 長的邊陣列。小寫字母（26）、總長 `1e5` 時是 `2.6e6` 個 `int`，約 10 MB——可以接受但不算小。\n\n**字元集大時的取捨**：\n- `SIGMA = 128`（任意 ASCII）配上總長 `1e5` 就是 `1.28e7` 個 `int`，約 50 MB，多半會 MLE。\n- 改用 `unordered_map<char,int>` 存邊：空間降到 `O(總字元數)`，但每次跳轉多一次雜湊查找，常數大 3~5 倍。\n- 或改用「兒子-兄弟」表示法（每節點存第一個子節點與下一個兄弟），空間 `O(總字元數)`，跳轉是 `O(SIGMA)` 的線性掃描。\n\n**01-trie**：`SIGMA = 2`，深度固定 `BITS`（30 或 60）。`n` 個數的空間是 `O(n × BITS)`，`n = 1e5`、30 位時是 `3e6` 個節點——要注意配置量。\n\n**對比雜湊表**：`unordered_set<string>` 查完整字串是 `O(L)` 期望（要算雜湊），但**答不出前綴查詢與最長共同前綴**。這正是字典樹存在的理由。\n\n## 常見錯誤與邊界條件\n\n- **`SIGMA` 開太小**。題目含大寫、數字或符號時仍寫 `c - 'a'`，會產生負下標或越界，直接記憶體毀損。動手前先確認字元集。\n- **`cnt` 與 `pass` 混用**。查「完整字串存不存在」用 `cnt`，查「有幾個以此為前綴」用 `pass`。用錯會在「`app` 是 `apple` 的前綴但本身不在字典裡」這種測資上出錯。\n- **01-trie 從低位插入**。逐位貪心完全失效，答案錯得很離譜。**一律從最高位插**。\n- **`BITS` 開太小**。值到 `1e9` 需要 30 位；到 `1e18` 需要 60 位並全程 `long long`。少一位就漏掉最高位資訊。\n- **在空 trie 上查詢**。`maxXor` 在沒有插入任何數時會走到不存在的節點。呼叫前確認非空，或插入一個哨兵。\n- **沒有支援刪除卻硬刪**。只維護 `cnt` 而不維護 `pass` 時，刪掉最後一個字串後分支仍然「存在」，前綴查詢會回報錯誤結果。要刪除就必須有 `pass`。\n- **節點池預估不足**。用固定大小的 `ch[MAXN][SIGMA]` 時，`MAXN` 要開到「總字元數 + 1」。用 `vector` 動態成長比較安全。\n- **空字串**。插入空字串時 `cnt[0]` 加一（根本身就是結尾），查詢時要記得根也可能是答案。\n\n## 與相似技巧的比較\n\n- **對比雜湊表**：雜湊查「完整字串」更快更省，但**答不出前綴問題**（「有幾個以 `ab` 開頭」「最長共同前綴是誰」）。需求是前綴就用 trie。\n- **對比排序 + 二分**：把字典排序後，某前綴的字串會落在連續一段，用兩次二分即可 `O(L log n)` 回答前綴計數。空間 `O(總字元數)`，比 trie 省得多，但不支援動態插入刪除，也答不出「最長共同前綴是誰」。\n- **對比 AC 自動機**（下一節）：AC 自動機就是「trie + fail 指標」，把 KMP 推廣到多模式串。要在一段文字裡同時匹配多個關鍵字就用它；只是靜態的前綴查詢用 trie 就夠。\n- **對比後綴樹／後綴自動機**：那些處理「一個字串的所有子串」；trie 處理「多個字串的前綴」。問題形狀不同，別混用。\n- **對比線性基**：兩者都能回答 XOR 相關問題。**01-trie 回答「與某個給定數 XOR 最大」**，**線性基回答「任意子集 XOR 最大」**——問的是「一對」還是「任意子集」，決定用哪一個。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 208 | [實現 Trie (前綴樹)](https://leetcode.cn/problems/implement-trie-prefix-tree) |  |\n| 211 | [新增與搜尋單字 - 資料結構設計](https://leetcode.cn/problems/design-add-and-search-words-data-structure) |  |\n| 421 | [陣列中兩個數的最大異或值](https://leetcode.cn/problems/maximum-xor-of-two-numbers-in-an-array) |  |\n| 648 | [單字替換](https://leetcode.cn/problems/replace-words) | 1543 |\n| 676 | [實現一個魔法字典](https://leetcode.cn/problems/implement-magic-dictionary) |  |\n| 720 | [詞典中最長的單字](https://leetcode.cn/problems/longest-word-in-dictionary) |  |\n| 745 | [前綴和後綴搜尋](https://leetcode.cn/problems/prefix-and-suffix-search) |  |\n| 1032 | [字元流](https://leetcode.cn/problems/stream-of-characters) | 1843 |\n| 1268 | [搜尋推薦系統](https://leetcode.cn/problems/search-suggestions-system) | 1573 |\n| 1707 | [與陣列中元素的最大異或值](https://leetcode.cn/problems/maximum-xor-with-an-element-from-array) | 2358 |\n| 2416 | [字串的前綴分數和](https://leetcode.cn/problems/sum-of-prefix-scores-of-strings) | 2291 |\n\n## 本節重點速查\n\n- 把共同前綴只存一份，插入／查詢／刪除都是 **`O(L)`，與字典大小無關**。\n- 維護兩個計數：**`cnt[u]`** 是「恰好等於這個前綴」的字串數（查完整字串用），**`pass[u]`** 是「以此為前綴」的字串數（查前綴用、也是支援刪除的關鍵）——混用是最常見的邏輯錯。\n- `SIGMA` 要對得上真正的字元集，含大小寫數字時別硬寫 `c - 'a'`。\n- 空間最壞 `O(總字元數 × SIGMA)`，字元集大就改用 `unordered_map` 存邊。\n- **01-trie 必須從最高位插入**，逐位貪心優先走「相反位」，正確性同樣來自 `2^b` 大於所有低位之和。\n- 01-trie 回答「與某個給定數 XOR 最大」，任意子集的 XOR 要用線性基。",
    },
    {
      id: 16,
      title: "7. AC 自動機",
      summary:
        "AC 自動機 = 字典樹 + KMP，適合多模式串匹配（如 1032、1408）。它與字串雜湊的用途部分重疊，但不能普遍互相替代。\n\n:::example AC 自動機 = 字典樹 + KMP fail 指標\n**C++ 模板**：\n\n```cpp\n// AC 自動機 = 字典樹 + KMP fail 指標\nstruct AhoCorasick {\n    vector<array<int, 26>> ch;  // 字典樹的邊\n    vector<int> fail;           // fail 指標（類似 KMP 的 next 陣列）\n    vector<int> cnt;            // 每個節點結尾的模式串數量\n\n    // 建立新節點\n    int newNode() {\n        ch.push_back({});\n        fail.push_back(0);\n        cnt.push_back(0);\n        ch.back().fill(0);\n        return ch.size() - 1;\n    }\n\n    // 建構子：初始化根節點\n    AhoCorasick() { newNode(); }\n\n    // 插入模式串到字典樹\n    void insert(const string& s) {\n        int cur = 0;\n        for (char c : s) {\n            int u = c - 'a';\n            // 如果該邊不存在，建立新節點\n            if (!ch[cur][u]) {\n                ch[cur][u] = newNode();\n            }\n            cur = ch[cur][u];\n        }\n        cnt[cur]++;  // 標記模式串結尾\n    }\n\n    // 建立 fail 指標（類似 KMP 的預處理）\n    void build() {\n        queue<int> q;\n        // 第一層節點的 fail 指標都指向根節點\n        for (int i = 0; i < 26; ++i) {\n            if (ch[0][i]) {\n                q.push(ch[0][i]);\n            }\n        }\n        // BFS 建立 fail 指標\n        while (!q.empty()) {\n            int u = q.front();\n            q.pop();\n            for (int i = 0; i < 26; ++i) {\n                if (ch[u][i]) {\n                    // 節點存在：設定 fail 指標並加入佇列\n                    fail[ch[u][i]] = ch[fail[u]][i];\n                    q.push(ch[u][i]);\n                } else {\n                    // 節點不存在：直接指向 fail 位置的對應邊（路徑壓縮）\n                    ch[u][i] = ch[fail[u]][i];\n                }\n            }\n        }\n    }\n};\n```\n:::",
      children: [
        {
          id: 17,
          title: "7.1 基礎",
          summary:
            "## 這個技術解決什麼問題\n\n一段長文字裡，要**同時**找出一大堆關鍵字（模式串）的所有出現位置。\n\n對每個關鍵字各跑一次 KMP 是 `O(關鍵字數 × 文字長度)`——文字被重掃很多遍。AC 自動機（Aho-Corasick）讓**自動機的文字掃描階段只走一遍**；若要列舉全部命中，總時間還要加上輸出數量：先把所有關鍵字塞進一棵字典樹，再給每個節點裝一條 **fail link**，指向「目前已匹配的後綴中，最長的、也是某個關鍵字前綴的那一段」。\n\n它就是 KMP 在多模式串上的推廣——KMP 的 `pi` 陣列是一條鏈上的 border，AC 自動機的 fail 是 trie 上的 border。\n\n## 辨識題型的訊號\n\n看到以下味道，就往 AC 自動機想：\n\n- **「多個關鍵字」要同時在文字裡匹配**：例如〔字元流〕1032 把字典串建成自動機、逐字元餵流；〔多次搜尋〕1001 在大文字中找多個小串的位置。\n- **暴力是「每個關鍵字各掃一遍文字」或「每個位置試比所有關鍵字」**，總時間是關鍵字數乘文字長，太慢；AC 自動機讓狀態轉移只需線性掃描文字；列舉命中時另加輸出成本。\n- **題目像「用一組片語拼出目標字串」的最小代價**（〔最小代價構造字串〕3213）：把可用片語建成自動機，掃目標串時用 fail link 找所有能在此結束的片語做轉移。\n- 看到「重用已比對的資訊、失配時不從頭再來」的需求，就是 KMP/AC 自動機家族。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要找的「模式串」是一個還是一群？一群就考慮 AC 自動機。\n2. 失配時我跳去哪裡？fail 指標代表的「最長可重用後綴」是什麼？\n3. 掃文字一遍時，我在每個節點如何收集「此處結束的所有關鍵字」？\n\n## 核心想法與直覺\n\n本節要解決的場景是：手上有「一大堆要找的關鍵字（模式串）」，要在一段長文字裡一次把它們全部找出來。例如關鍵字有 `he`、`she`、`his`，要掃一遍文章列出每個關鍵字出現的位置。\n\n最直覺的做法是「對每個關鍵字各跑一次搜尋」，文字會被重掃很多遍。AC 自動機（Aho-Corasick）的核心想法是：**先把所有關鍵字塞進一棵字典樹（trie），再以一次線性文字掃描更新匹配狀態**；若列出所有命中，仍需計入輸出成本。\n\n:::example AC 自動機節點：trie 的邊 + fail 指標 + 此處結束了幾個關鍵字\n關鍵在於補上一種「失敗時的捷徑」——失配指標（fail link）。當我們沿著文字往下匹配，突然下一個字元在 trie 裡走不下去時，不要從頭重來，而是跳到「目前已匹配後綴中，最長的、又是某關鍵字前綴」的節點繼續比。這個跳轉就靠 fail link，概念和 KMP 的 border（最長相同前後綴）完全一樣，只是搬到樹上。\n\n```cpp\n// AC 自動機節點：trie 的邊 + fail 指標 + 此處結束了幾個關鍵字\nstruct Node {\n    Node* child[26] = {};\n    Node* fail = nullptr;  // 失配時跳去的節點\n    int cnt = 0;           // 以此節點結尾的關鍵字數\n};\n```\n:::\n\n\n\n有了 fail link，掃文字時無論成功或失配，指標都只往前或沿 fail 跳，整段文字只需 `O(文字長度)` 就能找出全部關鍵字。\n\n## 狀態／資料結構定義\n\n**trie 部分**：`ch[u][c]` 是節點 `u` 走字元 `c` 的子節點，`cnt[u]` 記錄以 `u` 結尾的關鍵字數（或關鍵字 id）。\n\n**fail link**：`fail[u]` 指向「`u` 所代表字串的最長真後綴」中，仍是某個關鍵字前綴的那個節點。根與根的直接子節點的 `fail` 都是根。\n\n**建 fail 用 BFS**（按深度由淺到深，因為 `fail[u]` 的深度必定小於 `u`）：\n\n```\nfail[ch[u][c]] = ch[fail[u]][c]\n```\n\n**trie 圖（自動機化）**：直接把不存在的邊補成 `ch[u][c] = ch[fail[u]][c]`。這樣掃文字時**每步都是 `O(1)` 的一次跳轉**，不必沿 fail 鏈往回找——這是實作上最重要的簡化。\n\n**計數優化**：若題目要「每個位置命中多少關鍵字」，可在 BFS 時把 `cnt[u] += cnt[fail[u]]`，把 fail 鏈上的計數預先累加好；否則每次命中都要沿 fail 鏈走一遍，最壞退化。\n\n**`SIGMA`** 直接決定每節點的邊數與記憶體，字元集大時要重新估算。\n\n## 不變量或正確性證明\n\n**fail link 的不變量**：`fail[u]` 代表的字串，是 `u` 代表字串的**最長真後綴**，且它本身是某個關鍵字的前綴（即在 trie 中存在）。\n\n**BFS 建構的正確性**：設 `v = ch[u][c]`（`u` 加一個字元 `c` 到 `v`）。`v` 的真後綴，就是「`u` 的真後綴加上 `c`」。要找最長的、且在 trie 中存在的那一個——依定義，`u` 的最長真後綴是 `fail[u]`，所以優先試 `ch[fail[u]][c]`。若不存在就沿 `fail[fail[u]]` 繼續試。**做了 trie 圖之後，`ch[fail[u]][c]` 已經被補成「沿 fail 鏈找到的第一個存在的轉移」，所以一步到位。**\n\n因為 `fail[u]` 的深度嚴格小於 `u`，BFS 按深度處理保證用到 `fail[u]` 時它已經算好——這正是必須用 BFS 而非 DFS 的理由。\n\n**匹配的不變量**：掃文字時，當前節點 `u` 恆等於「文字已讀入前綴的最長後綴中，是某個關鍵字前綴的那一段」。\n\n**沒有漏解**：某關鍵字 `w` 在文字位置 `i` 結束 ⟺ `w` 是「文字前 `i+1` 個字元」的後綴 ⟺ `w` 對應的節點出現在 `u` 的 fail 鏈上。所以在每個位置沿 fail 鏈收集所有終止節點，就能不重不漏地列出所有命中。\n\n**trie 圖的等價性**：把不存在的邊補成 `ch[fail[u]][c]`，等價於「沿 fail 鏈往回找第一個有這條邊的祖先」。因為 fail 鏈上每一步都是取更短的後綴，補邊只是把這個查找過程預先算好，不改變語意。\n\n## 逐步演算法\n\n:::example 插入一個關鍵字，終點 cnt++\n**1. 建 trie：把所有關鍵字插進去。** 和一般字典樹一樣，終點累加 `cnt`。\n\n```cpp\n// 插入一個關鍵字，終點 cnt++\nvoid insert(Node* root, const string& s) {\n    Node* cur = root;\n    for (char c : s) {\n        int i = c - 'a';\n        if (!cur->child[i]) {\n            cur->child[i] = new Node();\n        }\n        cur = cur->child[i];\n    }\n    cur->cnt++;\n}\n```\n:::\n\n\n\n:::example BFS 建 fail：u 經字元 i 的子節點，其 fail = u->fail 經 i…\n**2. 用 BFS 建 fail link。** 根的子節點 fail 指向根；其餘節點的 fail 由父節點的 fail 推得：沿父 fail 找有同字元邊的節點。\n\n```cpp\n// BFS 建 fail：u 經字元 i 的子節點，其 fail = u->fail 經 i 的節點\nqueue<Node*> q;\nfor (int i = 0; i < 26; ++i) {\n    if (root->child[i]) {\n        root->child[i]->fail = root;\n        q.push(root->child[i]);\n    }\n}\nwhile (!q.empty()) {\n    Node* u = q.front();\n    q.pop();\n    for (int i = 0; i < 26; ++i) {\n        Node*& v = u->child[i];\n        if (v) {\n            v->fail = u->fail->child[i] ? u->fail->child[i] : root;\n            q.push(v);\n        } else {\n            v = u->fail->child[i] ? u->fail->child[i] : root;  // 補轉移，省去掃 fail\n        }\n    }\n}\n```\n:::\n\n\n\n:::example 掃文字：每步沿 fail 鏈累計命中的關鍵字\n**3. 掃文字匹配。** 逐字元沿邊走（上一步已把缺邊補成轉移），每到一個節點就沿 fail 鏈收集所有以此結尾的關鍵字。\n\n```cpp\n// 掃文字：每步沿 fail 鏈累計命中的關鍵字\nNode* cur = root;\nfor (char c : text) {\n    cur = cur->child[c - 'a'];\n    for (Node* t = cur; t != root; t = t->fail) {\n        total += t->cnt;\n    }\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。** 建表 `O(所有關鍵字總長)`；匹配 `O(文字長度 + 命中數)`。沿 fail 鏈收集可能退化，計數題常預先把 `cnt` 沿 fail 累加好以加速。注意字元集大小直接決定每節點的邊數與記憶體。\n\n## C++17 模板\n\n```cpp\n// AC 自動機（trie 圖版）：一次掃描同時匹配所有關鍵字\nstruct AhoCorasick {\n    static constexpr int SIGMA = 26;\n    vector<array<int, SIGMA>> ch{1};\n    vector<int> fail{0}, cnt{0};\n\n    int newNode() {\n        ch.push_back({});\n        fail.push_back(0);\n        cnt.push_back(0);\n        return (int)ch.size() - 1;\n    }\n\n    void insert(const string& s) {\n        int u = 0;\n        for (char c : s) {\n            int k = c - 'a';\n            if (!ch[u][k]) ch[u][k] = newNode();\n            u = ch[u][k];\n        }\n        ++cnt[u];\n    }\n\n    // 建 fail 並自動機化。必須 BFS：fail[u] 的深度嚴格小於 u\n    void build() {\n        queue<int> q;\n        for (int c = 0; c < SIGMA; ++c)\n            if (ch[0][c]) q.push(ch[0][c]);  // 根的子節點 fail 都是根（預設 0）\n\n        while (!q.empty()) {\n            int u = q.front();\n            q.pop();\n            // 把 fail 鏈上的計數預先累加，避免匹配時逐條走回去\n            cnt[u] += cnt[fail[u]];\n            for (int c = 0; c < SIGMA; ++c) {\n                int& v = ch[u][c];\n                if (v) {\n                    fail[v] = ch[fail[u]][c];\n                    q.push(v);\n                } else {\n                    v = ch[fail[u]][c];  // trie 圖：補邊，讓轉移永遠 O(1)\n                }\n            }\n        }\n    }\n\n    // 掃一遍文字，回傳命中的關鍵字總次數\n    long long query(const string& text) const {\n        long long total = 0;\n        int u = 0;\n        for (char c : text) {\n            u = ch[u][c - 'a'];  // 補過邊，永遠走得通\n            total += cnt[u];     // cnt 已沿 fail 累加\n        }\n        return total;\n    }\n};\n```\n\n1032（字元流）把 `query` 拆成逐字元餵入並保留 `u`；3213（最小代價構造字串）則在自動機上跑 DP，狀態是 `(文字位置, 自動機節點)`。\n\n## 時間與空間複雜度\n\n**建構 `O(所有關鍵字總長 × SIGMA)`**：BFS 走過每個節點，每個節點掃一遍 `SIGMA` 條邊做補邊。\n\n**匹配 `O(文字長度)`**（用 trie 圖 + 預累加 `cnt`）：每個字元一次 `O(1)` 跳轉。若要列出**每一次命中的位置與是哪個關鍵字**，則是 `O(文字長度 + 命中數)`——命中數本身可能很大（例如關鍵字都是 `a`、`aa`、`aaa`… 而文字全是 `a`）。\n\n**空間 `O(所有關鍵字總長 × SIGMA)`**：與字典樹相同。`SIGMA = 26`、總長 `1e5` 時約 10 MB。\n\n**沒有預累加 `cnt` 的退化**：每次命中都沿 fail 鏈往上走，最壞是 `O(文字長度 × 最大深度)`。計數類題目一定要在 BFS 時做 `cnt[u] += cnt[fail[u]]`。\n\n**對比逐一 KMP**：`O(Σ|p_i| + k × n)`，`k` 是關鍵字數。AC 自動機是 `O(Σ|p_i| × SIGMA + n)`——文字只掃一遍，`k` 很大時差距懸殊。\n\n## 常見錯誤與邊界條件\n\n- **用 DFS 建 fail**。`fail[u]` 依賴深度更淺的節點，必須 BFS 按層處理。用 DFS 會讀到還沒算好的 `fail`。\n- **沒有自動機化就直接跳轉**。不補邊的話，匹配時遇到不存在的轉移要沿 fail 鏈往回找，寫起來繁瑣且容易漏。補邊之後 `ch[u][c]` 永遠走得通，程式短很多。\n- **忘記 `cnt[u] += cnt[fail[u]]`**。計數會漏掉「是更長關鍵字後綴」的那些關鍵字，而且不加的話效能也會退化。\n- **重複關鍵字沒處理**。同一個關鍵字插入兩次時 `cnt` 會是 2；題目若要求「不同關鍵字數」就要改用 set 或先去重。\n- **`SIGMA` 與字元集不符**。含大寫或數字時 `c - 'a'` 會越界。\n- **節點池不足**。用固定陣列時 `MAXN` 要開到「所有關鍵字總長 + 1」。\n- **根節點的 fail**。根與根的直接子節點的 fail 都指向根（0）。初始化成別的值會讓整條鏈錯亂。\n- **空關鍵字**。插入空字串會讓 `cnt[0]` 加一，導致每個位置都命中。呼叫前先過濾。\n- 驗證方式：關鍵字少時對拍逐一 KMP 的結果。\n\n## 與相似技巧的比較\n\n- **對比 KMP**：AC 自動機就是 KMP 的多模式串版本。KMP 的 `pi` 是一條鏈上的 border，fail link 是 trie 上的 border；兩者的不變量與正確性論證完全平行。**單一模式串用 KMP，多個用 AC 自動機。**\n- **對比字典樹**：trie 只能查「完整字串或前綴」，無法在一段文字裡找出現位置。fail link 補上的正是「失配後退到哪裡」的資訊。\n- **對比逐一跑 KMP**：`k` 個關鍵字就掃 `k` 遍文字。關鍵字很多、文字很長時 AC 自動機明顯勝出；只有兩三個關鍵字時逐一 KMP 更省事。\n- **對比字串雜湊**：可以對每個長度各建一次雜湊表比對，但關鍵字長度不一時要對每種長度各掃一遍，退化成 `O(不同長度數 × n)`。\n- **對比後綴自動機**：後綴自動機處理「一個字串的所有子串」，AC 自動機處理「多個模式串對一段文字」。方向相反，別搞混。\n- **在自動機上做 DP**：這是 AC 自動機的高階用法（3213 最小代價構造字串），狀態是 `(位置, 自動機節點)`。KMP 也有對應的「在 `pi` 上做 DP」，是同一個思路。\n\n## 代表例題\n\n**題號**：3758、1408、1001、1032、3213\n\n重點是重用已比對資訊；練習把 border、LCP、hash 或自動機狀態寫成不變式。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 3758 | [將數字詞轉換為數字](https://leetcode.cn/problems/convert-number-words-to-digits/)（Premium） |  |\n| 1408 | [陣列中的字串匹配](https://leetcode.cn/problems/string-matching-in-an-array/) | 1223 |\n| 1001 | [面試題 17.17. 多次搜尋](https://leetcode.cn/problems/multi-search-lcci/) | 1873 |\n| 1032 | [字元流](https://leetcode.cn/problems/stream-of-characters/) | 1970 |\n| 3213 | [最小代價構造字串](https://leetcode.cn/problems/construct-string-with-minimum-cost/)（Premium） | 2171 |\n| 2781 | [最長合法子字串的長度](https://leetcode.cn/problems/length-of-the-longest-valid-substring/) | 2204 |\n| 3292 | [形成目標字串需要的最少字串數 II](https://leetcode.cn/problems/minimum-number-of-valid-strings-to-form-target-ii/) | 2662 |\n\n## 本節重點速查\n\n- AC 自動機 = **trie + fail link**，是 KMP 的多模式串推廣。\n- `fail[u]` 指向 `u` 代表字串的最長真後綴中、仍是某關鍵字前綴的節點。\n- **必須 BFS 建 fail**（`fail[u]` 的深度嚴格較小）。\n- 務必**自動機化**：把不存在的邊補成 `ch[u][c] = ch[fail[u]][c]`，讓匹配時每步都是 `O(1)` 跳轉。\n- 計數題要在 BFS 時做 **`cnt[u] += cnt[fail[u]]`**，否則會漏算「是更長關鍵字後綴」的關鍵字，效能也會退化。\n- 建構 `O(總長 × SIGMA)`、匹配 `O(文字長度)`（列出所有命中則加上命中數）。\n- 空間與 trie 同量級，`SIGMA` 直接決定記憶體。\n- 空關鍵字要先過濾。",
        },
      ],
    },
    {
      id: 18,
      title: "8. 後綴陣列 & 後綴自動機",
      summary:
        "後綴陣列可求字典序最大/最小子串、最長公共子串等。後綴自動機支援子串計數、本質不同子串數等。適用於 1163、1754、1044 等題。\n\n:::example 後綴陣列（倍增法 O(n log n)）\n**C++ 模板**：\n\n```cpp\n// 後綴陣列（倍增法 O(n log n)）\nvector<int> buildSa(const string& s) {\n    int n = s.size();\n    vector<int> sa(n), rank(n), tmp(n);\n    iota(sa.begin(), sa.end(), 0);  // 初始化 sa 為 [0, 1, 2, ..., n-1]\n\n    // 初始化 rank 為字元的 ASCII 值\n    for (int i = 0; i < n; ++i) {\n        rank[i] = s[i];\n    }\n\n    // 倍增排序：每次比較長度為 k 的子串\n    for (int k = 1; k < n; k <<= 1) {\n        // 比較函數：先比較前 k 個字元，再比較後 k 個字元\n        auto cmp = [&](int a, int b) {\n            if (rank[a] != rank[b]) {\n                return rank[a] < rank[b];\n            }\n            int ra = a + k < n ? rank[a + k] : -1;\n            int rb = b + k < n ? rank[b + k] : -1;\n            return ra < rb;\n        };\n\n        // 根據比較函數排序後綴陣列\n        sort(sa.begin(), sa.end(), cmp);\n\n        // 更新 rank：相同的後綴給相同的 rank\n        tmp[sa[0]] = 0;\n        for (int i = 1; i < n; ++i) {\n            tmp[sa[i]] = tmp[sa[i - 1]] + (cmp(sa[i - 1], sa[i]) ? 1 : 0);\n        }\n        rank = tmp;\n\n        // 如果所有後綴都已區分，提前退出\n        if (rank[sa[n - 1]] == n - 1) {\n            break;\n        }\n    }\n    return sa;\n}\n```\n:::",
      children: [
        {
          id: 19,
          title: "8.1 基礎",
          summary:
            '## 這個技術解決什麼問題\n\n圍繞「一個字串的所有後綴」做查詢：最長重複子串（1062）、最長重複子陣列（718）、不同子字串的個數（1698）。\n\n關鍵觀察是：**任何子字串，都是某個後綴的前綴**。所以「子字串的重複、相等、共用前綴」全都能轉成「後綴之間共用多長開頭」的問題。\n\n入門時不必硬寫後綴陣列的倍增建構。更好上手也更常用的組合是 **二分答案 + 字串雜湊**——答案（長度）具單調性（越長越難滿足），猜一個長度 `L`，用雜湊 `O(n)` 驗證「有沒有兩段長度 `L` 的子串相同」。\n\n## 辨識題型的訊號\n\n看到以下味道，就往後綴陣列 / 雜湊想：\n\n- **問題本質是「子串的重複、相等或共用前綴」**：〔最長重複子串〕1062、〔最長重複子陣列〕718（把陣列當字串）、〔字串的不同子字串個數〕1698。\n- **要比較「字串中任意兩段是否相同」很多次**：前綴 hash 讓每次比較 `O(1)`。\n- **答案具單調性**（長度越長越難滿足），可「二分答案」，再用 hash 驗證——這是最好上手的組合。\n- **暴力是枚舉所有子串對逐字比較**（`O(n^3)` 等級），太慢；轉成後綴/hash 把重複比較壓掉。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我能不能把「子串」重新表述成「某個後綴的前綴」？\n2. 答案有沒有單調性，可以二分一個長度再驗證？\n3. 我用什麼讓「兩段子串是否相等」變成 `O(1)`（前綴 hash？相鄰後綴 LCP？）\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 718 | 最長重複子陣列 | N/A | 重點是重用已比對資訊；練習把 border、LCP、hash 或自動機狀態寫成不變式。 |\n| 1062 | 最長重複子串（Premium） | N/A | 重點是重用已比對資訊；練習把 border、LCP、hash 或自動機狀態寫成不變式。 |\n| 1698 | 字串的不同子字串個數（Premium） | N/A | 重點是重用已比對資訊；練習把 border、LCP、hash 或自動機狀態寫成不變式。 |\n| 3135 | 透過新增或刪除結尾字元來同化字串（Premium） | N/A | 重點是重用已比對資訊；練習把 border、LCP、hash 或自動機狀態寫成不變式。 |\n| 面試題 16.18 | 模式匹配 | N/A | 重點是重用已比對資訊；練習把 border、LCP、hash 或自動機狀態寫成不變式。 |\n\n## 核心想法與直覺\n\n本節要解決的場景是：圍繞「一個字串的所有後綴」做查詢，例如「最長重複子串是什麼」「這字串有幾個不同的子字串」。關鍵觀察是：**任何子字串，都是某個後綴的前綴**。所以只要把所有後綴整理好，子字串的問題就變成後綴之間「共用多長開頭」的問題。\n\n:::example 後綴陣列：sa[k] = 字典序第 k 小的後綴，其起點下標\n「後綴陣列（suffix array）」就是把字串的所有後綴按字典序排好後，記下它們的起始位置。例如 `banana` 的後綴排序後，相鄰的後綴開頭往往很像，這正是我們要利用的。\n\n```cpp\n// 後綴陣列：sa[k] = 字典序第 k 小的後綴，其起點下標\n// banana 的後綴：a, ana, anana, banana, na, nana\n// 排序後 sa = {5, 3, 1, 0, 4, 2}\n```\n:::\n\n\n\n:::example 概念：最長重複子串 = 排序後相鄰後綴的最大 LCP\n排序後的第二件法寶是 LCP（Longest Common Prefix，最長共同前綴）：相鄰兩個後綴開頭共用多少字元。直覺上，**重複子串一定出現在「字典序相鄰」的後綴之間**——因為內容像的後綴排序後會挨在一起，所以「最長重複子串」就是相鄰後綴 LCP 的最大值。\n\n```cpp\n// 概念：最長重複子串 = 排序後相鄰後綴的最大 LCP\n// banana 中 ana 與 anana 相鄰，共用 "ana" → 最長重複子串長度 3\n```\n:::\n\n\n\n入門時不必硬寫複雜的後綴陣列建構；常用更好寫的替代：**二分答案 + 字串雜湊（hash）**。猜一個長度 `L`，用 hash 在 `O(n)` 內檢查「有沒有兩段長度 `L` 的子串相同」，對 `L` 做二分即可，同樣能解這類題。\n\n## 狀態／資料結構定義\n\n**二分 + 雜湊（推薦入門）**：\n- 二分的是**長度 `L`**，區間 `[0, n]`。\n- `check(L)`：把所有長度 `L` 的子串雜湊丟進 `unordered_set`，出現重複就回傳 true。用前綴雜湊取子串是 `O(1)`。\n- 要回傳實際子串時，在 `check` 裡記下命中的起點。\n\n**後綴陣列（`sa`）**：`sa[i]` 是字典序第 `i` 小的後綴的起點。\n\n**高度陣列（`height`）**：`height[i] = LCP(後綴 sa[i-1], 後綴 sa[i])`，即排序後**相鄰**兩個後綴的最長共同前綴。\n\n關鍵性質：任意兩後綴 `sa[i]`、`sa[j]`（`i < j`）的 LCP = `min(height[i+1..j])`。有了它，配上稀疏表就能 `O(1)` 回答任意兩後綴的 LCP。\n\n**兩個經典公式**：\n- **最長重複子串** = `max(height)`。\n- **不同子串個數** = `Σ (n - sa[i]) − Σ height[i]`——每個後綴貢獻它的所有前綴，再扣掉與前一個後綴重複算到的部分。\n\n## 不變量或正確性證明\n\n**「子串 ⟺ 後綴的前綴」**：子串 `s[l..r]` 就是後綴 `s[l..]` 的長度 `r-l+1` 前綴。這個雙射是本節所有方法的地基。\n\n**二分的單調性**：若存在兩段長度 `L` 的相同子串，則它們的長度 `L-1` 前綴也相同——所以 `check(L)` 為真蘊含 `check(L-1)` 為真，可行集合是一段前綴 `T…T F…F`，求最後一個為真（見二分的「求最大」一節，中點要偏右）。\n\n**`height` 的取 min 性質**：後綴按字典序排好後，排名相鄰的後綴共用前綴最多。對 `i < j`，`LCP(sa[i], sa[j]) = min(height[i+1..j])`。\n- 「`<=`」：LCP 不會超過路徑上任一相鄰對的 LCP，否則中間那個後綴會被夾在兩者之間卻共用更少前綴，與字典序排序矛盾。\n- 「`>=`」：若所有相鄰對都共用至少 `k` 個字元，則首尾兩個也共用至少 `k` 個（共同前綴具有傳遞性）。\n\n**最長重複子串 = `max(height)` 的證明**：任何重複子串是某兩個後綴的共同前綴，其長度 `<= LCP(那兩個後綴) = min(相鄰 height) <= max(height)`。反之 `max(height)` 本身就對應一對相鄰後綴的共同前綴，確實是重複子串。\n\n**不同子串個數公式的證明**：把所有子串按「它是哪個後綴的前綴」歸類，並約定歸給**字典序最小的那個**後綴。後綴 `sa[i]` 貢獻它的 `n - sa[i]` 個前綴，但其中長度 `<= height[i]` 的那些，已經作為 `sa[i-1]` 的前綴被算過了，要扣掉。所以答案是 `Σ (n - sa[i]) − Σ height[i]`，每個不同子串恰被數一次。\n\n**雜湊的碰撞風險**：`check(L)` 把 `n` 個雜湊兩兩比對，由生日悖論，單雜湊的碰撞機率是 `O(n^2/mod)`——`n = 1e5` 時幾乎必碰。**這類題一定要雙雜湊。**\n\n## 逐步演算法\n\n**1. 狀態表示：把子串問題轉成後綴問題。** 先明確要的是「最長重複」「不同子串個數」還是「兩串公共子串」，再決定用後綴排序的 LCP，或用 hash + 二分。\n\n:::example 前綴 hash：h[i] = s[0..i-1] 的 hash，p[i] = base^…\n**2. 前綴雜湊：讓「任意子串相等」`O(1)` 比較。** 預處理前綴 hash 與冪次，之後任一段子串的 hash 都能直接算。\n\n```cpp\n// 前綴 hash：h[i] = s[0..i-1] 的 hash，p[i] = base^i\nfor (int i = 0; i < n; ++i) {\n    h[i + 1] = h[i] * B + s[i];\n    p[i + 1] = p[i] * B;\n}\n// 子串 s[l..r) 的 hash：\nauto sub = [&](int l, int r) { return h[r] - h[l] * p[r - l]; };\n```\n:::\n\n\n\n:::example 檢查：是否存在長度 L 的重複子串\n**3. 二分長度 + hash 檢查重複。** 對候選長度 `L`，把所有長度 `L` 子串的 hash 丟進集合，撞到就代表存在重複。\n\n```cpp\n// 檢查：是否存在長度 L 的重複子串\nbool ok(int L) {\n    unordered_set<unsigned long long> seen;\n    for (int i = 0; i + L <= n; ++i) {\n        auto v = sub(i, i + L);\n        if (seen.count(v)) {\n            return true;  // 出現過 → 重複\n        }\n        seen.insert(v);\n    }\n    return false;\n}\n// 對 L 做二分：ok 為真就往更長找\n```\n:::\n\n\n\n**4. 不同子串個數。** 用後綴陣列時，答案 = `所有後綴長度總和 − 相鄰 LCP 總和`：每個後綴貢獻它的所有前綴，再扣掉與前一個後綴重複算到的部分。\n\n**5. 邊界與複雜度。** hash 有「碰撞」風險（兩段不同字串算出同 hash），重要題目用雙 hash 或撞到時再逐字驗證。二分 + hash 是 `O(n log n)`；後綴陣列建構常見 `O(n log n)`，LCP 表 `O(n)`。\n\n## C++17 模板\n\n以下用同型延伸示範題 28. 找出字串中第一個匹配項的下標 展示核心寫法。\n\n**完整題目**：在字串 `haystack` 中尋找字串 `needle` 第一次出現的位置，若不存在則回傳 `-1`。\n\n:::example KMP 字串匹配：先建立 needle 的前綴函數 pi，再在 haystack 上做線…\n**線索**：題目需要大量子串匹配、前後綴或相等比較；重用已比較資訊就是 KMP/Z/雜湊/自動機的線索。\n\n```cpp\nclass Solution {\npublic:\n    // KMP 字串匹配：先建立 needle 的前綴函數 pi，再在 haystack 上做線性搜尋\n    int strStr(string haystack, string needle) {\n            int m = needle.size();\n            vector<int> pi(m);  // pi[i]：needle[0..i] 的最長相等真前後綴長度\n            // 建構 pi 陣列\n            for (int i = 1; i < m; ++i) {\n                    int j = pi[i - 1];\n                    // 若失配，利用 pi 回退到上一個可能匹配的位置\n                    while (j && needle[i] != needle[j]) {\n                            j = pi[j - 1];  // 失配時回退，不從頭重新匹配\n                    }\n                    if (needle[i] == needle[j]) {\n                            ++j;\n                    }\n                    pi[i] = j;\n            }\n            // 在 haystack 中用 pi 輔助匹配\n            for (int i = 0, j = 0; i < (int)haystack.size(); ++i) {\n                    while (j && haystack[i] != needle[j]) {\n                            j = pi[j - 1];\n                    }\n                    if (haystack[i] == needle[j]) {\n                            ++j;\n                    }\n                    if (j == m) {  // 完整匹配，返回起始位置\n                            return i - m + 1;\n                    }\n            }\n            return -1;\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- `pi[i]` 表示前綴的最長 border；失配時跳到下一個可能 border，而不是回頭重比。\n- 預處理陣列或狀態機保存已比對資訊，主迴圈失配時不用回到原點重比。\n- 下標與長度的換算要固定，尤其是 border、LCP、半徑與回傳起點。\n\n## 時間與空間複雜度\n\n**二分 + 雜湊**：`O(n log n)` 時間（`log n` 輪二分，每輪 `O(n)` 建集合）、`O(n)` 空間。這是 718、1062 的標準解。\n\n**後綴陣列**：\n- 倍增法建構 `O(n log n)`（或 `O(n log^2 n)` 的簡易寫法），SA-IS 是 `O(n)` 但實作很長。\n- `height` 用 Kasai 演算法 `O(n)`。\n- 配稀疏表回答任意兩後綴 LCP：`O(n log n)` 預處理、`O(1)` 查詢。\n- 空間 `O(n log n)`（稀疏表）或 `O(n)`（只要 `sa` 與 `height`）。\n\n**選型**：\n- 只要「最長重複子串／子陣列」→ 二分 + 雜湊，`O(n log n)`，程式短得多。\n- 要「不同子串個數」→ 後綴陣列的公式最直接（雜湊做不到，因為要精確去重）。\n- 要「任意兩後綴的 LCP，且查詢很多」→ 後綴陣列 + 稀疏表。\n\n**1698 不同子字串個數**在 `n <= 1e5` 時，用後綴陣列是 `O(n log n)`；用雜湊 + set 存所有子串是 `O(n^2)`，會 TLE。\n\n## 常見錯誤與邊界條件\n\n- 索引從 0 開始時 border 長度與下標要分清。\n- 雜湊建議雙模或 unsigned long long，並理解碰撞風險。\n- 字元集大時不要硬開 26。\n- 空模式或長度 1 要特判。\n\n## 常見變形\n\n- KMP 處理模式串 border。\n- Z 函式處理每個後綴和整串的 LCP。\n- 雜湊適合快速判等，AC 自動機適合多模式。\n\n## 與相似技巧的比較\n\n- **對比字串雜湊**（4.1 節）：雜湊是本節「二分 + 驗證」組合的驗證器。它靈活好寫，但有碰撞機率，且答不出「不同子串個數」這種需要精確去重的問題。\n- **對比 Z 函式 / KMP**：那些處理的是「與固定前綴」或「單一模式串」的關係；後綴結構處理的是**任意兩段之間**的關係，適用面更廣、成本也更高。\n- **對比 LCP 二維表**（2.2 節）：`O(n^2)` 空間，`n <= 3000` 才可行。後綴陣列 + 稀疏表在 `O(n log n)` 空間內做到同樣的事。\n- **對比後綴自動機（SAM）**：SAM 是 `O(n)` 建構，能回答「不同子串個數」「每個子串出現次數」等更細的問題，且支援線上增量。功能比後綴陣列更強，但概念與實作都更難。\n- **對比後綴樹**：與 SAM 等價（後綴樹是反串 SAM 的 link 樹），競賽中少直接實作。\n- **務實建議**：先用二分 + 雙雜湊解掉八成的題；真的需要「精確去重」或「大量任意 LCP 查詢」時，才投資後綴陣列。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 718 | [最長重複子陣列](https://leetcode.cn/problems//maximum-length-of-repeated-subarray/) |  |\n| 1062 | [最長重複子串](https://leetcode.cn/problems//longest-repeating-substring/) |  |\n| 1698 | [字串的不同子字串個數](https://leetcode.cn/problems//number-of-distinct-substrings-in-a-string/) |  |\n| 3135 | [透過新增或刪除結尾字元來同化字串](https://leetcode.cn/problems//equalize-strings-by-adding-or-removing-characters-at-ends/) |  |\n| 面試題 16.18 | [模式匹配](https://leetcode.cn/problems//pattern-matching-lcci/) |  |\n| 1408 | [陣列中的字串匹配](https://leetcode.cn/problems//string-matching-in-an-array/) | 1223 |\n| 3722 | [反轉後字典序最小的字串](https://leetcode.cn/problems//lexicographically-smallest-string-after-reverse/) | 1414 |\n| 2904 | [最短且字典序最小的美麗子字串](https://leetcode.cn/problems//shortest-and-lexicographically-smallest-beautiful-string/) | 1483 |\n| 3076 | [陣列中的最短非公共子字串](https://leetcode.cn/problems//shortest-uncommon-substring-in-an-array/) | 1635 |\n| 1754 | [構造字典序最大的合併字串](https://leetcode.cn/problems//largest-merge-of-two-strings/) | 1829 |\n| 1316 | [不同的迴圈子字串](https://leetcode.cn/problems//distinct-echo-substrings/) | 1837 |\n| 1163 | [按字典序排在最後的子串](https://leetcode.cn/problems//last-substring-in-lexicographical-order/) | 1864 |\n| 2564 | [子字串異或查詢](https://leetcode.cn/problems//substring-xor-queries/) | 1959 |\n| 3213 | [最小代價構造字串](https://leetcode.cn/problems//construct-string-with-minimum-cost/) | 2171 |\n| 3729 | [統計有序陣列中可被 K 整除的子陣列數量](https://leetcode.cn/problems//count-distinct-subarrays-divisible-by-k-in-sorted-array/) | 2248 |\n| 3388 | [統計陣列中的美麗分割](https://leetcode.cn/problems//count-beautiful-splits-in-an-array/) | 2365 |\n| 3504 | [子字串連線後的最長迴文串 II](https://leetcode.cn/problems//longest-palindrome-after-substring-concatenation-ii/) | 2398 |\n| 1044 | [最長重複子串](https://leetcode.cn/problems//longest-duplicate-substring/) | 2429 |\n| 1923 | [最長公共子路徑](https://leetcode.cn/problems//longest-common-subpath/) | 2661 |\n\n## 本節重點速查\n\n- **任何子字串都是某個後綴的前綴**——這個雙射把子串問題轉成後綴問題。\n- 入門首選 **二分長度 + 字串雜湊**：答案單調（越長越難），`check(L)` 把所有長度 `L` 的子串雜湊丟進 set 找重複，`O(n log n)`。\n- **因為是兩兩比對，生日悖論讓單雜湊在 `n = 1e5` 時幾乎必碰——一定要雙雜湊。\n- **後綴陣列的兩個公式要記牢：**最長重複子串 = `max(height)`**、**不同子串個數 = `Σ(n - sa[i]) − Σ height[i]`**（每個後綴貢獻所有前綴，扣掉與前一個後綴重複的部分）。\n- 任意兩後綴的 LCP = `min(height[i+1..j])`，配稀疏表可 `O(1)` 查。\n- 需要精確去重（1698）或大量 LCP 查詢時才值得投資後綴陣列。',
        },
      ],
    },
    {
      id: 20,
      title: "9. 子序列自動機",
      summary:
        "預處理每個位置後最近的字元下標，用於快速判斷子序列匹配。適用於 792、514、2014 等題。\n\n:::example 子序列自動機：nxt[i][c] = 位置 i 之後最近的字元 c 的位置\n**C++ 模板**：\n\n```cpp\n// 子序列自動機：nxt[i][c] = 位置 i 之後最近的字元 c 的位置\nvector<array<int, 26>> buildSubseqAutomaton(const string& s) {\n    int n = s.size();\n    vector<array<int, 26>> nxt(n + 1);\n    nxt[n].fill(n);\n    for (int i = n - 1; i >= 0; i--) {\n        nxt[i] = nxt[i + 1];\n        nxt[i][s[i] - 'a'] = i;\n    }\n    return nxt;\n}\n```\n:::",
      children: [
        {
          id: 21,
          title: "9.1 基礎",
          summary:
            "## 這個技術解決什麼問題\n\n反覆問「字串 `t` 是不是字串 `s` 的子序列？」——而且 `t` 有成千上萬個。\n\n判斷單一個 `t` 用雙指標就好，`O(|s| + |t|)`。但若有 `q` 個查詢，每個都從頭掃 `s`，總計 `O(q × |s|)`，`s` 被重掃無數遍。\n\n子序列自動機的想法是：**先把 `s` 預處理成一張跳轉表 `nxt`，之後比對任何 `t` 時，每個字元都能「直接跳到它下一次出現的位置」**，於是單次查詢降到 `O(|t|)`——與 `s` 的長度無關。\n\n同一張表也回答「從位置 `i` 之後，字元 `c` 第一次出現在哪」，這正是 727（最小視窗子序列）、1055（形成字串的最短路徑）的核心需求。\n\n## 辨識題型的訊號\n\n看到以下味道，就往子序列自動機想：\n\n- **要對「很多個查詢字串」反覆判斷是否為同一母串的子序列**：典型如〔匹配子序列的單詞數〕792，一個 `s` 配上一大批單詞。\n- **需要在母串上「快速找下一個某字元的位置」**：例如〔最小視窗子序列〕727、〔形成字串的最短路徑〕1055 都仰賴「下一個字元在哪」的快速跳轉。\n- **暴力是「每個查詢都從頭雙指標掃母串」**，查詢一多就把母串重掃太多遍；預處理 `nxt` 表把每次比對降到只與查詢長度有關。\n- 出現「保留母串不變、反覆拿不同 `t` 來問」的結構，就是建表攤平成本的訊號（如〔重複 K 次的最長子序列〕2014 在枚舉候選時反覆驗證）。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的「母串」是固定的、要被反覆查詢的嗎？\n2. 我預處理出的「下一步跳轉表」`nxt[i][c]` 語意是什麼？\n3. 比對失敗的判定點在哪（跳到 `n` 代表此後再無該字元）？\n\n## 核心想法與直覺\n\n本節要解決的場景是：反覆問「字串 `t` 是不是字串 `s` 的子序列？」子序列是指「按順序、可跳過某些字元」挑出來的序列，例如 `ace` 是 `abcde` 的子序列。\n\n判斷單一個 `t` 很簡單：用兩個指標掃過去就好。但若要對「成千上萬個 `t`」一一判斷，每個都從頭掃 `s`，就會把 `s` 重掃無數遍。子序列自動機的核心想法是：**先把 `s` 預處理成一張表，之後比對任何 `t` 時，每個字元都能「直接跳到下一個它出現的位置」，不必線性掃。**\n\n:::example nxt[i][c] = 從 s 的位置 i（含）起，字元 c 第一次出現的下標；沒有則為…\n具體做法是建一張 `nxt[i][c]` 表：站在 `s` 的位置 `i`，下一個字元 `c` 出現在哪裡。\n\n```cpp\n// nxt[i][c] = 從 s 的位置 i（含）起，字元 c 第一次出現的下標；沒有則為 n\n// 例：s = \"abc\"，nxt[0]['a'] = 0，nxt[1]['a'] = 3(=n，表示沒有)\n```\n:::\n\n\n\n:::example 在自動機上走：每個字元一步跳到下一次出現位置\n有了這張表，比對 `t` 時就像在自動機上走路：當前在 `s` 的位置 `pos`，要匹配 `t` 的字元 `c`，就一步跳到 `nxt[pos][c] + 1`；只要中途跳到了 `n`（沒有），就代表 `t` 不是子序列。比對一個 `t` 只要 `O(t 的長度)`，與 `s` 多長無關。\n\n```cpp\n// 在自動機上走：每個字元一步跳到下一次出現位置\nint pos = 0;\nfor (char c : t) {\n    pos = nxt[pos][c - 'a'];\n    if (pos == n) { /* 失敗：t 不是子序列 */\n    }\n    ++pos;  // 跳到匹配位置的下一格\n}\n```\n:::\n\n## 狀態／資料結構定義\n\n**`nxt[i][c]`**：從位置 `i`（含 `i`）開始，字元 `c` 第一次出現的下標；不存在則設為 `n`（哨兵，代表「沒有了」）。\n\n**維度**：`(n + 1) × SIGMA`。多開一列 `nxt[n][*] = n`，讓遞推有終止條件、查詢時不必特判越界。\n\n**建表（由後往前）**：\n\n```\nnxt[n][c] = n  對所有 c\nnxt[i][c] = (s[i] == c) ? i : nxt[i+1][c]\n```\n\n這個方向不能反——`nxt[i]` 依賴 `nxt[i+1]`。\n\n**查詢**：`pos = 0`；對 `t` 的每個字元 `c`，令 `pos = nxt[pos][c]`；若 `pos == n` 表示找不到，回傳 false；否則 `++pos` 繼續下一個字元。全部走完即為子序列。\n\n**`++pos` 不能忘**：下一個字元必須從**已匹配位置的後面**開始找，否則同一個位置會被重複使用。\n\n**`SIGMA`** 要對得上真正的字元集；小寫字母 26，含大小寫要 52 或直接開 128。\n\n## 不變量或正確性證明\n\n**建表的不變量**：`nxt[i][c]` 恆等於 `min{ j >= i : s[j] == c }`（不存在則為 `n`）。\n\n由後往前歸納：`i = n` 時區間為空，定義為 `n`，成立。假設 `nxt[i+1]` 正確；對位置 `i`，若 `s[i] == c` 則最小的 `j` 就是 `i`；否則 `j` 必定 `> i`，即 `nxt[i+1][c]`。兩種情形都符合定義。\n\n**查詢的貪心正確性**（本節真正的重點）：貪心地把 `t` 的每個字元匹配到「最早可用的位置」，是最優的。\n\n用交換論證。設 `t` 是 `s` 的子序列，存在某組匹配位置 `p_1 < p_2 < … < p_m`。貪心給出的是 `g_1 < g_2 < … < g_m`，其中 `g_1` 是 `s` 中第一個等於 `t[0]` 的位置，所以 `g_1 <= p_1`。歸納假設 `g_k <= p_k`：則從 `g_k + 1` 開始找 `t[k]` 的最早位置，不會晚於從 `p_k + 1` 開始找到的 `p_{k+1}`（搜尋起點更早、目標相同），故 `g_{k+1} <= p_{k+1}`。\n\n因此貪心匹配若失敗（某步 `nxt` 回傳 `n`），任何匹配方式都會失敗——**貪心找不到就是真的不存在**。這是「最早匹配不吃虧」的標準論證。\n\n**`++pos` 的必要性**：不加的話，若 `t` 有連續相同字元（如 `\"aa\"`），第二個 `a` 會匹配到同一個位置，把「子序列」錯判成「只要字元存在就行」。\n\n## 逐步演算法\n\n:::example 建表：nxt[i] 繼承 nxt[i+1]，再更新 s[i] 為 i\n**1. 從右往左建 `nxt` 表。** 倒著掃 `s`：位置 `i` 的表先複製位置 `i + 1` 的，再把 `s[i]` 這個字元的下一次出現改成 `i`。\n\n```cpp\n// 建表：nxt[i] 繼承 nxt[i+1]，再更新 s[i] 為 i\nint n = s.size();\nvector<array<int, 26>> nxt(n + 1);\nnxt[n].fill(n);  // 邊界：位置 n 之後什麼都沒有\nfor (int i = n - 1; i >= 0; i--) {\n    nxt[i] = nxt[i + 1];\n    nxt[i][s[i] - 'a'] = i;  // 字元 s[i] 最近一次出現就在 i\n}\n```\n:::\n\n\n\n:::example 判斷 t 是否為 s 的子序列\n**2. 用表判斷子序列。** 從 `pos = 0` 開始，逐字元跳；跳到 `n` 即失敗。\n\n```cpp\n// 判斷 t 是否為 s 的子序列\nbool isSub(const string& t, vector<array<int, 26>>& nxt, int n) {\n    int pos = 0;\n    for (char c : t) {\n        pos = nxt[pos][c - 'a'];\n        if (pos == n) {\n            return false;\n        }\n        ++pos;\n    }\n    return true;\n}\n```\n:::\n\n\n\n**3. 批次查詢。** 一次建表，之後每個 `t` 都重複用同一張表，`m` 個查詢合計 `O(n × 字元集 + 所有 t 長度和)`。\n\n**4. 邊界與複雜度。** 建表時間與空間都是 `O(n × 字元集大小)`，字元集大時較吃記憶體（可改用每個字元的位置陣列 + 二分代替）。比對單一 `t` 是 `O(|t|)`。注意 `pos` 跳到 `n` 後務必立刻判失敗，避免越界。\n\n## C++17 模板\n\n以 792. 匹配子序列的單詞數 為例。\n\n**完整題目**：給字串 `s` 與字串陣列 `words`，回傳 `words` 中是 `s` 子序列的字串數目。\n\n**線索**：`words` 很多而 `s` 固定 ⟹ 預處理 `s` 一次，每個查詢降到 `O(|word|)`。\n\n```cpp\n// 792. 匹配子序列的單詞數：子序列自動機\nclass Solution {\npublic:\n    int numMatchingSubseq(string s, vector<string>& words) {\n        const int n = s.size(), SIGMA = 26;\n\n        // nxt[i][c] = 從位置 i（含）起，字元 c 第一次出現的下標；n 表示不存在\n        vector nxt(n + 1, vector<int>(SIGMA, n));\n        for (int i = n - 1; i >= 0; --i) {   // 必須由後往前：nxt[i] 依賴 nxt[i+1]\n            nxt[i] = nxt[i + 1];\n            nxt[i][s[i] - 'a'] = i;\n        }\n\n        auto isSubsequence = [&](const string& t) {\n            int pos = 0;\n            for (char c : t) {\n                pos = nxt[pos][c - 'a'];\n                if (pos == n) return false;  // 後面沒有這個字元了\n                ++pos;                       // 下一個字元要從這個位置之後找\n            }\n            return true;\n        };\n\n        int ans = 0;\n        for (const string& w : words) ans += isSubsequence(w);\n        return ans;\n    }\n};\n```\n\n## 時間與空間複雜度\n\n**建表 `O(n × SIGMA)`** 時間與空間。`n = 5e4`、`SIGMA = 26` 時是 `1.3e6` 個 `int`，約 5 MB。\n\n**每次查詢 `O(|t|)`**——**與 `n` 無關**，這是本技巧的全部價值。\n\n**總計 `O(n × SIGMA + Σ|t_i|)`**。對比每次雙指標的 `O(q × n)`：792 在 `n = 5e4`、`q = 5000`、`|t| <= 50` 時，前者約 `1.3e6 + 2.5e5`，後者是 `2.5e8`——差三個數量級。\n\n**空間是主要限制**：`SIGMA` 大時 `n × SIGMA` 會爆。`SIGMA = 128`、`n = 1e5` 就是 `1.28e7` 個 `int`（50 MB），多半 MLE。此時改用**分桶法**：對每個字元存它出現的所有位置（`vector<int> pos[SIGMA]`），查詢時用二分找「大於當前位置的第一個」，空間降到 `O(n)`，查詢變成 `O(|t| log n)`。\n\n**何時不值得**：只查一兩次時，雙指標的 `O(n + |t|)` 更省——建表本身就要 `O(n × SIGMA)`。\n\n## 常見錯誤與邊界條件\n\n- **忘記 `++pos`**。連續相同字元（`t = \"aa\"`）會匹配到同一個位置，把非子序列誤判成子序列。這是本節最常見也最隱蔽的錯。\n- **建表方向反了**。`nxt[i]` 依賴 `nxt[i+1]`，必須**由後往前**。寫成由前往後會讀到全是初值的列。\n- **沒有多開 `nxt[n]` 這一列**。少了哨兵列，遞推的起點不存在，且查詢時要多寫越界判斷。\n- **哨兵值用 `-1`**。用 `n` 當「不存在」比較順——`pos == n` 一個條件就判完，而且 `++pos` 之後仍在合法範圍內不會出事。\n- **`SIGMA` 與字元集不符**。含大寫或數字時 `c - 'a'` 會產生負下標。\n- **空的 `t`**。空字串是任何字串的子序列，迴圈不執行直接回 true，行為正確；但若題目另有定義要特判。\n- **空的 `s`**。`n = 0` 時 `nxt` 只有哨兵列，任何非空 `t` 都會回 false，正確。\n- **記憶體超限卻硬開二維表**。`SIGMA` 大就改用分桶 + 二分。\n- 驗證方式：對拍一份雙指標的暴力，特別測含連續重複字元的 `t`。\n\n## 與相似技巧的比較\n\n- **對比雙指標**：單次查詢雙指標是 `O(n + |t|)` 且 `O(1)` 空間，更省。**查詢次數多時才值得建表**——這是唯一的選型依據。\n- **對比分桶 + 二分**：對每個字元存出現位置，查詢時二分找「下一個」。空間 `O(n)`（不乘 `SIGMA`）、查詢 `O(|t| log n)`。`SIGMA` 大或記憶體吃緊時的標準替代。\n- **對比 AC 自動機**：那是「多模式串**子串**匹配」；本節是「多查詢**子序列**判定」。子串連續、子序列可跳——問題本質不同，別套錯。\n- **對比 KMP**：同樣是子串問題，不適用於子序列。\n- **對比 DP**：「`t` 是不是 `s` 的子序列」也能寫成 `O(|s| × |t|)` 的 DP，但那是殺雞用牛刀。真正需要 DP 的是「有幾種方式把 `t` 匹配成 `s` 的子序列」（115 不同的子序列）——那才是計數問題。\n- **同一張表的其他用途**：727（最小視窗子序列）、1055（形成字串的最短路徑）都靠「從位置 `i` 之後，字元 `c` 在哪」這個查詢，`nxt` 表直接可用。\n\n## 代表例題\n\n**題號**：514、727、792、1055、2014\n\n重點是重用已比對資訊；練習把 border、LCP、hash 或自動機狀態寫成不變式。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 514 | [自由之路](https://leetcode.cn/problems/freedom-trail/)（Premium） |  |\n| 727 | [最小視窗子序列](https://leetcode.cn/problems/minimum-window-subsequence/)（Premium） |  |\n| 792 | [匹配子序列的單詞數](https://leetcode.cn/problems/number-of-matching-subsequences/) |  |\n| 1055 | [形成字串的最短路徑](https://leetcode.cn/problems/shortest-way-to-form-string/)（Premium） |  |\n| 2014 | [重複 K 次的最長子序列](https://leetcode.cn/problems/longest-subsequence-repeated-k-times/)（Premium） | 2558 |\n\n## 本節重點速查\n\n- `nxt[i][c]` = 從位置 `i`（含）起字元 `c` 第一次出現的下標，不存在填 `n`。\n- **由後往前建表**（`nxt[i]` 依賴 `nxt[i+1]`），多開一列 `nxt[n][*] = n` 當哨兵。\n- 查詢時 `pos = nxt[pos][c]`，`== n` 就失敗，否則 **`++pos`**——**忘記 `++pos` 會讓 `\"aa\"` 匹配到同一個位置**，是最隱蔽的錯。\n- 貪心「匹配到最早可用位置」的最優性靠交換論證：搜尋起點更早，找到的位置不會更晚，所以貪心失敗就是真的不存在。\n- 建表 `O(n × SIGMA)`、**每次查詢 `O(|t|)` 與 `n` 無關**。\n- `SIGMA` 大或記憶體吃緊就改用「每字元存出現位置 + 二分」，空間 `O(n)`、查詢多一個 `log`。\n- 只查一兩次時雙指標更省。",
        },
      ],
    },
    {
      id: 22,
      title: "10. 其他",
      summary:
        "字串進階雜項：LCP／border 與週期性、重複子串計數、帶修改或線上詢問等。\n\n**路線**：能默寫 KMP／雙模雜湊後，再依題選 Manacher、後綴結構或自動機；每題記「為何暴力 TLE」。",
      children: [
        {
          id: 23,
          title: "10.1 進階",
          summary:
            "## 這個技術解決什麼問題\n\n收錄幾道把前面各節的工具**組合起來**的題，共通點是「重用已比對的資訊」，而重用的形式各不相同：\n\n- **466 統計重複個數**：`s1` 重複 `n1` 次的字串裡，`s2` 重複 `n2` 次的字串能被當成子序列取出幾次。直接模擬是 `O(n1 × |s1|)`，`n1` 到 `1e6` 時不可行。關鍵是**找循環節**——掃過每一份 `s1` 之後，「目前匹配到 `s2` 的第幾個字元」這個狀態只有 `|s2|` 種，必定在有限步內重複，找到循環後用除法一次跳過中間。\n- **3485 刪除元素後 K 個字串的最長公共前綴**：對每個刪除位置求「其餘字串的最長公共前綴」，用 trie 上的「經過計數」避免逐一重算。\n- **3491 電話號碼前綴**：判斷是否有任一號碼是另一號碼的前綴，trie 插入時順便檢查即可。\n\n這一節的價值不在新工具，而在**看出該把哪個不變式寫下來**。\n\n## 辨識題型的訊號\n\n看到以下味道，就往本節想：\n\n- **某個字串被重複串接到極大次數，要問能匹配/包含多少次**：例如〔統計重複個數〕466——狀態必然循環，偵測循環節後用算術跳算。\n- **要查「一群字串的共同前綴」或「誰是誰的前綴」**：例如〔刪除元素後 K 個字串的最長公共字首〕3485、〔電話號碼字首〕3491，把字串全插進 trie，用 `pass` 計數沿主幹下走。\n- **暴力會反覆重算同一段**（重複模擬巨量重複、或逐對互比前綴），這正是要壓進循環表或前綴樹的訊號。\n- 題目「不像」任何標準字串模板時，先回到「哪些量會變、哪些量要被快速查、該存哪種結構」三問。\n\n確認自己有沒有套對，問三個問題：\n\n1. 輸入裡有沒有「會重複出現、可壓成循環」的結構？它的狀態怎麼定義？\n2. 我需要反覆查詢的量是不是「前綴 / 共同開頭」？適合放進 trie 嗎？\n3. 我能不能說清楚答案是從哪個狀態或節點（如 `pass`、循環節長度）讀出來的？\n\n## 核心想法與直覺\n\n本節收的是不套單一模板、需要「看題目本身設計狀態」的進階字串題。它們共同的精神是兩句話：**(A) 發現輸入裡藏著「重複出現的結構」就把它壓成一個循環來算；(B) 要反覆問「一群字串的共同前綴」就把它們攤進前綴結構（字典樹）裡查。**\n\n:::example 重複結構：記錄「掃完一輪後停在另一串的哪個 index」\n先看 (A)。有些題目把一個短字串重複接很多很多次（例如把 `\"abc\"` 接 `10^6` 次），直接模擬會爆。觀察到：在一個固定字串裡掃另一個字串，**狀態（掃到哪個位置）一定會開始循環**。一旦記錄「每掃完一輪 base 串、目前停在另一串的哪個位置」，發現位置重複，就找到了循環節，剩下的次數用乘法一次算完，不必真的逐字模擬。\n\n```cpp\n// 重複結構：記錄「掃完一輪後停在另一串的哪個 index」\n// 同一個 index 再次出現 → 找到循環節，用算術跳過大量重複\n```\n:::\n\n\n\n:::example trie 節點：pass = 有幾個字串經過此節點（= 共用此前綴的字串數）\n再看 (B)。要問「這 K 個字串共用了多長的開頭」「某號碼是不是另一號碼的前綴」，逐對互比很慢。把所有字串插進字典樹（trie），共同前綴自然合併成同一條路徑：某節點被 `pass` 個字串經過，就代表有這麼多字串共用「走到此節點」這段前綴。\n\n```cpp\n// trie 節點：pass = 有幾個字串經過此節點（= 共用此前綴的字串數）\nstruct Node {\n    Node* child[26] = {};\n    int pass = 0;\n    bool isEnd = false;\n};\n```\n:::\n\n\n\n兩類題的共通做法都是「先想清楚什麼量會重複、需要被快速查詢，再選對的結構存它」，而不是硬背上層分類的通用招式。\n\n## 狀態／資料結構定義\n\n**循環節偵測（466）**：\n\n- 狀態是「掃完若干份 `s1` 之後，`s2` 匹配到第幾個字元」，記為 `j ∈ [0, |s2|)`。\n- `seen[j]` 記錄「第一次以狀態 `j` 開始某一份 `s1`」是在第幾份、當時已完成幾個 `s2`。\n- 偵測到 `j` 重複出現時，就得到一個循環：`(份數增量, 完成的 s2 增量)`。\n- 用除法一次跳過 `⌊剩餘份數 / 份數增量⌋` 個循環，再暴力跑完餘數。\n\n**trie 上的前綴計數（3485、3491）**：\n\n- `pass[u]`：經過節點 `u` 的字串數。\n- 「刪掉某個字串後，其餘的最長公共前綴」= 沿著該字串的路徑往下走，找最後一個 `pass[u] >= k`（扣除自己後仍有 `k` 個）的深度。\n\n**共通模式**：先寫下「什麼量在重複出現時代表狀態相同」，這個量就是循環節偵測的鍵，也是不變式的載體。\n\n## 不變量或正確性證明\n\n**循環節存在性（鴿巢原理）**：狀態 `j` 只有 `|s2|` 種取值。掃過 `|s2| + 1` 份 `s1` 之後，必有兩份的起始狀態相同——鴿巢原理保證循環一定在 `O(|s2|)` 份之內出現。\n\n**循環的正確性**：若第 `a` 份與第 `b` 份（`a < b`）的起始狀態同為 `j`，則從第 `a` 份到第 `b-1` 份這 `b - a` 份 `s1` 所產生的行為，會在之後**完全重複**——因為後續行為只由「起始狀態 + `s1` 的內容」決定，兩者都相同。因此可以把「剩下的份數」除以 `b - a`，一次跳過所有完整循環，只暴力跑餘數部分。\n\n這裡的關鍵是**狀態必須完整**：若「已完成幾個 `s2`」也影響後續行為，它就必須進狀態；幸好它不影響（只是累加計數），所以狀態只有 `j` 一維。**判斷什麼該進狀態、什麼只是累加量，是這類題唯一的難點。**\n\n**trie 前綴計數的正確性（3485）**：從根沿某字串的路徑往下，`pass[u]` 隨深度單調不增（越長的前綴，符合的字串越少）。所以「`pass[u] >= k`」是 `T…T F…F` 的形狀，最後一個為真的深度就是答案——這也解釋了為什麼可以邊走邊判、不必回頭。\n\n**3491 的正確性**：「`a` 是 `b` 的前綴」⟺ 在 trie 中 `a` 的終點是 `b` 路徑上的一個節點。插入時只要檢查兩件事——路過的節點中有沒有已標記為終點的（別人是我的前綴），以及自己的終點是否已有子節點（我是別人的前綴）。兩者涵蓋所有情形。\n\n## 逐步演算法\n\n**1. 先寫暴力，找出被重算的東西。** 進階題沒有罐頭模板：先把暴力寫出來，問自己「哪一段計算被反覆做？」那段就是要快取或壓成循環/前綴結構的對象。\n\n:::example 偵測循環：seen[state] = 第一次見到此狀態時的輪次\n**2.（重複結構）用「狀態 → 出現時機」表偵測循環。** 記錄每個狀態第一次出現在第幾輪，重複出現即循環節長度。\n\n```cpp\n// 偵測循環：seen[state] = 第一次見到此狀態時的輪次\nunordered_map<int, int> seen;\nfor (int round = 0; round < total; ++round) {\n    if (seen.count(state)) { /* round - seen[state] 即循環長度，可跳算 */\n    }\n    seen[state] = round;\n    state = advance(state);  // 掃過一輪 base 串後的新狀態\n}\n```\n:::\n\n\n\n:::example 插入字串，沿路 pass++\n**3.（共同前綴）插入時累加 pass。** 之後查「共用此前綴的字串數」`O(1)`。\n\n```cpp\n// 插入字串，沿路 pass++\nvoid insert(Node* root, const string& s) {\n    Node* cur = root;\n    for (char c : s) {\n        int i = c - 'a';\n        if (!cur->child[i]) {\n            cur->child[i] = new Node();\n        }\n        cur = cur->child[i];\n        cur->pass++;\n    }\n    cur->isEnd = true;\n}\n```\n:::\n\n\n\n:::example 求至少 K 個字串共用的最長前綴長度\n**4.（K 串最長公共前綴）沿主幹下走。** 從根往下，只要當前節點的 `pass >= K`（有至少 K 個字串都還共用到這裡），就可以再延一個字元，直到 `pass < K`。\n\n```cpp\n// 求至少 K 個字串共用的最長前綴長度\nint lcpOfK(Node* root, int K) {\n    Node* cur = root;\n    int len = 0;\n    while (true) {\n        Node* nxt = nullptr;\n        for (int i = 0; i < 26; ++i) {\n            if (cur->child[i] && cur->child[i]->pass >= K) {\n                nxt = cur->child[i];\n                break;\n            }\n        }\n        if (!nxt) {\n            break;\n        }\n        cur = nxt;\n        ++len;\n    }\n    return len;\n}\n```\n:::\n\n\n\n**5. 邊界與複雜度。** 循環法把 `O(超大次數)` 降成 `O(狀態數)`；trie 法建表 `O(總字元數)`、單次前綴查詢 `O(字串長度)`。若連狀態語意都說不清，就退回暴力，重新找「可維護的重複部分」。\n\n## C++17 模板\n\n以 466. 統計重複個數 為例——最能代表本節「找循環節」的精神。\n\n**完整題目**：定義 `str = [s, n]` 為 `s` 重複 `n` 次。給 `s1, n1, s2, n2`，求最大的 `m`，使得 `[[s2, n2], m]` 可以由 `[s1, n1]` 中刪除若干字元得到（即當成子序列取出 `m × n2` 個 `s2`）。\n\n```cpp\n// 466. 統計重複個數：以「s2 匹配到第幾個字元」為狀態找循環節\nclass Solution {\npublic:\n    int getMaxRepetitions(string s1, int n1, string s2, int n2) {\n        if (n1 == 0) return 0;\n        const int m = s2.size();\n\n        // seen[j] = 首次以狀態 j 開始某份 s1 時的 (已用份數, 已完成的 s2 數)\n        vector<pair<int, int>> seen(m, {-1, -1});\n\n        int j = 0, done = 0;\n        for (int used = 1; used <= n1; ++used) {\n            int start = j;\n            for (char c : s1) {                 // 掃完一整份 s1\n                if (c == s2[j]) {\n                    if (++j == m) { j = 0; ++done; }\n                }\n            }\n            if (seen[start].first == -1) {      // 第一次見到這個起始狀態\n                seen[start] = {used, done};\n                continue;\n            }\n            // 找到循環：每 cycleLen 份 s1 完成 cycleDone 個 s2\n            auto [prevUsed, prevDone] = seen[start];\n            int cycleLen = used - prevUsed, cycleDone = done - prevDone;\n            int cycles = (n1 - prevUsed) / cycleLen;\n            done = prevDone + cycles * cycleDone;\n            // 跳過整段循環後，暴力跑完餘下的份數\n            for (int rest = prevUsed + cycles * cycleLen; rest < n1; ++rest)\n                for (char c : s1)\n                    if (c == s2[j] && ++j == m) { j = 0; ++done; }\n            break;\n        }\n        return done / n2;\n    }\n};\n```\n\n## 時間與空間複雜度\n\n**466**：鴿巢原理保證循環在 `O(|s2|)` 份 `s1` 內出現，之後用除法一次跳過。時間 `O(|s2| × |s1|)`、空間 `O(|s2|)`——**與 `n1` 無關**，這正是它能處理 `n1 = 1e6` 的原因。\n\n直接模擬是 `O(n1 × |s1|)`，`n1 = 1e6`、`|s1| = 100` 時是 `1e8`，會超時。\n\n**3485**：建 trie `O(Σ|s_i|)`，每個刪除位置沿路徑走一次也是 `O(|s_i|)`，總計 `O(Σ|s_i|)`。空間與 trie 同量級。\n\n**3491**：插入時順便檢查，`O(Σ|s_i|)` 時間，空間 `O(Σ|s_i| × SIGMA)`（數字用 `SIGMA = 10`）。\n\n**循環節技巧的一般成本**：狀態數 `S` 時，偵測循環最多跑 `S + 1` 步，每步的成本視轉移而定。只要 `S` 不大（本節是 `|s2|`），就能把「跑 `N` 次」壓成「跑 `O(S)` 次 + 一次除法」，`N` 再大也不怕。\n\n## 常見錯誤與邊界條件\n\n- **狀態定義不完整**。若有某個量會影響後續行為卻沒進狀態，偵測到的「循環」是假的，答案會錯。動手前先問「後續行為完全由這些量決定嗎」。\n- **循環偵測後餘數沒跑完**。跳過整段循環之後，還剩 `(n1 - prevUsed) % cycleLen` 份要暴力跑。漏掉會少算。\n- **`n1` 太小、循環還沒出現就跑完了**。迴圈要能自然結束並回傳當時的 `done`，不能假設一定會找到循環。\n- **`done / n2` 的整除**。題目要的是完整的 `[s2, n2]` 有幾組，最後要除以 `n2` 並取整。\n- **`n1 == 0` 或 `s2` 含 `s1` 沒有的字元**。前者直接回 0；後者 `done` 恆為 0，程式自然正確，但值得先想過。\n- **3485 的 `pass` 要扣掉自己**。求「刪掉某字串後其餘的公共前綴」時，沿路徑判斷的是 `pass[u] - 1 >= k`（或視題目定義）。\n- **3491 的兩個方向都要檢查**。只檢查「別人是我的前綴」會漏掉「我是別人的前綴」。先全部插入再統一檢查，或插入時雙向檢查。\n- 驗證方式：466 用小 `n1` 對拍暴力模擬；trie 題用小資料手算。\n\n## 與相似技巧的比較\n\n- **對比純模擬**：本節每一題都有一個「暴力模擬」的正解，只是太慢。加速的方式不是換演算法，而是**找出可重用的結構**——循環節、trie 的共享前綴。\n- **對比快速冪 / 矩陣快速冪**：那也是「重複相同操作 `N` 次」的加速，靠的是結合律；本節靠的是狀態有限（鴿巢）。兩者都在回答「`N` 很大時怎麼辦」，適用條件不同。\n- **對比子序列自動機**（9.1 節）：466 的內層「在 `s1` 裡找 `s2` 的字元」也可以用 `nxt` 表加速，但因為 `s1` 短、循環節才是瓶頸，直接掃更簡單。\n- **對比 KMP / Z**：本節的 466 是**子序列**匹配（可跳過），不是子串匹配，KMP 完全用不上。這個區分值得再強調一次。\n- **對比字典樹**（第 6 節）：3485、3491 就是 trie 的直接應用，只是把 `pass` 計數用在稍微轉個彎的問題上。\n\n## 代表例題\n\n**題號**：466、3485、3491\n\n重點是重用已比對資訊；練習把 border、LCP、hash 或自動機狀態寫成不變式。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 466 | [統計重複個數](https://leetcode.cn/problems/count-the-repetitions/) |  |\n| 3485 | [刪除元素後 K 個字串的最長公共字首](https://leetcode.cn/problems/longest-common-prefix-of-k-strings-after-removal/)（Premium） | 2290 |\n| 3491 | [電話號碼字首](https://leetcode.cn/problems/phone-number-prefix/)（Premium） |  |\n\n## 本節重點速查\n\n- 本節是各節工具的組合題，共通心法是**找出可重用的結構**。\n- **466 的關鍵是循環節**：以「`s2` 匹配到第幾個字元」為狀態（只有 `|s2|` 種），鴿巢原理保證掃過 `O(|s2|)` 份 `s1` 後必定重複；\n- 找到循環後用除法一次跳過，再暴力跑完餘數，時間 **`O(|s2| × |s1|)` 與 `n1` 無關**。\n- 最大的坑是**狀態定義不完整**——若有量會影響後續行為卻沒進狀態，偵測到的循環是假的；\n- 另一個坑是跳過循環後忘了跑餘數。\n- **3485、3491 是 trie 的 `pass` 計數應用**：沿路徑走時 `pass[u]` 隨深度單調不增，所以「`pass >= k`」是 `T…T F…F`，邊走邊判即可；\n- 3491 要**雙向**檢查（我是別人的前綴、別人是我的前綴）。\n- 注意 466 是**子序列**匹配，KMP 用不上。",
        },
      ],
    },
    {
      id: 9014,
      title: "11. 常見面試模式",
      summary:
        "字串題大多可歸入少數幾種掃描或預處理框架。先從題目特徵對照下表，再決定要用雙指針、視窗、回文擴展還是前綴函數/字典樹。\n\n| 模式 | 訊號 | 核心做法 | 代表題 |\n| --- | --- | --- | --- |\n| 雙指針 | 反轉、判迴文、原地壓縮、合併兩段 | 左右(或快慢)指針相向或同向移動，`O(n)` 一次掃描 | [驗證迴文串](https://leetcode.cn/problems/valid-palindrome) |\n| 字頻/變位詞 | 判斷組成相同、找所有變位詞 | 用長度 26 或雜湊表記字元計數，比較計數陣列是否相等 | [找到字串中所有字母異位詞](https://leetcode.cn/problems/find-all-anagrams-in-a-string) |\n| 滑動視窗子字串 | 最長/最短滿足某條件的子字串 | 右指針擴張、左指針在違規時收縮，並維護視窗內統計 | [無重複字元的最長子串](https://leetcode.cn/problems/longest-substring-without-repeating-characters) |\n| 中心擴展回文 | 找最長迴文子串或計數迴文 | 枚舉奇偶中心向兩側擴展，`O(n^2)`；需更快用 Manacher | [最長迴文子串](https://leetcode.cn/problems/longest-palindromic-substring) |\n| KMP/前綴函數 | 子串匹配、找週期、最短補全 | 預處理前綴函數 $\\pi$，匹配時利用失配跳轉達到 `O(n + m)` | [找出字串中第一個匹配項的下標](https://leetcode.cn/problems/find-the-index-of-the-first-occurrence-in-a-string) |\n| 字典樹 | 大量字串前綴查詢、自動補全 | 以字元為邊建樹，插入與查詢均為 `O(L)`，`L` 為字串長度 | [實現 Trie（前綴樹）](https://leetcode.cn/problems/implement-trie-prefix-tree) |\n\n**小提醒**\n- 視窗題先確定「答案隨視窗擴張單調」才適用雙指針收縮，否則需改用其他結構。\n- 計數比較可用「視窗內違規字元數」做 `O(1)` 判定，避免每步重掃整個計數陣列。\n- 處理大小寫與非字母字元時，先想清楚是否需要在比較前正規化。",
    },
  ],
} as TutorialData.Root;
