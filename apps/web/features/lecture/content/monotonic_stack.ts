import type { TutorialData } from "@/types";

export const monotonicStack = {
  id: 1,
  title: "單調堆疊（矩形面積/貢獻法/最小字典序）",
  src: "https://leetcode.cn/circle/discuss/9oZFK9/",
  last_update: "2026-02-24T14:03:29.762933+00:00",
  summary:
    "![單調堆疊題單](https://pic.leetcode.cn/1711714526-rMdiIh-t3-c.png)\n\n> *他向遠方望去，無法看到高山背後的矮山，只看到一座座更高的山峰*\n\n**前置知識**：建議先完成 [資料結構題單](https://leetcode.cn/circle/discuss/mOr1u6/) 中的「列舉右，維護左」及「堆疊」的基礎題\n\n**本題單涵蓋**：\n- 單調堆疊基礎：查詢左/右側更大/更小元素\n- 矩形面積：柱狀圖、最大矩形\n- 貢獻法：計算每個元素的貢獻\n- 最小字典序：構造問題\n\n:::example 單調堆疊：求每個元素的下一個更大元素\n**C++ 單調堆疊模板 (下一個更大元素)**：\n\n```cpp\n// 單調堆疊：求每個元素的下一個更大元素\nvector<int> nextGreaterElement(vector<int>& nums) {\n    int n = nums.size();\n    vector<int> res(n, -1);\n    stack<int> st;\n    for (int i = 0; i < n; ++i) {\n        while (!st.empty() && nums[st.top()] < nums[i]) {\n            res[st.top()] = nums[i];  // 堆疊頂的下一個更大元素就是 nums[i]\n            st.pop();\n        }\n        st.push(i);\n    }\n    return res;\n}\n```\n:::\n",
  children: [
    {
      id: 2,
      title: "1. 基礎",
      summary:
        "## 這個技術解決什麼問題\n\n對陣列中的每個元素，找出「左邊或右邊第一個比它大（或比它小）的元素」。\n\n暴力做法是對每個位置往外掃到碰上邊界為止，`O(n^2)`。單調堆疊之所以能壓到 `O(n)`，是因為它注意到：**掃描過程中，被新元素蓋過的舊元素永遠不會再是任何人的答案**，可以立刻丟掉。丟掉的動作就是彈出，而彈出的那一刻正好就是結算它答案的時機。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就往單調堆疊想：\n\n- **問「下一個 / 上一個更大或更小的元素」。** 像 496、503、739 幾乎是模板題；901（股票價格跨度）問的是「往前連續多少天價格不超過今天」，本質也是找左邊第一個更大元素。\n- **暴力是對每個元素往左或往右掃一圈找邊界（`O(n^2)`），而你只關心「最近的那一個」。** 「最近」正是堆疊能省下重複比較的關鍵。\n- **元素一旦被某個新元素「蓋過」就不再有用。** 例如 853（車隊）裡，前車若比後車慢，後車永遠追不過去，可被一次結算掉——這種「被蓋過就出局」的結構天生適合堆疊。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要找的是「更大」還是「更小」？對應堆疊該遞增還是遞減？\n2. 堆疊裡存的是下標還是值？彈出時我要算的是距離、寬度，還是值本身？\n3. 掃完後還留在堆疊裡的元素代表什麼（通常是「那一側沒有符合條件的邊界」）？\n\n## 核心想法與直覺\n\n**延伸講解**：[單調堆疊【基礎演算法精講 26】](https://www.bilibili.com/video/BV1VN411J7S7/)\n\n本節要建立「單調堆疊（monotonic stack）」最基礎的直覺。它解決的核心問題只有一句話：**對陣列中的每個元素，快速找到「左邊或右邊第一個比它大（或比它小）的元素」。**\n\n先看暴力做法：要找「每日溫度」（739 題）中，每一天之後第幾天會更暖，你可以對每一天往右掃，遇到更高溫就停。這樣每個元素最壞要掃整個陣列，總共 `O(n^2)`，太慢。\n\n:::example 單調遞減堆疊：找「右邊第一個更大元素」\n單調堆疊的想法是：用一個堆疊（stack）暫存「還沒找到答案的元素」，並讓堆疊裡的值維持單調（例如由底到頂遞減）。當新元素來了、它比堆頂大，就代表堆頂「終於等到右邊第一個更大的元素」了，可以立刻結算並彈出。\n\n```cpp\n// 單調遞減堆疊：找「右邊第一個更大元素」\nstack<int> st;  // 存「下標」，對應的值由底到頂遞減\nfor (int i = 0; i < n; ++i) {\n    while (!st.empty() && nums[i] > nums[st.top()]) {\n        int j = st.top();\n        st.pop();        // nums[i] 就是 nums[j] 右邊第一個更大值\n        ans[j] = i - j;  // 結算 j 的答案\n    }\n    st.push(i);  // i 還沒找到答案，先存著\n}\n```\n:::\n\n\n\n關鍵在於：**每個元素只會被 push 一次、pop 一次**，所以整體是 `O(n)`，遠快過暴力的 `O(n^2)`。堆疊存的是「下標」而非「值」，這樣才能順手算出距離或寬度。\n\n## 狀態／資料結構定義\n\n`st`：一個存**下標**的堆疊，對應值由底到頂維持單調。要找「更大」就維持遞減，要找「更小」就維持遞增。\n\n存下標而非值，是為了在彈出時還能算距離（`i - j`）或寬度，並把答案回填到原位置。\n\n`ans`：答案陣列，預設填「不存在」的哨兵值（通常是 `-1` 或 `0`）。掃描結束後仍留在堆疊裡的下標，就是那一側找不到符合條件邊界的元素，它們維持預設值。\n\n## 不變量或正確性證明\n\n**堆疊不變量**：任一時刻，堆疊由底到頂的對應值嚴格遞減（求下一個更大元素時），而且堆疊裡的下標由底到頂遞增。\n\n**「還沒結算」不變量**：堆疊裡的元素恰好是「已掃描過、但右邊還沒出現更大值」的那些元素。\n\n**正確性**：當 `nums[i]` 彈出堆頂 `j` 時，`nums[i] > nums[j]`，所以 `i` 是 `j` 右邊某個更大的位置。它是不是**第一個**？是的——若存在 `j < m < i` 且 `nums[m] > nums[j]`，那麼在處理 `m` 時 `j` 就會被彈出，不可能活到 `i`。因此 `j` 仍在堆疊裡這件事本身，就證明了 `(j, i)` 之間沒有比 `nums[j]` 大的元素。\n\n**遞減性為何成立**：`i` 入堆疊前已把所有 `nums[·] < nums[i]` 的堆頂彈光，剩下的堆頂必 `>= nums[i]`，單調性得以保持。\n\n**均攤 `O(n)`**：每個下標最多入堆疊一次、出堆疊一次，因此 while 迴圈的總執行次數上界是 n，而不是每輪 `O(n)`。\n\n## 逐步演算法\n\n:::example 狀態表示：堆疊存下標，值維持遞減（為了找更大元素）\n**1. 決定堆疊存什麼、維持哪種單調性。** 一般存下標；要找「更大」就維持遞減堆疊，要找「更小」就維持遞增堆疊。\n\n```cpp\n// 狀態表示：堆疊存下標，值維持遞減（為了找更大元素）\nstack<int> st;           // st 由底到頂：對應值遞減\nvector<int> ans(n, -1);  // 預設找不到為 -1\n```\n:::\n\n\n\n:::example 更新流程：破壞單調性的堆頂一律彈出並結算\n**2. 掃描並彈出。** 每讀到一個新元素，就把所有「被它破壞單調性」的堆頂彈出，彈出的瞬間結算它們的答案。\n\n```cpp\n// 更新流程：破壞單調性的堆頂一律彈出並結算\nwhile (!st.empty() && nums[i] > nums[st.top()]) {\n    ans[st.top()] = nums[i];  // 右邊第一個更大值\n    st.pop();\n}\nst.push(i);\n```\n:::\n\n\n\n:::example 環形處理：掃兩遍，用取餘數模擬繞回開頭\n**3. 處理「環形」與收尾。** 像 503（下一個更大元素 II）是環形陣列，技巧是把陣列掃兩遍（用 `i % n` 取下標）。掃完後仍留在堆疊裡的元素，表示右邊沒有更大值，維持預設答案。\n\n```cpp\n// 環形處理：掃兩遍，用取餘數模擬繞回開頭\nfor (int i = 0; i < 2 * n; ++i) {\n    int x = nums[i % n];\n    while (!st.empty() && x > nums[st.top()]) {\n        ans[st.top()] = x;\n        st.pop();\n    }\n    if (i < n) {\n        st.push(i);  // 第二遍只結算、不再 push\n    }\n}\n```\n:::\n\n\n\n**4. 複雜度。** 時間 `O(n)`（每元素進出各一次），空間 `O(n)`（堆疊最壞存下全部）。\n\n## C++17 模板\n\n以下用本節代表題 496. 下一個更大元素 I 展示核心寫法。\n\n**完整題目**：對 `nums1` 中每個元素，找出它在 `nums2` 右側第一個比它大的元素，若不存在則為 `-1`。\n\n:::example 單調遞減棧：掃描 nums2，維護「尚未找到下一個更大元素」的元素\n**線索**：題目要找最近更大/更小邊界或某元素的貢獻範圍；這表示元素應在單調結構中等待被右側元素結算。\n\n```cpp\nclass Solution {\npublic:\n    // 單調遞減棧：掃描 nums2，維護「尚未找到下一個更大元素」的元素\n    vector<int> nextGreaterElement(vector<int>& nums1, vector<int>& nums2) {\n            unordered_map<int, int> nxt;  // nxt[x] = x 在 nums2 中的下一個更大元素\n            vector<int> st;\n            for (int x : nums2) {\n                    // 棧頂元素小於 x，表示 x 就是它們的下一個更大元素\n                    while (!st.empty() && st.back() < x) {\n                            nxt[st.back()] = x;\n                            st.pop_back();\n                    }\n                    st.push_back(x);\n            }\n            vector<int> ans;\n            for (int x : nums1) {\n                    ans.push_back(nxt.count(x) ? nxt[x] : -1);  // 找不到則回傳 -1\n            }\n            return ans;\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- 堆疊內維持遞減；當新元素更大時，它就是被彈出元素右側第一個更大值。\n- 容器中保存尚未結算的元素；新元素破壞單調性時，被彈出的元素就找到了右邊界。\n- 程式通常存下標而非數值，因為答案需要計算距離或回填到原位置。\n\n## 時間與空間複雜度\n\n**時間 `O(n)`**。外層迴圈掃 n 次；內層 while 看似巢狀，但每個下標一生只被彈出一次，所有 while 迭代加總不超過 n 次——這是均攤分析，不能只看巢狀迴圈的外觀。\n\n**空間 `O(n)`**：堆疊最壞會存下全部元素（輸入嚴格遞減時，沒有任何元素被彈出），加上 `O(n)` 的答案陣列。\n\n環形版本（503）掃兩遍，仍是 `O(n)`。\n\n## 常見錯誤與邊界條件\n\n- 先決定維持遞增還是遞減。\n- 相等元素用 `<` 還是 `<=` 會影響去重與邊界。\n- 需要寬度時入棧索引而不是值。\n- 哨兵可簡化最後清棧。\n\n## 常見變形\n\n- 循環陣列可掃兩遍。\n- 用索引入棧以計算距離或寬度。\n- 貢獻法中分別求左/右第一個嚴格或非嚴格邊界。\n\n## 與相似技巧的比較\n\n- **對比暴力雙層迴圈**：兩者都在找「最近的更大值」，差別是暴力每次重新掃，單調堆疊把已知的比較結果留在堆疊裡重複使用。\n- **對比單調佇列**：本質相同，都在丟掉「被蓋過就沒用」的元素。差別在取用端——堆疊從尾端取（關心「最近的邊界」），佇列從頭端取（關心「視窗內的極值」）。題目問的是「向左／右第一個」就用堆疊，問「固定視窗內最大／最小」就用佇列。\n- **對比排序 + 掃描**：找下一個更大元素也能靠排序後用有序集合查詢，但那是 `O(n log n)`；單調堆疊利用了「只關心最近」這個更強的條件，換到線性。\n- **對比 DP**：`ans[i]` 確實可以寫成從右往左的遞推（跳躍式往右追），複雜度同樣是均攤 `O(n)`，本質上就是把堆疊藏進遞推裡。\n\n## 代表例題\n\n**題號**：496、503、739、853、901\n\n要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 496 | [下一個更大元素 I](https://leetcode.cn/problems/next-greater-element-i) |  |\n| 503 | [下一個更大元素 II](https://leetcode.cn/problems/next-greater-element-ii) |  |\n| 739 | [每日溫度](https://leetcode.cn/problems/daily-temperatures) |  |\n| 853 | [車隊](https://leetcode.cn/problems/car-fleet) | 1678 |\n| 901 | [股票價格跨度](https://leetcode.cn/problems/online-stock-span) | 1709 |\n| 1475 | [商品折扣後的最終價格](https://leetcode.cn/problems/final-prices-with-a-special-discount-in-a-shop) | 1212 |\n\n## 本節重點速查\n\n- 堆疊存**下標**，值維持單調：找更大就遞減、找更小就遞增。\n- 新元素破壞單調性時彈出堆頂並結算——彈出的瞬間就知道被彈元素的右邊界，堆疊中它下面那個就是左邊界。\n- 每個元素進出各一次，均攤 `O(n)`。\n- 掃完仍在堆疊裡的元素代表那側沒有答案，維持預設值。\n- 相等元素用 `<` 還是 `<=` 會改變去重與邊界，動手前先想清楚；\n- 環形陣列掃兩遍、第二遍只結算不 push。",
    },
    {
      id: 3,
      title: "2. 進階",
      summary:
        "## 這個技術解決什麼問題\n\n基礎題只要「最近的更大／更小元素」；進階題要的是**彈出瞬間能同時取得的那一整包資訊**，或是把原本不單調的問題改造成堆疊吃得下的形狀。\n\n彈出堆頂 `j` 的那一刻，你其實同時握有三件事：`j` 的左邊界（堆疊中 `j` 下面那個）、`j` 的右邊界（正要進來的 `i`）、以及 `j` 被彈出的順序。進階題就是把答案掛在這三件事上。\n\n## 辨識題型的訊號\n\n- **需要同時用到左右兩側邊界，或在彈出時做計數／取次值。** 像 768（最多能完成排序的塊 II）要判斷「切到哪裡前綴已可獨立排序」，用單調堆疊維護區塊最大值即可。\n- **問題本身不單調，但換個掃描方向（由右往左、先建後拆）就能套堆疊。** 456 是典型代表。\n- **看似要遞迴或分治建結構，但每步都在挑「目前最大／最小」。** 654 就是把分治改寫成一次線性掃描。\n- 1063、1950 這類子陣列計數題，常把答案拆成「每個元素作為最大／最小值時，覆蓋多少子陣列」——這會自然帶到下一節的貢獻法。\n\n確認自己有沒有套對，問三個問題：\n\n1. 在彈出的那一刻，我同時知道了哪些資訊（左界？右界？次大值？）？\n2. 換個掃描方向，問題會不會變得單調好處理？\n3. 我要維護的「額外量」是什麼，它在 push 或 pop 的哪一步更新？\n\n## 核心想法與直覺\n\n本節在基礎的單調堆疊上再進一步：基礎題只要「找最近的更大／更小元素」，進階題則要**在彈出的瞬間，順手算出更多資訊，或是把問題巧妙轉化成單調堆疊能處理的形狀**。\n\n關鍵體會是：**單調堆疊的堆頂，永遠維持著「目前還有效的候選」。** 當一個新元素破壞單調性、把堆頂彈出時，你其實同時知道了被彈元素的「左邊界」（堆疊中它下面那一個）和「右邊界」（正要進來的新元素）。這對「左右邊界一起需要」的進階題特別有用。\n\n:::example 132 模式：從右往左，堆疊維護候選，k 記錄被彈出的次大值\n以 456（132 模式）為例：要找下標 `i < j < k` 使 `nums[i] < nums[k] < nums[j]`。技巧是**從右往左掃**，用單調堆疊維護「可能的中間值（k 的值）」，並記錄被彈出的最大值當作 `nums[k]` 的上界；之後只要遇到比這個上界還小的數，就是合法的 `nums[i]`。\n\n```cpp\n// 132 模式：從右往左，堆疊維護候選，k 記錄被彈出的次大值\nstack<int> st;\nint k = INT_MIN;  // k：目前可當「中間 j」配對的最佳 nums[k]\nfor (int i = n - 1; i >= 0; i--) {\n    if (nums[i] < k) {\n        return true;  // 找到 nums[i] < nums[k] < nums[j]\n    }\n    while (!st.empty() && nums[i] > st.top()) {\n        k = st.top();\n        st.pop();  // 彈出的值能當合法的 nums[k]\n    }\n    st.push(nums[i]);\n}\n```\n:::\n\n\n\n進階題的共通點是：**狀態不只「最近邊界」，還要在彈出時維護額外量**（次大值、區間長度、子陣列計數等）。\n\n## 狀態／資料結構定義\n\n堆疊本身仍是「下標 + 單調性」，額外狀態依題型而定：\n\n- **區塊最大值**（768）：堆疊每格存「這個區塊的最大值」，合併時把被彈出的區塊併進來。\n- **次大值**（456，132 模式）：一個變數 `third` 記錄「已被彈出的最大值」，它就是模式中的 `2`。\n- **父子關係**（654，最大二元樹）：堆疊存節點指標，彈出時決定掛成左子還是右子。\n- **計數量**（1063、1950）：彈出時累加「以被彈元素為極值的子陣列數」。\n\n共通形式是：`(下標, 附加量)`，附加量在 push 或 pop 的其中一步更新——先想清楚是哪一步，實作就不會亂。\n\n## 不變量或正確性證明\n\n**雙邊界不變量**：當 `nums[i]` 彈出堆頂 `j` 時，`(st.top(), i)` 恰好是 `j` 的極大延伸區間——左端是堆疊中 `j` 下面那個元素的下標，右端是 `i`。理由與基礎節相同：兩側的元素若破壞條件，`j` 早就被彈掉了。這保證了「左界、右界一起需要」的題目可以一次取得，不必跑兩遍。\n\n**132 模式的不變量**（456）：由右往左掃，維持一個遞減堆疊，並讓 `third` 永遠等於「已被彈出的元素中的最大值」。彈出只發生在 `nums[i] > st.top()` 時，所以 `third < nums[i]` 恆成立，且 `third` 右邊必存在一個更大的數（就是把它彈出去的那個）。因此只要遇到 `nums[i] < third`，`(nums[i], third 的來源, third)` 就構成合法的 132 模式。\n\n**方向轉換的正確性**：由右往左掃並不改變問題，只是把「找右邊的邊界」變成「找左邊的邊界」。只要對稱地翻轉單調性方向，所有基礎節的論證原封不動成立。\n\n**均攤仍是 `O(n)`**：附加量的更新是 `O(1)`，不影響每個元素進出各一次的計數。\n\n## 逐步演算法\n\n:::example 彈出瞬間同時拿到左右邊界\n**1. 想清楚「彈出瞬間」能確定什麼。** 被彈元素的左右邊界此刻同時已知，許多進階量都在這一刻計算。\n\n```cpp\n// 彈出瞬間同時拿到左右邊界\nint mid = st.top();\nst.pop();\nint rightBound = i;                          // 正在進來的元素\nint leftBound = st.empty() ? -1 : st.top();  // 堆疊中它下面的元素\n```\n:::\n\n\n\n**2. 善用「掃描方向」轉化問題。** 從右往左掃（132 模式）、或先建後拆，常能讓原本不單調的問題變得適合堆疊。\n\n:::example 最大二叉樹：單調遞減堆疊一次建樹\n**3. 用單調性直接「建結構」。** 654（最大二叉樹）要遞迴選最大值當根；用單調遞減堆疊一次掃描即可建好：新元素比堆頂大就把堆頂收為自己的左子樹。\n\n```cpp\n// 最大二叉樹：單調遞減堆疊一次建樹\nstack<TreeNode*> st;\nfor (int v : nums) {\n    TreeNode* cur = new TreeNode(v);\n    while (!st.empty() && st.top()->val < v) {\n        cur->left = st.top();\n        st.pop();  // 較小者成為當前節點左子樹\n    }\n    if (!st.empty()) {\n        st.top()->right = cur;\n    }\n    st.push(cur);\n}\n```\n:::\n\n\n\n**4. 複雜度。** 仍是每元素進出各一次，`O(n)` 時間、`O(n)` 空間；只是每次彈出多做了 `O(1)` 的額外計算。\n\n## C++17 模板\n\n以下用同型延伸示範題 496. 下一個更大元素 I 展示核心寫法。\n\n**完整題目**：對 `nums1` 中每個元素，找出它在 `nums2` 右側第一個比它大的元素，若不存在則為 `-1`。\n\n:::example 單調遞減棧：掃描 nums2，維護「尚未找到下一個更大元素」的元素\n**線索**：題目要找最近更大/更小邊界或某元素的貢獻範圍；這表示元素應在單調結構中等待被右側元素結算。\n\n```cpp\nclass Solution {\npublic:\n    // 單調遞減棧：掃描 nums2，維護「尚未找到下一個更大元素」的元素\n    vector<int> nextGreaterElement(vector<int>& nums1, vector<int>& nums2) {\n            unordered_map<int, int> nxt;  // nxt[x] = x 在 nums2 中的下一個更大元素\n            vector<int> st;\n            for (int x : nums2) {\n                    // 棧頂元素小於 x，表示 x 就是它們的下一個更大元素\n                    while (!st.empty() && st.back() < x) {\n                            nxt[st.back()] = x;\n                            st.pop_back();\n                    }\n                    st.push_back(x);\n            }\n            vector<int> ans;\n            for (int x : nums1) {\n                    ans.push_back(nxt.count(x) ? nxt[x] : -1);  // 找不到則回傳 -1\n            }\n            return ans;\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- 堆疊內維持遞減；當新元素更大時，它就是被彈出元素右側第一個更大值。\n- 容器中保存尚未結算的元素；新元素破壞單調性時，被彈出的元素就找到了右邊界。\n- 程式通常存下標而非數值，因為答案需要計算距離或回填到原位置。\n\n## 時間與空間複雜度\n\n**時間 `O(n)`**，**空間 `O(n)`**，與基礎節相同——每次彈出只多做 `O(1)` 的額外計算（合併區塊最大值、更新 `third`、累加計數）。\n\n例外是把附加量做成非 `O(1)` 的情形：例如彈出時要對被彈區間重新掃描，就會退回 `O(n^2)`。設計附加量時的原則是「合併必須 `O(1)`」——區塊最大值可以（取 max），區塊排序不行。\n\n654 建最大二元樹同樣是 `O(n)`，比「每次找區間最大值再遞迴」的 `O(n^2)` 好一個量級。\n\n## 常見錯誤與邊界條件\n\n- 先決定維持遞增還是遞減。\n- 相等元素用 `<` 還是 `<=` 會影響去重與邊界。\n- 需要寬度時入棧索引而不是值。\n- 哨兵可簡化最後清棧。\n\n## 常見變形\n\n- 循環陣列可掃兩遍。\n- 用索引入棧以計算距離或寬度。\n- 貢獻法中分別求左/右第一個嚴格或非嚴格邊界。\n\n## 與相似技巧的比較\n\n- **對比基礎單調堆疊**：資料結構一模一樣，差別只在「彈出時做什麼」。基礎題填一格答案，進階題順手合併狀態。\n- **對比笛卡爾樹**：654 的最大二元樹就是笛卡爾樹，而單調堆疊正是它的線性建構法。看到「遞迴取區間極值再分治」就該想到能用堆疊攤平。\n- **對比稀疏表／線段樹**：這些能回答任意區間極值查詢，但要 `O(n log n)` 預處理。單調堆疊只回答「最近邊界」，換來線性——需求更窄，代價更低。\n- **對比從左掃與從右掃**：兩者資訊量不同。456 由右往左才能先備好「右側的次大值」；若堅持由左往右，就得另外維護後綴結構。掃描方向是進階題最常見的破口。\n\n## 代表例題\n\n**題號**：456、654、768、1063、1950\n\n要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 456 | [模式](https://leetcode.cn/problems/132-pattern) |  |\n| 654 | [最大二叉樹](https://leetcode.cn/problems/maximum-binary-tree) |  |\n| 768 | [最多能完成排序的塊 II](https://leetcode.cn/problems/max-chunks-to-make-sorted-ii) |  |\n| 1063 | [有效子陣列的數目](https://leetcode.cn/problems/number-of-valid-subarrays)（Premium） |  |\n| 1950 | [所有子陣列最小值中的最大值](https://leetcode.cn/problems/maximum-of-minimum-values-in-all-subarrays)（Premium） |  |\n| 1966 | [未排序陣列中的可被二分搜尋的數](https://leetcode.cn/problems/binary-searchable-numbers-in-an-unsorted-array) |  |\n| 2282 | [在一個網格中可以看到的人數](https://leetcode.cn/problems/number-of-people-that-can-be-seen-in-a-grid) |  |\n| 2355 | [你能拿走的最大圖書數量](https://leetcode.cn/problems/maximum-number-of-books-you-can-take) |  |\n| 2832 | [每個元素為最大值的最大範圍](https://leetcode.cn/problems/maximal-range-that-each-element-is-maximum-in-it) |  |\n| 2863 | [最長半遞減子陣列的長度](https://leetcode.cn/problems/maximum-length-of-semi-decreasing-subarrays) |  |\n| 3221 | [最大陣列跳躍得分 II](https://leetcode.cn/problems/maximum-array-hopping-score-ii) |  |\n| 3555 | [排序每個滑動視窗中最小的子陣列](https://leetcode.cn/problems/smallest-subarray-to-sort-in-every-sliding-window) |  |\n| 1019 | [連結串列中的下一個更大節點](https://leetcode.cn/problems/next-greater-node-in-linked-list) | 1571 |\n| 962 | [最大寬度坡](https://leetcode.cn/problems/maximum-width-ramp) | 1608 |\n| 3814 | [預算下的最大總容量](https://leetcode.cn/problems/maximum-capacity-within-budget) | 1796 |\n| 3676 | [碗子陣列的數目](https://leetcode.cn/problems/count-bowl-subarrays) | 1848 |\n| 2054 | [兩個最好的不重疊活動](https://leetcode.cn/problems/two-best-non-overlapping-events) | 1883 |\n| 3542 | [將所有元素變為 0 的最少操作次數](https://leetcode.cn/problems/minimum-operations-to-convert-all-elements-to-zero) | 1890 |\n| 1124 | [表現良好的最長時間段](https://leetcode.cn/problems/longest-well-performing-interval) | 1908 |\n| 1130 | [葉值的最小代價生成樹](https://leetcode.cn/problems/minimum-cost-tree-from-leaf-values) | 1919 |\n| 3113 | [邊界元素是最大值的子陣列數目](https://leetcode.cn/problems/find-the-number-of-subarrays-where-boundary-elements-are-maximum) | 2046 |\n| 2866 | [美麗塔 II](https://leetcode.cn/problems/beautiful-towers-ii) | 2072 |\n| 975 | [奇偶跳](https://leetcode.cn/problems/odd-even-jump) | 2079 |\n| 1944 | [佇列中可以看到的人數](https://leetcode.cn/problems/number-of-visible-people-in-a-queue) | 2105 |\n| 2454 | [下一個更大元素 IV](https://leetcode.cn/problems/next-greater-element-iv) | 2175 |\n| 2289 | [使陣列按非遞減順序排列](https://leetcode.cn/problems/steps-to-make-array-non-decreasing) | 2482 |\n| 1776 | [車隊 II](https://leetcode.cn/problems/car-fleet-ii) | 2531 |\n| 2736 | [最大和查詢](https://leetcode.cn/problems/maximum-sum-queries) | 2533 |\n| 3420 | [統計 K 次操作以內得到非遞減子陣列的數目](https://leetcode.cn/problems/count-non-decreasing-subarrays-after-k-operations) | 2855 |\n\n## 本節重點速查\n\n- 進階題的關鍵體會：**彈出的瞬間，左界、右界、彈出順序三件事同時到手**。\n- 把答案掛在這一刻，就不必跑兩遍。\n- 附加量（區塊最大值、次大值 `third`、子陣列計數）要設計成 `O(1)` 可合併，否則均攤分析會破功。\n- 問題不單調時先試著換掃描方向——456 由右往左掃就變得單調。\n- 整體仍是 `O(n)` 時間、`O(n)` 空間。",
    },
    {
      id: 5,
      title: "3. 矩形",
      summary:
        "## 這個技術解決什麼問題\n\n答案是「某種矩形或區間的最大面積」，而高度受限於區間內的最矮者。\n\n暴力枚舉左右邊界是 `O(n^2)` 起跳。突破口是換一個枚舉對象：**不要枚舉區間，而是枚舉「誰當高度」**。固定以第 `i` 根長條的高度當矩形高度後，這個矩形能延伸到的範圍就完全確定了——左右兩側第一根比它矮的長條之間。於是問題化約成基礎節的「找最近的更矮邊界」。\n\n接雨水（42）是同一結構的鏡像：積水量由左右兩側的最高柱決定，一樣靠堆疊在彈出時逐層結算。\n\n## 辨識題型的訊號\n\n- **答案是「某種矩形 / 區間的最大面積或最大寬度」，而限制條件是高度（或數值）要一致或不被破壞。** 84、85 是直接的矩形題。\n- **42（接雨水）是它的鏡像：** 不是找最大矩形，而是每個位置能積多少水，取決於「左右兩側的最高柱」——同樣靠單調堆疊找邊界，彈出時結算一層水量。\n- **二維矩陣裡找全 1 的最大矩形 / 正方形（85、221、755）**，多半能「逐列壓成一維柱狀圖」再套本節技巧。\n- 暴力是枚舉左右邊界算面積（`O(n^2)` 以上），而每個矩形的高度其實由「最近的更矮邊界」決定——這就是改用堆疊的訊號。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的矩形「高度」由誰決定？它向左右延伸的邊界是不是「第一個更矮的」？\n2. 堆疊該遞增還是遞減？彈出時寬度怎麼算（右界 - 左界 - 1）？\n3. 如果是二維，我能不能逐列累積高度，化成一維柱狀圖？\n\n## 核心想法與直覺\n\n本節用單調堆疊解「矩形面積」類問題，代表題是 84（柱狀圖中最大的矩形）。給一排寬度都是 1、高度不同的長條，要找出「面積最大的矩形」。\n\n關鍵觀察：**以某根長條的高度 `h` 為矩形高度時，這個矩形能往左右延伸到哪裡？答案是「左右兩側第一根比它矮的長條」之間。** 因為一旦碰到更矮的長條，矩形就不能再保持高度 `h` 了。\n\n:::example 柱狀圖最大矩形：遞增堆疊，遇到更矮就結算堆頂\n所以問題化成：對每根長條，找它「左邊第一個更矮」和「右邊第一個更矮」的位置——這正是單調堆疊擅長的事。維持一個**遞增堆疊**（由底到頂高度遞增），當新長條比堆頂矮，就把堆頂彈出並結算它的最大矩形：此刻它的右邊界就是新長條，左邊界就是堆疊中它下面那一根。\n\n```cpp\n// 柱狀圖最大矩形：遞增堆疊，遇到更矮就結算堆頂\nstack<int> st;  // 存下標，高度由底到頂遞增\nint best = 0;\nfor (int i = 0; i <= n; ++i) {\n    int h = (i == n) ? 0 : heights[i];  // 末尾補 0 高，逼出剩餘長條\n    while (!st.empty() && h < heights[st.top()]) {\n        int top = st.top();\n        st.pop();\n        int left = st.empty() ? -1 : st.top();            // 左邊第一個更矮\n        best = max(best, heights[top] * (i - left - 1));  // 寬 = 右界-左界-1\n    }\n    st.push(i);\n}\n```\n:::\n\n\n\n矩形寬度 `i - left - 1` 是「右邊界與左邊界之間的格子數」，這就是堆疊存下標而非值的原因。\n\n## 狀態／資料結構定義\n\n**柱狀圖最大矩形**（84）：堆疊存下標，對應高度維持**遞增**。彈出 `j` 時，矩形高度是 `heights[j]`，右界是當前 `i`，左界是彈出後的新堆頂 `st.top()`，寬度 `i - st.top() - 1`。\n\n**哨兵**：頭尾各補一根高度 0（或只在尾端補），可以省掉「掃描結束後清空堆疊」與「堆疊為空時左界取 -1」兩個特判。\n\n**二維壓一維**（85、221）：`heights[c]` 表示以當前列為底、第 `c` 行往上連續 1 的個數，逐列更新後對每一列跑一次一維演算法。\n\n**接雨水**（42）：堆疊存下標、高度遞減，彈出時的積水是一個「橫條」——寬 `i - st.top() - 1`，高 `min(height[i], height[st.top()]) - height[彈出的那根]`。\n\n## 不變量或正確性證明\n\n**堆疊不變量**：堆疊中下標對應的高度嚴格遞增，且相鄰兩個下標之間的所有長條都不低於下面那一根（因為更矮的都已把它彈掉了）。\n\n**寬度公式的正確性**：彈出 `j` 時，新堆頂 `l = st.top()` 滿足 `heights[l] < heights[j]`，且 `(l, j)` 之間的長條都 `>= heights[j]`（由不變量）。右側 `i` 是第一個 `heights[i] < heights[j]` 的位置。因此以 `heights[j]` 為高的極大矩形正是 `(l, i)` 開區間，格子數 `i - l - 1`。\n\n**沒有漏解**：任何最大矩形的高度必等於它所涵蓋區間的最小長條高度（否則可以再往上長）。而演算法對**每一根**長條都算過「以它為高的極大矩形」，所以最優解一定被枚舉到。\n\n**哨兵的作用**：尾端補 0 保證所有殘留元素都會被彈出結算；頭端補 0 保證堆疊永不為空，左界永遠有值。\n\n**接雨水的分層不變量**：每次彈出結算的是一個「橫條」，不同次彈出的橫條互不重疊，因此逐次累加不會重複計算也不會漏。\n\n## 逐步演算法\n\n:::example 狀態表示：遞增堆疊存下標\n**1. 維持遞增堆疊，存下標。** 高度遞增才能保證彈出時左右邊界都是「第一個更矮的」。\n\n```cpp\n// 狀態表示：遞增堆疊存下標\nstack<int> st;  // heights[st.bottom] <= ... <= heights[st.top]\n```\n:::\n\n\n\n:::example 結算：寬度 = 右界 - 左界 - 1\n**2. 結算寬度。** 彈出堆頂時，左界是它下面那個下標，右界是當前下標。\n\n```cpp\n// 結算：寬度 = 右界 - 左界 - 1\nint top = st.top();\nst.pop();\nint left = st.empty() ? -1 : st.top();\nint width = i - left - 1;\nbest = max(best, heights[top] * width);\n```\n:::\n\n\n\n**3. 末尾補哨兵，逼出殘留長條。** 掃描到結尾時堆疊裡通常還剩遞增的一串，補一根高度 0（或在頭尾各補哨兵），讓它們全部被彈出結算。\n\n:::example 二維轉一維：逐列累積高度，再對每列跑柱狀圖\n**4. 升維到二維。** 85（最大矩形）把每一列當底，逐列累積「往上連續 1 的高度」形成柱狀圖，再對每一列套 84 的解法即可。221（最大正方形）則是同類「以行為底」的思路，只是用 DP 更直接。\n\n```cpp\n// 二維轉一維：逐列累積高度，再對每列跑柱狀圖\nfor (int c = 0; c < cols; ++c) {\n    height[c] = (matrix[r][c] == '1') ? height[c] + 1 : 0;\n}\n// 接著對 height[] 跑上面的最大矩形函式\n```\n:::\n\n\n\n**5. 複雜度。** 84 是 `O(n)`；85 對每一列各跑一次，`O(rows * cols)`。\n\n## C++17 模板\n\n以下用本節代表題 84. 柱狀圖中最大的矩形 展示核心寫法。\n\n**完整題目**：給定柱狀圖高度陣列，找出能由相鄰柱子形成的最大矩形面積。\n\n:::example 在末尾加哨兵高度 0，確保所有柱子最終都會被彈出\n**線索**：題目把每個位置當作高度或邊界，要求最大矩形/範圍貢獻；左右第一個更矮/更小就是單調棧線索。\n\n```cpp\nclass Solution {\npublic:\n    int largestRectangleArea(vector<int>& heights) {\n            // 在末尾加哨兵高度 0，確保所有柱子最終都會被彈出\n            vector<int> h = heights;\n            h.push_back(0);\n            vector<int> st;  // 單調遞增堆疊，存柱子下標\n            int ans = 0;\n            for (int i = 0; i < (int)h.size(); ++i) {\n                    // 當前柱子比堆疊頂端矮，彈出並計算以彈出柱子為高的最大矩形\n                    while (!st.empty() && h[st.back()] > h[i]) {\n                            int height = h[st.back()];\n                            st.pop_back();\n                            int left = st.empty()\n                                                                                        ? -1\n                                                                                        : st.back();  // 左邊界（新堆頂即左側第一個更矮的柱）\n                            ans = max(ans, height * (i - left - 1));  // 寬度 = i - left - 1\n                    }\n                    st.push_back(i);\n            }\n            return ans;\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- 每根柱子在被彈出時，左右第一個更矮位置都已確定；哨兵 0 可強制清空堆疊。\n- 容器中保存尚未結算的元素；新元素破壞單調性時，被彈出的元素就找到了右邊界。\n- 程式通常存下標而非數值，因為答案需要計算距離或回填到原位置。\n\n## 時間與空間複雜度\n\n**84 柱狀圖最大矩形**：時間 `O(n)`、空間 `O(n)`，每根長條進出堆疊各一次。\n\n**85 最大矩形（二維）**：對 `rows` 列各跑一次一維演算法，時間 `O(rows × cols)`、空間 `O(cols)`（只需一條高度陣列與一個堆疊）。這已是最優——光是讀入矩陣就要 `O(rows × cols)`。\n\n**42 接雨水**：時間 `O(n)`、空間 `O(n)`。同題的雙指標解法能把空間壓到 `O(1)`，但單調堆疊的版本更容易推廣到「逐層結算」的變形。\n\n面積可能溢位：`n` 與高度都到 `1e5` 量級時乘積超過 `int`，要用 `long long`。\n\n## 常見錯誤與邊界條件\n\n- 加入高度 0 哨兵強制彈出剩餘柱子。\n- 寬度是 `right - left - 1`。\n- 面積可能超出 int 時用 long long。\n- 矩陣題要逐行累積高度。\n\n## 常見變形\n\n- 接雨水可用單調堆疊或雙指標。\n- 最大矩形可把每一行轉成柱狀圖。\n- 子陣列最小值貢獻與矩形寬度思路相同。\n\n## 與相似技巧的比較\n\n- **對比分治**：84 也能用「找區間最小值再分治」解，平均 `O(n log n)`、最壞 `O(n^2)`。單調堆疊把同樣的遞迴結構攤平成一次線性掃描。\n- **對比雙指標**（42）：接雨水的雙指標解是 `O(1)` 空間、按「列」累加；堆疊解是 `O(n)` 空間、按「橫條」累加。前者更省，後者在題目變成「逐層結算」或「要知道每次結算的區間」時更好改。\n- **對比前綴最大值陣列**（42）：預先算左右最大值是最直觀的 `O(n)` 時間 / `O(n)` 空間解，但無法回答「哪些位置屬於同一層水」。\n- **對比 85 的 DP 解**：最大矩形也能用「每格向左延伸的寬度 + 向上掃描」做，複雜度相同，但堆疊版把每列獨立處理，程式短且不易寫錯。\n\n## 代表例題\n\n**題號**：42、84、85、221、755\n\n要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 42 | [接雨水](https://leetcode.cn/problems/trapping-rain-water) |  |\n| 84 | [柱狀圖中最大的矩形](https://leetcode.cn/problems/largest-rectangle-in-histogram) |  |\n| 85 | [最大矩形](https://leetcode.cn/problems/maximal-rectangle) |  |\n| 221 | [最大正方形](https://leetcode.cn/problems/maximal-square) |  |\n| 755 | [倒水](https://leetcode.cn/problems/pour-water)（Premium） |  |\n| 1277 | [統計全為 1 的正方形子矩陣](https://leetcode.cn/problems/count-square-submatrices-with-all-ones) | 1613 |\n| 1504 | [統計全 1 子矩形](https://leetcode.cn/problems/count-submatrices-with-all-ones) | 1845 |\n| 1793 | [好子陣列的最大分數](https://leetcode.cn/problems/maximum-score-of-a-good-subarray) | 1946 |\n\n## 本節重點速查\n\n- 枚舉「誰當高度」而不是枚舉區間——固定高度後，極大矩形的左右界就是兩側第一根更矮的長條。\n- 堆疊存下標、高度**遞增**，彈出時寬度是 `i - st.top() - 1`（開區間格子數）。\n- 頭尾補高度 0 的哨兵，可同時免除清空堆疊與空堆疊取左界兩個特判。\n- 二維題（85、221）逐列累積「往上連續 1 的高度」壓成一維柱狀圖再套用。\n- 面積用 `long long` 防溢位。",
    },
    {
      id: 6,
      title: "4. 貢獻法",
      summary:
        "## 這個技術解決什麼問題\n\n要把**所有子陣列**的某個統計量（最小值、最大值、範圍）加總起來。子陣列有 `O(n^2)` 個，逐一求值再相加太慢。\n\n貢獻法換一個求和順序：不問「每個子陣列的最小值是誰」，改問「**每個元素當了多少個子陣列的最小值**」。把次數乘上元素值再全部加起來，答案完全相同，但枚舉對象從 `O(n^2)` 個子陣列變成 `n` 個元素。\n\n剩下的問題「某元素當最小值的範圍有多大」，正是單調堆疊的本行。\n\n## 辨識題型的訊號\n\n- **題目要「對所有子陣列／子區間的某個統計量求總和」**，且這個量是最小值、最大值或兩者之差。907、2104 是典型。\n- **逐一枚舉子陣列會是 `O(n^2)` 起跳**，但你發現「每個元素能影響的子陣列」是一段連續範圍——這就該換成「按元素算貢獻」。\n- **答案可拆成「每個元素 × 它被覆蓋的子陣列數」**。能寫出「`nums[i]` 在哪些子陣列裡當最小／最大值」就成功一半。\n- **2962（最大元素出現至少 K 次）不是單調堆疊貢獻法題。** 它以「全域最大值至少出現 K 次」為合法條件，適合滑動視窗；可把[滑動視窗題解](https://leetcode.cn/problems/count-subarrays-where-max-element-appears-at-least-k-times/solutions/2560940/hua-dong-chuang-kou-fu-ti-dan-pythonjava-xvwg/)作為與本節的對照。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要加總的量，能不能改寫成「每個元素貢獻幾次」？\n2. 某元素當最小／最大值的左右延伸範圍，是不是由「第一個更小／更大邊界」決定？\n3. 有相等元素時，我哪一側用嚴格、哪一側用非嚴格來去重？\n\n## 核心想法與直覺\n\n本節介紹「貢獻法（contribution technique）」配合單調堆疊。它回答的是這類問題：**把所有子陣列的某個量（最小值、最大值、範圍…）加總起來。** 代表題是 907（子陣列的最小值之和）：對每一個子陣列取最小值，再把這些最小值全部相加。\n\n直接枚舉所有子陣列有 `O(n^2)` 個，逐一算最小值更慢。貢獻法換個角度想：**不要問「每個子陣列的最小值是誰」，而是問「每個元素，當了多少個子陣列的最小值」。** 把這個次數乘上元素值，全部加起來就是答案。\n\n:::example 元素 i 當最小值的子陣列數 = 左可延伸 × 右可延伸\n關鍵問題變成：`nums[i]` 在多少個子陣列中是最小值？答案是 `(左邊能延伸的距離) × (右邊能延伸的距離)`。左邊界是「左邊第一個比它小的元素」，右邊界是「右邊第一個比它小的元素」——又回到單調堆疊找邊界。\n\n```cpp\n// 元素 i 當最小值的子陣列數 = 左可延伸 × 右可延伸\nint left = i - L[i];   // L[i]：左邊第一個更小元素的下標\nint right = R[i] - i;  // R[i]：右邊第一個更小元素的下標\nlong long count = (long long)left * right;  // i 當最小值的子陣列個數\nans += count * nums[i];                     // i 的「貢獻」\n```\n:::\n\n\n\n每個元素只被算一次它的貢獻，總時間 `O(n)`。難點不在公式，而在**邊界的嚴格性**（見下節）。\n\n## 狀態／資料結構定義\n\n對每個下標 `i` 求兩個邊界：\n\n- `left[i]`：左邊第一個「比 `nums[i]` 小」的位置（不存在則 `-1`）。\n- `right[i]`：右邊第一個「比 `nums[i]` 小」的位置（不存在則 `n`）。\n\n則以 `nums[i]` 為最小值的子陣列數是 `(i - left[i]) * (right[i] - i)`——左端點有 `i - left[i]` 種選法，右端點有 `right[i] - i` 種，兩者獨立相乘。\n\n**去重的關鍵**：有相等元素時，一側用嚴格（`<`）、另一側用非嚴格（`<=`）。例如左側找「嚴格更小」、右側找「小於或等於」。這讓一段相等元素中，只有最左邊那個「認領」跨越它們的子陣列，每個子陣列恰好被算一次。\n\n累加值可能很大，用 `long long`，必要時取模。\n\n## 不變量或正確性證明\n\n**劃分不變量**：對每個子陣列 `[l, r]`，設 `p` 是其中最小值的位置（相等時依上面的嚴格性規則取唯一的代表，例如最左邊那個）。則 `left[p] < l <= p <= r < right[p]`，反之亦然。因此「所有子陣列」被完全劃分成 n 個互不相交的類別，第 `i` 類是「以 `i` 為最小值代表」的那些子陣列。\n\n總和 `Σ_{[l,r]} min(l,r) = Σ_i nums[i] × |第 i 類|`，而 `|第 i 類| = (i - left[i]) × (right[i] - i)`。\n\n**為什麼嚴格性不對稱是必要的**：若兩側都用嚴格 `<`，相等元素之間的子陣列會被多個位置同時認領，總和偏大；若兩側都用 `<=`，這些子陣列一個都不被認領，總和偏小。一嚴一寬恰好讓每個子陣列命中唯一代表——這是本節最容易寫錯、也最值得推一次的地方。\n\n**邊界計算的正確性**：`left`、`right` 由基礎節的單調堆疊求得，正確性見該節。\n\n## 逐步演算法\n\n:::example 求左邊第一個更小元素的下標 L[i]\n**1. 用單調堆疊求左右邊界。** 求「左邊第一個更小」用遞增堆疊掃一遍；右邊界同理（或反向掃）。\n\n```cpp\n// 求左邊第一個更小元素的下標 L[i]\nstack<int> st;\nfor (int i = 0; i < n; ++i) {\n    while (!st.empty() && nums[st.top()] >= nums[i]) {\n        st.pop();\n    }\n    L[i] = st.empty() ? -1 : st.top();\n    st.push(i);\n}\n```\n:::\n\n\n\n:::example 右邊界用「嚴格更小」(>)，與左邊的 (>=) 搭配去重\n**2. 一側嚴格、一側非嚴格，避免重複計數。** 若有相等元素（如 `[2, 2]`），同一個子陣列的最小值會被兩個 2 同時認領。解法是約定「左邊用嚴格小於、右邊用小於等於」（或反過來），確保每個子陣列只被算一次。上面 `>=` 與下面 `>` 的差別就是這個關鍵。\n\n```cpp\n// 右邊界用「嚴格更小」(>)，與左邊的 (>=) 搭配去重\nwhile (!st.empty() && nums[st.top()] > nums[i]) {\n    st.pop();\n}\nR[i] = st.empty() ? n : st.top();\n```\n:::\n\n\n\n:::example 累加每個元素的貢獻\n**3. 累加貢獻，注意溢位。** 子陣列數量可能很大，用 `long long` 並視題目取模。\n\n```cpp\n// 累加每個元素的貢獻\nlong long ans = 0;\nfor (int i = 0; i < n; ++i) {\n    ans += (long long)(i - L[i]) * (R[i] - i) % MOD * nums[i] % MOD;\n}\n```\n:::\n\n\n\n**4. 衍生題型。** 2104（子陣列範圍和）= 所有子陣列最大值之和 − 最小值之和，跑兩次貢獻法相減。1856（子陣列最小乘積最大值）則對每個元素算「以它為最小值的最大區間和」，配前綴和。2962、3359 則是把「計數」掛在彈出時做。\n\n**5. 複雜度。** 求邊界 `O(n)`，累加 `O(n)`，總共 `O(n)` 時間、`O(n)` 空間。\n\n## C++17 模板\n\n以 907. 子陣列的最小值之和 為例：對每個 `nums[i]`，求出它當最小值的極大範圍，再把 `nums[i] × 子陣列數` 累加起來。\n\n**完整題目**：給整數陣列 `nums`，對它的每一個連續子陣列取最小值，回傳所有最小值之和，答案對 `1e9 + 7` 取模。\n\n這裡用一次掃描同時完成邊界計算與累加：元素 `j` 被彈出的當下，右界就是當前的 `i`、左界就是彈出後的新堆頂，兩者一到齊立刻結算它的貢獻。\n\n```cpp\n// 907. 子陣列的最小值之和：單調堆疊 + 貢獻法，一次掃描結算\nclass Solution {\npublic:\n    int sumSubarrayMins(vector<int>& nums) {\n        const long long MOD = 1'000'000'007;\n        const int n = nums.size();\n        vector<int> st;  // 存下標，對應值嚴格遞增\n        long long ans = 0;\n\n        // i == n 時當作一根「高度負無窮」的哨兵，逼出堆疊裡剩下的元素\n        for (int i = 0; i <= n; ++i) {\n            // 左側嚴格更小、右側小於或等於：一嚴一寬，相等元素才不會重複計數\n            while (!st.empty() && (i == n || nums[st.back()] >= nums[i])) {\n                int j = st.back();\n                st.pop_back();\n                int left = st.empty() ? -1 : st.back();  // 左邊第一個嚴格更小\n                long long count = (long long)(j - left) * (i - j);\n                ans = (ans + count % MOD * nums[j]) % MOD;\n            }\n            st.push_back(i);\n        }\n        return (int)ans;\n    }\n};\n```\n\n## 時間與空間複雜度\n\n**時間 `O(n)`**：兩次單調堆疊掃描求 `left`、`right`（或一次掃描同時求出），再一次 `O(n)` 累加。\n\n**空間 `O(n)`**：兩條邊界陣列與一個堆疊。\n\n衍生題的成本：\n- **2104 子陣列範圍和** = 所有子陣列最大值之和 − 最小值之和，跑兩次貢獻法，仍是 `O(n)`。\n- **1856 子陣列最小乘積最大值**：對每個元素算「以它為最小值的極大區間和」，配一條前綴和陣列，`O(n)`。\n\n乘積 `(i - left[i]) * (right[i] - i)` 在 `n = 1e5` 時可達 `2.5e9`，超過 `int`，務必 `long long`。\n\n## 常見錯誤與邊界條件\n\n- **兩側都用嚴格或都用非嚴格**。相等元素會被重複認領（答案偏大）或一個都不被認領（答案偏小）。固定寫成「左嚴格、右非嚴格」（或反過來），並在紙上用 `[2, 2]` 驗一次。\n- **忘記用 `long long`**。`(j - left) * (i - j)` 在 `n = 3e4` 時就可能超過 `int`；乘上元素值後更容易溢位。先轉 `long long` 再相乘，不要乘完才轉。\n- **取模的時機**。`count` 先取模再乘 `nums[j]`，否則兩個接近 `1e9` 的數相乘會爆 `long long` 的安全範圍。\n- **漏掉哨兵**。掃描結束時堆疊裡通常還留著一串遞增元素，若不補一次「無窮小」的彈出，它們的貢獻全部漏算。\n- **左界取錯**。彈出 `j` 之後才讀 `st.back()`，順序寫反會拿到 `j` 自己。堆疊空了要用 `-1` 代表「左邊沒有更小的元素」。\n- **把 `left`／`right` 記成「相鄰更小」而非「第一個更小」**。中間允許有比 `nums[j]` 大的元素，範圍是開區間 `(left, right)`，不是連續遞減段。\n\n## 與相似技巧的比較\n\n- **對比逐一枚舉子陣列**：同樣的和，只是換了求和順序。`Σ over 子陣列` 改成 `Σ over 元素`，這個「換軸」是貢獻法的全部精髓。\n- **對比分治**：也能用「找區間最小值、分治左右、跨越部分另算」解，`O(n log n)`。貢獻法用堆疊把跨越部分一次算完，降到線性。\n- **對比矩形面積節**：兩者用的邊界完全相同（左右第一個更小），差別在結算什麼——矩形節取 `高 × 寬` 的最大值，貢獻法取 `值 × 子陣列數` 的總和。認出這點，兩節的模板可以共用。\n- **對比「計數滿足條件的子陣列」**（2962、3359）：那類題把計數掛在彈出時做，形式相近，但劃分依據是條件而非極值，去重規則要重新想。\n\n## 代表例題\n\n**題號**：3359、2104、2962、907、1856\n\n要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 3359 | [查詢最大元素不超過 K 的有序子矩陣](https://leetcode.cn/problems/find-sorted-submatrices-with-maximum-element-at-most-k)（Premium） |  |\n| 2104 | [子陣列範圍和（最大值-最小值）](https://leetcode.cn/problems/sum-of-subarray-ranges) | 1504 |\n| 2962 | [統計最大元素出現至少 K 次的子陣列](https://leetcode.cn/problems/count-subarrays-where-max-element-appears-at-least-k-times) | 1701 |\n| 907 | [子陣列的最小值之和](https://leetcode.cn/problems/sum-of-subarray-minimums) | 1976 |\n| 1856 | [子陣列最小乘積的最大值](https://leetcode.cn/problems/maximum-subarray-min-product) | 2051 |\n| 2334 | [元素值大於變化閾值的子陣列](https://leetcode.cn/problems/subarray-with-elements-greater-than-varying-threshold) | 2381 |\n| 2818 | [操作使得分最大](https://leetcode.cn/problems/apply-operations-to-maximize-score) | 2397 |\n| 2281 | [巫師的總力量和（最小值×和）](https://leetcode.cn/problems/sum-of-total-strength-of-wizards) | 2621 |\n| 3430 | [最多 K 個元素的子陣列的最值之和](https://leetcode.cn/problems/maximum-and-minimum-sums-of-at-most-size-k-subarrays) | 2645 |\n\n## 本節重點速查\n\n- 把「對所有子陣列求和」換軸成「每個元素貢獻幾次」，枚舉量從 `O(n^2)` 降到 `O(n)`。\n- 以 `nums[i]` 為最小值的子陣列數 = `(i - left[i]) × (right[i] - i)`，兩個邊界用單調堆疊求。\n- **相等元素一側用嚴格、一側用非嚴格**，否則會重複計算或漏算——這是本節唯一的難點，值得推一次劃分證明。\n- 乘積會超過 `int`，用 `long long`。\n- 2104 = 最大值和 − 最小值和，跑兩次即可。",
    },
    {
      id: 8,
      title: "5. 最小字典序",
      summary:
        '## 這個技術解決什麼問題\n\n在「刪除 K 個字元」或「保留長度 m 的子序列」這類限制下，求字典序最小（或最大）的結果。\n\n字典序的性質是**越前面的位權重越大**，所以應該貪心地讓前面的位盡量小。但單純的貪心會卡住：目前保留的字元，可能因為後面出現更小的字元而變成錯誤決定。單調堆疊提供的正是「**可反悔的貪心**」——保留在堆疊裡的都是暫定答案，遇到更優的就把先前的決定彈掉。\n\n額度（還能刪幾個）或唯一性（每個字元只能出現一次）則是彈出時的附加約束。\n\n## 辨識題型的訊號\n\n- **要在「保留 / 刪除若干元素」後，得到字典序最小（或最大、最具競爭力）的序列。** 402（最小）、1673（最具競爭力子序列，本質同 402）、321（拼接最大數，方向反過來找最大）都是。\n- **答案要求「貪心地讓前面的位最優」**，而你發現「後面來了更優的，前面剛保留的就該反悔刪掉」——這種「可反悔的貪心」就用單調堆疊。\n- **常伴隨額度限制（刪 K 個 / 留長度 m）或唯一性限制（每元素只出現一次，如 316、1081）**，這些限制決定彈出條件要加哪些判斷。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要的是字典序最小還是最大？對應堆疊該維持遞增還是遞減？\n2. 彈出除了「比堆頂更優」外，還受什麼限制（額度？該字元後面是否還會出現？）？\n3. 掃完後額度若沒用完、或有前導零，我該怎麼收尾？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 316 | 去除重複字母 | N/A | 要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。 |\n| 321 | 拼接最大數 | N/A | 要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。 |\n| 402 | 移掉 K 位數字 | N/A | 要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。 |\n| 1081 | [不同字元的最小子序列](https://leetcode.cn/problems/smallest-subsequence-of-distinct-characters/) | 2185 | 要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。 |\n| 1673 | [找出最具競爭力的子序列](https://leetcode.cn/problems/find-the-most-competitive-subsequence/) | 1802 | 要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。 |\n\n## 核心想法與直覺\n\n本節用單調堆疊處理「最小字典序（lexicographically smallest）」問題。代表題是 402（移掉 K 位數字）：給一個數字字串，移掉 K 個字元，讓剩下的數字最小。\n\n:::example 移掉 K 位數字：遞增堆疊，新字元更小且還能刪就彈堆頂\n字典序的直覺是：**越前面的位放越小的數字越好。** 所以從左往右掃，若發現「前一個保留的字元比現在這個還大」，把前一個移掉就能讓結果變小——這正是單調堆疊的「破壞單調性就彈出」。我們維持一個**遞增堆疊（由底到頂遞增）**，當新字元比堆頂小、而且還有刪除額度時，就彈掉堆頂。\n\n```cpp\n// 移掉 K 位數字：遞增堆疊，新字元更小且還能刪就彈堆頂\nstring st;  // 當作堆疊用，存最終保留的字元\nfor (char c : num) {\n    while (!st.empty() && k > 0 && st.back() > c) {\n        st.pop_back();  // 堆頂比 c 大，移掉它能讓數字更小\n        k--;            // 用掉一次刪除額度\n    }\n    st.push_back(c);\n}\n```\n:::\n\n\n\n這裡和前幾節不同的是：**彈出受「額度 K」限制**——不是想彈就彈，刪夠 K 個就停。它本質仍是「貪心地讓高位盡量小」，而堆疊保證每個字元只進出一次，整體 `O(n)`。\n\n## 狀態／資料結構定義\n\n`st`：暫定答案，維持單調（求最小就遞增、求最大就遞減）。它同時就是最後要輸出的序列，因此常直接用 `string` 或 `vector` 而非 `std::stack`。\n\n附加狀態依題型而定：\n\n- **額度型**（402 刪 K 位、1673 保留長度 m）：`k` 表示還能刪幾個，彈出時 `k--`，`k` 用完就停止彈出。1673 的等價寫法是「剩餘可用元素數 `n - i` 要夠補滿長度」。\n- **唯一性型**（316、1081 去重後字典序最小）：`inStack[c]` 標記字元是否已在堆疊中（已在就跳過，不重複加入），`lastIndex[c]` 或剩餘計數表示「這個字元後面還會不會再出現」——只有還會再出現的字元才可以被彈掉。\n- **拼接型**（321）：先對單一陣列求「長度 t 的最大子序列」，再枚舉兩陣列的長度分配並合併。\n\n## 不變量或正確性證明\n\n**堆疊不變量**：任一時刻，堆疊內容是「已掃描的前綴中，在目前剩餘額度下能達到的字典序最優子序列」。\n\n**交換論證（為什麼可以貪心地彈）**：設堆頂為 `a`、新元素為 `b`，且 `b < a`。考慮任何一個保留了 `a` 的合法解，把 `a` 換成 `b`：因為 `a` 的位置在 `b` 之前，替換後在第一個相異位上變小，字典序嚴格更優，且長度不變、仍合法。所以「彈掉 `a`」不會失去最優解——存在一個最優解與這個決定相容。\n\n**額度限制下的正確性**：只有還有額度時才允許彈出。額度用盡後剩下的元素直接接在後面，因為此時已無法再刪，任何調整都不合法。\n\n**唯一性限制下的正確性**：只有「這個字元後面還會再出現」時才允許彈出。否則彈掉就再也補不回來，會產生不合法解（缺字元）。這個條件把交換論證的前提補齊——替換必須仍然合法。\n\n**單調性的維持**：每次彈出都在恢復單調，因此堆疊由底到頂遞增（求最小時）恆成立。\n\n**均攤 `O(n)`**：每個元素最多入堆疊一次、出堆疊一次。\n\n## 逐步演算法\n\n:::example 核心彈出條件：更小的字元來了 + 還有額度\n**1. 維持遞增堆疊，受刪除額度限制。** 只有「堆頂更大、且還有額度」時才彈。\n\n```cpp\n// 核心彈出條件：更小的字元來了 + 還有額度\nwhile (!st.empty() && k > 0 && st.back() > c) {\n    st.pop_back();\n    k--;\n}\nst.push_back(c);\n```\n:::\n\n\n\n:::example 額度沒用完：移除尾端 k 個（此時序列已遞增）\n**2. 額度沒用完要從尾端補刪。** 若掃完還剩 `k > 0`（例如輸入本身遞增如 "12345"），代表前面沒機會刪，直接砍掉尾端 k 個。\n\n```cpp\n// 額度沒用完：移除尾端 k 個（此時序列已遞增）\nst.resize(st.size() - k);\n```\n:::\n\n\n\n:::example 去前導零，並處理結果為空的情況\n**3. 清前導零、處理空字串。** 402 要去掉開頭多餘的 0；最後若全被刪光要回 "0"。\n\n```cpp\n// 去前導零，並處理結果為空的情況\nint i = 0;\nwhile (i < st.size() && st[i] == \'0\') {\n    ++i;\n}\nstring res = st.substr(i);\nreturn res.empty() ? "0" : res;\n```\n:::\n\n\n\n:::example 去除重複字母：後面還會出現才敢彈；已在堆疊中就不重複加\n**4. 變體：限制「只能用一次」或「不可重複」。** 316 / 1081（去除重複字母，留下字典序最小且每字母只出現一次）要多兩個條件：彈出前先確認「被彈字元後面還會再出現」，且「該字元尚未在堆疊中」才 push。\n\n```cpp\n// 去除重複字母：後面還會出現才敢彈；已在堆疊中就不重複加\nif (inStack[c]) {\n    continue;  // c 已保留，跳過\n}\nwhile (!st.empty() && st.back() > c && lastPos[st.back()] > i) {\n    inStack[st.back()] = false;\n    st.pop_back();  // 後面還有，安心彈\n}\nst.push_back(c);\ninStack[c] = true;\n```\n:::\n\n\n\n**5. 複雜度。** 每字元進出堆疊各一次，`O(n)` 時間；額外的計數陣列 `O(字元集大小)` 或 `O(n)` 空間。\n\n## C++17 模板\n\n以下用同型延伸示範題 496. 下一個更大元素 I 展示核心寫法。\n\n**完整題目**：對 `nums1` 中每個元素，找出它在 `nums2` 右側第一個比它大的元素，若不存在則為 `-1`。\n\n:::example 單調遞減棧：掃描 nums2，維護「尚未找到下一個更大元素」的元素\n**線索**：題目要找最近更大/更小邊界或某元素的貢獻範圍；這表示元素應在單調結構中等待被右側元素結算。\n\n```cpp\nclass Solution {\npublic:\n    // 單調遞減棧：掃描 nums2，維護「尚未找到下一個更大元素」的元素\n    vector<int> nextGreaterElement(vector<int>& nums1, vector<int>& nums2) {\n            unordered_map<int, int> nxt;  // nxt[x] = x 在 nums2 中的下一個更大元素\n            vector<int> st;\n            for (int x : nums2) {\n                    // 棧頂元素小於 x，表示 x 就是它們的下一個更大元素\n                    while (!st.empty() && st.back() < x) {\n                            nxt[st.back()] = x;\n                            st.pop_back();\n                    }\n                    st.push_back(x);\n            }\n            vector<int> ans;\n            for (int x : nums1) {\n                    ans.push_back(nxt.count(x) ? nxt[x] : -1);  // 找不到則回傳 -1\n            }\n            return ans;\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- 堆疊內維持遞減；當新元素更大時，它就是被彈出元素右側第一個更大值。\n- 容器中保存尚未結算的元素；新元素破壞單調性時，被彈出的元素就找到了右邊界。\n- 程式通常存下標而非數值，因為答案需要計算距離或回填到原位置。\n\n## 時間與空間複雜度\n\n**時間 `O(n)`**（字元集固定時），**空間 `O(n)`**。每個元素進出堆疊各一次；`inStack` 與剩餘計數都是 `O(Σ)` 的固定大小陣列（小寫字母 26、數字 10）。\n\n**321 拼接最大數**是例外：要枚舉「從第一個陣列取 `t` 個、第二個取 `k - t` 個」的所有分配，每種分配各跑一次子序列選取與一次合併比較，總時間 `O(k × (m + n))`（或寫成 `O(k^2 × (m + n))` 視合併實作而定）。\n\n收尾要注意：額度沒用完時，要從尾端再刪掉剩下的 `k` 個（因為此時堆疊已單調遞增，刪尾端最划算）；402 還要去掉前導零，並處理結果為空字串應輸出 `"0"` 的情形。\n\n## 常見錯誤與邊界條件\n\n- 先決定維持遞增還是遞減。\n- 相等元素用 `<` 還是 `<=` 會影響去重與邊界。\n- 需要寬度時入棧索引而不是值。\n- 哨兵可簡化最後清棧。\n\n## 常見變形\n\n- 循環陣列可掃兩遍。\n- 用索引入棧以計算距離或寬度。\n- 貢獻法中分別求左/右第一個嚴格或非嚴格邊界。\n\n## 與相似技巧的比較\n\n- **對比一般貪心**：普通貪心一旦決定就不回頭；這裡的堆疊讓決定可以撤銷，本質是「帶反悔的貪心」。反悔貪心的另一種常見載體是堆（優先佇列），差別在撤銷的對象是「最近一個」（堆疊）還是「最差一個」（堆）。\n- **對比 DP**：`dp[i][j]` = 前 `i` 個元素保留 `j` 個的最優解，是 `O(nk)` 的正解，但被交換論證證明了貪心可行後就沒必要。認出「交換論證成立」是把 DP 降成 `O(n)` 的關鍵。\n- **對比基礎單調堆疊**：資料結構相同，但彈出條件多了額度與合法性約束。基礎節可以無條件彈，本節不行——這是最常見的錯誤來源。\n- **對比排序**：字典序最小的子序列不能靠排序得到，因為必須保持原有的相對順序（是子序列，不是重排）。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 316 | [去除重複字母](https://leetcode.cn/problems/remove-duplicate-letters) |  |\n| 321 | [拼接最大數](https://leetcode.cn/problems/create-maximum-number) |  |\n| 402 | [移掉 K 位數字](https://leetcode.cn/problems/remove-k-digits) |  |\n| 1081 | [不同字元的最小子序列](https://leetcode.cn/problems/smallest-subsequence-of-distinct-characters) | 2185 |\n| 1673 | [找出最具競爭力的子序列](https://leetcode.cn/problems/find-the-most-competitive-subsequence) | 1802 |\n| 2030 | [含特定字母的最小子序列](https://leetcode.cn/problems/smallest-k-length-subsequence-with-occurrences-of-a-letter) | 2562 |\n| 3816 | [刪除重複字元後的字典序最小字串](https://leetcode.cn/problems/lexicographically-smallest-string-after-deleting-duplicate-characters) | 2377 |\n\n## 本節重點速查\n\n- 字典序最優 = 可反悔的貪心，用單調堆疊承載：求最小維持遞增，遇到更小的就彈掉堆頂。\n- 正確性靠交換論證——把後面更小的字元換到前面，字典序嚴格更優且仍合法。\n- **彈出必須帶條件**：額度型看還能不能再刪（402、1673），唯一性型看該字元後面是否還會出現（316、1081），否則會產生不合法解。\n- 收尾記得處理額度沒用完（刪尾端）、前導零、以及空結果要輸出 `"0"`。\n- 均攤 `O(n)`。',
    },
    {
      id: 9012,
      title: "6. 常見面試模式",
      summary:
        "單調堆疊/單調佇列的題目幾乎都圍繞「維護一段具有單調性的候選集合，並在元素出入時即時得到答案」。先辨認下表的訊號，再套用對應寫法即可。\n\n| 模式 | 訊號 | 核心做法 | 代表題 |\n| --- | --- | --- | --- |\n| 下一個更大/更小元素 | 求每個元素右(左)側第一個比它大或小的值 | 單調堆疊維護待解元素，遇到破壞單調性者即彈出並結算，整體 `O(n)` | [每日溫度](https://leetcode.cn/problems/daily-temperatures) |\n| 直方圖/矩形 | 以高度求最大矩形或圍水 | 單調遞增堆疊找每根柱子左右第一個更矮者，得到可延伸寬度 | [柱狀圖中最大的矩形](https://leetcode.cn/problems/largest-rectangle-in-histogram) |\n| 貢獻計數 | 統計每個元素作為極值的子陣列數量 | 用單調堆疊求左右邊界，元素貢獻為 $left \\times right$ 後加總 | [子陣列的最小值之和](https://leetcode.cn/problems/sum-of-subarray-minimums) |\n| 字典序刪除 | 刪 `k` 個字元/數字使結果最小或單調 | 單調堆疊，當前元素小於棧頂且仍有刪除額度就彈出 | [移掉 K 位數字](https://leetcode.cn/problems/remove-k-digits) |\n| 滑動視窗極值 | 固定或可變視窗內的最大/最小值 | 單調佇列，隊首為當前極值，過期下標從隊首彈出 | [滑動視窗最大值](https://leetcode.cn/problems/sliding-window-maximum) |\n| DP + 單調佇列 | 轉移式為某區間內 `dp` 的極值 | 把 `dp` 值塞進單調佇列做區間極值優化，將 `O(nk)` 降為 `O(n)` | [跳躍遊戲 VI](https://leetcode.cn/problems/jump-game-vi) |\n\n**小提醒**\n- 想清楚堆疊裡存「下標」還是「值」，多數題存下標方便算寬度與邊界。\n- 處理重複值時，靠「左閉右開」之類的不對稱比較避免子陣列被重複計數。\n- 結算殘留：迴圈結束後堆疊裡剩下的元素往往代表「右側沒有更大者」，需額外處理。",
    },
  ],
} as TutorialData.Root;
