import type { TutorialData } from "@/types";

export const grid = {
  id: 1,
  title: "網格圖（DFS/BFS/綜合應用）",
  src: "https://leetcode.cn/circle/discuss/YiXPXW/",
  last_update: "2026-04-18T11:53:23.324686+00:00",
  summary:
    "**網格圖題目核心技巧**：\n- 方向陣列：`int dirs[4][2] = {{0, 1}, {0, -1}, {1, 0}, {-1, 0}};`\n- 邊界檢查：`x >= 0 && x < m && y >= 0 && y < n`\n- 訪問陣列：`vector<vector<bool>> visited(m, vector<bool>(n, false));`\n\n:::example 網格圖 DFS/BFS 模板\n**本題單涵蓋**：\n- DFS\n- BFS\n- 綜合應用\n\n```cpp\n// 網格圖 DFS/BFS 模板\nint dirs[4][2] = {{0, 1}, {0, -1}, {1, 0}, {-1, 0}};\n\n// DFS 遍歷連通塊\nvoid dfs(vector<vector<int>>& grid, int x, int y) {\n    int m = grid.size(), n = grid[0].size();\n    grid[x][y] = 0;  // 標記已訪問\n    for (auto& d : dirs) {\n        int nx = x + d[0], ny = y + d[1];\n        if (nx >= 0 && nx < m && ny >= 0 && ny < n && grid[nx][ny]) {\n            dfs(grid, nx, ny);\n        }\n    }\n}\n\n// BFS 求最短路\nint bfs(vector<vector<int>>& grid, int sx, int sy, int tx, int ty) {\n    int m = grid.size(), n = grid[0].size();\n    queue<pair<int, int>> q;\n    q.push({sx, sy});\n    grid[sx][sy] = 0;\n    int step = 0;\n    while (!q.empty()) {\n        int sz = q.size();\n        while (sz--) {\n            auto [x, y] = q.front();\n            q.pop();\n            if (x == tx && y == ty) {\n                return step;\n            }\n            for (auto& d : dirs) {\n                int nx = x + d[0], ny = y + d[1];\n                if (nx >= 0 && nx < m && ny >= 0 && ny < n && grid[nx][ny]) {\n                    grid[nx][ny] = 0;\n                    q.push({nx, ny});\n                }\n            }\n        }\n        ++step;\n    }\n    return -1;\n}\n```\n:::\n",
  children: [
    {
      id: 2,
      title: "1. 網格圖 DFS",
      summary:
        "## 這個技術解決什麼問題\n\n二維格子上「連在一起的一塊區域」相關的問題：這張圖有幾個連通塊、某一塊多大、整塊染成別的顏色、從邊界能碰到哪些格子。\n\n網格圖其實就是一張隱式的圖——節點是格子 `(row, col)`，邊是四個（或八個）相鄰方向。之所以不必真的建圖，是因為鄰居可以由座標直接算出來。認清這一點之後，所有圖論的遍歷技巧都能直接搬過來，DFS 只是其中最好寫的一種。\n\n它回答「連不連通、整塊長什麼樣」，不回答「最少幾步」——後者是 BFS 的工作。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「網格圖 DFS」想：\n\n- **輸入是一個二維陣列，問題關於「連在一起的一塊區域」。** 島嶼數量、島嶼周長（463）、區域大小、把某塊區域整片染色，都是 DFS 的拿手好戲。\n- **你只在乎「能不能連通 / 整塊長什麼樣」，不在乎「最短幾步」。** 要算步數最短時改用 BFS；只要遍歷整塊就用 DFS，因為它好寫、自然遞迴。\n- **可以從特定起點（如四條邊界）反向思考。** 「被圍繞的區域」「水流問題」都是先標出「碰得到邊界 / 碰得到海」的格子，再回推答案。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的節點是什麼？是純 `(row, col)`，還是要再加上鑰匙集合、剩餘步數等額外狀態？\n2. 我的 `visited` 存在哪裡？就地改值會不會破壞之後還要用的資料？\n3. 答案是「連通塊的數量」「某塊的大小」還是「遍歷後的標記矩陣」？我從哪裡讀出它？\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n本節要解決的場景是：給你一張像棋盤一樣的二維格子（matrix），每一格可能是陸地、水、牆或門。你想知道「從某一格出發，走遍所有相連的格子」會碰到哪些東西——這就是網格圖的深度優先搜尋（DFS）。\n\n:::example 方向陣列：上、下、左、右四個鄰格的座標偏移\n關鍵的觀念轉換是：**把每一格 `(row, col)` 當成圖上的一個節點，相鄰的格子之間有一條邊。** 通常「相鄰」指上下左右四個方向，有些題目是八方向。我們用一個「方向陣列」一次寫好所有偏移，避免重複貼四段幾乎一樣的程式。\n\n```cpp\n// 方向陣列：上、下、左、右四個鄰格的座標偏移\nint dr[4] = {-1, 1, 0, 0};\nint dc[4] = {0, 0, -1, 1};\n// (r + dr[k], c + dc[k]) 就是第 k 個鄰格\n```\n:::\n\n\n\n:::example DFS 核心：標記目前格、再往四個方向遞迴\nDFS 的精神是「一條路走到底，走不通再退回來換方向」。在格子上，就是站在一格、把它標記成「走過」、然後遞迴走進每個還沒走過的鄰格。為什麼一定要標記走過？因為 A 的鄰居是 B，B 的鄰居又是 A，不標記就會在兩格間無限來回。`visited` 標記正是把「圖可能有環」這件事擋掉的護欄。\n\n```cpp\n// DFS 核心：標記目前格、再往四個方向遞迴\nvoid dfs(vector<vector<int>>& g, int r, int c) {\n    g[r][c] = -1;  // 就地標記走過（也可用獨立 visited 陣列）\n    for (int k = 0; k < 4; ++k) {\n        int nr = r + dr[k], nc = c + dc[k];\n        if (inGrid(nr, nc) && g[nr][nc] == 1) {  // 邊界 + 是陸地 + 沒走過\n            dfs(g, nr, nc);\n        }\n    }\n}\n```\n:::\n\n\n\n像「島嶼數量」（200）就是：對每個還沒走過的陸地格啟動一次 DFS，一次 DFS 會把整座島染掉，啟動了幾次就有幾座島。\n\n## 狀態／資料結構定義\n\n**節點**：`(row, col)`。若同一格會以不同情況被造訪（帶鑰匙、朝向不同），節點就要升維，那是「綜合應用」一節的主題。\n\n**visited**：兩種寫法。\n- 另開 `vector<vector<bool>> visited(m, vector<bool>(n))`，不破壞輸入。\n- 就地改值（走過的陸地改成水），省下 `O(mn)` 空間，但破壞原始資料——題目之後還要用到原矩陣，或要跑第二次搜尋時就不能這樣做。\n\n**方向陣列**：固定寫成 `const int dirs[5] = {1, 0, -1, 0, 1};`，用 `dirs[k]`、`dirs[k + 1]` 取一組偏移，四個方向一個迴圈解決，比手寫四份 `if` 不易出錯。\n\n**答案的出口**：連通塊數量是「啟動了幾次 DFS」，區域大小是「單次 DFS 的回傳值累加」，染色題則直接讀最後的矩陣。\n\n## 不變量或正確性證明\n\n**visited 不變量**：一個格子被標記為已訪問之後，永遠不會再被加入搜尋。這保證每格最多被展開一次，複雜度才是 `O(mn)` 而不是指數。\n\n**連通塊完整性**：從格子 `s` 啟動的 DFS，訪問到的格子集合恰好是 `s` 所在的連通塊。\n- 「⊆」：每一步都只走到相鄰的同類格，所以訪問到的都與 `s` 連通。\n- 「⊇」：反證。設連通塊中有格子 `t` 未被訪問，取一條 `s` 到 `t` 的路徑，路徑上必有相鄰的一對 `(u, v)`，`u` 已訪問而 `v` 未訪問。但 DFS 展開 `u` 時會檢查它的所有鄰居，`v` 必被加入——矛盾。\n\n**計數的正確性**：外層迴圈只在「尚未訪問的目標格」上啟動 DFS，而每次啟動會把整個連通塊染掉。因此啟動次數與連通塊數一一對應，不重不漏。\n\n**反向思考題的正確性**（130 被圍繞的區域、417 水流問題）：先從邊界啟動 DFS 標出「碰得到邊界」的格子，剩下沒被標到的就是「被完全圍住」的。這是取補集，成立的前提是「碰得到邊界」這個性質對連通塊而言是全有或全無——同一連通塊要嘛都碰得到，要嘛都碰不到。\n\n## 逐步演算法\n\n:::example 邊界檢查：座標是否還在 m x n 的格子內\n**1. 邊界檢查獨立成一個小函式。** 每次要走進鄰格前，先確認沒走出棋盤外。把它寫成一行函式，主邏輯就乾淨很多。\n\n```cpp\n// 邊界檢查：座標是否還在 m x n 的格子內\nbool inGrid(int r, int c, int m, int n) {\n    return r >= 0 && r < m && c >= 0 && c < n;\n}\n```\n:::\n\n\n\n**2. 決定 visited 怎麼存。** 可以另開一個 `visited` 二維陣列，也可以「就地改值」——例如把走過的陸地直接改成水。後者省空間，但會破壞原始輸入，看題目允不允許。\n\n:::example 計數型：每啟動一次 DFS = 一個連通塊\n**3. 對需要的起點啟動 DFS。** 「島嶼數量」是掃整張圖、遇到新陸地就啟動並計數；「被圍繞的區域」（130）則反過來，只從**邊界**上的格子啟動 DFS，把連到邊界的區域標成安全，剩下沒被標記的才是被包圍的。\n\n```cpp\n// 計數型：每啟動一次 DFS = 一個連通塊\nint count = 0;\nfor (int r = 0; r < m; ++r) {\n    for (int c = 0; c < n; ++c) {\n        if (g[r][c] == 1) {\n            dfs(g, r, c);\n            ++count;\n        }\n    }\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 遞迴太深（整張圖連成一條長蛇）可能爆堆疊，格子很大時改用顯式 stack 的迭代 DFS 較安全。\n- 若同一格「以不同狀態」可重複到達（例如「太平洋大西洋水流問題」417 要分別從兩片海各做一次反向 DFS），visited 要對每次搜尋重置或分開存。\n- 複雜度：每格最多進出一次，`O(m * n)`；遞迴堆疊最壞 `O(m * n)`。\n\n## C++17 模板\n\n以下用本節代表題 200. 島嶼數量 展示核心寫法。\n\n**完整題目**：給定由水與陸地組成的網格，計算四方向相連的陸地島嶼數量。\n\n:::example DFS：將與 (r,c) 相連的所有 '1' 標記為 '0'（原地沉島）\n**線索**：題目在矩陣中移動、擴散或統計連通塊；格子可直接當節點，方向陣列就是隱式邊。\n\n```cpp\nclass Solution {\npublic:\n    int numIslands(vector<vector<char>>& grid) {\n            int m = grid.size(), n = grid[0].size(), ans = 0;\n            int dirs[5] = {1, 0, -1, 0,\n                                                                        1};  // 方向陣列，相鄰兩元素構成上下左右四個方向向量\n            // DFS：將與 (r,c) 相連的所有 '1' 標記為 '0'（原地沉島）\n            function<void(int, int)> dfs = [&](int r, int c) {\n                    grid[r][c] = '0';  // 標記已訪問，避免重複計算\n                    for (int k = 0; k < 4; ++k) {\n                            int nr = r + dirs[k], nc = c + dirs[k + 1];\n                            if (0 <= nr && nr < m && 0 <= nc && nc < n && grid[nr][nc] == '1') {\n                                    dfs(nr, nc);\n                            }\n                    }\n            };\n            // 掃描整個網格，每遇到一個未訪問的 '1' 就是一座新島嶼\n            for (int r = 0; r < m; ++r) {\n                    for (int c = 0; c < n; ++c) {\n                            if (grid[r][c] == '1') {\n                                    ans++, dfs(r, c);\n                            }\n                    }\n            }\n            return ans;\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- 每次遇到未訪問陸地就開一個連通塊，DFS 把同島所有格子標記掉。\n- 雙層迴圈枚舉格子，方向陣列負責產生相鄰節點。\n- 修改原網格或標記 visited 的時機，決定同一格是否會被重複處理。\n\n## 時間與空間複雜度\n\n**時間 `O(m × n)`**：每格最多被標記一次、展開一次，每次展開檢查常數個（4 或 8）鄰居。外層迴圈掃過所有格子也是 `O(mn)`。\n\n**空間 `O(m × n)`**：`visited` 矩陣 `O(mn)`（就地改值可省去），加上遞迴堆疊最壞 `O(mn)`——整張圖連成一條長蛇時，遞迴深度等於格子數。\n\n**爆堆疊的實際風險**：`1000 × 1000` 的全陸地網格，遞迴深度可達 `1e6`，多數評測環境會 stack overflow。格子大時改用顯式 `stack` 的迭代 DFS，或改用 BFS（佇列在堆積上，不吃呼叫堆疊）。\n\n417 這類要跑兩次獨立搜尋的題目，兩份 visited 分開存，空間仍是 `O(mn)`。\n\n## 常見錯誤與邊界條件\n\n- 方向陣列保持一致，例如 `{1, 0, -1, 0, 1}`。\n- 遞迴 DFS 在大網格可能爆棧，可改顯式棧。\n- 修改原矩陣前確認題目允許。\n- 狀態含鑰匙/方向時 visited 需升維。\n\n## 常見變形\n\n- DFS 計連通塊與面積。\n- BFS 求最短步數或擴散時間。\n- 帶轉向/代價可轉成 0-1 BFS 或 Dijkstra。\n\n## 與相似技巧的比較\n\n- **對比 BFS**：兩者都能遍歷整個連通塊，複雜度相同。DFS 遞迴好寫、適合「整塊處理」；BFS 逐層擴散、能順便得到最短步數。只要問到「最少幾步」就必須用 BFS。\n- **對比並查集**：並查集也能數連通塊，且支援「動態加邊」（例如 305 島嶼數量 II 逐一填海造陸）。靜態一次性的計數用 DFS 更直接；反覆合併查詢就換並查集。\n- **對比 Flood Fill 的顯式建圖**：把網格真的建成鄰接表是 `O(mn)` 的額外空間與時間，毫無好處。網格圖的鄰居可由座標直接算出，這是它最大的便利。\n- **對比「多源」的反向思考**：130、417 用的不是新演算法，而是換一個起點集合。看到「求被完全包圍 / 碰不到邊界的部分」就先想補集。\n\n## 代表例題\n\n**題號**：130、200、305、417、463\n\n把格子當節點處理；練習明確 visited、方向陣列、障礙與多源/單源的差別。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 130 | [被圍繞的區域](https://leetcode.cn/problems/surrounded-regions) |  |\n| 200 | [島嶼數量](https://leetcode.cn/problems/number-of-islands) |  |\n| 305 | [島嶼數量 II](https://leetcode.cn/problems/number-of-islands-ii)（Premium） |  |\n| 417 | [太平洋大西洋水流問題](https://leetcode.cn/problems/pacific-atlantic-water-flow) |  |\n| 463 | [島嶼的周長](https://leetcode.cn/problems/island-perimeter) |  |\n| 489 | [掃地機器人](https://leetcode.cn/problems/robot-room-cleaner) |  |\n| 529 | [掃雷遊戲](https://leetcode.cn/problems/minesweeper) |  |\n| 695 | [島嶼的最大面積](https://leetcode.cn/problems/max-area-of-island) |  |\n| 733 | [影象渲染](https://leetcode.cn/problems/flood-fill) |  |\n| 827 | [最大人工島](https://leetcode.cn/problems/making-a-large-island) |  |\n| 2061 | [掃地機器人清掃過的空間個數](https://leetcode.cn/problems/number-of-spaces-cleaning-robot-cleaned) |  |\n| 2852 | [所有單元格的遠離程度之和](https://leetcode.cn/problems/sum-of-remoteness-of-all-cells) |  |\n| LCP 63 | [LCP 63. 彈珠遊戲](https://leetcode.cn/problems/EXvqDp) |  |\n| LCS 03 | [LCS 03. 主題空間](https://leetcode.cn/problems/YesdPw) |  |\n| 面試題 16.19 | [面試題 16.19. 水域大小](https://leetcode.cn/problems/pond-sizes-lcci) |  |\n| 3619 | [總價值可以被 K 整除的島嶼數目](https://leetcode.cn/problems/count-islands-with-total-value-divisible-by-k) | 1461 |\n| 2658 | [網格圖中魚的最大數目](https://leetcode.cn/problems/maximum-number-of-fish-in-a-grid) | 1490 |\n| 892 | [三維形體的表面積](https://leetcode.cn/problems/surface-area-of-3d-shapes) | 1508 |\n| 1034 | [邊界著色](https://leetcode.cn/problems/coloring-a-border) | 1579 |\n| 1020 | [飛地的數量](https://leetcode.cn/problems/number-of-enclaves) | 1615 |\n| 2684 | [矩陣中移動的最大次數](https://leetcode.cn/problems/maximum-number-of-moves-in-a-grid) | 1626 |\n| 1254 | [統計封閉島嶼的數目](https://leetcode.cn/problems/number-of-closed-islands) | 1659 |\n| 1905 | [統計子島嶼](https://leetcode.cn/problems/count-sub-islands) | 1679 |\n| 1391 | [檢查網格中是否存在有效路徑](https://leetcode.cn/problems/check-if-there-is-a-valid-path-in-a-grid) | 1746 |\n| 1559 | [二維網格圖中探測環](https://leetcode.cn/problems/detect-cycles-in-2d-grid) | 1838 |\n| 1970 | [你能穿過矩陣的最後一天](https://leetcode.cn/problems/last-day-where-you-can-still-cross) | 2124 |\n\n## 本節重點速查\n\n網格是隱式圖：節點是 `(row, col)`，鄰居由方向陣列算出，不必建圖。DFS 用於「連通塊數量、區塊大小、整塊染色」；問最少步數要改用 BFS。啟動了幾次 DFS 就有幾個連通塊。visited 可另開矩陣或就地改值——後者省空間但破壞輸入，之後還要用原矩陣就不能用。方向陣列固定寫成 `{1, 0, -1, 0, 1}` 減少出錯。時間與空間都是 `O(mn)`；大網格遞迴會爆堆疊，改用顯式 stack。「被圍繞的區域」這類題先從邊界搜尋再取補集。\n",
    },
    {
      id: 4,
      title: "2. 網格圖 BFS",
      summary:
        "## 這個技術解決什麼問題\n\n在二維格子上求「最少幾步」，或「每一格離最近的目標多遠」。\n\nDFS 能走遍連通塊，但走訪順序與距離無關，先碰到終點不代表路徑最短。BFS 換成逐層擴散——先處理離起點 0 步的格子，再 1 步、2 步——於是第一次碰到終點時，步數必然是最少的。\n\n多源版本更是這一節的重點：把所有目標同時當起點丟進佇列，一次擴散就能得到「每格離最近目標的距離」，不必對每個目標各跑一次。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「網格圖 BFS」想：\n\n- **問「最少步數 / 最短距離」，而且每步代價一樣。** 這是 BFS 的招牌題型；只要邊權都相等，BFS 比 Dijkstra 更簡單也更快。\n- **要算「每格離最近的某類格子多遠」。** 用多源 BFS：所有目標同時當起點，一次擴完，例如「牆與門」、「離建築物最近的距離」（317）。\n- **移動規則特別（滾到底、固定方向）。** 「迷宮」系列的球不是一步一格而是滑到撞牆，這時一個「節點」是停下來的位置，邊則是各方向滑一次的結果——觀念不變，只是鄰格的定義換了。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的每步代價真的都一樣嗎？（不一樣就要換 0-1 BFS 或 Dijkstra）\n2. 起點是一個還是一群？（一群就用多源 BFS）\n3. 一個「節點」需要記哪些東西？只有座標，還是要加上方向、鑰匙、停留點？\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n本節要解決的場景是：在一張二維格子上，求「從起點走到終點最少幾步」，或「每一格離最近的某個目標有多遠」。這類「步數最少」的問題，靠的是網格圖的廣度優先搜尋（BFS）。\n\n跟 DFS 一樣，我們把每格 `(row, col)` 看成節點、相鄰格之間有邊。差別在走法：**BFS 像水波一樣一圈一圈往外擴。** 先處理離起點 0 步的格子，再處理 1 步的、2 步的……用一個佇列（queue）排隊，先進先出。\n\n:::example BFS 核心：用 queue 一圈圈往外擴，第一次到達即最短\n為什麼這樣就能得到最短步數？因為每條邊的「成本」都是 1（走一格算一步）。BFS 按距離由近到遠擴展，所以**第一次碰到某格時，那個距離一定就是最短的**，之後再也不必更新它。這就是 BFS 能保證最短路的關鍵前提：每步代價相同。\n\n```cpp\n// BFS 核心：用 queue 一圈圈往外擴，第一次到達即最短\nqueue<pair<int, int>> q;\nq.push({sr, sc});\ndist[sr][sc] = 0;  // 起點距離為 0\nwhile (!q.empty()) {\n    auto [r, c] = q.front();\n    q.pop();\n    for (int k = 0; k < 4; ++k) {\n        int nr = r + dr[k], nc = c + dc[k];\n        if (inGrid(nr, nc) && dist[nr][nc] == -1) {  // 沒走過\n            dist[nr][nc] = dist[r][c] + 1;             // 距離 +1\n            q.push({nr, nc});\n        }\n    }\n}\n```\n:::\n\n\n\n:::example 多源 BFS：一開始就把所有目標塞進佇列、距離設 0\n有一種強力變形叫「多源 BFS」：把**所有**目標格在一開始就全部塞進佇列、距離都設 0，然後一起往外擴。這樣每格自然得到「離最近目標的距離」。「牆與門」（286）正是把所有門當作源頭一次擴出去，比一扇門一扇門分別 BFS 快得多。\n\n```cpp\n// 多源 BFS：一開始就把所有目標塞進佇列、距離設 0\nfor (auto& [r, c] : targets) {\n    dist[r][c] = 0;\n    q.push({r, c});\n}\n// 之後的擴展迴圈與單源完全相同\n```\n:::\n\n## 狀態／資料結構定義\n\n**節點**：`(row, col)`，若移動規則帶額外資訊（方向、鑰匙、停留點）就要升維。\n\n**佇列**：`queue<pair<int,int>>`，或用一維編碼 `row * n + col` 省記憶體。\n\n**距離的兩種記法**：\n- `dist` 矩陣，初始化為 `-1` 表示未訪問，入隊時寫入 `dist[nr][nc] = dist[r][c] + 1`。\n- 「逐層」寫法：外層 `while (!q.empty())` 先取 `size = q.size()`，內層跑完這一整層，步數 `++`。要輸出「第幾層」時比較直觀。\n\n**visited 與 dist 合一**：`dist[r][c] != -1` 本身就是已訪問標記，不必再開一個矩陣。\n\n**多源初始化**：所有源頭在迴圈開始前一次全部入隊、距離設 0。\n\n**特殊移動**（490 迷宮的球會滑到撞牆）：一個「節點」是停下來的位置，鄰居是四個方向各滑一次的落點——資料結構不變，只換鄰居的定義。\n\n## 不變量或正確性證明\n\n**佇列不變量**：任一時刻，佇列中的元素距離最多只有兩種相鄰的值 `d` 與 `d + 1`，且所有 `d` 排在所有 `d + 1` 之前。\n\n用歸納法：初始佇列只有距離 0 的源頭。展開一個距離 `d` 的節點時，新入隊的都是 `d + 1`，接在尾端；而佇列前端剩下的仍是 `d`。不變量保持。\n\n**最短性**：由上述不變量，出隊順序的距離單調不減。因此格子第一次被寫入 `dist` 時得到的值就是最短距離——若存在更短的路徑，那條路徑的前一格距離更小，會更早出隊並更早把它寫入，矛盾。\n\n**這個論證的前提是每步代價相等**。一旦邊權不同（有的 0 有的 1、或任意非負），「先進先出」不再等於「距離由小到大」，不變量立刻破裂，就必須換 0-1 BFS（雙端佇列）或 Dijkstra（優先佇列）。\n\n**多源的正確性**：把所有源頭距離設 0 一起入隊，等價於在圖上加一個虛擬超級源點，連往每個真實源頭且邊權 0。對這個新圖跑單源 BFS，得到的就是「到最近源頭的距離」。\n\n## 逐步演算法\n\n:::example dist 同時當 visited：-1 = 尚未到達\n**1. 準備距離陣列當作 visited。** 用 `dist` 同時記「到沒到過」（-1 表示沒到）和「最短距離」，一石二鳥。\n\n```cpp\n// dist 同時當 visited：-1 = 尚未到達\nvector<vector<int>> dist(m, vector<int>(n, -1));\n```\n:::\n\n\n\n**2. 初始化佇列。** 單源就塞起點；多源就把所有源頭一次塞進去。\n\n:::example 入隊條件：界內 + 可走 + 尚未到達\n**3. 逐層擴展，第一次到達就定案。** 取出隊首、看四個鄰格，只有「在界內、可通行、還沒到過」的才更新距離並入隊。因為步代價都是 1，更新後就不必再改。\n\n```cpp\n// 入隊條件：界內 + 可走 + 尚未到達\nif (inGrid(nr, nc) && g[nr][nc] != WALL && dist[nr][nc] == -1) {\n    dist[nr][nc] = dist[r][c] + 1;\n    q.push({nr, nc});\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- **每步代價不全是 1 就不能用樸素 BFS：** 代價只有 0 或 1 用雙端佇列 0-1 BFS，非負任意權用 Dijkstra。\n- 若到達同一格的「狀態」不只座標（例如「迷宮」490 的球會一路滾到撞牆才停，或帶著鑰匙），visited 要把那些額外資訊一起算進去。\n- 複雜度：每格進出佇列一次，`O(m * n)`。\n\n## C++17 模板\n\n以下用同型延伸示範題 200. 島嶼數量 展示核心寫法。\n\n**完整題目**：給定由水與陸地組成的網格，計算四方向相連的陸地島嶼數量。\n\n:::example DFS：將與 (r,c) 相連的所有 '1' 標記為 '0'（原地沉島）\n**線索**：題目在矩陣中移動、擴散或統計連通塊；格子可直接當節點，方向陣列就是隱式邊。\n\n```cpp\nclass Solution {\npublic:\n    int numIslands(vector<vector<char>>& grid) {\n            int m = grid.size(), n = grid[0].size(), ans = 0;\n            int dirs[5] = {1, 0, -1, 0,\n                                                                        1};  // 方向陣列，相鄰兩元素構成上下左右四個方向向量\n            // DFS：將與 (r,c) 相連的所有 '1' 標記為 '0'（原地沉島）\n            function<void(int, int)> dfs = [&](int r, int c) {\n                    grid[r][c] = '0';  // 標記已訪問，避免重複計算\n                    for (int k = 0; k < 4; ++k) {\n                            int nr = r + dirs[k], nc = c + dirs[k + 1];\n                            if (0 <= nr && nr < m && 0 <= nc && nc < n && grid[nr][nc] == '1') {\n                                    dfs(nr, nc);\n                            }\n                    }\n            };\n            // 掃描整個網格，每遇到一個未訪問的 '1' 就是一座新島嶼\n            for (int r = 0; r < m; ++r) {\n                    for (int c = 0; c < n; ++c) {\n                            if (grid[r][c] == '1') {\n                                    ans++, dfs(r, c);\n                            }\n                    }\n            }\n            return ans;\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- 每次遇到未訪問陸地就開一個連通塊，DFS 把同島所有格子標記掉。\n- 雙層迴圈枚舉格子，方向陣列負責產生相鄰節點。\n- 修改原網格或標記 visited 的時機，決定同一格是否會被重複處理。\n\n## 時間與空間複雜度\n\n**時間 `O(m × n)`**：每格最多入隊一次、出隊一次，每次檢查常數個鄰居。多源不改變這個上界——源頭再多，總入隊次數仍受格子數限制。\n\n**空間 `O(m × n)`**：`dist` 矩陣加上佇列，佇列最壞會裝下一整層（`O(min(m, n))` 到 `O(mn)`）。\n\n**與 DFS 的空間差異**：BFS 的佇列配置在堆積上，不吃呼叫堆疊，因此大網格上不會 stack overflow。這是它在 `1000 × 1000` 級別的題目上比遞迴 DFS 更安全的實務理由。\n\n節點升維時複雜度乘上狀態數：帶 `K` 把鑰匙就是 `O(m × n × 2^K)`，動手前先估算。\n\n## 常見錯誤與邊界條件\n\n- 方向陣列保持一致，例如 `{1, 0, -1, 0, 1}`。\n- 遞迴 DFS 在大網格可能爆棧，可改顯式棧。\n- 修改原矩陣前確認題目允許。\n- 狀態含鑰匙/方向時 visited 需升維。\n\n## 常見變形\n\n- DFS 計連通塊與面積。\n- BFS 求最短步數或擴散時間。\n- 帶轉向/代價可轉成 0-1 BFS 或 Dijkstra。\n\n## 與相似技巧的比較\n\n- **對比 DFS**：同樣 `O(mn)`，但 BFS 額外保證最短。只要問「最少步數」就別用 DFS。反過來，只是要遍歷整塊時 DFS 程式更短。\n- **對比 0-1 BFS**：邊權混有 0 和 1 時樸素 BFS 失效，改用 `deque`：0 邊 `push_front`、1 邊 `push_back`。仍是 `O(mn)`。\n- **對比 Dijkstra**：邊權為任意非負數時才需要優先佇列，代價是多一個 `log`。網格題若每步代價相同，用 Dijkstra 只是白白變慢。\n- **對比多次單源 BFS**：求「離最近的某類格子多遠」時，對每個源頭各跑一次是 `O(源頭數 × mn)`；多源 BFS 一次搞定，是 `O(mn)`。看到「最近的任一個」就該想到多源。\n- **對比雙向 BFS**：起點與終點都明確且圖很大時，從兩端同時擴散可以把搜尋量從 `O(b^d)` 降到 `O(b^(d/2))`，但網格題通常規模不大，用不太到。\n\n## 代表例題\n\n**題號**：286、317、490、499、505\n\n把格子當節點處理；練習明確 visited、方向陣列、障礙與多源/單源的差別。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 286 | [牆與門](https://leetcode.cn/problems/walls-and-gates)（Premium） |  |\n| 317 | [離建築物最近的距離](https://leetcode.cn/problems/shortest-distance-from-all-buildings)（Premium） |  |\n| 490 | [迷宮](https://leetcode.cn/problems/the-maze)（Premium） |  |\n| 499 | [迷宮 III](https://leetcode.cn/problems/the-maze-iii)（Premium） |  |\n| 505 | [迷宮 II](https://leetcode.cn/problems/the-maze-ii)（Premium） |  |\n| 542 | [矩陣](https://leetcode.cn/problems/01-matrix) |  |\n| 675 | [為高爾夫比賽砍樹](https://leetcode.cn/problems/cut-off-trees-for-golf-event) |  |\n| 749 | [隔離病毒](https://leetcode.cn/problems/contain-virus) |  |\n| 909 | [蛇梯棋](https://leetcode.cn/problems/snakes-and-ladders) | 2020 |\n| 934 | [最短的橋](https://leetcode.cn/problems/shortest-bridge) | 1826 |\n| 994 | [腐爛的橘子](https://leetcode.cn/problems/rotting-oranges) | 1433 |\n| 1091 | [二進位制矩陣中的最短路徑](https://leetcode.cn/problems/shortest-path-in-binary-matrix) | 1658 |\n| 1162 | [地圖分析](https://leetcode.cn/problems/as-far-from-land-as-possible) | 1666 |\n| 1210 | [穿過迷宮的最少移動次數](https://leetcode.cn/problems/minimum-moves-to-reach-target-with-rotations) | 2022 |\n| 1293 | [網格中的最短路徑](https://leetcode.cn/problems/shortest-path-in-a-grid-with-obstacles-elimination) | 1967 |\n| 1730 | [獲取食物的最短路徑](https://leetcode.cn/problems/shortest-path-to-get-food) |  |\n| 1765 | [地圖中的最高點](https://leetcode.cn/problems/map-of-highest-peak) | 1783 |\n| 1926 | [迷宮中離入口最近的出口](https://leetcode.cn/problems/nearest-exit-from-entrance-in-maze) | 1638 |\n| 2146 | [價格範圍內最高排名的 K 樣物品](https://leetcode.cn/problems/k-highest-ranked-items-within-a-price-range) | 1837 |\n| 2812 | [找出最安全路徑](https://leetcode.cn/problems/find-the-safest-path-in-a-grid) | 2154 |\n| 2814 | [避免淹死併到達目的地的最短時間](https://leetcode.cn/problems/minimum-time-takes-to-reach-destination-without-drowning) |  |\n\n## 本節重點速查\n\nBFS 像水波逐層外擴，佇列裡的距離最多只有 `d` 和 `d + 1` 兩種且有序——這個不變量就是「第一次到達即最短」的證明。**前提是每步代價相等**：混有 0/1 邊改用 deque（0 邊 push_front），任意非負權改用 Dijkstra。求「離最近的某類格子多遠」用多源 BFS：所有源頭距離設 0 一起入隊，等價於加一個虛擬超級源點，`O(mn)` 一次算完。`dist` 初始化 `-1` 兼作 visited。移動規則特殊時（滑到撞牆）只換鄰居定義，演算法不變。\n",
    },
    {
      id: 5,
      title: "3. 0-1 BFS",
      summary:
        "## 這個技術解決什麼問題\n\n邊權只有 0 和 1 兩種的最短路。\n\n典型場景是「某些移動免費、某些移動要付 1 次代價」：沿著箭頭走免費、改方向花 1（1368）；同跑道往前免費、側跳花 1（1824）；走空地免費、拆障礙花 1（2290）。\n\n樸素 BFS 在這裡會算錯，因為它假設每步代價都是 1；Dijkstra 能算對，但要付一個 `log` 的堆開銷。0-1 BFS 用一個雙端佇列取代優先佇列，在這個特例上做到與 BFS 相同的線性複雜度。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「0-1 BFS」想：\n\n- **求最短路 / 最小代價，而每一步的花費只有兩種：0 或 1。** 「改方向花 1、順方向免費」「換道花 1、直行免費」「移除障礙花 1、空地免費」（如「到達角落需要移除障礙物的最小數目」2290）都是經典訊號。\n- **本來想用 Dijkstra，但發現權重特別單純（只有 0 和 1）。** 這時 0-1 BFS 是更輕量的替代品，少了堆、常數更小。\n- **題目強調「最少幾次某種特殊操作」（側跳、轉向、拆牆）。** 那個特殊操作就是權重 1 的邊，其餘正常移動是權重 0 的邊。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的邊權是不是真的只有 0 和 1？有沒有混進其它數值？\n2. 哪種移動是 0、哪種是 1？我能一句話講清楚嗎？\n3. 我用 deque 了嗎？0 邊有沒有 `push_front`、1 邊有沒有 `push_back`？\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n本節要解決的場景是：在格子（或圖）上求最短路，但「走某些步是免費的、走另一些步要付 1 元」。例如「使網格圖至少有一條有效路徑的最小代價」（1368）：每格本來指向一個方向，沿著它指的方向走不用錢，要改方向才花 1。這種**邊權只有 0 或 1** 的最短路，用 0-1 BFS 最漂亮。\n\n先回顧：普通 BFS 之所以能保證最短，是因為每步代價都是 1，用一般佇列「先進先出」就能讓距離由小到大被取出。但現在有 0 邊，走 0 邊不該增加距離——如果還用普通佇列，距離小的格子可能排在距離大的後面，順序就亂了。\n\n:::example 0-1 BFS 核心：0 邊走前門(push_front)，1 邊走後門(push_ba…\n0-1 BFS 的核心想法只有一句話：**改用雙端佇列（deque）；走 0 邊就放到隊首，走 1 邊就放到隊尾。** 這樣「距離小的永遠在前面」這個性質就被保住了，效果等同 Dijkstra，但因為只有兩種權重，免去了堆（heap）的 `log` 開銷。\n\n```cpp\n// 0-1 BFS 核心：0 邊走前門(push_front)，1 邊走後門(push_back)\ndeque<pair<int, int>> dq;\ndq.push_front({sr, sc});\ndist[sr][sc] = 0;\nwhile (!dq.empty()) {\n    auto [r, c] = dq.front();\n    dq.pop_front();\n    for (int k = 0; k < 4; ++k) {\n        int nr = r + dr[k], nc = c + dc[k];\n        if (!inGrid(nr, nc)) {\n            continue;\n        }\n        int w = (k == g[r][c] ? 0 : 1);  // 順著箭頭=0，改向=1\n        if (dist[r][c] + w < dist[nr][nc]) {\n            dist[nr][nc] = dist[r][c] + w;\n            if (w == 0) {\n                dq.push_front({nr, nc});  // 免費 → 插隊到最前\n            } else {\n                dq.push_back({nr, nc});  // 付費 → 排到最後\n            }\n        }\n    }\n}\n```\n:::\n\n\n\n為什麼正確？deque 裡任一時刻的距離最多只有兩種相鄰的值（像 d 和 d+1）。把 0 邊放前面、1 邊放後面，剛好維持「從前到後距離不下降」。於是第一次彈出某格時，它的距離已是最短——這和 Dijkstra「每次取最小」是一樣的保證。\n\n## 狀態／資料結構定義\n\n**雙端佇列** `deque<pair<int,int>>` 取代普通佇列，這是與樸素 BFS 唯一的結構差異。\n\n**入隊規則**：走 0 邊 `push_front`，走 1 邊 `push_back`。\n\n**dist 矩陣**：初始化為 `INT_MAX`，源點為 0。與樸素 BFS 不同的是，**`dist` 可能被鬆弛多次**，所以不能把「寫過 dist」當成 visited。\n\n**出隊時的過期檢查**：同一格可能以不同距離被放進 deque 多次。出隊時若 `d > dist[r][c]` 就直接跳過這個過期項目——這與 Dijkstra 的懶刪除是同一招。\n\n**先把「免費」與「付費」寫成一句話**再動手：哪種移動是 0、哪種是 1。這一句沒想清楚，後面一定寫錯。\n\n## 不變量或正確性證明\n\n**deque 不變量**：任一時刻，deque 中節點的暫定距離最多只有兩種相鄰的值 `d` 與 `d + 1`，且所有 `d` 排在所有 `d + 1` 之前。\n\n歸納：展開一個距離 `d` 的節點時，0 邊產生距離 `d` 的節點放到**前端**（與現有的 `d` 同組），1 邊產生距離 `d + 1` 的節點放到**後端**（與現有的 `d + 1` 同組）。兩種操作都維持了「前小後大、至多兩層」的排列。\n\n**最短性**：由不變量，出隊順序的距離單調不減。因此第一次出隊某節點時，它的 `dist` 已經是最短——這與 Dijkstra「每次取全域最小」的保證完全相同，只是因為距離值域只有兩層，用 deque 就能取代優先佇列。\n\n**為什麼 0 邊必須 push_front**：若把 0 邊也放後端，一個距離仍是 `d` 的節點會排在既有的 `d + 1` 節點之後，單調性破裂，就可能先定案一條較長的路徑。\n\n**為什麼要過期檢查**：允許同一節點多次入隊，是為了避免實作一個支援 decrease-key 的結構。多餘的舊項目出隊時距離較大，跳過即可，不影響正確性。\n\n## 逐步演算法\n\n**1. 把「免費」和「付費」分清楚。** 動手前先想清楚：哪種移動算 0、哪種算 1。「最少側跳次數」（1824）裡，沿同一跑道往前是 0，換跑道（側跳）是 1。\n\n:::example 初始化：所有格距離設為極大，起點為 0\n**2. dist 初始化為無限大，鬆弛時才更新。** 跟 Dijkstra 一樣，看到更短的距離才改、才入隊。\n\n```cpp\n// 初始化：所有格距離設為極大，起點為 0\nvector<vector<int>> dist(m, vector<int>(n, INT_MAX));\ndist[sr][sc] = 0;\n```\n:::\n\n\n\n:::example 0 邊插隊首，1 邊排隊尾——這一行就是 0-1 BFS 的靈魂\n**3. 入隊位置由邊權決定。** 這是與普通 BFS 唯一的關鍵差別。\n\n```cpp\n// 0 邊插隊首，1 邊排隊尾——這一行就是 0-1 BFS 的靈魂\nif (w == 0) {\n    dq.push_front({nr, nc});\n} else {\n    dq.push_back({nr, nc});\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 同一格可能被放進 deque 多次（距離被更新就再放一次），所以**判斷終止要靠「彈出時距離是否仍最小」**，或在彈出時略過已過期的舊紀錄。\n- 一定要確認權重「真的只有 0 和 1」。若出現 2、3 等其它值，0-1 BFS 不再正確，得換成 Dijkstra。\n- 複雜度：每條邊最多處理常數次，`O(節點數 + 邊數)`，在格子上即 `O(m * n)`，比 Dijkstra 的 `O(m * n * log)` 更快。\n\n## C++17 模板\n\n以 2290. 到達角落需要移除障礙物的最小數目 為例。\n\n**完整題目**：給 `m × n` 的 `grid`，`0` 是空地、`1` 是障礙。從左上角走到右下角（四方向移動），可以移除任意數量的障礙，求最少要移除幾個。\n\n**線索**：走進空地不花錢、走進障礙花 1——邊權只有 0 和 1，正是 0-1 BFS 的形狀。\n\n```cpp\n// 2290. 到達角落需要移除障礙物的最小數目：0-1 BFS\nclass Solution {\npublic:\n    int minimumObstacles(vector<vector<int>>& grid) {\n        const int m = grid.size(), n = grid[0].size();\n        const int dirs[5] = {1, 0, -1, 0, 1};\n        vector<vector<int>> dist(m, vector<int>(n, INT_MAX));\n\n        deque<pair<int, int>> dq;\n        dist[0][0] = 0;\n        dq.emplace_back(0, 0);\n\n        while (!dq.empty()) {\n            auto [r, c] = dq.front();\n            dq.pop_front();\n\n            for (int k = 0; k < 4; ++k) {\n                int nr = r + dirs[k], nc = c + dirs[k + 1];\n                if (nr < 0 || nr >= m || nc < 0 || nc >= n) continue;\n\n                int w = grid[nr][nc];  // 空地 0、障礙 1，就是這條邊的權重\n                if (dist[r][c] + w >= dist[nr][nc]) continue;  // 沒有變短就不鬆弛\n                dist[nr][nc] = dist[r][c] + w;\n\n                // 0 邊放前端、1 邊放後端 —— deque 因此只含 d 與 d+1 兩層\n                if (w == 0) dq.emplace_front(nr, nc);\n                else dq.emplace_back(nr, nc);\n            }\n        }\n        return dist[m - 1][n - 1];\n    }\n};\n```\n\n1368（改方向花 1）、1824（側跳花 1）、3286（扣血花 1）都只是把 `w` 的算法換掉，骨架一字不改。\n\n## 時間與空間複雜度\n\n**時間 `O(V + E)`**，在網格上就是 `O(m × n)`：每條邊最多觸發一次入隊，每個項目最多出隊一次，過期檢查是 `O(1)`。\n\n**空間 `O(V)`**：`dist` 矩陣加上 deque，deque 最壞會同時裝下兩層節點。\n\n**與 Dijkstra 的實際差距**：Dijkstra 是 `O(E log V)`。當邊權只有 0/1 時，0-1 BFS 省掉的不只是漸進的 `log`，還有優先佇列較大的常數，實測通常快數倍。\n\n**邊權不只 0/1 就不能用**。若出現權重 2，deque 裡會同時存在三層距離，不變量破裂。此時要嘛拆點（把權重 2 的邊拆成兩條權重 1 的邊，加一個中繼節點），要嘛老實換 Dijkstra。\n\n## 常見錯誤與邊界條件\n\n- **0 邊用 `push_back`**。這是最致命也最難察覺的錯——程式仍會跑完並輸出一個看似合理的答案，只是不保證最短。0 邊一定要 `push_front`。\n- **把「寫過 dist」當成 visited**。0-1 BFS 允許同一格被多次鬆弛，用 visited 提早封鎖會漏掉更短的路徑。判斷條件要寫成「新距離嚴格更小才鬆弛並入隊」。\n- **忘記過期檢查**。若採用「出隊時再檢查」的寫法，必須比對 `d > dist[r][c]` 並跳過；混用兩種寫法（入隊時鬆弛又不檢查）會重複展開，效能退化。\n- **權重取錯位置**。代價通常掛在「走進去的那一格」（`grid[nr][nc]`）而不是「離開的那一格」。起點自己的障礙值一般不計，動手前先確認題目怎麼算。\n- **邊權混進 2 以上**。deque 的兩層不變量立刻破裂。要嘛拆點成多條權重 1 的邊，要嘛改用 Dijkstra。\n- **`dist` 初始值溢位**。用 `INT_MAX` 當初值時，`dist[r][c] + w` 可能溢位；先判斷 `dist[r][c] == INT_MAX` 就跳過，或改用 `0x3f3f3f3f`。\n- **起點終點重合或 `1 × 1` 網格**：答案是 0，別讓迴圈邏輯漏掉這個情形。\n\n## 與相似技巧的比較\n\n- **對比樸素 BFS**：資料結構從 queue 換成 deque，多了 `push_front`。樸素 BFS 是 0-1 BFS 在「沒有 0 邊」時的特例。\n- **對比 Dijkstra**：兩者的正確性論證相同（每次取出的都是全域最小距離），差別只在取最小值的方式。0/1 權用 deque 是 `O(1)`，任意非負權要用堆是 `O(log V)`。0-1 BFS 可以視為 Dijkstra 在桶數只有 2 時的退化版。\n- **對比分層圖**：「最多改 k 次方向」這類題，可以把狀態擴成 `(格子, 已用次數)` 跑樸素 BFS，狀態數乘 `k`；用 0-1 BFS 則把「改方向」直接編碼成權重 1 的邊，不必擴維。兩者都對，看哪個模型更好寫。\n- **對比 Dial 演算法**：邊權在 `0..C` 的小整數範圍時，用 `C + 1` 個桶輪流取，是 0-1 BFS 的自然推廣。\n\n## 代表例題\n\n**題號**：56、LCP 56、1368、1824、2290\n\n把格子當節點處理；練習明確 visited、方向陣列、障礙與多源/單源的差別。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 56 | [LCP 56. 信物傳送](https://leetcode.cn/problems/6UEx57/) |  |\n| LCP 56 | [LCP 56. 信物傳送](https://leetcode.cn/problems/6UEx57) |  |\n| 1368 | [使網格圖至少有一條有效路徑的最小代價](https://leetcode.cn/problems/minimum-cost-to-make-at-least-one-valid-path-in-a-grid) | 2069 |\n| 1824 | [最少側跳次數](https://leetcode.cn/problems/minimum-sideway-jumps) | 1778 |\n| 2290 | [到達角落需要移除障礙物的最小數目](https://leetcode.cn/problems/minimum-obstacle-removal-to-reach-corner) | 2138 |\n| 3286 | [穿越網格圖的安全路徑](https://leetcode.cn/problems/find-a-safe-walk-through-a-grid) | 1608 |\n| 3552 | [網格傳送門旅遊](https://leetcode.cn/problems/grid-teleportation-traversal) | 2036 |\n\n## 本節重點速查\n\n邊權只有 0 和 1 時，用 `deque` 取代 queue：**0 邊 `push_front`、1 邊 `push_back`**。這讓 deque 裡永遠只有 `d` 與 `d + 1` 兩層且前小後大，出隊順序因此單調不減，第一次出隊即最短——與 Dijkstra 同樣的保證，但省掉 `log`。`dist` 會被多次鬆弛，不能當 visited 用；出隊時要檢查 `d > dist[...]` 並跳過過期項目。動手前先用一句話講清楚「哪種移動免費、哪種付費」。出現權重 2 以上就不能用，改拆點或 Dijkstra。時間 `O(mn)`。\n",
    },
    {
      id: 6,
      title: "4. 綜合應用",
      summary:
        "## 這個技術解決什麼問題\n\n格子上的搜尋，但「座標」已不足以描述一次造訪。\n\n當題目加上會累積或改變的附加條件——蒐集到哪些鑰匙、剩多少能量、現在第幾個時刻、面朝哪個方向——同一格在不同條件下就是**不同的節點**，可以各自被訪問一次。把附加條件漏出節點之外，BFS 會把本該分開的情況誤當成同一格，於是提早剪掉正解。\n\n這一節其實沒有新演算法，只有一個觀念升級：**先把節點定義對，再套前三節的 DFS / BFS / 0-1 BFS。**\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「綜合應用（帶狀態的網格搜尋）」想：\n\n- **在格子上移動，但有「會累積或改變的附加條件」。** 蒐集機關 / 鑰匙、限定步數或能量、地形隨時間變化，都意味著座標之外還要再記東西。\n- **同一格允許用不同方式重複經過。** 一旦「重複到達同格但情況不同還算有意義」，就代表 visited 必須加上狀態維度。\n- **要蒐集的目標數量很少（個位數到十幾個）。** 這通常是 bitmask 的強烈暗示：用一個整數的每個位元代表一個目標的有無。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的節點除了座標，還必須記哪些量才不會算錯？\n2. 這些額外狀態怎麼編碼？我的 visited 維度有沒有跟著加上去？\n3. 全部狀態數乘起來大概多少？會不會超出時間或記憶體？\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n本節要解決的場景是：題目仍是在格子上移動，但加了一層「狀態」，使得單純記座標已經不夠。最典型的就是 LCP 系列尋寶類題目——「尋寶」（LCP 13）、「變換的迷宮」（LCP 31）、「腳踏車炫技賽場」（LCP 45）——它們把 BFS / DFS 與額外資訊（拿到哪些機關、第幾個時刻、剩多少能量）綜合在一起。\n\n關鍵觀念升級成一句話：**節點不再只是 `(row, col)`，而是 `(row, col, 額外狀態)`。** 同一格在「沒拿鑰匙」和「拿了鑰匙」兩種情況下，是兩個不同的節點，可以各自被訪問一次。沒有把額外狀態放進節點，BFS 就會把本該分開的兩種情況誤當成同一格而漏算。\n\n:::example 用 bitmask 表示「已蒐集哪些」：第 i 位為 1 = 第 i 樣到手\n一個常見的額外狀態是「已蒐集的物品集合」，用位元遮罩（bitmask）表示：第 i 位是 1 代表第 i 樣已拿到。\n\n```cpp\n// 用 bitmask 表示「已蒐集哪些」：第 i 位為 1 = 第 i 樣到手\nint mask = 0;\nmask |= (1 << i);            // 標記拿到第 i 樣\nbool got = mask & (1 << i);  // 檢查第 i 樣有沒有\n```\n:::\n\n\n\n:::example visited 對 (座標, mask) 整組標記，而非只看座標\n於是 visited 要對「座標 + 狀態」整組去重，而不是只看座標：\n\n```cpp\n// visited 對 (座標, mask) 整組標記，而非只看座標\n// dim: 行數 m、列數 n、狀態種類 1<<K\nbool visited[m][n][1 << K];\nif (!visited[r][c][mask]) {\n    visited[r][c][mask] = true;\n    // ... 入隊 / 遞迴\n}\n```\n:::\n\n\n\n時間也可能是狀態的一部分。「變換的迷宮」裡牆會隨時刻開合，所以「同一格在第 3 秒」和「在第 5 秒」是不同節點，節點得帶上時間。\n\n## 狀態／資料結構定義\n\n**節點** = `(row, col, 附加狀態)`。常見的附加狀態與編碼方式：\n\n- **蒐集集合**（拿到哪些鑰匙／機關）→ bitmask。目標數 `K` 通常在十幾個以內，用一個 `int` 的第 `i` 位表示第 `i` 個目標是否已取得。題目給出「物品數很小」本身就是 bitmask 的強烈暗示。\n- **時間**（牆會隨時刻開合，LCP 31）→ 時刻 `t`，通常對週期取模避免無限膨脹。\n- **資源餘額**（剩幾次跳躍、剩多少電量）→ 直接當一維，並注意「同格但餘額更多」嚴格更優，可用來剪枝。\n- **方向**（轉彎要計費）→ 4 種朝向。\n\n**visited 必須與節點同維**：`visited[row][col][mask]`。維度漏一層，就等於宣告那個狀態不影響後續決策——那通常正是錯誤的來源。\n\n**估算狀態總數**：`m × n × 狀態種類`。`50 × 50 × 2^13 ≈ 2000 萬`，勉強可行；再多一位就要重新設計。動手前一定要先乘一遍。\n\n## 不變量或正確性證明\n\n**擴維後的正確性**：把 `(row, col, s)` 當成新圖的節點、把「移動並更新狀態」當成新圖的邊，這張擴展圖上的最短路就是原問題的答案。前三節的所有論證原封不動適用——BFS 的逐層不變量、0-1 BFS 的兩層 deque 不變量，都只依賴「節點」與「邊權」，與節點內部長什麼樣無關。\n\n**為什麼 visited 必須含狀態**：設 `(r, c, s1)` 已訪問，現在要訪問 `(r, c, s2)` 且 `s1 != s2`。若 visited 只記座標就會跳過它。但從 `s2` 出發可達的狀態集合，未必被 `s1` 涵蓋（例如 `s2` 多一把鑰匙，能開 `s1` 開不了的門），因此可能漏掉唯一的解。\n\n**狀態單調時可以剪枝**：若附加狀態存在偏序，且「更優的狀態可達的解集合包含較差狀態的」，就能只保留 Pareto 最優解。例如「剩餘能量」越多越好——同格同時刻下，能量較低者可以直接丟棄。這個剪枝的正確性需要單獨論證，不能想當然。\n\n**時間為狀態時的終止性**：牆的開合有週期 `T`，則 `(r, c, t)` 與 `(r, c, t + T)` 等價，對 `T` 取模後狀態數有限，搜尋必然終止。\n\n## 逐步演算法\n\n**1. 先寫清楚「哪些量會變」。** 動手前列出：座標一定要、再加上會影響後續決策的量（拿了哪些、剩幾次某資源、現在第幾時刻、面朝哪個方向）。這串東西合起來就是你的節點。\n\n:::example 把 (r, c, mask) 編碼成一個整數，方便當 key\n**2. 設計狀態編碼。** 把額外狀態壓成整數，方便當陣列索引或塞進 set。\n\n```cpp\n// 把 (r, c, mask) 編碼成一個整數，方便當 key\nint encode(int r, int c, int mask, int n, int K) {\n    return (r * n + c) * (1 << K) + mask;\n}\n```\n:::\n\n\n\n:::example 擴展時：座標走一步，順手更新 mask（踩到第 i 樣就點亮該位）\n**3. 選對搜尋骨架。** 求最短時間 / 步數用 BFS（每步代價相同）或 Dijkstra（代價不同）；只問可不可達、要列舉所有走法用 DFS。骨架不變，差別只在「擴展鄰居時順手更新額外狀態」。\n\n```cpp\n// 擴展時：座標走一步，順手更新 mask（踩到第 i 樣就點亮該位）\nint nmask = mask;\nif (item[nr][nc] >= 0) {\n    nmask |= (1 << item[nr][nc]);\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- visited 維度要與狀態維度一致；漏掉一維就會把不同狀態混為一談而出錯。\n- 狀態數會乘起來：格子 `m * n` 再乘狀態種類 `2^K`，K 一大就會爆。所以這類題的物品數通常很小（十幾個以內），正是暗示你可以用 bitmask。\n- 複雜度：大約 `O(m * n * 狀態數 * 鄰居數)`，務必先估算別超記憶體。\n\n## C++17 模板\n\n這類題沒有單一模板題，但骨架是固定的：**把附加狀態編進節點，其餘照抄 BFS**。以最常見的「蒐集鑰匙」型（bitmask 狀態）為例：\n\n```cpp\n// 帶狀態的網格 BFS 骨架：節點是 (row, col, mask)\n// mask 的第 i 位 = 第 i 把鑰匙是否已取得\nint shortestPathWithKeys(vector<string>& grid, int startR, int startC, int keyCount) {\n    const int m = grid.size(), n = grid[0].size();\n    const int full = (1 << keyCount) - 1;  // 全部蒐集完的目標狀態\n    const int dirs[5] = {1, 0, -1, 0, 1};\n\n    // visited 必須與節點同維，漏掉 mask 這一維就會把不同狀態混為一談\n    vector visited(m, vector(n, vector<char>(1 << keyCount, 0)));\n\n    queue<tuple<int, int, int>> q;\n    q.emplace(startR, startC, 0);\n    visited[startR][startC][0] = 1;\n\n    for (int steps = 0; !q.empty(); ++steps) {\n        for (int sz = q.size(); sz > 0; --sz) {  // 逐層擴散，steps 即為層數\n            auto [r, c, mask] = q.front();\n            q.pop();\n            if (mask == full) return steps;\n\n            for (int k = 0; k < 4; ++k) {\n                int nr = r + dirs[k], nc = c + dirs[k + 1];\n                if (nr < 0 || nr >= m || nc < 0 || nc >= n) continue;\n\n                char cell = grid[nr][nc];\n                if (cell == '#') continue;                        // 牆\n                int next = mask;\n                if (islower(cell)) next |= 1 << (cell - 'a');     // 撿鑰匙：更新狀態\n                if (isupper(cell) && !(next >> (cell - 'A') & 1)) continue;  // 沒鑰匙開不了門\n\n                if (visited[nr][nc][next]) continue;\n                visited[nr][nc][next] = 1;\n                q.emplace(nr, nc, next);\n            }\n        }\n    }\n    return -1;\n}\n```\n\n換一種附加狀態，只要換掉 `next` 的算法與 visited 的維度：\n\n- **LCP 31 變換的迷宮**：狀態是 `(時刻 t, 剩餘臨時消除次數, 剩餘永久消除次數)`，`t` 對迷宮的週期取模。\n- **LCP 45 腳踏車炫技賽場**：狀態是「目前速度」，鄰居由速度決定；同格同速度只需訪問一次。\n- **LCP 75 傳送卷軸**：對手會挑最壞的傳送點，是「你走最短、對手取最大」的極小極大，先用 BFS 預處理每格到終點的距離，再二分答案。\n- **LCP 13 尋寶**：機關數 16，直接擴維會爆；要先用 BFS 求出關鍵點兩兩之間的最短路，再在關鍵點上做狀態壓縮 DP。\n\n## 時間與空間複雜度\n\n**時間 `O(m × n × S × D)`**，其中 `S` 是附加狀態的種類數、`D` 是每個節點的鄰居數（網格上是 4 或 8）。每個擴展節點最多展開一次。\n\n**空間 `O(m × n × S)`**：visited 與 dist 都要開到滿維度。\n\n典型量級：\n- 鑰匙題 `K = 6` → `S = 64`，`50 × 50 × 64 = 16 萬`，輕鬆。\n- LCP 13 尋寶 `K = 16` → `S = 65536`，配上 `m × n` 就會爆——這類題通常還要先把「格子」壓縮成「關鍵點之間的最短路」，再在關鍵點上跑狀態壓縮 DP，不能直接在原網格上擴維。\n\n**這是本節最容易踩的坑**：擴維在數學上永遠正確，但狀態數是乘法成長的。先估算，再決定是直接擴維，還是要先做一次預處理把圖縮小。\n\n## 常見錯誤與邊界條件\n\n- **visited 漏掉狀態維度**。本節最常見、也最難除錯的錯：`visited[r][c]` 會讓「拿了鑰匙的自己」被「沒拿鑰匙的自己」擋在門外，答案偏大或直接回 `-1`。維度一定要跟節點一致。\n- **沒有先估算狀態總數**。`m × n × 2^K` 是乘法成長，`K = 16` 時 `2^16 = 65536`，配上 `50 × 50` 就是 1.6 億個節點，必然 TLE/MLE。動手前先乘一遍；太大就要改成「關鍵點最短路 + 狀態壓縮 DP」。\n- **狀態更新的順序寫反**。撿鑰匙要在「檢查門」之前更新 `mask`（同一格既是鑰匙又要通行時），或依題意明確定義先後。這個順序錯了只在少數測資上失敗，很難查。\n- **時間狀態忘了取模**。牆隨時刻開合時，`t` 不取週期模數會讓狀態數無限成長，佇列永遠不空。\n- **在擴展圖上誤用「同格只走一次」的直覺**。原網格的直覺在擴展圖上不成立——同一格被訪問 `2^K` 次是正常的。\n- **資源型狀態的剪枝未經證明**。「同格同時刻、能量較低者直接丟棄」需要「能量越多可達解越多」這個單調性成立才對，套用前先確認題目沒有「能量過多反而不行」的規則。\n- **多組獨立搜尋共用 visited**。像 417 那樣要跑兩次的題目，兩次的 visited 要分開或重置。\n\n## 與相似技巧的比較\n\n- **對比前三節**：演算法完全相同，只有節點定義變了。判斷依據就一句話——「同一格用不同方式到達，之後的走法會不會不同？」會，就得擴維。\n- **對比狀態壓縮 DP**：兩者都用 bitmask 表示「已取得的集合」。差別在 BFS 求的是最短步數且轉移由圖結構決定；狀壓 DP 通常先用 BFS/Floyd 預處理出關鍵點之間的距離，再在集合上做 DP。LCP 13 這類「格子大、關鍵點少」的題必須走後者。\n- **對比 A\\* 與啟發式搜尋**：狀態空間很大時可用估價函數優先展開有希望的節點。正確性要求估價函數不高估（admissible），實作與除錯成本高，競賽網格題通常用不到。\n- **對比記憶化搜尋**：附加狀態是「剩餘資源」且問的是可行性而非最短步數時，改寫成 `dfs(row, col, remaining)` 加記憶化往往更直觀。\n\n## 代表例題\n\n**題號**：13、LCP 13、31、LCP 31、45\n\n把格子當節點處理；練習明確 visited、方向陣列、障礙與多源/單源的差別。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 13 | [LCP 13. 尋寶](https://leetcode.cn/problems/xun-bao/) |  |\n| LCP 13 | [LCP 13. 尋寶](https://leetcode.cn/problems/xun-bao) |  |\n| 31 | [LCP 31. 變換的迷宮](https://leetcode.cn/problems/Db3wC1/) |  |\n| LCP 31 | [LCP 31. 變換的迷宮](https://leetcode.cn/problems/Db3wC1) |  |\n| 45 | [LCP 45. 腳踏車炫技賽場](https://leetcode.cn/problems/kplEvH/) |  |\n| LCP 45 | [LCP 45. 腳踏車炫技賽場](https://leetcode.cn/problems/kplEvH) |  |\n| 75 | [LCP 75. 傳送卷軸](https://leetcode.cn/problems/rdmXM7/) |  |\n| LCP 75 | [LCP 75. 傳送卷軸](https://leetcode.cn/problems/rdmXM7) |  |\n| 329 | [矩陣中的最長遞增路徑](https://leetcode.cn/problems/longest-increasing-path-in-a-matrix) |  |\n| 694 | [不同島嶼的數量](https://leetcode.cn/problems/number-of-distinct-islands) |  |\n| 711 | [不同島嶼的數量 II](https://leetcode.cn/problems/number-of-distinct-islands-ii) |  |\n| 778 | [水位上升的泳池中游泳](https://leetcode.cn/problems/swim-in-rising-water) |  |\n| 864 | [獲取所有鑰匙的最短路徑](https://leetcode.cn/problems/shortest-path-to-get-all-keys) | 2259 |\n| 1036 | [逃離大迷宮](https://leetcode.cn/problems/escape-a-large-maze) | 2165 |\n| 1102 | [得分最高的路徑](https://leetcode.cn/problems/path-with-maximum-minimum-value) | 2011 |\n| 1263 | [推箱子](https://leetcode.cn/problems/minimum-moves-to-move-a-box-to-their-target-location) | 2297 |\n| 1631 | [最小體力消耗路徑](https://leetcode.cn/problems/path-with-minimum-effort) | 1948 |\n| 1778 | [未知網格中的最短路徑](https://leetcode.cn/problems/shortest-path-in-a-hidden-grid) |  |\n| 2258 | [逃離火災](https://leetcode.cn/problems/escape-the-spreading-fire) | 2347 |\n| 2556 | [二進位制矩陣中翻轉最多一次使路徑不連通](https://leetcode.cn/problems/disconnect-path-in-a-binary-matrix-by-at-most-one-flip) | 2369 |\n| 2577 | [在網格圖中訪問一個格子的最少時間](https://leetcode.cn/problems/minimum-time-to-visit-a-cell-in-a-grid) | 2382 |\n| 2617 | [網格圖中最少訪問的格子數](https://leetcode.cn/problems/minimum-number-of-visited-cells-in-a-grid) | 2582 |\n| 3568 | [清理教室的最少移動](https://leetcode.cn/problems/minimum-moves-to-clean-the-classroom) | 2143 |\n\n## 本節重點速查\n\n**節點不再只是 `(row, col)`，而是 `(row, col, 附加狀態)`**——蒐集集合用 bitmask、時刻對週期取模、資源餘額直接當一維。visited 必須同維，漏一層就會把不同狀態混為一談而漏解。演算法本身完全不變，前三節的 DFS / BFS / 0-1 BFS 直接套用，因為它們的正確性論證只依賴「節點」與「邊權」。最大的坑是狀態數乘法成長：先算 `m × n × S` 再動手；`K` 一大（如 LCP 13 的 16 個機關）就不能直接擴維，要先把格子壓成關鍵點間的最短路再跑狀態壓縮 DP。\n",
    },
    {
      id: 9010,
      title: "5. 常見面試模式",
      summary:
        "網格圖題本質上是把二維矩陣看成隱式的圖，每個格子是點、相鄰格子是邊。下表整理常見模式與其訊號，遇到時可快速對照解法。\n\n| 模式 | 訊號 | 核心做法 | 代表題 |\n| --- | --- | --- | --- |\n| 洪水填充 | 求連通塊、區域著色、計算島嶼 | 從某格 DFS/BFS 擴散並標記已訪問，時間 `O(mn)` | [島嶼數量](https://leetcode.cn/problems/number-of-islands) |\n| 多源 BFS | 多個起點同時擴散、求最近距離 | 把所有源點先入佇列再一起 BFS，時間 `O(mn)` | [腐爛的橘子](https://leetcode.cn/problems/rotting-oranges) |\n| 最短路 BFS | 邊權皆為 1 的最短步數 | 單源 BFS 逐層擴散，首次抵達即最短，時間 `O(mn)` | [二進位矩陣中的最短路徑](https://leetcode.cn/problems/shortest-path-in-binary-matrix) |\n| 帶狀態 BFS / Dijkstra | 狀態含位置加額外資訊（鑰匙、體力） | 把狀態擴充進佇列節點，邊權不一時改用 Dijkstra | [獲取所有鑰匙的最短路徑](https://leetcode.cn/problems/shortest-path-to-get-all-keys) |\n| 回溯 | 需在網格上搜尋路徑且可走過再復原 | DFS 嘗試每個方向，遞迴前標記、回溯時還原 | [單字搜尋](https://leetcode.cn/problems/word-search) |\n| 輪廓線 DP | 逐格鋪設、相鄰格互相約束 | 以一條輪廓線狀態壓縮，逐格轉移，狀態約 $O(mn \\cdot 2^m)$ | [鋪瓷磚](https://leetcode.cn/problems/tiling-a-rectangle-with-the-fewest-squares) |\n\n**小提醒**\n\n- 用一個方向陣列 `dirs = {{-1, 0}, {1, 0}, {0, -1}, {0, 1}}` 統一處理上下左右移動，程式更簡潔。\n- 多源 BFS 與其逐個源點各跑一次，不如把所有源點一次性入佇列，時間直接降到 `O(mn)`。\n- 訪問標記可直接改寫原網格（如標為 `0`）以省去額外的 `visited` 陣列。",
    },
  ],
} as TutorialData.Root;
