import type { TutorialData } from "@/types";

export const dataStructure = {
  id: 1,
  title: "資料結構（字首和/堆疊/佇列/堆/字典樹/並查集/樹狀陣列/線段樹）",
  src: "https://leetcode.cn/circle/discuss/mOr1u6/",
  last_update: "2026-02-24T15:43:15.314902+00:00",
  summary:
    "![資料結構題單](https://pic.leetcode.cn/1713836847-cDYwhf-Programming.jfif)\n\n**本題單涵蓋**：\n- 列舉技巧：列舉右維護左、列舉中間\n- 字首和/差分：一維/二維、區間操作\n- 堆疊與佇列：單調堆疊、單調佇列、優先佇列\n- 雜湊表：計數、去重、查詢\n- 字典樹：字首檢索、字串統計\n- 並查集：連通性、動態連通\n- 樹狀陣列：單點修改、區間查詢\n- 線段樹：區間修改、區間查詢",
  children: [
    {
      id: 2,
      title: "1. 常用列舉技巧",
      summary:
        "本章講的不是某個容器，而是**列舉順序**：同樣要枚舉一組下標，換一個順序枚舉，就能把 `O(n^2)` 壓成 `O(n)`。\n\n共通手法是「固定一個，摘要其餘」——先選定一個當前正在枚舉的位置，其他位置不再重掃，而是壓縮成一份邊掃邊長大的摘要（`unordered_map`、計數表、前綴最小值等）。\n\n**如何挑子單元**\n\n- **1.1 列舉右，維護左**：要找的是一個「配對」`(j, i)` 且 `j < i`，例如兩數之和、買賣股票、相同數字的最近距離。把 `i` 當右端點，左側全部塞進摘要。紀律是**先查詢、再把自己加入**，否則會自己配自己。\n- **1.2 進階**：同一招但摘要變複雜——要記的不只是「看過哪些值」，而是前綴和的餘數、狀態壓縮後的 mask、或多個維度的最佳值。\n- **1.3 列舉中間**：要找的是三個或四個位置的組合（如三數之和、等差三元組）。枚舉兩端會退化，改成**枚舉中間那個**，再讓左右兩側各自維護摘要，就能把三層迴圈降成兩層。\n- **1.4 遍歷對角線**：矩陣題中「同一條對角線」的格子滿足 `r + c` 或 `r - c` 固定。按這個值分組後，二維問題就變回一維掃描。\n\n**判斷自己選對沒有**：問「我固定的是哪一個位置？剩下的部分能不能用一份 `O(1)` 查詢的摘要回答？」答得出來就套對了。",
      children: [
        {
          id: 3,
          title: "1.1 列舉右，維護左",
          summary:
            "## 這個技術解決什麼問題\n\n「找出滿足某條件的一對下標 $(j, i)$，其中 $j < i$」是最常見的題型之一（1 兩數之和、121 買賣股票、219 鄰近重複、454 四數相加）。樸素做法是雙層迴圈，$O(n^2)$。\n\n真正的浪費在哪裡？對每個右端點 $i$，內層迴圈都把 $[0, i)$ 重掃一遍。但相鄰兩次掃描的範圍只差一個元素——**$99\\%$ 的工作是重複的**。\n\n本節的技巧就是把「重掃左側」換成「維護一份左側摘要」：摘要在掃描過程中增量更新，每次 $O(1)$；查詢也是 $O(1)$。$O(n^2) \\to O(n)$。\n\n## 辨識題型的訊號\n\n看到這些特徵，就往「列舉右，維護左」想：\n\n- **題目在找「兩個位置的配對」**：兩數之和、四數相加（題 454，把四個拆成兩兩配對）、可被 60 整除的歌曲對，本質都是「右端點需要一個特定的左端點」。\n- **暴力是雙層迴圈 `O(n^2)`，而你發現右端點需要的左端點資訊，可以提前整理成一張表 `O(1)` 查到。**\n- **配對有方向性（j < i）**，剛好讓「左邊的先進摘要、右邊的來查詢」這個流程成立。\n\n:::example 嗅到味道的瞬間：右端點需要的東西，左邊早就能查\n```cpp\n// 嗅到味道的瞬間：右端點需要的東西，左邊早就能查\n// if (seen.count(need)) ...   // 若這句能 O(1) 回答，就是本技巧\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我把哪個維度當「右端點」來列舉？\n2. 我的「左側摘要」記的是什麼（map、計數、最小值）？\n3. 我有沒有遵守「先查詢、再把自己加入摘要」的順序？\n\n## 核心想法與直覺\n\n本節要解決的場景是：題目要你在一堆數字裡找「某兩個位置的配對」，例如「有沒有兩個數加起來等於 target」「相同數字之間距離有多近」。最直覺的暴力法是兩層迴圈，左邊一個、右邊一個，全部配一遍——這是 `O(n^2)`，資料一大就超時。\n\n「列舉右，維護左」的核心想法是：**把雙層迴圈改成單層**。我們只用一個迴圈，從左掃到右，把目前這個元素當成「右端點」；至於「左端點」，不再每次都重掃一遍，而是用一個邊掃邊長大的摘要（通常是 `unordered_map`）記住「左邊已經看過哪些東西」。\n\n:::example 列舉右、維護左：每個 nums[i] 當右端點，map 記住左邊看過的值\n以「兩數之和（題 1）」為例：固定右端點 `nums[i]`，我們需要的左端點是某個值等於 `target - nums[i]` 的元素。只要左邊看過的數字都記在 map 裡，這個查詢就是 `O(1)`。\n\n```cpp\n// 列舉右、維護左：每個 nums[i] 當右端點，map 記住左邊看過的值\nunordered_map<int, int> seen;  // 值 -> 它的索引（這是「左側摘要」）\nfor (int i = 0; i < n; ++i) {\n    int need = target - nums[i];  // 我這個右端點，需要怎樣的左端點？\n    if (seen.count(need)) {\n        return {seen[need], i};  // 左邊有 → O(1) 配對成功\n    }\n    seen[nums[i]] = i;  // 再把自己加入摘要，留給後面的右端點用\n}\n```\n:::\n\n\n\n為什麼正確？因為任何一對 `(j, i)` 且 `j < i`，當迴圈走到右端點 `i` 時，左端點 `j` 早就被放進摘要了，所以不會漏配。整體只掃一遍，複雜度從 `O(n^2)` 降到 `O(n)`。\n\n關鍵的一行紀律是「**先查詢、再加入**」：要在查完答案之後才把目前元素放進摘要，否則就會出現「自己配自己」的錯誤。\n\n## 狀態／資料結構定義\n\n- **列舉變數**：下標 $i$，代表「右端點」。\n- **左側摘要** $M_i$：一個只依賴 $\\{a_0, \\dots, a_{i-1}\\}$ 的資料結構。常見形態：\n  - `unordered_map<int,int> seen`：值 → 最後出現的下標（題 1、219）。\n  - `unordered_map<int,int> cnt`：值 → 出現次數（計數型，題 1512、2364）。\n  - 單一純量 `minSoFar` / `maxSoFar`：左側的極值（題 121、2016）。\n- **查詢介面**：給定右端點 $a_i$，能 $O(1)$ 從 $M_i$ 取出「有多少 / 有沒有 / 最好的」左端點。\n\n摘要的形態由「右端點需要知道左側的什麼」反推，而不是先選容器再想用途。\n\n## 不變量或正確性證明\n\n**不變量。** 在迴圈第 $i$ 輪執行「查詢」那一行的瞬間，摘要 $M$ 恰好等於 $f(a_0, a_1, \\dots, a_{i-1})$——**包含全部下標嚴格小於 $i$ 的元素，且不包含 $a_i$ 自己**。\n\n- 初始化：$i = 0$ 時 $M$ 為空，對應空集合。成立。\n- 保持：第 $i$ 輪先查詢（此時 $M$ 仍是第 $i-1$ 輪結束時的狀態，即 $\\{a_0..a_{i-1}\\}$），查完才把 $a_i$ 併入，使 $M$ 變成 $\\{a_0..a_i\\}$，正是第 $i+1$ 輪所需。\n\n**不重不漏。** 每個合法的配對 $(j, i)$ 且 $j < i$，恰在「處理右端點 $i$」時被統計一次：\n- *不漏*：$j < i \\Rightarrow a_j$ 在第 $i$ 輪查詢前已進入摘要。\n- *不重*：每對配對只有唯一的較大下標 $i$，而統計只在右端點發生。\n\n**這條論證點名了兩個前提。**\n\n1. **「先查詢、再加入」的順序不可交換。** 若先加入再查詢，$M$ 就變成 $\\{a_0..a_i\\}$，會把 $(i, i)$ 這個非法的「自己配自己」算進去。題 1 若寫反，`nums = [3,3], target = 6` 會回傳 `[0,0]`。\n2. **摘要必須可增量維護。** 「把 $a_i$ 併入 $M$」必須是 $O(1)$（或至少遠小於重算）。若題目要的左側資訊是「左邊的中位數」，加入一個元素不是 $O(1)$，就得換成對頂堆（見 6.7），本節的骨架仍在但成本改變。\n\n**極值型（題 121）的變體證明。** 令 $m_i = \\min_{j<i} a_j$。遞推 $m_{i+1} = \\min(m_i, a_i)$ 顯然正確，且答案 $\\max_i (a_i - m_i)$ 覆蓋了所有 $j < i$ 的配對——因為對固定的 $i$，$a_i - a_j$ 在 $a_j = m_i$ 時最大。**這一步用到「對固定右端點，最佳左端點只有一個，不必記住全部」**，這是把 map 換成純量的依據；一旦題目改成「第二便宜的那天」，這個依據就失效。\n\n## 逐步演算法\n\n:::example 狀態表示：依題目選一種「左側摘要」\n**1. 狀態表示：想清楚「左側摘要」要記什麼。** 視題目而定，可能是值對索引的 map、出現次數的計數表、或目前看過的最小值/最大值。\n\n```cpp\n// 狀態表示：依題目選一種「左側摘要」\nunordered_map<int, int> cnt;  // 計數題：值 -> 出現次數\nint minSoFar = INT_MAX;       // 最佳化題：左邊看過的最小值\n```\n:::\n\n\n\n:::example 更新：先用左側摘要更新答案，再把目前元素併入摘要\n**2. 更新流程：固定右端點，先用摘要算答案，再把自己加入。** 順序不能反。\n\n```cpp\n// 更新：先用左側摘要更新答案，再把目前元素併入摘要\nans += cnt[target - nums[i]];  // 1) 用「左邊看過的」回答以 i 為右端點的問題\ncnt[nums[i]]++;  // 2) 自己加入摘要，成為後面右端點的「左側」\n```\n:::\n\n\n\n:::example 以「買賣股票（題 121）」為例：右端點是賣出日，左側摘要是歷史最低買入價\n**3. 答案讀取：每個配對只在右端點被算一次。** 因為左端點一定比右端點早出現，所以由右端點負責統計，不會重複也不會遺漏。\n\n```cpp\n// 以「買賣股票（題 121）」為例：右端點是賣出日，左側摘要是歷史最低買入價\nfor (int p : prices) {\n    ans = max(ans, p - minSoFar);  // 今天賣，配上左邊最便宜那天買\n    minSoFar = min(minSoFar, p);   // 更新左側摘要\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 「先查再加」的順序是最常見的坑；題 219（鄰近重複元素）還要在加入後刪掉超出視窗範圍的舊紀錄。\n- 計數題要小心同值多次出現，map 要存次數而非只存有無。\n- 複雜度：單層掃描加 `O(1)` 查詢，整體 `O(n)`；空間 `O(n)` 存摘要。\n\n## C++17 模板\n\n以下用同型延伸示範題 498. 對角線遍歷 展示核心寫法。\n\n**完整題目**：給定矩陣，依題目指定的鋸齒狀對角線順序回傳所有元素。\n\n:::example 對角線順序遍歷：按反對角線編號 s = r + c 分組，偶數對角線反轉方向\n**線索**：題目看似多重枚舉，但其中一維可以固定，另一側用已掃描資訊或分組鍵維護。\n\n```cpp\nclass Solution {\npublic:\n    // 對角線順序遍歷：按反對角線編號 s = r + c 分組，偶數對角線反轉方向\n    vector<int> findDiagonalOrder(vector<vector<int>>& mat) {\n            int m = mat.size(), n = mat[0].size();\n            vector<int> ans;\n            for (int s = 0; s <= m + n - 2; ++s) {  // s 為反對角線編號，共 m+n-1 條\n                    vector<int> cur;\n                    // 枚舉該對角線上所有合法的列索引\n                    for (int r = max(0, s - n + 1); r <= min(m - 1, s); ++r) {\n                            int c = s - r;\n                            cur.push_back(mat[r][c]);\n                    }\n                    if (s % 2 == 0) {\n                            reverse(cur.begin(), cur.end());  // 偶數對角線由下往上，需反轉\n                    }\n                    ans.insert(ans.end(), cur.begin(), cur.end());\n            }\n            return ans;\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- 同一條副對角線滿足 `r + c` 相同；先按對角線分組，再依題目要求決定是否反轉。\n- 外層迴圈固定一個維度，內層只處理與該維度相關的必要候選。\n- 分組鍵、左右端點或對角線編號要先算清合法範圍，避免漏算邊界。\n\n## 時間與空間複雜度\n\n主迴圈 $n$ 輪，每輪 $O(1)$ 攤銷（雜湊表的查詢與插入是期望 $O(1)$），總計 **$O(n)$ 時間**。\n\n空間 $O(\\min(n, |V|))$，$|V|$ 是值域中實際出現的相異值個數。極值型是 $O(1)$。\n\n**注意雜湊表的常數。** `unordered_map<int,int>` 的單次操作常數約是陣列的 5–20 倍。若值域已知且不大（如題 1010 的餘數只有 60 種、字元只有 26 種），改用 `vector<int>` 或固定大小陣列可以快數倍——這在 $n$ 達 $10^6$ 時是能否卡過的差別。\n\n題 454（四數相加 II）是把 $O(n^4)$ 拆成兩半：先用 $O(n^2)$ 建 `A+B` 的和的計數表，再用 $O(n^2)$ 枚舉 `C+D` 查表，總計 $O(n^2)$ 時間與空間。**這是「把配對的兩邊各自預處理」的推廣，與 4.8 的折半列舉同源。**\n\n## 常見錯誤與邊界條件\n\n- 先寫出枚舉變數的合法範圍。\n- 二維轉一維時檢查邊界是否越界。\n- 分組鍵可能為負，使用 map 或加偏移。\n- 若每組需要排序，總複雜度要算所有組的總和。\n\n## 常見變形\n\n- 枚舉右端點並維護左側資訊。\n- 枚舉中心點處理左右對稱。\n- 依 `r + c` 或 `r - c` 分組遍歷矩陣對角線。\n\n## 與相似技巧的比較\n\n- **與雙指標／滑動視窗**：滑動視窗要求左端點隨右端點**單調移動**，依賴「加入元素使某量單調變化」的性質，因此怕負數。本節不要求單調，只要求「左側資訊可增量維護」，所以有負數也能用（見 2.2）。\n- **與排序 + 二分**：若允許打亂順序，排序後用二分或雙指標也能解兩數之和，$O(n \\log n)$。但**排序會破壞下標**，題目若要回傳原始下標就得額外記錄。本節保序且更快。\n- **與 1.3 列舉中間**：本節固定右端點、維護一側；1.3 固定中間、維護兩側。判準是「特殊角色的那個元素在配對的哪個位置」。\n- **與前綴和 + 雜湊表（2.2）**：那是本節的直接應用——把 $a_i$ 換成前綴和 $s_i$，把「配對」換成「$s_i - s_j = k$」。**2.2 不是新技巧，是本節套在前綴和序列上。**\n- **與 DP**：`minSoFar` 其實就是一個一維 DP 狀態。當左側摘要是純量且有遞推式時，這個技巧與「滾動變數 DP」是同一件事。\n\n## 代表例題\n\n**題號**：1、121、219、454、624\n\n要固定一個枚舉維度，再維護另一側已看過的資訊；重點是避免雙重暴力重複掃描。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1 | [兩數之和](https://leetcode.cn/problems/two-sum) |  |\n| 121 | [買賣股票的最佳時機](https://leetcode.cn/problems/best-time-to-buy-and-sell-stock) |  |\n| 219 | [存在重複元素 II](https://leetcode.cn/problems/contains-duplicate-ii) |  |\n| 454 | [四數相加 II](https://leetcode.cn/problems/4sum-ii) |  |\n| 624 | [陣列列表中的最大距離](https://leetcode.cn/problems/maximum-distance-in-arrays) |  |\n| 面試題 16.24 | [數對和](https://leetcode.cn/problems/pairs-with-sum-lcci) |  |\n| 1512 | [好數對的數目](https://leetcode.cn/problems/number-of-good-pairs) | 1161 |\n| 2441 | [與對應負數同時存在的最大正整數](https://leetcode.cn/problems/largest-positive-integer-that-exists-with-its-negative) | 1168 |\n| 2016 | [增量元素之間的最大差值](https://leetcode.cn/problems/maximum-difference-between-increasing-elements) | 1246 |\n| 2815 | [陣列中的最大數對和](https://leetcode.cn/problems/max-pair-sum-in-an-array) | 1295 |\n| 2342 | [數位和相等數對的最大和](https://leetcode.cn/problems/max-sum-of-a-pair-with-equal-sum-of-digits) | 1309 |\n| 1128 | [等價多米諾骨牌對的數量](https://leetcode.cn/problems/number-of-equivalent-domino-pairs) | 1333 |\n| 1679 | [K 和數對的最大數目](https://leetcode.cn/problems/max-number-of-k-sum-pairs) | 1346 |\n| 2260 | [必須拿起的最小連續卡牌數](https://leetcode.cn/problems/minimum-consecutive-cards-to-pick-up) | 1365 |\n| 2001 | [可互換矩形的組數](https://leetcode.cn/problems/number-of-pairs-of-interchangeable-rectangles) | 1436 |\n| 3713 | [最長的平衡子串 I](https://leetcode.cn/problems/longest-balanced-substring-i) | 1490 |\n| 3623 | [統計梯形的數目 I](https://leetcode.cn/problems/count-number-of-trapezoids-i) | 1580 |\n| 2364 | [統計壞數對的數目](https://leetcode.cn/problems/count-number-of-bad-pairs) | 1622 |\n| 3805 | [統計凱撒加密對數目](https://leetcode.cn/problems/count-caesar-cipher-pairs) | 1624 |\n| 3371 | [識別陣列中的最大異常值](https://leetcode.cn/problems/identify-the-largest-outlier-in-an-array) | 1644 |\n| 3761 | [映象對之間最小絕對距離](https://leetcode.cn/problems/minimum-absolute-distance-between-mirror-pairs) | 1669 |\n| 1014 | [最佳觀光組合](https://leetcode.cn/problems/best-sightseeing-pair) | 1730 |\n| 1814 | [統計一個陣列中好對子的數目](https://leetcode.cn/problems/count-nice-pairs-in-an-array) | 1738 |\n| 3584 | [子序列首尾元素的最大乘積](https://leetcode.cn/problems/maximum-product-of-first-and-last-elements-of-a-subsequence) | 1763 |\n| 2905 | [找出滿足差值條件的下標 II](https://leetcode.cn/problems/find-indices-with-index-and-value-difference-ii) | 1764 |\n| 3027 | [人員站位的方案數 II](https://leetcode.cn/problems/find-the-number-of-ways-to-place-people-ii/) | 2020 |\n| 3548 | [等和矩陣分割 II](https://leetcode.cn/problems/equal-sum-grid-partition-ii) | 2245 |\n\n**延伸閱讀**：[LeetCode 1 動畫圖解](https://leetcode.cn/problems/two-sum/solution/dong-hua-cong-liang-shu-zhi-he-zhong-wo-0yvmj/)，重點觀察「先查再加入」如何保證左端下標嚴格小於右端。\n\n## 本節重點速查\n\n- 把雙層迴圈的內層換成**增量維護的左側摘要**：$O(n^2) \\to O(n)$。\n- 不變量：查詢的那一刻，摘要恰好是 $\\{a_0, \\dots, a_{i-1}\\}$——**不多不少**。\n- 紀律只有一條：**先查詢、再加入**。反了就會「自己配自己」。\n- 摘要形態由「右端點需要知道左邊的什麼」反推：要值 → map；要個數 → 計數表；要最好的一個 → 純量。\n- 用純量取代 map 的依據是「對固定右端點，最佳左端點唯一」；題目一改就要重新檢查。\n- 值域小時用陣列取代 `unordered_map`，常數差好幾倍。\n",
        },
        {
          id: 5,
          title: "1.2 進階",
          summary:
            "## 這個技術解決什麼問題\n\n1.1 的摘要以「原始值」為鍵，只能回答「左邊有沒有等於 $x$ 的元素」。但很多題目的配對條件不是相等：\n\n- 1010：$(a + b) \\bmod 60 = 0$。\n- 1497：把 $2n$ 個數配成 $n$ 對，每對和可被 $k$ 整除。\n- 3185：$(h_i + h_j) \\bmod 24 = 0$。\n- 1995：四元組 $a + b = c + d$（$i<j<k<l$）。\n\n若照原始值建表，$10^9$ 的值域根本裝不下，而且「互補」的對象也不是單一值。本節的進步是：**先把原始值映射成題目真正在意的「特徵」，再對特徵做互補配對。** 特徵的種類數往往只有 $O(k)$ 個，摘要瞬間變小，查詢仍是 $O(1)$。\n\n## 辨識題型的訊號\n\n看到這些特徵，就往進階版「列舉右，維護左」想：\n\n- **配對條件不是「相等」而是某種數學關係**：可被某數整除、和為定值、奇偶互補——都能轉成「特徵互補」。\n- **直接列舉原始值會爆，但抽象成餘數/mask 後種類數很小**，摘要瞬間變小。\n- **多元組（三、四個一組）題目**，可以拆成兩半，各自當左右側摘要。\n\n:::example 嗅到味道的瞬間：配對條件能整理成 (右特徵) 配 (互補左特徵)\n```cpp\n// 嗅到味道的瞬間：配對條件能整理成 (右特徵) 配 (互補左特徵)\n// ans += cnt[need];   // 若能這樣 O(1) 查，就是本技巧\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我把原始值抽象成了什麼「特徵」當 key？\n2. 右端點與左端點的「互補關係」是怎麼推出來的？\n3. 邊界（餘數為 0、多元組怎麼拆）我處理對了嗎？\n\n## 核心想法與直覺\n\n本節是「列舉右，維護左」的進階應用。基本款處理的是「值對值」的直接配對；進階款的核心進步在於：**右端點需要的左端點，不一定是某個固定值，而是某個「特徵」相同的一群左端點**。我們改成用「特徵」當 map 的 key，把同特徵的左端點打包成一個計數。\n\n:::example 進階：用「特徵（餘數）」當 key，統計同特徵的左端點數量\n最典型的是「按餘數分組」。看題 1010「總持續時間可被 60 整除的歌曲」：兩首歌 `a`、`b` 滿足 `(a + b) % 60 == 0`，等價於 `a % 60` 和 `b % 60` 互補。於是右端點只要找「餘數為 `(60 - a % 60) % 60`」的左端點有幾個。\n\n```cpp\n// 進階：用「特徵（餘數）」當 key，統計同特徵的左端點數量\nvector<int> cnt(60, 0);  // cnt[r] = 左邊餘數為 r 的歌曲數\nlong long ans = 0;\nfor (int t : time) {\n    int r = t % 60;\n    ans += cnt[(60 - r) % 60];  // 右端點配上互補餘數的所有左端點\n    cnt[r]++;                   // 自己以餘數特徵加入摘要\n}\n```\n:::\n\n\n\n這裡的進步有兩層：第一，key 從「原始值」抽象成「題目真正關心的特徵」（餘數、奇偶、可整除性）；第二，配對條件常常是「互補」而非「相等」，要先把數學關係推導出來，才知道右端點該查哪個 key。題 1497（陣列對可被 k 整除）就是同一招，把 key 換成 `% k` 的餘數。\n\n只要特徵能 `O(1)` 算出、互補關係能 `O(1)` 查到，整體仍是單層掃描 `O(n)`。\n\n## 狀態／資料結構定義\n\n- **特徵函數** $\\varphi: V \\to K$，把原始值映射到一個小值域。常見的 $\\varphi$：\n  - 餘數：$\\varphi(x) = x \\bmod k$（1010、1497、3185、2964）。\n  - 位元遮罩：$\\varphi(s) = $ 各字元出現次數的奇偶（2506、1371）。\n  - 正規化的形狀：$\\varphi(w,h) = w/\\gcd \\cdot 10^9 + h/\\gcd$（2001 可互換矩形）。\n- **互補函數** $\\psi: K \\to K$（或 $K \\to$ 集合），滿足：$(x, y)$ 合法 $\\iff \\varphi(y) = \\psi(\\varphi(x))$。\n- **摘要**：`cnt[key]` = 左側特徵為 `key` 的元素個數。計數而非布林，因為要數配對數量。\n\n**多元組的拆分**：$k$ 元組先固定「切點」，把它拆成「左側的 $p$ 元組」與「右側的 $q$ 元組」，兩邊各自維護一份摘要。1995 的 $a+b=c+d$ 改寫成 $a - c = d - b$ 後固定中間兩個下標，就退化成 1.3。\n\n## 不變量或正確性證明\n\n**核心引理（特徵的充分性）。** 設配對條件為謂詞 $P(x, y)$。若存在 $\\varphi$ 與 $\\psi$ 使得\n$$P(x, y) \\iff \\varphi(y) = \\psi(\\varphi(x)),$$\n則「左邊有幾個 $y$ 滿足 $P(x,y)$」$=$ `cnt[psi(phi(x))]`。\n\n這一步看似顯然，但它精確說出了本技巧成立的前提：**條件必須能只透過特徵判斷，而且互補的特徵必須唯一（或是可枚舉的少數幾個）**。逐一檢查：\n\n- $(a+b) \\bmod k = 0$：$\\varphi(x) = x \\bmod k$，$\\psi(r) = (k - r) \\bmod k$。**唯一互補，成立。**\n- $a + b = t$：$\\varphi(x) = x$，$\\psi(x) = t - x$。**成立**（這就是 1.1）。\n- $|a - b| \\le d$：**沒有唯一互補特徵**，互補的是一個區間 $[a-d, a+d]$。此時要改用有序集合 + `lower_bound`（$O(\\log n)$）或滑動視窗，不能用雜湊表。\n- 「$a$ 與 $b$ 至多差一次字元交換」（3267）：互補集合的大小是 $O(\\text{字串長}^2)$，仍可枚舉，於是**枚舉所有互補特徵再查表**——這是引理的推廣形式（$\\psi$ 回傳集合）。\n\n**$\\psi(r) = (k - r) \\bmod k$ 的外層 `% k` 不可省。** 當 $r = 0$ 時，$k - 0 = k \\notin [0, k)$，會查到一個不存在的鍵，導致「餘數為 0 的元素彼此無法配對」——而那正是最容易出現的一組配對。**這一行的 `% k` 是正確性，不是防禦性寫法。**\n\n**計數的不重不漏。** 沿用 1.1 的不變量（查詢時摘要恰為 $\\{a_0..a_{i-1}\\}$）。每個合法對 $(j,i)$，$j<i$，在第 $i$ 輪被 `ans += cnt[need]` 計入恰一次。**注意這裡不需要再乘 2 或除以 2**——因為順序已由「$j$ 在左、$i$ 在右」固定。若改用「先建完整計數表再對每個元素查詢」的寫法，每對會被算兩次，就必須除以 2，而且要特別處理 $\\varphi(x) = \\psi(\\varphi(x))$ 的自配對情形。**兩種寫法都對，但混用就錯**；本節統一採用「邊掃邊配」，因為它沒有這些例外。\n\n**1995（統計特殊四元組 $a_i + a_j + a_k = a_l$，$i<j<k<l$）的正確性。** 移項成\n$$a_i + a_j = a_l - a_k .$$\n左邊只依賴 $\\{i, j\\}$，右邊只依賴 $\\{k, l\\}$——**這正是「特徵只依賴一側」這個前提的具體形式**。於是固定 $j$，維護一張表 `cnt`，其鍵是 $a_l - a_k$，範圍限制為 $j < k < l$。\n\n實作上讓 $j$ **由大到小**掃：進入第 $j$ 輪時，先把所有 $c = j+1$ 的新配對 $(c, d)$（$d > c$）併入 `cnt`，然後對每個 $i < j$ 累加 `cnt[a[i] + a[j]]`。\n\n*不變量*：查詢的那一刻，`cnt` 恰好等於所有滿足 $j < k < l$ 的 $(k,l)$ 對的 $a_l - a_k$ 的計數。\n- 保持：從 $j+1$ 掃到 $j$ 時，新增的合法 $(k,l)$ 恰是 $k = j+1$ 的那些（$k \\ge j+2$ 的在前幾輪已加入，且永遠合法，不需移除）。這就是為什麼 `cnt` **不能在每輪清空**——它是單調增長的。\n- *不重不漏*：每個四元組 $(i,j,k,l)$ 在第 $j$ 輪、內層下標 $i$ 時恰被算一次。\n\n$j$ 必須由大到小，因為「$k > j$」這個限制隨 $j$ 減小而放寬，只增不減，才能用「只加不刪」的摘要。若由小到大掃，就得刪除項目，摘要不再單調。**這與 12.1 離線查詢排序的思想是同一條：把限制排成單調的順序，讓資料結構只需支援插入。**\n\n## 逐步演算法\n\n:::example 狀態表示：以特徵為 key 的計數摘要\n**1. 狀態表示：先決定「特徵」是什麼。** 把原始值映射成題目真正在意的量（餘數、奇偶 mask、是否出現過）。\n\n```cpp\n// 狀態表示：以特徵為 key 的計數摘要\nunordered_map<int, int> cnt;  // 特徵 -> 左邊有幾個這種特徵的元素\n```\n:::\n\n\n\n:::example 更新：先用互補特徵查左側，再把自己加入\n**2. 推導互補關係：右端點該查哪個 key。** 把配對條件用數學整理成「右端點特徵 → 需要的左端點特徵」。\n\n```cpp\n// 更新：先用互補特徵查左側，再把自己加入\nint r = t % k;\nint need = (k - r) % k;  // 互補餘數（注意 r==0 時 need 也要是 0）\nans += cnt[need];        // 配對所有符合的左端點\ncnt[r]++;\n```\n:::\n\n\n\n:::example 巢狀型（題 1031 兩段不重疊子陣列）：左側維護「到目前為止某段的最佳值」\n**3. 答案讀取：右端點負責累加。** 每對只在右端點統計一次；計數題用 `+=` 累加而非比較大小。\n\n```cpp\n// 巢狀型（題 1031 兩段不重疊子陣列）：左側維護「到目前為止某段的最佳值」\nbestLeft = max(bestLeft, sumOfLeftWindow);  // 左側摘要是「最佳前綴」\nans = max(ans, bestLeft + sumOfRightWindow);\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 互補餘數務必寫成 `(k - r) % k`，否則 `r == 0` 會錯算成 `k`。\n- 多元組題（題 1995 統計特殊四元組）常把四個拆成「2 + 2」或「3 + 1」，固定中間、左右各維護一個摘要。\n- 複雜度：單層掃描加 `O(1)` 查詢，`O(n)`；空間 `O(特徵種類數)`。\n\n## C++17 模板\n\n```cpp\n// 總持續時間可被 60 整除的歌曲對（LeetCode 1010）\n// 特徵 = 模 60 的餘數；互補 = (60 - r) % 60。\nclass Solution {\n public:\n  int numPairsDivisibleBy60(std::vector<int>& time) {\n    constexpr int kMod = 60;\n    std::array<int, kMod> cnt{};        // 值域已知且小 → 用陣列，不用 unordered_map\n    long long ans = 0;\n    for (int t : time) {\n      int r = t % kMod;                 // 特徵\n      int need = (kMod - r) % kMod;     // 互補特徵；外層 % 不可省（r == 0）\n      ans += cnt[need];                 // 先查：左側有幾個互補的\n      ++cnt[r];                         // 再加：自己成為後續的左側\n    }\n    return static_cast<int>(ans);\n  }\n};\n```\n\n```cpp\n// 統計特殊四元組 a[i] + a[j] + a[k] == a[l]，i<j<k<l（LeetCode 1995）\n// 移項成 a[i] + a[j] == a[l] - a[k]：左邊只依賴 {i,j}，右邊只依賴 {k,l}。\nclass Solution {\n public:\n  int countQuadruplets(std::vector<int>& nums) {\n    int n = nums.size();\n    std::unordered_map<int, int> cnt;      // (a[l] - a[k]) 的計數，限 k > j\n    long long ans = 0;\n    for (int j = n - 3; j >= 1; --j) {\n      // j 減 1，合法的 (k, l) 只增不減：新增的恰好是 k == j + 1 那一批\n      for (int l = j + 2; l < n; ++l) {\n        ++cnt[nums[l] - nums[j + 1]];\n      }\n      // 此刻 cnt 恰為所有 j < k < l 的 a[l] - a[k]（cnt 不可清空）\n      for (int i = 0; i < j; ++i) {\n        auto it = cnt.find(nums[i] + nums[j]);\n        if (it != cnt.end()) { ans += it->second; }\n      }\n    }\n    return static_cast<int>(ans);\n  }\n};\n```\n\n`cnt` 跨輪累積、**永不清空**，是這段程式碼唯一的巧思：它把「$k > j$」這個隨 $j$ 遞減而放寬的限制，變成只需插入、不需刪除的操作。用 `find` 而非 `cnt[key]` 查詢，是為了避免 `operator[]` 在鍵不存在時插入一筆值為 0 的項目——那會讓表無謂膨脹（正確性不受影響，但在 $n$ 大時是可觀的常數）。\n\n## 時間與空間複雜度\n\n單層掃描版（1010、1497、3185、2506）：$O(n)$ 時間，$O(|K|)$ 空間（$|K|$ 是特徵種類數，通常是 $k$ 或 $2^{26}$ 中實際出現的部分）。\n\n多元組拆分版（1995、2874、2964）：固定 $t$ 個下標就是 $O(n^t)$ 外層乘上 $O(1)$ 查表。四元組拆成 2+2 是 $O(n^2)$，比暴力的 $O(n^4)$ 少兩個數量級。\n\n「巢狀最佳化」版（1031 兩段不重疊子陣列、2555 兩條線段）：左側摘要不是計數而是「到目前為止的最佳值」，仍是 $O(n)$。**這一類的正確性依據是「最佳左端點只有一個」，與 1.1 的極值型同源，而不是本節的互補配對。**\n\n$O(k)$ 大小的陣列（1010 的 60、3185 的 24）比 `unordered_map` 快數倍；**只要 $|K|$ 已知且不大，一律用陣列**。\n\n## 常見錯誤與邊界條件\n\n- 互補餘數漏掉外層取模，寫成 `k - r`（$r = 0$ 時查到不存在的鍵，餘數 0 的配對全部漏掉）。\n- C++ 的 `%` 對負數回傳負值，`x % k` 在 $x < 0$ 時要寫成 `((x % k) + k) % k`（1497 的輸入含負數）。\n- 配對數用 `int` 累加溢位（$n = 10^5$ 時配對數可達 $5 \\times 10^9$，必須 `long long`）。\n- 用「先建完整表再逐一查詢」的寫法卻忘記除以 2 或忘記扣掉自配對。\n- 1497 只需檢查「每種餘數的個數能否兩兩配完」（$cnt[r] = cnt[k-r]$，且 $cnt[0]$ 與 $cnt[k/2]$ 必須是偶數），而不是數配對數——**題型雖同，問法不同**。\n- 多元組拆分時兩側範圍重疊，同一下標被用兩次。\n- 特徵函數本身算錯（2001 的矩形比值未約分、或用浮點數比較）。\n- `unordered_map` 在最壞情況退化成 $O(n)$（競賽中的雜湊攻擊，可加隨機種子）。\n\n## 與相似技巧的比較\n\n- **與 1.1**：本節把鍵從「值」換成「特徵」，骨架完全相同。**1.1 是 $\\varphi = \\mathrm{id}$ 的特例。**\n- **與 2.2（前綴和 + 雜湊表）**：那是把本節套在前綴和序列上，$\\varphi(s_i) = s_i \\bmod k$（974、523）或 $\\varphi = \\mathrm{id}$（560）。**遇到「子陣列和可被 $k$ 整除」，就是 2.2 加上本節的餘數特徵。**\n- **與 2.4（狀態壓縮前綴和）**：特徵是位元遮罩，互補是「XOR 後為 0 或只有一個 1」。1371 的互補集合有 $1 + 5$ 個候選，用的正是「$\\psi$ 回傳集合」的推廣形式。\n- **與排序 + 雙指標**：條件是「和落在某範圍」而非「和等於定值」時，雜湊表失效（沒有唯一互補），改排序後雙指標。**判準：等號用雜湊，不等號用排序。**\n- **與 4.8 折半列舉**：454（四數相加 II）先算 `A+B` 的表再查 `C+D`，就是折半的思想用在「已經天然分成兩組」的輸入上。\n- **與 1.3 列舉中間**：多元組拆成 $p + q$ 時，若切點在中間且左右都要摘要，就是 1.3。\n\n## 代表例題\n\n**題號**：1010、1031、1214、1497、1995\n\n要固定一個枚舉維度，再維護另一側已看過的資訊；重點是避免雙重暴力重複掃描。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1010 | [總持續時間可被 60 整除的歌曲](https://leetcode.cn/problems/pairs-of-songs-with-total-durations-divisible-by-60) | 1377 |\n| 1031 | [兩個無重疊子陣列的最大和](https://leetcode.cn/problems/maximum-sum-of-two-non-overlapping-subarrays) | 1680 |\n| 1214 | [查詢兩棵二叉搜索樹之和](https://leetcode.cn/problems/two-sum-bsts)（Premium） | 1389 |\n| 1497 | [檢查陣列對是否可以被 k 整除](https://leetcode.cn/problems/check-if-array-pairs-are-divisible-by-k) | 1787 |\n| 1995 | [統計特殊四元組](https://leetcode.cn/problems/count-special-quadruplets) | 1352 |\n| 2506 | [統計相似字串對的數目](https://leetcode.cn/problems/count-pairs-of-similar-strings) | 1335 |\n| 2555 | [兩個線段獲得的最多獎品](https://leetcode.cn/problems/maximize-win-from-two-segments) | 2081 |\n| 2748 | [美麗下標對的數目](https://leetcode.cn/problems/number-of-beautiful-pairs) | 1301 |\n| 2874 | [有序三元組中的最大值 II](https://leetcode.cn/problems/maximum-value-of-an-ordered-triplet-ii) | 1583 |\n| 2964 | [可被整除的三元組數量](https://leetcode.cn/problems/number-of-divisible-triplet-sums) |  |\n| 3185 | [構成整天的下標對數目 II](https://leetcode.cn/problems/count-pairs-that-form-a-complete-day-ii) | 1385 |\n| 3267 | [統計近似相等數對 II](https://leetcode.cn/problems/count-almost-equal-pairs-ii) | 2545 |\n| 3404 | [統計特殊子序列的數目](https://leetcode.cn/problems/count-special-subsequences) | 2445 |\n| 3480 | [刪除一個衝突對後最大子陣列數目](https://leetcode.cn/problems/maximize-subarrays-after-removing-one-conflicting-pair) | 2764 |\n\n**思維擴充例題**：\n\n- [LeetCode 3027 人員站位的方案數 II](https://leetcode.cn/problems/find-the-number-of-ways-to-place-people-ii/)：固定一側後維護另一側可配對的摘要。\n- [LeetCode 3548 等和矩陣分割 II](https://leetcode.cn/problems/equal-sum-grid-partition-ii/)：把二維切割條件壓成可重用的一維總和／例外檢查。\n- [LeetCode 3713 最長的平衡子字串 I](https://leetcode.cn/problems/longest-balanced-substring-i/)：避免重掃左側狀態，改成一次遍歷維護候選。\n\n## 本節重點速查\n\n- 把鍵從**原始值**換成**特徵** $\\varphi$，配對條件寫成 $\\varphi(y) = \\psi(\\varphi(x))$。\n- 前提：**條件必須只透過特徵判斷，且互補特徵唯一（或可枚舉的少數幾個）**。等號成立時用雜湊，變成不等號／區間就要改排序 + 二分。\n- 互補餘數一律寫 `(k - r) % k`——外層取模是正確性，$r=0$ 時必須映射回 0。\n- 負數取模要先 `((x % k) + k) % k`。\n- 「邊掃邊配」不需除以 2 也沒有自配對問題；「先建全表再查」兩者都要處理。**別混用。**\n- 多元組拆成 $p+q$，兩側範圍必須嚴格不交；摘要的鍵只能依賴一側，否則換一輪就整張表失效。\n- 配對計數一律用 `long long`；$|K|$ 已知且小就用陣列取代 `unordered_map`。\n",
        },
        {
          id: 6,
          title: "1.3 列舉中間",
          summary:
            "## 這個技術解決什麼問題\n\n三元組題（$i < j < k$）的暴力是 $O(n^3)$。1.1 的「列舉右、維護左」只消掉一層，還剩 $O(n^2)$。\n\n本節再進一步：**固定中間那個下標 $j$，讓左右兩側各自獨立統計，再用乘法（或加法）合併。** 對每個 $j$，答案的貢獻寫成\n$$\\text{contrib}(j) = L(j) \\times R(j),$$\n其中 $L(j)$ 只看 $[0, j)$、$R(j)$ 只看 $(j, n)$。外層 $O(n)$ 枚舉 $j$，兩份摘要各自 $O(1)$ 增量維護，整體 $O(n)$——**三層迴圈一次消掉兩層**。\n\n適用題目：447（迴旋鏢，$j$ 是頂點）、456（132 模式，$j$ 是那個 `3`）、1534（好三元組）、1930（長度 3 的迴文子序列，$j$ 是中心）、2909（山形三元組，$j$ 是山頂）、3128（直角三角形，$j$ 是直角頂點）、2242（圖上枚舉中間一條邊）。\n\n## 辨識題型的訊號\n\n看到這些特徵，就往「列舉中間」想：\n\n- **答案是三元組 `i < j < k`，且中間元素 `j` 的角色特殊**（是頂點、是模式裡的某一位、是峰值）。\n- **左右兩側的條件可以各自獨立統計，最後用乘法合併**——這是消掉一層迴圈的關鍵。\n- **暴力三層 `O(n^3)` 會超時**，但固定中間後左右都能用摘要 `O(1)` 查。\n\n:::example 嗅到味道的瞬間：以 j 為中間，貢獻能寫成「左 × 右」\n```cpp\n// 嗅到味道的瞬間：以 j 為中間，貢獻能寫成「左 × 右」\n// ans += leftSatisfy * rightSatisfy;\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我選哪個元素當「中間 `j`」？它的特殊角色是什麼？\n2. 左、右兩份摘要各記什麼，能否獨立統計？\n3. 我有沒有照「移出右 → 算貢獻 → 加入左」的順序，避免把 `j` 自己算進去？\n\n## 核心想法與直覺\n\n本節處理「三個位置」的題目：要找 `i < j < k` 的三元組，而且常常是「中間那個 `j` 決定左右兩邊該配什麼」。例如題 447「迴旋鏢」要找頂點 `j`，配上到 `j` 距離相等的兩個點；題 456「132 模式」中間的 `3` 兩側分別要小的和更小的；題 1534「好三元組」中間元素要同時滿足對左、對右的差值限制。\n\n:::example 列舉中間：固定 j，左右各維護一個計數摘要，乘法合併\n這類題若三層迴圈就是 `O(n^3)`。核心想法是：**固定中間那個元素 `j`，左右兩側分別用一個摘要維護**。答案 = (左側符合條件的數量) ×（或 +）(右側符合條件的數量)。因為「左 × 右」的乘法計數，把第三層迴圈消掉了。\n\n```cpp\n// 列舉中間：固定 j，左右各維護一個計數摘要，乘法合併\nlong long ans = 0;\nunordered_map<int, int> leftCnt, rightCnt;\nfor (int x : nums) {\n    rightCnt[x]++;  // 一開始所有元素都在右側\n}\nfor (int j = 0; j < n; ++j) {\n    rightCnt[nums[j]]--;  // j 不算右側，先移出\n    // 此刻：leftCnt 是 j 左邊的摘要，rightCnt 是 j 右邊的摘要\n    // ans += f(leftCnt, rightCnt);      // 用左右摘要算「以 j 為中間」的貢獻\n    leftCnt[nums[j]]++;  // 處理完再把 j 併入左側，準備下一個 j\n}\n```\n:::\n\n\n\n關鍵節奏是「**先把 `j` 移出右側、算完貢獻、再把 `j` 加入左側**」。這樣每個 `j` 看到的左右摘要都恰好對應「嚴格在 `j` 左邊」與「嚴格在 `j` 右邊」，不會把自己誤算進去。這招把 `O(n^3)` 降為 `O(n^2)` 甚至 `O(n)`。\n\n## 狀態／資料結構定義\n\n- **中間下標** $j$：由外層迴圈枚舉。\n- **左側摘要** $L$：只依賴 $\\{a_0, \\dots, a_{j-1}\\}$。\n- **右側摘要** $R$：只依賴 $\\{a_{j+1}, \\dots, a_{n-1}\\}$。\n\n兩份摘要的內容視題目而定：\n\n| 題目 | $j$ 的角色 | 左側摘要 | 右側摘要 |\n| --- | --- | --- | --- |\n| 1930 | 迴文中心 | 已出現的字元集合 | 尚未出現的字元集合 |\n| 2909 | 山頂 | $\\min_{i<j} a_i$ | $\\min_{k>j} a_k$ |\n| 3128 | 直角頂點 | 同列的 1 的個數 | 同行的 1 的個數 |\n| 1534 | 中間元素 | 滿足 $|a_i - a_j| \\le b$ 的個數 | 滿足 $|a_j - a_k| \\le c$ 的個數 |\n| 447 | 迴旋鏢頂點 | — | 到 $j$ 距離為 $d$ 的點數（左右不分） |\n\n**注意 447 不分左右**：它要的是「無序對」，$j$ 以外的所有點放同一份摘要即可，配對數用 $c(c-1)$（有序對）。**要不要分左右，取決於題目的三元組是否要求下標有序。**\n\n## 不變量或正確性證明\n\n**主不變量。** 在第 $j$ 輪計算貢獻的那一瞬間：\n$$L = f(a_0, \\dots, a_{j-1}), \\qquad R = g(a_{j+1}, \\dots, a_{n-1}).$$\n**$a_j$ 自己既不在 $L$ 也不在 $R$。** 這靠固定的三步節奏維持：\n\n1. `R.remove(a[j])`——把 $a_j$ 從右側移出（初始化時所有元素都在 $R$）。\n2. 計算 `contrib(j)`。\n3. `L.insert(a[j])`——把 $a_j$ 併入左側，供下一輪使用。\n\n歸納：$j = 0$ 時 $L$ 為空、$R$ 在步驟 1 後恰為 $\\{a_1..a_{n-1}\\}$；若第 $j$ 輪成立，步驟 3 使 $L = \\{a_0..a_j\\}$，第 $j+1$ 輪的步驟 1 使 $R = \\{a_{j+2}..a_{n-1}\\}$，正是所需。\n\n**三步的順序不可交換。** 若步驟 1 放到步驟 2 之後，$a_j$ 會被算成自己右邊的元素，產生 $i < j = k$ 這種非法三元組。若步驟 3 放到步驟 2 之前，同樣會產生 $i = j < k$。**這兩個錯誤在「元素互異」的測資上看不出來**，只有出現重複值時才會多算——正是最難除錯的一類 bug。\n\n**乘法合併的合法性（獨立性條件）。** 寫成 $L(j) \\times R(j)$ 的前提是：\n$$\\text{三元組 } (i,j,k) \\text{ 合法} \\iff P(i,j) \\wedge Q(j,k),$$\n即條件可以**拆成「左–中」與「中–右」兩個獨立的謂詞，不含直接連結 $i$ 與 $k$ 的項**。\n\n- 1534 好三元組：條件是 $|a_i - a_j| \\le a$、$|a_j - a_k| \\le b$、$|a_i - a_k| \\le c$。**第三個條件直接連結 $i$ 與 $k$，不可拆！** 所以 1534 不能用純乘法，只能 $O(n^2)$（固定 $i, j$ 再用樹狀陣列數合法的 $k$）或直接 $O(n^3)$（$n \\le 100$）。**這是本節最重要的反例：看起來像三元組就套乘法，是錯的。**\n- 1930 長度 3 的迴文子序列 `xyx`：條件是 $a_i = a_k$（連結 $i$ 與 $k$！）。但因為值域只有 26，可以**枚舉外層字元 $x$**，把它從「連結」變成「常數」，再對每個 $x$ 取最左與最右出現位置，數中間的相異字元數。**把耦合的維度提到外層枚舉，是解開不可分解條件的標準手法。**\n- 2909 山形三元組：$a_i > a_j < a_k$，兩個條件分別只連 $(i,j)$ 與 $(j,k)$。**可拆，成立。**\n- 3128 直角三角形：三點分別在同列、同行，條件天然分解。**成立。**\n\n**447 的計數公式。** 固定頂點 $j$，把其餘 $n-1$ 個點按到 $j$ 的距離分組，某距離有 $c$ 個點。題目要的是**有序**對 $(i, k)$ 且 $i \\ne k$，故貢獻是 $c \\times (c-1)$，不是 $\\binom{c}{2}$。距離用**平方距離**比較，避免開根號的浮點誤差（$\\mathrm{dx}^2 + \\mathrm{dy}^2$，用 `long long` 防溢位）。\n\n**2242（節點序列最大得分）的排除重複端點。** 枚舉中間的一條邊 $(u,v)$，左邊取 $u$ 的鄰居中分數最大者、右邊取 $v$ 的鄰居中分數最大者。但取到的可能就是 $v$ 或 $u$ 自己，或兩邊取到同一個點——**四個節點必須互異**。解法是每個節點只保留分數最大的前 3 個鄰居：因為要排除的最多是 2 個（另一端點 + 對面選中的點），第 3 名必定可用。**「保留前 $k+1$ 名」是處理這類排除的通用手法**，$k$ 是最多要排除的個數。\n\n## 逐步演算法\n\n:::example 狀態表示：左右兩側摘要（這裡用計數表）\n**1. 狀態表示：左右各一份摘要。** 內容看題目：計數表、最小值、或迴旋鏢題裡「各距離出現幾次」。\n\n```cpp\n// 狀態表示：左右兩側摘要（這裡用計數表）\nunordered_map<int, int> leftCnt, rightCnt;\n```\n:::\n\n\n\n:::example 初始化：全部元素先進右側摘要\n**2. 初始化：通常先把所有元素放右側。** 因為一開始中間點還沒選，整列都在中間點的右邊。\n\n```cpp\n// 初始化：全部元素先進右側摘要\nfor (int x : nums) {\n    rightCnt[x]++;\n}\n```\n:::\n\n\n\n:::example 更新：以 j 為中間時的標準三步\n**3. 更新流程：移出右、算貢獻、加入左。** 三步順序固定。\n\n```cpp\n// 更新：以 j 為中間時的標準三步\nrightCnt[nums[j]]--;                           // 1) j 退出右側\nans += (long long)leftSatisfy * rightSatisfy;  // 2) 左右滿足數相乘\nleftCnt[nums[j]]++;                            // 3) j 進入左側\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 迴旋鏢（題 447）對每個中間點，按距離分組後用 `c * (c - 1)` 算有序對；別忘了順序。\n- 進階如題 2242（節點序列最大得分）在圖上「枚舉中間一條邊」，左右各取最大值，要排除重複端點。\n- 複雜度：外層枚舉中間 `O(n)`，內層摘要操作 `O(1)~O(n)`，整體 `O(n)~O(n^2)`，遠優於 `O(n^3)`。\n\n## C++17 模板\n\n```cpp\n// 長度為 3 的不同迴文子序列（LeetCode 1930）\n// 中心 j 的角色是「中間那個字元」，但條件 a[i] == a[k] 耦合了左右，\n// 因此把外層字元 x 提到最外層枚舉，讓條件退化成可分解的形式。\nclass Solution {\n public:\n  int countPalindromicSubsequence(std::string s) {\n    int n = s.size(), ans = 0;\n    for (char x = 'a'; x <= 'z'; ++x) {\n      int lo = -1, hi = -1;\n      for (int i = 0; i < n; ++i) {\n        if (s[i] == x) {\n          if (lo < 0) { lo = i; }\n          hi = i;                                  // 最左與最右的 x\n        }\n      }\n      if (hi - lo < 2) { continue; }               // 中間放不下任何字元\n      std::array<bool, 26> mid{};\n      for (int j = lo + 1; j < hi; ++j) {\n        mid[s[j] - 'a'] = true;                    // 中間出現過的相異字元\n      }\n      ans += std::count(mid.begin(), mid.end(), true);\n    }\n    return ans;\n  }\n};\n```\n\n```cpp\n// 元素和最小的山形三元組 II（LeetCode 2909）\n// j 是山頂的相反（谷底）：a[i] > a[j] < a[k]，兩個條件各只連一側，可分解。\nclass Solution {\n public:\n  int minimumSum(std::vector<int>& nums) {\n    int n = nums.size();\n    if (n < 3) { return -1; }\n    std::vector<int> sufMin(n + 1, INT_MAX);\n    for (int k = n - 1; k >= 0; --k) {\n      sufMin[k] = std::min(sufMin[k + 1], nums[k]);   // 右側摘要，預先算好\n    }\n    long long ans = LLONG_MAX;\n    int preMin = nums[0];                             // 左側摘要，邊掃邊更新\n    for (int j = 1; j + 1 < n; ++j) {\n      if (preMin < nums[j] && sufMin[j + 1] < nums[j]) {\n        ans = std::min<long long>(ans, preMin + nums[j] + sufMin[j + 1]);\n      }\n      preMin = std::min(preMin, nums[j]);             // j 併入左側，供下一輪\n    }\n    return ans == LLONG_MAX ? -1 : static_cast<int>(ans);\n  }\n};\n```\n\n第二個模板示範了另一種常見寫法：**右側摘要用後綴陣列一次算好，左側摘要邊掃邊維護。** 這比「一開始全放右側、逐一移出」更省事，適用於摘要是極值或前綴和這類可預計算的量；當摘要是計數表（元素會被移出）時，就得用三步節奏。\n\n## 時間與空間複雜度\n\n**標準形（可分解 + 摘要 $O(1)$ 更新）**：$O(n)$ 時間、$O(n)$ 或 $O(|V|)$ 空間（2909、2874、3583）。\n\n**外層再枚舉一維**（1930 枚舉 26 個字元、3128 枚舉列）：$O(n \\cdot |\\Sigma|)$ 或 $O(mn)$。\n\n**447**：外層枚舉 $n$ 個頂點，內層 $O(n)$ 建距離表，$O(n^2)$ 時間、$O(n)$ 空間。$n \\le 500$，可過。\n\n**不可分解、需要第三個資料結構的**（2552 統計上升四元組、1534）：固定兩個下標 $O(n^2)$，內層用樹狀陣列或前綴計數 $O(1)$，整體 $O(n^2)$。**這已經不是本節的乘法合併，而是 9.1 的範疇。**\n\n**與 $O(n^3)$ 的落差**：$n = 10^5$ 時 $O(n^3) = 10^{15}$（不可行）、$O(n^2) = 10^{10}$（不可行）、$O(n) = 10^5$（瞬間）。**三元組題只要 $n > 500$，就必須做到 $O(n)$ 或 $O(n \\log n)$。**\n\n## 常見錯誤與邊界條件\n\n- 三步節奏順序寫錯，把 $a_j$ 算進自己的左側或右側（**只在有重複值的測資上出錯**）。\n- 計數表移出後值變成 0 卻沒從 `unordered_map` 刪除，導致「相異值個數」多算（用 `if (--cnt[x] == 0) cnt.erase(x);`）。\n- 乘法合併用在不可分解的條件上（1534 的第三個約束）。\n- 447 用 $\\binom{c}{2}$ 而非 $c(c-1)$（題目要有序對）。\n- 447 用浮點距離（誤差）或 `int` 平方（$10^4$ 的座標差平方和達 $8\\times10^8$，接近 `int` 上限，配對時務必 `long long`）。\n- 2909 的 `sufMin` 開成長度 $n$ 而非 $n+1$（$j = n-2$ 時存取 `sufMin[n-1]` 沒問題，但寫成 `sufMin[j+1]` 且 $j$ 跑到 $n-1$ 就越界）。\n- `preMin` 初始化成 `INT_MAX` 卻參與加法（溢位，本模板改成 `nums[0]` 並用 `long long` 比較）。\n- $n < 3$ 未特判。\n- 2242 忘記排除重複端點（只留前 1 名不夠，要前 3 名）。\n- 乘積用 `int` 溢位（$L$ 與 $R$ 都到 $10^5$ 時乘積 $10^{10}$）。\n\n## 與相似技巧的比較\n\n- **與 1.1**：1.1 維護一側，本節維護兩側。**判準：三元組中「角色特殊」的元素在中間，就用本節；在右端就用 1.1。**\n- **與前綴／後綴陣列（2.1）**：當摘要是可預計算的極值或和時，本節退化成「前綴陣列 + 後綴陣列 + 枚舉分界」，這是同一招的最常見形態（2909、3084、1653）。**看到「左邊最小、右邊最大」就往這裡想。**\n- **與單調堆疊（4.7）**：456（132 模式）也是列舉中間，但它要的是「右邊比 $a_j$ 小的最大值」，這用單調堆疊從右往左掃更自然，$O(n)$。**當左右摘要需要的是「第一個滿足某序關係的元素」時，單調堆疊優於計數表。**\n- **與樹狀陣列（9.1）**：條件不可分解、必須固定兩個下標時，內層的「有幾個小於 $x$」用樹狀陣列，$O(n^2 \\log n) \\to$ 常見的 2552、3257 都走這條。\n- **與 1.4 遍歷對角線**：3128（直角三角形）的分組鍵是列與行，1.4 的分組鍵是 $i \\pm j$。**都是「先分組再組合計數」，只是鍵不同。**\n- **與 2867（統計樹中的合法路徑）**：那是把「列舉中間」搬到樹上——枚舉質數節點當中間點，左右換成各棵子樹，乘法合併時要扣掉「同一棵子樹內配對」的部分。**樹上版本的「不重不漏」比陣列版難，見 trees 3.13。**\n\n## 代表例題\n\n**題號**：447、456、1534、1930、2242\n\n要固定一個枚舉維度，再維護另一側已看過的資訊；重點是避免雙重暴力重複掃描。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 447 | [迴旋鏢的數量](https://leetcode.cn/problems/number-of-boomerangs) |  |\n| 456 | [模式](https://leetcode.cn/problems/132-pattern) |  |\n| 1534 | [統計好三元組](https://leetcode.cn/problems/count-good-triplets) | 1279 |\n| 1930 | [長度為 3 的不同迴文子序列](https://leetcode.cn/problems/unique-length-3-palindromic-subsequences) | 1533 |\n| 2242 | [節點序列的最大得分](https://leetcode.cn/problems/maximum-score-of-a-node-sequence) | 2304 |\n| 2552 | [統計上升四元組](https://leetcode.cn/problems/count-increasing-quadruplets) | 2433 |\n| 2867 | [統計樹中的合法路徑數目](https://leetcode.cn/problems/count-valid-paths-in-a-tree) | 2428 |\n| 2874 | [有序三元組中的最大值 II](https://leetcode.cn/problems/maximum-value-of-an-ordered-triplet-ii) | 1583 |\n| 2909 | [元素和最小的山形三元組 II](https://leetcode.cn/problems/minimum-sum-of-mountain-triplets-ii) | 1479 |\n| 3067 | [在帶權樹網路中統計可連線伺服器對數目](https://leetcode.cn/problems/count-pairs-of-connectable-servers-in-a-weighted-tree-network) | 1909 |\n| 3073 | [最大遞增三元組](https://leetcode.cn/problems/maximum-increasing-triplet-value) |  |\n| 3128 | [直角三角形](https://leetcode.cn/problems/right-triangles) | 1541 |\n| 3257 | [放三個車的價值之和最大 II](https://leetcode.cn/problems/maximum-value-sum-by-placing-three-rooks-ii) | 2553 |\n| 3455 | [最短匹配子字串](https://leetcode.cn/problems/shortest-matching-substring) | 2303 |\n| 3583 | [統計特殊三元組](https://leetcode.cn/problems/count-special-triplets) | 1510 |\n\n## 本節重點速查\n\n- 固定中間的 $j$，左右各一份摘要，貢獻寫成 $L(j) \\times R(j)$：$O(n^3) \\to O(n)$。\n- 節奏固定三步：**移出右 → 算貢獻 → 加入左**。順序錯了只在重複值的測資上暴露。\n- **乘法合併的前提是條件可分解**：$(i,j,k)$ 合法 $\\iff P(i,j) \\wedge Q(j,k)$，**不能有直接連結 $i$ 與 $k$ 的項**。1534 就是不可拆的反例。\n- 條件耦合了 $i$ 與 $k$ 時，**把耦合的那一維提到最外層枚舉**（1930 枚舉 26 個字元）。\n- 摘要是極值／前綴和 → 用後綴陣列預算，寫起來更短；摘要是計數表 → 必須用三步節奏。\n- 447 要的是有序對，用 $c(c-1)$；距離用平方且開 `long long`。\n- 排除重複端點時「保留前 $k+1$ 名」，$k$ 是最多要排除的個數（2242 保留前 3）。\n",
        },
        {
          id: 7,
          title: "1.4 遍歷對角線",
          summary:
            "## 這個技術解決什麼問題\n\n矩陣題若要「沿對角線」處理（498 對角線遍歷、1329 按對角線排序、3446 按對角線排序、2711 對角線相異值、562 最長連續 1 線段、面試題 17.23 最大黑方陣），直覺寫法是「找到每條對角線的起點，然後 `while` 迴圈 `++i, ++j` 走到出界」。起點的計算分成「第一行」與「最後一列」兩段，$m \\ne n$ 時邊界極易寫錯。\n\n本節要建立的是一個更省事的視角：**同一條對角線上的格子有一個共同的整數編號，於是「沿對角線處理」等價於「按編號分組」**。分組之後，二維問題就變成一堆一維問題，邊界問題全部消失。\n\n## 辨識題型的訊號\n\n看到這些特徵，就往「遍歷對角線」想：\n\n- **題目明確提到「對角線」**，或要比較 / 排序 / 統計斜向相鄰的格子（題 498、1329、2711、面試題 17.23 最大黑方陣）。\n- **你發現某性質只跟 `i - j` 或 `i + j` 有關**，那就是對角線在暗示你。\n- **沿對角線需要前綴和、最長連續段之類的累積資訊**（題 562 最長連續 1 線段），順著走最自然。\n\n:::example 嗅到味道的瞬間：分組鍵正好是 i - j 或 i + j\n```cpp\n// 嗅到味道的瞬間：分組鍵正好是 i - j 或 i + j\n// diag[i - j].push_back(a[i][j]);\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要的對角線是 ↘（`i - j`）還是 ↙（`i + j`）方向？\n2. 我該用「分組到 map」還是「順著對角線走」？\n3. 負索引、起點、邊界這些細節我處理好了嗎？\n\n## 核心想法與直覺\n\n:::example 對角線的身分證：i - j 標一條「↘ 對角線」，i + j 標一條「↙ 對角線」\n本節處理二維矩陣裡「沿對角線」的問題。先認識一個非常好用的小事實：在一個矩陣中，**同一條「左上到右下」的對角線上，所有格子的 `i - j` 都相等**；而「右上到左下」的對角線上，所有格子的 `i + j` 都相等。\n\n```cpp\n// 對角線的身分證：i - j 標一條「↘ 對角線」，i + j 標一條「↙ 對角線」\nint d1 = i - j;  // 同一條 ↘ 對角線上的格子，d1 都一樣\nint d2 = i + j;  // 同一條 ↙ 對角線上的格子，d2 都一樣\n```\n:::\n\n\n\n:::example 遍歷對角線：用 i - j 把格子分組（用 map 自動歸類）\n有了這個 key，就能把「沿對角線處理」變成「先把同一條對角線的格子分到同一組，再各組分別處理」。例如題 1329「將矩陣按對角線排序」，就是把每條 ↘ 對角線的元素收集起來、排序、再寫回去。\n\n```cpp\n// 遍歷對角線：用 i - j 把格子分組（用 map 自動歸類）\nmap<int, vector<int>> diag;\nfor (int i = 0; i < m; ++i) {\n    for (int j = 0; j < n; ++j) {\n        diag[i - j].push_back(a[i][j]);  // 同一條對角線進同一個 vector\n    }\n}\n```\n:::\n\n\n\n:::example 順著一條 ↘ 對角線走：i、j 同步加一直到出界\n另一種寫法不用 map，而是「順著對角線走」：固定一條對角線的起點，然後同時 `i++, j++`（或 `i++, j--`）一路走到邊界。這在題 498「對角線遍歷」、題 2711（對角線上不同值的數量）很常見，因為我們需要邊走邊維護「沿對角線的前綴/後綴資訊」。\n\n```cpp\n// 順著一條 ↘ 對角線走：i、j 同步加一直到出界\nwhile (i < m && j < n) {\n    // process(a[i][j]);   // 這條對角線上，從左上往右下逐格處理\n    ++i;\n    ++j;\n}\n```\n:::\n\n\n\n無論哪種寫法，核心都是「**把二維問題沿固定方向切成一條條一維線，再逐線解決**」。\n\n## 狀態／資料結構定義\n\n對 $m \\times n$ 矩陣中的格子 $(r, c)$，$0 \\le r < m$、$0 \\le c < n$：\n\n- **主對角線（↘，往右下）編號**：$d_1 = r - c$，範圍 $[-(n-1), m-1]$，共 $m + n - 1$ 條。\n- **副對角線（↙／↗，往右上）編號**：$d_2 = r + c$，範圍 $[0, m+n-2]$，共 $m + n - 1$ 條。\n\n負編號有兩種處理：用 `map<int, vector<int>>`（自動排序，$O(\\log)$），或**加偏移量** $d_1 + n - 1 \\in [0, m+n-2]$ 後用 `vector<vector<int>>`（$O(1)$，常數小得多，推薦）。\n\n**每條對角線內的順序**：按 $r$ 遞增即是「從左上往右下」。分組時只要外層 `for r`、內層 `for c`，`push_back` 的順序天然就是 $r$ 遞增，不需額外排序。\n\n## 不變量或正確性證明\n\n**引理（對角線的判準）。** 兩個格子 $(r_1,c_1)$、$(r_2,c_2)$ 在同一條 ↘ 對角線上 $\\iff r_1 - c_1 = r_2 - c_2$；在同一條 ↗ 對角線上 $\\iff r_1 + c_1 = r_2 + c_2$。\n\n*證明。* ($\\Rightarrow$) 沿 ↘ 方向移動一步是 $(r,c) \\to (r+1, c+1)$，$r - c$ 是這個變換的不變量；沿路徑走 $t$ 步後仍相等。\n($\\Leftarrow$) 設 $r_1 - c_1 = r_2 - c_2$，令 $t = r_2 - r_1$。由等式得 $c_2 - c_1 = t$，故從 $(r_1,c_1)$ 沿 ↘ 走 $t$ 步（$t<0$ 則往 ↖ 走 $|t|$ 步）恰好到達 $(r_2,c_2)$，且路徑上每一格都在矩陣內（因為 $r$ 從 $r_1$ 單調走到 $r_2$、$c$ 同步，兩端都在界內，中間必然也在）。↗ 同理，此時 $c_2 - c_1 = -t$。$\\square$\n\n**這條引理點名了它依賴的前提：矩陣是「矩形」的。** 中間走過的格子必定在界內，用到的是 $[0,m) \\times [0,n)$ 這個區域對「兩端點在內則線段在內」封閉（凸性）。若題目給的是不規則的鋸齒陣列（每列長度不同），$r - c$ 相同的兩格未必真的沿對角線相連，這個判準就不能直接用。\n\n**分組法與「順著走」等價。** 定義映射 $(r,c) \\mapsto (d_1, r) = (r - c, r)$。它是單射（由 $d_1$ 與 $r$ 唯一還原 $c = r - d_1$），因此把每格放進 `diag[d1]` 並按 $r$ 排序，得到的就是「順著對角線從左上走到右下」的序列，一格不多一格不少。$\\square$\n\n**1329／3446（按對角線排序）的正確性。** 收集 → 排序 → 寫回，三步。寫回時必須**用與收集時完全相同的走訪順序**，否則會把排好的值填錯位置。最穩妥的寫法是為每條對角線維護一個游標 `idx[d]`，收集與寫回都用同一組雙層迴圈：因為映射是單射，第二次雙層迴圈訪問 $(r,c)$ 時取到的 `diag[d1][idx[d1]++]` 必定對應同一個位置。**不要在寫回時改用「順著走」的迴圈**——順序看似一樣，但一旦某條對角線的起點算錯，錯誤會非常隱蔽。\n\n**498（鋸齒對角線遍歷）的方向規則。** 題目要求編號 $s = r + c$ 為**偶數**時由左下往右上（$r$ 遞減），奇數時由右上往左下（$r$ 遞增）。若按 $r$ 遞增收集，則偶數條需要 `reverse`。驗證：$s = 0$ 只有 $(0,0)$；$s = 1$ 應輸出 $(0,1), (1,0)$，即 $r$ 遞增，符合「奇數不反轉」。**這種「規則從第一個例子反推」的驗證，比記憶方向可靠。**\n\n**562（最長連續 1 線段）四個方向的統一。** 水平（$c$ 遞增，鍵是 $r$）、垂直（$r$ 遞增，鍵是 $c$）、↘（鍵 $r-c$）、↗（鍵 $r+c$）。四個方向用同一套「按鍵分組 + 在組內求最長連續 1」的骨架，只換鍵與組內順序。**這說明「對角線」不是特例，而是「按線性函數分組」這個更一般模式的四個實例。**\n\n## 逐步演算法\n\n:::example 狀態表示：分組容器（key 是對角線編號）\n**1. 狀態表示：選定對角線方向與 key。** ↘ 用 `i - j`，↙ 用 `i + j`；決定要分組還是順著走。\n\n```cpp\n// 狀態表示：分組容器（key 是對角線編號）\nmap<int, vector<int>> diag;  // i - j -> 這條對角線的元素\n```\n:::\n\n\n\n:::example 收集：用 i - j 歸類（注意 i - j 可能為負，用 map 或加偏移量）\n**2. 收集 / 遍歷：把同條對角線的格子湊在一起。**\n\n```cpp\n// 收集：用 i - j 歸類（注意 i - j 可能為負，用 map 或加偏移量）\nfor (int i = 0; i < m; ++i) {\n    for (int j = 0; j < n; ++j) {\n        diag[i - j].push_back(a[i][j]);\n    }\n}\n```\n:::\n\n\n\n:::example 處理：每條對角線排序後，按原順序寫回矩陣（題 1329）\n**3. 逐線處理後寫回：對每組做排序 / 累計 / 比較。**\n\n```cpp\n// 處理：每條對角線排序後，按原順序寫回矩陣（題 1329）\nfor (auto& [d, v] : diag) {\n    sort(v.begin(), v.end());\n}\n// 再開一輪雙迴圈，用 idx 從各自的 v 依序取回填入 a[i][j]\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- `i - j` 會是負數，用 `map` 自動處理，或加上偏移量 `i - j + n` 改用陣列。\n- 順著走的寫法要小心起點：第一條到最後一條對角線的起點分別在第一列與第一行（或最後一行）。\n- 複雜度：每個格子只被處理一次，分組是 `O(mn)`；若每條對角線排序，總共 `O(mn log(mn))`。\n\n## C++17 模板\n\n以下用本節代表題 498. 對角線遍歷 展示核心寫法。\n\n**完整題目**：給定矩陣，依題目指定的鋸齒狀對角線順序回傳所有元素。\n\n:::example 對角線順序遍歷：按反對角線編號 s = r + c 分組，偶數對角線反轉方向\n**線索**：題目看似多重枚舉，但其中一維可以固定，另一側用已掃描資訊或分組鍵維護。\n\n```cpp\nclass Solution {\npublic:\n    // 對角線順序遍歷：按反對角線編號 s = r + c 分組，偶數對角線反轉方向\n    vector<int> findDiagonalOrder(vector<vector<int>>& mat) {\n            int m = mat.size(), n = mat[0].size();\n            vector<int> ans;\n            for (int s = 0; s <= m + n - 2; ++s) {  // s 為反對角線編號，共 m+n-1 條\n                    vector<int> cur;\n                    // 枚舉該對角線上所有合法的列索引\n                    for (int r = max(0, s - n + 1); r <= min(m - 1, s); ++r) {\n                            int c = s - r;\n                            cur.push_back(mat[r][c]);\n                    }\n                    if (s % 2 == 0) {\n                            reverse(cur.begin(), cur.end());  // 偶數對角線由下往上，需反轉\n                    }\n                    ans.insert(ans.end(), cur.begin(), cur.end());\n            }\n            return ans;\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- 同一條副對角線滿足 `r + c` 相同；先按對角線分組，再依題目要求決定是否反轉。\n- 外層迴圈固定一個維度，內層只處理與該維度相關的必要候選。\n- 分組鍵、左右端點或對角線編號要先算清合法範圍，避免漏算邊界。\n\n## 時間與空間複雜度\n\n**分組**：每格恰被訪問一次，$O(mn)$ 時間、$O(mn)$ 空間。\n\n**分組後的處理**：\n- 只掃描 / 求最值 / 前綴和：$O(mn)$。\n- 每條對角線排序：設第 $t$ 條長度 $\\ell_t$，總計 $\\sum_t \\ell_t \\log \\ell_t \\le (\\sum_t \\ell_t) \\log(\\max_t \\ell_t) = O(mn \\log \\min(m,n))$。**注意上界是 $\\log \\min(m,n)$ 而不是 $\\log(mn)$**——最長的對角線只有 $\\min(m,n)$ 格。$m=n=100$ 時這個差別是 $\\log 100 \\approx 7$ 對上 $\\log 10^4 \\approx 13$。\n- 每條對角線內求相異值個數（2711）：用 `unordered_set` 或 26 位元的 `bitset` 掃兩遍（前綴相異數 + 後綴相異數），$O(mn)$。\n\n**空間可以省。** 若只需要順序輸出（498），不必真的建 `vector<vector<int>>`，直接對每個 $s$ 用內層迴圈枚舉合法的 $r \\in [\\max(0, s-n+1), \\min(m-1, s)]$ 即可，空間 $O(1)$（不計輸出）。**這個範圍公式來自兩個約束 $0 \\le r < m$ 與 $0 \\le s - r < n$ 的交集**，是「順著走」寫法的無邊界錯誤版本。\n\n## 常見錯誤與邊界條件\n\n- 先寫出枚舉變數的合法範圍。\n- 二維轉一維時檢查邊界是否越界。\n- 分組鍵可能為負，使用 map 或加偏移。\n- 若每組需要排序，總複雜度要算所有組的總和。\n\n## 常見變形\n\n- 枚舉右端點並維護左側資訊。\n- 枚舉中心點處理左右對稱。\n- 依 `r + c` 或 `r - c` 分組遍歷矩陣對角線。\n\n## 與相似技巧的比較\n\n- **與 grid 主題的 BFS／DFS**：那是沿四連通或八連通移動，關心的是連通性；本節不做移動，只做分組。**如果題目說「斜向相鄰」但要判連通，那是 DFS，不是本節。**\n- **與 1.3 列舉中間**：3128（直角三角形）按「同列」「同行」分組後乘法合併，與本節按 $r \\pm c$ 分組是同一個模式（**按線性函數分組，再對每組獨立計數**），只是鍵不同。\n- **與 N 皇后（trees 4.5）**：那裡用 $r+c$ 與 $r-c+n-1$ 當衝突標記的下標，依據正是本節的引理。**同一條數學事實，一個拿來分組、一個拿來判衝突。**\n- **與二維前綴和（2.6）**：前綴和沿行列方向累積；若題目要的是「沿對角線的前綴和」（如 562、面試題 17.23 的斜向連續段），就要用 `dp[r][c] = dp[r-1][c-1] + 1` 這種**沿對角線的遞推**，本質是在對角線這條一維序列上做前綴，與本節同源。\n- **與 map 與陣列的選擇**：$d_1$ 為負是唯一的麻煩。用 `map` 一行搞定但慢；用偏移量 `d1 + n - 1` 配 `vector` 快得多。**題目規模大就一律用偏移量。**\n\n## 代表例題\n\n**題號**：498、562、1329、面試題 17.23、2711\n\n要固定一個枚舉維度，再維護另一側已看過的資訊；重點是避免雙重暴力重複掃描。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 498 | [對角線遍歷](https://leetcode.cn/problems/diagonal-traverse) |  |\n| 562 | [矩陣中最長的連續1線段](https://leetcode.cn/problems/longest-line-of-consecutive-one-in-matrix)（Premium） |  |\n| 1329 | [將矩陣按對角線排序](https://leetcode.cn/problems/sort-the-matrix-diagonally) | 1548 |\n| 面試題 17.23 | [最大黑方陣](https://leetcode.cn/problems/max-black-square-lcci) |  |\n| 2711 | [對角線上不同值的數量差](https://leetcode.cn/problems/difference-of-number-of-distinct-values-on-diagonals) | 1429 |\n| 3446 | [按對角線進行矩陣排序](https://leetcode.cn/problems/sort-matrix-by-diagonals) | 1373 |\n\n**延伸閱讀**：對角線分組之外，若題目要同時維護每個位置左、右兩側的摘要，可再對照動態規劃章 **17. 前字尾分解**與 [LeetCode 238](https://leetcode.cn/problems/product-of-array-except-self/)。\n\n## 本節重點速查\n\n- **↘ 對角線 $\\iff r - c$ 相同；↗ 對角線 $\\iff r + c$ 相同。** 各有 $m+n-1$ 條。\n- 把「沿對角線走」改成「**按編號分組**」，邊界問題全部消失；分組時外層 `for r`、內層 `for c`，組內順序天然是 $r$ 遞增。\n- $r - c \\in [-(n-1), m-1]$ 為負，**加偏移量 $n-1$** 後用 `vector` 比 `map` 快得多。\n- 排序題（1329／3446）**收集與寫回必須用同一組雙層迴圈 + 每條對角線一個游標**，別在寫回時改成「順著走」。\n- 不建表的寫法：對每個 $s = r+c$，合法 $r \\in [\\max(0, s-n+1), \\min(m-1, s)]$——這個範圍來自兩個下標約束的交集。\n- 每條對角線排序的總成本是 $O(mn \\log \\min(m,n))$，不是 $\\log(mn)$。\n- 引理依賴矩陣是矩形（凸的）；鋸齒陣列不適用。\n",
        },
      ],
    },
    {
      id: 9,
      title: "2. 字首和",
      summary:
        "字首和把「重複累加同一段」換成「一次預處理、之後每次 `O(1)` 相減」。訊號很固定：**陣列不會變動，但要反覆查詢區間的某個彙總量**。\n\n**左閉右開公式**：子陣列 `[left, right)` 的元素和為 `sum[right] - sum[left]`。把下標區間定義成左閉右開，就不需要加一減一了。`sum` 開 `n + 1` 格、`sum[0] = 0`，空區間也能自然處理。\n\n**如何挑子單元**\n\n- **2.1 基礎**：查詢已經直接給了 `left` 與 `right`，套公式即可。\n- **2.2 / 2.7 字首和與雜湊表（或有序集合）**：查詢**沒有**給區間，而是問「有幾個子陣列的和等於 k」「和能被 k 整除」。改成枚舉右端點 `right`，問「左邊有幾個 `sum[left]` 等於 `sum[right] - k`」，用雜湊表計數；若條件是不等式（和 ≥ k、差最小），雜湊表換成有序集合才能查前驅後繼。\n- **2.3 距離和**：先排序，再用字首和把「到所有點的距離總和」拆成左右兩側各自的等差式，避免逐點相減。\n- **2.4 狀態壓縮字首和**：要維護的不是數值總和，而是一組布林（哪些字元出現奇數次），把它壓成 mask 後照樣做前綴 XOR。\n- **2.6 / 2.8 二維字首和**：矩形區域查詢，用容斥 `S[r2][c2] - S[r1][c2] - S[r2][c1] + S[r1][c1]`；同一個下標約定要從頭貫徹到尾。\n\n**最常見的錯**：忘了放入 `sum[0] = 0` 這個「空前綴」，導致「從頭開始的那一段」被漏算。\n\n**延伸閱讀**：[LeetCode 303 前綴和講解](https://leetcode.cn/problems/range-sum-query-immutable/solution/qian-zhui-he-ji-qi-kuo-zhan-fu-ti-dan-py-vaar/)。建議固定左閉右開語意：`sum(left, right) = pre[right] - pre[left]`。\n",
      children: [
        {
          id: 10,
          title: "2.1 基礎",
          summary:
            "## 這個技術解決什麼問題\n\n「回答 $q$ 次『區間 $[l, r]$ 的和是多少』」若每次重掃是 $O(n)$，總計 $O(nq)$。$n = q = 10^5$ 時是 $10^{10}$，不可行。\n\n前綴和把成本重新分配：**預處理一次 $O(n)$，之後每次查詢 $O(1)$**，總計 $O(n + q)$。\n\n這個技巧的價值遠不止於「查區間和」。它真正的內容是一句話：**若一個量沿序列可累加，就把「區間的量」改寫成「兩個前綴的差」。** 只要能寫成差，區間問題就退化成兩點問題，而兩點問題可以配上雜湊表（2.2）、有序集合（2.7）、單調堆疊、二分（2.5）等各種工具。第 2 章後面七節全部建立在這一句話上。\n\n## 辨識題型的訊號\n\n看到這些特徵，就往「前綴和」想：\n\n- **題目反覆問「某段區間的總和 / 個數」**，而且查詢次數多（題 303 區域和檢索）。\n- **暴力是「每次查詢都重掃整段」`O(nq)`**，但其實只要先把累積和算好就能 `O(1)` 回答。\n- **答案能寫成「到 r 為止的量」減「到 l 為止的量」**——只要量是可累加的（和、計數、奇偶），就適用。\n\n:::example 嗅到味道的瞬間：區間量能寫成兩端相減\n```cpp\n// 嗅到味道的瞬間：區間量能寫成兩端相減\n// ans = prefix[r + 1] - prefix[l];\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的 `prefix[i]` 精確代表什麼（前幾個的和）？\n2. 含兩端區間 `[l, r]` 對應的減法下標寫對了嗎？\n3. 我有沒有補上 `prefix[0] = 0` 並用 `long long` 防溢位？\n\n## 核心想法與直覺\n\n本節介紹「前綴和（prefix sum）」，這是處理「區間總和」最基本也最重要的工具。問題場景是：給一個陣列，反覆問「第 `l` 到第 `r` 個元素加起來是多少？」如果每次都從 `l` 加到 `r`，一次查詢就是 `O(n)`，問 `q` 次就是 `O(nq)`，太慢。\n\n:::example 前綴和的定義：prefix[i] = nums[0] + ... + nums[i-1]\n核心想法只有一句：**先算好「從頭到每個位置」的累積和，任何一段區間就能用兩個累積值相減得到。** 定義 `prefix[i]` 為前 `i` 個元素的和（`prefix[0] = 0`，代表「前 0 個」是空的）。\n\n```cpp\n// 前綴和的定義：prefix[i] = nums[0] + ... + nums[i-1]\nvector<long long> prefix(n + 1, 0);  // 多一格放「前 0 個」= 0\nfor (int i = 0; i < n; ++i) {\n    prefix[i + 1] = prefix[i] + nums[i];  // 每格 = 前一格 + 目前元素\n}\n```\n:::\n\n\n\n:::example 區間和 = 兩個前綴的差，O(1) 回答\n有了它，區間 `[l, r]`（含兩端，0-indexed）的和就是一個減法：\n\n```cpp\n// 區間和 = 兩個前綴的差，O(1) 回答\nlong long sum = prefix[r + 1] - prefix[l];  // 砍掉前 l 個，留下 l..r\n```\n:::\n\n\n\n為什麼對？`prefix[r + 1]` 是「前 r+1 個的和」，`prefix[l]` 是「前 l 個的和」，相減正好剩下中間那段。預處理是 `O(n)`，之後每次查詢 `O(1)`，總共 `O(n + q)`，遠勝暴力。\n\n同樣的想法能推廣到二維（前綴矩陣，用容斥算子矩形和）、推廣到「前綴餘數」「前綴奇偶」等其他累積量。重點永遠是「**把區間量改寫成兩個前綴狀態的差**」。\n\n## 狀態／資料結構定義\n\n**定義（前綴和陣列）：**\n$$s_0 = 0, \\qquad s_i = \\sum_{t=0}^{i-1} a_t \\quad (1 \\le i \\le n).$$\n\n長度是 $n+1$，**不是 $n$**。$s_i$ 的語意是「前 $i$ 個元素的和」，即 $a[0..i-1]$。\n\n於是\n$$\\sum_{t=l}^{r} a_t = s_{r+1} - s_l .$$\n\n**多開一格哨兵 $s_0 = 0$ 是本節最重要的實作決定。** 它讓「區間從 0 開始」不需要特判：$\\sum_{t=0}^{r} a_t = s_{r+1} - s_0 = s_{r+1}$。若省掉哨兵而定義 $s_i = a_0 + \\dots + a_i$，公式變成 $s_r - s_{l-1}$，$l = 0$ 時要寫 `l == 0 ? s[r] : s[r] - s[l-1]`——這個三元運算子就是所有前綴和 bug 的溫床。\n\n**可累加的量不限於加法。** 只要 $(\\oplus, e)$ 是一個**群**（有結合律、單位元、且每個元素有逆元），同一套公式就成立：\n\n| 運算 | 單位元 | 逆運算 | 用途 |\n| --- | --- | --- | --- |\n| $+$ | 0 | $-$ | 區間和 |\n| XOR | 0 | XOR 自己 | 區間異或（1738、1310） |\n| $\\times$（無 0） | 1 | 除法 | 區間乘積（724 變體） |\n| 計數 | 0 | $-$ | 區間內某條件的個數（2559、1523） |\n\n**逆元存在是關鍵。** $\\max$ 與 $\\min$ 有結合律、有單位元，但**沒有逆元**——「$\\max$ 前綴」無法相減還原區間 $\\max$。這正是 9.3 線段樹與 9.7 ST 表存在的理由。\n\n## 不變量或正確性證明\n\n**引理。** 對 $0 \\le l \\le r < n$，$\\displaystyle\\sum_{t=l}^{r} a_t = s_{r+1} - s_l$。\n\n*證明。* 由定義 $s_{r+1} = \\sum_{t=0}^{r} a_t$、$s_l = \\sum_{t=0}^{l-1} a_t$。相減得 $\\sum_{t=0}^{r} a_t - \\sum_{t=0}^{l-1} a_t = \\sum_{t=l}^{r} a_t$（第二個和是第一個和的前綴，$l \\le r+1$ 保證下標範圍合法）。$\\square$\n\n**遞推的正確性（歸納）。** `s[i+1] = s[i] + a[i]`：\n- 基底 $s_0 = 0$（空和）。\n- 歸納：若 $s_i = \\sum_{t<i} a_t$，則 $s_i + a_i = \\sum_{t \\le i} a_t = s_{i+1}$。$\\square$\n\n**這條論證點名了三個前提。**\n\n1. **陣列在查詢期間不變。** 一旦某個 $a_j$ 改了，所有 $s_i$（$i > j$）都要更新，$O(n)$。**「單點修改 + 區間查詢」混合出現時，前綴和失效，必須換樹狀陣列（9.1）。** 這是選型的第一判準。\n2. **運算必須有逆元。** 見上表。沒有逆元就不能相減。\n3. **不能溢位。** $s_i$ 是 $i$ 個元素的和，即使每個 $a_t$ 是 `int`，$s_n$ 也可能達到 $n \\cdot \\max|a|$。$n = 10^5$、$|a| \\le 10^9$ 時是 $10^{14}$，**必須 `long long`**。這不是防禦性寫法，是絕大多數前綴和題的實際需求。\n\n**53（最大子陣列和）的前綴和視角。** $\\max_{l \\le r} (s_{r+1} - s_l) = \\max_r \\left( s_{r+1} - \\min_{l \\le r} s_l \\right)$。固定右端點，最佳左端點是「歷史最小前綴」——**這正是 1.1 的「列舉右、維護左」套在前綴和序列上**。Kadane 演算法是同一件事的另一種寫法，但前綴和視角能直接推廣到「長度至少 $k$」「和不超過上限」等變體，Kadane 不行。\n\n**1523（區間內奇數個數）的計數型前綴。** 令 $c_i$ = 前 $i$ 個整數中奇數的個數 $= \\lceil i/2 \\rceil$。答案是 $c_{high+1} - c_{low}$。這裡的「陣列」是隱式的（無限長的整數序列），前綴和用**閉式公式**而非陣列。**當序列有閉式前綴時，連 $O(n)$ 的預處理都省了，$O(1)$ 空間。**\n\n## 逐步演算法\n\n:::example 狀態表示：長度 n+1，prefix[0] = 0\n**1. 狀態表示：定義 `prefix[i]` 的確切含義並補一格零。** 多開一格 `prefix[0] = 0` 能讓邊界公式統一，避免特判。\n\n```cpp\n// 狀態表示：長度 n+1，prefix[0] = 0\nvector<long long> prefix(n + 1, 0);\n```\n:::\n\n\n\n:::example 建表：遞推，每格只看前一格\n**2. 建表：一次線性掃描填好累積和。**\n\n```cpp\n// 建表：遞推，每格只看前一格\nfor (int i = 0; i < n; ++i) {\n    prefix[i + 1] = prefix[i] + nums[i];\n}\n```\n:::\n\n\n\n:::example 讀取：區域和檢索（題 303）的核心\n**3. 答案讀取：區間和用減法 O(1) 取得。** 想清楚下標：含兩端的 `[l, r]` 是 `prefix[r + 1] - prefix[l]`。\n\n```cpp\n// 讀取：區域和檢索（題 303）的核心\nlong long query(int l, int r) { return prefix[r + 1] - prefix[l]; }\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 下標要對齊：用 `prefix[r + 1] - prefix[l]` 就不會差一位；若用 `prefix[r] - prefix[l - 1]` 則 `l = 0` 要特判。\n- 和可能很大，用 `long long` 防溢位。\n- 簡單的奇偶/數量題（如題 1523 統計區間內奇數數目）甚至能用閉式公式取代陣列，本質也是「兩端前綴相減」。\n- 複雜度：建表 `O(n)`，每次查詢 `O(1)`，空間 `O(n)`。\n\n## C++17 模板\n\n以下用同型延伸示範題 560. 和為 K 的子陣列 展示核心寫法。\n\n**完整題目**：給定整數陣列與 `k`，計算和為 `k` 的連續子陣列數量。\n\n:::example 前綴和 + 雜湊表：統計和為 k 的子陣列數量\n**線索**：題目反覆詢問連續區間量；若區間值能由兩個前綴狀態相減，就應優先想到前綴和。\n\n```cpp\nclass Solution {\npublic:\n    // 前綴和 + 雜湊表：統計和為 k 的子陣列數量\n    int subarraySum(vector<int>& nums, int k) {\n            unordered_map<int, int> cnt;  // cnt[pre] = 前綴和為 pre 的出現次數\n            cnt[0] = 1;                   // 空前綴，處理從頭開始的子陣列\n            int pre = 0, ans = 0;\n            for (int x : nums) {\n                    pre += x;             // 更新前綴和\n                    ans += cnt[pre - k];  // 若 pre - k 曾出現，代表存在一段子陣列和為 k\n                    cnt[pre]++;\n            }\n            return ans;\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- 若當前前綴和為 `pre`，要找和為 k 的左端點，就需要之前出現過 `pre - k`。\n- 前綴狀態先保存空前綴，讓從下標 0 開始的區間也能統一處理。\n- 查詢區間時只做兩個前綴狀態相減；計數題則查找歷史中需要的前綴值。\n\n## 時間與空間複雜度\n\n預處理 $O(n)$ 時間、$O(n)$ 空間；單次查詢 $O(1)$。$q$ 次查詢總計 $O(n + q)$。\n\n與 $O(nq)$ 的暴力相比：$n = q = 10^5$ 時 $10^{10} \\to 2\\times10^5$。\n\n**空間可以省成 $O(1)$ 的情形**：若查詢只有一次、或前綴和只被順序使用一次（53、2559 的部分寫法），用一個滾動變數即可，不必存整個陣列。**判準是「會不會回頭查任意位置的前綴」**：會，就存陣列；不會，就用變數。\n\n**多維推廣**：二維是 $O(mn)$ 預處理、$O(1)$ 查詢（2.6）；$d$ 維是 $O(n^d)$ 預處理、$O(2^d)$ 查詢（容斥的項數）。\n\n## 常見錯誤與邊界條件\n\n- 前綴陣列常開 n+1，讓空前綴為 0。\n- 區間 `[l, r]` 對應 `pre[r + 1] - pre[l]`。\n- 含負數時不能用滑動視窗替代前綴和。\n- 和可能溢位時用 long long。\n\n## 常見變形\n\n- 一維前綴和、二維前綴和。\n- 前綴和 + hash map 統計子陣列數。\n- 前綴和 + 有序集合處理上下界或最大不超過 k。\n\n## 與相似技巧的比較\n\n- **與差分（3.1）**：前綴和與差分互為逆運算。前綴和是「多次查詢、不修改」；差分是「多次區間修改、最後一次查詢」。**兩者都要求修改與查詢分階段**；一旦交錯，就得升級到樹狀陣列或線段樹。\n- **與樹狀陣列（9.1）**：樹狀陣列 = 支援單點修改的前綴和，代價是查詢從 $O(1)$ 變 $O(\\log n)$。**只有真的需要修改才付這個代價。**\n- **與 ST 表（9.7）／線段樹（9.3）**：$\\max/\\min/\\gcd$ 沒有逆元，前綴相減不成立，必須用這兩者。**「有沒有逆元」是這個選型分岔的唯一判準。**\n- **與滑動視窗**：滑動視窗也算區間和，但它依賴「元素非負 ⇒ 區間和隨長度單調」，才能雙指標同向移動。**有負數時滑動視窗失效，前綴和 + 雜湊表（2.2）接手。** 反之，全正數時滑動視窗是 $O(1)$ 空間，更好。\n- **與 1.1**：53 的解法就是 1.1 套在前綴和序列上。**第 2 章大部分內容都可以理解為「先做前綴和轉換，再套第 1 章的技巧」。**\n\n## 代表例題\n\n**題號**：53、303、3511、3540、1523\n\n要把區間量改寫成前綴狀態之差；若是計數題，重點是查找過去需要的前綴值。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 53 | [最大子陣列和](https://leetcode.cn/problems/maximum-subarray) |  |\n| 303 | [區域和檢索 - 陣列不可變](https://leetcode.cn/problems/range-sum-query-immutable) |  |\n| 3511 | [構造正陣列](https://leetcode.cn/problems/make-a-positive-array)（Premium） |  |\n| 3540 | [訪問所有房屋的最短時間](https://leetcode.cn/problems/minimum-time-to-visit-all-houses)（Premium） |  |\n| 1523 | [在區間範圍內統計奇數數目](https://leetcode.cn/problems/count-odd-numbers-in-an-interval-range) | 1209 |\n| 3427 | [變長子陣列求和](https://leetcode.cn/problems/sum-of-variable-length-subarrays) | 1216 |\n| 848 | [字母移位](https://leetcode.cn/problems/shifting-letters) | 1353 |\n| 2559 | [統計範圍內的母音字串數](https://leetcode.cn/problems/count-vowel-strings-in-ranges) | 1435 |\n| 1310 | [子陣列異或查詢](https://leetcode.cn/problems/xor-queries-of-a-subarray) | 1460 |\n| 3152 | [特殊陣列 II](https://leetcode.cn/problems/special-array-ii) | 1523 |\n| 1749 | [任意子陣列和的絕對值的最大值](https://leetcode.cn/problems/maximum-absolute-sum-of-any-subarray) | 1542 |\n| 3361 | [兩個字串的切換距離](https://leetcode.cn/problems/shift-distance-between-two-strings) | 1553 |\n| 3652 | [按策略買賣股票的最佳時機](https://leetcode.cn/problems/best-time-to-buy-and-sell-stock-using-strategy) | 1557 |\n\n## 本節重點速查\n\n- $s_0 = 0$、$s_i = a_0 + \\dots + a_{i-1}$，長度 **$n+1$**；區間和 $= s_{r+1} - s_l$。\n- **哨兵 $s_0 = 0$ 是為了讓 $l = 0$ 不必特判**，省掉三元運算子這個 bug 溫床。\n- 公式記不清就驗 $l = r$：$s_{r+1} - s_r = a_r$ ✔。\n- 成立的三個前提：**陣列不變、運算有逆元、不溢位**。$\\max/\\min$ 沒有逆元，要用線段樹或 ST 表；要修改就用樹狀陣列。\n- 前綴和一律開 `long long`。\n- 53 = 前綴和 + 1.1：$\\max_r (s_{r+1} - \\min_{l \\le r} s_l)$，`minPrefix` 初值是 $s_0 = 0$，且**先算答案再更新**。\n- 有閉式前綴（1523）時連陣列都不用建，$O(1)$ 空間。\n",
        },
        {
          id: 12,
          title: "2.2 字首和與雜湊表",
          summary:
            "## 這個技術解決什麼問題\n\n2.1 回答的是「給定區間，求它的和」。本節反過來：**給定和的條件，求有幾個區間（或最長／最短的區間）滿足它。** 區間本身是未知數。\n\n- 560：和恰為 $k$ 的子陣列個數。\n- 325：和為 $k$ 的最長子陣列。\n- 523／974：和可被 $k$ 整除的子陣列。\n- 525：0 與 1 個數相等的最長子陣列。\n- 437：樹上和為 target 的路徑數。\n- 1524：和為奇數的子陣列個數。\n\n若陣列全為正數，滑動視窗就能解（$O(1)$ 空間）。**但只要有負數（或 0），加入一個元素不保證讓和變大，雙指標無法單調推進，滑動視窗徹底失效。** 本節是這種情形的通解。\n\n核心只有一步改寫：\n$$\\sum_{t=l}^{r} a_t = k \\iff s_{r+1} - s_l = k \\iff s_l = s_{r+1} - k .$$\n\n左邊是「區間條件」（兩個未知數），右邊是「兩個前綴值之間的關係」（配對問題）。**於是 1.1 的「列舉右、維護左」可以原封不動套上去**——只是把原陣列換成前綴和序列。\n\n## 辨識題型的訊號\n\n看到這些特徵，就往「前綴和 + 雜湊表」想：\n\n- **問「有幾個 / 最長的子陣列，其和滿足某條件」，而且區間沒被指定**（題 437 路徑總和 III、題 325 和為 k 的最長子陣列）。\n- **有負數，滑動視窗失效**——因為加元素不一定讓和變大，雙指標推不動，這時前綴和 + map 才是正解。\n- **條件能改寫成「目前前綴」與「歷史前綴」的相等或同餘關係**（和為 k、可被 k 整除、0/1 平衡）。\n\n:::example 嗅到味道的瞬間：條件能移項成 historyPrefix = curPrefix - k\n```cpp\n// 嗅到味道的瞬間：條件能移項成 historyPrefix = curPrefix - k\n// ans += cnt[pre - k];\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的 map key 是「前綴和」還是它的某種變形（餘數、平衡值）？\n2. 我把條件移項成「目前前綴需要哪個歷史前綴」了嗎？\n3. 初始值（`cnt[0] = 1` 或 `first[0] = -1`）有沒有設對？\n\n## 核心想法與直覺\n\n本節把「前綴和」和「雜湊表（hash map）」結合起來，專門解「有幾個子陣列滿足某個和的條件」這類**計數**問題。基礎前綴和回答的是「指定區間 `[l, r]` 的和」；但很多題沒有指定區間，而是問「和等於 `k` 的子陣列有幾個」「最長的這種子陣列多長」，這時要換個角度。\n\n關鍵改寫：子陣列 `[l, r]` 的和 = `prefix[r + 1] - prefix[l]`。若要它等於 `k`，就是 `prefix[r + 1] - prefix[l] == k`，移項得到 `prefix[l] == prefix[r + 1] - k`。\n\n:::example 前綴和 + 雜湊表：統計和為 k 的子陣列個數（題 560 型）\n換句話說：**固定右端點 `r`，我需要的是「左邊有幾個前綴和恰好等於 `prefix[r + 1] - k`」。** 這正好是「列舉右、維護左」：邊掃邊把看過的前綴和丟進 map 計數，當前位置就能 `O(1)` 查到有多少合法左端點。\n\n```cpp\n// 前綴和 + 雜湊表：統計和為 k 的子陣列個數（題 560 型）\nunordered_map<long long, int> cnt;\ncnt[0] = 1;  // 關鍵初始：空前綴 prefix=0 出現過 1 次\nlong long pre = 0;\nint ans = 0;\nfor (int x : nums) {\n    pre += x;             // pre 就是目前的 prefix[r+1]\n    ans += cnt[pre - k];  // 左邊有幾個前綴和 = pre - k\n    cnt[pre]++;           // 把目前前綴和加入 map，留給後面用\n}\n```\n:::\n\n\n\n`cnt[0] = 1` 這行很重要：它代表「什麼都不取的空前綴」，這樣「從頭開始、整段和就等於 k」的情況才會被算到。\n\n這個框架極有彈性：把 map 的 key 換成「前綴和 `% k`」就能做「可被 k 整除」（題 523），key 換成「前綴和的奇偶平衡值」就能做「0、1 數量相等」（題 525）。萬變不離其宗：**把條件整理成「目前前綴」與「某個歷史前綴」的關係，再用 map 查歷史。**\n\n## 狀態／資料結構定義\n\n- **前綴和** $s_i$（沿用 2.1 的定義，$s_0 = 0$）。實作上通常不存陣列，只用一個滾動變數 `s`。\n- **摘要** `cnt`：把「已出現的前綴值」映射到題目需要的東西：\n  - 數個數（560、974、1524）→ `unordered_map<long long,int>`：前綴值 → 出現次數。\n  - 求最長（325、525、1124）→ `unordered_map<long long,int>`：前綴值 → **最早**出現的下標。\n  - 求最短（不常見）→ 前綴值 → 最晚出現的下標。\n  - 判存在（523 要求長度 $\\ge 2$）→ 前綴值 → 最早下標，再檢查距離。\n- **鍵的選擇**：條件不是「和為 $k$」而是某種同餘或狀態時，鍵要換成特徵（這就是 1.2 的 $\\varphi$）：\n\n| 題目 | 條件 | 鍵 $\\varphi(s)$ | 查詢的鍵 |\n| --- | --- | --- | --- |\n| 560 | 和 $= k$ | $s$ | $s - k$ |\n| 974／523 | 和 $\\equiv 0 \\pmod k$ | $((s \\bmod k) + k) \\bmod k$ | 同一個鍵 |\n| 525 | 0/1 個數相等 | 把 0 換成 $-1$ 後的 $s$ | 同一個鍵 |\n| 1524 | 和為奇數 | $s \\bmod 2$ | $1 - (s \\bmod 2)$ |\n| 1590 | 移除最短子陣列使總和可被 $p$ 整除 | $s \\bmod p$ | $(s - \\text{total}) \\bmod p$ |\n\n**「和 $\\equiv 0$」與「和 $= k$」的差別**：前者查的是**同一個鍵**（兩個前綴同餘 ⟹ 差可被 $k$ 整除），後者查的是**平移過的鍵**。這個差別是 974 與 560 唯一的不同。\n\n## 不變量或正確性證明\n\n**主不變量。** 在處理右端點 $r$（即前綴 $s_{r+1}$）並執行查詢的那一刻，`cnt` 恰好記錄了 $\\{s_0, s_1, \\dots, s_r\\}$ 的資訊——**包含 $s_0 = 0$，不包含 $s_{r+1}$**。\n\n*初始化*：迴圈開始前 `cnt[0] = 1`（或 `pos[0] = -1`），對應 $s_0$。\n*保持*：每輪先查詢，再把當前的 $s_{r+1}$ 併入，於是下一輪的 `cnt` 是 $\\{s_0..s_{r+1}\\}$。\n\n**`cnt[0] = 1` 這一行的意義。** 它代表「空前綴」$s_0 = 0$。以子陣列 $[0, r]$ 為例，它對應 $l = 0$，需要在摘要中找到 $s_0$。**若漏掉這行初始化，所有「從下標 0 開始」的子陣列全部不會被計入。** 這是本節第一大坑，而且在測資的第一個子陣列不從 0 開始時完全不會暴露。\n\n求最長的版本對應的是 `pos[0] = -1`：長度 $= r - l + 1 = r - (\\text{記錄的下標})$，$s_0$ 的「下標」是 $-1$，才能算出 $[0, r]$ 的長度為 $r + 1$。\n\n**不重不漏。** 每個滿足條件的子陣列 $[l, r]$ 唯一對應一組 $(s_l, s_{r+1})$，其中 $l \\le r$。由不變量，處理 $r$ 時 $s_l$（$l \\le r$）都已在摘要中且 $s_{r+1}$ 尚未進入，故：\n- *不漏*：$s_l$ 一定被找到。\n- *不重*：每個子陣列只有一個右端點，只被統計一次。\n- *不多算*：$s_{r+1}$ 未進入摘要，排除了 $l = r+1$（空子陣列）。**這就是「先查後加」在本節的具體後果。**\n\n**求最長時為何存「最早」的下標。** 對固定右端點 $r$，要讓 $r - l$ 最大就要讓 $l$ 最小。同一個前綴值可能出現多次，只有**第一次**出現的下標能給出最長區間，後續的出現一律不覆蓋。寫成 `if (!pos.count(key)) pos[key] = i;`。**若寫成無條件覆蓋，答案會偏小；這個錯誤只在有重複前綴值時暴露。**\n\n反過來，求「和恰為 $k$ 的**個數**」時必須記全部出現次數，不能只記第一次。**「求個數」與「求最長」的摘要語意不同，不能互抄。**\n\n**負數取模的陷阱（974／523）。** 兩前綴 $s_i \\equiv s_j \\pmod k$ $\\iff$ $k \\mid (s_i - s_j)$。這個等價需要用**數學上的模**（結果落在 $[0, k)$），但 C++ 的 `%` 對負被除數回傳負值：`-1 % 3 == -1`。於是 $s_i = -1$ 與 $s_j = 2$ 明明同餘，鍵卻分別是 $-1$ 與 $2$，配不上。必須寫 `((s % k) + k) % k`。**這是本節第二大坑。**\n\n**525 的 $0 \\to -1$ 變換。** 「$[l,r]$ 中 0 與 1 的個數相等」$\\iff$ 把 0 記為 $-1$、1 記為 $+1$ 後，$\\sum_{t=l}^{r} b_t = 0$。*證明*：設區間內有 $z$ 個 0、$o$ 個 1，則 $\\sum b = o - z$，等於 0 $\\iff o = z$。$\\square$ **這個變換把「兩個計數相等」轉成「和為零」，是把不可累加的條件轉成可累加的標準手法**，與 805 的均值變換同源。\n\n**437（樹上路徑和）的移植。** 把「陣列的前綴」換成「根到當前節點的路徑和」，`cnt` 記錄根到路徑上各祖先的前綴值。關鍵差別是**回溯時必須把當前節點的前綴從 `cnt` 移除**，否則兄弟子樹會互相污染——因為不變量要求「摘要恰為當前路徑上的祖先」，而不是「所有訪問過的節點」。這與 trees 4.7 中「路徑搜尋的 `vis` 必須還原」是同一條紀律。\n\n## 逐步演算法\n\n:::example 狀態表示：計數型 vs 最長型\n**1. 狀態表示：選對 map 的 key 與 value。** 計數題 value 存「出現次數」；求最長題 value 存「該前綴第一次出現的索引」。\n\n```cpp\n// 狀態表示：計數型 vs 最長型\nunordered_map<long long, int> cnt;  // key: 前綴(或其餘數) -> 出現次數\nunordered_map<long long, int> first;  // key -> 最早出現的索引（求最長用）\n```\n:::\n\n\n\n:::example 更新（計數）：先查歷史，再把自己加入\n**2. 推導查詢量：目前前綴需要哪個歷史前綴。** 把條件移項成 `historyKey = f(curPrefix)`。\n\n```cpp\n// 更新（計數）：先查歷史，再把自己加入\npre += x;\nans += cnt[pre - k];  // 需要的歷史前綴 = pre - k\ncnt[pre]++;\n```\n:::\n\n\n\n:::example 最長型（題 525 連續陣列）：把 0 視作 -1，找和為 0 的最長段\n**3. 答案讀取：計數題累加次數，最長題取索引差。**\n\n```cpp\n// 最長型（題 525 連續陣列）：把 0 視作 -1，找和為 0 的最長段\nif (first.count(pre)) {\n    ans = max(ans, i - first[pre]);\n} else {\n    first[pre] = i;  // 只記最早出現的位置，差才會最大\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 計數型一定要 `cnt[0] = 1`；最長型要 `first[0] = -1`（代表開頭前的位置）。\n- 「先查再加」順序不能反，否則會把自己算進去。\n- 餘數題用 `((pre % k) + k) % k` 處理負數。\n- 複雜度：單層掃描加 `O(1)` 雜湊操作，整體 `O(n)`，空間 `O(n)`。\n\n## C++17 模板\n\n以下用本節代表題 560. 和為 K 的子陣列 展示核心寫法。\n\n**完整題目**：給定整數陣列與 `k`，計算和為 `k` 的連續子陣列數量。\n\n:::example 前綴和 + 雜湊表：統計和為 k 的子陣列數量\n**線索**：題目反覆詢問連續區間量；若區間值能由兩個前綴狀態相減，就應優先想到前綴和。\n\n```cpp\nclass Solution {\npublic:\n    // 前綴和 + 雜湊表：統計和為 k 的子陣列數量\n    int subarraySum(vector<int>& nums, int k) {\n            unordered_map<int, int> cnt;  // cnt[pre] = 前綴和為 pre 的出現次數\n            cnt[0] = 1;                   // 空前綴，處理從頭開始的子陣列\n            int pre = 0, ans = 0;\n            for (int x : nums) {\n                    pre += x;             // 更新前綴和\n                    ans += cnt[pre - k];  // 若 pre - k 曾出現，代表存在一段子陣列和為 k\n                    cnt[pre]++;\n            }\n            return ans;\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- 若當前前綴和為 `pre`，要找和為 k 的左端點，就需要之前出現過 `pre - k`。\n- 前綴狀態先保存空前綴，讓從下標 0 開始的區間也能統一處理。\n- 查詢區間時只做兩個前綴狀態相減；計數題則查找歷史中需要的前綴值。\n\n## 時間與空間複雜度\n\n$O(n)$ 期望時間（雜湊表單次操作期望 $O(1)$），$O(n)$ 空間（最壞情形所有前綴值互異）。\n\n**與滑動視窗的取捨**：全正數時滑動視窗是 $O(n)$ 時間、$O(1)$ 空間，且常數小得多。**只有出現負數或 0 時才需要付出 $O(n)$ 的空間。** 面試中被問「有負數怎麼辦」，答案就是本節。\n\n**鍵的值域小時改用陣列**：974（模 $k$，$k \\le 10^4$）用 `vector<int> cnt(k)` 比 `unordered_map` 快數倍。**只要鍵落在已知的小範圍，一律換陣列。**\n\n437（樹上路徑）：$O(n)$ 時間（每個節點進出摘要各一次）、$O(h)$ 額外空間加上遞迴深度。**樸素的「對每個節點往下 DFS 一遍」是 $O(n^2)$**（鏈狀樹時 $n = 1000$ 就慢），這是本節在樹上帶來的收穫。\n\n## 常見錯誤與邊界條件\n\n- 前綴陣列常開 n+1，讓空前綴為 0。\n- 區間 `[l, r]` 對應 `pre[r + 1] - pre[l]`。\n- 含負數時不能用滑動視窗替代前綴和。\n- 和可能溢位時用 long long。\n\n## 常見變形\n\n- 一維前綴和、二維前綴和。\n- 前綴和 + hash map 統計子陣列數。\n- 前綴和 + 有序集合處理上下界或最大不超過 k。\n\n## 與相似技巧的比較\n\n- **與滑動視窗**：**判準只有一個——有沒有負數。** 全正 → 滑動視窗（$O(1)$ 空間）；有負 → 本節。「和至少為 $k$ 的最短子陣列」在有負數時（862）還需要單調佇列，見 5.4。\n- **與 1.1／1.2**：本節 = 前綴和轉換 + 1.1。鍵換成特徵時就是 1.2。**不是新技巧，是組合。**\n- **與 2.7（前綴和 + 有序集合）**：條件從「等於」變成「不超過／最接近／落在範圍」時，雜湊表沒有用（沒有唯一互補鍵），必須改用有序集合做 `lower_bound`，$O(n \\log n)$。**等號用雜湊，不等號用有序集合。**\n- **與 2.4（狀態壓縮前綴和）**：鍵從數值換成位元遮罩，配對條件從「相等」變成「XOR 後合法」。**同一個骨架，鍵的型別不同。**\n- **與 9.1 樹狀陣列**：「有幾個前綴值小於 $x$」這種帶序的計數，雜湊表做不到，要用樹狀陣列（327 區間和的個數）。**計數 + 不等式 = 樹狀陣列。**\n- **與 2.3（距離和）**：那裡的前綴和用在排序後的陣列上，回答的是「一段區間的和」而非「找滿足條件的區間」，屬於 2.1 的直接應用。\n\n## 代表例題\n\n**題號**：325、437、523、525、548\n\n要把區間量改寫成前綴狀態之差；若是計數題，重點是查找過去需要的前綴值。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 325 | [和等於 k 的最長子陣列長度](https://leetcode.cn/problems/maximum-size-subarray-sum-equals-k)（Premium） |  |\n| 437 | [路徑總和 III](https://leetcode.cn/problems/path-sum-iii) |  |\n| 523 | [連續的子陣列和](https://leetcode.cn/problems/continuous-subarray-sum) |  |\n| 525 | [連續陣列](https://leetcode.cn/problems/contiguous-array) |  |\n| 548 | [將陣列分割成和相等的子陣列](https://leetcode.cn/problems/split-array-with-equal-sum)（Premium） |  |\n| 560 | [和為 K 的子陣列](https://leetcode.cn/problems/subarray-sum-equals-k) |  |\n| 1983 | [範圍和相等的最寬索引對](https://leetcode.cn/problems/widest-pair-of-indices-with-equal-range-sum) |  |\n| 2031 | [比 0 多的子陣列個數](https://leetcode.cn/problems/count-subarrays-with-more-ones-than-zeros) |  |\n| 2489 | [固定比率的子字串數](https://leetcode.cn/problems/number-of-substrings-with-fixed-ratio) |  |\n| 2950 | [可整除子串的數量](https://leetcode.cn/problems/number-of-divisible-substrings) |  |\n| 面試題 17.05 | [字母與數字](https://leetcode.cn/problems/find-longest-subarray-lcci) |  |\n| 1442 | [形成兩個異或相等陣列的三元組數目](https://leetcode.cn/problems/count-triplets-that-can-form-two-arrays-of-equal-xor) | 1525 |\n| 930 | [和相同的二元子陣列](https://leetcode.cn/problems/binary-subarrays-with-sum) | 1592 |\n| 1524 | [和為奇數的子陣列數目](https://leetcode.cn/problems/number-of-sub-arrays-with-odd-sum) | 1611 |\n| 3755 | [最大平衡異或子陣列的長度](https://leetcode.cn/problems/find-maximum-balanced-xor-subarray-length) | 1663 |\n| 974 | [和可被 K 整除的子陣列](https://leetcode.cn/problems/subarray-sums-divisible-by-k) | 1676 |\n| 2588 | [統計美麗子陣列數目](https://leetcode.cn/problems/count-the-number-of-beautiful-subarrays) | 1697 |\n| 3026 | [最大好子陣列和](https://leetcode.cn/problems/maximum-good-subarray-sum) | 1817 |\n| 1477 | [找兩個和為目標值且不重疊的子陣列](https://leetcode.cn/problems/find-two-non-overlapping-sub-arrays-each-with-target-sum) | 1851 |\n| 1546 | [和為目標值且不重疊的非空子陣列的最大數目](https://leetcode.cn/problems/maximum-number-of-non-overlapping-subarrays-with-sum-equals-target) | 1855 |\n| 1124 | [表現良好的最長時間段](https://leetcode.cn/problems/longest-well-performing-interval) | 1908 |\n| 3728 | [邊界與內部和相等的穩定子陣列](https://leetcode.cn/problems/stable-subarrays-with-equal-boundary-and-interior-sum) | 1909 |\n| 3381 | [長度可被 K 整除的子陣列的最大元素和](https://leetcode.cn/problems/maximum-subarray-sum-with-length-divisible-by-k) | 1943 |\n| 2488 | [統計中位數為 K 的子陣列](https://leetcode.cn/problems/count-subarrays-with-median-k) | 1999 |\n| 1590 | [使陣列和能被 P 整除](https://leetcode.cn/problems/make-sum-divisible-by-p) | 2039 |\n| 2845 | [統計趣味子陣列的數目](https://leetcode.cn/problems/count-of-interesting-subarrays) | 2073 |\n| 3739 | [統計主要元素子陣列數目 II](https://leetcode.cn/problems/count-subarrays-with-majority-element-ii) | 2090 |\n| 1074 | [元素和為目標值的子矩陣數量](https://leetcode.cn/problems/number-of-submatrices-that-sum-to-target) | 2189 |\n| 3714 | [最長的平衡子串 II](https://leetcode.cn/problems/longest-balanced-substring-ii) | 2202 |\n| 2025 | [分割陣列的最多方案數](https://leetcode.cn/problems/maximum-number-of-ways-to-partition-an-array) | 2218 |\n| 3729 | [統計有序陣列中可被 K 整除的子陣列數量](https://leetcode.cn/problems/count-distinct-subarrays-divisible-by-k-in-sorted-array) | 2248 |\n| 2949 | [統計美麗子字串 II](https://leetcode.cn/problems/count-beautiful-substrings-ii) | 2445 |\n\n**延伸閱讀**：[LeetCode 560 前綴和＋雜湊表圖解](https://leetcode.cn/problems/subarray-sum-equals-k/solution/qian-zhui-he-ha-xi-biao-cong-liang-ci-bi-4mwr/)，可對照本章 1.1 的「列舉右、維護左」。\n\n## 本節重點速查\n\n- 改寫是全部：$\\sum_{[l,r]} a = k \\iff s_l = s_{r+1} - k$。區間問題 → 配對問題 → 套 1.1。\n- **`cnt[0] = 1` / `pos[0] = -1` 是必寫的一行**，代表空前綴 $s_0 = 0$；漏掉就少算所有從 0 開始的區間。\n- **先查後加**，排除空子陣列。\n- 求**個數** → 存出現次數；求**最長** → 存**最早**下標且找到時不覆蓋。兩者語意不同，別互抄。\n- 「和 $\\equiv 0 \\pmod k$」查**同一個鍵**；「和 $= k$」查**平移過的鍵**。\n- C++ 的 `%` 對負數回傳負值，一律寫 `((s % k) + k) % k`。\n- 「兩個計數相等」→ 把其中一類記為 $-1$，轉成「和為零」（525）。\n- 搬到樹上（437）時，**回溯必須把當前前綴移出摘要**，否則兄弟子樹互相污染。\n- 有負數才用本節；全正數用滑動視窗更省空間。\n",
        },
        {
          id: 14,
          title: "2.3 距離和",
          summary:
            "## 這個技術解決什麼問題\n\n目標函數含絕對值總和的題目：\n\n$$f(x) = \\sum_{i=0}^{n-1} |x - a_i|$$\n\n1685（有序陣列中差絕對值之和）要對每個 $a_j$ 算 $f(a_j)$；2602（使陣列元素全部相等的最少操作次數）要對每個查詢 $q$ 算 $f(q)$；1703（得到連續 K 個 1 的最少相鄰交換次數）與 2968（執行操作使頻率分數最大）要在滑動視窗內算 $f(\\text{中位數})$；2615（等值距離和）按值分組後對每組算。\n\n樸素做法：每個查詢掃全陣列，$O(nq)$。$n = q = 10^5$ 是 $10^{10}$。\n\n**絕對值是障礙，因為它不可累加**（$|x-a|$ 沒有前綴和的遞推）。但只要**排序**，對固定的 $x$，陣列就被切成「$\\le x$」與「$> x$」兩段，每段內部絕對值可以去掉：\n$$f(x) = \\underbrace{\\big(m \\cdot x - \\textstyle\\sum_{i<m} a_i\\big)}_{\\text{左段}} + \\underbrace{\\big(\\textstyle\\sum_{i \\ge m} a_i - (n-m)\\cdot x\\big)}_{\\text{右段}},$$\n其中 $m$ 是滿足 $a_i \\le x$ 的元素個數。兩個 $\\sum$ 都是前綴和，$O(1)$ 取得；$m$ 用二分 $O(\\log n)$ 找。**每次查詢 $O(\\log n)$，$q$ 次總計 $O((n+q)\\log n)$。**\n\n## 辨識題型的訊號\n\n看到這些特徵，就往「距離和」想：\n\n- **目標含 `|x - a_i|` 的總和 / 最小化**，例如「把所有數變成相等的最少操作數」「到所有點的距離和」。\n- **暴力是對每個點掃全部 `O(n^2)`**，但排序後左右各是一段連續區間，可用前綴和拆。\n- **絕對值是障礙**——一旦排序，`|a[i] - a[j]|` 就能依「誰大誰小」拆成有號相減。\n\n:::example 嗅到味道的瞬間：排序後絕對值能拆成「個數×值 - 區間和」\n```cpp\n// 嗅到味道的瞬間：排序後絕對值能拆成「個數×值 - 區間和」\n// left = i * a[i] - pre[i];\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我排序了嗎？排序後左右大小關係確定了嗎？\n2. 我能把左、右兩側的距離各寫成「個數 × 目前值 ∓ 前綴區間和」嗎？\n3. 我用 `long long` 防溢位了嗎？\n\n## 核心想法與直覺\n\n本節是前綴和的一個經典應用：計算「距離和」，也就是「某個點到一群點的距離總和」`sum(|x - a_i|)`。問題場景例如題 2615「等值距離和」：對每個位置，求它到所有其他位置的距離之和。暴力對每個點都掃一遍是 `O(n^2)`，太慢。\n\n關鍵突破是：**只要陣列已排序，絕對值就能拆掉。** 對排好序的 `a`，計算 `a[i]` 到所有點的距離和時，左邊的點都 `<= a[i]`，右邊的點都 `>= a[i]`，於是：\n\n```\nsum |a[i] - a[j]|\n    = (左邊: a[i] 比它們大)  i*a[i] - (a[0]+...+a[i-1])\n    + (右邊: a[i] 比它們小)  (a[i+1]+...+a[n-1]) - (n-1-i)*a[i]\n```\n\n:::example 距離和：排序後，左右兩側用前綴和拆掉絕對值\n左、右兩坨求和都是「一段連續區間的和」，正好用前綴和 `O(1)` 取得。於是每個點 `O(1)`，整體 `O(n)`（排序則 `O(n log n)`）。\n\n```cpp\n// 距離和：排序後，左右兩側用前綴和拆掉絕對值\nsort(a.begin(), a.end());\nvector<long long> pre(n + 1, 0);\nfor (int i = 0; i < n; ++i) {\n    pre[i + 1] = pre[i] + a[i];\n}\n// 對 a[i]：左側和 = pre[i]，右側和 = pre[n] - pre[i+1]\nlong long left = (long long)i * a[i] - pre[i];  // 左邊每個都被 a[i] 減\nlong long right =\n        (pre[n] - pre[i + 1]) - (long long)(n - 1 - i) * a[i];  // 右邊每個都減 a[i]\nlong long ans = left + right;\n```\n:::\n\n\n\n直覺是：左側那群數，每個都要算 `a[i] - a[j]`，共 `i` 個，所以是 `i * a[i]` 減掉「左側總和」；右側對稱。整段絕對值就被前綴和化簡成幾個減法。題 1685（有序陣列差絕對值之和）就是這條公式的直接套用。\n\n## 狀態／資料結構定義\n\n- **排序後**的陣列 $a_0 \\le a_1 \\le \\dots \\le a_{n-1}$。若題目要求輸出保持原順序（1685 的輸入本來就有序；2602 只需按查詢輸出），排序前要記得保存原下標或確認不影響。\n- **前綴和** $s_i = a_0 + \\dots + a_{i-1}$，長度 $n+1$，型別 `long long`。\n- **分界點** $m = |\\{i : a_i \\le x\\}|$。用 `upper_bound(a.begin(), a.end(), x) - a.begin()` 取得。\n- 對「$x$ 恰為某個 $a_j$」的情形（1685、2615），$m$ 就是 $j$ 或 $j+1$，連二分都省了。\n\n**滑動視窗版（1703、2968）**：視窗 $[l, r]$ 內取中位數 $a_{mid}$（$mid = (l+r)/2$），代價是視窗內元素到 $a_{mid}$ 的距離和，同樣用前綴和拆兩段。\n\n## 不變量或正確性證明\n\n**引理一（拆絕對值）。** 設 $a$ 已排序，$m = |\\{i : a_i \\le x\\}|$。則\n$$\\sum_{i=0}^{n-1} |x - a_i| = \\left(m x - s_m\\right) + \\left((s_n - s_m) - (n-m)x\\right).$$\n\n*證明。* 因為 $a$ 遞增，$\\{i : a_i \\le x\\} = \\{0, 1, \\dots, m-1\\}$ 是一個**前綴**——這正是排序的作用，也是唯一用到排序的地方。於是\n- 對 $i < m$：$a_i \\le x$，故 $|x - a_i| = x - a_i$，這 $m$ 項的和是 $mx - \\sum_{i<m} a_i = mx - s_m$。\n- 對 $i \\ge m$：$a_i > x$，故 $|x - a_i| = a_i - x$，這 $n-m$ 項的和是 $(s_n - s_m) - (n-m)x$。\n\n相加即得。$\\square$\n\n**這條論證點名了它依賴的前提：$\\{i : a_i \\le x\\}$ 必須是連續的一段。** 若不排序，這個集合是散落的下標，無法用前綴和表示。**排序不是「為了方便」，是這個公式成立的必要條件。**\n\n**引理二（最佳的 $x$ 是中位數）。** $f(x) = \\sum_i |x - a_i|$ 在 $x = a_{\\lfloor n/2 \\rfloor}$（或任一中位數）處取得最小值。\n\n*證明。* 把 $x$ 從 $-\\infty$ 往右移動。由引理一，\n$$f'(x) = m - (n - m) = 2m - n$$\n（在 $x$ 不等於任何 $a_i$ 處；$f$ 是分段線性的凸函數）。當 $m < n/2$ 時斜率為負（往右走 $f$ 下降），$m > n/2$ 時斜率為正。故最小值出現在斜率變號處，即 $m$ 首次達到 $\\lceil n/2 \\rceil$ 的位置——就是中位數。$n$ 為偶數時斜率在中間一段為 0，故兩個中位數之間**任一點**都最佳。$\\square$\n\n**這個引理的用途**：1703、2968、462（最少移動次數使陣列元素相等 II）都直接靠它把「該選哪個 $x$」這個看似要枚舉的問題變成 $O(1)$。**注意它只對「距離和」成立**；若目標是 $\\sum (x-a_i)^2$，最佳點變成平均數；若是 $\\max_i |x - a_i|$，變成 $(\\min + \\max)/2$。**三種目標，三個不同的最佳點，別記混。**\n\n**1703（最少相鄰交換得到連續 $k$ 個 1）的化約。** 設 1 的位置是 $p_0 < p_1 < \\dots < p_{c-1}$。把 $k$ 個 1 聚到一起的相鄰交換次數，等於把它們移到某段連續位置的距離和。關鍵變換是 $q_t = p_t - t$：\n\n*為什麼要減 $t$*？目標是讓選中的 $k$ 個 1 佔據連續的格子。若最終第 $t$ 個 1 落在位置 $y + t$，則移動距離是 $\\sum_t |p_t - (y+t)| = \\sum_t |q_t - y|$。**減去 $t$ 把「移到連續位置」轉成「移到同一個位置」**，於是引理一、二可以直接套用，$y$ 取 $q$ 在視窗內的中位數。$\\square$\n\n這個變換與 2.2 的「0 換成 $-1$」是同一類手法：**把一個帶結構的條件，透過重新參數化變成標準形。**\n\n**2615（等值距離和）的分組。** 對每個值 $v$，取所有 $\\mathrm{nums}[i] = v$ 的下標 $I_v = \\{i_0 < i_1 < \\dots\\}$。對 $i_t \\in I_v$，答案是 $\\sum_{u} |i_t - i_u|$——這就是引理一，其中「陣列」是 $I_v$、「$x$」是 $i_t$，且 $m = t$（因為 $I_v$ 本身遞增，$x$ 恰是第 $t$ 個元素）。**每組各自建前綴和，總空間仍是 $O(n)$，因為所有組的大小加起來是 $n$。**\n\n## 逐步演算法\n\n:::example 排序：之後 a[i] 左邊都比它小、右邊都比它大\n**1. 排序：把絕對值變成有方向的大小關係。** 距離和的題幾乎都從排序開始（除非順序本身有意義）。\n\n```cpp\n// 排序：之後 a[i] 左邊都比它小、右邊都比它大\nsort(a.begin(), a.end());\n```\n:::\n\n\n\n:::example 前綴和：pre[i] = 前 i 個元素的和\n**2. 建前綴和：左右兩側的區間和都要能 O(1) 取。**\n\n```cpp\n// 前綴和：pre[i] = 前 i 個元素的和\nvector<long long> pre(n + 1, 0);\nfor (int i = 0; i < n; ++i) {\n    pre[i + 1] = pre[i] + a[i];\n}\n```\n:::\n\n\n\n:::example 計算 a[i] 的距離和\n**3. 逐點計算：左貢獻 + 右貢獻。** 用「個數 × 目前值 ∓ 區間和」的公式。\n\n```cpp\n// 計算 a[i] 的距離和\nlong long left = (long long)i * a[i] - pre[i];\nlong long right = (pre[n] - pre[i + 1]) - (long long)(n - 1 - i) * a[i];\nres[i] = left + right;\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 一律用 `long long`，距離和很容易溢位 `int`。\n- 若題目是「移到同一點的最少代價」（題 2602），最佳目標點是中位數，仍靠這套前綴和算每個候選的距離和。\n- 進階題（題 1703、2968）會把「距離和」嵌進滑動視窗，視窗滑動時用同樣公式增量更新。\n- 複雜度：排序 `O(n log n)`，逐點 `O(1)`，整體 `O(n log n)`，空間 `O(n)`。\n\n## C++17 模板\n\n```cpp\n// 距離和的通用查詢結構：排序 + 前綴和，單次查詢 O(log n)。\nclass DistanceSum {\n  std::vector<long long> a_;        // 排序後的元素\n  std::vector<long long> s_;        // s_[i] = a_[0..i-1] 的和\n\n public:\n  explicit DistanceSum(std::vector<int> nums) {\n    a_.assign(nums.begin(), nums.end());\n    std::sort(a_.begin(), a_.end());              // 前提：{i : a_i <= x} 必須是前綴\n    s_.assign(a_.size() + 1, 0);\n    for (size_t i = 0; i < a_.size(); ++i) { s_[i + 1] = s_[i] + a_[i]; }\n  }\n\n  // 回傳 sum_i |x - a_i|\n  long long query(long long x) const {\n    int n = a_.size();\n    int m = std::upper_bound(a_.begin(), a_.end(), x) - a_.begin();   // a_[0..m-1] <= x\n    long long left = m * x - s_[m];                                   // 左段：x - a_i\n    long long right = (s_[n] - s_[m]) - static_cast<long long>(n - m) * x;\n    return left + right;\n  }\n\n  // 使所有元素相等的最小距離和（最佳點是中位數）\n  long long best() const { return a_.empty() ? 0 : query(a_[a_.size() / 2]); }\n};\n```\n\n```cpp\n// 得到連續 K 個 1 的最少相鄰交換次數（LeetCode 1703）\n// 變換 q[t] = p[t] - t，把「移到連續位置」化成「移到同一點」，再取視窗中位數。\nclass Solution {\n public:\n  int minMoves(std::vector<int>& nums, int k) {\n    std::vector<long long> q;\n    for (int i = 0, t = 0; i < static_cast<int>(nums.size()); ++i) {\n      if (nums[i] == 1) { q.push_back(i - t); ++t; }     // 關鍵變換\n    }\n    int c = q.size();\n    std::vector<long long> s(c + 1, 0);\n    for (int i = 0; i < c; ++i) { s[i + 1] = s[i] + q[i]; }\n\n    long long ans = LLONG_MAX;\n    for (int l = 0; l + k <= c; ++l) {\n      int r = l + k - 1;\n      int mid = l + (r - l) / 2;                          // 視窗中位數的下標\n      long long x = q[mid];\n      // 左段 [l, mid]：x - q[i]；右段 [mid+1, r]：q[i] - x\n      long long leftCnt = mid - l + 1;\n      long long left = leftCnt * x - (s[mid + 1] - s[l]);\n      long long rightCnt = r - mid;\n      long long right = (s[r + 1] - s[mid + 1]) - rightCnt * x;\n      ans = std::min(ans, left + right);\n    }\n    return static_cast<int>(ans);\n  }\n};\n```\n\n## 時間與空間複雜度\n\n**建構**：排序 $O(n \\log n)$ + 前綴和 $O(n)$。\n\n**單次查詢**：二分 $O(\\log n)$，公式 $O(1)$。若 $x$ 恰是某個 $a_j$（1685、2615），$m$ 直接由下標得出，查詢降到 **$O(1)$**。\n\n**總計**：$O((n + q)\\log n)$，或 $O(n \\log n)$（僅排序）當所有查詢都是陣列元素。\n\n**滑動視窗版（1703、2968）**：外層 $O(n)$ 個視窗，每個 $O(1)$，總計 $O(n)$（若 1 的位置已排序則不需再排）。\n\n**空間**：$O(n)$。2615 的分組版所有組的前綴和加起來仍是 $O(n)$。\n\n與 $O(nq)$ 暴力的落差：$n = q = 10^5$ 時 $10^{10} \\to 3.4 \\times 10^6$。\n\n**溢位是這一節的常態而非例外。** $m \\cdot x$ 在 $m = 10^5$、$x = 10^9$ 時是 $10^{14}$；距離和本身可達 $10^{14}$。**所有中間量一律 `long long`**，包括 `(long long)(n - m) * x` 這種乘法——只寫 `(n-m) * x` 時兩個運算元都是 `int`，會先以 `int` 相乘再提升，仍然溢位。\n\n## 常見錯誤與邊界條件\n\n- 忘記排序（引理一的前提破裂，答案完全錯）。\n- `upper_bound` 與 `lower_bound` 混用（差別在 $a_i = x$ 的元素歸到哪一段——**兩者都對，只要公式一致**：`upper_bound` 把等於 $x$ 的歸左段，`lower_bound` 歸右段，而 $|x - a_i| = 0$，所以結果相同；但若題目要的是「嚴格小於」的計數，就必須分清）。\n- `int` 乘法溢位（`(n-m) * x` 沒有轉型）。\n- 前綴和用 `int`。\n- 中位數下標寫成 `(l + r + 1) / 2`（偶數長度時取右中位數，對距離和仍最佳，但與左中位數的公式要一致，混用會算錯段長）。\n- 1703 忘記 $q_t = p_t - t$ 的變換（直接對 $p$ 取距離和，得到的是「全部移到同一格」的次數，答案偏大）。\n- 1703 的視窗邊界 `l + k <= c`。\n- 2615 未按值分組就整體算。\n- 2615 對每個位置重新建前綴和（$O(n^2)$）。\n- `best()` 在空陣列上存取 `a_[0]`。\n- 2968 的目標是「頻率分數最大」，要在距離和 $\\le k$ 的約束下求最長視窗，屬於雙指標 + 本節公式的組合，別漏掉外層的單調性論證。\n\n## 與相似技巧的比較\n\n- **與 2.1**：本節是 2.1 的直接應用，新增的只有「排序讓集合變前綴」與「拆絕對值」兩步。\n- **與 binary_search 主題**：查詢時的 `upper_bound` 是二分；若題目改成「使距離和不超過 $k$ 的最大視窗」，外層還要再二分或雙指標，屬於組合題。\n- **與 6.7 對頂堆**：若元素會**動態加入或刪除**（滑動視窗中位數、資料流），排序後的前綴和無法維護，要改用對頂堆（兩側各維護 sum）或樹狀陣列。**判準：陣列固定 → 本節；動態變化 → 對頂堆／樹狀陣列。**\n- **與 9.1 樹狀陣列**：需要「動態插入元素並查詢距離和」時，用兩個樹狀陣列（一個記個數、一個記和）在值域上做，$O(\\log V)$ 每次操作。這是本節的動態版。\n- **與 greedy 的「中位數貪心」**：引理二在 greedy 主題也會出現（462、2033）。**這裡給的是完整證明（分段線性凸函數的斜率變號），不只是「取中位數就對了」。**\n- **與平方距離**：目標若是 $\\sum (x - a_i)^2$，最佳點是平均數，且展開後 $= n x^2 - 2x\\sum a_i + \\sum a_i^2$，只需前綴和與平方前綴和，連排序都不用。**這說明「要不要排序」取決於目標函數是否分段。**\n\n## 代表例題\n\n**題號**：1685、1703、2602、2615、2968\n\n要把區間量改寫成前綴狀態之差；若是計數題，重點是查找過去需要的前綴值。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1685 | [有序陣列中差絕對值之和](https://leetcode.cn/problems/sum-of-absolute-differences-in-a-sorted-array) | 1496 |\n| 1703 | [得到連續 K 個 1 的最少相鄰交換次數](https://leetcode.cn/problems/minimum-adjacent-swaps-for-k-consecutive-ones) | 2467 |\n| 2602 | [使陣列元素全部相等的最少操作次數](https://leetcode.cn/problems/minimum-operations-to-make-all-array-elements-equal) | 1903 |\n| 2615 | [等值距離和](https://leetcode.cn/problems/sum-of-distances) | 1793 |\n| 2968 | [執行操作使頻率分數最大](https://leetcode.cn/problems/apply-operations-to-maximize-frequency-score) | 2444 |\n| 3086 | [拾起 K 個 1 需要的最少行動次數](https://leetcode.cn/problems/minimum-moves-to-pick-k-ones) | 2673 |\n| 3422 | [將子陣列元素變為相等所需的最小運算元](https://leetcode.cn/problems/minimum-operations-to-make-subarray-elements-equal) |  |\n\n**延伸閱讀**：[LeetCode 2602 排序＋前綴和圖解](https://leetcode.cn/problems/minimum-operations-to-make-all-array-elements-equal/solution/yi-tu-miao-dong-pai-xu-qian-zhui-he-er-f-nf55/)，用二分切開 `<= x` 與 `> x` 後分別計算距離。\n\n## 本節重點速查\n\n- $f(x) = \\sum |x - a_i|$：**排序後**用前綴和拆成兩段，\n- $$f(x) = (mx - s_m) + \\big((s_n - s_m) - (n-m)x\\big),\\quad m = |\\{a_i \\le x\\}| .$$\n- 排序是**正確性前提**，不是效率優化——它讓 $\\{i : a_i \\le x\\}$ 成為一個前綴。\n- $f$ 是分段線性凸函數，斜率 $2m - n$，故最小值在**中位數**。平方距離的最佳點是平均數，$\\max$ 距離的最佳點是 $(\\min+\\max)/2$，別記混。\n- 「移到連續位置」用 $q_t = p_t - t$ 變成「移到同一點」（1703）。\n- $x$ 恰是陣列元素時 $m$ 由下標直接得出，查詢降到 $O(1)$。\n- **所有中間量 `long long`**，包括 `(long long)(n-m) * x` 的顯式轉型。\n- 元素會動態進出時本節失效，改用對頂堆或樹狀陣列。\n",
        },
        {
          id: 15,
          title: "2.4 狀態壓縮字首和",
          summary:
            "## 這個技術解決什麼問題\n\n有一類子陣列／子字串題，條件不是關於「和」，而是關於「每種元素出現次數的奇偶」：\n\n- 1371：最長子字串，其中每個母音（a e i o u）都出現偶數次。\n- 1915：有多少子字串可重排成迴文（至多一個字元出現奇數次）。\n- 1542：最長的可重排成迴文的子字串。\n- 1177：查詢子串能否在重排 + 至多 $k$ 次替換後變成迴文。\n- 2791：樹上路徑版本（把「區間」換成「路徑」）。\n\n樸素做法枚舉所有 $O(n^2)$ 個區間，每個再檢查一次計數表，$O(n^2 \\Sigma)$。\n\n本節的做法：**把「各字元的出現次數奇偶」壓成一個整數（每種字元佔一位），這個整數的前綴形式可以用 XOR 累積。** 於是「區間內某字元出現偶數次」變成「兩個前綴狀態在那一位相同」，整個條件變成前綴狀態之間的配對——回到 2.2 的骨架。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「狀態壓縮字首和」想：\n\n- **要找連續子陣列/子字串，條件和「出現次數的奇偶」或「某幾種類別的狀態」有關。** 例如「每個母音出現偶數次」「至多一個字元出現奇數次（可重排成回文）」。\n- **數值大小不重要，只在乎奇 / 偶或有 / 無。** 這提示你可以把狀態壓成 bitmask，再用 XOR 抵消。\n\n:::example 嗅到味道的瞬間：條件只看奇偶 → 用一個 bit 表示，整體狀態壓成一個整數\n```cpp\n// 嗅到味道的瞬間：條件只看奇偶 → 用一個 bit 表示，整體狀態壓成一個整數\nmask ^= (1 << id);  // id 是某類別的編號\n```\n:::\n\n\n\n- **暴力是枚舉所有區間再逐段檢查（`O(n^2)` 以上）；** 壓縮成前綴狀態 + hash map 後降到 `O(n)`。\n- 像 1371（母音偶數次）、1542 / 1915（回文相關）、2791（樹上路徑）都是同一招：把路徑/區間狀態壓成 mask，用 XOR 配對。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的「狀態」是什麼？能不能壓成一個整數的若干 bit？\n2. 區間狀態是不是等於「兩個前綴 mask 的 XOR」？目標 mask 是多少？\n3. 我用什麼結構記錄看過的前綴 mask？初始的空前綴有沒有先放進去？\n\n## 核心想法與直覺\n\n本節要解決的場景是：題目問「有沒有 / 有幾段連續子陣列，滿足某種『可累積』的性質」，而這個性質不是單純的和，而是某種「狀態」——例如某些字母出現的奇偶性。\n\n:::example 前綴和：prefix[i] 代表「前 i 個元素的累積」\n先複習最基本的工具。所謂「前綴和」就是先把「從開頭到位置 i」的累積結果算好存起來，之後任何一段區間都能用兩個前綴相減得到，不必再從頭掃一次：\n\n```cpp\n// 前綴和：prefix[i] 代表「前 i 個元素的累積」\nvector<int> prefix(n + 1, 0);\nfor (int i = 0; i < n; ++i) {\n    prefix[i + 1] = prefix[i] + a[i];  // 累積一格\n}\nint sumLR = prefix[r + 1] - prefix[l];  // 區間 [l, r] 的和，O(1)\n```\n:::\n\n\n\n:::example 狀態壓縮：每遇到一個字母 c，翻轉它對應的 bit（奇偶切換）\n本節的進階點在於：要累積的不是數值，而是「狀態」。當題目只在乎「每種字母出現幾次是奇數還是偶數」時，次數的真正大小不重要，重要的只有「奇 / 偶」這一個 bit。於是我們用一個整數的二進位位元（bitmask）來壓縮整個狀態：第 k 個字母出現奇數次，就讓第 k 個 bit 為 1。\n\n```cpp\n// 狀態壓縮：每遇到一個字母 c，翻轉它對應的 bit（奇偶切換）\nmask ^= (1 << (c - 'a'));  // XOR 一次 = 出現次數的奇偶反轉\n```\n:::\n\n\n\n關鍵的數學事實是：**區間 [l, r] 的狀態 = mask[r] XOR mask[l-1]**。因為 XOR 會把「兩端都出現過的字母」抵消，剩下的正是這段區間內奇偶性。所以「找一段子字串滿足某狀態」就變成「找兩個前綴 mask，使它們 XOR 後等於目標」——這正好可以用 hash map 把看過的 mask 記下來，邊掃邊查，整體 `O(n)`。\n\n## 狀態／資料結構定義\n\n**狀態壓縮**：字元集 $\\Sigma$（母音是 5 種，小寫字母是 26 種）。定義\n$$\\mathrm{mask}_i = \\bigoplus_{t=0}^{i-1} 2^{\\mathrm{id}(a_t)},$$\n即前 $i$ 個字元的「奇偶指紋」：第 $b$ 位為 1 $\\iff$ 第 $b$ 種字元在 $a[0..i-1]$ 中出現奇數次。$\\mathrm{mask}_0 = 0$。\n\n遞推：`mask ^= 1 << id(c);`——**XOR 就是模 2 加法**，出現兩次自動抵消。\n\n**摘要**：\n- 求最長（1371、1542）→ `first[mask]` = 該狀態最早出現的下標（初始 `first[0] = -1`，其餘為 $-2$ 或用 map 判存在）。\n- 求個數（1915）→ `cnt[mask]` = 該狀態出現次數（初始 `cnt[0] = 1`）。\n\n**摘要容器的選擇**：$|\\Sigma| = 5$ → 陣列大小 32；$|\\Sigma| = 26$ → 陣列大小 $2^{26} \\approx 6.7\\times10^7$，開 `int` 是 268 MB，**通常超限**。1915 因此改用 `unordered_map`，或利用「只需查 27 個鍵」的特性——見下。\n\n## 不變量或正確性證明\n\n**引理一（區間狀態 = 前綴狀態的 XOR）。**\n$$\\mathrm{mask}(l, r) := \\bigoplus_{t=l}^{r} 2^{\\mathrm{id}(a_t)} = \\mathrm{mask}_{r+1} \\oplus \\mathrm{mask}_{l}.$$\n\n*證明。* $\\mathrm{mask}_{r+1} \\oplus \\mathrm{mask}_l = \\left(\\bigoplus_{t \\le r}\\right) \\oplus \\left(\\bigoplus_{t < l}\\right)$。前 $l$ 項在兩邊都出現，XOR 後抵消（$x \\oplus x = 0$），只剩 $t \\in [l, r]$。$\\square$\n\n**這一步用到的是 XOR 群結構：每個元素是自己的逆元。** 對照 2.1 的表格，XOR 有逆元，所以「前綴相減」（這裡是相 XOR）成立。若題目要的是**實際次數**（不只奇偶），XOR 就不夠了——那要 $\\Sigma$ 個獨立的前綴和陣列，$O(n\\Sigma)$ 空間。**「只在乎奇偶」是本節的入場券。**\n\n**引理二（各條件的鍵翻譯）。**\n\n| 題目要求 | 等價的 mask 條件 | 對每個 $r$ 要查的鍵 |\n| --- | --- | --- |\n| 每種字元都出現偶數次（1371） | $\\mathrm{mask}_{r+1} \\oplus \\mathrm{mask}_l = 0$ | 1 個：$\\mathrm{mask}_{r+1}$ |\n| 可重排成迴文（1915、1542） | $\\mathrm{mask}_{r+1} \\oplus \\mathrm{mask}_l$ 的 popcount $\\le 1$ | $1 + |\\Sigma|$ 個：$\\mathrm{mask}_{r+1}$ 與 $\\mathrm{mask}_{r+1} \\oplus 2^b$ |\n\n*「可重排成迴文 $\\iff$ 至多一個字元出現奇數次」的證明。* ($\\Rightarrow$) 迴文中除了正中間那個字元（長度為奇數時）之外，每個字元都與對稱位置的字元配對，故出現偶數次。($\\Leftarrow$) 若至多一個字元出現奇數次，把每種字元對半分放左右、奇數的那一個放正中，即構成迴文。$\\square$\n\n**這正是「popcount $\\le 1$」的來源，而不是憑記憶。**\n\n**主不變量。** 處理右端點 $r$ 並查詢的那一刻，摘要恰好記錄 $\\{\\mathrm{mask}_0, \\dots, \\mathrm{mask}_r\\}$——含 $\\mathrm{mask}_0 = 0$，不含 $\\mathrm{mask}_{r+1}$。與 2.2 完全相同，「先查後加」與「初始化 $\\mathrm{mask}_0$」兩條紀律照舊。\n\n**1915 為何是 $1 + |\\Sigma|$ 次查詢而非枚舉所有狀態。** 條件是 $\\mathrm{popcount}(\\mathrm{mask}_{r+1} \\oplus \\mathrm{mask}_l) \\le 1$，即 $\\mathrm{mask}_l \\in \\{\\mathrm{mask}_{r+1}\\} \\cup \\{\\mathrm{mask}_{r+1} \\oplus 2^b : b \\in \\Sigma\\}$。**這個集合只有 $1 + |\\Sigma|$ 個元素**，逐一查表即可，$O(n|\\Sigma|)$。這是 1.2 中「$\\psi$ 回傳集合」推廣形式的最典型例子：**互補鍵不唯一，但可枚舉。**\n\n反過來說，若條件是「popcount $\\le 2$」，候選鍵有 $1 + |\\Sigma| + \\binom{|\\Sigma|}{2}$ 個，$|\\Sigma| = 26$ 時是 352，仍可行；「popcount $\\le k$」一般是 $O(|\\Sigma|^k)$，$k$ 一大就崩。**這個成本結構決定了本節的適用邊界。**\n\n**1177（查詢版）為何不能用配對。** 它問的是「子串 $[l,r]$ 至多替換 $k$ 個字元後能否重排成迴文」，區間是**給定的**，不是要找的。此時直接算 $\\mathrm{mask}_{r+1} \\oplus \\mathrm{mask}_l$ 的 popcount $p$，需要的替換次數是 $\\lfloor p/2 \\rfloor$——**因為每次替換可以把兩個奇數變成兩個偶數**（把其中一個字元改成另一個），故答案是 $\\lfloor p/2 \\rfloor \\le k$。這裡摘要退化成前綴陣列，每個查詢 $O(1)$。**同一個狀態壓縮，用在「找區間」與「查區間」上是兩種不同的骨架。**\n\n**2791（樹上最長迴文路徑）的移植。** 把「前綴」換成「根到節點的路徑指紋」，把「區間」換成「兩節點間的路徑」：$\\mathrm{mask}(u,v) = \\mathrm{mask}_u \\oplus \\mathrm{mask}_v$（LCA 的部分被 XOR 抵消兩次，恰好消掉，**且不需要真的求出 LCA**）。搭配 trees 3.11 的 DSU on tree 或「合併子樹時查表」的做法。**XOR 在樹上自動處理 LCA，是這個技巧在樹上格外好用的原因。**\n\n## 逐步演算法\n\n:::example 狀態表示：mask 的第 k bit = 第 k 個字母到目前為止是否出現奇數次\n**1. 狀態表示：把前綴狀態壓成一個整數 mask。** 不存次數，只存奇偶，用 bit 表示。\n\n```cpp\n// 狀態表示：mask 的第 k bit = 第 k 個字母到目前為止是否出現奇數次\nint mask = 0;  // 全偶（沒看到任何字母）的初始狀態\n```\n:::\n\n\n\n:::example 更新：逐字元翻轉對應 bit\n**2. 更新流程：邊掃邊翻 bit。** 每讀一個字元就 XOR 對應 bit，mask 隨之變成「到目前位置」的前綴狀態。\n\n```cpp\n// 更新：逐字元翻轉對應 bit\nfor (char c : s) {\n    mask ^= (1 << (c - 'a'));\n    // 此刻 mask = 前綴狀態，接著去查 / 記錄\n}\n```\n:::\n\n\n\n:::example 讀取：first[mask] 記某狀態第一次出現的位置\n**3. 答案讀取：用 hash map 查歷史前綴。** 想要某段是「全偶」就找曾經出現過、與目前相同的 mask（相同 XOR 後為 0）；要計數就累加出現次數。\n\n```cpp\n// 讀取：first[mask] 記某狀態第一次出現的位置\nif (first.count(mask)) {\n    ans = max(ans, i - first[mask]);  // 這段區間狀態為全偶\n} else {\n    first[mask] = i;  // 只留最早，子字串才最長\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 一開始要放入「空前綴」狀態 `first[0] = -1`（或 `count[0] = 1`），否則從頭開始就符合條件的那段會被漏掉。\n- 字母種類決定 bit 數：小寫字母 26 個就用 26 bit；母音題只關心 5 個母音，mask 範圍很小。\n- 時間 `O(n)`、空間 `O(n)`（hash map 最多存 n 個不同 mask）。\n\n## C++17 模板\n\n```cpp\n// 每個母音包含偶數次的最長子字串（LeetCode 1371）\n// 狀態只有 5 位 → 32 種，用陣列存「該狀態最早出現的下標」。\nclass Solution {\n public:\n  int findTheLongestSubstring(std::string s) {\n    constexpr std::string_view kVowels = \"aeiou\";\n    std::array<int, 32> first;\n    first.fill(-2);                       // -2 表示尚未出現\n    first[0] = -1;                        // mask_0 = 0，「下標」是 -1\n\n    int mask = 0, ans = 0;\n    for (int i = 0; i < static_cast<int>(s.size()); ++i) {\n      size_t p = kVowels.find(s[i]);\n      if (p != std::string_view::npos) { mask ^= 1 << p; }   // XOR = 模 2 加法\n\n      if (first[mask] == -2) {\n        first[mask] = i;                  // 只記最早：要最長就要左端點最小\n      } else {\n        ans = std::max(ans, i - first[mask]);   // 長度 = i - l，l 是記錄的下標\n      }\n    }\n    return ans;\n  }\n};\n```\n\n```cpp\n// 可以形成回文的最長子字串 / 統計迴文子字串（LeetCode 1915 的計數版）\n// 條件：popcount(mask_r ^ mask_l) <= 1 → 每個右端點查 1 + 26 個鍵。\nclass Solution {\n public:\n  long long wonderfulSubstrings(std::string word) {\n    std::vector<long long> cnt(1 << 10, 0);   // 本題字元集是 a..j，10 位\n    cnt[0] = 1;                                // mask_0 = 0\n    int mask = 0;\n    long long ans = 0;\n    for (char c : word) {\n      mask ^= 1 << (c - 'a');\n      ans += cnt[mask];                        // 差為 0：全部偶數次\n      for (int b = 0; b < 10; ++b) {\n        ans += cnt[mask ^ (1 << b)];           // 差恰一位：只有一個字元奇數次\n      }\n      ++cnt[mask];                             // 先查後加\n    }\n    return ans;\n  }\n};\n```\n\n## 時間與空間複雜度\n\n**1371 型（條件是「差為 0」）**：每個右端點 1 次查詢，$O(n)$ 時間、$O(2^{|\\Sigma|})$ 空間。$|\\Sigma| = 5$ 時空間是 32，可忽略。\n\n**1915 型（條件是「差的 popcount $\\le 1$」）**：每個右端點 $1 + |\\Sigma|$ 次查詢，$O(n|\\Sigma|)$ 時間。$|\\Sigma| = 10$ 時是 $11n$。\n\n**空間是本節的主要限制。** $2^{|\\Sigma|}$ 在 $|\\Sigma| = 26$ 時是 $6.7\\times10^7$ 格，`int` 陣列 268 MB，超限。三種對策：\n1. 用 `unordered_map`（只存實際出現的 $\\le n+1$ 個狀態），$O(n)$ 空間，常數較大。\n2. 若題目的字元集實際更小（1915 只有 a–j），就按實際大小開。\n3. 1542 這類「求最長」的題目，狀態數上界是 $n+1$（每個前綴一個），用 map 就夠。\n\n**與暴力的落差**：$O(n^2\\Sigma) \\to O(n\\Sigma)$。$n = 10^5$、$\\Sigma = 26$ 時是 $2.6\\times10^{11} \\to 2.6\\times10^6$。\n\n## 常見錯誤與邊界條件\n\n- 漏掉 `first[0] = -1` / `cnt[0] = 1`（所有從下標 0 開始的區間消失，與 2.2 同一個坑）。\n- 「未出現」的哨兵值與合法下標混淆（用 `-2` 或 `-1` 要與 $\\mathrm{mask}_0$ 的 $-1$ 區分開；本模板用 `-2` 表示未出現）。\n- 求最長時覆蓋了已存在的 `first[mask]`（必須只記最早）。\n- 求個數與求最長的摘要語意互抄。\n- 「先加後查」（1915 會把長度為 0 的空串算進去）。\n- `1 << p` 中 $p$ 超過 31（$|\\Sigma| > 31$ 時要用 `long long` 或 `bitset`）。\n- 1915 的答案用 `int` 溢位（$n = 10^5$ 時子串數達 $5\\times10^9$）。\n- 把「至多一個奇數」誤寫成「恰好一個奇數」（漏掉全偶的情形）。\n- 1177 把答案寫成 $p \\le k$ 而非 $\\lfloor p/2 \\rfloor \\le k$。\n- 1177 的查詢區間是閉區間，`mask[right+1] ^ mask[left]` 的下標要對齊。\n- $2^{26}$ 的陣列直接開在函式內（堆疊溢位，要用 `static` 或 `vector`）。\n- 2791 在樹上忘記 XOR 已自動消去 LCA 而額外處理。\n\n## 與相似技巧的比較\n\n- **與 2.2**：完全同一個骨架，只是鍵從「前綴和的數值」換成「前綴狀態的位元遮罩」，累積運算從 `+` 換成 `^`。**2.2 的每一條紀律（初始化、先查後加、最早 vs 計數）在這裡原樣適用。**\n- **與 1.2**：查 $1 + |\\Sigma|$ 個鍵，正是 1.2 中「互補特徵不唯一但可枚舉」的形式。\n- **與 bitwise_operations 主題**：那裡談位元運算本身；本節把位元遮罩當成「壓縮的狀態」使用。**兩者的交集是「popcount 與 lowbit 的技巧」。**\n- **與滑動視窗**：條件「每種字元恰好出現 $k$ 次」有單調性時可用滑動視窗；但「偶數次」沒有單調性（加一個字元可能讓條件成立也可能破壞），**滑動視窗在這裡不成立**。\n- **與狀態壓縮 DP**：兩者都用 mask 當狀態，但那裡的 mask 是「選了哪些物品」（枚舉子集），這裡是「奇偶指紋」（前綴累積）。**mask 的語意完全不同，別把子集枚舉的直覺帶過來。**\n- **與 9.1 樹狀陣列**：若條件涉及「數值大小」而非「相等／XOR」，雜湊表不夠，要用樹狀陣列。本節的條件天生是等值配對，所以雜湊表就夠。\n\n## 代表例題\n\n**題號**：1177、1371、1542、1915、2791\n\n要把區間量改寫成前綴狀態之差；若是計數題，重點是查找過去需要的前綴值。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1177 | [構建迴文串檢測](https://leetcode.cn/problems/can-make-palindrome-from-substring) | 1848 |\n| 1371 | [每個母音包含偶數次的最長子字串](https://leetcode.cn/problems/find-the-longest-substring-containing-vowels-in-even-counts) | 2041 |\n| 1542 | [找出最長的超讚子字串](https://leetcode.cn/problems/find-longest-awesome-substring) | 2222 |\n| 1915 | [最美子字串的數目](https://leetcode.cn/problems/number-of-wonderful-substrings) | 2235 |\n| 2791 | [樹中可以形成迴文的路徑數](https://leetcode.cn/problems/count-paths-that-can-form-a-palindrome-in-a-tree) | 2677 |\n\n## 本節重點速查\n\n- 把「各字元出現次數的奇偶」壓成一個整數，用 **XOR** 累積成前綴狀態：$\\mathrm{mask}(l,r) = \\mathrm{mask}_{r+1} \\oplus \\mathrm{mask}_l$（前 $l$ 項自我抵消）。\n- 入場券是「**只在乎奇偶，不在乎實際次數**」；要實際次數就得開 $\\Sigma$ 條前綴和。\n- **可重排成迴文 $\\iff$ 至多一個字元出現奇數次 $\\iff$ popcount $\\le 1$**（配對法證明，不是背的）。\n- 「差為 0」→ 每個右端點查 1 個鍵；「popcount $\\le 1$」→ 查 $1 + |\\Sigma|$ 個鍵。$O(|\\Sigma|^k)$ 決定了適用邊界。\n- 2.2 的三條紀律照舊：**初始化 $\\mathrm{mask}_0$、先查後加、最長存最早／計數存次數**。\n- 空間 $2^{|\\Sigma|}$ 是主要限制；$|\\Sigma| = 26$ 時改用 `unordered_map`（實際狀態數 $\\le n+1$）。\n- 1177 是「查給定區間」而非「找區間」，答案是 $\\lfloor \\mathrm{popcount}/2 \\rfloor \\le k$（一次替換修掉兩個奇數）。\n- 搬到樹上（2791）時 **XOR 自動消去 LCA**，不必求 LCA。\n",
        },
        {
          id: 16,
          title: "2.5 進階",
          summary:
            "## 這個技術解決什麼問題\n\n2.1 把區間和變成 $O(1)$ 可取；2.2 用雜湊表回答「和恰為 $k$」。本節處理第三類問法：**在前綴和序列上做最佳化**——找最好的端點、最長的區間、最大的組合值。\n\n三種常見形態：\n\n1. **排序後前綴和單調 ⇒ 可二分**（2389 和有限的最長子序列、2300 咒語與藥水、1898）。子序列題允許打亂順序，排序後前綴和嚴格遞增，「最多能取幾個」就是一次 `upper_bound`。\n2. **枚舉分界點，左右各取最佳**（1031 兩段不重疊子陣列、2555 兩條線段、1888、689）。這是 1.3「列舉中間」套在前綴和上。\n3. **前綴和 + 排序 + 貪心／堆**（1788、2542、2813、1383）。先用前綴和把「取一段」的成本 $O(1)$ 化，再對另一維排序。\n\n共同的槓桿是：**前綴和把「一段的量」壓成 $O(1)$，於是原本 $O(n)$ 的內層可以換成二分、單調維護或堆。**\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「字首和：進階」想：\n\n- **問題能化成「區間和 / 累積量」，且還要在其上找最大、最小或滿足上限的最佳端點。** 單純的區間查詢用基本前綴和就好；要「找最佳」才升級到本節。\n- **資料順序可被打亂（子序列題），排序後前綴和遞增，剛好能二分。**\n\n:::example 嗅到味道的瞬間：前綴和遞增 + 要找「不超過上限的最長段」→ 二分\n```cpp\n// 嗅到味道的瞬間：前綴和遞增 + 要找「不超過上限的最長段」→ 二分\nupper_bound(prefix.begin(), prefix.end(), limit);\n```\n:::\n\n\n\n- **暴力是對每個端點再掃一遍找另一端（`O(n^2)`）；** 借前綴的單調性改用二分或單次掃描，降到 `O(n log n)` 或 `O(n)`。\n- 像 2389（和有限的最長子序列）排序後二分、2300（咒語藥水）對另一陣列排序後二分，都是「前綴 / 排序 + 二分」的組合拳。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要的量能不能寫成「兩個前綴之差」？\n2. 我的前綴是否單調（天生遞增，或排序後遞增）？\n3. 對每個右端點，我用什麼方法（二分 / 維護最小前綴）找最佳左端點？\n\n## 核心想法與直覺\n\n本節是「字首和」的進階應用：當題目把區間的某個量（和、長度、收益）拆成「兩個前綴之差」後，還需要再搭配排序、二分搜尋或雙指標，才能快速找到「最佳的另一端」。\n\n:::example 前綴和：任一區間和 = 兩個前綴相減\n先回到最核心的等式。前綴和讓任一段區間的和能 `O(1)` 取得：\n\n```cpp\n// 前綴和：任一區間和 = 兩個前綴相減\nvector<long long> prefix(n + 1, 0);\nfor (int i = 0; i < n; ++i) {\n    prefix[i + 1] = prefix[i] + a[i];\n}\n// 區間 [l, r] 的和 = prefix[r + 1] - prefix[l]\n```\n:::\n\n\n\n進階的關鍵想法是：很多題目問的不是「某個固定區間的和」，而是「在滿足某條件下，最好的區間是哪一段」。這時把問題改寫成前綴後，常會變成「對每個右端點 r，找一個前綴 prefix[l] 使結果最佳」。如果前綴本身是「單調遞增」的，就能用二分搜尋；如果只關心「最小的前綴」，就一路維護目前看過的最小值。\n\n:::example 進階：前綴和遞增 → 用二分找「總和不超過上限」的最大長度\n例如「和有限的最長子序列」：先排序、再做前綴和，前綴和必定遞增，於是「能買幾個」就變成在遞增陣列上二分查找：\n\n```cpp\n// 進階：前綴和遞增 → 用二分找「總和不超過上限」的最大長度\nsort(nums.begin(), nums.end());\n// prefix 已算好且遞增\nint cnt = upper_bound(prefix.begin(), prefix.end(), limit) - prefix.begin() - 1;\n```\n:::\n\n\n\n核心精神是：**前綴和把「區間量」化簡成「兩點之差」，再借助前綴的單調性（自然遞增，或排序後遞增）把「找最佳另一端」變成 `O(log n)` 的二分**，整體 `O(n log n)`。\n\n## 狀態／資料結構定義\n\n**形態一（排序 + 二分）**：\n- 排序後的 $a$，前綴和 $s$（$s_0 = 0$，`long long`）。\n- 因為 $a_i \\ge 0$，$s$ **嚴格遞增**（有 0 時是非嚴格遞增，仍可二分）。\n- 查詢「和不超過 $q$ 時最多取幾個」= `upper_bound(s.begin(), s.end(), q) - s.begin() - 1`。\n\n**形態二（枚舉分界）**：\n- $\\mathrm{bestL}[i]$ = 在 $[0, i)$ 中，長度為 $k_1$ 的子陣列和的最大值（前綴最佳）。\n- $\\mathrm{bestR}[i]$ = 在 $[i, n)$ 中，長度為 $k_2$ 的子陣列和的最大值（後綴最佳）。\n- 答案 $= \\max_i (\\mathrm{bestL}[i] + \\mathrm{bestR}[i])$。\n\n**形態三（前綴和 + 貪心）**：\n- 通常先按某個鍵排序（1788 按位置、2542 按 `nums2` 降序），再用前綴和或堆維護「取 $k$ 個的最佳和」。\n\n## 不變量或正確性證明\n\n**形態一的正確性依賴「前綴和單調」。**\n\n*引理。* 若所有 $a_i \\ge 0$ 且已排序遞增，則 $s$ 非嚴格遞增，且**取前 $m$ 小的元素是所有大小為 $m$ 的子集中和最小者**。\n\n*證明（交換論證）。* 設 $T$ 是任一大小為 $m$ 的下標集合，且 $T \\ne \\{0,1,\\dots,m-1\\}$。則存在 $j \\in T$、$i \\notin T$ 且 $i < j$（否則 $T$ 就是前 $m$ 個）。把 $j$ 換成 $i$，因 $a_i \\le a_j$，總和不增。有限次交換後 $T$ 變成前 $m$ 個，故前 $m$ 個的和最小。$\\square$\n\n*推論*：「和 $\\le q$ 時最多能取幾個」的答案是最大的 $m$ 使 $s_m \\le q$，可二分。**這裡用了兩個前提：元素非負（保證 $s$ 單調，二分才有意義）、可任意選取（子序列題，不是子陣列題）。** 若題目要求**連續**子陣列，前 $m$ 小就不合法，這條論證失效——那要改用滑動視窗或 2.2。\n\n**2300（咒語與藥水）的單調性。** 對每個 spell $x$，需要 potion $y$ 滿足 $x \\cdot y \\ge \\mathrm{success}$，即 $y \\ge \\lceil \\mathrm{success}/x \\rceil$。排序 potions 後用 `lower_bound`。**除法向上取整必須寫成 `(success + x - 1) / x`**（在 `long long` 下），寫成 `success / x` 會少算邊界；用浮點 `ceil(success / (double)x)` 在 $10^{10}$ 量級會有精度誤差。**這是一個「整數運算的正確性細節」，與前綴和無關但同樣是失分點。**\n\n**形態二的不重不漏（1031）。** 要選兩個**不重疊**的子陣列，長度分別為 $f$ 與 $s$。任兩個不重疊區間必有先後，故存在分界點 $i$ 使一個完全落在 $[0,i)$、另一個完全落在 $[i,n)$。\n\n*不漏*：對任一組合法解，取「左邊那段的右端點 + 1」當 $i$，它會被枚舉到。\n*不重*：可能同一組解對應多個 $i$，但因為取的是 $\\max$，重複不影響答案。\n*順序*：兩段的長度不同，**必須把 $(f,s)$ 與 $(s,f)$ 兩種順序都算一遍**——這是最常漏的一半。\n\n**$\\mathrm{bestL}$ 的遞推。** $\\mathrm{bestL}[i] = \\max(\\mathrm{bestL}[i-1],\\; s_i - s_{i-f})$（$i \\ge f$）。這是「前綴最大值」的標準遞推，正確性來自 $\\max$ 的結合律。**注意這裡不是前綴和相減，而是對「所有可能的定長區間和」取前綴 $\\max$——$\\max$ 沒有逆元，只能單向累積，不能相減。** 這正好呼應 2.1 提到的限制。\n\n**2555（兩條線段獲得最多獎品）的變體。** 兩條長度為 $k$ 的線段，用雙指標算出「以 $i$ 為右端點的線段能覆蓋多少獎品」，再對它取前綴最大值，最後枚舉分界。**與 1031 同構，只是「長度」是座標長度而非元素個數，要用 `upper_bound` 找左端點。**\n\n**1788（最大化花園的美觀值）的前綴和用法。** 要選同種花當首尾，中間可以砍掉負數。對每種花，取最左與最右出現位置 $l, r$，答案 = $2 \\cdot a_l + (\\text{區間 } (l, r) \\text{ 內所有正數之和})$。「區間內正數之和」用**只累加正數的前綴和**：$p_i = \\sum_{t<i} \\max(a_t, 0)$。**把「取正部」寫進前綴和的定義，是本節最常用的一招——它讓「可以自由丟棄不利元素」這個自由度變成 $O(1)$ 可算。**\n\n還要處理「首尾兩朵花本身是負數」的情形：它們必須保留（題目要求首尾同種且都算進去），所以 $2a_l$ 即使是負也要加。**別把「取正部」的優化誤套到強制保留的元素上。**\n\n## 逐步演算法\n\n:::example 狀態表示：排序後做前綴和，prefix 從此單調遞增\n**1. 狀態表示：先想清楚 prefix[i] 代表什麼。** 通常是「前 i 項的和」，必要時先排序讓前綴具備單調性。\n\n```cpp\n// 狀態表示：排序後做前綴和，prefix 從此單調遞增\nsort(nums.begin(), nums.end());\nvector<long long> prefix(n + 1, 0);\nfor (int i = 0; i < n; ++i) {\n    prefix[i + 1] = prefix[i] + nums[i];\n}\n```\n:::\n\n\n\n:::example 更新：對每個查詢的上限，二分找最長前綴\n**2. 更新流程：邊掃右端點，邊查最佳左端點。** 用二分搜尋（前綴遞增時）或維護目前最小前綴（求最大區間和時）。\n\n```cpp\n// 更新：對每個查詢的上限，二分找最長前綴\nfor (long long q : queries) {\n    int len = upper_bound(prefix.begin(), prefix.end(), q) - prefix.begin() - 1;\n    ans.push_back(len);\n}\n```\n:::\n\n\n\n:::example 讀取：最大區間和 = 目前前綴 - 之前看過的最小前綴\n**3. 答案讀取：直接由前綴差或二分結果得到。** 不需要回頭重算整段。\n\n```cpp\n// 讀取：最大區間和 = 目前前綴 - 之前看過的最小前綴\nlong long best = LLONG_MIN, mn = 0;\nfor (int i = 1; i <= n; ++i) {\n    best = max(best, prefix[i] - mn);\n    mn = min(mn, prefix[i]);\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 排序會打亂原索引；若答案需要原位置，記得另存索引或確認題目允許重排（子序列題通常允許）。\n- 前綴和可能溢位，金額/總和題請用 `long long`。\n- 排序 `O(n log n)`，每次二分 `O(log n)`，多次查詢合計仍 `O((n + q) log n)`。\n\n## C++17 模板\n\n```cpp\n// 和有限的最長子序列（LeetCode 2389）：排序 + 前綴和 + 二分。\nclass Solution {\n public:\n  std::vector<int> answerQueries(std::vector<int>& nums, std::vector<int>& queries) {\n    std::sort(nums.begin(), nums.end());            // 前 m 小的和最小（交換論證）\n    int n = nums.size();\n    std::vector<long long> s(n + 1, 0);\n    for (int i = 0; i < n; ++i) { s[i + 1] = s[i] + nums[i]; }\n\n    std::vector<int> ans;\n    ans.reserve(queries.size());\n    for (int q : queries) {\n      // 最大的 m 使 s[m] <= q；s 遞增，可二分\n      int m = std::upper_bound(s.begin(), s.end(), static_cast<long long>(q))\n              - s.begin() - 1;\n      ans.push_back(m);\n    }\n    return ans;\n  }\n};\n```\n\n```cpp\n// 兩個無重疊子陣列的最大和（LeetCode 1031）：枚舉分界，左右各取最佳。\nclass Solution {\n  // 固定「長度 f 的那段在左、長度 sLen 的那段在右」時的最大和\n  static int solve(const std::vector<long long>& p, int f, int sLen) {\n    int n = p.size() - 1;\n    int best = 0;\n    long long bestLeft = 0;                        // [0, i) 中長度 f 的最大段和\n    // i 是分界：左段結束於 i，右段從 i 開始\n    for (int i = f; i + sLen <= n; ++i) {\n      bestLeft = std::max(bestLeft, p[i] - p[i - f]);        // 前綴最大值遞推\n      long long cur = bestLeft + (p[i + sLen] - p[i]);\n      best = std::max<long long>(best, cur);\n    }\n    return best;\n  }\n\n public:\n  int maxSumTwoNoOverlap(std::vector<int>& nums, int firstLen, int secondLen) {\n    int n = nums.size();\n    std::vector<long long> p(n + 1, 0);\n    for (int i = 0; i < n; ++i) { p[i + 1] = p[i] + nums[i]; }\n    // 兩種先後順序都要試，漏掉一半是最常見的錯誤\n    return std::max(solve(p, firstLen, secondLen), solve(p, secondLen, firstLen));\n  }\n};\n```\n\n## 時間與空間複雜度\n\n**形態一**：排序 $O(n \\log n)$ + 前綴和 $O(n)$ + 每個查詢 $O(\\log n)$，總計 $O((n + q)\\log n)$。空間 $O(n)$。\n\n**形態二**：前綴和 $O(n)$，枚舉分界 $O(n)$（`bestLeft` 是滾動變數，$O(1)$ 空間），兩種順序各跑一次仍是 $O(n)$。**總計 $O(n)$ 時間、$O(n)$ 空間**（只有前綴和陣列）。\n\n若推廣到「$k$ 段不重疊定長子陣列」（689 是 $k=3$），就變成 DP：$\\mathrm{dp}[j][i]$ = 前 $i$ 個元素選 $j$ 段的最大和，$O(kn)$。**$k = 2$ 時手寫兩種順序還可以；$k \\ge 3$ 一律轉 DP，否則排列數爆炸。**\n\n**形態三**：排序主導，$O(n \\log n)$。\n\n與暴力的落差：2389 暴力是 $O(qn \\log n)$（每個查詢重排），本節是 $O((n+q)\\log n)$；1031 暴力枚舉兩段是 $O(n^2)$，本節 $O(n)$。\n\n## 常見錯誤與邊界條件\n\n- 1031 只算一種先後順序（漏掉一半的解）。\n- `bestLeft` 初值設 0 但元素可能全負（本題約束 $\\mathrm{nums}[i] \\ge 0$ 所以安全，**換一題就要改成 `LLONG_MIN` 並小心加法溢位**）。\n- 枚舉分界的迴圈範圍 `i >= f && i + sLen <= n` 寫錯導致越界或漏解。\n- 2389 的 `upper_bound` 忘記減 1（回傳的是「第一個 $> q$ 的位置」，即「$\\le q$ 的個數」，而 $s$ 有 $n+1$ 項含 $s_0$，所以減 1 才是元素個數）。\n- 2389 把 `q` 隱式當 `int` 與 `long long` 比較（型別不一致時 `upper_bound` 的比較器會出錯，要顯式轉型）。\n- 2300 的向上取整用浮點（精度）或寫成 `success / x`（少算）。\n- 2300 的乘積 `x * y` 用 `int` 溢位。\n- 對**連續**子陣列套用「取前 $m$ 小」的交換論證（前提是可任意選取）。\n- 元素有負數時仍假設前綴和單調而去二分。\n- 1788 把強制保留的首尾也套「取正部」。\n- 1788 只考慮相鄰同種花而非最左最右（不是最優）。\n- 前綴和一律 `long long`。\n\n## 與相似技巧的比較\n\n- **與 2.1／2.2**：本節不新增資料結構，只是把前綴和當成「$O(1)$ 取一段」的原語，接上二分、前綴最大值、堆。**先確認 2.1 的三個前提（不變、有逆元、不溢位）成立，再談本節。**\n- **與 1.3 列舉中間**：形態二就是 1.3，只是左右摘要換成「定長區間和的前綴最大值」。\n- **與 binary_search 主題**：形態一的二分是「在單調序列上找分界」，屬於最基本的二分；若題目要二分**答案**（如「最小化最大值」），那是另一回事，別混。\n- **與滑動視窗**：「和不超過 $k$ 的最長**連續**子陣列」（元素非負）用滑動視窗 $O(n)$，比排序 + 二分快且不需額外空間。**連續 → 滑動視窗；可任選 → 排序 + 前綴和 + 二分。** 這是形態一最重要的判準。\n- **與 DP**：$k \\ge 3$ 段不重疊的問題必須轉 DP（689、1911）。**手寫排列組合只在 $k = 2$ 時划算。**\n- **與 6.x 堆**：「取 $k$ 個使某量最大」若還帶另一維約束（2542 最大子序列的分數），前綴和不夠，要配上小根堆維護「當前選中的 $k$ 個中最小的」。\n\n## 代表例題\n\n**題號**：1788、2819、2955、2389、2300\n\n要把區間量改寫成前綴狀態之差；若是計數題，重點是查找過去需要的前綴值。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1788 | [最大化花園的美觀度](https://leetcode.cn/problems/maximize-the-beauty-of-the-garden)（Premium） |  |\n| 2819 | [購買巧克力後的最小相對損失](https://leetcode.cn/problems/minimum-relative-loss-after-buying-chocolates)（Premium） |  |\n| 2955 | [同端子串的數量](https://leetcode.cn/problems/number-of-same-end-substrings)（Premium） |  |\n| 2389 | [和有限的最長子序列](https://leetcode.cn/problems/longest-subsequence-with-limited-sum) | 1388 |\n| 2300 | [咒語和藥水的成功對數](https://leetcode.cn/problems/successful-pairs-of-spells-and-potions) | 1477 |\n| 3709 | [設計考試分數記錄器](https://leetcode.cn/problems/design-exam-scores-tracker) | 1648 |\n| 1031 | [兩個無重疊子陣列的最大和](https://leetcode.cn/problems/maximum-sum-of-two-non-overlapping-subarrays) | 1680 |\n| 1895 | [最大的幻方](https://leetcode.cn/problems/largest-magic-square) | 1781 |\n| 2055 | [蠟燭之間的盤子](https://leetcode.cn/problems/plates-between-candles) | 1819 |\n| 1744 | [你能在你最喜歡的那天喫到你最喜歡的糖果嗎？](https://leetcode.cn/problems/can-you-eat-your-favorite-candy-on-your-favorite-day) | 1859 |\n| 1878 | [矩陣中最大的三個菱形和](https://leetcode.cn/problems/get-biggest-three-rhombus-sums-in-a-grid) | 1898 |\n| 3756 | [連線非零數字並乘以其數字和 II](https://leetcode.cn/problems/concatenate-non-zero-digits-and-multiply-by-sum-ii) | 1968 |\n| 2245 | [轉角路徑的乘積中最多能有幾個尾隨零](https://leetcode.cn/problems/maximum-trailing-zeros-in-a-cornered-path) | 2037 |\n| 1712 | [將陣列分成三個子陣列的方案數](https://leetcode.cn/problems/ways-to-split-array-into-three-subarrays) | 2079 |\n| 1862 | [向下取整數對和](https://leetcode.cn/problems/sum-of-floored-pairs) | 2170 |\n| 3748 | [統計穩定子陣列的數目](https://leetcode.cn/problems/count-stable-subarrays) | 2209 |\n| 2281 | [巫師的總力量和](https://leetcode.cn/problems/sum-of-total-strength-of-wizards) | 2621 |\n| 3445 | [奇偶頻次間的最大差值 II](https://leetcode.cn/problems/maximum-difference-between-even-and-odd-frequency-ii) | 2694 |\n| 2983 | [迴文串重新排列查詢](https://leetcode.cn/problems/palindrome-rearrangement-queries) | 2780 |\n\n## 本節重點速查\n\n- 前綴和在這一節的角色是**原語**：把「一段的量」壓成 $O(1)$，讓外層能換成二分／前綴最大值／堆。\n- **形態一（排序 + 二分）**：非負 + 可任選 ⇒ 前 $m$ 小的和最小（交換論證），$s$ 單調，`upper_bound` 一步到位。**連續子陣列不適用，改滑動視窗。**\n- **形態二（枚舉分界）**：兩段不重疊 ⇒ 必有先後 ⇒ 枚舉分界點，左右各取最佳。**兩種先後順序都要算**，$k \\ge 3$ 轉 DP。\n- $\\mathrm{bestL}$ 用「前綴最大值遞推」而非相減——$\\max$ 沒有逆元。\n- 「可自由丟棄不利元素」→ 把 $\\max(a_t, 0)$ 寫進前綴和的定義（1788）；但**強制保留的元素不適用**。\n- 向上取整寫 `(a + b - 1) / b`，不要用浮點。\n- 前綴和、乘積一律 `long long`。\n",
        },
        {
          id: 18,
          title: "2.6 二維字首和",
          summary:
            "## 這個技術解決什麼問題\n\n矩陣版的 2.1：**多次查詢「某個矩形區域的和」**。暴力每次 $O(mn)$，$q$ 次就是 $O(qmn)$。二維前綴和把它變成預處理 $O(mn)$、單次查詢 $O(1)$。\n\n一維的「兩點相減」在二維變成**四點容斥**。核心公式只有一條，但正負號與下標偏移是所有錯誤的來源，所以本節的重點不是「知道公式」，而是**能從定義重新推出公式**，這樣寫錯時能自己驗。\n\n典型應用有三層：\n- **直接查詢**（304 二維區域和檢索、1738 找出第 K 大的異或座標值）。\n- **枚舉矩形 + $O(1)$ 判定**（1277 全為 1 的正方形子矩陣、1292 元素和小於等於閾值的正方形最大邊長、3070）。\n- **前綴和當成 DP 的加速器**（1314 矩陣區域和、1504 統計全 1 子矩形）。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「二維字首和」想：\n\n- **輸入是一個矩陣，且要多次詢問某個子矩形的總和 / 平均。** 例如 304（矩陣區域和檢索）就是經典的多次查詢。\n- **要枚舉所有可能的子矩形，並對每個都需要它的和。** 先建前綴矩陣，枚舉時每個矩形和 `O(1)` 取得。\n\n:::example 嗅到味道的瞬間：要枚舉很多子矩形且都要它的和 → 先建二維前綴和\n```cpp\n// 嗅到味道的瞬間：要枚舉很多子矩形且都要它的和 → 先建二維前綴和\n// 之後每個矩形和都是四角容斥，O(1)\n```\n:::\n\n\n\n- **暴力是對每個查詢重掃整塊（`O(m·n)`/次），總體會炸；** 二維前綴和把查詢攤成 `O(1)`。\n- 像 1314（矩陣區域和）對每格取鄰域和、1292（邊長最大的正方形）枚舉邊長配合二分、3070（子矩陣計數）枚舉左上角，背後都需要 `O(1)` 取子矩形和；1738 則把加法換成 XOR 前綴，是同一框架的變體。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的 `pre[i][j]` 精確代表「哪一塊」的總和？邊界補零了嗎？\n2. 建表的容斥式（上 + 左 − 左上 + 自己）寫對了嗎？\n3. 查詢的四角容斥（大 − 上 − 左 + 左上）四個索引都對齊了嗎？\n\n## 核心想法與直覺\n\n本節把一維的前綴和搬到二維：給一個矩陣，要反覆查詢「某個子矩形裡所有數字的總和」。如果每次都把矩形一格格加起來，一次查詢就要 `O(行 × 列)`，查詢很多次會非常慢。二維前綴和讓每次查詢降到 `O(1)`。\n\n:::example 二維前綴和：pre[i][j] = 左上角到 (i-1, j-1) 的總和\n先定義前綴矩陣。令 `pre[i][j]` 表示「從左上角 (0,0) 到 (i-1, j-1) 這塊矩形的總和」。它可以靠左邊、上面已算好的格子遞推出來：\n\n```cpp\n// 二維前綴和：pre[i][j] = 左上角到 (i-1, j-1) 的總和\nfor (int i = 0; i < m; ++i) {\n    for (int j = 0; j < n; ++j) {\n        pre[i + 1][j + 1] = pre[i][j + 1] + pre[i + 1][j] - pre[i][j] + mat[i][j];\n    }\n}\n```\n:::\n\n\n\n這條式子的關鍵是「上面那塊」加「左邊那塊」會把左上角重疊算了兩次，所以要 `-pre[i][j]` 補回來——這就是有名的「容斥」。\n\n:::example 查詢：子矩形 (r1,c1)~(r2,c2) 的和，O(1)\n查詢時也是同一套容斥。要算左上角 (r1,c1)、右下角 (r2,c2) 的子矩形和，用大塊減去上面、左邊兩條，再把多扣掉的左上角加回：\n\n```cpp\n// 查詢：子矩形 (r1,c1)~(r2,c2) 的和，O(1)\nint sum = pre[r2 + 1][c2 + 1] - pre[r1][c2 + 1] - pre[r2 + 1][c1] + pre[r1][c1];\n```\n:::\n\n\n\n核心精神：**先花 `O(行 × 列)` 把整張前綴矩陣建好，之後每次矩形查詢都靠「四個角的容斥」`O(1)` 完成。** 多 padding 一圈零（用 `+1` 的索引）能讓邊界不必特別判斷。\n\n## 狀態／資料結構定義\n\n**定義。** 對 $m \\times n$ 的矩陣 $a$，令\n$$S[i][j] = \\sum_{r < i} \\sum_{c < j} a[r][c], \\qquad 0 \\le i \\le m,\\ 0 \\le j \\le n .$$\n\n$S$ 的大小是 $(m+1) \\times (n+1)$，第 0 列與第 0 行全為 0——**與一維同樣的哨兵理由：讓 $r_1 = 0$ 或 $c_1 = 0$ 不必特判。**\n\n**遞推（建表）：**\n$$S[i+1][j+1] = S[i][j+1] + S[i+1][j] - S[i][j] + a[i][j].$$\n\n**查詢（左上 $(r_1,c_1)$、右下 $(r_2,c_2)$ 的閉矩形）：**\n$$\\mathrm{sum} = S[r_2+1][c_2+1] - S[r_1][c_2+1] - S[r_2+1][c_1] + S[r_1][c_1].$$\n\n**兩條式子的符號恰好相反**（建表是 $+,+,-$，查詢是 $-,-,+$），這不是巧合：建表是「把兩塊拼起來扣掉重疊」，查詢是「從大塊挖掉兩塊再補回多扣的」。記住這個對偶就不會抄反。\n\n**推廣到任意可逆運算。** 和 2.1 一樣，只要運算構成群就通用。1738 用的是 XOR：\n$$S[i+1][j+1] = S[i][j+1] \\oplus S[i+1][j] \\oplus S[i][j] \\oplus a[i][j],$$\n查詢時四項也全部 XOR（XOR 自逆，正負號消失了——**這是 XOR 版比加法版好寫的唯一原因**）。\n\n## 不變量或正確性證明\n\n**引理（容斥）。** 記 $R(i,j) = \\{(r,c) : r < i,\\ c < j\\}$（左上角的開矩形），則\n$$\\{(r,c): r_1 \\le r \\le r_2,\\ c_1 \\le c \\le c_2\\} = R(r_2{+}1, c_2{+}1) \\setminus \\big(R(r_1, c_2{+}1) \\cup R(r_2{+}1, c_1)\\big),$$\n且 $R(r_1, c_2{+}1) \\cap R(r_2{+}1, c_1) = R(r_1, c_1)$。\n\n*證明。* 取任一 $(r,c) \\in R(r_2{+}1, c_2{+}1)$，即 $r \\le r_2$ 且 $c \\le c_2$。它不在目標矩形內 $\\iff$ $r < r_1$ 或 $c < c_1$ $\\iff$ 它屬於 $R(r_1, c_2{+}1)$ 或 $R(r_2{+}1, c_1)$。而同時屬於兩者 $\\iff r < r_1$ 且 $c < c_1$ $\\iff (r,c) \\in R(r_1,c_1)$。$\\square$\n\n由容斥原理 $|A \\setminus (B \\cup C)| = |A| - |B| - |C| + |B \\cap C|$（推廣到加權和亦成立，因為加法可交換結合），得查詢公式。\n\n**這條論證點名的前提：$R$ 必須是「以原點為一角的矩形」。** 也就是說，$S$ 的語意必須是**從 $(0,0)$ 累積到 $(i-1,j-1)$ 的整塊**，而不是任何別的形狀。這聽起來理所當然，但在 2.8 會遇到「三角形前綴」（只累積嚴格左上）的情形——那時 $B \\cap C$ 就不再是 $R(r_1,c_1)$，容斥式子要重推。**「四點容斥」不是萬用公式，它是矩形這個特定形狀的產物。**\n\n**推廣到 $d$ 維。** 查詢需要 $2^d$ 項，符號是 $(-1)^{\\text{被截掉的維度數}}$。$d = 3$ 時是 8 項，仍可手寫；$d \\ge 4$ 通常改用「逐維做一維前綴和」的寫法（見下），既不會抄錯符號也自動正確。\n\n**逐維前綴的等價寫法（更不容易錯）：**\n```\nfor each row i: 做一次橫向一維前綴和\nfor each col j: 做一次縱向一維前綴和\n```\n*正確性*：橫向掃完後 $T[i][j] = \\sum_{c<j} a[i][c]$；縱向再掃一次得 $\\sum_{r<i}\\sum_{c<j} a[r][c] = S[i][j]$。這用的是**求和可交換次序**（Fubini），與容斥無關。**推薦在高維或運算複雜時用這種寫法。**\n\n**1292（元素和 ≤ threshold 的最大正方形邊長）的單調性。** 令 $f(k) = 1$ 若存在邊長 $k$ 的正方形其和 $\\le$ threshold。因矩陣元素**非負**，邊長 $k$ 的正方形包含某個邊長 $k-1$ 的正方形，故其和不小於後者。於是 $f$ 單調（$f(k)=1 \\Rightarrow f(k-1)=1$），**可以二分答案**，$O(mn\\log\\min(m,n))$。\n\n但更簡單的是**遞增指標**：從 $k=1$ 開始，只在「當前 $k+1$ 可行」時才 $k{+}{+}$，總計 $O(mn)$。理由是 $k$ 只增不減，攤還後每個格子只做 $O(1)$ 次判定。**這裡「元素非負」是單調性的前提；若矩陣可以有負數，$f$ 不單調，二分與遞增指標都失效，只能 $O(mn\\min(m,n))$ 全枚舉。**\n\n**1314（矩陣區域和）的邊界裁剪。** 答案 $\\mathrm{ans}[i][j]$ 是以 $(i,j)$ 為中心、半徑 $k$ 的方塊和，方塊可能超出邊界。做法是把座標夾到合法範圍：\n$$r_1 = \\max(i-k, 0),\\quad r_2 = \\min(i+k, m-1),\\quad c_1 = \\max(j-k,0),\\quad c_2=\\min(j+k,n-1).$$\n**夾取（clamp）是正確的，因為「超出邊界的部分貢獻為 0」等價於「只取交集」。** 但若題目改成「超出邊界視為某個非零預設值」，clamp 就錯了，得補上邊界外的貢獻——**前提要看清楚。**\n\n## 逐步演算法\n\n:::example 狀態表示：pre 比原矩陣多一行一列（補零邊界）\n**1. 狀態表示：開一個比原矩陣大一圈的前綴矩陣。** 多出來的第 0 行 / 第 0 列全為 0，避免越界判斷。\n\n```cpp\n// 狀態表示：pre 比原矩陣多一行一列（補零邊界）\nvector<vector<int>> pre(m + 1, vector<int>(n + 1, 0));\n```\n:::\n\n\n\n:::example 建表：容斥遞推填滿 pre\n**2. 更新流程（建表）：用「上 + 左 − 左上 + 自己」遞推。**\n\n```cpp\n// 建表：容斥遞推填滿 pre\nfor (int i = 0; i < m; ++i) {\n    for (int j = 0; j < n; ++j) {\n        pre[i + 1][j + 1] = pre[i][j + 1] + pre[i + 1][j] - pre[i][j] + mat[i][j];\n    }\n}\n```\n:::\n\n\n\n:::example 讀取：回傳子矩形和\n**3. 答案讀取：四角容斥，O(1) 取任意子矩形和。**\n\n```cpp\n// 讀取：回傳子矩形和\nint query(int r1, int c1, int r2, int c2) {\n    return pre[r2 + 1][c2 + 1] - pre[r1][c2 + 1] - pre[r2 + 1][c1] + pre[r1][c1];\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 四個角的索引一旦寫錯（少加一、減錯一條），結果就全錯；牢記「大塊 − 上 − 左 + 左上」並善用 `+1` 偏移。\n- 大矩陣總和可能溢位，必要時用 `long long`。\n- 建表 `O(m·n)`、每次查詢 `O(1)`、空間 `O(m·n)`。\n\n## C++17 模板\n\n```cpp\n// 二維區域和檢索 - 矩陣不可變（LeetCode 304）：二維前綴和標準骨架。\nclass NumMatrix {\n  std::vector<std::vector<long long>> s_;   // (m+1) x (n+1)，s_[0][*] = s_[*][0] = 0\n\n public:\n  explicit NumMatrix(std::vector<std::vector<int>>& matrix) {\n    int m = matrix.size(), n = m ? matrix[0].size() : 0;\n    s_.assign(m + 1, std::vector<long long>(n + 1, 0));\n    for (int i = 0; i < m; ++i) {\n      for (int j = 0; j < n; ++j) {\n        // 建表：兩塊相加，扣掉重複計入的左上角\n        s_[i + 1][j + 1] = s_[i][j + 1] + s_[i + 1][j] - s_[i][j] + matrix[i][j];\n      }\n    }\n  }\n\n  int sumRegion(int r1, int c1, int r2, int c2) {\n    // 查詢：大塊挖掉上、左兩塊，再補回被扣兩次的左上角（符號與建表相反）\n    long long v = s_[r2 + 1][c2 + 1] - s_[r1][c2 + 1] - s_[r2 + 1][c1] + s_[r1][c1];\n    return static_cast<int>(v);\n  }\n};\n```\n\n```cpp\n// 元素和小於等於閾值的正方形最大邊長（LeetCode 1292）：遞增指標 + O(1) 判定。\nclass Solution {\n public:\n  int maxSideLength(std::vector<std::vector<int>>& mat, int threshold) {\n    int m = mat.size(), n = mat[0].size();\n    std::vector<std::vector<long long>> s(m + 1, std::vector<long long>(n + 1, 0));\n    for (int i = 0; i < m; ++i) {\n      for (int j = 0; j < n; ++j) {\n        s[i + 1][j + 1] = s[i][j + 1] + s[i + 1][j] - s[i][j] + mat[i][j];\n      }\n    }\n\n    int k = 0;                       // 目前已知可行的最大邊長；只增不減\n    for (int i = 0; i < m; ++i) {\n      for (int j = 0; j < n; ++j) {\n        // 嘗試把邊長推大：以 (i, j) 為右下角、邊長 k+1 的正方形\n        while (i - k >= 0 && j - k >= 0) {\n          long long v = s[i + 1][j + 1] - s[i - k][j + 1] - s[i + 1][j - k] + s[i - k][j - k];\n          if (v > threshold) { break; }\n          ++k;                       // 可行，繼續試更大的（元素非負 ⇒ 單調）\n        }\n      }\n    }\n    return k;\n  }\n};\n```\n\n`while` 迴圈看似 $O(\\min(m,n))$，但 `k` 全程只增不減、上界 $\\min(m,n)$，**攤還後整個雙層迴圈仍是 $O(mn)$**。這是本節唯一需要攤還分析的地方。\n\n## 時間與空間複雜度\n\n**建表** $O(mn)$ 時間、$O(mn)$ 空間；**單次查詢** $O(1)$（4 次陣列存取、3 次加減）。$q$ 次查詢總計 $O(mn + q)$，相對暴力的 $O(qmn)$。\n\n**枚舉所有子矩形**是 $O(m^2n^2)$ 個，配 $O(1)$ 判定仍是 $O(m^2n^2)$——$m = n = 100$ 時是 $10^8$，勉強；$m = n = 300$ 就不行了。**二維前綴和不能把「枚舉所有矩形」變快，它只把每次判定變快。** 要更快得靠 1504 那種「固定上下界 + 一維前綴和 + 雜湊表」的降維，$O(m^2 n)$。\n\n**空間優化**：若只需一次掃描，可以只保留兩列（滾動），$O(n)$ 空間。但大多數題目要隨機查詢，省不掉。\n\n**$d$ 維**：建表 $O(n^d)$（逐維寫法是 $d$ 次 $O(n^d)$ 掃描），查詢 $O(2^d)$。\n\n## 常見錯誤與邊界條件\n\n- 查詢公式的符號抄成建表的符號（$+,+,-$ 與 $-,-,+$ 相反）。\n- 下標忘記 $+1$（`s[r2][c2]` 少算最後一列一行）。\n- `s` 開成 $m \\times n$ 而非 $(m+1)\\times(n+1)$，被迫特判 $r_1 = 0$。\n- 空矩陣時 `matrix[0].size()` 直接崩潰（要先判 `m`）。\n- 用 `int` 存二維前綴和——**$100 \\times 100$ 的格子每格 $10^5$ 就是 $10^9$，已經逼近 `int` 上限，$300\\times300$ 必爆**。\n- 1292 的 `while` 條件寫成 `if`（就不是遞增指標，會漏解）。\n- 1292 對可能有負數的矩陣套用單調性。\n- 1314 的 clamp 邊界寫成 `min(i+k, m)` 而非 `m-1`。\n- 1738 的 XOR 版誤用減法（XOR 沒有「減」，自己就是逆）。\n- 1738 求第 $k$ 大時對整個 $mn$ 陣列排序（$O(mn\\log mn)$ 可過，但 `nth_element` 是 $O(mn)$，且要注意是「第 $k$ 大」不是「第 $k$ 小」）。\n- 把 clamp 用在「邊界外有預設值」的題目上。\n\n## 與相似技巧的比較\n\n- **與 2.1 一維前綴和**：完全同源，只是容斥從 2 項變 4 項。**同樣要求矩陣不變、運算有逆元、不溢位。** 若要支援單點修改，得換二維樹狀陣列（$O(\\log m \\log n)$）。\n- **與 2.8 二維前綴最小值**：$\\min/\\max$ 沒有逆元，四點容斥不成立。2.8 用的是「只往左上遞推」的 DP，形狀與語意都不同——**別把本節的公式套過去。**\n- **與 3.3 二維差分**：互為逆運算。二維差分處理「多次矩形加值、最後查詢」（2536 子矩陣元素加 1、850）；本節處理「不修改、多次查詢」。\n- **與 1504（統計全 1 子矩形）的降維法**：當要枚舉的矩形數量本身就是 $O(m^2n^2)$ 時，$O(1)$ 判定救不了。降維法固定上下邊界（$O(m^2)$），把每一行壓成一個數，再對這一維用一維技巧（$O(n)$ 或 $O(n\\log n)$）。**判準：查詢次數是輸入給的 ⇒ 用前綴和；查詢次數是自己枚舉出來的 ⇒ 先想降維。**\n- **與 DP**：1277（全為 1 的正方形子矩陣）用 $\\mathrm{dp}[i][j] = \\min(\\text{三鄰居}) + 1$ 是 $O(mn)$，比二維前綴和 + 枚舉邊長的 $O(mn\\min(m,n))$ 更快。**「正方形」這個額外約束讓 DP 有機可乘；一般矩形就沒有。**\n\n## 代表例題\n\n**題號**：304、1292、1314、1738、3070\n\n要把區間量改寫成前綴狀態之差；若是計數題，重點是查找過去需要的前綴值。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 304 | [二維區域和檢索 - 矩陣不可變](https://leetcode.cn/problems/range-sum-query-2d-immutable) |  |\n| 1292 | [元素和小於等於閾值的正方形的最大邊長](https://leetcode.cn/problems/maximum-side-length-of-a-square-with-sum-less-than-or-equal-to-threshold) | 1735 |\n| 1314 | [矩陣區域和](https://leetcode.cn/problems/matrix-block-sum) | 1484 |\n| 1738 | [找出第 K 大的異或座標值](https://leetcode.cn/problems/find-kth-largest-xor-coordinate-value) | 1671 |\n| 3070 | [元素和小於等於 k 的子矩陣的數目](https://leetcode.cn/problems/count-submatrices-with-top-left-element-and-sum-less-than-k) | 1499 |\n| 3212 | [統計 X 和 Y 頻數相等的子矩陣數量](https://leetcode.cn/problems/count-submatrices-with-equal-frequency-of-x-and-y) | 1673 |\n\n**延伸閱讀**：[LeetCode 304 二維前綴和圖解](https://leetcode.cn/problems/range-sum-query-2d-immutable/solution/tu-jie-yi-zhang-tu-miao-dong-er-wei-qian-84qp/)，重點是從半開矩形定義重新推出四角容斥。\n\n## 本節重點速查\n\n- $S[i][j] = \\sum_{r<i}\\sum_{c<j} a[r][c]$，大小 $(m+1)\\times(n+1)$，首列首行為 0。\n- 建表 $S[i{+}1][j{+}1] = S[i][j{+}1] + S[i{+}1][j] - S[i][j] + a[i][j]$（$+,+,-$）。\n- 查詢 $S[r_2{+}1][c_2{+}1] - S[r_1][c_2{+}1] - S[r_2{+}1][c_1] + S[r_1][c_1]$（$-,-,+$）。**兩者符號相反。**\n- 公式忘了就用容斥重推：大塊 $-$ 上塊 $-$ 左塊 $+$ 左上塊。\n- 高維或運算複雜時改用**逐維一維前綴和**，不會抄錯符號。\n- 容斥的前提是 $R$ 為「以原點為一角的矩形」；$\\min/\\max$ 無逆元不適用（見 2.8）。\n- 1292 的遞增指標靠「元素非負 ⇒ 邊長單調」，攤還 $O(mn)$。\n- 一律 `long long`；空矩陣先判 `m`。\n- 枚舉所有矩形仍是 $O(m^2n^2)$——那要靠降維（1504），不是靠前綴和。\n",
        },
        {
          id: 19,
          title: "2.7 字首和與雜湊表 > 字首和與有序集合",
          summary:
            "## 這個技術解決什麼問題\n\n2.2 用雜湊表解「區間和**恰為** $k$」：把條件改寫成 $s_l = s_{r+1} - k$，右邊是一個**唯一確定的鍵**，查表 $O(1)$。\n\n但很多題目的條件不是等號：\n\n- 「區間和**不超過** $k$，求最大」（363 矩形區域不超過 K 的最大數值和）\n- 「區間和為正，求**最小**」（3364 最小正和子陣列）\n- 「兩數之差在 $[a,b]$ 內」（220 存在重複元素 III）\n\n這些條件對應的不是一個鍵，而是**一段鍵的區間**。雜湊表無法回答「範圍內有沒有元素／最接近的是哪個」，因為它不保存順序。此時要換成**有序集合**（`std::set` / `std::multiset`，紅黑樹），用 `lower_bound` 在 $O(\\log n)$ 內找到邊界。\n\n**代價是每次查詢從 $O(1)$ 變 $O(\\log n)$，換來的是能回答範圍型查詢。** 這是 2.2 之後最自然的一次升級。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「字首和 + hash map / 有序集合」想：\n\n- **要找子陣列和等於某固定值的數量 / 是否存在** → hash map 查 `prefix - k`。\n- **要找子陣列和『不超過上限』『最接近』『落在某範圍』，且陣列可能有負數** → 有序集合 + `lower_bound`。\n\n:::example 嗅到味道的瞬間：含負數無法用滑動視窗，但要找最接近的區間和 → 有序集合\n```cpp\n// 嗅到味道的瞬間：含負數無法用滑動視窗，但要找最接近的區間和 → 有序集合\nauto it = seen.lower_bound(pre - target);\n```\n:::\n\n\n\n- **暴力是雙層迴圈枚舉所有區間（`O(n^2)`）；** 配上 hash map / 有序集合後降到 `O(n)` 或 `O(n log n)`。\n- 像 3364（最小正和子陣列）需要找最接近的前綴差，363（矩形不超過 K 的最大和）把每組列壓成一維後對前綴用有序集合二分，都是本節的典型套路。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要的是「精確等於」（用 hash map）還是「範圍 / 最接近」（用有序集合）？\n2. 我有沒有先放入空前綴 `0`？「先查再記」的順序對嗎？\n3. 陣列有負數嗎？若有，是否已避開滑動視窗、改用有序集合？\n\n## 核心想法與直覺\n\n本節把「前綴和」配上一個能快速查詢的容器——hash map 或有序集合（`set` / `map`）——用來在掃描過程中找出「滿足某條件的另一個前綴」。\n\n先回到基本式：區間 `[l, r]` 的和等於 `prefix[r + 1] - prefix[l]`。很多題目其實是反過來問：「對目前的右端點 r，有沒有一個過去的前綴，使這段和等於 / 不超過 / 最接近某個值？」這就需要一個能查歷史前綴的結構。\n\n:::example 前綴和 + hash map：統計和為 k 的子陣列個數\n**用 hash map：適合『精確等於』。** 例如要找「和恰為 k 的子陣列數量」，對每個 `prefix` 去查有沒有 `prefix - k` 出現過：\n\n```cpp\n// 前綴和 + hash map：統計和為 k 的子陣列個數\nunordered_map<long long, int> cnt{{0, 1}};  // 空前綴先放一個\nlong long pre = 0;\nint ans = 0;\nfor (int x : a) {\n    pre += x;\n    ans += cnt[pre - k];  // 有幾個過去前綴能湊出和 k\n    cnt[pre]++;           // 記錄目前前綴\n}\n```\n:::\n\n\n\n:::example 前綴和 + 有序集合：找最接近目標的歷史前綴\n**用有序集合：適合『範圍 / 最接近』。** hash map 只能查「相等」，但若要找「最接近、剛好大於某值、不超過某上限」的前綴，就需要前綴有序。把看過的前綴丟進 `set`，再用 `lower_bound` 找：\n\n```cpp\n// 前綴和 + 有序集合：找最接近目標的歷史前綴\nset<long long> seen{0};\nauto it = seen.lower_bound(pre - target);  // 第一個 >= 目標的前綴\n                                                                                      // it 與其前一個就是候選，取較佳者\n```\n:::\n\n\n\n核心精神：**前綴和把區間問題化成「兩個前綴的關係」，再用 hash map（查相等）或有序集合（查範圍 / 最近）把『尋找配對前綴』壓到 `O(1)` 或 `O(log n)`，整體 `O(n)` 或 `O(n log n)`。**\n\n## 狀態／資料結構定義\n\n沿用 1.1「列舉右端點、維護左端點集合」的框架，只是左側摘要從 `unordered_map` 換成有序容器。\n\n**形態一：左端點集合是「所有更早的前綴」（363）**\n- `std::set<long long> seen`，初始放入 $s_0 = 0$。\n- 外層迴圈固定右端點，`cur` 為當前前綴和 $s_{r+1}$。\n- 查詢：`it = seen.lower_bound(cur - k)`，若 `it != seen.end()` 則 `cur - *it` 是「$\\le k$ 且最大」的候選。\n- 更新：`seen.insert(cur)`。\n\n**形態二：左端點集合是一個滑動視窗（3364、長度受限的變體）**\n- `std::multiset<long long> win`，維護所有**合法**的左端點前綴。必須用 `multiset`——**前綴和會重複，`set` 會吃掉重複值，導致視窗大小與集合大小不同步。**\n- 右端點推進時：先把新變成合法的前綴 `insert`，把過期的前綴 `erase(win.find(v))`。\n- 查詢：依題意用 `lower_bound` / `upper_bound` 找邊界候選。\n\n**形態三：值域上的有序集合（220，不是前綴和但同一套推理）**\n- 集合裝的是「視窗內的元素值」，查 $[\\,x-t,\\ x+t\\,]$ 內是否有元素：`it = win.lower_bound(x - t); it != win.end() && *it <= x + t`。\n\n## 不變量或正確性證明\n\n**主不變量（形態一）。** 在處理右端點 $r$ 的那一刻，\n$$\\texttt{seen} \\;=\\; \\{\\, s_l \\;:\\; 0 \\le l \\le r \\,\\},$$\n也就是「所有可以當左端點的前綴」，含 $s_0 = 0$（對應從頭開始的區間），且**不含** $s_{r+1}$（否則會取到空區間）。維持方式與 2.2 相同：**先查詢，再插入 `cur`。**\n\n**核心引理（為什麼查邊界就夠）。** 固定右端點，令 $s_{r+1} = C$。要找\n$$\\max\\{\\,C - x \\;:\\; x \\in \\texttt{seen},\\ C - x \\le k\\,\\}.$$\n條件 $C - x \\le k \\iff x \\ge C - k$；而目標 $C - x$ 隨 $x$ **嚴格遞減**。故最佳的 $x$ 是可行集合中**最小的那個**，即 $\\min\\{x \\in \\texttt{seen} : x \\ge C-k\\}$——這正是 `lower_bound(C - k)` 回傳的元素。$\\square$\n\n**這條論證點名了它依賴的前提：目標函數對被查詢的鍵是單調的。** 因為 $C-x$ 對 $x$ 單調，「可行區間的邊界」才會是最優解，於是一次 `lower_bound` 就夠。若目標不單調（例如要最大化 $|C - x|$，或目標同時牽涉 $x$ 與 $l$ 的下標），邊界不再必然最優，就得查多個候選、甚至換線段樹。**「單調 ⇒ 查邊界」是所有 `lower_bound` 型解法的共同骨架，不只在這一節。**\n\n**與 2.2 的分界，說得更精確一點。** 雜湊表能用的充要條件是：合法左鍵的集合是**單點** $\\{\\psi(C)\\}$。條件變成 $x \\ge C-k$ 之後，合法集合是一條**射線**，單點查詢無法覆蓋；而枚舉射線上的所有值在值域大時不可行。這正是 1.2 提過的「$|a-b| \\le d$ 沒有唯一補數」在前綴和上的化身——**同一個判準，換了個舞台。**\n\n**形態二的視窗不變量（3364：長度介於 $l$ 與 $r$ 的最小正和子陣列）。** 子陣列 $[i, j-1]$（前綴下標 $i$ 到 $j$）的長度是 $j - i$，要求 $l \\le j - i \\le r$，即\n$$j - r \\;\\le\\; i \\;\\le\\; j - l .$$\n處理前綴下標 $j$ 前，維持\n$$\\texttt{win} \\;=\\; \\{\\, s_i \\;:\\; \\max(0,\\, j-r) \\le i \\le j-l \\,\\}\\quad(\\text{可重集}).$$\n**推進規則**：$j$ 從 $l$ 開始遞增；每步先 `insert(s[j-l])`（新變合法的下界端），若 $j - r - 1 \\ge 0$ 則 `erase(win.find(s[j-r-1]))`（剛過期的）。**兩個動作的順序無所謂（它們碰的是不同元素），但都必須在查詢之前完成**——否則視窗與定義不符，這是本形態唯一的失分點。\n\n**查詢的推導。** 要 $s_j - s_i > 0$ 且最小，即 $s_i < s_j$ 且 $s_i$ **最大**。`it = win.lower_bound(s[j])` 給出第一個 $\\ge s_j$ 的位置；**前一個位置**（`--it`，需先確認 `it != win.begin()`）就是最大的 $< s_j$ 的元素。同樣是「目標對 $x$ 單調 ⇒ 取邊界」。\n\n*為什麼不能用 `upper_bound`？* `upper_bound(s[j])` 跳過所有等於 $s_j$ 的元素，但我們要排除的正是「等於」（差為 0，不是正數）。用 `lower_bound` 再退一格，剛好把等於的那批也留在右側。**這一格之差就是「和為正」與「和非負」的區別。**\n\n**multiset 刪除必須用迭代器。** `win.erase(v)` 會刪掉**所有**等於 $v$ 的元素；`win.erase(win.find(v))` 只刪一個。前綴和重複是常態（原陣列有 0 或正負相消時），**寫錯這一行的程式在多數測資上會對，在特定測資上錯得莫名其妙**。\n\n**363 的降維。** 要在矩陣中找「和不超過 $k$ 的最大子矩形」。固定上下兩條列邊界 $(r_1, r_2)$ 後，把每一行壓成一個數（該行在 $[r_1,r_2]$ 內的和），問題就退化成一維的「和不超過 $k$ 的最大子陣列和」，套形態一。\n\n*不漏*：任一子矩形都有唯一的上下邊界，故會被某組 $(r_1,r_2)$ 枚舉到。\n*不重*：不同 $(r_1,r_2)$ 給出的是不同矩形集合，取 $\\max$ 不受影響。\n**注意這裡的行壓縮要用一維前綴和或增量累加，不能每次重算，否則多一個 $O(m)$。**\n\n## 逐步演算法\n\n:::example 狀態表示：pre 累積前綴，容器存看過的前綴\n**1. 狀態表示：邊掃邊累積 prefix，並選對容器。** 求「精確等於」用 hash map；求「最接近 / 範圍」用有序 `set`。\n\n```cpp\n// 狀態表示：pre 累積前綴，容器存看過的前綴\nlong long pre = 0;\nset<long long> seen{0};  // 含空前綴；計數題改用 unordered_map\n```\n:::\n\n\n\n:::example 更新：先查歷史，再把目前前綴記下\n**2. 更新流程：先查再記。** 順序很重要——先用目前 prefix 去查歷史，再把目前 prefix 放進容器，才不會把自己當成另一端誤算。\n\n```cpp\n// 更新：先查歷史，再把目前前綴記下\nfor (int x : a) {\n    pre += x;\n    /* 這裡用 pre 查容器，求答案 */\n    seen.insert(pre);  // 最後才記錄自己\n}\n```\n:::\n\n\n\n:::example 讀取（範圍型）：找滿足下界、且最小的合格區間\n**3. 答案讀取：依需求查相等或查範圍。**\n\n```cpp\n// 讀取（範圍型）：找滿足下界、且最小的合格區間\nauto it = seen.lower_bound(pre - target);\nif (it != seen.end()) {\n    ans = min(ans, /* 由 *it 還原的區間量 */ 0LL);\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 一定要先放「空前綴」（`0`）進容器，否則「從開頭起算」那段會被漏掉。\n- 前綴和可能很大或為負，用 `long long`；有負數時不能用滑動視窗，正是要靠有序集合處理。\n- hash map 版 `O(n)`；有序集合版每步 `O(log n)`，合計 `O(n log n)`。\n\n## C++17 模板\n\n```cpp\n// 矩形區域不超過 K 的最大數值和（LeetCode 363）：降維 + 前綴和 + 有序集合。\nclass Solution {\n public:\n  int maxSumSubmatrix(std::vector<std::vector<int>>& matrix, int k) {\n    int m = matrix.size(), n = matrix[0].size();\n    long long ans = LLONG_MIN;\n\n    for (int top = 0; top < m; ++top) {\n      std::vector<long long> colSum(n, 0);          // [top, bottom] 內每一行的和\n      for (int bottom = top; bottom < m; ++bottom) {\n        for (int j = 0; j < n; ++j) { colSum[j] += matrix[bottom][j]; }  // 增量，不重算\n\n        // 一維問題：和不超過 k 的最大子陣列和\n        std::set<long long> seen{0};                // s_0 = 0，代表「從頭開始」\n        long long cur = 0;\n        for (int j = 0; j < n; ++j) {\n          cur += colSum[j];                         // cur = s_{j+1}\n          // 要 cur - x <= k 且 cur - x 最大 => x 最小且 x >= cur - k\n          auto it = seen.lower_bound(cur - k);\n          if (it != seen.end()) { ans = std::max(ans, cur - *it); }\n          seen.insert(cur);                         // 先查後插，排除空區間\n        }\n      }\n    }\n    return static_cast<int>(ans);\n  }\n};\n```\n\n```cpp\n// 最小正和子陣列（LeetCode 3364）：滑動視窗版的有序集合。\nclass Solution {\n public:\n  int minimumSumSubarray(std::vector<int>& nums, int l, int r) {\n    int n = nums.size();\n    std::vector<long long> s(n + 1, 0);\n    for (int i = 0; i < n; ++i) { s[i + 1] = s[i] + nums[i]; }\n\n    long long ans = LLONG_MAX;\n    std::multiset<long long> win;                   // 必須 multiset：前綴和會重複\n    for (int j = l; j <= n; ++j) {\n      win.insert(s[j - l]);                         // 新變合法的左端點\n      if (j - r - 1 >= 0) {\n        win.erase(win.find(s[j - r - 1]));          // 用迭代器刪，只刪一個\n      }\n      // 要 s[j] - x > 0 且最小 => x < s[j] 且 x 最大\n      auto it = win.lower_bound(s[j]);              // 第一個 >= s[j]\n      if (it != win.begin()) {\n        --it;                                       // 退一格：最大的 < s[j]\n        ans = std::min(ans, s[j] - *it);\n      }\n    }\n    return ans == LLONG_MAX ? -1 : static_cast<int>(ans);\n  }\n};\n```\n\n## 時間與空間複雜度\n\n**形態一（一維）**：每個右端點一次 `lower_bound` + 一次 `insert`，各 $O(\\log n)$，總計 $O(n\\log n)$ 時間、$O(n)$ 空間。\n\n**363**：外層 $O(m^2)$ 組列邊界，內層 $O(n\\log n)$，總計 $O(m^2 n\\log n)$。**應該把較小的維度放外層**：先判斷 $m$ 與 $n$ 誰小，必要時把矩陣轉置，複雜度變 $O(\\min^2 \\cdot \\max \\cdot \\log\\max)$。$m = n = 100$ 時約 $10^4 \\times 100 \\times 7 \\approx 7\\times10^6$，可過。\n\n**3364**：$n$ 次迭代，每次 $O(\\log n)$，總計 $O(n\\log n)$、空間 $O(n)$。（本題官方約束 $n \\le 100$，$O(n^2)$ 暴力也能過——**這裡寫 $O(n\\log n)$ 是為了讓解法能推廣到 $n = 10^5$ 的同型題。**）\n\n**常數的現實**：`std::set` 是紅黑樹，每個節點三個指標 + 顏色，記憶體與快取表現都遠差於陣列。實測上比 `unordered_map` 慢 2–5 倍、比排序後二分慢更多。**能離線處理（先收集所有查詢再排序）時，「排序 + 二分」或「排序 + 樹狀陣列」通常比 `set` 快數倍**；`set` 的價值在於**線上**（邊插入邊查詢，且插入的內容依賴先前的查詢結果）。\n\n## 常見錯誤與邊界條件\n\n- `set` 忘記初始插入 $0$（漏掉「從頭開始」的區間）。\n- 先插入 `cur` 再查詢（取到空區間，和為 0，在「$\\le k$ 且 $k \\ge 0$」時會回傳錯誤的 0）。\n- `lower_bound` 與 `upper_bound` 用反——**記法：`lower_bound(v)` 是「第一個 $\\ge v$」，`upper_bound(v)` 是「第一個 $> v$」**。\n- 忘記檢查 `it != seen.end()` 就解參考。\n- 退一格前忘記檢查 `it != win.begin()`（`--begin()` 是未定義行為，常常不會崩，只是讀到垃圾）。\n- 用 `set` 而非 `multiset` 導致重複前綴被吞、視窗與集合不同步。\n- `multiset::erase(value)` 一次刪光所有相同值。\n- 用 `std::set::lower_bound` 的**成員函式**寫成 `std::lower_bound(s.begin(), s.end(), v)`（後者對非隨機存取迭代器退化成 $O(n)$ 線性掃描，$O(n\\log n)$ 變 $O(n^2)$，**這是最隱蔽的效能陷阱**）。\n- `cur - k` 溢位（`k` 可以是 $-2^{31}$，用 `long long` 算）。\n- 363 忘記轉置導致 $m \\gg n$ 時超時。\n- 363 的 `colSum` 每輪重算而非增量。\n\n## 與相似技巧的比較\n\n| 條件形態 | 合法左鍵集合 | 工具 | 單次成本 |\n| --- | --- | --- | --- |\n| 和 $= k$ | 單點 | `unordered_map`（2.2） | $O(1)$ |\n| 和 $\\le k$ / 最接近 | 射線／區間 | `set` + `lower_bound`（本節） | $O(\\log n)$ |\n| 和 $\\le k$ 且元素非負 | 射線，但端點單調 | 滑動視窗 | 攤還 $O(1)$ |\n| 需要「區間內的計數」 | 區間 + 聚合 | 樹狀陣列 / 線段樹（9.x） | $O(\\log n)$ |\n\n- **與滑動視窗**：若元素全非負，前綴和單調遞增，「和 $\\le k$ 的最長區間」左端點也單調，雙指標 $O(n)$ 完勝。**有負數才需要本節。** 這是選型的第一個問題。\n- **與 2.2**：判準是「合法左鍵是一個點還是一段」。\n- **與樹狀陣列（9.1）**：若要問的不是「最接近的元素」而是「範圍內有幾個」（例如逆序對、327 區間和的個數），`set` 不夠（它不支援 $O(\\log n)$ 求排名），要用**離散化 + 樹狀陣列**。**327 與 363 長得很像，但一個問「有幾個」、一個問「最大的是哪個」，工具不同。**\n- **與 2.5 排序 + 二分**：2.5 的資料是**離線**的（一次排好序，之後只查詢）；本節是**線上**的（集合隨掃描增長）。**能離線就離線，快得多。**\n- **與 1.2**：$|a-b| \\le d$ 型條件在 1.2 就已宣告「雜湊表無解」，本節給出正式的替代品。\n\n## 代表例題\n\n**題號**：363、3364\n\n要把區間量改寫成前綴狀態之差；若是計數題，重點是查找過去需要的前綴值。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 363 | [矩形區域不超過 K 的最大數值和](https://leetcode.cn/problems/max-sum-of-rectangle-no-larger-than-k) |  |\n| 3364 | [最小正和子陣列](https://leetcode.cn/problems/minimum-positive-sum-subarray) | 1301 |\n\n## 本節重點速查\n\n- 條件從「$=$」變成「$\\le$／最接近／範圍內」⇒ 合法左鍵從**單點**變成**區間** ⇒ 雜湊表換**有序集合**，$O(1)$ 換 $O(\\log n)$。\n- 核心引理：目標對被查鍵**單調** ⇒ 最優解在可行區間的**邊界** ⇒ 一次 `lower_bound` 就夠。**不單調就不成立。**\n- `lower_bound(v)` = 第一個 $\\ge v$；要「最大的 $< v$」就 `lower_bound(v)` 後退一格，**退之前檢查 `!= begin()`**。\n- 初始插入 $s_0 = 0$，且**先查後插**。\n- 視窗型要用 `multiset`，刪除寫 `erase(find(v))`。\n- 務必用成員函式 `s.lower_bound(v)`，不是 `std::lower_bound`（後者 $O(n)$）。\n- 363 = 固定上下界降維 + 形態一；把小的維度放外層，必要時轉置。\n- 元素全非負 ⇒ 改用滑動視窗；要問「範圍內有幾個」⇒ 改用樹狀陣列。\n",
        },
        {
          id: 20,
          title: "2.8 二維字首和 > 二維字首最小值",
          summary:
            "## 這個技術解決什麼問題\n\n2.6 的四點容斥處理「矩形區域和」。但把 $+$ 換成 $\\min$ 之後，容斥立刻失效——**$\\min$ 沒有逆元，前綴相減沒有意義。**\n\n然而有一類題目只需要**角落錨定**的區域極值：「所有在我左上方（含同列同行）的格子中，最小的是多少？」這種形狀不需要容斥，一個 $O(mn)$ 的遞推就能全部算出來。代表題是 3148（矩陣中的最大得分）：求\n$$\\max\\{\\, a[r_2][c_2] - a[r_1][c_1] \\;:\\; r_1 \\le r_2,\\ c_1 \\le c_2,\\ (r_1,c_1)\\ne(r_2,c_2) \\,\\}.$$\n\n固定右下角 $(r_2,c_2)$，就是「用當前值減去左上區域的最小值」——**這正是 1.1「列舉右、維護左」推廣到二維**：外層枚舉的不再是一個右端點，而是一個右下角；左側摘要不再是一條前綴，而是一整片左上區域。\n\n本節要說清楚的是：**為什麼 $\\min$ 不能容斥卻可以遞推，以及這條分界線的真正依據是什麼。**\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「二維字首最小值」想：\n\n- **在矩陣 DP 中，每一格的轉移需要『所有左上方格子的最小 / 最大值』。** 例如要算「跳躍差值最大」「到此為止的最低成本」。\n- **單純的求和前綴不夠用，你要的是極值而非總和。**\n\n:::example 嗅到味道的瞬間：轉移要回看「左上整塊的最小值」→ 預建前綴最小值表\n```cpp\n// 嗅到味道的瞬間：轉移要回看「左上整塊的最小值」→ 預建前綴最小值表\nlong long prevMin = min(best[i - 1][j], best[i][j - 1]);\n```\n:::\n\n\n\n- **暴力是對每格再往左上掃一整塊找極值（`O((m·n)^2)`）；** 前綴最小值表把每次回看攤成 `O(1)`，總體 `O(m·n)`。\n- 像 3148（矩陣中的最大得分）要對每格取「嚴格左上最小值」再算差，正是這張表的典型用法。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的聚合是 `min` 還是 `max`？邊界墊的初值對應正確嗎？\n2. 我需要「含自己」還是「嚴格左上」的範圍？\n3. 因為 `min`/`max` 冪等，我有沒有誤加了求和才需要的容斥（多扣左上角）？\n\n## 核心想法與直覺\n\n本節是二維前綴的一個變形：把「累加求和」換成「取最小值（或最大值）」，做出一張「二維前綴最小值」表，讓我們在 DP 或查詢時能 `O(1)` 取得「某個左上角區域裡的最佳值」。\n\n:::example 二維前綴最小值：best[i][j] = 左上角到 (i, j) 區域的最小值\n先回想普通的二維前綴和是怎麼遞推的：每格由「上、左、左上、自己」組合而成。前綴最小值的精神一樣，只是把加法換成 `min`。令 `best[i][j]` 表示「左上角到 (i,j) 這塊矩形裡的最小值」：\n\n```cpp\n// 二維前綴最小值：best[i][j] = 左上角到 (i, j) 區域的最小值\nbest[i][j] = min({grid[i][j],\n                  i ? best[i - 1][j] : INF,    // 上方區域的最小\n                  j ? best[i][j - 1] : INF});  // 左方區域的最小\n```\n:::\n\n\n\n注意這裡和「求和」不同：求和要做容斥（減左上角），但 `min` 是冪等的（同一個值算兩次不影響結果），所以「上方最小」與「左方最小」直接再取 `min` 就好，不需要扣回左上角，反而更簡單。\n\n:::example 用前綴最小值做 DP 轉移：當前最佳 = 當前值 - 左上方最小值\n這張表常用在 DP 裡。像「矩陣中的最大得分」要計算「從某個更左上的格子跳到 (i,j) 的最大差值」，就需要快速知道「(i,j) 左上方所有格子的最小值」——把它預先壓進前綴最小值表，每次 DP 轉移就只是一次查表：\n\n```cpp\n// 用前綴最小值做 DP 轉移：當前最佳 = 當前值 - 左上方最小值\nint prevMin = bestExclusive(i, j);     // (i,j) 嚴格左上的最小值\nans = max(ans, grid[i][j] - prevMin);  // 差值越大越好\n```\n:::\n\n\n\n核心精神：**把「某個方向上的最佳值」預先累積成一張二維表，之後任何一格要回看它的左上方最佳值，都從 `O(面積)` 降成 `O(1)`。**\n\n## 狀態／資料結構定義\n\n**定義（左上前綴最小值）。**\n$$M[i][j] \\;=\\; \\min\\{\\, a[r][c] \\;:\\; 0 \\le r \\le i,\\ 0 \\le c \\le j \\,\\}.$$\n\n注意這裡是**閉區間、含自己**，與 2.6 的 $S[i][j]$（開區間、不含第 $i$ 列第 $j$ 行）語意不同。**兩節的下標約定刻意不同，因為 2.6 需要哨兵來避免特判，而本節的遞推自然帶有邊界，硬套哨兵反而要處理「空區域的 $\\min$ 是 $+\\infty$」。**\n\n**遞推：**\n$$M[i][j] \\;=\\; \\min\\big(a[i][j],\\; M[i-1][j],\\; M[i][j-1]\\big),$$\n越界項視為 $+\\infty$。\n\n**排除自己的版本（3148 需要）：**\n$$M^{-}[i][j] \\;=\\; \\min\\big(M[i-1][j],\\; M[i][j-1]\\big),$$\n即「左上區域但不含 $(i,j)$ 自己」。答案候選是 $a[i][j] - M^{-}[i][j]$，然後 $M[i][j] = \\min(a[i][j], M^{-}[i][j])$。**先算候選、再併入自己**——與 2.1 模板中 53 的「先算答案再更新 `minPrefix`」是同一條紀律，理由也一樣：避免自己配自己。\n\n**實作上只需要一列滾動陣列**：處理第 $i$ 列時，`row[j]` 在被更新前保存的正是 $M[i-1][j]$，更新後是 $M[i][j]$；同一列往右掃時左邊的 `row[j-1]` 已經是 $M[i][j-1]$。$O(n)$ 空間。\n\n**四個方向的變體。** 有些題目要的是右上、左下、右下的角落極值。做法是把同一段遞推換個掃描方向跑四次（$i$ 遞增／遞減 × $j$ 遞增／遞減），存成四個陣列。**不要試圖用一個陣列硬湊——方向搞混是這類題最常見的錯誤。**\n\n## 不變量或正確性證明\n\n**引理 1（遞推正確）。** $M[i][j] = \\min(a[i][j],\\, M[i-1][j],\\, M[i][j-1])$。\n\n*證明。* 記 $R(i,j) = \\{(r,c): r\\le i,\\ c\\le j\\}$。則\n$$R(i,j) \\;=\\; \\{(i,j)\\} \\;\\cup\\; R(i-1,j) \\;\\cup\\; R(i,j-1).$$\n「$\\supseteq$」顯然。「$\\subseteq$」：取 $(r,c) \\in R(i,j)$，若 $r < i$ 則 $(r,c) \\in R(i-1,j)$；若 $r = i$ 且 $c < j$ 則 $(r,c) \\in R(i,j-1)$；若 $r=i, c=j$ 則是 $(i,j)$ 本身。三種情形窮盡。\n\n由於 $\\min$ 對集合的**聯集**滿足 $\\min(A \\cup B) = \\min(\\min A, \\min B)$（無論 $A, B$ 是否相交），對三塊分別取 $\\min$ 再取 $\\min$ 即得。$\\square$\n\n**這裡的關鍵前提是冪等性（idempotence）：$\\min(x,x) = x$。**\n\n$R(i-1,j)$ 與 $R(i,j-1)$ **是相交的**（交集是 $R(i-1,j-1)$，通常非空）。對加法而言，重疊區域會被算兩次，必須用容斥扣回去，所以 2.6 才有那個 $+S[r_1][c_1]$ 的修正項。對 $\\min$ 而言，重複計入同一個元素毫無影響——**冪等性讓「覆蓋」取代了「分割」**：只要三塊的聯集等於目標區域，不必互斥。\n\n反過來說，**加法要求分割（disjoint decomposition），$\\min$ 只要求覆蓋（cover）。** 這條分界線解釋了本章與第 9 章的許多對照：\n\n| 運算 | 逆元 | 冪等 | 區間分解方式 | 對應工具 |\n| --- | --- | --- | --- | --- |\n| $+$、XOR | 有 | 否 | 必須互斥；可前綴相減 | 前綴和（2.1／2.6） |\n| $\\min$、$\\max$、$\\gcd$、AND、OR | 無 | 是 | 允許重疊覆蓋 | ST 表（9.7）、角落遞推（本節） |\n| 矩陣乘法等 | 視情況 | 否 | 必須互斥且**有序** | 線段樹（9.3） |\n\nST 表用兩段長度 $2^k$ 且**互相重疊**的區間覆蓋任意查詢區間，靠的正是同一個冪等性；線段樹則因為要支援非冪等運算，只能用互斥的 $O(\\log n)$ 段拼接。**知道這一條，9.7 就不用重新學一次。**\n\n**引理 2（3148 的路徑分數等於端點差）。** 從 $(r_1,c_1)$ 只能往右或往下走到 $(r_2,c_2)$，每步得分是「新格子減舊格子」。設路徑為 $p_0 \\to p_1 \\to \\dots \\to p_t$，總分為\n$$\\sum_{u=1}^{t} \\big(a[p_u] - a[p_{u-1}]\\big) \\;=\\; a[p_t] - a[p_0],$$\n**望遠鏡求和（telescoping）**，中間項全部相消。\n\n*推論*：總分**與路徑無關**，只取決於起終點。於是原問題退化成「找一對 $(r_1,c_1) \\preceq (r_2,c_2)$ 使 $a[r_2][c_2] - a[r_1][c_1]$ 最大」，而「$\\preceq$」正是「在左上區域內」。**這一步是本題的真正難點；把它看穿之後，剩下的就只是本節的遞推。** 若把得分改成「每步的絕對值」或「每步乘上某個係數」，望遠鏡不成立，就必須真的做路徑 DP。\n\n**引理 3（枚舉右下角不重不漏）。** 每一組合法的 $((r_1,c_1),(r_2,c_2))$ 恰有一個右下角 $(r_2,c_2)$，故在 $(r_2,c_2)$ 這一輪會被考慮到；而 $M^{-}[r_2][c_2]$ 是該輪所有合法左上角中 $a$ 最小者，故 $a[r_2][c_2] - M^{-}[r_2][c_2]$ 是該輪的最大值。對所有右下角取 $\\max$ 即為全局最大。\n\n*「不含自己」的必要性*：題目要求 $(r_1,c_1) \\ne (r_2,c_2)$。若用 $M$ 而非 $M^{-}$，當 $a[i][j]$ 本身就是區域最小值時會算出 $0$，在**全部答案皆為負**的矩陣（嚴格遞減的網格）上就會錯誤地回傳 0。**這正是必須用 $M^{-}$、且必須「先算候選再併入自己」的原因。**\n\n**本節不能做什麼。** $M$ 只能回答**角落錨定**的區域極值。任意矩形 $[r_1,r_2]\\times[c_1,c_2]$ 的最小值**無法**由 $M$ 導出（沒有逆元，無從扣除）。要做任意矩形 RMQ 得用二維 ST 表（$O(mn\\log m\\log n)$ 預處理、$O(1)$ 查詢）或二維線段樹。**看到「任意矩形的 min」就別想套本節。**\n\n## 逐步演算法\n\n:::example 狀態表示：best[i][j] 為左上區域最小值；邊界用極大值墊底\n**1. 狀態表示：定義 best[i][j] 的範圍與聚合函式。** 想清楚是「閉區間到 (i,j)」還是「嚴格小於 (i,j)」，以及聚合是 `min` 還是 `max`。\n\n```cpp\n// 狀態表示：best[i][j] 為左上區域最小值；邊界用極大值墊底\nvector<vector<long long>> best(m, vector<long long>(n, LLONG_MAX));\n```\n:::\n\n\n\n:::example 更新：min 遞推（冪等，不必扣左上角）\n**2. 更新流程：左到右、上到下，用 min 遞推。** 因為每格只依賴上方與左方，順序掃過去即可，無需容斥。\n\n```cpp\n// 更新：min 遞推（冪等，不必扣左上角）\nfor (int i = 0; i < m; ++i) {\n    for (int j = 0; j < n; ++j) {\n        best[i][j] = grid[i][j];\n        if (i) {\n            best[i][j] = min(best[i][j], best[i - 1][j]);\n        }\n        if (j) {\n            best[i][j] = min(best[i][j], best[i][j - 1]);\n        }\n    }\n}\n```\n:::\n\n\n\n:::example 讀取：(i,j) 嚴格左上方的最小值\n**3. 答案讀取：要「嚴格左上」時取上一格的前綴最小。** DP 通常需要「不含自己」的左上最小值，組合上方與左方的前綴最小即可。\n\n```cpp\n// 讀取：(i,j) 嚴格左上方的最小值\nlong long up = i ? best[i - 1][j] : LLONG_MAX;\nlong long left = j ? best[i][j - 1] : LLONG_MAX;\nlong long prevMin = min(up, left);  // 不含 grid[i][j] 本身\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 邊界（第 0 行 / 第 0 列）要先墊 `LLONG_MAX`（求 min）或 `LLONG_MIN`（求 max），否則第一格會誤把無效值算進來。\n- 想清楚要「含自己」還是「嚴格左上」，差一格會讓 DP 全錯。\n- 建表 `O(m·n)`、每次查詢 `O(1)`、空間 `O(m·n)`。\n\n## C++17 模板\n\n```cpp\n// 矩陣中的最大得分（LeetCode 3148）：左上前綴最小值，滾動一列。\nclass Solution {\n public:\n  int maxScore(std::vector<std::vector<int>>& grid) {\n    int m = grid.size(), n = grid[0].size();\n    const int kInf = INT_MAX;\n\n    std::vector<int> row(n, kInf);      // 掃到第 i 列時，row[j] 尚未更新前 = M[i-1][j]\n    int ans = INT_MIN;\n\n    for (int i = 0; i < m; ++i) {\n      int left = kInf;                  // = M[i][j-1]，j = 0 時為空\n      for (int j = 0; j < n; ++j) {\n        int up = row[j];                // = M[i-1][j]\n        int pre = std::min(up, left);   // = M^-[i][j]：左上區域，不含自己\n\n        if (pre != kInf) {              // 有合法左上角才更新答案（避免 INT_MAX 溢位）\n          ans = std::max(ans, grid[i][j] - pre);\n        }\n        int cur = std::min(grid[i][j], pre);   // = M[i][j]：先算候選，再併入自己\n\n        row[j] = cur;                   // 供下一列使用\n        left = cur;                     // 供同列右邊使用\n      }\n    }\n    return ans;\n  }\n};\n```\n\n`pre != kInf` 的檢查不是防禦性冗餘：$(0,0)$ 沒有任何合法左上角，若直接算 `grid[0][0] - INT_MAX` 會**有號整數溢位（未定義行為）**，實際跑出來是個很大的正數，答案就錯了。**要嘛加這個檢查，要嘛把運算搬到 `long long`——不能兩者都不做。**\n\n```cpp\n// 四方向角落極值的通用寫法：同一段遞推換掃描方向跑四次。\n// dirR/dirC 分別表示列、行的掃描是遞增(true)還是遞減(false)。\nstatic std::vector<std::vector<int>> cornerMin(const std::vector<std::vector<int>>& a,\n                                               bool dirR, bool dirC) {\n  int m = a.size(), n = a[0].size();\n  std::vector<std::vector<int>> M(m, std::vector<int>(n, INT_MAX));\n  for (int t = 0; t < m; ++t) {\n    int i = dirR ? t : m - 1 - t;\n    int pi = dirR ? i - 1 : i + 1;                    // 前一列\n    for (int u = 0; u < n; ++u) {\n      int j = dirC ? u : n - 1 - u;\n      int pj = dirC ? j - 1 : j + 1;                  // 前一行\n      int best = a[i][j];\n      if (pi >= 0 && pi < m) { best = std::min(best, M[pi][j]); }\n      if (pj >= 0 && pj < n) { best = std::min(best, M[i][pj]); }\n      M[i][j] = best;\n    }\n  }\n  return M;\n}\n```\n\n## 時間與空間複雜度\n\n**建表** $O(mn)$ 時間（每格常數次比較）。**空間**：完整保存是 $O(mn)$；若只需邊掃邊用，滾動一列即 $O(n)$，如上面第一個模板。\n\n**查詢**角落錨定的區域極值 $O(1)$。\n\n**與暴力的落差**：3148 暴力枚舉兩個角落是 $O(m^2n^2)$，$m=n=1000$ 時 $10^{12}$；本節 $O(mn) = 10^6$。\n\n**四方向版本**：四次遞推、四個陣列，$O(mn)$ 時間、$O(mn)$ 空間，常數 ×4。\n\n**任意矩形 RMQ 的代價（對照）**：二維 ST 表 $O(mn\\log m\\log n)$ 時間與空間、$O(1)$ 查詢。$m=n=1000$ 時空間是 $10^6 \\times 10 \\times 10 = 10^8$ 個 int，**通常爆記憶體**——這也是為什麼角落錨定的特例值得單獨學：它便宜得多。\n\n## 常見錯誤與邊界條件\n\n- 用 2.6 的四點容斥算 $\\min$（**根本不成立，$\\min$ 沒有逆元**）。\n- 用 $M$ 而非 $M^{-}$，導致「起終點相同」的非法解混入（全負矩陣回傳 0）。\n- 先併入自己再算候選（同一個錯誤的另一種寫法）。\n- `grid[i][j] - INT_MAX` 有號溢位（要嘛檢查哨兵、要嘛用 `long long`）。\n- `ans` 初值設 0 而非 `INT_MIN`（同樣是全負矩陣出錯）。\n- 滾動陣列時 `row[j]` 已被本列更新卻仍當成 $M[i-1][j]$ 用（**必須先把 `row[j]` 讀進 `up` 再覆寫**）。\n- `left` 忘記在每列開頭重設為 $+\\infty$。\n- 四方向版本把掃描方向與遞推方向搞反（往右上取極值卻由左往右掃）。\n- 把本節套到「任意矩形的 min」（要 ST 表）。\n- 誤以為 3148 的路徑得分與路徑有關而去做 DP（望遠鏡相消，只看端點）。\n\n## 與相似技巧的比較\n\n- **與 2.6 二維前綴和**：形狀看似都是「左上區域」，但 2.6 能算**任意矩形**（靠容斥），本節只能算**角落錨定區域**。差別的根源是逆元。**選型時先問「這個運算有沒有逆元」。**\n- **與 9.7 ST 表**：同樣靠冪等性，但 ST 表用重疊的 $2^k$ 段覆蓋**任意**區間，代價是 $O(n\\log n)$ 空間。**本節是 ST 表在「區間必以角落為錨」時的退化版，便宜一個 $\\log$。**\n- **與 9.3 線段樹**：線段樹支援修改，本節不支援；線段樹不要求冪等，本節要求。\n- **與 1.1 列舉右維護左**：本節就是它的二維版——枚舉右下角，維護左上區域的摘要。**「先查詢再併入自己」這條紀律一字不改地沿用。**\n- **與 DP**：$M[i][j]$ 的遞推本身就是一個最簡單的網格 DP。反過來，很多網格 DP（1277 全 1 正方形、221 最大正方形）的轉移也是「取三鄰居的 $\\min$ 再加 1」——**形狀一模一樣，只是語意從『區域極值』變成『能延伸多長』。** 認出這個共同骨架，兩邊都好寫。\n- **與單調堆疊（4.7）**：若要的是「往左第一個比我小的元素」而非「左邊所有元素的最小值」，那是單調堆疊的領域，$O(n)$ 且資訊更細。**「所有元素的極值」用前綴極值；「第一個滿足條件的元素」用單調堆疊。**\n\n## 代表例題\n\n**題號**：3148\n\n要把區間量改寫成前綴狀態之差；若是計數題，重點是查找過去需要的前綴值。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 3148 | [矩陣中的最大得分](https://leetcode.cn/problems/maximum-difference-score-in-a-grid) | 1820 |\n\n## 本節重點速查\n\n- $M[i][j] = \\min(a[i][j],\\, M[i-1][j],\\, M[i][j-1])$，越界視為 $+\\infty$；滾動一列即 $O(n)$ 空間。\n- $\\min$ **沒有逆元** ⇒ 不能四點容斥；$\\min$ **冪等** ⇒ 允許重疊覆蓋，遞推才成立。**分割 vs. 覆蓋，就是加法與極值的分界。**\n- 只能算**角落錨定**區域；任意矩形極值要 ST 表（9.7）。\n- 3148：路徑得分望遠鏡相消 ⇒ 只看端點差 ⇒ 枚舉右下角 + 左上最小值。\n- 必須用**不含自己**的 $M^{-}$，且**先算候選再併入自己**（否則全負矩陣回傳 0）。\n- `INT_MAX` 當哨兵時記得檢查再相減，否則有號溢位。\n- 滾動陣列要**先讀 `row[j]` 再覆寫**；`left` 每列開頭重設。\n- 四方向變體就是同一段遞推換四種掃描方向，別硬湊成一個陣列。\n",
        },
      ],
    },
    {
      id: 21,
      title: "3. 差分",
      summary:
        "差分與字首和的關係，類似**導數**與**積分**的關係：陣列 a 的差分的字首和就是陣列 a（不變）。字首和處理「陣列不動、反覆查區間」；差分剛好相反，處理**反覆改區間、最後才看一次結果**。\n\n核心是把「對 `[l, r]` 每一格加 v」這個 `O(n)` 動作，改寫成只動兩格：`d[l] += v`、`d[r + 1] -= v`。所有修改做完後，對 `d` 求一次字首和就還原成真正的陣列。m 次修改因此從 `O(nm)` 降到 `O(n + m)`。\n\n**如何挑子單元**\n\n- **3.1 一維差分**：訊號是「給一堆區間操作，問最後每格的值」或「問最大重疊數」——航班預訂、會議室排程、車輛載客都是這一型。`d` 要開 `n + 1` 格，否則 `r + 1` 會越界。\n- **3.2 二維差分**：修改的是矩形區域。四個角各動一次（`+v`、`-v`、`-v`、`+v`），最後對兩個維度各求一次字首和。\n\n**判斷用字首和還是差分**：查詢多、修改少 → 字首和；修改多、查詢集中在最後 → 差分。兩者都要修改又都要查詢，就該往第 9 章的樹狀陣列／線段樹走。\n\n**延伸閱讀**：[LeetCode 1094 差分講解](https://leetcode.cn/problems/car-pooling/solution/suan-fa-xiao-ke-tang-chai-fen-shu-zu-fu-9d4ra/)，可把上車、下車視為「開始生效／停止生效」的兩個事件。\n",
      children: [
        {
          id: 22,
          title: "3.1 一維差分",
          summary:
            "思維擴充套件：\n\n*   [56\\. 合併區間](https://leetcode.cn/problems/merge-intervals/) 做法見 [我的評論](https://leetcode.cn/problems/merge-intervals/solutions/2798138/jian-dan-zuo-fa-yi-ji-wei-shi-yao-yao-zh-f2b3/comments/2323402/?parent=2316868)\n*   [57\\. 插入區間](https://leetcode.cn/problems/insert-interval/)\n*   [732\\. 我的日程安排表 III](https://leetcode.cn/problems/my-calendar-iii/)\n*   [2406\\. 將區間分為最少組數](https://leetcode.cn/problems/divide-intervals-into-minimum-number-of-groups/) 1713\n*   [253\\. 會議室 II](https://leetcode.cn/problems/meeting-rooms-ii/)（會員題）\n*   [759\\. 員工空閒時間](https://leetcode.cn/problems/employee-free-time/)（會員題）",
          children: [
            {
              id: 23,
              title: "3.1.1 基礎",
              summary:
                "## 這個技術解決什麼問題\n\n前綴和（2.1）處理「多次查詢區間和、不修改」。差分處理**完全對偶**的情形：**多次區間整體加值，最後只查一次結果。**\n\n暴力是每筆操作都把區間逐格加一遍：$m$ 筆操作、區間長 $L$，總計 $O(mL)$。1109（航班預訂統計）有 $m = 2\\times10^4$ 筆、$n = 2\\times10^4$ 個航班，最壞 $4\\times10^8$，超時。\n\n差分把每筆區間操作攤成**兩次單點修改** $O(1)$，最後做一次前綴和 $O(n)$ 還原，總計 $O(n + m)$。\n\n這個技巧的內容也是一句話：**「對一段整體加 $v$」等價於「在左端點記 $+v$、在右端點後一格記 $-v$」。** 微積分的說法是：階梯函數的導數是兩個脈衝。很多題目表面上沒有「區間加值」，但只要能改寫成「某個量在 $l$ 開始生效、在 $r$ 之後失效」，就是差分——1854（人口最多的年份）的出生 $+1$、死亡 $-1$ 就是最典型的例子。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「一維差分」想：\n\n- **有很多次「把一段區間整體加上某個值」的操作，且只在最後才需要結果。** 例如航班預訂（1109）、拼車（1094）、生存人數統計。\n- **是「先批次修改、後一次查詢」，不是邊改邊查。** 若每次修改後馬上要查中間值，差分就不適用（那要樹狀陣列 / 線段樹）。\n\n:::example 嗅到味道的瞬間：要對 [l, r] 整段加值，但你不想逐格跑迴圈\n```cpp\n// 嗅到味道的瞬間：要對 [l, r] 整段加值，但你不想逐格跑迴圈\ndiff[l] += v;\ndiff[r + 1] -= v;  // 只改端點，最後再前綴和還原\n```\n:::\n\n\n\n- **暴力是每筆操作都 for 迴圈把區間每格加一次（`O(m × 區間長)`）；** 差分把每筆攤成 `O(1)`，最後一次還原。\n- 不少題目會先把事件轉成「在某點 +1、某點 −1」的形式（如人口最多的年份 1854：出生 +1、死亡 −1），本質就是差分。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的操作是不是「對一整段加同一個值」？\n2. 是不是「先全部改完、最後才查」，而非邊改邊查？\n3. 我有沒有記得寫 `diff[r + 1] -= v`，並把陣列開到 `n + 1` 避免越界？\n\n## 核心想法與直覺\n\n本節要解決的場景是：對一個陣列做「很多次區間加值」——把 `[l, r]` 這一整段都 `+v`——最後才問每個位置的最終值。如果每次都老老實實把區間裡每一格都加一次，一次操作就要 `O(區間長度)`，操作多了會非常慢。差分（difference array）把單次區間加值壓成「只改兩個端點」。\n\n:::example 差分：把區間 [l, r] 全部 +v，只改兩個端點，O(1)\n差分是前綴和的「反向操作」。我們不直接記值，而是記「相鄰兩格的差」。關鍵觀察是：要讓一整段 `[l, r]` 都 `+v`，只需在差分陣列上做兩件事——讓 `l` 這格開始 `+v`，並在 `r + 1` 這格把它「關掉」`-v`：\n\n```cpp\n// 差分：把區間 [l, r] 全部 +v，只改兩個端點，O(1)\ndiff[l] += v;      // 從 l 開始，後面每格都會 +v\ndiff[r + 1] -= v;  // 到 r+1 取消，超出區間不再受影響\n```\n:::\n\n\n\n:::example 還原：對差分做一次前綴和，得到每個位置的最終值\n為什麼「只改兩格」就等於「整段都加」？因為最後我們會對差分陣列做一次前綴和還原：`a[i] = a[i - 1] + diff[i]`。`+v` 從 `l` 一路被累積到 `r`，到 `r + 1` 又被 `-v` 抵消，於是恰好只有 `[l, r]` 這段被加到：\n\n```cpp\n// 還原：對差分做一次前綴和，得到每個位置的最終值\nfor (int i = 1; i < n; ++i) {\n    diff[i] += diff[i - 1];  // 累積回真正的值\n}\n// 此刻 diff[i] 就是位置 i 的最終結果\n```\n:::\n\n\n\n核心精神：**把 m 次區間修改，全部變成「只動端點」的 `O(1)` 標記，最後用一次 `O(n)` 前綴和統一還原。** 總成本從 `O(m × 區間長)` 降為 `O(m + n)`。\n\n## 狀態／資料結構定義\n\n**定義（差分陣列）。** 對長度 $n$ 的陣列 $a$，令 $a_{-1} = 0$，\n$$d_i = a_i - a_{i-1}, \\qquad 0 \\le i < n .$$\n\n**還原（前綴和）：** $a_i = \\sum_{t \\le i} d_t$。差分與前綴和互為逆運算。\n\n**區間加值 $[l, r] \\mathrel{+}= v$：**\n$$d_l \\mathrel{+}= v, \\qquad d_{r+1} \\mathrel{-}= v .$$\n\n**陣列必須開 $n+1$ 格**（下標 $0 \\dots n$），因為 $r$ 可以是 $n-1$，此時 $d_{r+1} = d_n$ 要有地方放。$d_n$ 只是個垃圾桶，還原時不讀它——**但少開這一格就得寫 `if (r + 1 < n)` 特判，那是 bug 溫床，和 2.1 的哨兵一樣的道理，只是方向相反。**\n\n**兩種常見的座標約定，務必先確認題目是哪一種：**\n\n| 語意 | 差分寫法 |\n| --- | --- |\n| 閉區間 $[l, r]$（1094 上車 $l$、下車 $r$ 仍在車上） | `d[l] += v; d[r+1] -= v;` |\n| 半開區間 $[l, r)$（1094 實際語意：`to` 站已下車；1854 死亡年份不計） | `d[l] += v; d[r] -= v;` |\n\n**這兩行只差一格，錯了會在端點處全盤皆錯。** 判準是問自己：「$r$ 這一格算不算在內？」\n\n**值域很大或非整數時**（座標到 $10^9$、時間是浮點），不能直接開陣列，要改用 `map<key, delta>` 或「事件排序掃描」——那是 3.1.2 的內容。\n\n## 不變量或正確性證明\n\n**主不變量。** 在任意一串區間加值操作之後，恆有\n$$a_i \\;=\\; \\sum_{t=0}^{i} d_t \\qquad \\text{對所有 } 0 \\le i < n .$$\n\n*證明（對操作次數歸納）。*\n\n**基底**：初始 $a \\equiv 0$、$d \\equiv 0$，等式成立。\n\n**歸納步**：設不變量在前 $k$ 筆操作後成立。第 $k+1$ 筆是 $[l,r] \\mathrel{+}= v$，它把真實陣列改成 $a_i' = a_i + v\\cdot[\\,l \\le i \\le r\\,]$，把差分陣列改成 $d_l' = d_l + v$、$d_{r+1}' = d_{r+1} - v$，其餘不變。於是\n$$\\sum_{t \\le i} d_t' \\;=\\; \\sum_{t \\le i} d_t \\;+\\; v\\cdot[\\,l \\le i\\,] \\;-\\; v\\cdot[\\,r+1 \\le i\\,] \\;=\\; a_i + v\\cdot[\\,l \\le i \\le r\\,] \\;=\\; a_i' .$$\n中間那步用到 $[\\,l\\le i\\,] - [\\,r+1 \\le i\\,] = [\\,l \\le i \\le r\\,]$（當 $l \\le r$）。$\\square$\n\n**這條論證點名了四個前提，每一個對應一種會踩雷的題型。**\n\n1. **操作必須可疊加（線性）。** 歸納步只用到「$a_i$ 增加 $v$」這件事的**可加性**。若操作改成「區間**賦值**為 $v$」（覆蓋而非累加），$a_i' = v$ 與舊值無關，上式徹底不成立——**區間賦值必須用帶懶標記的線段樹（9.4），差分無能為力。** 這是最重要的一條分界。\n2. **值的運算必須有逆元。** 需要「$-v$」來抵銷。區間**乘**法可以用「差分於對數空間」或「商差分」($d_i = a_i / a_{i-1}$) 處理，但一旦出現 0 就崩潰。\n3. **操作與查詢必須分階段。** 不變量本身在任何時刻都成立，所以「隨時想知道 $a_i$」在理論上沒問題——**問題是每次還原要 $O(n)$。** 若操作與查詢交錯 $q$ 次，總成本 $O(nq)$，還不如樹狀陣列的 $O((n+q)\\log n)$。**判準不是「能不能」，而是「划不划算」。**\n4. **$l \\le r$。** 空區間（$l > r$）會讓那個恆等式反號，變成區間 $[r+1, l-1]$ 減 $v$。多數題目保證 $l \\le r$，但自己構造區間時（例如 798 那種「把每個位置的可得分輪調量轉成區間」）很容易生出空區間或跨越邊界的環狀區間，**必須自己擋掉或拆成兩段。**\n\n**推論（差分讀出的是「覆蓋次數」）。** 若每筆操作都是 $+1$，則還原後的 $a_i$ 就是「有幾個區間覆蓋位置 $i$」。$\\max_i a_i$ 便是最大重疊數——1094（拼車）、1854（人口最多的年份）、2848（統計汽車上的所有整數點）全是這一句話的直接應用。\n\n**3355（零陣列變換 I）的可行性判準。** 給定 nums 與若干查詢區間，每個查詢可對區間內**任意子集**各減 1，問能否讓 nums 全變 0。\n\n*宣稱*：可行 $\\iff$ 對每個 $i$，$\\mathrm{cover}_i \\ge \\mathrm{nums}_i$，其中 $\\mathrm{cover}_i$ 是覆蓋 $i$ 的查詢數。\n\n*必要性*：位置 $i$ 每個查詢最多讓它減 1，故總減量 $\\le \\mathrm{cover}_i$。\n*充分性*：各位置的減量**互相獨立**（每個查詢可自由選子集，位置之間沒有耦合），所以只要每個位置各自的預算夠，就各自減足即可。$\\square$\n\n**充分性依賴「可任選子集」這個前提。** 若題目改成「必須整段一起減」（3356 零陣列變換 II 的部分變體、或要求每個查詢減固定量），位置之間就耦合了，這條判準立刻失效，得改用貪心 + 差分或二分答案。**同一個差分陣列，判準卻不同——差別在題目給的自由度。**\n\n## 逐步演算法\n\n:::example 狀態表示：diff 比原陣列多一格，存放區間加值的端點標記\n**1. 狀態表示：開一個差分陣列。** 為了安全地寫 `r + 1`，長度通常開 `n + 1`。\n\n```cpp\n// 狀態表示：diff 比原陣列多一格，存放區間加值的端點標記\nvector<int> diff(n + 1, 0);\n```\n:::\n\n\n\n:::example 更新：對每筆操作 (l, r, v) 打端點標記\n**2. 更新流程：每個操作只改兩個端點。** 完全不碰中間的格子。\n\n```cpp\n// 更新：對每筆操作 (l, r, v) 打端點標記\nfor (auto& op : ops) {\n    int l = op[0], r = op[1], v = op[2];\n    diff[l] += v;\n    diff[r + 1] -= v;  // r+1 可能等於 n，所以 diff 要開 n+1\n}\n```\n:::\n\n\n\n:::example 讀取：前綴和還原成每個位置的最終值\n**3. 答案讀取：所有操作做完後，一次前綴和還原。**\n\n```cpp\n// 讀取：前綴和還原成每個位置的最終值\nvector<int> a(n);\na[0] = diff[0];\nfor (int i = 1; i < n; ++i) {\n    a[i] = a[i - 1] + diff[i];\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- `diff[r + 1]` 可能落在最後一格之外，所以差分陣列要開 `n + 1`（或先判斷 `r + 1 < n` 再寫），否則會越界。\n- 若題目問的是「過程中是否超過容量」（如拼車 1094），可在還原時邊累加邊檢查，發現超標立刻回傳。\n- 打標記每筆 `O(1)`、還原 `O(n)`，總共 `O(m + n)`，空間 `O(n)`。\n\n## C++17 模板\n\n以下用本節代表題 1109. 航班預訂統計 展示核心寫法。\n\n**完整題目**：給定多筆航班區間座位預訂，回傳每個航班最後被預訂的座位數。\n\n:::example 差分陣列：對區間 [first, last] 批量加減，最後前綴和還原\n**線索**：題目反覆詢問連續區間量；若區間值能由兩個前綴狀態相減，就應優先想到前綴和。\n\n```cpp\nclass Solution {\npublic:\n    // 差分陣列：對區間 [first, last] 批量加減，最後前綴和還原\n    vector<int> corpFlightBookings(vector<vector<int>>& bookings, int n) {\n            vector<int> diff(n + 1);  // 差分陣列，多一格方便邊界處理\n            for (auto& b : bookings) {\n                    diff[b[0] - 1] += b[2];  // 區間左端點加上訂位數（1-indexed 轉 0-indexed）\n                    diff[b[1]] -= b[2];  // 區間右端點後一格減去訂位數\n            }\n            // 前綴和還原每個航班的實際乘客數\n            vector<int> ans(n);\n            int cur = 0;\n            for (int i = 0; i < n; ++i) {\n                    cur += diff[i];\n                    ans[i] = cur;\n            }\n            return ans;\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- 差分把區間加轉成兩個端點更新；最後做一次前綴和即可還原每個位置的實際值。\n- 前綴狀態先保存空前綴，讓從下標 0 開始的區間也能統一處理。\n- 查詢區間時只做兩個前綴狀態相減；計數題則查找歷史中需要的前綴值。\n\n## 時間與空間複雜度\n\n**建構**：$m$ 筆操作各 $O(1)$，共 $O(m)$。**還原**：一次前綴和 $O(n)$。**總計 $O(n + m)$ 時間、$O(n)$ 空間。**\n\n與暴力 $O(mL)$ 的落差：1109 從 $4\\times10^8$ 降到 $4\\times10^4$。\n\n**還原可以原地做**：`for (int i = 1; i < n; ++i) d[i] += d[i-1];` 之後 `d` 就是答案，不需額外陣列，$O(1)$ 額外空間。\n\n**若值域是座標而非下標**（1854 的年份 $1950 \\dots 2050$、2848 的座標 $1 \\dots 100$），空間是 $O(V)$ 而非 $O(n)$。$V$ 大到開不下時（$10^9$）改用 `map`，時間變 $O(m\\log m + m)$、空間 $O(m)$——**代價是一個 $\\log$ 與很差的快取表現，但把空間從值域規模降到操作規模。** 見 3.1.2。\n\n**交錯查詢的成本對照**（決定選型的關鍵表）：\n\n| 情境 | 差分陣列 | 樹狀陣列（9.1） | 懶標記線段樹（9.4） |\n| --- | --- | --- | --- |\n| 區間加、最後一次全查 | $O(n+m)$ ✔ | $O((n+m)\\log n)$ | $O((n+m)\\log n)$ |\n| 區間加、交錯單點查 $q$ 次 | $O(nq)$ ✘ | $O((m+q)\\log n)$ ✔ | $O((m+q)\\log n)$ |\n| 區間加、交錯區間和查詢 | ✘ | 需兩個 BIT | $O((m+q)\\log n)$ ✔ |\n| 區間**賦值** | ✘ 不成立 | ✘ | $O((m+q)\\log n)$ ✔ |\n\n## 常見錯誤與邊界條件\n\n- 前綴陣列常開 n+1，讓空前綴為 0。\n- 區間 `[l, r]` 對應 `pre[r + 1] - pre[l]`。\n- 含負數時不能用滑動視窗替代前綴和。\n- 和可能溢位時用 long long。\n\n## 常見變形\n\n- 一維前綴和、二維前綴和。\n- 前綴和 + hash map 統計子陣列數。\n- 前綴和 + 有序集合處理上下界或最大不超過 k。\n\n## 與相似技巧的比較\n\n- **與 2.1 前綴和**：嚴格互逆。**前綴和 = 先修改完、多次查詢區間和；差分 = 多次修改區間、最後查詢。** 兩者都要求「修改與查詢分階段」，代價與收益完全鏡像。\n- **與 3.2 二維差分**：同一套思路，區間加值變成矩形加值，兩個端點標記變成**四個角落標記**（容斥）。\n- **與 3.1.2 事件掃描**：當座標稀疏或需要「合併區間」「找空檔」而非「逐點計數」時，差分陣列開不下，改成「端點事件 + 排序 + 掃描」。**本質完全相同——事件掃描就是把差分陣列的非零位置抽出來排序。**\n- **與樹狀陣列（9.1）**：樹狀陣列是「差分 + 可修改」。實際上「區間加、單點查」的標準做法就是**對差分陣列建樹狀陣列**——兩次單點修改、一次前綴和查詢。**認出這一點，9.1 的那個技巧就不用背了。**\n- **與懶標記線段樹（9.4）**：懶標記可以看成「掛在樹節點上的差分」，把 $O(n)$ 個標記壓成 $O(\\log n)$ 個。差分陣列是它在「不需中途查詢」時的退化版。\n- **與滑動視窗**：若操作區間長度固定且左端點單調，滑動視窗也能 $O(n)$ 完成，且不需額外陣列。**但區間長度不一或順序任意時，只有差分能做。**\n\n## 代表例題\n\n**題號**：370、1094、1109、面試題 16.10、1854\n\n放在「基礎」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 370 | [區間加法](https://leetcode.cn/problems/range-addition)（Premium） |  |\n| 1094 | [拼車](https://leetcode.cn/problems/car-pooling) | 1441 |\n| 1109 | [航班預訂統計](https://leetcode.cn/problems/corporate-flight-bookings) | 1570 |\n| 面試題 16.10 | [生存人數](https://leetcode.cn/problems/living-people-lcci) |  |\n| 1854 | [人口最多的年份](https://leetcode.cn/problems/maximum-population-year) | 1370 |\n| 1893 | [檢查是否區域內所有整數都被覆蓋](https://leetcode.cn/problems/check-if-all-the-integers-in-a-range-are-covered) | 1307 |\n| 2848 | [與車相交的點](https://leetcode.cn/problems/points-that-intersect-with-cars) | 1230 |\n| 2960 | [統計已測試裝置](https://leetcode.cn/problems/count-tested-devices-after-test-operations) | 1169 |\n| 3355 | [零陣列變換 I](https://leetcode.cn/problems/zero-array-transformation-i) | 1591 |\n\n## 本節重點速查\n\n- $d_i = a_i - a_{i-1}$（$a_{-1}=0$）；還原 $a_i = \\sum_{t\\le i} d_t$。**差分與前綴和互逆。**\n- 區間加值 $[l,r] \\mathrel{+}= v$ ⇒ `d[l] += v; d[r+1] -= v;`。**陣列開 $n+1$ 格**，省掉 `r+1 < n` 的特判。\n- **先確認閉區間還是半開區間**（`d[r+1]` vs `d[r]`），差一格就整題錯。\n- 不變量：任意操作序列後 $a_i = \\sum_{t\\le i} d_t$，用歸納 + $[l\\le i]-[r{+}1\\le i]=[l\\le i\\le r]$ 證。\n- 四個前提：**操作可疊加**（區間賦值不行，要線段樹）、**有逆元**、**修改與查詢分階段**（交錯就換樹狀陣列）、**$l \\le r$**。\n- 全部 $+1$ 時，還原結果就是「覆蓋次數」；$\\max$ 即最大重疊數（1094、1854、2848）。\n- 還原可原地做，$O(1)$ 額外空間；總計 $O(n+m)$。\n- 座標稀疏 ⇒ `map` 或事件掃描（3.1.2）；要中途查詢 ⇒ 樹狀陣列（9.1）。\n",
            },
            {
              id: 24,
              title: "3.1.2 進階",
              summary:
                "## 這個技術解決什麼問題\n\n3.1.1 的差分陣列有兩個硬限制：**座標必須密集**（要開得下陣列），以及**問的必須是「每一格的值」**。本節處理突破這兩點之後的三類題目。\n\n**一、座標稀疏或非整數 ⇒ 事件掃描（掃描線）。** 區間端點在 $[0, 10^9]$ 或是浮點時間，陣列開不下。做法是只保留差分陣列的**非零位置**：把每個區間拆成兩個事件 $(l, +v)$、$(r{+}1, -v)$，排序後掃描並累加。$O(n\\log n)$ 時間、$O(n)$ 空間，與值域無關。**事件掃描不是新演算法，它就是差分陣列的稀疏表示。**\n\n**二、問的是區間結構而非逐格數值 ⇒ 合併區間家族。** 56（合併區間）、57（插入區間）、759（員工空閒時間）要的是合併後的區間列表、空檔位置，不是覆蓋次數。這類題排序後線性掃描即可，不需要真的做差分。\n\n**三、差分的軸不是原陣列的下標，而是「答案的候選值」。** 這是本節最值得學的一招。798（得分最高的最小輪調）對**輪調量 $k$** 做差分；1674（使陣列互補的最少操作次數）對**目標和 $T$** 做差分；2528 對**電廠位置**做差分。共同形態是：*每個輸入元素會讓某一段候選答案變好（或變壞）固定的量*——那就在答案軸上做區間加值，最後掃一遍取最佳。**辨識訊號：「對每個候選答案都要算一次代價，而每個元素對代價的貢獻是分段常數」。**\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「差分 / 掃描線（進階）」想：\n\n- **輸入是一堆區間 / 時段，要問「最多同時重疊幾個」「合併後的區間」「哪裡有空檔」。** 例如合併區間（56）、插入區間（57）、員工空閒時間（759）。\n- **座標範圍很大或非整數，無法直接開陣列逐格統計。** 這時把區間轉成端點事件、排序掃描，是標準解法。\n\n:::example 嗅到味道的瞬間：手上是一堆 [start, end]，要知道任一時刻重疊數\n```cpp\n// 嗅到味道的瞬間：手上是一堆 [start, end]，要知道任一時刻重疊數\nev.push_back({start, +1});\nev.push_back({end, -1});  // 化為事件再掃描\n```\n:::\n\n\n\n- **暴力是對每個時刻 / 每對區間兩兩比較（`O(n^2)` 或 `O(時間範圍)`）；** 事件化 + 排序掃描降到 `O(n log n)`。\n- 像 798（得分最高的最小輪調）會把每個位置「能得分的輪調量」轉成區間加減標記，本質仍是差分。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我能不能把每個區間拆成「起點 +1、終點 −1」兩個事件？\n2. 端點相接到底算不算重疊？我的排序規則處理了嗎？\n3. 座標範圍允許用差分陣列，還是必須排序掃描（或先離散化）？\n\n## 核心想法與直覺\n\n本節是「一維差分」的進階：當資料不是現成的整數陣列，而是一堆「區間 / 事件」（會議、航班、空閒時段）時，常把每個區間拆成「在起點 +1、在終點 −1」的標記，再掃過去累加，藉此知道「任一時刻同時有幾個區間重疊」。這其實就是差分的事件版，常被稱為「掃描線」。\n\n:::example 事件化：每個區間貢獻「進場 +1、離場 -1」\n先回顧差分的本質：要讓 `[l, r]` 整段 `+v`，只需在端點打標記 `diff[l] += v, diff[r + 1] -= v`，最後一次前綴和還原。進階用法是把「值」固定成 1，於是還原後每個位置的數字就代表「覆蓋它的區間數」：\n\n```cpp\n// 事件化：每個區間貢獻「進場 +1、離場 -1」\nevents.push_back({start, +1});\nevents.push_back({end, -1});  // 注意端點開閉，決定 end 或 end+1\n```\n:::\n\n\n\n:::example 掃描線：排序事件後累加，cur 即目前重疊區間數\n當座標範圍很大或不是整數（例如時間點達到 10^9），開不了那麼大的陣列，就改成「把事件排序後掃描」。按座標排序，一路把 `+1 / -1` 累加，`cur` 就是目前重疊數：\n\n```cpp\n// 掃描線：排序事件後累加，cur 即目前重疊區間數\nsort(events.begin(), events.end());\nint cur = 0, peak = 0;\nfor (auto& [x, d] : events) {\n    cur += d;               // 進場或離場\n    peak = max(peak, cur);  // 例如求最大同時重疊\n}\n```\n:::\n\n\n\n核心精神：**把每個區間化成兩個端點事件，用差分 / 掃描線把「區間重疊」變成『一條線掃過去時的加減累計』**，避免逐格、逐時刻暴力統計。座標小就用差分陣列 `O(n)`，座標大或連續就排序掃描 `O(n log n)`。\n\n## 狀態／資料結構定義\n\n**事件掃描。**\n- 事件列表 `vector<pair<Key, Delta>> ev`，`Key` 是座標（`int`/`long long`/`double`），`Delta` 是增量。\n- 區間 $[l, r]$ 加 $v$ ⇒ `ev.push_back({l, v}); ev.push_back({r + 1, -v});`（閉區間、整數座標）。\n- 排序後掃描，維護 `cur`（當前覆蓋值）與 `prev`（上一個事件座標）；區段 $[\\texttt{prev}, \\texttt{key})$ 上的值恆為 `cur`。\n- **同一座標的多個事件必須一次全部處理完再取值**，否則會讀到中間狀態。標準寫法是用 `while (j < ev.size() && ev[j].first == key)` 把同座標的批次吃完。\n\n用 `std::map<Key, Delta>` 也可以（自動排序、自動合併同座標），代價是 $O(\\log n)$ 常數與差得多的快取表現。**離線（所有區間一次給齊）就用 vector + sort；線上（邊插入邊查詢）才用 map。**\n\n**合併區間。** 排序後只需一個「當前累積區間」$[L, R]$，不需任何額外容器。\n\n**答案軸差分。** 差分陣列的下標不是 $0..n-1$，而是候選答案的值域 $0..V$。**必須先確認 $V$ 夠小**（1674 的 $T \\le 2\\cdot\\mathrm{limit} \\le 2\\times10^5$、798 的 $k < n$），否則要改事件掃描。\n\n## 不變量或正確性證明\n\n**引理 1（合併區間：按左端點排序後單向掃描是正確的）。**\n\n設區間已按左端點遞增排序（左端點相同時右端點任意）。掃描時維護當前累積區間 $[L, R]$。宣稱：若第 $i$ 個區間滿足 $l_i > R$，則**其後所有區間都與 $[L,R]$ 不相交**，故 $[L,R]$ 可以安全輸出、不會再被擴張。\n\n*證明。* 對任意 $j > i$，排序保證 $l_j \\ge l_i > R$，故 $[l_j, r_j] \\cap [L, R] = \\varnothing$。$\\square$\n\n**這條引理正是「一次線性掃描就夠、不必回頭」的全部依據，而它依賴的前提是「按左端點排序」。** 換成按右端點排序，$l_j \\ge l_i$ 不再成立，反例：$[0,10], [1,2], [3,4]$ 按右端點排序成 $[1,2],[3,4],[0,10]$，掃到 $[3,4]$ 時會誤判 $[1,2]$ 已完結。**排序鍵不是隨便挑的，它是正確性的前提。**\n\n（順帶一提：**按右端點排序**是另一族問題——無重疊區間的最大選取數、435、452 射氣球——的正確性前提。**看到「區間 + 排序」先問一句：這題的貪心是靠左端點還是右端點？**）\n\n**引理 2（同座標事件的處理順序 = 區間的開閉語意）。**\n\n考慮兩個區間 $[1,5]$ 與 $[5,9]$，問「最大同時重疊數」。\n\n- 若端點是**閉區間**且「在 5 這一刻兩者都算存在」（如 2021 街上最亮的位置：燈的照射範圍是閉區間），答案是 2。此時 $-1$ 事件應放在座標 $r+1 = 6$，與 $+1$ 事件天然不會撞在一起。\n- 若語意是**半開** $[l, r)$（如會議室：09:00–10:00 與 10:00–11:00 不衝突），答案是 1。此時 $-1$ 事件放在座標 $r$，**必須讓同座標的 $-1$ 先於 $+1$ 被處理**，否則 `cur` 會短暫衝到 2 而被誤記錄。\n\n*結論*：把 $-1$ 放在 $r+1$（閉）或 $r$（半開）、以及同座標時 $-1$ 是否先行，這兩個決定**共同編碼了區間的開閉語意**。實作上有兩種等價寫法，二選一，不要混用：\n\n1. 只調整放置座標（$r$ vs $r+1$），排序時同座標**一次批次處理完再取值**；\n2. 排序鍵用 `pair(coord, delta)`——`delta = -1` 自然排在 `+1` 前面——然後逐個事件處理。\n\n**寫法 2 只在半開語意下正確**，因為它硬性規定了「先減後加」。閉區間語意用寫法 1。**這是掃描線最常見的錯誤來源，而它不是 bug，是語意沒想清楚。**\n\n**引理 3（798：讓 $i$ 不得分的輪調量構成 $\\mathbb{Z}_n$ 上一段連續區塊）。**\n\n輪調量 $k$ 時，原本在下標 $i$ 的元素移到下標 $j = (i - k) \\bmod n$，得分條件是 $A_i \\le j$。\n\n故「不得分」$\\iff j \\in \\{0, 1, \\dots, A_i - 1\\}$。由 $k = (i - j) \\bmod n$，不得分的 $k$ 集合是\n$$\\{\\, (i - j) \\bmod n \\;:\\; 0 \\le j < A_i \\,\\} \\;=\\; \\{\\, i,\\ i-1,\\ \\dots,\\ i - A_i + 1 \\,\\} \\bmod n,$$\n即**以 $i$ 為右端、長度恰為 $A_i$ 的一段循環連續區塊**（$A_i = 0$ 時為空）。$\\square$\n\n於是：初始所有 $k$ 的分數都是 $n$，對每個 $i$ 在上述區塊上 $-1$，最後取最大分數中最小的 $k$。**跨越 0 的區塊要拆成 $[\\,(i-A_i+1) \\bmod n,\\ n-1\\,]$ 與 $[\\,0,\\ i\\,]$ 兩段**——差分陣列是線性的，環狀必須拆開（或把陣列加倍）。這是把差分用在答案軸上的最乾淨的例子。\n\n**引理 4（1674：每一對的代價對目標和 $T$ 是分段常數）。**\n\n配對 $(a, b) = (\\mathrm{nums}_i, \\mathrm{nums}_{n-1-i})$，每個元素可改成 $[1, \\mathrm{limit}]$ 中任意值。令 $\\mathrm{lo} = \\min(a,b)$、$\\mathrm{hi} = \\max(a,b)$。則把這一對的和變成 $T$ 所需的最少修改次數是\n\n$$\\mathrm{cost}(T) = \\begin{cases} 0, & T = a + b,\\\\ 1, & \\mathrm{lo} + 1 \\le T \\le \\mathrm{hi} + \\mathrm{limit},\\ T \\ne a+b,\\\\ 2, & \\text{其他}. \\end{cases}$$\n\n*證明。* $T = a+b$ 顯然不用改。只改一個元素時，保留其中一個 $x \\in \\{a,b\\}$，另一個設為 $T - x$，需 $1 \\le T - x \\le \\mathrm{limit}$，即 $T \\in [x+1,\\ x+\\mathrm{limit}]$。兩種保留選擇的聯集是 $[\\mathrm{lo}+1, \\mathrm{lo}+\\mathrm{limit}] \\cup [\\mathrm{hi}+1, \\mathrm{hi}+\\mathrm{limit}] = [\\mathrm{lo}+1, \\mathrm{hi}+\\mathrm{limit}]$——**聯集之所以連續，是因為 $\\mathrm{hi} + 1 \\le \\mathrm{lo} + \\mathrm{limit} + 1$（由 $\\mathrm{hi} \\le \\mathrm{limit}$、$\\mathrm{lo} \\ge 1$），兩段有重疊或相接。** 兩個都改則任何 $T \\in [2, 2\\cdot\\mathrm{limit}]$ 都做得到，故其餘情形是 2。$\\square$\n\n於是總代價 $\\mathrm{Cost}(T) = \\sum_i \\mathrm{cost}_i(T)$：先令全部為 $2n/2$，再對每對在 $[\\mathrm{lo}+1, \\mathrm{hi}+\\mathrm{limit}]$ 上 $-1$、在單點 $T = a+b$ 上再 $-1$。一次差分 + 一次掃描，$O(n + \\mathrm{limit})$。**「兩段的聯集恰好連續」這個小事實是能用單一區間加值的前提；若 $\\mathrm{hi} - \\mathrm{lo} > \\mathrm{limit}$（在本題的約束下不可能），就得拆成兩段。**\n\n**引理 5（995：由左至右的貪心翻轉唯一且最優）。**\n\n要把 0/1 陣列全變成 1，每次只能翻轉長度為 $k$ 的連續段。\n\n*宣稱*：從左掃描，若當前位置（考慮既有翻轉後）為 0，則**必須**在此處起一段翻轉。\n\n*證明。* 位置 $i$ 只能被左端點在 $[i-k+1, i]$ 的翻轉影響。由左至右處理到 $i$ 時，左端點 $< i$ 的翻轉都已決定，而位置 $i$ 若仍是 0，唯一還能改變它的是左端點恰為 $i$ 的翻轉。故此翻轉是被**強制**的，不存在選擇，也就談不上「更好的選擇」。$\\square$\n\n*差分在這裡的角色*：需要 $O(1)$ 知道「位置 $i$ 目前被翻轉了幾次」。用差分陣列 `diff`，起一段翻轉時 `diff[i] += 1; diff[i+k] -= 1;`，掃描時滾動累加 `flip += diff[i]`，當前值就是 `a[i] ^ (flip & 1)`。**這是差分與滑動視窗的混合：邊決策邊維護，而非最後才還原。** 3.1.1 說「操作與查詢分階段」是划算與否的判準——這裡查詢是嚴格由左至右的，每個位置只查一次，所以仍然是 $O(n)$。\n\n## 逐步演算法\n\n:::example 狀態表示：事件 (座標, +1 進場 / -1 離場)\n**1. 狀態表示：把每個區間拆成兩個端點事件。** 想清楚端點是開區間還是閉區間，決定離場放在 `end` 還是 `end + 1`、以及排序時同點的先後。\n\n```cpp\n// 狀態表示：事件 (座標, +1 進場 / -1 離場)\nvector<pair<int, int>> ev;\nfor (auto& iv : intervals) {\n    ev.push_back({iv[0], +1});\n    ev.push_back({iv[1], -1});  // 端點相接是否算重疊 → 調整排序規則\n}\n```\n:::\n\n\n\n:::example 更新：依座標排序，同座標時讓 -1 先於 +1（端點相接不算重疊）\n**2. 更新流程：排序後掃描累加。** 座標若是小整數也可改用差分陣列，原理相同。\n\n```cpp\n// 更新：依座標排序，同座標時讓 -1 先於 +1（端點相接不算重疊）\nsort(ev.begin(), ev.end(), [](auto& a, auto& b) {\n    return a.first != b.first ? a.first < b.first : a.second < b.second;\n});\n```\n:::\n\n\n\n:::example 讀取：邊掃邊取最大重疊數\n**3. 答案讀取：掃描中即時取得所需統計。** 求最大重疊、找空檔、合併區間都在這趟掃描內完成。\n\n```cpp\n// 讀取：邊掃邊取最大重疊數\nint cur = 0, ans = 0;\nfor (auto& [x, d] : ev) {\n    cur += d;\n    ans = max(ans, cur);\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 端點開閉是最常見的坑：相接的兩區間算不算重疊，靠「同座標時 −1 與 +1 的排序順序」控制。\n- 座標可離散化（先收集所有端點、排序去重、映射成索引）再用差分陣列，避免巨大空間。\n- 事件數 `2n`，排序 `O(n log n)`、掃描 `O(n)`，合計 `O(n log n)`；用差分陣列則 `O(範圍)`。\n\n## C++17 模板\n\n以下用同型延伸示範題 1109. 航班預訂統計 展示核心寫法。\n\n**完整題目**：給定多筆航班區間座位預訂，回傳每個航班最後被預訂的座位數。\n\n:::example 差分陣列：對區間 [first, last] 批量加減，最後前綴和還原\n**線索**：題目反覆詢問連續區間量；若區間值能由兩個前綴狀態相減，就應優先想到前綴和。\n\n```cpp\nclass Solution {\npublic:\n    // 差分陣列：對區間 [first, last] 批量加減，最後前綴和還原\n    vector<int> corpFlightBookings(vector<vector<int>>& bookings, int n) {\n            vector<int> diff(n + 1);  // 差分陣列，多一格方便邊界處理\n            for (auto& b : bookings) {\n                    diff[b[0] - 1] += b[2];  // 區間左端點加上訂位數（1-indexed 轉 0-indexed）\n                    diff[b[1]] -= b[2];  // 區間右端點後一格減去訂位數\n            }\n            // 前綴和還原每個航班的實際乘客數\n            vector<int> ans(n);\n            int cur = 0;\n            for (int i = 0; i < n; ++i) {\n                    cur += diff[i];\n                    ans[i] = cur;\n            }\n            return ans;\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- 差分把區間加轉成兩個端點更新；最後做一次前綴和即可還原每個位置的實際值。\n- 前綴狀態先保存空前綴，讓從下標 0 開始的區間也能統一處理。\n- 查詢區間時只做兩個前綴狀態相減；計數題則查找歷史中需要的前綴值。\n\n## 時間與空間複雜度\n\n| 形態 | 時間 | 空間 | 備註 |\n| --- | --- | --- | --- |\n| 事件掃描（vector + sort） | $O(n\\log n)$ | $O(n)$ | 與值域無關 |\n| 事件掃描（`std::map`） | $O(n\\log n)$ | $O(n)$ | 常數大 2–5 倍，但支援線上插入 |\n| 合併區間 56/57/759 | $O(n\\log n)$ | $O(n)$ | 排序主導；57 若輸入已排序可 $O(n)$ |\n| 答案軸差分（798、1674） | $O(n + V)$ | $O(V)$ | $V$ = 候選答案個數 |\n| 差分 + 由左至右貪心（995、3356） | $O(n)$ | $O(n)$ | 滾動累加，不需回頭 |\n| 二分答案 + 差分（2528、3356） | $O(n\\log V)$ | $O(n)$ | 每次判定 $O(n)$ |\n\n**答案軸差分與「對每個候選答案暴力算」的落差**：1674 暴力是 $O(n \\cdot \\mathrm{limit})$（$10^5 \\times 10^5 = 10^{10}$），差分是 $O(n + \\mathrm{limit}) = 2\\times10^5$。**這是本節最大的槓桿。**\n\n**事件掃描的排序可否省掉？** 若座標本身有界且密集，直接用 3.1.1 的陣列版 $O(n + V)$ 更快（省掉 $\\log$）。**判準只有一個：$V$ 開不開得下。** $V \\le 10^7$ 大致是分水嶺。\n\n## 常見錯誤與邊界條件\n\n- 前綴陣列常開 n+1，讓空前綴為 0。\n- 區間 `[l, r]` 對應 `pre[r + 1] - pre[l]`。\n- 含負數時不能用滑動視窗替代前綴和。\n- 和可能溢位時用 long long。\n\n## 常見變形\n\n- 一維前綴和、二維前綴和。\n- 前綴和 + hash map 統計子陣列數。\n- 前綴和 + 有序集合處理上下界或最大不超過 k。\n\n## 與相似技巧的比較\n\n- **與 3.1.1**：事件掃描 = 差分陣列的稀疏版本，兩者不變量完全相同。**先想陣列版，開不下才升級。**\n- **與 3.2 二維差分**：矩形加值的稀疏版是「掃描線 + 線段樹」（850 矩形面積 II），因為兩個維度中只有一維能靠排序處理，另一維仍需資料結構。**一維事件掃描不需線段樹，二維就需要——這是維度帶來的真實成本。**\n- **與 sorting 主題的區間貪心**：56/57 是「合併」，435/452 是「選最多不重疊」。**合併靠左端點排序，選取靠右端點排序**，兩者的正確性前提相反，不要互抄。\n- **與 6.x 堆**：253（會議室 II）可以用「最小堆維護各會議室的結束時間」解，也可以用事件掃描。**事件掃描只回答「最多幾間」，堆還能回答「每間分別是誰」。要輸出分配方案就得用堆。**\n- **與二分答案**：2528、3356 的形態是「答案單調 + 判定用差分 $O(n)$」。**判定函數能不能寫成一次差分掃描，往往決定這題可不可做。**\n- **與 1589（所有排列中的最大和）**：先用差分算出每個下標被查詢覆蓋的次數 $c_i$，再把大的 $\\mathrm{nums}$ 配給大的 $c$。最優性來自**排序不等式**（交換論證：若 $c_p > c_q$ 卻把小值配給 $p$，交換後總和不減）。**差分只負責算出 $c$，最優性是另一段獨立的論證——別把兩件事混成一句「排序就好」。**\n\n## 代表例題\n\n**題號**：56、57、759、798、2015\n\n放在「進階」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 56 | [合併區間](https://leetcode.cn/problems/merge-intervals) |  |\n| 57 | [插入區間](https://leetcode.cn/problems/insert-interval) |  |\n| 759 | [員工空閒時間](https://leetcode.cn/problems/employee-free-time)（Premium） |  |\n| 798 | [得分最高的最小輪調](https://leetcode.cn/problems/smallest-rotation-with-highest-score) |  |\n| 2015 | [每段建築物的平均高度](https://leetcode.cn/problems/average-height-of-buildings-in-each-segment)（Premium） |  |\n| 2021 | [街上最亮的位置](https://leetcode.cn/problems/brightest-position-on-street) |  |\n| 2237 | [計算街道上滿足所需亮度的位置數量](https://leetcode.cn/problems/count-positions-on-street-with-required-brightness) |  |\n| 3009 | [折線圖上的最大交點數量](https://leetcode.cn/problems/maximum-number-of-intersections-on-the-chart) |  |\n| 3279 | [活塞佔據的最大總面積](https://leetcode.cn/problems/maximum-total-area-occupied-by-pistons) |  |\n| 3453 | [分割正方形 I](https://leetcode.cn/problems/separate-squares-i) | 1735 |\n| 2381 | [字母移位 II](https://leetcode.cn/problems/shifting-letters-ii) | 1793 |\n| 995 | [K 連續位的最小翻轉次數](https://leetcode.cn/problems/minimum-number-of-k-consecutive-bit-flips) | 1835 |\n| 1589 | [所有排列中的最大和](https://leetcode.cn/problems/maximum-sum-obtained-of-any-permutation) | 1871 |\n| 1526 | [形成目標陣列的子陣列最少增加次數](https://leetcode.cn/problems/minimum-number-of-increments-on-subarrays-to-form-a-target-array) | 1872 |\n| 2327 | [知道祕密的人數](https://leetcode.cn/problems/number-of-people-aware-of-a-secret) | 1894 |\n| 3356 | [零陣列變換 II](https://leetcode.cn/problems/zero-array-transformation-ii) | 1913 |\n| 1943 | [描述繪畫結果](https://leetcode.cn/problems/describe-the-painting) | 1969 |\n| 3224 | [使差值相等的最少陣列改動次數](https://leetcode.cn/problems/minimum-array-changes-to-make-differences-equal) | 1996 |\n| 2251 | [花期內花的數目](https://leetcode.cn/problems/number-of-flowers-in-full-bloom) | 2022 |\n| 2772 | [使陣列中的所有元素都等於零](https://leetcode.cn/problems/apply-operations-to-make-all-array-elements-equal-to-zero) | 2029 |\n| 3229 | [使陣列等於目標陣列所需的最少操作次數](https://leetcode.cn/problems/minimum-operations-to-make-array-equal-to-target) | 2067 |\n| 3529 | [統計水平子串和垂直子串重疊格子的數目](https://leetcode.cn/problems/count-cells-in-overlapping-horizontal-and-vertical-substrings) | 2105 |\n| 3347 | [執行操作後元素的最高頻率 II](https://leetcode.cn/problems/maximum-frequency-of-an-element-after-performing-operations-ii) | 2156 |\n| 2528 | [最大化城市的最小電量](https://leetcode.cn/problems/maximize-the-minimum-powered-city) | 2236 |\n| 1674 | [使陣列互補的最少操作次數](https://leetcode.cn/problems/minimum-moves-to-make-array-complementary) | 2333 |\n| 3362 | [零陣列變換 III](https://leetcode.cn/problems/zero-array-transformation-iii) | 2424 |\n| 3655 | [區間乘法查詢後的異或 II](https://leetcode.cn/problems/xor-after-range-multiplication-queries-ii) | 2454 |\n| 3017 | [按距離統計房屋對數目 II](https://leetcode.cn/problems/count-the-number-of-houses-at-a-certain-distance-ii) | 2709 |\n\n**補充例題**：\n\n- [LeetCode 2406 將區間分為最少組數](https://leetcode.cn/problems/divide-intervals-into-minimum-number-of-groups/)：最少組數等於任一時刻重疊的最大區間數；閉區間端點相等仍算重疊。\n\n## 本節重點速查\n\n- 事件掃描 = 差分的稀疏表示：$[l,r]$ 加 $v$ ⇒ 事件 $(l,+v)$、$(r{+}1,-v)$，排序後累加。**與值域無關，$O(n\\log n)$。**\n- **同座標事件的處理順序就是區間的開閉語意**：閉區間 ⇒ $-1$ 放 $r+1$ 且同座標批次處理完再取值；半開 ⇒ $-1$ 放 $r$ 且排序時 $-1$ 先行。二選一，不要混。\n- 合併區間靠**左端點**排序（引理 1：$l_j \\ge l_i > R$ ⇒ 不必回頭）；選最多不重疊靠**右端點**排序。**排序鍵是正確性前提。**\n- **差分可以做在「答案軸」上**：798 對輪調量、1674 對目標和、2528 對位置。訊號是「每個元素對每個候選答案的貢獻是分段常數」。\n- 798 的不得分區塊是以 $i$ 為右端、長度 $A_i$ 的循環段；**環狀要拆兩段或倍長。**\n- 1674 的三段代價：$0$ / $1$（$[\\mathrm{lo}+1, \\mathrm{hi}+\\mathrm{limit}]$）/ $2$；聯集連續是能用單一區間加值的前提。\n- 995 的由左至右貪心是**被強制的**（位置 $i$ 只剩左端點為 $i$ 的翻轉能救），差分負責 $O(1)$ 查當前翻轉次數。\n- $V$ 開得下就用陣列（$O(n+V)$，省一個 $\\log$）；開不下才用排序或 `map`。\n",
            },
          ],
        },
        {
          id: 26,
          title: "3.2 二維差分",
          summary:
            "## 這個技術解決什麼問題\n\n3.1.1 的一維差分把「區間加值」壓成兩次單點修改。二維差分做同一件事：**把「子矩形整體加 $v$」壓成四次單點修改**，最後做一次二維前綴和還原。\n\n暴力是每筆操作逐格加：$q$ 筆操作、矩形面積 $A$，$O(qA)$。2536（子矩陣元素加 1）有 $n \\le 500$、$q \\le 10^4$，最壞 $10^4 \\times 2.5\\times10^5 = 2.5\\times10^9$，超時。二維差分是 $O(q + mn)$。\n\n它與 2.6 二維前綴和恰好對偶，而且**同一題常常兩個都要用**：2132（用郵票貼滿網格圖）用二維前綴和判斷「這個 $h\\times w$ 視窗是不是全空」，用二維差分記錄「哪些格子被貼到」。**看到「先問能不能放、再記錄放了哪些」的雙層結構，就是這一對搭檔。**\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「差分」想：\n\n- **大量的「區間 / 子矩形整段加同一個值」操作，最後才一次性問結果。** 例如 LeetCode 2536「子矩陣元素加 1」就是反覆對子矩形 +1，問每格最終次數。\n- **操作期間不需要中途查詢，可以「先全部記下、最後再結算」。** 這正是差分能成立的前提：\n\n:::example 嗅到味道：每次都是「整段加同一個值」，而且只在最後才要答案\n```cpp\n// 嗅到味道：每次都是「整段加同一個值」，而且只在最後才要答案\n// addRange(l, r, v);  // 重複很多次\n// 然後才問：每一格現在是多少？\n```\n:::\n\n\n\n- **暴力做法是每次操作都逐格修改（一維 `O(n)`、二維 `O(n*m)`），題目數據量讓它超時。** 850「矩形面積 II」、2132「用郵票貼滿網格圖」也都靠差分把覆蓋次數壓成線性。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的操作是不是「對一段區間 / 一塊矩形加同一個值」？\n2. 我能不能等所有操作做完，再一次還原答案？\n3. 一維我改哪兩個位置、二維我改哪四個角，符號對不對？\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n本節要解決的場景是：對一個陣列（或矩陣）做「很多次區間加值」，例如「把第 2 到第 5 格全部 +3」，重複幾萬次之後，問每一格最後是多少。\n\n最直覺的做法是每次操作就乖乖把區間裡每一格都加一遍，但這樣一次操作就要 `O(n)`，多次操作會慢到不行。差分（difference array）的想法是：**不要每格都改，只在「區間的開頭和結尾後一格」做兩次記號，等全部記號蓋完，再用一次前綴和把答案還原出來。**\n\n:::example 一維差分：把 [l, r] 加 v，只改兩端\n先看一維。我們維護一個 `diff` 陣列，要把區間 `[l, r]` 全部加上 `v` 時，只動兩個位置：\n\n```cpp\n// 一維差分：把 [l, r] 加 v，只改兩端\ndiff[l] += v;      // 從 l 開始「多」了 v\ndiff[r + 1] -= v;  // 到了 r+1 就「收回」這個 v\n```\n:::\n\n\n\n這兩個記號的意義是：「從 `l` 開始，每一格都比左邊多 `v`；到了 `r + 1` 又把這個增量收回去」。所以對 `diff` 做一次前綴和，`a[i] = a[i - 1] + diff[i]`，就還原出每一格被加了多少。單次操作只要 `O(1)`，最後還原一次 `O(n)`。\n\n:::example 二維差分：對子矩形加 v，動四個角\n二維是同樣的想法，只是一個矩形 `(r1, c1)`～`(r2, c2)` 要在「四個角」做加減記號：\n\n```cpp\n// 二維差分：對子矩形加 v，動四個角\ndiff[r1][c1] += v;\ndiff[r1][c2 + 1] -= v;\ndiff[r2 + 1][c1] -= v;\ndiff[r2 + 1][c2 + 1] += v;  // 多扣的補回來\n```\n:::\n\n\n\n為什麼是四個角？把矩形想成「左上角開始的一大塊加 `v`」，多加到的部分再用另外兩個角扣掉，重疊被扣兩次的那塊再用第四個角補回來——這正是容斥。最後對 `diff` 做「二維前綴和」就還原出每格的最終值。\n\n## 狀態／資料結構定義\n\n**定義。** 對 $m \\times n$ 的目標矩陣 $a$（初始全 0），維護差分矩陣 $d$，大小 $(m+1) \\times (n+1)$。\n\n**子矩形加值** $[r_1, r_2] \\times [c_1, c_2] \\mathrel{+}= v$：\n$$d[r_1][c_1] \\mathrel{+}= v,\\quad d[r_1][c_2{+}1] \\mathrel{-}= v,\\quad d[r_2{+}1][c_1] \\mathrel{-}= v,\\quad d[r_2{+}1][c_2{+}1] \\mathrel{+}= v.$$\n\n**還原**：對 $d$ 做一次二維前綴和（可原地）：\n$$a[i][j] \\;=\\; \\sum_{r \\le i}\\sum_{c \\le j} d[r][c].$$\n\n**四個角落的符號是 $+,-,-,+$**，與 2.6 的**查詢**公式（$-,-,+$ 加上主項）同形。這不是巧合，下一節會證明兩者是同一個張量積結構。\n\n**尺寸必須是 $(m+1)\\times(n+1)$**，因為 $r_2$ 可到 $m-1$、$c_2$ 可到 $n-1$，於是 $d[m][\\cdot]$ 與 $d[\\cdot][n]$ 要有地方放。**多開的那一列一行是垃圾桶，還原時不讀它們**——和一維一樣，這是為了省掉 `if (r2 + 1 < m)` 這種特判。\n\n**座標很大時要離散化。** 850（矩形面積 II）與 LCP 74（最強祝福力場）的座標到 $10^9$，做法是：收集所有出現過的 $x$、$y$ 座標去重排序，在壓縮後的網格上做二維差分，最後把每個網格單元的面積 $(\\Delta x)(\\Delta y)$ 按「覆蓋次數是否 $> 0$」加總。LCP 74 的邊界會落在半整數（力場邊長是奇數時中心在格點，邊界在 $\\pm 0.5$），**標準處理是把所有座標乘 2 化成整數，全程避免浮點比較。**\n\n## 不變量或正確性證明\n\n**主不變量。** 任意一串子矩形加值之後，恆有 $a[i][j] = \\sum_{r\\le i}\\sum_{c\\le j} d[r][c]$。\n\n*證明。* 對操作次數歸納。基底顯然。歸納步：一筆 $[r_1,r_2]\\times[c_1,c_2] \\mathrel{+}= v$ 讓真實值改變 $v \\cdot \\big[\\,r_1 \\le i \\le r_2\\,\\big]\\cdot\\big[\\,c_1 \\le j \\le c_2\\,\\big]$。另一方面，四個脈衝對 $\\sum_{r\\le i}\\sum_{c\\le j}$ 的貢獻是\n$$v\\Big([\\,r_1\\le i\\,] - [\\,r_2{+}1\\le i\\,]\\Big)\\Big([\\,c_1\\le j\\,] - [\\,c_2{+}1\\le j\\,]\\Big),$$\n因為四項 $(+,-,-,+)$ 恰好是把上式展開。而 $[\\,r_1\\le i\\,]-[\\,r_2{+}1\\le i\\,] = [\\,r_1\\le i\\le r_2\\,]$（一維差分的恆等式，見 3.1.1），兩維各一次，乘起來正是矩形指示函數。$\\square$\n\n**這個證明說出了二維差分的真正身分：它是兩個一維差分的張量積。**\n\n$$\\underbrace{(\\,+1 \\text{ at } r_1,\\ -1 \\text{ at } r_2{+}1\\,)}_{\\text{列方向的一維差分}} \\;\\otimes\\; \\underbrace{(\\,+1 \\text{ at } c_1,\\ -1 \\text{ at } c_2{+}1\\,)}_{\\text{行方向的一維差分}}$$\n\n四個角落 = $2 \\times 2$ 個乘積項，符號 = 兩個符號相乘。**這也立刻給出 $d$ 維推廣：$2^d$ 個角落，符號是 $(-1)^{\\text{取到 }+1\\text{ 偏移的維度數}}$。** 不必再背公式。\n\n**張量積結構點名了它依賴的前提：加值區域必須是「區間的直積」，也就是軸對齊矩形。**\n\n若要對**對角帶狀區域**、**圓形**、或**任意多邊形**整體加值，區域不可分解成 $I \\times J$，四角落法徹底失效。可行的替代：對角帶狀可以先做 1.4 的座標變換 $(r-c, r+c)$ 把它變回矩形；一般多邊形則要用掃描線逐列處理，退化成 $m$ 次一維差分（$O(m + q\\cdot m)$）。**「能不能寫成直積」是使用二維差分的唯一入場券。**\n\n**另外三個前提沿用 3.1.1，一字不改**：操作必須可疊加（**子矩形賦值不行**，要二維線段樹或 KD 樹）；值必須有逆元；操作與查詢必須分階段（中途要查就得用二維樹狀陣列，$O(\\log m\\log n)$）。\n\n**還原可以原地做的理由。** 二維前綴和的遞推 $d[i][j] \\mathrel{+}= d[i-1][j] + d[i][j-1] - d[i-1][j-1]$，在 $i$、$j$ 皆遞增的掃描順序下，讀到的三個鄰居**都已經是前綴和形式**，而寫入的 $d[i][j]$ 之後不會再被當成原始差分讀取。**順序正確性就是這條「只讀已完成、只寫未來不再需要原值的格子」的觀察**——與 DP 滾動陣列的更新順序論證同構。\n\n**2132（用郵票貼滿網格圖）的貪心最優性。**\n\n*宣稱*：把郵票貼在**每一個貼得下的位置**（該 $h\\times w$ 視窗內全為空），若仍有空格未被覆蓋，則答案為否。\n\n*證明。* 設存在一組合法貼法 $P$ 覆蓋了所有空格。$P$ 中每張郵票的位置都必然是「貼得下的位置」（否則會壓到障礙物，不合法），所以 $P \\subseteq$ 全體可貼位置 $P^*$。覆蓋是單調的：$P \\subseteq P^*$ ⇒ $P$ 覆蓋的格子 $\\subseteq$ $P^*$ 覆蓋的格子。故若 $P$ 能全覆蓋，$P^*$ 必也能。反之若 $P^*$ 覆蓋不全，任何 $P$ 都不行。$\\square$\n\n**這條論證依賴兩個前提：郵票可以互相重疊、且郵票數量沒有上限。** 少了任一個，「多貼不會更差」就不成立——若限制最多貼 $k$ 張，這就變成集合覆蓋（NP-hard）；若不許重疊，就變成精確覆蓋。**題目那句不起眼的「郵票可以重疊」是整個貪心的地基。**\n\n**850（矩形面積 II）為什麼不能直接把覆蓋次數當權重。** 離散化後每個網格單元 $[x_k, x_{k+1}) \\times [y_l, y_{l+1})$ 的覆蓋次數 $c_{kl}$ 可能 $> 1$，但**聯集面積**只計一次。所以要加總的是\n$$\\sum_{k,l} [\\,c_{kl} > 0\\,]\\cdot (x_{k+1}-x_k)(y_{l+1}-y_l),$$\n不是 $\\sum c_{kl}\\cdot(\\dots)$。**差分只負責高效算出 $c$，「聯集」這個語意要在最後那一步用 $[\\,c>0\\,]$ 表達。** 這是離散化 + 差分最常見的概念錯誤。\n\n*離散化的閉開約定*：矩形 $[x_1,x_2)\\times[y_1,y_2)$（面積題天然是半開）對應的網格單元下標範圍是 $[\\,\\mathrm{idx}(x_1),\\ \\mathrm{idx}(x_2)-1\\,]$。**這裡不能再 $+1$**——半開區間的右端點本身不屬於區域。與 3.1.2 引理 2 是同一個「開閉語意決定端點」的問題，只是換到二維。\n\n## 逐步演算法\n\n:::example 狀態表示：開一個比原圖大一圈的差分陣列（避免 +1 越界）\n**1. 狀態表示：用差分陣列代替原陣列。** 真正存的是「相鄰格之間的差」，而不是每格的值。\n\n```cpp\n// 狀態表示：開一個比原圖大一圈的差分陣列（避免 +1 越界）\nvector<vector<long long>> diff(n + 1, vector<long long>(m + 1, 0));\n```\n:::\n\n\n\n:::example 更新：對矩形 (r1,c1)~(r2,c2) 加 v\n**2. 更新流程：每個操作只蓋四個角。** 不論這個矩形多大，都只動四個位置，`O(1)`。\n\n```cpp\n// 更新：對矩形 (r1,c1)~(r2,c2) 加 v\ndiff[r1][c1] += v;\ndiff[r1][c2 + 1] -= v;\ndiff[r2 + 1][c1] -= v;\ndiff[r2 + 1][c2 + 1] += v;\n```\n:::\n\n\n\n:::example 還原：把差分轉回每格真正的值\n**3. 答案讀取：全部操作做完後，用二維前綴和一次還原。** 每格等於「上、左已還原值相加，扣掉左上重疊，再加自己的差分」。\n\n```cpp\n// 還原：把差分轉回每格真正的值\nfor (int i = 1; i <= n; ++i) {\n    for (int j = 1; j <= m; ++j) {\n        diff[i][j] += diff[i - 1][j] + diff[i][j - 1] - diff[i - 1][j - 1];\n    }\n}\n// 此時 diff[i][j] 就是該格的最終值\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- `r + 1`、`c + 1` 可能越界，所以陣列要多開一圈、或用大一號的下標起點。\n- 二維四個角的符號必須「成對」：兩個 `+`、兩個 `-`，少一個就全錯。\n- 複雜度：`q` 次操作 + 一次還原 = `O(q + n*m)`，遠優於暴力的 `O(q * n * m)`；空間 `O(n*m)`。\n\n## C++17 模板\n\n```cpp\n// 二維差分的通用封裝：多次子矩形加值，最後一次還原。\nclass Diff2D {\n  int m_, n_;\n  std::vector<std::vector<long long>> d_;   // (m+1) x (n+1)，多開的一列一行是垃圾桶\n\n public:\n  Diff2D(int m, int n) : m_(m), n_(n), d_(m + 1, std::vector<long long>(n + 1, 0)) {}\n\n  // 閉區間 [r1, r2] x [c1, c2] 整體加 v；四個角落符號 +,-,-,+\n  void add(int r1, int c1, int r2, int c2, long long v) {\n    d_[r1][c1] += v;\n    d_[r1][c2 + 1] -= v;\n    d_[r2 + 1][c1] -= v;\n    d_[r2 + 1][c2 + 1] += v;\n  }\n\n  // 原地二維前綴和還原；回傳 m x n 的結果視圖（讀 d_[i][j], i < m, j < n）\n  const std::vector<std::vector<long long>>& build() {\n    for (int i = 0; i < m_; ++i) {\n      for (int j = 0; j < n_; ++j) {\n        long long up = i ? d_[i - 1][j] : 0;\n        long long left = j ? d_[i][j - 1] : 0;\n        long long ul = (i && j) ? d_[i - 1][j - 1] : 0;\n        d_[i][j] += up + left - ul;       // 讀到的鄰居都已是前綴和，故可原地\n      }\n    }\n    return d_;\n  }\n};\n\n// 子矩陣元素加 1（LeetCode 2536）\nclass Solution {\n public:\n  std::vector<std::vector<int>> rangeAddQueries(int n, std::vector<std::vector<int>>& queries) {\n    Diff2D diff(n, n);\n    for (const auto& q : queries) { diff.add(q[0], q[1], q[2], q[3], 1); }\n    const auto& g = diff.build();\n\n    std::vector<std::vector<int>> ans(n, std::vector<int>(n));\n    for (int i = 0; i < n; ++i) {\n      for (int j = 0; j < n; ++j) { ans[i][j] = static_cast<int>(g[i][j]); }\n    }\n    return ans;\n  }\n};\n```\n\n```cpp\n// 用郵票貼滿網格圖（LeetCode 2132）：二維前綴和判定 + 二維差分記錄。\nclass Solution {\n public:\n  bool possibleToStamp(std::vector<std::vector<int>>& grid, int h, int w) {\n    int m = grid.size(), n = grid[0].size();\n\n    // (1) 二維前綴和：O(1) 判斷 h x w 視窗是否全空\n    std::vector<std::vector<int>> s(m + 1, std::vector<int>(n + 1, 0));\n    for (int i = 0; i < m; ++i) {\n      for (int j = 0; j < n; ++j) {\n        s[i + 1][j + 1] = s[i][j + 1] + s[i + 1][j] - s[i][j] + grid[i][j];\n      }\n    }\n\n    // (2) 二維差分：貼得下就貼（重疊無害，見貪心證明）\n    std::vector<std::vector<int>> d(m + 1, std::vector<int>(n + 1, 0));\n    for (int i = 0; i + h <= m; ++i) {\n      for (int j = 0; j + w <= n; ++j) {\n        int r2 = i + h - 1, c2 = j + w - 1;\n        int occupied = s[r2 + 1][c2 + 1] - s[i][c2 + 1] - s[r2 + 1][j] + s[i][j];\n        if (occupied == 0) {\n          d[i][j] += 1;\n          d[i][c2 + 1] -= 1;\n          d[r2 + 1][j] -= 1;\n          d[r2 + 1][c2 + 1] += 1;\n        }\n      }\n    }\n\n    // (3) 原地還原覆蓋次數，順便檢查每個空格是否被蓋到\n    for (int i = 0; i < m; ++i) {\n      for (int j = 0; j < n; ++j) {\n        int up = i ? d[i - 1][j] : 0;\n        int left = j ? d[i][j - 1] : 0;\n        int ul = (i && j) ? d[i - 1][j - 1] : 0;\n        d[i][j] += up + left - ul;\n        if (grid[i][j] == 0 && d[i][j] == 0) { return false; }\n      }\n    }\n    return true;\n  }\n};\n```\n\n## 時間與空間複雜度\n\n**建構**：$q$ 筆操作各 $O(1)$（四次寫入），共 $O(q)$。**還原**：一次二維前綴和 $O(mn)$。**總計 $O(mn + q)$ 時間、$O(mn)$ 空間。**\n\n與暴力 $O(qA)$ 的落差：2536 從 $2.5\\times10^9$ 降到 $2.6\\times10^5$。\n\n**2132**：前綴和 $O(mn)$ + 枚舉所有貼法 $O(mn)$（每個位置 $O(1)$ 判定）+ 還原 $O(mn)$ = $O(mn)$。\n\n**850（離散化版）**：座標數 $O(n)$，網格 $O(n^2)$ 個單元，差分 $O(n)$ 次操作 + $O(n^2)$ 還原 = $O(n^2)$。$n \\le 200$ 時 $4\\times10^4$，輕鬆。**若 $n$ 到 $10^5$，$O(n^2)$ 就不行了，必須改「掃描線 + 線段樹」的 $O(n\\log n)$**——那時第二維無法靠排序解決，只能上資料結構。\n\n**$d$ 維推廣**：每次操作 $O(2^d)$，還原 $O(d \\cdot N)$（逐維做一維前綴和），$N$ 是總格數。**$d \\ge 4$ 時建議用「逐維前綴和」的寫法，不要手寫 $2^d$ 個角落——符號一定會抄錯。**\n\n## 常見錯誤與邊界條件\n\n- 四個角落的符號抄錯（正確是 $+,-,-,+$；**忘記最後那個 $+v$ 是最常見的**，症狀是右下方整片多加了 $v$）。\n- 矩陣開成 $m\\times n$ 而非 $(m+1)\\times(n+1)$，`d[r2+1][c2+1]` 越界。\n- 還原時把掃描順序寫成遞減（原地前綴和只在遞增順序下正確）。\n- 還原時讀到未還原的邊界列行（迴圈範圍應是 $i<m,\\ j<n$，垃圾桶那一列一行不參與）。\n- 閉區間與半開區間混用——**離散化後的面積題是半開，右端點對應 `idx(x2) - 1`，不要再 $+1$**。\n- 850 把覆蓋次數當權重累加（應該用 $[\\,c>0\\,]$）。\n- 850 忘記座標去重（重複座標會產生寬度為 0 的單元，雖不影響面積但會浪費時間，且容易寫出 `idx` 錯位）。\n- 850 的面積用 `int`（$10^9 \\times 10^9$ 必爆，**且中途取模前要先用 `long long`**）。\n- LCP 74 用浮點座標比較（應乘 2 化整數）。\n- LCP 74 的力場邊界搞混（邊長 $s$、中心 $(x,y)$ 時範圍是 $[x - s/2,\\ x + s/2]$，乘 2 後是 $[2x - s,\\ 2x + s]$）。\n- 2132 忘記「郵票不能壓到障礙物」而無條件貼。\n- 2132 的視窗枚舉範圍寫成 `i < m` 而非 `i + h <= m`。\n- 對非矩形區域套用四角落法（**張量積前提不成立**）。\n- 子矩形**賦值**用差分（不成立，要線段樹）。\n\n## 與相似技巧的比較\n\n- **與 3.1.1 一維差分**：張量積關係。一維兩個端點、二維四個角落、$d$ 維 $2^d$ 個角落，符號是各維符號的乘積。**理解張量積就不必分別記憶。**\n- **與 2.6 二維前綴和**：嚴格互逆。**二維前綴和 = 建好不動、多次查矩形和；二維差分 = 多次改矩形、最後查一次。** 2132 同時用到兩者，因為它既要查詢（能不能貼）又要修改（貼了哪裡）。\n- **與二維樹狀陣列**：支援「矩形加、單點查」（對差分矩陣建二維 BIT）或「單點加、矩形和查」，$O(\\log m\\log n)$。**只有在修改與查詢真的交錯時才值得。**\n- **與掃描線 + 線段樹（850 的進階解）**：當一個維度的規模大到不能離散化成 $O(n^2)$ 網格時，把 $x$ 軸當時間掃描、$y$ 軸用線段樹維護「當前被覆蓋的總長度」。**二維差分的稀疏化不像一維那麼便宜——一維的稀疏版只要排序，二維的稀疏版需要一棵樹。這是維度的真實代價。**\n- **與 1.4 遍歷對角線**：若加值區域是對角帶狀，先用 $(r-c,\\ r+c)$ 變換把它轉成矩形，再套本節。**變換的前提是矩陣為矩形（凸），與 1.4 那條引理相同。**\n\n## 代表例題\n\n**題號**：LCP 74、850、2132、2536\n\n放在「二維差分」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| LCP 74 | [最強祝福力場](https://leetcode.cn/problems/xepqZ5) |  |\n| 850 | [矩形面積 II](https://leetcode.cn/problems/rectangle-area-ii) | 2236 |\n| 2132 | [用郵票貼滿網格圖](https://leetcode.cn/problems/stamping-the-grid) | 2364 |\n| 2536 | [子矩陣元素加 1](https://leetcode.cn/problems/increment-submatrices-by-one) | 1583 |\n\n**延伸閱讀**：[LeetCode 2132 從一維差分到二維差分](https://leetcode.cn/problems/stamping-the-grid/solution/wu-nao-zuo-fa-er-wei-qian-zhui-he-er-wei-zwiu/)，可對照矩形四角更新與最後的二維前綴還原。\n\n## 本節重點速查\n\n- 子矩形 $[r_1,r_2]\\times[c_1,c_2]$ 加 $v$ ⇒ 四個角落 `d[r1][c1] += v; d[r1][c2+1] -= v; d[r2+1][c1] -= v; d[r2+1][c2+1] += v;`（$+,-,-,+$）。\n- **二維差分 = 兩個一維差分的張量積**；$d$ 維就是 $2^d$ 個角落，符號 = 各維符號相乘。\n- 矩陣開 $(m+1)\\times(n+1)$；還原用**原地二維前綴和**，$i,j$ 皆遞增。\n- 入場券是「加值區域可寫成區間直積（軸對齊矩形）」——對角帶狀先做 $(r-c, r+c)$ 變換，一般多邊形只能逐列一維差分。\n- 沿用 3.1.1 的三個前提：**可疊加**（賦值不行）、**有逆元**、**修改與查詢分階段**。\n- 2132 = 二維前綴和判定 + 二維差分記錄；貪心「貼滿所有可貼位置」的前提是**郵票可重疊且數量無限**。\n- 850 離散化後要用 $[\\,c>0\\,]$ 而非 $c$ 當權重；半開區間的右端是 `idx(x2) - 1`；面積用 `long long`。\n- LCP 74 的半整數邊界一律**乘 2 化整數**。\n- 二維的稀疏化要靠掃描線 + 線段樹，不像一維只要排序。\n",
        },
      ],
    },
    {
      id: 27,
      title: "4. 堆疊",
      summary:
        "堆疊是 LIFO：最後放進去的最先拿出來。它在解題裡的真正用途只有一個——**保存「還沒等到配對／還沒確定答案」的元素**，等到條件成立的那一刻再一次結清。理解它和「函式呼叫棧」的對應，就能把任何遞迴改寫成顯式棧。\n\n**如何挑子單元**\n\n- **4.1 基礎**：純粹的後進先出，例如最小棧、用兩個棧實作佇列。\n- **4.2 進階**：棧本身要額外維護資訊（同步維護最小值、支援區間反轉的棧序列）。\n- **4.3 鄰項消除**：相鄰兩個元素滿足某條件就同歸於盡——移除相鄰重複字元、行星碰撞。新元素進來前，先反覆和棧頂比對並消除。\n- **4.4 合法括號字串（RBS）**：左括號進棧、右括號配對出棧；只需要計數時，棧可以退化成一個計數器，但**中途一旦變負就非法**這件事不能省。\n- **4.5 表示式解析**：中綴轉後綴、四則運算、字串解碼。通常要兩個棧（運算元與運算子），或在遇到 `(` 時把當前狀態整包壓入。\n- **4.6 對頂堆疊**：用兩個棧夾住一個游標，模擬可左右移動的文字編輯器。\n- **4.7 單調堆疊**：棧內元素保持遞增或遞減，用來找「下一個更大／更小元素」與貢獻法。這是最常考的一型，完整內容另見「單調堆疊」題單。\n\n**共通訊號**：你發現需要「回頭找最近一個滿足某條件的元素」，而且較早的元素會被較晚的元素擋住——那就是棧。",
      children: [
        {
          id: 28,
          title: "4.1 基礎",
          summary:
            '## 這個技術解決什麼問題\n\n堆疊只回答一種問題，但這種問題到處都是：**「最近的那一個是誰？」**\n\n當處理第 $i$ 個元素時，若你需要的資訊來自「最近一個還沒被結算掉的元素」，而不是全部歷史，堆疊就是正確的容器。它的三個操作都是 $O(1)$，而**後進先出的順序恰好編碼了「巢狀」與「就近配對」這兩種結構**。\n\n三種典型形態：\n\n1. **抵銷／退格**：844（比較含退格的字串）的 `#` 刪掉最近打出的字元；2390（從字串中移除星號）的 `*` 刪掉最近的字母。\n2. **巢狀解析**：71（簡化路徑）的 `..` 回到上一層目錄；括號與表示式（4.4、4.5）。\n3. **模擬與結算**：682（棒球比賽）依規則推入與彈出；946（驗證堆疊序列）直接模擬 push/pop。\n\n暴力做法是每次都往回線性搜尋「上一個還有效的元素」，$O(n^2)$。堆疊讓每個元素**最多進出各一次**，總計 $O(n)$——這個「每個元素只進出一次」的攤還論證，會在 4.7 單調堆疊裡以更強的形式再出現一次。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「堆疊」想：\n\n- **需要「最近的那一個」優先處理。** 像 844「比較含退格的字串」，每個退格 `#` 要刪掉最近打出的字元——「最近」就是堆疊的招牌訊號。\n- **元素要成對配對、或要逐步抵銷 / 結算。** 946「驗證堆疊序列」、682「棒球比賽」都是把資料推入再依規則彈出結算。\n\n:::example 嗅到味道：遇到某元素時，要回頭動「最近放入」的那個\n```cpp\n// 嗅到味道：遇到某元素時，要回頭動「最近放入」的那個\n// if (cur == \'#\') st.pop();   // 退格刪掉最近的字元\n```\n:::\n\n\n\n- **暴力做法是反覆往回找「上一個未處理的」（一次 `O(n)`，整體 `O(n^2)`），太慢；** 堆疊讓每個元素只進出一次，均攤 `O(1)`。1441「用堆疊操作構建陣列」更是直接模擬 push/pop。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的棧裡每個元素，代表的是哪一件「還沒處理完的事」？\n2. 遇到新元素時，我用什麼條件決定要 pop 結算，還是 push 等待？\n3. 掃描結束後，棧裡剩下的東西代表什麼？要怎麼用它得出答案？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 71 | 簡化路徑 | N/A | 用堆疊保存尚未匹配或尚未結算的元素；練習每次 push/pop 對不變式的影響。 |\n| 682 | 棒球比賽 | N/A | 用堆疊保存尚未匹配或尚未結算的元素；練習每次 push/pop 對不變式的影響。 |\n| 844 | [比較含退格的字串](https://leetcode.cn/problems/backspace-string-compare/) | 1228 | 用堆疊保存尚未匹配或尚未結算的元素；練習每次 push/pop 對不變式的影響。 |\n| 946 | [驗證堆疊序列](https://leetcode.cn/problems/validate-stack-sequences/) | 1462 | 用堆疊保存尚未匹配或尚未結算的元素；練習每次 push/pop 對不變式的影響。 |\n| 1441 | [用堆疊操作構建陣列](https://leetcode.cn/problems/build-an-array-with-stack-operations/) | 1180 | 用堆疊保存尚未匹配或尚未結算的元素；練習每次 push/pop 對不變式的影響。 |\n\n## 核心想法與直覺\n\n本節介紹「堆疊（stack）」這個最基礎、卻用途極廣的工具。堆疊的規則只有一句話：**後進先出（LIFO）——最後放進去的東西，最先被拿出來。** 想像一疊盤子，你只能從最上面拿，也只能往最上面放。\n\n為什麼這個簡單規則這麼有用？因為很多問題都有「最近、最內層的那一個」要先處理的特性。例如算式裡的括號，一定是「最內層的先配對」；又例如路徑 `a/b/../c`，遇到 `..` 要退回「最近進入的那一層」。堆疊天生就記得「最近放進去的是誰」，正好對上。\n\n:::example 堆疊的三個基本操作（後進先出）\nC++ 直接用 `std::stack`，三個操作都是 `O(1)`：\n\n```cpp\n// 堆疊的三個基本操作（後進先出）\nstack<int> st;\nst.push(3);  // 放入\nst.push(7);\nint x = st.top();  // 看最上面：7（最後放的）\nst.pop();          // 拿掉最上面，之後 top() 變成 3\n```\n:::\n\n\n\n使用堆疊的核心心法是：**棧裡每個元素都代表「一件還沒處理完、等著之後資訊才能結算」的事情。** 處理新資料時，先看棧頂能不能跟它「配對」或「結算」；可以就 `pop` 掉解決掉，不行就 `push` 進去繼續等。\n\n:::example 用棧維護目前所在的目錄層\n以「簡化路徑」為例，把路徑用 `/` 切成一段一段，棧裡放的是「目前還在的目錄名」：\n\n```cpp\n// 用棧維護目前所在的目錄層\nfor (auto& part : segments) {\n    if (part == "" || part == ".") {\n        continue;  // 沒意義，略過\n    } else if (part == "..") {\n        if (!st.empty()) {\n            st.pop();\n        }\n    }  // 退回上一層\n    else {\n        st.push(part);  // 進入新目錄\n    }\n}\n```\n:::\n\n\n\n掃完之後，棧裡由底到頂就是化簡後的路徑。整個過程每段只進出棧一次，總共 `O(n)`。\n\n## 狀態／資料結構定義\n\n**實作選擇。** C++ 的 `std::stack` 預設以 `std::deque` 為底層容器，分段配置，快取表現不佳。**競賽與面試一律建議直接用 `std::vector` 當堆疊**：\n\n| 動作 | `std::stack` | `std::vector` |\n| --- | --- | --- |\n| 推入 | `st.push(x)` | `st.push_back(x)` |\n| 彈出 | `st.pop()`（不回傳值） | `st.pop_back()` |\n| 讀頂 | `st.top()` | `st.back()` |\n| 判空 | `st.empty()` | `st.empty()` |\n| 額外能力 | 無 | **可隨機存取、可遍歷、可 `reserve`** |\n\n「可遍歷」不是小事：4.4、4.7 常需要在結束後檢視整個堆疊內容，`std::stack` 做不到。**若答案是字串，直接用 `std::string` 當堆疊**（`push_back` / `pop_back` / `back`），省掉最後的反轉。\n\n**堆疊裡該放什麼。** 這是設計的核心問題，答案通常是三種之一：\n\n- **元素本身**（844 放字元、682 放分數）；\n- **下標**（4.7 單調堆疊幾乎一律放下標，因為之後要算距離；放值就找不回位置）；\n- **一整包狀態**（4.5 表示式解析放 `{已累積結果, 待乘係數}`；巢狀結構的「現場保存」）。\n\n**經驗法則：如果之後要用到「它在哪裡」，就放下標；只要用到「它是什麼」，才放值。**\n\n## 不變量或正確性證明\n\n**引理 1（71 簡化路徑）。** 以 `/` 切分後逐段處理：空段與 `.` 略過；`..` 彈出棧頂（棧空則忽略）；其餘推入。則處理完前 $k$ 段時，**堆疊由底到頂恰為該前綴所對應的規範絕對路徑的目錄序列**。\n\n*證明（對段數歸納）。* 基底：0 段時堆疊為空，對應根目錄 `/`。歸納步：設前 $k$ 段的規範路徑是 $D_1/\\dots/D_t$。第 $k+1$ 段若是 `.` 或空，語意上不改變當前目錄，堆疊也不變；若是 `..`，語意是移到父目錄，$t \\ge 1$ 時為 $D_1/\\dots/D_{t-1}$（彈出），$t = 0$ 時已在根目錄，父目錄仍是根（忽略）；若是普通名稱 $D$，語意是進入子目錄，變成 $D_1/\\dots/D_t/D$（推入）。三種情形都與操作一致。$\\square$\n\n**這條論證點名了一個前提：輸入是絕對路徑。** 「$t=0$ 時 `..` 可以忽略」只在根目錄沒有父目錄時成立。**若輸入是相對路徑（如 `a/../../b`），開頭多出來的 `..` 必須保留在結果裡**（規範形式是 `../b`），此時堆疊底部要允許堆積 `..`，彈出前得先檢查棧頂是否也是 `..`。同一份程式碼，前提一改就錯——這正是為什麼要把前提寫出來。\n\n**引理 2（946 驗證堆疊序列：貪心彈出是被強制的）。**\n\n演算法：依序推入 `pushed` 的每個元素，每次推入後，只要棧頂等於 `popped[j]` 就彈出並 `j++`。最後檢查堆疊是否為空。\n\n*宣稱*：若某一刻棧頂 $x$ 等於 `popped[j]`，則**立刻彈出是唯一可行的選擇**，不會失去任何解。\n\n*證明。* 假設存在一組合法操作序列 $S$ 在此刻不彈出 $x$，而是先推入若干元素 $y_1, \\dots, y_t$（$t \\ge 1$）。在 $S$ 中，$x$ 遲早要被彈出並成為 `popped[j]`（因為序列合法且元素相異），而彈出 $x$ 之前必須先把 $y_1,\\dots,y_t$ 全部彈出。但那些彈出動作會產生 `popped[j], popped[j+1], \\dots` 中位於 $x$ 之前的輸出——與「$x$ 是下一個要輸出的元素」矛盾。故 $t = 0$，即必須立刻彈出。$\\square$\n\n**這條論證依賴「元素互異」這個前提**（題目明確給出）。若允許重複值，「棧頂等於 `popped[j]`」不再蘊含「棧頂就是那個該彈的元素」，貪心失效。**題目條件裡的『所有元素互不相同』從來不是裝飾品。**\n\n*完備性*：迴圈結束後所有元素都已推入。若仍有殘留且棧頂不等於 `popped[j]`，則該元素永遠無法在正確時機彈出，故回答否。\n\n**引理 3（844 的 $O(1)$ 空間解：由右往左掃描 + skip 計數）。**\n\n從右往左掃描，維護 `skip`（待抵銷的字元數）：遇到 `#` 則 `skip++`；遇到字母時，若 `skip > 0` 則 `skip--` 並丟棄該字母，否則輸出該字母。\n\n*宣稱*：輸出序列恰為最終字串的反轉。\n\n*證明。* 關鍵觀察是**每個 `#` 恰好刪掉它左邊「最近的、尚未被刪的」字母**（這正是退格的定義）。由右往左掃描時，`skip` 的值恆等於「已經看過但尚未找到刪除對象的 `#` 個數」。遇到字母時，若 `skip > 0`，說明右邊還有 `#` 在等一個對象，而這個字母是它們能找到的最近者，故被刪；若 `skip = 0`，則沒有任何 `#` 會刪它，故它存活。存活字母由右往左依序輸出，即為結果的反轉。$\\square$\n\n**這個解法之所以能省掉堆疊，是因為它只需要「還有幾個待抵銷」這個計數，不需要知道被抵銷的是誰。** 這是一個通用的降維觀察：**當堆疊裡的元素全部同質、且只用到「有幾個」時，堆疊可以退化成一個計數器。** 4.4 的括號計數法（20 的單一括號版）用的是同一個道理。\n\n**引理 4（每個元素最多進出各一次 ⇒ 總體 $O(n)$）。** 推入操作至多 $n$ 次（每個元素一次），而彈出次數不超過推入次數，故兩者總和 $\\le 2n$。雖然單次迴圈迭代內的 `while` 可能彈出多個元素，**攤還後總成本仍是 $O(n)$。** 這是本章所有堆疊解法共用的複雜度論證，4.7 會再用一次。\n\n## 逐步演算法\n\n:::example 狀態表示：依題意決定棧裡放什麼\n**1. 狀態表示：想清楚「棧裡一個元素代表什麼」。** 這是最關鍵的一步——可能是一個未配對的括號、一個未結算的下標、一個目錄名。\n\n```cpp\n// 狀態表示：依題意決定棧裡放什麼\nstack<char> st;  // 例如放等待配對的括號\n                                  // stack<int> st;      // 或放等待結算的下標 / 數值\n```\n:::\n\n\n\n:::example 更新：先看棧頂能否處理當前元素，再決定 pop 或 push\n**2. 更新流程：遇到新元素，先問棧頂。** 能配對 / 能結算就 `pop`，否則 `push` 進去等。\n\n```cpp\n// 更新：先看棧頂能否處理當前元素，再決定 pop 或 push\nif (!st.empty() && canSettle(st.top(), cur)) {\n    st.pop();  // 和棧頂結算掉\n} else {\n    st.push(cur);  // 暫存，等之後的資訊\n}\n```\n:::\n\n\n\n:::example 讀取：掃完後處理棧內剩餘元素\n**3. 答案讀取：邊掃邊結算，最後再清棧。** 掃描結束後，棧裡剩下的就是「沒被配對 / 還沒處理」的部分，常用來檢查合法性或組出答案。\n\n```cpp\n// 讀取：掃完後處理棧內剩餘元素\nbool ok = st.empty();  // 例：括號題，棧空才代表全部配對\n                                              // 或從棧底到棧頂組合出最終結果\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 每次 `top()` / `pop()` 前一定先檢查 `!st.empty()`，否則對空棧操作是未定義行為。\n- 棧頂的「語意」要全程固定，不能一下放下標、一下放數值。\n- 複雜度：每個元素最多進棧一次、出棧一次，整體 `O(n)`，空間最壞 `O(n)`。\n\n## C++17 模板\n\n以下用 2434. 使用機器人列印字典序最小的字串 展示堆疊（Stack）的基本 push / pop 不變式的核心寫法。\n\n**完整題目**：有字串 s 與一個一開始為空的機器人字串 t（當作堆疊）。每一步可以把 s 最前面的字元推入 t，或把 t 最上面的字元彈出並寫到答案紙上。當 s 與 t 都清空時，求字典序最小的答案字串。\n\n:::example 貪心 + 單調棧：模擬機器人將 s 轉移到紙上，盡可能讓輸出字典序最小\n**線索**：「先進去的字元要等後面才能決定能不能輸出」就是堆疊 push / pop 的不變式問題：只有當堆疊頂端字元，已經不可能再被後面更小的字元超越時，才把它彈出寫進答案。\n\n```cpp\nclass Solution {\npublic:\n    // 貪心 + 單調棧：模擬機器人將 s 轉移到紙上，盡可能讓輸出字典序最小\n    string robotWithString(string s) {\n            int n = s.size();\n            // suffixMin[i] = s[i..n-1] 中最小字元，用於判斷棧頂是否可以彈出\n            vector<char> suffixMin(n + 1, \'z\' + 1);\n            for (int i = n - 1; i >= 0; --i) {\n                    suffixMin[i] = min(suffixMin[i + 1], s[i]);\n            }\n            string ans;\n            vector<char> st;  // 模擬機器人的手（暫存字串 t）\n            for (int i = 0; i < n; ++i) {\n                    st.push_back(s[i]);  // 從 s 取字元放入手中\n                    // 若棧頂字元 <= 後綴最小值，彈出不會造成字典序變大，貪心輸出\n                    while (!st.empty() && st.back() <= suffixMin[i + 1]) {\n                            ans.push_back(st.back());\n                            st.pop_back();\n                    }\n            }\n            // s 已全部處理，將剩餘字元依序輸出\n            while (!st.empty()) {\n                    ans.push_back(st.back());\n                    st.pop_back();\n            }\n            return ans;\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- `suffixMin[i]` 是 `s[i..n - 1]` 的最小字元，代表「之後還可能進來的最小值」。\n- `st` 就是機器人字串 t；推入目前字元後，立刻判斷能不能輸出。\n- `while` 的不變式：堆頂只要 `<=` 後面剩餘的最小字元，現在輸出一定不劣，就彈出寫進答案。\n- 掃完 s 後，把堆中殘餘字元全部彈出，這些一定是遞減排好的安全輸出。\n\n## 時間與空間複雜度\n\n**時間** $O(n)$（引理 4 的攤還論證）。**空間** $O(n)$——最壞情況下所有元素都留在堆疊裡（例如 844 的輸入沒有任何 `#`）。\n\n**能否省到 $O(1)$？** 只有當「堆疊內容可以用常數個數值概括」時才行：\n- 844 由右往左掃 ⇒ 一個 `skip` 計數，$O(1)$（引理 3）；\n- 20 若只有一種括號 ⇒ 一個平衡計數器，$O(1)$；有三種括號就不行（要記住種類與順序）。\n\n**判準：堆疊裡的元素是否同質、是否只用到「數量」。** 這條判準在 4.4 會被反覆使用。\n\n**常數層面**：`std::vector` 比 `std::stack`（底層 `deque`）快，且能 `reserve(n)` 避免重配置。字串題直接用 `std::string` 當堆疊，最後不必反轉。\n\n## 常見錯誤與邊界條件\n\n- 想清楚「彈出條件」對應的不變式，是堆疊題的核心，不要憑感覺寫 `while`。\n- 後綴最小值要先預處理，否則每次重算會退化成 `O(n^2)`。\n- 收尾別忘了把堆疊清空，殘留元素也是答案的一部分。\n\n## 常見變形\n\n- 括號 / 標籤匹配：用堆疊保存未配對的開符號，遇到閉符號時彈出檢查。\n- 鄰項消除（見 4.3）：堆頂與當前元素可消除時就 pop，否則 push。\n- 單調堆疊：把「之後最小值」換成單調性條件，即可解下一個更大元素等問題。\n\n## 與相似技巧的比較\n\n- **與遞迴**：遞迴的呼叫堆疊就是一個隱式堆疊。**任何遞迴都能改寫成顯式堆疊**，好處是不受遞迴深度限制（$n = 10^5$ 的鏈狀結構會爆棧），代價是要手動保存「現場」。4.5 的表示式求值就是把遞迴下降改寫成顯式堆疊的典型。\n- **與佇列（5.x）**：堆疊處理「最近的」，佇列處理「最早的」。**BFS 用佇列、DFS 用堆疊**，這個對應在圖論裡直接決定搜尋順序。\n- **與雙端佇列（5.3）**：兩端都能進出。若只用一端就是堆疊，只用「一端進、另一端出」就是佇列。**需要兩端操作（如 4.6 對頂堆疊、5.4 單調佇列）才值得用 `deque`。**\n- **與單調堆疊（4.7）**：單調堆疊是「堆疊 + 額外的單調性不變量」。**本節的堆疊只保證 LIFO；4.7 額外保證棧內元素單調遞增（或遞減），這個不變量才是它能回答「下一個更大元素」的原因。**\n- **與 4.3 鄰項消除**：4.3 是本節「抵銷」形態的專章，差別在於消除後**新產生的相鄰對還會繼續消**（1047、1209），需要在推入前先與棧頂比對。\n- **與計數器**：見引理 3 與複雜度一節——同質元素只問數量時，堆疊可降級為計數器。**先問「我真的需要記住是誰嗎」，能不用堆疊就別用。**\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 71 | [簡化路徑](https://leetcode.cn/problems/simplify-path) |  |\n| 682 | [棒球比賽](https://leetcode.cn/problems/baseball-game) |  |\n| 844 | [比較含退格的字串](https://leetcode.cn/problems/backspace-string-compare) | 1228 |\n| 946 | [驗證堆疊序列](https://leetcode.cn/problems/validate-stack-sequences) | 1462 |\n| 1441 | [用堆疊操作構建陣列](https://leetcode.cn/problems/build-an-array-with-stack-operations) | 1180 |\n| 1472 | [設計瀏覽器歷史記錄](https://leetcode.cn/problems/design-browser-history) | 1454 |\n| 2390 | [從字串中移除星號](https://leetcode.cn/problems/removing-stars-from-a-string) | 1348 |\n| 3412 | [計算字串的映象分數](https://leetcode.cn/problems/find-mirror-score-of-a-string) | 1578 |\n\n## 本節重點速查\n\n- 堆疊只回答一件事：**「最近的那一個是誰？」** 三種形態：抵銷／退格、巢狀解析、模擬結算。\n- 實作用 `std::vector`（可遍歷、可 `reserve`、快取友善），字串題用 `std::string`；避免 `std::stack`。\n- **放下標還是放值**：之後要用「在哪裡」就放下標（4.7 幾乎一律如此），只用「是什麼」才放值。\n- 71 的不變量：堆疊 = 當前前綴的規範目錄序列。**前提是絕對路徑**；相對路徑時開頭的 `..` 必須保留。\n- 946 的貪心是**被強制的**（棧頂就是下一個要輸出的元素，不立刻彈就永遠彈不到），**前提是元素互異**。\n- 844 的 $O(1)$ 解：由右往左 + `skip` 計數。**堆疊元素同質且只問數量時，可退化為計數器。**\n- 複雜度 $O(n)$ 來自「每個元素最多進出各一次」的攤還論證，內層 `while` 不影響總量。\n- 遞迴 = 隱式堆疊；深度過大時改寫成顯式堆疊。\n',
        },
        {
          id: 29,
          title: "4.2 進階",
          summary:
            "## 這個技術解決什麼問題\n\n本節是「設計資料結構」型的堆疊題：除了 push／pop，還要 $O(1)$ 或 $O(\\log n)$ 回答某個額外查詢——最小值（155）、最大值（716）、出現頻率最高的元素（895），或維護一組堆疊（1172）。\n\n這類題目的共同槓桿是一個結構性事實：\n\n> **後進先出讓「歷史狀態」天然被保存。** 彈出第 $k$ 個元素之後，系統回到的正是推入它之前的狀態；而那個狀態就躺在堆疊的下一格。\n\n換句話說，**只要那個統計量可以逐格保存，就能 $O(1)$ 回退**。這是堆疊獨有的性質：佇列（先進先出）刪掉的是最舊的元素，剩下的元素從來沒有一起出現過某個「歷史狀態」，所以同樣的問題在佇列上就要用單調雙端佇列（5.4）或兩個堆疊來湊。**「為什麼堆疊做得到而佇列做不到」是本節最值得帶走的一句話。**\n\n636（函式的獨佔時間）則是另一種形態：**堆疊直接模擬真實世界的呼叫堆疊**，因為日誌本身就是良巢狀的。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「進階堆疊」想：\n\n- **是一道「設計資料結構」題，要支援 push / pop，並額外 `O(1)` 回答某統計量。** 155「最小堆疊」、716「最大堆疊」就是直接點名要查最小 / 最大值。\n- **統計量會隨著 push / pop「自然回退」。** 因為後進先出，舊狀態天生被保留在下一格：\n\n:::example 嗅到味道：pop 之後，要查的值該「自動回到」上一個狀態\n```cpp\n// 嗅到味道：pop 之後，要查的值該「自動回到」上一個狀態\n// pop();  // 最小值不必重算，退回 st.top().second 即可\n```\n:::\n\n\n\n- **要按「出現次數」或「某種優先順序」加上後進先出取出元素。** 895「最大頻率堆疊」、1172「餐盤堆疊」、636「函式的獨佔時間」都是在堆疊上疊加額外維度（頻率、位置、時間）。\n- **暴力做法是每次查詢都重掃整個棧（`O(n)`），太慢；** 進階堆疊把它均攤成 `O(1)`。\n\n確認自己有沒有套對，問三個問題：\n\n1. 除了「最上面是誰」，我還要 `O(1)` 回答什麼統計量？\n2. 這個統計量在 push 時怎麼算、在 pop 時會不會自動退回？\n3. 我是要在每格「隨行記錄」，還是要「分層 / 開輔助棧」來維護它？\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n本節是堆疊的進階用法。基礎堆疊只關心「最上面是誰」，但很多題目還要在 push / pop 之間隨時回答額外問題，例如「現在棧裡的最小值是多少？」「同一個數出現最多次的、最靠近頂端的是哪個？」這些都要 `O(1)` 答出來。\n\n直覺會想：要查最小值，掃一遍整個棧不就好了？但那是 `O(n)`，題目要求每次操作都 `O(1)`。進階堆疊的核心技巧是：**在棧裡同時「順便」維護你要查的那個統計量，讓它跟著元素一起進、一起出。**\n\n:::example 進階堆疊：每格同時記「值」與「此刻的最小值」\n以「最小堆疊」（LeetCode 155）為例，要 `O(1)` 取得當前最小值。做法是棧裡每個元素除了存值，還存「包含自己在內、到目前為止的最小值」：\n\n```cpp\n// 進階堆疊：每格同時記「值」與「此刻的最小值」\nstack<pair<int, int>> st;  // {值, 含自己的最小值}\nvoid push(int x) {\n    int mn = st.empty() ? x : min(x, st.top().second);\n    st.push({x, mn});  // 把最小值一起塞進這一格\n}\nint getMin() { return st.top().second; }  // O(1)\n```\n:::\n\n\n\n關鍵在於：因為堆疊後進先出，「目前的最小值」一定也跟著棧頂走——`pop` 掉一個元素，最小值自動回到上一格記的值，不必重算。\n\n:::example 分層堆疊：第 f 層存「出現第 f 次」的元素\n更進階的「最大頻率堆疊」（895）則是用「分層的堆疊」：把出現第 1 次的數放第 1 層、第 2 次放第 2 層……取出最高頻時，就從「最高那一層」的棧頂拿。\n\n```cpp\n// 分層堆疊：第 f 層存「出現第 f 次」的元素\nunordered_map<int, int> cnt;  // 每個值的出現次數\nunordered_map<int, stack<int>>\n    group;  // 層號 -> 該層的棧\n            // push x：cnt[x]++ 後，把 x 放到 group[cnt[x]]\n```\n:::\n\n\n\n這樣 pop 時只要看「最高層的棧頂」，就是「頻率最高、且其中最晚加入」的那個，依然 `O(1)`～`O(log)`。\n\n## 狀態／資料結構定義\n\n**155 最小堆疊——寫法 A（輔助堆疊，推薦）**\n- `std::vector<int> st`：主堆疊。\n- `std::vector<int> mn`：同步堆疊，`mn[i]` = `st[0..i]` 的最小值。\n- push：`st.push_back(x); mn.push_back(mn.empty() ? x : min(mn.back(), x));`\n- pop：兩個一起 `pop_back()`。\n- getMin：`mn.back()`。\n- 空間 $O(n)$，但邏輯簡單、幾乎不會寫錯。**面試預設寫這個。**\n\n**155 寫法 B（差值編碼，$O(1)$ 額外空間）**\n- 只存一個 `long long` 的 `mn`，堆疊裡存的不是 $x$ 而是編碼值。\n- push $x$：若 `st.empty()`，`mn = x`，推入 $x$；否則若 $x < mn$，推入 $2x - mn$（一個**小於 $x$** 的哨兵），再令 `mn = x`；否則直接推入 $x$。\n- pop：若 `st.back() < mn`，說明它是哨兵，還原 `mn = 2*mn - st.back()`；再彈出。\n- top：若 `st.back() < mn` 回傳 `mn`，否則回傳 `st.back()`。\n- **必須用 `long long`**：$2x - mn$ 在 $x, mn$ 皆為 $\\pm2^{31}$ 量級時會溢位 `int`。\n\n**716 最大堆疊**：需要 `popMax()`（刪除任意位置的最大元素），輔助堆疊法失效（它只能回退，不能挖中間）。標準做法是**雙向鏈結串列 + 有序集合**：`std::list<int>` 保存序列，`std::map<int, vector<list::iterator>>` 按值索引位置。`popMax` 取 `map` 的最後一個鍵、取其最後一個迭代器、從 `list` 中 $O(1)$ 刪除。**所有操作 $O(\\log n)$。**\n\n**895 最大頻率堆疊**\n- `unordered_map<int,int> cnt`：每個值目前的出現次數。\n- `unordered_map<int, vector<int>> group`：`group[f]` = 「曾經達到過次數 $f$」的元素，按達到的時間順序排列。\n- `int maxF`：目前最大次數。\n- push $x$：`int c = ++cnt[x]; group[c].push_back(x); maxF = max(maxF, c);`\n- pop：`int x = group[maxF].back(); group[maxF].pop_back(); --cnt[x]; if (group[maxF].empty()) --maxF; return x;`\n- **注意這裡完全沒有「重新排序」或「堆」，全是 $O(1)$ 的陣列尾端操作。**\n\n**636 函式的獨佔時間**：`vector<int> st`（正在執行的函式 id 鏈）+ `int prev`（上一個時間切點）+ `vector<int> ans`。\n\n**1172 餐盤堆疊**：`vector<vector<int>> stacks` + `priority_queue<int, vector<int>, greater<>> avail`（未滿堆疊的下標，允許有過期項，懶刪除）。\n\n## 不變量或正確性證明\n\n**引理 1（155 輔助堆疊的正確性）。** 恆有 $\\texttt{mn}[i] = \\min\\{\\texttt{st}[0], \\dots, \\texttt{st}[i]\\}$。\n\n*證明（對操作次數歸納）。* push $x$ 時新增 $\\texttt{mn}[i] = \\min(\\texttt{mn}[i-1], x) = \\min(\\min_{t<i}\\texttt{st}[t],\\ x) = \\min_{t \\le i}\\texttt{st}[t]$；pop 只是移除最後一項，不影響前綴的定義。$\\square$\n\n**這條不變量成立的前提是「刪除的一定是最後推入的元素」。** 前綴最小值只能整格丟棄、不能從中間挖掉——這正是 716 的 `popMax` 讓輔助堆疊法失效的原因，也是為什麼「最小佇列」需要完全不同的機制（5.4 單調佇列）。**同一個統計量，容器的刪除語意一改，方法就得全換。**\n\n**引理 2（155 差值編碼的正確性）。** 設推入 $x$ 時的舊最小值為 $\\mu$。\n\n*(a) 哨兵可辨識*：只有在 $x < \\mu$ 時才推入 $2x - \\mu$。此時 $2x - \\mu < x < \\mu = $ 更新後的 `mn`... 更精確地說，更新後 `mn` $= x$，而存入的值 $2x - \\mu < x$（因 $x < \\mu$）。反之，若 $x \\ge \\mu$，存入的是 $x \\ge \\mu = $ 更新後的 `mn`。故「存入值 $<$ 當前 `mn`」**恰好**刻畫「這一格是哨兵」，沒有偽陽性也沒有偽陰性。\n\n*(b) 可還原*：哨兵 $v = 2x - \\mu$ 且當前 `mn` $= x$，故 $\\mu = 2x - v = 2\\cdot\\texttt{mn} - v$，彈出時據此還原。$\\square$\n\n**這個技巧的代價是把值域擴大一倍**——$2x - \\mu$ 可能到 $\\pm 2^{32}$。**寫 `int` 會 UB，這不是理論顧慮：LeetCode 155 的測資包含 $\\pm 2^{31}$ 級的輸入。**\n\n**引理 3（895 最大頻率堆疊）。** 維持兩條不變量：\n\n- (I1) `group[f]` 恰好包含所有「當前次數 $\\ge f$」的元素，且每個元素在其中出現的位置對應它**第 $f$ 次被 push 的時刻**（按時間遞增）。\n- (I2) `maxF` = 當前所有元素次數的最大值。\n\n*(I1) 的維持*：push $x$ 使其次數變成 $c$，此時且僅此時 $x$ 第 $c$ 次被推入，於是加入 `group[c]`；它在 `group[1..c-1]` 中的紀錄不需變動。pop 時從 `group[maxF]` 取走 $x$ 的最後一筆紀錄並令次數減一，恰好抵銷。\n\n*正確性（彈出的是對的元素）*：pop 要取「次數最大者；若並列，取最靠近棧頂（最晚推入）者」。\n\n由 (I2)，答案的次數是 `maxF`。由 (I1)，`group[maxF]` 中的元素當前次數皆 $\\ge$ `maxF`，而 `maxF` 已是最大值，故**恰為 `maxF`**。於是對 `group[maxF]` 中的每個元素 $y$，它的第 `maxF` 次推入就是它的**最後一次**推入。因此 `group[maxF]` 的排列順序（按第 `maxF` 次推入的時間）等同於按「最後一次推入的時間」排序，取尾端就是最晚被推入的那個——正是題目要的並列規則。$\\square$\n\n**這個資料結構的巧妙之處在於：它不做任何排序，卻靠「按達到的次數分層」把並列規則自動化了。** 關鍵前提是上一段那句「`group[maxF]` 中的元素次數恰為 `maxF`」——若沒有 (I2)，這句話不成立，尾端就不必然是答案。\n\n*為什麼 `--maxF` 只要減一就夠*：pop 讓某個元素次數從 `maxF` 降到 `maxF - 1`。若 `group[maxF]` 因此變空，表示沒有元素還維持在 `maxF`，而先前所有元素次數 $\\le$ `maxF`，故新的最大值恰為 `maxF - 1`（`group[maxF-1]` 必非空，因為剛剛那個元素還在裡面）。**這是為什麼不需要重新掃描求最大值。**\n\n**引理 4（636 的獨佔時間結算）。** 不變量：`st` 由底到頂 = 當前正在執行的呼叫鏈；`prev` = 上一個已結算到的時間點。\n\n- `start id at t`：區間 $[\\texttt{prev},\\ t-1]$ 屬於當前棧頂（若有），故 `ans[st.back()] += t - prev`；然後 `st.push_back(id); prev = t;`\n- `end id at t`：區間 $[\\texttt{prev},\\ t]$ 屬於棧頂（`end` 的時間戳是**閉的**，該單位時間仍在執行），故 `ans[st.back()] += t - prev + 1; st.pop_back(); prev = t + 1;`\n\n*正確性*：每個單位時間恰好被結算一次，且被結算給「當時位於棧頂的函式」——棧頂正是當時真正在執行（未被子呼叫佔用）的那個函式。**`start` 用半開、`end` 用閉，這個不對稱來自題目的時間戳定義，不是實作偏好；搞混就會系統性地差 1。**\n\n**這個解法依賴一個很強的前提：日誌是良巢狀的**（每個 `end` 對應最近未關閉的 `start`）。堆疊之所以夠用，正是因為巢狀。若允許任意交錯（A 開始、B 開始、A 結束、B 結束），堆疊模型直接崩潰，得改用「事件排序 + 區間拆分」。\n\n**引理 5（1172 的懶刪除）。** `avail` 堆中可能含有「已經被填滿」的堆疊下標（因為填滿時不方便從堆中刪除）。維持較弱的不變量：\n\n> **`avail` 是「所有未滿堆疊下標」的超集合。**\n\n`push` 時反覆 `pop` 堆頂直到找到一個真正未滿的下標（或堆空則新開一個）。**弱不變量 + 使用時驗證**是懶刪除的標準形式，6.6 會專章討論。*攤還正確性*：每個過期項只會被丟棄一次，故總丟棄次數不超過總插入次數。\n\n## 逐步演算法\n\n:::example 狀態表示：值 + 隨行統計（這裡是最小值）\n**1. 狀態表示：主棧 + 隨行的輔助資訊。** 想清楚要查什麼，就讓棧裡每格帶著那個資訊，或另開一個輔助結構同步維護。\n\n```cpp\n// 狀態表示：值 + 隨行統計（這裡是最小值）\nstack<pair<int, int>> st;  // {value, minSoFar}\n```\n:::\n\n\n\n:::example 更新：push 時把新統計算好一起放入\n**2. 更新流程：push 時算好、一起塞；pop 時自動退回。** 新元素的統計量由「它自己」和「舊棧頂的統計量」合併而來。\n\n```cpp\n// 更新：push 時把新統計算好一起放入\nint mn = st.empty() ? x : min(x, st.top().second);\nst.push({x, mn});  // pop 時不必重算，退回上一格即可\n```\n:::\n\n\n\n:::example 讀取：O(1) 取出維護好的統計量\n**3. 答案讀取：直接讀棧頂帶的資訊。** 不再掃整個棧。\n\n```cpp\n// 讀取：O(1) 取出維護好的統計量\nint getMin() { return st.top().second; }\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 空棧時呼叫 `top()` / `getMin()` 會出錯，介面要先保證棧非空。\n- 分層堆疊 pop 後若某層空了，記得把那層拿掉，避免讀到空棧。\n- 複雜度：每個操作 `O(1)`（最小堆疊）或 `O(log)`（涉及層數的設計），空間 `O(n)`。\n\n## C++17 模板\n\n以下用 895. 最大頻率堆疊 展示堆疊的進階應用（分層堆疊）的核心寫法。\n\n**完整題目**：設計 FreqStack，支援 `push(val)` 與 `pop()`。`pop()` 要移除並回傳目前出現頻率最高的元素；若有多個頻率相同，回傳最接近堆頂（最晚被 push）的那一個。\n\n:::example 頻率棧：每次 pop 回傳當前頻率最高（且最晚入棧）的元素\n**線索**：需求同時牽涉「頻率最高」與「同頻率時最靠近堆頂」。把每個頻率層各開一個堆疊，就能用堆疊的 LIFO 天然處理「同頻率取最新」，再用 `maxFreq` 指向最高層即可。\n\n```cpp\n// 頻率棧：每次 pop 回傳當前頻率最高（且最晚入棧）的元素\nclass FreqStack {\n    unordered_map<int, int> freq;  // freq[val] = val 目前的頻率\n    unordered_map<int, vector<int>>\n                    group;        // group[f] = 頻率恰好為 f 時依序入棧的元素\n    int maxFreq = 0;  // 當前最大頻率\n\npublic:\n    void push(int val) {\n            int f = ++freq[val];        // 更新 val 的頻率\n            maxFreq = max(maxFreq, f);  // 維護最大頻率\n            group[f].push_back(val);    // 加入對應頻率的群組\n    }\n\n    int pop() {\n            int val = group[maxFreq].back();  // 取最高頻率群組的最頂元素\n            group[maxFreq].pop_back();\n            --freq[val];  // 更新該元素的頻率\n            if (group[maxFreq].empty()) {\n                    --maxFreq;  // 最高頻率群組已空，降低 maxFreq\n            }\n            return val;\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- `freq[val]` 記錄每個值目前的出現次數。\n- `group[f]` 是「出現次數恰為 f 的元素」的堆疊，越晚 push 越靠後。\n- `push` 時把 val 放進第 `f` 層，並更新 `maxFreq`。\n- `pop` 永遠從 `maxFreq` 這層的堆頂取，天然滿足「同頻率取最新」；該層空了就把 `maxFreq` 退一層。\n\n## 時間與空間複雜度\n\n| 結構 | 操作 | 時間 | 額外空間 |\n| --- | --- | --- | --- |\n| 155 輔助堆疊 | push/pop/top/getMin | $O(1)$ | $O(n)$ |\n| 155 差值編碼 | push/pop/top/getMin | $O(1)$ | $O(1)$ |\n| 716 鏈結串列 + `map` | push/pop/top/peekMax/popMax | $O(\\log n)$ | $O(n)$ |\n| 895 分層 | push/pop | $O(1)$ 攤還 | $O(n)$ |\n| 636 | 整體 | $O(\\text{logs})$ | $O(\\text{depth})$ |\n| 1172 懶刪除堆 | push/pop/popAtStack | $O(\\log n)$ 攤還 | $O(n)$ |\n\n**895 為什麼是攤還 $O(1)$ 而非最壞 $O(1)$**：`unordered_map` 的雜湊在最壞情況會退化，且 `group[c]` 的 `vector` 有可能觸發重配置。實務上把 `group` 換成 `vector<vector<int>>`（下標即次數，因為次數 $\\le$ push 總數）可以去掉雜湊，更快。\n\n**716 為什麼不能 $O(1)$**：`popMax` 需要在任意位置刪除並維持「最大值」可查。這在比較模型下等價於優先佇列，$\\Omega(\\log n)$ 的下界來自排序下界。**看到「刪除任意位置的極值」就別再想 $O(1)$。**\n\n**155 的兩種寫法怎麼選**：差值編碼省空間但常數更大（每次 top 都要比較與可能的還原），且有溢位陷阱。**$n$ 不大時輔助堆疊更好；面試時先寫輔助堆疊、再口頭補充差值編碼，是最穩的答法。**\n\n## 常見錯誤與邊界條件\n\n- `maxFreq` 只會在當前層被清空時下降，不需要重新掃描所有層。\n- 用 `vector` 當堆疊，`push_back` / `pop_back` / `back` 即可，避免 `std::stack` 的封裝開銷。\n- `pop` 後記得同步 `--freq[val]`，否則之後的頻率判斷會錯。\n\n## 常見變形\n\n- 把「頻率」換成其他可分層的鍵（例如優先級、版本），即可重用分層堆疊。\n- 若要支援查詢而非彈出，可改成分層的可索引結構。\n- 結合雜湊與堆疊，是許多「O(1) 設計題」的通用骨架。\n\n## 與相似技巧的比較\n\n- **與 4.1 基礎堆疊**：本節多的是「額外統計量」。判準是：**這個統計量能不能隨 pop 自然回退？** 能（min/max）就同步堆疊；不能（任意刪除）就得配資料結構。\n- **與 5.4 單調佇列**：「滑動視窗最小值」是同樣的問題搬到佇列上。因為佇列刪的是最舊元素，前綴最小值無法回退，必須改用「單調雙端佇列」——**同一個查詢，容器換了，方法完全不同**，這個對照最能說明本節開頭那句話。\n- **與 5.2 用兩個堆疊實作佇列**：反過來的方向。「最小佇列」的經典解法正是「兩個最小堆疊」——用 4.2 的結構去補 5.x 的不足。\n- **與 6.x 堆**：716 與 1172 都用到堆或有序集合。**堆疊只能回退，堆能取極值；需要「任意順序取極值」就必須付 $O(\\log n)$。**\n- **與 6.6 懶刪除堆**：1172 是懶刪除的預告。**弱不變量（超集合）+ 使用時驗證**這個模式，在 6.6、Dijkstra、以及各種「無法從堆中刪除中間元素」的場合反覆出現。\n- **與 895 的替代解法**：也可以用 `priority_queue<tuple<int,int,int>>`（次數、序號、值）$O(\\log n)$。**分層法之所以能 $O(1)$，是因為它利用了「次數每次只 $\\pm 1$」這個離散結構——與計數排序優於比較排序是同一個道理。**\n\n## 代表例題\n\n**題號**：155、636、716、895、1172\n\n用堆疊保存尚未匹配或尚未結算的元素；練習每次 push/pop 對不變式的影響。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 155 | [最小堆疊](https://leetcode.cn/problems/min-stack) |  |\n| 636 | [函式的獨佔時間](https://leetcode.cn/problems/exclusive-time-of-functions) |  |\n| 716 | [最大堆疊](https://leetcode.cn/problems/max-stack)（Premium） |  |\n| 895 | [最大頻率堆疊](https://leetcode.cn/problems/maximum-frequency-stack) | 2028 |\n| 1172 | [餐盤堆疊](https://leetcode.cn/problems/dinner-plate-stacks) | 2110 |\n| 1381 | [設計一個支援增量操作的堆疊](https://leetcode.cn/problems/design-a-stack-with-increment-operation) | 1286 |\n| 2434 | [使用機器人列印字典序最小的字串](https://leetcode.cn/problems/using-a-robot-to-print-the-lexicographically-smallest-string) | 1953 |\n| 2524 | [子陣列的最大頻率分數](https://leetcode.cn/problems/maximum-frequency-score-of-a-subarray) |  |\n| 2589 | [完成所有任務的最少時間](https://leetcode.cn/problems/minimum-time-to-complete-all-tasks) | 2381 |\n| 3170 | [刪除星號以後字典序最小的字串](https://leetcode.cn/problems/lexicographically-minimum-string-after-removing-stars) | 1772 |\n\n## 本節重點速查\n\n- **堆疊的獨有性質：LIFO 讓歷史狀態天然保存，統計量可以逐格 $O(1)$ 回退。** 佇列沒有這個性質（要用 5.4 單調佇列）。\n- 155 輔助堆疊：`mn[i] = min(st[0..i])`，push/pop 同步。**前提是只從尾端刪除。**\n- 155 差值編碼：$x < \\mu$ 時存 $2x - \\mu$；「存入值 $<$ 當前 `mn`」恰好刻畫哨兵。**一定要 `long long`。**\n- 716 需要 `popMax`（挖中間），輔助堆疊失效 ⇒ 鏈結串列 + `map`，$O(\\log n)$，且有下界。\n- 895 分層：`group[c]` 記錄「第 $c$ 次推入」的時刻順序。因 `group[maxF]` 內元素次數恰為 `maxF`，其尾端就是最晚推入者——**並列規則被結構自動滿足**。`maxF` 只需 $-1$。\n- 636：棧頂 = 當前實際執行者；**`start` 結算 $t - \\texttt{prev}$、`end` 結算 $t - \\texttt{prev} + 1$**，不對稱來自時間戳定義。**前提是日誌良巢狀。**\n- 1172：懶刪除，維持「`avail` 是未滿下標的超集合」這條**弱不變量**，使用時再驗證。\n- 設計題的通用問法：**這個量能否隨 pop 自然回退？** 能 ⇒ 同步堆疊 $O(1)$；不能 ⇒ 配堆或有序集合 $O(\\log n)$。\n",
        },
        {
          id: 30,
          title: "4.3 鄰項消除",
          summary:
            "## 這個技術解決什麼問題\n\n一類題目長成這樣：**「只要相鄰的兩個（或 $k$ 個）元素滿足某條件就把它們刪掉，刪完之後左右接起來可能又形成新的可刪對，重複直到不能刪為止。」**\n\n1047（刪除字串中的所有相鄰重複項）刪相鄰相同字元；1544（整理字串）刪相鄰的大小寫配對；1003（檢查替換後的詞是否有效）刪子串 `abc`；1209 刪 $k$ 個相鄰相同；735（行星碰撞）刪掉碰撞中較小的行星。\n\n暴力是「掃一遍找到一處就刪，然後從頭再掃」，每次刪除 $O(n)$、最多刪 $O(n)$ 次，$O(n^2)$。\n\n堆疊把它壓成 $O(n)$，關鍵是一個結構性事實：**刪除之後新產生的左鄰居，恰好就是棧頂。** 於是「回頭找新的可刪對」這件事變成 $O(1)$ 的 `st.back()`。\n\n但在寫程式之前，有一個**必須先問清楚的問題**：由左至右、每次只跟棧頂比對，得到的結果和「以其他順序刪除」得到的結果，一定相同嗎？如果不同，貪心就沒有意義。下一節會證明：對大部分題目答案是「相同」，而對少數題目（1717）答案是「不同，而且順序決定分數」。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「鄰項消除」想：\n\n- **「相鄰的兩個元素滿足條件就刪除 / 合併」，而且刪完還會引發新的相鄰消除。** 1047、1544「整理字串」都是相鄰相同（或大小寫成對）就消。\n- **消除後「左右接起來」的新鄰居要能 `O(1)` 找到。** 堆疊的棧頂正好就是那個左鄰居：\n\n:::example 嗅到味道：刪掉中間後，需要立刻知道「新的左邊是誰」\n```cpp\n// 嗅到味道：刪掉中間後，需要立刻知道「新的左邊是誰」\n// st.top();   // 棧頂永遠是當前倖存的最右元素\n```\n:::\n\n\n\n- **括號 / 配對型的逐步抵銷。** 1003「檢查替換後的詞是否有效」用棧不斷消掉 `abc` 子串，正是消除思路。\n- **暴力做法是消一次就重掃整串找下一處（`O(n^2)`），太慢；** 堆疊讓每個元素只進出一次，`O(n)`。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的「相鄰消除條件」是什麼？兩個元素湊在一起時誰會消失？\n2. 消掉之後，新的左鄰居是不是就是現在的棧頂？\n3. 掃完後，棧裡剩下的順序要不要反轉才是答案？\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n本節談堆疊的一個經典套路：「鄰項消除」。場景是這樣——掃描一個序列，**相鄰的兩個元素如果符合某種條件，就會互相抵銷 / 合併 / 爆掉**；抵銷後，原本不相鄰的左右兩邊又變成新的鄰居，可能再次觸發消除，像連鎖反應一樣。\n\n最直白的例子是「刪除字串中所有相鄰重複項」（LeetCode 1047）：`abbaca` 裡 `bb` 相鄰相同，刪掉變 `aaca`，這時 `aa` 又相鄰，再刪變 `ca`。問題是：刪掉中間一段後，要怎麼快速知道「新的左鄰居是誰」？\n\n:::example 鄰項消除骨架：棧頂就是當前的「左鄰居」\n答案就是堆疊。**棧頂永遠是「目前還活著的最右一個元素」，也就是新元素的左鄰居。** 拿新元素跟棧頂比一下：能消就 `pop`（兩個一起消失），不能消就 `push`（成為新的最右）。連鎖消除自然發生，因為消掉棧頂後，下一個棧頂又是新的左鄰居。\n\n```cpp\n// 鄰項消除骨架：棧頂就是當前的「左鄰居」\nstack<char> st;\nfor (char c : s) {\n    if (!st.empty() && st.top() == c) {\n        st.pop();  // 相鄰相同 → 一起消掉\n    } else {\n        st.push(c);  // 否則留下，成為新棧頂\n    }\n}\n// 棧底到棧頂就是消除後剩下的字串\n```\n:::\n\n\n\n:::example 帶計數的消除：棧裡存 {字元, 連續出現次數}\n帶計數的版本（1209「相鄰重複項 II」，連續 k 個才消）只要在棧裡多存一個次數：\n\n```cpp\n// 帶計數的消除：棧裡存 {字元, 連續出現次數}\nstack<pair<char, int>> st;\nfor (char c : s) {\n    if (!st.empty() && st.top().first == c) {\n        if (++st.top().second == k) {\n            st.pop();  // 湊滿 k 個就整組消掉\n        }\n    } else {\n        st.push({c, 1});\n    }\n}\n```\n:::\n\n\n\n「小行星碰撞」（735）則是用正負號和大小決定誰爆炸，本質一樣：新來的去跟棧頂「對撞」，輸的消失、贏的可能繼續撞下一個棧頂。\n\n## 狀態／資料結構定義\n\n**標準骨架（1047、1544、1003、2696）**：一個堆疊，掃描時\n```\nfor each x in s:\n    if (堆疊非空 && 可以與棧頂消除) 彈出棧頂（並丟棄 x）\n    else 推入 x\n```\n字串題直接用 `std::string` 當堆疊，最後不必反轉。\n\n**消除條件依題而異，這是唯一要改的地方**：\n\n| 題目 | 消除條件 | 消除動作 |\n| --- | --- | --- |\n| 1047 | `x == st.back()` | 彈出棧頂，丟棄 `x` |\n| 1544 | `(x ^ st.back()) == 32`（同字母異大小寫） | 同上 |\n| 2696 | 棧頂為 `A` 且 `x == 'B'`（或 `C`/`D`） | 同上 |\n| 1003 | `x == 'c'` 且棧頂兩格為 `\"ab\"` | 彈出兩格 |\n| 1209 | `x == st.back().ch` 且計數達 $k$ | 整組彈出 |\n\n**1209 需要壓縮表示**：堆疊元素是 `pair<char,int>`（字元 + 連續出現次數），而不是單一字元。推入時若與棧頂同字元則 `++count`，`count == k` 時整組彈出。**用「字元 + 次數」而非「$k$ 個字元」是為了讓 $k$ 很大時仍是 $O(n)$ 空間，也讓判定變成 $O(1)$ 比較而非 $O(k)$。**\n\n**735 行星碰撞的堆疊語意不同**：堆疊裡放的是「已經穩定、彼此永遠不會再碰撞」的行星。正號表示向右、負號表示向左。推入新行星時要用 `while` 迴圈連續處理（一顆左行行星可能連撞掉好幾顆右行行星），且有三種結局（新的被撞毀 / 棧頂被撞毀 / 兩者同歸於盡），**必須用一個 `alive` 旗標或 `goto`-free 的結構清楚表達，否則很容易漏掉「新行星已毀就不該推入」這一支。**\n\n## 不變量或正確性證明\n\n### 定理 1（消除的合流性：結果與刪除順序無關）\n\n這是本節所有貪心解法的地基。用改寫系統的語言：把「刪除一處」看成一步改寫 $S \\to S'$。\n\n**（終止性）** 每一步都讓字串長度嚴格變短，故任何改寫序列都會在 $\\le n$ 步內停在一個**不可再改寫**的字串（範式）。\n\n**（局部合流性）** 若 $S$ 有兩處不同的可刪位置 $R_1 \\ne R_2$，需證：分別刪除後，兩個結果都能繼續改寫到同一個字串。\n\n- *情形 A：$R_1$ 與 $R_2$ 不相交。* 刪掉其中一個不影響另一個仍然可刪（它的兩個元素仍相鄰），故兩邊都能再刪另一個，結果相同。**注意這裡用到「刪除只會讓兩側接合，不會拆散未被刪的相鄰關係」。**\n- *情形 B：$R_1$ 與 $R_2$ 重疊。* 這時要逐題檢查。1047 的重疊只可能是 `aaa`（位置 $(0,1)$ 與 $(1,2)$），刪任一者都得到 `a` ✔。1209（刪 $k$ 個相同）的重疊是 $k+1$ 個以上的同字元連續段，刪任一段結果相同 ✔。**1003 的 `abc` 根本不可能重疊**——因為 `abc` 沒有非空的「既是真前綴又是真後綴」的邊界（border），兩個出現位置必定不交 ✔。2696 的 `AB`、`CD` 字母互不相同，同理不交 ✔。\n\n由 Newman 引理（終止 + 局部合流 $\\Rightarrow$ 合流），**範式唯一**。$\\square$\n\n**推論：任何「總是刪得到就刪」的策略都會得到同一個答案，於是由左至右的堆疊掃描是正確的。**\n\n**這條定理點名了它依賴的前提：情形 B 必須逐題驗證。** 「重疊時殘留相同」不是普遍成立的性質——1717 就是反例（見定理 3）。**看到新的消除規則，第一件事是問「兩處消除可以重疊嗎？重疊時刪哪個都一樣嗎？」而不是直接套堆疊模板。** 一個現成的判準：**若被刪的模式串沒有非空 border（是「無邊界字串」），兩處出現必定不相交，情形 B 自動不存在。**\n\n### 定理 2（堆疊掃描的不變量）\n\n處理完前綴 $s[0..i-1]$ 後，**堆疊內容 = 該前綴獨自化簡到底所得的範式**。\n\n*證明（歸納）。* 基底：空前綴對應空堆疊。歸納步：設堆疊 $T$ 是 $s[0..i-1]$ 的範式（即 $T$ 內部已無可刪處）。加入 $s_i$ 後，新字串 $T\\,s_i$ 中**唯一可能的可刪處必然含 $s_i$**（因為 $T$ 內部無可刪處），也就是「$s_i$ 與 $T$ 的尾端」。若可刪則刪，刪完後尾端又可能與 $s_i$ 的新左鄰形成可刪處（1003、1209 用 `while` 處理），反覆直到不可刪；此時得到的正是 $T\\,s_i$ 的範式，由定理 1 它也就是 $s[0..i]$ 的範式。$\\square$\n\n**「$T$ 內部已無可刪處，所以只要檢查尾端」——這一句就是為什麼掃描是 $O(n)$ 而不是每次重掃 $O(n^2)$。**\n\n### 定理 3（735 行星碰撞：堆疊的形狀不變量）\n\n行星碰撞**不是**對稱的消除：只有「左邊向右飛、右邊向左飛」才會相撞（`+` 在左、`-` 在右）。\n\n**不變量**：堆疊內容由底到頂形如「一段負數（向左飛）接一段正數（向右飛）」，即 $-,-,\\dots,-,+,+,\\dots,+$。\n\n*維持*：一顆負數行星若出現在正數之上，兩者必定相撞（左邊的正數向右、上面的負數向左），演算法會處理掉，不可能留下。\n\n*推論*：**堆疊內部永遠不會有待處理的碰撞**，所以新行星只需要與棧頂互動。若新行星為正，它在最右且向右飛，與棧頂（正數向右，或負數向左）都在遠離，直接推入；若為負，它會與所有棧頂的正數依序相撞，用 `while` 處理到棧空、棧頂為負、或它自己被撞毀為止。$\\square$\n\n**這條不變量比定理 2 更強也更具體：它不只說「無可刪處」，還說出了堆疊的具體形狀。** 有了它，三種碰撞結局的處理就有明確的終止條件，不會寫成一團 `if` 巢狀。\n\n### 定理 4（1717：不合流的消除，順序決定分數）\n\n刪 `ab` 得 $x$ 分、刪 `ba` 得 $y$ 分。**這兩種消除會重疊**：`aba` 中位置 $(0,1)$ 是 `ab`、位置 $(1,2)$ 是 `ba`，定理 1 的情形 B 不再自動成立。\n\n*但重疊時的殘留相同*：`aba` 刪 `ab` 剩 `a`，刪 `ba` 也剩 `a`；`bab` 刪 `ab` 剩 `b`，刪 `ba` 也剩 `b`。**兩種選擇留下的字串完全一樣，差別只在得分。**\n\n*推論（貪心的正確性）*：設 $x \\ge y$。任何時刻若兩種消除重疊，選 `ab` 得分較高而後續狀態相同，故不劣；若不重疊，由情形 A 順序無關。因此「先用一次堆疊掃描消光所有 `ab`，再用一次掃描消光所有 `ba`」是最優的。$\\square$\n\n**這個對照才是本節真正的重點**：1047 那類題目「順序無關」是一條**需要證明的定理**，不是理所當然；而 1717 恰好是它失效的地方，於是必須另外提供一個交換論證。**兩者的程式碼幾乎一樣，正確性的來源卻完全不同。**\n\n## 逐步演算法\n\n:::example 狀態表示：倖存元素的堆疊\n**1. 狀態表示：棧裡放「目前倖存、尚未被消掉」的元素。** 棧頂就是當前要拿來比對的左鄰居。\n\n```cpp\n// 狀態表示：倖存元素的堆疊\nstack<char> st;  // 或 stack<pair<char,int>> 帶計數\n```\n:::\n\n\n\n:::example 更新：碰撞型——新元素持續和棧頂對決，直到不再消除\n**2. 更新流程：新元素先跟棧頂「對決」。** 能消就 `pop`（可能連鎖，用迴圈處理），不能消就 `push`。\n\n```cpp\n// 更新：碰撞型——新元素持續和棧頂對決，直到不再消除\nwhile (!st.empty() && willAnnihilate(st.top(), cur)) {\n    st.pop();  // 棧頂被消掉，換下一個棧頂繼續對決\n}\nif (survives(cur, st)) {\n    st.push(cur);  // 自己存活才入棧\n}\n```\n:::\n\n\n\n:::example 讀取：棧底→棧頂組出結果\n**3. 答案讀取：掃完後棧裡就是最終倖存序列。** 由棧底到棧頂即為答案順序，常需反轉或直接組字串。\n\n```cpp\n// 讀取：棧底→棧頂組出結果\nstring res;\nwhile (!st.empty()) {\n    res += st.top();\n    st.pop();\n}\nreverse(res.begin(), res.end());  // 棧是反的，轉回正序\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 對決前務必檢查 `!st.empty()`；棧空代表新元素沒有左鄰居，直接保留。\n- 帶計數時，計數歸零 / 湊滿才 `pop`，別漏掉「剛好湊滿」的時刻。\n- 複雜度：每個元素最多入棧、出棧各一次，整體 `O(n)`，空間 `O(n)`。\n\n## C++17 模板\n\n以下用 2197. 替換陣列中的非互質數 展示用堆疊做鄰項消除的核心寫法。\n\n**完整題目**：給陣列 nums，重複執行：只要存在相鄰且不互質（最大公因數大於 1）的兩個數，就把它們換成兩數的最小公倍數。直到沒有相鄰非互質對為止，回傳最終陣列（保證唯一）。\n\n:::example 用 stack 模擬：依序處理每個元素，與棧頂反覆合併直到互質\n**線索**：「合併後可能又和左邊產生新的可合併對」這種連鎖反應，正是鄰項消除：用堆疊保存已處理結果，新元素不斷嘗試和堆頂合併，直到互質才落地。\n\n```cpp\nclass Solution {\npublic:\n    // 用 stack 模擬：依序處理每個元素，與棧頂反覆合併直到互質\n    vector<int> replaceNonCoprimes(vector<int>& nums) {\n            vector<long long> st;\n            for (int x : nums) {\n                    long long cur = x;\n                    // 若與棧頂不互質，合併為 LCM 後繼續嘗試\n                    while (!st.empty()) {\n                            long long g = gcd(st.back(), cur);\n                            if (g == 1) {\n                                    break;\n                            }\n                            cur = st.back() / g * cur;  // LCM(st.back(), cur)\n                            st.pop_back();\n                    }\n                    st.push_back(cur);\n            }\n            return vector<int>(st.begin(), st.end());\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- `st` 保存目前已穩定的結果，堆頂是最右邊、最可能再合併的元素。\n- 對每個新數 `cur`，反覆和堆頂算 `gcd`；只要大於 1 就合併成 LCM 並 pop。\n- `st.back() / g * cur` 先除再乘可避免溢位，並用 `long long` 保險。\n- 互質（`g == 1`）時跳出，把 `cur` 推入堆疊收尾。\n\n## 時間與空間複雜度\n\n**時間 $O(n)$。** 每個元素最多推入一次、彈出一次（1209 的計數合併只讓推入更少），內層 `while` 的總執行次數受總彈出次數限制，攤還後仍是 $O(n)$。\n\n**空間 $O(n)$**，最壞情況沒有任何消除（例如 `abcdef` 之於 1047）。\n\n**1209 的空間**：用 `pair<char,int>` 壓縮後仍是 $O(n)$，但**當輸入是 $10^5$ 個相同字元、$k = 10^5$ 時，壓縮版只用 1 格，未壓縮版用 $10^5$ 格**。時間上，未壓縮版每次判定要看 $k$ 格（或維護額外計數），壓縮版恆為 $O(1)$。\n\n**735 的攤還**：每顆行星最多被推入一次、被撞毀一次。內層 `while` 可能一次撞掉多顆，但總撞毀數 $\\le n$，故 $O(n)$。\n\n**與暴力的落差**：$O(n^2) \\to O(n)$。$n = 10^5$ 時 $10^{10} \\to 10^5$。\n\n## 常見錯誤與邊界條件\n\n- 一定要 `while` 反覆合併，因為合併後可能與更左邊再次非互質。\n- LCM 可能超過 int，計算過程用 `long long`，輸出再轉回。\n- 「除盡再乘」的順序是避免溢位與保持整數的關鍵。\n\n## 常見變形\n\n- 字串鄰項消除（1047 / 1209）：堆頂與當前字元相同就抵銷或計數。\n- 小行星碰撞（735）：用方向與大小決定彈出哪一側。\n- 運算式化簡：相鄰可化簡項合併成單一項。\n\n## 與相似技巧的比較\n\n- **與 4.1 基礎堆疊**：4.1 的「抵銷」（844 退格）是本節的最簡特例——消除規則不看內容，只看「有沒有 `#`」。**因此 844 能退化成計數器，而 1047 不行（要記住是哪個字元）。**\n- **與 4.4 括號**：括號匹配也是相鄰消除（`()` 消掉），且 `()` 無 border，情形 B 不存在，合流性自動成立。**4.4 之所以能用「計數器」解單一種括號，是因為所有左括號同質；多種括號就必須用堆疊記住種類。**\n- **與 4.7 單調堆疊**：兩者都在推入前用 `while` 彈出棧頂，形狀極像，但**目的相反**：本節彈出是為了「刪掉不再需要的元素」，單調堆疊彈出是為了「結算被彈出元素的答案」。**看到 `while (!st.empty() && cond) st.pop_back();` 先問：彈出的東西是被丟棄，還是在被結算？**\n- **與 KMP／字串匹配**：定理 1 情形 B 的判準「模式串有沒有 border」正是 KMP 失敗函數在算的東西。**無 border ⇒ 兩處出現不重疊 ⇒ 合流性免驗。**\n- **與遞迴下降**：1003 也可以用「反覆 `s.find(\"abc\")` 並刪除」實作，$O(n^2)$；堆疊版把它壓成 $O(n)$。\n- **與 3561（相鄰字元消除的變體）與 2751（機器人碰撞）**：2751 是 735 的加權版（多了健康值），堆疊不變量完全一樣，只是碰撞結算規則變複雜。**認出 735 的形狀不變量，2751 就只是換算術。**\n\n## 代表例題\n\n**題號**：735、1003、1047、1209、1544\n\n用堆疊保存尚未匹配或尚未結算的元素；練習每次 push/pop 對不變式的影響。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 735 | [小行星碰撞](https://leetcode.cn/problems/asteroid-collision) |  |\n| 1003 | [檢查替換後的詞是否有效](https://leetcode.cn/problems/check-if-word-is-valid-after-substitutions) | 1427 |\n| 1047 | [刪除字串中的所有相鄰重複項](https://leetcode.cn/problems/remove-all-adjacent-duplicates-in-string) | 1286 |\n| 1209 | [刪除字串中的所有相鄰重複項 II](https://leetcode.cn/problems/remove-all-adjacent-duplicates-in-string-ii) | 1542 |\n| 1544 | [整理字串](https://leetcode.cn/problems/make-the-string-great) | 1344 |\n| 1717 | [刪除子字串的最大得分](https://leetcode.cn/problems/maximum-score-from-removing-substrings) | 1868 |\n| 2197 | [替換陣列中的非互質數](https://leetcode.cn/problems/replace-non-coprime-numbers-in-array) | 2057 |\n| 2216 | [美化陣列的最少刪除數](https://leetcode.cn/problems/minimum-deletions-to-make-array-beautiful) | 1510 |\n| 2696 | [刪除子串後的字串最小長度](https://leetcode.cn/problems/minimum-string-length-after-removing-substrings) | 1282 |\n| 2751 | [機器人碰撞](https://leetcode.cn/problems/robot-collisions) | 2092 |\n| 3561 | [移除相鄰字元](https://leetcode.cn/problems/resulting-string-after-adjacent-removals) | 1397 |\n| 3703 | [移除K-平衡子字串](https://leetcode.cn/problems/remove-k-balanced-substrings) | 1802 |\n\n## 本節重點速查\n\n- 骨架：由左至右掃描，**能與棧頂消除就消，否則推入**；字串題用 `std::string` 當堆疊。\n- **核心不變量（定理 2）**：堆疊內容 = 已處理前綴的範式；因為內部已無可刪處，**只需檢查棧頂**，這就是 $O(n)$ 的來源。\n- **合流性（定理 1）是需要證明的**：終止性顯然，局部合流要檢查「兩處消除重疊時，刪哪個都一樣嗎」。**模式串無 border ⇒ 不可能重疊 ⇒ 免驗**（`abc`、`AB`、`()`）。\n- 1047 的重疊是 `aaa`、1209 是 $k+1$ 段連續，兩者都殘留相同 ✔。\n- **1717 是反例**：`ab` 與 `ba` 會重疊，順序影響分數；但重疊時殘留相同，故「先消分數高的那種」最優——**同樣的程式碼，不同的正確性論證。**\n- 1209 用 `pair<char,int>` 壓縮，判定 $O(1)$、極端輸入省空間。\n- 735 的不變量是**形狀**：堆疊由底到頂 = 負數段 + 正數段，故內部無待處理碰撞；新行星只跟棧頂互動，`while` 處理連環撞，記得用 `alive` 旗標避免「已毀還推入」。\n- 攤還 $O(n)$：每個元素最多進出各一次，內層 `while` 不影響總量。\n",
        },
        {
          id: 31,
          title: "4.4 合法括號字串（RBS）",
          summary:
            "## 這個技術解決什麼問題\n\n括號題有三大類問法：**判斷合法**（20）、**改到合法且求最少代價**（921、1249、1541、1963）、**在合法子串上計算**（32、856）。\n\n它們共用同一個地基——一條把「遞迴定義的合法性」翻譯成「兩個可線性驗證的數值條件」的定理（見定理 1）。有了它，原本要枚舉所有配對方式（指數級）的問題，變成一次掃描。\n\n第二個地基是選型：**單一種括號用計數器 $O(1)$ 空間，多種括號必須用堆疊 $O(n)$ 空間。** 這不是實作偏好，而是資訊量的差異，定理 2 會說明。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「合法括號字串」想：\n\n- **輸入是括號（或可看成左右配對的符號），要判斷合法、補到合法、或在合法子串上做計算。** 20「有效的括號」是判斷，921「使括號有效的最少新增」是補齊，856「括號的分數」是計分。\n- **需要找到「最近一個還沒配對的左括號」。** 這就是堆疊或計數器的招牌訊號：\n\n:::example 嗅到味道：遇到右括號，要對應「最近的未配對左括號」\n```cpp\n// 嗅到味道：遇到右括號，要對應「最近的未配對左括號」\n// st.top();   // 最近的未配對左括號（多種括號時必用堆疊）\n```\n:::\n\n\n\n- **只有一種括號且只問「合不合法 / 最少改幾個」時，計數法 `O(1)` 空間更省。** 678「有效的括號字串」帶萬用字元 `*`，可用「平衡的上下界」這個計數延伸來解。\n- **暴力做法是反覆嘗試所有配對組合（指數級），完全不可行；** 堆疊 / 計數把它壓成一次線性掃描。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我是只有一種括號（可用計數器），還是多種（必須用堆疊）？\n2. 遇到右括號時，我拿它去配對哪一個「最近未配對的左括號」？\n3. 合法的判定條件是什麼——棧是否清空、或計數是否非負且歸零？\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n本節聚焦「合法括號字串（Regular Bracket Sequence, RBS）」的判斷與計算。一個括號字串合法，直覺說就是「每個左括號都能找到一個在它右邊、且配對正確的右括號」，例如 `(())` 合法，`())(` 不合法。\n\n判斷合法有兩種互通的視角，各有用途：\n\n:::example 堆疊判斷括號合法（支援多種括號）\n**視角一：堆疊配對。** 從左到右掃，遇到左括號就 `push`，遇到右括號就看棧頂能不能配對：能就 `pop`，不能（或棧空）就立刻不合法。掃完後棧要剛好空，才代表全部配對成功。多種括號 `()[]{}` 時必須用堆疊，因為要知道「最近未配對的左括號是哪一種」。\n\n```cpp\n// 堆疊判斷括號合法（支援多種括號）\nstack<char> st;\nfor (char c : s) {\n    if (c == '(' || c == '[' || c == '{') {\n        st.push(c);\n    } else {\n        if (st.empty() || !match(st.top(), c)) {\n            return false;  // 配不上\n        }\n        st.pop();  // 配對成功\n    }\n}\nreturn st.empty();  // 全部配對完才合法\n```\n:::\n\n\n\n:::example 計數法：單一種括號的合法判斷\n**視角二：計數平衡。** 如果只有一種括號 `()`，可以不用堆疊，只用一個計數器：左括號 +1、右括號 -1。**合法的充要條件是：過程中計數永遠不為負，且最後剛好為 0。** 計數代表「目前還沒被配對的左括號數量」，一旦變負就表示右括號過多。\n\n```cpp\n// 計數法：單一種括號的合法判斷\nint bal = 0;\nfor (char c : s) {\n    bal += (c == '(') ? 1 : -1;\n    if (bal < 0) {\n        return false;  // 右括號太多，提前出局\n    }\n}\nreturn bal == 0;  // 收支平衡才合法\n```\n:::\n\n\n\n這兩個視角的橋樑是：棧裡「未配對左括號的數量」其實就等於計數器的值。理解這點，像「最長有效括號」「括號的分數」這類進階題就有了統一的下手點——用下標入棧記錄位置，或用棧累積分數。\n\n## 狀態／資料結構定義\n\n**合法括號字串（RBS）的遞迴定義**：空串合法；若 $A$ 合法則 $(A)$ 合法；若 $A, B$ 合法則 $AB$ 合法。\n\n**計數器（單一種括號）**：一個整數 `bal`，`(` 時 `+1`、`)` 時 `-1`。語意是「當前未配對的左括號數」。\n\n**堆疊（多種括號，20）**：遇左括號推入其**對應的右括號**（推入期望值而非原字元，比對時只要一次 `==`，比 `switch` 乾淨）；遇右括號時檢查棧頂是否相符。\n\n**堆疊放下標（32、1249）**：要算長度或要刪除，就必須知道位置，堆疊裡放下標而非字元。32 的堆疊底部要放一個哨兵 `-1`，代表「最後一個未匹配的右括號的下標」。\n\n**區間狀態（678，帶萬用字元 `*`）**：兩個整數 `lo`、`hi`，代表「當前未配對左括號數」的所有可能取值構成的區間 $[\\texttt{lo}, \\texttt{hi}]$。\n\n**分數堆疊（856）**：`vector<int>`，每一格代表一個尚未閉合的層級目前累積的分數。\n\n## 不變量或正確性證明\n\n### 定理 1（合法性的線性刻畫）\n\n字串 $s$（只含 `(`、`)`）合法 $\\iff$\n\n- **(C1)** 全體 `(` 與 `)` 個數相等，即最終 $\\mathrm{bal} = 0$；\n- **(C2)** 每個前綴的 $\\mathrm{bal} \\ge 0$。\n\n*（$\\Rightarrow$）* 對遞迴定義歸納。空串顯然。$(A)$：前綴要嘛是 `(` 加上 $A$ 的某個前綴（$\\mathrm{bal} = 1 + (\\ge 0) > 0$），要嘛是整個 `(A)`（$\\mathrm{bal}=0$）；總和為 $0$。$AB$：$A$ 的前綴 $\\ge 0$；$B$ 的前綴之前有 $A$ 貢獻的 $0$，故仍 $\\ge 0$；總和 $0+0=0$。\n\n*（$\\Leftarrow$）* 對長度歸納。$s$ 非空時，由 (C2) 在長度 1 的前綴得 $s_0 = $ `(`。令 $k$ 為最小的使 $\\mathrm{bal}(s[0..k]) = 0$ 的下標（由 (C1) 至少 $k = |s|-1$ 可用，故存在）。則 $s[1..k-1]$ 滿足 (C1)（因 $\\mathrm{bal}$ 從 1 回到 0，中段淨變化為 $-0$… 精確地說 $\\mathrm{bal}(s[1..k-1]) = 0$）與 (C2)（否則 $s[0..j]$ 會在 $j<k$ 時歸零，與 $k$ 最小矛盾），故 $s_k = $ `)` 且 $s[1..k-1]$ 合法，於是 $s[0..k] = (\\,A\\,)$ 合法。剩下的 $s[k+1..]$ 同樣滿足兩條件，由歸納也合法。兩段相接仍合法。$\\square$\n\n**這條定理是整節的地基。** 它把一個遞迴（樹狀）的性質換成兩個掃描時可維護的數值條件，於是所有「最少插入／刪除／交換」的題目都能改用計數論證，不必真的建出配對結構。\n\n**推論（921 使括號有效的最少新增）。** 掃描時把 $\\mathrm{bal}$ 夾在 $\\ge 0$：每當 $\\mathrm{bal}$ 要變成 $-1$，記一次 `need++` 並把 $\\mathrm{bal}$ 重設為 0；掃完後答案是 `need + bal`。\n\n*下界*：每個造成 $\\mathrm{bal} < 0$ 的 `)` 都必須在它左側補一個 `(`，這些 `)` 互不相同，故至少 `need` 個；掃完剩下的 $\\mathrm{bal}$ 個 `(` 各自需要一個 `)`。兩類插入互不重複（一個插在該 `)` 左邊、一個插在字串尾），故答案 $\\ge$ `need + bal`。\n*上界*：照上述方式插入後，(C1)、(C2) 皆滿足，由定理 1 合法。$\\square$\n\n**推論（1963 使字串平衡的最少交換次數）。** 若 $s$ 中 `[`、`]` 各半，答案是 $\\lceil m/2 \\rceil$，其中 $m$ 是掃描中未匹配的 `]` 個數。*理由*：一次交換最多能修正兩個未匹配位置（把最右邊多餘的 `[` 與最左邊多餘的 `]` 對調，同時消掉兩個），故下界 $\\lceil m/2\\rceil$；而這個貪心交換確實每次消掉兩個（$m$ 為奇數時最後一次消一個），故可達。$\\square$\n\n### 定理 2（為什麼一種括號能用計數器、多種不行）\n\n單一種括號時，所有左括號**完全同質**：定理 1 的兩個條件只涉及「數量」，不涉及「是誰」。因此堆疊裡的內容可以用一個整數概括，$O(1)$ 空間。\n\n多種括號時這不成立。反例：`([)]` 滿足「每種括號各自計數平衡、且各自的前綴計數非負」，卻不合法。**問題出在巢狀順序**——合法性要求配對區間**兩兩之間要麼包含要麼不交**，不能交叉。這是一個**順序**資訊，而不是**數量**資訊，計數器無法表達。\n\n*形式化*：把左括號的種類序列記下來，就是一個長度可達 $n/2$ 的字串；要判定交叉，必須能取回「最近一個未配對左括號的種類」——這正是堆疊提供的能力，且 $\\Omega(n)$ 空間是必要的（否則無法區分 $2^{n/2}$ 種不同的巢狀種類序列）。$\\square$\n\n**這條論證給了一個可以直接套用的判準：問「我需要記住『是誰』還是只需要『有幾個』？」** 這與 4.1 引理 3（844 退格可退化成計數器）是同一個問題的兩個面向。\n\n### 定理 3（678：可能的未配對數構成一個區間）\n\n`*` 可當 `(`、`)` 或空。令 $P_i \\subseteq \\mathbb{Z}_{\\ge 0}$ 為「處理完前綴 $s[0..i]$ 後，未配對左括號數的所有可能取值」。\n\n**宣稱**：$P_i$ 恆為一個整數區間 $[\\mathrm{lo}_i, \\mathrm{hi}_i]$（可能為空）。\n\n*證明（歸納）。* $P_{-1} = \\{0\\}$ 是區間。歸納步：\n- $s_i = $ `(`：$P_i = (P_{i-1} + 1)$，區間平移仍是區間。\n- $s_i = $ `)`：$P_i = (P_{i-1} - 1) \\cap \\mathbb{Z}_{\\ge 0}$，區間平移後與 $[0,\\infty)$ 相交，仍是區間。\n- $s_i = $ `*`：$P_i = \\big((P_{i-1}-1) \\cup P_{i-1} \\cup (P_{i-1}+1)\\big) \\cap \\mathbb{Z}_{\\ge 0}$。三個區間兩兩相鄰（平移量差 1），聯集連續，仍是區間。$\\square$\n\n**「集合是區間」正是只追蹤 `lo` 與 `hi` 兩個數就足夠的理由**——否則得存整個集合，$O(n)$ 空間。維護規則：`(` 時兩者 $+1$；`)` 時兩者 $-1$；`*` 時 `lo--, hi++`。然後 `lo = max(lo, 0)`（負的未配對數不可能，且截斷後仍是區間）；若 `hi < 0` 則整個集合為空，立即回傳失敗。最後判 `lo == 0`。\n\n**最容易錯的一行是 `lo = max(lo, 0)`。** 它的正當性是：$P_i$ 的定義已經包含「$\\ge 0$」的約束（未配對數不可能是負的），截斷只是把不合法的取值排除，不會誤增可能性。**若忘記截斷，`lo` 會被 `*` 一路拉成很負的數，最後 `lo == 0` 判定永遠失敗。**\n\n### 定理 4（32 最長有效括號的堆疊不變量）\n\n堆疊裡存**下標**，初始放入哨兵 $-1$。掃描到 $i$：`(` 則推入 $i$；`)` 則先彈出一格，若堆疊變空則推入 $i$，否則更新答案 $\\mathrm{ans} = \\max(\\mathrm{ans},\\ i - \\texttt{st.back()})$。\n\n**不變量**：任何時刻，堆疊由底到頂是\n$$[\\,\\text{最後一個「無法被匹配的 `)`」的下標（或哨兵 } -1)\\,,\\ \\text{其後所有未匹配 `(` 的下標（遞增）}\\,].$$\n\n*維持*：`(` 一定加入未匹配集合；`)` 若能匹配則消掉最近一個未匹配 `(`（棧頂），若不能（堆疊只剩底部那個障礙）則它自己成為新的障礙。\n\n*正確性*：以 $i$ 結尾的最長有效子串，其左界恰好是「最後一個障礙位置 $+1$」。因為障礙之後的所有 `(` 都已被匹配掉（堆疊為空表示如此），而障礙本身不可能被包含在任何有效子串裡。故長度 $= i - \\texttt{st.back()}$。$\\square$\n\n**哨兵 $-1$ 的作用**：讓「有效子串從下標 0 開始」不必特判，與 2.1 的 $s_0 = 0$ 是同一種設計。**沒有哨兵就要寫 `st.empty() ? i + 1 : i - st.back()`，這正是最常見的 off-by-one 來源。**\n\n### 定理 5（856 括號的分數：結構歸納）\n\n堆疊初始放一格 0。`(` 時推入 0；`)` 時彈出 $v$，令 $\\texttt{st.back()} \\mathrel{+}= \\max(2v, 1)$。\n\n**不變量**：堆疊第 $d$ 格（由底算起，$d \\ge 0$）保存「深度 $d$ 這一層目前已閉合的所有子結構的分數總和」。\n\n*維持*：遇 `(` 開啟新一層，分數從 0 開始；遇 `)` 閉合當前層，其累積分數 $v$ 若為 0 表示這層是空的 `()`，計 1 分；否則計 $2v$ 分（規則 $(A) = 2\\cdot A$）。結算後併入父層（規則 $AB = A + B$ 就是加總）。這正好對應文法 $S \\to \\texttt{()} \\mid (S) \\mid SS$ 的三條規則。$\\square$\n\n**這是「把遞迴下降改寫成顯式堆疊」的教科書例子**：堆疊格數 = 遞迴深度，格內容 = 該層的區域變數。\n\n## 逐步演算法\n\n:::example 狀態表示（二選一）\n**1. 狀態表示：選堆疊或計數器。** 多種括號、或需要記位置 / 分數 → 用堆疊；單一種括號只判合法 → 用計數器。\n\n```cpp\n// 狀態表示（二選一）\nstack<int> st;  // 多種括號或要記下標 / 分數\n        // int bal = 0;  // 單種括號的平衡計數\n```\n:::\n\n\n\n:::example 更新：右括號時嘗試和棧頂結算\n**2. 更新流程：左括號累積，右括號嘗試結算。** 右括號時檢查能否與「最近的未配對左括號」配對。\n\n```cpp\n// 更新：右括號時嘗試和棧頂結算\nif (c == '(') {\n    st.push(i);  // 記下左括號位置\n} else if (!st.empty()) {\n    st.pop();  // 配掉一個左括號\n} else {\n    baseIndex = i;  // 棧空：此處不可能屬於有效段\n}\n```\n:::\n\n\n\n:::example 讀取：以下標差計算最長有效括號\n**3. 答案讀取：依題意取結論。** 純判斷看「棧是否空 / bal 是否為 0」；求最長有效段則用「當前下標 − 棧頂下標」算長度。\n\n```cpp\n// 讀取：以下標差計算最長有效括號\nif (!st.empty()) {\n    ans = max(ans, i - st.top());  // 有效段長度\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 右括號時務必先判 `!st.empty()` / `bal >= 0`，空棧仍配對就是 bug。\n- 計數法只適用「單一種括號」；一旦有 `[]{}` 混用，平衡數相同也可能交錯不合法，必須用堆疊。\n- 複雜度：一次掃描 `O(n)`，堆疊空間最壞 `O(n)`，計數法空間 `O(1)`。\n\n## C++17 模板\n\n以下用本節代表題 20. 有效的括號 展示核心寫法。\n\n**完整題目**：判斷只含括號字元的字串是否有效，括號必須按正確順序閉合。\n\n:::example 用 stack 對照括號：遇左括號壓入對應右括號，遇右括號檢查是否吻合\n**線索**：題目要找最近更大/更小邊界或某元素的貢獻範圍；這表示元素應在單調結構中等待被右側元素結算。\n\n```cpp\nclass Solution {\npublic:\n    // 用 stack 對照括號：遇左括號壓入對應右括號，遇右括號檢查是否吻合\n    bool isValid(string s) {\n            vector<char> st;\n            for (char c : s) {\n                    if (c == '(') {\n                            st.push_back(')');\n                    } else if (c == '[') {\n                            st.push_back(']');\n                    } else if (c == '{') {\n                            st.push_back('}');\n                    } else {\n                            // 右括號：棧空或棧頂不吻合則無效\n                            if (st.empty() || st.back() != c) {\n                                    return false;\n                            }\n                            st.pop_back();\n                    }\n            }\n            return st.empty();  // 棧空代表所有括號都成功配對\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- 堆疊保存尚未匹配的右括號期待值；遇到右括號時必須和棧頂匹配，最後棧要清空。\n- 容器中保存尚未結算的元素；新元素破壞單調性時，被彈出的元素就找到了右邊界。\n- 程式通常存下標而非數值，因為答案需要計算距離或回填到原位置。\n\n## 時間與空間複雜度\n\n| 題型 | 方法 | 時間 | 空間 |\n| --- | --- | --- | --- |\n| 20 多種括號判定 | 堆疊 | $O(n)$ | $O(n)$（必要，定理 2） |\n| 單一種括號判定／最少插入（921、1541） | 計數器 | $O(n)$ | $O(1)$ |\n| 678 帶 `*` | 區間 `lo`/`hi` | $O(n)$ | $O(1)$ |\n| 32 最長有效 | 堆疊放下標 | $O(n)$ | $O(n)$ |\n| 32 最長有效 | 左右兩趟計數 | $O(n)$ | $O(1)$ |\n| 856 分數 | 分數堆疊 | $O(n)$ | $O(\\text{深度})$ |\n| 1249 移除無效括號 | 兩趟 / 堆疊 | $O(n)$ | $O(n)$ |\n\n**32 的 $O(1)$ 空間解法值得記**：由左至右維護 `left`、`right` 計數，相等時更新答案，`right > left` 時歸零；再由右至左做對稱的一趟。**兩趟的必要性**：單趟由左至右無法處理 `(()` 這種「左括號有剩」的情形（永遠不會相等），必須靠反向那趟補上。**這個「正反各掃一趟」的補償技巧在很多前綴／後綴不對稱的題目都用得到。**\n\n**678 也可以用兩個堆疊（分別記 `(` 與 `*` 的下標）$O(n)$ 解，但區間法 $O(1)$ 更短更快。** 只有在需要**還原具體方案**（哪個 `*` 當什麼）時才需要堆疊版。\n\n## 常見錯誤與邊界條件\n\n- 先決定維持遞增還是遞減。\n- 相等元素用 `<` 還是 `<=` 會影響去重與邊界。\n- 需要寬度時入棧索引而不是值。\n- 哨兵可簡化最後清棧。\n\n## 常見變形\n\n- 循環陣列可掃兩遍。\n- 用索引入棧以計算距離或寬度。\n- 貢獻法中分別求左/右第一個嚴格或非嚴格邊界。\n\n## 與相似技巧的比較\n\n- **與 4.3 鄰項消除**：括號匹配就是「消掉相鄰的 `()`」。`()` 沒有非空 border，故兩處出現不可能重疊，定理 1 的合流性自動成立——**這是為什麼括號題可以放心地由左至右貪心。**\n- **與 4.5 表示式解析**：4.5 的括號用來表達運算優先級與巢狀結構，堆疊裡放的是「現場狀態」而非字元。**856 是兩者之間的橋樑**：它的堆疊放分數，已經是「現場」而非符號。\n- **與 4.1 的計數器降級**：定理 2 給出了精確判準——**同質元素只問數量 ⇒ 計數器；異質元素要問順序 ⇒ 堆疊。**\n- **與 DP（32 的另一解）**：$\\mathrm{dp}[i]$ = 以 $i$ 結尾的最長有效長度，轉移分 `s[i-1] == '('` 與 `== ')'` 兩種情形。$O(n)$ 時間、$O(n)$ 空間，比堆疊版難寫但容易推廣到「有效子串的個數」等變體。\n- **與貪心（1963、921）**：這些題的最優性靠「下界 + 可達」的雙向論證，**堆疊只是用來算出那個計數 $m$，最優性是另一段獨立推理**。不要把「用了堆疊」當成正確性的說明。\n- **與 5.x 佇列**：括號的巢狀本質上是樹，只有堆疊（深度優先）能表達；佇列（廣度優先）在這裡完全用不上。\n\n## 代表例題\n\n**題號**：20、32、678、856、921\n\n用堆疊保存尚未匹配或尚未結算的元素；練習每次 push/pop 對不變式的影響。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 20 | [有效的括號](https://leetcode.cn/problems/valid-parentheses) |  |\n| 32 | [最長有效括號](https://leetcode.cn/problems/longest-valid-parentheses) |  |\n| 678 | [有效的括號字串](https://leetcode.cn/problems/valid-parenthesis-string) |  |\n| 856 | [括號的分數](https://leetcode.cn/problems/score-of-parentheses) | 1563 |\n| 921 | [使括號有效的最少新增](https://leetcode.cn/problems/minimum-add-to-make-parentheses-valid) | 1242 |\n| 1021 | [刪除最外層的括號](https://leetcode.cn/problems/remove-outermost-parentheses) | 1311 |\n| 1111 | [有效括號的巢狀深度](https://leetcode.cn/problems/maximum-nesting-depth-of-two-valid-parentheses-strings) | 1749 |\n| 1190 | [反轉每對括號間的子串](https://leetcode.cn/problems/reverse-substrings-between-each-pair-of-parentheses) | 1486 |\n| 1249 | [移除無效的括號](https://leetcode.cn/problems/minimum-remove-to-make-valid-parentheses) | 1657 |\n| 1541 | [平衡括號字串的最少插入次數](https://leetcode.cn/problems/minimum-insertions-to-balance-a-parentheses-string) | 1759 |\n| 1614 | [括號的最大巢狀深度](https://leetcode.cn/problems/maximum-nesting-depth-of-the-parentheses) | 1323 |\n| 1963 | [使字串平衡的最小交換次數](https://leetcode.cn/problems/minimum-number-of-swaps-to-make-the-string-balanced) | 1689 |\n| 2116 | [判斷一個括號字串是否有效](https://leetcode.cn/problems/check-if-a-parentheses-string-can-be-valid) | 2038 |\n\n## 本節重點速查\n\n- **定理 1**：$s$ 合法 $\\iff$ (C1) 總數相等 $\\wedge$ (C2) 每個前綴 $\\mathrm{bal} \\ge 0$。**把遞迴定義換成兩個掃描條件，是整節的地基。**\n- 921：`need + bal`（$\\mathrm{bal}$ 觸負就 `need++` 並歸零）；下界靠「每個負向 `)` 各需一次插入」，上界靠定理 1。\n- 1963：答案 $\\lceil m/2 \\rceil$，一次交換最多消兩個未匹配。\n- **定理 2**：一種括號 ⇒ 元素同質、只問數量 ⇒ 計數器 $O(1)$；**多種括號要問巢狀順序** ⇒ 必須堆疊，`([)]` 是計數器的反例。\n- 678：**可能的未配對數恆為區間**（三種平移的聯集連續），故只需 `lo`/`hi`。**`lo = max(lo, 0)` 不可省**，`hi < 0` 立即失敗，最後判 `lo == 0`。\n- 32：堆疊放下標 + **哨兵 $-1$**；不變量是「底部 = 最後一個障礙，其上 = 未匹配 `(`」，長度 $= i - \\texttt{st.back()}$。另有 $O(1)$ 空間的左右兩趟解，反向那趟是為了處理 `(()`。\n- 856：堆疊第 $d$ 格 = 深度 $d$ 已閉合子結構的分數和；`)` 時 $\\max(2v, 1)$ 併入父層，對應文法三條規則。\n- `()` 無 border ⇒ 消除不重疊 ⇒ 由左至右貪心自動正確（承 4.3 定理 1）。\n",
        },
        {
          id: 32,
          title: "4.5 表示式解析",
          summary:
            "## 這個技術解決什麼問題\n\n輸入是一串要「按規則計算或驗證」的字元，而且帶有**優先級**或**巢狀**結構：224／227（基本計算器）、150（逆波蘭表示式）、394（字串解碼）、726（原子的數量）、736（Lisp 語法解析）、1106（解析布林表示式）、8（atoi）、65（有效數字）。\n\n暴力做法（反覆掃描找優先級最高的運算子再計算）是 $O(n^2)$，而且遇到巢狀括號就難以維護。\n\n正確的思路是把題目當成**一個小型編譯器前端**：先寫下文法，再選一種實作（遞迴下降或顯式堆疊）。**幾乎所有解析題的困難都不在程式碼，而在沒有先把文法與優先級寫清楚就開始寫 `if`。**\n\n本節的核心觀念只有兩個：\n\n1. **優先級決定「什麼時候可以結算」。** 高優先級的運算子必須立刻結算，低優先級的要等。\n2. **巢狀決定「什麼時候要保存現場」。** 進入括號時把當前的部分結果推入堆疊，離開時取回並合併——這就是遞迴呼叫堆疊的手動版。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「表示式解析」想：\n\n- **輸入是一串要「按規則計算或驗證」的字元，且有優先級或巢狀結構。** 224「基本計算器」「227 基本計算器 II」就是帶括號、帶 `+ - * /` 的中綴求值。\n- **已經是後綴 / 前綴形式，或可先轉成後綴。** 150「逆波蘭表示式求值」只需一個數字棧即可：\n\n:::example 嗅到味道：看到運算子就要「回頭取最近兩個運算元」\n```cpp\n// 嗅到味道：看到運算子就要「回頭取最近兩個運算元」\n// b = st.top(); st.pop(); a = st.top(); st.pop();   // 取兩個運算元\n```\n:::\n\n\n\n- **要逐字元判斷「格式 / 狀態」是否合法。** 8「atoi」、65「有效數字」是逐字元維護解析狀態的典型。\n- **暴力做法（如反覆找優先級最高的運算子再算）會是 `O(n^2)`；** 堆疊讓一次線性掃描就能尊重優先級與括號。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的輸入是中綴（要兩個棧）、後綴（一個數字棧），還是純格式驗證？\n2. 遇到運算子時，我用什麼優先級規則決定「先算棧裡的、還是先入棧」？\n3. 括號要怎麼當邊界處理，掃完後殘餘的運算子怎麼清空？\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n本節談「表示式解析」：把一串字元（像 `\"3 + 4 * 2\"` 或 `\"(1 + (4 + 5))\"`）真正算出數值，或解析成有意義的結構。難點在於運算有「優先順序」和「括號巢狀」——`*` 要先於 `+`、括號裡要先算——不能單純從左讀到右就加減。\n\n解這類題的萬用工具還是堆疊，核心想法是：**用堆疊把「還不能立刻計算的東西」暫存起來，等湊齊足夠資訊再結算。** 對中綴算式 `a + b * c`，常用「兩個堆疊」：一個放數字，一個放運算子。\n\n:::example 解析中綴算式的核心：遇到運算子先結算高優先級的\n關鍵規則是運算子的優先級：**要把一個新運算子入棧前，先把棧裡「優先級不低於它」的運算子都先算掉。** 這樣高優先級（如 `*`）會先被結算，自然實現「先乘後加」。\n\n```cpp\n// 解析中綴算式的核心：遇到運算子先結算高優先級的\nwhile (!ops.empty() && prio(ops.top()) >= prio(cur)) {\n    int b = nums.top();\n    nums.pop();\n    int a = nums.top();\n    nums.pop();\n    nums.push(apply(a, ops.top(), b));  // 算掉棧頂運算子\n    ops.pop();\n}\nops.push(cur);  // 再把當前運算子入棧\n```\n:::\n\n\n\n括號的處理很優雅：遇到 `(` 就把它當「邊界」推入運算子棧，遇到 `)` 就一路結算到碰見 `(` 為止——等於先把括號內算完。\n\n:::example 逆波蘭求值：只需一個數字棧\n如果輸入已經是「逆波蘭（後綴）表示式」（LeetCode 150），就更簡單了，只要一個數字棧：遇到數字 `push`，遇到運算子就 `pop` 兩個算完再 `push` 回去：\n\n```cpp\n// 逆波蘭求值：只需一個數字棧\nfor (auto& tok : tokens) {\n    if (isOperator(tok)) {\n        int b = st.top();\n        st.pop();\n        int a = st.top();\n        st.pop();\n        st.push(apply(a, tok, b));  // 算完把結果推回\n    } else {\n        st.push(stoi(tok));\n    }\n}\n// 最後棧裡剩下的唯一數字就是答案\n```\n:::\n\n\n\n像「字串轉整數 atoi」（8）、「有效數字」（65）則屬於另一種解析：逐字元用狀態判斷格式合不合法、累積數值，雖不一定用棧，但同樣是「逐字元掃描 + 維護解析狀態」的思路。\n\n## 狀態／資料結構定義\n\n**先寫文法。** 四則運算帶括號的標準文法（左結合）：\n```\nexpr   → term (('+' | '-') term)*\nterm   → factor (('*' | '/') factor)*\nfactor → number | '(' expr ')' | '-' factor      // 最後一項是一元負號\n```\n**這三行決定了一切**：優先級由層級表示（`factor` 綁最緊），左結合由 `*` 迴圈（而非右遞迴）表示。\n\n**實作一：遞迴下降。** 三個函式對應三條規則，共用一個 `size_t i` 當游標（用成員變數或以參考傳遞）。**最貼近文法、最好除錯，但受遞迴深度限制**（`((((...))))` 深度 $10^5$ 會爆棧）。\n\n**實作二：單一數值堆疊（227，無括號）。**\n- `vector<long long> st`：已完成的**加法項**，符號已折進數值。\n- `char op = '+'`：上一個看到的運算子。\n- 每讀完一個數 `num`，依 `op` 動作：`+` 推入 `num`；`-` 推入 `-num`；`*` 令 `st.back() *= num`；`/` 令 `st.back() /= num`。\n- 答案 = `st` 的總和。\n\n**實作三：符號堆疊（224，有括號但只有 `+ -`）。**\n- `int sign = 1`、`long long res = 0`、`vector<int> st`（保存進入括號前的 `res` 與 `sign`）。\n- `(` 時推入 `res` 與 `sign`，然後重置 `res = 0, sign = 1`；`)` 時 `res = 前res + 前sign * res`。\n\n**實作四：狀態堆疊（394 字串解碼）。** 堆疊元素是 `pair<int, string>`：進入 `[` 時推入「當前重複次數」與「已累積的字串」，離開 `]` 時取回並拼接。\n\n**共同模式：括號 = 保存現場 + 遞迴 + 合併。** 四種實作只是「現場」的內容不同。\n\n**數字的讀取要一次讀完整個 token**，不要一位一位混進主迴圈：`while (i < n && isdigit(s[i])) num = num * 10 + (s[i++] - '0');`。**多位數是解析題第一個踩雷處。**\n\n## 不變量或正確性證明\n\n### 定理 1（227 的堆疊不變量：優先級決定結算時機）\n\n**不變量**：處理完某個加法項之後，\n$$\\texttt{sum(st)} \\;=\\; \\text{目前已完整讀入的算式前綴之值}.$$\n\n*證明（對加法項數歸納）。* 基底：`st` 為空，值為 0 ✔。歸納步：設已處理的前綴值等於 `sum(st)`。接著讀到 `op` 與 `num`：\n- `op` 是 `+` 或 `-`：這開啟一個**新的加法項**，其值目前是 $\\pm\\texttt{num}$，推入後總和恰好增加 $\\pm\\texttt{num}$ ✔。\n- `op` 是 `*` 或 `/`：由文法，`*`／`/` 綁得比 `+`／`-` 緊，所以 `num` 屬於**當前這個加法項**，而該項的值正是 `st.back()`。就地修改 `st.back()` 等於修改該項的值，總和隨之正確更新 ✔。$\\square$\n\n**這條論證點名了兩個前提。**\n\n1. **`*`／`/` 的優先級嚴格高於 `+`／`-`。** 這保證了「看到 `*` 時，要相乘的左運算元一定是最近推入的那一格」。若優先級相同（如把 `*` 改成同級），`st.back()` 就未必是正確的左運算元。\n2. **左結合。** `8 / 3 / 2` 必須是 $(8/3)/2 = 1$，而 `st.back() /= num` 依序執行正好給出左結合。**若某個運算子是右結合（如次方 `^`），就不能就地修改，必須改用另一個堆疊或遞迴。**\n\n*推論*：一個加法項一旦看到下一個 `+`／`-`（或字串結束），就**確定不會再被修改**——因為之後的任何運算子都綁得不比 `+` 緊。**「什麼時候可以安全結算」永遠由『後面還有沒有可能綁更緊的東西』決定，這是所有優先級解析的共同原理。**\n\n*整數除法的方向*：C++ 的 `/` 對負數是向零截斷（`-7/2 == -3`），與 LeetCode 227 的要求一致。**但 166、29 等題要的是向下取整，必須自己處理**——別假設語言的預設就是題目要的。\n\n### 定理 2（150 逆波蘭：合法性的線性刻畫）\n\n給 token 定義權重：運算元 $w = +1$，$k$ 元運算子 $w = -(k-1)$（二元運算子即 $-1$）。\n\n**宣稱**：一個 token 序列是合法的後綴表示式 $\\iff$ **每個非空前綴的權重和 $\\ge 1$，且總和恰為 $1$。**\n\n*（$\\Rightarrow$）* 對表示式樹歸納：權重和 = 求值後堆疊上的元素個數。單一運算元為 1；$E_1 E_2 \\oplus$ 的權重和 $= 1 + 1 - 1 = 1$。任一前綴對應「若干棵已完成的子樹 + 一棵未完成的」，個數 $\\ge 1$。\n\n*（$\\Leftarrow$）* 對長度歸納：末尾 token 若是運算元則其前綴總和為 0，違反「$\\ge 1$」除非序列長度為 1；若是二元運算子，把序列從右往左找出最後一個使前綴和為 1 的切點，可拆成兩段各自合法。$\\square$\n\n**這條定理與 4.4 的定理 1 是同一件事的推廣**：括號的 $\\mathrm{bal} \\ge 0$ 與 $\\mathrm{bal}_{\\text{final}} = 0$，在這裡變成權重和 $\\ge 1$ 與 $= 1$。**「堆疊高度的前綴約束」是所有巢狀結構合法性的統一刻畫。**\n\n*求值的不變量*：處理完某前綴後，數值堆疊由底到頂 = 該前綴中**所有極大完整子表示式**的值，順序與原文一致。遇到二元運算子時彈出兩個——**先彈出的是右運算元**，這是減法與除法出錯的第一名原因。\n\n### 定理 3（394 字串解碼的深度不變量）\n\n**不變量**：堆疊第 $d$ 格（由底算起）保存「深度 $d$ 這一層在進入第 $d+1$ 層之前已經累積好的字串」與「第 $d+1$ 層的重複次數」；變數 `cur` 保存**當前最深層**已累積的字串。\n\n*維持*：讀到 `k[` 時，當前層的 `cur` 已完成到此，連同 `k` 一起推入，`cur` 重置為空（開新層）；讀到 `]` 時，彈出 $(k, \\text{prev})$，令 `cur = prev + repeat(cur, k)`（合併回父層）。\n\n*正確性*：這正是文法 $S \\to (\\text{letters} \\mid \\text{num} [\\,S\\,])^*$ 的結構歸納——每個 `]` 恰好閉合最近一個未閉合的 `[`（4.4 定理 1 保證輸入良括號），而閉合時子層的值已經完整。$\\square$\n\n**這個不變量的價值在於它說清楚了「`cur` 不在堆疊裡」**。初學者常把 `cur` 也推入堆疊，導致多一層或少一層。**記法：堆疊存的是「父層的殘缺結果」，`cur` 存的是「當前層的殘缺結果」，兩者永遠差一層。**\n\n### 定理 4（8 atoi 的溢位判定）\n\n累加 `res = res * 10 + d` 之前必須檢查，而**檢查必須在乘法之前做**——溢位一旦發生就是未定義行為，事後檢查沒有意義。\n\n正數方向的正確條件是\n$$\\texttt{res} > \\frac{\\texttt{INT\\_MAX} - d}{10} \\quad\\Longrightarrow\\quad \\text{溢位（夾到 INT\\_MAX）}.$$\n\n*正確性*：$\\texttt{res}\\cdot 10 + d > \\texttt{INT\\_MAX} \\iff \\texttt{res} \\cdot 10 > \\texttt{INT\\_MAX} - d \\iff \\texttt{res} > \\frac{\\texttt{INT\\_MAX}-d}{10}$。最後一步的整數除法向下取整不會出錯，因為 $\\texttt{res}$ 是整數：$\\texttt{res} > \\lfloor y/10 \\rfloor \\iff 10\\,\\texttt{res} > y$ 對整數 $\\texttt{res}$、$y$ 成立（$10\\,\\texttt{res} > y \\iff \\texttt{res} > y/10 \\iff \\texttt{res} \\ge \\lfloor y/10\\rfloor + 1$）。$\\square$\n\n**更簡單且同樣正確的做法是全程用 `long long` 累加，並在超過 $2^{31}$ 時提早夾住**（因為輸入長度有限，`long long` 不會溢位）。**但面試常要求不用更寬的型別，那就得寫上面那條不等式——而且要能說出它為什麼對。**\n\n*負數方向*：常見做法是統一以正數累加、最後套符號，此時上界要用 $|\\texttt{INT\\_MIN}| = 2147483648$，**不能直接用 `INT_MAX`**，否則輸入 `-2147483648` 會被錯誤夾成 `-2147483647`。\n\n## 逐步演算法\n\n:::example 狀態表示：中綴用兩個棧\n**1. 狀態表示：依算式型態決定用幾個棧。** 後綴 → 一個數字棧；中綴 → 數字棧 + 運算子棧（含括號）。\n\n```cpp\n// 狀態表示：中綴用兩個棧\nstack<long long> nums;  // 運算元\nstack<char> ops;        // 運算子與左括號\n```\n:::\n\n\n\n:::example 更新：遇到右括號，結算到對應的左括號為止\n**2. 更新流程：分四種字元處理。** 數字累積、運算子按優先級先結算、`(` 入棧當邊界、`)` 結算到 `(`。\n\n```cpp\n// 更新：遇到右括號，結算到對應的左括號為止\nwhile (!ops.empty() && ops.top() != '(') {\n    calcTop(nums, ops);  // 取兩數 + 一運算子算出結果推回 nums\n}\nops.pop();  // 丟掉那個 '('\n```\n:::\n\n\n\n:::example 讀取：清空殘餘運算子，得到最終值\n**3. 答案讀取：掃完後把殘餘運算子全結算。** 沒有括號擋著的剩餘運算子，依序算完，最後數字棧剩一個值就是答案。\n\n```cpp\n// 讀取：清空殘餘運算子，得到最終值\nwhile (!ops.empty()) {\n    calcTop(nums, ops);\n}\nlong long ans = nums.top();\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 多位數字要連續讀完整個數（`while (isdigit) num = num * 10 + ...`），別一位一位當成獨立數。\n- 負號 / 一元正負、空白、優先級相同時的左結合，都是常見坑。\n- 複雜度：每個字元 / token 只處理一次，`O(n)`；兩個棧空間 `O(n)`。\n\n## C++17 模板\n\n以下用 1106. 解析布林表示式 展示用堆疊做表示式解析的核心寫法。\n\n**完整題目**：給一個布林運算式字串，由 `t`（真）、`f`（假）、`!`（非）、`&`（與）、`|`（或）以及括號、逗號組成，例如 `&(t, f, t)`、`!(f)`。求其求值結果（true / false）。\n\n:::example 用 stack 模擬運算式求值：遇 ')' 彈出操作數並依運算符計算結果\n**線索**：巢狀括號的求值順序「最內層先算」，正是堆疊的工作：把字元一路壓堆，遇到 `)` 就把這一層的運算元彈出，套用括號前的運算子，再把結果壓回。\n\n```cpp\nclass Solution {\npublic:\n    // 用 stack 模擬運算式求值：遇 ')' 彈出操作數並依運算符計算結果\n    bool parseBoolExpr(string expression) {\n            vector<char> st;\n            for (char c : expression) {\n                    if (c == ',') {\n                            continue;  // 逗號只是分隔符，略過\n                    }\n                    if (c != ')') {\n                            st.push_back(c);  // 將運算符與操作數壓棧\n                            continue;\n                    }\n                    // 遇到 ')' 時：收集括號內所有操作數\n                    vector<char> operands;\n                    while (st.back() != '(') {\n                            operands.push_back(st.back());\n                            st.pop_back();\n                    }\n                    st.pop_back();        // 彈出 '('\n                    char op = st.back();  // 取出括號前的運算符 '!'/'&'/'|'\n                    st.pop_back();\n                    char res = 't';\n                    if (op == '!') {\n                            res = operands[0] == 't' ? 'f' : 't';  // NOT：反轉單一操作數\n                    } else if (op == '&') {\n                            // AND：有任一 'f' 結果即為 'f'\n                            for (char o : operands) {\n                                    if (o == 'f') {\n                                            res = 'f';\n                                    }\n                            }\n                    } else {\n                            // OR：有任一 't' 結果即為 't'\n                            res = 'f';\n                            for (char o : operands) {\n                                    if (o == 't') {\n                                            res = 't';\n                                    }\n                            }\n                    }\n                    st.push_back(res);  // 將計算結果壓回棧\n            }\n            return st.back() == 't';\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- 逗號只是分隔符，直接略過。\n- 非 `)` 的字元（含運算子、括號、t / f）一律壓堆。\n- 遇到 `)` 時，彈出到 `(` 為止收集本層運算元，再彈掉 `(` 與其前的運算子。\n- 依運算子求值後，把單一結果字元壓回堆疊，供外層繼續使用。\n\n## 時間與空間複雜度\n\n| 題目 | 方法 | 時間 | 空間 |\n| --- | --- | --- | --- |\n| 150 RPN | 單一數值堆疊 | $O(n)$ | $O(n)$ |\n| 227（無括號） | 單一數值堆疊 | $O(n)$ | $O(n)$；可優化到 $O(1)$（見下） |\n| 224（有括號、只有 `+ -`） | 符號堆疊 | $O(n)$ | $O(\\text{深度})$ |\n| 224 + 227 合體 | 遞迴下降 / 狀態堆疊 | $O(n)$ | $O(\\text{深度})$ |\n| 394 字串解碼 | 狀態堆疊 | $O(\\text{輸出長度})$ | $O(\\text{輸出長度})$ |\n| 8 / 65 | 狀態機 | $O(n)$ | $O(1)$ |\n| 726 原子的數量 | 堆疊 + `map` | $O(n^2)$ 或 $O(n\\log n)$ | $O(n)$ |\n\n**227 可以做到 $O(1)$ 空間**：不必真的存下所有加法項，只要維護「已完成項的總和 `sum`」與「當前項的值 `cur`」兩個變數即可——**這是定理 1 不變量的直接推論（我們只需要總和，不需要個別項）。** 又一次「同質元素只問聚合值 ⇒ 堆疊可降級」的例子（4.1、4.4 都出現過）。\n\n**394 的複雜度是輸出長度而非輸入長度**：`10[10[10[ab]]]` 的輸入是常數級，輸出是 2000 個字元。**估計複雜度時要以輸出為準**，這也是為什麼這類題目會限制輸出長度。\n\n**726 的 $O(n^2)$ 來自字串拼接與 `map` 合併**：把子層的 `map` 併入父層時，最壞情況每層都要複製整個 `map`。用「一個全域 `map` + 乘數堆疊」可以降到 $O(n\\log n)$。\n\n**遞迴下降的深度風險**：`(((...)))` 巢狀 $10^5$ 層時遞迴會爆棧。**顯式堆疊沒有這個問題**，這是它唯一的實質優勢；其餘情況遞迴下降都更好寫。\n\n## 常見錯誤與邊界條件\n\n- 求值前務必先彈掉左括號，再彈運算子，順序錯就會讀到錯字元。\n- `&` 預設 true 再遇 f 轉 false；`|` 預設 false 再遇 t 轉 true，邏輯對稱好記。\n- 運算元數量不定，要用迴圈彈到 `(`，不可寫死取兩個。\n\n## 常見變形\n\n- 逆波蘭求值（150）：遇運算子就彈出兩個運算元計算。\n- 基本計算器（224 / 227）：用堆疊處理括號與運算子優先級。\n- 巢狀字串解碼（394）：遇 `]` 彈出重複次數與字串片段展開。\n\n## 與相似技巧的比較\n\n- **與 4.4 括號**：4.4 只判斷結構是否合法，本節在合法結構上求值。**定理 2 顯示兩者的合法性刻畫是同一條「堆疊高度前綴約束」。**\n- **與 4.3 鄰項消除**：4.3 的消除是無結構的（只看相鄰），本節有優先級與巢狀。**看到優先級就別想套 4.3 的模板。**\n- **與遞迴**：遞迴下降與顯式堆疊嚴格等價，堆疊格 = 呼叫框架，格內容 = 區域變數。**先用遞迴下降寫對，再視深度需求改寫成堆疊。**\n- **與 4.2 設計題**：394、726 的堆疊放的是「整包狀態」，與 4.2 的 636（呼叫鏈）同一種用法。\n- **與雙堆疊法（調度場演算法）**：處理任意優先級表時的通用解——一個運算元堆疊、一個運算子堆疊，遇到優先級不高於棧頂的運算子就先結算。**題目只有兩層優先級時（227），單堆疊法更短；優先級超過兩層或要支援右結合、一元運算子時，雙堆疊更不容易錯。**\n- **與狀態機（8、65）**：這兩題沒有巢狀，只有「合法的字元順序」，用 DFA（狀態轉移表）最清楚。**沒有巢狀就不需要堆疊——別把所有解析題都當成括號題。**\n\n## 代表例題\n\n**題號**：8、65、150、224、227\n\n用堆疊保存尚未匹配或尚未結算的元素；練習每次 push/pop 對不變式的影響。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 8 | [字串轉換整數 (atoi)](https://leetcode.cn/problems/string-to-integer-atoi) |  |\n| 65 | [有效數字](https://leetcode.cn/problems/valid-number) |  |\n| 150 | [逆波蘭表示式求值](https://leetcode.cn/problems/evaluate-reverse-polish-notation) |  |\n| 224 | [基本計算器](https://leetcode.cn/problems/basic-calculator) |  |\n| 227 | [基本計算器 II](https://leetcode.cn/problems/basic-calculator-ii) |  |\n| 394 | [字串解碼](https://leetcode.cn/problems/decode-string) |  |\n| 439 | [三元表示式解析器](https://leetcode.cn/problems/ternary-expression-parser) |  |\n| 591 | [標籤驗證器](https://leetcode.cn/problems/tag-validator) |  |\n| 726 | [原子的數量](https://leetcode.cn/problems/number-of-atoms) |  |\n| 736 | [Lisp 語法解析](https://leetcode.cn/problems/parse-lisp-expression) |  |\n| 770 | [基本計算器 IV](https://leetcode.cn/problems/basic-calculator-iv) |  |\n| 772 | [基本計算器 III](https://leetcode.cn/problems/basic-calculator-iii) |  |\n| 1006 | [笨階乘](https://leetcode.cn/problems/clumsy-factorial) | 1408 |\n| 1087 | [花括號展開](https://leetcode.cn/problems/brace-expansion) | 1480 |\n| 1096 | [花括號展開 II](https://leetcode.cn/problems/brace-expansion-ii) | 2349 |\n| 1106 | [解析布林表示式](https://leetcode.cn/problems/parsing-a-boolean-expression) | 1880 |\n| 1597 | [根據中綴表示式構造二叉表示式樹](https://leetcode.cn/problems/build-binary-expression-tree-from-infix-expression) |  |\n| 1628 | [設計帶解析函式的表示式樹](https://leetcode.cn/problems/design-an-expression-tree-with-evaluate-function) |  |\n| 1896 | [反轉表示式值的最少操作次數](https://leetcode.cn/problems/minimum-cost-to-change-the-final-value-of-expression) | 2532 |\n| 3749 | [計算有效表示式](https://leetcode.cn/problems/evaluate-valid-expressions) |  |\n\n## 本節重點速查\n\n- **動筆前先寫文法**：`expr → term (('+'|'-') term)*`、`term → factor (('*'|'/') factor)*`、`factor → number | '(' expr ')' | '-' factor`。優先級 = 層級，左結合 = 迴圈。\n- **兩個原理**：優先級決定「何時可安全結算」（後面不可能出現綁更緊的東西時）；巢狀決定「何時保存現場」。\n- 227 的不變量：`sum(st)` = 已讀前綴之值。`+/-` 推入新項、`*//` 就地改 `st.back()`。**前提是 `*//` 優先級更高且左結合。** 只要總和 ⇒ 可降級成兩個變數，$O(1)$ 空間。\n- 150 的合法性（定理 2）：運算元 $+1$、二元運算子 $-1$，**每個前綴和 $\\ge 1$ 且總和 $= 1$**；求值時**先彈出的是右運算元**。\n- 394：堆疊存「父層殘缺結果 + 重複次數」，`cur` 存當前層；**兩者永遠差一層**。複雜度以**輸出長度**計。\n- 8 的溢位檢查必須在乘法**之前**：`res > (INT_MAX - d) / 10`；負數方向的上界是 $2147483648$，不是 `INT_MAX`。\n- 多位數要一次讀完整個 token。整數除法方向（截斷 vs 向下）要看題目，不要假設。\n- 遞迴下降最好寫、受深度限制；顯式堆疊不怕深度。優先級超過兩層或有右結合時改用雙堆疊（調度場）。\n- 沒有巢狀的解析題（8、65）用狀態機，不需要堆疊。\n",
        },
        {
          id: 33,
          title: "4.6 對頂堆疊",
          summary:
            "## 這個技術解決什麼問題\n\n序列中間的插入與刪除很貴：`std::vector` 要搬動後方一整段，$O(n)$；$q$ 次操作就是 $O(nq)$。2296（設計一個文字編輯器）有 $2\\times10^5$ 次操作、文字長度也到 $2\\times10^5$，暴力必定超時。\n\n**對頂結構的觀察是：如果所有操作都發生在「同一個移動中的分界點」附近，就把序列從分界處剖成兩半，各自用一個堆疊裝，讓分界點永遠位於兩個棧頂之間。**\n\n於是：\n- 在分界處插入 / 刪除 = 對左棧 `push_back` / `pop_back`，$O(1)$；\n- 分界點左移 / 右移一格 = 從一個棧頂搬一個元素到另一個棧頂，$O(1)$。\n\n原本 $O(n)$ 的中間操作全變成棧頂操作。這個結構在文字編輯器裡叫 **gap buffer**（間隙緩衝區），是真實編輯器（Emacs）用了幾十年的資料結構。\n\n**同一個想法換一個排序維度就是「對頂堆」**：分界不再是「位置」而是「大小」，兩個堆各裝一半，堆頂之間就是中位數（295、480、1825）。**「把序列從關鍵分界剖成兩半，讓昂貴的操作退化成端點操作」——這句話才是本節的內容，堆疊只是其中一種實現。**\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「對頂堆疊」想：\n\n- **有一個「游標 / 分界」會左右移動，且要在分界處頻繁插入或刪除。** 2296「設計一個文字編輯器」就是游標 + 中間編輯的標準形。\n- **在序列中間操作很貴，但若拆成「分界兩側」就能變成棧頂操作。** 這正是對頂結構的價值：\n\n:::example 嗅到味道：所有編輯都集中在「游標附近」這個分界點\n```cpp\n// 嗅到味道：所有編輯都集中在「游標附近」這個分界點\n// left.top();  right.top();   // 游標兩側最近的元素都在棧頂，O(1) 可達\n```\n:::\n\n\n\n- **要維護「一半小、一半大」的分界量（中位數、第 K 大）並動態更新。** 此時改用「對頂堆」（max-heap + 小根堆），靠搬堆頂維持兩側平衡，思路與對頂棧一致。\n- **暴力做法是每次都搬動分界後方的一整段（`O(n)`），總體 `O(n^2)`；** 對頂結構把每次操作均攤成 `O(1)`～`O(log n)`。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的「分界 / 游標」是什麼？它兩側各放進哪個棧（或哪個堆）？\n2. 分界移動時，我把元素從哪個頂端搬到哪個頂端？\n3. 我要的答案，是不是只看分界兩側的棧頂 / 堆頂就能取得？\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n本節談「對頂堆疊」：用兩個開口相對的堆疊，中間夾著一個「分界 / 游標」，需要時把元素從一個棧的頂端搬到另一個棧的頂端，就能讓「游標左右移動」「在中間插入 / 刪除」都變成 `O(1)` 的棧頂操作。\n\n:::example 對頂堆疊：游標夾在兩個棧頂之間\n最典型的例子是「設計一個文字編輯器」（LeetCode 2296）。一行文字加一個游標，要支援：游標左移、右移、在游標處輸入字元、刪除游標左邊的字元。如果用一個普通字串，在中間插入 / 刪除得搬動後面一大串，`O(n)`。對頂堆疊的巧思是：**把游標左邊的字放進「左棧」（棧頂緊貼游標），右邊的字放進「右棧」（棧頂也緊貼游標）。** 於是游標永遠在「兩個棧頂之間」。\n\n```cpp\n// 對頂堆疊：游標夾在兩個棧頂之間\n// 文字 = (左棧由底到頂) + 游標 + (右棧由頂到底)\nstack<char> left;   // 游標左邊，棧頂 = 游標左側第一個字\nstack<char> right;  // 游標右邊，棧頂 = 游標右側第一個字\n```\n:::\n\n\n\n:::example 游標左移：把左棧頂搬到右棧頂（游標往左跨過一個字）\n有了這個排版，每個操作都只是棧頂搬動：\n\n```cpp\n// 游標左移：把左棧頂搬到右棧頂（游標往左跨過一個字）\nif (!left.empty()) {\n    right.push(left.top());\n    left.pop();\n}\n// 輸入字元 c：直接壓進左棧頂（在游標左側出現）\nleft.push(c);\n// 刪除游標左邊：彈掉左棧頂\nif (!left.empty()) {\n    left.pop();\n}\n```\n:::\n\n\n\n核心不變式是：**左棧頂與右棧頂分別是游標左、右最近的字元**，所以一切編輯都發生在「兩個棧的頂端」，全部 `O(1)`。要輸出游標附近的字，也只要看左棧頂端那幾個。這個「兩個容器相對、靠搬移棧頂維持分界」的想法，也延伸到「對頂堆 / 兩個堆」維護中位數：小的一半放 max-heap、大的一半放小根堆，分界就是中位數。\n\n## 狀態／資料結構定義\n\n**對頂堆疊（2296）**\n- `std::string left`：游標**左側**的文字，由底到頂 = 由左到右（**順序**）。\n- `std::string right`：游標**右側**的文字，由底到頂 = 由右到左（**逆序**）。\n- 完整文字 $= \\texttt{left} + \\mathrm{reverse}(\\texttt{right})$；游標恰在兩者之間。\n\n用 `std::string` 而非 `std::stack<char>`：`push_back`／`pop_back`／`back` 齊全，且連續記憶體、可 `substr`，最後回傳「游標左邊最多 10 個字元」時直接 `left.substr(...)`。\n\n**四個操作**\n\n| 操作 | 實作 | 成本 |\n| --- | --- | --- |\n| `addText(t)` | 逐字 `left.push_back` | $O(\\lvert t\\rvert)$ |\n| `deleteText(k)` | 從 `left` 尾端刪 $\\min(k, \\lvert\\texttt{left}\\rvert)$ 個 | $O(\\min(k, \\lvert\\texttt{left}\\rvert))$ |\n| `cursorLeft(k)` | 搬 $\\min(k, \\lvert\\texttt{left}\\rvert)$ 個字元 `left` → `right` | 同上 |\n| `cursorRight(k)` | 搬 $\\min(k, \\lvert\\texttt{right}\\rvert)$ 個字元 `right` → `left` | 同上 |\n\n四個操作都回傳「游標左邊最多 10 個字元」，即 `left.substr(left.size() - min<size_t>(10, left.size()))`。\n\n**注意 `right` 是逆序存放**，這是整個結構的關鍵設計：只有逆序，游標右移才會是「從 `right` 的棧頂取」；若順序存放，右移就得從 `right` 的**頭部**取，退化成 $O(n)$。\n\n**對頂堆（中位數，295）**\n- `priority_queue<int> lo`（大根堆）裝較小的一半；`priority_queue<int, vector<int>, greater<>> hi`（小根堆）裝較大的一半。\n- 大小約束：$\\lvert\\texttt{lo}\\rvert = \\lvert\\texttt{hi}\\rvert$ 或 $\\lvert\\texttt{lo}\\rvert = \\lvert\\texttt{hi}\\rvert + 1$。\n- 中位數：奇數個時 `lo.top()`，偶數個時 $(\\texttt{lo.top()} + \\texttt{hi.top()})/2$。\n\n## 不變量或正確性證明\n\n### 定理 1（對頂堆疊的表示不變量）\n\n**不變量 (I)**：$\\text{文字} = \\texttt{left} + \\mathrm{reverse}(\\texttt{right})$，且游標位置 $= \\lvert\\texttt{left}\\rvert$。\n\n*維持*：逐操作檢查。\n\n- `addText(t)`：語意是在游標處插入 $t$ 並把游標移到 $t$ 之後。新文字 $= \\texttt{left} + t + \\mathrm{reverse}(\\texttt{right})$，新游標 $= \\lvert\\texttt{left}\\rvert + \\lvert t\\rvert$。實作把 $t$ 逐字接到 `left` 尾端，恰好得到 (I) ✔\n- `deleteText(k)`：刪掉游標左邊 $\\min(k, \\lvert\\texttt{left}\\rvert)$ 個字元；`left` 尾端就是游標左邊最近的字元，故 `pop_back` 正確 ✔\n- `cursorLeft(k)`：游標左移一格意即「原本在游標左邊最近的字元，現在變成游標右邊最近的字元」。`left.back()` 搬到 `right` 的尾端；由於 `right` 是**逆序**，其尾端正是「游標右邊最近的」，故 (I) 保持 ✔\n- `cursorRight(k)`：對稱 ✔\n\n$\\square$\n\n**這條不變量點名了它依賴的前提：所有插入與刪除都發生在游標處。** 若題目允許「刪除第 $j$ 個字元」這種與游標無關的隨機位置操作，兩個堆疊就得先把游標移過去，單次 $O(n)$，優勢消失——**那時要換平衡樹（10.1 Splay）或塊狀鏈表。** 對頂結構的全部價值建立在「操作具有局部性」這個假設上。\n\n### 定理 2（複雜度：為什麼這是「攤還」而不是「每次 $O(1)$」）\n\n游標移動 $k$ 格要搬 $k$ 個字元，**單次並非 $O(1)$**。整體複雜度是\n\n$$O\\Big(\\sum_{\\text{各次操作}} \\min(k_i,\\ \\text{可移動量})\\Big) \\;\\le\\; O\\Big(\\sum_i k_i\\Big).$$\n\n2296 的約束保證「所有操作的 $k$ 與文字長度總和 $\\le 2\\times10^5$」，故總成本線性於輸入規模。\n\n**$\\min$ 這個夾取不只是防越界，它是複雜度論證的一部分**：若使用者呼叫 `cursorLeft(10^9)` 而文字只有 10 個字元，實際只搬 10 次。**沒有這個夾取，程式不只會越界，複雜度上界也不再成立。**\n\n*對照*：用 `std::list<char>` + 迭代器當游標，插入／刪除是真正的 $O(1)$，但游標移動仍是 $O(k)$（要逐個 `++it`），而且節點分散、快取表現差好幾倍。**兩者的漸近複雜度相同，對頂堆疊贏在常數。**\n\n### 定理 3（對頂堆的分割不變量與插入正確性）\n\n**不變量**：\n- (P1) $\\max(\\texttt{lo}) \\le \\min(\\texttt{hi})$（每個小半邊的元素都不大於每個大半邊的元素）；\n- (P2) $\\lvert\\texttt{lo}\\rvert - \\lvert\\texttt{hi}\\rvert \\in \\{0, 1\\}$。\n\n**插入 $x$ 的標準寫法**（順序不可交換）：\n```\nlo.push(x);                      // 先無條件丟進 lo\nhi.push(lo.top()); lo.pop();     // 把 lo 的最大值移到 hi\nif (hi.size() > lo.size()) { lo.push(hi.top()); hi.pop(); }   // 平衡\n```\n\n*(P1) 的維持證明*：設插入前 (P1) 成立，記 $L = \\texttt{lo}$、$H = \\texttt{hi}$，$\\max L \\le \\min H$。\n\n第一步後 `lo` $= L \\cup \\{x\\}$；令 $m = \\max(L \\cup \\{x\\})$。第二步把 $m$ 移到 `hi`。\n\n- 新的 `lo` $= (L\\cup\\{x\\})\\setminus\\{m\\}$。若 $m = x$，則新 `lo` $= L$，$\\max L \\le \\min H \\le \\min(H\\cup\\{m\\})$ ✔\n- 若 $m \\in L$，則新 `lo` $= (L\\setminus\\{m\\})\\cup\\{x\\}$。$\\max(L\\setminus\\{m\\}) \\le \\max L \\le \\min H$ ✔；而 $x \\le m = \\max L \\le \\min H$ ✔\n\n兩種情形都得 $\\max(\\text{新 lo}) \\le \\min H \\le \\min(H \\cup \\{m\\})= \\min(\\text{新 hi})$。第三步只是把 `hi` 的最小值搬回 `lo`，$\\min H' \\le \\min(H'\\setminus\\{\\min H'\\})$ 且它 $\\ge \\max(\\text{lo})$，(P1) 仍成立。$\\square$\n\n**「先丟進 `lo`、再把 `lo` 的最大值移給 `hi`」這個繞路是必要的。** 直覺寫法「$x \\le \\texttt{lo.top()}$ 就放 `lo`，否則放 `hi`」也對，但需要額外處理 `lo` 為空的情形，而且平衡邏輯要分四種分支。**上面那個三行寫法無分支、無空堆特判，正確性就是上面這段證明。**\n\n*中位數的正確性*：由 (P1)，`lo` 恰是整體最小的 $\\lvert\\texttt{lo}\\rvert$ 個元素。由 (P2)，總數 $n$ 為奇數時 $\\lvert\\texttt{lo}\\rvert = (n+1)/2$，故 `lo.top()` 是第 $\\lceil n/2\\rceil$ 小 = 中位數；$n$ 為偶數時 $\\lvert\\texttt{lo}\\rvert = \\lvert\\texttt{hi}\\rvert = n/2$，中位數是第 $n/2$ 與第 $n/2+1$ 小的平均，即 `(lo.top() + hi.top()) / 2.0`。$\\square$\n\n**溢位提醒**：`(lo.top() + hi.top()) / 2.0` 在兩者皆為 $\\pm2^{31}$ 級時，`int` 相加會溢位。**寫成 `(lo.top() + 0.0 + hi.top()) / 2` 或先轉 `long long`。**\n\n*480（滑動視窗中位數）的額外難點*：要刪除離開視窗的元素，但堆不支援任意刪除。**解法是懶刪除（6.6）**——維護一個「待刪除計數表」，只在元素浮到堆頂時才真正丟棄，同時用兩個計數器追蹤兩邊的「有效大小」。**這是本節與 6.6 的接點。**\n\n## 逐步演算法\n\n:::example 狀態表示：游標左側、右側各一個棧\n**1. 狀態表示：兩個相對的棧，分界是游標。** 約定好哪個棧裝左半、哪個裝右半，且兩邊棧頂都緊貼分界。\n\n```cpp\n// 狀態表示：游標左側、右側各一個棧\nstack<char> left, right;  // 分界（游標）就在 left.top() 與 right.top() 之間\n```\n:::\n\n\n\n:::example 更新：游標右移（把右棧頂搬回左棧頂）\n**2. 更新流程：移動分界 = 搬棧頂；增刪 = 動棧頂。** 游標移動就把元素在兩棧之間搬一個；插入 / 刪除只動靠游標那側的棧頂。\n\n```cpp\n// 更新：游標右移（把右棧頂搬回左棧頂）\nif (!right.empty()) {\n    left.push(right.top());\n    right.pop();\n}\n```\n:::\n\n\n\n:::example 讀取：取游標左側最近的 k 個字（注意取出是反序）\n**3. 答案讀取：直接從棧頂取分界附近的資訊。** 例如回傳游標左邊最多 k 個字，就連續看左棧頂。\n\n```cpp\n// 讀取：取游標左側最近的 k 個字（注意取出是反序）\nstring res;\nstack<char> tmp = left;\nfor (int i = 0; i < k && !tmp.empty(); ++i) {\n    res += tmp.top();\n    tmp.pop();\n}\nreverse(res.begin(), res.end());\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 搬移 / 刪除前一定先判對應棧 `!empty()`，游標已在最邊界時操作要無副作用。\n- 兩棧的「方向」要全程一致：哪邊是左、棧頂貼著誰，不能中途搞混。\n- 複雜度：每個操作都是常數次棧頂動作，`O(1)`；整體空間 `O(n)`。\n\n## C++17 模板\n\n以下用 2296. 設計一個文字編輯器 展示對頂堆疊（雙堆疊夾游標）的核心寫法。\n\n**完整題目**：設計一個帶游標的文字編輯器，支援：`addText` 在游標左側插入文字、`deleteText(k)` 刪除游標左側最多 k 個字元並回傳實刪數、`cursorLeft(k)` / `cursorRight(k)` 移動游標 k 次，移動後回傳游標左側最多 10 個字元。\n\n:::example 雙字串模擬游標：left 存游標左側文字，right 存游標右側文字（反向）\n**線索**：游標把文字切成左右兩段，且所有操作都發生在游標附近（兩段的交界）。用兩個堆疊背對背放：左堆疊頂端 = 游標左邊一格，右堆疊頂端 = 游標右邊一格，移動就是兩堆互相搬一個元素。\n\n```cpp\n// 雙字串模擬游標：left 存游標左側文字，right 存游標右側文字（反向）\nclass TextEditor {\n    string left, right;\n\npublic:\n    void addText(string text) { left += text; }\n\n    int deleteText(int k) {\n            int d = min(k, (int)left.size());  // 實際可刪除的字數\n            left.erase(left.size() - d);\n            return d;\n    }\n\n    string cursorLeft(int k) {\n            // 將 left 尾端字元逐一移至 right 頭端，模擬游標左移\n            while (k > 0 && !left.empty()) {\n                    right.push_back(left.back());\n                    left.pop_back();\n                    --k;\n            }\n            return left.substr(\n                            max(0, (int)left.size() - 10));  // 回傳游標左側最多 10 個字元\n    }\n\n    string cursorRight(int k) {\n            // 將 right 尾端字元逐一移回 left，模擬游標右移\n            while (k > 0 && !right.empty()) {\n                    left.push_back(right.back());\n                    right.pop_back();\n                    --k;\n            }\n            return left.substr(\n                            max(0, (int)left.size() - 10));  // 回傳游標左側最多 10 個字元\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- `left` 是游標左側文字，尾端就是游標位置；`right` 反向保存游標右側文字。\n- `addText` 直接接到 `left` 尾端，等同在游標前插入。\n- `cursorLeft` 把 `left` 尾端字元搬到 `right` 尾端，游標左移；`cursorRight` 反之。\n- 每次回傳 `left` 末尾最多 10 個字元，用 `substr` 取尾段。\n\n## 時間與空間複雜度\n\n| 結構 | 操作 | 時間 | 空間 |\n| --- | --- | --- | --- |\n| 對頂堆疊 | 游標處插入／刪除 | $O(1)$（每字元） | $O(n)$ |\n| 對頂堆疊 | 游標移動 $k$ | $O(\\min(k, \\text{可移動量}))$ | — |\n| 對頂堆疊 | 2296 整體 | $O(\\sum k_i + \\sum \\lvert t_i\\rvert)$ | $O(n)$ |\n| `std::list` + 迭代器 | 同上 | 相同漸近，常數差數倍 | $O(n)$（額外指標） |\n| 平衡樹 / 塊狀鏈表 | 任意位置插入刪除 | $O(\\log n)$ / $O(\\sqrt n)$ | $O(n)$ |\n| 對頂堆 | 插入 / 查中位數 | $O(\\log n)$ / $O(1)$ | $O(n)$ |\n| 對頂堆 + 懶刪除（480） | 滑動一步 | 攤還 $O(\\log n)$ | $O(n)$ |\n\n**對頂堆疊沒有 $\\log$**，這是它相對平衡樹的最大優勢；代價是**只能在一個移動中的分界點附近操作**。**看到「游標」「當前位置」「分界」這種字眼就想它；看到「任意位置」就放棄它。**\n\n**2296 每次操作回傳字串**：回傳長度上限 10，故不影響漸近複雜度；但**不要每次都建立整個文字再截尾**，那會是 $O(n)$ 每次。\n\n## 常見錯誤與邊界條件\n\n- `right` 是反序保存，搬移時只動兩堆的頂端，務必方向一致。\n- 取尾段 10 字時用 `max(0, size - 10)` 防止下標為負。\n- `deleteText` 要回傳「實際刪除數」，先和 `left.size()` 取 min。\n\n## 常見變形\n\n- 瀏覽器上一頁 / 下一頁、復原 / 重做，都可用對頂堆疊建模。\n- 雙端佇列同樣能實作，但兩堆疊的語意更貼近游標。\n- 把字元換成操作記錄，即成命令模式的歷史堆疊。\n\n## 與相似技巧的比較\n\n- **與 4.1／4.2 單一堆疊**：單一堆疊只有一個端點，只能表達「最近的」。對頂結構有**兩個**端點夾住一個分界，能表達「分界兩側」。\n- **與 5.3 雙端佇列**：`deque` 也是兩端可操作，但它的兩端是**序列的頭尾**；對頂堆疊的兩個棧頂是**序列中間的同一個位置**。**用途完全不同：`deque` 處理「首尾」，對頂處理「中間」。**\n- **與對頂堆（295、480、1825、703）**：同一個「剖成兩半」的想法，分界從**位置**換成**大小**。**位置分界 ⇒ 堆疊（$O(1)$）；大小分界 ⇒ 堆（$O(\\log n)$），因為大小關係需要維護有序性。**\n- **與 6.3 第 K 小／大**：703（資料流中第 K 大）是對頂堆的退化版——只需要一個大小固定為 $K$ 的小根堆，另一半根本不用存。**當只查一側時，另一側可以丟掉。**\n- **與 10.1 平衡樹（Splay）**：平衡樹支援任意位置的插入刪除與分裂合併，$O(\\log n)$。**對頂堆疊是它在「操作具有局部性」時的 $O(1)$ 特化。** 局部性一旦失效（隨機位置操作），就得付出 $\\log$。\n- **與 6.6 懶刪除堆**：對頂堆遇到「要刪除任意元素」（480 滑動視窗）時必須配懶刪除，因為 `priority_queue` 沒有 `erase`。\n\n## 代表例題\n\n**題號**：2296\n\n用堆疊保存尚未匹配或尚未結算的元素；練習每次 push/pop 對不變式的影響。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 2296 | [設計一個文字編輯器](https://leetcode.cn/problems/design-a-text-editor) | 1912 |\n\n## 本節重點速查\n\n- 核心想法：**把序列從關鍵分界剖成兩半，讓昂貴的中間操作退化成兩個端點操作。**\n- 2296 的不變量：文字 $= \\texttt{left} + \\mathrm{reverse}(\\texttt{right})$，游標 $= \\lvert\\texttt{left}\\rvert$。**`right` 必須逆序存**，否則游標右移退化成 $O(n)$。\n- 四個操作全是棧頂操作；`min(k, 可移動量)` 的夾取**既防越界也是複雜度上界的依據**。\n- 複雜度是 $O(\\sum k_i)$，**不是每次 $O(1)$**；靠題目對 $\\sum k$ 的約束才成立。\n- 前提是**所有操作都在游標附近**；隨機位置操作要換平衡樹或塊狀鏈表。\n- 對頂堆：`lo` 大根堆裝小的一半、`hi` 小根堆裝大的一半；不變量 $\\max(\\texttt{lo}) \\le \\min(\\texttt{hi})$ 與大小差 $\\in\\{0,1\\}$。\n- 插入的三行無分支寫法（**先丟 `lo`、把 `lo` 的最大值移給 `hi`、再視情況搬回**）正確性見定理 3，別自己改順序。\n- 中位數：奇數 `lo.top()`，偶數 `(lo.top() + hi.top()) / 2.0`（**注意 `int` 相加溢位**）。\n- 要刪除任意元素（480）就配懶刪除（6.6）。\n- **位置分界用堆疊（$O(1)$），大小分界用堆（$O(\\log n)$）。**\n",
        },
        {
          id: 34,
          title: "4.7 單調堆疊",
          summary:
            "## 這個技術解決什麼問題\n\n單調堆疊回答的是一族「**最近的、比我大（或小）的元素在哪裡**」的問題：\n\n- **直接問距離／位置**：739（每日溫度）、496／503（下一個更大元素）、901（股票價格跨度）、1019。\n- **問「以我為極值能延伸多遠」的區間貢獻**：84（柱狀圖中最大的矩形）、85、907（子陣列的最小值之和）、1856、2104、3113。\n- **在字典序上做貪心刪除**：402（移掉 K 位數字）、316（去除重複字母）、321。\n- **其他被改寫成上述形式的題**：42（接雨水）、1124（表現良好的最長時間段）、962（最大寬度坡）、2866（美麗塔 II）。\n\n暴力對每個元素往兩邊掃是 $O(n^2)$。單調堆疊的關鍵觀察是：\n\n> **如果 $j < i$ 且 $a_j \\le a_i$，那麼對任何 $t > i$，$j$ 都不可能是「$t$ 左邊最近的更大元素」——因為 $i$ 擋在中間，而且 $i$ 不比 $j$ 小。$j$ 從此以後永遠沒用了，可以丟掉。**\n\n把「已經沒用的元素」丟掉，剩下的自然就是單調的。$O(n^2)$ 的重複掃描之所以能壓成 $O(n)$，正是因為那些被反覆掃到的元素其實早該被淘汰。\n\n**辨識訊號**：發現自己在寫「對每個 $i$，往左（右）一路掃到某個條件成立為止」，而且不同的 $i$ 掃過的區域高度重疊——那就是單調堆疊。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「單調堆疊」想：\n\n- **要對每個元素找「左／右第一個更大或更小的元素」。** 像「每日溫度」「下一個更大元素」就是教科書型。\n- **要算「以某元素為最小／最大值能延伸多遠」的區間貢獻。** 例如柱狀圖最大矩形、子陣列最小值之和。\n- **暴力是對每個元素往兩邊掃 `O(n^2)`，需要壓到 `O(n)`。** 一旦發現「很多重複的往回掃」，就是單調棧的味道：\n\n:::example 嗅到味道：暴力往回找第一個更大值，O(n^2)\n```cpp\n// 嗅到味道：暴力往回找第一個更大值，O(n^2)\n// for j = i-1 .. 0: if (a[j] > a[i]) { ...; break; }\n// 改用單調棧，把回掃均攤成 O(1)\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的棧要維持遞增還是遞減？（取決於要找更大還是更小）\n2. 棧裡存的是下標還是值？我需要算距離嗎？\n3. 元素被彈出的那一刻，我確定了它的哪一側邊界？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n- 本節題單尚未配置代表題；建議先用章節標題關鍵字到題庫搜尋同型題，並記錄暴力解與正解差異。\n\n## 核心想法與直覺\n\n本節要解決的場景是：對陣列裡的每個元素，找出它「左邊或右邊第一個比它大（或比它小）的元素」。例如要算每天之後「第幾天股價才更高」（每日溫度），或一根長方形往左右能延伸到哪裡（柱狀圖最大矩形）。\n\n最直覺的暴力做法是：對每個元素，往左或往右一路掃，找到第一個符合條件的就停。最壞情況每個元素都要掃整條陣列，總共 `O(n^2)`，太慢。\n\n單調堆疊（monotonic stack）的核心想法是：用一個棧（stack，後進先出）保存「目前還沒找到答案、還在等待邊界的元素」，而且讓棧裡的值維持遞增或遞減的順序（這就叫「單調」）。當新元素破壞了這個順序時，被擠出去的那些元素，剛好就是「找到答案的時刻」。\n\n:::example 維持遞減棧：新元素比棧頂大，就一路彈出並結算\n舉個例子，找「右邊第一個更大的元素」，我們維持一個「由底到頂遞減」的棧：\n\n```cpp\n// 維持遞減棧：新元素比棧頂大，就一路彈出並結算\nstack<int> st;  // 存「還沒找到右邊更大值」的下標\nfor (int i = 0; i < n; ++i) {\n    while (!st.empty() && a[i] > a[st.top()]) {\n        ans[st.top()] = i;  // a[i] 就是 st.top() 右邊第一個更大值\n        st.pop();\n    }\n    st.push(i);  // 自己也進棧，等待之後的更大值\n}\n```\n:::\n\n\n\n為什麼快？因為每個元素「進棧一次、出棧一次」，總共最多 `2n` 次操作，所以整體是 `O(n)`。被彈出的瞬間就確定了它的答案，不需要回頭重掃。\n\n棧裡通常存**下標而非值**，因為有了下標既能取值 `a[st.top()]`，又能算「距離」（例如相差幾天、矩形有多寬）。\n\n## 狀態／資料結構定義\n\n**存下標，不要存值。** 因為幾乎所有題目最後都要算距離、寬度或區間長度，只存值就找不回位置。需要值時寫 `a[st.back()]`。\n\n**四種組合，先想清楚要哪一種：**\n\n| 要找的東西 | 掃描方向 | 彈出條件 | 棧內單調性 |\n| --- | --- | --- | --- |\n| 右邊第一個**更大** | 由左至右 | `a[st.back()] < a[i]` | 遞減 |\n| 右邊第一個**更小** | 由左至右 | `a[st.back()] > a[i]` | 遞增 |\n| 左邊第一個**更大** | 由右至左 | `a[st.back()] < a[i]` | 遞減 |\n| 左邊第一個**更小** | 由右至左 | `a[st.back()] > a[i]` | 遞增 |\n\n**記法**：**要找「更大」就維持遞減棧**（棧裡留下的都是還沒被超越的大值）；找「更小」就維持遞增棧。方向決定的是「左邊」還是「右邊」。\n\n**一趟掃描其實可以同時得到兩側邊界。** 由左至右掃時，當 $i$ 把 $j$ 彈出：$i$ 就是 $j$ 的右邊界，而**彈出後的新棧頂**就是 $j$ 的左邊界。84、907、1856 都靠這一點，不必掃兩趟。\n\n**哨兵。** 84 在陣列兩端各加一根高度 0 的柱子（或起始壓入下標 $-1$、結尾用一個高度 $-\\infty$ 的虛擬元素），可以省掉「掃完後堆疊還有殘留」與「左邊界不存在」兩種特判。**這與 2.1 的 $s_0=0$、4.4 的哨兵 $-1$ 是同一種設計紀律。**\n\n**相等元素的比較要用 `<` 還是 `<=`，是正確性問題**，見定理 3。\n\n## 不變量或正確性證明\n\n以「找右邊第一個更大元素」為例（遞減棧，彈出條件 `a[st.back()] < a[i]`）。\n\n### 定理 1（主不變量）\n\n處理完下標 $i$ 之後，堆疊由底到頂的下標 $j_1 < j_2 < \\dots < j_k$ 恰好是\n\n$$\\{\\, j \\le i \\;:\\; \\forall\\, t \\in (j, i],\\ a_t < a_j \\,\\},$$\n\n也就是「從 $j$ 往右看到 $i$ 為止，沒有任何元素 $\\ge a_j$」的那些下標；並且 $a_{j_1} \\ge a_{j_2} \\ge \\dots \\ge a_{j_k}$。\n\n*證明（對 $i$ 歸納）。*\n\n基底 $i=0$：堆疊只有 $\\{0\\}$，區間 $(0,0]$ 為空，條件真空成立。\n\n歸納步：設處理完 $i-1$ 後不變量成立。處理 $i$ 時，先彈出所有滿足 $a_{j} < a_i$ 的棧頂，再壓入 $i$。\n\n- *（被彈出的確實該走）* 若 $a_j < a_i$，則 $t = i \\in (j, i]$ 且 $a_i > a_j$，$j$ 不再滿足集合條件，必須移除。\n- *（留下的確實該留）* 未被彈出的 $j$ 滿足 $a_j \\ge a_i$。由歸納假設 $\\forall t\\in(j,i-1],\\ a_t < a_j$，再加上 $a_i \\le a_j$，得 $\\forall t \\in (j,i],\\ a_t < a_j$ ⋯ 這裡若 $a_i = a_j$ 則是 $a_i \\le a_j$ 而非嚴格小於，對應的集合定義要改成 $a_t \\le a_j$——**這正是定理 3 要處理的相等問題**；先假設元素互異。\n- *（$i$ 自己）* 區間 $(i,i]$ 為空，$i$ 必屬於集合。\n- *（單調性）* 由集合條件：若 $j_p < j_q$ 同在堆疊中，則 $j_q \\in (j_p, i]$，故 $a_{j_q} < a_{j_p}$。$\\square$\n\n### 定理 2（正確性：彈出的那一刻就是答案）\n\n**當 $i$ 把 $j$ 彈出時，$i$ 是「$j$ 右邊第一個滿足 $a_i > a_j$ 的下標」；而彈出 $j$ 之後的新棧頂 $j'$（若存在）是「$j$ 左邊第一個滿足 $a_{j'} > a_j$ 的下標」。**\n\n*右邊界*：$j$ 在被彈出前仍在堆疊中，由定理 1，$\\forall t\\in(j, i-1],\\ a_t < a_j$；而 $a_i > a_j$。故 $i$ 是第一個。$\\square$\n\n*左邊界*：$j'$ 與 $j$ 在堆疊中相鄰且 $j' < j$。由定理 1 的集合條件套用在 $j'$ 上（取 $i := j$）：$\\forall t \\in (j', j],\\ a_t < a_{j'}$，特別地 $(j', j)$ 之間沒有任何元素 $\\ge a_{j'} > a_j$⋯ 更直接地：假設存在 $t \\in (j', j)$ 使 $a_t > a_j$，那麼 $t$ 不會被 $j$ 彈出（$a_t > a_j$），所以 $t$ 會留在堆疊中夾在 $j'$ 與 $j$ 之間，與「相鄰」矛盾。故 $j'$ 是左邊第一個更大者。$\\square$\n\n**這條定理是單調堆疊真正的價值所在**：一次彈出**同時**確定了 $j$ 的左右兩個邊界，於是「以 $a_j$ 為極值的最大延伸區間」是 $(j', i)$，寬度 $i - j' - 1$。84 的矩形面積、907 的貢獻計數、1856 的區間乘積全都是這一行。\n\n### 定理 3（相等元素：計數題必須破對稱，極值題不必）\n\n考慮 907（子陣列的最小值之和）：要算 $\\sum_{l \\le r} \\min(a_l,\\dots,a_r)$。做法是對每個 $j$ 算「有多少個子陣列以 $a_j$ 為最小值」，乘上 $a_j$ 後加總。\n\n若陣列有重複值，一個子陣列可能有多個位置達到最小值。**若兩側都用嚴格比較，這個子陣列會被計算多次；若兩側都用非嚴格比較，會被計算 0 次。**\n\n*正確做法（把每個子陣列指派給它最小值的**最左**出現位置）*：定義\n- $L_j$ = 左邊第一個滿足 $a_{L_j} < a_j$ 的下標（**嚴格**小於，不存在則 $-1$）；\n- $R_j$ = 右邊第一個滿足 $a_{R_j} \\le a_j$ 的下標（**非嚴格**，不存在則 $n$）。\n\n則以 $j$ 為「最左最小值」的子陣列個數是 $(j - L_j)\\cdot(R_j - j)$。\n\n*證明（恰好一次）。* 取任一子陣列 $[l, r]$，令 $m$ 為其最小值，$p$ 為 $[l,r]$ 中**最左**達到 $m$ 的位置。\n- *$[l,r]$ 在 $p$ 被計到*：對 $t \\in [l, p)$ 有 $a_t > m$（否則 $p$ 不是最左），故 $L_p < l$，即 $l \\in (L_p, p]$ ✔；對 $t \\in (p, r]$ 有 $a_t \\ge m$，故 $R_p > r$，即 $r \\in [p, R_p)$ ✔。\n- *$[l,r]$ 不會在其他 $q$ 被計到*：若 $q \\ne p$ 且 $l \\le q \\le r$。若 $a_q > m$，則 $[l,r]$ 中存在 $a_p = m < a_q$，故 $L_q \\ge l$ 或 $R_q \\le r$，落在計數範圍外。若 $a_q = m$ 則 $q > p$（$p$ 最左），此時 $a_p \\le a_q$ 且 $p < q$，故 $L_q \\ge p \\ge l$，於是 $l \\notin (L_q, q]$ ✔。$\\square$\n\n**「一嚴格一非嚴格」不是玄學，它就是『把每個子陣列指派給最左（或最右）的極值位置』這個劃分。** 換成「$L$ 非嚴格、$R$ 嚴格」也對，那是指派給最右位置。**兩邊同時嚴格或同時非嚴格則必錯。**\n\n*反觀 84（柱狀圖最大矩形）*：答案取 $\\max$ 而非求和。若有相等高度的柱子，某些柱子算出的寬度會偏小，**但相等高度中最左（或最右）那一根一定會算出完整寬度**，故 $\\max$ 不受影響。**極值題可以容忍重複與遺漏（只要最優解至少被算到一次），計數題不行。** 這條區別值得單獨記住。\n\n### 定理 4（攤還分析：為什麼有內層 `while` 還是 $O(n)$）\n\n用**位能法**。令位能 $\\Phi = $ 堆疊當前元素個數（恆 $\\ge 0$，初始為 0）。\n\n第 $i$ 次迭代若彈出 $p_i$ 個元素、壓入 1 個：\n- 實際成本 $c_i = p_i + 1$；\n- 位能變化 $\\Delta\\Phi_i = 1 - p_i$；\n- **攤還成本** $\\hat{c}_i = c_i + \\Delta\\Phi_i = (p_i + 1) + (1 - p_i) = 2$。\n\n故總實際成本\n$$\\sum_i c_i = \\sum_i \\hat{c}_i - (\\Phi_n - \\Phi_0) = 2n - \\Phi_n \\le 2n .$$\n$\\square$\n\n**這個論證的關鍵是「彈出的成本被壓入時預付了」**：每個元素一生只被壓入一次、彈出一次，所以無論某一次迭代彈出多少個，總量都不超過 $n$。**看到 `for` 裡面套 `while` 就以為是 $O(n^2)$，是初學者最常見的複雜度誤判；正確的問法是「這個 `while` 消耗的是什麼資源，那個資源總共有多少」。** 同樣的論證會在 8.x 並查集的路徑壓縮、5.4 單調佇列、以及各種攤還結構重複出現。\n\n### 定理 5（402 移掉 K 位數字的貪心）\n\n要從數字串中刪掉 $k$ 位，使剩下的數字最小。\n\n*宣稱*：若存在相鄰的 $d_t > d_{t+1}$，則刪掉**第一個**這樣的 $d_t$ 是最優的。\n\n*證明。* 設第一個下降位置為 $t$，即 $d_0 \\le d_1 \\le \\dots \\le d_t$ 且 $d_t > d_{t+1}$。\n- *刪 $d_t$ 優於刪任何 $d_s$（$s > t$）*：兩者長度相同，比較字典序時前 $t$ 位相同，第 $t$ 位分別是 $d_{t+1}$（刪 $d_t$）與 $d_t$（刪 $d_s$），而 $d_{t+1} < d_t$，故刪 $d_t$ 嚴格較小。\n- *刪 $d_t$ 不劣於刪任何 $d_s$（$s < t$）*：刪 $d_s$ 後第 $s$ 位變成 $d_{s+1} \\ge d_s$，故不會比原串小；而刪 $d_t$ 使第 $t$ 位嚴格變小。逐位比較可得刪 $d_t$ 不劣。$\\square$\n\n*推論*：用遞增棧，遇到 `a[st.back()] > a[i]` 且還有刪除額度就彈出——**這正好就是「刪掉第一個下降位置」反覆執行**。若掃完仍有額度剩餘（原串本身非遞減），從尾端刪剩下的（因為此時刪最後幾位最小）。最後去掉前導零、處理空串回傳 `\"0\"`。\n\n**316（去除重複字母）在此之上多兩個約束**：每個字母恰好出現一次，且只有在「該字母後面還會再出現」時才能彈出（需要 `cnt` 陣列），已在棧中的字母要跳過（需要 `inStack` 標記）。**這兩個標記不是實作細節，它們是把貪心限制在可行域內的必要條件。**\n\n## 逐步演算法\n\n:::example 狀態表示：棧存下標，本例維持值遞減\n**1. 狀態表示：棧裡存下標，維持單調不變式。** 「不變式」就是「每一步都成立的性質」，這裡是「棧由底到頂保持遞增或遞減」。\n\n```cpp\n// 狀態表示：棧存下標，本例維持值遞減\nstack<int> st;  // st 中下標對應的 a 值，由底到頂遞減\n```\n:::\n\n\n\n:::example 更新：彈出所有比新元素小的棧頂（找右邊更大值）\n**2. 更新流程：新元素破壞單調性就彈出結算。** 掃到新元素時，把棧頂中「不再滿足單調性」的元素一路彈出；每彈出一個，就確定了它的一側邊界。\n\n```cpp\n// 更新：彈出所有比新元素小的棧頂（找右邊更大值）\nwhile (!st.empty() && a[i] > a[st.top()]) {\n    int j = st.top();\n    st.pop();\n    ans[j] = i - j;  // 例：右邊更大值的距離\n}\nst.push(i);\n```\n:::\n\n\n\n:::example 答案讀取：柱狀圖矩形寬度 = 右邊界 - 左邊界 - 1\n**3. 答案讀取：邊界由「彈出時機」決定。** 下一個更大／更小值、矩形寬度、或貢獻次數，都由「左右兩側第一個破壞單調性的位置」算出。\n\n```cpp\n// 答案讀取：柱狀圖矩形寬度 = 右邊界 - 左邊界 - 1\nint width = i - st.top() - 1;  // st.top() 是左邊界，i 是右邊界\nint area = a[j] * width;\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 遇到相等元素時，用 `<` 還是 `<=` 會改變去重與左右邊界的歸屬；用「貢獻法」算面積／個數時，常需要「一側嚴格、一側非嚴格」才不會重複計算。\n- 掃完後棧裡可能還有元素（右邊沒有更大值），要記得處理（給預設答案或補一個哨兵）。\n- 複雜度：每個下標進出棧各一次，總共 `O(n)` 時間、`O(n)` 空間。\n\n## C++17 模板\n\n以下用同型延伸示範題 496. 下一個更大元素 I 展示核心寫法。\n\n**完整題目**：對 `nums1` 中每個元素，找出它在 `nums2` 右側第一個比它大的元素，若不存在則為 `-1`。\n\n:::example 單調遞減棧：掃描 nums2，維護「尚未找到下一個更大元素」的元素\n**線索**：題目要找最近更大/更小邊界或某元素的貢獻範圍；這表示元素應在單調結構中等待被右側元素結算。\n\n```cpp\nclass Solution {\npublic:\n    // 單調遞減棧：掃描 nums2，維護「尚未找到下一個更大元素」的元素\n    vector<int> nextGreaterElement(vector<int>& nums1, vector<int>& nums2) {\n            unordered_map<int, int> nxt;  // nxt[x] = x 在 nums2 中的下一個更大元素\n            vector<int> st;\n            for (int x : nums2) {\n                    // 棧頂元素小於 x，表示 x 就是它們的下一個更大元素\n                    while (!st.empty() && st.back() < x) {\n                            nxt[st.back()] = x;\n                            st.pop_back();\n                    }\n                    st.push_back(x);\n            }\n            vector<int> ans;\n            for (int x : nums1) {\n                    ans.push_back(nxt.count(x) ? nxt[x] : -1);  // 找不到則回傳 -1\n            }\n            return ans;\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- 堆疊內維持遞減；當新元素更大時，它就是被彈出元素右側第一個更大值。\n- 容器中保存尚未結算的元素；新元素破壞單調性時，被彈出的元素就找到了右邊界。\n- 程式通常存下標而非數值，因為答案需要計算距離或回填到原位置。\n\n## 時間與空間複雜度\n\n**時間 $O(n)$**（定理 4 的位能論證），**空間 $O(n)$**（最壞情況所有元素都在堆疊裡，例如嚴格遞減的輸入之於遞減棧）。\n\n**與暴力的落差**：$O(n^2) \\to O(n)$。$n = 10^5$ 時 $10^{10} \\to 10^5$。\n\n**503 環狀陣列**：把陣列在概念上倍長，迴圈跑 $2n$ 次、下標取 `i % n`，只在前 $n$ 次壓入（或全部壓入但只在 $i < n$ 時記錄）。**仍是 $O(n)$，常數 ×2；不需要真的複製陣列。** 正確性前提：任一元素的「下一個更大」若存在，必在繞一圈之內。\n\n**42 接雨水的三種解法**：\n| 方法 | 時間 | 空間 | 說明 |\n| --- | --- | --- | --- |\n| 前綴最大 + 後綴最大 | $O(n)$ | $O(n)$ | 按「列」算，最直觀 |\n| 相向雙指標 | $O(n)$ | $O(1)$ | 按「列」算，空間最優 |\n| 單調遞減棧 | $O(n)$ | $O(n)$ | 按「橫層」算 |\n**三者複雜度同級，雙指標空間最優。** 單調堆疊解在這題不是最佳選擇，但它是理解「凹槽 = 彈出時左右兩個邊界」的好練習，且能推廣到 84。\n\n**907／2104 的替代解**：也可以用「貢獻法 + 分治」或「笛卡兒樹」，但單調堆疊最短。**2104（子陣列範圍和）= 子陣列最大值之和 $-$ 最小值之和**，跑兩次單調堆疊即可，注意兩次的嚴格／非嚴格要各自破對稱。\n\n## 常見錯誤與邊界條件\n\n- 先決定維持遞增還是遞減。\n- 相等元素用 `<` 還是 `<=` 會影響去重與邊界。\n- 需要寬度時入棧索引而不是值。\n- 哨兵可簡化最後清棧。\n\n## 常見變形\n\n- 循環陣列可掃兩遍。\n- 用索引入棧以計算距離或寬度。\n- 貢獻法中分別求左/右第一個嚴格或非嚴格邊界。\n\n## 與相似技巧的比較\n\n- **與 4.3 鄰項消除**：程式碼形狀幾乎相同（`while` 彈棧頂），但**目的相反**：4.3 彈出是「丟棄」，本節彈出是「結算答案」。**寫的時候先問自己：彈出的元素是被刪掉，還是它的答案在此刻確定了？**\n- **與 5.4 單調佇列**：單調佇列多了「從頭部淘汰過期元素」，用來解滑動視窗極值。**單調堆疊的窗口左界由資料決定（第一個更大者），單調佇列的左界由下標決定（固定寬度）。** 這是選型的判準。\n- **與 2.x 前綴和**：1124（表現良好的最長時間段）與 962（最大寬度坡）都是「先做前綴和／原序列，再在其上用單調棧找最遠的合法左端點」。**單調堆疊在這裡維護的是「候選左端點集合」，而且候選集必然單調——因為若 $s_{j_1} \\ge s_{j_2}$ 且 $j_1 < j_2$，$j_2$ 永遠不會比 $j_1$ 更優。** 這是「淘汰無用元素」這個核心觀察的另一種形態。\n- **與 9.7 ST 表／線段樹**：單調堆疊是**離線**的（一次掃描算出所有元素的答案），ST 表能回答**任意區間**的極值查詢。**只要每個元素各問一次，用單調堆疊；要回答任意給定區間，用 ST 表。**\n- **與笛卡兒樹**：單調堆疊的過程其實在建笛卡兒樹（每次彈出對應建立父子關係）。84、907 都可以理解成在笛卡兒樹上做遞迴。**知道這層關係，遇到「以每個元素為極值的區間」的複雜變體時會有第二條路。**\n- **與 DP**：901（股票價格跨度）也能寫成 $\\mathrm{dp}[i] = 1 + \\sum \\mathrm{dp}[\\dots]$ 的跳躍遞推，那其實就是單調堆疊的「路徑壓縮」版本。**兩者本質相同。**\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 42 | [接雨水](https://leetcode.cn/problems/trapping-rain-water) |  |\n| 84 | [柱狀圖中最大的矩形](https://leetcode.cn/problems/largest-rectangle-in-histogram) |  |\n| 85 | [最大矩形](https://leetcode.cn/problems/maximal-rectangle) |  |\n| 316 | [去除重複字母](https://leetcode.cn/problems/remove-duplicate-letters) |  |\n| 402 | [移掉 K 位數字](https://leetcode.cn/problems/remove-k-digits) |  |\n| 456 | [132 模式](https://leetcode.cn/problems/132-pattern) |  |\n| 496 | [下一個更大元素 I](https://leetcode.cn/problems/next-greater-element-i) |  |\n| 503 | [下一個更大元素 II](https://leetcode.cn/problems/next-greater-element-ii) |  |\n| 739 | [每日溫度](https://leetcode.cn/problems/daily-temperatures) |  |\n| 901 | [股票價格跨度](https://leetcode.cn/problems/online-stock-span) |  |\n| 907 | [子陣列的最小值之和](https://leetcode.cn/problems/sum-of-subarray-minimums) |  |\n| 962 | [最大寬度坡](https://leetcode.cn/problems/maximum-width-ramp) |  |\n| 1019 | [鏈結串列中的下一個更大節點](https://leetcode.cn/problems/next-greater-node-in-linked-list) |  |\n| 1124 | [表現良好的最長時間段](https://leetcode.cn/problems/longest-well-performing-interval) |  |\n| 1793 | [好子陣列的最大分數](https://leetcode.cn/problems/maximum-score-of-a-good-subarray) |  |\n| 1856 | [子陣列最小乘積的最大值](https://leetcode.cn/problems/maximum-subarray-min-product) |  |\n| 2104 | [子陣列範圍和](https://leetcode.cn/problems/sum-of-subarray-ranges) |  |\n| 2454 | [下一個更大元素 IV](https://leetcode.cn/problems/next-greater-element-iv) |  |\n| 2866 | [美麗塔 II](https://leetcode.cn/problems/beautiful-towers-ii) |  |\n| 3113 | [邊界元素是最大值的子陣列數目](https://leetcode.cn/problems/find-the-number-of-subarrays-where-boundary-elements-are-maximum) |  |\n\n**建議順序**：先 739 / 496 建立骨架 → 503 練環狀 → 84 / 42 練「彈出時的左右邊界」→ 907 / 2104 練相等元素的破對稱 → 402 / 316 練字典序貪心 → 962 / 1124 練「前綴和上的候選集」→ 1856 / 2454 / 2866 / 3113 綜合。\n\n## 本節重點速查\n\n- 核心觀察：**$j < i$ 且 $a_j \\le a_i$ ⇒ $j$ 永遠不會再是任何人的「左邊最近更大者」，可以丟掉。** 丟掉沒用的，剩下的自然單調。\n- **存下標不存值**（要算距離）。找「更大」用遞減棧、找「更小」用遞增棧；掃描方向決定是左邊還是右邊。\n- **定理 2**：$i$ 彈出 $j$ 時，$i$ 是 $j$ 的右邊界、**彈出後的新棧頂**是 $j$ 的左邊界——一次彈出同時得到兩側，84／907／1856 全靠這行。\n- **定理 3（相等元素）**：計數題必須「一側嚴格、一側非嚴格」，這等於把每個子陣列指派給最左（或最右）的極值位置；**兩側同時嚴格或同時非嚴格必錯**。極值題（84）取 $\\max$，可以容忍重複。\n- **定理 4（攤還）**：位能 $\\Phi=$ 棧大小，攤還成本恆為 2，總計 $\\le 2n$。**`for` 套 `while` 不等於 $O(n^2)$——要問「這個 `while` 消耗什麼資源，總量多少」。**\n- 兩端加哨兵（84 加高度 0 的柱子）省掉「殘留」與「邊界不存在」兩種特判。\n- 503 環狀：跑 $2n$ 次、下標取模，不必複製陣列。\n- 402 的貪心：**刪掉第一個下降位置**（定理 5）；316 另需 `cnt` 與 `inStack` 把貪心限制在可行域。\n- 選型：每個元素各問一次 ⇒ 單調堆疊；任意區間查極值 ⇒ ST 表；固定寬度視窗 ⇒ 單調佇列（5.4）。\n",
        },
      ],
    },
    {
      id: 35,
      title: "5. 佇列",
      summary:
        "佇列是 FIFO，最典型的用途是 BFS——先進先出保證了**按層擴張**，所以第一次到達某個狀態時的步數就是最短步數。相對地，堆疊常用在 DFS，但那個棧通常由遞迴代勞，不必自己維護。BFS 的完整練習見 [網格圖題單](https://leetcode.cn/circle/discuss/YiXPXW/) 與 [圖論題單](https://leetcode.cn/circle/discuss/01LUak/)。\n\n**如何挑子單元**\n\n- **5.1 基礎**：純 FIFO 的使用與 BFS 骨架。\n- **5.2 設計**：要自己實作佇列語意的題目，例如循環佇列、最近請求次數；重點在於決定「哪些舊資料該淘汰」。\n- **5.3 雙端佇列**：兩端都能進出。0-1 BFS 就靠它——邊權 0 的鄰居推到**前端**、邊權 1 的推到**後端**，就能用 BFS 做出 Dijkstra 的效果。\n- **5.4 單調佇列**：佇列內保持單調，用來 `O(1)` 取得**滑動視窗最大／最小值**。訊號是「定長視窗 + 求極值」，或 DP 轉移要在一段區間裡取最佳前驅。\n\n**和堆的分界**：如果彈出順序由「進入順序」決定，用佇列；由「元素大小」決定，用堆（第 6 章）。單調佇列之所以比堆快，正是因為它同時利用了這兩個順序。\n\n**延伸閱讀**：佇列與 BFS 的最小對照題是 [LeetCode 102 二元樹的層序遍歷](https://leetcode.cn/problems/binary-tree-level-order-traversal/)；佇列保存的正是「已發現、尚未展開」的前沿。\n",
      children: [
        {
          id: 36,
          title: "5.1 基礎",
          summary:
            "## 這個技術解決什麼問題\n\n佇列（queue）是 **FIFO：先進先出**。它處理的是「**順序必須被保留、而且只從兩端動**」的問題：\n\n- **時間視窗**：933（最近的請求次數）、362（敲擊計數器）、346（資料流中的移動平均）——過期的元素一定是最早進來的那些，所以只要從頭端丟。\n- **輪轉／循環**：649（Dota2 參議院）、950（按遞增順序顯示卡牌）——「輪到誰」本身就是佇列。\n- **層序遍歷 / BFS**：所有「最少步數且每步代價相同」的題。\n- **資源池**：379（電話目錄管理系統）——可用資源排隊等待分配。\n- **帶惰性刪除的「第一個滿足條件者」**：1429（第一個唯一數字）。\n\n**它和堆疊的差別不只是「另一端」。** 堆疊保留的是**巢狀歷史**（4.2），佇列保留的是**到達順序**。一旦題目說「先來先服務」「按原順序」「一層一層」，答案就是佇列。\n\n**反過來的辨識訊號**：如果你發現自己在用 `vector` 的 `erase(begin())`（$O(n)$）反覆刪頭，那就該換成佇列或雙指標。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「佇列」想：\n\n- **要按「先到先處理」的順序消費資料。** 排隊、任務調度、訊息處理都是。\n- **要求「最少步數 / 最短路徑」且每步代價相同。** 這是 BFS 的招牌，底層就靠佇列保證層序。\n- **資料流中要維護「最近一段時間 / 一個視窗」內的元素，並清掉過期的。** 像「最近的請求次數」「敲擊計數器」：\n\n:::example 嗅到味道：最早進來的最早過期，從頭端清\n```cpp\n// 嗅到味道：最早進來的最早過期，從頭端清\n// while (q.front() 已過期) q.pop();\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的佇列元素要存哪些欄位，才能恢復出完整狀態？\n2. 我是「先到先處理」（FIFO）還是其實需要排序（那要用堆）？\n3. 過期 / 已訪問的判斷，發生在入隊還是出隊？\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n本節介紹最基礎的資料結構之一：佇列（queue，又叫隊列）。它的規則只有一句話：**先進先出（FIFO，First In First Out）**，就像排隊買票，先排的人先服務，後到的人排在尾巴。\n\n:::example 佇列的基本操作：尾進、頭出\n佇列只開放兩端：從尾端「入隊（push）」，從頭端「出隊（pop）」，並可查看頭端目前是誰（front）。C++ 的 `std::queue` 直接提供這些操作，每個都是 `O(1)`：\n\n```cpp\n// 佇列的基本操作：尾進、頭出\nqueue<int> q;\nq.push(1);          // 入隊：1\nq.push(2);          // 入隊：1 2\nint f = q.front();  // 看頭：1，O(1)\nq.pop();            // 出隊：移除頭端 1，剩 2\n```\n:::\n\n\n\n為什麼需要「先進先出」？因為很多問題天生就有「處理順序」：\n\n- **層序擴展（BFS，廣度優先搜尋）**：從起點出發，先到的格子先被處理，這樣「第一次抵達終點」必然是最短步數。\n- **資料流 / 時間視窗**：事件按時間先後到達，最早到的也最早過期，正好用頭端清掉。\n\n:::example 資料流視窗：頭端清過期，回傳視窗內數量\n以「最近的請求次數」為例，每次請求帶一個時間戳，我們要回答「最近 3000 毫秒內有幾次請求」。做法是把時間戳推進佇列，再從頭端把太舊（過期）的彈掉：\n\n```cpp\n// 資料流視窗：頭端清過期，回傳視窗內數量\nq.push(t);                      // 新請求時間 t 入隊\nwhile (q.front() < t - 3000) {  // 頭端是最早的，過期就丟\n    q.pop();\n}\nreturn q.size();  // 剩下的就是視窗內請求數\n```\n:::\n\n\n\n關鍵直覺：因為時間戳是遞增推入的，**最早入隊的一定最早過期**，所以只需從頭端清理，永遠不用翻找中間，效率很高。\n\n## 狀態／資料結構定義\n\n**C++ 的三種選擇：**\n\n| 型別 | 頭部彈出 | 隨機存取 | 可遍歷 | 適用 |\n| --- | --- | --- | --- | --- |\n| `std::queue<T>`（底層 `deque`） | $O(1)$ | ✗ | ✗ | 語意最清楚的純 FIFO |\n| `std::deque<T>` | $O(1)$ | ✓ | ✓ | 需要看中間元素、或兩端都要動 |\n| `vector<T>` + `head` 指標 | $O(1)$ 攤還 | ✓ | ✓ | 需要保留全部歷史（如 BFS 回溯路徑） |\n\n**選型判準**：**需要在彈出後仍然讀到已彈出的元素，就用 `vector` + `head`**（例如 BFS 結束後要沿 `parent` 回溯，或 649 需要下標）。只進不回頭就用 `queue`。\n\n**兩種常見的封裝：**\n\n1. **視窗佇列**：`queue<int> q;`，狀態是「所有仍在視窗內的事件時間戳」。加入新事件後，`while (!q.empty() && q.front() <= t - W) q.pop();`。答案就是 `q.size()`（933、362）。\n2. **增量統計佇列**：除了佇列本身，另外維護一個聚合量（`sum`、`cnt`）。彈出時扣除、加入時累加，使查詢 $O(1)$（346）。\n\n**BFS 的兩種寫法**：\n\n- **不分層**：`q` 存 `(node, dist)` 或另開 `dist[]` 陣列。\n- **分層**：`for (int sz = q.size(); sz--; )` 一次處理完整層。**`sz` 必須在迴圈開始前快照**，否則本層新推入的節點會被算進本層。\n\n## 不變量或正確性證明\n\n### 定理 1（視窗佇列：為什麼只要從頭端丟）\n\n933／362 的不變量是：**每次操作結束後，`q` 恰好等於集合 $\\{t' : t' \\text{ 已發生且 } t' > t - W\\}$，且由頭到尾遞增。**\n\n*證明*：由歸納。加入 $t$ 時先 push 再 pop。\n- *遞增性*：依賴**前提「時間戳單調不減」**（題目保證 `ping` 的參數嚴格遞增）。既然 $t \\ge$ 所有已存元素，push 後仍遞增。\n- *剛好丟掉該丟的*：由遞增性，過期元素（$t' \\le t - W$）構成一個**前綴**。所以「反覆彈頭直到頭端不過期」剛好移除全部過期元素、不會誤傷。$\\square$\n\n**這條定理完全依賴「時間戳單調」這個前提。** 若事件可能亂序抵達（例如亂序的日誌），過期元素就不再是前綴，必須改用 `multiset` 或樹狀陣列（9.x），複雜度升為 $O(\\log n)$。**這是佇列能用與不能用的分界線，值得每次都確認一次。**\n\n*推論（346 移動平均）*：`sum` 隨 push／pop 增減，任一時刻 `sum` $=$ 佇列元素之和。**用 `double` 累加會有浮點誤差累積**（加了又減，殘差不會自動抵銷）；若元素是整數，`sum` 應宣告為 `long long`，最後才除。\n\n### 定理 2（BFS 的層序不變量）\n\n**在 BFS 的任一時刻，佇列中的節點距離只可能取兩個相鄰值 $d$ 與 $d+1$，且所有 $d$ 排在所有 $d+1$ 前面。**\n\n*證明（歸納）*。初始只有源點（距離 0）。設當前不變量成立，佇列為 $[d,\\dots,d,\\,d+1,\\dots,d+1]$。彈出頭端（距離 $d$）並推入它的未訪問鄰居（距離 $d+1$），推到尾端——仍是「若干 $d$ 後接若干 $d+1$」。當所有 $d$ 被彈完，佇列變成純 $d+1$，形態退化為 $[d+1,\\dots]$，不變量對 $(d+1, d+2)$ 繼續成立。$\\square$\n\n*推論*：**節點按距離非遞減的順序出隊**，所以第一次訪問到終點時的距離就是最短距離。**這個推論依賴「每條邊權重相同」**；邊權不同就必須換成 Dijkstra（優先佇列，6.x）。\n\n*推論 2*：`for (int sz = q.size(); sz--; )` 這一圈處理的恰好是距離 $d$ 的全部節點——這就是分層寫法成立的理由。\n\n### 定理 3（649 Dota2 參議院：貪心 + 雙佇列）\n\n兩派輪流行動，每個議員可以禁掉一名對方議員；求哪一派獲勝。\n\n*宣稱*：**輪到某議員時，禁掉「循環順序上排在他後面、最近的一名對手」是最優的。**\n\n*證明（交換論證）*。設當前議員屬 R，他有兩個選擇：禁最近的對手 $x$，或禁更遠的對手 $y$。\n- 禁 $x$：$y$ 仍在，但在本輪中 $y$ 行動的時機晚於 $x$ 原本的時機。\n- 禁 $y$：$x$ 仍在，$x$ 會在**更早**的時刻行動並禁掉一名 R。\n\n把「禁 $x$」的後續策略平移到「禁 $y$」的局面上，R 方在「禁 $y$」的局面下至少要多承受一次來自 $x$ 的即時打擊，而所得（$y$ 被移除）在時序上更晚才有價值。故禁 $x$ 不劣。$\\square$\n\n*實作對應*：用兩個佇列 `qR`、`qD` 存各派議員的**下標**（初始已按下標遞增）。每回合比較兩隊頭端 $i_R$、$i_D$：**下標小者行動**，禁掉對方的頭端（直接 `pop`），自己則以 `i + n` 重新入隊。\n\n*不變量*：**兩個佇列各自遞增，且兩隊頭端的較小者就是「全域下一個行動的人」。** 加上 `n` 再入隊，等價於把他放到下一輪的相同相對位置——這保持了循環順序。任一佇列空掉時，另一派獲勝。\n\n### 定理 4（1429 第一個唯一數字：惰性刪除的弱不變量）\n\n維護 `queue<int> q` 與 `unordered_map<int,int> cnt`。`add(x)` 時 `cnt[x]++` 並 `q.push(x)`；`showFirstUnique()` 時 `while (!q.empty() && cnt[q.front()] > 1) q.pop();`。\n\n*不變量（弱）*：**`q` 是「所有仍唯一的數字，按首次加入順序排列」的一個超集合**，且非唯一元素只可能堆積在前段（因為一旦某元素變成非唯一，它永遠不會再變回唯一——`cnt` 只增不減）。\n\n*正確性*：由「`cnt` 單調不減」這個**前提**，「唯一」是一個**單向失效**的性質。因此彈頭清理是安全的：被彈掉的元素永遠不會再需要。清理後的頭端若存在，就是最早加入且仍唯一者。$\\square$\n\n**「弱不變量 + 單向失效」是惰性刪除的通用模式**，4.2 的 1172、6.6 的延遲刪除堆都是同一招。**它的前提永遠是：被判定失效的元素不會復活。**\n\n### 定理 5（950 按遞增順序顯示卡牌：逆向模擬）\n\n正向難以直接構造，改成**把輸出過程反過來跑**：把牌值由大到小處理，每步先「把佇列尾端搬到頭端」再「把當前牌壓到頭端」。\n\n*證明*：正向操作是「取頭端輸出、再把新的頭端移到尾端」。其逆操作恰為「把尾端移到頭端、再在頭端插入」。由歸納，反向跑完 $n$ 步得到的排列，正向執行必產生遞增輸出。$\\square$\n\n**「正向難、逆向易」在模擬題中很常見；判準是原操作是否可逆。** 這裡每一步都是雙射，所以可逆。\n\n## 逐步演算法\n\n:::example 狀態表示：BFS 佇列存節點與所在層\n**1. 狀態表示：想清楚佇列元素要存什麼。** 元素必須包含「足以恢復狀態」的欄位。BFS 常存「節點 + 距離 / 層數」；資料流常存「時間戳」。\n\n```cpp\n// 狀態表示：BFS 佇列存節點與所在層\nqueue<pair<int, int>> q;  // {節點, 距離}\nq.push({start, 0});\n```\n:::\n\n\n\n:::example 更新：取頭端，擴展鄰居\n**2. 更新流程：彈出頭端，再擴展下一批。** 每次取出頭端後，把它能到達、且合法又沒走過的新狀態入隊。\n\n```cpp\n// 更新：取頭端，擴展鄰居\nauto [u, d] = q.front();\nq.pop();\nfor (int v : adj[u]) {\n    if (!visited[v]) {\n        visited[v] = true;  // 入隊就標記，避免重複\n        q.push({v, d + 1});\n    }\n}\n```\n:::\n\n\n\n:::example 答案讀取：BFS 第一次到目標即最短\n**3. 答案讀取：依題型不同。** BFS 類「第一次抵達目標」就是最短步數；資料流類則由「頭端」代表最早仍有效的元素。\n\n```cpp\n// 答案讀取：BFS 第一次到目標即最短\nif (u == target) {\n    return d;\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- `visited` 的標記時機要在「入隊時」而非「出隊時」，否則同一節點可能被重複入隊。\n- 若狀態多了維度（鑰匙、方向、剩餘次數），`visited` 也要對應多維，否則會誤判已訪問。\n- 複雜度：每個狀態最多進出佇列各一次，BFS 為 `O(V + E)`；資料流操作均攤 `O(1)`。\n\n## C++17 模板\n\n以下用本節代表題 933. 最近的請求次數 展示核心寫法。\n\n**完整題目**：設計計數器，對每次請求時間 `t`，回傳 `[t - 3000, t]` 內的請求數。\n\n:::example 移除超出時間窗口（早於 t-3000）的舊請求\n**線索**：題目限制連續子陣列/子字串，且合法性可透過移動左端恢復，這是滑動視窗的線索。\n\n```cpp\nclass RecentCounter {\n    queue<int> q;  // 儲存 [t-3000, t] 範圍內的請求時間戳\n\npublic:\n    int ping(int t) {\n            q.push(t);\n            // 移除超出時間窗口（早於 t-3000）的舊請求\n            while (q.front() < t - 3000) {\n                    q.pop();\n            }\n            return q.size();  // 當前窗口內的請求數\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- 佇列按時間遞增保存請求；每次查詢前移除過期時間，剩下的就是目前視窗內的請求。\n- 右端加入新元素後更新窗口狀態；若條件被破壞，就移動左端直到恢復合法。\n- 每個下標最多被加入與移出一次，因此整體是線性掃描。\n\n## 時間與空間複雜度\n\n| 操作 | `std::queue` / `deque` |\n| --- | --- |\n| `push` / `pop` / `front` / `size` | $O(1)$ |\n| 遍歷 | $O(n)$（`queue` 不支援，須換 `deque`） |\n\n**視窗佇列（933、362）**：單次 `ping` 最壞 $O(n)$（一次清掉大量過期元素），但**攤還 $O(1)$**——每個元素只入隊一次、出隊一次，位能論證與 4.7 定理 4 完全相同。**$q$ 次操作總計 $O(q)$。** 空間 $O(W)$ 級（視窗內的事件數）。\n\n**BFS**：$O(V + E)$ 時間、$O(V)$ 空間。**注意 `visited` 必須在「入隊時」標記，不是「出隊時」**，否則同一節點可能被推入多次，最壞退化到 $O(V^2)$ 甚至指數。\n\n**649**：每回合至少移除一人，故總回合數 $< n$；每回合 $O(1)$。總計 $O(n)$，空間 $O(n)$。\n\n**1429**：`add` 為 $O(1)$；`showFirstUnique` 攤還 $O(1)$（每個元素最多被彈一次）。\n\n**950**：排序 $O(n\\log n)$ 主導，模擬 $O(n)$。\n\n**常數提醒**：`std::queue` 預設底層是 `std::deque`，配置是分塊的，比 `vector` 稍慢但支援 $O(1)$ 頭部彈出。**在極度追求常數的場合（BFS 上百萬節點），改用 `vector<int> q; int head = 0;` 通常快 2–3 倍**，代價是記憶體不回收——但那正好讓你能事後遍歷全部訪問順序。\n\n## 常見錯誤與邊界條件\n\n- 先判斷合法性方向，再寫 while 條件。\n- 答案計數時常用 `right - left + 1` 或 `left`，兩者語意不同。\n- 頻次表字元集可用陣列，值域大時用 map。\n- 含負數時滑窗的單調性可能失效。\n\n## 常見變形\n\n- 越短越合法：違法時收縮，更新最長。\n- 越長越合法：合法時收縮，更新最短。\n- 恰好型可轉成 `atMost(k) - atMost(k - 1)`。\n\n## 與相似技巧的比較\n\n- **與 4.x 堆疊**：**LIFO 保留巢狀結構，FIFO 保留到達順序。** 「括號、遞迴、回溯、最近的未匹配者」用堆疊；「排隊、層序、最短步數、最早的未過期者」用佇列。**用堆疊做 BFS 會變成 DFS，這是最常見的張冠李戴。**\n- **與 5.3 雙端佇列**：`deque` 是佇列的超集。**只在真的需要「兩端都增刪」時才用**——用 `deque` 寫純 FIFO 沒有錯，但用 `queue` 更能向讀者宣告意圖。\n- **與 5.4 單調佇列**：單調佇列在 FIFO 的基礎上，額外從**尾端**淘汰「劣勢元素」。**普通佇列保留全部，單調佇列只保留可能成為答案的候選。**\n- **與 2.x 滑動視窗（雙指標）**：本節的視窗佇列本質就是滑動視窗。**當「視窗內容」可以只用左右指標與若干聚合量表示時，用雙指標（$O(1)$ 空間）；當你需要真的枚舉／彈出視窗內的個別元素時，用佇列。**\n- **與 6.x 優先佇列**：優先佇列按「優先級」出隊，佇列按「到達時間」出隊。**題目若說「每步代價相同」用佇列（BFS）；「代價不同」用優先佇列（Dijkstra）。** 0-1 邊權則用雙端佇列 BFS（5.3），這是三者之間的橋樑。\n- **與環形陣列（5.2 的 622）**：固定容量時，用 `vector` + 頭尾下標取模可以完全避免動態配置。**上限已知就用環形陣列，未知就用 `deque`。**\n\n## 代表例題\n\n**題號**：346、362、379、649、933\n\n用佇列維持時間順序或視窗順序；練習清理過期元素與保持端點語意一致。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 346 | [資料流中的移動平均值](https://leetcode.cn/problems/moving-average-from-data-stream)（Premium） |  |\n| 362 | [敲擊計數器](https://leetcode.cn/problems/design-hit-counter)（Premium） |  |\n| 379 | [電話目錄管理系統](https://leetcode.cn/problems/design-phone-directory)（Premium） |  |\n| 649 | [Dota2 參議院](https://leetcode.cn/problems/dota2-senate) |  |\n| 933 | [最近的請求次數](https://leetcode.cn/problems/number-of-recent-calls) | 1338 |\n| 950 | [按遞增順序顯示卡牌](https://leetcode.cn/problems/reveal-cards-in-increasing-order) | 1686 |\n| 1429 | [第一個唯一數字](https://leetcode.cn/problems/first-unique-number) |  |\n| 2534 | [透過門的時間](https://leetcode.cn/problems/time-taken-to-cross-the-door) |  |\n| 3829 | [設計共享出行系統](https://leetcode.cn/problems/design-ride-sharing-system) | 1594 |\n\n## 本節重點速查\n\n- **佇列 = 保留「到達順序」；堆疊 = 保留「巢狀歷史」。** 看到「先來先服務／層序／最少步數（等權）」就是佇列。\n- **視窗佇列（定理 1）成立的前提是「時間戳單調不減」**——過期元素才會構成前綴，才能只從頭端丟。亂序輸入必須改用 `multiset` / 樹狀陣列。\n- **攤還 $O(1)$**：每個元素只入隊一次、出隊一次，位能論證同 4.7 定理 4。**`for` 裡的 `while` 不代表 $O(n^2)$。**\n- **BFS 不變量（定理 2）**：佇列中只有兩個相鄰距離、且小的在前 ⇒ 出隊順序按距離非遞減 ⇒ 首次到達即最短。**前提是邊權相同。**\n- **BFS 的 `visited` 在「入隊」時標記，不是出隊時。** 分層時 `sz = q.size()` 必須先快照。\n- **649**：雙佇列存下標，下標小者先行動、禁對方頭端、自己 `+n` 重新入隊；貪心「禁最近對手」由交換論證保證最優。\n- **惰性刪除（1429）的通用前提**：判定失效後不會復活（`cnt` 只增不減）。同模式見 4.2 的 1172、6.6。\n- **選型**：需要事後回讀已彈出的元素 ⇒ `vector` + `head`；純 FIFO ⇒ `queue`；兩端都動 ⇒ `deque`；容量固定 ⇒ 環形陣列。\n",
        },
        {
          id: 37,
          title: "5.2 設計",
          summary:
            "## 這個技術解決什麼問題\n\n設計題問的不是「怎麼算出答案」，而是「**在給定的操作集合與複雜度要求下，該用什麼狀態去表示這個抽象型別**」。本節的題目分成三類：\n\n- **用 A 模擬 B**：232（用堆疊實現佇列）、225（用佇列實現堆疊）。核心是**順序的轉換代價**。\n- **固定容量的環形結構**：622（設計循環佇列）、641（設計循環雙端佇列）。核心是**取模算術與空／滿的區分**。\n- **多容器分工**：1670（設計前中後佇列）、3508（設計路由器）。核心是**用一組不變量把「中間」或「過期」變成某個容器的端點**。\n\n**共通的解題流程**：先寫下每個操作的目標複雜度 → 反推「什麼必須是 $O(1)$ 可取得的」 → 設計狀態使那些量都落在某個容器的端點或某個維護好的變數上 → 最後才寫程式。**先想不變量，再寫程式碼，順序反過來一定會漏 case。**\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就是「設計題」：\n\n- **題目直接要你「設計 / 實作一個 XXX」**，並列出要支援的方法清單與容量限制。\n- **用 A 結構模擬 B 結構**：用佇列實現堆疊、用堆疊實現佇列、用陣列實現迴圈佇列 / 雙端佇列。\n- **要在固定容量下高效增刪兩端**，提示你用指標循環或多容器分工：\n\n:::example 嗅到味道：固定容量、兩端操作 → 迴圈緩衝\n```cpp\n// 嗅到味道：固定容量、兩端操作 → 迴圈緩衝\n// tail = (tail + 1) % capacity;\n```\n:::\n\n\n\n確認自己有沒有設計對，問三個問題：\n\n1. 每個操作「應該回傳什麼、改變什麼」，我都寫清楚了嗎？\n2. 我的不變式是什麼？每個操作做完後它還成立嗎？\n3. 哪些操作可能是 `O(n)`，能不能靠攤還或多容器分工壓到 `O(1)`？\n\n三題都答得出來，模型就設計對了。\n\n## 核心想法與直覺\n\n本節談的是「設計題」：題目不給你一個現成問題，而是要你**自己動手實作一個資料結構**，讓它支援指定的幾個操作。例如「用兩個佇列實現一個堆疊」「設計一個固定容量的迴圈佇列」。\n\n設計題的核心不是某個演算法技巧，而是：**先想清楚每個操作的語意，再選一組底層容器與不變式，讓所有操作都正確且夠快。**「不變式」就是「不管做了多少操作，都恆成立的性質」，它是設計正不正確的保證。\n\n:::example 兩個棧實現佇列：in 收、out 吐，out 空了才搬\n舉個經典例子：用兩個堆疊（stack，後進先出）實作一個佇列（先進先出）。一個 `in` 棧負責接收新元素，一個 `out` 棧負責輸出；當 `out` 空了，就把 `in` 整批倒過去——倒一次順序剛好反轉兩次，變回先進先出：\n\n```cpp\n// 兩個棧實現佇列：in 收、out 吐，out 空了才搬\nstack<int> in, out;\nvoid push(int x) { in.push(x); }  // 新元素一律進 in\nint pop() {\n    if (out.empty()) {       // out 空了才搬，攤還成本低\n        while (!in.empty()) {  // 把 in 倒進 out，順序反轉\n            out.push(in.top());\n            in.pop();\n        }\n    }\n    int x = out.top();\n    out.pop();  // out 頂端就是最早進來的\n    return x;\n}\n```\n:::\n\n\n\n這裡的不變式是：「out 裡的元素，越接近頂端越早入隊」。只要維持它，`pop` 就一定吐出最早的元素。雖然單次搬運可能搬很多個，但每個元素一生只會被搬一次，所以攤還（amortized）下來仍是 `O(1)`。\n\n:::example 迴圈佇列：指標用取餘數繞回開頭\n另一種常見設計是「迴圈佇列」：用一個固定大小的陣列，加上 `head`、`tail` 兩個指標循環移動，避免出隊後前面空間浪費：\n\n```cpp\n// 迴圈佇列：指標用取餘數繞回開頭\nint buf[N], head = 0, tail = 0, cnt = 0;\nvoid enqueue(int x) {\n    buf[tail] = x;\n    tail = (tail + 1) % N;\n    ++cnt;\n}\n```\n:::\n\n## 狀態／資料結構定義\n\n### 232 用兩個堆疊實現佇列\n\n```\nstack<int> in;   // 新元素進這裡\nstack<int> out;  // 舊元素從這裡出\n```\n**狀態解讀**：邏輯佇列（由前到後）＝ `out` 由頂到底，接上 `in` 由底到頂。\n\n### 225 用佇列實現堆疊\n\n只需一個 `queue<int> q`。`push(x)` 時先 `q.push(x)`，再把前面的 `q.size()-1` 個元素依序彈出並重新入隊（「自旋」）。**狀態解讀**：`q` 的頭端永遠是「最後推入的元素」。\n\n### 622 設計循環佇列\n\n```\nvector<int> buf;   // 大小 = capacity\nint head = 0;      // 隊首下標\nint cnt = 0;       // 目前元素個數\n```\n**用 `cnt` 而不是 `tail`。** 只存 `head`／`tail` 時，`head == tail` 同時代表「空」與「滿」，必須犧牲一個格子來區分（容量 $k$ 就開 $k+1$）。**維護 `cnt` 可以用滿全部空間，而且 `isEmpty`／`isFull` 直接讀，少一類 bug。** 寫入位置是 `(head + cnt) % capacity`。\n\n### 641 設計循環雙端佇列\n\n同上，多一個前端插入：`head = (head - 1 + capacity) % capacity`。**必須加 `capacity` 再取模**——C++ 的 `%` 對負數回傳負數（`-1 % 5 == -1`），這是本節最常見的 runtime error 來源。\n\n### 1670 設計前中後佇列\n\n```\ndeque<int> A, B;   // A 是前半，B 是後半；邏輯序列 = A 接 B\n```\n**不變量 (I)**：$|A| = \\lfloor n/2 \\rfloor$，$|B| = \\lceil n/2 \\rceil$，其中 $n = |A| + |B|$。\n**這條不變量的用途**：把「中間」變成 `A.back()` 或 `B.front()`，於是四個位置（前、中、後）全都是某個 `deque` 的端點，全部 $O(1)$。\n\n### 3508 設計路由器\n\n```\nqueue<array<int,3>> fifo;                    // 記憶體上限的淘汰順序\nset<array<int,3>> seen;                      // 去重（同 (src,dst,ts) 只留一份）\nunordered_map<int, vector<int>> ts;          // 每個目的地的時間戳（遞增）\nunordered_map<int, int> startIdx;            // 該目的地已被淘汰掉的前綴長度\n```\n**`ts[d]` 用 `vector` 加一個起始下標，不用 `deque` 也不真的刪除**——因為要對它做二分搜尋。**惰性刪除 + 有序陣列 = 可二分的滑動視窗**，這個組合在離線題（12.x）中會再出現。\n\n## 不變量或正確性證明\n\n### 定理 1（232 的搬移時機：為什麼「只在 `out` 空時才倒」）\n\n*不變量 (Q)*：任一操作結束後，邏輯佇列由前到後 $=$ `out` 由頂到底，接上 `in` 由底到頂。\n\n*證明*。`push(x)` 把 $x$ 放到 `in` 頂端，也就是邏輯序列的最後 —— (Q) 保持。\n`pop()`／`peek()` 時若 `out` 非空，取 `out.top()` 即邏輯最前者 —— (Q) 保持。\n若 `out` 為空，把 `in` 整個搬到 `out`：搬移是逐個 `out.push(in.top()); in.pop();`，等於把 `in` 反轉。原本邏輯序列 $=$ `in` 由底到頂 $= (x_1, \\dots, x_m)$；搬完後 `out` 由頂到底也是 $(x_1,\\dots,x_m)$ —— (Q) 保持。$\\square$\n\n**為什麼必須「`out` 空才搬」？** 若 `out` 還有 $(y_1,\\dots,y_k)$ 而把 `in` 的 $(x_1,\\dots,x_m)$ 倒上去，`out` 由頂到底變成 $(x_1,\\dots,x_m,y_1,\\dots,y_k)$，但正確順序是 $(y_1,\\dots,y_k,x_1,\\dots,x_m)$——**順序被破壞**。這不是效率問題，是正確性問題。\n\n*攤還分析*：令位能 $\\Phi = |{\\tt in}|$。`push` 實際成本 1、$\\Delta\\Phi = +1$，攤還 2。一次觸發搬移的 `pop` 實際成本 $m+1$、$\\Delta\\Phi = -m$，攤還 1。**每個元素一生最多被搬移一次**，故 $q$ 次操作總計 $O(q)$，**攤還 $O(1)$**。\n\n### 定理 2（225 為什麼做不到攤還 $O(1)$：模擬是有方向的）\n\n232 能攤還 $O(1)$，225 不能——**這個不對稱不是巧合。**\n\n*論證（黑盒模型：只能用佇列介面 `push`／`pop`／`front`／`size`）*。設佇列中目前有 $k+1$ 個元素，我們要取出「最後推入」的那一個。由 FIFO 語意，唯一能讀到的是頭端；要讓目標元素浮到頭端，必須先把排在它前面的 $k$ 個元素取出。每次取出只能移動一個元素，故成本 $\\Omega(k)$。$\\square$\n\n**根本差別**：**堆疊的 `pop` 天生產生逆序，佇列的任何操作都保序。** 「把佇列變成堆疊」需要製造逆序，而逆序無法用保序操作攤還地製造；「把堆疊變成佇列」只需要一次逆序，而堆疊本來就免費提供。\n\n*實作上的取捨*：`push` $O(n)$／`pop` $O(1)$，或 `push` $O(1)$／`pop` $O(n)$，兩種都對，**選哪種取決於題目的操作分佈**；LeetCode 沒有給分佈，習慣寫前者（自旋在 `push`）。\n\n### 定理 3（622 環形佇列的位址不變量）\n\n*不變量 (C)*：邏輯上第 $i$ 個元素（$0 \\le i < cnt$）存在 `buf[(head + i) % capacity]`。\n\n*證明*。初始 $cnt = 0$，真空成立。\n- `enQueue(v)`（要求 $cnt < capacity$）：寫入 `buf[(head + cnt) % capacity]`，然後 $cnt{+}{+}$。新元素的邏輯下標正是舊的 $cnt$，位址吻合 —— (C) 保持。\n- `deQueue()`（要求 $cnt > 0$）：`head = (head+1) % capacity; cnt--;`。原本邏輯下標 $i$ 的元素變成 $i-1$，而位址 $(head_{new} + (i-1)) \\% cap = (head_{old} + i) \\% cap$ 不變 —— (C) 保持。$\\square$\n\n*推論*：`Front()` $=$ `buf[head]`，`Rear()` $=$ `buf[(head + cnt - 1) % capacity]`。**`Rear()` 不能寫成 `buf[tail - 1]`——`tail` 可能是 0。**\n\n**空／滿的區分**：本設計用 $cnt$ 判斷，$cnt = 0$ 空、$cnt = capacity$ 滿，**兩者不會混淆**。若改用 `head`／`tail` 雙指標，$head = tail$ 同時代表空與滿，資訊量不足以區分 —— 因為 $(head, tail)$ 只有 $capacity^2$ 種狀態，卻要表示 $capacity + 1$ 種大小加上位置，鴿籠原理保證必有碰撞。**犧牲一格（只允許存 $capacity - 1$ 個）或多存一個 `cnt`／`bool full`，是僅有的三種修法。**\n\n### 定理 4（1670 的平衡不變量與 `pushMiddle`／`popMiddle` 的位置）\n\n題目定義：`pushMiddle` 插入到下標 $\\lfloor n/2 \\rfloor$（$n$ 為插入前的長度）；`popMiddle` 刪除下標 $\\lfloor (n-1)/2 \\rfloor$（有兩個中間時取靠前者）。\n\n在不變量 (I)（$|A| = \\lfloor n/2\\rfloor$）下：\n\n- **`pushMiddle`**：目標下標 $\\lfloor n/2 \\rfloor = |A|$，也就是 $B$ 的第 0 個位置 ⇒ `B.push_front(v)`。\n- **`popMiddle`**：目標下標 $\\lfloor (n-1)/2 \\rfloor$。\n  - $n$ 偶數：$\\lfloor (n-1)/2\\rfloor = n/2 - 1 = |A| - 1$ ⇒ `A.back()`。\n  - $n$ 奇數：$\\lfloor (n-1)/2 \\rfloor = (n-1)/2 = |A|$ ⇒ `B.front()`。\n\n  **這個 case split 是題目「兩個中間取靠前者」直接推出來的，不是實作偏好。** 寫錯就是把偶數長度的中間取到後面那個。\n\n*再平衡*：每個操作後恢復 (I)。若 $|A| > \\lfloor n/2\\rfloor$，把 `A.back()` 搬到 `B.push_front`；若 $|A| < \\lfloor n/2 \\rfloor$，把 `B.front()` 搬到 `A.push_back`。**每次操作最多搬一個元素**（因為每個操作只讓 $n$ 變動 1、只讓某一側變動 1，偏差量至多為 1），故仍是 $O(1)$。\n\n*邊界*：$n = 0$ 時 `popFront`／`popMiddle`／`popBack` 回傳 $-1$；$n = 1$ 時 (I) 給 $|A| = 0, |B| = 1$，`popMiddle` 走奇數分支取 `B.front()` ✔。**用 $n = 0,1,2,3$ 各手算一次，是驗證這類設計題最有效的方法。**\n\n### 定理 5（3508 惰性淘汰 + 二分：兩個前提）\n\n`addPacket` 超過記憶體上限時淘汰最舊的封包；`getCount(dst, lo, hi)` 問某目的地在時間區間內的封包數。\n\n*不變量*：對每個目的地 $d$，`ts[d]` 是一個**遞增**序列，其中下標 $\\ge$ `startIdx[d]` 的部分恰好是「仍在記憶體中、目的地為 $d$」的封包時間戳。\n\n*前提 1（時間戳單調）*：題目保證 `addPacket` 的 `timestamp` 非遞減，所以 `ts[d]` 天然遞增，**才能二分**。若亂序，得改用 `multiset` 或離線排序。\n*前提 2（淘汰順序 = 加入順序）*：FIFO 淘汰保證被移除的一定是 `ts[d]` 的**前綴**，所以只要推進 `startIdx[d]`，不必真的從 `vector` 中刪除。$\\square$\n\n*查詢*：在 `ts[d]` 的 `[startIdx[d], end)` 區間上做兩次二分（`lower_bound(lo)`、`upper_bound(hi)`），相減即答案。\n\n**這兩個前提和 5.1 定理 1 是同一條**：**「過期／被淘汰者恆為前綴」是所有惰性刪除的共同基礎。**\n\n## 逐步演算法\n\n:::example 狀態表示：兩棧 + 不變式（out 越上面越早入隊）\n**1. 狀態表示：選底層容器並定義不變式。** 先決定要用幾個棧 / 佇列 / 陣列，並寫下「恆成立的性質」。\n\n```cpp\n// 狀態表示：兩棧 + 不變式（out 越上面越早入隊）\nstack<int> in, out;\n```\n:::\n\n\n\n:::example peek：和 pop 共用搬運邏輯，但不移除\n**2. 逐個操作對著語意實作。** 把每個要求的操作（push / pop / peek / empty…）都對照「它應該回傳什麼」來寫，並確保操作後不變式仍成立。\n\n```cpp\n// peek：和 pop 共用搬運邏輯，但不移除\nint peek() {\n    if (out.empty()) {\n        while (!in.empty()) {\n            out.push(in.top());\n            in.pop();\n        }\n    }\n    return out.top();\n}\n```\n:::\n\n\n\n:::example empty：兩個容器都空才算空\n**3. 答案讀取：直接回傳底層狀態。** 設計題的「答案」就是各操作的回傳值，通常從某個端點（front / top）直接讀。\n\n```cpp\n// empty：兩個容器都空才算空\nbool empty() { return in.empty() && out.empty(); }\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 空容器時呼叫 pop / peek 要先判空，避免存取無效元素。\n- 迴圈佇列要用 `cnt` 或預留一格來分辨「滿」與「空」，否則 `head == tail` 兩種情況無法區分。\n- 複雜度：好的設計讓每個操作 `O(1)` 或攤還 `O(1)`；先寫不變式，再驗證每個操作都維持它。\n\n## C++17 模板\n\n以下用同型延伸示範題 933. 最近的請求次數 展示核心寫法。\n\n**完整題目**：設計計數器，對每次請求時間 `t`，回傳 `[t - 3000, t]` 內的請求數。\n\n:::example 移除超出時間窗口（早於 t-3000）的舊請求\n**線索**：題目限制連續子陣列/子字串，且合法性可透過移動左端恢復，這是滑動視窗的線索。\n\n```cpp\nclass RecentCounter {\n    queue<int> q;  // 儲存 [t-3000, t] 範圍內的請求時間戳\n\npublic:\n    int ping(int t) {\n            q.push(t);\n            // 移除超出時間窗口（早於 t-3000）的舊請求\n            while (q.front() < t - 3000) {\n                    q.pop();\n            }\n            return q.size();  // 當前窗口內的請求數\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- 佇列按時間遞增保存請求；每次查詢前移除過期時間，剩下的就是目前視窗內的請求。\n- 右端加入新元素後更新窗口狀態；若條件被破壞，就移動左端直到恢復合法。\n- 每個下標最多被加入與移出一次，因此整體是線性掃描。\n\n## 時間與空間複雜度\n\n| 題目 | 操作 | 時間 | 空間 |\n| --- | --- | --- | --- |\n| 232 用堆疊實現佇列 | `push`／`pop`／`peek`／`empty` | 攤還 $O(1)$（單次最壞 $O(n)$） | $O(n)$ |\n| 225 用佇列實現堆疊 | `push` | $O(n)$ | $O(n)$ |\n| 225 | `pop`／`top`／`empty` | $O(1)$ | |\n| 622 循環佇列 | 全部 | $O(1)$ 最壞 | $O(k)$ |\n| 641 循環雙端佇列 | 全部 | $O(1)$ 最壞 | $O(k)$ |\n| 1670 前中後佇列 | 全部 | $O(1)$ 最壞 | $O(n)$ |\n| 3508 路由器 | `addPacket` | 攤還 $O(\\log n)$（`set` 去重） | $O(m)$，$m$ 為記憶體上限 |\n| 3508 | `forwardPacket` | 攤還 $O(\\log n)$ | |\n| 3508 | `getCount` | $O(\\log n)$ | |\n\n**「攤還 $O(1)$」與「最壞 $O(1)$」在面試中是不同的答案。** 232 是攤還，622／641／1670 是最壞——**環形陣列與多容器分工之所以值得寫，正是因為它們給出最壞情況保證**。若題目提到即時系統或延遲上限，攤還就不夠了。\n\n**3508 的空間**：`ts[d]` 用 `vector` + `startIdx` 惰性刪除，**被淘汰的前綴不會回收**，最壞空間是「歷史上出現過的全部封包數」而非記憶體上限。若操作數極大，需定期做一次 `erase(begin, begin+startIdx)` 壓縮（攤還仍 $O(1)$，因為只在 `startIdx > size/2` 時做）。**這是惰性刪除的固定代價，答題時要主動提。**\n\n## 常見錯誤與邊界條件\n\n- 先判斷合法性方向，再寫 while 條件。\n- 答案計數時常用 `right - left + 1` 或 `left`，兩者語意不同。\n- 頻次表字元集可用陣列，值域大時用 map。\n- 含負數時滑窗的單調性可能失效。\n\n## 常見變形\n\n- 越短越合法：違法時收縮，更新最長。\n- 越長越合法：合法時收縮，更新最短。\n- 恰好型可轉成 `atMost(k) - atMost(k - 1)`。\n\n## 與相似技巧的比較\n\n- **與 4.2 堆疊設計題**：兩者的方法論相同——**先列出每個操作要 $O(1)$ 拿到什麼，再設計狀態讓那些量落在端點上**。差別在 4.2 可以「回滾」（LIFO 保留歷史），本節多半不行，所以更依賴「多容器分工」。\n- **與 4.6 相向雙棧**：1670 的兩個 `deque` 和 4.6 的 gap buffer 是同一個想法——**把「中間」變成兩個容器的介面**。4.6 的介面是游標，1670 的介面是中位；4.6 的平衡由使用者的移動決定，1670 的平衡由不變量 (I) 強制維持。\n- **與 6.6 對頂堆**：4.6 定理 3 的雙堆求中位數與 1670 形狀極像，但**目標不同**：對頂堆要的是「值的中位數」（所以用堆，按值分割），1670 要的是「位置的中間」（所以用 deque，按下標分割）。**看清楚「中間」指的是值還是位置，是這兩題唯一的分歧點。**\n- **與 5.1 普通佇列**：622 是 `queue` 在固定容量下的手工實作。**面試問「你會怎麼實作 `std::queue`」，答案就是 622 或分塊鏈結串列。**\n- **與 9.x 樹狀陣列**：3508 的 `getCount` 若時間戳亂序，就要換成「以時間為下標的樹狀陣列」，$O(\\log n)$ 單點加、區間求和。**有序 + 二分是 $O(\\log n)$；無序就要付出結構的代價。**\n- **與 12.x 離線演算法**：3508 的「惰性刪除 + 前綴指標」正是離線掃描的縮影。**只要刪除順序固定且與查詢無關，就可以不刪，只推指標。**\n\n## 代表例題\n\n**題號**：225、232、622、641、1670\n\n用佇列維持時間順序或視窗順序；練習清理過期元素與保持端點語意一致。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 225 | [用佇列實現堆疊](https://leetcode.cn/problems/implement-stack-using-queues) |  |\n| 232 | [用堆疊實現佇列](https://leetcode.cn/problems/implement-queue-using-stacks) |  |\n| 622 | [設計迴圈佇列](https://leetcode.cn/problems/design-circular-queue) |  |\n| 641 | [設計迴圈雙端佇列](https://leetcode.cn/problems/design-circular-deque) |  |\n| 1670 | [設計前中後佇列](https://leetcode.cn/problems/design-front-middle-back-queue) | 1610 |\n| 3508 | [設計路由器](https://leetcode.cn/problems/implement-router) | 1851 |\n\n## 本節重點速查\n\n- **設計題的流程**：列出操作與目標複雜度 → 反推哪些量必須 $O(1)$ 可得 → 設計狀態讓它們落在端點或維護變數上 → 寫下不變量 → 最後才寫程式。\n- **232**：兩堆疊，**只在 `out` 為空時才把 `in` 倒過去**（否則順序錯，是正確性 bug 不是效率問題）；位能 $\\Phi=|{\\tt in}|$ ⇒ 攤還 $O(1)$。\n- **225**：`push` 自旋 $O(n)$。**做不到攤還 $O(1)$**——堆疊的 `pop` 免費製造逆序，佇列的所有操作都保序（定理 2）。\n- **622／641**：維護 `head` + `cnt`，**不要用 `head`／`tail` 雙指標**（$head=tail$ 空滿不分，鴿籠原理保證資訊不足）。寫入位置 `(head+cnt)%cap`，前端插入 **`(head-1+cap)%cap`——負數取模必須先加 `cap`**。\n- **1670**：不變量 $|A|=\\lfloor n/2\\rfloor$；`pushMiddle` ⇒ `B.push_front`；`popMiddle` 偶數取 `A.back()`、奇數取 `B.front()`（由「兩個中間取靠前者」推得）；每次操作最多搬一個元素。\n- **3508**：惰性淘汰只推 `startIdx`，靠**「淘汰順序 = 加入順序 ⇒ 被刪者恆為前綴」**與**「時間戳單調 ⇒ 可二分」**兩個前提；空間不會自動回收，要主動說明或定期壓縮。\n- **攤還 $O(1)$ ≠ 最壞 $O(1)$**：232 是攤還，622／641／1670 是最壞。有延遲上限的場合只有後者可用。\n- 用 $n = 0,1,2,3$ 手算一遍，是驗證設計題最有效的方法。\n",
        },
        {
          id: 38,
          title: "5.3 雙端佇列",
          summary:
            "## 這個技術解決什麼問題\n\n雙端佇列（deque, double-ended queue）在兩端都支援 $O(1)$ 的插入與刪除。它出現在三種場合：\n\n- **兩端都要增刪**：641（設計循環雙端佇列）、需要「左右輪流取用」的模擬題。\n- **把昂貴的整體操作改成端點操作**：2810（故障鍵盤）——每次遇到 `'i'` 就反轉字串是 $O(n^2)$，改成「翻轉一個布林旗標、往另一端加」是 $O(n)$。**這是本節最值得學的招式：不真的做那個操作，只改變「你從哪一端讀」。**\n- **維護一個「可能有用的候選集合」，一端進、另一端出、還要從進的那端淘汰劣勢者**：2071（你可以安排的最多任務數目）、5.4 的單調佇列、0-1 BFS。\n\n**辨識訊號**：發現自己在用 `reverse()`、`insert(begin())`、`erase(begin())` 這類 $O(n)$ 操作，而且是在迴圈裡——先問「這個操作能不能改成端點操作或一個旗標」。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「雙端佇列」想：\n\n- **需要在序列的「兩端」都做加入 / 移除**，而普通佇列（只能頭出尾進）不夠用。\n- **「反轉」「從兩側擠壓」「左右輪流取用」等操作頻繁**，直接搬動會變慢：\n\n:::example 嗅到味道：要反覆在開頭插入或刪除\n```cpp\n// 嗅到味道：要反覆在開頭插入或刪除\n// vector 在開頭 insert/erase 是 O(n)，改用 deque 變 O(1)\n```\n:::\n\n\n\n- **要在滑動視窗中維護單調候選**（下一節單調佇列），底層就是雙端佇列：尾端彈劣勢、頭端彈過期。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我真的需要「兩端都操作」嗎？還是普通佇列 / 堆疊就夠？\n2. 頭端與尾端各代表資料的哪一側？語意對得上嗎？\n3. 有沒有可以用「旗標 / 指標」取代實際搬動的機會，省下成本？\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n本節介紹雙端佇列（deque，全名 double-ended queue）。它比普通佇列更靈活：**頭尾兩端都能 `O(1)` 地加入或移除元素**。普通佇列只能尾進頭出，雙端佇列則四種操作都行。\n\n:::example 雙端佇列：頭尾都能進出，皆為 O(1)\nC++ 提供 `std::deque`，常用操作如下：\n\n```cpp\n// 雙端佇列：頭尾都能進出，皆為 O(1)\ndeque<int> dq;\ndq.push_back(1);   // 尾端加：1\ndq.push_front(0);  // 頭端加：0 1\ndq.pop_front();    // 頭端移：剩 1\ndq.pop_back();     // 尾端移：剩空\n```\n:::\n\n\n\n:::example 故障鍵盤：用方向旗標代替真的反轉\n為什麼需要「兩端都能動」？因為有些題目資料會從兩個方向變化。例如「故障鍵盤」：打字過程中遇到字元 `i` 就要把目前字串整個反轉。如果真的每次都反轉字串，最壞是 `O(n^2)`。聰明的做法是用雙端佇列加一個「方向旗標」：正常時往尾端寫，遇到反轉就改成往頭端寫，**根本不真的搬動字元**，最後再依方向一次輸出：\n\n```cpp\n// 故障鍵盤：用方向旗標代替真的反轉\ndeque<char> dq;\nbool reversed = false;\nfor (char c : s) {\n    if (c == 'i') {\n        reversed = !reversed;\n        continue;\n    }  // 切換方向\n    if (reversed) {\n        dq.push_front(c);  // 反轉狀態往頭寫\n    } else {\n        dq.push_back(c);  // 正常往尾寫\n    }\n}\n```\n:::\n\n\n\n關鍵直覺：雙端佇列讓「在開頭插入」也變成 `O(1)`，於是「往兩個方向延伸 / 收縮」的需求就能高效完成。它也是「單調佇列」（下一節）的底層容器——因為單調佇列需要從尾端彈劣勢候選、從頭端彈過期候選。\n\n## 狀態／資料結構定義\n\n`std::deque<T>` 的關鍵性質（和 `vector`／`list` 都不同）：\n\n| 性質 | `vector` | `deque` | `list` |\n| --- | --- | --- | --- |\n| 頭端插入／刪除 | $O(n)$ | $O(1)$ | $O(1)$ |\n| 尾端插入／刪除 | 攤還 $O(1)$ | $O(1)$ | $O(1)$ |\n| 隨機存取 `[i]` | $O(1)$ | $O(1)$ | $O(n)$ |\n| 記憶體連續 | ✓ | ✗（分塊） | ✗ |\n| 插入後**參考**是否失效 | 全失效 | **兩端插入不失效** | 不失效 |\n| 插入後**迭代器**是否失效 | 可能全失效 | **全失效** | 不失效 |\n\n**兩個常被忽略的點**：\n1. `deque` **支援 `[i]` 隨機存取**，這讓它能同時扮演「陣列」與「雙端佇列」——5.4 的單調佇列常需要讀中間元素來除錯。\n2. **`deque` 的迭代器在兩端插入後會失效，但參考（reference）不會。** 想在插入過程中保留位置，存下標不要存迭代器。\n\n**兩種典型狀態設計：**\n\n**(A) 內容 + 方向旗標**（2810）\n```\ndeque<char> dq;\nbool rev = false;   // true 表示「dq 目前存的是真實字串的反序」\n```\n真實字串 $=$ `rev ? reverse(dq) : dq`。加字元時看 `rev` 決定 `push_back` 還是 `push_front`。\n\n**(B) 候選集合**（2071、5.4）\n```\ndeque<int> cand;    // 尾端進、兩端都可能出\n```\n不變量規定「什麼元素有資格待在裡面」以及「兩端各代表什麼極值」。\n\n## 不變量或正確性證明\n\n### 定理 1（2810 旗標法：不做的反轉才是最快的反轉）\n\n*不變量 (R)*：處理完輸入的每個前綴後，\n$$\\text{真實字串} = \\begin{cases} \\texttt{dq} \\text{ 由頭到尾}, & \\texttt{rev} = \\text{false} \\\\ \\texttt{dq} \\text{ 由尾到頭}, & \\texttt{rev} = \\text{true} \\end{cases}$$\n\n*證明（對輸入長度歸納）*。空字串時 `dq` 空、`rev = false`，成立。\n\n- *遇到 `'i'`*：真實字串反轉。只把 `rev` 取反：若原本 `rev = false`，真實字串是「頭到尾」，反轉後應為「尾到頭」，而新的 `rev = true` 正是這個讀法 —— (R) 保持。`rev = true` 的情形對稱。\n- *遇到一般字元 $c$*：真實字串在**尾端**接上 $c$。\n  - `rev = false`：讀法是頭到尾，尾端即 `dq` 的尾端 ⇒ `push_back(c)` ✔。\n  - `rev = true`：讀法是尾到頭，真實字串的尾端對應 `dq` 的**頭端** ⇒ `push_front(c)` ✔。$\\square$\n\n**這個證明的價值在於它點名了前提：所有修改都發生在「真實字串的尾端」。** 如果題目允許在中間插入字元，旗標法就崩潰了——因為 `rev` 只能改變讀取方向，不能改變「哪個位置是中間」。屆時要用平衡樹或 4.6 的 gap buffer。\n\n*複雜度*：每個字元 $O(1)$，總計 $O(n)$；直接 `reverse` 是 $O(n^2)$。**輸入全是 `'i'` 時，樸素解的 $10^5$ 個字元會做 $10^5$ 次 $O(n)$ 反轉，必定 TLE。**\n\n### 定理 2（2071：二分答案的單調性）\n\n`check(k)` = 「能否完成至少 $k$ 個任務」。\n\n*宣稱 A（單調性）*：若 $k$ 可行，則 $k-1$ 可行。\n*證明*：從一組完成 $k$ 個任務的可行指派中任意刪掉一組 (工人, 任務)，剩下的指派仍然合法（每個工人仍滿足自己那個任務的門檻，藥丸用量只減不增）。$\\square$\n\n*宣稱 B（選誰）*：若 $k$ 可行，則「用**最強的 $k$ 名工人**做**最簡單的 $k$ 個任務**」也可行。\n*證明（交換論證）*。設有一組可行指派用到工人 $w$ 而未用到 $w' \\ge w$：把 $w$ 換成 $w'$，$w'$ 能力更強，原本 $w$ 做得到的（含吃藥）$w'$ 也做得到 ⇒ 仍可行。反覆交換即得「工人集合可換成最強的 $k$ 名」。任務端對稱：把某個被選任務 $t$ 換成未被選且更簡單的 $t' \\le t$，原本能做 $t$ 的工人必能做 $t'$ ⇒ 仍可行。$\\square$\n\n由宣稱 A，可行的 $k$ 構成前綴，**可以二分**；由宣稱 B，`check(k)` 只需檢查那一組固定的工人與任務。\n\n### 定理 3（2071 的貪心：吃藥時要吃最難的那個）\n\n`check(k)`：任務、工人皆升冪排序，取 `tasks[0..k-1]` 與 `workers[n-k..n-1]`。**由弱到強**遍歷這 $k$ 名工人 $w$：\n1. 把所有滿足 `tasks[j] <= w + strength` 的任務推入 `dq` 尾端（任務升冪，故 `dq` 內也升冪）。\n2. 若 `dq` 空 ⇒ 失敗（這名工人無事可做，但 $k$ 名工人必須各做一件）。\n3. 若 `tasks[dq.front()] <= w` ⇒ **不吃藥，做最簡單的那個**，`pop_front()`。\n4. 否則必須吃藥；若 `pills == 0` ⇒ 失敗。否則 `pills--`，**做 `dq` 中最難的那個**，`pop_back()`。\n\n*不變量*：`dq` 恰好是「尚未指派、且當前工人吃藥後做得到」的任務集合，且升冪排列。由「工人升冪遍歷 ⇒ `w + strength` 單調不減」，加入是單向的，**每個任務最多入隊一次、出隊一次 ⇒ 攤還 $O(1)$**。\n\n*步驟 3 的正確性*：不吃藥時做最簡單的任務。因為後續工人都更強，把難的留給他們不劣（標準交換論證）。\n\n*步驟 4 的正確性（這步反直覺，要證）*：既然這顆藥非吃不可，就該用在 `dq` 中**最難**的任務上。\n*證明*。設某個最優解中，工人 $w$ 吃藥做了 $t \\in \\texttt{dq}$，而 `dq` 中最難的 $h$（$t \\le h$）被後面某位工人 $w' \\ge w$ 承擔。交換兩人的任務：\n- $w$ 改做 $h$：因 $h \\in \\texttt{dq}$，有 $h \\le w + \\textit{strength}$，吃同一顆藥即可 ✔。\n- $w'$ 改做 $t$：若原本 $w'$ 不吃藥做 $h$（$h \\le w'$），則 $t \\le h \\le w'$，仍不吃藥 ✔；若原本吃藥做 $h$（$h \\le w' + \\textit{strength}$），則 $t \\le h \\le w' + \\textit{strength}$，用同一顆藥 ✔。\n\n兩種情況下藥丸總用量都不變、可行性保持。故存在一個最優解符合「吃藥時取最難者」。$\\square$\n\n**注意證明中真正用到的前提**：**工人按能力升冪遍歷**（保證 $w' \\ge w$）、**`dq` 內任務都滿足 $\\le w + strength$**。少了任何一個，交換論證就不成立。\n\n## 逐步演算法\n\n:::example 狀態表示：頭=左側、尾=右側\n**1. 狀態表示：決定每一端代表什麼語意。** 想清楚「頭端」「尾端」各自代表資料的哪一側（時間早 / 晚、字串左 / 右）。\n\n```cpp\n// 狀態表示：頭=左側、尾=右側\ndeque<char> dq;  // 例：dq.front() 是字串最左字元\n```\n:::\n\n\n\n:::example 更新：兩端各取所需\n**2. 更新流程：依需求從對應端進出。** 哪一端需要加入或移除，就呼叫對應的 `push_front/back`、`pop_front/back`。\n\n```cpp\n// 更新：兩端各取所需\ndq.push_back(x);  // 右端新增\ndq.pop_front();   // 移除最左（最舊）\n```\n:::\n\n\n\n:::example 答案讀取：依方向把 deque 拼成字串\n**3. 答案讀取：從端點直接取，或一次遍歷輸出。** 答案常落在某一端；若需完整序列，依目前方向決定正讀或反讀。\n\n```cpp\n// 答案讀取：依方向把 deque 拼成字串\nstring res(dq.begin(), dq.end());\nif (reversed) {\n    reverse(res.begin(), res.end());\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 兩端操作前都要判空，`front() / back()` 對空容器是未定義行為。\n- 用「方向旗標」取代真反轉時，記得最後輸出要依旗標決定方向，別漏掉。\n- 複雜度：每端的 push / pop 皆 `O(1)`；上述故障鍵盤整體 `O(n)`，遠優於每次真反轉的 `O(n^2)`。\n\n## C++17 模板\n\n**模板一：2810 故障鍵盤（旗標 + 雙端佇列）**\n\n```cpp\nclass Solution {\npublic:\n    string finalString(string s) {\n        deque<char> dq;\n        bool rev = false;\n        for (char c : s) {\n            if (c == 'i') { rev = !rev; continue; }\n            if (rev) dq.push_front(c); else dq.push_back(c);\n        }\n        string res(dq.begin(), dq.end());\n        if (rev) reverse(res.begin(), res.end());  // 只在最後反轉一次\n        return res;\n    }\n};\n```\n\n**模板二：2071 你可以安排的最多任務數目（二分答案 + 雙端佇列貪心）**\n\n```cpp\nclass Solution {\npublic:\n    int maxTaskAssign(vector<int>& tasks, vector<int>& workers, int pills, int strength) {\n        sort(tasks.begin(), tasks.end());\n        sort(workers.begin(), workers.end());\n        int n = workers.size(), m = tasks.size();\n\n        auto check = [&](int k) -> bool {\n            // 最簡單的 k 個任務 tasks[0..k-1]，最強的 k 名工人 workers[n-k..n-1]\n            deque<int> dq;          // 存任務「值」，升冪\n            int p = pills, j = 0;   // j：下一個尚未考慮的任務下標\n            for (int i = n - k; i < n; ++i) {\n                int w = workers[i];\n                while (j < k && tasks[j] <= w + strength) dq.push_back(tasks[j++]);\n                if (dq.empty()) return false;\n                if (dq.front() <= w) {          // 不吃藥，做最簡單的\n                    dq.pop_front();\n                } else {                        // 必須吃藥，做最難的\n                    if (p == 0) return false;\n                    --p;\n                    dq.pop_back();\n                }\n            }\n            return true;\n        };\n\n        int lo = 0, hi = min(n, m), ans = 0;\n        while (lo <= hi) {                       // 閉區間二分\n            int mid = lo + (hi - lo) / 2;\n            if (check(mid)) { ans = mid; lo = mid + 1; }\n            else hi = mid - 1;\n        }\n        return ans;\n    }\n};\n```\n\n**模板三：0-1 BFS（雙端佇列的另一個經典用途）**\n\n```cpp\n// 邊權只有 0 與 1 的最短路：權 0 走 push_front，權 1 走 push_back\nvector<int> zeroOneBFS(int n, const vector<vector<pair<int,int>>>& g, int src) {\n    vector<int> dist(n, INT_MAX);\n    deque<int> dq;\n    dist[src] = 0;\n    dq.push_back(src);\n    while (!dq.empty()) {\n        int u = dq.front();\n        dq.pop_front();\n        for (auto [v, w] : g[u]) {\n            if (dist[u] + w < dist[v]) {\n                dist[v] = dist[u] + w;\n                if (w == 0) dq.push_front(v); else dq.push_back(v);\n            }\n        }\n    }\n    return dist;\n}\n```\n\n## 時間與空間複雜度\n\n| 題目 | 時間 | 空間 |\n| --- | --- | --- |\n| 2810 故障鍵盤 | $O(n)$（樸素反轉為 $O(n^2)$） | $O(n)$ |\n| 2071 你可以安排的最多任務數目 | $O((n + m)\\log(n+m))$ 排序 $+\\ O(k\\log \\min(n,m))$ 二分 $\\Rightarrow O(n\\log n)$ | $O(n)$ |\n| 0-1 BFS | $O(V + E)$ | $O(V)$ |\n| 641 循環雙端佇列 | 每個操作最壞 $O(1)$ | $O(k)$ |\n\n**2071 為什麼是 $O(n \\log n)$ 而不是 $O(n\\log^2 n)$**：單次 `check(k)` 是 $O(k)$（每個任務最多入隊出隊各一次，攤還 $O(1)$），二分 $O(\\log n)$ 次，故檢查部分是 $O(n\\log n)$——**與排序同級，排序才是主導項**。若 `check` 內改用 `multiset`（也是常見寫法），單次變 $O(k\\log k)$，總計 $O(n\\log^2 n)$，在 $n = 5\\times10^4$ 下仍能過，但雙端佇列版本更快也更好證。\n\n**`deque` 的常數**：分塊配置使它比 `vector` 慢約 1.5–2 倍，且**不保證記憶體連續**（不能 `&dq[0]` 當 C 陣列用）。**在效能關鍵的迴圈裡，若兩端操作次數上界已知（例如 $\\le n$），用 `vector<int> buf(2*n); int head = n, tail = n;` 手動實作會更快**——5.4 的單調佇列常這樣寫。\n\n## 常見錯誤與邊界條件\n\n- **`dq.front()` / `dq.back()` 對空容器是未定義行為**，不會拋例外。任何取端點的地方前面都要有 `if (!dq.empty())` 或由不變量保證非空（例如 2071 的步驟 2 先檢查再取）。\n- **`pop_front()` / `pop_back()` 回傳 `void`**，不像某些語言會回傳被刪元素。要先取值再彈：`int v = dq.front(); dq.pop_front();`。\n- **迭代器在兩端插入後全部失效。** `for (auto it = dq.begin(); it != dq.end(); ++it) { dq.push_back(...); }` 是 UB。要邊插邊掃，改用下標。\n- **2810：不要每次遇到 `'i'` 就真的 `reverse`。** 這是本題唯一的考點，樸素解在 $|s| = 10^5$ 必 TLE。\n- **2810 最後那一次 `reverse` 別忘了。** `rev` 為 true 時輸出必須反過來——這是最常見的「本地小測資都對、提交就錯」。\n- **2071 二分的邊界**：`hi = min(n, m)`（工人與任務數的較小者），`lo = 0`（一個都不做恆可行）。**用閉區間 `while (lo <= hi)` 並在成功時記錄 `ans`**，避免 off-by-one。\n- **2071 的 `pills` 要用區域副本**，`check` 每次呼叫都要重置。用 lambda 捕獲時寫 `int p = pills;` 而不是直接改外層的 `pills`——**這是二分答案題最容易犯的狀態污染錯誤。**\n- **2071 步驟 4 寫成 `pop_front()` 是最常見的錯**：吃藥時要吃最難的（定理 3），不是最簡單的。\n- **0-1 BFS 的 `push_front` 不能省略距離檢查**：同一節點可能多次入隊，必須用 `dist[u] + w < dist[v]` 過濾，否則可能無限迴圈。**它與 Dijkstra 一樣需要「發現更短才鬆弛」，不是靠 `visited` 標記。**\n- **負數下標取模**：手動實作環形雙端佇列時，`(head - 1 + capacity) % capacity`，**`+ capacity` 不可省**。\n\n## 與相似技巧的比較\n\n- **與 5.1 佇列**：`deque` 是 `queue` 的超集，且 `std::queue` 預設就以 `deque` 為底層容器。**只需要 FIFO 就用 `queue`——它會讓讀者知道你不打算從另一端動手，是一種型別層級的文件。**\n- **與 5.4 單調佇列**：單調佇列**就是**一個帶不變量的 `deque`（尾端淘汰劣勢、頭端淘汰過期）。本節提供容器，5.4 提供不變量。\n- **與 4.6 相向雙棧**：兩者都能達成「兩端 $O(1)$」。**差別在中間**：gap buffer 讓「游標處」也是 $O(1)$，`deque` 不行。**需要在中間動手就用 4.6 或平衡樹，只在兩端動手用 `deque`。**\n- **與 `std::list`**：`list` 任意位置 $O(1)$ 插刪，但**沒有隨機存取、快取極差、每個節點多兩個指標**。**除非需要「持有一個位置很久並在該處插刪」（例如 4.2 的 716、LRU 快取），否則不要用 `list`。**\n- **與旗標法的一般化**：2810 的「不做反轉，只改讀取方向」屬於**惰性 / 延遲操作**的家族，同族還有：矩陣轉置只改索引函式、線段樹的 lazy 標記（9.3）、字串旋轉只改起點。**判準是：這個操作能否被表示成「對存取路徑的一個變換」，而非對資料本身的變換。**\n- **與 6.x 優先佇列**：2071 的候選集合若需要按值取極值且加入順序無序，就得用 `multiset`／堆；**這裡之所以能用 `deque`，是因為任務升冪加入使集合天然有序**——這是把 $O(\\log n)$ 降成 $O(1)$ 的關鍵前提。\n\n## 代表例題\n\n**題號**：2071、2810\n\n用佇列維持時間順序或視窗順序；練習清理過期元素與保持端點語意一致。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 2071 | [你可以安排的最多工數目](https://leetcode.cn/problems/maximum-number-of-tasks-you-can-assign) | 2648 |\n| 2810 | [故障鍵盤](https://leetcode.cn/problems/faulty-keyboard) | 1193 |\n\n## 本節重點速查\n\n- `deque`：兩端 $O(1)$、**支援 `[i]` 隨機存取**、記憶體不連續；**兩端插入後參考不失效但迭代器會失效**。\n- **2810 的招式：不真的做反轉，只翻轉「從哪一端讀」的旗標**（定理 1）。前提是**所有修改都發生在真實字串的尾端**；允許中間插入就得換 gap buffer / 平衡樹。$O(n^2) \\to O(n)$。\n- **旗標法是「惰性操作」家族的一員**：把對資料的變換改成對存取路徑的變換。\n- **2071**：可行性對 $k$ 單調 ⇒ 二分；「最強 $k$ 工人 + 最簡單 $k$ 任務」不劣（交換論證）。\n- **2071 貪心（定理 3）**：工人由弱到強；不吃藥時取 `dq.front()`（最簡單），**吃藥時取 `dq.back()`（最難）**。前提是工人升冪遍歷且 `dq` 內都 $\\le w + strength$。\n- `deque` 能取代 `multiset` 的前提是**元素以有序方式加入**——這是 $O(\\log n) \\to O(1)$ 的來源。\n- **0-1 BFS**：權 0 走 `push_front`、權 1 走 `push_back`，$O(V+E)$；仍需 `dist[u]+w < dist[v]` 的鬆弛檢查，不能只靠 `visited`。\n- `front()`／`back()` 對空容器是 UB；`pop_*` 回傳 `void`，先取值再彈。\n- 效能關鍵處，用 `vector` + 手動 `head`／`tail` 取代 `deque` 通常快 1.5–2 倍。\n",
        },
        {
          id: 39,
          title: "5.4 單調佇列",
          summary:
            "## 這個技術解決什麼問題\n\n單調佇列回答的是：「**在一個只會往右移動的區間裡，反覆查詢最大值或最小值**」，把樸素的 $O(nk)$ 壓成 $O(n)$。\n\n三類典型應用：\n\n- **固定寬度視窗的極值**：239（滑動視窗最大值）、2398（預算內的最多機器人數目，視窗內 max 乘以長度）。\n- **變寬度視窗但左界單調**：1438（絕對差不超過限制的最長連續子陣列）、2762（不間斷子陣列）——同時維護 max 與 min 兩條單調佇列。\n- **DP 轉移的區間最優**：$\\mathrm{dp}[i] = a_i + \\max_{j \\in [i-k,\\, i-1]} \\mathrm{dp}[j]$（跳躍遊戲 VI）、862（和至少為 K 的最短子陣列）、1499（滿足不等式的最大值）。**這一類是單調佇列真正的主戰場**——把一維 DP 的 $O(nk)$ 轉移降成 $O(n)$。\n\n**核心觀察**（和 4.7 單調堆疊同源）：\n\n> 若 $j < i$ 且 $a_j \\le a_i$，則**任何包含 $j$ 的、右端不小於 $i$ 的視窗都同時包含 $i$**，此時 $a_j$ 永遠不可能是最大值（$a_i$ 至少一樣大且活得更久）。$j$ 從此無用，可以丟掉。\n\n**這個觀察成立的前提是「視窗右端單調不減」。** 這條前提是單調佇列適用與否的分界線，見定理 1 之後的討論。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「單調佇列」想：\n\n- **在滑動視窗 / 連續區間內反覆查詢最大或最小值。** 像「滑動視窗最大值」「絕對差不超過限制的最長連續子陣列」（同時維護視窗 max 與 min）。\n- **DP 轉移需要「某個區間內的最佳前一項」**，例如 `dp[i] = a[i] + max(dp[j])`，其中 `j` 落在一個滑動範圍內——單調佇列可把這個 max 均攤成 `O(1)`。\n- **暴力是每個視窗重新掃一遍找最值 `O(nk)`，要壓到 `O(n)`：**\n\n:::example 嗅到味道：每滑一格都重掃視窗找最大值\n```cpp\n// 嗅到味道：每滑一格都重掃視窗找最大值\n// for j in window: best = max(best, a[j]);   // O(k) 每格\n// 改用單調佇列，頭端 O(1) 直接拿到\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要找最大還是最小？佇列該遞減還是遞增？\n2. 佇列存下標，我用什麼條件判斷「已滑出視窗」？\n3. 清過期、入隊、讀答案三件事的先後順序對嗎？\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n本節要解決的場景是：在一個固定大小的「滑動視窗」裡，隨著視窗往右滑，**隨時回答視窗內的最大值（或最小值）**。例如「滑動視窗最大值」：給一個陣列和視窗大小 `k`，每滑一格就要報出當前 `k` 個數裡的最大值。\n\n暴力做法是每次都把視窗內 `k` 個數掃一遍找最大，總共 `O(nk)`，太慢。單調佇列（monotonic queue）能把它壓到 `O(n)`。\n\n核心想法：用一個雙端佇列（deque）保存「**可能成為未來答案的候選下標**」，並維持兩個不變式：\n\n1. 佇列裡的下標都還在視窗範圍內（過期的從頭端清掉）。\n2. 對應的值由頭到尾遞減（新元素若比尾端大，尾端那些「又舊又小」的就永遠贏不了，從尾端清掉）。\n\n:::example 單調遞減佇列：尾端清掉比新元素小的劣勢候選\n維持這兩點後，**頭端永遠是視窗內的最大值**。關鍵直覺：一個元素若比後來的新元素小、又比它早進場，那它再也不可能是任何視窗的最大值，可以安心丟棄。\n\n```cpp\n// 單調遞減佇列：尾端清掉比新元素小的劣勢候選\nwhile (!dq.empty() && a[dq.back()] <= a[i]) {\n    dq.pop_back();  // 又舊又小，永遠贏不了，丟掉\n}\ndq.push_back(i);  // 新下標入尾\n```\n:::\n\n\n\n:::example 頭端清過期：下標太舊就移除\n每次取答案前，先把「已滑出視窗」的頭端清掉：\n\n```cpp\n// 頭端清過期：下標太舊就移除\nwhile (dq.front() <= i - k) {\n    dq.pop_front();  // 已不在視窗內\n                                      // 清完後 dq.front() 就是視窗最大值的下標\n}\n```\n:::\n\n\n\n因為每個下標最多進佇列一次、出佇列一次，總共 `O(n)`。佇列存的是**下標**而非值，這樣才能判斷「是否已滑出視窗」。\n\n## 狀態／資料結構定義\n\n```cpp\ndeque<int> dq;   // 存「下標」，不存值\n```\n**存下標**：因為要判斷元素是否已經滑出視窗（`dq.front() <= i - k`），只有值就辦不到。\n\n**兩端的分工完全不同，不要搞混：**\n\n| 端點 | 淘汰理由 | 判斷依據 | 對應的「時間 / 價值」 |\n| --- | --- | --- | --- |\n| **頭端** `pop_front` | **過期**——已滑出視窗 | **下標** | 時間 |\n| **尾端** `pop_back` | **劣勢**——被新來的元素支配 | **值** | 價值 |\n\n**「頭端管時間、尾端管價值」是本節唯一需要背的句子。** 4.7 的單調堆疊只有尾端（沒有過期概念），5.1 的普通佇列只有頭端（不淘汰劣勢者）——**單調佇列是兩者的合體**。\n\n**求最大值 ⇒ 遞減佇列**（尾端彈出所有 $\\le a_i$ 者），**求最小值 ⇒ 遞增佇列**。判準同 4.7。\n\n**三步骨架**（順序不能換）：\n1. **尾端去劣**：`while (!dq.empty() && a[dq.back()] <= a[i]) dq.pop_back();`\n2. **入隊**：`dq.push_back(i);`\n3. **頭端去期**：`while (dq.front() <= i - k) dq.pop_front();`，然後 `a[dq.front()]` 即答案。\n\n**步驟 3 放在 2 之後而非之前**，可以保證 `dq` 恆非空（$i$ 剛進去），少一次 `empty()` 檢查；但**若視窗左界由條件（而非固定寬度）決定，就必須改成「先擴右、再縮左、最後讀答案」的順序**，見 1438。\n\n## 不變量或正確性證明\n\n以「固定寬度 $k$、求最大值」為例（遞減佇列）。\n\n### 定理 1（主不變量）\n\n處理完下標 $i$（已執行完三步骨架）後，`dq` 由頭到尾的下標 $j_1 < j_2 < \\dots < j_m$ 恰好是\n$$\\{\\, j \\in (i-k,\\ i] \\;:\\; \\forall\\, t \\in (j,\\ i],\\ a_t < a_j \\,\\},$$\n且 $a_{j_1} > a_{j_2} > \\dots > a_{j_m}$（假設元素互異）。特別地 **`a[dq.front()]` $= \\max\\{a_t : t \\in (i-k, i]\\}$**。\n\n*證明*。\n- *尾端結構*：與 4.7 定理 1 逐字相同——每次入隊前彈掉所有 $a_j \\le a_i$，故留下的都滿足「$(j, i]$ 中無人 $\\ge a_j$」，單調性隨之而來。\n- *頭端範圍*：步驟 3 彈掉所有 $j \\le i - k$。由下標遞增，過期者構成**前綴**，所以「反覆彈頭直到頭端合法」恰好移除全部過期者、不會誤傷。（這與 5.1 定理 1 是同一條理由。）\n- *頭端即最大*：設 $M$ 是視窗 $(i-k, i]$ 的最大值位置，取其中**最右**的一個。則 $\\forall t \\in (M, i],\\ a_t < a_M$，故 $M \\in \\texttt{dq}$。又 `dq` 內值嚴格遞減、$j_1$ 最小，若 $j_1 \\ne M$ 則 $a_{j_1} > a_M$，與 $M$ 為最大矛盾。故 $j_1 = M$。$\\square$\n\n**這個證明用到兩個前提，缺一不可：**\n\n1. **視窗右端單調不減**（$i$ 遞增）——尾端去劣才合法。若允許右端左移（視窗可以縮回去），被彈掉的元素會「復活」，單調佇列徹底失效。\n2. **視窗左端單調不減**——頭端去期才只需彈前綴。若左端可以往回走，過期集合就不是前綴。\n\n**兩端都必須單調右移。** 一旦題目允許任意區間查詢（例如離線給定 $q$ 個互不包含的區間），就要改用 ST 表（9.7，$O(1)$ 查詢、只支援可重複貢獻運算）或線段樹（9.3）。**這是選型時第一個要確認的事。**\n\n### 定理 2（攤還 $O(n)$）\n\n令位能 $\\Phi = |\\texttt{dq}|$。第 $i$ 步彈出 $p_i$ 個（頭尾合計）、壓入 1 個：\n$$\\hat{c}_i = (p_i + 1) + (1 - p_i) = 2 .$$\n故總成本 $\\le 2n$。**每個下標一生只入隊一次、出隊一次**，無論從哪一端出去。$\\square$\n\n**與 4.7 定理 4 完全相同的論證，只是元素現在有兩個出口。** 位能法在這裡的好處正是它不在意元素從哪端離開。\n\n### 定理 3（862：字首和不單調時的雙向淘汰）\n\n求最短的子陣列使其和 $\\ge K$，元素**可為負**。令 $s_0 = 0,\\ s_r = a_0 + \\dots + a_{r-1}$，要找最小的 $r - l$ 使 $s_r - s_l \\ge K$。\n\n**因為有負數，$s$ 不單調，雙指標（滑動視窗）不適用**——這是本題的難點所在。改用單調**遞增**佇列存 $l$ 的候選下標。對每個 $r$：\n\n**(a) 頭端淘汰（已可成交者不必再等）**：`while (!dq.empty() && s[r] - s[dq.front()] >= K) { ans = min(ans, r - dq.front()); dq.pop_front(); }`\n\n*為什麼可以彈掉？* 設 $l = \\texttt{dq.front()}$ 且 $s_r - s_l \\ge K$。對任何 $r' > r$，若 $l$ 仍可成交，長度 $r' - l > r - l$，**嚴格更差**。故 $l$ 的最優貢獻已在此刻結算完畢。$\\square$\n**這一步用到的前提是「$r$ 只增不減」**——與定理 1 的前提 1 相同。\n\n**(b) 尾端淘汰（被支配者）**：`while (!dq.empty() && s[dq.back()] >= s[r]) dq.pop_back();` 然後 `dq.push_back(r);`\n\n*為什麼可以彈掉？* 設 $l' = \\texttt{dq.back()} < r$ 且 $s_{l'} \\ge s_r$。對任何未來的 $r'' > r$：\n- 若 $l'$ 可成交（$s_{r''} - s_{l'} \\ge K$），則 $s_{r''} - s_r \\ge s_{r''} - s_{l'} \\ge K$，$r$ **也**可成交；\n- 且 $r'' - r < r'' - l'$，$r$ 給出的長度**嚴格更短**。\n\n故 $l'$ 在任何未來都被 $r$ 支配（**兩個維度同時不劣**：既更容易成交、又更短），可以安全丟棄。$\\square$\n\n**這個「雙維度支配」論證是 862 的全部內容**，也是所有單調佇列題的通用模板：**證明「新元素在所有未來查詢中都不劣於舊元素」，舊元素才能丟。** 寫任何單調佇列題時，都應該能把這句話具體化成兩行不等式。\n\n*結果*：`dq` 內的 $s$ 值嚴格遞增，每個下標入隊出隊各一次，$O(n)$。\n\n### 定理 4（1438／2762：雙佇列 + 視窗合法性的單調性）\n\n求最長子陣列使 $\\max - \\min \\le \\textit{limit}$。同時維護遞減佇列 `mx` 與遞增佇列 `mn`。\n\n*關鍵引理*：**若 $[l, r]$ 合法，則其任一子區間 $[l', r'] \\subseteq [l, r]$ 也合法。**\n*證明*：子區間的 $\\max$ 不增、$\\min$ 不減，故 $\\max - \\min$ 不增。$\\square$\n\n*推論*：對固定的 $r$，合法的 $l$ 構成一個**後綴** $[L(r), r]$，且 $L(r)$ 隨 $r$ 單調不減。**因此雙指標成立**——這正是滑動視窗的可行性條件，也讓「左端單調右移」（定理 1 前提 2）自動滿足。\n\n*實作*：右端擴張時把 $r$ 推入兩條佇列；當 `a[mx.front()] - a[mn.front()] > limit` 時 `++l`，並且**若 `mx.front() < l` 則 `mx.pop_front()`，`mn` 同理**。答案取 $\\max(r - l + 1)$。\n\n**注意這裡的頭端淘汰不是「$\\le i-k$」而是「$< l$」**——視窗左界由條件決定而非固定寬度。**這是固定視窗與可變視窗在程式碼上的唯一差別，但很多人會把兩者的骨架混用而寫出錯誤的彈出條件。**\n\n### 定理 5（1499：把二元條件拆成兩個單變數）\n\n求 $\\max (y_i + y_j + |x_i - x_j|)$，其中 $x$ 已升冪、$|x_i - x_j| \\le k$。\n\n對 $i < j$（故 $x_i \\le x_j$），\n$$y_i + y_j + (x_j - x_i) = \\underbrace{(y_i - x_i)}_{\\text{只和 } i \\text{ 有關}} + \\underbrace{(y_j + x_j)}_{\\text{只和 } j \\text{ 有關}} .$$\n\n**這個拆分是全題的關鍵**：一旦目標函數能寫成「$f(i) + g(j)$」，固定 $j$ 後就只需要視窗 $\\{i : x_j - x_i \\le k\\}$ 內 $f(i)$ 的最大值 ⇒ 單調佇列。\n\n*視窗的單調性*：$x$ 升冪 ⇒ 條件 $x_i \\ge x_j - k$ 的下界隨 $j$ 單調不減 ⇒ 左端單調右移 ✔ 定理 1 前提 2 成立。\n\n**「拆成 $f(i) + g(j)$」是所有單調佇列優化 DP 的統一形式。** 看到 $\\mathrm{dp}[j] = \\max_i (\\mathrm{dp}[i] + w(i,j))$，第一件事就是嘗試把 $w(i,j)$ 拆開；拆不開（例如 $w$ 有交叉項）就要考慮斜率優化或決策單調性，那已超出本節範圍。\n\n## 逐步演算法\n\n:::example 狀態表示：存下標，維持值遞減（求最大值）\n**1. 狀態表示：deque 存下標，維持雙不變式。** 兩個不變式同時成立：下標都在視窗內、對應值單調。\n\n```cpp\n// 狀態表示：存下標，維持值遞減（求最大值）\ndeque<int> dq;  // 由頭到尾，a[dq[i]] 遞減\n```\n:::\n\n\n\n:::example 更新：尾清劣勢 → 入隊 → 頭清過期\n**2. 更新流程：尾端清劣勢、頭端清過期。** 新元素進來前清尾端比它差的候選；視窗左端前進時清頭端過期的。\n\n```cpp\n// 更新：尾清劣勢 → 入隊 → 頭清過期\nwhile (!dq.empty() && a[dq.back()] <= a[i]) {\n    dq.pop_back();\n}\ndq.push_back(i);\nwhile (dq.front() <= i - k) {\n    dq.pop_front();\n}\n```\n:::\n\n\n\n:::example 答案讀取：視窗成形後，頭端即最大值\n**3. 答案讀取：頭端就是當前視窗最值。** 當視窗已成形（`i >= k - 1`）時，頭端對應的值即答案，可直接記錄或當作 DP 的最佳前驅。\n\n```cpp\n// 答案讀取：視窗成形後，頭端即最大值\nif (i >= k - 1) {\n    ans.push_back(a[dq.front()]);\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 「先清過期、先入隊、還是先讀答案」的順序取決於當前元素是否屬於要查詢的視窗，搞錯會差一格。\n- 求最小值時把比較反向：尾端清掉「比新元素大」的候選，頭端就變成最小值。\n- 複雜度：每個下標進出佇列各一次，整體 `O(n)` 時間、`O(k)` 空間。\n\n**操作順序不要死背，先寫出查詢視窗的語意。** 每輪先移除已不在查詢區間內的隊首。若本輪查詢包含當前元素 `i`，就先清尾、入隊，再讀答案；若 DP 轉移只允許使用先前位置、查詢不含 `i`，就先讀答案，再把 `i` 入隊。這條規則同時避免漏掉當前元素與誤用尚未合法的狀態。\n\n## C++17 模板\n\n以下用本節代表題 239. 滑動視窗最大值 展示核心寫法。\n\n**完整題目**：給定陣列與視窗大小 `k`，回傳每個滑動視窗中的最大值。\n\n:::example 單調遞減雙端隊列：隊列存下標，隊首始終為當前窗口最大值的下標\n**線索**：題目限制連續子陣列/子字串，且合法性可透過移動左端恢復，這是滑動視窗的線索。\n\n```cpp\nclass Solution {\npublic:\n    // 單調遞減雙端隊列：隊列存下標，隊首始終為當前窗口最大值的下標\n    vector<int> maxSlidingWindow(vector<int>& nums, int k) {\n            deque<int> dq;\n            vector<int> ans;\n            for (int i = 0; i < (int)nums.size(); ++i) {\n                    // 移除已滑出窗口的隊首元素\n                    while (!dq.empty() && dq.front() <= i - k) {\n                            dq.pop_front();\n                    }\n                    // 維護單調性：移除所有小於 nums[i] 的隊尾元素（它們不可能是最大值）\n                    while (!dq.empty() && nums[dq.back()] <= nums[i]) {\n                            dq.pop_back();\n                    }\n                    dq.push_back(i);\n                    if (i >= k - 1) {\n                            ans.push_back(nums[dq.front()]);  // 隊首即為當前窗口最大值\n                    }\n            }\n            return ans;\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- 單調佇列保存可能成為最大值的下標；過期元素從隊首移除，被新元素支配的元素從隊尾移除。\n- 右端加入新元素後更新窗口狀態；若條件被破壞，就移動左端直到恢復合法。\n- 每個下標最多被加入與移出一次，因此整體是線性掃描。\n\n## 時間與空間複雜度\n\n| 題目 | 樸素 | 單調佇列 | 空間 |\n| --- | --- | --- | --- |\n| 239 滑動視窗最大值 | $O(nk)$ | $O(n)$ | $O(k)$ |\n| 862 和至少為 K 的最短子陣列 | $O(n^2)$ | $O(n)$ | $O(n)$ |\n| 1438 / 2762 | $O(n^2)$ | $O(n)$ | $O(n)$ |\n| 1499 滿足不等式的最大值 | $O(n^2)$ | $O(n)$ | $O(n)$ |\n| 2398 預算內的最多機器人數目 | $O(n^2)$ | $O(n)$ | $O(n)$ |\n| DP 區間轉移 $\\mathrm{dp}[i]=a_i+\\max_{[i-k,i-1]}\\mathrm{dp}[j]$ | $O(nk)$ | $O(n)$ | $O(k)$ |\n\n**時間 $O(n)$**（定理 2 的位能論證），**空間 $O(k)$ 或 $O(n)$**（視窗最大長度）。\n\n**與其他做法的量化比較**（$n = 10^5$、$k = 10^4$）：\n- 樸素 $O(nk) = 10^9$ ⇒ TLE。\n- `multiset` 滑動視窗 $O(n\\log k) \\approx 1.3\\times10^6$ 次比較，但**常數大（紅黑樹節點配置、快取不友善）**，實測常慢 5–10 倍。\n- 單調佇列 $O(n) = 10^5$，且全是連續記憶體上的整數比較。\n\n**常數優化**：把 `deque<int>` 換成 `vector<int> q(n); int h = 0, t = 0;`（`t` 為尾後下標），入隊 `q[t++] = i`、彈尾 `--t`、彈頭 `++h`。**因為每個下標最多入隊一次，`n` 就是容量上界，不會溢位。** 這樣寫通常比 `std::deque` 快 2 倍以上，是競賽的標準寫法。\n\n## 常見錯誤與邊界條件\n\n- 先建立第一個完整視窗，再開始更新答案。\n- 移出元素後頻次為 0 要刪除或扣 distinct。\n- 平均值比較可改成和比較，避免浮點誤差。\n- k 可能為 0、1 或等於 n 時要特判。\n\n- 不要固定背「入隊 → 出隊 → 更新」：先刪過期元素，再依查詢是否包含當前元素，決定「入隊後查」或「查後入隊」。\n\n## 常見變形\n\n- 固定 k 的頻次表、distinct 個數。\n- 環形陣列可複製或用取模。\n- 搭配單調佇列維護固定視窗最大/最小。\n\n## 與相似技巧的比較\n\n- **與 4.7 單調堆疊**：**單調堆疊的區間左界由「資料」決定（第一個更大者），單調佇列的左界由「下標」或「條件」決定。** 另一個說法：**堆疊只從尾端出，佇列兩端都出**；多出來的頭端出口就是「過期」的概念。**題目說「固定寬度 / 最近 k 個 / 距離不超過 k」⇒ 單調佇列；說「左邊第一個更大的」⇒ 單調堆疊。**\n- **與 5.1 普通佇列**：普通佇列保留視窗內**全部**元素，單調佇列只保留**可能成為答案的候選**。**兩者的頭端淘汰邏輯完全一樣，差別只在尾端多了去劣。**\n- **與 `multiset` 滑動視窗**：`multiset` 支援**任意刪除**，所以視窗左端可以任意跳、甚至可以刪中間的元素；單調佇列不行。**代價是 $O(\\log k)$ 與很差的常數。** 判準：**若被刪除的永遠是「最舊的」，用單調佇列；若刪除位置任意，用 `multiset`。**（4.6 定理 3 的對頂堆與 480 的延遲刪除也屬後者。）\n- **與 6.x 優先佇列 + 延遲刪除**：堆頂若已過期就彈掉，$O(n\\log n)$。**寫起來比單調佇列短，但慢一個 $\\log$**；當「劣勢淘汰」的判準不明確（例如比較函數複雜）時，這是穩妥的退路。\n- **與 9.7 ST 表**：ST 表 $O(n\\log n)$ 預處理、$O(1)$ 任意區間極值，**但需要離線知道整個陣列**，且只支援可重複貢獻的運算（min/max/gcd）。**視窗單調移動 ⇒ 單調佇列（$O(n)$、線上）；區間任意 ⇒ ST 表。**\n- **與 9.3 線段樹**：支援單點修改 + 任意區間查詢。**若元素在過程中會被修改，單調佇列與 ST 表都不能用，只剩線段樹。**\n- **與斜率優化 / 決策單調性**：當 $w(i,j)$ 無法拆成 $f(i)+g(j)$（有 $i\\cdot j$ 之類的交叉項）時，單調佇列不夠，要升級成單調佇列 + 凸包（斜率優化）。**定理 5 的「可拆分」正是兩者的分界。**\n\n## 代表例題\n\n**題號**：LCR 184、239、862、1438、1499\n\n放在「單調佇列」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| LCR 184 | [設計自助結算系統](https://leetcode.cn/problems/dui-lie-de-zui-da-zhi-lcof) |  |\n| 239 | [滑動視窗最大值](https://leetcode.cn/problems/sliding-window-maximum) |  |\n| 862 | [和至少為 K 的最短子陣列](https://leetcode.cn/problems/shortest-subarray-with-sum-at-least-k) | 2307 |\n| 1438 | [絕對差不超過限制的最長連續子陣列](https://leetcode.cn/problems/longest-continuous-subarray-with-absolute-diff-less-than-or-equal-to-limit) | 1672 |\n| 1499 | [滿足不等式的最大值](https://leetcode.cn/problems/max-value-of-equation) | 2456 |\n| 2398 | [預算內的最多機器人數目](https://leetcode.cn/problems/maximum-number-of-robots-within-budget) | 1917 |\n| 2762 | [不間斷子陣列](https://leetcode.cn/problems/continuous-subarrays) | 1940 |\n| 3589 | [計數質數間隔平衡子陣列](https://leetcode.cn/problems/count-prime-gap-balanced-subarrays) | 2235 |\n\n## 本節重點速查\n\n- **頭端管時間（下標過期）、尾端管價值（被支配）。** 這是單調佇列與單調堆疊（只有尾端）、普通佇列（只有頭端）的唯一差別。\n- **存下標不存值**（要判斷過期）。求 max ⇒ 遞減佇列；求 min ⇒ 遞增佇列。\n- **兩個前提缺一不可（定理 1）：視窗右端單調不減、左端單調不減。** 任一端可以往回走，就必須改用 ST 表（9.7）或線段樹（9.3）。\n- **通用證明模板**：要丟掉舊元素 $l'$，必須證明新元素在**所有未來查詢**中都不劣——通常是兩個維度同時不劣（862 定理 3：既更容易成交、又更短）。寫題時應該能把它具體化成兩行不等式。\n- **攤還 $O(n)$**（定理 2）：每個下標入隊一次、出隊一次，位能法不在意它從哪端離開。\n- **固定寬度的頭端條件是 `dq.front() <= i - k`；可變視窗是 `dq.front() < l`。** 兩套骨架不要混用。\n- **862 的教訓**：有負數 ⇒ 字首和不單調 ⇒ 樸素滑動視窗失效 ⇒ 改單調佇列。**「元素可為負」是這類題的關鍵字。**\n- **1499 的教訓**：把目標拆成 $f(i) + g(j)$，就能用單調佇列優化 DP；拆不開就要斜率優化。\n- 效能寫法：用 `vector<int> q(n)` + `h`／`t` 兩個下標取代 `std::deque`，通常快 2 倍以上。\n- **順序規則**：先刪過期隊首； 查詢包含當前元素就先入隊再查，不包含就先查再入隊。\n",
        },
      ],
    },
    {
      id: 40,
      title: "6. 堆（優先佇列）",
      summary:
        "堆（優先佇列）只承諾一件事：`O(1)` 看到最大或最小值，`O(log n)` 插入與彈出。它**不維護完整順序**，所以「取第二小」「找前驅後繼」這種需求要改用有序集合。訊號是「每一步都要拿當前最優的那個」，而候選集合會隨著過程不斷加入與移除。\n\n延伸閱讀：[為什麼堆化的時間複雜度是 $O(n)$？](https://leetcode.cn/problems/take-gifts-from-the-richest-pile/solutions/2501655/yuan-di-dui-hua-o1-kong-jian-fu-ti-dan-p-fzdh/)\n\n**如何挑子單元**\n\n- **6.1 基礎 / 6.2 進階**：直接取最值、合併 k 個有序序列。\n- **6.3 第 K 小/大**：求第 K **大**用**小根堆**且只留 K 個元素（堆頂就是答案），反之亦然。這個方向很容易寫反，記法是「留下的那一堆就是答案候選，堆頂是其中最差的一個」。\n- **6.4 重排元素**：例如任務排程、重構字串，每回合取出出現次數最多的幾個，用完再放回。\n- **6.5 反悔堆**：先貪心地全部接受，一旦超出限制，就從堆裡彈掉「當初最不划算」的那個決定。它讓貪心在事後可以修正。\n- **6.6 懶刪除堆**：堆不支援刪除任意元素，所以改成彈出時才檢查「這個元素是否已經過期」，過期就丟掉繼續彈。\n- **6.7 對頂堆**：一個大根堆配一個小根堆，中間卡住第 K 名；用來動態維護中位數或滑動視窗第 K 小/大。每次操作後都要重新平衡兩堆大小。",
      children: [
        {
          id: 41,
          title: "6.1 基礎",
          summary:
            "## 這個技術解決什麼問題\n\n堆（heap）／優先佇列（priority queue）解決的是：「**資料動態變動，而你要反覆取出當前的最大或最小值**」。\n\n- **排序**只做一次，之後插入新元素就得重排 —— $O(n\\log n)$ 每次。\n- **掃全部找最值**是 $O(n)$ 每次。\n- **堆**：插入與取極值都是 $O(\\log n)$，查看極值 $O(1)$。\n\n三種典型場景：\n\n- **動態 Top-K**：703（資料流中的第 K 大元素）、2530（執行 K 次操作後的最大分數）。\n- **貪心的「每步取最划算」**：1046（最後一塊石頭的重量）、1167（連線木棍的最低費用）、1792（最大平均透過率）、2233（K 次增加後的最大乘積）。**堆是貪心演算法的標準配件**——貪心告訴你「取最優的那個」，堆告訴你「怎麼快速取到」。\n- **掃描線 + 資源池**：253（會議室 II）、1834（單執行緒 CPU）。\n\n**辨識訊號**：出現「每次取最大／最小」「第 K 大」「合併最小的兩個」「最早結束的那個」，而且資料在過程中會增減。\n\n**反辨識訊號**：若資料**靜態**且只問一次第 K 大，用 `nth_element`（$O(n)$ 期望）比堆快；若要**全部**排序，直接 `sort`。**堆的價值在「動態」與「只取一小撮」。**\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「堆」想：\n\n- **要反覆取「最大 / 最小 / 第 K 大」，且資料動態增加。** 像「資料流中的第 K 大元素」「最後一塊石頭的重量」（每次取兩塊最重的相撞）。\n- **每步都要在一堆候選裡挑「目前最划算的那個」。** 像「連線木棍的最低費用」（每次合併最短兩根）、「最大平均透過率」（每次提升收益最大的班級）——這是貪心 + 堆的經典組合。\n- **暴力是每次查詢都重新排序 / 掃全部找最值，太慢：**\n\n:::example 嗅到味道：每次都 sort 或線性掃找最小\n```cpp\n// 嗅到味道：每次都 sort 或線性掃找最小\n// sort(v.begin(), v.end()); take v[0];   // 每次 O(n log n)\n// 改用堆，單次取最值只要 O(log n)\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要的是最大還是最小？該用 max-heap 還是小根堆？\n2. 堆元素的排序鍵是什麼？還需要帶哪些定位資訊？\n3. 堆大小有上限嗎（Top-K）？超出時該彈掉哪一個？\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n本節介紹堆（heap），在 C++ 裡就是 `priority_queue`（優先佇列）。它解決的場景是：手上有一堆會動態增減的候選，而你**需要反覆取出目前的最大值（或最小值）**，例如查最大、最小、或 Top-K（前 K 大）。\n\n:::example 堆的三個基本操作（預設 max-heap，top() 取最大值）\n如果每次查詢都重新排序，是 `O(n log n)`；堆讓你只花 `O(log n)` 就完成一次加入或取出最值。它的三個常用操作：\n\n```cpp\n// 堆的三個基本操作（預設 max-heap，top() 取最大值）\npriority_queue<int> pq;\npq.push(5);  // 加入，O(log n)\npq.push(8);\nint mx = pq.top();  // 8：目前最大值，O(1)\npq.pop();           // 移除最大值，O(log n)\n```\n:::\n\n\n\n它為什麼快？堆內部是一棵「父節點總是 ≥ 子節點」的完全二元樹，所以最大值永遠在頂端，取它是 `O(1)`；加入或移除後只需沿著樹的一條路徑「上浮 / 下沉」修復，路徑長度只有 `O(log n)`。\n\n:::example 小根堆維護 Top-K：堆頂即第 K 大\n一個最常見的招式是「Top-K」：要維護「目前第 K 大的元素」，只需用一個**小根堆**保留 K 個最大值。新元素若比堆頂（這 K 個裡最小的）大，就替換掉堆頂；堆頂始終是第 K 大：\n\n```cpp\n// 小根堆維護 Top-K：堆頂即第 K 大\npriority_queue<int, vector<int>, greater<int>> pq;  // greater → 小根堆\nvoid add(int x) {\n    pq.push(x);\n    if (pq.size() > k) {\n        pq.pop();  // 超過 K 個就丟最小的\n    }\n}\nint kthLargest() { return pq.top(); }  // 堆頂 = 第 K 大\n```\n:::\n\n\n\n注意：C++ `priority_queue` **預設是 max-heap**（`top()` 取最大）；要小根堆得用 `greater<int>` 或把值取負。\n\n## 狀態／資料結構定義\n\n### `std::priority_queue` 的語意\n\n```cpp\npriority_queue<int> mx;                                  // 大頂堆（預設）\npriority_queue<int, vector<int>, greater<int>> mn;       // 小頂堆\npriority_queue<pair<int,int>> pq;                        // 先比 first，再比 second\n```\n\n**`priority_queue<T, Container, Comp>` 的 `top()` 是「在 `Comp` 這個『小於』關係下最大的元素」。** 所以傳 `greater<int>`（把「大於」當成「小於」）就得到小頂堆。**這一層反轉是所有人第一次都會寫反的地方**，記法是：**`greater` ⇒ 小頂堆**。\n\n**自訂比較器**（注意方向與 `sort` 相反）：\n```cpp\nstruct Task { int cost, id; };\nauto cmp = [](const Task& a, const Task& b) {\n    if (a.cost != b.cost) return a.cost > b.cost;   // cost 小的優先 → 寫 >\n    return a.id > b.id;                             // 同 cost 時 id 小的優先\n};\npriority_queue<Task, vector<Task>, decltype(cmp)> pq(cmp);\n```\n**心法：`sort` 的比較器寫「誰排前面」，`priority_queue` 的比較器寫「誰排後面」。** 若不確定，用一組小資料實測，不要靠記憶。\n\n### 底層陣列表示\n\n堆是一棵**完全二元樹**，用陣列 `h[0..n-1]` 表示：節點 $i$ 的左右子為 $2i+1$、$2i+2$，父為 $\\lfloor (i-1)/2 \\rfloor$。\n\n**堆性質（小頂堆）**：對所有非根節點 $i$，$h[\\mathrm{parent}(i)] \\le h[i]$。\n\n**注意堆性質只約束父子，不約束兄弟。** 因此**堆不是排序的**：`h[1]` 不一定小於 `h[2]`，而且**第二小的元素一定是根的某個子節點，但第三小可能在任何深度 $\\le 2$ 的位置**。「堆頂之外的順序」是所有堆題的常見誤解來源。\n\n### 三個核心操作\n\n| 操作 | 做法 | 複雜度 |\n| --- | --- | --- |\n| `push` | 放到陣列尾端，**上濾**（與父交換直到滿足堆性質） | $O(\\log n)$ |\n| `pop` | 把尾端搬到根，縮小，**下濾**（與較小的子交換） | $O(\\log n)$ |\n| `build` | 從 $\\lfloor n/2 \\rfloor - 1$ 往 0 依序**下濾** | $O(n)$，見定理 3 |\n\n## 不變量或正確性證明\n\n### 定理 1（上濾與下濾的區域性）\n\n*引理*：若一棵完全二元樹**除了節點 $v$ 之外**處處滿足堆性質，且 $v$ 只違反與其父的關係，則反覆「與父交換」直到不再違反，可恢復全樹的堆性質。\n\n*證明*。設 $v$ 的值為 $x$，父為 $p$（值 $y$），$y > x$（違反小頂堆）。交換後：\n- $v$ 位置放 $y$：其子孫原本 $\\ge x$⋯ 不夠，需要 $\\ge y$？ 不。原本 $p$ 是 $v$ 的父，$p$ 的另一個子樹的根 $u$ 滿足 $y \\le h[u]$，交換後 $y$ 移到 $v$ 的位置，$v$ 的子樹原本滿足 $x \\le$ 子孫，但現在根變成 $y > x$⋯\n\n  正確的論證是：**上濾只在「$v$ 是新插入的葉節點」或「$v$ 的子樹整體不變」時使用**。此時 $v$ 的子樹在交換後根變為 $y$，而 $y$ 原本是 $p$，$p \\le v$ 的所有子孫（因為交換前 $p$ 是 $v$ 的祖先，堆性質沿路徑傳遞：$p \\le$ $v$ 的子孫）。故新根 $y$ 仍 $\\le$ 子孫 ✔。\n- $p$ 位置放 $x$：$x < y \\le$ ($p$ 的另一子樹全體) ✔；而 $x$ 與 $p$ 的父可能違反，繼續上濾。\n\n  每次上濾使違反點的深度嚴格減 1，最多 $\\lfloor \\log_2 n \\rfloor$ 次後到根，必然終止。$\\square$\n\n*對稱地*，**下濾**要求「$v$ 的兩棵子樹都已是合法的堆」，此時把 $v$ 與**較小的子**交換，可保證交換後該子位置滿足堆性質（新值 $\\le$ 另一子、$\\le$ 原本自己的子孫）。\n\n**兩者的前提不同、不可互換**：上濾要求「除了往上這條路徑外都合法」，下濾要求「兩棵子樹都合法」。`push` 只能上濾、`pop` 只能下濾，就是因為這個前提差異。\n\n### 定理 2（`pop` 為什麼要「把最後一個搬到根」）\n\n直接刪掉根會留下一個洞，破壞完全二元樹的形狀。搬最後一個元素到根，**形狀恆保持為完全二元樹**（這是陣列表示能成立的必要條件），再下濾修復值的性質。\n\n**「形狀不變量」與「值不變量」分開維護，是所有堆操作的設計原則。** 形狀由「總是在陣列尾端增刪」保證，值由上濾／下濾保證。\n\n### 定理 3（`build_heap` 是 $O(n)$ 不是 $O(n\\log n)$）\n\n自底向上對每個非葉節點下濾。高度為 $h$ 的節點下濾成本 $O(h)$；高度為 $h$ 的節點至多 $\\lceil n / 2^{h+1} \\rceil$ 個。總成本\n$$\\sum_{h=0}^{\\lfloor \\log_2 n\\rfloor} \\left\\lceil \\frac{n}{2^{h+1}} \\right\\rceil O(h) \\;=\\; O\\!\\left(n \\sum_{h=0}^{\\infty} \\frac{h}{2^{h+1}}\\right) \\;=\\; O(n) ,$$\n因為 $\\sum_{h \\ge 0} h/2^{h} = 2$ 收斂。$\\square$\n\n**直覺**：**大部分節點是葉節點（成本 0），只有極少數節點在高處（成本高）。** 反過來由上而下逐個 `push` 則是 $O(n\\log n)$——因為此時大部分節點都要走完整條到根的路徑。**「$n$ 個元素建堆」與「`push` $n$ 次」不是同一件事**，這是面試常考的區分。C++ 對應 `std::make_heap`（$O(n)$）與 `priority_queue` 的區間建構子（內部呼叫 `make_heap`，也是 $O(n)$）。\n\n### 定理 4（703：大小為 K 的小頂堆的不變量）\n\n維護一個**小頂堆**，恆保持大小 $\\le K$：每次插入後若 `pq.size() > K` 就 `pop()`。\n\n*不變量*：**堆中恰好是「目前為止所有元素中最大的 $\\min(K, n)$ 個」**，故 `pq.top()` 就是第 $K$ 大。\n\n*證明（歸納）*。設處理完前 $n$ 個元素後不變量成立。加入 $x$：\n- 若 $|pq| < K$：直接加，堆中是全部 $n+1$ 個，即「最大的 $n+1$ 個」✔。\n- 若 $|pq| = K$：加入後 $K+1$ 個，彈出最小者。設原堆為前 $n$ 個中最大的 $K$ 個，記為 $S$。新集合 $S \\cup \\{x\\}$ 的最小者恰是 $\\{$前 $n{+}1$ 個元素$\\}$ 中第 $K+1$ 大以下的某個⋯ 更直接地：前 $n+1$ 個元素中最大的 $K$ 個，必定是 $S \\cup \\{x\\}$ 去掉其最小者。理由：任何不在 $S \\cup \\{x\\}$ 的元素 $y$，都不在前 $n$ 個的前 $K$ 大之列，故存在 $S$ 中至少 $K$ 個元素 $\\ge y$，於是 $y$ 在前 $n+1$ 個中的排名 $> K$。$\\square$\n\n**為什麼求「第 K 大」要用小頂堆（而不是大頂堆）？** 因為要能 $O(1)$ 拿到「這 $K$ 個裡最弱的那個」以便淘汰。**記法：求第 K 大 ⇒ 小頂堆；求第 K 小 ⇒ 大頂堆。** 恆為反的。\n\n### 定理 5（1167 連線木棍：Huffman 貪心的最優性）\n\n每次合併兩根，費用為兩者長度和；求把 $n$ 根合成一根的最小總費用。\n\n*宣稱*：**每次合併當前最短的兩根是最優的。**\n\n*證明（交換論證）*。合併過程對應一棵二元樹：葉是原始木棍，內部節點是合併結果，**總費用 $= \\sum_i \\ell_i \\cdot d_i$**，其中 $d_i$ 是葉 $i$ 的深度（因為木棍 $i$ 每參與一次合併就被計入一次費用，參與次數 = 它到根的邊數）。\n\n設 $a \\le b$ 是最短的兩根。取任一最優樹 $T$：\n1. *$T$ 中存在兩片深度最大且互為兄弟的葉* $x, y$（最深的葉的父節點必有兩個子，且都是葉，否則還有更深的）。\n2. *把 $a$ 與 $x$ 交換*：設 $d_a \\le d_x$（$x$ 最深）。費用變化 $= (\\ell_a d_x + \\ell_x d_a) - (\\ell_a d_a + \\ell_x d_x) = (\\ell_x - \\ell_a)(d_a - d_x) \\le 0$（因 $\\ell_a \\le \\ell_x$、$d_a \\le d_x$）。故不劣。\n3. 同理把 $b$ 與 $y$ 交換，仍不劣。\n\n於是存在一個最優樹使 $a, b$ 互為兄弟且在最深層。把它們合併成一個長度 $a+b$ 的葉，問題規模減一，對 $n$ 歸納即得。$\\square$\n\n**這個證明的兩個支柱值得單獨記住**：(i) **總費用可以改寫成 $\\sum \\ell_i d_i$**（把「過程」翻譯成「靜態的樹指標」）；(ii) **重排不等式式的交換**——長的配淺的、短的配深的不劣。1046（最後一塊石頭的重量）雖然是「相撞取差」而非「合併取和」，但同樣靠「每次取最大兩個」的貪心，理由是差值只由當前最大兩者決定。\n\n### 定理 6（1792 最大平均透過率：邊際效益遞減 ⇒ 貪心正確）\n\n有 $m$ 個班級，第 $i$ 班原本 $p_i / t_i$；有 `extra` 個一定會過的學生要分配。目標最大化平均透過率。\n\n*邊際收益*：把一個學生分給狀態為 $(p, t)$ 的班級，收益\n$$\\Delta(p,t) = \\frac{p+1}{t+1} - \\frac{p}{t} = \\frac{t-p}{t(t+1)} .$$\n\n*引理（遞減 / 凹性）*：對同一班連續分配時，$\\Delta(p+1, t+1) = \\dfrac{t-p}{(t+1)(t+2)} < \\dfrac{t-p}{t(t+1)} = \\Delta(p, t)$（當 $t > p$）。$\\square$\n\n*定理（可分離凹函數的資源分配）*：若各班的收益函數關於「分到的人數」是**凹的**（邊際收益遞減），則「每次把下一個學生分給當前邊際收益最大的班級」得到全域最優。\n\n*證明概要（交換論證）*。設貪心解為 $G$、某最優解為 $O$，且它們第一次分歧於第 $s$ 步：貪心給了班 $A$，最優給了班 $B$。因為 $O$ 總共比 $G$ 多給 $A$ 的人數不足⋯ 標準做法：由於 $\\Delta_A$（在 $G$ 的當下狀態）$\\ge \\Delta_B$，把 $O$ 中某個給 $B$ 的學生改給 $A$。由凹性，$A$ 少收到的那個學生的邊際值 $\\ge$ 貪心當下的 $\\Delta_A$⋯ 精確地：凹性保證「$A$ 的第 $k$ 個學生的邊際收益」隨 $k$ 遞減，因此把一個學生從 $B$ 移到 $A$，$A$ 增加的量 $\\ge$ 該班的最後一個邊際值 $\\ge \\Delta_B \\ge$ $B$ 失去的量。故 $O$ 不會變差，分歧點右移一步。歸納可得 $G$ 最優。$\\square$\n\n**「邊際收益遞減」是所有『用堆做貪心分配』題的共同前提**（2233 K 次增加後的最大乘積、2530 執行 K 次操作後的最大分數、1962 移除石子使剩下的石頭最少 都是同一族）。**寫這類題時先驗證凹性，凹性不成立（邊際收益會變大）貪心就會錯。**\n\n*實作*：堆按 $\\Delta$ 排序，每次取出、分配一個、重算 $\\Delta$、放回。\n\n### 定理 7（253 會議室 II：答案 = 最大重疊數）\n\n*宣稱*：所需會議室數 $=\\max_t |\\{i : t \\in [s_i, e_i)\\}|$。\n\n*下界*：若某時刻 $t$ 有 $c$ 場會議同時進行，它們兩兩衝突，必須各佔一間 ⇒ 至少 $c$ 間。\n*上界（構造）*：按開始時間排序，維護一個「各會議室的結束時間」的小頂堆。新會議開始時，若堆頂（最早結束）$\\le$ 新會議開始時間，就重用該會議室（`pop` 後 `push` 新結束時間）；否則開一間新的。此演算法開新房間的時刻，恰是當前所有房間都被佔用的時刻，即重疊數達到新高。故用的房間數 $=$ 最大重疊數。$\\square$\n\n**「按左端點排序 + 小頂堆存右端點」是區間調度的標準骨架**（3.1.2 的差分事件掃描是同一件事的另一種寫法，$O(n)$ vs $O(n\\log n)$，但堆的版本能同時回答「是哪一間」）。\n\n## 逐步演算法\n\n:::example 狀態表示：{結束時間} 排序，例：會議室\n**1. 狀態表示：堆元素帶上排序鍵與定位資訊。** 元素要包含「用來比較的鍵」以及之後需要的資訊（值、下標、過期邊界、來源序列等）。\n\n```cpp\n// 狀態表示：{結束時間} 排序，例：會議室\npriority_queue<int, vector<int>, greater<int>> pq;  // 存各會議的結束時間\n```\n:::\n\n\n\n:::example 更新：會議室——能複用就 pop 最早結束的，否則開新間\n**2. 更新流程：加入後只做局部調整；有上限就立刻彈出。** 每次 push / pop 都只修復一條路徑；若堆大小有上限（如 Top-K），超出就馬上 pop。\n\n```cpp\n// 更新：會議室——能複用就 pop 最早結束的，否則開新間\nif (!pq.empty() && pq.top() <= start) {\n    pq.pop();  // 最早結束的會議已散，房間可複用\n}\npq.push(end);  // 放入這場會議的結束時間\n```\n:::\n\n\n\n:::example 答案讀取：堆裡的元素數 = 同時進行的會議數 = 所需房間數\n**3. 答案讀取：堆頂即當前最優候選。** 直接讀 `top()`。若候選可能過期，讀前要先清理堆頂（見「懶刪除」一節）。\n\n```cpp\n// 答案讀取：堆裡的元素數 = 同時進行的會議數 = 所需房間數\nint rooms = pq.size();\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 預設 max-heap，求最小值記得改用 `greater < >` 或存負值，別搞反方向。\n- 自訂比較器必須符合「嚴格弱序」（不能對相等元素同時回傳 true），否則行為未定義。\n- 複雜度：每次 push / pop 為 `O(log n)`，取頂 `O(1)`；處理 n 個元素整體 `O(n log n)`。\n\n## C++17 模板\n\n以下用 1792. 最大平均透過率 展示堆（優先佇列）的基礎貪心的核心寫法。\n\n**完整題目**：有若干班級，第 i 班 `pass[i]` 人通過、`total[i]` 人應試。另有 `extraStudents` 個一定會通過的學生，可任意分配到各班（每人讓某班的 pass 與 total 各加 1）。求分配後所有班級通過率平均值的最大值。\n\n:::example 貪心 + 最大堆：每次將額外學生分配給「增益最大」的班級\n**線索**：每多放一個學生到某班，帶來的增益 `(p+1)/(t+1) - p/t` 會隨該班再加人而遞減。要最大化總增益，就每次把學生放到「目前邊際增益最大」的班，這正是用最大堆反覆取最大值的貪心。\n\n```cpp\nclass Solution {\npublic:\n    // 貪心 + 最大堆：每次將額外學生分配給「增益最大」的班級\n    double maxAverageRatio(vector<vector<int>>& classes, int extraStudents) {\n            // gain(p, t)：加一個優等生後通過率的增益\n            auto gain = [](double p, double t) { return (p + 1) / (t + 1) - p / t; };\n            priority_queue<tuple<double, int, int>> pq;  // 最大堆，按增益排序\n            for (auto& c : classes) {\n                    pq.push({gain(c[0], c[1]), c[0], c[1]});\n            }\n            // 每輪取增益最大的班級加入一個優等生，再重新計算並推回堆\n            while (extraStudents > 0) {\n                    auto [g, p, t] = pq.top();\n                    pq.pop();\n                    pq.push({gain(p + 1, t + 1), p + 1, t + 1});\n                    --extraStudents;\n            }\n            double sum = 0;\n            while (!pq.empty()) {\n                    auto [g, p, t] = pq.top();\n                    pq.pop();\n                    sum += (double)p / t;\n            }\n            return sum / classes.size();  // 回傳所有班級的平均通過率\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- `gain(p, t)` 算的是「再加一個必過學生」帶來的通過率增量。\n- 堆中以增益為鍵，堆頂永遠是當下最值得投入的班。\n- 每次取出堆頂、把該班加一人後重新計算增益並壓回。\n- 學生分配完後把各班通過率加總取平均。\n\n## 時間與空間複雜度\n\n| 操作 | 二元堆 | 說明 |\n| --- | --- | --- |\n| `top()` | $O(1)$ | |\n| `push` | $O(\\log n)$ 最壞，**攤還 $O(1)$**（隨機輸入） | 平均上濾距離是常數 |\n| `pop` | $O(\\log n)$ 最壞與攤還 | 下濾幾乎總是走到底 |\n| 建堆（`make_heap`） | $O(n)$ | 定理 3 |\n| 查找任意元素 | $O(n)$ | **堆不支援搜尋** |\n| 刪除任意元素 | $O(n)$ 定位 $+ O(\\log n)$ | 故有 6.6 懶刪除 |\n\n**題目層級**：\n- 703（第 K 大）：`add` $O(\\log K)$，空間 $O(K)$。**空間是 $O(K)$ 而非 $O(n)$，這正是用堆的理由。**\n- 1046 / 1167：$O(n\\log n)$ 時間、$O(n)$ 空間。**1167 若木棍長度值域小，可用「兩個佇列合流」在排序後做到 $O(n)$**（一個存原始升冪、一個存合併結果，兩者都遞增）——這是 Huffman 的線性實作，值得知道。\n- 253：$O(n\\log n)$（排序主導）、$O(n)$。\n- 1792：$O(m + \\textit{extra}\\cdot\\log m)$。\n\n**常數注意**：\n- **`priority_queue` 沒有 `reserve`，但底層 `vector` 有。** 可以先建一個 `vector` 再用區間建構子（$O(n)$ 建堆），比逐個 `push` 快得多。\n- **`pop()` 回傳 `void`**：`int x = pq.top(); pq.pop();`。\n- **存大物件時用 `priority_queue<pair<int,int>>` 而非 `priority_queue<BigStruct>`**——堆的上濾下濾會做 $O(\\log n)$ 次交換，物件越大越慢。\n\n## 常見錯誤與邊界條件\n\n- 增益遞減是貪心正確性的關鍵；若增益非單調，這種逐次取最大就不成立。\n- 用浮點增益當堆鍵，注意精度；比較大小通常足夠，不需嚴格相等判斷。\n- `tuple` 第一維是 double 鍵，後兩維存狀態，重算後再壓回。\n\n## 常見變形\n\n- IPO（502）：用堆每次選當前可做且利潤最大的專案。\n- 最後一塊石頭重量（1046）：每次取最大兩塊相撞。\n- 連接繩子最小成本（1167）：每次取最小兩段合併。\n\n## 與相似技巧的比較\n\n- **與 `sort`**：靜態、要全部順序 ⇒ `sort`（$O(n\\log n)$，常數小）。動態、只要極值 ⇒ 堆。**「排序後只用前幾個」通常可以換成堆或 `nth_element`。**\n- **與 `nth_element`**：靜態陣列求第 $K$ 小，`nth_element` 是**期望 $O(n)$**，比堆的 $O(n\\log K)$ 快。**但它會重排原陣列、且最壞 $O(n^2)$（實作通常有 introselect 保護）。資料流／動態場景不能用。**\n- **與 `std::set` / `multiset`**：`set` 也能 $O(\\log n)$ 取極值，**而且支援任意刪除與 `lower_bound`**。代價是常數大 3–5 倍、記憶體多。**判準：只需要極值 ⇒ 堆；還需要「找某個值」或「刪中間」⇒ `multiset`（或 6.6 懶刪除堆）。**\n- **與 5.4 單調佇列**：兩者都能取視窗極值。**單調佇列 $O(1)$ 攤還但要求視窗兩端單調右移；堆 $O(\\log n)$ 但不限移動方式。** 視窗固定右移就用單調佇列。\n- **與 6.3 二分答案**：求「第 K 小」時，堆是 $O(k\\log k)$、對值域二分是 $O(n\\log C)$。**$k$ 很小用堆，$k$ 接近 $n$ 用二分。**\n- **與計數排序 / 桶**：若值域很小（例如 $\\le 100$），用 `cnt[]` 陣列 + 從高往低掃，可以做到 $O(1)$ 取極值。**6.4 的 621 任務排程器就是這種情形，用陣列比用堆更快也更好寫。**\n- **與 9.x 線段樹**：線段樹能做「區間最值 + 單點修改」，堆只能做「全域最值」。**需要區間限定就上線段樹。**\n\n## 代表例題\n\n**題號**：253、703、1046、1167、1792\n\n請套用「堆（優先佇列）：基礎」的狀態、不變式與實作方式，先寫出資料表示、更新流程與答案讀取規則。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 253 | [會議室 II](https://leetcode.cn/problems/meeting-rooms-ii)（Premium） |  |\n| 703 | [資料流中的第 K 大元素](https://leetcode.cn/problems/kth-largest-element-in-a-stream) |  |\n| 1046 | [最後一塊石頭的重量](https://leetcode.cn/problems/last-stone-weight) | 1173 |\n| 1167 | [連線木棍的最低費用](https://leetcode.cn/problems/minimum-cost-to-connect-sticks)（Premium） | 1482 |\n| 1792 | [最大平均透過率](https://leetcode.cn/problems/maximum-average-pass-ratio) | 1818 |\n| 1801 | [積壓訂單中的訂單總數](https://leetcode.cn/problems/number-of-orders-in-the-backlog) | 1711 |\n| 1834 | [單執行緒 CPU](https://leetcode.cn/problems/single-threaded-cpu) | 1798 |\n| 1845 | [座位預約管理系統](https://leetcode.cn/problems/seat-reservation-manager) | 1429 |\n| 1942 | [最小未被佔據椅子的編號](https://leetcode.cn/problems/the-number-of-the-smallest-unoccupied-chair) | 1695 |\n| 1962 | [移除石子使總數最小](https://leetcode.cn/problems/remove-stones-to-minimize-the-total) | 1419 |\n| 2208 | [將陣列和減半的最少操作次數](https://leetcode.cn/problems/minimum-operations-to-halve-array-sum) | 1550 |\n| 2233 | [K 次增加後的最大乘積](https://leetcode.cn/problems/maximum-product-after-k-increments) | 1686 |\n| 2336 | [無限集中的最小數字](https://leetcode.cn/problems/smallest-number-in-infinite-set) | 1375 |\n| 2406 | [將區間分為最少組數](https://leetcode.cn/problems/divide-intervals-into-minimum-number-of-groups) | 1713 |\n| 2462 | [僱傭 K 位工人的總代價](https://leetcode.cn/problems/total-cost-to-hire-k-workers) | 1764 |\n| 2530 | [執行 K 次操作後的最大分數](https://leetcode.cn/problems/maximal-score-after-applying-k-operations) | 1386 |\n| 2558 | [從數量最多的堆取走禮物](https://leetcode.cn/problems/take-gifts-from-the-richest-pile) | 1277 |\n| 3066 | [超過閾值的最少運算元 II](https://leetcode.cn/problems/minimum-operations-to-exceed-threshold-value-ii) | 1400 |\n| 3264 | [K 次乘運算後的最終陣列 I](https://leetcode.cn/problems/final-array-state-after-k-multiplication-operations-i) | 1178 |\n| 3275 | [第 K 近障礙物查詢](https://leetcode.cn/problems/k-th-nearest-obstacle-queries) | 1420 |\n| 3296 | [移山所需的最少秒數](https://leetcode.cn/problems/minimum-number-of-seconds-to-make-mountain-height-zero) | 1695 |\n| 3478 | [選出和最大的 K 個元素](https://leetcode.cn/problems/choose-k-elements-with-maximum-sum) | 1753 |\n\n## 本節重點速查\n\n- **`priority_queue` 預設是大頂堆；傳 `greater<T>` 得到小頂堆。** 比較器的方向與 `sort` **相反**（`sort` 寫「誰在前」，`priority_queue` 寫「誰在後」）。不確定就實測。\n- **求第 K 大 ⇒ 維護大小為 K 的小頂堆**（定理 4），堆頂即答案，空間 $O(K)$。求第 K 小反之。\n- **堆只保證父子關係，兄弟之間無序**——`h[1] < h[2]` 不成立，堆不是排序的。\n- **`make_heap` 是 $O(n)$，`push` $n$ 次是 $O(n\\log n)$**（定理 3：大多數節點在底層、成本為 0）。有一整批初始資料就用區間建構子。\n- **上濾與下濾的前提不同**：上濾要求「只有往上這條路徑違反」，下濾要求「兩棵子樹都已合法」。所以 `push` 只能上濾、`pop` 只能下濾。\n- **貪心 + 堆的正確性靠交換論證**：1167 靠「費用 $=\\sum \\ell_i d_i$ + 長的配淺的」（定理 5）；1792 靠**邊際收益遞減（凹性）**（定理 6）。**凹性不成立，貪心就會錯——這是這類題唯一要檢查的前提。**\n- **253 區間調度骨架**：左端點排序 + 小頂堆存右端點；答案 = 最大重疊數（下界由衝突給出、上界由構造給出）。\n- **堆不支援搜尋與任意刪除**（$O(n)$）。需要刪中間 ⇒ 6.6 懶刪除堆或 `multiset`。\n- `pop()` 回傳 `void`；先 `top()` 取值再 `pop()`。\n",
        },
        {
          id: 42,
          title: "6.2 進階",
          summary:
            "## 這個技術解決什麼問題\n\n基礎堆（6.1）解決「動態取極值」。進階題把堆放進更大的框架裡，共有三種：\n\n- **K 路合流**：多條各自有序的資料來源要按全域順序消費——23（合併 K 個升序連結串列）、355（設計推特）、632（最小區間）、373／378（見 6.3）。**堆在這裡扮演「$k$ 條鏈的前緣（frontier）」。**\n- **瓶頸路 / 邊界擴張**：407（接雨水 II）、778（水位上升的泳池中游泳）、1631（最小體力消耗路徑）。**堆在這裡扮演 Dijkstra 的優先佇列，只是距離函數從「路徑和」換成「路徑上的最大值」。**\n- **貪心的「取最優、再帶入新候選」**：502（IPO）、1834（單執行緒 CPU）、2402（會議室 III）、857（僱用 K 名工人的最低成本）。**候選集合會隨掃描而擴大，堆負責在擴大的集合上取極值。**\n\n這三類的共同點是：**堆本身不是演算法，而是某個框架的「取最優」子程序**。所以寫這類題的順序永遠是——**先確定框架與貪心的正確性，再決定堆裡放什麼**。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「進階堆」想：\n\n- **有多個已排序來源要合併 / 交錯處理。** 像「合併 K 個升序連結串列」「最小區間」（從 K 個列表各挑一個，使範圍最小）。\n- **每步都要「取目前最優、再帶入新候選」的迭代。** 像「IPO」（每次在可負擔的專案裡挑利潤最大）、「接雨水 II」（每次從最矮的邊界往內灌水）。\n- **要動態維護資料的中位數 / 分位**，用雙堆把兩半的邊界各放堆頂：\n\n:::example 嗅到味道：要邊加入邊取中位數\n```cpp\n// 嗅到味道：要邊加入邊取中位數\n// 一個 max-heap + 一個小根堆，堆頂即中間值\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的堆代表「哪些來源的最前線候選」？大小是 `O(k)` 嗎？\n2. 取出一個之後，我該補進哪個後繼，才能維持「最前線」語意？\n3. 是單堆就夠，還是要雙堆維護兩半分佈？\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n本節是堆（`priority_queue`）的進階應用。基礎篇學會「動態取最值」後，進階題通常把堆當成「**多路選擇的引擎**」：每一步從多個來源中挑出當前最優的那個，挑完還會帶進新的候選。\n\n:::example K 路合併：堆裡永遠是各串列「目前最小的頭」\n最典型的招式是「K 路合併」。以「合併 K 個升序連結串列」為例：每條串列自己已排序，我們要合出一條總排序。直覺是每次比較 K 個串列的當前頭節點，挑最小的接上去——而「從 K 個候選裡挑最小」正是小根堆的拿手好戲。把每條串列的頭丟進堆，取出最小的接上後，再把它的下一個節點補進堆：\n\n```cpp\n// K 路合併：堆裡永遠是各串列「目前最小的頭」\npriority_queue<pair<int, ListNode*>, vector<pair<int, ListNode*>>,\n                              greater<>>\n        pq;  // 小根堆，比較值\nfor (auto* h : lists) {\n    if (h) {\n        pq.push({h->val, h});\n    }\n}\nwhile (!pq.empty()) {\n    auto [v, node] = pq.top();\n    pq.pop();  // 取全域最小\n    tail->next = node;\n    tail = node;  // 接到結果尾巴\n    if (node->next) {\n        pq.push({node->next->val, node->next});  // 補下一個\n    }\n}\n```\n:::\n\n\n\n關鍵直覺：堆裡始終只放「每個來源的最前線候選」（K 個），所以堆大小固定為 `O(k)`，每次取出 / 補入是 `O(log k)`。若總共 N 個元素，整體 `O(N log k)`，遠勝把全部丟一起排序。\n\n:::example 雙堆求中位數：左 max-heap 裝小半、右小根堆裝大半\n另一類進階題用「雙堆」維護資料分佈。例如要持續取中位數，就用「一個 max-heap 裝較小的一半、一個小根堆裝較大的一半」，並讓兩堆大小差不超過 1，中位數就落在堆頂：\n\n```cpp\n// 雙堆求中位數：左 max-heap 裝小半、右小根堆裝大半\npriority_queue<int> lo;  // 較小的一半（max-heap）\npriority_queue<int, vector<int>, greater<>> hi;  // 較大的一半（小根堆）\n```\n:::\n\n## 狀態／資料結構定義\n\n### (A) K 路合流\n\n```cpp\n// 23：堆中恆為「每條尚未耗盡的串列的當前頭節點」\nauto cmp = [](ListNode* a, ListNode* b) { return a->val > b->val; };  // 小頂堆\npriority_queue<ListNode*, vector<ListNode*>, decltype(cmp)> pq(cmp);\n```\n**堆的大小恆 $\\le k$，不是 $\\le n$。** 這是 $O(n\\log k)$ 而非 $O(n\\log n)$ 的來源。\n\n```cpp\n// 632：堆中恆為「每條串列的當前指標所指元素」(value, listId, idx)\npriority_queue<array<int,3>, vector<array<int,3>>, greater<>> pq;\nint curMax;   // 額外維護堆中的最大值——堆只能 O(1) 看最小值\n```\n**`curMax` 必須自己維護**：堆看不到自己的最大值。因為每次只換掉一個元素、且新元素 $\\ge$ 被換掉的，`curMax = max(curMax, newVal)` 就夠了——**這條增量更新的合法性來自「替換的一定是最小者、新值一定不小於它」**。\n\n### (B) 瓶頸路 / 邊界擴張\n\n```cpp\n// 407：堆中是「當前水池邊界」上的格子，按高度排序\npriority_queue<array<int,3>, vector<array<int,3>>, greater<>> pq;  // (height, r, c)\nvector<vector<char>> vis(m, vector<char>(n, 0));\nint level = 0;   // 已彈出的最大高度 = 當前水位\n```\n**`vis` 必須在「入堆時」標記**（和 BFS 同理），否則同一格會被推入多次。\n\n### (C) 掃描 + 候選池\n\n```cpp\n// 502：兩個容器分工\nsort(idx by capital);                                   // 依門檻升冪，指針 j 單向推進\npriority_queue<int> profitHeap;                         // 已負擔得起、尚未做的專案利潤（大頂堆）\n```\n**「一個排序陣列（按進入條件）+ 一個堆（按價值）」是這一族的通用形狀**：排序陣列決定「什麼時候有資格進場」，堆決定「進場後誰最優」。1834、2402、857、1353 全是同一個骨架。\n\n## 不變量或正確性證明\n\n### 定理 1（K 路合流：堆是「前緣」，彈出順序即全域有序）\n\n*不變量 (F)*：任一時刻，堆中恰好包含每條**尚未耗盡**串列的當前頭元素，各一個。\n\n*證明*：初始把每條串列的頭放入 ✔。每次彈出串列 $i$ 的頭 $x$ 後，若 $i$ 還有下一個元素就推入，否則 $i$ 耗盡 ✔。$\\square$\n\n*定理*：**彈出序列是全域非遞減的。**\n*證明*。設某次彈出 $x$（來自串列 $i$），下一次彈出 $y$。要證 $x \\le y$。$y$ 所屬串列 $j$ 在彈出 $x$ 時已有一個元素 $z$ 在堆中（由 (F)，若 $j$ 未耗盡）。因 $x$ 是彈出時的最小值，$x \\le z$。而 $y$ 若就是 $z$ 則 $x \\le y$ ✔；若 $y$ 是 $z$ 之後的元素，由串列 $j$ 自身有序，$z \\le y$，故 $x \\le z \\le y$ ✔。$\\square$\n\n**這條證明點名的前提是「每條來源自身有序」。** 少了它，堆只是個排序器，複雜度退化成 $O(n\\log n)$ 且需要一次放入全部元素（空間 $O(n)$）。**「每條來源有序」正是把空間從 $O(n)$ 壓到 $O(k)$ 的全部理由。**\n\n*推論（355 設計推特）*：`getNewsFeed` 只要最新的 10 則。把每位追蹤者的推文串列（按時間逆序）當成一條有序來源，合流時**取滿 10 則就停**，成本 $O(f + 10\\log f)$（$f$ 為追蹤數），而非 $O(\\text{總推文數})$。**「只要前 $m$ 個就在第 $m$ 次彈出後停」是 K 路合流最實用的推論。**\n\n### 定理 2（407／778／1631 是同一題：最小瓶頸路）\n\n定義從格子 $u$ 到格線外部的一條路徑的**瓶頸值**為路徑上（含起點）所有格子高度的最大值。令\n$$B(u) = \\min_{\\text{路徑 } P: u \\to \\text{外部}} \\ \\max_{v \\in P} h_v .$$\n\n*引理*：**格子 $u$ 上的積水量 $= \\max(0,\\ B'(u) - h_u)$**，其中 $B'(u)$ 是「不含 $u$ 自己」的最小瓶頸值。\n\n*證明概要*。($\\le$) 若水位高於 $B'(u)$，則沿著那條瓶頸值為 $B'(u)$ 的路徑，水會流到外面——水不可能穩定在該高度。($\\ge$) 若水位是 $B'(u)$，任何一條通往外部的路徑上都存在一格高度 $\\ge B'(u)$（否則該路徑的瓶頸值更小，與 $B'$ 的最小性矛盾），水被擋住。$\\square$\n\n*演算法（Dijkstra on the bottleneck metric）*：把所有邊界格推入小頂堆並標記已訪問。反覆彈出高度最小的格 $u$：\n```\nlevel = max(level, h[u]);\nfor 每個未訪問的鄰居 v:\n    water += max(0, level - h[v]);\n    vis[v] = 1;  pq.push({h[v], v});\n```\n\n*不變量 (L)*：**`level` 單調不減，且在彈出 $u$ 之後，`level` $= \\max\\{h_w : w \\text{ 已彈出}\\}$，同時等於「已彈出集合的邊界水位」。**\n\n*正確性*：這與 Dijkstra 的證明同構，只是「路徑代價 = 邊權和」換成「路徑代價 = 節點值的 max」。關鍵性質仍然成立：\n- **子路徑最優性**：最小瓶頸路的任一前綴也是到該點的最小瓶頸路（因為 $\\max$ 對前綴單調）。\n- **貪心可交換**：每次彈出的最小值不可能被之後更大的值改善（$\\max$ 只增不減）。\n\n因此 `level` 在彈出 $v$ 的那一刻就是 $B'(v)$，積水量正確。$\\square$\n\n**把 407 認出來是「最小瓶頸路」而不是「二維版的接雨水」，是解這一族題的關鍵。** 778（等待水位上升）問的是 $B(\\text{終點})$ 本身、1631（最小體力消耗）把節點值換成邊的差值——**三題共用同一份程式碼骨架**，只差在代價定義。（1631 也可以用並查集按邊權升冪合併，見 8.x；那是同一個問題的 Kruskal 視角。）\n\n**一維的 42 接雨水為什麼不需要堆？** 因為一維只有左右兩條路徑，$B'(i) = \\min(\\max_{j<i} h_j,\\ \\max_{j>i} h_j)$，前後綴最大值就能算完。**二維的路徑數是指數級，才需要 Dijkstra。**\n\n### 定理 3（502 IPO：資本單調 ⇒ 純貪心正確）\n\n有 $k$ 次機會；專案 $i$ 需要資本 $c_i$、獲利 $p_i \\ge 0$；做完資本增加 $p_i$。\n\n*引理（可行集單調）*：資本 $w$ 只增不減 ⇒ 「負擔得起的專案集合」$\\{i : c_i \\le w\\}$ 只增不減。**這依賴前提 $p_i \\ge 0$**（題目保證）。若有負獲利專案，可行集可能縮小，貪心立刻失效。\n\n*定理*：**每一步在當前可行集中取獲利最大者，是最優的。**\n*證明（交換論證）*。設最優解 $O$ 的第一步做了專案 $b$，而貪心做 $a$（$p_a \\ge p_b$，且 $a$ 當下可行）。\n- 若 $O$ 之後有做 $a$：把 $O$ 中 $a$ 與 $b$ 的位置互換。$a$ 提前做仍可行（第一步就負擔得起）；$b$ 延後做也仍可行（資本只增不減，之後的資本 $\\ge$ 現在 $\\ge c_b$）。總獲利不變。\n- 若 $O$ 之後沒做 $a$：把 $b$ 換成 $a$。獲利變化 $p_a - p_b \\ge 0$，不劣；且因 $p_a \\ge p_b$，後續每一步的資本都不比原來少，原本可行的後續選擇仍然可行。\n\n兩種情形下都存在一個「第一步與貪心相同」的最優解，歸納即得。$\\square$\n\n**這個證明點名了兩個前提**：**(i) 獲利非負（可行集單調）；(ii) 專案不重複做**。少了 (i)，就要改用 6.5 的反悔堆。\n\n*實作*：專案按 $c_i$ 升冪排序，指標 $j$ 單向推進，把所有 $c_i \\le w$ 的 $p_i$ 倒進大頂堆；取堆頂加到 $w$。**指標 $j$ 只進不退，所以總推入次數是 $n$，攤還 $O(\\log n)$。**\n\n### 定理 4（632 最小區間：枚舉「區間最小值」是完備的）\n\n從 $k$ 條升冪串列中各取一個數，使這 $k$ 個數的極差最小（等價：找最小區間 $[a,b]$ 使每條串列至少有一個數落在其中）。\n\n*引理*：最優區間 $[a, b]$ 可以取 $a$ 為某個實際存在的元素（把 $a$ 往上推到區間內的最小元素，$b$ 不變，仍覆蓋全部串列且不變長）。同理 $b$ 也可取實際元素。\n\n*演算法*：堆中恆為「每條串列的當前指標元素」（不變量 (F)），額外維護 `curMax`。當前候選區間是 $[\\texttt{pq.top()}, \\texttt{curMax}]$——**它必定覆蓋所有串列各一個元素**（由 (F)）。記錄後，彈出最小者、把它所屬串列的指標右移一格並推入、更新 `curMax`。\n\n*完備性證明*：需要證明「每個可能成為最優解的區間都會被枚舉到」。設最優區間為 $[a^*, b^*]$，$a^*$ 是串列 $i$ 的第 $t$ 個元素。考慮演算法中「串列 $i$ 的指標剛好指到第 $t$ 個元素、且 $a^*$ 是堆中最小值」的那一刻——這一刻必然存在，因為每條串列的指標都單調右移，且 $a^*$ 在成為堆中最小值之前不會被彈出。此刻堆中每條串列的元素都 $\\ge a^*$（$a^*$ 是最小），而且各自是「$\\ge a^*$ 的最小者」（指標從未跳過任何元素）。故 $\\texttt{curMax} \\le b^*$，記錄到的區間不比 $[a^*, b^*]$ 長。$\\square$\n\n**「指標從不跳過元素，所以堆中的元素恆是各串列中 $\\ge$ 當前最小值的最小者」——這句話就是完備性的全部。**\n\n### 定理 5（1834／2402／857 的共同骨架：兩階段掃描）\n\n這一族的形狀是：**事件按時間（或某個門檻）升冪；到達時間 $t$ 時，把所有「已可用」的候選倒進堆；再從堆中取最優者處理。**\n\n*不變量*：任一時刻，堆中恰好是「已經可用、但尚未被處理」的候選集合。\n\n*為什麼指標不會回退*：**候選的「可用時間」與掃描的時間都單調不減**，所以推入是單向的，總推入次數 $= n$。這保證了整體 $O(n\\log n)$ 而不是 $O(n^2\\log n)$。\n\n*1834 的關鍵細節*：若堆為空而任務尚未到達，**時間要直接跳到下一個任務的到達時間**（`t = max(t, tasks[j].enqueueTime)`），不能一秒一秒走——否則會 TLE 或死迴圈。\n\n**這個骨架與 5.2 的「排序 + 惰性推進」以及 12.x 的離線掃描是同一個東西。** 認出它之後，這一族題的差異只剩下「堆的比較器怎麼寫」。\n\n## 逐步演算法\n\n:::example 狀態表示：{值, 來源序列號, 該序列內下標}\n**1. 狀態表示：堆元素帶足「往下走」的資訊。** K 路合併要記「值 + 來自哪個來源 / 下一步在哪」；雙堆要分清楚哪邊裝大、哪邊裝小。\n\n```cpp\n// 狀態表示：{值, 來源序列號, 該序列內下標}\npriority_queue<tuple<int, int, int>, vector<tuple<int, int, int>>,\n                              greater<>>\n        pq;  // 例：最小區間需要知道來源\n```\n:::\n\n\n\n:::example 更新：取最小，補同來源的下一個元素\n**2. 更新流程：取出最優後，補進它的後繼。** 這是進階題的精髓——取一個、補一個，讓堆持續代表「各路最前線」。\n\n```cpp\n// 更新：取最小，補同來源的下一個元素\nauto [v, seq, idx] = pq.top();\npq.pop();\nif (idx + 1 < nums[seq].size()) {\n    pq.push({nums[seq][idx + 1], seq, idx + 1});\n}\n```\n:::\n\n\n\n:::example 答案讀取：雙堆中位數\n**3. 答案讀取：從堆頂或兩堆頂組合答案。** K 路合併逐步接出序列；雙堆由堆頂直接算中位數；「最小區間」由「當前堆內最大與最小」之差更新答案。\n\n```cpp\n// 答案讀取：雙堆中位數\ndouble median = lo.size() > hi.size() ? lo.top() : (lo.top() + hi.top()) / 2.0;\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 補入後繼前務必判界，否則越界存取。\n- 雙堆要時時「再平衡」，把多的一邊堆頂移到另一邊，維持大小差 ≤ 1。\n- 複雜度：K 路合併 `O(N log k)`；雙堆每次插入 `O(log n)`、取中位數 `O(1)`。\n\n## C++17 模板\n\n以下用 1851. 包含每個查詢的最小區間 展示堆（優先佇列）的進階離線處理的核心寫法。\n\n**完整題目**：給若干區間 `intervals[i] = [l, r]` 與多個查詢 `queries[j]`。對每個查詢，求所有包含該查詢點的區間中，長度（`r - l + 1`）最小者；若無則回傳 -1。\n\n:::example 離線掃描 + 最小堆：將 queries 排序後逐一處理，堆存「尚未過期」的區間\n**線索**：若把查詢「離線」由小到大排序，左端點 `<=` 查詢的區間可以逐步加入，且不必移除；只要在取答案前，用懶刪除把右端點已小於查詢點的區間從堆頂清掉，剩下的堆頂就是最小長度。\n\n```cpp\nclass Solution {\npublic:\n    // 離線掃描 + 最小堆：將 queries 排序後逐一處理，堆存「尚未過期」的區間\n    vector<int> minInterval(vector<vector<int>>& intervals,\n                                                                                                    vector<int>& queries) {\n            sort(intervals.begin(), intervals.end());  // 依左端點排序，方便按序加入\n            int m = queries.size();\n            vector<int> order(m);\n            iota(order.begin(), order.end(), 0);\n            // 對查詢下標按查詢值排序，實現離線處理\n            sort(order.begin(), order.end(),\n                                [&](int a, int b) { return queries[a] < queries[b]; });\n            priority_queue<pair<int, int>, vector<pair<int, int>>, greater<>>\n                            pq;  // (區間長度, 右端點)\n            vector<int> ans(m, -1);\n            int i = 0, n = intervals.size();\n            for (int j : order) {\n                    int q = queries[j];\n                    // 將所有左端點 <= q 的區間加入堆\n                    while (i < n && intervals[i][0] <= q) {\n                            int len = intervals[i][1] - intervals[i][0] + 1;\n                            pq.push({len, intervals[i][1]});\n                            ++i;\n                    }\n                    // 移除右端點 < q 的過期區間（不包含 q）\n                    while (!pq.empty() && pq.top().second < q) {\n                            pq.pop();\n                    }\n                    if (!pq.empty()) {\n                            ans[j] = pq.top().first;  // 堆頂即為包含 q 的最小區間長度\n                    }\n            }\n            return ans;\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- 區間依左端點排序、查詢依值排序，掃描時只需單向前進。\n- 左端點 `<=` 當前查詢的區間，全部以 `(長度, 右端點)` 推入最小堆。\n- 取答案前，把右端點 `< q`（已不含 q）的堆頂懶刪除掉。\n- 清理後堆頂的長度，就是包含 q 的最小區間長度。\n\n## 時間與空間複雜度\n\n| 題目 | 時間 | 空間 | 說明 |\n| --- | --- | --- | --- |\n| 23 合併 K 個升序連結串列 | $O(n\\log k)$ | $O(k)$ | $n$ 為總節點數。分治兩兩合併也是 $O(n\\log k)$、空間 $O(\\log k)$ |\n| 355 設計推特 | `postTweet` $O(1)$；`getNewsFeed` $O(f + 10\\log f)$ | $O(\\text{推文} + \\text{關係})$ | 只取前 10 則就停 |\n| 407 接雨水 II | $O(mn\\log(mn))$ | $O(mn)$ | 每格入堆出堆各一次 |\n| 502 IPO | $O(n\\log n)$ | $O(n)$ | 排序主導 |\n| 632 最小區間 | $O(N\\log k)$ | $O(k)$ | $N$ 為所有元素總數 |\n| 778 / 1631 | $O(mn\\log(mn))$ | $O(mn)$ | 亦可用並查集 $O(mn\\,\\alpha)$ + 排序 |\n\n**23 的兩種解法值得對比**：\n| 解法 | 時間 | 空間 | 備註 |\n| --- | --- | --- | --- |\n| 小頂堆 | $O(n\\log k)$ | $O(k)$ | 線上、可提前停止 |\n| 分治兩兩合併 | $O(n\\log k)$ | $O(\\log k)$ 遞迴 | 常數更小、無需堆 |\n| 逐條合併 | $O(nk)$ | $O(1)$ | $k$ 大時退化 |\n**同樣是 $O(n\\log k)$，分治的常數更小**（只有指標比較，沒有堆的上濾下濾）。**但只有堆的版本能「取到第 $m$ 個就停」**，355 需要這個性質。\n\n**407 的空間**：`vis` 用 `vector<vector<char>>` 而非 `vector<vector<bool>>`——後者是位元壓縮的特化，存取慢且不能取址。$m, n \\le 200$ 時差別不大，但這是值得養成的習慣。\n\n## 常見錯誤與邊界條件\n\n- 結果要寫回查詢的原始下標，記得用 `order` 還原順序。\n- 懶刪除只在「讀堆頂前」做，過期區間留在堆中不影響正確性。\n- 區間排序鍵（左端點）與堆排序鍵（長度）是兩回事，別混用。\n\n## 常見變形\n\n- 離線掃描線 + 堆，是處理「對每個查詢求極值」的通用套路。\n- 把長度換成其他權重（成本、價值），即可求對應的最小 / 最大。\n- 若需線上回答，可改用區間樹或合併排序樹。\n\n## 與相似技巧的比較\n\n- **與 6.1 基礎堆**：差別在**堆外面包了什麼框架**。基礎題是「堆即演算法」，進階題是「堆是子程序」。**寫進階題時，先把框架的正確性證出來，再想堆裡放什麼。**\n- **與 6.3 第 K 小**：6.3 的 373／378 也是 K 路合流，但**候選是「按需生成」的**（彈出一個才產生它的後繼），需要額外處理重複生成。本節的 23／632 候選來源是現成的串列，較單純。\n- **與 6.5 反悔堆**：502 的貪心**不需要反悔**（定理 3：可行集單調 + 獲利非負）。**一旦「做了某個選擇會讓未來變差」，就必須升級成反悔堆。** 這是 6.2 與 6.5 的分界線。\n- **與 6.6 懶刪除堆**：2402（會議室 III）需要「把用完的會議室放回可用池」，這是**推入**不是刪除，所以不需要懶刪除。**只有當「堆中某個元素因外部原因作廢」時才需要 6.6。**\n- **與 Dijkstra**：407／778／1631 就是 Dijkstra，只是代價運算子從 $+$ 換成 $\\max$。**Dijkstra 的正確性只要求代價運算子滿足「單調性」（$c \\le c \\oplus w$）與「結合性」，$\\max$ 兩者皆滿足**——這也是為什麼同一份程式碼能跑兩種問題。\n- **與並查集（8.x）**：1631／778 的另一解是「邊按權重升冪加入並查集，直到起終點連通」。**$O(E\\,\\alpha(V))$ 比 Dijkstra 快，但必須離線（要先拿到所有邊）。** 線上查詢就只能用堆。\n- **與 3.1.2 差分事件掃描**：253 那類「只問重疊數」的題用差分是 $O(n)$，比堆快。**堆的優勢在於它能回答「是哪一個」而不只是「有幾個」**（2402 要知道具體是哪間會議室）。\n\n## 代表例題\n\n**題號**：23、355、407、502、632\n\n請套用「堆（優先佇列）：進階」的狀態、不變式與實作方式，先寫出資料表示、更新流程與答案讀取規則。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 23 | [合併 K 個升序連結串列](https://leetcode.cn/problems/merge-k-sorted-lists) |  |\n| 355 | [設計推特](https://leetcode.cn/problems/design-twitter) |  |\n| 407 | [接雨水 II](https://leetcode.cn/problems/trapping-rain-water-ii) |  |\n| 502 | [IPO](https://leetcode.cn/problems/ipo) |  |\n| 632 | [最小區間](https://leetcode.cn/problems/smallest-range-covering-elements-from-k-lists) |  |\n| 778 | [水位上升的泳池中游泳](https://leetcode.cn/problems/swim-in-rising-water) |  |\n| 1500 | [設計檔案分享系統](https://leetcode.cn/problems/design-a-file-sharing-system) |  |\n| 3506 | [查詢消除細菌菌株所需時間](https://leetcode.cn/problems/find-time-required-to-eliminate-bacterial-strains) |  |\n| LCP 33 | [蓄水](https://leetcode.cn/problems/o8SXZn) |  |\n| 2931 | [購買物品的最大開銷](https://leetcode.cn/problems/maximum-spending-after-buying-items) | 1822 |\n| 3781 | [二進位制交換後的最大分數](https://leetcode.cn/problems/maximum-score-after-binary-swaps) | 1823 |\n| 1705 | [喫蘋果的最大數目](https://leetcode.cn/problems/maximum-number-of-eaten-apples) | 1930 |\n| 1631 | [最小體力消耗路徑](https://leetcode.cn/problems/path-with-minimum-effort) | 1948 |\n| 1882 | [使用伺服器處理任務](https://leetcode.cn/problems/process-tasks-using-servers) | 1979 |\n| 1354 | [多次求和構造目標陣列](https://leetcode.cn/problems/construct-target-array-with-multiple-sums) | 2015 |\n| 1353 | [最多可以參加的會議數目](https://leetcode.cn/problems/maximum-number-of-events-that-can-be-attended) | 2016 |\n| 1235 | [規劃兼職工作](https://leetcode.cn/problems/maximum-profit-in-job-scheduling) | 2023 |\n| 2542 | [最大子序列的分數](https://leetcode.cn/problems/maximum-subsequence-score) | 2056 |\n| 1383 | [最大的團隊表現值](https://leetcode.cn/problems/maximum-performance-of-a-team) | 2091 |\n| 2402 | [會議室 III](https://leetcode.cn/problems/meeting-rooms-iii) | 2093 |\n| 2503 | [矩陣查詢可獲得的最大分數](https://leetcode.cn/problems/maximum-number-of-points-from-grid-queries) | 2196 |\n| 2163 | [刪除元素後和的最小差值](https://leetcode.cn/problems/minimum-difference-in-sums-after-removal-of-elements) | 2225 |\n| 1199 | [建造街區的最短時間](https://leetcode.cn/problems/minimum-time-to-build-blocks) | 2250 |\n| 857 | [僱傭 K 名工人的最低成本](https://leetcode.cn/problems/minimum-cost-to-hire-k-workers) | 2260 |\n| 1606 | [找到處理最多請求的伺服器](https://leetcode.cn/problems/find-servers-that-handled-most-number-of-requests) | 2276 |\n| 1851 | [包含每個查詢的最小區間](https://leetcode.cn/problems/minimum-interval-to-include-each-query) | 2286 |\n| 2940 | [找到 Alice 和 Bob 可以相遇的建築](https://leetcode.cn/problems/find-building-where-alice-and-bob-can-meet) | 2327 |\n| 3399 | [字元相同的最短子字串 II](https://leetcode.cn/problems/smallest-substring-with-identical-characters-ii) | 2376 |\n| 2589 | [完成所有任務的最少時間](https://leetcode.cn/problems/minimum-time-to-complete-all-tasks) | 2381 |\n| 3266 | [K 次乘運算後的最終陣列 II](https://leetcode.cn/problems/final-array-state-after-k-multiplication-operations-ii) | 2509 |\n| 1675 | [陣列的最小偏移量](https://leetcode.cn/problems/minimize-deviation-in-array) | 2533 |\n| 2617 | [網格圖中最少訪問的格子數](https://leetcode.cn/problems/minimum-number-of-visited-cells-in-a-grid) | 2582 |\n| 2532 | [過橋的時間](https://leetcode.cn/problems/time-to-cross-a-bridge) | 2589 |\n\n## 本節重點速查\n\n- 進階堆題 = **框架 + 堆**。**先證框架，再決定堆裡放什麼**；堆本身從來不是難點。\n- **K 路合流（定理 1）**：不變量是「堆中恰有每條未耗盡串列的當前頭」，彈出序列全域有序；**前提是每條來源自身有序**，這也是空間 $O(k)$ 而非 $O(n)$ 的理由。**只要前 $m$ 個就在第 $m$ 次彈出後停**（355）。\n- **407 / 778 / 1631 是同一題：最小瓶頸路。** 積水量 $=\\max(0, B'(u) - h_u)$，`level` 單調不減即 Dijkstra 不變量；**代價運算子從 $+$ 換成 $\\max$，Dijkstra 的證明照樣成立。**\n- 一維的 42 不需要堆，因為只有兩條路徑；二維路徑指數多，才需要。\n- **502 純貪心成立的兩個前提**：獲利非負（可行集單調）、專案不重做。**破了就要升級成 6.5 反悔堆。**\n- **632**：堆看不到自己的最大值，`curMax` 要自己維護；完備性來自「指標從不跳過元素」。\n- **掃描 + 候選池骨架（定理 5）**：排序陣列決定「何時有資格進場」，堆決定「進場後誰最優」；**兩個指標都單調不退**，總推入 $n$ 次。1834 記得把時間直接跳到下一個到達時間。\n- 23 的分治解與堆解同為 $O(n\\log k)$，**分治常數更小、空間更省，但不能提前停止。**\n- `vis` 在**入堆時**標記，不是出堆時。\n",
        },
        {
          id: 43,
          title: "6.3 第 K 小/大",
          summary:
            "## 這個技術解決什麼問題\n\n題目只要**某一個排名的值**（第 $K$ 小 / 第 $K$ 大 / 前 $K$ 個），而候選總數遠大於 $K$——甚至是指數級（2386 的所有子序列和有 $2^n$ 個）。**全部生成再排序既慢又放不下。**\n\n兩條主線：\n\n- **按需生成 + 堆**：候選之間有「下一個」的關係，彈出一個就推入它的後繼。適用於 23、264（醜數 II）、373（查詢和最小的 K 對數字）、378（有序矩陣中第 K 小）、1439、2386。複雜度 $O(K\\log K)$ 級，**與候選總數無關**。\n- **對「值」二分 + 計數**：若能快速算出「$\\le x$ 的候選有幾個」，就對答案二分。適用於 378、786、1439、2386。複雜度 $O(n\\log C)$ 級，**與 $K$ 無關**。\n\n**選型判準**：**$K$ 小用堆，$K$ 大（接近候選總數）用二分。** 兩者在 378 上分別是 $O(K\\log n)$ 與 $O(n\\log C)$，$K$ 可到 $n^2$ 時二分完勝。\n\n**另有第三條路**：靜態陣列、單次查詢第 $K$ 小 ⇒ `nth_element`（期望 $O(n)$）。**別在該用 `nth_element` 的地方堆一個堆。**\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「用堆求第 K 小/大」想：\n\n- **要的不是全部排序，只是某個排名的值（第 K 小、第 K 大、Top-K）。** 全排序是浪費，堆只維護你需要的那一小撮。\n- **候選可以「按需生成」，且每個候選有明確的「下一個」。** 例如多條升序串列的頭、`nums1[i] + nums2[j]` 的和、矩陣每行的下一格——彈出一個就能推出接班的候選：\n\n:::example 嗅到味道：彈出最小後，總能指出「同源的下一個候選」\n```cpp\n// 嗅到味道：彈出最小後，總能指出「同源的下一個候選」\n// pq.push(nextCandidateOf(popped));\n```\n:::\n\n\n\n- **資料是多路有序、或能組合成有序鏈。** 像「查詢和最小的 K 對數字」「有序矩陣中第 K 小」，本質都是把多條遞增鏈用堆合流。\n- **若只問「第 K 個值」而非「前 K 個」，還可考慮對「值」二分 + 計數**（如 378、786），把 `O(n log K)` 再壓到 `O(n log(值域))`。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的堆是max-heap 還是 min-heap？top() 對應「第 K 小」還是「第 K 大」？\n2. 堆元素除了值，還需要哪些定位資訊才能找到「下一個候選」？\n3. 彈出一個後，我補進的「下一個」是什麼？要不要去重？\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n本節要解決的場景是：在一堆數字裡，我們不想全部排序，只想知道「第 K 小（或第 K 大）的那一個」，而且資料常常是動態產生的——例如一邊合併好幾條已排序的串列，一邊源源不斷生出新的候選。\n\n先想最笨的方法：把全部 n 個數丟進陣列排序，再取第 K 個，這要 `O(n log n)`，而且常常根本沒必要把全部都生出來。\n\n:::example 小根堆：top() 永遠是目前最小值\n關鍵工具是「堆（heap）」，也就是 C++ 的 `priority_queue`。堆能在 `O(log n)` 內取出並移除目前的極值，這正好契合「每次只要一個最優候選」的需求。\n\n```cpp\n// 小根堆：top() 永遠是目前最小值\npriority_queue<int, vector<int>, greater<int>> pq;\npq.push(5);\npq.push(2);\npq.push(8);\nint mn = pq.top();  // 2：目前最小值，O(1)\npq.pop();           // 移除最小值，之後 top() 變 5\n```\n:::\n\n\n\n第 K 小/大的核心想法是：**用堆當「邊界探照燈」，每次只展開「下一個可能最小」的候選，連續取 K 次。** 因為堆永遠把最小的放在頂端，只要彈出 K 次，第 K 次彈出的就是答案。重點是不用把所有候選都先算出來，而是「需要時才生成」。\n\n:::example 彈出最小候選，並把它的「下一個」補進堆\n以「合併 K 個升序串列」為例：每條串列都已排好序，所以每條的「目前最前面那一個」就是它的最小候選。把 K 個頭放進小根堆，每次彈出最小的那個，再把它在原串列的下一個補進堆——這樣堆裡永遠只有 K 個候選，卻能正確地依序吐出全域最小值。\n\n```cpp\n// 彈出最小候選，並把它的「下一個」補進堆\nauto [val, listId, idx] = pq.top();  // 目前全域最小\npq.pop();\nif (idx + 1 < lists[listId].size()) {  // 同一條串列還有下一個\n    pq.push({lists[listId][idx + 1], listId, idx + 1});\n}\n```\n:::\n\n\n\n這樣 n 個元素各進出堆一次，堆大小最多 K，總複雜度 `O(n log K)`，遠優於全排序。\n\n## 狀態／資料結構定義\n\n### (A) 前緣堆（frontier heap）\n\n把候選集合看成一個**偏序集**，堆中存的是「已彈出集合的邊界」。\n\n**373（查詢和最小的 K 對數字）**：候選是下標對 $(i,j)$，值 $= \\textit{nums1}[i] + \\textit{nums2}[j]$，兩陣列皆升冪。\n$$(i,j) \\preceq (i',j') \\iff i \\le i' \\ \\wedge\\ j \\le j' .$$\n**值沿 $\\preceq$ 單調不減**——這是全部推理的基礎。\n\n```cpp\npriority_queue<array<long long,3>, vector<array<long long,3>>, greater<>> pq;  // (sum, i, j)\npq.push({(long long)nums1[0] + nums2[0], 0, 0});\n// 彈出 (i,j) 時：一律 push (i, j+1)；只有 j == 0 時才 push (i+1, j)\n```\n\n**「$j = 0$ 才往下走」這條規則不是效率技巧，是去重機制**（定理 2）。另一種寫法是無條件推入兩個後繼並用 `unordered_set` 去重——**能用結構去重就別用雜湊表，省一個常數大的容器**。\n\n**378（有序矩陣中第 K 小）**：把每一列當成一條升冪串列，初始推入前 $\\min(k, n)$ 列的第 0 個元素，彈出 $(i,j)$ 後推入 $(i, j+1)$。**初始只需 $\\min(k,n)$ 列**——第 $k$ 小不可能來自第 $k+1$ 列以後（每列的首元素已經佔掉一個名額）。\n\n### (B) 二分 + 計數\n\n```cpp\nlong long lo = 最小可能值, hi = 最大可能值;\nwhile (lo < hi) {\n    long long mid = lo + (hi - lo) / 2;            // 注意負數時用 floor_div\n    if (count_le(mid) >= k) hi = mid; else lo = mid + 1;\n}\nreturn lo;\n```\n**狀態只有 `lo`／`hi`，難點全在 `count_le` 怎麼寫成 $O(n)$。**\n\n**378 的 `count_le(x)` 走階梯**：從左下角 $(n-1, 0)$ 出發，`matrix[r][c] <= x` 就 `cnt += r+1; ++c;`，否則 `--r`。**不變量：任一時刻，第 $c$ 欄以左、第 $r$ 列以下的元素都已計入，且沒有重複計入。** 每步 $r$ 減或 $c$ 增，總步數 $\\le 2n$。\n\n## 不變量或正確性證明\n\n### 定理 1（前緣堆的主不變量）\n\n令 $P$ 為候選的偏序集，值函數 $f$ 沿 $\\preceq$ 單調不減。令 $D$ 為已彈出的元素集合。\n\n*不變量 (I)*：\n1. **$D$ 是一個下閉集（down-set）**：若 $y \\in D$ 且 $x \\preceq y$，則 $x \\in D$。\n2. **$P \\setminus D$ 的每個極小元都在堆中**，且堆 $\\subseteq P\\setminus D$。\n\n*定理*：在 (I) 之下，**第 $t$ 次彈出的元素就是 $f$ 的第 $t$ 小值**（重複值按任意固定順序）。\n\n*證明*。只需證每次彈出的都是 $P\\setminus D$ 中 $f$ 最小者。\n- $\\min_{x \\in P\\setminus D} f(x)$ 必在某個**極小元**上取到：設 $x$ 達到最小值，若 $x$ 不極小，存在 $x' \\prec x$ 且 $x' \\in P \\setminus D$（由 (I).1，$D$ 下閉，$x \\notin D$ 不保證 $x' \\notin D$⋯ 反過來：若 $x' \\in D$ 也無妨，我們沿 $\\prec$ 往下走，最終會停在一個 $P\\setminus D$ 的極小元 $x''$，且由單調性 $f(x'') \\le f(x)$）。\n- 由 (I).2，該極小元在堆中；由堆 $\\subseteq P\\setminus D$，堆頂 $\\ge \\min_{P\\setminus D} f$。兩者夾擠得堆頂 $= \\min_{P\\setminus D} f$。$\\square$\n\n**這個證明點名了唯一的前提：$f$ 沿偏序單調不減。** 373 靠「兩陣列皆升冪」，378 靠「每列升冪」，2386 靠「取絕對值後升冪排序」。**任何一題若值不單調，前緣堆立刻失效。**\n\n### 定理 2（373 的去重規則：每個候選恰生成一次）\n\n推入規則：初始 $(0,0)$；彈出 $(i,j)$ 時推入 $(i, j+1)$（若合法），並在 $j = 0$ 時推入 $(i+1, 0)$（若合法）。\n\n*宣稱*：每個 $(i,j)$ 恰被推入一次。\n*證明*：\n- $(0,0)$：只在初始化時推入。\n- $(i, j)$ 且 $j > 0$：只可能由 $(i, j-1)$ 的第一條規則產生（第二條規則只產生 $j=0$ 的元素）。而 $(i,j-1)$ 只被彈出一次。\n- $(i, 0)$ 且 $i > 0$：只可能由 $(i-1, 0)$ 的第二條規則產生。$\\square$\n\n*宣稱（覆蓋性，即 (I).2）*：若 $(i,j)$ 是 $P\\setminus D$ 的極小元，則它在堆中。\n*證明*：極小表示它的所有直接前驅（$(i-1,j)$、$(i,j-1)$，存在者）都在 $D$ 中。\n- $j > 0$：$(i, j-1) \\in D$ 已被彈出 ⇒ 已推入 $(i,j)$ ✔。\n- $j = 0,\\ i > 0$：$(i-1, 0) \\in D$ ⇒ 已推入 $(i, 0)$ ✔。\n- $(0,0)$：初始已推入 ✔。$\\square$\n\n**堆的大小上界**：任一時刻堆中至多 $O(\\min(K, n))$ 個元素（每彈出一個最多推入兩個，但推入 $(i+1,0)$ 只在 $j=0$ 時發生，故第一欄最多貢獻 $n$ 個）。**所以複雜度是 $O(K\\log \\min(K,n))$，不是 $O(K \\log K)$。**\n\n### 定理 3（二分答案：單調性與「答案必為實際元素」）\n\n令 $C(x) = |\\{\\text{候選} \\le x\\}|$。\n\n*引理 1（單調）*：$C$ 對 $x$ 單調不減。故 $\\{x : C(x) \\ge k\\}$ 是一個上閉的射線，**可以二分**。\n\n*引理 2*：令 $x^{*} = \\min\\{x \\in \\mathbb{Z} : C(x) \\ge k\\}$，則 **$x^{*}$ 一定是某個實際存在的候選值**，且就是第 $k$ 小。\n*證明*：若 $x^{*}$ 不是任何候選的值，則沒有候選落在 $(x^{*}-1, x^{*}]$，故 $C(x^{*}-1) = C(x^{*}) \\ge k$，與 $x^{*}$ 的最小性矛盾。$\\square$\n\n**引理 2 用到的前提是「候選取值為整數」**（或更一般地：在一個可枚舉的離散集合上）。**若候選是實數（例如 786 的分數），就不能對整數二分**——要嘛對分數的實數值做固定輪數的浮點二分（有精度風險），要嘛改用「對分數 $p/q$ 二分」的 Stern–Brocot 式做法，或直接用前緣堆。**786 的推薦寫法是二分 + 雙指標計數，同時記錄達到計數上界的那一對分數本身**，這樣回傳的一定是實際存在的分數，避開精度問題。\n\n*引理 3（786 的計數）*：對固定 $x$，計算 $|\\{(i,j) : i<j,\\ p_i/p_j \\le x\\}|$。因 $p$ 升冪，對每個 $j$，滿足 $p_i \\le x\\, p_j$ 的 $i$ 構成前綴，且該前綴長度隨 $j$ **單調不減**（$x p_j$ 遞增）⇒ 雙指標一次掃描 $O(n)$。**「單調不減」正是雙指標成立的條件，必須明確驗證。**\n\n### 定理 4（2386 第 K 大子序列和：化歸為「非負子集和的第 K 小」）\n\n令 $S = \\sum_{a_i > 0} a_i$（所有正數之和，也就是最大子序列和）。\n\n*引理（雙射）*：對任意子集 $A \\subseteq [n]$，\n$$\\mathrm{sum}(A) \\;=\\; S \\;-\\; \\sum_{i \\in B} |a_i|, \\qquad B \\;=\\; \\{i : a_i > 0,\\ i \\notin A\\} \\;\\cup\\; \\{i : a_i < 0,\\ i \\in A\\} .$$\n*證明*：$\\mathrm{sum}(A) = \\sum_{i\\in A, a_i>0} a_i + \\sum_{i \\in A, a_i<0} a_i = \\left(S - \\sum_{i\\notin A, a_i>0} a_i\\right) - \\sum_{i\\in A, a_i<0}|a_i|$。$\\square$\n\n*映射 $A \\mapsto B$ 是雙射*（對每個下標，$A$ 是否含它與 $B$ 是否含它一一對應：正數取反、負數同步）。\n\n*推論*：**第 $k$ 大的 $\\mathrm{sum}(A)$ $= S - $（第 $k$ 小的 $\\sum_{i\\in B}|a_i|$，取遍所有 $2^n$ 個子集 $B$）。**\n\n於是問題變成：給定非負陣列 $b_i = |a_i|$，求所有子集和中第 $k$ 小者。**這個新問題的候選值域是非負的、且空集合的和 $0$ 是最小者**——結構好得多。\n\n*前緣堆生成*：把 $b$ 升冪排序。堆中存 $(\\text{和}, \\text{最後選用的下標})$。初始 $(b_0, 0)$；彈出 $(s, i)$ 後推入 $(s + b_{i+1},\\ i+1)$（加一個）與 $(s - b_i + b_{i+1},\\ i+1)$（換掉最後一個）。**這兩條規則恰好把「以 $i$ 結尾的子集」擴展成「以 $i+1$ 結尾的子集」，且每個非空子集只由它的「去掉最大下標」或「把最大下標往前換」唯一產生。** 彈出 $k-1$ 次即得（第 1 小是空集的 $0$，不在堆中，要另外計入）。\n\n**這一題把「指數級候選」壓成 $O(k\\log k)$，靠的完全是定理 1 的單調性 + 一個巧妙的雙射。** 沒有雙射，負數會破壞單調性，前緣堆用不了。\n\n### 定理 5（264 醜數 II：三指標的去重）\n\n$\\mathrm{dp}[0]=1$，$\\mathrm{dp}[i] = \\min(2\\,\\mathrm{dp}[p_2],\\ 3\\,\\mathrm{dp}[p_3],\\ 5\\,\\mathrm{dp}[p_5])$。\n\n*不變量*：$p_c$ 指向「下一個尚未被乘以 $c$ 的醜數」，故 $c \\cdot \\mathrm{dp}[p_c]$ 是「因子 $c$ 這條鏈上，尚未產生的最小醜數」。三條鏈的並集恰為全部醜數（每個醜數 $u > 1$ 至少有一個質因子 $\\in \\{2,3,5\\}$，故至少由一條鏈產生）。\n\n*去重*：因為一個醜數可能由多條鏈產生（例如 $6 = 2\\times 3 = 3 \\times 2$），**取到最小值後，所有達到該最小值的指標都要前進**：\n```cpp\nif (v == 2 * dp[p2]) ++p2;\nif (v == 3 * dp[p3]) ++p3;      // 是 if，不是 else if\nif (v == 5 * dp[p5]) ++p5;\n```\n**寫成 `else if` 就會產生重複的醜數**——這是本題唯一的坑，也是「多鏈合流必須處理重複」的最小示例。\n\n*與堆解的對比*：用 `priority_queue` + `unordered_set` 去重是 $O(n\\log n)$；三指標是 $O(n)$、無額外容器。**當「下一個候選」可以用固定數量的指標表示時，指標永遠優於堆。**\n\n## 逐步演算法\n\n:::example 堆元素：值 + 來源序列編號 + 在該序列的下標\n**1. 狀態表示：堆元素要帶足「定位資訊」。** 只放值還不夠，常要連同它「來自哪、下一個在哪」一起放，才能在彈出後補進正確的下一個候選。\n\n```cpp\n// 堆元素：值 + 來源序列編號 + 在該序列的下標\nstruct Node {\n    int val, listId, idx;\n};\nauto cmp = [](const Node& a, const Node& b) { return a.val > b.val; };\npriority_queue<Node, vector<Node>, decltype(cmp)> pq(cmp);\n```\n:::\n\n\n\n:::example 每條序列的開頭先入堆\n**2. 初始化：先放入每個來源的第一個候選。** 讓堆一開始就涵蓋所有「最小可能性」。\n\n```cpp\n// 每條序列的開頭先入堆\nfor (int i = 0; i < lists.size(); ++i) {\n    if (!lists[i].empty()) {\n        pq.push({lists[i][0], i, 0});\n    }\n}\n```\n:::\n\n\n\n:::example 取出第 K 小：彈 K 次，最後一次的值就是答案\n**3. 反覆彈出 + 補進下一個。** 每彈出一個最小值，就把它所在來源的下一個補進來；想求「第 K 小」就數到第 K 次彈出即可。\n\n```cpp\n// 取出第 K 小：彈 K 次，最後一次的值就是答案\nint kth(int k) {\n    int val = 0;\n    while (k--) {\n        auto [v, id, idx] = pq.top();\n        pq.pop();\n        val = v;\n        if (idx + 1 < lists[id].size()) {\n            pq.push({lists[id][idx + 1], id, idx + 1});\n        }\n    }\n    return val;\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 補進下一個前一定要檢查「還有沒有下一個」（`idx + 1` 沒越界），否則會讀到不存在的元素。\n- 若值可能重複（像醜數 II 用不同質因數會生出相同值），記得「去重」：彈出時若和上一個答案相同就跳過、不算一次。\n- 複雜度：堆大小若控制在 K，取前 K 個是 `O(K log K)` 到 `O(n log K)`；空間 `O(K)`。\n\n## C++17 模板\n\n以下用 1439. 有序矩陣中的第 k 個最小陣列和 展示用堆求第 K 小 / 大的核心寫法。\n\n**完整題目**：給一個每列都升序排列的矩陣 mat，從每一列各取一個數組成一個陣列，其和為該陣列和。求所有可能陣列和中，第 k 小的值。\n\n:::example 合併兩個已排序向量的前 k 小元素（利用最小堆實現 k 路歸並）\n**線索**：「從多個有序來源組合出第 K 小的和」可以逐列合併：把目前前 k 小的和當成一個有序序列，與下一列再用最小堆做「合併兩有序陣列取前 k 小和」，避免枚舉所有組合。\n\n```cpp\nclass Solution {\n    // 合併兩個已排序向量的前 k 小元素（利用最小堆實現 k 路歸並）\n    vector<int> mergeTwo(const vector<int>& a, const vector<int>& b, int k) {\n            priority_queue<tuple<int, int, int>, vector<tuple<int, int, int>>,\n                                                                        greater<>>\n                            pq;  // (和, i, j)\n            // 初始化：a 的前 k 個元素各搭配 b[0]\n            for (int i = 0; i < (int)a.size() && i < k; ++i) {\n                    pq.push({a[i] + b[0], i, 0});\n            }\n            vector<int> res;\n            while (!pq.empty() && (int)res.size() < k) {\n                    auto [s, i, j] = pq.top();\n                    pq.pop();\n                    res.push_back(s);\n                    // 擴展：固定 a[i]，嘗試 b 的下一個元素\n                    if (j + 1 < (int)b.size()) {\n                            pq.push({a[i] + b[j + 1], i, j + 1});\n                    }\n            }\n            return res;\n    }\n\npublic:\n    // 逐列歸並：將每一列的元素與當前前 k 小的累積結果合併\n    int kthSmallest(vector<vector<int>>& mat, int k) {\n            vector<int> cur = {0};  // 初始「空行」的和為 0\n            for (auto& row : mat) {\n                    cur = mergeTwo(cur, row, k);\n            }\n            return cur[k - 1];  // 最終前 k 小中的第 k 個即為答案\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- `cur` 永遠保存「目前已合併各列、前 k 小的和」。\n- `mergeTwo` 把 `cur` 與下一列視為兩個有序陣列，用最小堆取兩兩相加的前 k 小。\n- 堆元素 `(a[i] + b[j], i, j)`，每次彈出最小，再推進同一條鏈的下一個 `j`。\n- 逐列合併後，`cur[k - 1]` 即第 k 小的陣列和。\n\n## 時間與空間複雜度\n\n| 題目 | 前緣堆 | 二分 + 計數 | 其他 |\n| --- | --- | --- | --- |\n| 373 K 對數字 | $O(K\\log\\min(K,n))$，空間 $O(\\min(K,n))$ | — | |\n| 378 矩陣第 K 小 | $O(K\\log n)$，空間 $O(n)$ | $O(n\\log\\frac{\\max-\\min}{1})$，空間 $O(1)$ | **$K$ 可達 $n^2$，二分通常更好** |\n| 786 第 K 小質數分數 | $O(K\\log n)$ | $O(n\\log C)$ 雙指標計數 | |\n| 1439 第 K 小陣列和 | $O(mK\\log K)$（逐列合併，每列只留前 $K$ 小） | $O(mn\\log C)$ | |\n| 2386 第 K 大子序列和 | $O(n\\log n + k\\log k)$ | $O(n\\log n + n\\log C)$ | 候選總數 $2^n$ |\n| 264 醜數 II | $O(n\\log n)$（堆） | — | **三指標 $O(n)$、空間 $O(n)$** |\n\n**378 的兩解量化對比**（$n = 300$，$K$ 最大 $= 90000$）：\n- 前緣堆：$90000 \\times \\log 300 \\approx 7\\times 10^5$ 次堆操作。\n- 二分：$\\log(2\\times10^9) \\approx 31$ 輪 $\\times\\ 600$ 步 $\\approx 1.9 \\times 10^4$ 步，**且空間 $O(1)$**。\n**二分快一個數量級以上。$K$ 大時務必選二分。**\n\n**1439 的「逐列合併只留前 K」為什麼正確**：最終的第 $K$ 小和，其在前 $t$ 列上的部分和必定也在「前 $t$ 列的前 $K$ 小和」之中（否則已有 $K$ 個更小的部分和，補上後面各列的最小值後仍全部更小，矛盾）。**這是「前綴最優性」的一個實例，也是所有「只留前 K」剪枝的共同理由。**\n\n## 常見錯誤與邊界條件\n\n- 每步都把結果截斷到前 k 個，否則狀態會指數爆炸。\n- 堆裡存下標而非展開所有和，才是節省空間的關鍵。\n- 起始 `cur = {0}` 代表空組合，讓第一列也走同一套合併邏輯。\n\n## 常見變形\n\n- 查詢和最小的 K 對數字（373）：兩有序陣列版本的同一手法。\n- 醜數 II / 超級醜數：用堆按倍數展開取第 k 小。\n- 第 K 小的質數分數：把候選視為有序鏈，用堆或二分逼近。\n\n## 與相似技巧的比較\n\n- **與 6.1 的「大小為 K 的堆」**：那是**已知全部候選、逐個掃過去**（$O(n\\log K)$）；本節是**候選按需生成、總數可能無限或指數級**。**候選數 $n$ 有限且能全掃 ⇒ 6.1；不能全掃 ⇒ 本節。**\n- **與 `nth_element`**：靜態陣列、單次查詢 ⇒ `nth_element` 期望 $O(n)$，比任何堆解都快。**但它會重排陣列、最壞 $O(n^2)$、且不能用於「按需生成」的候選。**\n- **與 6.2 的 K 路合流**：定理 1 的前緣堆是 K 路合流的推廣——**K 路合流是「偏序 = $k$ 條互不相交的鏈」的特例**。認識到這一點，373／378 就只是換了一種偏序。\n- **與二分答案（一般）**：本節的二分是「對答案的值二分」，成敗全在 `count_le` 能否做到 $O(n)$。**若 `count_le` 只能 $O(n\\log n)$，總複雜度 $O(n\\log n\\log C)$ 可能就不如堆了。**\n- **與 5.4 雙指標**：786 的計數靠雙指標，前提是「前綴長度隨 $j$ 單調不減」。**幾乎每個 $O(n)$ 的 `count_le` 背後都是一個單調性論證**，寫之前先把它說清楚。\n- **與 DP**：1439 的逐列合併其實是 DP（狀態 = 前 $t$ 列的前 $K$ 小和集合）。**「只保留前 $K$ 個狀態」是有界 DP 的通用剪枝**，正確性靠前綴最優性。\n\n## 代表例題\n\n**題號**：23、264、373、378、786\n\n請套用「第 K 小/大」的狀態、不變式與實作方式，先寫出資料表示、更新流程與答案讀取規則。\n\n- 373 查詢和最小的 K 對數字：把每個 `nums1[i]` 搭配 `nums2` 的和視為一條遞增鏈，用小根堆只展開當前最小的下一對；適合對照「產生前 K 個」與「只求第 K 個值」的差別。\n- 378 有序矩陣中第 K 小的元素：矩陣行列有序時，不必攤平成 `n^2` 個數；對值 `x` 從左下或右上計算 `<= x` 的數量，二分第一個計數達到 `k` 的值。\n- 786 第 K 個最小的質數分數：分數有序但候選是有理數；可二分值並用雙指標計數，同時記錄不超過門檻的最大分數作答案候選。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 23 | [合併 K 個升序連結串列](https://leetcode.cn/problems/merge-k-sorted-lists) |  |\n| 264 | [醜數 II](https://leetcode.cn/problems/ugly-number-ii) |  |\n| 373 | [查詢和最小的 K 對數字](https://leetcode.cn/problems/find-k-pairs-with-smallest-sums) |  |\n| 378 | [有序矩陣中第 K 小的元素](https://leetcode.cn/problems/kth-smallest-element-in-a-sorted-matrix) |  |\n| 786 | [第 K 個最小的質數分數](https://leetcode.cn/problems/k-th-smallest-prime-fraction) |  |\n| 1439 | [有序矩陣中的第 k 個最小陣列和](https://leetcode.cn/problems/find-the-kth-smallest-sum-of-a-matrix-with-sorted-rows) | 2134 |\n| 2386 | [找出陣列的第 K 大和](https://leetcode.cn/problems/find-the-k-sum-of-an-array) | 2648 |\n| 3691 | [最大子陣列總值 II](https://leetcode.cn/problems/maximum-total-subarray-value-ii) | 2469 |\n\n## 本節重點速查\n\n- **$K$ 小用堆（$O(K\\log\\cdot)$），$K$ 大用對值二分（$O(n\\log C)$）。** 378 在 $K = n^2$ 時二分快一個數量級以上，且空間 $O(1)$。\n- **前緣堆的唯一前提：值沿偏序單調不減**（定理 1）。已彈出集合恆為下閉集，補集的極小元恆在堆中，故堆頂即下一個最小值。\n- **373 的去重靠結構而非雜湊表**：一律推 $(i, j+1)$，只有 $j=0$ 時才推 $(i+1, 0)$——每個候選恰生成一次（定理 2）。\n- **264 的三指標必須用 `if` 不是 `else if`**，否則產生重複醜數。**能用固定數量的指標表示「下一個候選」時，指標永遠優於堆。**\n- **二分的答案一定是實際存在的候選值**（定理 3 引理 2），前提是候選取整數；實數（786）要改成「記錄達標時的那一對」以避開精度。\n- **每個 $O(n)$ 的 `count_le` 背後都有一個單調性論證**（378 的階梯走法、786 的雙指標），寫之前先講清楚。\n- **2386**：用雙射 $\\mathrm{sum}(A) = S - \\sum_{B}|a_i|$ 把「第 $K$ 大子序列和」化成「非負值的第 $K$ 小子集和」，$2^n$ 個候選壓到 $O(k\\log k)$。**沒有這個雙射，負數會破壞單調性。**\n- **「只留前 $K$ 個」的剪枝**（1439）靠前綴最優性：全域第 $K$ 小的前綴部分必在前綴的前 $K$ 小之中。\n- 靜態、單次、只要一個排名 ⇒ `nth_element`，別堆一個堆。\n",
        },
        {
          id: 44,
          title: "6.4 重排元素",
          summary:
            "## 這個技術解決什麼問題\n\n一堆帶重複次數的元素要排成一列，限制是「**相同的元素不能靠太近**」：\n\n- **相鄰不同**（間隔 $\\ge 2$）：767（重構字串）、1054（距離相等的條形碼）、984（字串的另一種表示）。\n- **間隔 $\\ge k$**：358（K 距離重排字串）、1405（最長快樂字串，額外限制「同一字元不能連續三次」）。\n- **有冷卻時間、允許閒置，問最短總長**：621（任務排程器）。\n\n**共同骨架**：*每一步都選「剩餘次數最多、且目前不在冷卻中」的元素。* 直覺是「先處理最難安排的」——次數最多的元素最容易在末尾擠成一團，必須優先攤開。\n\n**兩個要分清楚的問法**：\n- **「能不能排出來」**（767、358）：答案是充要條件 + 構造，見定理 1、4。\n- **「最短要多長」**（621）：答案是一個閉式公式，見定理 3。**這題其實不需要堆**，用計數陣列直接套公式即可。\n\n**暴力是枚舉全排列（$n!$）**；貪心 + 堆是 $O(n\\log \\Sigma)$，$\\Sigma$ 為字元種類數。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「用堆貪心重排」想：\n\n- **要把帶數量的元素排成一列，且「相同的不能太近」**（相鄰不同、間隔 K、冷卻時間）。這正是 767 重構字串、621 任務排程器、358 K 距離重排的共同骨架。\n- **每一步的最佳選擇是「剩餘最多的那個」**——需要反覆查極值，普通陣列每次掃一遍太慢，堆把它壓成 `O(log m)`：\n\n:::example 嗅到味道：每一步都要「目前剩餘最多」的元素\n```cpp\n// 嗅到味道：每一步都要「目前剩餘最多」的元素\n// auto cur = pq.top();   // max-heap 一拿就到，不必每次重掃\n```\n:::\n\n\n\n- **題目會問「能不能排出來」或「最少需要多長時間/多少間隔」。** 像 984、1054 也是同一族：限制相鄰或等距，用堆挑最多者再隔開。\n- **暴力做法是窮舉所有排列檢查合法性（指數級）**，貪心 + 堆把它降成近線性。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的堆排序鍵是「剩餘次數」嗎？top() 拿到的是最急迫的元素嗎？\n2. 我用什麼緩衝（單變數 / 長度 K 的佇列）來強制隔開相同元素？\n3. 什麼情況代表「排不出來」？我怎麼從狀態判斷？\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n本節要解決的場景是：手上有一堆「帶數量」的元素（例如某字母出現 5 次、某任務要執行 3 次），要把它們重新排成一列，使得**相同的元素不能靠太近**——常見限制是「相鄰兩個不能相同」，或更嚴格的「相同元素至少間隔 K 格」。\n\n直覺上會卡住的點是：到底每一步該先放哪個？答案是一個很自然的貪心：**每次都先放「目前剩餘數量最多」的那個元素。** 因為數量最多的最難安排，越晚放越容易擠在一起、無處可放，所以要優先消化它。\n\n:::example max-heap：以「剩餘次數」排序，top() 是目前最多的字元\n要「每一步都拿到剩餘最多的」，正是堆的拿手好戲。用 max-heap 以「剩餘次數」為排序鍵：\n\n```cpp\n// max-heap：以「剩餘次數」排序，top() 是目前最多的字元\npriority_queue<pair<int, char>> pq;  // {剩餘次數, 字元}\npq.push({5, 'a'});\npq.push({2, 'b'});\nauto [cnt, ch] = pq.top();  // {5, 'a'}：目前最該先放的\n```\n:::\n\n\n\n:::example 取出最多的放一個，扣 1 後「先別放回堆」，暫存到 hold\n但有個陷阱：放了 'a' 之後，下一步不能又馬上放 'a'（會相鄰相同）。所以技巧是**「先把剛放過的扣一次、暫時擱在旁邊，等放完別的元素再放回堆」**。這樣就保證它不會連續出現。\n\n```cpp\n// 取出最多的放一個，扣 1 後「先別放回堆」，暫存到 hold\nauto [cnt, ch] = pq.top();\npq.pop();\nresult += ch;\nhold = {cnt - 1, ch};  // 等下一輪再考慮把它放回堆\n```\n:::\n\n\n\n下一輪放完「另一個」元素後，再把擱置的放回堆，它就能在隔了至少一格之後重新參選。這就是「重排元素」的全部精神：**用堆挑最急迫的，用一個暫存緩衝強制隔開相同元素。**\n\n若無解（某元素數量超過一半、無論怎麼排都會相鄰），最後會出現「堆空了但還有元素卡在暫存」，據此即可回傳失敗。\n\n## 狀態／資料結構定義\n\n**基本狀態**：`cnt[c]` = 元素 $c$ 的剩餘次數。\n\n**(A) 相鄰不同（間隔 2）**——大頂堆 + 一個「上一步用過的」暫存：\n```cpp\npriority_queue<pair<int,char>> pq;   // (剩餘次數, 字元)；預設大頂堆，first 優先\npair<int,char> prev = {0, 0};        // 上一步放的元素，冷卻一步後放回\nwhile (!pq.empty()) {\n    auto [c, ch] = pq.top(); pq.pop();\n    res += ch;\n    if (prev.first > 0) pq.push(prev);   // 上一步的元素現在解除冷卻\n    prev = {c - 1, ch};\n}\n```\n**`prev` 就是長度為 1 的冷卻佇列。** 這個寫法的關鍵是**先取、再把上一步的放回**——順序反了就會把剛用過的元素立刻再取一次。\n\n**(B) 間隔 $k$**——大頂堆 + 長度 $k-1$ 的冷卻佇列：\n```cpp\npriority_queue<pair<int,char>> pq;\nqueue<pair<int,char>> cool;          // 冷卻中的元素，長度恆 <= k-1\nwhile (!pq.empty() || !cool.empty()) {\n    if (pq.empty()) return \"\";       // 有元素還沒排完卻全在冷卻中 → 無解\n    auto [c, ch] = pq.top(); pq.pop();\n    res += ch;\n    cool.push({c - 1, ch});\n    if ((int)cool.size() == k) {     // 最早進冷卻的已滿 k-1 步\n        auto p = cool.front(); cool.pop();\n        if (p.first > 0) pq.push(p);\n    }\n}\n```\n**冷卻佇列的長度恆為 $\\min(k-1, \\text{已放置數})$**，這條不變量保證「距離 $< k$ 的元素都在佇列裡、不在堆裡」。**$k \\le 1$ 要特判直接回傳原字串**（`cool.size() == k` 永遠不成立會死迴圈）。\n\n**(C) 621 不需要堆**——只要 `cnt[26]`、`maxCount`、`cntMax`（達到最大次數的任務種類數），套定理 3 的公式。\n\n## 不變量或正確性證明\n\n以下令 $n$ 為總元素數、$m = \\max_c \\mathrm{cnt}(c)$、$p = |\\{c : \\mathrm{cnt}(c) = m\\}|$。\n\n### 定理 1（相鄰不同的充要條件）\n\n**存在一個相鄰皆不同的排列 $\\iff m \\le \\lceil n/2 \\rceil$。**\n\n*必要性*。設某元素 $x$ 出現 $m$ 次。在任何合法排列中，$x$ 的任兩次出現之間至少要夾一個非 $x$ 元素，故需要至少 $m - 1$ 個非 $x$ 元素，即 $n - m \\ge m - 1$，得 $m \\le (n+1)/2$，即 $m \\le \\lceil n/2\\rceil$。$\\square$\n\n*充分性（構造）*。把元素按次數降冪展開成一個序列 $s_0, s_1, \\dots, s_{n-1}$（同一元素的所有份連續）。把它們依序填入位置\n$$0,\\ 2,\\ 4,\\ \\dots,\\ 1,\\ 3,\\ 5,\\ \\dots$$\n（先填所有偶數位，再填所有奇數位）。\n\n*宣稱*：任兩個相鄰位置的元素不同。\n*證明*。相鄰位置 $i, i+1$ 在填入順序中的距離是 $\\lceil n/2\\rceil$（偶數位 $i$ 是第 $i/2$ 個填的，奇數位 $i+1$ 是第 $\\lceil n/2\\rceil + i/2$ 個填的）。而同一元素在展開序列中佔連續的 $\\mathrm{cnt}(c) \\le m \\le \\lceil n/2\\rceil$ 個位置。**兩個填入順序相差 $\\lceil n/2 \\rceil$ 的槽位，不可能同屬一段長度 $\\le \\lceil n/2\\rceil$ 的連續區間**——除非恰好是該區間的兩端，此時 $\\mathrm{cnt}(c) = \\lceil n/2\\rceil + 1 > m$，矛盾。$\\square$\n\n**這個構造比堆貪心更好證，也更快（$O(n + \\Sigma\\log\\Sigma)$，且無堆的常數）。** 1054（距離相等的條形碼）用它是最短的寫法。**「按次數降冪展開後隔位填入」值得當成一個獨立的招式記住。**\n\n### 定理 2（貪心「取剩餘最多者」在間隔 2 時的安全性）\n\n*引理*：若當前狀態（剩餘計數 $\\mathrm{cnt}$、上一步放的元素 $\\textit{prev}$）存在可行完成，則存在一個**在這一步選擇剩餘次數最大的可用元素 $a$** 的可行完成。\n\n*證明（交換）*。設某可行完成在這一步放 $b \\ne a$，其中 $a$ 可用（$a \\ne \\textit{prev}$）且 $\\mathrm{cnt}(a) \\ge \\mathrm{cnt}(b)$。\n\n因 $\\mathrm{cnt}(a) \\ge 1$，該完成在後續某位置放了 $a$；取**最早**的那個位置 $q$。把位置 $t$（放 $b$）與 $q$（放 $a$）互換：\n- *位置 $t$ 放 $a$*：$a \\ne \\textit{prev}$ ✔；位置 $t+1$ 原本放的元素 $\\ne a$（因為 $q$ 是 $t$ 之後最早的 $a$，若 $q = t+1$ 則互換後 $t$ 放 $a$、$t+1$ 放 $b$，仍合法；若 $q > t+1$ 則 $t+1$ 上不是 $a$）✔。\n- *位置 $q$ 放 $b$*：需 $q-1$ 與 $q+1$ 上不是 $b$。$q - 1$ 上若是 $b$，則原本 $q-1$ 放 $b$、$q$ 放 $a$；互換後 $q$ 放 $b$ 與 $q-1$ 的 $b$ 相鄰 ✗ —— 此時改用另一個交換：把 $t$ 的 $b$ 與 $q-1$ 的 $b$ 視為同一元素，直接令互換對象為 $(t, q)$ 之外⋯\n\n  更簡潔的處理：**若 $q-1$ 或 $q+1$ 上是 $b$，則把該處的 $b$ 與 $t$ 的 $b$ 互換（同元素，序列不變），再對新的 $q$ 重做上述論證。** 由於 $b$ 的出現位置兩兩不相鄰（原解合法），$q$ 的左右至多一側是 $b$，重做一次即可脫離。\n\n因此總能得到一個在位置 $t$ 放 $a$ 的可行解。$\\square$\n\n**這條引理點名的前提是「元素之間除了自身的間隔限制外沒有其他約束」**——也就是元素是可互換的。若題目給每個元素額外的位置限制（例如某些位置只能放特定元素），交換論證立刻失效，就得改用二分圖匹配或流。\n\n### 定理 3（621 任務排程器的閉式公式）\n\n**答案 $= \\max\\big((m-1)(k+1) + p,\\ \\ n\\big)$**，其中 $k$ 是冷卻時間、$n$ 是任務總數。\n\n*下界 1*：總長度至少要放得下所有任務，故 $\\ge n$。\n\n*下界 2*：取任一出現 $m$ 次的任務 $x$。它的第 $i$ 次與第 $i+1$ 次出現之間至少隔 $k$ 個時間單位，故前 $m-1$ 次出現各自「佔據」一段長度恰為 $k+1$ 的區塊（從該次出現算起）。這 $m-1$ 段互不重疊，共 $(m-1)(k+1)$。在最後一段（第 $m$ 次出現所在的位置及其後）中，**所有 $p$ 個出現 $m$ 次的任務都必須各出現一次**（它們的最後一次出現都落在最後一個區塊裡，且彼此不能同時），故最後至少再加 $p$。合計 $\\ge (m-1)(k+1) + p$。$\\square$\n\n*達成性（構造）*。把時間軸切成 $m-1$ 個長度 $k+1$ 的列，加上最後一列。**按次數降冪，把每個任務的 $\\mathrm{cnt}(c)$ 份分別放進前 $\\mathrm{cnt}(c)$ 列的同一欄。** 同一任務的兩份分屬相鄰兩列的同一欄，間隔恰為 $k+1 > k$ ✔。\n- 若不同任務數 $\\le k+1$：所有任務都放得進 $k+1 個欄位$，空白處填閒置，總長 $= (m-1)(k+1) + p$。\n- 若不同任務數 $> k+1$：欄位不夠，多出來的任務插進原本的閒置格。**每插入一格，所有後續格子右移，同任務的間隔只會變大不會變小**，故仍合法；插到沒有閒置格為止，總長 $= n$。$\\square$\n\n**公式的兩項分別對應「被最頻繁任務撐開的骨架」與「任務多到不需要閒置」兩種情形。** 這也是為什麼答案是兩者取 $\\max$。\n\n**621 不需要堆**：$O(n + 26)$ 即可。**看到「求最短長度」而不是「求具體排列」，先想公式。** 若題目要求輸出具體排列（621 的變體「任務排程器 II」），才需要模擬。\n\n### 定理 4（358 間隔 $k$ 的必要條件）\n\n**若存在間隔 $\\ge k$ 的合法排列，則 $(m-1)\\cdot k + p \\le n$。**\n\n*證明*：與定理 3 下界 2 相同——前 $m-1$ 次出現各佔一段長度 $\\ge k$ 的不重疊區塊，最後一段至少要容納 $p$ 個達到最大次數的元素。$\\square$\n\n**注意 358 與 621 的差別：621 允許閒置（可以拉長總長度），358 不允許（總長固定為 $n$）。** 所以 621 的公式是「答案」，358 的同一個式子只是「可行性判準」。**這條差異是這兩題最容易混淆的地方。**\n\n*$k = 2$ 時*，條件化為 $(m-1)\\cdot 2 + p \\le n$。當 $p = 1$ 得 $m \\le (n+1)/2 = \\lceil n/2\\rceil$，與定理 1 一致 ✔。\n\n### 定理 5（為什麼是「剩餘次數最多」：唯一不消耗餘裕的選擇）\n\n定義**餘裕**\n$$\\mathrm{slack} \\;=\\; (\\text{剩餘位置數}) \\;-\\; \\big[(m-1)k + p\\big] ,$$\n由定理 4，$\\mathrm{slack} < 0$ 時必定無解。看每放一個元素後 slack 怎麼變（剩餘位置數固定減 1）：\n\n- **放一個剩餘次數為 $m$ 的元素**：\n  - 若 $p > 1$：新的 $m$ 不變、$p$ 減 1 ⇒ 界限減 1 ⇒ **slack 不變**。\n  - 若 $p = 1$：新的最大次數變成 $m-1$，界限從 $(m-1)k + 1$ 變成 $(m-2)k + p'$ ⇒ **界限至少減 $k - p' + 1$，slack 通常增加**。\n- **放一個剩餘次數 $< m$ 的元素**：$m$ 與 $p$ 都不變 ⇒ 界限不變，而剩餘位置數減 1 ⇒ **slack 減 1**。\n\n**結論：只有「取剩餘次數最大者」不會消耗餘裕；取其他元素每次都白白損失 1 單位。** 這就是貪心的精確意義——它是**唯一一個對「必要條件的緊繃程度」沒有負面影響的選擇**。\n\n**這個位能論證點名了它依賴的前提**：(i) 定理 4 的界限確實是必要條件；(ii) 元素之間可互換（定理 2 的前提）。**在間隔 $k \\ge 3$ 的一般情形，把「不消耗餘裕」升級成完整的最優性證明，還需要一個位置重指派的引理**（給定兩組各自 $k$-分散的位置集合、其中一組較大，可以重新指派使當前位置歸給次數較多的元素）；本節不展開，但這一步是唯一的缺口，其餘推理都是完整的。\n\n### 定理 6（1405 最長快樂字串的額外限制）\n\n限制是「同一字元不能連續出現 3 次」，不是固定間隔。**因此冷卻佇列長度不是常數，而是取決於「上一步是否已經連放兩個相同的」。**\n\n*貪心*：每次取剩餘最多者；**若它與已輸出的最後兩個字元相同，就改取剩餘次數第二多者**（若不存在則結束）。\n\n*正確性（交換）*：與定理 2 同構——設此步應放 $a$（剩餘最多且合法）而某最優解放 $b$。因 $\\mathrm{cnt}(a) \\ge \\mathrm{cnt}(b)$，最優解後續必有 $a$，交換之。**額外要檢查的是「連續三次」這個局部條件，而它只涉及相鄰三格，交換後逐一驗證即可。**\n\n**「取最多者、被擋住就取次多者」是這一族的通用退化寫法**——堆頂被冷卻擋住時，退而取次頂，而不是等待。\n\n## 逐步演算法\n\n:::example 統計 + 入堆\n**1. 狀態表示：統計次數，建 max-heap。** 先數每個元素出現幾次，把 `{次數, 元素}` 全推進 max-heap，讓堆頂永遠是最該先放的。\n\n```cpp\n// 統計 + 入堆\nunordered_map<char, int> freq;\nfor (char c : s) {\n    freq[c]++;\n}\npriority_queue<pair<int, char>> pq;\nfor (auto [c, f] : freq) {\n    pq.push({f, c});\n}\n```\n:::\n\n\n\n:::example 相鄰不相同版（間隔 1）：用一個變數當暫存\n**2. 更新流程：放一個、暫存、再放回。** 每輪取堆頂放入結果並扣 1，先擱到暫存區；下一輪開頭再把上一輪的暫存放回堆（若它還有剩）。\n\n```cpp\n// 相鄰不相同版（間隔 1）：用一個變數當暫存\npair<int, char> hold = {0, '#'};\nwhile (!pq.empty()) {\n    auto [cnt, ch] = pq.top();\n    pq.pop();\n    result += ch;\n    if (hold.first > 0) {\n        pq.push(hold);  // 把上一輪擱置的放回\n    }\n    hold = {cnt - 1, ch};  // 這輪的擱置，下一輪放回\n}\n```\n:::\n\n\n\n:::example 間隔 K：用 queue 緩衝，滿 K 才放回堆\n**3. 一般化：間隔 K 用佇列當緩衝。** 若要求「相同元素至少間隔 K」（如任務排程器、K 距離重排），把暫存區換成長度 K 的佇列：每放一個就入佇列，佇列滿 K 後才把最舊的放回堆。\n\n```cpp\n// 間隔 K：用 queue 緩衝，滿 K 才放回堆\nqueue<pair<int, char>> cooldown;\nauto [cnt, ch] = pq.top();\npq.pop();\nresult += ch;\ncooldown.push({cnt - 1, ch});\nif (cooldown.size() >= K) {  // 冷卻夠久了\n    auto back = cooldown.front();\n    cooldown.pop();\n    if (back.first > 0) {\n        pq.push(back);\n    }\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 結束時若結果長度不足（堆提早空、緩衝裡還有殘留），代表無法重排，回傳空字串或對應失敗值。\n- 任務排程器問的是「總時間」而非排列本身，可直接用公式 `(maxCnt - 1) * (n + 1) + 出現 maxCnt 的元素個數` 與總長度取較大者。\n- 複雜度：每個元素進出堆常數次，`O(n log m)`（m 為相異元素數），空間 `O(m)`。\n\n## C++17 模板\n\n以下用 1054. 距離相等的條形碼 展示用堆重排元素的核心寫法。\n\n**完整題目**：給陣列 barcodes，重新排列使得任意相鄰兩個元素都不相同。題目保證一定存在合法排列，回傳任一種即可。\n\n:::example 貪心策略：每次取頻率最高的兩個值交替放置，保證相鄰不同\n**線索**：要讓相同元素不相鄰，直覺是「先擺出現次數最多的」。用最大堆依頻率排序，每一輪取出頻率最高的兩種不同元素各放一個，就能保證這兩個不相鄰，且高頻元素被穩定攤開。\n\n```cpp\nclass Solution {\npublic:\n    vector<int> rearrangeBarcodes(vector<int>& barcodes) {\n            // 貪心策略：每次取頻率最高的兩個值交替放置，保證相鄰不同\n            unordered_map<int, int> cnt;\n            for (int b : barcodes) {\n                    ++cnt[b];\n            }\n            // 最大堆以頻率為優先鍵，確保頻率高的優先被選\n            priority_queue<pair<int, int>> pq;\n            for (auto& [val, c] : cnt) {\n                    pq.push({c, val});\n            }\n            int n = barcodes.size();\n            vector<int> ans(n);\n            int i = 0;\n            // 每輪同時取出頻率最大的兩個，填入相鄰位置\n            while (pq.size() >= 2) {\n                    auto [c1, v1] = pq.top();\n                    pq.pop();\n                    auto [c2, v2] = pq.top();\n                    pq.pop();\n                    ans[i++] = v1;\n                    ans[i++] = v2;\n                    // 使用後若還有剩餘則放回堆\n                    if (c1 - 1 > 0) {\n                            pq.push({c1 - 1, v1});\n                    }\n                    if (c2 - 1 > 0) {\n                            pq.push({c2 - 1, v2});\n                    }\n            }\n            // 最多剩一個元素（題目保證合法解存在，頻率最高者不超半數）\n            if (!pq.empty()) {\n                    ans[i] = pq.top().second;\n            }\n            return ans;\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- 先用雜湊表統計每個值的出現次數。\n- 最大堆以頻率為鍵，堆頂是目前最該被攤開的值。\n- 每輪取出頻率最高的兩種「不同」值各放一個，保證它們不相鄰。\n- 次數減一後若仍大於 0 就壓回；最後可能剩單一個值補在尾端。\n\n## 時間與空間複雜度\n\n| 題目 | 做法 | 時間 | 空間 |\n| --- | --- | --- | --- |\n| 767 重構字串 | 隔位填入（定理 1） | $O(n + \\Sigma\\log\\Sigma)$ | $O(n)$ |\n| 767 / 1054 | 大頂堆貪心 | $O(n\\log\\Sigma)$ | $O(\\Sigma)$ |\n| 358 K 距離重排 | 堆 + 冷卻佇列 | $O(n\\log\\Sigma)$ | $O(\\Sigma)$ |\n| 621 任務排程器 | **公式** | $O(n + 26)$ | $O(26)$ |\n| 621 | 堆模擬（若要輸出排列） | $O(n\\log 26)$ | $O(26)$ |\n| 1405 最長快樂字串 | 堆貪心（只有 3 種字元） | $O(n)$ | $O(1)$ |\n\n**$\\Sigma$ 通常是 26 或 100，所以 $\\log\\Sigma \\le 7$——堆的成本其實是常數。** 但在 $\\Sigma$ 小的時候，**直接用 `cnt[26]` 每次線性掃找最大值（$O(\\Sigma)$）往往比堆更快**，因為省掉了堆的節點搬移與快取失效。**$\\Sigma \\le 26$ 時，$O(n\\Sigma) = 26n$ 完全可接受。**\n\n**621 用公式是 $O(n)$、用模擬是 $O(n\\log 26)$，且模擬還要處理「時間跳躍」的細節。** 只問長度時務必用公式。\n\n**空間**：所有做法都是 $O(\\Sigma)$（不計輸出）。**注意堆裡放的是「(剩餘次數, 字元)」而不是逐份元素**，否則空間會退化成 $O(n)$、時間退化成 $O(n\\log n)$。\n\n## 常見錯誤與邊界條件\n\n- 一次取兩個不同值是避免自我相鄰的關鍵，不可只取一個。\n- 題目保證有解（最高頻率 `<= (n + 1) / 2`），否則需先判可行性。\n- 最後堆若剩一個，其頻率必為 1，直接補在尾端不會相鄰。\n\n## 常見變形\n\n- 任務排程器（621）：相同任務需間隔冷卻，用頻率攤開。\n- 重構字串（767）：相鄰不得相同的字元版本。\n- K 距離間隔重排（358）：把「不相鄰」推廣為「間隔至少 K」。\n\n## 與相似技巧的比較\n\n- **與 6.1 的貪心 + 堆**：本節是它的一個特化——**多了「冷卻」這個時間維度的約束**，所以堆之外還要一個佇列。**「堆管價值、佇列管時間」與 5.4 單調佇列的「尾端管價值、頭端管時間」是同一種分工。**\n- **與 3.1.2 差分／掃描線**：621 的公式解本質是「算最頻繁元素撐出的骨架長度」，屬於計數論證而非資料結構。**很多「重排」題的最優值有閉式解，先花五分鐘找公式，找不到再上資料結構。**\n- **與二分圖匹配**：若元素有「只能放在某些位置」的額外限制，交換論證失效（定理 2 的前提被破壞），要改用匹配或網路流。**判準：元素之間是否可互換。**\n- **與 6.5 反悔堆**：本節的貪心**不反悔**——因為定理 5 保證每一步都不消耗餘裕。**一旦某步的選擇可能在未來被推翻，才需要反悔堆。**\n- **與計數排序**：$\\Sigma$ 小時用 `cnt[]` 陣列取代堆更快。**堆的優勢在 $\\Sigma$ 大（例如元素是任意整數）時才顯現。**\n- **與 4.3 鄰項消除**：兩者都在處理「相鄰元素的關係」，但方向相反：4.3 是**移除**相鄰的相同項，本節是**安排**使相鄰不同。**4.3 的答案唯一（合流性），本節的答案通常不唯一。**\n\n## 代表例題\n\n**題號**：358、621、767、984、1054\n\n請套用「重排元素」的狀態、不變式與實作方式，先寫出資料表示、更新流程與答案讀取規則。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 358 | [K 距離間隔重排字串](https://leetcode.cn/problems/rearrange-string-k-distance-apart)（Premium） |  |\n| 621 | [任務排程器](https://leetcode.cn/problems/task-scheduler) |  |\n| 767 | [重構字串](https://leetcode.cn/problems/reorganize-string) |  |\n| 984 | [不含 AAA 或 BBB 的字串](https://leetcode.cn/problems/string-without-aaa-or-bbb) | 1474 |\n| 1054 | [距離相等的條形碼](https://leetcode.cn/problems/distant-barcodes) | 1702 |\n| 1405 | [最長快樂字串](https://leetcode.cn/problems/longest-happy-string) | 1821 |\n| 3081 | [替換字串中的問號使分數最小](https://leetcode.cn/problems/replace-question-marks-in-string-to-minimize-its-value) | 1905 |\n\n## 本節重點速查\n\n- **共同骨架：每步取「剩餘次數最多且不在冷卻中」的元素。** 堆管價值、冷卻佇列管時間。\n- **相鄰不同的充要條件：$m \\le \\lceil n/2\\rceil$**（定理 1）。**構造用「按次數降冪展開後先填偶數位再填奇數位」——比堆貪心更快也更好證。**\n- **621 的公式：$\\max\\big((m-1)(k+1)+p,\\ n\\big)$**（定理 3）。兩項分別是「最頻繁任務撐開的骨架」與「任務多到不必閒置」。**只問長度就別模擬。**\n- **621 允許閒置、358 不允許**——同一個式子在 621 是答案、在 358 只是可行性判準（定理 4）。這是兩題最常混淆之處。\n- **貪心的精確意義（定理 5）**：定義 $\\mathrm{slack} = \\text{剩餘位置} - [(m-1)k + p]$，**只有取最大次數者不會消耗 slack**，取其他元素每次白白損失 1。\n- 交換論證的前提是**元素可互換**；一旦有「某位置只能放某元素」的限制，就要改用匹配／流。\n- 實作：`prev`／冷卻佇列的元素**要在放置之後才推回堆**；`k <= 1` 要特判否則死迴圈；堆裡放 `(次數, 字元)` 而非逐份元素。\n- $\\Sigma \\le 26$ 時，直接掃 `cnt[26]` 找最大值往往比堆更快。\n",
        },
        {
          id: 45,
          title: "6.5 反悔堆",
          summary:
            "## 這個技術解決什麼問題\n\n有一類題長這樣：**沿著某個固定順序掃描，邊走邊做取捨，受一個累積型的限制約束**（油量、時間、磚塊、前綴和非負、預算）。\n\n純貪心會錯，因為「現在看起來該選的」到後面才知道不該選；但**枚舉所有取捨是指數級**。\n\n**反悔貪心的解法**：*先無條件地把每一項都拿下來；一旦違反約束，就從已拿的當中退掉最糟的一個。* 堆負責 $O(\\log n)$ 找出「最糟的已選項」。\n\n典型題：\n- **871 最低加油次數**：路過就先把油「記在帳上」，油不夠時才回頭「補加」最大的那桶。\n- **630 課程表 III**：先把課排進去，超過期限就退掉**耗時最長**的一門。\n- **1642 可以到達的最遠建築**：磚塊不夠時，把**最大的那次爬升**改用梯子。\n- **2599 / LCP 30**：前綴和變負時，退掉**最負的**那個元素。\n- **2813 子序列最大優雅度**：先取利潤最大的 $k$ 個，再用後面的項替換掉「重複類別中利潤最小」的已選項。\n\n**辨識訊號**：\n1. 有一個**單向掃描的順序**（位置、期限、時間）；\n2. 有一個**累積量**必須滿足不等式；\n3. 「已選集合」的大小或代價是目標；\n4. 而且**「撤銷一個已選項」是合法且可計算的**。\n\n第 4 點是關鍵前提：**必須能算出撤銷的效果，且撤銷不會讓已經走過的路變得不合法。**\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「反悔堆」想：\n\n- **沿一個順序掃描，邊走邊做選擇，受某個累積上限約束**（時間、油量、次數、字首和非負）。像 871 加油、630 課程表 III、1642 最遠建築都是這個樣子。\n- **發現「純貪心一步到位會錯，但允許事後反悔就對了」。** 關鍵是：已選數量固定時，只要永遠保留最划算的一批即可：\n\n:::example 嗅到味道：當下先收，超限再丟掉「最虧的已選」\n```cpp\n// 嗅到味道：當下先收，超限再丟掉「最虧的已選」\n// if (overLimit) { undo(worstChosen); }\n```\n:::\n\n\n\n- **要的是「最少次數」「最多能選幾個」「保持某條件成立」**，而每次違規都能用「丟掉一個最糟的已選」來救。例如 1642 用「磚塊有限，梯子拿來反悔最大的爬升」。\n- **暴力做法是窮舉所有取捨組合（指數級）**；反悔堆把它變成一次掃描 + 堆操作。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的「掃描排序鍵」和「堆反悔鍵」分別是什麼？\n2. 什麼條件代表「超限、必須反悔」？反悔時我丟的是堆頂的誰？\n3. 反悔時，我有沒有同步修正累積資源？最終答案從哪個狀態讀出？\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n本節要解決的場景是：我們沿著一條時間軸（或某個順序）一路掃過去，看到候選就想「先收下再說」，但有一個資源上限（時間、油量、次數）不能超過。一旦超標怎麼辦？不是放棄當前這個，而是**回頭把「之前收過、但現在看起來最虧」的那個丟掉**——這就是「反悔（regret）」。\n\n為什麼可以這樣？因為很多題目滿足一個性質：**到目前為止「已選了 k 個」這件事一定可行，問題只是「選哪 k 個最划算」。** 既然數量固定，那就讓已選集合永遠保留「最划算的那一批」。當新候選加入導致超限，就反悔掉已選裡「代價最大／收益最差」的一個，整體只會更好或持平。\n\n:::example max-heap 保存「已選的代價」，top() = 最該反悔（代價最大）的那個\n要快速找到「最該反悔的那個」，又是堆的拿手好戲：把已選元素以「代價」為鍵放進 max-heap，堆頂就是最該被丟的。\n\n```cpp\n// max-heap 保存「已選的代價」，top() = 最該反悔（代價最大）的那個\npriority_queue<int> chosen;\nchosen.push(3);  // 已選一個代價 3 的\nchosen.push(7);\nint worst = chosen.top();  // 7：若要反悔，先丟它\n```\n:::\n\n\n\n:::example 油不夠了 → 反悔：把之前看過、油最多的那站「現在才加」\n以「最低加油次數」為例：開車向前開，每經過一個加油站，就把它的油量先「記在堆裡」當作「可隨時取用的反悔額度」。當油不夠開到下一站時，就從堆裡拿出「油量最大的那一站」灌進來（等於追認一次加油），加油次數 +1。每次都拿最大的，自然讓加油次數最少。\n\n```cpp\n// 油不夠了 → 反悔：把之前看過、油最多的那站「現在才加」\nwhile (fuel < need && !chosen.empty()) {\n    fuel += chosen.top();  // 取最大油量灌入\n    chosen.pop();\n    ++refuels;  // 算一次加油\n}\n```\n:::\n\n\n\n核心一句話：**先樂觀地接納，超限就用堆挑出最虧的反悔掉。** 因為每個候選最多被加入一次、反悔一次，總成本仍是 `O(n log n)`。\n\n## 狀態／資料結構定義\n\n**通用骨架**：\n```cpp\npriority_queue<int> regret;   // 已選項中「最糟者」在堆頂（方向依題目而定）\nlong long acc = 初始值;        // 受約束的累積量\nint count = 0;                 // 已選 / 已用次數\nfor (每個項目 x，按掃描順序) {\n    無條件選入 x;  acc += 貢獻(x);  regret.push(代價(x));\n    while (acc 違反約束) {\n        if (regret.empty()) return 無解;\n        acc -= 撤銷(regret.top());  regret.pop();  ++count;\n    }\n}\n```\n\n**堆的方向要對應「最糟」的定義，這是唯一需要逐題判斷的地方**：\n\n| 題目 | 堆中存什麼 | 堆頂是 | 撤銷的意義 |\n| --- | --- | --- | --- |\n| 871 加油 | 路過但未加的油量 | **最大** | 「補加」這桶油（次數 +1） |\n| 630 課程表 III | 已選課程的耗時 | **最大** | 退掉最耗時的課 |\n| 1642 建築 | 已走過的正爬升 | **最小**（大小為 `ladders` 的小頂堆） | 把最小的那次改回用磚塊 |\n| 2599 前綴和非負 | 已選的負元素 | **最小（最負）** | 把最負的搬到最後 |\n| 2813 優雅度 | 已選中「類別重複」者的利潤 | **最小** | 換成一個新類別的項 |\n\n**871 的方向最反直覺**：堆裡放的不是「已加的油」而是「路過的加油站」，撤銷的動作是**追加**而不是移除。**這是「反悔」的另一種面貌——把『當時沒做的事』事後補做。** 它成立的前提見定理 1。\n\n**1642 的兩種等價寫法**：\n- **大頂堆存所有爬升，磚塊不夠就把最大的改用梯子**（要維護已用梯子數）；\n- **小頂堆恆保持大小 $\\le$ `ladders`，代表「分配給梯子的爬升」，被擠出來的用磚塊**。\n後者更簡潔，且與 6.1 定理 4「大小為 $K$ 的堆」是同一個不變量。\n\n## 不變量或正確性證明\n\n### 定理 1（871 最低加油次數：時機無關性）\n\n*關鍵前提（時機無關）*：**在加油站 $i$ 加油，與「先開過去、事後再宣稱在 $i$ 加了油」是等價的**——只要車在到達 $i$ 之後的任何時刻回頭把這桶油計入，路徑上的每一點油量都不會變成負值嗎？\n\n嚴格地說：設實際策略是「在站 $i_1 < i_2 < \\dots < i_j$ 加油」。車能開到位置 $x$ 的充要條件是：對**每一個前綴位置** $y \\le x$，都有\n$$\\text{起始油量} + \\sum_{i_s \\le y} \\text{fuel}(i_s) \\;\\ge\\; y .$$\n**演算法的做法是「先假設不加油地開，卡住時才從已路過的站中取最大的補上」。** 這樣做出來的加油集合是否滿足上式？\n\n*引理*：貪心在到達位置 $x$ 時所用的加油次數 $j(x)$，等於「能開到 $x$ 所需的最少次數」，且此時的油量是「用 $j(x)$ 次加油能達到的最大值」。\n\n*證明（歸納）*。設到達站 $i$ 前不變量成立：已用 $j$ 次，且油量 $= \\text{start} + (\\text{已路過站中最大的 } j \\text{ 桶之和}) - (\\text{已走距離})$。\n- 若油夠開到下一站，$j$ 不變，不變量保持（新路過的站進堆，不影響「最大 $j$ 桶」是否改變？會改變——若新站的油量比已選的某桶大，「最大 $j$ 桶」應該換人）。\n\n  這正是**堆的寫法自動處理的事**：把所有路過的站都放進大頂堆，只在需要時彈出最大者。彈出的一定是「所有已路過站中最大的、尚未使用的那桶」，所以任何時刻「已使用的 $j$ 桶」恆為**已路過站中最大的 $j$ 桶**。$\\square$\n- 若油不夠：必須增加加油次數。由上述性質，用 $j+1$ 次能達到的最大油量就是「最大 $j+1$ 桶之和」，貪心正是取出第 $j+1$ 大的那桶。若堆已空仍不夠 ⇒ 用任何次數都不夠 ⇒ 無解。$\\square$\n\n*最少性*：由引理，對每個 $j$，貪心給出「用 $j$ 次能到達的最遠距離」；因此第一次到達終點時的 $j$ 就是最小值。$\\square$\n\n**這個證明點名的前提是「油可以無限儲存、加油不花時間」**——即**資源是可累積且無上限的**。若油箱有容量上限，「事後補加」就不再等價（可能溢出），反悔堆立刻失效，得改用 DP。**這是 871 與其變體的分界線。**\n\n### 定理 2（630 課程表 III：兩層最優的強不變量）\n\n課程 $(d_i, e_i)$ = (耗時, 截止日)；同時只能上一門課；求最多能修幾門。\n\n*引理 A（排序）*：**對任一可行的課程子集，按截止日升冪安排一定可行。**\n*證明（交換）*：若某可行安排中存在相鄰兩門 $u, v$ 順序為 $u$ 先、$v$ 後但 $e_u > e_v$，交換之。交換後 $v$ 的結束時間變早（不會違規），$u$ 的結束時間等於原本 $v$ 的結束時間 $\\le e_v < e_u$（不會違規）。反覆消除逆序即得。$\\square$\n\n*推論*：子集 $S$ 可行 $\\iff$ 按截止日升冪排列後，每個前綴的耗時總和 $\\le$ 該前綴最後一門的截止日。\n\n*演算法*：把課程按 $e_i$ 升冪處理。維護一個**大頂堆**（存已選課程的耗時）與 `total`（已選總耗時）。\n```\ntotal += d_i;  heap.push(d_i);\nif (total > e_i) { total -= heap.top(); heap.pop(); }\n```\n\n*強不變量 (S)*：處理完前 $i$ 門課後，堆中的集合 $S_i$ 滿足\n1. $S_i$ 可行；\n2. $|S_i|$ 是「前 $i$ 門課的可行子集」中的**最大值**；\n3. 在所有大小為 $|S_i|$ 的可行子集中，$S_i$ 的**總耗時最小**。\n\n**條件 3 是這個證明的靈魂**——只有「同樣門數之下總耗時最小」，才能保證後續的課有最大的空間可以插入。**只證條件 2 是證不下去的。**\n\n*證明（對 $i$ 歸納）*。$i=0$ 顯然。設 (S) 對 $i-1$ 成立，處理第 $i$ 門課 $(d_i, e_i)$。\n- *情形 A：$\\mathrm{total}(S_{i-1}) + d_i \\le e_i$*。取 $S_i = S_{i-1}\\cup\\{i\\}$。\n  - 可行：由引理 A 的推論，把 $i$ 接在最後（它的截止日最大），前綴和條件正是 $\\mathrm{total} + d_i \\le e_i$ ✔。\n  - 最大：任一前 $i$ 門的可行子集 $T$，若 $|T| > |S_{i-1}| + 1$，則 $T \\setminus \\{i\\}$ 是前 $i-1$ 門的可行子集且大小 $> |S_{i-1}|$，違反歸納假設 2 ✔。\n  - 最小耗時：設 $T$ 可行、$|T| = |S_{i-1}|+1$。若 $i \\in T$，則 $T\\setminus\\{i\\}$ 大小為 $|S_{i-1}|$、可行，由假設 3 其耗時 $\\ge \\mathrm{total}(S_{i-1})$，故 $\\mathrm{total}(T) \\ge \\mathrm{total}(S_i)$ ✔。若 $i \\notin T$，則 $T$ 是前 $i-1$ 門中大小 $|S_{i-1}|+1$ 的可行子集，與假設 2 矛盾 ✔。\n- *情形 B：$\\mathrm{total}(S_{i-1}) + d_i > e_i$*。令 $M = \\max(S_{i-1} \\cup \\{i\\})$（最大耗時），$S_i = (S_{i-1}\\cup\\{i\\})\\setminus\\{M\\}$，故 $|S_i| = |S_{i-1}|$。\n  - 可行：$\\mathrm{total}(S_i) = \\mathrm{total}(S_{i-1}) + d_i - M \\le \\mathrm{total}(S_{i-1})$（因 $M \\ge d_i$），而 $S_{i-1}$ 可行且新加入的課截止日最大，故所有前綴條件仍成立 ✔。\n  - 最大：若存在大小 $|S_{i-1}|+1$ 的可行子集 $T \\subseteq [i]$，則 $\\mathrm{total}(T) \\le e_i$（$T$ 的總耗時不超過最大截止日）。$T\\setminus\\{i\\}$（或 $T$ 本身若 $i\\notin T$）是前 $i-1$ 門中大小 $\\ge |S_{i-1}|$ 的可行子集，由假設 3，$\\mathrm{total}(S_{i-1}) \\le \\mathrm{total}(T \\setminus \\{i\\})$，於是 $\\mathrm{total}(S_{i-1}) + d_i \\le \\mathrm{total}(T) \\le e_i$，與情形 B 的前提矛盾 ✔。\n  - 最小耗時：$S_i$ 是在「$S_{i-1} \\cup \\{i\\}$」中去掉最大者，故是該多重集合中大小為 $|S_{i-1}|$ 的最小耗時子集；再結合假設 3 可得全域最小 ✔。$\\square$\n\n**這個「主目標最大 + 次要目標最小」的雙層不變量，是所有反悔堆題的證明範式。** 630 是它最乾淨的示例，值得完整讀一遍。\n\n### 定理 3（1642 可以到達的最遠建築）\n\n*引理*：**若已經走到建築 $i$，則所需磚塊的最小值 $=$（前 $i$ 段正爬升之和）$-$（其中最大的 `ladders` 個之和）。**\n*證明*：每段正爬升必須用磚塊或梯子；梯子最多 `ladders` 個，且一段爬升用梯子省下的磚塊恰等於該段高度。要最小化磚塊，就把梯子用在最大的 `ladders` 段上（這是「選 $k$ 個使和最大」的平凡貪心）。$\\square$\n\n*推論（單調性）*：可到達的建築編號構成一個前綴——若能到 $i$，用同樣的分配也能到 $i-1$。**所以「走到不能走為止」是正確的終止條件，不需要二分。**\n\n*實作*：小頂堆恆保持大小 $\\le$ `ladders`，內容是「目前分配給梯子的爬升」。新爬升推入後若超量，彈出最小者並從磚塊扣除。**堆的不變量與 6.1 定理 4 完全相同：大小為 $K$ 的小頂堆恆為「目前為止最大的 $K$ 個」。**\n\n### 定理 4（2599 / LCP 30：搬走最負的元素）\n\n要求每個前綴和 $\\ge 0$（LCP 30 是 $\\ge 1$，且可把元素搬到最後）；求最少搬動次數。\n\n*觀察*：**把元素搬到最後，等價於在掃描階段把它移除**（因為搬到最後的元素不影響任何中間前綴；LCP 30 需另外確認總和足夠，題目已保證或需先判斷）。\n\n*貪心*：從左到右累加；一旦 $\\mathrm{acc} < 0$，從已加入的元素中取出**最小（最負）**的一個移除，`acc -= that`（減去負數即增加），次數 +1。\n\n*不變量*：處理完前 $i$ 個元素後，\n1. 移除次數是「使前 $i$ 個的所有前綴和非負」所需的**最小值**；\n2. 在所有達到該最小次數的移除方案中，當前的 $\\mathrm{acc}$ **最大**。\n\n*證明概要*。條件 2 是關鍵：移除 $r$ 個元素能達到的最大 $\\mathrm{acc}$，就是「總和減去最小的 $r$ 個」，貪心正是這麼做的。條件 1 由條件 2 推得：若貪心在第 $i$ 步不得不移除第 $r+1$ 個，表示用 $r$ 次移除能達到的最大 $\\mathrm{acc}$ 仍為負，故任何方案都至少要 $r+1$ 次。$\\square$\n\n**又是「主目標最小 + 次要目標最大」的雙層不變量**，與定理 2 同構。**寫任何反悔堆題時，先把這兩層寫下來，證明就自己浮現了。**\n\n### 定理 5（什麼時候「反悔」是合法的）\n\n反悔堆能用，需要三個前提，缺一不可：\n\n1. **撤銷可計算**：能 $O(1)$ 算出移除某已選項對累積量的影響（通常是「減掉它的貢獻」）。\n2. **撤銷不破壞歷史**：移除一個已選項後，**先前所有已檢查過的約束仍然成立**。871、630、1642、2599 都滿足——因為移除只會讓累積量朝「更寬鬆」的方向走。**若移除某項會讓早先的某個約束反而變緊，就不能反悔。**\n3. **順序固定**：掃描順序由題目結構決定（位置、截止日、時間），**不是我們可以選的**。若順序也要優化，那是排程問題，通常需要 DP 或流。\n\n**檢查這三點只要一分鐘，卻能避免把一個 DP 題硬套成貪心。**\n\n## 逐步演算法\n\n:::example 例：課程表 III，按「截止時間」升序掃描\n**1. 排序鍵與堆鍵分清楚。** 通常先依「掃描順序」排序（截止時間、位置、座標）決定處理先後；堆則以「反悔的依據」（代價、收益、所需資源）為鍵。兩者是不同的東西。\n\n```cpp\n// 例：課程表 III，按「截止時間」升序掃描\nsort(courses.begin(), courses.end(),\n          [](auto& a, auto& b) { return a[1] < b[1]; });  // a[1]=deadline\npriority_queue<int> chosen;  // 堆鍵：課程「耗時」，方便反悔最久的\n```\n:::\n\n\n\n:::example 先接納：累積資源 + 入堆\n**2. 掃描 + 樂觀接納。** 依序處理每個候選，先當作「選下去」，更新累積資源並把它入堆。\n\n```cpp\n// 先接納：累積資源 + 入堆\nint total = 0;  // 已用時間\nfor (auto& c : courses) {\n    total += c[0];  // c[0]=耗時，先選下去\n    chosen.push(c[0]);\n    // ...接著檢查是否超限（步驟 3）\n}\n```\n:::\n\n\n\n:::example 超過截止 → 反悔掉「最耗時」的那門，騰出空間\n**3. 超限就反悔堆頂。** 若違反限制（總耗時超過當前截止），彈出堆頂（耗時最大者）並把累積值修正回來；這樣已選的數量不變，卻換成更省的組合。\n\n```cpp\n// 超過截止 → 反悔掉「最耗時」的那門，騰出空間\nif (total > c[1]) {       // c[1]=deadline\n    total -= chosen.top();  // 修正累積值\n    chosen.pop();\n}\n```\n:::\n\n\n\n:::example 課程表 III：最多能修幾門 = 堆裡剩下的數量\n**4. 答案讀取與複雜度。** 掃完後，堆裡剩下的「個數」或維護的「累積收益」就是答案——它已是所有局部反悔後的最佳結果。\n\n```cpp\n// 課程表 III：最多能修幾門 = 堆裡剩下的數量\nreturn chosen.size();\n```\n:::\n\n\n\n- 注意修正累積值要和反悔動作成對出現，漏改就會算錯資源。\n- 有些題堆鍵要取相反方向（反悔「收益最差」用小根堆）；想清楚「最該丟的是誰」。\n- 複雜度：每個候選最多進出堆一次，加上排序，`O(n log n)`；空間 `O(n)`。\n\n## C++17 模板\n\n以下用 871. 最低加油次數 展示反悔堆的核心寫法。\n\n**完整題目**：車子要從 0 開到 target，起步有 `startFuel` 升油，每升跑 1 單位距離。途中有若干加油站 `stations[i] = [位置, 油量]`，到站可選擇加油。求到達 target 的最少加油次數；無法到達回傳 -1。\n\n:::example 貪心 + 最大堆：盡可能不加油，油量不足時從已經過的站中選加油量最大的補\n**線索**：與其事先決定在哪些站加油，不如「先一路開過去，把沿途能加的油記下來」，等到開不動時，再反悔去加之前記下的最大那桶油。這種「先不選、卡住再回頭挑最大」就是反悔堆。\n\n```cpp\nclass Solution {\npublic:\n    int minRefuelStops(int target, int startFuel, vector<vector<int>>& stations) {\n            // 貪心 + 最大堆：盡可能不加油，油量不足時從已經過的站中選加油量最大的補\n            priority_queue<int> pq;  // 存放已通過但尚未使用的加油站油量\n            long long fuel = startFuel;\n            int stops = 0, i = 0, n = stations.size();\n            while (fuel < target) {\n                    // 將所有當前油量可到達的加油站加入候選堆\n                    while (i < n && stations[i][0] <= fuel) {\n                            pq.push(stations[i][1]);\n                            ++i;\n                    }\n                    // 無可用加油站且油量仍不足，無法到達終點\n                    if (pq.empty()) {\n                            return -1;\n                    }\n                    // 貪心選擇：加油量最大的站（回溯補油，等效於在那站加油）\n                    fuel += pq.top();\n                    pq.pop();\n                    ++stops;\n            }\n            return stops;\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- `fuel` 同時是「目前油量」與「能到達的最遠位置」（從 0 起算）。\n- 凡是位置 `<=` 目前可達距離的加油站，先把油量丟進最大堆備用，不急著加。\n- 一旦 `fuel < target` 又沒新站可達，就反悔加入堆中最大的一桶油。\n- 每次反悔加油 `stops + 1`，直到能抵達 target。\n\n## 時間與空間複雜度\n\n| 題目 | 時間 | 空間 | 說明 |\n| --- | --- | --- | --- |\n| 871 最低加油次數 | $O(n\\log n)$ | $O(n)$ | 每站入堆出堆各一次 |\n| 630 課程表 III | $O(n\\log n)$ | $O(n)$ | 排序主導 |\n| 1642 最遠建築 | $O(n\\log L)$，$L$ = 梯子數 | $O(L)$ | **堆大小是 $L$ 不是 $n$** |\n| 2599 / LCP 30 | $O(n\\log n)$ | $O(n)$ | |\n| 2813 優雅度 | $O(n\\log n)$ | $O(n)$ | 排序 + 堆 |\n\n**攤還論證**：每一項最多入堆一次、出堆一次，故堆操作總數 $\\le 2n$，總時間 $O(n\\log n)$。**這與 4.7、5.4 的位能論證是同一件事——反悔堆的 `while` 迴圈同樣不會讓複雜度變成 $O(n^2)$。**\n\n**1642 的空間 $O(L)$ 值得強調**：用「小頂堆恆保持大小 $\\le$ `ladders`」的寫法，堆最多 $L$ 個元素；用「大頂堆存全部爬升」的寫法則是 $O(n)$。**兩者時間同級，但前者常數與空間都更好。**\n\n**溢位**：871 的距離與油量、630 的總耗時、2599 的前綴和都要用 `long long`。**$n \\le 10^5$、單值 $\\le 10^9$ ⇒ 總和可達 $10^{14}$，`int` 必爆。**\n\n## 常見錯誤與邊界條件\n\n- 堆裡保存的是「曾經路過、還沒用的油」，反悔時才真正消費。\n- 排序鍵（站點位置）與堆鍵（油量）是兩回事，別混淆。\n- 距離可能很大，`fuel` 用 `long long` 較保險。\n\n## 常見變形\n\n- 課程表 III（630）：超時就反悔退掉耗時最長的課。\n- IPO / 會議安排：先接受候選，違反限制再反悔最差選擇。\n- 可達最遠建築（1642）：磚塊不足時反悔對最大落差改用梯子。\n\n## 與相似技巧的比較\n\n- **與 6.2 的純貪心（502 IPO）**：502 **不需要反悔**，因為「資本只增不減 ⇒ 可行集單調」（6.2 定理 3）。**一旦某個選擇會讓未來的可行集縮小，就要升級成反悔堆。這是兩節的精確分界。**\n- **與 6.4 的重排貪心**：6.4 定理 5 證明了「取最大者不消耗餘裕」，所以不必反悔。**反悔堆處理的是「不得不先做決定，但決定會被後續資訊推翻」的情形。**\n- **與 DP**：反悔堆能解的題，通常都有一個 $O(n \\cdot C)$ 的 DP 解（$C$ 為資源上限）。**當資源上限很大（$10^9$）時 DP 不可行，反悔堆的 $O(n\\log n)$ 是唯一出路；反之若資源上限小、或約束之間互相耦合（例如油箱有容量上限，破壞了定理 1 的前提），就得回頭用 DP。**\n- **與反悔自動機**：另一種反悔形式是「**選一個項時，同時把一個『撤銷券』放回堆裡**」。經典例子是「低買高賣」型的股票題：賣出時把賣價再推入一次，代表「這筆交易可以事後改成中轉」。**反悔堆撤銷已做的決定，反悔自動機則預先為撤銷保留機會。** 兩者證明思路相同，都是雙層不變量。\n- **與交換論證（一般貪心）**：本節的證明都是「雙層不變量 + 歸納」而非單純的交換。**差別在於：反悔堆的解在每個前綴上都最優，這比『存在一個最優解與貪心一致』更強，也更好用歸納推下去。**\n- **與 8.x 並查集**：某些「反悔」題（例如「每個時刻只能用一個資源，找最晚可用的空位」）用並查集做「找 $\\le t$ 的最大空閒位置」更快，$O(n\\,\\alpha)$。**若撤銷的對象是「時間槽」而非「已選項」，先想並查集。**\n\n## 代表例題\n\n**題號**：LCP 30、630、871、1642、2599\n\n請套用「反悔堆」的狀態、不變式與實作方式，先寫出資料表示、更新流程與答案讀取規則。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| LCP 30 | [魔塔遊戲](https://leetcode.cn/problems/p0NxJO) |  |\n| 630 | [課程表 III](https://leetcode.cn/problems/course-schedule-iii) |  |\n| 871 | [最低加油次數](https://leetcode.cn/problems/minimum-number-of-refueling-stops) | 2074 |\n| 1642 | [可以到達的最遠建築](https://leetcode.cn/problems/furthest-building-you-can-reach) | 1962 |\n| 2599 | [使字首和陣列非負](https://leetcode.cn/problems/make-the-prefix-sum-non-negative)（Premium） |  |\n| 2813 | [子序列最大優雅度](https://leetcode.cn/problems/maximum-elegance-of-a-k-length-subsequence) | 2582 |\n| 3049 | [標記所有下標的最早秒數 II](https://leetcode.cn/problems/earliest-second-to-mark-indices-ii) | 3111 |\n| 3362 | [零陣列變換 III](https://leetcode.cn/problems/zero-array-transformation-iii) | 2424 |\n| 3711 | [不出現負餘額的最大交易額](https://leetcode.cn/problems/maximum-transactions-without-negative-balance) |  |\n\n## 本節重點速查\n\n- **骨架：先無條件全拿，違反約束就退掉最糟的一個。** 堆負責 $O(\\log n)$ 找出「最糟的已選項」。\n- **證明範式：雙層不變量——「主目標最優」＋「同分時次要指標最優」。** 630 是「門數最多 + 同門數下總耗時最小」，2599 是「移除最少 + 同次數下累積和最大」。**只證第一層一定推不下去。**\n- **能用反悔堆的三個前提（定理 5）**：撤銷可計算、撤銷不破壞已檢查過的約束、掃描順序由題目固定。\n- **871 的反悔方向是「事後補加」而非「移除」**；它依賴「資源可累積且無上限」——**油箱有容量上限就得改 DP。**\n- **1642 用「小頂堆恆保持大小 $\\le$ ladders」**：時間 $O(n\\log L)$、空間 $O(L)$，比大頂堆存全部好。\n- **2599 的「搬到最後」等價於「掃描時移除」**——認出這個等價是解題的第一步。\n- **與 502（6.2）的分界**：可行集單調 ⇒ 純貪心；某個選擇會讓未來變差 ⇒ 反悔堆。\n- 攤還 $O(n\\log n)$：每項最多入堆出堆各一次，`while` 不會退化。\n- 累積量一律 `long long`。\n",
        },
        {
          id: 46,
          title: "6.6 懶刪除堆",
          summary:
            "## 這個技術解決什麼問題\n\n`priority_queue` 只能 $O(1)$ 看堆頂、$O(\\log n)$ 彈堆頂。**它沒有「刪掉中間某個元素」或「修改某個元素」的介面**——找到那個元素本身就要 $O(n)$。\n\n但很多題偏偏需要：\n\n- **掃描線中，元素會「過期」**：218（天際線問題）——建築的右邊界掃過後，它的高度就該從候選中消失。\n- **同一個 id 會被反覆更新，舊值作廢**：2034（股票價格波動）、3092、3391。\n- **要刪掉指定位置 / 指定值的元素**：2349（設計數字容器系統）、1172（餐盤堆疊）。\n- **堆與另一個結構共同維護同一批資料，另一邊刪了這邊也要刪**：716（最大堆疊，見 4.2）、480（滑動視窗中位數，見 6.7）。\n\n**懶刪除的想法**：*不真的刪，只記下「這個元素已經作廢」；等它浮到堆頂時再丟掉。* 因為**堆頂以外的元素永遠不會被讀到**，作廢的元素躲在裡面完全不影響答案。\n\n**辨識訊號**：需要「動態極值 + 刪除任意元素」，而且**刪除是單向的（作廢後不會復活）**。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「懶刪除堆」想：\n\n- **需要反覆查詢最大／最小值，同時資料又會被更新或刪除。** 若只增不刪，普通堆就夠；一旦要改 / 刪某個既有元素，才需要懶刪除。\n- **你想刪掉堆中間的某個元素，卻發現 `priority_queue` 辦不到。** 這正是這個技巧存在的理由：\n\n:::example 嗅到味道的瞬間：你想做這件事，但標準堆做不到\n```cpp\n// 嗅到味道的瞬間：你想做這件事，但標準堆做不到\n// pq.remove(someOldValue);   // priority_queue 沒有這個功能，編譯不過\n// 於是改用懶刪除：先留著，等它浮到頂端再判斷要不要丟\n```\n:::\n\n\n\n- **同一個 id 會被多次更新**，每次更新都讓它先前的值作廢——這就是典型的「舊快照需要被視為過期」。\n- **暴力做法是每次查詢都重新掃一遍所有資料找最大／最小（一次 `O(n)`），太慢；** 懶刪除堆把它均攤成 `O(log n)`。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的「真相」存在哪裡？（通常是一個 map）\n2. 我往堆裡推的「快照」長什麼樣？key 是什麼？\n3. 我在哪裡、用什麼條件判斷堆頂「過期」？\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n本節要解決的場景是：我們需要「隨時取出目前的最大值或最小值」，而且資料還會不斷被新增、修改、甚至刪除。\n\n:::example 堆的三個基本操作（預設為 max-heap，top() 取到最大值）\n先複習工具本身。C++ 的 `priority_queue` 就是一個「堆（heap）」，它有三個常用操作，而且都很快：\n\n```cpp\n// 堆的三個基本操作（預設為 max-heap，top() 取到最大值）\npriority_queue<int> pq;\npq.push(5);\npq.push(2);\npq.push(8);\nint mx = pq.top();  // 8：目前最大值，O(1)\npq.pop();           // 移除最大值 8，之後 top() 變成 5，O(log n)\n```\n:::\n\n\n\n但堆有一個關鍵限制：它**只能刪掉「堆頂」那一個元素，不能刪掉「中間任意一個」**。例如堆裡有 {8, 5, 2}，你想單獨刪掉 5，標準 `priority_queue` 做不到。\n\n偏偏很多題目需要這種能力：某個元素的值被改了、過期了、或被別的操作移除了，於是它在堆裡的舊紀錄應該作廢。這時就用「懶刪除（lazy deletion）」。\n\n核心想法只有一句話：**不要真的進堆裡刪，而是先留著它，等它浮到堆頂、又被發現已經過期時，再順手丟掉。**\n\n做法是把資料拆成兩份：\n\n- 一份是「真相（canonical state）」，放在能 `O(1)` 查詢的結構（通常是 `unordered_map`、版本號或 alive 旗標），永遠記錄每個元素目前正確的狀態。\n- 另一份是堆，只保存「快照（snapshot）」——某個時刻看到的候選，可能已經過時。\n\n:::example 懶刪除的清理：把過期的堆頂一路丟掉，直到堆頂有效\n讀答案前先做一次「清理」：比對堆頂快照和真相是否一致，不一致就代表它過期，`pop` 掉再看下一個，直到堆頂有效為止。\n\n```cpp\n// 懶刪除的清理：把過期的堆頂一路丟掉，直到堆頂有效\nwhile (!pq.empty() && isStale(pq.top())) {  // isStale：這個快照和真相不符嗎？\n    pq.pop();  // 過期就丟，不影響後面的正確性\n}\n// 迴圈結束後，pq.top() 一定是「仍然有效」的最大／最小值\n```\n:::\n\n\n\n堆裡雖然堆著一些過期垃圾，但「過期的遲早會浮到頂端被清掉」，所以答案永遠正確；而且每個快照最多被 push 一次、pop 一次，整體仍是 `O(n log n)`。\n\n## 狀態／資料結構定義\n\n三種實作，適用情境不同。\n\n### (A) 雙堆對消——刪除的是「值」\n\n```cpp\npriority_queue<int> pq, del;   // 兩個堆用同一個比較器\nvoid push(int x)   { pq.push(x); }\nvoid erase(int x)  { del.push(x); }          // 只登記，不真刪\nint  top() {\n    while (!del.empty() && del.top() == pq.top()) { del.pop(); pq.pop(); }\n    return pq.top();\n}\n```\n**前提：`del` 中的每個元素都確實在 `pq` 中**（以多重集合計）。使用者必須保證只刪存在的元素。\n\n### (B) 計數表——刪除的是「值」，但不想再開一個堆\n\n```cpp\npriority_queue<int> pq;\nunordered_map<int,int> pending;   // 待刪次數\nint size = 0;\nvoid erase(int x) { ++pending[x]; --size; }\nint  top() {\n    while (!pq.empty()) {\n        int v = pq.top();\n        auto it = pending.find(v);\n        if (it == pending.end() || it->second == 0) return v;\n        if (--it->second == 0) pending.erase(it);\n        pq.pop();\n    }\n    ...\n}\n```\n**(A) 與 (B) 等價**；(B) 省一個堆但多一個雜湊表，實測差不多。**(A) 的優點是不需要 `unordered_map`，在值域大、雜湊碰撞嚴重時更穩。**\n\n### (C) 版本戳記——刪除的是「某個 id 的舊快照」\n\n```cpp\npriority_queue<pair<int,int>> pq;      // (value, id)\nunordered_map<int,int> cur;            // id -> 目前的權威值\nvoid update(int id, int v) { cur[id] = v; pq.push({v, id}); }\npair<int,int> top() {\n    while (!pq.empty() && cur[pq.top().second] != pq.top().first) pq.pop();\n    return pq.top();\n}\n```\n**這是 2034、3092 這類「同一 id 反覆更新」題的標準寫法。** 判斷過期不靠登記，而是**跟一張權威表比對**——這比 (A)/(B) 更省事，因為不需要知道「舊值是多少」。\n\n**(C) 的一個微妙點**：若某 id 的值改成 $v_1$、再改回 $v_0$，堆中早先那個 $(v_0, id)$ 的舊條目會被判定為「有效」。**這不是 bug**——它的值確實等於當前權威值，用它回答完全正確。**「按值比對」在值回到原狀時會意外復活，但復活的那份與現況一致，所以無害。** 若條目還帶有其他欄位（例如時間戳），就必須改用真正的版本號而非值。\n\n## 不變量或正確性證明\n\n### 定理 1（雙堆對消的正確性）\n\n*不變量 (D)*：任一時刻，**真實集合（多重集合）$= \\mathrm{ms}(\\texttt{pq}) - \\mathrm{ms}(\\texttt{del})$**，且 $\\mathrm{ms}(\\texttt{del}) \\subseteq \\mathrm{ms}(\\texttt{pq})$。\n\n*定理*：`top()` 的清理迴圈結束後，`pq.top()` 就是真實集合的最大值。\n\n*證明*。兩個堆使用**同一個比較器**，故 `pq.top()` 是 `pq` 的最大值、`del.top()` 是 `del` 的最大值。\n- 若 `del.top() == pq.top()`：這個最大值有一份已被登記刪除，兩邊同時彈出，(D) 保持（兩邊各減一份相同元素）。\n- 若 `del.top() != pq.top()`：由 $\\mathrm{ms}(\\texttt{del}) \\subseteq \\mathrm{ms}(\\texttt{pq})$ 得 `del.top() <= pq.top()`；不相等即 `del.top() < pq.top()`。於是 `pq.top()` 這個值在 `del` 中一份也沒有（`del` 的最大值都比它小），故它屬於真實集合，且是其中最大者（真實集合 $\\subseteq$ `pq`）。$\\square$\n\n**「兩個堆必須用同一個比較器」是最常被忽略的前提。** 一邊 `less` 一邊 `greater`，這段程式碼會安靜地給出錯誤答案。\n\n### 定理 2（攤還複雜度）\n\n**每個元素最多被推入 `pq` 一次、被彈出一次；`del` 同理。** 故 $q$ 次操作（含 $d$ 次刪除）的堆操作總數 $\\le 2(q + d)$，總時間 $O((q+d)\\log(q+d))$。\n\n*位能論證*：令 $\\Phi = |\\texttt{pq}| + |\\texttt{del}|$。清理迴圈每執行一輪，$\\Phi$ 減 2，而每次 `push`／`erase` 只讓 $\\Phi$ 增 1。**故清理迴圈的總執行次數不超過總推入次數**——`while` 迴圈同樣不會讓複雜度退化。$\\square$\n\n**這與 4.7、5.4、6.5 的攤還論證是同一條。** 看到 `while (…) pop()` 就以為是 $O(n)$ 單次，是反覆出現的誤判。\n\n### 定理 3（懶刪除的適用前提：單向失效）\n\n懶刪除成立需要：**一個元素一旦被判定作廢，就永遠作廢。**\n\n*理由*：清理迴圈把作廢元素**物理刪除**了。若它之後又該變回有效，資料已經沒了。\n\n*檢查表*：\n- (A)(B)：登記刪除後不會撤銷 ✔（`erase` 沒有逆操作）。\n- (C)：判定依據是「$\\ne$ 當前權威值」。**這不是單向的**（值可能改回來），但如前所述，改回來時舊條目與現況一致，仍然正確。**真正危險的是「條目攜帶額外資訊」的情形**——例如 (value, id, timestamp)，值改回來但時間戳不同，用舊條目就錯了。此時必須用嚴格遞增的版本號比對。\n- **4.2 的 1172（餐盤堆疊）** 是弱不變量的例子：堆是「非滿堆疊下標」的**超集合**，讀取時要驗證 `stacks[i].size() < capacity`。單向性由「一個堆疊被填滿後只可能因 `pop` 而變回非滿，而 `pop` 會重新推入」保證。**這裡的單向性不是自動成立的，是靠 `pop` 主動補推入來維持的**，讀 1172 的程式碼時要看清楚這一點。\n\n### 定理 4（218 天際線：事件排序的三條規則就是幾何語意）\n\n把每棟建築 $(l, r, h)$ 拆成兩個事件：$(l, -h)$「進場」與 $(r, +h)$「離場」。所有事件按 `pair` 的字典序升冪排序。\n\n**這個 $\\pm h$ 的編碼一次解決三個並列情形，每一條都對應一個幾何事實：**\n\n1. **同一 $x$ 上，進場排在離場之前**（因為 $-h < 0 < +h$）。\n   *理由*：兩棟建築在同一點相接時，天際線不應該掉到地面再升起來。\n2. **同一 $x$ 上的多個進場，高的排前面**（$-h$ 小者在前 ⇔ $h$ 大者在前）。\n   *理由*：高的先進場，較矮者進場時不會產生新的關鍵點，避免輸出重複點。\n3. **同一 $x$ 上的多個離場，矮的排前面**（$+h$ 小者在前）。\n   *理由*：矮的先離場不改變當前最大高度；高的最後離場，只產生一個關鍵點。\n\n*演算法*：大頂堆存「目前在場的高度」，離場事件用懶刪除登記。每處理完一個 $x$ 上的所有事件後，取清理後的堆頂 $H$（空則 0）；**若 $H \\ne$ 上一次記錄的高度，就輸出 $(x, H)$。**\n\n*不變量*：處理完 $x$ 上的所有事件後，堆（清理後）恰為「所有 $l \\le x < r$ 的建築高度」的多重集合，故堆頂即該點的天際線高度。\n\n*為什麼「$H \\ne$ 前一次」這個判斷就足以去除多餘點*：關鍵點的定義是「高度發生變化的位置」。因為我們在**每個 $x$ 的所有事件處理完之後**才讀取一次高度，中間的臨時狀態不會外洩。$\\square$\n\n**218 是「掃描線 + 懶刪除堆」的教科書題**，值得完整寫一遍。另解是用 `multiset<int>`（`erase(find(h))` 精確刪除）——程式碼更短，但常數更大。\n\n### 定理 5（什麼時候不該用懶刪除）\n\n**空間**是懶刪除唯一的真實代價：**作廢的條目在浮到堆頂之前都不會被回收。**\n\n- *最壞情形*：某個 id 被更新 $m$ 次但從未成為堆頂，堆中就躺著 $m-1$ 個垃圾。$q$ 次操作最壞 $O(q)$ 空間，**即使真實集合只有 1 個元素。**\n- *緩解*：記錄垃圾數 `garbage`，當 `garbage > pq.size() / 2` 時**整堆重建**（把有效元素倒出來 `make_heap`）。重建 $O(n)$，且兩次重建之間至少產生 $n/2$ 個垃圾，**攤還 $O(1)$**。\n- *替代方案*：`std::multiset`／`std::map` 支援 $O(\\log n)$ 精確刪除，**空間恆等於真實大小**。2034（股票價格波動）與 2349（設計數字容器系統）用 `multiset`／`map<int, set<int>>` 都比懶刪除堆短且無垃圾問題。\n\n**判準**：\n| 情境 | 推薦 |\n| --- | --- |\n| 刪除次數遠少於插入，或元素本來就會很快浮上堆頂（掃描線） | 懶刪除堆 |\n| 刪除頻繁、記憶體吃緊、或需要 `lower_bound` | `multiset` / `map` |\n| 需要 decrease-key（Dijkstra 稠密圖） | 索引堆，或直接懶刪除（重複入堆） |\n\n**Dijkstra 的標準寫法其實就是懶刪除**：不做 decrease-key，而是重複推入 $(dist, v)$，彈出時檢查 `d > dist[v]` 就跳過。**「重複入堆 + 出堆時驗證」是懶刪除最常見的化身，只是大家不這麼叫它。**\n\n## 逐步演算法\n\n任何懶刪除堆題目都可拆成四個固定步驟，逐一搭配程式碼：\n\n:::example 狀態表示：current 是真相，pq 是快照堆\n**1. 狀態表示：真相與快照分開。** 真相放在能 `O(1)` 查到「某 id 現在正確值」的結構；堆只放快照。\n\n```cpp\n// 狀態表示：current 是真相，pq 是快照堆\nunordered_map<int, int> current;    // id -> 目前正確的值（真相）\npriority_queue<pair<int, int>> pq;  // {value, id} 快照，可能過期\n```\n:::\n\n\n\n:::example 更新：改真相 + 推新快照（不刪舊的）\n**2. 更新流程：只改真相、只往堆裡加。** 更新時永遠只動真相，再把新快照 push 進堆；絕不回頭去堆裡找舊快照刪除（那很慢，正是本技巧要避免的事）。\n\n```cpp\n// 更新：改真相 + 推新快照（不刪舊的）\nvoid update(int id, int value) {\n    current[id] = value;   // 1) 更新真相\n    pq.push({value, id});  // 2) 推入新快照；舊快照留著之後再清\n}\n```\n:::\n\n\n\n:::example 讀取：清掉過期堆頂後，回傳有效的最大值\n**3. 答案讀取：讀之前先清理。** 每個會用到 `top()` 的地方都先跑清理迴圈。判斷「過期」的方法是：快照記的值和真相現在的值不一樣。\n\n```cpp\n// 讀取：清掉過期堆頂後，回傳有效的最大值\nint queryMax() {\n    while (!pq.empty()) {\n        auto [value, id] = pq.top();\n        if (current[id] == value) {  // 快照與真相一致 → 有效\n            return value;\n        }\n        pq.pop();  // 不一致 → 過期，丟掉\n    }\n    return -1;  // 堆空，沒有有效元素\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 所有「讀取入口」都要清理，漏掉一個就可能回傳到過期答案。\n- 判斷過期要用「完整的鍵」：若值可能相同（例如價格一樣但時間不同），只比值會誤判，要連 id / timestamp 一起比。\n- 複雜度：每個快照最多進出堆一次，n 次操作合計 `O(n log n)`；空間最壞 `O(操作數)`，因為過期快照被清掉前都還佔著位子。\n\n## C++17 模板\n\n以下用 2349. 設計數字容器系統 展示懶刪除堆的核心寫法。\n\n**完整題目**：支援 `change(index, number)` 把某個 index 指派給 number，並支援 `find(number)` 回傳目前指派給 number 的最小 index；若不存在則回傳 -1。\n\n:::example current：記錄每個 index 目前對應的 number\n**線索**：`change` 會讓舊的 `(number, index)` 關係失效，但 C++ `priority_queue` 不能從堆中間刪掉舊 index。只要把 `index -> number` 當作唯一真相，舊 index 留在堆裡，等 `find` 時清理即可。\n\n```cpp\nclass NumberContainers {\n    // current：記錄每個 index 目前對應的 number\n    unordered_map<int, int> current;\n    // heaps：每個 number 對應一個最小堆，儲存所有曾被分配的 index（含過時的）\n    unordered_map<int, priority_queue<int, vector<int>, greater<int>>> heaps;\n\npublic:\n    void change(int index, int number) {\n            current[index] = number;    // 更新 index 的當前值\n            heaps[number].push(index);  // 懶惰式：只加入不立即清除舊紀錄\n    }\n\n    int find(int number) {\n            auto it = heaps.find(number);\n            if (it == heaps.end()) {\n                    return -1;\n            }\n\n            auto& heap = it->second;\n            // 懶惰刪除：跳過堆頂中已不再對應此 number 的過時 index\n            while (!heap.empty()) {\n                    int index = heap.top();\n                    auto currentIt = current.find(index);\n                    if (currentIt != current.end() && currentIt->second == number) {\n                            return index;  // 找到最小且有效的 index\n                    }\n                    heap.pop();  // 淘汰過時紀錄\n            }\n            return -1;\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- `current[index]` 是 canonical state，表示 index 目前真正屬於哪個 number。\n- `heaps[number]` 是候選集合，堆頂理論上是最小 index，但可能是舊資料。\n- `change` 不刪舊資料，只更新 `current` 並推入新的 index，因此單次是 `O(log m)`。\n- `find` 的 while 迴圈就是 lazy deletion：堆頂若已不屬於這個 number，就彈掉並繼續看下一個候選。\n- while 結束後，留下的堆頂一定同時滿足「在堆頂最小」與「仍符合 current 狀態」。\n\n## 時間與空間複雜度\n\n| 操作 | 懶刪除堆 | `multiset` |\n| --- | --- | --- |\n| `push` | $O(\\log n)$ | $O(\\log n)$ |\n| `erase(x)` | $O(\\log n)$（只登記） | $O(\\log n)$（`erase(find(x))`） |\n| `top` | 攤還 $O(\\log n)$ | $O(1)$（`*rbegin()`） |\n| 空間 | $O(\\text{總操作數})$ 最壞 | $O(\\text{真實大小})$ |\n| 常數 | 小（連續記憶體） | 大 3–5 倍（紅黑樹節點） |\n\n| 題目 | 時間 | 空間 |\n| --- | --- | --- |\n| 218 天際線問題 | $O(n\\log n)$ | $O(n)$ |\n| 2034 股票價格波動 | 每次操作 $O(\\log n)$ | $O(n)$（懶刪除最壞） |\n| 2349 數字容器系統 | 每次操作 $O(\\log n)$ | $O(n)$ |\n| 1172 餐盤堆疊 | 攤還 $O(\\log n)$ | $O(n)$ |\n| Dijkstra（懶刪除版） | $O(E\\log E)$ | $O(E)$ |\n\n**注意 Dijkstra 是 $O(E\\log E)$ 而非 $O(E\\log V)$**——因為堆中最多有 $E$ 個條目（每條邊可能推入一次）。**兩者只差常數（$\\log E \\le 2\\log V$），但答題時說清楚更精準。**\n\n**`erase(find(x))` vs `erase(x)`**：`multiset::erase(x)` 會刪掉**所有**等於 $x$ 的元素，`erase(find(x))` 只刪一個。**這是 `multiset` 用於懶刪除替代方案時最常見的錯誤**（2.7 已提過同一件事，值得再記一次）。\n\n## 常見錯誤與邊界條件\n\n- 不要把堆本身當作真實狀態；堆只負責快速找候選。\n- 每個會讀取 `top()` 的函式，都要先呼叫清理邏輯或內嵌 while 清理。\n- 判斷過期時要用完整 key，例如 price 相同時仍需檢查 timestamp。\n- 若過期元素累積過多但查詢很少，記憶體會增加；通常仍符合操作數上界，必要時可改用 `set`/`multiset`。\n- C++ 小根堆常寫成 `priority_queue < T, vector < T >, greater < T >>`。\n\n## 常見變形\n\n- 用最大堆與最小堆各存一份快照，查詢時各自清理，例如股票價格最大/最小。\n- 堆元素帶版本號，更新時版本加一，堆頂版本不等於目前版本就過期。\n- 掃描線中堆元素帶右邊界，當右邊界已越過目前位置就過期。\n- 雙端結構或多個堆共用一份 alive 標記，從任一端刪除後，其他堆等到查詢時再清理。\n\n## 與相似技巧的比較\n\n- **與 6.1 基礎堆**：懶刪除只是在基礎堆外面加一層「有效性驗證」。**核心洞見是「堆頂以外的元素從不被讀取，所以垃圾躲在裡面無害」。**\n- **與 `multiset`**：功能上 `multiset` 全面覆蓋懶刪除堆，還多了 `lower_bound`。**懶刪除的唯一優勢是常數與快取局部性**；在 $10^5 \\sim 10^6$ 量級且時限緊時，這個優勢是實質的。\n- **與 5.4 單調佇列**：單調佇列的「頭端過期」也是一種刪除，但它**不需要懶刪除**——因為過期元素恆為前綴，可以直接彈。**「被刪的恆為某一端的前綴」⇒ 用佇列；「被刪的位置任意」⇒ 懶刪除或 `multiset`。**\n- **與 4.2 的 1172 / 716**：4.2 從「堆疊設計」的角度看同一個技巧。**716 的 `popMax` 之所以要放棄輔助堆疊改用 `list + map`，正是因為堆疊的懶刪除會破壞 LIFO 順序**——懶刪除只對「無序容器 + 只讀極值」安全。\n- **與 6.7 對頂堆**：480（滑動視窗中位數）必須在兩個堆上同時做懶刪除，**額外的難點是「兩邊的有效大小」不再等於 `heap.size()`**，必須另外用計數器追蹤。見 6.7。\n- **與並查集（8.x）**：若「刪除」的語意是「這個位置用掉了，下次找下一個可用的」，並查集的「指向下一個可用位置」寫法是 $O(\\alpha)$，比任何堆都快。**判準：刪除後要找的是「相鄰的下一個」而非「全域極值」。**\n- **與離線處理（12.x）**：若所有刪除時機事先已知，可以離線排序後用掃描線一次處理，完全避開刪除。**能離線就離線，這通常是最快的路。**\n\n## 代表例題\n\n**題號**：218、716、1172、2034、2349\n\n掃描事件時把建築的高度和右邊界放進最大堆；每個 x 先清掉 `right <= x` 的過期建築，再用堆頂高度決定輪廓。\n\n- 716 最大堆疊（Premium）：同時支援棧頂和最大值時，可用節點 id/版本標記刪除；從棧或堆取值前都要跳過已失效節點。\n- 1172 餐盤堆疊：用堆維護可插入的最左棧與可彈出的最右棧；棧滿、棧空或下標越界的候選在查詢時延遲清理。\n- 2034 股票價格波動：`timestamp -> price` 是真實價格表；最大堆和最小堆保存價格快照，查最大/最小前清掉價格已被更新的 timestamp。\n- 2349 設計數字容器系統：`index -> number` 是真實狀態；每個 number 一個最小堆保存候選 index，`find` 前清掉已改成其他 number 的 index。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 218 | [天際線問題](https://leetcode.cn/problems/the-skyline-problem) |  |\n| 716 | [最大堆疊](https://leetcode.cn/problems/max-stack)（Premium） |  |\n| 1172 | [餐盤堆疊](https://leetcode.cn/problems/dinner-plate-stacks) | 2110 |\n| 2034 | [股票價格波動](https://leetcode.cn/problems/stock-price-fluctuation) | 1832 |\n| 2349 | [設計數字容器系統](https://leetcode.cn/problems/design-a-number-container-system) | 1540 |\n| 2353 | [設計食物評分系統](https://leetcode.cn/problems/design-a-food-rating-system) | 1782 |\n| 3092 | [最高頻率的 ID](https://leetcode.cn/problems/most-frequent-ids) | 1793 |\n| 3391 | [設計一個高效的層跟蹤三維二進位制矩陣](https://leetcode.cn/problems/design-a-3d-binary-matrix-with-efficient-layer-tracking) |  |\n| 3408 | [設計工作管理員](https://leetcode.cn/problems/design-task-manager) | 1807 |\n| 3510 | [移除最小數對使陣列有序 II](https://leetcode.cn/problems/minimum-pair-removal-to-sort-array-ii) | 2608 |\n| 3607 | [電網維護](https://leetcode.cn/problems/power-grid-maintenance) | 1700 |\n| 3672 | [子陣列中加權眾數的總和](https://leetcode.cn/problems/sum-of-weighted-modes-in-subarrays) |  |\n| 3815 | [設計拍賣系統](https://leetcode.cn/problems/design-auction-system) | 1854 |\n\n## 本節重點速查\n\n- **核心洞見：堆頂以外的元素從不被讀取，所以作廢的元素躲在堆裡無害——等它浮上來再丟。**\n- 三種寫法：**(A) 雙堆對消**（刪值）、**(B) 計數表**（刪值）、**(C) 版本戳記**（刪某 id 的舊快照，跟權威表比對）。**同一 id 反覆更新就用 (C)。**\n- **(A) 的兩個前提**：兩個堆用**同一個比較器**、`del` 是 `pq` 的子多重集合。前者寫錯會安靜地給錯答案。\n- **適用前提是「單向失效」**（定理 3）：作廢後不復活。(C) 按值比對時值可能改回來，但那份與現況一致，無害；**若條目攜帶額外欄位就必須用真正的版本號。**\n- **攤還 $O(\\log n)$**：每個條目最多入堆出堆各一次；`while` 清理迴圈不會退化。\n- **唯一的代價是空間**：垃圾最壞 $O(\\text{操作數})$。緩解是「垃圾過半就整堆重建」（攤還 $O(1)$）。\n- **218 的事件編碼 $(l, -h)$／$(r, +h)$ 一次解決三種並列**：進場先於離場、進場高者先、離場矮者先——每條都對應一個幾何事實。**每個 $x$ 的事件全部處理完才讀一次高度，才能正確去重。**\n- **Dijkstra 的「重複入堆 + 出堆驗證」就是懶刪除**，複雜度 $O(E\\log E)$。\n- `multiset::erase(x)` 刪全部，`erase(find(x))` 只刪一個。\n- **判準**：刪除的是「某端的前綴」⇒ 佇列；「全域極值 + 任意刪除且刪除不多」⇒ 懶刪除堆；「刪除頻繁或要 `lower_bound`」⇒ `multiset`；「刪除時機全部已知」⇒ 離線掃描。\n",
        },
        {
          id: 47,
          title: "6.7 對頂堆（滑動視窗第 K 小/大）",
          summary:
            "## 這個技術解決什麼問題\n\n資料在動態變化（一直加入，或視窗滑動進出），卻要隨時查詢**排序後某個固定位置的值**——中位數、第 $K$ 小、分界值。\n\n- **295 資料流的中位數**：只增不刪，查中位數。\n- **480 滑動視窗中位數**：固定寬度視窗，有進有出。\n- **1825 求出 MK 平均值**：去掉最小 $k$ 個與最大 $k$ 個後求平均——需要**分界 + 區段和**。\n- **2102 序列順序查詢**：分界位置本身會隨查詢次數移動。\n- **LCP 24 / 462 / 296**：目標函數是 $\\sum |x_i - c|$，最優的 $c$ 就是中位數（定理 4）。\n\n**核心想法**：*用兩個堆把序列切成「前半」與「後半」，讓要查的那個位置恰好卡在兩個堆的堆頂。* 前半用**大頂堆**（堆頂是前半的最大者），後半用**小頂堆**（堆頂是後半的最小者），**分界線兩側的臨界元素就都是 $O(1)$ 可得。**\n\n**暴力是每次操作重排 $O(n\\log n)$，對頂堆把單次操作壓到 $O(\\log n)$。**\n\n**辨識訊號**：要查的是「排序後第 $p$ 個」，而 $p$ 由總數決定（$n/2$）或由題目給定，且資料動態變動。**若 $p$ 每次查詢都不同、任意指定，對頂堆就不適用**（見「與相似技巧的比較」）。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「對頂堆」想：\n\n- **資料動態變化（持續加入，或滑動視窗進出），卻要隨時查「中位數 / 第 K 小/大 / 分界值」。** 像 295 資料流中位數、480 滑動視窗中位數正是教科書例子。\n- **答案落在「排序後的某個固定位置」，而你不想每次重排。** 用兩個堆把那個位置卡在中間：\n\n:::example 嗅到味道：你只在乎「正中間」或「第 K 名」那個分界，不需要完整排序\n```cpp\n// 嗅到味道：你只在乎「正中間」或「第 K 名」那個分界，不需要完整排序\n// median = 兩堆頂；不必 sort 全部\n```\n:::\n\n\n\n- **需要在動態集合上做「前 K 大求和 / 去掉極端值後的平均」**（如 1825 求 MK 平均值），用對頂堆維護分界，兩側再各自維護 sum 即可 `O(1)` 取總和。\n- **暴力做法是每次操作都重新排序 `O(n log n)`**，對頂堆把單次操作壓成 `O(log n)`。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的左堆、右堆各是max-heap 還是 min-heap？哪個堆頂對應我要的分界值？\n2. 我維持的兩條不變式（左≤右、大小差）是什麼？rebalance 怎麼搬堆頂？\n3. 若要刪除，我怎麼懶刪除？用什麼「有效大小」來判斷平衡？\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n本節要解決的場景是：資料一直在變（加入新數、或滑動視窗滑掉舊數），而我們要隨時回答「中位數是多少」或「第 K 小/大是多少」。每次都重新排序太慢，需要一種能「動態維護排名分界」的結構。\n\n核心想法叫「對頂堆（two heaps）」：**把所有數從中間切成兩半，左半放小的、右半放大的，分界線正好落在我們要的位置（中位數或第 K 名）。** 只要兩堆隨時保持平衡，分界值就能 `O(1)` 讀出。\n\n:::example 左 max-heap + 右 min-heap，分界值就在兩堆頂\n具體用兩個堆：左半用**max-heap**（堆頂是左半最大者，也就是「左邊界」），右半用**小根堆**（堆頂是右半最小者，也就是「右邊界」）。\n\n```cpp\n// 左 max-heap + 右 min-heap，分界值就在兩堆頂\npriority_queue<int> lo;  // 左半：max-heap，top() 是左半最大\npriority_queue<int, vector<int>, greater<int>>\n        hi;  // 右半：小根堆，top() 是右半最小\n                  // 不變式：lo 裡每個數 <= hi 裡每個數；且 lo.size() 與 hi.size()\n                  // 差不超過 1\n```\n:::\n\n\n\n只要維持兩個不變式：(1) 左半所有數 ≤ 右半所有數；(2) 兩堆大小相差不超過 1，那麼：\n\n- 總數為奇數時，較大那一堆的堆頂就是中位數；\n- 總數為偶數時，兩個堆頂的平均就是中位數。\n\n:::example 讀中位數：看兩堆大小決定\n```cpp\n// 讀中位數：看兩堆大小決定\ndouble median() {\n    if (lo.size() > hi.size()) {\n        return lo.top();  // 奇數，中位在左堆頂\n    }\n    return (lo.top() + hi.top()) / 2.0;  // 偶數，取兩堆頂平均\n}\n```\n:::\n\n\n\n新數來時，先丟到某一側，再「rebalance」：若哪邊多了，就把那邊的堆頂搬到另一邊，維持上面兩條不變式。**整個技巧就是用兩個堆夾住分界線，靠搬堆頂保持平衡。** 每次插入只動到堆頂幾下，是 `O(log n)`，遠勝每次重排序。\n\n若是滑動視窗（要刪掉滑出去的舊數），由於堆不能刪中間元素，搭配「懶刪除」：先記下「待刪」，等它浮到堆頂再真正丟掉，並用一個有效 size 來修正大小判斷。\n\n## 狀態／資料結構定義\n\n```cpp\npriority_queue<int> lo;                                    // 前半，大頂堆\npriority_queue<int, vector<int>, greater<int>> hi;         // 後半，小頂堆\n```\n\n**兩條不變量**（4.6 定理 3 已證，此處直接沿用）：\n- **(P1)** $\\max(\\texttt{lo}) \\le \\min(\\texttt{hi})$——`lo` 的每個元素都不大於 `hi` 的每個元素。\n- **(P2)** $|\\texttt{lo}| = |\\texttt{hi}|$ 或 $|\\texttt{lo}| = |\\texttt{hi}| + 1$。\n\n在 (P1)(P2) 之下：\n- $n$ 奇數 ⇒ 中位數 $=$ `lo.top()`；\n- $n$ 偶數 ⇒ 中位數 $= (\\texttt{lo.top()} + \\texttt{hi.top()}) / 2.0$。\n\n**「第 $K$ 小」的版本**：把 (P2) 換成 $|\\texttt{lo}| = K$（固定），則 `lo.top()` 恆為第 $K$ 小。**中位數只是 $K = \\lceil n/2\\rceil$ 的特例。**\n\n**帶區段和的版本（1825）**：除了兩個堆，額外維護 `sumLo`、`sumHi`，每次搬動元素時同步加減。**這是「$O(1)$ 取得前 $K$ 大之和」的標準做法**，比每次遍歷堆快得多。1825 需要三段（最小 $k$ / 中間 / 最大 $k$），就是三個容器加三個和。\n\n**帶懶刪除的版本（480）**——這是本節唯一真正困難的部分：\n```cpp\npriority_queue<int> lo;\npriority_queue<int, vector<int>, greater<int>> hi;\nunordered_map<int,int> delayed;   // 待刪的值 -> 次數\nint balance = 0;                  // 有效大小之差：|lo|_eff - |hi|_eff\n```\n**`lo.size()` 包含垃圾，絕對不能拿來做平衡判斷**——必須用 `balance` 這個獨立計數器。**這是 480 的全部難點，也是最常見的錯誤來源。**\n\n## 不變量或正確性證明\n\n### 定理 1（分界不變量與中位數的正確性）\n\n在 (P1)(P2) 之下，把 `lo` 的元素由大到小、`hi` 的元素由小到大排列，整體就是原集合的排序。設 $n = |\\texttt{lo}| + |\\texttt{hi}|$。\n\n- 由 (P1)，`lo` 中的元素在排序中全部排在 `hi` 之前，故 **`lo.top()` 是排序後的第 $|\\texttt{lo}|$ 個**（1-indexed），**`hi.top()` 是第 $|\\texttt{lo}|+1$ 個**。\n- 由 (P2)，$|\\texttt{lo}| = \\lceil n/2\\rceil$。$n$ 奇數時 $\\lceil n/2\\rceil = (n+1)/2$ 正是中位數位置 ✔；$n$ 偶數時第 $n/2$ 與第 $n/2+1$ 個分別是兩個堆頂 ✔。$\\square$\n\n**注意這裡真正被使用的只有 (P1)**——(P2) 只是把「第 $|\\texttt{lo}|$ 個」調到我們想要的位置。**所以要查第 $K$ 小，只要把 (P2) 改成 $|\\texttt{lo}| = K$，其餘完全不動。** 認清這一點，2102（分界位置隨查詢移動）就只是「每次查詢後把 (P2) 的目標值加一」。\n\n### 定理 2（插入的三行寫法為什麼保持 (P1)(P2)）\n\n```cpp\nlo.push(x);                                  // (1)\nhi.push(lo.top()); lo.pop();                 // (2)\nif (hi.size() > lo.size()) { lo.push(hi.top()); hi.pop(); }   // (3)\n```\n\n*證明*（4.6 定理 3 的複述，因為它是本節的地基）。\n- (1) 之後 `lo` 可能違反 (P1)（$x$ 可能大於 `hi` 的某些元素）。\n- (2) 把 `lo` 的**最大者**移到 `hi`。移走之後，`lo` 剩下的元素都 $\\le$ 那個最大者 $\\le$ ⋯ 需要它 $\\le \\min(\\texttt{hi})$：新的 $\\min(\\texttt{hi})$ 就是剛移進去的那個或原本的 $\\min$，兩者都 $\\ge$ `lo` 的所有剩餘元素 ✔。**(P1) 恢復。**\n- (2) 之後大小關係可能變成 $|\\texttt{hi}| = |\\texttt{lo}| + 1$，(3) 把一個搬回來，恢復 (P2)。搬的是 `hi` 的最小者，搬到 `lo` 後它是 `lo` 的最大者，仍 $\\le$ 剩下的 `hi` ✔ **(P1) 保持。**$\\square$\n\n**這個寫法沒有 `if/else` 分支，是它值得記住的原因**——「先無條件過一手，再修平衡」比「先比較 $x$ 與堆頂再決定放哪邊」少一類邊界錯誤（例如 `lo` 為空時 `lo.top()` 是 UB）。\n\n### 定理 3（480 滑動視窗中位數：懶刪除下的平衡）\n\n**問題所在**：懶刪除之後，`lo.size()` 是「有效元素 + 垃圾」的總數。**用它做平衡判斷會把垃圾算進去，導致分界線跑掉，中位數錯誤。**\n\n**解法**：另外維護 `balance` $= |\\texttt{lo}|_{\\text{eff}} - |\\texttt{hi}|_{\\text{eff}}$，所有操作都只更新 `balance`，永遠不看 `size()`。\n\n```cpp\nauto prune = [&](auto& h) {                       // 清掉堆頂的垃圾\n    while (!h.empty()) {\n        auto it = delayed.find(h.top());\n        if (it == delayed.end()) break;\n        if (--it->second == 0) delayed.erase(it);\n        h.pop();\n    }\n};\nauto rebalance = [&]() {\n    if (balance > 1)      { hi.push(lo.top()); lo.pop(); balance -= 2; prune(lo); }\n    else if (balance < 0) { lo.push(hi.top()); hi.pop(); balance += 2; prune(hi); }\n};\nauto insert = [&](int x) {\n    if (lo.empty() || x <= lo.top()) { lo.push(x); ++balance; }\n    else                             { hi.push(x); --balance; }\n    rebalance();\n};\nauto erase = [&](int x) {\n    ++delayed[x];\n    if (x <= lo.top()) { --balance; if (x == lo.top()) prune(lo); }\n    else               { ++balance; if (x == hi.top()) prune(hi); }\n    rebalance();\n};\n```\n\n*不變量*：\n- **(E1)** `balance` 恆等於有效元素的個數差，且操作結束後 $\\in \\{0, 1\\}$；\n- **(E2)** 兩個堆的**堆頂都是有效元素**（`prune` 保證）；\n- **(E3)** (P1) 對有效元素成立。\n\n*為什麼 `erase` 只在 `x == top` 時才 `prune`*：若 $x$ 不是堆頂，它躺在堆中間，**根本不影響堆頂的讀取**，等它自己浮上來再清即可。**這正是懶刪除的核心（6.6）：垃圾只有在堆頂才礙事。**\n\n*為什麼 `erase` 用 `x <= lo.top()` 判斷歸屬是對的*：由 (E2)，`lo.top()` 是有效的、且是 `lo` 中有效元素的最大者。由 (E3)，$x \\le \\texttt{lo.top()}$ ⟺ $x$ 屬於 `lo`（若 $x$ 屬於 `hi` 則 $x \\ge \\min(\\texttt{hi}) \\ge \\texttt{lo.top()}$，相等時歸哪邊都可以，因為值相同）。**相等時的歸屬歧義無害——刪掉的是同一個值，多重集合的結果相同。** $\\square$\n\n*為什麼 `rebalance` 之後要再 `prune`*：搬動會讓新的堆頂暴露出來，而它可能是垃圾。**漏掉這個 `prune`，下一次的 `x <= lo.top()` 判斷就會用到一個已刪除的值。**\n\n**480 值得完整手寫一遍的理由**：它把「懶刪除」（6.6）與「對頂堆」（本節）疊在一起，兩層不變量互相牽制。**寫之前先把 (E1)(E2)(E3) 抄在紙上，寫完逐條檢查每個分支是否維持——這是唯一可靠的方法。**\n\n*更短的替代解*：`multiset` + 一個指向中位數的迭代器。插入／刪除後把迭代器 `++`／`--` 修正。$O(\\log k)$、程式碼一半長度、沒有垃圾。**面試或比賽時建議用這個；對頂堆版本適合用來練不變量。**（刪除時若刪到迭代器本身，要先移動迭代器再 `erase`。）\n\n### 定理 4（中位數最小化 $\\sum|x_i - c|$）\n\n**定理**：$f(c) = \\sum_{i=1}^{n} |x_i - c|$ 在 $c$ 取任一中位數時達到最小。\n\n*證明*。$f$ 是有限個絕對值函數之和，故**凸**且**分段線性**。在區間 $(x_{(j)}, x_{(j+1)})$ 上，\n$$f'(c) = \\#\\{i : x_i < c\\} - \\#\\{i : x_i > c\\} = j - (n - j) = 2j - n .$$\n$f'$ 隨 $j$ 遞增，符號在 $j = n/2$ 處由負轉正。\n- $n$ 奇數：$f'$ 在 $c < x_{((n+1)/2)}$ 時為負、之後為正 ⇒ 唯一最小點 $c = x_{((n+1)/2)}$。\n- $n$ 偶數：$f' = 0$ 於 $[x_{(n/2)}, x_{(n/2+1)}]$ ⇒ 整段皆為最小點。$\\square$\n\n*推論（$O(1)$ 求最小值）*：用對頂堆同時維護 `sumLo`、`sumHi`，令 $m = \\texttt{lo.top()}$、$a = |\\texttt{lo}|$、$b = |\\texttt{hi}|$，則\n$$f(m) = \\big(a\\cdot m - \\texttt{sumLo}\\big) + \\big(\\texttt{sumHi} - b\\cdot m\\big) .$$\n**「維護分界 + 維護兩側和」讓每次查詢從 $O(n)$ 降到 $O(1)$**，這是 462、296、LCP 24、以及各種「把陣列變成等差／全相等的最小代價」題的共同骨架。\n\n**變形提醒**：若要變成「相鄰元素相差 1 的遞增序列」，先做 $x_i \\leftarrow x_i - i$ 的變換再取中位數（把等差問題化歸為全相等問題）。**變換之後定理 4 直接適用**——這是這一族題唯一的技巧。\n\n### 定理 5（1825 MK 平均值：三段結構）\n\n要求「去掉最小 $k$ 個與最大 $k$ 個後的平均」，視窗大小 $m$。維護三個容器：\n- `small`：最小的 $k$ 個（大頂堆）\n- `mid`：中間的 $m - 2k$ 個（雙端有序結構，或兩個堆）\n- `large`：最大的 $k$ 個（小頂堆）\n\n以及 `sumMid`。\n\n*不變量*：$\\max(\\texttt{small}) \\le \\min(\\texttt{mid})$ 且 $\\max(\\texttt{mid}) \\le \\min(\\texttt{large})$，且 $|\\texttt{small}| = |\\texttt{large}| = k$（在元素數 $\\ge m$ 時）。\n\n*插入／刪除的處理*：新元素先按值放進正確的容器，然後**逐級搬動**恢復大小約束（`small` 太大就把最大者推給 `mid`，依此類推）。**每次操作最多搬動 $O(1)$ 個元素**（因為每個容器的大小最多偏差 1），故仍是 $O(\\log m)$。\n\n**實作建議**：這題用 `multiset` + 三個迭代器（或 `multiset` + 前綴和）遠比三個堆好寫。**當「段數 $\\ge 3$」時，對頂堆的寫法會爆炸，改用有序容器。** 這是對頂堆的實際適用邊界。\n\n## 逐步演算法\n\n:::example 兩個堆 + 不變式（如上）\n**1. 狀態表示：左 max-heap + 右 min-heap。** 約定好「左半 ≤ 右半」「左堆大小 ≥ 右堆大小（最多多 1）」這兩個不變式。\n\n```cpp\n// 兩個堆 + 不變式（如上）\npriority_queue<int> lo;                             // 小的一半\npriority_queue<int, vector<int>, greater<int>> hi;  // 大的一半\n```\n:::\n\n\n\n:::example 插入一個數並重新平衡\n**2. 插入流程：先入一側，再 rebalance。** 常見寫法：先放進 lo，把 lo 堆頂（可能太大）推給 hi，若 hi 變多再把 hi 堆頂推回 lo，最後大小恰好滿足不變式。\n\n```cpp\n// 插入一個數並重新平衡\nvoid add(int x) {\n    lo.push(x);\n    hi.push(lo.top());\n    lo.pop();                     // 把左半最大推到右半，保證左<=右\n    if (hi.size() > lo.size()) {  // 右半多了\n        lo.push(hi.top());\n        hi.pop();  // 推回去，維持 lo>=hi\n    }\n}\n```\n:::\n\n\n\n:::example 標記待刪，並依所屬側調整有效大小（balance）\n**3. 刪除（滑動視窗）：懶刪除 + 修正有效大小。** 刪除時先判斷它屬於哪一側、調整該側的「有效大小」並登記待刪；之後在用到堆頂前清掉已被標記的過期堆頂。\n\n```cpp\n// 標記待刪，並依所屬側調整有效大小（balance）\nunordered_map<int, int> toDel;  // 值 -> 待刪次數\nvoid remove(int x) {\n    toDel[x]++;\n    if (x <= lo.top()) {\n        loSize--;  // 它原屬左側\n    } else {\n        hiSize--;  // 它原屬右側\n    }\n}\n// 讀堆頂前先清：把堆頂已標記者真正 pop 掉\nauto prune = [&](auto& pq) {\n    while (!pq.empty() && toDel[pq.top()] > 0) {\n        toDel[pq.top()]--;\n        pq.pop();\n    }\n};\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 平衡時要用「有效大小（loSize / hiSize）」判斷，而不是堆的實際長度——因為堆裡可能還躺著未清的過期元素。\n- 偶數筆資料取平均時小心溢位，必要時轉成 `long long` 或 `double` 再相加。\n- 複雜度：每次插入/刪除 `O(log n)`，查中位數 `O(1)`；滑動視窗整體 `O(n log n)`。\n\n## C++17 模板\n\n以下用 2102. 序列順序查詢 展示對頂堆（用兩個堆維護第 K 名分界）的核心寫法。\n\n**完整題目**：設計 SORTracker：`add(name, score)` 加入一個地點（分數越高越好，分數相同時名字字典序越小者越好）；`get()` 第 i 次呼叫時，回傳目前所有地點中第 i 好的名字。\n\n:::example ByWorst：分數低或同分名字大的排前面（最差優先，用於 better 堆的堆頂 =…\n**線索**：每次 `get` 取的名次會逐次往後（第 1、2、3 好…）。用「對頂堆」把已查到的前段與其餘候選用兩個堆夾住交界：較好的一側用小根堆（堆頂是其中最差），其餘用 max-heap（堆頂是其中最好），交界即下一個答案。\n\n```cpp\nclass SORTracker {\n    struct Loc {\n            int score;\n            string name;\n    };\n    // ByWorst：分數低或同分名字大的排前面（最差優先，用於 better 堆的堆頂 =\n    // 目前第 i 名邊界）\n    struct ByWorst {\n            bool operator()(const Loc& a, const Loc& b) const {\n                    if (a.score != b.score) {\n                            return a.score > b.score;\n                    }\n                    return a.name < b.name;\n            }\n    };\n    // ByBest：分數高或同分名字小的排前面（最佳優先，用於 worse\n    // 堆存放排名更差的元素）\n    struct ByBest {\n            bool operator()(const Loc& a, const Loc& b) const {\n                    if (a.score != b.score) {\n                            return a.score < b.score;\n                    }\n                    return a.name > b.name;\n            }\n    };\n    // better：存前 i 名，堆頂為第 i 名（最差的那個）\n    priority_queue<Loc, vector<Loc>, ByWorst> better;\n    // worse：存第 i+1 名以後，堆頂為第 i+1 名（最佳的那個）\n    priority_queue<Loc, vector<Loc>, ByBest> worse;\n\npublic:\n    SORTracker() {}\n\n    void add(string name, int score) {\n            // 新元素先加入 better，再把 better 的「最差者」移到 worse，維持 better\n            // 恰好有 i 個\n            better.push({score, name});\n            worse.push(better.top());\n            better.pop();\n    }\n\n    string get() {\n            // 每次 get 呼叫代表查第 i 名，i 遞增；把 worse 最佳者移入 better 使 better\n            // 多一個\n            better.push(worse.top());\n            worse.pop();\n            return better.top().name;  // better 堆頂即當前第 i 名\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- `better` 是已被取用的前段，堆頂為其中「最差」者（也就是上一次的答案）。\n- `worse` 是其餘候選，堆頂為其中「最好」者。\n- `add` 先把新地點丟進 `better`，再把 `better` 最差者擠回 `worse`，維持 `better` 大小等於已查次數。\n- `get` 把 `worse` 最好者升進 `better`，此時 `better` 的最差者就是下一個名次的答案。\n\n## 時間與空間複雜度\n\n| 題目 | 操作 | 時間 | 空間 |\n| --- | --- | --- | --- |\n| 295 資料流的中位數 | `addNum` / `findMedian` | $O(\\log n)$ / $O(1)$ | $O(n)$ |\n| 480 滑動視窗中位數 | 每個視窗 | $O(\\log k)$ 攤還 | $O(k)$ 有效 + 垃圾 |\n| 1825 MK 平均值 | `addElement` / `calculateMKAverage` | $O(\\log m)$ / $O(1)$ | $O(m)$ |\n| 2102 序列順序查詢 | `add` / `get` | $O(\\log n)$ | $O(n)$ |\n| 462 / LCP 24 | 全部 | $O(n\\log n)$（排序即可，不需堆） | $O(n)$ |\n\n**462 這類離線題不需要對頂堆**——直接排序取中位數 $O(n\\log n)$。**對頂堆的價值只在「線上、邊加邊查」。** 看到題目給的是一個固定陣列而非資料流，先想排序。\n\n**480 的空間**：垃圾最壞 $O(n)$（$n$ 為總元素數而非視窗大小 $k$）。**若 $n$ 很大而 $k$ 很小，建議改用 `multiset`（空間恆為 $O(k)$）。**\n\n**溢位**：\n- `(lo.top() + hi.top()) / 2.0` 中，兩個 `int` 相加可能溢位。**寫成 `((long long)lo.top() + hi.top()) / 2.0`。** 480 的測資明確包含 `INT_MAX` 來抓這個錯。\n- 定理 4 的 $f(m)$ 與 1825 的 `sumMid` 都要 `long long`。\n\n## 常見錯誤與邊界條件\n\n- 兩個堆的比較器方向要剛好相反，否則交界會取錯。\n- `add` 必須維持 `better` 大小不變、`get` 才讓它增長，名次才會對齊。\n- 自訂結構體比較器時，記得分數與名字的雙重鍵都要處理。\n\n## 常見變形\n\n- 資料流中位數（295）：max-heap 放小一半、小根堆放大一半，交界即中位數。\n- 滑動視窗中位數（480）：對頂堆加懶刪除維護視窗。\n- 第 K 大元素流（703）：固定大小的小根堆守住第 K 大。\n\n## 與相似技巧的比較\n\n- **與 4.6 定理 3**：4.6 從「相向雙容器」的角度介紹了對頂堆的基本形。**本節多了三件事：一般化到第 $K$ 小、懶刪除（480）、以及維護區段和（1825、定理 4 推論）。**\n- **與 1670 設計前中後佇列（5.2）**：形狀極像但語意不同——**1670 的「中間」是位置的中間（按下標分割，用 `deque`），本節的「中間」是值的中位（按值分割，用堆）。** 看清楚是位置還是值，是這兩題唯一的分歧點。\n- **與 `multiset` + 中位數迭代器**：功能相同，程式碼更短、無垃圾、常數略大。**實務首選；對頂堆的優勢是常數與「不依賴迭代器穩定性」。**\n- **與有序統計樹（pb_ds `tree_order_statistics_node_update`）**：支援 `find_by_order(k)`（第 $k$ 小）與 `order_of_key(x)`（排名），**$K$ 可以每次查詢都不同**。**這是對頂堆的硬邊界：對頂堆只能維護一條（或固定幾條）分界線，任意排名查詢必須換工具。**\n- **與值域樹狀陣列 / 線段樹（9.x）**：在值域上建樹，用「樹上二分」找第 $K$ 小，$O(\\log C)$。**當值域可離散化且查詢排名任意時，這是最通用的解。** 缺點是要先知道所有可能的值（或動態開點）。\n- **與 5.4 單調佇列**：單調佇列只能取視窗的**極值**，不能取中位數。**「極值」用單調佇列（$O(1)$ 攤還），「任意排名」用對頂堆或樹（$O(\\log k)$）。**\n- **與 6.3 第 K 小**：6.3 是「一次性求出第 $K$ 小」，本節是「持續維護第 $K$ 小」。**一次性 ⇒ `nth_element` 或二分；持續維護 ⇒ 對頂堆。**\n\n## 代表例題\n\n**題號**：LCP 24、295、480、1825、2102\n\n請套用「對頂堆（滑動視窗第 K 小/大）」的狀態、不變式與實作方式，先寫出資料表示、更新流程與答案讀取規則。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| LCP 24 | [數字遊戲](https://leetcode.cn/problems/5TxKeK)（Premium） |  |\n| 295 | [資料流的中位數](https://leetcode.cn/problems/find-median-from-data-stream) |  |\n| 480 | [滑動視窗中位數](https://leetcode.cn/problems/sliding-window-median) |  |\n| 1825 | [求出 MK 平均值](https://leetcode.cn/problems/finding-mk-average) | 2396 |\n| 2102 | [序列順序查詢](https://leetcode.cn/problems/sequentially-ordinal-rank-tracker) | 2159 |\n| 2653 | [滑動子陣列的美麗值](https://leetcode.cn/problems/sliding-subarray-beauty) | 1786 |\n| 3013 | [將陣列分成最小總代價的子陣列 II](https://leetcode.cn/problems/divide-an-array-into-subarrays-with-minimum-cost-ii) | 2540 |\n| 3321 | [計運算元陣列的 x-sum II](https://leetcode.cn/problems/find-x-sum-of-all-k-long-subarrays-ii) | 2598 |\n| 3369 | [設計陣列統計跟蹤器](https://leetcode.cn/problems/design-an-array-statistics-tracker) |  |\n| 3422 | [將子陣列元素變為相等所需的最小運算元](https://leetcode.cn/problems/minimum-operations-to-make-subarray-elements-equal) |  |\n| 3505 | [使 K 個子陣列內元素相等的最少運算元](https://leetcode.cn/problems/minimum-operations-to-make-elements-within-k-subarrays-equal) | 2539 |\n\n**延伸閱讀**：[LeetCode 295 大小堆講解](https://leetcode.cn/problems/find-median-from-data-stream/solutions/3015873/ru-he-zi-ran-yin-ru-da-xiao-dui-jian-ji-4v22k/)，再延伸到滑動視窗時，需額外處理懶刪除或改用有序多重集合。\n\n## 本節重點速查\n\n- **核心：兩個堆把序列切成兩半，要查的位置卡在分界線上——`lo` 大頂堆、`hi` 小頂堆。**\n- **真正起作用的只有 (P1) $\\max(\\texttt{lo}) \\le \\min(\\texttt{hi})$**；(P2) 只是把分界線調到想要的位置。**改成 $|\\texttt{lo}| = K$ 就是第 $K$ 小**（2102 只是讓 $K$ 隨查詢遞增）。\n- **無分支的插入三行**：`lo.push(x); hi.push(lo.top()); lo.pop(); if (hi.size() > lo.size()) {搬回來}`。比「先比較再決定放哪邊」少一類邊界錯誤。\n- **480 的全部難點：`heap.size()` 包含垃圾，不能用來平衡——必須另設 `balance` 計數器。** 三條不變量 (E1)(E2)(E3) 要抄下來逐分支檢查。`rebalance` 之後**必須再 `prune`**。\n- **`erase` 只在 `x == top` 時才 `prune`**——垃圾只有在堆頂才礙事。\n- **480 實務首選是 `multiset` + 中位數迭代器**，程式碼一半長度、無垃圾。\n- **中位數最小化 $\\sum|x_i - c|$**（定理 4，凸 + 分段線性，斜率 $2j-n$ 變號處）。配合 `sumLo`／`sumHi` 可 $O(1)$ 求出最小值。**「相鄰差 1 的遞增序列」先做 $x_i \\leftarrow x_i - i$ 再套用。**\n- **段數 $\\ge 3$（1825）就別用堆了**，改 `multiset` + 前綴和。\n- **硬邊界**：對頂堆只能維護固定的分界線。**任意排名查詢 ⇒ 有序統計樹或值域樹狀陣列。**\n- `(lo.top() + hi.top()) / 2.0` 記得先轉 `long long`。\n",
        },
      ],
    },
    {
      id: 48,
      title: "7. 字典樹（trie）",
      src: "https://leetcode.cn/circle/discuss/mOr1u6/",
      summary:
        "[模板](https://leetcode.cn/problems/implement-trie-prefix-tree/solutions/2993894/cong-er-cha-shu-dao-er-shi-liu-cha-shu-p-xsj4/)\n\n:::example 字典樹：逐字元插入與查詢字串\n**Trie 核心模板**：\n\n```cpp\n// 字典樹：逐字元插入與查詢字串\nstruct Trie {\n    Trie* children[26] = {};  // 26 個子節點（a-z）\n    bool is_end = false;      // 是否為某個字串的結尾\n    void insert(string word) {\n        Trie* node = this;\n        for (char c : word) {\n            if (!node->children[c - 'a']) {\n                node->children[c - 'a'] = new Trie();\n            }\n            node = node->children[c - 'a'];\n        }\n\n        node->is_end = true;\n    }\n};\n```\n:::",
      children: [
        {
          id: 49,
          title: "7.1 基礎",
          summary:
            "## 這個技術解決什麼問題\n\n一本字典有 $n$ 個字串、總長 $L$。要回答「$s$ 在不在字典裡」「字典裡有幾個字以 $s$ 開頭」「字典裡哪個字是 $s$ 的前綴」這類問題。\n\n- **雜湊表能回答第一個問題**（$O(|s|)$），**但完全無法回答後兩個**——雜湊把字串打散了，前綴關係在雜湊值上不留任何痕跡。\n- **排序 + 二分能回答前綴計數**（前綴相同的字在字典序中連續），**但只能離線**，且每次比較是 $O(|s|)$。\n- **逐一比對是 $O(L)$**，字典一大就不能接受。\n\n**核心想法**：*把字典裡所有字串的所有前綴，共用地存成一棵樹——每個節點代表一個前綴，每條邊代表一個字元。* 於是「以 $s$ 開頭」就是「$s$ 對應節點的整棵子樹」，前綴關係從「要比對字元」變成「是不是祖先」。\n\n**這才是 trie 的本質：把前綴關係轉成樹上的祖先關係。** 記住這句話，7.1～7.4 的所有題目都只是它的不同用法：\n- 648 單字替換 = 找最淺的祖先；\n- 720 最長單字 = 在「合法節點」構成的子樹裡找最深；\n- 820 單字壓縮編碼（反向插入）= 數葉子；\n- 7.4 的 0-1 trie = 把整數當成固定長度的二進位字串。\n\n**代價是空間**：節點數最壞是 $L+1$，每個節點若用陣列存 26 個孩子，就是 $26L$ 個 `int`。**trie 是典型的「用空間換時間」，資料量上限沒算清楚就會 MLE。**\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「字典樹」想：\n\n- **大量字串要做前綴查詢、前綴計數，或「以某段開頭的字」**。208 實現 Trie、677 鍵值對映、720 最長單字都直接在建這棵樹。\n- **題目強調「共用前綴」或「最短/最長前綴匹配」**，例如 648 單字替換（用字典裡最短的字根替換）、820 單字壓縮編碼（共用字尾，反過來插入）：\n\n:::example 嗅到味道：許多字開頭重複，逐一比對很浪費 → 合併成前綴樹\n```cpp\n// 嗅到味道：許多字開頭重複，逐一比對很浪費 → 合併成前綴樹\n// for (auto& w : dict) if (startsWith(query, w)) ...   // 改成在 trie\n// 上一次走完\n```\n:::\n\n\n\n- **需要「邊走邊判斷是否已拼出一個合法單字」**（如替換、分詞）——`isEnd` 標記正是為此而設。\n- **暴力做法是每次查詢都掃過整本字典 `O(字典總長)`**，trie 把單次查詢壓成 `O(查詢字串長)`。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的節點需要哪些欄位？（子節點、isEnd，要不要 pass / end 計數？）\n2. 我是在查「完整單字存在」還是「前綴存在」？最後該看哪個欄位？\n3. 字元集多大？節點該用固定陣列還是 map？\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n本節要解決的場景是：手上有一大堆字串（單字、字典），需要反覆問「有沒有以某段前綴開頭的字？」「這個前綴出現幾次？」。若每次都把所有字串逐一比對，會很慢；而很多字串其實**共用同樣的開頭**，重複比較是浪費。\n\n「字典樹（trie，又叫前綴樹）」的核心想法就是：**把所有字串的共同前綴合併成一棵樹，每個節點代表「走到這裡所拼出的那段前綴」，從根往下走一步就多拼一個字元。** 例如 \"cat\"、\"car\"、\"can\" 三個字，會共用 \"ca\" 這段路徑，到第三層才分岔成 t / r / n。\n\n:::example trie 節點：26 條子邊 + 結尾標記（+ 可選的經過計數）\n每個節點要記三樣東西：通往各子字元的指標、（選用的）經過次數、以及「是否有單字在此結束」的標記。\n\n```cpp\n// trie 節點：26 條子邊 + 結尾標記（+ 可選的經過計數）\nstruct Node {\n    Node* child[26] = {};  // child[c] 表示往字元 'a'+c 的邊\n    bool isEnd = false;    // 是否有單字剛好在此結束\n    int pass = 0;          // 有多少單字經過此節點（前綴計數用）\n};\n```\n:::\n\n\n\n:::example 插入 word：沿字元逐層往下，缺節點就建\n插入一個字串，就是從根開始沿字元一層層往下走，沒有對應子節點就新建：\n\n```cpp\n// 插入 word：沿字元逐層往下，缺節點就建\nNode* p = root;\nfor (char ch : word) {\n    int c = ch - 'a';\n    if (!p->child[c]) {\n        p->child[c] = new Node();\n    }\n    p = p->child[c];\n    p->pass++;  // 這條前綴又多被一個單字經過\n}\np->isEnd = true;  // 整個單字在此結束\n```\n:::\n\n\n\n:::example 查前綴是否存在：沿字元走，斷了就沒有\n查詢前綴也一樣往下走：能走完整段前綴就代表「存在以它開頭的字」，途中斷掉就代表沒有。要問「完整單字是否存在」，就再檢查終點的 `isEnd`。**整個技巧就是把字元拆成樹上的路徑，讓前綴查詢只跟前綴長度有關，與字典大小無關。**\n\n```cpp\n// 查前綴是否存在：沿字元走，斷了就沒有\nNode* find(const string& s) {\n    Node* p = root;\n    for (char ch : s) {\n        int c = ch - 'a';\n        if (!p->child[c]) {\n            return nullptr;  // 路斷了\n        }\n        p = p->child[c];\n    }\n    return p;  // 走到底的節點：看 isEnd 判單字、看 pass 取前綴計數\n}\n```\n:::\n\n\n\n查一段長 L 的前綴只要 `O(L)`，與字典有幾個字無關，這是 trie 最大的好處。\n\n## 狀態／資料結構定義\n\n**寫法 A：指標／結構體（好讀，適合設計題如 208、677）**\n\n```cpp\nstruct Node {\n    Node* ch[26]{};      // 值初始化為 nullptr\n    bool  isEnd = false; // 從根到此節點的字串，本身是一個字典單字\n    int   cnt   = 0;     // 有多少個插入的字串「經過」此節點（＝以此前綴開頭的字數）\n    int   val   = 0;     // 677 這類鍵值題用\n};\n```\n\n**寫法 B：靜態陣列池（快，適合大資料量與 0-1 trie）**\n\n```cpp\nconstexpr int MAXN = 100005 * 32;   // 上界＝所有字串總長 + 1，務必自己算\nint ch[MAXN][26], cnt[MAXN];\nbool isEnd[MAXN];\nint tot = 0;                        // 已配置的節點數，節點 0 是根\n```\n\n**三個欄位的語意必須分清楚——這是 trie 題最常見的混淆來源：**\n\n| 欄位 | 語意 | 回答什麼 |\n| --- | --- | --- |\n| 節點存在 | 這個前綴出現過 | `startsWith(s)` |\n| `isEnd` | 這個前綴**本身**是一個單字 | `search(s)` |\n| `cnt` | 有幾個單字以這個前綴開頭 | `countPrefix(s)`、可刪除的引用計數 |\n\n**根節點代表空字串**，深度 $d$ 的節點代表長度 $d$ 的前綴。\n\n**字元集不是 26 就要改**：小寫字母 26、大小寫加數字 62、任意 ASCII 128。**字元集大而稀疏時用 `unordered_map<char, Node*> ch;`**——時間多一個雜湊常數，空間從 $O(\\Sigma \\cdot |\\text{節點}|)$ 降到 $O(|\\text{邊}|)$。\n\n## 不變量或正確性證明\n\n### 定理 1（結構不變量：節點 ↔ 前綴的雙射）\n\n設插入的字串集合為 $S$，令\n$$P(S) = \\{\\, p : p \\text{ 是某個 } s \\in S \\text{ 的前綴（含空串與 } s \\text{ 本身）} \\,\\}.$$\n\n**不變量 (T)**：任何時刻，trie 的節點集合與 $P(S)$ 之間存在雙射，且節點 $u$ 對應的字串恰為「從根到 $u$ 的邊標籤依序連接」。\n\n*證明*（對插入次數歸納）。\n- **基底**：$S = \\emptyset$，trie 只有根，$P(\\emptyset) = \\{\\varepsilon\\}$，根對應空串 ✔。\n- **歸納步**：假設插入 $s_1,\\dots,s_{k-1}$ 後 (T) 成立，現插入 $s_k$。插入程序沿 $s_k$ 的字元逐個下走，孩子不存在就新建。走到第 $i$ 步時所在節點對應 $s_k[0..i)$。\n  - **新增的節點**恰好是 $s_k$ 那些「先前不存在的前綴」，也就是 $P(S \\cup \\{s_k\\}) \\setminus P(S)$ ✔；\n  - **不新增任何其他節點**，也不刪除節點 ✔；\n  - 對應關係由「沿邊下走」定義，與 (T) 的敘述一致 ✔。$\\square$\n\n**(T) 是整節的地基。** 它把「字串的前綴關係」翻譯成「樹上的祖先關係」：\n\n> **推論 1.1**：$p$ 是 $q$ 的前綴 $\\iff$ 節點 $u_p$ 是節點 $u_q$ 的祖先（或相等）。\n\n*證明*：由 (T)，$u_q$ 對應的字串由根到 $u_q$ 的路徑決定；$u_p$ 是祖先 $\\iff$ 該路徑的某個前段走到 $u_p$ $\\iff$ $p$ 是 $q$ 的前段。$\\square$\n\n**推論 1.1 就是所有 trie 題的共同轉譯規則。** 下面每個定理都只是在推論 1.1 上加一句話。\n\n### 定理 2（查詢的正確性，以及 `search` 與 `startsWith` 的唯一差別）\n\n*前提*：插入 $s$ 時，**只在終點節點**把 `isEnd` 設為 `true`，且從不設回 `false`（無刪除的情形）。\n\n- `startsWith(s)`：沿 $s$ 下走，中途缺孩子回 `false`，走完回 `true`。\n  由 (T)，「走得完」$\\iff$ $s \\in P(S)$ $\\iff$ $s$ 是某個插入字串的前綴 ✔。\n- `search(s)`：同上，再加一句 `return isEnd[u]`。\n  由前提，`isEnd[u]` 為真 $\\iff$ 曾有某次插入以 $u$ 為終點 $\\iff$ $s \\in S$ ✔。\n\n**這兩個函式的差別只有最後一行。** 也就是說：**「$s$ 是否在字典裡」與「$s$ 是否為字典裡某字的前綴」是兩件事，而 trie 天生能區分它們——這正是雜湊表做不到的地方。**\n\n*支援刪除時*：不要真的刪節點，改成維護 `cnt`（經過此節點的字串數），刪除時沿路 `--cnt`。**節點的 `cnt == 0` 表示這個前綴已無人使用**，此時可以（但不必）回收。**用 `cnt` 而不是實際刪除，是因為刪節點必須確認它沒有其他後代——那需要額外資訊，而 `cnt` 恰好就是那個資訊。**\n\n### 定理 3（648 單字替換：為什麼「走到第一個 `isEnd` 就停」是對的）\n\n*問題*：字典裡有一堆字根，把句子中每個單字換成「能作為它前綴的**最短**字根」。\n\n*引理*：一個字串 $w$ 的所有前綴，依長度排序構成一條**鏈**——任兩個前綴之間必有一個是另一個的前綴。\n\n*證明*：長度為 $i$ 與 $j$（$i \\le j$）的前綴分別是 $w[0..i)$ 與 $w[0..j)$，前者顯然是後者的前綴。$\\square$\n\n*定理*：沿 $w$ 從根下走，**第一個遇到的 `isEnd` 節點**就是最短的合法字根。\n\n*證明*：由推論 1.1，「$r$ 是 $w$ 的前綴且 $r$ 在字典裡」$\\iff$「$u_r$ 在從根到 $u_w$ 的路徑上且 `isEnd[u_r]`」。下走的順序恰是路徑上節點由淺到深的順序，也就是候選字根**由短到長**的順序。故第一個命中者長度最小 ✔。$\\square$\n\n**這個證明的關鍵前提是「候選集合構成一條鏈」**——所以「最短」是良定義的、且不需要任何比較，走到就是答案。**若題目改成「找字典裡與 $w$ 有最長公共前綴的字」，候選就不再是鏈（分佈在不同子樹），第一個命中就不再是答案。** 遇到 trie 題先問：**我要找的東西是不是落在一條根到葉的路徑上？** 是 ⇒ 邊走邊判；不是 ⇒ 要搜子樹（7.2）。\n\n### 定理 4（720 詞典中最長的單字：前綴封閉性）\n\n*問題*：找出字典中最長的字 $w$，使得 $w$ 的**每一個**前綴（長度 $1 \\dots |w|$）都在字典裡；同長取字典序最小。\n\n*定理*：合法字的集合 $V$ 在 trie 上構成一棵**包含根的連通子樹**；因此只要做 DFS，**遇到 `isEnd` 為假的孩子就直接剪枝**，就不會漏掉任何合法字。\n\n*證明*。\n- **$V$ 前綴封閉**：若 $w \\in V$，則 $w$ 的任一前綴 $p$ 的所有前綴也都是 $w$ 的前綴，故都在字典裡，故 $p \\in V$ ✔。\n- 由推論 1.1，前綴封閉 $\\iff$ $\\{u_w : w \\in V\\} \\cup \\{\\text{root}\\}$ 對「取父節點」封閉 $\\iff$ 它是包含根的連通子樹 ✔。\n- **剪枝的正確性**：若某節點 $u$ 的 `isEnd` 為假，則 $u$ 對應的字串不在字典裡，於是 $u$ 的任何後代 $v$ 都有一個不在字典裡的前綴，故 $v \\notin V$。**剪掉 $u$ 的整棵子樹不會丟失任何 $V$ 中的元素** ✔。$\\square$\n\n*字典序最小*：DFS 時**按 `a` 到 `z` 的順序遍歷孩子**，同深度先到達者字典序較小；用「嚴格大於才更新答案」即可保住最小者。\n\n**「答案集合對某個操作封閉 ⇒ 它在結構上是連通的 ⇒ 可以安全剪枝」是一個通用套路**，不只出現在 trie。看到「每個前綴都要滿足 X」，先想封閉性。\n\n### 定理 5（820 單字壓縮編碼：為什麼反向插入後答案是葉子深度之和）\n\n*問題*：把一組字串編碼成 `s#` 的串接，允許一個字串**共用另一個字串的後綴位置**。求最短編碼長度。\n\n*觀察鏈*：\n$$A \\text{ 可併入 } B \\iff A \\text{ 是 } B \\text{ 的後綴} \\iff \\mathrm{rev}(A) \\text{ 是 } \\mathrm{rev}(B) \\text{ 的前綴} \\overset{\\text{推論 1.1}}{\\iff} u_{\\mathrm{rev}(A)} \\text{ 是 } u_{\\mathrm{rev}(B)} \\text{ 的祖先}.$$\n\n於是：把所有字串**反轉後插入 trie**，答案 $= \\sum_{\\text{葉節點 } v} (\\mathrm{depth}(v) + 1)$。\n\n*證明（兩個方向）*。\n- **上界（構造）**：對每個葉節點 $v$，把它對應的字串（反轉回來）加上 `#` 寫進編碼。任何非葉的 `isEnd` 節點 $u$，其字串是某個葉字串的後綴，可以直接指向該葉字串的尾段。故此編碼合法，長度為 $\\sum_v (\\mathrm{depth}(v)+1)$ ✔。\n- **下界**：設兩個葉 $v_1 \\ne v_2$，它們互不為祖先（葉的定義 + 不同節點），故對應字串互不為對方後綴，**它們在編碼中的 `#` 必不相同**（若共用同一個 `#`，兩者都是從該 `#` 往前讀，短者必為長者的後綴，矛盾）。每個葉獨佔一個 `#` 及其前面 $\\mathrm{depth}(v)$ 個字元；不同葉佔用的區段互不重疊（同上論證）。故任何合法編碼長度 $\\ge \\sum_v (\\mathrm{depth}(v)+1)$ ✔。$\\square$\n\n**必須先去重**（用 `set` 或 trie 自身的 `isEnd`）：兩個相同的字串對應同一個節點，只能算一次。**忘記去重是這題的標準錯誤。**\n\n### 定理 6（規模上界）\n\n**節點數 $\\le L + 1$**，其中 $L$ 是所有插入字串的總長。\n\n*證明*：由 (T)，節點數 $= |P(S)|$。每個字串 $s$ 貢獻至多 $|s|$ 個非空前綴，加上共用的空串 ⇒ $|P(S)| \\le 1 + \\sum_{s}|s| = L+1$ ✔。$\\square$\n\n**這個上界是開靜態陣列的唯一依據。** 例如 208 的約束是「總呼叫 $3\\times10^4$ 次、每字長 $\\le 2000$」⇒ $L \\le 6\\times10^7$，$26L$ 個 `int` 是 6 GB——**這種情況必須用 `unordered_map` 存孩子，或改用指標動態配置。開陣列前一定要乘出來看。**\n\n## 逐步演算法\n\n:::example 節點欄位按題目需要取捨\n**1. 狀態表示：定義節點。** 子節點陣列（字元集大小決定寬度）、結尾標記，視題目需要再加經過計數 `pass`、字尾計數 `end`。\n\n```cpp\n// 節點欄位按題目需要取捨\nstruct Node {\n    Node* child[26] = {};\n    int end = 0;   // 有幾個單字在此結束（支援重複插入/刪除）\n    int pass = 0;  // 有幾個單字經過此節點\n};\nNode* root = new Node();\n```\n:::\n\n\n\n:::example 插入一個單字\n**2. 插入：沿字元建路徑。** 逐字元下行，缺節點就 new，沿途更新計數，終點標記結束。\n\n```cpp\n// 插入一個單字\nvoid insert(const string& w) {\n    Node* p = root;\n    for (char ch : w) {\n        int c = ch - 'a';\n        if (!p->child[c]) {\n            p->child[c] = new Node();\n        }\n        p = p->child[c];\n        p->pass++;\n    }\n    p->end++;\n}\n```\n:::\n\n\n\n:::example 完整單字是否存在\n**3. 查詢：search 與 startsWith 共用走法。** 兩者都先沿字元走到底；差別只在最後看 `end`（完整單字）還是只看「有沒有走到」（前綴）。\n\n```cpp\n// 完整單字是否存在\nbool search(const string& w) {\n    Node* p = walk(w);  // walk：同上的逐字元下行，斷了回 nullptr\n    return p && p->end > 0;\n}\n// 是否有單字以 prefix 開頭\nbool startsWith(const string& prefix) { return walk(prefix) != nullptr; }\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 計數類題目（前綴出現幾次、刪除單字）要靠 `pass` / `end`；刪除就沿路把 `pass` 減回去、終點 `end` 減 1。\n- 字元集大小直接決定每個節點的寬度與總空間：小寫字母用 26；含大小寫/數字就改 `unordered_map<char, Node*>` 省空間。\n- 複雜度：插入/查詢一個長 L 的字串都是 `O(L)`；空間最壞 `O(總字元數 × 字元集大小)`。\n\n## C++17 模板\n\n以下用本節代表題 208. 實現 Trie 展示核心寫法。\n\n**完整題目**：設計 Trie，支援插入單詞、查詢完整單詞、查詢是否存在指定前綴。\n\n:::example 以陣列模擬 Trie，tr[0] 為根節點\n**線索**：題目大量查前綴、字典詞或逐位最優配對；共同前綴可被壓縮成樹路徑。\n\n```cpp\nclass Trie {\n    struct Node {\n            int child[26];  // 26 個小寫字母的子節點編號，-1 表示不存在\n            bool end = false;  // 標記此節點是否為某個單字的結尾\n            Node() { fill(std::begin(child), std::end(child), -1); }\n    };\n    // 以陣列模擬 Trie，tr[0] 為根節點\n    vector<Node> tr{Node()};\n\npublic:\n    void insert(string word) {\n            int u = 0;  // 從根節點出發\n            for (char ch : word) {\n                    int c = ch - 'a';\n                    // 若子節點不存在則新建\n                    if (tr[u].child[c] == -1) {\n                            tr[u].child[c] = tr.size();\n                            tr.push_back(Node());\n                    }\n                    u = tr[u].child[c];\n            }\n            tr[u].end = true;  // 標記單字結尾\n    }\n    bool search(string word) {\n            int u = 0;\n            for (char ch : word) {\n                    int c = ch - 'a';\n                    if (tr[u].child[c] == -1) {\n                            return false;  // 路徑中斷，單字不存在\n                    }\n                    u = tr[u].child[c];\n            }\n            return tr[u].end;  // 必須走到結尾標記才算完整單字\n    }\n    bool startsWith(string prefix) {\n            int u = 0;\n            for (char ch : prefix) {\n                    int c = ch - 'a';\n                    if (tr[u].child[c] == -1) {\n                            return false;\n                    }\n                    u = tr[u].child[c];\n            }\n            return true;  // 能走完前綴路徑即可，無需結尾標記\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- Trie 把共同前綴壓到同一條路徑；查詢完整單詞需檢查終止標記，前綴查詢則只需走到節點。\n- 每個節點代表一段前綴；插入和查詢都沿著同一條字元路徑前進。\n- 完整單詞查詢要檢查終止標記，前綴查詢只需要能走到對應節點。\n\n## 時間與空間複雜度\n\n設 $L$ 為總長、$|s|$ 為單次操作字串長、$\\Sigma$ 為字元集大小。\n\n| 操作 | 陣列孩子 | `unordered_map` 孩子 |\n| --- | --- | --- |\n| `insert(s)` | $O(\\lvert s\\rvert)$ | $O(\\lvert s\\rvert)$ 期望 |\n| `search(s)` / `startsWith(s)` | $O(\\lvert s\\rvert)$ | $O(\\lvert s\\rvert)$ 期望 |\n| 建整本字典 | $O(L)$ | $O(L)$ 期望 |\n| 空間 | $O(\\Sigma L)$ | $O(L)$ |\n\n**注意 `search` 的複雜度與字典大小 $n$ 完全無關**——這是 trie 相對於「逐一比對 $O(L)$」的全部價值。\n\n各題：\n- **208**：所有操作 $O(|s|)$，空間 $O(\\Sigma L)$。\n- **648**：建樹 $O(L_{\\text{dict}})$，處理句子 $O(L_{\\text{sentence}})$。\n- **720**：建樹 $O(L)$，DFS $O(\\Sigma \\cdot |\\text{節點}|)$。**這題也可以排序後用 `unordered_set` 遞推，$O(L\\log n)$ 且好寫**——trie 不是唯一解。\n- **820**：$O(L)$。\n- **677**：`insert` 與 `sum` 都 $O(|s|)$。**注意 `insert` 可能覆寫既有鍵，要傳「差值」沿路更新 `val` 的前綴和，或者查詢時走子樹加總。**\n\n**常數提醒**：`Node* ch[26]{}` 每個節點 208 bytes（64 位元），節點多時 cache miss 主導耗時。**極端追求速度時改用靜態陣列池 + `int` 索引**，記憶體連續、快 2～3 倍。\n\n## 常見錯誤與邊界條件\n\n- 節點數上限約為所有字串長度總和。\n- 完整單詞與前綴要分別標記。\n- 字元集不是小寫英文字母時要改 child 結構。\n- 0-1 Trie 從最高位往低位走。\n\n## 常見變形\n\n- 普通 Trie 儲存單詞與前綴計數。\n- 0-1 Trie 處理最大 XOR。\n- AC 自動機在 Trie 上加入 fail 邊做多模式匹配。\n\n## 與相似技巧的比較\n\n- **與雜湊表 `unordered_set<string>`**：精確查詢兩者都是 $O(|s|)$，且雜湊常數更小、空間更省。**只要題目不涉及前綴，就用雜湊表，別建 trie。** trie 的不可替代性在於前綴查詢、前綴計數、以及「邊走邊判斷」。\n- **與排序 + 二分**：把字典排序後，以 $s$ 為前綴的字在陣列中構成一段連續區間，用兩次 `lower_bound` 就能計數，$O(|s|\\log n)$、空間 $O(L)$。**離線且只需前綴計數時，這個做法比 trie 省得多。** trie 勝在支援動態插入與 $O(|s|)$（無 $\\log$）。\n- **與 7.2 的 trie 搜尋**：本節的查詢都沿**單一路徑**下走（定理 3 的「鏈」）。**7.2 處理的是查詢本身有分支的情形**（211 的 `.` 萬用字元、212 在網格上搜尋），必須 DFS 整棵子樹並剪枝。**判準：候選是否落在一條根到葉的路徑上。**\n- **與 AC 自動機**：trie + 失配指標，能在 $O(|text| + \\sum|pattern|)$ 內找出文字中所有模式的所有出現位置。**212 那類「多模式匹配」用 AC 自動機是正解形態，trie + DFS 剪枝只是在題目規模小時夠用。**\n- **與後綴陣列 / 後綴自動機**：那些處理的是「**一個**字串的所有子串」，trie 處理的是「**多個**字串的前綴」。820 之所以能用 trie，是因為它把後綴問題透過反轉化歸成前綴問題——**這個化歸只在「整個字串的後綴」時有效，對「任意子串」無效。**\n- **與 7.4 的 0-1 trie**：完全同一個資料結構，只是 $\\Sigma = 2$、每個「字串」是固定 32 位元。**因為長度固定，0-1 trie 的每次查詢都恰好走 32 步、必定走得完，不需要 `isEnd`**——這是它與一般 trie 在寫法上的主要差異。\n- **與 9.x 的值域樹狀陣列／線段樹**：0-1 trie 本質上是一棵值域上的動態開點線段樹。**兩者可以互換，選擇取決於你要做的是「逐位貪心」（trie 直觀）還是「區間求和」（線段樹直觀）。**\n\n## 代表例題\n\n**題號**：208、648、677、720、820\n\n要共用前綴路徑；練習分清單詞終止、前綴計數與子節點結構。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 208 | [實現 Trie (字首樹)](https://leetcode.cn/problems/implement-trie-prefix-tree) |  |\n| 648 | [單詞替換](https://leetcode.cn/problems/replace-words) |  |\n| 677 | [鍵值對映](https://leetcode.cn/problems/map-sum-pairs) |  |\n| 720 | [詞典中最長的單詞](https://leetcode.cn/problems/longest-word-in-dictionary) |  |\n| 820 | [單詞的壓縮編碼](https://leetcode.cn/problems/short-encoding-of-words) |  |\n| 1233 | [刪除子資料夾](https://leetcode.cn/problems/remove-sub-folders-from-the-filesystem) | 1545 |\n| 1268 | [搜尋推薦系統](https://leetcode.cn/problems/search-suggestions-system) | 1573 |\n| 1804 | [實現 Trie （字首樹） II](https://leetcode.cn/problems/implement-trie-ii-prefix-tree) |  |\n| 2168 | [每個數字的頻率都相同的獨特子字串的數量](https://leetcode.cn/problems/unique-substrings-with-equal-digit-frequency) |  |\n| 2261 | [含最多 K 個可整除元素的子陣列](https://leetcode.cn/problems/k-divisible-elements-subarrays) | 1724 |\n| 2416 | [字串的字首分數和](https://leetcode.cn/problems/sum-of-prefix-scores-of-strings) | 1725 |\n| 3597 | [分割字串](https://leetcode.cn/problems/partition-string) | 1347 |\n\n## 本節重點速查\n\n- **一句話：trie 把「字串的前綴關係」轉成「樹上的祖先關係」（推論 1.1）。** 所有 trie 題都是這句話加一個條件。\n- **三個欄位別混**：**節點存在** = 前綴出現過（`startsWith`）；**`isEnd`** = 這個前綴本身是單字（`search`）；**`cnt`** = 以此為前綴的單字數（計數、可刪除）。`search` 與 `startsWith` 只差最後一行。\n- **根 = 空字串**，深度 $d$ 的節點 = 長度 $d$ 的前綴。\n- **648**：候選字根構成一條鏈 ⇒ 由淺到深走，**第一個 `isEnd` 就是最短答案**，不需比較。\n- **720**：合法字集合前綴封閉 ⇒ 在 trie 上是連通子樹 ⇒ **遇到 `isEnd == false` 的孩子直接剪枝**；按 `a`–`z` 順序 DFS 自然取到字典序最小。\n- **820**：`A` 是 `B` 的後綴 $\\iff$ `rev(A)` 是 `rev(B)` 的前綴 ⇒ **反向插入，答案 = $\\sum_{\\text{葉}}(\\mathrm{depth}+1)$**。**記得先去重。**\n- **節點數 $\\le L+1$（定理 6）。開靜態陣列前先把 $26L$ 乘出來**——208 的最壞情況是 6 GB，必須改用 `map` 孩子或指標。\n- **支援刪除用 `cnt` 沿路遞減，不要真的刪節點。**\n- **判斷該不該用 trie**：只做精確查詢 ⇒ 雜湊表；離線前綴計數 ⇒ 排序 + 二分；**動態 + 前綴 ⇒ trie**。\n- **判斷該用 7.1 還是 7.2**：候選落在一條根到葉的路徑上 ⇒ 邊走邊判（本節）；查詢本身有分支 ⇒ DFS 子樹 + 剪枝（7.2）。\n",
        },
        {
          id: 50,
          title: "7.2 進階",
          summary:
            "## 這個技術解決什麼問題\n\n7.1 的查詢都沿**一條路徑**下走：給定 $s$，路徑唯一，走不動就失敗。本節處理的是**路徑不唯一**的情形——查詢本身帶分支，或者要在別的結構（網格、另一組字串）上同時匹配整本字典。\n\n四類典型需求：\n\n1. **查詢帶萬用字元**（211 新增與搜尋單字：`.` 可配任意字元）。每個 `.` 都讓路徑分裂成 $\\Sigma$ 條，必須 DFS。\n2. **在一個大結構裡同時找很多個單字**（212 單字搜尋 II、425 單字方塊）。逐一搜尋會把「共同前綴」的工作重算 $n$ 次；**把字典建成 trie，一次搜尋就同時推進所有單字，並在共同前綴失敗時一次剪掉一整批。**\n3. **同時牽涉前綴與後綴**（336 迴文對、745 前綴和後綴搜尋）。正插一棵、反插一棵，或把兩者編碼進同一個鍵。\n4. **設計題需要按前綴分組聚合**（642 設計搜尋自動補全、677 鍵值對映的加強版、1858 包含所有前綴的最長單字）。\n\n**共同的價值主張**：*把 $n$ 次獨立搜尋合併成一次樹上搜尋，共同前綴只算一次，失敗時整棵子樹一起剪掉。* 剪枝不是優化，**在 212 這類題裡它是能不能過的分界線。**\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「進階字典樹」想：\n\n- **查詢字串裡有萬用字元/可配任意字元**，需要在 trie 上分支搜尋（211 新增與搜尋單字）。\n- **要在一張網格或一長串文字裡同時找「很多個單字」**，逐一搜尋會重算大量共同前綴——把單字集建成 trie 一次搜完並剪枝（212 單字搜尋 II、425 單字方塊）：\n\n:::example 嗅到味道：要比對的「模式」有一整批 → 建成 trie，外層搜尋與它同步走\n```cpp\n// 嗅到味道：要比對的「模式」有一整批 → 建成 trie，外層搜尋與它同步走\n// if (!node->child[c]) return;   // 沒有任何模式以此開頭，立刻剪枝\n```\n:::\n\n\n\n- **題目同時牽涉前綴與字尾**（如 336 迴文對：判斷某字反轉後能否與另一字拼成迴文），常把字串正插或反插 trie 來加速配對。\n- **暴力做法是「每個模式各掃一次文字/網格」**，進階 trie 把共享前綴的工作合併、靠剪枝省掉大量無效路徑。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的查詢需要「分支」嗎？（萬用字元 → 對所有子節點遞迴）\n2. 我有「一整批模式」要找嗎？建成 trie 後外層搜尋如何與它同步前進、在哪裡剪枝？\n3. 結尾節點要存什麼（isEnd / 單字 / 索引）才方便命中時收集與去重？\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n本節延續字典樹，處理更進階的查詢：**前綴裡帶有萬用字元（`.` 可配任意字元）、在網格中沿 trie 同步搜尋多個單字、或利用前綴/字尾結構加速配對。** 基礎 trie 是「沿著唯一一條字元路徑往下走」，進階題的關鍵差別是：**有時不只走一條路，而要在 trie 上做分支搜尋（DFS / 回溯）。**\n\n:::example 在 trie 上做萬用字元 DFS：'.' 要嘗試所有子節點\n先看「萬用字元搜尋」（211 新增與搜尋單字）。一般字元就照常往下走；但遇到 `.` 時，它能配任何字元，所以要**對當前節點的「每一個子節點」都遞迴試一遍**，任一條成功即算成功。\n\n```cpp\n// 在 trie 上做萬用字元 DFS：'.' 要嘗試所有子節點\nbool dfs(Node* p, const string& w, int i) {\n    if (!p) {\n        return false;\n    }\n    if (i == w.size()) {\n        return p->isEnd;\n    }\n    char ch = w[i];\n    if (ch == '.') {  // 萬用字元：試所有分支\n        for (int c = 0; c < 26; ++c) {\n            if (dfs(p->child[c], w, i + 1)) {\n                return true;\n            }\n        }\n        return false;\n    }\n    return dfs(p->child[ch - 'a'], w, i + 1);  // 一般字元：只走那一條\n}\n```\n:::\n\n\n\n:::example 網格 DFS 與 trie 同步前進：trie 沒這條邊就直接剪枝\n再看「在網格中找一堆單字」（212 單字搜尋 II）。若對每個單字各自在網格 DFS，會重複走很多相同路徑。進階作法是**把全部目標單字先建成一棵 trie，然後在網格上 DFS 時「一格對應 trie 走一步」**：網格走到字母 'x'，trie 也跟著走 child['x'-'a']；若 trie 那一步沒有子節點，代表「沒有任何目標單字會這樣開頭」，可立刻剪枝、不必再往下。\n\n```cpp\n// 網格 DFS 與 trie 同步前進：trie 沒這條邊就直接剪枝\nvoid dfs(int r, int c, Node* node) {\n    char ch = board[r][c];\n    Node* nxt = node->child[ch - 'a'];\n    if (!nxt) {\n        return;  // 沒有單字以此開頭 → 剪枝\n    }\n    if (nxt->isEnd) {\n        collect(nxt->word);  // 走到某單字結尾，收集答案\n    }\n    // ...標記已訪問，往四方向遞迴，傳入 nxt...\n}\n```\n:::\n\n\n\n**進階 trie 的精神就是：把「逐一比對每個模式」改成「在共享前綴樹上一次搜尋並大量剪枝」。** 萬用字元靠分支遞迴，多模式匹配靠 trie 與外層搜尋同步前進。\n\n## 狀態／資料結構定義\n\n在 7.1 的節點上追加欄位，欄位的選擇決定你能回答什麼問題：\n\n```cpp\nstruct Node {\n    Node* ch[26]{};\n    int   wordId  = -1;        // 212：此節點是哪個單字的終點（-1 表示不是）\n    int   cnt     = 0;         // 子樹中還剩幾個未被找到的單字（用於 212 的剪枝）\n    int   best    = -1;        // 745：子樹中權重最大的索引\n    vector<int> palIdx;        // 336：子樹方向上「剩餘部分是迴文」的單字索引\n};\n```\n\n**用 `wordId` 而不是 `bool isEnd`**：212 找到單字後要輸出它，且要**防止重複輸出**——把 `wordId` 設回 `-1` 就同時完成了「記錄」與「去重」兩件事。\n\n**各題的具體結構**：\n\n- **211**：純 7.1 的 trie，複雜度全在搜尋函式。\n- **212**：字典建 trie；網格 DFS 時攜帶一個 `Node*`，**網格的路徑與 trie 的路徑同步前進**。\n- **336**：把每個字**反轉後**插入 trie，節點上額外記 `palIdx`（見定理 3 的兩種情形）。\n- **425**：字典建 trie，`cnt` 或節點上掛「以此為前綴的單字索引清單」，供 $O(1)$ 取出候選。\n- **745**：不用兩棵樹——把每個字編碼成所有的 `後綴 + '{' + 完整字`（`'{'` 是 `'z'+1`）插入一棵 trie，查詢 `suffix + '{' + prefix`。**這是「兩個維度合成一個鍵」的經典技巧。**\n\n## 不變量或正確性證明\n\n### 定理 1（211 萬用字元搜尋：前緣集合不變量）\n\n*搜尋函式*：\n```cpp\nbool dfs(Node* u, const string& w, int i) {\n    if (i == (int)w.size()) return u->isEnd;\n    if (w[i] != '.') {\n        Node* v = u->ch[w[i]-'a'];\n        return v && dfs(v, w, i+1);\n    }\n    for (int c = 0; c < 26; ++c)\n        if (u->ch[c] && dfs(u->ch[c], w, i+1)) return true;\n    return false;\n}\n```\n\n**不變量 (F)**：`dfs(u, w, i)` 被呼叫時，$u$ 對應的字串 $p$ 滿足 $|p| = i$ 且 $p$ 匹配模式前綴 $w[0..i)$；反之，**每一個匹配 $w[0..i)$ 且存在於 trie 中的長度-$i$ 字串，都恰好對應一次呼叫。**\n\n*證明*（對 $i$ 歸納）。\n- $i=0$：唯一匹配空模式的字串是空串，對應根，恰好呼叫一次 ✔。\n- 歸納步：設 (F) 對 $i$ 成立。字串 $q$（$|q|=i+1$）匹配 $w[0..i{+}1)$ $\\iff$ $q[0..i)$ 匹配 $w[0..i)$ **且** $q[i]$ 匹配 $w[i]$。\n  - $w[i] \\ne \\texttt{'.'}$：後者 $\\iff q[i] = w[i]$，程式只遞迴這一個孩子 ✔。\n  - $w[i] = \\texttt{'.'}$：後者恆真，程式遞迴所有存在的孩子 ✔。\n  由歸納假設，$q[0..i)$ 恰好對應一次呼叫，故 $q$ 也恰好對應一次 ✔。$\\square$\n\n由 (F) 與 7.1 定理 2，`i == |w|` 時 `u->isEnd` 為真 $\\iff$ 找到一個匹配的字典單字。**「恰好一次」的部分保證了沒有重複展開**——這是複雜度分析的前提。\n\n*複雜度*：設模式中有 $d$ 個 `.`。最壞情形下，被訪問的節點數不超過 $\\min\\big(\\Sigma^{d}\\cdot|w|,\\ |\\text{節點總數}|\\big)$。**兩個上界都要看**：$d$ 小時第一項緊，$d$ 大時第二項緊（訪問不可能超過整棵樹）。\n\n*重要的剪枝*：**先按長度分桶**——`vector<Trie> tries(26)`，`addWord(w)` 放進 `tries[w.size()]`。**因為 trie 上長度 $i$ 的節點只可能匹配長度 $i$ 的模式，長度不同的單字完全不必參與搜尋。** 這一行在 211 的實測中通常是 5～10 倍的加速。\n\n### 定理 2（212 單字搜尋 II：邊搜邊剪的正確性與攤還）\n\n*搜尋骨架*：網格 DFS 攜帶 trie 節點，兩者同步移動。\n\n**不變量 (S)**：DFS 進入 `dfs(r, c, u)` 時，$u$ 對應的字串**恰為**當前網格路徑上的字元序列。\n\n*證明*：進入前檢查 `u->ch[grid[r][c]]` 存在才遞迴，且遞迴傳入該孩子。由歸納，路徑字串與 trie 路徑逐字元對應 ✔。$\\square$\n\n由 (S) 與 7.1 推論 1.1：**「網格路徑拼出的字在字典裡」$\\iff$「同步到達的節點 `wordId >= 0`」**。所以檢查一個字典單字與檢查所有字典單字的成本相同——**這就是把字典建成 trie 的全部收益。**\n\n**兩個剪枝，以及它們為什麼安全：**\n\n- **剪枝 A（找到後移除）**：命中時記錄答案並令 `u->wordId = -1`。\n  *安全性*：同一個單字被找到第二次不會產生新答案（答案是集合）。**這同時解決了「同一個字在網格中有多條路徑」造成的重複輸出。**\n- **剪枝 B（回溯時摘掉空子樹）**：DFS 從節點 $u$ 返回後，若 $u$ 沒有孩子且 `u->wordId == -1`，就把父節點指向 $u$ 的指標設為 `nullptr`。\n  *安全性*：這樣的 $u$ 的子樹裡已無任何未找到的單字，**任何經過 $u$ 的網格路徑都不可能產生新答案**，摘掉不影響結果 ✔。\n  *效益（攤還）*：每個 trie 節點最多被摘一次，總摘除成本 $O(|\\text{節點}|)$。**而每摘掉一個節點，就永久消除了一整支未來的搜尋分支。** 沒有剪枝 B 時，網格上會反覆走進「只剩已找到單字」的死路；**在 LeetCode 的最壞測資上，剪枝 B 是 TLE 與 AC 的分界。**\n\n*另一種等價寫法*：用 `cnt`（子樹剩餘單字數）。命中時沿路 `--cnt`，DFS 時 `if (u->cnt == 0) return;`。**邏輯與剪枝 B 相同，但不改動樹結構，比較不容易寫錯。**\n\n*必須注意的前提*：**同一格不能重複使用**——用 `visited` 或原地改字元為 `'#'`，**回溯時務必還原**。這與 trie 無關，但是這題最常見的錯誤。\n\n### 定理 3（336 迴文對：完整的分類，以及為什麼需要兩種情形）\n\n*問題*：給定互不相同的字串 $w_0,\\dots,w_{n-1}$，找出所有有序對 $(i,j)$，$i \\ne j$，使得 $w_i + w_j$ 是迴文。\n\n設 $A = w_i$、$B = w_j$、$m = |A|$、$k = |B|$。\n\n**引理**：$A+B$ 是迴文，當且僅當下列之一成立：\n\n- **(情形 I) $m \\ge k$**：$A[0..k) = \\mathrm{rev}(B)$，且 $A[k..m)$ 本身是迴文。\n- **(情形 II) $m < k$**：$B[k-m..k) = \\mathrm{rev}(A)$，且 $B[0..k-m)$ 本身是迴文。\n\n*證明（情形 I）*。令 $T = A+B$，$|T| = m+k$。$T$ 是迴文 $\\iff$ $T[t] = T[m+k-1-t]$ 對所有 $t$。\n- 取 $t \\in [0,k)$：$T[t] = A[t]$（因 $t < k \\le m$），$T[m+k-1-t] = B[k-1-t]$（因 $m+k-1-t \\ge m$）。故條件為 $A[t] = B[k-1-t]$，即 $A[0..k) = \\mathrm{rev}(B)$ ✔。\n- 取 $t \\in [k, m)$（此區間對稱於自身：$m+k-1-t$ 也落在 $[k,m)$）：$T[t] = A[t]$、$T[m+k-1-t] = A[m+k-1-t]$。令 $t' = t-k$，條件成為 $A[k+t'] = A[m-1-t']$，即 $A[k..m)$ 是迴文 ✔。\n- 兩段合起來覆蓋了所有 $t \\in [0, m)$，而 $t \\ge m$ 的條件與 $t < k$ 的對稱等價，不提供新約束。$\\square$\n\n情形 II 由對稱性（考慮 $\\mathrm{rev}(T) = \\mathrm{rev}(B)+\\mathrm{rev}(A)$）同理可得。\n\n**為什麼必須分兩種情形，不能只做一種？** 因為 trie 的下走是有方向的：**把所有字反轉後插入 trie，沿 $A$ 往下走，只能檢測到「$A$ 有一個前綴等於某個 $\\mathrm{rev}(B)$」，也就是 $k \\le m$。** 當 $k > m$ 時，$A$ 走完了樹卻還停在半路，這時要問的是「**停在這個節點下方、剩餘部分是迴文的字有哪些**」——這正是 `palIdx` 存在的理由。\n\n*實作*：插入 $\\mathrm{rev}(w_j)$ 時，在路徑上每個節點 $u$（對應 $\\mathrm{rev}(w_j)$ 的長度-$d$ 前綴）記錄：**若 $\\mathrm{rev}(w_j)[d..]$ 是迴文，則把 $j$ 加入 `u->palIdx`**（等價於 $w_j$ 的前 $k-d$ 個字元是迴文，即情形 II 的條件）。查詢 $A$ 時：\n- 每走到一個 `isEnd` 節點（深度 $d \\le m$），檢查 $A[d..m)$ 是否迴文 ⇒ 情形 I 的解；\n- 走完 $A$（深度 $m$）後，該節點的 `palIdx` 全部是 ⇒ 情形 II 的解。\n\n**三個必踩的坑**：\n1. **空字串**。若 $w_j = \\varepsilon$，則 $\\varepsilon + A$ 是迴文 $\\iff$ $A$ 是迴文——這由情形 I（$k=0$）自動涵蓋，前提是你**允許 $d=0$ 時就檢查 `isEnd`**（根節點也要檢查）。\n2. **$i = j$ 要排除**。情形 I 在 $d = m$ 且 $A$ 自身是迴文時會配到自己。\n3. **重複計數**。$m = k$ 時情形 I 與 II 會描述同一對；把情形 II 的條件寫成嚴格 $m < k$（即只在走完 $A$ 後才收 `palIdx`，且不含深度恰為 $m$ 的 `isEnd`）即可避免。\n\n*複雜度*：$O\\!\\left(\\sum_j |w_j| + \\sum_i |w_i|^2\\right)$。**平方項來自「對 $A$ 的每個切點判斷後綴是否迴文」**——可用 Manacher 或雜湊預處理降到 $O(\\sum|w_i|)$，但在 LeetCode 的約束下不必要。\n\n### 定理 4（425 單字方塊：行列對偶約束）\n\n*問題*：用字典中的字排成 $L\\times L$ 方陣，使得**第 $i$ 行讀出的字 $=$ 第 $i$ 列讀出的字**（對所有 $i$）。\n\n*等價條件*：$\\mathrm{sq}[i][j] = \\mathrm{sq}[j][i]$ 對所有 $i,j$——**方陣關於主對角線對稱。**\n\n**定理**：若已確定前 $k$ 行（$k \\ge 1$），則第 $k$ 行的**前 $k$ 個字元被完全決定**，且必須等於\n$$\\mathrm{sq}[0][k],\\ \\mathrm{sq}[1][k],\\ \\dots,\\ \\mathrm{sq}[k-1][k]$$\n（即前 $k$ 行的第 $k$ 個字元依序拼成的字串）。\n\n*證明*：對稱條件給出 $\\mathrm{sq}[k][t] = \\mathrm{sq}[t][k]$，右側對 $t < k$ 都在已確定的前 $k$ 行內 ✔。反之，若第 $k$ 行滿足這個前綴條件，則它與前 $k$ 行之間的所有對稱約束都已滿足；與後續行的約束會在填那些行時檢查。**故此條件既必要又充分地刻畫了「可以繼續」**，回溯搜尋不會漏解也不會產生非法解。$\\square$\n\n**這就是為什麼要 trie**：每一步都需要「以某個特定前綴開頭的所有單字」，這正是 trie 的 $O(\\text{前綴長} + \\text{答案數})$ 查詢（在節點上掛索引清單）。**用暴力篩選字典是 $O(n)$ 每步，指數級搜尋乘上這個因子就過不了。**\n\n**通用心得**：**回溯 + trie 的組合，關鍵永遠是「找出一個既必要又充分的『可繼續』條件」。** 條件太弱（只檢查必要性的一部分）會搜出非法解，太強會漏解。425 的條件恰好是對稱約束在「已填部分」上的完整投影。\n\n### 定理 5（745 前綴和後綴搜尋：把兩個維度編碼成一個鍵）\n\n*問題*：預處理一組字串，支援查詢「同時以 $p$ 為前綴、以 $s$ 為後綴的字中，索引最大者」。\n\n*做法*：對每個字 $w$（索引 $i$），把**所有** `w的後綴 + '{' + w` 插入 trie，路徑上每個節點的 `best` 更新為 $\\max(\\texttt{best}, i)$。查詢時走 `s + '{' + p`，回傳終點的 `best`。\n\n*正確性*：分隔符 `'{'`（ASCII 恰為 `'z'+1`）**不出現在任何單字中**，所以鍵 `X{Y` 唯一分解為 $(X, Y)$。於是\n$$\\texttt{s+'\\{'+p} \\text{ 是 } \\texttt{suf+'\\{'+w} \\text{ 的前綴} \\iff \\big(s = \\mathrm{suf} \\text{ 的前綴且 } \\mathrm{suf} \\text{ 走完}\\big) \\dots$$\n更精確地：因為 `'{'` 是唯一的分隔符，前綴關係成立當且僅當 $s = \\mathrm{suf}$（分隔符位置必須對齊）**且** $p$ 是 $w$ 的前綴。而「$s$ 出現在某個 `suf` 的位置」$\\iff$ $s$ 是 $w$ 的後綴（因為我們插入了 $w$ 的所有後綴）✔。$\\square$\n\n**前提是分隔符不在字元集內。** 若字串可能包含任意字元，這個技巧就失效，得改用兩棵 trie 求索引集合交集（或 bitset）。\n\n*複雜度*：建樹 $O(\\sum |w|^2)$（每個字插入 $|w|+1$ 個鍵），查詢 $O(|p|+|s|)$。**在 $|w| \\le 10$ 時完全可行；字串一長就要換方法。**\n\n## 逐步演算法\n\n:::example 結尾節點直接掛上完整單字，命中即可收集\n**1. 狀態表示：節點存得更多。** 除了子節點與 `isEnd`，多模式題常把「完整單字字串」直接存在結尾節點，方便走到時收集；或存單字索引。\n\n```cpp\n// 結尾節點直接掛上完整單字，命中即可收集\nstruct Node {\n    Node* child[26] = {};\n    string word;  // 非空表示有單字在此結束\n};\n```\n:::\n\n\n\n:::example 把所有目標單字插入，結尾掛上字串\n**2. 建樹：把所有模式插進同一棵 trie。** 多模式匹配的第一步永遠是把待找的單字集合建成一棵 trie。\n\n```cpp\n// 把所有目標單字插入，結尾掛上字串\nfor (auto& w : words) {\n    Node* p = root;\n    for (char ch : w) {\n        int c = ch - 'a';\n        if (!p->child[c]) {\n            p->child[c] = new Node();\n        }\n        p = p->child[c];\n    }\n    p->word = w;\n}\n```\n:::\n\n\n\n:::example 網格 DFS 與 trie 同步（核心片段）\n**3. 搜尋：分支 DFS 或同步前進。** 萬用字元用「遇 `.` 試所有子節點」的遞迴；網格多模式用「外層 DFS 每走一格、trie 同步走一步，沒邊就剪枝」。\n\n```cpp\n// 網格 DFS 與 trie 同步（核心片段）\nvoid dfs(int r, int c, Node* node, vector<string>& res) {\n    Node* nxt = node->child[board[r][c] - 'a'];\n    if (!nxt) {\n        return;  // 剪枝\n    }\n    if (!nxt->word.empty()) {  // 命中一個完整單字\n        res.push_back(nxt->word);\n        nxt->word.clear();  // 去重：找過就清掉\n    }\n    char tmp = board[r][c];\n    board[r][c] = '#';  // 標記避免重複用\n    for (auto [dr, dc] : dirs) {\n        int nr = r + dr, nc = c + dc;\n        if (inBound(nr, nc)) {\n            dfs(nr, nc, nxt, res);\n        }\n    }\n    board[r][c] = tmp;  // 回溯還原\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 萬用字元越多，分支越爆；最壞每個 `.` 都要試 26 條，遞迴深度為字串長。\n- 多模式 DFS 要記得「回溯還原」訪問標記，並對已找到的單字去重（清掉 `word` 或用 set）。\n- 複雜度：插入仍是 `O(總字元數)`；網格搜尋最壞 `O(格數 × 4^最長單字)`，但 trie 剪枝在實務上大幅縮小。\n\n## C++17 模板\n\n以下用同型延伸示範題 208. 實現 Trie 展示核心寫法。\n\n**完整題目**：設計 Trie，支援插入單詞、查詢完整單詞、查詢是否存在指定前綴。\n\n:::example 以陣列模擬 Trie，tr[0] 為根節點\n**線索**：題目大量查前綴、字典詞或逐位最優配對；共同前綴可被壓縮成樹路徑。\n\n```cpp\nclass Trie {\n    struct Node {\n            int child[26];  // 26 個小寫字母的子節點編號，-1 表示不存在\n            bool end = false;  // 標記此節點是否為某個單字的結尾\n            Node() { fill(std::begin(child), std::end(child), -1); }\n    };\n    // 以陣列模擬 Trie，tr[0] 為根節點\n    vector<Node> tr{Node()};\n\npublic:\n    void insert(string word) {\n            int u = 0;  // 從根節點出發\n            for (char ch : word) {\n                    int c = ch - 'a';\n                    // 若子節點不存在則新建\n                    if (tr[u].child[c] == -1) {\n                            tr[u].child[c] = tr.size();\n                            tr.push_back(Node());\n                    }\n                    u = tr[u].child[c];\n            }\n            tr[u].end = true;  // 標記單字結尾\n    }\n    bool search(string word) {\n            int u = 0;\n            for (char ch : word) {\n                    int c = ch - 'a';\n                    if (tr[u].child[c] == -1) {\n                            return false;  // 路徑中斷，單字不存在\n                    }\n                    u = tr[u].child[c];\n            }\n            return tr[u].end;  // 必須走到結尾標記才算完整單字\n    }\n    bool startsWith(string prefix) {\n            int u = 0;\n            for (char ch : prefix) {\n                    int c = ch - 'a';\n                    if (tr[u].child[c] == -1) {\n                            return false;\n                    }\n                    u = tr[u].child[c];\n            }\n            return true;  // 能走完前綴路徑即可，無需結尾標記\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- Trie 把共同前綴壓到同一條路徑；查詢完整單詞需檢查終止標記，前綴查詢則只需走到節點。\n- 每個節點代表一段前綴；插入和查詢都沿著同一條字元路徑前進。\n- 完整單詞查詢要檢查終止標記，前綴查詢只需要能走到對應節點。\n\n## 時間與空間複雜度\n\n| 題目 | 時間 | 空間 | 備註 |\n| --- | --- | --- | --- |\n| 211 新增與搜尋單字 | 插入 $O(\\lvert w\\rvert)$；查詢最壞 $O(\\Sigma^{d}\\lvert w\\rvert)$，$d$ 為 `.` 數 | $O(\\Sigma L)$ | **按長度分桶可大幅剪枝** |\n| 212 單字搜尋 II | $O(M N \\cdot 3^{\\ell})$，$\\ell$ 為最長單字長 | $O(L)$ | 指數底是 3（不走回頭路）；剪枝 B 讓實測遠低於此 |\n| 336 迴文對 | $O\\!\\left(\\sum\\lvert w\\rvert + \\sum \\lvert w\\rvert^{2}\\right)$ | $O(\\Sigma L)$ | 平方項來自迴文判斷，可用雜湊消去 |\n| 425 單字方塊 | 指數級，實測受剪枝主導 | $O(L)$ | trie 把每步的候選枚舉降到輸出規模 |\n| 745 前綴和後綴搜尋 | 建 $O(\\sum\\lvert w\\rvert^{2})$，查 $O(\\lvert p\\rvert+\\lvert s\\rvert)$ | $O(\\Sigma \\sum\\lvert w\\rvert^{2})$ | 只適用短字串 |\n\n**212 的 $3^{\\ell}$ 從哪來**：網格 DFS 每格有 4 個鄰居，但其中一個是來路，故分支因子為 3。**這是上界，不是實際耗時**——實際耗時由「trie 中真正存在的路徑數」決定，而剪枝 B 讓這個數字隨搜尋進行單調下降。\n\n**空間的實際風險**：745 的 $O(\\Sigma \\sum|w|^2)$ 在 $|w| \\le 10$、$n \\le 10^4$ 時是 $26 \\times 10^6$ 個指標 ≈ 800 MB。**必須改用 `unordered_map` 存孩子。** 這種「複雜度式子看起來還好、乘出來爆炸」的情況在進階 trie 題裡很常見，**養成把數字代進去乘一遍的習慣。**\n\n## 常見錯誤與邊界條件\n\n- 節點數上限約為所有字串長度總和。\n- 完整單詞與前綴要分別標記。\n- 字元集不是小寫英文字母時要改 child 結構。\n- 0-1 Trie 從最高位往低位走。\n\n## 常見變形\n\n- 普通 Trie 儲存單詞與前綴計數。\n- 0-1 Trie 處理最大 XOR。\n- AC 自動機在 Trie 上加入 fail 邊做多模式匹配。\n\n## 與相似技巧的比較\n\n- **與 7.1**：判準是**查詢路徑是否唯一**。唯一 ⇒ 邊走邊判（7.1 定理 3 的「鏈」論證）；有分支 ⇒ DFS 子樹 + 剪枝（本節）。\n- **與 AC 自動機**：212 那類「一段文字中找所有模式」的**正統**解法是 AC 自動機（trie + 失配指標 + BFS 建自動機），$O(|text| + \\sum|pattern| + \\text{命中數})$。**本節的 DFS + 剪枝只是在「文字是網格、必須回溯」時的替代品**——網格上沒有線性掃描的概念，失配指標派不上用場。**遇到一維文字的多模式匹配，用 AC 自動機。**\n- **與字串雜湊**：336 的「判斷子串是否迴文」用雜湊是 $O(1)$；425 的「以某前綴開頭的字」用 `unordered_map<string, vector<int>>` 也能做，且好寫得多。**雜湊在「等值判斷」上勝過 trie，trie 在「共享前綴的批量推進與剪枝」上不可替代。** 425 若用雜湊表，就失去了「一次推進所有候選」的能力，但因為每步都要完整前綴，雜湊表其實夠用——**425 是 trie 與雜湊表都能做的題，選 trie 是為了節點上掛索引清單的便利。**\n- **與 Manacher / 迴文自動機**：336 的瓶頸是「判斷 $O(|w|)$ 個切點的後綴是否迴文」。Manacher 預處理一次 $O(|w|)$ 可全部 $O(1)$ 回答。**trie 負責配對，Manacher 負責迴文判斷，兩者是正交的。**\n- **與 bitset 交集**：745 若字串長不能容忍 $\\sum|w|^2$，改成兩棵 trie（正插與反插），節點上掛 `bitset<n>`，查詢時取交集後找最高位。$O(n/64)$ 每次查詢。**這是「兩維度合成單鍵」失效時的標準退路。**\n- **與 9.x 線段樹合併**：336 的 `palIdx`、745 的 `best`，本質上都是「在 trie 節點上聚合子樹資訊」。**當聚合的東西是可合併的集合（而非單一最大值）時，就會演變成線段樹合併或啟發式合併。**\n\n## 代表例題\n\n**題號**：211、212、336、425、527\n\n要共用前綴路徑；練習分清單詞終止、前綴計數與子節點結構。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 211 | [新增與搜尋單詞 - 資料結構設計](https://leetcode.cn/problems/design-add-and-search-words-data-structure) |  |\n| 212 | [單詞搜尋 II](https://leetcode.cn/problems/word-search-ii) |  |\n| 336 | [迴文對](https://leetcode.cn/problems/palindrome-pairs) |  |\n| 425 | [單詞方塊](https://leetcode.cn/problems/word-squares)（Premium） |  |\n| 527 | [單詞縮寫](https://leetcode.cn/problems/word-abbreviation)（Premium） |  |\n| 588 | [設計記憶體檔案系統](https://leetcode.cn/problems/design-in-memory-file-system) |  |\n| 616 | [給字串新增加粗標籤](https://leetcode.cn/problems/add-bold-tag-in-string) |  |\n| 642 | [設計搜尋自動補全系統](https://leetcode.cn/problems/design-search-autocomplete-system) |  |\n| 676 | [實現一個魔法字典](https://leetcode.cn/problems/implement-magic-dictionary) |  |\n| 745 | [字首和字尾搜尋](https://leetcode.cn/problems/prefix-and-suffix-search) |  |\n| 758 | [字串中的加粗單詞](https://leetcode.cn/problems/bold-words-in-string) |  |\n| 1065 | [字串的索引對](https://leetcode.cn/problems/index-pairs-of-a-string) | 1389 |\n| 1166 | [設計檔案系統](https://leetcode.cn/problems/design-file-system) | 1479 |\n| 1858 | [包含所有字首的最長單詞](https://leetcode.cn/problems/longest-word-with-all-prefixes) |  |\n| 1948 | [刪除系統中的重複資料夾](https://leetcode.cn/problems/delete-duplicate-folders-in-system) | 2534 |\n| 3045 | [統計前字尾下標對 II](https://leetcode.cn/problems/count-prefix-and-suffix-pairs-ii) | 2328 |\n| 3093 | [最長公共字尾查詢](https://leetcode.cn/problems/longest-common-suffix-queries) | 2118 |\n\n## 本節重點速查\n\n- **本節與 7.1 的分界：查詢路徑是否唯一。** 有分支就要 DFS 子樹，剪枝從優化變成必需。\n- **211**：前緣集合不變量（定理 1）保證 DFS 不重不漏。**按單字長度分桶是最有效的一行剪枝。** 複雜度取 $\\min(\\Sigma^d|w|,\\ |\\text{節點數}|)$ 兩個上界。\n- **212**：網格路徑與 trie 路徑**同步前進**（不變量 (S)）。兩個剪枝——**A：命中後把 `wordId` 設回 `-1`**（兼作去重）；**B：回溯時摘掉無孩子且無單字的節點**（每節點最多摘一次，攤還 $O(|\\text{節點}|)$，是 TLE/AC 的分界）。等價寫法是維護子樹剩餘單字數 `cnt`。別忘了回溯還原網格標記。\n- **336**：$A+B$ 迴文的完整分類——**$|A|\\ge|B|$ ⇒ $A$ 的前 $|B|$ 位 $=\\mathrm{rev}(B)$ 且 $A$ 的剩餘部分是迴文；$|A|<|B|$ ⇒ 對稱形式。** 必須兩種都處理，因為 trie 下走有方向。三個坑：空字串、$i=j$、$|A|=|B|$ 時的重複計數。\n- **425**：對稱約束 ⇒ **第 $k$ 行的前 $k$ 個字元被前 $k$ 行的第 $k$ 列完全決定**（既必要又充分）。回溯 + trie 的關鍵永遠是找出這種「可繼續」條件。\n- **745**：`後綴 + '{' + 完整字` 把兩個維度編碼成一個鍵。**前提是分隔符不在字元集內。** 空間會爆，用 `map` 孩子或改 bitset 交集。\n- **一維文字的多模式匹配用 AC 自動機，不要用本節的 DFS 剪枝**——後者是為了網格回溯才存在的。\n- **複雜度式子要把數字代進去乘一遍**，進階 trie 題的空間爆炸通常不在式子的形狀上，在常數 $\\Sigma$ 上。\n",
        },
        {
          id: 51,
          title: "7.3 字典樹優化 DP",
          summary:
            '## 這個技術解決什麼問題\n\n有一類 DP，狀態是「字串的前綴」，轉移是「切下一段合法單字」：\n\n$$f(i) \\leftarrow \\bigoplus_{\\substack{j < i \\\\ s[j..i) \\in \\mathrm{dict}}} g\\big(f(j)\\big)$$\n\n（$\\oplus$ 依題目是「或」（139 可行性）、「加」（計數）、「取最小」（面試題 17.13）、「收集」（140 列舉）。）\n\n**DP 本身完全沒有難度，難的是枚舉轉移。** 樸素做法對每個 $(j, i)$ 都去字典裡查 $s[j..i)$：\n\n- 用 `unordered_set<string>`：$O(n^2)$ 個子串，每個要構造 + 雜湊，$O(n^2 \\cdot \\bar{\\ell})$；\n- 逐一比對字典：$O(n \\cdot |\\mathrm{dict}| \\cdot \\bar{\\ell})$。\n\n**trie 的作用只有一個：把「從位置 $j$ 出發，有哪些 $i$ 使 $s[j..i)$ 是字典單字」這件事，從『每個 $i$ 各查一次』變成『沿字串走一趟，順路報告所有命中』。** 走的長度自然被字典中最長單字的長度 $\\ell_{\\max}$ 截斷，而且一旦走進不存在的孩子就立刻停止——**這個「提前終止」在實測中比複雜度式子顯示的還有效。**\n\n**先講清楚 trie 在這裡的定位：它不改變 DP 的狀態、不改變轉移的語意、不改變答案。它只是一個枚舉器。** 想不清楚 DP 就先別碰 trie；DP 想清楚了，trie 只是替換掉迴圈裡的那一句判斷。\n\n典型題：139 單字拆分（可行性）、140 單字拆分 II（列舉全部方案）、472 連接詞（每個字各做一次 139）、面試題 17.13 恢復空格（最小化未識別字元數）、2977 字串轉換的最小成本 II（轉移帶成本，且要先做 Floyd 閉包）。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「字典樹優化 DP」想：\n\n- **題目是「把字串切成字典單字」類**（139 單字拆分、140 單字拆分 II、472 連接詞、面試題 17.13 恢復空格），DP 轉移需要反覆判斷「某段子字串是不是字典單字」。\n- **字典很大或單字共用大量前綴，逐一比對太慢**，需要把「枚舉合法的下一段」做得更快：\n\n:::example 嗅到味道：DP 每步都要問「這段是不是字典裡的詞」 → 沿 trie 走順手判 isEn…\n```cpp\n// 嗅到味道：DP 每步都要問「這段是不是字典裡的詞」 → 沿 trie 走順手判 isEnd\n// if (p->isEnd) transfer(dp);\n```\n:::\n\n\n\n- **轉移帶成本/計數，且子串匹配是瓶頸**（如 2977 用兩本字典做字串轉換的最小成本，把可替換子串建成 trie 加速枚舉）。\n- **暴力 DP 是 `O(n × 字典大小 × 單字長)`**，trie 把「比對字典」這層折成「沿字串走一條路 + 剪枝」。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的 `dp[i]` 代表什麼？是可行性、方案數還是最小成本？邊界 `dp[0]` 設成什麼？\n2. 我從哪些起點 `j` 出發沿 trie 走？在哪一步（`isEnd`）做轉移、在哪一步剪枝（缺邊 break）？\n3. 答案在 `dp[n]` 嗎？要不要還原方案、需不需要記憶化？\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n本節要解決的場景是「字串切分型的動態規劃（DP）」：給一個長字串 `s` 和一本字典，問「能不能把 `s` 切成字典裡的單字」「有幾種切法」「最少切幾段」等等（單字拆分系列）。這類題的標準 DP 是：令 `dp[i]` 表示「`s` 的前 `i` 個字元能不能（或最佳地）被切好」，轉移時要枚舉「最後一段是哪個字典單字」。\n\n:::example 樸素轉移：對每個 i，逐一試每個字典單字（重複比較多）\n樸素轉移的瓶頸在這裡：對每個位置 `i`，要回頭嘗試「以 `i` 結尾的每一個可能單字」，若直接拿字典一個個比對子字串，會做大量重複的字元比較。\n\n```cpp\n// 樸素轉移：對每個 i，逐一試每個字典單字（重複比較多）\nfor (int i = 1; i <= n; ++i) {\n    for (auto& w : dict) {  // 每個單字都要比一次子字串\n        if (i >= w.size() && s.substr(i - w.size(), w.size()) == w &&\n                dp[i - w.size()]) {\n            dp[i] = true;\n        }\n    }\n}\n```\n:::\n\n\n\n:::example trie 優化轉移：從 j 出發沿 s 在 trie 上走，遇 isEnd 就轉移\n「字典樹優化 DP」的核心想法是：**把字典建成 trie，從某個起點 `j` 沿著 `s` 的字元在 trie 上往前走，一邊走一邊就能知道「`s[j..k]` 是不是字典裡的某個單字」**——只要走到的 trie 節點有 `isEnd` 標記，就代表 `s[j..k]` 是合法單字，可以做一次轉移。這把「比對所有單字」變成「沿字串走一條路、順手檢查每個 `isEnd`」，共享前綴的比較只做一次。\n\n```cpp\n// trie 優化轉移：從 j 出發沿 s 在 trie 上走，遇 isEnd 就轉移\nNode* p = root;\nfor (int k = j; k < n; ++k) {\n    int c = s[k] - \'a\';\n    if (!p->child[c]) {\n        break;  // 沒有單字以 s[j..k] 開頭 → 剪枝停走\n    }\n    p = p->child[c];\n    if (p->isEnd) {                           // s[j..k] 是一個字典單字\n        dp[k + 1] = combine(dp[k + 1], dp[j]);  // 做一次 DP 轉移\n    }\n}\n```\n:::\n\n\n\n關鍵優勢有兩個：一是**共用前綴只比一次**；二是**走到 trie 沒有對應邊就能立刻 break**，省掉所有不可能的更長嘗試。**整個技巧就是「DP 負責記住子問題答案，trie 負責高效地枚舉每一步的合法轉移」。**\n\n## 狀態／資料結構定義\n\n**DP 狀態**（以 $s$ 的長度 $n$ 為界，全部是一維前綴狀態）：\n\n| 題目 | 狀態 | 初值 | 轉移 | 答案 |\n| --- | --- | --- | --- | --- |\n| 139 | `f[i]`：`s[0..i)` 可否完全拆分 | `f[0] = true` | 或 | `f[n]` |\n| 140 | `g[j]`：`s[j..n)` 的所有拆分方案 | `g[n] = {""}` | 收集 | `g[0]` |\n| 472 | 對每個字各跑一次 139 | 同上 | 或 | 逐字判定 |\n| 17.13 | `f[i]`：`s[0..i)` 最少未識別字元數 | `f[0] = 0` | 取 min | `f[n]` |\n| 2977 | `f[i]`：改造 `s[0..i)` 的最小成本 | `f[0] = 0` | 取 min | `f[n]` |\n\n**trie 結構**（7.1 的寫法 A 即可，但要注意方向）：\n\n```cpp\nstruct Node {\n    Node* ch[26]{};\n    bool  isEnd = false;   // 139/140/472\n    int   id    = -1;      // 17.13 需要單字長度；2977 需要單字編號\n};\n```\n\n**方向的選擇是本節唯一的結構性決策**（見定理 2）：\n\n- **正向 trie（單字照原樣插入）**：搭配**刷表法**——外層 $j$ 從小到大，若 `f[j]` 有效，就從 $j$ 沿 trie 往右走，命中就更新 `f[i]`。\n- **反向 trie（單字反轉後插入）**：搭配**填表法**——外層 $i$ 從小到大，從 $i-1$ 往左走，命中就用 `f[j]` 更新 `f[i]`。\n\n**17.13 用反向 trie 更順**，因為它的轉移還有一項 `f[i] = f[i-1] + 1`（把第 $i-1$ 個字元丟棄），填表法能把兩項寫在一起。**139/140 用正向 trie 更順。**\n\n**140 的 `g` 存的是「後綴的所有拆分」而不是「前綴的」**——因為要拼句子時前面的詞先出現，用後綴狀態遞迴時字串拼接方向自然。**記憶化用 `unordered_map<int, vector<string>>` 或 `vector<vector<string>>` 皆可。**\n\n## 不變量或正確性證明\n\n### 定理 1（trie 走訪恰好枚舉出所有轉移）\n\n**命題**：從位置 $j$ 出發，令 $u_0 = \\text{root}$，$u_{t+1} = u_t\\text{.ch}[s[j+t]]$（缺孩子則停止）。則\n\n$$\\{\\, i > j : s[j..i) \\in \\mathrm{dict} \\,\\} \\;=\\; \\{\\, j+t : u_t \\text{ 存在且 } u_t.\\texttt{isEnd} \\,\\}$$\n\n且走訪按 $t$ 遞增，**即按 $i$ 遞增的順序報告命中**。\n\n*證明*。由 7.1 的不變量 (T)，$u_t$ 存在 $\\iff$ $s[j..j{+}t)$ 是某個字典單字的前綴；由 7.1 定理 2，$u_t.\\texttt{isEnd}$ $\\iff$ $s[j..j{+}t) \\in \\mathrm{dict}$。兩者合起來即得等式。順序由迴圈變數 $t$ 遞增給出。$\\square$\n\n**兩個推論，都是效率的來源：**\n\n> **推論 1.1（提前終止是安全的）**：$u_t$ 不存在時可立即 `break`。因為 $s[j..j{+}t)$ 已不是任何字典單字的前綴，由前綴的傳遞性，$s[j..j{+}t\')$（$t\' > t$）也不是任何字典單字的前綴，更不可能是字典單字。\n\n> **推論 1.2（走訪長度 $\\le \\ell_{\\max}$）**：trie 的深度不超過最長單字長度，故 $t \\le \\ell_{\\max}$。\n\n**推論 1.1 才是重點。** 複雜度式子只保證 $O(n\\ell_{\\max})$，但實際上大多數位置走幾步就撞牆——**字典越「不像」目標字串，trie 越快，而暴力法對此毫無感覺。**\n\n### 定理 2（方向決定寫法：正向 trie 必須刷表，填表必須反向 trie）\n\n**命題**：trie 的走訪只能沿字串**遞增**方向進行（因為邊是「下一個字元」）。因此：\n\n- 用**正向 trie**（單字照原樣插入），從 $j$ 出發只能發現「以 $j$ 為**左**端點」的合法段，所以必須寫成**刷表法**：`for j: if (f[j]) for i in trie_walk(j): update f[i]`。\n- 要寫**填表法**（`for i: f[i] = combine over j`），就得從 $i$ 往**左**枚舉左端點，也就是要能「從右端點出發匹配」——這需要把單字**反轉後**插入，並沿 $s$ 反向讀取字元。\n\n*證明*：trie 的節點對應「從根沿邊讀出的字串」，走一步就消耗字串的下一個字元。要匹配 $s[j..i)$，起點必須是 $j$（正向）或 $i-1$ 往左（若 trie 中存的是反轉單字，則沿 $s[i-1], s[i-2], \\dots$ 走恰好匹配 $\\mathrm{rev}(s[j..i))$，而 $\\mathrm{rev}(s[j..i)) \\in \\mathrm{rev}(\\mathrm{dict}) \\iff s[j..i) \\in \\mathrm{dict}$）✔。$\\square$\n\n**這條看似瑣碎，但它是本節最常見的實作錯誤源頭**：拿正向 trie 卻想寫填表法，於是寫成「對每個 $i$，對每個 $j$，從 $j$ 走到 $i$」——複雜度退回 $O(n^2)$，trie 白建了。**寫之前先決定刷表還是填表，再決定 trie 的方向。**\n\n*另一個等價視角*：刷表法的 `f[j]` 在被使用時必須已經最終確定。因為轉移只從 $j$ 到 $i > j$（**單向、無環**），按 $j$ 遞增處理即可——**這就是無後效性在這裡的具體形式。**\n\n### 定理 3（139 的正確性：最優子結構與無後效性）\n\n**命題**：`f[i] = true` $\\iff$ $s[0..i)$ 可以拆成若干個字典單字。\n\n*證明*（對 $i$ 強歸納）。\n- **$i=0$**：空串拆成零個單字，`f[0] = true` ✔。\n- **($\\Leftarrow$)** 設 $s[0..i)$ 有一個合法拆分，其**最後一段**為 $s[j..i)$，則 $s[j..i) \\in \\mathrm{dict}$ 且 $s[0..j)$ 有合法拆分。由歸納假設 `f[j] = true`；由定理 1，走訪從 $j$ 出發時會報告 $i$；故 `f[i]` 被設為 `true` ✔。\n- **($\\Rightarrow$)** 設 `f[i]` 被設為 `true`，則存在 $j$ 使 `f[j] = true` 且 $s[j..i) \\in \\mathrm{dict}$。由歸納假設 $s[0..j)$ 有合法拆分，接上 $s[j..i)$ 即得 $s[0..i)$ 的合法拆分 ✔。$\\square$\n\n**這個證明依賴兩件事，兩件事都值得點名**：\n\n1. **「最後一段」的存在性與唯一枚舉**：任何非空拆分都有唯一的最後一段，且它的左端點 $j$ 在 $[0, i)$ 中——**枚舉 $j$ 就窮盡了所有拆分**。這是所有「線性劃分 DP」的共同骨架。\n2. **無後效性**：$s[0..j)$ 能否拆分，**與 $s[j..i)$ 怎麼切、與 $i$ 之後是什麼，完全無關**。所以 `f[j]` 可以先算好、算一次、重複使用。**若題目加上「相鄰兩段不能是同一個單字」之類的約束，這條就破了，狀態必須擴充**——這是判斷「能不能直接套本節模板」的檢查點。\n\n### 定理 4（140 的複雜度本質上是輸出敏感的，記憶化不能改變這一點）\n\n**命題**：140 的答案數量最壞是 $\\Theta(2^{n/2})$，因此任何列舉全部方案的演算法在最壞情況下都是指數級。**記憶化能消除「重複探索」，但不能消除「輸出本身很大」。**\n\n*下界構造*：$s = \\texttt{"aaaa...a"}$（$n$ 個 `a`），$\\mathrm{dict} = \\{\\texttt{"a"}, \\texttt{"aa"}\\}$。拆分數 $=$ 用 1 和 2 組成 $n$ 的方法數 $= F_{n+1}$（費氏數），即 $\\Theta(\\varphi^{n})$ ✔。\n\n*記憶化後的成本*：設 `g[j]` 為 $s[j..n)$ 的方案列表。則\n$$\\text{總成本} = O\\!\\big(n\\,\\ell_{\\max}\\big) \\;+\\; \\sum_{j} \\big|\\,\\texttt{g}[j]\\,\\big| \\cdot O(\\text{單句長度})$$\n第一項是 trie 走訪，第二項是**輸出的總體積**。**記憶化把第一項從指數壓成多項式，第二項不可能壓縮**（你必須把答案寫出來）。$\\square$\n\n**未記憶化的版本更糟**：$s = \\texttt{"aaa...ab"}$、$\\mathrm{dict} = \\{\\texttt{"a"},\\texttt{"aa"}\\}$，**答案是空的**，但樸素 DFS 會探索 $\\Theta(\\varphi^n)$ 條路徑才發現每條都在末尾失敗。**這是 140 的經典 TLE 測資。** 記憶化（`g[j]` 記為空列表）立刻把它變成 $O(n\\ell_{\\max})$。\n\n**實務建議**：先跑一次 139 的 `f` 陣列，DFS 時檢查 `f[j]`——**這對「有解」的情形沒幫助（成本仍是輸出量），對「部分位置無解」的情形則能砍掉大量分支。** 這是一個「不改變最壞複雜度、但顯著改善實際表現」的剪枝，值得寫。\n\n### 定理 5（472 連接詞：按長度遞增處理的正確性）\n\n*定義*：連接詞 = 完全由字典中**至少兩個**較短單字串接而成的字。\n\n**命題**：把字典按長度**非遞減**排序後依序處理；處理單字 $w$ 時，trie 中只插入了所有長度**嚴格小於** $|w|$ 的單字。此時對 $w$ 跑 139，結果正確。\n\n*證明*。\n- **每個組成部分嚴格更短**：$w$ 由 $k \\ge 2$ 個非空單字串接而成，每個部分的長度 $\\le |w| - 1 < |w|$（因為其他部分至少佔 1 個字元）✔。**這裡用到「字典中無空字串」——這正是為什麼必須特判空字串。**\n- 故所有可能的組成部分都在「長度 $< |w|$」的集合中，而該集合已完整插入 trie ✔。\n- 反之，trie 中的每個字都是字典單字，故找到的拆分都合法 ✔。$\\square$\n\n**兩個實作細節，都由證明直接讀出**：\n\n1. **必須先把所有長度 $< |w|$ 的字插完，才處理 $w$**。同長度的字之間互不影響（長度 $L$ 的字無法由若干長度 $L$ 的字串接而成，因為那至少要 $2L > L$），所以「同長度批次插入」與「逐一插入」都可以，**但絕不能把 $w$ 自己先插進去再判它**——那樣每個字都會被判成「由自己一段組成」。\n2. **必須要求 $k \\ge 2$**。實作上就是：跑 139 時，`f[0] = true`，但**不允許整段 $w$ 直接命中**——由第 1 點，$w$ 尚未插入 trie，所以這自動成立。**這是「先排序後插入」這個設計最漂亮的地方：$k\\ge2$ 的約束被結構本身保證了，不需要額外判斷。**\n\n### 定理 6（面試題 17.13：加入「丟棄字元」轉移後仍保持最優子結構）\n\n$$f(i) = \\min\\Big(\\underbrace{f(i-1) + 1}_{\\text{丟棄 } s[i-1]},\\ \\min_{\\substack{j<i,\\ s[j..i)\\in\\mathrm{dict}}} f(j)\\Big),\\qquad f(0)=0.$$\n\n*正確性*：任何一種「標記方式」（把 $s$ 的某些不相交子串標記為字典單字，其餘字元算未識別）都可以按**最後一個字元**分類：它要嘛屬於某個被標記的單字（則該單字為 $s[j..i)$，剩下的是 $s[0..j)$ 的子問題），要嘛未被識別（剩下的是 $s[0..i-1)$ 的子問題）。**兩類互斥且窮盡**，故遞推式正確 ✔。\n\n*無後效性*：$f(j)$ 的值不依賴 $j$ 之後如何標記——**因為「未識別字元數」是可加的，且標記區間不相交。** 若題目改成「未識別字元必須連續」或「相鄰單字不能重複」，可加性就破了。$\\square$\n\n**這題用反向 trie**：外層 $i$ 遞增，內層從 $s[i-1]$ 往左走，命中深度 $d$ 時得到 $j = i-d$。**兩項轉移都在同一個 $i$ 的迴圈裡，程式最短。**\n\n## 逐步演算法\n\n:::example 把字典建成 trie，結尾標記\n**1. 建字典樹。** 先把字典所有單字插入 trie，結尾打 `isEnd`（轉換成本題還可在結尾掛上單字長度、成本等資訊）。\n\n```cpp\n// 把字典建成 trie，結尾標記\nstruct Node {\n    Node* child[26] = {};\n    bool isEnd = false;\n};\nNode* root = new Node();\nfor (auto& w : dict) {\n    Node* p = root;\n    for (char ch : w) {\n        int c = ch - \'a\';\n        if (!p->child[c]) {\n            p->child[c] = new Node();\n        }\n        p = p->child[c];\n    }\n    p->isEnd = true;\n}\n```\n:::\n\n\n\n:::example 例：單字拆分（可行性），dp[i] = 前 i 個字元可否切好\n**2. 定義 DP 狀態。** 依題意決定 `dp[i]` 的含義：可行性（bool）、方案數（int）、或最小成本（取 min）。設好邊界 `dp[0]`。\n\n```cpp\n// 例：單字拆分（可行性），dp[i] = 前 i 個字元可否切好\nvector<char> dp(n + 1, false);\ndp[0] = true;  // 空字串視為可切\n```\n:::\n\n\n\n:::example 轉移主體\n**3. 雙層迴圈轉移：外層起點 j，內層沿 trie 走。** 只從「已可達」的起點出發（`dp[j]` 為真／有限），沿 `s` 在 trie 上前進，遇 `isEnd` 就更新 `dp[k + 1]`，遇缺邊就 break。\n\n```cpp\n// 轉移主體\nfor (int j = 0; j < n; ++j) {\n    if (!dp[j]) {\n        continue;  // 起點不可達就跳過\n    }\n    Node* p = root;\n    for (int k = j; k < n; ++k) {\n        int c = s[k] - \'a\';\n        if (!p->child[c]) {\n            break;  // 剪枝\n        }\n        p = p->child[c];\n        if (p->isEnd) {\n            dp[k + 1] = true;  // 可行性版本；計數版改成 += dp[j]\n        }\n    }\n}\n```\n:::\n\n\n\n:::example 整個字串能否被切好\n**4. 讀答案與複雜度。** 答案通常在 `dp[n]`（整串的結果）。\n\n```cpp\n// 整個字串能否被切好\nreturn dp[n];\n```\n:::\n\n\n\n- 計數版用 `dp[k + 1] += dp[j]`，最小成本版用 `dp[k + 1] = min(dp[k + 1], dp[j] + cost)`。\n- 需要還原方案（如 140 輸出所有切法）時，沿 trie 找到每個合法切點後做記憶化回溯，避免指數重算。\n- 複雜度：建樹 `O(字典總長)`；DP 轉移 `O(n × L)`（L 為從某點出發在 trie 上能走的最長距離），優於樸素的「每點比對整本字典」。\n\n## C++17 模板\n\n**模板一：139 單字拆分（正向 trie + 刷表法）**\n\n```cpp\nclass Solution {\n    struct Node {\n        Node* ch[26]{};\n        bool isEnd = false;\n    };\npublic:\n    bool wordBreak(string s, vector<string>& wordDict) {\n        Node* root = new Node();\n        for (const string& w : wordDict) {\n            Node* u = root;\n            for (char c : w) {\n                int k = c - \'a\';\n                if (!u->ch[k]) u->ch[k] = new Node();\n                u = u->ch[k];\n            }\n            u->isEnd = true;\n        }\n        int n = s.size();\n        vector<char> f(n + 1, false);\n        f[0] = true;\n        for (int j = 0; j < n; ++j) {\n            if (!f[j]) continue;                 // 刷表法：只從可達狀態出發\n            Node* u = root;\n            for (int i = j; i < n; ++i) {\n                int k = s[i] - \'a\';\n                if (!u->ch[k]) break;            // 推論 1.1：提前終止\n                u = u->ch[k];\n                if (u->isEnd) f[i + 1] = true;   // s[j..i] 是單字\n            }\n        }\n        return f[n];\n    }\n};\n```\n\n**模板二：472 連接詞（按長度排序 + 增量插入）**\n\n```cpp\nclass Solution {\n    struct Node {\n        Node* ch[26]{};\n        bool isEnd = false;\n    };\n    Node* root = new Node();\n\n    void insert(const string& w) {\n        Node* u = root;\n        for (char c : w) {\n            int k = c - \'a\';\n            if (!u->ch[k]) u->ch[k] = new Node();\n            u = u->ch[k];\n        }\n        u->isEnd = true;\n    }\n    bool splittable(const string& s) {           // 就是模板一的 wordBreak\n        int n = s.size();\n        vector<char> f(n + 1, false);\n        f[0] = true;\n        for (int j = 0; j < n; ++j) {\n            if (!f[j]) continue;\n            Node* u = root;\n            for (int i = j; i < n; ++i) {\n                int k = s[i] - \'a\';\n                if (!u->ch[k]) break;\n                u = u->ch[k];\n                if (u->isEnd) f[i + 1] = true;\n            }\n        }\n        return f[n];\n    }\npublic:\n    vector<string> findAllConcatenatedWordsInADict(vector<string>& words) {\n        sort(words.begin(), words.end(),\n             [](const string& a, const string& b) { return a.size() < b.size(); });\n        vector<string> ans;\n        for (const string& w : words) {\n            if (w.empty()) continue;             // 空字串必須跳過（定理 5）\n            if (splittable(w)) ans.push_back(w); // 此時 trie 只含更短的字\n            insert(w);                           // 判完才插入自己\n        }\n        return ans;\n    }\n};\n```\n\n**模板三：面試題 17.13 恢復空格（反向 trie + 填表法）**\n\n```cpp\nclass Solution {\n    struct Node {\n        Node* ch[26]{};\n        bool isEnd = false;\n    };\npublic:\n    int respace(vector<string>& dictionary, string sentence) {\n        Node* root = new Node();\n        for (const string& w : dictionary) {\n            Node* u = root;\n            for (int t = w.size() - 1; t >= 0; --t) {   // 反轉插入\n                int k = w[t] - \'a\';\n                if (!u->ch[k]) u->ch[k] = new Node();\n                u = u->ch[k];\n            }\n            u->isEnd = true;\n        }\n        int n = sentence.size();\n        vector<int> f(n + 1, 0);\n        for (int i = 1; i <= n; ++i) {\n            f[i] = f[i - 1] + 1;                        // 丟棄 s[i-1]\n            Node* u = root;\n            for (int j = i - 1; j >= 0; --j) {          // 從右端點往左走\n                int k = sentence[j] - \'a\';\n                if (!u->ch[k]) break;\n                u = u->ch[k];\n                if (u->isEnd) f[i] = min(f[i], f[j]);   // s[j..i) 是單字\n            }\n        }\n        return f[n];\n    }\n};\n```\n\n**模板四：140 單字拆分 II（記憶化 + 可行性前置剪枝）**\n\n```cpp\nclass Solution {\n    unordered_map<int, vector<string>> memo;\n    vector<char> feasible;                        // 先跑一次 139\npublic:\n    vector<string> wordBreak(string s, vector<string>& wordDict) {\n        unordered_set<string> dict(wordDict.begin(), wordDict.end());\n        int n = s.size(), maxLen = 0;\n        for (const string& w : wordDict) maxLen = max(maxLen, (int)w.size());\n        feasible.assign(n + 1, false);\n        feasible[n] = true;\n        for (int j = n - 1; j >= 0; --j)\n            for (int L = 1; L <= maxLen && j + L <= n; ++L)\n                if (feasible[j + L] && dict.count(s.substr(j, L))) {\n                    feasible[j] = true;\n                    break;\n                }\n        return dfs(s, 0, dict, maxLen);\n    }\nprivate:\n    vector<string> dfs(const string& s, int j,\n                       const unordered_set<string>& dict, int maxLen) {\n        if (j == (int)s.size()) return {""};\n        auto it = memo.find(j);\n        if (it != memo.end()) return it->second;\n        vector<string> res;\n        for (int L = 1; L <= maxLen && j + L <= (int)s.size(); ++L) {\n            if (!feasible[j + L]) continue;       // 剪枝：右邊拆不完就別遞迴\n            string w = s.substr(j, L);\n            if (!dict.count(w)) continue;\n            for (const string& rest : dfs(s, j + L, dict, maxLen))\n                res.push_back(rest.empty() ? w : w + " " + rest);\n        }\n        return memo[j] = res;\n    }\n};\n```\n\n（140 的字典很小時用 `unordered_set` 比 trie 更短；**若字典大到雜湊成為瓶頸，把內層換成模板一的 trie 走訪即可，DP 結構不變**——這正是定理 1 所說的「trie 只是枚舉器」。）\n\n## 時間與空間複雜度\n\n設 $n = |s|$、$m = |\\mathrm{dict}|$、$\\ell_{\\max}$ 為最長單字長、$L = \\sum_{w}|w|$。\n\n| 題目 | 樸素 | trie 版 | 空間 |\n| --- | --- | --- | --- |\n| 139 | $O(n^{2}\\ell_{\\max})$（`substr` + 雜湊） | $O(n\\,\\ell_{\\max} + L)$ | $O(n + \\Sigma L)$ |\n| 472 | $O(m\\,\\bar{\\ell}^{2}\\,\\ell_{\\max})$ | $O\\!\\left(\\sum_w |w|\\,\\ell_{\\max} + L\\right)$ | $O(\\Sigma L)$ |\n| 17.13 | $O(n^{2}\\ell_{\\max})$ | $O(n\\,\\ell_{\\max} + L)$ | $O(n + \\Sigma L)$ |\n| 140 | 指數（無記憶化） | $O(n\\,\\ell_{\\max} + \\lvert\\text{輸出}\\rvert)$ | $O(\\lvert\\text{輸出}\\rvert)$ |\n| 2977 | — | $O\\!\\left(m^{3} + n\\cdot\\text{命中數}\\right)$ | $O(\\Sigma L + m^{2})$ |\n\n**$O(n\\ell_{\\max})$ 這個式子要理解，不要背**：外層 $n$ 個起點，每個起點沿 trie 最多走 $\\ell_{\\max}$ 步（推論 1.2），每步 $O(1)$。**跟字典大小 $m$ 無關**——這是 trie 相對於「逐一比對」的全部收益。\n\n**與 `unordered_set` + `substr` 的真實差距**：後者是 $O(n\\ell_{\\max})$ 次 `substr`（每次配置一個 `string`，$O(\\ell)$ 拷貝 + 可能的堆配置）加上雜湊計算。**式子上都是 $O(n\\ell_{\\max}^{?})$ 級別，但常數差 5～20 倍**，且 trie 有提前終止。$n = 300$、$\\ell_{\\max} = 20$ 的 139 兩者都能過；$n \\ge 10^4$ 就必須用 trie。\n\n**2977 的 $m^3$** 來自 Floyd：轉換成本要先在「原字串 ↔ 目標字串」的有向圖上求傳遞閉包的最短路，**因為一次替換的結果可以再被替換**。這一步與 trie 無關，但是這題的正確性關鍵——**沒做閉包會漏掉多步替換的更便宜路徑。** 枚舉命中則用 AC 自動機（7.2）比純 trie 更省。\n\n## 常見錯誤與邊界條件\n\n- **拿正向 trie 卻寫填表法**（定理 2）。症狀是「對每個 $i$，對每個 $j$，重新從 $j$ 走一遍」，複雜度退回 $O(n^2\\ell_{\\max})$，trie 完全白建。**先決定刷表／填表，再決定 trie 方向。**\n- **刷表法忘記 `if (!f[j]) continue;`**。不僅慢，在 140 那種「收集方案」的變體裡會直接產生錯誤答案（從不可達的狀態刷出方案）。\n- **`u->isEnd` 檢查的位置**。必須在**移動到孩子之後**檢查，而不是移動之前——否則會把根（空字串）誤判為單字，造成 `f[j] → f[j]` 的自環。\n- **472 忘記跳過空字串**。輸入含 `""` 時，`splittable("")` 回傳 `f[0] = true`，空字串被誤判為連接詞。**定理 5 的證明明確用到「無空字串」這個前提。**\n- **472 先插入自己再判定**。每個字都會被判成連接詞。**順序必須是「判定 → 插入」。**\n- **472 用字典序排序而非長度排序**。`sort(words.begin(), words.end())` 是字典序，`"aa"` 會排在 `"b"` 前面但 `"b"` 更短——**長度順序才是定理 5 需要的。**\n- **140 沒有記憶化**。`"aaa...ab"` 直接 TLE（定理 4）。\n- **140 的空字串拼接**。`g[n] = {""}` 是哨兵，回傳時要寫 `rest.empty() ? w : w + " " + rest`，否則句尾會多一個空格。\n- **17.13 忘記 `f[i] = f[i-1] + 1` 這一項**。只寫字典命中的轉移，會讓「沒有任何單字結尾於 $i$」的位置停在初值 $+\\infty$。\n- **字元集假設**。模板都假設小寫字母。**題目若含大寫或數字，`ch[26]` 會越界**——這是 UB，可能表現為隨機錯誤答案而非崩潰。\n- **`new` 出來的節點沒回收**。LeetCode 上不影響判題，但**在多測資的本地測試或「設計題反覆建樹」時會累積成 MLE**。用 `vector<Node>` 池或 `deque<Node>` 配置更安全（注意 `vector` 擴容會使指標失效，池要 `reserve` 或改存索引）。\n- **狀態擴充的信號**。題目若加上「相鄰兩段不能相同」「總段數不超過 $k$」「每個單字最多用一次」，定理 3 的無後效性就破了——**必須把違反可加性的那個量加進狀態**，本節的一維模板不再適用。\n\n## 與相似技巧的比較\n\n- **與 `unordered_set` + `substr`**：語意完全等價（定理 1 說明兩者枚舉的是同一個集合）。**字典小、$n$ 小 ⇒ 用 set，程式短；$n$ 大或字典總長大 ⇒ 用 trie。** 決定因素是常數與提前終止，不是漸近複雜度。\n- **與字串雜湊（滾動雜湊）**：可以 $O(1)$ 取任意子串的雜湊值，把 139 做到 $O(n\\ell_{\\max})$ 且常數極小。**缺點是有碰撞風險，且不支援「提前終止」**——雜湊必須算完整個子串才知道不在字典裡，trie 走兩步就能放棄。\n- **與 AC 自動機**：AC 自動機能在 $O(n + \\text{命中數})$ 內找出「$s$ 中所有字典單字的所有出現位置」，**比本節的 $O(n\\ell_{\\max})$ 更優**。代價是建自動機（BFS 求 fail 指標）與程式長度。**$\\ell_{\\max}$ 很大（例如 $10^3$）而 $n$ 也大時，AC 自動機是唯一選擇；2977 就是這種規模。** $\\ell_{\\max}$ 小（$\\le 20$）時 trie 完全夠用。\n- **與 7.1／7.2**：7.1 是查詢單一字串，7.2 是查詢帶分支。**本節的走訪其實就是 7.1 的形態（路徑唯一），只是重複執行 $n$ 次、且中途每個 `isEnd` 都要用**——差別在「沿路收集所有命中」而非「走到底才回答」。\n- **與區間 DP**：本節是**線性劃分 DP**（狀態是前綴，轉移枚舉最後一段），$O(n)$ 狀態、$O(\\ell_{\\max})$ 轉移。**若合法段的定義依賴段內的其他資訊（例如「這一段本身要是迴文」），就變成 132 分割迴文串 II 那類題**——結構相同，只是把 trie 換成迴文預處理表。**「把『某段是否合法』的判斷抽出來單獨加速」是這一整族題的統一思路。**\n- **與 KMP / Z 函數**：那些解決的是「單一模式在文字中的出現」，本節是「多個模式」。**單模式用 KMP，多模式用 trie 或 AC 自動機。**\n\n## 代表例題\n\n**題號**：139、140、472、面試題 17.13、2977\n\n要共用前綴路徑；練習分清單詞終止、前綴計數與子節點結構。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 139 | [單詞拆分](https://leetcode.cn/problems/word-break) |  |\n| 140 | [單詞拆分 II](https://leetcode.cn/problems/word-break-ii) |  |\n| 472 | [連線詞](https://leetcode.cn/problems/concatenated-words) |  |\n| 面試題 17.13 | [恢復空格](https://leetcode.cn/problems/re-space-lcci)（Premium） |  |\n| 2977 | [轉換字串的最小成本 II](https://leetcode.cn/problems/minimum-cost-to-convert-string-ii) | 2696 |\n\n## 本節重點速查\n\n- **trie 在 DP 裡只是枚舉器**：它不改變狀態、不改變轉移語意、不改變答案。**DP 想不清楚就別碰 trie。**\n- **核心命題（定理 1）**：從 $j$ 沿 trie 走，恰好按 $i$ 遞增報告所有使 $s[j..i)\\in\\mathrm{dict}$ 的 $i$。兩個推論：**撞牆立刻 `break` 是安全的**（前綴傳遞性）、**走訪長度 $\\le \\ell_{\\max}$**。\n- **方向決定寫法（定理 2）**：**正向 trie ⇒ 刷表法**（`for j: if(f[j]) walk`）；**填表法 ⇒ 反向 trie**（單字反轉插入，從 $i-1$ 往左走）。搞混就退回 $O(n^2)$。\n- **139 的兩個前提**：最後一段的枚舉窮盡所有拆分；**未識別／已識別的量可加 ⇒ 無後效性**。題目加上「相鄰段不同」之類的約束就要擴充狀態。\n- **140 是輸出敏感的**：答案數可達 $\\Theta(\\varphi^n)$，記憶化只能消除重複探索，不能壓縮輸出。**`"aaa...ab"` 是經典 TLE 測資**；先跑 139 的可行性表做剪枝。\n- **472 的三個順序性要求**：**按長度非遞減排序**（不是字典序）、**判定完才插入自己**、**跳過空字串**。$k\\ge2$ 的約束由「尚未插入自己」自動保證，不需額外判斷。\n- **17.13 別漏掉 `f[i] = f[i-1] + 1`** 這一項。\n- **複雜度是 $O(n\\ell_{\\max} + L)$，與字典大小 $m$ 無關**——這是全部收益所在。\n- **$\\ell_{\\max}$ 大到不能接受時換 AC 自動機**（$O(n + \\text{命中數})$）；字典小、$n$ 小時 `unordered_set` 就夠。\n- **`ch[26]` 假設小寫字母**，題目字元集不同時會越界成 UB。\n',
        },
        {
          id: 52,
          title: "7.4 0-1 字典樹（異或字典樹）",
          summary:
            "## 這個技術解決什麼問題\n\n一組整數 $S$，反覆問「給定 $x$，在 $S$ 裡挑一個 $y$，使 $x \\oplus y$ 最大／最小」，或「$S$ 中有多少對的異或值落在某範圍內」。\n\n暴力是每次查詢掃全部 $O(|S|)$，$n$ 與查詢數都到 $10^5$ 時就是 $10^{10}$。\n\n**核心想法**：*把每個整數看成固定長度的二進位字串（高位在前），插進一棵 $\\Sigma = 2$ 的 trie。* 於是：\n\n- **異或的「逐位」性質**與 trie 的「逐層」結構完全對齊——第 $b$ 層的分支恰好對應第 $b$ 位的取值；\n- 「最大化 $x \\oplus y$」變成「從最高位開始，每層都盡量走與 $x$ 當前位**相反**的孩子」，$O(\\text{位數})$；\n- 節點上掛 `cnt`（子樹中的數字個數），就能做**前綴計數**——「異或值 $<$ 某個界」的對數，$O(\\text{位數})$。\n\n**與 7.1 的關係**：**0-1 trie 就是 $\\Sigma=2$ 的 trie，沒有任何新結構。** 但因為所有「字串」等長，它有兩個特殊性質，直接簡化了寫法：\n\n1. **每次查詢必定恰好走滿 $B$ 層，不會中途走不動**（只要 trie 非空——見定理 2 的前提）；\n2. **不需要 `isEnd`**——終點深度固定，`isEnd` 沒有資訊量。用 `cnt` 取代它。\n\n**辨識訊號**：題面出現「XOR / 異或」＋「最大／最小／計數」＋「從一堆數裡挑」。看到這三件事湊齊，先想 0-1 trie。**只有單一固定的 $x$、沒有反覆查詢的話，排序或雜湊可能更簡單**（見「與相似技巧的比較」）。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「0-1 字典樹」想：\n\n- **題目出現「XOR（異或）最大／最小」，而且要在一堆數裡選一個配對。** 這幾乎是 0-1 trie 的招牌，例如 421「兩個數的最大異或值」。\n- **想對 XOR 結果做「逐位最佳化」**：高位優先、能湊 1 就湊 1——這種貪心天生配合從高位往下走的樹。\n- **暴力是每次查詢都掃全部數字（`O(n)`），而資料量大到不能接受。** trie 把單次查詢壓到 `O(位數)`。\n- **有「異或值落在某範圍內的數對數量」之類的計數需求**，例如 1803，可用節點上的 `cnt` 做前綴計數。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要最佳化的是不是「XOR 的逐位結果」，而且高位優先？\n2. 我把數字攤成幾位？最高位夠不夠涵蓋最大值？\n3. 查詢時我每一位想走相同邊還是相反邊，為什麼？\n\n## 核心想法與直覺\n\n本節要解決的場景是：給一堆整數，反覆問「某個數和集合裡的哪個數做 XOR（異或）會最大？」。暴力做法是每次都拿它跟全部數字配對算一次，一次查詢就 `O(n)`，太慢。\n\n先複習 XOR：兩個位元相同得 0、不同得 1。所以「想讓 XOR 結果大」，就要讓**高位儘量湊出不同的位元**——高位的 1 比一堆低位的 1 加起來還值錢。\n\n:::example 0-1 字典樹的節點：只有兩條邊，對應這一位是 0 或 1\n關鍵工具是「0-1 字典樹（trie）」。一般字典樹是把字串一個字一個字往下拆成樹；這裡換成把整數攤成固定長度的二進位（例如 30 位），每個數從最高位往最低位走，每一位只有兩種選擇：走 `0` 那條邊，或走 `1` 那條邊。相同前綴的數會共用同一段路徑。\n\n```cpp\n// 0-1 字典樹的節點：只有兩條邊，對應這一位是 0 或 1\nstruct Node {\n    Node* child[2] = {nullptr, nullptr};  // child[0] 走 0、child[1] 走 1\n    int cnt = 0;  // 有幾個數經過這裡（支援刪除/計數時用）\n};\n```\n:::\n\n\n\n:::example 逐位貪心：每一位都想走「和 x 相反」的邊，湊出最大 XOR\n把數字插進樹之後，要找「和 `x` 異或最大」的數，就從最高位開始**貪心走相反的位元**：`x` 這一位是 0 就盡量去走 1 的邊，是 1 就盡量去走 0 的邊，因為相反才能在這一位湊出 1。\n\n```cpp\n// 逐位貪心：每一位都想走「和 x 相反」的邊，湊出最大 XOR\nint best = 0;\nfor (int b = HIGH; b >= 0; b--) {\n    int bit = (x >> b) & 1;\n    int want = bit ^ 1;       // 想要相反的位元\n    if (node->child[want]) {  // 相反邊存在 → 這一位能湊出 1\n        best |= (1 << b);\n        node = node->child[want];\n    } else {\n        node = node->child[bit];  // 只能走相同邊，這一位是 0\n    }\n}\n```\n:::\n\n\n\n因為每個數最多 30 位，插入與查詢都只走固定層數，單次操作是 `O(位數)`，等於常數，整體 `O(n × 位數)`。\n\n## 狀態／資料結構定義\n\n```cpp\nconstexpr int B = 30;                 // 最高位下標；值域 < 2^(B+1)\nconstexpr int MAXN = 200005 * (B + 1) + 5;   // 節點數上界 = 插入個數 × (B+1) + 1\n\nint ch[MAXN][2];\nint cnt[MAXN];                        // 子樹中的數字個數（含重複）\nint tot = 0;                          // 節點 0 是根，已配置 tot+1 個節點\n```\n\n**`B` 怎麼定**：$B = \\lceil \\log_2(\\max\\text{值}+1)\\rceil - 1$。\n- 421：$\\text{nums}[i] \\le 2^{31}-1$ ⇒ $B = 30$。\n- 1803：$\\text{nums}[i] \\le 2\\times10^4$、$\\text{high} \\le 2\\times10^4$ ⇒ $2^{15} = 32768 > 2\\times10^4$ ⇒ $B = 14$。\n- 1707：$\\le 10^9 < 2^{30}$ ⇒ $B = 29$。\n\n**$B$ 取太小會漏掉高位，答案偏小且極難除錯；取太大只是慢一點、耗記憶體多一點。** 不確定就取大的，但要重算 `MAXN`。\n\n**`cnt` 的三個用途，缺一不可：**\n\n| 用途 | 題目 |\n| --- | --- |\n| 判斷子樹是否非空（貪心時能不能往這邊走） | 421、1707 |\n| 子樹計數（「異或值 $<L$ 的個數」一次加一整棵子樹） | 1803 |\n| 支援刪除（`--cnt` 而非真的刪節點） | 1938、滑動視窗類變形 |\n\n**不要用 `ch[u][b] != 0` 判斷子樹非空**——一旦支援刪除，節點還在但已無數字。**一律用 `cnt[ch[u][b]] > 0`。** 這是本節最容易埋下的隱形錯誤。\n\n**節點 0 同時當作根與「空指標」**：`ch` 初值全 0，而根就是 0。這在只插入的場景可行（因為根永遠不是任何節點的孩子），**但 `cnt[0]` 會被根的計數污染**——所以判空要用 `ch[u][b] && cnt[ch[u][b]]`，或者把根改成節點 1、保留 0 當空。**本節模板採後者更安全的寫法：`tot` 從 0 開始，根是 0，判空同時檢查指標與 `cnt`。**\n\n## 不變量或正確性證明\n\n### 定理 1（高位優先貪心的正確性：位權的支配性）\n\n**命題**：設 $u, v$ 是兩個 $B+1$ 位的非負整數，$b$ 是它們**最高的相異位**。若 $u$ 在第 $b$ 位是 1、$v$ 是 0，則 $u > v$。\n\n*證明*：$u$ 與 $v$ 在 $> b$ 的位完全相同，設該部分的共同值為 $H$。則\n$$u \\ge H + 2^b, \\qquad v \\le H + \\sum_{i=0}^{b-1} 2^i = H + 2^b - 1 < H + 2^b \\le u. \\qquad\\square$$\n\n**推論（貪心的合法性）**：要在候選集合中最大化一個 $B+1$ 位的值，**可以從最高位往低位逐位決定：每一位都優先取 1，只要「取 1 之後仍有候選存活」**。這樣得到的值是最大的。\n\n*證明*：設貪心在第 $b$ 位取了 1，而某個「更優解」在該位取 0。由於兩者在 $>b$ 的位相同（貪心的前綴是可行的，且該解也在同一候選集內），由本定理該解嚴格小於任何在第 $b$ 位取 1 的可行值——矛盾 ✔。$\\square$\n\n**這是整節的地基，而它依賴一個常被忽略的前提：所有數字必須被視為等長的位串。** 若你用「有效位數」（去掉前導零）來走 trie，不同數字的深度就不同，位權對齊就壞了，定理 1 完全失效。**這是 0-1 trie 必須補足前導零的根本原因。**\n\n### 定理 2（trie 上的貪心走訪實現了定理 1 的策略）\n\n**不變量 (X)**：在查詢 $\\max_{y \\in S}(x \\oplus y)$ 的過程中，走到第 $b$ 層的節點 $u$ 時，**$u$ 的子樹中的數字集合，恰好等於「所有與已作決策一致的 $y$」**——也就是那些能讓 $x\\oplus y$ 的高位部分等於已經決定的那些位的 $y$。\n\n*證明*：由 7.1 的不變量 (T)，$u$ 的子樹恰為「以 $u$ 對應的位串為前綴」的所有數字。而每一步我們選的孩子 $c$ 決定了 $y$ 的第 $b$ 位，從而決定了 $(x\\oplus y)$ 的第 $b$ 位 $= x_b \\oplus c$ ✔。$\\square$\n\n**貪心步**：在第 $b$ 層，想讓 $(x\\oplus y)_b = 1$，需要 $y_b = x_b \\oplus 1$。故\n```cpp\nint want = (x >> b & 1) ^ 1;\nif (ch[u][want] && cnt[ch[u][want]]) { ans |= 1 << b; u = ch[u][want]; }\nelse                                   u = ch[u][want ^ 1];\n```\n\n由 (X)，`cnt[ch[u][want]] > 0` $\\iff$ 「第 $b$ 位取 1 之後仍有候選存活」。**這正是定理 1 推論所要求的判斷**，故走訪結果最大 ✔。\n\n**前提是 trie 非空**：若 $S = \\emptyset$，`else` 分支會走到空節點。**所有 0-1 trie 題都要先確認「查詢時集合非空」**——421 的作法是「先插入 `nums[0]`，再從 $i=1$ 開始邊查邊插」，1707 的作法是「集合為空就回答 $-1$」。\n\n**求最小值**：把 `want` 改成 `x >> b & 1`（走**相同**的位）即可，證明對偶。**注意「最小異或對」有更簡單的解法**：排序後只需檢查相鄰對（因為最小異或的兩個數在排序後必相鄰——它們的最高相異位最低，故數值最接近）。**不要為了用 trie 而用 trie。**\n\n### 定理 3（1803 計數：「異或值 $< L$」的集合可以分解成 $O(B)$ 棵完整子樹）\n\n**命題**：固定 $x$ 與界 $L$，令 $v = x \\oplus y$。則\n$$\\{\\, y : v < L \\,\\} \\;=\\; \\biguplus_{\\substack{b\\,:\\,L_b = 1}} \\Big\\{\\, y : v_{i} = L_{i} \\ \\forall i > b,\\ \\ v_b = 0 \\,\\Big\\}$$\n且右側各集合**兩兩不交**（$\\uplus$ 表示不交併）。\n\n*證明*。\n- **($\\subseteq$)** 設 $v < L$。則 $v \\ne L$，令 $b$ 為 $v$ 與 $L$ 的最高相異位。由定理 1，$v < L$ 迫使 $L_b = 1$、$v_b = 0$；且依定義 $v_i = L_i$ 對所有 $i > b$ ✔。\n- **($\\supseteq$)** 設 $v$ 屬於某個 $b$ 對應的集合。則 $v$ 與 $L$ 在 $>b$ 位相同、在第 $b$ 位 $L$ 為 1 而 $v$ 為 0，由定理 1 得 $v < L$ ✔。\n- **不交**：每個 $v$ 的「與 $L$ 的最高相異位」唯一，故它只屬於該 $b$ 對應的集合 ✔。$\\square$\n\n**這個分解為什麼有用**：每一個 $b$ 對應的集合，在 trie 上**恰好是一整棵子樹**——「$v$ 在 $>b$ 位等於 $L$」把路徑唯一確定到第 $b$ 層的某個節點 $u$，而「$v_b = 0$」指定了 $u$ 的一個孩子，該孩子的**整棵子樹**都滿足條件。**所以計數不需要遞迴，只要沿一條路徑走下去，順路把若干個 `cnt` 加起來。**\n\n```cpp\n// 回傳 S 中滿足 (x ^ y) < L 的 y 的個數\nint countLess(int x, int L) {\n    int u = 0, res = 0;\n    for (int b = B; b >= 0; --b) {\n        if (u < 0) break;\n        int xb = x >> b & 1, Lb = L >> b & 1;\n        if (Lb == 1) {\n            // v_b = 0 分支（y_b == x_b）整棵子樹都滿足 v < L\n            int c = ch[u][xb];\n            if (c) res += cnt[c];\n            // 繼續沿 v_b = 1（= L_b）走\n            u = ch[u][xb ^ 1];\n        } else {\n            // L_b = 0 ⇒ 必須 v_b = 0 才能維持「高位相等」\n            u = ch[u][xb];\n        }\n        if (!u) break;\n    }\n    return res;\n}\n```\n\n**「$\\le L$」用 `countLess(x, L+1)`；「落在 $[\\text{low}, \\text{high}]$」用 `countLess(x, high+1) - countLess(x, low)`。** 1803 就是後者，對每個 $i$ 查詢 $[0, i)$ 中的數（**邊查邊插，天然避免重複計數與自己配自己**）。\n\n**注意迴圈結束時 $u$ 對應的是 $v = L$ 本身，不計入**——這正是嚴格小於所要求的。\n\n### 定理 4（1707 離線排序：單側約束 ⇒ 只需插入，不需刪除）\n\n*問題*：查詢 $(x_i, m_i)$，要在 $\\{\\,y \\in \\text{nums} : y \\le m_i\\,\\}$ 中最大化 $x_i \\oplus y$。\n\n**命題**：把 `nums` 升序排序、查詢按 $m$ 升序排序，用一個指標邊插邊答，結果正確。\n\n*證明*：令 $S(m) = \\{y \\in \\text{nums} : y \\le m\\}$。約束是**單側**的，故 $m \\le m' \\Rightarrow S(m) \\subseteq S(m')$——**$S$ 對 $m$ 單調遞增。** 按 $m$ 升序處理時，從 $S(m_{k})$ 到 $S(m_{k+1})$ 只需**插入**新增的元素，永不需要刪除 ✔。$\\square$\n\n**前提是「單側」。** 若約束變成 $l_i \\le y \\le r_i$（雙側），單調性就沒了，這時有三條路：\n1. **可持久化 trie**：對每個前綴版本建一棵樹，查詢時用 `cnt[版本r] - cnt[版本l-1]` 做差分——**這是通解，也是競賽中「區間內最大異或」的標準答案**；\n2. **離線 + 差分**（把 $[l,r]$ 拆成 $[1,r] - [1,l-1]$，需要答案可差分——最大值不可差分，計數可以）；\n3. **支援刪除的 trie**（`--cnt`），適用於雙指標／滑動視窗的場景。\n\n**「約束是否單側」決定了要不要可持久化，這是這一族題的分水嶺。**\n\n### 定理 5（1938 樹上查詢：DFS 進出即為集合的插入與刪除）\n\n*問題*：一棵樹，每個查詢 $(node_i, val_i)$ 問：$node_i$ 到根的路徑上，$val_i \\oplus \\text{parentVal}$ 的最大值。\n\n**不變量 (D)**：在 DFS 到達節點 $p$ 時，若進入節點就插入其值、離開節點就刪除（`--cnt` 沿路），則**0-1 trie 中的元素恰為根到 $p$ 的路徑上的所有值**。\n\n*證明*（對 DFS 的執行歸納）：DFS 的呼叫堆疊恰為根到當前節點的路徑；插入與刪除與進入／離開嚴格配對，故 trie 中的元素集合恆等於堆疊中的節點值 ✔。$\\square$\n\n**所以只要把每個查詢掛在對應節點上，DFS 到那裡時查一次即可**，總複雜度 $O((n+q)B)$。\n\n*刪除的實作*：沿 $val$ 的位路徑走下去，每個節點 `--cnt`。**節點不回收**（回收會破壞 `ch` 的結構且無必要）——這就是為什麼判空必須用 `cnt` 而非指標。\n\n**這個「DFS 進出 = 集合維護」的套路不限於 0-1 trie**，任何支援插入／刪除的結構都適用（可撤銷並查集、`multiset`、值域 BIT）。**看到「路徑上的某種查詢」，先想這個套路，再想樹上倍增或樹鏈剖分。**\n\n### 定理 6（2479：把「兩棵不相交子樹的異或和」化歸為「兩組數的最大異或對」）\n\n*問題*：一棵樹，每個節點有值。選兩棵**不相交**的子樹，最大化兩者子樹和的異或。\n\n*化歸鏈*：\n1. 一次 DFS 求出每個節點的子樹和 $s_u$；\n2. **兩棵子樹不相交 $\\iff$ 兩個根互不為祖先**（樹的子樹要嘛包含要嘛不交）；\n3. 對每個節點 $u$，「與 $u$ 不相交的子樹」= 全部子樹 $-$ ($u$ 的祖先鏈) $-$ ($u$ 的子樹)。用 DFS 序處理：**在 DFS 中，離開 $u$ 的子樹時再把 $u$ 的子樹內所有節點插入 trie，就保證 trie 中只有「已完成且不含 $u$、也非 $u$ 祖先」的節點**。\n\n*細節*：標準寫法是「DFS 時先遞迴所有孩子，處理完一個孩子子樹就把它整棵插入 trie 並用 $u$ 的其他孩子去查」——這會在同一個父節點下配對不同孩子的子樹。**加上「$u$ 自己與已完成的兄弟子樹」的配對即可覆蓋所有不相交對**：任兩棵不相交子樹的根，其 LCA 是某個節點 $w$，且它們分別位於 $w$ 的不同孩子子樹中（或其中一個就是 $w$——但 $w$ 的子樹包含另一個，矛盾）✔。\n\n**這題的價值在於化歸，不在 trie。** trie 只是最後一步的工具。**大部分 0-1 trie 的難題都長這樣：真正的難點是把題目化成「兩組數的最大異或對」，化成之後套模板。**\n\n## 逐步演算法\n\n:::example 狀態表示：用陣列模擬樹，son[i][0/1] 是第 i 個節點的兩條邊\n**1. 狀態表示：節點存兩條邊，必要時存經過次數。** 位數從最高位算起，常見 30 或 31 位。\n\n```cpp\n// 狀態表示：用陣列模擬樹，son[i][0/1] 是第 i 個節點的兩條邊\nconst int B = 29;  // 從第 29 位（約 5e8）開始\nint son[N * 30][2], cnt[N * 30], idx = 0;\n```\n:::\n\n\n\n:::example 插入 x：從高位到低位逐層建立節點\n**2. 插入：逐位往下，沒有節點就新建。** 同時把經過次數加一（之後若要刪除或計數會用到）。\n\n```cpp\n// 插入 x：從高位到低位逐層建立節點\nvoid insert(int x) {\n    int p = 0;\n    for (int b = B; b >= 0; b--) {\n        int u = (x >> b) & 1;\n        if (!son[p][u]) {\n            son[p][u] = ++idx;\n        }\n        p = son[p][u];\n        cnt[p]++;\n    }\n}\n```\n:::\n\n\n\n:::example 查詢和 x 異或的最大值\n**3. 答案讀取：逐位貪心走相反邊。** 走得到相反邊就累加這一位的貢獻，走不到就只能走同邊。\n\n```cpp\n// 查詢和 x 異或的最大值\nint query(int x) {\n    int p = 0, res = 0;\n    for (int b = B; b >= 0; b--) {\n        int u = (x >> b) & 1;\n        if (son[p][u ^ 1]) {\n            res |= (1 << b);\n            p = son[p][u ^ 1];\n        } else {\n            p = son[p][u];\n        }\n    }\n    return res;\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 要刪除或限制「只用某些數」時，靠 `cnt` 判斷邊是否還「有效」：`cnt` 歸零視同這條邊不存在。\n- 位數要取夠：涵蓋題目最大值，否則高位漏算會錯。\n- 複雜度：每次插入／查詢 `O(B)`，B 是位數（常數級），總共 `O(n B)`；空間最壞 `O(n B)`。\n\n## C++17 模板\n\n**模板一：421 陣列中兩個數的最大異或值（邊插邊查）**\n\n```cpp\nclass Solution {\n    static constexpr int B = 30;\n    vector<array<int,2>> ch;\n    vector<int> cnt;\n    int newNode() {\n        ch.push_back({0, 0});\n        cnt.push_back(0);\n        return (int)ch.size() - 1;\n    }\n    void insert(int x) {\n        int u = 0;\n        for (int b = B; b >= 0; --b) {\n            int d = x >> b & 1;\n            if (!ch[u][d]) ch[u][d] = newNode();\n            u = ch[u][d];\n            ++cnt[u];\n        }\n    }\n    int queryMax(int x) {\n        int u = 0, res = 0;\n        for (int b = B; b >= 0; --b) {\n            int want = (x >> b & 1) ^ 1;\n            int c = ch[u][want];\n            if (c && cnt[c]) {\n                res |= 1 << b;\n                u = c;\n            } else {\n                u = ch[u][want ^ 1];\n            }\n        }\n        return res;\n    }\npublic:\n    int findMaximumXOR(vector<int>& nums) {\n        ch.assign(1, {0, 0});\n        cnt.assign(1, 0);\n        insert(nums[0]);\n        int ans = 0;\n        for (size_t i = 1; i < nums.size(); ++i) {\n            ans = max(ans, queryMax(nums[i]));   // 只跟前面的配對，不重不漏\n            insert(nums[i]);\n        }\n        return ans;\n    }\n};\n```\n\n**模板二：1803 統計異或值在範圍內的數對（定理 3 的計數）**\n\n```cpp\nclass Solution {\n    static constexpr int B = 14;                 // nums[i], high <= 2e4 < 2^15\n    vector<array<int,2>> ch;\n    vector<int> cnt;\n    int newNode() {\n        ch.push_back({0, 0});\n        cnt.push_back(0);\n        return (int)ch.size() - 1;\n    }\n    void insert(int x) {\n        int u = 0;\n        for (int b = B; b >= 0; --b) {\n            int d = x >> b & 1;\n            if (!ch[u][d]) ch[u][d] = newNode();\n            u = ch[u][d];\n            ++cnt[u];\n        }\n    }\n    int countLess(int x, int L) {                // #{ y in trie : (x ^ y) < L }\n        int u = 0, res = 0;\n        for (int b = B; b >= 0 && u; --b) {\n            int xb = x >> b & 1, Lb = L >> b & 1;\n            if (Lb) {\n                int c = ch[u][xb];\n                if (c) res += cnt[c];\n                u = ch[u][xb ^ 1];\n            } else {\n                u = ch[u][xb];\n            }\n        }\n        return res;\n    }\npublic:\n    int countPairs(vector<int>& nums, int low, int high) {\n        ch.assign(1, {0, 0});\n        cnt.assign(1, 0);\n        int ans = 0;\n        for (int x : nums) {\n            ans += countLess(x, high + 1) - countLess(x, low);\n            insert(x);                           // 邊查邊插：每對只算一次\n        }\n        return ans;\n    }\n};\n```\n\n**模板三：1707 與陣列中元素的最大異或值（離線 + 單調插入）**\n\n```cpp\nclass Solution {\n    static constexpr int B = 29;                 // <= 1e9 < 2^30\n    vector<array<int,2>> ch;\n    void insert(int x) {\n        int u = 0;\n        for (int b = B; b >= 0; --b) {\n            int d = x >> b & 1;\n            if (!ch[u][d]) {\n                ch.push_back({0, 0});\n                ch[u][d] = (int)ch.size() - 1;\n            }\n            u = ch[u][d];\n        }\n    }\n    int queryMax(int x) {\n        int u = 0, res = 0;\n        for (int b = B; b >= 0; --b) {\n            int want = (x >> b & 1) ^ 1;\n            if (ch[u][want]) {\n                res |= 1 << b;\n                u = ch[u][want];\n            } else {\n                u = ch[u][want ^ 1];\n            }\n        }\n        return res;\n    }\npublic:\n    vector<int> maximizeXor(vector<int>& nums, vector<vector<int>>& queries) {\n        sort(nums.begin(), nums.end());\n        int q = queries.size();\n        vector<int> ord(q);\n        iota(ord.begin(), ord.end(), 0);\n        sort(ord.begin(), ord.end(),\n             [&](int a, int b) { return queries[a][1] < queries[b][1]; });\n        ch.assign(1, {0, 0});\n        vector<int> ans(q, -1);\n        size_t p = 0;\n        for (int id : ord) {\n            int x = queries[id][0], m = queries[id][1];\n            while (p < nums.size() && nums[p] <= m) insert(nums[p++]);\n            if (p > 0) ans[id] = queryMax(x);    // p == 0 表示集合為空\n        }\n        return ans;\n    }\n};\n```\n\n（模板三沒有 `cnt`，因為它**只插不刪**——這時「指標非空」與「子樹非空」等價。**一旦需要刪除就必須加回 `cnt`。**）\n\n## 時間與空間複雜度\n\n設 $n$ 為插入個數、$q$ 為查詢數、$B+1$ 為位數。\n\n| 操作 | 時間 | 說明 |\n| --- | --- | --- |\n| `insert` | $O(B)$ | 恰好走 $B+1$ 層，沒有提前終止 |\n| `queryMax` / `queryMin` | $O(B)$ | 同上 |\n| `countLess` | $O(B)$ | 定理 3：一條路徑 + 沿路加 `cnt` |\n| 刪除（`--cnt`） | $O(B)$ | 不回收節點 |\n\n| 題目 | 時間 | 空間 |\n| --- | --- | --- |\n| 421 | $O(nB)$ | $O(nB)$ |\n| 1707 | $O\\!\\big((n + q)\\log + (n+q)B\\big)$ | $O(nB)$ |\n| 1803 | $O(nB)$ | $O(nB)$ |\n| 1938 | $O\\!\\big((n+q)B\\big)$ | $O(nB)$ |\n| 2479 | $O(nB)$ | $O(nB)$ |\n\n**空間才是 0-1 trie 的真實限制。** $n = 2\\times10^5$、$B = 30$ ⇒ 最多 $6.2\\times10^6$ 個節點，每節點兩個 `int` 孩子加一個 `int` 計數 = 12 bytes ⇒ **約 75 MB**。這已經逼近多數判題系統的 256 MB（含其他開銷）。三個對策：\n\n1. **把 $B$ 壓到真正需要的位數**（1803 從 30 降到 14，空間直接砍一半以上）；\n2. **用 `vector` 動態增長而非開滿靜態陣列**——實際節點數通常遠小於上界（共用前綴越多，節點越少）；\n3. **改用「逐位雜湊」解法**（見下節），空間 $O(n)$。\n\n**與暴力的對比**：421 暴力是 $O(n^2)$，$n = 2\\times10^5$ 時是 $4\\times10^{10}$；trie 是 $6\\times10^6$。**這是 $10^4$ 倍的差距，而不是常數級的改善**——這種時候別猶豫，直接寫 trie。\n\n## 常見錯誤與邊界條件\n\n- **`B` 取太小**。最高位被截斷，答案系統性偏小。**由值域算，不要憑印象。** 例如「$\\le 10^9$」需要 $B = 29$（$2^{30} > 10^9$），寫成 $B=20$ 就錯。\n- **沒有補足前導零 / 用「有效位數」建樹**。這直接摧毀定理 1 的位權對齊前提。**所有數字必須從同一個最高位 $B$ 開始插入**，小的數字自然走出一串 `0` 邊。\n- **支援刪除後仍用 `ch[u][b] != 0` 判空**。節點還在但 `cnt` 已經是 0，貪心會走進一棵空子樹，回傳一個不存在的 $y$。**一律用 `cnt[ch[u][b]] > 0`。**\n- **查詢時 trie 為空**。`else` 分支走到節點 0（根）或未初始化的節點，結果是垃圾值而非崩潰。**421 要先插一個；1707 要判 `p > 0` 回傳 $-1$。**\n- **自己跟自己配對**。421 若先把全部插入再逐一查詢，$x \\oplus x = 0$ 不影響最大值（其他配對只會更大或相等），**所以 421 這麼寫剛好沒事**；但 **1803 這麼寫就會多算**（$x\\oplus x = 0$ 可能落在 $[low, high]$ 內），而且每對會算兩次。**「邊查邊插」是這一族題的標準寫法，同時解決兩個問題。**\n- **`countLess` 忘記處理 $u$ 變成 0（走到不存在的節點）**。要在迴圈條件或迴圈內 `break`，否則會讀到 `ch[0]`（根）的孩子，計數暴增。\n- **`res |= 1 << b` 當 $b = 31$ 時溢位**。`1 << 31` 對 `int` 是 UB。**$B \\le 30$ 時安全；要用到第 31 位就寫 `1LL << b` 並用 `long long`。**\n- **負數**。0-1 trie 假設非負。有負數時先整體加上偏移量——**但要注意加偏移會改變異或的結果**，所以只有在題目本身是「比較大小 / 求第 K 小」時才能這樣做。**純異或題若含負數，正確做法是用補碼的完整 32 位（$B = 31$，配 `unsigned`）。**\n- **`MAXN` 算錯**。靜態陣列版本的節點上界是 $n \\times (B+1) + 1$，**不是 $n$，也不是 $n \\times B$**。少算一層在極端測資上會越界。\n- **1707 的答案要按原始查詢順序輸出**。排序時排的是索引，不是查詢本身；忘了這點會拿到亂序答案。\n- **1803 的 `countLess(x, low)` 不是 `countLess(x, low - 1)`**。`countLess` 是嚴格小於，所以 $[low, high]$ 的計數是 `countLess(high+1) - countLess(low)`。**這一格差錯是這題的頭號 WA。**\n\n## 與相似技巧的比較\n\n- **與 7.1 一般 trie**：**同一個資料結構，$\\Sigma = 2$。** 差別只有三點：等長 ⇒ 不需 `isEnd`、必定走滿、位權對齊使貪心成立。**7.1 的推論 1.1（前綴 ⟺ 祖先）在這裡就是「高位相同 ⟺ 同一棵子樹」。**\n- **與「逐位雜湊」解法（421 的另一解）**：從高位往低位維護 `ans`，每一位試探 `ans | (1<<b)` 是否可達——把所有數的前 $b$ 位放進 `unordered_set`，檢查是否存在 $a, b$ 使 $a \\oplus b = \\text{候選}$。$O(nB)$ 時間、$O(n)$ 空間。**時間相同、空間好得多、程式短，但只適用「單一全域最大值」，無法處理帶約束的查詢（1707）或計數（1803）。** 421 單題建議用雜湊解，其餘用 trie。\n- **與排序 + 相鄰比較（最小異或對）**：最小異或的兩個數在排序後必定相鄰。$O(n\\log n)$、$O(1)$ 額外空間。**求最小用排序，求最大用 trie**——這個不對稱來自「最大異或的兩數在數值上通常相距很遠」。\n- **與值域樹狀陣列 / 動態開點線段樹**：**0-1 trie 本質上就是值域上的動態開點線段樹**（`cnt` 就是區間計數）。兩者可互換，選擇看你要做什麼：**逐位貪心用 trie 直觀**（每一層直接對應一個位元決策），**區間求和、區間第 K 小用線段樹直觀**。「線段樹上二分找第 K 小」與「0-1 trie 上走訪」是同一個演算法的兩種寫法。\n- **與可持久化 trie**：處理「任意區間 $[l,r]$ 內的最大異或」。對每個前綴建一個版本（共用未修改的節點，每版本新增 $O(B)$ 個節點），查詢用 `cnt[版本r] - cnt[版本l-1]` 判斷子樹是否非空。**空間 $O(nB)$，時間仍 $O(B)$。這是「約束雙側」時的通解**（定理 4 的討論）。\n- **與線性基（XOR 基底）**：處理的是「**任意子集**的異或最大值」，是完全不同的問題——線性基做的是線性代數（$\\mathbb{F}_2$ 上的高斯消去），0-1 trie 做的是集合查詢。**「選一個數配對」⇒ trie；「選任意多個數異或起來」⇒ 線性基。** 這兩者常被混淆，因為題面都寫「最大異或」。\n- **與分治按位遞迴**：1803 也可以用「按最高位把數組分成兩半，遞迴統計跨組對」的分治法，$O(nB)$、$O(n)$ 空間、無需建樹。**本質上就是把 trie 的遞迴展開，不顯式存節點。** 空間敏感時值得考慮。\n\n## 代表例題\n\n**題號**：421、1707、1803、1938、2479\n\n要共用前綴路徑；練習分清單詞終止、前綴計數與子節點結構。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 421 | [陣列中兩個數的最大異或值](https://leetcode.cn/problems/maximum-xor-of-two-numbers-in-an-array) |  |\n| 1707 | [與陣列中元素的最大異或值](https://leetcode.cn/problems/maximum-xor-with-an-element-from-array) | 2359 |\n| 1803 | [統計異或值在範圍內的數對有多少](https://leetcode.cn/problems/count-pairs-with-xor-in-a-range) | 2479 |\n| 1938 | [查詢最大基因差](https://leetcode.cn/problems/maximum-genetic-difference-query) | 2503 |\n| 2479 | [兩個不重疊子樹的最大異或值](https://leetcode.cn/problems/maximum-xor-of-two-non-overlapping-subtrees)（Premium） |  |\n| 2935 | [找出強數對的最大異或值 II](https://leetcode.cn/problems/maximum-strong-pair-xor-ii) | 2349 |\n| 3632 | [異或至少為 K 的子陣列數目](https://leetcode.cn/problems/subarrays-with-xor-at-least-k) |  |\n\n## 本節重點速查\n\n- **0-1 trie = $\\Sigma=2$ 的 trie，把整數看成定長位串。** 等長帶來三個簡化：不需 `isEnd`、必定走滿 $B+1$ 層、位權對齊使貪心成立。\n- **定理 1（位權支配）**：最高相異位為 1 者較大，因為 $2^b > \\sum_{i<b}2^i$。**這是高位優先貪心的唯一依據，前提是所有數字等長**——用「有效位數」建樹就全錯了。\n- **求最大走相反位（`want = xb ^ 1`），求最小走相同位。** 但**最小異或對用排序後檢查相鄰對更簡單**，不要為了用 trie 而用 trie。\n- **定理 3（計數分解）**：$\\{v < L\\}$ 可分解成 $O(B)$ 棵不交的完整子樹——沿一條路徑走，`L_b == 1` 時加上 `cnt[ch[u][xb]]` 再往 `xb^1` 走。**區間計數是 `countLess(high+1) - countLess(low)`，別寫成 `low-1`。**\n- **「邊查邊插」是標準寫法**：同時解決「自己配自己」與「每對算兩次」。\n- **約束單側（$y \\le m$）⇒ 離線排序、只插不刪（定理 4）；約束雙側 ⇒ 可持久化 trie。** 這是分水嶺。\n- **支援刪除就必須用 `cnt` 判空，不能用指標判空。** 節點不回收。\n- **「路徑上的查詢」用 DFS 進出維護集合（定理 5）**，不必樹上倍增。\n- **難題的難點通常在化歸**（2479 的「不相交子樹 ⟺ 互不為祖先」），trie 只是最後一步。\n- **空間是真實限制**：$n=2\\times10^5$、$B=30$ 約 75 MB。**先把 $B$ 壓到值域真正需要的位數。**\n- **別跟線性基搞混**：「選一個數配對」⇒ trie；「選任意子集異或」⇒ 線性基。\n",
        },
      ],
    },
    {
      id: 53,
      title: "8. 並查集",
      src: "https://leetcode.cn/circle/discuss/mOr1u6/",
      summary:
        ":::example 模板來源 https://leetcode.cn/circle/discuss/m_or…\n模板：\n\n```cpp\n// 模板來源 https://leetcode.cn/circle/discuss/m_or1u6/\nstruct UnionFind {\n    vector<int> fa, sz;\n    int cc;\n    explicit UnionFind(int n) : fa(n), sz(n, 1), cc(n) {\n        iota(fa.begin(), fa.end(), 0);\n    }\n\n    int find(int x) {\n        if (fa[x] != x) {\n            fa[x] = find(fa[x]);  // 路徑壓縮\n        }\n        return fa[x];\n    }\n\n    bool isSame(int x, int y) { return find(x) == find(y); }\n\n    bool merge(int x, int y) {\n        x = find(x);\n        y = find(y);\n        if (x == y) {\n            return false;\n        }\n        fa[x] = y;  // 小的掛到大的下面（啟發式合併）\n        sz[y] += sz[x];\n        cc -= 1;\n        return true;\n    }\n\n    int getSize(int x) { return sz[find(x)]; }\n};\n```\n:::",
      children: [
        {
          id: 54,
          title: "8.1 基礎",
          summary:
            "## 這個技術解決什麼問題\n\n有一組元素，不斷地宣告「這兩個是同一類」，中間穿插「這兩個現在是不是同一類？」「現在有幾類？」的查詢。\n\n暴力有兩條路，兩條都不行：\n\n- **每次查詢跑一次 BFS/DFS**：單次 $O(n+m)$，查詢多就爆炸。\n- **每類存一個集合，合併時把小的搬進大的**：查詢 $O(1)$，但合併要搬元素，樸素實作最壞 $O(n)$ 一次。\n\n**核心想法**：*不要維護「集合裡有誰」，只維護「每個元素的頂頭上司是誰」。* 每一類選一個代表元，每個元素存一個 `parent` 指向同類的另一個元素，順著 `parent` 一路往上必定走到代表元。於是：\n\n- 「$x$ 與 $y$ 同類？」= 「兩者的代表元相同？」\n- 「把 $x$ 與 $y$ 併起來」= 「把其中一個代表元的 `parent` 指向另一個」——**只動一個指標。**\n\n代價是「找代表元」要沿鏈往上爬。**並查集的全部技術含量，就在兩個讓這條鏈不變長的技巧**：**路徑壓縮**（爬完之後把沿路節點直接掛到根上）與**按秩／按大小合併**（永遠讓矮的掛到高的下面）。兩者合用後單次操作是攤還 $O(\\alpha(n))$，$\\alpha$ 是反 Ackermann 函數，在任何現實規模下 $\\le 4$——**實務上就當常數。**\n\n**適用的硬前提：關係只增不減。** 並查集能高效合併，但**不能拆分**——`parent` 指標一旦指出去，就沒有資訊可以還原「這個子樹原本屬於誰」。看到題目要刪邊、刪點，本節的工具直接失效，要走 8.2 的離線倒序，或改用不壓縮路徑的可撤銷並查集。\n\n**辨識訊號**：出現「連通塊個數」「是否屬於同一組」「合併帳號／朋友圈」「等價、同義、同類」這些字眼，而且**沒有刪除操作**。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「並查集」想：\n\n- **題目反覆問「這兩個元素是否相連／屬於同一組」，或「有幾個連通塊」。** 例如 990「等式方程的可滿足性」把 `a == b` 當作合併、`a != b` 當作檢查矛盾。\n- **關係只會「越併越多」、不會拆開**：朋友越加越多、帳號越合越大。只增不減正是並查集的舒適區。\n- **多個條件其實是在說「這些東西是同一類」**，例如 721「帳號合併」把共用 email 的帳號併在一起。\n- **暴力是每次查詢都 BFS/DFS 一遍（`O(n)`）**，但合併與查詢非常頻繁，受不了——並查集把它壓到近乎常數。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要合併的「兩個元素」分別是什麼？怎麼編成 0..n-1 的編號？\n2. 我在哪裡用 `find` 比較老大來判斷同夥？\n3. 除了連通性，我還需要在老大身上維護什麼摘要（大小？數量？矛盾旗標？）\n\n## 核心想法與直覺\n\n本節要解決的場景是：有一堆元素，它們之間會「兩兩變成同一夥」，而你想隨時回答「這兩個元素現在算不算同一夥？」或「現在總共分成幾夥？」。例如朋友圈、電路是否連通、帳號是否屬於同一人。\n\n直覺做法是每次都從一個點出發 BFS/DFS 看能不能走到另一個點，但這樣每問一次就掃一遍，太慢。「並查集（Union-Find / Disjoint Set Union, DSU）」用一個聰明的想法解決：**讓每一夥推派一個「代表元（老大）」，判斷同夥只要看兩人的老大是不是同一個。**\n\n:::example 初始：每個人自成一夥，自己就是自己的老大\n實作上用一個 `parent` 陣列，`parent[x]` 記錄 `x` 的「上司」，順著上司一路往上爬，爬到「自己是自己上司」的那個人，就是這一夥的老大。\n\n```cpp\n// 初始：每個人自成一夥，自己就是自己的老大\nvector<int> parent(n);\nfor (int i = 0; i < n; ++i) {\n    parent[i] = i;\n}\n```\n:::\n\n\n\n:::example find：找老大，順手把 x 直接接到老大底下（路徑壓縮）\n`find(x)` 就是「往上爬找老大」。為了讓下次更快，爬的同時把沿路的人直接掛到老大底下，這叫「路徑壓縮」。\n\n```cpp\n// find：找老大，順手把 x 直接接到老大底下（路徑壓縮）\nint find(int x) {\n    if (parent[x] != x) {\n        parent[x] = find(parent[x]);\n    }\n    return parent[x];\n}\n```\n:::\n\n\n\n:::example union：把 x 那夥併進 y 那夥\n合併兩夥（`union`）就是「讓一個老大認另一個老大當上司」。\n\n```cpp\n// union：把 x 那夥併進 y 那夥\nvoid merge(int x, int y) {\n    parent[find(x)] = find(y);  // 兩個老大，其中一個聽另一個的\n}\n```\n:::\n\n\n\n有了路徑壓縮（再加上按大小合併），每次操作幾乎是常數時間，n 次操作合計接近 `O(n)`。\n\n## 狀態／資料結構定義\n\n```cpp\nstruct DSU {\n    vector<int> fa, sz;\n    int comp;                       // 目前的連通塊個數\n\n    explicit DSU(int n) : fa(n), sz(n, 1), comp(n) {\n        iota(fa.begin(), fa.end(), 0);   // 初始每個元素自成一類\n    }\n    int find(int x) {\n        while (fa[x] != x) {\n            fa[x] = fa[fa[x]];      // 路徑減半（迭代版壓縮）\n            x = fa[x];\n        }\n        return x;\n    }\n    bool unite(int x, int y) {      // 回傳「這次合併是否真的發生」\n        x = find(x);\n        y = find(y);\n        if (x == y) return false;\n        if (sz[x] < sz[y]) swap(x, y);\n        fa[y] = x;\n        sz[x] += sz[y];\n        --comp;\n        return true;\n    }\n    bool same(int x, int y) { return find(x) == find(y); }\n};\n```\n\n**三個欄位的語意**：\n\n| 欄位 | 語意 | 有效性 |\n| --- | --- | --- |\n| `fa[x]` | $x$ 的父節點；`fa[x] == x` 表示 $x$ 是代表元 | 對所有 $x$ |\n| `sz[x]` | 以 $x$ 為根的整棵樹的節點數 | **只有 $x$ 是根時才有意義** |\n| `comp` | 連通塊個數 | 全域 |\n\n**`sz` 只在根上有效，是最常被誤用的地方。** 想查「$x$ 所在的組有多大」，必須寫 `sz[find(x)]`，不能寫 `sz[x]`。同理，任何掛在「集合」上的資訊（組內和、組內最大值、組內某個標記）都只在根上維護，讀取前先 `find`。\n\n**`unite` 回傳 `bool` 的價值**：`false` 表示兩者本來就同類。**684 的答案、Kruskal 的「這條邊是否入選」、「這條邊是否構成環」全部靠這個回傳值**，不要寫成 `void`。\n\n**元素不是整數時**：先用 `unordered_map<string,int>` 把字串／座標／pair 映射成 $0..n-1$ 的整數（721 帳號合併、990 等式方程都要這一步）。**二維網格用 $r \\times C + c$ 攤平，不要用 `map<pair<int,int>,int>`——慢 10 倍以上。**\n\n**`iota` 初始化不能忘。** 忘了初始化 `fa`（全為 0）會讓所有元素一開始就在同一類，症狀是「答案永遠是 1」。\n\n## 不變量或正確性證明\n\n### 定理 1（森林不變量：`fa` 恆為森林，且每棵樹恰為一個等價類）\n\n設已處理的合併請求為 $E = \\{(x_1,y_1),\\dots,(x_k,y_k)\\}$，令 $\\sim_E$ 為由 $E$ 生成的最小等價關係（自反、對稱、遞移閉包）。\n\n**不變量 (U)**：\n1. 有向圖 $\\{x \\to \\texttt{fa}[x] : \\texttt{fa}[x] \\ne x\\}$ **無環**，故構成一片森林，每棵樹有唯一的根（`fa[r] == r`）；\n2. 每棵樹的節點集合，**恰好**是 $\\sim_E$ 的一個等價類。\n\n*證明*（對操作次數歸納）。\n\n- **基底**：初始 `fa[x] == x` 對所有 $x$，森林由 $n$ 棵單點樹組成；$\\sim_\\emptyset$ 的等價類就是 $n$ 個單元素集 ✔。\n\n- **`find` 不改變 (U)**：路徑壓縮把 $x$ 的 `fa` 改成 $x$ 的某個**祖先**（`fa[fa[x]]` 或根）。\n  - *無環*：新的父節點仍是原樹中 $x$ 的祖先，$x$ 到根的路徑仍存在且變短，不可能產生指向後代的邊 ✔。\n  - *節點集合不變*：壓縮只在**同一棵樹內部**改指標，沒有節點換樹 ✔。\n  - *根不變*：$x$ 到根的路徑仍終止於同一個根 ✔。\n\n- **`unite(x,y)` 維持 (U)**：設 $r_x = \\texttt{find}(x)$、$r_y = \\texttt{find}(y)$。\n  - $r_x = r_y$：什麼都不做。而此時 $x \\sim_E y$ 已成立，加入 $(x,y)$ 不改變 $\\sim_E$ ✔。\n  - $r_x \\ne r_y$：令 `fa[r_y] = r_x`。因為 $r_y$ 原本是根（無出邊），新增這條邊不可能形成環（兩棵樹原本不相交）✔。合併後的節點集 = 兩棵樹節點集的聯集 = 兩個等價類的聯集，這正是加入 $(x,y)$ 後 $\\sim_{E \\cup \\{(x,y)\\}}$ 的等價類 ✔。$\\square$\n\n**(U) 的第 2 條就是並查集的全部正確性**：`same(x,y)` 回傳 `find(x) == find(y)`，由 (U) 這等價於「$x$ 與 $y$ 在同一棵樹」，等價於 $x \\sim_E y$。\n\n**注意證明中「路徑壓縮把父指標改成祖先」這個前提被用了兩次**（無環、根不變）。**任何「把父指標指向非祖先節點」的優化都是錯的**——這聽起來理所當然，但在寫帶權並查集（8.6）時很容易在壓縮時算錯權值，效果等同於把節點掛到了錯誤的地方。\n\n### 定理 2（按大小合併 ⇒ 樹高 $\\le \\lfloor \\log_2 n\\rfloor$）\n\n**引理**：只用按大小合併（不壓縮路徑）時，高度為 $h$ 的樹至少有 $2^h$ 個節點。\n\n*證明*（對合併次數歸納）。單點樹 $h=0$、$1 = 2^0$ 個節點 ✔。設把樹 $T_2$（高 $h_2$、$s_2$ 個節點）掛到 $T_1$（高 $h_1$、$s_1$ 個節點）下，且 $s_1 \\ge s_2$。新樹高 $h = \\max(h_1,\\ h_2+1)$，節點數 $s = s_1+s_2$。\n- 若 $h = h_1$：$s \\ge s_1 \\ge 2^{h_1} = 2^h$ ✔。\n- 若 $h = h_2+1$：$s = s_1+s_2 \\ge 2s_2 \\ge 2\\cdot 2^{h_2} = 2^{h}$ ✔（用到 $s_1 \\ge s_2$，這正是「按大小」）。$\\square$\n\n**推論**：$2^h \\le n \\Rightarrow h \\le \\log_2 n$。故單次 `find` 最壞 $O(\\log n)$。\n\n**同樣的論證換個角度，得到更好用的形式：**\n\n> **定理 2'（節點深度只會增加 $\\log_2 n$ 次）**：固定一個節點 $v$。$v$ 的深度只在「$v$ 所在的樹被掛到另一棵樹下」時增加 1，而此時 $v$ 所在連通塊的大小**至少翻倍**（因為被掛的是較小的那棵）。連通塊大小從 1 出發、上限為 $n$，故最多翻倍 $\\log_2 n$ 次 ✔。\n\n**定理 2' 是「按大小合併」最漂亮的說法**，而且它是一個**勢能／攤還式**的論證：不去追蹤樹的形狀，只追蹤一個單調遞增且有上界的量（連通塊大小），用它的變化次數來限制代價。**這個套路在 4.7（單調堆疊）與 6.6（懶刪除堆）都出現過，值得抽象記住：找一個單調變化、有界的量，用它的變化總量來限制操作總量。**\n\n**按秩合併（rank = 樹高的上界）的引理與證明形式相同**，只是把「大小」換成「秩」：秩為 $r$ 的樹至少有 $2^r$ 個節點。**兩者效果等價，按大小的好處是 `sz` 順便能回答「這組多大」，所以本節模板用大小。**\n\n### 定理 3（路徑壓縮為什麼有效：父節點的秩嚴格遞增）\n\n**引理**：在只用路徑壓縮（配合按秩合併）的並查集中，沿任何一條 `fa` 鏈往上走，**節點的秩嚴格遞增**。\n\n*證明*：合併時只把根掛到秩不小於它的根下，且相等時被掛者的秩不變、掛上者的秩 $+1$——兩種情形都保證「父的秩 $>$ 子的秩」。路徑壓縮把節點掛到更高的祖先，由遞移性其秩更大 ✔。$\\square$\n\n**這個引理是複雜度分析的核心**：它說明一條長度為 $\\ell$ 的路徑上有 $\\ell$ 個嚴格遞增的秩。而路徑壓縮把整條路徑上的節點**全部**接到根上，**每個被壓縮的節點，其父節點的秩至少跳到根的秩**——也就是說，一次 $O(\\ell)$ 的代價換來了 $\\ell-1$ 個節點的「父秩大幅提升」，而每個節點的父秩最多提升 $\\log_2 n$ 次。\n\n**完整的攤還分析（Tarjan 1975）證明：路徑壓縮 + 按秩合併下，$m$ 次操作的總代價是 $O(m\\,\\alpha(n))$**，其中 $\\alpha$ 是反 Ackermann 函數。**這個證明需要把秩分成 Ackermann 尺度的區塊並分別計費，本節不展開**——但上面的引理已經足以說明「為什麼壓縮有效」，而且它是後續帶權並查集正確性的直接依據（8.6）。\n\n**你需要記住的三個數字**：\n\n| 策略 | 單次最壞 | $m$ 次總計 |\n| --- | --- | --- |\n| 都不用 | $O(n)$ | $O(mn)$ |\n| 只按大小／按秩 | $O(\\log n)$ | $O(m\\log n)$ |\n| 只路徑壓縮 | $O(n)$ | $O(m\\log_{1+m/n} n)$ 攤還 |\n| 兩者都用 | $O(\\log n)$ | $O(m\\,\\alpha(n))$ |\n\n**「只路徑壓縮」的單次最壞仍是 $O(n)$**（第一次 `find` 一條長鏈），但攤還很好，**在競賽中通常單獨使用就夠了**。**只有「必須保證單次最壞」或「必須可撤銷（不能壓縮）」時，按秩合併才不可省。**\n\n### 定理 4（990 等式方程：為什麼必須先處理所有 `==`）\n\n*問題*：給一組形如 `a==b` 或 `a!=b` 的方程，問是否可滿足。\n\n**演算法**：先把所有 `==` 全部 `unite`，再逐一檢查每個 `a!=b` 是否 `same(a,b)`；有一個成立就矛盾。\n\n*正確性（兩個方向）*。\n- **($\\Rightarrow$ 有解則不報矛盾)**：設存在賦值滿足全部方程。相等關係在該賦值下是等價關係，且包含所有 `==` 對，故包含它們生成的等價閉包 $\\sim_E$。若某個 `a!=b` 滿足 $a \\sim_E b$，則該賦值下 $a$ 與 $b$ 相等，與 `a!=b` 矛盾 ✔。故演算法不會誤報。\n- **($\\Leftarrow$ 不報矛盾則有解)**：構造賦值——**給每個等價類一個不同的值**。所有 `==` 自動滿足（同類同值）；所有 `a!=b` 因為 $a \\not\\sim_E b$ 而分屬不同類、取值不同，也滿足 ✔。$\\square$\n\n**「先併後查」的順序不可交換**，理由由證明直接讀出：($\\Leftarrow$) 方向用的是**最終**的等價類，而不是中間狀態。若邊讀邊處理，一個早出現的 `a!=b` 可能在後面的 `==` 把它們併起來之前就通過了檢查。**這是這題唯一的坑。**\n\n**這個「等價閉包 + 每類給不同值」的構造是一個通用模板**：任何「只有相等約束與不等約束」的可滿足性問題都可以這樣解。**注意它不適用於「$a < b$」這種偏序約束**——偏序不是等價關係，那要用拓撲排序找環。\n\n### 定理 5（684 冗餘連線：第一條「已連通」的邊就是答案）\n\n*問題*：$n$ 個節點、$n$ 條邊的無向連通圖（恰含一個環），刪一條邊使其變成樹。若有多解，回傳**輸入中最後出現**的那一條。\n\n**演算法**：按輸入順序 `unite`，**第一次回傳 `false` 的那條邊就是答案。**\n\n*證明*。\n- **可刪的邊恰為環上的邊**：刪掉環上任一邊仍連通（環上兩點還有另一條路）；刪掉非環邊會斷開（它是橋）✔。\n- **設 $e_t$ 是第一條使 `unite` 回傳 `false` 的邊**，即處理它時兩端已連通。那麼 $e_t$ 與「前 $t-1$ 條邊構成的連通路徑」形成一個環 ⇒ **$e_t$ 在環上** ✔。\n- **$e_t$ 是輸入中最後出現的環邊**：反設環上還有一條邊 $e_{t'}$，$t' > t$。環上的邊共同構成唯一的環（圖恰含一個環），而處理到 $e_t$ 時，環上除 $e_t$ 以外的邊**必須全部已被處理**（否則 $e_t$ 的兩端不會連通——連通的證據只能來自環上的另一條路徑）。故 $t' < t$，矛盾 ✔。$\\square$\n\n**這個證明依賴「圖恰含一個環」**（$n$ 點 $n$ 邊且連通）。**685 冗餘連線 II 是有向版，圖可能有「入度為 2 的節點」與「環」兩種問題疊加，不能直接套**——那是 8.2 的內容。\n\n### 定理 6（為什麼並查集不能刪邊）\n\n**命題**：在路徑壓縮的並查集中，無法在 $O(\\text{polylog})$ 時間內支援「刪除一條先前加入的邊」。\n\n*直觀論證*：`fa` 只記錄了「$x$ 屬於某棵樹」，**完全沒有記錄「$x$ 是因為哪條邊被併進來的」**。刪掉邊 $(u,v)$ 後，要判斷樹是否分裂、以及誰跟誰一起走，需要重建整個連通關係——而路徑壓縮已經把原始的合併歷史抹掉了（節點被直接掛到根上，中間結構永久丟失）。\n\n**三條出路，選哪條取決於題目的形態**：\n1. **離線倒序**：把所有操作讀進來，倒著處理，刪邊變成加邊（8.2 的 803 打磚塊、1970 你能穿過矩陣的最後一天）。**這是最常用的，前提是操作可以離線。**\n2. **可撤銷並查集**：**放棄路徑壓縮**，只按秩合併（保證 $O(\\log n)$），用一個堆疊記錄每次合併改動了哪些欄位，撤銷時回滾。**只能撤銷「最近一次」，配合線段樹分治可處理任意時段的邊。**\n3. **動態連通性（LCT / 歐拉序樹）**：真正的線上刪邊結構，$O(\\log^2 n)$。**遠超一般面試與多數競賽的範圍。**\n\n**「只併不拆」不是並查集的缺點，是它的定義域。** 看到刪除，第一反應應該是「能不能離線倒過來」，而不是「怎麼改並查集」。\n\n## 逐步演算法\n\n:::example 狀態表示：parent 指上司，sz 記錄每夥大小（存在老大身上）\n**1. 狀態表示：`parent` 指向上司，可另存 `size` 在老大身上。** `size[r]` 記錄以 `r` 為老大的那夥有幾個人。\n\n```cpp\n// 狀態表示：parent 指上司，sz 記錄每夥大小（存在老大身上）\nvector<int> parent, sz;\nvoid init(int n) {\n    parent.resize(n);\n    sz.assign(n, 1);\n    for (int i = 0; i < n; ++i) {\n        parent[i] = i;\n    }\n}\n```\n:::\n\n\n\n:::example find：找老大並壓縮路徑\n**2. find：往上爬 + 路徑壓縮。** 這是整個結構快的關鍵，務必加上壓縮。\n\n```cpp\n// find：找老大並壓縮路徑\nint find(int x) { return parent[x] == x ? x : parent[x] = find(parent[x]); }\n```\n:::\n\n\n\n:::example union：小夥併進大夥，同步更新大小\n**3. union：按大小合併並維護摘要。** 把小夥併進大夥，避免樹長太高；合併時順手更新 `size`、連通塊數量等資訊。\n\n```cpp\n// union：小夥併進大夥，同步更新大小\nvoid merge(int x, int y) {\n    int rx = find(x), ry = find(y);\n    if (rx == ry) {\n        return;  // 本來就同夥，不用做事\n    }\n    if (sz[rx] > sz[ry]) {\n        swap(rx, ry);  // 確保 rx 是較小的\n    }\n    parent[rx] = ry;\n    sz[ry] += sz[rx];\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 連通判斷：`find(a) == find(b)` 就是同夥。\n- 想知道「有幾夥」：初始為 n，每成功合併一次就減一。\n- 普通並查集**只能合併、不能拆夥**；遇到「刪邊」類題，常用離線倒序處理（把刪邊倒過來看成加邊）。\n- 複雜度：搭配路徑壓縮與按大小合併，單次操作均攤近乎 `O(1)`（嚴格是反阿克曼函數 `O(α(n))`）。\n\n## C++17 模板\n\n以下用同型延伸示範題 547. 省份數量 展示核心寫法。\n\n**完整題目**：給定城市連通矩陣，計算互相直接或間接連通的省份數量。\n\n:::example 路徑壓縮：遞迴找根並直接將沿途節點接到根上\n**線索**：題目只需要維護等價關係或動態連通塊，且關係主要是加入而非刪除。\n\n```cpp\nclass Solution {\n    vector<int> parent;\n    // 路徑壓縮：遞迴找根並直接將沿途節點接到根上\n    int find(int x) { return parent[x] == x ? x : parent[x] = find(parent[x]); }\n\npublic:\n    int findCircleNum(vector<vector<int>>& isConnected) {\n            int n = isConnected.size();\n            parent.resize(n);\n            iota(parent.begin(), parent.end(), 0);  // 初始化：每個節點各自為根\n            // 遍歷上三角矩陣，將相連的城市合併到同一集合\n            for (int i = 0; i < n; ++i) {\n                    for (int j = i + 1; j < n; ++j) {\n                            if (isConnected[i][j]) {\n                                    parent[find(i)] = find(j);  // 聯集操作\n                            }\n                    }\n            }\n            // 統計根節點數量即為省份數\n            int ans = 0;\n            for (int i = 0; i < n; ++i) {\n                    ans += find(i) == i;\n            }\n            return ans;\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- 並查集把連通的點合併到同一代表元；最後代表元數量就是連通塊數。\n- `find` 取得代表元，`union` 只合併兩個代表元；額外資訊也應掛在代表元上。\n- 最後統計答案時要再次 `find`，避免使用尚未壓縮的中間父節點。\n\n## 時間與空間複雜度\n\n設 $n$ 為元素數、$m$ 為操作數（合併 + 查詢）。\n\n| 操作 | 時間 |\n| --- | --- |\n| 初始化 | $O(n)$ |\n| `find` / `unite` / `same` | 攤還 $O(\\alpha(n))$，最壞 $O(\\log n)$ |\n| 全部 $m$ 次操作 | $O(n + m\\,\\alpha(n))$ |\n\n**空間 $O(n)$**（`fa` 與 `sz` 各一個 `int` 陣列）。\n\n**$\\alpha(n)$ 有多小**：$\\alpha(n) \\le 4$ 對所有 $n < 2^{2^{2^{16}}}$——這個數遠大於宇宙中的原子數。**寫題解時說「近乎常數」是準確的，說「$O(1)$」則不精確。**\n\n各題：\n- **684 冗餘連線**：$O(n\\,\\alpha(n))$。\n- **721 帳號合併**：設 email 總數 $L$。建映射 $O(L)$（雜湊），合併 $O(L\\,\\alpha)$，最後每組排序 $O(L\\log L)$。**瓶頸在排序，不在並查集。**\n- **737 句子相似性 II**：$O\\big((|pairs| + |words|)\\,\\alpha\\big)$，加上字串映射的雜湊成本。\n- **990 等式方程**：$O(|equations| \\cdot \\alpha)$，元素只有 26 個。\n- **1101 彼此熟識的最早時間**：$O(m\\log m)$——**瓶頸是按時間排序**，並查集部分只有 $O(m\\,\\alpha)$。\n\n**實務常數提醒**：\n- **`find` 的迭代版（路徑減半）比遞迴版快，而且不會爆堆疊。** $n = 10^5$ 的長鏈遞迴 `find` 在某些判題環境會 stack overflow。\n- **完整壓縮（兩趟：先找根，再把沿路全部指向根）與路徑減半（一趟：`fa[x] = fa[fa[x]]`）的漸近複雜度相同**（都是 $O(\\alpha)$ 攤還，配合按秩合併）。路徑減半只掃一趟、程式短，**通常更快。**\n- **多測資時用 `iota` 重設 `fa`，不要 `memset`**（`memset` 只能填 0）。\n\n## 常見錯誤與邊界條件\n\n- `parent[find(x)] = find(y)`，不要直接連未壓縮節點。\n- 額外資訊要掛在根節點並在合併時更新。\n- 需要刪邊時普通並查集不支援線上操作。\n- 座標題先做好離散化。\n\n## 常見變形\n\n- 按秩/大小合併。\n- 帶權並查集維護相對距離或比例。\n- 離線倒序處理刪邊問題。\n\n## 與相似技巧的比較\n\n- **與 BFS/DFS 求連通塊**：**一次性地問「有幾個連通塊」，DFS 更簡單**（$O(n+m)$，不需要任何資料結構）。**並查集的價值在「邊加邊問」**——圖是動態長出來的，或者查詢穿插在合併之間。**判準：連通關係是靜態還是動態。**\n- **與二分圖染色 / BFS 判奇環**：判斷「能否二分」時，DFS 染色與「擴展域並查集」（把每個點拆成 $x$ 與 $x+n$ 兩份，代表「同組」與「異組」）都可以。**DFS 染色更直觀，擴展域並查集在「約束是離線給的、且要邊加邊判」時更順手**（886 可能的二分法、990 的變形）。\n- **與 Kruskal 最小生成樹**：Kruskal 就是「排序後依序 `unite`，回傳 `true` 的邊入選」。**`unite` 的 `bool` 回傳值在這裡就是「這條邊是否入選」。** 1101 彼此熟識的最早時間本質上是「Kruskal 跑到連通為止」。\n- **與拓撲排序**：並查集處理**等價關係**（自反、對稱、遞移），拓撲排序處理**偏序**（反對稱）。**「$a$ 與 $b$ 同類」⇒ 並查集；「$a$ 必須在 $b$ 之前」⇒ 拓撲排序。** 定理 4 的證明用到了對稱性，所以那個構造對偏序無效。\n- **與 8.6 帶權並查集**：本節的 `same` 只回答「是否同類」，帶權版還能回答「差多少／比值多少」。**結構相同，只是每條 `fa` 邊上掛一個權值，壓縮時要同步累積。**\n- **與 8.4／8.5 的「指向下一個可用位置」**：那是並查集的一種**非連通性**用法——`fa[i] = i+1` 表示「位置 $i$ 已用，下一個候選是 $i+1$」。**它借用的只有「路徑壓縮讓重複跳躍變快」這個機制，與等價類無關。** 認清這一點，8.4 就不再神秘。\n- **與可撤銷並查集 / 線段樹分治**：需要刪邊時的標準組合。**代價是放棄路徑壓縮**（撤銷需要保留歷史），複雜度退化到 $O(\\log n)$ 每次操作。\n- **與 `unordered_map` 直接存集合**：「小集合併入大集合」（啟發式合併）也能得到 $O(n\\log n)$，而且**能枚舉集合內容**——並查集做不到這件事（要枚舉組內元素必須額外掃一遍全部元素按 `find` 分桶）。**721 帳號合併最後那一步就是在做這件事。**\n\n## 代表例題\n\n**題號**：684、721、737、990、1101\n\n把等價或連通關係合併到代表元；練習在 union 時同步維護集合資訊。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 684 | [冗餘連線](https://leetcode.cn/problems/redundant-connection) |  |\n| 721 | [賬戶合併](https://leetcode.cn/problems/accounts-merge) |  |\n| 737 | [句子相似性 II](https://leetcode.cn/problems/sentence-similarity-ii)（Premium） |  |\n| 990 | [等式方程的可滿足性](https://leetcode.cn/problems/satisfiability-of-equality-equations) | 1638 |\n| 1101 | [彼此熟識的最早時間](https://leetcode.cn/problems/the-earliest-moment-when-everyone-become-friends)（Premium） | 1558 |\n| 1258 | [近義詞句子](https://leetcode.cn/problems/synonymous-sentences) | 1847 |\n| 3493 | [屬性圖](https://leetcode.cn/problems/properties-graph) | 1565 |\n| 3532 | [針對圖的路徑存在性查詢 I](https://leetcode.cn/problems/path-existence-queries-in-a-graph-i) | 1659 |\n\n## 本節重點速查\n\n- **核心**：不記「集合有誰」，只記「父親是誰」。同類 $\\iff$ 代表元相同。\n- **兩條不變量（定理 1）**：`fa` 無環（森林）＋ **每棵樹恰為一個等價類**。路徑壓縮之所以安全，是因為它只把節點掛到**自己的祖先**上——節點不換樹、根不變。\n- **`sz[x]` 只在 $x$ 是根時有意義。** 查組大小要寫 `sz[find(x)]`。任何掛在集合上的資訊都只在根上維護。\n- **`unite` 一定要回傳 `bool`**（本來是否同類）。684 的答案、Kruskal 的選邊、環的偵測全靠它。\n- **按大小合併 ⇒ 樹高 $\\le\\log_2 n$（定理 2）**，更好用的說法是**定理 2'：一個節點的深度最多增加 $\\log_2 n$ 次，因為每次增加時它的連通塊至少翻倍。**「找一個單調有界的量來限制操作總數」是通用的攤還套路。\n- **路徑壓縮有效的原因**：沿 `fa` 鏈往上秩嚴格遞增（定理 3），壓縮一次讓整條路徑的父秩跳到根。兩者合用是攤還 $O(\\alpha(n))$，$\\alpha \\le 4$。\n- **競賽中單用路徑壓縮通常就夠**；「必須保證單次最壞」或「必須可撤銷」時才需要按秩合併。\n- **`find` 用迭代版（路徑減半），不要遞迴**——長鏈會爆堆疊。\n- **990 必須「先併全部 `==`，再查全部 `!=`」**，順序不可交換（定理 4）。等價閉包 + 每類給不同值，是這類可滿足性問題的通用構造；**對「$a<b$」型的偏序約束無效。**\n- **684：第一條讓 `unite` 回傳 `false` 的邊就是答案**（定理 5），前提是「$n$ 點 $n$ 邊且恰含一個環」。**685 有向版不能直接套。**\n- **並查集不能刪邊（定理 6）。** 看到刪除先想「能不能離線倒過來」（8.2），其次才是可撤銷並查集（放棄壓縮）。\n- **忘記 `iota` 初始化 ⇒ 所有元素一開始就同類 ⇒ 答案永遠是 1。**\n",
        },
        {
          id: 55,
          title: "8.2 進階",
          summary:
            "## 這個技術解決什麼問題\n\n8.1 說過並查集的定義域是「只併不拆」。本節處理的，全是**題目看起來要拆、但可以想辦法變成併**的情形，以及**連通性不是題目直接問的東西、需要先建模**的情形。\n\n四類：\n\n1. **刪邊／刪點後查連通性，而且操作能一次全部讀入（離線）** ⇒ **倒著跑**，刪變成併。803 打磚塊、1970 你能穿過矩陣的最後一天、2503 矩陣查詢可獲得的最大分數。\n2. **答案對某個閾值單調** ⇒ **按閾值排序後逐步加邊**，第一次連通時就是答案。778 水位上升的泳池、1102 得分最高的路徑、1631 最小體力消耗路徑。**這與 6.2 定理 2 的「最小瓶頸路」是同一件事，只是換了工具。**\n3. **題意其實在說「這些東西該歸成同一類」，只是包裝過**。765 情侶牽手、947 移除最多的同行或同列石頭、959 由斜槓劃分區域、839 相似字串組。**難點在建模，不在並查集。**\n4. **需要在合併的同時維護額外資訊或選擇特定代表元**。685 冗餘連線 II、1061 按字典序排列最小的等效字串、2092 找出知曉秘密的所有專家。\n\n**共同的心法**：*並查集本身沒有變難，變難的是「把題目翻譯成合併操作」這一步。* 本節的每個定理，證的都是「這個翻譯是對的」。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「進階並查集」想：\n\n- **題目要「刪邊／刪點」後查連通性，而且操作能一次全部拿到（離線）。** 倒著看把刪變成併，例如 803「打磚塊」、803 類的倒序模擬。\n- **正著模擬很痛苦，但倒著看變單調好做。** 「拆」難、「併」易，這個不對稱就是訊號。\n- **題意其實在說「這些東西應該歸成同一類」，只是包裝成情侶、座位、方程式等。** 例如 765「情侶牽手」、685「冗餘連線 II」。\n- **暴力每次刪完都重算連通塊（`O(n)` 每次）太慢**，並查集 + 離線把整體壓到近乎線性。\n\n確認自己有沒有套對，問三個問題：\n\n1. 題目有沒有「刪除」？能不能離線、然後倒過來變成「加入」？\n2. 我的「一個節點」對應到題目裡的什麼？合併代表什麼關係？\n3. 倒序算完之後，我有沒有把答案轉回正序輸出？\n\n## 核心想法與直覺\n\n本節是並查集的進階用法。基礎並查集只能「越併越多」，但很多題目偏偏要「刪東西」或「處理一個不能隨便合併的結構」。本節的核心招式有兩個：**離線倒序**與**精心設計合併對象**。\n\n:::example 離線倒序：把「刪除」倒過來當「加入」處理\n先談最常用的「離線倒序」。並查集不能拆夥，但「拆夥」倒過來看就是「合併」。所以遇到「不斷刪除元素／邊，每次刪完問連通狀態」的題目，把所有刪除收集起來、從最後一刀往回看：**原本的刪除，倒著就變成加回來**，於是又能用普通並查集了。\n\n```cpp\n// 離線倒序：把「刪除」倒過來當「加入」處理\nfor (int i = q - 1; i >= 0; i--) {  // 從最後一個操作往前\n    merge(op[i].a, op[i].b);          // 倒著看：刪變成併\n    answer[i] = curState;  // 記錄這一刻的狀態，最後再正序輸出\n}\n```\n:::\n\n\n\n例如 803「打磚塊」：正著模擬「敲掉磚塊後有幾塊掉落」很難，倒著看就變成「把磚塊一塊塊貼回去，連通塊怎麼長大」，瞬間好做。\n\n:::example 765 的核心：相鄰兩座位的兩人「應同對」，把他們的對編號合併\n第二招是把題意巧妙地對應到「該和誰合併」。例如 765「情侶牽手」：把每對情侶視為一個編號，沙發上每個座位的兩人應該屬於同一對；不同對的人坐在一起就把這兩「對」合併，最後「需要的交換次數 = 人數/2 − 連通塊數」。重點是**想清楚「節點是誰、合併代表什麼」**，並查集本身還是那套。\n\n```cpp\n// 765 的核心：相鄰兩座位的兩人「應同對」，把他們的對編號合併\nfor (int i = 0; i < n; i += 2) {\n    merge(row[i] / 2, row[i + 1] / 2);  // 除以 2 得到「情侶對」的編號\n}\n// 答案 = 對數 - 連通塊數\n```\n:::\n\n## 狀態／資料結構定義\n\n沿用 8.1 的 `DSU`，本節在它之上加三種常見的擴充。\n\n**（一）虛擬節點（超級源點）**——把「與某個外部條件相連」也表達成連通性。\n\n```cpp\nDSU dsu(n * m + 1);          // 節點 0 是虛擬的「天花板」\nconst int SKY = n * m;       // 或直接用 0，本節統一用最後一個編號\n// 第 0 列的每塊磚都 unite(cell, SKY)\n```\n**803 的「穩定」= 與天花板連通**，1970 的「能穿過」= 上邊界與下邊界連通（**兩個虛擬節點**）。**「某個性質是否成立」若能寫成「與某個固定點連通」，就可以用虛擬節點吸收掉。**\n\n**（二）擴展域（種類並查集）**——把每個元素拆成多份，表達「敵對／異類」。\n\n```cpp\nDSU dsu(2 * n);              // x 表示「x 在 A 組」，x + n 表示「x 在 B 組」\n// 「x 與 y 不同組」：unite(x, y + n); unite(y, x + n);\n// 「x 與 y 同組」：  unite(x, y);     unite(x + n, y + n);\n// 矛盾檢查：same(x, x + n)\n```\n**適用於「關係只有兩種，且第二種是第一種的『反』」**（886 可能的二分法、1202 的變形）。**三種以上的循環關係（食物鏈）就開三倍域。**\n\n**（三）在根上掛聚合資訊**——組大小、組內最小編號、組內某個計數。\n\n```cpp\nstruct DSU {\n    vector<int> fa, sz, mn;   // mn[root] = 該組的最小元素編號（1061 用）\n    // ...\n    bool unite(int x, int y) {\n        x = find(x); y = find(y);\n        if (x == y) return false;\n        if (sz[x] < sz[y]) swap(x, y);\n        fa[y] = x;\n        sz[x] += sz[y];\n        mn[x] = min(mn[x], mn[y]);   // 聚合：與合併方向無關才安全\n        return true;\n    }\n};\n```\n**聚合量必須滿足「可交換、可結合」**（min、max、sum、xor 都可以）。**若聚合量依賴合併方向（例如「先加入者優先」），就不能這樣寫**——那需要在根上存完整資訊或改用啟發式合併。\n\n## 不變量或正確性證明\n\n### 定理 1（離線倒序：刪除 $\\Rightarrow$ 加入的等價性）\n\n設初始圖為 $G_0$，第 $t$ 次操作刪除邊集 $D_t$（$t = 1..T$），令 $G_t = G_{t-1} \\setminus D_t$。**查詢 $q_t$ 只依賴 $G_t$ 的連通結構。**\n\n**命題**：倒序處理——從 $G_T$ 開始，依次把 $D_T, D_{T-1}, \\dots, D_1$ 加回去——所經歷的圖序列恰為 $G_T, G_{T-1}, \\dots, G_0$。\n\n*證明*：$G_{t-1} = G_t \\cup D_t$（$D_t \\subseteq G_{t-1}$，刪掉再加回就是原圖）。對 $t$ 從 $T$ 遞減歸納即得 ✔。$\\square$\n\n**兩個必要前提，缺一不可：**\n\n1. **離線**：所有刪除操作必須事先知道，才能先建出 $G_T$。**線上（每次操作依賴前一次的答案）就不能用。**\n2. **查詢只依賴當時的圖**，不依賴「到達該狀態的歷史」。**803 需要特別檢查這一點**（見定理 2）。\n\n**還有一個實作上的前提**：**要先把所有被刪的東西刪掉，得到 $G_T$，再開始倒序。** 忘記這一步（直接在完整圖上倒序）是這一族題最常見的錯誤。\n\n### 定理 2（803 打磚塊：為什麼答案是「加回後穩定數的增量減一」）\n\n*問題*：網格中 1 表示磚塊。與**頂列**相連（透過上下左右的磚塊）的磚塊是**穩定**的。依序敲掉 $\\text{hits}[i]$ 位置的磚塊，問每次敲擊後有多少磚塊**掉落**（不含被敲掉的那塊）。\n\n**演算法**：\n1. 把 `hits` 中所有位置**先全部設為 0**（不管原本是不是磚塊），得到最終網格 $G_T$；\n2. 對 $G_T$ 建並查集，虛擬節點 `SKY` 連接頂列所有磚塊，記 $S_T = |\\{\\text{與 SKY 連通的磚塊}\\}|$；\n3. 倒序 $i = T-1 \\dots 0$：若 $\\text{hits}[i]$ 在**原始**網格中不是磚塊，答案為 0（該次敲擊什麼也沒發生）；否則把它加回、與四鄰及（若在頂列）SKY 合併，得到新的穩定數 $S'$，**答案 $= \\max(0,\\ S' - S - 1)$**，然後令 $S \\leftarrow S'$。\n\n*正確性*。固定一次敲擊，設敲前的網格為 $H$（含被敲的磚 $b$），敲後為 $H' = H \\setminus \\{b\\}$。令 $\\mathrm{St}(X)$ 為網格 $X$ 中穩定磚塊的集合。\n\n- **引理（單調性）**：$\\mathrm{St}(H') \\subseteq \\mathrm{St}(H)$。\n  *證明*：$H'$ 中任一條「從磚塊到頂列」的路徑，在 $H$ 中也存在（$H' \\subseteq H$）✔。\n- **掉落的磚塊恰為 $\\mathrm{St}(H) \\setminus \\mathrm{St}(H') \\setminus \\{b\\}$**。\n  *證明*：一塊磚會掉落 $\\iff$ 它敲前穩定、敲後不穩定，且它不是 $b$ 自己。**注意「敲前不穩定的磚」不會掉**——它早就掉過了（或者說，題目的模型中它從一開始就不在），這是題目語意，也是為什麼要用「穩定數」而非「磚塊數」來計算 ✔。\n- 故掉落數 $= |\\mathrm{St}(H)| - |\\mathrm{St}(H')| - [\\,b \\in \\mathrm{St}(H)\\,]$。\n\n倒序時，$S' = |\\mathrm{St}(H)|$、$S = |\\mathrm{St}(H')|$。**若 $b$ 加回後穩定**（$b \\in \\mathrm{St}(H)$），掉落數 $= S' - S - 1$；**若 $b$ 加回後仍不穩定**，則 $\\mathrm{St}(H) = \\mathrm{St}(H')$，$S' = S$，掉落數 $= 0$——而公式給出 $S'-S-1 = -1$，**這正是要用 $\\max(0,\\cdot)$ 的原因** ✔。$\\square$\n\n**三個容易錯的細節，每一個都在證明裡有對應：**\n\n- **`hits` 可能重複，或指向空格**。步驟 1「不管原本是不是磚塊都設 0」加上步驟 3「查原始網格判斷是否為磚塊」，共同處理了這兩種情況。**重複的 `hits`：第二次敲同一格時原始網格是磚塊、但它在倒序中已經被加回過**——所以要用一個「當前網格」陣列而非原始網格來判斷「這格現在是不是空的」，或者更簡單：把重複的位置在步驟 3 中視為「原始網格為 0」處理。\n- **$\\max(0,\\cdot)$ 不能省。**\n- **`SKY` 的連接要在加回磚塊時同步做**：若加回的磚在第 0 列，除了與四鄰合併，還要 `unite(cell, SKY)`。\n\n### 定理 3（765 情侶牽手：答案 $=$ 情侶對數 $-$ 連通塊數）\n\n*建模*：座位 $2i$ 與 $2i+1$ 是一張沙發。把**情侶對**編號為 $0..n-1$（人 $p$ 屬於情侶對 $\\lfloor p/2 \\rfloor$）。對每張沙發，把坐在上面的兩個人所屬的情侶對 `unite`。\n\n**命題**：最少交換次數 $= n - (\\text{連通塊數})$，其中 $n$ 是情侶對數（沙發數）。\n\n*證明*。設連通塊為 $C_1,\\dots,C_k$，$|C_j| = c_j$，$\\sum c_j = n$。\n\n- **每個連通塊內部自洽**：$C_j$ 中的 $c_j$ 對情侶恰好佔據 $c_j$ 張沙發（每張沙發貢獻一條邊，$c_j$ 個節點的連通塊至少 $c_j - 1$ 條邊；而總邊數 = 總沙發數 = $n$ = 總節點數，故**每個連通塊恰好是一個「基環樹」，即恰含一個環**，邊數 = 節點數 $c_j$）✔。\n- **上界（構造 $c_j - 1$ 次交換）**：若 $c_j = 1$，該沙發已坐著一對情侶，0 次 ✔。若 $c_j \\ge 2$，任取一張「坐著兩個不同情侶對」的沙發，把其中一人與其伴侶交換過來——**該情侶對就從連通塊中分離成一個孤立點**，剩下 $c_j - 1$ 個節點仍連通（環被打開成鏈，仍連通）。遞迴得 $c_j - 1$ 次 ✔。\n- **下界**：目標狀態是「每個連通塊都是孤立點」，即 $n$ 個連通塊。**一次交換最多讓連通塊數增加 1**——交換只改動兩張沙發（兩條邊）的端點，而**改動一條邊最多讓連通塊數 $+1$**；仔細看，一次交換是把兩條邊 $(a,b),(c,d)$ 換成 $(a,c),(b,d)$（或 $(a,d),(b,c)$），這在圖上是「2-opt」操作，最多把一個連通塊拆成兩個 ✔。故從 $k$ 個連通塊到 $n$ 個至少需要 $n-k$ 次 ✔。$\\square$\n\n**這個「答案 = 元素數 − 連通塊數」的形式在交換類問題中反覆出現**（例如「最少交換次數使陣列有序」用置換環分解）。**共同結構：目標是把所有元素變成不動點，一次操作最多打散一個環。** 認出這個形狀，證明就是套模板。\n\n### 定理 4（778 水位上升：單調性 ⇒ 排序後逐步加點）\n\n*問題*：$n\\times n$ 網格，格 $(i,j)$ 的高度 $h_{ij}$。水位 $t$ 時可以在所有 $h \\le t$ 的格子間自由移動（四連通）。求從 $(0,0)$ 游到 $(n-1,n-1)$ 的最小 $t$。\n\n**引理（單調性）**：令 $P(t) = $「在 $\\{h \\le t\\}$ 的格子中，$(0,0)$ 與 $(n-1,n-1)$ 連通」。則 $t \\le t' \\Rightarrow P(t) \\Rightarrow P(t')$。\n\n*證明*：$\\{h \\le t\\} \\subseteq \\{h \\le t'\\}$，前者中的路徑在後者中仍存在 ✔。$\\square$\n\n**由單調性，答案 $= \\min\\{t : P(t)\\}$，可用兩種方式求：**\n\n- **二分 $t$ + 每次 BFS**：$O(n^2 \\log(\\max h))$。\n- **排序所有格子後遞增加入並查集**：$O(n^2\\log n \\cdot \\alpha)$，**第一次 `same(起點, 終點)` 為真時的高度就是答案**。\n\n*後者的正確性*：加入格子 $c$（高度 $h_c$）時，並查集中恰為 $\\{h \\le h_c\\}$ 的連通結構（前提是**按高度非遞減排序**，且相同高度的要**全部加完再檢查**——否則會回報一個偏小的答案）。由引理，第一次連通的高度就是最小的 $t$ ✔。$\\square$\n\n**這就是 6.2 定理 2 的「最小瓶頸路」，換成並查集的說法。** 三種解法的關係：\n\n| 解法 | 複雜度 | 適用 |\n| --- | --- | --- |\n| 二分 + BFS | $O(n^2\\log V)$ | 高度值域小、只問一次 |\n| Dijkstra（$\\oplus = \\max$） | $O(n^2\\log n)$ | 要輸出路徑、或起點固定終點多變 |\n| 排序 + 並查集 | $O(n^2\\log n)$ | **要回答多組「$t$ 為何值時 $u,v$ 連通」** |\n\n**第三種的獨特價值是可以離線批量回答**：把所有查詢按閾值排序，一趟掃完（2503 矩陣查詢可獲得的最大分數就是這個形態）。**更進一步就是 Kruskal 重構樹**，能線上回答「$u,v$ 之間的瓶頸值」。\n\n### 定理 5（685 冗餘連線 II：有向版的完整分類）\n\n*問題*：一棵有根樹（每個非根節點入度 1，根入度 0）加上一條多餘的有向邊，共 $n$ 點 $n$ 邊。刪一條邊使其恢復為有根樹；多解時回傳輸入中最後出現的。\n\n**分類**：多加一條有向邊 $(u,v)$，只可能造成兩種「病症」：\n\n- **病症 A：某個節點 $v$ 的入度變成 2**（$v$ 原本就有父親）。\n- **病症 B：出現有向環**（$v$ 原本是根，於是根有了父親，形成環）。\n\n**且兩者不會同時以「兩個獨立問題」的形式出現**——只加了一條邊，最多製造一個問題點。但**病症 A 與 B 可以同時發生**：$v$ 有兩個父親，且其中一條邊參與了一個環。\n\n**演算法**：\n1. 掃一遍找入度為 2 的節點 $v$。若沒有 ⇒ **純病症 B**，退化成 684 的有向版：按順序 `unite`，**第一條使兩端已連通的邊**就是答案（證明同 8.1 定理 5）。\n2. 若有 ⇒ 記下指向 $v$ 的兩條邊 $e_1$（較早）與 $e_2$（較晚）。**先試著刪 $e_2$**：把除 $e_2$ 外的所有邊 `unite`，若過程中沒有出現「已連通」⇒ 刪 $e_2$ 可行，答案是 $e_2$；否則答案是 $e_1$。\n\n*正確性*。\n- 情形 1（無入度 2）：每個節點入度 $\\le 1$ 且共 $n$ 條邊 ⇒ 每個節點入度恰為 1 ⇒ **圖中必有環**（從任一點反向沿父指標走必定循環）。刪掉環上的邊即可，而「最後出現的環邊」就是第一條使 `unite` 回傳 `false` 的邊 ✔。\n- 情形 2（有入度 2 的 $v$）：多餘的邊**必定是 $e_1$ 或 $e_2$**（其他邊都是原樹的邊，刪掉會斷開）✔。\n  - 刪 $e_2$ 後若剩餘 $n-1$ 條邊構成樹（無環、連通）⇒ $e_2$ 是多餘邊，且它比 $e_1$ 晚出現 ⇒ 依「回傳最後出現者」的規則，答案是 $e_2$ ✔。\n  - 刪 $e_2$ 後仍有環 ⇒ 環由 $e_1$ 與原樹邊構成 ⇒ 多餘的邊只能是 $e_1$（刪 $e_2$ 解決不了環）✔。$\\square$\n\n**這題的教訓不是並查集，是「窮盡所有病症並證明它們互斥或可分別處理」。** 直接套 684 會在有入度 2 的測資上錯，而且錯得很隱蔽。**遇到有向圖的連通性，先問「入度／出度的約束是什麼」，再問「並查集能不能表達它」——並查集本身只懂無向連通，有向的部分必須靠額外的入度統計。**\n\n### 定理 6（959 由斜槓劃分區域：四分格建模的正確性）\n\n*問題*：$n\\times n$ 網格，每格是 `/`、`\\` 或空格。問這些斜槓把正方形分成幾塊。\n\n**建模**：把每格切成 4 個三角形（上 0、右 1、下 2、左 3）。\n\n- 格內：`/` 連 (0,3) 與 (1,2)；`\\` 連 (0,1) 與 (2,3)；空格連全部 4 個。\n- 格間：當前格的 1（右）連右鄰格的 3（左）；當前格的 2（下）連下鄰格的 0（上）。\n\n**答案 = 連通塊數。**\n\n*正確性的關鍵*：**「4 個三角形」這個切分足夠細——任何一條斜槓都恰好沿著三角形的邊界，不會穿過任何三角形的內部。** 於是：\n\n- 每個三角形內部是連通的、且完全屬於某一塊區域 ✔；\n- 兩個相鄰三角形屬於同一塊區域 $\\iff$ 它們之間**沒有斜槓隔開** $\\iff$ 演算法中它們被 `unite` ✔；\n- 區域是三角形的連通閉包 ⇒ 區域數 = 連通塊數 ✔。$\\square$\n\n**「切得夠細，使得所有障礙都落在格子邊界上」是這一族網格題的通用手法。** 若題目的斜線更複雜（例如允許水平／垂直的牆），就切成 $3\\times3$ 或更細的子格。**判準：每一種障礙都必須能被表達成「某兩個子格之間不連通」，而不能穿過子格內部。**\n\n### 定理 7（1061 指定代表元：為什麼要放棄按大小合併）\n\n*問題*：給一組等價的字元對，把字串中每個字元換成其等價類中**字典序最小**的字元。\n\n**做法一（改變合併方向）**：`unite` 時**永遠讓字典序小的當根**：\n```cpp\nif (x > y) swap(x, y);\nfa[y] = x;                     // 小的當根\n```\n**代價**：放棄了按大小合併，樹高可能退化。**但配合路徑壓縮，攤還仍是 $O(\\log n)$**（8.1 表格的「只路徑壓縮」那一行），而且這題只有 26 個元素，完全不是問題。\n\n**做法二（保留按大小合併，額外維護 `mn`）**：根上多存一個 `mn[root]`，合併時取 `min`。**這是通用解**——聚合量滿足交換律與結合律，與合併方向無關（見「狀態／資料結構定義」第三點）。\n\n**該選哪個**：\n- 元素少、或代表元的選擇規則就是全部所需資訊 ⇒ **做法一**，程式短。\n- 元素多、或需要保證 $O(\\alpha)$、或需要同時維護多個聚合量 ⇒ **做法二**。\n\n**通用原則：不要為了「讓代表元有意義」而犧牲平衡策略，除非規模很小。** 代表元只是實作細節，題目要的資訊應該掛在根上，而不是編碼進「誰是根」這件事。\n\n## 逐步演算法\n\n:::example 標準並查集底座（路徑壓縮 + 按大小合併）\n**1. 先準備一個帶大小的標準並查集。** 進階題只是換了使用方式，底層仍是 `find` + `union`。\n\n```cpp\n// 標準並查集底座（路徑壓縮 + 按大小合併）\nvector<int> fa, sz;\nint find(int x) { return fa[x] == x ? x : fa[x] = find(fa[x]); }\nvoid merge(int x, int y) {\n    x = find(x);\n    y = find(y);\n    if (x != y) {\n        fa[x] = y;\n        sz[y] += sz[x];\n    }\n}\n```\n:::\n\n\n\n:::example 收集操作 → 反向遍歷時，刪除自然變加入\n**2. 判斷是否需要離線倒序：題目有沒有「刪」？** 若有刪邊/刪點且可一次拿到全部操作，就收集起來反著做。\n\n```cpp\n// 收集操作 → 反向遍歷時，刪除自然變加入\nvector<Op> ops = readAllOperations();  // 離線：先全部讀進來\n                                                                              // 之後 for(i = m-1; i >= 0; --i) 處理\n```\n:::\n\n\n\n:::example 例：685 冗餘連線 II，先把「節點被指兩個父親」「成環」兩種情況分開\n**3. 設計節點與合併語意。** 把「什麼是一個點、合併代表什麼關係」想清楚，這是進階題最難也最關鍵的一步。\n\n```cpp\n// 例：685 冗餘連線 II，先把「節點被指兩個父親」「成環」兩種情況分開\n// 再用並查集找出該刪哪條邊（合併代表「這條邊被保留」）\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 倒序做完別忘了**把答案再轉回正序**輸出。\n- 倒序時若需要「動態加回節點」，把該節點到鄰居的邊一條條補上即可。\n- 複雜度：仍是近乎線性 `O((n + q) α(n))`，離線只是改變處理順序，不增加數量級。\n\n## C++17 模板\n\n**模板一：803 打磚塊（離線倒序 + 虛擬天花板）**\n\n```cpp\nclass Solution {\n    vector<int> fa, sz;\n    int find(int x) {\n        while (fa[x] != x) {\n            fa[x] = fa[fa[x]];\n            x = fa[x];\n        }\n        return x;\n    }\n    void unite(int x, int y) {\n        x = find(x);\n        y = find(y);\n        if (x == y) return;\n        if (sz[x] < sz[y]) swap(x, y);\n        fa[y] = x;\n        sz[x] += sz[y];\n    }\npublic:\n    vector<int> hitBricks(vector<vector<int>>& grid, vector<vector<int>>& hits) {\n        int n = grid.size(), m = grid[0].size();\n        int SKY = n * m;\n        fa.resize(n * m + 1);\n        iota(fa.begin(), fa.end(), 0);\n        sz.assign(n * m + 1, 1);\n\n        auto cur = grid;                              // 步驟 1：先全部敲掉\n        for (auto& h : hits) cur[h[0]][h[1]] = 0;\n\n        const int dx[4] = {-1, 1, 0, 0}, dy[4] = {0, 0, -1, 1};\n        auto id = [&](int r, int c) { return r * m + c; };\n\n        for (int r = 0; r < n; ++r) {                 // 步驟 2：建 G_T\n            for (int c = 0; c < m; ++c) {\n                if (!cur[r][c]) continue;\n                if (r == 0) unite(id(r, c), SKY);\n                if (r > 0 && cur[r-1][c]) unite(id(r, c), id(r-1, c));\n                if (c > 0 && cur[r][c-1]) unite(id(r, c), id(r, c-1));\n            }\n        }\n        auto stable = [&]() { return sz[find(SKY)] - 1; };   // 扣掉 SKY 自己\n\n        int T = hits.size();\n        vector<int> ans(T, 0);\n        int prev = stable();\n        for (int i = T - 1; i >= 0; --i) {            // 步驟 3：倒序加回\n            int r = hits[i][0], c = hits[i][1];\n            if (grid[r][c] == 0) continue;            // 原本就不是磚塊 → 0\n            if (cur[r][c] == 1) continue;             // 重複的 hits，已加回 → 0\n            cur[r][c] = 1;\n            if (r == 0) unite(id(r, c), SKY);\n            for (int d = 0; d < 4; ++d) {\n                int nr = r + dx[d], nc = c + dy[d];\n                if (nr < 0 || nr >= n || nc < 0 || nc >= m) continue;\n                if (cur[nr][nc]) unite(id(r, c), id(nr, nc));\n            }\n            int now = stable();\n            ans[i] = max(0, now - prev - 1);          // 定理 2\n            prev = now;\n        }\n        return ans;\n    }\n};\n```\n\n**模板二：765 情侶牽手（連通塊計數）**\n\n```cpp\nclass Solution {\n    vector<int> fa;\n    int find(int x) {\n        while (fa[x] != x) {\n            fa[x] = fa[fa[x]];\n            x = fa[x];\n        }\n        return x;\n    }\npublic:\n    int minSwapsCouples(vector<int>& row) {\n        int n = row.size() / 2;                       // 情侶對數 = 沙發數\n        fa.resize(n);\n        iota(fa.begin(), fa.end(), 0);\n        int comp = n;\n        for (int i = 0; i < n; ++i) {\n            int a = find(row[2*i] / 2), b = find(row[2*i+1] / 2);\n            if (a != b) {\n                fa[a] = b;\n                --comp;\n            }\n        }\n        return n - comp;                              // 定理 3\n    }\n};\n```\n\n**模板三：778 水位上升的泳池（排序 + 遞增加點）**\n\n```cpp\nclass Solution {\n    vector<int> fa;\n    int find(int x) {\n        while (fa[x] != x) {\n            fa[x] = fa[fa[x]];\n            x = fa[x];\n        }\n        return x;\n    }\npublic:\n    int swimInWater(vector<vector<int>>& grid) {\n        int n = grid.size();\n        vector<int> pos(n * n);                       // 高度 h 的格子在哪\n        for (int r = 0; r < n; ++r)\n            for (int c = 0; c < n; ++c)\n                pos[grid[r][c]] = r * n + c;\n\n        fa.resize(n * n);\n        iota(fa.begin(), fa.end(), 0);\n        vector<char> on(n * n, false);\n        const int dx[4] = {-1, 1, 0, 0}, dy[4] = {0, 0, -1, 1};\n\n        for (int t = 0; t < n * n; ++t) {             // 高度即為 0..n*n-1 的排列\n            int r = pos[t] / n, c = pos[t] % n;\n            on[pos[t]] = true;\n            for (int d = 0; d < 4; ++d) {\n                int nr = r + dx[d], nc = c + dy[d];\n                if (nr < 0 || nr >= n || nc < 0 || nc >= n) continue;\n                if (!on[nr * n + nc]) continue;\n                int a = find(pos[t]), b = find(nr * n + nc);\n                if (a != b) fa[a] = b;\n            }\n            if (find(0) == find(n * n - 1)) return t; // 第一次連通\n        }\n        return -1;\n    }\n};\n```\n\n（778 的高度恰為 $0..n^2-1$ 的排列，所以連排序都省了。**高度有重複時，必須把同一高度的格子全部加完才檢查連通性**——見定理 4 的括號註記。）\n\n## 時間與空間複雜度\n\n設 $n$ 為元素／格子數、$m$ 為邊數／操作數、$q$ 為查詢數。\n\n| 題目 | 時間 | 空間 | 瓶頸 |\n| --- | --- | --- | --- |\n| 803 打磚塊 | $O\\big((nm + \\lvert hits\\rvert)\\,\\alpha\\big) $ | $O(nm)$ | 建 $G_T$ |\n| 765 情侶牽手 | $O(n\\,\\alpha)$ | $O(n)$ | — |\n| 778 水位上升 | $O(n^{2}\\alpha)$（高度是排列，免排序） | $O(n^{2})$ | — |\n| 一般「排序 + 加邊」 | $O(m\\log m + m\\,\\alpha)$ | $O(n)$ | **排序** |\n| 685 冗餘連線 II | $O(n\\,\\alpha)$ | $O(n)$ | — |\n| 959 斜槓劃分區域 | $O(n^{2}\\alpha)$ | $O(n^{2})$ | 節點數是 $4n^2$ |\n| 947 移除石頭 | $O(n\\,\\alpha)$（用行／列當虛擬節點） | $O(n)$ | — |\n| 839 相似字串組 | $O(n^{2}\\ell + n^{2}\\alpha)$ | $O(n)$ | **兩兩比較 $O(n^2\\ell)$** |\n\n**兩個「瓶頸不在並查集」的例子值得記住**：\n- **「排序 + 加邊」類題目的瓶頸永遠是排序**（$O(m\\log m)$），並查集只佔 $O(m\\alpha)$。**想優化就從「能不能免排序」下手**（778 的高度是排列、計數排序、值域小）。\n- **839 的瓶頸是兩兩比較。** $n \\le 300$、$\\ell \\le 300$ ⇒ $2.7\\times10^7$，剛好能過。**若 $n$ 再大一級，就必須換成「枚舉每個字串的所有可能交換」來建邊**——判斷該枚舉哪一邊，比較 $n^2\\ell$ 與 $n\\ell^2$。\n\n**離線倒序的額外成本是 $O(1)$**——它只是改變處理順序，不增加任何漸近開銷。**這是它如此划算的原因：一個純粹的「換個方向看」就把不可做變成可做。**\n\n## 常見錯誤與邊界條件\n\n- **倒序前忘記先建 $G_T$**。直接在完整圖上倒序，第一步的狀態就錯了。**「先刪光，再倒著加回來」是兩個步驟，不是一個。**\n- **803 忘記 $\\max(0,\\cdot)$**。加回的磚塊本身不穩定時公式給出 $-1$。\n- **803 沒處理重複的 `hits` 或空格上的 `hits`**。兩者都要回傳 0，判斷依據不同：空格看**原始網格**，重複看**當前網格**。\n- **803 的 `stable()` 忘記扣掉 SKY 自己**。`sz[find(SKY)]` 包含虛擬節點，要 $-1$。\n- **虛擬節點的編號與真實節點衝突**。$n\\times m$ 的網格用 `n*m` 當虛擬節點，陣列要開 `n*m+1`。**開成 `n*m` 是越界，症狀通常是隨機錯誤而非崩潰。**\n- **778 高度有重複時邊加邊查**。必須把同高度的全部加完再檢查，否則答案偏小。**題目保證是排列時可以不管，但變形題要小心。**\n- **擴展域忘記「雙向」表達不同組**。`unite(x, y+n)` 只說了一半，還要 `unite(y, x+n)`。**只寫一半在多數測資上碰巧會對，在鏈式約束上會錯。**\n- **擴展域的陣列開 `2*n` 但索引寫成 `x + n` 時 `x` 已經是 `find` 過的根**。**必須用原始編號做偏移**，`find(x) + n` 是錯的。\n- **685 只處理了環、沒處理入度 2**（或反之）。兩種病症都要檢查。\n- **959 的三角形編號順序不一致**。上右下左（0123）與上下左右（0123）混用會讓格間連接錯位。**寫在註解裡，並在格間連接處對照一次。**\n- **在根上維護聚合量，但讀取時忘記 `find`**。`mn[x]` 是垃圾，`mn[find(x)]` 才對（8.1 的老問題，在本節因為聚合量變多而更容易犯）。\n- **合併時聚合量的更新寫在 `swap` 之前**。`swap(x,y)` 之後 `x` 才是新根，聚合必須在 `swap` 之後、且寫進 `x`。\n- **1061 改變合併方向後仍假設 $O(\\alpha)$**。放棄按大小合併後是 $O(\\log n)$；規模大時要改用「聚合量」寫法。\n\n## 與相似技巧的比較\n\n- **與 8.1**：8.1 是「工具本身」，本節是「怎麼把題目翻譯成這個工具」。**本節的每個定理都在證明一個翻譯的正確性**，而不是在改進並查集。\n- **與可撤銷並查集 + 線段樹分治**：處理「每條邊只在某個時間區間內存在」的一般情形。**離線倒序（定理 1）是它的特例**——所有邊的存在區間都是「從某時刻到最後」這種後綴形式。**看到「邊有存活區間，而且不是後綴」就要上線段樹分治。** 代價是放棄路徑壓縮，$O(m\\log^2 n)$。\n- **與 Kruskal 重構樹**：定理 4 的「排序 + 加邊」若要**線上**回答「$u$ 與 $v$ 的瓶頸值」，就把每次合併建成一個新節點（權值 = 當前閾值），最後得到一棵二元樹，查詢變成 LCA。**$O(m\\log m)$ 預處理、$O(\\log n)$ 查詢。** 這是本節第 2 類題的終極形態。\n- **與 Dijkstra（$\\oplus=\\max$）**：6.2 定理 2 證明了最小瓶頸路可以用 Dijkstra 求。**Dijkstra 適合單源多終點且要輸出路徑；並查集適合離線批量回答任意點對。** 兩者複雜度相近，選擇看查詢形態。\n- **與 BFS/DFS + 二分答案**：778 用二分 + BFS 是 $O(n^2\\log V)$，寫起來更短。**值域小的時候二分更划算；要回答多個查詢時並查集更划算。**\n- **與拓撲排序**：685 的有向部分（入度統計）本質上是拓撲的思路。**並查集只懂無向連通，有向圖的環／入度必須另外處理**——這是 684 與 685 難度差距的全部來源。\n- **與 8.3～8.6**：後面四節都是本節「建模」思路的專門化——**8.3 用質因數當橋樑節點、8.4／8.5 用 `fa` 表達「下一個可用位置」（與連通性無關）、8.6 在邊上掛權值。** 8.4／8.5 尤其值得注意：**它們借的只是「路徑壓縮讓重複跳躍變快」這個機制**，不要用等價類的直覺去理解。\n\n## 代表例題\n\n**題號**：LCP 71、685、765、778、803\n\n把等價或連通關係合併到代表元；練習在 union 時同步維護集合資訊。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| LCP 71 | [集水器](https://leetcode.cn/problems/kskhHQ)（Premium） |  |\n| 685 | [冗餘連線 II](https://leetcode.cn/problems/redundant-connection-ii) |  |\n| 765 | [情侶牽手](https://leetcode.cn/problems/couples-holding-hands) |  |\n| 778 | [水位上升的泳池中游泳](https://leetcode.cn/problems/swim-in-rising-water) |  |\n| 803 | [打磚塊](https://leetcode.cn/problems/bricks-falling-when-hit) |  |\n| 839 | [相似字串組](https://leetcode.cn/problems/similar-string-groups) | 2054 |\n| 947 | [移除最多的同行或同列石頭](https://leetcode.cn/problems/most-stones-removed-with-same-row-or-column) | 2035 |\n| 959 | [由斜槓劃分割槽域](https://leetcode.cn/problems/regions-cut-by-slashes) | 2136 |\n| 1061 | [按字典序排列最小的等效字串](https://leetcode.cn/problems/lexicographically-smallest-equivalent-string) |  |\n| 1202 | [交換字串中的元素](https://leetcode.cn/problems/smallest-string-with-swaps) | 1855 |\n| 1579 | [保證圖可完全遍歷](https://leetcode.cn/problems/remove-max-number-of-edges-to-keep-graph-fully-traversable) | 2132 |\n| 1632 | [矩陣轉換後的秩](https://leetcode.cn/problems/rank-transform-of-a-matrix) | 2530 |\n| 1722 | [執行交換操作後的最小漢明距離](https://leetcode.cn/problems/minimize-hamming-distance-after-swap-operations) | 1892 |\n| 1970 | [你能穿過矩陣的最後一天](https://leetcode.cn/problems/last-day-where-you-can-still-cross) | 2124 |\n| 2076 | [處理含限制條件的好友請求](https://leetcode.cn/problems/process-restricted-friend-requests) | 2131 |\n| 2092 | [找出知曉祕密的所有專家](https://leetcode.cn/problems/find-all-people-with-secret) | 2004 |\n| 2157 | [字串分組](https://leetcode.cn/problems/groups-of-strings) | 2499 |\n| 2371 | [最小化網格中的最大值](https://leetcode.cn/problems/minimize-maximum-value-in-a-grid) |  |\n| 2421 | [好路徑的數目](https://leetcode.cn/problems/number-of-good-paths) | 2445 |\n| 2459 | [透過移動專案到空白區域來排序陣列](https://leetcode.cn/problems/sort-array-by-moving-items-to-empty-space) |  |\n| 2471 | [逐層排序二叉樹所需的最少運算元目](https://leetcode.cn/problems/minimum-number-of-operations-to-sort-a-binary-tree-by-level) | 1635 |\n| 2503 | [矩陣查詢可獲得的最大分數](https://leetcode.cn/problems/maximum-number-of-points-from-grid-queries) | 2196 |\n| 2812 | [找出最安全路徑](https://leetcode.cn/problems/find-the-safest-path-in-a-grid) | 2154 |\n| 2867 | [統計樹中的合法路徑數目](https://leetcode.cn/problems/count-valid-paths-in-a-tree) | 2428 |\n| 3235 | [判斷矩形的兩個角落是否可達](https://leetcode.cn/problems/check-if-the-rectangle-corner-is-reachable) | 3774 |\n| 3551 | [數位和排序需要的最小交換次數](https://leetcode.cn/problems/minimum-swaps-to-sort-by-digit-sum) | 1507 |\n| 3600 | [升級後最大生成樹穩定性](https://leetcode.cn/problems/maximize-spanning-tree-stability-with-upgrades) | 2301 |\n| 3608 | [包含 K 個連通分量需要的最小時間](https://leetcode.cn/problems/minimum-time-for-k-connected-components) | 1893 |\n| 3613 | [最小化連通分量的最大成本](https://leetcode.cn/problems/minimize-maximum-component-cost) | 1642 |\n| 3695 | [交換元素後的最大交替和](https://leetcode.cn/problems/maximize-alternating-sum-using-swaps) | 1984 |\n\n## 本節重點速查\n\n- **本節難在建模，不在並查集。** 每個定理證的都是「這個翻譯對不對」。\n- **離線倒序（定理 1）**：刪 ⇒ 併。兩個前提——**離線**、**查詢只依賴當時的圖**。實作上**必須先刪光建出 $G_T$，再倒著加回**。額外成本為零，是最划算的技巧。\n- **803**：穩定 = 與虛擬天花板連通；**答案 $=\\max(0,\\ S'-S-1)$**。$-1$ 扣掉磚塊自己，$\\max$ 處理「加回後仍不穩定」。空格與重複 `hits` 分別看原始網格與當前網格。`stable()` 記得扣掉 SKY。\n- **765**：把情侶對當節點、沙發當邊，**答案 $= n - \\text{連通塊數}$**。「答案 = 元素數 − 連通塊數」是交換類問題的通用形狀（一次操作最多打散一個環）。\n- **閾值單調 ⇒ 排序後遞增加邊，第一次連通即答案（定理 4）。** 這與 6.2 的最小瓶頸路是同一件事。**同閾值的元素要全部加完再檢查。** 瓶頸是排序而非並查集。\n- **685 的兩種病症**：入度 2、有向環，**可以同時發生**。有入度 2 時先試刪較晚那條。**並查集只懂無向連通，有向的入度約束必須額外統計。**\n- **959 的四分格**：切得夠細，使所有障礙都落在子格邊界上，不穿過子格內部。\n- **虛擬節點**把「與某個外部條件相連」表達成連通性；**擴展域**（$x$ 與 $x+n$）表達「敵對」，記得雙向寫。\n- **在根上掛聚合量時，聚合必須可交換可結合，且更新要寫在 `swap` 之後、讀取要先 `find`。**\n- **指定代表元（1061）**：改變合併方向最短但放棄平衡（$O(\\log n)$）；維護 `mn` 是通用解。**規模大就用後者。**\n- **邊的存活區間不是後綴 ⇒ 線段樹分治 + 可撤銷並查集；要線上查瓶頸值 ⇒ Kruskal 重構樹。**\n",
        },
        {
          id: 56,
          title: "8.3 GCD 並查集",
          summary:
            "## 這個技術解決什麼問題\n\n一組數字，兩個數「有公因數」（或「$\\gcd > $ 某個門檻」「$\\mathrm{lcm} \\le$ 某個門檻」）就連一條邊，問連通塊個數、最大連通塊、能否全連通、能否透過交換排序。\n\n**直接兩兩配對是 $O(n^2\\log V)$**（每對算一次 $\\gcd$）。$n = 2\\times10^4$ 就是 $4\\times10^8$ 次 $\\gcd$，過不了。\n\n**核心想法**：*不要在數字之間直接連邊，改成「數字 — 質因數 — 數字」的兩層結構。* 把每個質數也當成並查集裡的一個節點，把每個數字與它的所有質因數合併。於是：\n\n$$\\gcd(a,b) > 1 \\iff a \\text{ 與 } b \\text{ 有共同的質因數 } p \\iff a \\text{ 與 } b \\text{ 都連到節點 } p.$$\n\n**邊數從 $O(n^2)$ 降到 $O(n\\log V)$**——因為一個 $\\le 10^9$ 的數最多只有 9 個相異質因數（定理 2）。\n\n**這是一個非常一般的手法，值得抽象記住：**\n\n> **當「兩兩之間有邊」的條件可以寫成「共享某個屬性」時，就把屬性本身當成節點，把 $O(n^2)$ 的完全圖壓成 $O(n\\cdot k)$ 的二分圖。**\n\n同一個手法在其他地方也用：**947 移除石頭**（共享行號或列號 ⇒ 把行、列當節點）、**721 帳號合併**（共享 email ⇒ 把 email 當節點）、**1998／2709**（共享質因數）。**「共享屬性 ⇒ 屬性當橋樑」是本節真正要學的東西，質因數只是它最常見的實例。**\n\n**辨識訊號**：連通條件裡出現 $\\gcd$、$\\mathrm{lcm}$、「有公因數」「互質」「共用因數」，而且 $n$ 大到不能兩兩枚舉。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「GCD 並查集」想：\n\n- **連通條件是「兩數 `gcd > 1`」或「共用因數」。** 例如 952、2709、1998「最大公因數排序」。\n- **若直接兩兩配對是 `O(n²)`，數字量大到受不了。** 透過「質因數當橋樑」把它降到 `O(n sqrt(V))`。\n- **題目最後問「分成幾組／最大組多大／能否全部連成一塊」**——這是連通塊的招牌問法。\n\n:::example 嗅到味道：看到「gcd > 1 才相連」，先想「共用質因數 → 經質因數合併」\n```cpp\n// 嗅到味道：看到「gcd > 1 才相連」，先想「共用質因數 → 經質因數合併」\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的連通條件是不是「共用質因數」？能不能用質因數當橋樑？\n2. 質因數節點的編號有沒有和數值節點分開、不會撞號？\n3. 統計答案時，我數的是「數值節點」還是不小心連質因數也數進去了？\n\n## 核心想法與直覺\n\n本節處理一類特別的連通性題目：**兩個數字只要「有公因數（最大公因數 > 1）」，就算相連**，問你最後分成幾組、最大一組多大。例如 952「按公因數計算最大元件大小」、2709「最大公約數遍歷」。\n\n直覺做法是把每兩個數都算一次 gcd，看大於 1 就合併。但這要 `O(n²)` 對配對，數字一多就爆。\n\n:::example 核心轉換：不是數字配對數字，而是「數字 ↔ 它的質因數」連起來\n關鍵觀察：**兩個數有公因數，等於它們「共用了某個質因數」。** 與其讓「數字和數字」直接合併，不如讓「數字和它的每個質因數」合併——只要兩個數共用同一個質因數，它們就會透過那個質因數被串到同一組裡。\n\n```cpp\n// 核心轉換：不是數字配對數字，而是「數字 ↔ 它的質因數」連起來\n// 例：12 = 2 × 2 × 3 → 把 12 和質因數 2、3 各 merge 一次\n// 18 = 2 × 3 × 3 → 把 18 和 2、3 各 merge 一次\n// 於是 12 和 18 透過質因數 2（或 3）自動同組\n```\n:::\n\n\n\n:::example 對每個數，分解質因數，把「數值」和「質因數」併在一起\n這樣每個數字只要對「它的質因數們」做合併，數量遠小於兩兩配對。把質因數也納入並查集（用一個和數字不衝突的編號），最後同一組裡的「數字」就是答案要數的東西。\n\n```cpp\n// 對每個數，分解質因數，把「數值」和「質因數」併在一起\nvoid addNumber(int val) {\n    int x = val;\n    for (int p = 2; (long long)p * p <= x; ++p) {\n        if (x % p == 0) {\n            merge(val, primeId(p));  // 數值 ↔ 質因數 p\n            while (x % p == 0) {\n                x /= p;  // 把 p 除乾淨\n            }\n        }\n    }\n    if (x > 1) {\n        merge(val, primeId(x));  // 剩下的大質數\n    }\n}\n```\n:::\n\n\n\n分解一個數是 `O(sqrt(V))`，n 個數合計 `O(n sqrt(V))`，遠快過 `O(n²)`。\n\n## 狀態／資料結構定義\n\n**節點集合 = 數字（或索引）$\\cup$ 質數。** 兩種編號方式：\n\n**（一）值域小（$V \\le 10^5$ 左右）：直接開陣列，質數用它的值當編號**\n```cpp\nDSU dsu(V + 1);            // 節點 v 同時代表「值 v」與「質數 v」——它們本來就該是同一個\nfor (int x : nums)\n    for (int p : primeFactors(x))\n        dsu.unite(x, p);   // 值 x 與質數 p 合併\n```\n**這裡有一個漂亮的巧合：質數 $p$ 自己也是一個可能出現的值，而「值 $p$」與「質數 $p$」本來就該同組**（$\\gcd(p,p)=p>1$）。**所以共用同一個編號不但不衝突，還是正確的。**\n\n**（二）值域大（$\\le 10^9$）：質數用 `unordered_map<int,int>` 分配編號**\n```cpp\nDSU dsu(n + MAXPRIMES);            // 前 n 個是索引，之後是質數\nunordered_map<int,int> pid;\nauto getPid = [&](int p) {\n    auto it = pid.find(p);\n    if (it != pid.end()) return it->second;\n    return pid[p] = n + (int)pid.size();\n};\n```\n**此時節點要用「索引」而非「值」**——因為值可能重複，而題目通常問的是索引的連通性（1998 最大公因數排序、2709 最大公因數遍歷）。\n\n**質因數分解的兩種方式，依值域選：**\n\n```cpp\n// (A) 試除，O(sqrt(x))：值域大時用\nvector<int> primeFactors(int x) {\n    vector<int> res;\n    for (int p = 2; (long long)p * p <= x; ++p) {\n        if (x % p) continue;\n        res.push_back(p);\n        while (x % p == 0) x /= p;\n    }\n    if (x > 1) res.push_back(x);      // 剩下的大質數，絕對不能漏\n    return res;\n}\n\n// (B) 最小質因數篩（SPF），預處理 O(V log log V)，單次分解 O(log x)\nvector<int> spf(V + 1);\nvoid sieve() {\n    iota(spf.begin(), spf.end(), 0);\n    for (int i = 2; (long long)i * i <= V; ++i)\n        if (spf[i] == i)\n            for (int j = i * i; j <= V; j += i)\n                if (spf[j] == j) spf[j] = i;\n}\n// 分解：while (x > 1) { int p = spf[x]; res.push_back(p); while (x % p == 0) x /= p; }\n```\n\n**選哪個**：$n$ 個數各做一次試除是 $O(n\\sqrt V)$；SPF 篩是 $O(V\\log\\log V + n\\log V)$。**$n\\sqrt V$ vs $V$：$n$ 小值域大 ⇒ 試除；$n$ 大值域小 ⇒ 篩。** 952（$V \\le 10^5$、$n \\le 2\\times10^4$）：$2\\times10^4 \\times 316 = 6\\times10^6$ 試除也很快，兩者皆可。\n\n**（三）「因數當橋樑」的變體（1627）**：條件是 $\\gcd > T$ 而不是 $\\gcd > 1$，此時**質數不夠用**（$\\gcd$ 可能是合數），要枚舉所有 $> T$ 的因數當橋樑——見定理 3。\n\n## 不變量或正確性證明\n\n### 定理 1（質因數橋接的等價性）\n\n設 $A$ 是給定的數字集合。定義兩個圖：\n\n- $G_1$：頂點 $A$，$a$ 與 $b$ 相連 $\\iff \\gcd(a,b) > 1$。\n- $G_2$：**二分圖**，頂點 $A \\cup P$（$P$ = 出現過的所有質數），$a$ 與 $p$ 相連 $\\iff p \\mid a$。\n\n**命題**：對任意 $a, b \\in A$，**$a$ 與 $b$ 在 $G_1$ 中連通 $\\iff$ 在 $G_2$ 中連通。**\n\n*證明*。\n\n- **($\\Rightarrow$)** 只需證單條邊。設 $\\gcd(a,b) = g > 1$，取 $g$ 的任一質因數 $p$，則 $p \\mid a$ 且 $p \\mid b$，故 $a - p - b$ 是 $G_2$ 中的路徑 ✔。連通性由路徑的串接得到。\n- **($\\Leftarrow$)** 設 $G_2$ 中有路徑 $a = a_0 - p_1 - a_1 - p_2 - \\cdots - p_k - a_k = b$（二分圖的路徑必定數字與質數交錯）。對每個 $i$，$p_i$ 同時整除 $a_{i-1}$ 與 $a_i$，故 $\\gcd(a_{i-1}, a_i) \\ge p_i > 1$，即 $a_{i-1}$ 與 $a_i$ 在 $G_1$ 中直接相連 ✔。串起來得 $a$ 與 $b$ 在 $G_1$ 中連通。$\\square$\n\n**注意證明用到「二分圖的路徑必定交錯」**——這保證了路徑上的相鄰數字對都真的共享一個質數。**若你把「合數因數」也加進去當節點，這個性質仍然成立**（$d \\mid a$ 且 $d \\mid b$ $\\Rightarrow \\gcd \\ge d > 1$），只是節點變多、白費工。**所以只用質因數，不是為了正確性，是為了效率。**\n\n**兩個前提要點名：**\n\n1. **$1$ 沒有質因數，所以它在 $G_2$ 中是孤立點。** 這與 $G_1$ 一致（$\\gcd(1,x) = 1$），所以**不需要特判**——但很多人會下意識想「1 跟誰都能連」而寫錯。**2709 最大公因數遍歷的標準測資就是 `nums = [1]`（回傳 `true`，因為只有一個節點）與 `nums = [1,2]`（回傳 `false`）。**\n2. **答案要的是「數字的連通性」，質數節點只是橋樑。** 統計連通塊大小時**不能把質數節點算進去**——要嘛只統計數字節點，要嘛在 `sz` 之外另開一個 `cntNum[root]` 只累加真實數字。**這是本節的頭號 bug。**\n\n### 定理 2（每個數的相異質因數個數是 $O(\\log V)$）\n\n**命題**：$v \\le 10^9$ 的相異質因數不超過 **9** 個。\n\n*證明*：設 $v$ 有 $k$ 個相異質因數，則 $v \\ge$ 最小的 $k$ 個質數之積。而\n$$2\\cdot3\\cdot5\\cdot7\\cdot11\\cdot13\\cdot17\\cdot19\\cdot23 = 223\\,092\\,870 < 10^9,$$\n$$2\\cdot3\\cdot5\\cdot7\\cdot11\\cdot13\\cdot17\\cdot19\\cdot23\\cdot29 = 6\\,469\\,693\\,230 > 10^9.$$\n故 $k \\le 9$ ✔。$\\square$\n\n**一般地，$v$ 的相異質因數個數是 $O(\\log v / \\log\\log v)$**，上界 $\\log_2 v$ 也夠用。**這就是為什麼「每個數連到它的所有質因數」只產生 $O(n\\log V)$ 條邊，而不是更多。**\n\n**實用的數字**（背下來，估複雜度時直接用）：\n\n| 值域 | 相異質因數上限 |\n| --- | --- |\n| $10^5$ | 6 |\n| $10^6$ | 7 |\n| $10^9$ | 9 |\n| $10^{18}$ | 15 |\n\n### 定理 3（1627 帶閾值的圖連通性：因數當橋樑 + 調和級數）\n\n*問題*：城市編號 $1..n$，$x$ 與 $y$ 相連 $\\iff \\gcd(x,y) > T$。回答若干組連通性查詢。\n\n**關鍵等價**：\n$$\\gcd(x,y) > T \\iff \\exists\\, d > T \\text{ 使 } d \\mid x \\text{ 且 } d \\mid y.$$\n*證明*：($\\Rightarrow$) 取 $d = \\gcd(x,y)$ ✔。($\\Leftarrow$) $d$ 是公因數 ⇒ $\\gcd(x,y) \\ge d > T$ ✔。$\\square$\n\n**所以橋樑不是質數，是「所有 $> T$ 的整數」。** 演算法：\n\n```cpp\nfor (int d = T + 1; d <= n; ++d)\n    for (int m = 2 * d; m <= n; m += d)\n        dsu.unite(d, m);          // d 的所有倍數彼此相連\n```\n\n*正確性*：對每個 $d > T$，把 $d$ 的所有倍數合併成一組——這正好表達了「共享因數 $d$」。由上面的等價，並查集的連通性恰為題目的連通性 ✔。**這裡把 $d$ 自己當橋樑節點（$d$ 也是一個城市），與定理 1 的巧合同理。**\n\n*複雜度*：內層迴圈執行 $\\lfloor n/d \\rfloor - 1$ 次，總計\n$$\\sum_{d=T+1}^{n} \\left(\\frac{n}{d} - 1\\right) < n\\sum_{d=T+1}^{n}\\frac1d \\approx n\\ln\\frac{n}{T} = O\\!\\left(n\\log\\frac{n}{T}\\right).$$\n**$T$ 越大越快；$T = 0$ 時退化成 $O(n\\log n)$，仍完全可接受。** $\\square$\n\n**這個「枚舉 $d$ 及其倍數，總和是調和級數」的模式在數論題中反覆出現**（篩法、莫比烏斯、求所有數的因數和）。**看到「對每個 $d$ 掃它的倍數」，直接寫 $O(n\\log n)$，不要以為是 $O(n^2)$。**\n\n### 定理 4（1998 最大公因數排序：連通塊內可任意重排）\n\n*問題*：可以任意次交換 $\\text{nums}[i]$ 與 $\\text{nums}[j]$，只要 $\\gcd(\\text{nums}[i], \\text{nums}[j]) > 1$。問能否排成非遞減。\n\n**引理（連通圖的邊生成整個對稱群）**：設 $G$ 是頂點集 $S$（$|S| = k$）上的連通圖。則由 $G$ 的邊所對應的對換 $\\{(u\\ v) : (u,v) \\in E(G)\\}$ 生成的置換群，就是 $S$ 上的**完整對稱群** $\\mathfrak{S}_k$。\n\n*證明*（對 $k$ 歸納）。$k=1$ 顯然。設 $k \\ge 2$，取 $G$ 的一棵生成樹 $T$ 與其一個葉節點 $\\ell$，設 $\\ell$ 的唯一鄰居為 $u$。由歸納假設，$T - \\ell$ 上的對換生成 $\\mathfrak{S}_{S\\setminus\\{\\ell\\}}$。而任一對換 $(\\ell\\ v)$（$v \\ne u$）可寫成\n$$(\\ell\\ v) = (u\\ v)\\,(\\ell\\ u)\\,(u\\ v),$$\n右側三個因子都已在生成集中（$(u\\ v)$ 來自歸納假設，$(\\ell\\ u)$ 是樹邊）✔。故所有對換都能生成，而對換生成 $\\mathfrak{S}_k$ ✔。$\\square$\n\n**推論**：**在同一個連通塊內，元素可以任意重排。**\n\n*所以演算法是*：\n1. 用質因數橋接建並查集（節點用**索引**，因為值可能重複）；\n2. 把 `nums` 排序得到 `sorted`；\n3. 檢查每個位置 $i$：`sorted[i]` 與 `nums[i]` 是否屬於同一個連通塊。全部符合則 `true`。\n\n*步驟 3 的正確性*：\n- **($\\Leftarrow$ 全符合則可排序)**：連通塊 $C$ 佔據的位置集合固定（交換不會把元素移出連通塊——**因為連通性是定義在值上的，而交換不改變值的集合**）。由推論，塊內可任意重排，所以只要「排序後每個位置該放的值仍在原塊內」，就能達成 ✔。\n- **($\\Rightarrow$ 可排序則全符合)**：任何交換序列都不會把值移出它所屬的連通塊，故排序後位置 $i$ 上的值必與原本 $\\text{nums}[i]$ 同塊 ✔。$\\square$\n\n**實作細節**：值有重複時，`sorted[i]` 與 `nums[i]` 可能是同一個值的不同出現。**用「值 → 代表根」的映射**（`root_of_value[v]`）比用索引更好寫，因為相同的值必定在同一個連通塊（$\\gcd(v,v) = v > 1$，只要 $v > 1$）。**$v = 1$ 是例外：兩個 1 之間 $\\gcd = 1$，不能交換——但它們也不需要交換（相同的值換了等於沒換），所以不影響結論。**\n\n### 定理 5（952 與 2709：同一個並查集，兩種讀法）\n\n**952 按公因數計算最大組件大小**：答案 = **數字節點**的最大連通塊大小。\n\n由定理 1，連通塊就是「$\\gcd > 1$ 關係的等價閉包」。**統計時要用 `cntNum[root]`（只算真實數字），不能用 `sz[root]`（含質數節點）。**\n\n**2709 最大公因數遍歷**：問「任兩個索引之間是否都能到達」，即**是否只有一個連通塊**。\n\n三個邊界：\n- $n = 1$ ⇒ 直接 `true`；\n- 有任何一個 $\\text{nums}[i] = 1$ 且 $n > 1$ ⇒ `false`（1 是孤立點，定理 1 的前提 1）；\n- 否則檢查所有索引是否同根。\n\n**3378 統計最小公倍數圖中的連通塊數目**（見定理 6）與這兩題共用同一個框架，只是橋樑的選法不同。**認出「橋樑是什麼」是本節每一題的唯一難點。**\n\n### 定理 6（3378 lcm 圖：用「最小倍數」當橋樑）\n\n*問題*：$\\text{nums}$ 互不相同，$a$ 與 $b$ 相連 $\\iff \\mathrm{lcm}(a,b) \\le T$。求連通塊數。\n\n**先砍掉不可能的**：$\\mathrm{lcm}(a,b) \\ge \\max(a,b)$，故**任何 $> T$ 的元素都是孤立點**，直接各算一塊。以下只看 $\\le T$ 的元素。\n\n**演算法**：對每個 $g = 1, 2, \\dots, T$：\n- 令 $S_g = \\{x \\in \\text{nums} : g \\mid x\\}$，若 $|S_g| \\le 1$ 則跳過；\n- 令 $m_g = \\min S_g$；\n- 對每個 $x \\in S_g$，若 $\\dfrac{x \\cdot m_g}{g} \\le T$，則 `unite(x, m_g)`。\n\n**命題**：這樣得到的連通性，恰為題目定義的連通性。\n\n*證明*。\n\n- **（不多連）** 若演算法 `unite(x, m_g)`，則 $g \\mid x$、$g \\mid m_g$，故 $\\gcd(x,m_g) \\ge g$，於是\n  $$\\mathrm{lcm}(x, m_g) = \\frac{x\\,m_g}{\\gcd(x,m_g)} \\le \\frac{x\\,m_g}{g} \\le T,$$\n  即 $(x, m_g)$ 真的是一條邊 ✔。\n- **（不漏連）** 設 $\\mathrm{lcm}(a,b) \\le T$，令 $g = \\gcd(a,b)$。則 $a, b \\in S_g$，且 $m_g \\le \\min(a,b)$。於是\n  $$\\frac{a\\cdot m_g}{g} \\le \\frac{a\\cdot b}{g} = \\mathrm{lcm}(a,b) \\le T,$$\n  故演算法會 `unite(a, m_g)`；同理 `unite(b, m_g)`。**所以 $a$ 與 $b$ 透過 $m_g$ 連通** ✔。$\\square$\n\n**這個證明的漂亮之處在於第二部分**：不需要 $a$ 與 $b$ 直接相連，只要它們都連到 $m_g$ 就夠了——**用一個「代表元」把 $S_g$ 內部的星狀連接壓成 $O(|S_g|)$ 條邊，而不是 $O(|S_g|^2)$ 條。**「取最小者當中心」是這裡的關鍵，因為 $m_g$ 最小 ⇒ 與它做 lcm 最容易滿足門檻。\n\n*複雜度*：外層 $g$ 從 1 到 $T$，內層掃 $g$ 的倍數 ⇒ $\\sum_{g\\le T} T/g = O(T\\log T)$，與定理 3 同一個調和級數 ✔。\n\n## 逐步演算法\n\n:::example 質因數 p 對應的並查集編號（偏移一段避免和數值撞號）\n**1. 狀態表示：數值與質因數共用同一個並查集。** 給質因數一套不和數值衝突的編號。\n\n```cpp\n// 質因數 p 對應的並查集編號（偏移一段避免和數值撞號）\nint primeId(int p) { return p + OFFSET; }  // OFFSET 大於所有可能的數值\n```\n:::\n\n\n\n:::example 分解 + 合併（同上）：數值和它的每個相異質因數相連\n**2. 逐數分解質因數並合併。** 每個質因數只需 merge 一次，不必把 12 的兩個 2 都做。\n\n```cpp\n// 分解 + 合併（同上）：數值和它的每個相異質因數相連\nfor (int v : nums) {\n    addNumber(v);\n}\n```\n:::\n\n\n\n:::example 統計每組有幾個原始數字，取最大\n**3. 答案讀取：統計每個老大底下有幾個「數值」。** 注意只數「真正的數字節點」，質因數節點不算。\n\n```cpp\n// 統計每組有幾個原始數字，取最大\nunordered_map<int, int> cnt;\nint best = 0;\nfor (int v : nums) {\n    best = max(best, ++cnt[find(v)]);\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 質因數編號千萬別和數值撞號，否則會把不相干的東西併在一起。\n- 重複的數值要小心：可能要去重或正確計數，看題目要的是「元素個數」還是「相異值個數」。\n- 複雜度：分解 `O(n sqrt(V))`，並查集操作近乎常數，總體 `O(n sqrt(V))`。\n\n## C++17 模板\n\n**模板一：952 按公因數計算最大組件大小（SPF 篩 + 質因數橋接）**\n\n```cpp\nclass Solution {\n    vector<int> fa;\n    int find(int x) {\n        while (fa[x] != x) {\n            fa[x] = fa[fa[x]];\n            x = fa[x];\n        }\n        return x;\n    }\n    void unite(int x, int y) {\n        x = find(x);\n        y = find(y);\n        if (x != y) fa[x] = y;\n    }\npublic:\n    int largestComponentSize(vector<int>& nums) {\n        int V = *max_element(nums.begin(), nums.end());\n        vector<int> spf(V + 1);\n        iota(spf.begin(), spf.end(), 0);\n        for (int i = 2; (long long)i * i <= V; ++i)\n            if (spf[i] == i)\n                for (int j = i * i; j <= V; j += i)\n                    if (spf[j] == j) spf[j] = i;\n\n        fa.resize(V + 1);\n        iota(fa.begin(), fa.end(), 0);\n        for (int x : nums) {\n            int t = x;\n            while (t > 1) {\n                int p = spf[t];\n                unite(x, p);              // 值 x 與質數 p 同組\n                while (t % p == 0) t /= p;\n            }\n        }\n        unordered_map<int, int> cntNum;   // 只統計真實數字，不含質數節點\n        int ans = 0;\n        for (int x : nums) ans = max(ans, ++cntNum[find(x)]);\n        return ans;\n    }\n};\n```\n\n**模板二：2709 最大公因數遍歷（值域大 ⇒ 試除 + 質數雜湊編號）**\n\n```cpp\nclass Solution {\n    vector<int> fa;\n    int find(int x) {\n        while (fa[x] != x) {\n            fa[x] = fa[fa[x]];\n            x = fa[x];\n        }\n        return x;\n    }\n    void unite(int x, int y) {\n        x = find(x);\n        y = find(y);\n        if (x != y) fa[x] = y;\n    }\npublic:\n    bool canTraverseAllPairs(vector<int>& nums) {\n        int n = nums.size();\n        if (n == 1) return true;\n        for (int x : nums)\n            if (x == 1) return false;     // 1 是孤立點（定理 1 前提 1）\n\n        fa.resize(n);\n        iota(fa.begin(), fa.end(), 0);\n        unordered_map<int, int> firstIdx; // 質數 p -> 第一個含有 p 的索引\n        for (int i = 0; i < n; ++i) {\n            int t = nums[i];\n            for (int p = 2; (long long)p * p <= t; ++p) {\n                if (t % p) continue;\n                auto [it, inserted] = firstIdx.try_emplace(p, i);\n                if (!inserted) unite(i, it->second);\n                while (t % p == 0) t /= p;\n            }\n            if (t > 1) {                  // 剩下的大質數，絕對不能漏\n                auto [it, inserted] = firstIdx.try_emplace(t, i);\n                if (!inserted) unite(i, it->second);\n            }\n        }\n        int r = find(0);\n        for (int i = 1; i < n; ++i)\n            if (find(i) != r) return false;\n        return true;\n    }\n};\n```\n\n（**這裡不另外配置質數節點，改用「第一個擁有該質數的索引」當代表**——效果等價，省掉一半節點。**兩種寫法都對，選一種寫熟即可。**）\n\n**模板三：1627 帶閾值的圖連通性（因數當橋樑 + 調和枚舉）**\n\n```cpp\nclass Solution {\n    vector<int> fa;\n    int find(int x) {\n        while (fa[x] != x) {\n            fa[x] = fa[fa[x]];\n            x = fa[x];\n        }\n        return x;\n    }\npublic:\n    vector<bool> areConnected(int n, int threshold, vector<vector<int>>& queries) {\n        fa.resize(n + 1);\n        iota(fa.begin(), fa.end(), 0);\n        for (int d = threshold + 1; d <= n; ++d) {\n            for (int m = 2 * d; m <= n; m += d) {\n                int a = find(d), b = find(m);\n                if (a != b) fa[a] = b;\n            }\n        }\n        vector<bool> ans;\n        ans.reserve(queries.size());\n        for (auto& q : queries) ans.push_back(find(q[0]) == find(q[1]));\n        return ans;\n    }\n};\n```\n\n（`threshold == 0` 時所有城市都連通，上面的迴圈從 $d=1$ 開始會自動處理——**不需要特判**，但值得在腦中驗證一次。）\n\n## 時間與空間複雜度\n\n設 $n$ 為數字個數、$V$ 為值域上界、$T$ 為閾值、$q$ 為查詢數。\n\n| 題目 | 時間 | 空間 |\n| --- | --- | --- |\n| 952 | $O(V\\log\\log V + n\\log V \\cdot \\alpha)$ | $O(V)$ |\n| 2709 | $O(n\\sqrt{V}\\,\\alpha)$ | $O(n + \\lvert P\\rvert)$ |\n| 1998 | $O(n\\log n + n\\sqrt{V}\\,\\alpha)$ | $O(n + V)$ |\n| 1627 | $O\\!\\left(n\\log\\frac{n}{T}\\cdot\\alpha + q\\,\\alpha\\right)$ | $O(n)$ |\n| 3378 | $O(T\\log T\\cdot\\alpha + n)$ | $O(T)$ |\n\n**分解方式的選擇（這是本節唯一的效率決策）：**\n\n| | 預處理 | 單次分解 | 何時用 |\n| --- | --- | --- | --- |\n| 試除 $O(\\sqrt x)$ | 無 | $\\sqrt{V}$ | $V$ 大（$10^9$）、$n$ 小 |\n| SPF 篩 | $O(V\\log\\log V)$ | $O(\\log x)$ | $V$ 小（$\\le 10^6$）、$n$ 大 |\n\n**乘出來比較**：$V = 10^5$、$n = 2\\times10^4$ ⇒ 試除 $2\\times10^4 \\times 316 = 6.3\\times10^6$；篩 $10^5 \\times 3 + 2\\times10^4\\times17 = 6.4\\times10^5$。**篩快 10 倍，但要多 $O(V)$ 空間。** $V = 10^9$ 時篩不可能，只能試除。\n\n**並查集本身從來不是瓶頸**——$O(n\\log V \\cdot \\alpha)$ 相對於分解成本可以忽略。**優化這類題要從「分解」與「枚舉方式」下手，不要去調並查集。**\n\n**調和級數的實際數值**（估 1627、3378 這類枚舉時直接代）：$\\sum_{d\\le n} n/d \\approx n\\ln n$。$n = 10^4$ ⇒ $9.2\\times10^4$；$n = 10^5$ ⇒ $1.2\\times10^6$；$n = 10^6$ ⇒ $1.4\\times10^7$。**全都很小，不用怕。**\n\n## 常見錯誤與邊界條件\n\n- **統計連通塊大小時把質數節點算進去**。`sz[root]` 包含橋樑節點，必須另外用 `cntNum[find(x)]` 只累加真實數字。**這是本節的頭號 bug，症狀是答案偏大。**\n- **試除分解漏掉最後的大質數**。`for (p = 2; p*p <= x; ++p)` 結束後若 `x > 1`，剩下的 `x` 本身是質數，**必須加進去**。漏掉會讓「兩個大質數相同」的數字無法連通。\n- **`p * p` 溢位**。$x \\le 10^9$ 時 `p` 可達 $31623$，`p*p` 約 $10^9$ 仍在 `int` 範圍；**但 $x \\le 10^{18}$ 時必須寫 `(long long)p * p`。** 養成一律加轉型的習慣。\n- **忘記處理 1**。1 沒有質因數，是孤立點。**2709 的 `[1]` 回傳 `true`、`[1,2]` 回傳 `false`；952 中的 1 自成一塊（大小 1）。**\n- **值重複時用「值」當節點卻要回答「索引」的問題**。1998 中 `nums` 可以有重複值，若用值當節點就分不清是哪一個索引。**解法：要嘛用索引當節點，要嘛（更簡單）用「值 → 根」的映射，因為相同的值必定同根。**\n- **並查集陣列開得不夠大**。用值當節點時要開 `max(nums) + 1`；混用索引與質數時要開 `n + 質數個數`。**用 `unordered_map` 動態分配質數編號時，別忘了預留空間或改用 `vector` 動態擴容。**\n- **1627 的 `threshold` 可能為 0**。迴圈從 `threshold + 1 = 1` 開始，$d=1$ 會把所有城市連起來——**這是正確的**（$\\gcd \\ge 1 > 0$），但要確認自己的迴圈邊界沒寫成 `d = max(2, threshold+1)`。\n- **3378 忘記把 $> T$ 的元素各算一塊**。它們是孤立點，不能漏計。\n- **`unordered_map` 在大量插入時退化**。質數個數可達 $O(n\\log V)$；若 TLE，改用 `reserve` 或把質數離散化後用 `vector`。\n- **以為「共用因數」要枚舉所有因數**。只需質因數（定理 1 的說明）。枚舉所有因數在 $\\gcd > 1$ 的題目中是白費工，**但在 $\\gcd > T$ 的題目中（1627）則是必要的**——兩者別搞混。\n\n## 與相似技巧的比較\n\n- **與 8.1／8.2**：本節不改變並查集本身，只是換了「節點是什麼」。**8.2 的建模是幾何／時間上的，本節的建模是數論上的。** 共同的抽象是「把 $O(n^2)$ 的完全圖用共享屬性壓成二分圖」。\n- **與 947 移除石頭、721 帳號合併**：**完全同一個手法**——947 把「行號」「列號」當橋樑節點，721 把「email」當橋樑節點。**如果你會 721，本節你已經會了一半；剩下的一半是數論（怎麼分解、怎麼枚舉倍數）。**\n- **與直接建圖 + BFS**：邊數 $O(n\\log V)$，BFS 也是 $O(n\\log V)$，**複雜度相同**。並查集的優勢是**不用顯式存邊**（省記憶體）、以及支援「邊加邊查」。**只問一次連通塊數的話，BFS 完全可行且更直觀。**\n- **與莫比烏斯反演／狄利克雷前綴和**：那些解決的是「有多少對數的 $\\gcd$ 恰為 $d$」這類**計數**問題，本節解決的是**連通性**問題。**兩者常出現在同一題的不同小問中，枚舉倍數的調和級數技巧是共通的。**\n- **與線性篩（歐拉篩）**：SPF 篩是 $O(V\\log\\log V)$，線性篩是 $O(V)$。**在 $V \\le 10^6$ 的規模下兩者實測差距不到 2 倍**，SPF 篩程式短、不易寫錯，**優先用 SPF。**\n- **與 8.4／8.5**：那兩節的 `fa` 表達的是「下一個可用位置」，與連通性無關。**本節的 `fa` 仍是正統的等價類**——不要把 8.4 的直覺帶進來。\n\n## 代表例題\n\n**題號**：952、1627、1998、2709、3378\n\n把等價或連通關係合併到代表元；練習在 union 時同步維護集合資訊。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 952 | [按公因數計算最大元件大小](https://leetcode.cn/problems/largest-component-size-by-common-factor) | 2272 |\n| 1627 | [帶閾值的圖連通性](https://leetcode.cn/problems/graph-connectivity-with-threshold) | 2221 |\n| 1998 | [陣列的最大公因數排序](https://leetcode.cn/problems/gcd-sort-of-an-array) | 2429 |\n| 2709 | [最大公約數遍歷](https://leetcode.cn/problems/greatest-common-divisor-traversal) | 2172 |\n| 3378 | [統計最小公倍數圖中的連通塊數目](https://leetcode.cn/problems/count-connected-components-in-lcm-graph) | 2532 |\n\n## 本節重點速查\n\n- **核心手法**：「兩兩有邊」的條件若能寫成「共享某個屬性」，就**把屬性當成節點**，$O(n^2)$ 完全圖 ⇒ $O(nk)$ 二分圖。質因數只是最常見的屬性；947 是行列，721 是 email。\n- **定理 1**：$\\gcd(a,b)>1 \\iff$ 共享質因數 $\\iff$ 在「數字—質數」二分圖中相連。**只用質因數就夠**（合數因數不增加連通性）。\n- **兩個前提**：**1 沒有質因數 ⇒ 孤立點**（2709 的 `[1,2]` 是 `false`）；**質數節點不能算進連通塊大小**（頭號 bug，要另開 `cntNum`）。\n- **值域小時「值 $v$」與「質數 $v$」可以共用同一個編號**——它們本來就該同組。\n- **定理 2**：$v\\le10^9$ 最多 9 個相異質因數（$2\\cdots23 = 2.2\\times10^8$，再乘 29 就超過 $10^9$）。所以邊數是 $O(n\\log V)$。\n- **$\\gcd > T$（1627）不能只用質數**——要枚舉所有 $d > T$ 當橋樑，因為 $\\gcd(x,y)>T \\iff \\exists d>T$ 同時整除兩者。複雜度是調和級數 $O(n\\log\\frac{n}{T})$。\n- **定理 4（1998）**：連通圖的邊對換生成完整對稱群 ⇒ **連通塊內可任意重排**。判斷方式：排序後每個位置的值是否仍與原值同塊。\n- **定理 6（3378）**：$\\mathrm{lcm}(a,b)\\le T$ 用 **$S_g$ 中的最小元素當中心**做星狀連接——因為 $\\frac{a\\,m_g}{g}\\le\\frac{ab}{g}=\\mathrm{lcm}(a,b)$，$m_g$ 最小最容易過門檻。$>T$ 的元素各自孤立。\n- **分解方式**：$V$ 大 $n$ 小 ⇒ 試除 $O(\\sqrt V)$（**別漏掉最後的大質數**）；$V$ 小 $n$ 大 ⇒ SPF 篩。\n- **並查集從來不是瓶頸**，優化要從分解與枚舉下手。\n- **「對每個 $d$ 掃它的倍數」是 $O(n\\log n)$，不是 $O(n^2)$。**\n",
        },
        {
          id: 57,
          title: "8.4 陣列上的並查集",
          summary:
            "## 這個技術解決什麼問題\n\n一條長度 $n$ 的資源帶（天數、座位、格子），反覆執行同一種操作：\n\n> **「從位置 $i$ 開始往右找第一個還沒被佔用的位置，佔用它。」**\n\n暴力是每次從 $i$ 一格一格往右掃，單次 $O(n)$，$q$ 次請求就是 $O(nq)$。$n, q$ 都到 $10^5$ 就過不了。\n\n**核心想法**：*讓 `fa[i]` 記錄「從 $i$ 出發，下一個值得嘗試的位置」。* 位置 $i$ 一旦被佔用，就令 `fa[i] = i + 1`——意思是「別再問我了，去問我右邊那個」。查詢就是沿 `fa` 一路跳到底，而**路徑壓縮讓下一次查詢直接跳過整段連續的已佔用區**。\n\n**這是並查集的一種「非等價類」用法，必須先講清楚，否則會用 8.1 的直覺去理解而卡住：**\n\n| | 8.1／8.2／8.3 | 本節 |\n| --- | --- | --- |\n| `fa` 的語意 | 同一等價類的代表元 | **下一個可用位置** |\n| 合併方向 | 由平衡策略決定（按大小） | **由語意固定：永遠指向右邊** |\n| `find(x)` 的意義 | $x$ 屬於哪一類 | **$\\ge x$ 的第一個空位** |\n| 能否問「$x$ 與 $y$ 同類嗎」 | 能 | **不能，也沒有意義** |\n\n**本節借用的只有「路徑壓縮讓重複跳躍變快」這個機制。** 認清這一點，剩下的都很簡單。\n\n**辨識訊號（三個要同時成立）**：\n1. 資源是**一維排列**的（天、位置、編號）；\n2. **一旦佔用就永久失效**（單調消耗，不會釋放）；\n3. 反覆查詢「某位置右邊（或左邊）第一個可用的」。\n\n**典型題**：1353 最多可以參加的會議數目、1488 避免洪水氾濫、2382 刪除操作後的最大子段和（配合 8.2 的倒序）、2334 元素值大於變化閾值的子陣列、3666 允許的最大變換次數。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「陣列上的並查集」想：\n\n- **反覆要找「某位置右邊（或左邊）第一個還沒被佔用的格子」，並佔用它。** 例如 1353 安排會議、1488 安排抽水日。\n- **暴力是每次往旁邊一格格掃找空位（`O(n)` 每次）**，請求很多時受不了——並查集讓查詢跳過整段已滿區。\n- **資源是一維、一旦用掉就不再可用（單調消耗）。** 「用過就不還」這種單向性，正好對應 `parent[i] = i + 1` 的指向右鄰。\n\n:::example 嗅到味道：看到「找右邊第一個空位 + 用掉它」，就想 parent[i]=i+1\n```cpp\n// 嗅到味道：看到「找右邊第一個空位 + 用掉它」，就想 parent[i]=i+1\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的 `parent[i]` 代表的是「從 i 往右第一個可用位置」嗎？\n2. 佔用一個位置後，我有沒有把它指向右鄰（`parent[p] = p + 1`）？\n3. 我有沒有檢查 `find` 回傳值還在合法範圍內、超界就代表失敗？\n\n## 核心想法與直覺\n\n本節是並查集的一個經典套路：**在一維陣列上，用並查集快速找到「右邊第一個還沒被用過的位置」**。常見於「安排時間／座位／資源，每次要找下一個可用的格子」這類題目，例如 1353「最多可以參加的會議數目」、1488「避免洪水氾濫」。\n\n直覺做法是：要找位置 `i` 右邊第一個空格，就從 `i` 往右一格一格掃。最壞情況每次都掃很遠，整體 `O(n²)`。\n\n:::example parent[i] = 「從 i 起往右第一個可用位置」；初始每格都可用\n並查集的巧思是讓 `parent[i]` 代表「從 `i` 開始，往右第一個還可用的位置」。一開始每個位置都可用，所以 `parent[i] = i`。**一旦位置 `i` 被佔用，就把它「指向 `i + 1`」**，意思是「我滿了，去找我右邊的」。下次再 `find(i)` 就會自動跳過所有已滿的格子，直接落在第一個空位上。\n\n```cpp\n// parent[i] = 「從 i 起往右第一個可用位置」；初始每格都可用\nvector<int> parent(n + 1);\nfor (int i = 0; i <= n; ++i) {\n    parent[i] = i;\n}\n```\n:::\n\n\n\n:::example find：跳過所有已滿格子，回傳右邊第一個可用位置\n```cpp\n// find：跳過所有已滿格子，回傳右邊第一個可用位置\nint find(int x) { return parent[x] == x ? x : parent[x] = find(parent[x]); }\n```\n:::\n\n\n\n:::example 拿到一個可用位置並佔用它\n用法是：`p = find(i)` 拿到可用位置，用掉它之後立刻 `parent[p] = p + 1`（指向右鄰），表示這格滿了。路徑壓縮會把連續的一段滿格直接接到最右端，所以均攤幾乎是常數。\n\n```cpp\n// 拿到一個可用位置並佔用它\nint p = find(i);      // i 右邊第一個空位\nif (p <= maxPos) {    // 還在合法範圍內\n    use(p);             // 安排在這個位置\n    parent[p] = p + 1;  // 佔用後指向右鄰，下次自動跳過\n}\n```\n:::\n\n## 狀態／資料結構定義\n\n**寫法 A：指向右鄰（最常用）**\n\n```cpp\nstruct NextFree {\n    vector<int> fa;                       // fa[i] = 從 i 出發下一個值得試的位置\n    explicit NextFree(int n) : fa(n + 1) { // 注意是 n + 1，多一個哨兵\n        iota(fa.begin(), fa.end(), 0);\n    }\n    int find(int x) {                     // 回傳 >= x 的第一個空位；n 表示沒有\n        while (fa[x] != x) {\n            fa[x] = fa[fa[x]];\n            x = fa[x];\n        }\n        return x;\n    }\n    void occupy(int x) { fa[x] = x + 1; } // x 被用掉，指向右鄰\n};\n```\n\n**哨兵 `fa[n] = n` 是必要的**：當 $\\ge x$ 的位置全被佔滿時，`find` 回傳 $n$，呼叫端據此判斷「沒有可用位置」。**陣列開成 `n` 而不是 `n+1` 會越界，而且是那種「大部分測資都對、極端測資才崩」的越界。**\n\n**注意 `occupy(x)` 必須傳入 `find` 的結果，不能傳任意位置。** 寫 `occupy(find(i))` 而不是 `occupy(i)`——否則會把一個本來就非空的位置的指標覆蓋掉（見「常見錯誤」）。\n\n**寫法 B：在根上記錄區間右端（可以恢復按大小合併）**\n\n寫法 A 的合併方向被語意固定（永遠指向右），**因此不能按大小合併**，攤還界比正統並查集差（見「時間與空間複雜度」）。想要 $O(\\alpha)$ 保證，可以改成：\n\n```cpp\nstruct NextFree2 {\n    vector<int> fa, sz, nxt;              // nxt[root] = 該連通塊的右端 + 1\n    // find/unite 用正統的按大小合併\n    int nextFree(int x) { return nxt[find(x)]; }   // 語意不變，實作換成聚合量\n};\n```\n**佔用 $x$ 時把 $x$ 與 $x+1$ `unite`，並在新根上維護 `nxt = max(兩邊的 nxt)`。** 這就回到 8.2 「在根上掛聚合量」的正統做法。**規模不大時寫法 A 就夠；要保證最壞複雜度就用寫法 B。**\n\n**雙向（同時要「左邊第一個空位」）**：**開兩個獨立的陣列**，`L` 指向左鄰、`R` 指向右鄰，佔用時**兩個都要更新**。不要試圖用一個陣列表達雙向——那不是並查集能做的事。\n\n**「合併相鄰區段」的變體（1562／2382）**：這時 `fa` 回到正統語意（連通塊 = 一段連續的活躍區間），只是**合併對象固定為左右鄰居**。此時可以正常用按大小合併，並在根上掛「區段和」「區段長度」等聚合量。**這個變體與寫法 A 不是同一件事，別混。**\n\n## 不變量或正確性證明\n\n### 定理 1（主不變量：`find(x)` 恰為 $\\ge x$ 的第一個空位）\n\n令 $U \\subseteq \\{0,\\dots,n-1\\}$ 為已佔用位置的集合，$\\mathrm{free}(x) = \\min\\{\\,j \\ge x : j \\notin U\\,\\}$（約定 $n \\notin U$，故最小值必存在且 $\\le n$）。\n\n**不變量 (N)**：任何時刻，對所有 $x \\in [0, n]$，\n$$\\texttt{fa}[x] = \\begin{cases} x, & x \\notin U,\\\\ \\text{某個} > x \\text{的值}, & x \\in U,\\end{cases}$$\n且**沿 `fa` 從 $x$ 出發必定終止**。\n\n**命題**：在 (N) 之下，`find(x)` $= \\mathrm{free}(x)$。\n\n*證明*（對 $n - x$ 歸納，即從右往左）。\n- $x = n$：`fa[n] = n`（哨兵，$n \\notin U$），`find(n) = n = free(n)` ✔。\n- $x < n$ 且 $x \\notin U$：由 (N)，`fa[x] = x`，`find(x) = x`；而 $\\mathrm{free}(x) = x$ ✔。\n- $x < n$ 且 $x \\in U$：由 (N)，`fa[x] = y > x`。歸納假設給出 `find(y) = free(y)`。又因為 $x \\in U$，$\\mathrm{free}(x) = \\mathrm{free}(x+1)$。**還需要 $\\mathrm{free}(y) = \\mathrm{free}(x+1)$**——這由下面的維護論證保證：`fa[x]` 只會被設成 $x+1$，或（路徑壓縮時）設成某個 `find` 的結果，而後者依歸納假設是一個**空位**，且是 $\\ge x+1$ 的第一個空位。兩種情形都滿足 $\\mathrm{free}(y) = \\mathrm{free}(x+1)$ ✔。$\\square$\n\n**(N) 的維護**：\n- **`occupy(x)`（$x$ 原本是空位）**：令 `fa[x] = x + 1`。$x$ 加入 $U$，(N) 的第二行對 $x$ 成立；其他位置的 `fa` 不變，而它們的 $\\mathrm{free}$ 值可能改變——**但由上面的歸納論證，`find` 仍會走到正確的答案**（因為指標鏈只是「往右跳」，終點由沿途每一格的即時狀態決定）✔。\n- **路徑壓縮 `fa[x] = fa[fa[x]]`**：新值仍 $> x$，且它在鏈上，故 `find` 的終點不變 ✔。\n\n**這個證明裡最值得注意的一句是「終點由沿途每一格的即時狀態決定」。** 也就是說：**`fa[x]` 不需要（也不可能）永遠等於 $\\mathrm{free}(x)$，它只需要是一個「不會跳過任何空位」的中繼站。** 這正是為什麼 `occupy` 只寫一格就夠，不必更新所有指向 $x$ 的位置。\n\n**推論（絕不會跳過空位）**：`fa[x] > x` 只發生在 $x \\in U$ 時，故鏈上經過的每一格都是已佔用的。**「跳過的一定是已佔用的」是這個結構的安全性核心。**\n\n### 定理 2（1488 避免洪水：取「最早的可用抽水日」是安全的）\n\n*問題*：第 $i$ 天若 `rains[i] > 0` 則該湖蓄滿水；若 `rains[i] == 0` 則可以選一個湖抽乾。同一個湖在未抽乾前再次下雨就氾濫。求一組可行的抽水安排。\n\n**演算法**：掃描每一天。遇到湖 $L$ 下雨且 $L$ 上次下雨是第 $t$ 天 ⇒ 必須在 $(t, i)$ 之間找一個晴天抽 $L$。**用本節的結構取 $\\ge t+1$ 的第一個未使用晴天**；找不到（或 $\\ge i$）則無解。\n\n**命題**：取最早的可用晴天是安全的——若存在任何可行解，這樣做也能得到可行解。\n\n*證明（交換論證）*。設存在可行解 $O$，且 $O$ 與貪心在前 $k-1$ 次「必須抽水」的決策上一致，在第 $k$ 次分歧：貪心選晴天 $d$，$O$ 選晴天 $d'$，且由貪心的「取最早」有 $d < d'$（$d$ 在當時可用，而 $O$ 前面的決策與貪心相同，故 $d$ 在 $O$ 中也未被前 $k-1$ 次使用）。\n\n$O$ 中的 $d$ 有兩種情形：\n- **$d$ 未被 $O$ 使用**：把 $O$ 的第 $k$ 次決策從 $d'$ 改成 $d$。合法性：$d$ 落在 $(t, i)$ 內（貪心找到它時已檢查），故抽 $L$ 有效 ✔。\n- **$d$ 被 $O$ 用於抽另一個湖 $L'$**：則 $L'$ 的約束區間 $(t', i')$ 包含 $d$。**交換兩者**：$L$ 改在 $d$ 抽、$L'$ 改在 $d'$ 抽。$L$ 合法同上；$L'$ 合法需要 $d' \\in (t', i')$——由 $d' > d > t'$ 得下界；上界需要 $d' < i'$。**這裡用到「第 $k$ 次分歧的約束右端 $i$ 是所有尚未處理的必須抽水事件中最早的」**：因為我們按天掃描，$i' \\ge i > d'$ ✔。\n\n兩種情形都得到一個與貪心一致更久的可行解，矛盾於 $k$ 的最大性 ✔。$\\square$\n\n**這個證明點名的前提是「按天掃描，所以後面的截止日不會更早」。** 若題目允許亂序給出約束，交換論證的第二種情形就會失效。**看到「取最早可用資源」的貪心，永遠要問一句：後面的需求會不會有更緊的截止日？**\n\n### 定理 3（1353 最多可以參加的會議數目：按結束日排序 + 取最早空位是最優的）\n\n*演算法*：把會議按 `end` 遞增排序；對每個會議 $j$，取 $d = \\texttt{find}(s_j)$，若 $d \\le e_j$ 則參加並 `occupy(d)`，否則放棄。\n\n**命題**：此貪心參加的會議數是最大值。\n\n*證明（交換論證）*。把問題看成二分圖匹配：會議 $j$ 可以匹配天 $d \\in [s_j, e_j]$，求最大匹配。設 $M^{*}$ 是一個最大匹配，且在所有最大匹配中，**與貪心在處理順序的前綴上一致最久**。設第一個分歧點是會議 $j$。\n\n- **情形 1：貪心匹配 $j \\mapsto d$，$M^{*}$ 不匹配 $j$。**\n  由「前綴一致」，前面的會議在兩者中佔用同一組天，故 $d$ 在 $M^{*}$ 中未被前面的會議佔用。\n  - 若 $d$ 在 $M^{*}$ 中完全空閒 ⇒ $M^{*} \\cup \\{(j,d)\\}$ 更大，與最大性矛盾。\n  - 若 $d$ 被某個**後面的**會議 $j'$ 佔用 ⇒ 把 $M^{*}$ 改成「$j \\mapsto d$、$j'$ 不參加」。大小不變，仍是最大匹配，但前綴一致更久 ⇒ 矛盾 ✔。\n- **情形 2：貪心不匹配 $j$，$M^{*}$ 匹配 $j \\mapsto d$。**\n  貪心放棄 $j$ 表示 $[s_j, e_j]$ 中**每一天都已被前面的會議佔用**；由前綴一致，$M^{*}$ 中這些天也被同樣的會議佔用。那 $M^{*}$ 無法再把 $d \\in [s_j,e_j]$ 分給 $j$ ⇒ **此情形不可能發生** ✔。\n- **情形 3：兩者都匹配 $j$，但天不同：貪心 $j \\mapsto d$、$M^{*} : j \\mapsto d'$。** 由貪心取最早可用天，$d < d'$。\n  - 若 $d$ 在 $M^{*}$ 中空閒 ⇒ 直接改成 $j\\mapsto d$ ✔。\n  - 若 $d$ 被後面的會議 $j'$ 佔用 ⇒ **交換**：$j \\mapsto d$、$j' \\mapsto d'$。合法性檢查：$s_{j'} \\le d < d'$ 給出下界；**$d' \\le e_j \\le e_{j'}$ 給出上界——這裡用到「按 `end` 遞增排序」，所以後處理的會議截止日不會更早** ✔。\n  兩種情形都讓前綴一致更久 ⇒ 矛盾 ✔。$\\square$\n\n**三個情形窮盡了所有可能，且每一個都導出矛盾，故貪心即最優。** 注意情形 3 的合法性驗證是整個證明中唯一用到排序條件的地方——**排序不是為了方便，是正確性的前提。** 若改成按 `start` 排序，情形 3 就會失效，而且真的會出錯（例如 `[[1,2],[1,1]]` 按 start 排序可能先把第 1 天分給 `[1,2]`，導致 `[1,1]` 無日可用）。\n\n### 定理 4（2334：按值遞減加入，插入時檢查即可）\n\n*問題*：找一個子陣列，使得**其中每個元素都大於 $\\texttt{threshold}/k$**（$k$ 為子陣列長度）。回傳任一個滿足的 $k$，不存在回傳 $-1$。\n\n*等價條件*：子陣列的**最小值** $v$ 與長度 $k$ 滿足 $v \\cdot k > \\texttt{threshold}$。\n\n**演算法**：把所有位置按值**遞減**排序，依序「啟用」。啟用位置 $p$ 時，與左右已啟用的鄰居合併，得到所在區段長度 $k$；若 $\\text{nums}[p] \\cdot k > \\texttt{threshold}$ 則回傳 $k$。\n\n**命題**：存在合法子陣列 $\\iff$ 演算法在某次插入時回傳。\n\n*證明*。\n- **($\\Leftarrow$ 回傳的必合法)**：插入 $p$ 時，同一區段中的所有元素都**已被啟用**，即值都 $\\ge \\text{nums}[p]$。故該區段的最小值 $\\ge \\text{nums}[p]$，而 $\\text{nums}[p]\\cdot k > \\texttt{threshold}$ ⇒ 每個元素都 $> \\texttt{threshold}/k$ ✔。\n- **($\\Rightarrow$ 合法的必被抓到)**：設合法子陣列為 $[l, r]$，長度 $k_0$，最小值 $v_0$ 出現在位置 $p_0$，即 $v_0 k_0 > \\texttt{threshold}$。當演算法啟用 $p_0$ 時，$[l,r]$ 中其餘位置的值都 $\\ge v_0$，故**都已啟用**（遞減順序）。因此 $p_0$ 所在區段包含整個 $[l,r]$，長度 $k \\ge k_0$，於是 $\\text{nums}[p_0]\\cdot k = v_0 k \\ge v_0 k_0 > \\texttt{threshold}$，演算法回傳 ✔。$\\square$\n\n**「按值排序後逐一啟用，使得任何時刻已啟用集合恰為 $\\{\\text{值} \\ge v\\}$」是一個極常用的套路**（8.2 定理 4 的 778 是它的無向圖版本）。**本節的貢獻只是把「合併相鄰」這件事做成 $O(\\alpha)$。**\n\n### 定理 5（2382：倒序加回 + 正值保證答案單調）\n\n*問題*：依 `removeQueries` 的順序刪除元素，每次刪除後回報「最大的連續非刪除子段和」。\n\n**演算法**：倒序（8.2 定理 1）——從全刪光開始，逐一加回，加回時與左右已存在的段合併，在根上維護段和，維護一個全域 `best`，答案倒著填。\n\n**命題**：`best` 在加回過程中**單調不減**，所以「維護一個全域最大值」是對的。\n\n*證明*：設加回位置 $p$（值 $x$），其左段和為 $A$、右段和為 $B$（不存在則為 0）。新段和為 $A + x + B$。**題目保證 $\\text{nums}[i] \\ge 1$**，故 $A + x + B > \\max(A, B)$——**被消滅的兩個段（$A$ 與 $B$）都被一個更大的段取代**，其他段不受影響。故所有段和的最大值不減 ✔。$\\square$\n\n**這個「單向失效」的論證與 6.6（懶刪除堆）、5.1（1429 第一個唯一數字）是同一個形狀：只要能證明「被移除的候選一定被更好的候選取代」，就可以只維護一個全域最大值，不需要真的維護整個集合。**\n\n**前提是「所有元素非負」，必須點名。** 若 `nums` 可以有負數，$A + x + B$ 可能小於 $\\max(A,B)$，此時全域最大值可能下降，就必須用 `multiset` 維護所有段和（或線段樹）。\n\n## 逐步演算法\n\n:::example 多開一格當界線：find 回傳到 n 就代表沒有可用位置了\n**1. 狀態表示：`parent[i]` = 從 `i` 起往右第一個可用位置。** 通常多開一格 `n + 1` 當作「越界哨兵」。\n\n```cpp\n// 多開一格當界線：find 回傳到 n 就代表沒有可用位置了\nvector<int> parent(n + 2);\nfor (int i = 0; i < (int)parent.size(); ++i) {\n    parent[i] = i;\n}\n```\n:::\n\n\n\n:::example 會議按結束日排序後，貪心地找最早可用的一天\n**2. 確定每個請求的「起點」與「上界」。** 例如會議可在 `[start, end]` 任一天參加，就從 `find(start)` 找，找到的位置不能超過 `end`。\n\n```cpp\n// 會議按結束日排序後，貪心地找最早可用的一天\nsort(events.begin(), events.end(), byEndDay);\n```\n:::\n\n\n\n:::example 對每個請求找位置、佔用\n**3. 取用 + 佔用：`find` 後把該格指向右鄰。** 這是把「用掉」轉成並查集合併的關鍵動作。\n\n```cpp\n// 對每個請求找位置、佔用\nfor (auto& e : events) {\n    int day = find(e.start);\n    if (day <= e.end) {\n        ++ans;\n        parent[day] = day + 1;\n    }  // 佔用\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 一定要檢查 `find` 回傳值是否仍在合法上界內（如 `<= end`），超過就代表沒空位、這個請求失敗。\n- 哨兵要留夠：`parent` 開到 `n + 1` 以上，避免 `p + 1` 越界。\n- 複雜度：n 個請求各一次 `find`，均攤近乎 `O(n α(n))`，遠勝逐格掃描的 `O(n²)`。\n\n## C++17 模板\n\n**模板一：1353 最多可以參加的會議數目**\n\n```cpp\nclass Solution {\n    vector<int> fa;\n    int find(int x) {\n        while (fa[x] != x) {\n            fa[x] = fa[fa[x]];\n            x = fa[x];\n        }\n        return x;\n    }\npublic:\n    int maxEvents(vector<vector<int>>& events) {\n        int maxDay = 0;\n        for (auto& e : events) maxDay = max(maxDay, e[1]);\n        fa.resize(maxDay + 2);                 // 天數 1..maxDay，哨兵 maxDay+1\n        iota(fa.begin(), fa.end(), 0);\n\n        sort(events.begin(), events.end(),\n             [](const vector<int>& a, const vector<int>& b) { return a[1] < b[1]; });\n\n        int ans = 0;\n        for (auto& e : events) {\n            int d = find(e[0]);                // >= start 的第一個空閒日\n            if (d <= e[1]) {\n                ++ans;\n                fa[d] = d + 1;                 // 佔用 d\n            }\n        }\n        return ans;\n    }\n};\n```\n\n**模板二：1488 避免洪水氾濫（`set` 版與並查集版對照）**\n\n```cpp\nclass Solution {\n    vector<int> fa;\n    int find(int x) {\n        while (fa[x] != x) {\n            fa[x] = fa[fa[x]];\n            x = fa[x];\n        }\n        return x;\n    }\npublic:\n    vector<int> avoidFlood(vector<int>& rains) {\n        int n = rains.size();\n        fa.resize(n + 1);\n        iota(fa.begin(), fa.end(), 0);         // fa[i] = >= i 的第一個晴天\n        // 先把所有雨天標成「不可用」\n        for (int i = 0; i < n; ++i)\n            if (rains[i] > 0) fa[i] = i + 1;\n\n        unordered_map<int, int> last;          // 湖 -> 上次下雨的日子\n        vector<int> ans(n, 1);\n        for (int i = 0; i < n; ++i) {\n            if (rains[i] == 0) continue;\n            ans[i] = -1;\n            int lake = rains[i];\n            auto it = last.find(lake);\n            if (it != last.end()) {\n                int d = find(it->second + 1);  // 最早的可用晴天（定理 2）\n                if (d >= i) return {};         // 無解\n                ans[d] = lake;\n                fa[d] = d + 1;                 // 該晴天用掉了\n            }\n            last[lake] = i;\n        }\n        return ans;\n    }\n};\n```\n\n（`set<int> sunny` + `lower_bound` 是等價的解法，**程式更短、複雜度 $O(n\\log n)$ 有保證、而且支援「把晴天放回去」**。本節的並查集版本勝在常數與「不需要刪除」的場景。）\n\n**模板三：2382 刪除操作後的最大子段和（倒序 + 合併相鄰）**\n\n```cpp\nclass Solution {\n    vector<int> fa;\n    vector<long long> sum;\n    int find(int x) {\n        while (fa[x] != x) {\n            fa[x] = fa[fa[x]];\n            x = fa[x];\n        }\n        return x;\n    }\npublic:\n    vector<long long> maximumSegmentSum(vector<int>& nums,\n                                        vector<int>& removeQueries) {\n        int n = nums.size();\n        fa.resize(n);\n        iota(fa.begin(), fa.end(), 0);\n        sum.assign(n, 0);\n        vector<char> alive(n, false);\n\n        vector<long long> ans(n, 0);\n        long long best = 0;\n        for (int q = n - 1; q >= 1; --q) {     // 倒序，ans[n-1] = 0\n            int p = removeQueries[q];\n            alive[p] = true;\n            sum[p] = nums[p];\n            if (p > 0 && alive[p-1]) {         // 與左段合併\n                int r = find(p - 1);\n                sum[p] += sum[r];\n                fa[r] = p;\n            }\n            if (p + 1 < n && alive[p+1]) {     // 與右段合併\n                int r = find(p + 1);\n                sum[p] += sum[r];\n                fa[r] = p;\n            }\n            best = max(best, sum[p]);          // 定理 5：單調不減\n            ans[q - 1] = best;\n        }\n        return ans;\n    }\n};\n```\n\n（**合併時一律把根設成 $p$**，這樣 `sum[p]` 就是新段的和；犧牲了按大小合併，換來程式簡潔。$n \\le 10^5$ 完全夠用。）\n\n## 時間與空間複雜度\n\n| 操作 | 時間 |\n| --- | --- |\n| `find` | 攤還近乎常數（見下方討論） |\n| `occupy` | $O(1)$ |\n| 初始化 | $O(n)$ |\n\n**這裡必須誠實：本節的寫法 A 放棄了按大小／按秩合併**（合併方向被語意固定為「指向右」），所以不能直接引用 8.1 的 $O(\\alpha(n))$ 保證。\n\n- **只用路徑壓縮的一般上界**是 $O\\!\\big(m\\log_{1+m/n} n\\big)$；$m = \\Theta(n)$ 時即 $O(n\\log n)$。\n- **但本節的合併模式非常特殊（永遠是「$i$ 與 $i+1$ 合併」，即區間並查集）**，這個特例有更緊的分析（Gabow–Tarjan 的靜態樹並查集可做到 $O(n)$）。**完整證明超出本節範圍**，這裡只點明：**寫法 A 的實測表現接近線性，但它的最壞界不是 $O(\\alpha)$；需要嚴格保證時用寫法 B。**\n\n各題：\n\n| 題目 | 時間 | 空間 | 瓶頸 |\n| --- | --- | --- | --- |\n| 1353 | $O(m\\log m + D)$ | $O(D)$，$D$ = 最大天數 | **排序** |\n| 1488 | $O(n)$ 近乎 | $O(n)$ | — |\n| 2382 | $O(n)$ 近乎 | $O(n)$ | — |\n| 2334 | $O(n\\log n)$ | $O(n)$ | **按值排序** |\n\n**又一次：瓶頸幾乎都在排序，不在並查集。** 這與 8.2、8.3 的結論一致——**並查集是這些題裡最便宜的部分。**\n\n**與 `std::set` 解法的比較（這是本節最實際的決策）：**\n\n| | 並查集（本節） | `set<int>` + `lower_bound` |\n| --- | --- | --- |\n| 單次查詢 | 攤還近乎 $O(1)$ | $O(\\log n)$ 保證 |\n| 常數 | 小（陣列，cache 友善） | 大（紅黑樹，指標跳躍） |\n| **支援「放回去」** | **不支援** | **支援（`insert`）** |\n| 程式長度 | 中 | 短 |\n\n**實測上並查集快 2～5 倍，但 `set` 版更短更不容易錯，而且能處理「資源會被釋放」的變形。** 面試中先寫 `set` 版說清楚思路，再提「可以用並查集把 $\\log$ 去掉」是很好的回答。\n\n## 常見錯誤與邊界條件\n\n- **陣列開成 `n` 而不是 `n + 1`（缺哨兵）**。所有位置佔滿時 `find` 走到 `fa[n]` 越界。**這是本節最經典的越界，而且往往只在極端測資才觸發。**\n- **`occupy(i)` 傳入未經 `find` 的位置**。正確寫法是 `int d = find(i); ...; fa[d] = d + 1;`。若直接寫 `fa[i] = i + 1`，當 $i$ 本來就已被佔用時，會**覆蓋掉一條已壓縮的捷徑**——結果不是錯誤答案（不變量 (N) 仍成立，因為 $i+1$ 仍不跳過空位），而是**效能退化**。但若 $i$ 是空位而你並未真的使用它，那就是**真的錯了**（憑空吃掉一個空位）。\n- **忘記檢查 `find` 的回傳是否為哨兵**。1353 要檢查 `d <= end`，1488 要檢查 `d < i`。**漏檢會用到一個不存在的位置。**\n- **把本節的結構拿去問「$x$ 與 $y$ 連通嗎」**。`find(x) == find(y)` 在這裡的意思是「它們右邊的第一個空位相同」，**與連通性無關**。\n- **雙向需求只開一個陣列**。要同時找左邊與右邊的空位，必須開兩個獨立陣列並在佔用時**都更新**。\n- **1488 忘記把雨天標成不可用**。雨天不能抽水，初始化時就要 `fa[i] = i + 1`。\n- **1488 的 `ans` 初值**。晴天若沒被指派抽水，答案可以是任意合法湖編號（題目要求 $\\ge 1$），**初值填 1 而不是 0 或 -1**。\n- **2382 的和要用 `long long`**。$n \\le 10^5$、$\\text{nums}[i] \\le 10^9$ ⇒ 總和可達 $10^{14}$。\n- **2382 的答案錯位**。刪除第 $q$ 次之後的答案，對應「加回到剩 $q$ 個被刪」的狀態。**寫的時候用具體小例子（$n=2$）驗證一次索引。**\n- **2334 相同值的處理**。按值遞減排序時，相同值的位置可以任意順序啟用——**由定理 4 的證明，只要「所有 $\\ge v$ 的都已啟用」，順序無關**，但要確認排序是穩定或不影響結果。\n- **遞迴版 `find` 爆堆疊**。初始鏈長可達 $n$，$n = 10^5$ 的遞迴在某些環境會 stack overflow。**用迭代版。**\n\n## 與相似技巧的比較\n\n- **與 8.1／8.2／8.3**：**語意完全不同**。那三節的 `fa` 是等價類代表元，本節的 `fa` 是「下一個候選」。**唯一共用的是路徑壓縮這個機制。** 用等價類的直覺理解本節會處處卡住。\n- **與 `std::set` + `lower_bound`**：功能上 `set` 更強（支援放回、複雜度有保證），並查集更快更省。**判準：資源會不會被釋放。** 會 ⇒ 只能用 `set`；不會 ⇒ 兩者皆可，看你要短還是要快。\n- **與線段樹上二分**：維護一個 0/1 陣列，查詢「$\\ge x$ 的第一個 1」。$O(\\log n)$ 保證，**而且支援區間修改與釋放**，還能回答區間計數。**功能最強、程式最長。** 當題目同時需要「找空位」與「數某段有幾個空位」時，線段樹是唯一選擇（9.3）。\n- **與 8.5 區間並查集**：8.5 處理的是「一次填滿一整段」，本節是「一次佔用一個點」。**兩者的不變量幾乎相同**——8.5 只是把 `occupy(d)` 換成「把整段的指標都指向段尾之後」，並靠路徑壓縮讓重疊部分只被掃一次。**看完本節再看 8.5，會發現它是同一個結構的區間版。**\n- **與「合併相鄰區段」的變體（1562／2382）**：如「狀態／資料結構定義」所述，**那個變體的 `fa` 回到正統的等價類語意**（連通塊 = 一段連續的活躍區間），可以正常按大小合併、在根上掛聚合量。**它更像 8.2 而不像本節的寫法 A**，只是常常被歸在一起講。\n- **與貪心／匹配理論**：本節的題目（1353、1488）的**難點在證明貪心正確，不在資料結構**。定理 2 與定理 3 都是交換論證，而且都在同一個地方用到「按截止日排序」這個前提。**先把貪心證對，再挑資料結構。**\n\n## 代表例題\n\n**題號**：1353、1488、1562、2334、2382\n\n把等價或連通關係合併到代表元；練習在 union 時同步維護集合資訊。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1353 | [最多可以參加的會議數目](https://leetcode.cn/problems/maximum-number-of-events-that-can-be-attended) | 2016 |\n| 1488 | [避免洪水氾濫](https://leetcode.cn/problems/avoid-flood-in-the-city) | 1974 |\n| 1562 | [查詢大小為 M 的最新分組](https://leetcode.cn/problems/find-latest-group-of-size-m) | 1928 |\n| 2334 | [元素值大於變化閾值的子陣列](https://leetcode.cn/problems/subarray-with-elements-greater-than-varying-threshold) | 2381 |\n| 2382 | [刪除操作後的最大子段和](https://leetcode.cn/problems/maximum-segment-sum-after-removals) | 2136 |\n| 2612 | [最少翻轉運算元](https://leetcode.cn/problems/minimum-reverse-operations) | 2824 |\n| 3666 | [使二進位制字串全為 1 的最少操作次數](https://leetcode.cn/problems/minimum-operations-to-equalize-binary-string) | 2477 |\n\n## 本節重點速查\n\n- **語意先講清楚**：本節的 `fa[i]` 是「**從 $i$ 出發下一個值得試的位置**」，不是等價類代表元。**不能拿來問連通性。** 借的只有路徑壓縮。\n- **三個適用條件**：一維資源、**用了就永久失效**、反覆查「右邊第一個可用的」。\n- **主不變量（定理 1）**：`find(x)` $=\\ge x$ 的第一個空位。安全性核心是「**跳過的一定是已佔用的**」。`fa[x]` 不必等於答案，只要是「不跳過空位的中繼站」。\n- **哨兵 `fa[n] = n` 不能省**，陣列開 `n + 1`。回傳 $n$ 表示沒有可用位置，**呼叫端一定要檢查**。\n- **`occupy` 要傳 `find` 的結果**，不是原始位置。\n- **寫法 A 放棄了按大小合併**（方向被語意固定），最壞界不是 $O(\\alpha)$；要保證就用**寫法 B**（正統並查集 + 根上記 `nxt`）。\n- **1488／1353 的難點是證貪心，不是資料結構。** 兩個交換論證都用到「按截止日／結束日排序，後面的截止日不會更早」——**排序是正確性前提，不是方便。**\n- **2334**：按值**遞減**啟用 ⇒ 已啟用集合恆為 $\\{\\ge v\\}$ ⇒ 插入時檢查 $v\\cdot k > \\texttt{threshold}$ 即可，不重不漏。\n- **2382**：倒序加回 + **所有元素非負**（前提！）⇒ 段和最大值單調不減 ⇒ 只需維護一個全域 `best`。有負數就要 `multiset`。\n- **雙向需求開兩個陣列**，佔用時兩個都更新。\n- **`set` + `lower_bound` 是等價替代**：慢 2～5 倍但更短、複雜度有保證、**而且支援資源釋放**。同時要「找空位」又要「區間計數」⇒ 線段樹。\n- **瓶頸幾乎都在排序**，不在並查集。\n",
        },
        {
          id: 58,
          title: "8.5 區間並查集",
          summary:
            "## 這個技術解決什麼問題\n\n8.4 處理的是「一次佔用**一個點**」。本節把它推廣到：\n\n> **「把整段 $[l, r]$ 都標記掉，但每個位置一生只需要被真正處理一次。」**\n\n暴力是每次操作跑一遍 $[l, r]$，$q$ 次操作最壞 $O(nq)$。可是**如果每個位置只需要被處理一次，那所有操作加起來的「真正工作量」上限就是 $n$**——問題只剩下「如何跳過已處理的位置」。\n\n**這正是 8.4 的結構要做的事**：`fa[i]` 指向「從 $i$ 出發下一個未處理的位置」，於是\n\n```cpp\nfor (int i = find(l); i <= r; i = find(i + 1)) {\n    // 處理位置 i（一生只會進來這一次）\n    fa[i] = i + 1;                 // 標記為已處理\n}\n```\n\n**這個五行迴圈就是本節的全部。** 它的攤還總成本是 $O((n + q)\\,\\alpha(n))$，不論每段有多長、操作有多少次。\n\n**適用的關鍵前提（本節唯一需要小心的地方）**：\n\n> **一個位置只需要被處理一次。**\n\n有兩種常見的方式讓這個前提成立：\n1. **先到先得**：問「這次操作**新**染了多少格」（2158）——已染的格子本來就不該再算。\n2. **後蓋前 ⇒ 倒序變成先到先得**：問「所有區間賦值做完後每格是什麼顏色」（LCP 52）——**倒著處理操作，第一個蓋到某格的就是最終答案**，蓋過就不必再看。這與 8.2 定理 1 的倒序思想是同一招，只是那裡把「刪除」倒成「加入」，這裡把「覆蓋」倒成「首次寫入」。\n\n**若前提不成立就不能用。** 例如「區間加 1，最後查每格的值」——每格會被處理很多次，總量不是 $O(n)$，必須用差分（第 3 章）或線段樹（9.3）。\n\n**典型題**：2158 每天新增的漆補區域、LCP 52 二叉搜尋樹染色、1851 包含每個查詢的最小區間、3244 新增道路查詢後的最短距離 II。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「區間並查集」想：\n\n- **反覆對連續區間做「填滿／染色／覆蓋」，而每個位置一旦處理就不再變。** 例如 LCP 52、2158「每天繪製新區域的數量」。\n- **暴力是每次把整段區間逐格重塗，重疊區被一再掃描（總和很大）。** 並查集讓已塗段被一步跳過。\n- **位置具「處理一次後失效」的單調性**——和陣列上的並查集同一個直覺，只是輸入是「一段區間」而非單點。\n\n:::example 嗅到味道：看到「對區間反覆覆蓋，且覆蓋過就定型」，就想 nxt[i]=i+1 跳過\n```cpp\n// 嗅到味道：看到「對區間反覆覆蓋，且覆蓋過就定型」，就想 nxt[i]=i+1 跳過\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 每個位置是不是「處理一次後就永久定型」、不會被重複處理？\n2. 我的迴圈是不是用 `find` 在區間內跳過已處理段，而非逐格走？\n3. 處理完一格後，我有沒有把它指向右鄰來標記「已處理」？\n\n## 核心想法與直覺\n\n本節是並查集在「區間」上的套路：題目反覆對一段連續區間做「染色／覆蓋／填滿」，而且**每個位置只會被有效處理一次**（染過就不再重染、填過就不再填）。重點是要能快速跳過「已經處理過」的一整段，例如 LCP 52「二叉搜索樹染色」、3244「新增道路查詢後的最短距離 II」。\n\n直覺做法是：把區間 `[l, r]` 一格一格塗過去。但如果很多區間重疊，已經塗過的格子會被重複掃，最壞 `O(n × 區間長)`。\n\n:::example parent[i] = 從 i 起第一個「尚未被處理」的位置\n並查集的巧思和「陣列上的並查集」同源：讓 `parent[i]` 代表「從 `i` 起，第一個還沒被處理的位置」。要處理區間 `[l, r]` 時，用 `find` 從 `l` 出發，**只碰還沒處理的位置；處理完一個就把它指向右鄰**，於是下次自動跳過。整段區間累積起來，每個位置一生只被真正處理一次。\n\n```cpp\n// parent[i] = 從 i 起第一個「尚未被處理」的位置\nvector<int> nxt(n + 1);\nfor (int i = 0; i <= n; ++i) {\n    nxt[i] = i;\n}\nint find(int x) { return nxt[x] == x ? x : nxt[x] = find(nxt[x]); }\n```\n:::\n\n\n\n:::example 處理區間 [l, r]：只走未處理位置，處理完就指向右鄰跳過\n```cpp\n// 處理區間 [l, r]：只走未處理位置，處理完就指向右鄰跳過\nfor (int i = find(l); i <= r; i = find(i)) {\n    paint(i);        // 對位置 i 做染色／覆蓋\n    nxt[i] = i + 1;  // 標記已處理：下次自動跳過\n}\n```\n:::\n\n\n\n關鍵在於：雖然看起來有迴圈，但**整個演算法中「真正進迴圈體 paint」的次數加起來最多 n 次**（每格只染一次），其餘都被 `find` 直接跳過，所以總體接近 `O((n + q) α(n))`。\n\n## 狀態／資料結構定義\n\n```cpp\nstruct IntervalDSU {\n    vector<int> fa;                        // fa[i] = 從 i 出發下一個未處理的位置\n    explicit IntervalDSU(int n) : fa(n + 1) {   // n + 1：哨兵不可省\n        iota(fa.begin(), fa.end(), 0);\n    }\n    int find(int x) {\n        while (fa[x] != x) {\n            fa[x] = fa[fa[x]];\n            x = fa[x];\n        }\n        return x;\n    }\n    // 處理 [l, r] 中所有尚未處理的位置，回傳處理了幾個\n    template <class F>\n    int sweep(int l, int r, F&& f) {\n        int cnt = 0;\n        for (int i = find(l); i <= r; i = find(i + 1)) {\n            f(i);\n            fa[i] = i + 1;\n            ++cnt;\n        }\n        return cnt;\n    }\n};\n```\n\n**與 8.4 的差別只有一行**：8.4 呼叫一次 `find` 就結束，本節用 `for` 迴圈掃到 $r$ 為止。**不變量、哨兵、路徑壓縮全部相同。**\n\n**三個實作細節：**\n\n- **`i = find(i + 1)` 而不是 `i = i + 1`。** 寫成 `++i` 就退化成暴力，複雜度回到 $O(nq)$——而且因為結果仍然正確，你只會看到 TLE 而不是 WA，很難察覺。\n- **`fa[i] = i + 1` 必須在迴圈體內。** 漏掉會無窮迴圈（`find(i+1)` 之後 `find` 又回到 $i$）。\n- **哨兵**：`fa` 開 `n + 1` 個元素，`fa[n] = n`。當 $[l, r]$ 之後全滿時 `find` 回傳 $n$，迴圈條件 `i <= r` 自然終止（前提 $r < n$）。\n\n**值域不是小整數時**：例如 LCP 52 的節點值可到 $10^9$。兩條路——**離散化**後用本結構，或**直接用 `set<int>`**：\n\n```cpp\n// set 版：語意完全相同，程式更短，支援任意值域與「放回去」\nauto it = s.lower_bound(l);\nwhile (it != s.end() && *it <= r) { f(*it); it = s.erase(it); }\n```\n**`set` 版單次 $O(\\log n)$ 有保證、程式短、值域任意；陣列版常數小 2～5 倍。** 值域大又不想離散化就用 `set`，這是本節最常見的實務選擇。\n\n## 不變量或正確性證明\n\n### 定理 1（`sweep` 恰好走訪 $[l,r]$ 中所有未處理位置，各一次）\n\n沿用 8.4 定理 1 的不變量 (N)：`find(x)` $= \\min\\{\\,j \\ge x : j \\text{ 未處理}\\,\\}$（無則回傳哨兵 $n$）。\n\n*證明*。迴圈變數依序取值 $i_0 = \\texttt{find}(l)$，$i_{k+1} = \\texttt{find}(i_k + 1)$。\n- **不漏**：由 (N)，$i_0$ 是 $\\ge l$ 的第一個未處理位置；每輪把 $i_k$ 標為已處理後，$i_{k+1}$ 是 $\\ge i_k+1$ 的第一個未處理位置。故 $(i_k)$ 恰為 $[l, \\infty)$ 中未處理位置的遞增列舉，在 $> r$ 時停止 ✔。\n- **不重**：每輪立刻執行 `fa[i] = i + 1`，且**標記不可逆**（沒有任何操作會把已處理位置改回未處理），故 $i$ 不會再次出現 ✔。$\\square$\n\n### 定理 2（攤還總成本 $O((n + q)\\,\\alpha(n))$）\n\n*證明*。把所有 `find` 呼叫分成兩類：\n- **成功迭代的 `find`**：每次都伴隨一個位置從「未處理」變成「已處理」。由**單向性**（定理 1 的不重論證），這種事件全域最多發生 $n$ 次。\n- **終止的 `find`**：每次 `sweep` 恰好多做一次回傳值 $> r$ 的 `find`。$q$ 次操作就是 $q$ 次。\n\n總 `find` 次數 $\\le n + q$，每次攤還 $O(\\alpha(n))$（實務上為小常數，最壞界的討論見 8.4「時間與空間複雜度」）✔。$\\square$\n\n**這個論證的形狀值得記下來：「每次昂貴的迭代都對應一個不可逆的狀態變化，而狀態變化總量有上界」。** 它與 8.1 定理 2′（節點深度增加 ⇒ 連通塊至少倍增）、4.7（單調堆疊每元素進出各一次）、6.6（懶刪除每元素刪一次）是同一個攤還模式。**這是資料結構章節反覆出現的主旋律。**\n\n**定理 2 點名的前提是「標記不可逆」。** 若題目允許「取消染色」，這個界立刻崩潰——被取消的位置可以被反覆處理，總量無上界。此時必須改用線段樹。\n\n### 定理 3（倒序把「後蓋前」變成「先到先得」）\n\n*設定*：$q$ 個區間賦值操作依序執行，第 $t$ 個把 $[l_t, r_t]$ 全部設為 $c_t$。問最終每個位置的值。\n\n**命題**：位置 $p$ 的最終值 $= c_{t^{*}(p)}$，其中 $t^{*}(p) = \\max\\{\\,t : p \\in [l_t, r_t]\\,\\}$。**倒序（$t$ 從 $q$ 到 $1$）掃描時，第一次覆蓋到 $p$ 的操作恰為 $t^{*}(p)$。**\n\n*證明*。正序語意即「最後一次寫入獲勝」，故最終值為 $c_{t^{*}(p)}$（若無操作覆蓋 $p$ 則為初值）。倒序掃描時操作編號遞減，第一個滿足 $p \\in [l_t, r_t]$ 的 $t$ 就是這類 $t$ 中的最大者，即 $t^{*}(p)$ ✔。$\\square$\n\n**推論**：倒序之下每個位置只需被處理一次，定理 2 的前提成立，總複雜度 $O((n+q)\\alpha)$。\n\n**這個轉換的價值在於它把一個「必須支援區間覆蓋」的問題，變成一個「只需要支援一次性標記」的問題。** LCP 52 就是這樣：正序做要區間賦值（線段樹 + 懶標記），倒序做只要一個 `set` 加二十行。\n\n**適用邊界**：倒序法要求**離線**（所有操作事先已知），且**查詢只在最後**。若中途穿插查詢，倒序就失去意義——回到線段樹。\n\n### 定理 4（1851：按長度遞增分配，每個查詢被最短的覆蓋區間接住）\n\n*問題*：給定區間集合與若干查詢 $x$，對每個 $x$ 求「包含 $x$ 的最短區間長度」（無則 $-1$）。\n\n**演算法**：把查詢**排序**成 $x_0 \\le x_1 \\le \\cdots \\le x_{m-1}$（記住原始下標）。把區間按**長度遞增**排序。依序處理每個區間 $[l, r]$：用二分找出查詢陣列中值落在 $[l, r]$ 的下標範圍 $[a, b]$，對這段中**尚未被回答**的查詢，填入本區間的長度並標記為已回答。\n\n**命題**：每個查詢得到的是包含它的最短區間長度。\n\n*證明*。\n- **每個查詢至多被回答一次**（標記不可逆），且回答它的是**處理順序中第一個**包含它的區間。\n- 區間按長度遞增處理 ⇒ 第一個包含 $x$ 的區間就是**包含 $x$ 的最短區間** ✔（長度相同時取誰都對，因為答案只要長度）。\n- 若無區間包含 $x$，則 $x$ 始終未被標記，輸出 $-1$ ✔。$\\square$\n\n**這個證明依賴兩個前提，都必須點名：**\n1. **查詢已排序** ⇒ 「值落在 $[l,r]$ 的查詢」在陣列上是**連續一段**，才能用本節的區間掃描。若不排序，這些查詢散落各處，區間並查集就無從下手。\n2. **區間按長度遞增** ⇒ 「先到」等於「最短」。\n\n**這是把非區間問題「掰成」區間問題的典型手法：排序製造連續性。** 8.2 定理 4（778）用排序製造單調性，這裡用排序製造**連續性**——兩者都是「先排序，讓一個看似無序的集合獲得結構」。\n\n### 定理 5（3244：不交叉前提下，答案 = 存活節點數 $-\\,1$）\n\n*問題*：節點 $0..n-1$，初始有道路 $i \\to i+1$。每個查詢加入一條單向道路 $u \\to v$（$u < v$），並回報 $0 \\to n-1$ 的最短路長度。**題目保證任意兩個查詢不交叉**：不存在 $u_1 < u_2 < v_1 < v_2$。\n\n**演算法**：加入 $u \\to v$ 時，用 `sweep(u+1, v-1, ...)` 把中間所有尚存活的節點標記為**被吸收**，答案 $=n-1-(\\text{累計被吸收數})$。\n\n令 $A$ 為已吸收節點集，$S = \\{0,\\dots,n-1\\}\\setminus A$，把 $S$ 遞增排成 $p_0 < p_1 < \\cdots < p_m$。注意 $0, n-1$ 永不被吸收（吸收只發生在 $(u,v)$ 內部，而 $0 \\le u < v \\le n-1$），故 $p_0 = 0$、$p_m = n-1$。\n\n**不變量 (B)**：任何時刻，對每個 $k < m$ 都存在一條道路 $p_k \\to p_{k+1}$。\n\n*證明（對操作次數歸納）*。初始 $S$ 為全體，$p_{k+1} = p_k + 1$，鏈上道路即是 ✔。\n設加入道路 $(u, v)$：\n- **若 $u \\in A$**：$u$ 被某條先前道路 $(u', v')$ 吸收，即 $u' < u < v'$。不交叉條件排除 $u' < u < v' < v$，故 $v \\le v'$。於是 $(u,v)$ 的內部 $\\subseteq (u',v')$ 的內部，**早已全被吸收**，本次操作不改變 $S$，(B) 自動保持 ✔。\n- **若 $v \\in A$**：同理存在 $(u',v')$ 使 $u' < v < v'$；不交叉排除 $u < u' < v < v'$，故 $u' \\le u$；而 $u \\in S$ 表示 $u$ 不在 $(u',v')$ 內部，故 $u' \\ge u$，得 $u' = u$。於是道路 $(u, v')$ 已存在且 $(u,v)$ 內部已全被吸收，$S$ 不變 ✔。\n- **$u, v \\in S$**：操作把 $(u,v)$ 內所有存活節點吸收。**新的 $S$ 中，$u$ 的後繼恰為 $v$，而道路 $(u,v)$ 剛剛加入** ✔；其餘相鄰對不受影響 ✔。$\\square$\n\n**命題**：最短路長度 $= m = |S| - 1$。\n\n*證明*。\n- **上界**：由 (B)，$0 = p_0 \\to p_1 \\to \\cdots \\to p_m = n-1$ 是一條長度 $m$ 的合法路徑 ✔。\n- **下界**：定義 $r(x) = |\\{p \\in S : p \\le x\\}|$。**宣稱任何一條道路 $x \\to y$ 都滿足 $r(y) - r(x) \\le 1$。**\n  - 鏈上道路 $i \\to i+1$：$r$ 至多增加 1 ✔。\n  - 加入的道路 $(u,v)$：加入時 $(u,v)$ 的內部已全被吸收，而**吸收不可逆**，故此後恆有 $S \\cap (u,v) = \\varnothing$，即 $r(v) - r(u) \\le 1$ ✔。\n\n  於是任何 $0 \\to n-1$ 的路徑長度 $\\ge r(n-1) - r(0) = (m+1) - 1 = m$ ✔。$\\square$\n\n**上下界都證了，等號成立。** 注意下界論證裡「吸收不可逆」又出現了一次——**它同時撐起了複雜度（定理 2）與正確性（本定理的下界）**，是這個結構的樞紐性質。\n\n**不交叉前提用在哪裡？** 只用在 (B) 的歸納中。**它是必要的**：若允許交叉，先加 $(0,5)$ 吸收 $1..4$，再加 $(3,8)$ 吸收 $5..7$，此時 $S = \\{0, 8, 9, \\dots\\}$，但**根本不存在 $0 \\to 8$ 的道路**，(B) 崩潰，答案也就錯了。（這正是 3243「最短距離 I」與 3244「II」的差別：I 沒有不交叉保證，只能每次重跑 BFS。）\n\n## 逐步演算法\n\n:::example 初始化：每格都「未處理」，外加一格哨兵\n**1. 狀態表示：`nxt[i]` = 從 `i` 起第一個未處理位置。** 多開一格哨兵避免越界。\n\n```cpp\n// 初始化：每格都「未處理」，外加一格哨兵\nvector<int> nxt(n + 2);\nfor (int i = 0; i < (int)nxt.size(); ++i) {\n    nxt[i] = i;\n}\n```\n:::\n\n\n\n:::example 走訪區間內所有「尚未處理」的位置\n**2. 對每個區間操作，用 `find` 在區間內前進。** 迴圈條件用 `i <= r`，每步用 `find(i)` 跳過已處理段。\n\n```cpp\n// 走訪區間內所有「尚未處理」的位置\nfor (int i = find(l); i <= r; i = find(i)) {\n    /* 在這裡做這格該做的事 */\n    nxt[i] = i + 1;\n}\n```\n:::\n\n\n\n:::example 例：染色時記錄每格顏色與被染次數\n**3. 邊處理邊維護答案／摘要。** 染色題在 paint 時記顏色或計數；最短距離題在「合併」時更新區間端點資訊。\n\n```cpp\n// 例：染色時記錄每格顏色與被染次數\ncolor[i] = c;\ncntOfColor[c]++;\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 區間可能左右開閉不同，務必對齊到正確的整數下標再跑迴圈。\n- 哨兵 `nxt` 要開到 `r` 的最大值 +1 以上，避免 `i + 1` 越界。\n- 複雜度：每格一生只被 paint 一次，配合路徑壓縮，整體近乎 `O((n + q) α(n))`，遠勝逐格重塗。\n\n## C++17 模板\n\n**模板一：2158 每天新增的漆補區域**\n\n```cpp\nclass Solution {\n    vector<int> fa;\n    int find(int x) {\n        while (fa[x] != x) {\n            fa[x] = fa[fa[x]];\n            x = fa[x];\n        }\n        return x;\n    }\npublic:\n    vector<int> amountPainted(vector<vector<int>>& paint) {\n        int maxEnd = 0;\n        for (auto& p : paint) maxEnd = max(maxEnd, p[1]);\n        fa.resize(maxEnd + 2);                 // 位置 0..maxEnd-1，留哨兵\n        iota(fa.begin(), fa.end(), 0);\n\n        vector<int> ans;\n        ans.reserve(paint.size());\n        for (auto& p : paint) {\n            int l = p[0], r = p[1] - 1;        // [start, end) -> 閉區間 [l, r]\n            int cnt = 0;\n            for (int i = find(l); i <= r; i = find(i + 1)) {\n                fa[i] = i + 1;                 // 標記已漆\n                ++cnt;\n            }\n            ans.push_back(cnt);\n        }\n        return ans;\n    }\n};\n```\n\n**模板二：3244 新增道路查詢後的最短距離 II**\n\n```cpp\nclass Solution {\n    vector<int> fa;\n    int find(int x) {\n        while (fa[x] != x) {\n            fa[x] = fa[fa[x]];\n            x = fa[x];\n        }\n        return x;\n    }\npublic:\n    vector<int> shortestDistanceAfterQueries(int n, vector<vector<int>>& queries) {\n        fa.resize(n + 1);\n        iota(fa.begin(), fa.end(), 0);         // fa[i] = 下一個存活節點\n\n        int cur = n - 1;                       // 目前最短路長度 = 存活節點數 - 1\n        vector<int> ans;\n        ans.reserve(queries.size());\n        for (auto& q : queries) {\n            int u = q[0], v = q[1];\n            for (int i = find(u + 1); i < v; i = find(i + 1)) {\n                fa[i] = i + 1;                 // 節點 i 被吸收\n                --cur;\n            }\n            ans.push_back(cur);\n        }\n        return ans;\n    }\n};\n```\n\n**模板三：1851 包含每個查詢的最小區間**\n\n```cpp\nclass Solution {\n    vector<int> fa;\n    int find(int x) {\n        while (fa[x] != x) {\n            fa[x] = fa[fa[x]];\n            x = fa[x];\n        }\n        return x;\n    }\npublic:\n    vector<int> minInterval(vector<vector<int>>& intervals, vector<int>& queries) {\n        int m = queries.size();\n        vector<int> idx(m);\n        iota(idx.begin(), idx.end(), 0);\n        sort(idx.begin(), idx.end(),\n             [&](int a, int b) { return queries[a] < queries[b]; });\n        vector<int> qs(m);                     // 排序後的查詢值\n        for (int i = 0; i < m; ++i) qs[i] = queries[idx[i]];\n\n        sort(intervals.begin(), intervals.end(),\n             [](const vector<int>& a, const vector<int>& b) {\n                 return a[1] - a[0] < b[1] - b[0];   // 按長度遞增\n             });\n\n        fa.resize(m + 1);\n        iota(fa.begin(), fa.end(), 0);\n        vector<int> ans(m, -1);\n        for (auto& it : intervals) {\n            int l = it[0], r = it[1];\n            int a = lower_bound(qs.begin(), qs.end(), l) - qs.begin();\n            int b = int(upper_bound(qs.begin(), qs.end(), r) - qs.begin()) - 1;\n            for (int i = find(a); i <= b; i = find(i + 1)) {\n                ans[idx[i]] = r - l + 1;       // 定理 4：先到即最短\n                fa[i] = i + 1;\n            }\n        }\n        return ans;\n    }\n};\n```\n\n（1851 的**主流解法是排序 + 最小堆**，複雜度相同、程式更短。這裡放並查集版是為了展示「排序製造連續性」這個轉換——**遇到「每個查詢只需要被第一個符合條件的東西回答」時，這個模式可以直接套用。**）\n\n## 時間與空間複雜度\n\n| | 時間 | 空間 |\n| --- | --- | --- |\n| 核心結構 | $O((n + q)\\,\\alpha(n))$ 攤還（定理 2） | $O(n)$ |\n| 2158 | $O(M + q)$ 近乎，$M$ = 最大端點 | $O(M)$ |\n| 3244 | $O(n + q)$ 近乎 | $O(n)$ |\n| LCP 52 | $O(n\\log n)$（`set` 版） | $O(n)$ |\n| 1851 | $O(n\\log n + m\\log m)$ | $O(n + m)$ |\n\n**再一次：瓶頸在排序或 `set`，不在並查集本身。** 2158 與 3244 完全沒有 $\\log$，是真正的近線性。\n\n**與寫法 A 相同的誠實聲明**（詳見 8.4）：合併方向被語意固定為「指向右」，**不能按大小合併**，所以嚴格的最壞界不是 $O(\\alpha)$；但這個特殊形態（總是與右鄰合併）實測接近線性。**需要嚴格保證時改用 8.4 的寫法 B**（正統並查集 + 根上記 `nxt`）。\n\n**空間陷阱**：2158 的端點可到 $5\\times10^4$，開陣列沒問題；但若題目端點到 $10^9$，就**必須離散化或改用 `set`**——盲目 `resize(maxEnd + 2)` 會直接 MLE。\n\n## 常見錯誤與邊界條件\n\n- **迴圈遞增寫成 `++i`**。`for (int i = find(l); i <= r; ++i)` 結果正確但退化成 $O(nq)$。**這是本節最隱蔽的錯誤：不是 WA 而是 TLE。** 必須是 `i = find(i + 1)`。\n- **忘記在迴圈內寫 `fa[i] = i + 1`**。`find(i+1)` 之後 `find` 仍回到 $i$，**無窮迴圈**。\n- **哨兵沒開**。`fa` 要 `n + 1` 個元素。$[l,r]$ 之後全滿時 `find` 讀 `fa[n]`。\n- **半開／閉區間搞混**。2158 的 `paint[i] = [start, end)` 對應**位置 `start..end-1`**，寫成 `r = p[1]` 會多漆一格。**下筆前先確認題目的區間是開是閉。**\n- **在「區間累加」的題目上套用本節結構**。前提是「每格只處理一次」；累加類每格會被處理多次，總量無上界。**用差分或線段樹。**\n- **忘記倒序**。「區間賦值，最後查每格顏色」正序做需要懶標記；**倒序才能用本節結構**（定理 3）。中途有查詢就不能倒序。\n- **1851 忘記還原原始順序**。查詢排序後要記住原下標，填答案時寫 `ans[idx[i]]`。\n- **1851 的二分邊界**。`b` 用 `upper_bound(r) - 1`；若 $a > b$（沒有查詢落在區間內）迴圈自然不執行，不必特判——但 `find(a)` 要求 $a \\le m$，由 `lower_bound` 的性質這永遠成立。\n- **3244 的掃描範圍是 `[u+1, v-1]`**。掃 `[u, v]` 會把端點也吸收掉，答案全錯。\n- **3244 誤用在 3243 上**。3243 沒有不交叉保證，(B) 不成立（見定理 5 的反例），必須每次重跑 BFS。**兩題只差一個約束，解法完全不同。**\n- **值域大時直接開陣列**。$10^9$ 的值域要離散化或用 `set`。\n- **遞迴版 `find` 爆堆疊**。初始鏈長可達 $n$。用迭代版。\n\n## 與相似技巧的比較\n\n- **與 8.4**：**同一個不變量，同一個哨兵，同一個路徑壓縮。** 差別只在 8.4 呼叫一次 `find`，本節用迴圈掃到 $r$。**先讀 8.4 再讀本節，會發現本節幾乎沒有新東西——新的只有「攤還總量 $\\le n$」這個論證（定理 2）。**\n- **與 `set<int>` + `lower_bound`／`erase`**：語意完全等價，**值域任意（不必離散化）、複雜度 $O(\\log n)$ 有保證、而且支援把元素放回去**。代價是常數大 2～5 倍。**判準：值域小且不需要放回 ⇒ 陣列版；否則 `set`。** LCP 52 的節點值到 $10^9$，`set` 是自然選擇。\n- **與珂朵莉樹（ODT，`map` 維護值相同的極長區間）**：ODT 支援**真正的重複區間賦值**與區間查詢，功能比本節強得多。**它的複雜度依賴「隨機資料下區間數量期望為 $O(\\log n)$」，在有意構造的資料上會退化**——競賽中要小心，LeetCode 上通常安全。**若題目要求「多次區間賦值 + 中途查詢」，ODT 或線段樹是正解，本節不適用。**\n- **與線段樹（區間賦值 + 懶標記，9.3）**：功能最強——支援中途查詢、支援取消、支援區間聚合，$O(\\log n)$ 有保證。**代價是程式長 5 倍。** 當「每格只處理一次」的前提被打破時（有撤銷、有累加、有中途查詢），線段樹是唯一出路。\n- **與差分（第 3 章）**：「區間加、最後查」用差分 $O(n + q)$，比什麼都快。**差分不能做區間賦值，本節不能做區間累加——兩者互補，別搞混。**\n- **與掃描線**：掃描線把區間事件排序後一次掃過，也能算「總覆蓋長度」（如 2158 的變形）。**差別在掃描線需要離線且輸出是聚合量；本節可以線上回答「這次新增了多少」。** 2158 恰好要求每次操作的增量，本節更直接。\n- **與 8.1／8.2／8.3**：那三節的 `fa` 是**等價類代表元**；本節與 8.4 的 `fa` 是**下一個未處理位置**。**不要混用直覺。** 唯一的共同點是路徑壓縮這個機制。\n\n## 代表例題\n\n**題號**：LCP 52、1851、2158、3244\n\n把等價或連通關係合併到代表元；練習在 union 時同步維護集合資訊。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| LCP 52 | [二叉搜索樹染色](https://leetcode.cn/problems/QO5KpG)（Premium） |  |\n| 1851 | [包含每個查詢的最小區間](https://leetcode.cn/problems/minimum-interval-to-include-each-query) | 2286 |\n| 2158 | [每天繪製新區域的數量](https://leetcode.cn/problems/amount-of-new-area-painted-each-day)（Premium） |  |\n| 3244 | [新增道路查詢後的最短距離 II](https://leetcode.cn/problems/shortest-distance-after-road-addition-queries-ii) | 2270 |\n\n## 本節重點速查\n\n- **核心是五行迴圈**：`for (int i = find(l); i <= r; i = find(i + 1)) { 處理 i; fa[i] = i + 1; }`。**遞增一定要 `find(i + 1)`，`++i` 會 TLE 但不會 WA。**\n- **唯一的適用前提**：**每個位置一生只需要被處理一次**。兩種來源——「先到先得」（2158）或「**倒序讓後蓋前變成先到先得**」（LCP 52，定理 3）。\n- **攤還 $O((n+q)\\alpha)$（定理 2）**：成功迭代 $\\le n$ 次（不可逆），終止 `find` $\\le q$ 次。**「昂貴迭代對應不可逆狀態變化」是資料結構章反覆出現的攤還模式**（4.7、6.6、8.1 定理 2′ 同形）。\n- **「標記不可逆」同時撐起複雜度與正確性**——3244 的下界論證也用它。有撤銷就整套崩潰，改用線段樹。\n- **哨兵 `fa[n] = n` 不可省**；閉區間 vs 半開區間要對清楚（2158 是 $[\\text{start}, \\text{end})$）。\n- **1851**：**排序製造連續性**——查詢排序後「落在 $[l,r]$ 的查詢」才是連續一段；區間按長度遞增 ⇒ 先到即最短。答案要用 `ans[idx[i]]` 還原原順序。\n- **3244**：答案 $=$ 存活節點數 $-1$，掃描範圍是 **$[u+1, v-1]$**。**不交叉前提是必要的**，`(0,5)` 後接 `(3,8)` 就是反例——這也是 3243 不能這樣做的原因。\n- **值域大就用 `set` + `lower_bound`／`erase`**：語意等價、值域任意、$O(\\log n)$ 保證、支援放回；只是常數大。\n- **不能做區間累加**（用差分）、**不能做中途查詢的多次區間賦值**（用 ODT 或線段樹）。\n",
        },
        {
          id: 59,
          title: "8.6 帶權並查集（邊權並查集）",
          summary:
            '## 這個技術解決什麼問題\n\n普通並查集只能回答「$x$ 與 $y$ 同一類嗎」。但很多題目給的不是「同類」，而是**帶量的相對關係**：\n\n- $a / b = 2.0$、$b / c = 3.0$，問 $a / c$（399 除法求值）；\n- $a - b = 5$、$b - c = -2$，問 $a - c$；\n- $a$ 與 $b$ 奇偶性相同、$b$ 與 $c$ 相異，問 $a$ 與 $c$。\n\n這些關係都有兩個共通性質：\n\n1. **可傳遞、可合成**：知道 $x \\to y$ 與 $y \\to z$ 就能算出 $x \\to z$；\n2. **可逆**：知道 $x \\to y$ 就能算出 $y \\to x$。\n\n**這正是「群」的定義。** 帶權並查集把每條 `fa` 邊掛上一個群元素 $w[x] = \\mathrm{rel}(x, \\texttt{fa}[x])$，於是：\n\n> **$x$ 與 $y$ 的關係 $=$（$x$ 到根的合成）$\\ominus$（$y$ 到根的合成）。**\n\n只要兩者同根，關係就能在近乎 $O(1)$ 內算出——**而且根本不需要知道任何一個變數的絕對值。**\n\n**它同時解決三件事**：\n- **查詢**：回答任兩個同組元素之間從未被直接給出的關係；\n- **合併**：把兩組用一條新關係接起來；\n- **矛盾偵測**：新關係若連接兩個**已同組**的元素，就必須與既有推導一致，否則整個方程組無解（2307）。\n\n**辨識訊號**：題目給的是**成對的相對量**（比值、差值、同餘、奇偶），要你推導未給出的配對，或判斷是否自相矛盾。\n\n**它做不到的事（必須先講清楚）**：**帶權並查集永遠回答不了絕對值。** 每個連通塊的根可以被賦予任意值，整塊隨之伸縮而所有相對關係不變。若題目要求具體數值（例如「$a$ 等於多少」），要嘛題目另給了錨點，要嘛這個工具就不對。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「帶權並查集」想：\n\n- **元素之間有「可傳遞、可累積的相對關係」**：比值（`a/b`）、差值（`a - b`）、同餘等。例如 399「除法求值」。\n- **題目給的是「兩兩之間的關係」，要你推出「另外兩個沒直接給的元素」之間的關係。** 關係能沿路徑串起來，正是帶權並查集的本事。\n- **需要在合併時順手偵測「條件互相矛盾」。** 例如 2307：若新條件和已連通推出的關係不符，即為矛盾。\n\n:::example 嗅到味道：看到「給 a 對 b 的比值/差，問 a 對 c」，就想帶權並查集\n```cpp\n// 嗅到味道：看到「給 a 對 b 的比值/差，問 a 對 c」，就想帶權並查集\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的 `w[x]` 代表「x 相對上司」的什麼量？運算是乘除還是加減？\n2. `find` 做路徑壓縮時，我有沒有把沿途權值正確累積到 `w[x]`？\n3. 合併時，我推導出的新邊權能不能讓原本給定的關係仍然成立？\n\n## 核心想法與直覺\n\n本節是並查集的加權版。普通並查集只回答「兩個元素是否同夥」；**帶權並查集還在「到老大的路上」記一個權值，於是能回答「同夥的兩個元素之間，相對關係是多少」**。最經典的例子是 399「除法求值」：給你 `a/b = 2`、`b/c = 3`，問 `a/c = ?`（答案 6）。\n\n:::example 帶權並查集：parent 指上司，w[x] 記錄 x 相對上司的關係（如 x / par…\n核心想法：在 `parent[x]` 之外，再開一個 `w[x]`，記錄「`x` 相對於它上司的關係」。對除法題，`w[x]` 就是 `x / parent[x]` 的值。順著上司一路爬到老大，沿途把這些關係「合起來」（除法是相乘），就得到 `x` 相對於老大的關係。\n\n```cpp\n// 帶權並查集：parent 指上司，w[x] 記錄 x 相對上司的關係（如 x / parent[x]）\nvector<int> parent;\nvector<double> w;  // 除法題用乘法累積；同餘/差值題改成加法\n```\n:::\n\n\n\n:::example find：路徑壓縮時，把沿途權值累乘，更新成「相對老大」的關係\n`find` 時做路徑壓縮，但**壓縮的同時要把權值一起「累乘上去」**，因為 `x` 直接接到老大後，`w[x]` 必須變成「`x` 相對老大」的值。\n\n```cpp\n// find：路徑壓縮時，把沿途權值累乘，更新成「相對老大」的關係\nint find(int x) {\n    if (parent[x] == x) {\n        return x;\n    }\n    int root = find(parent[x]);\n    w[x] *= w[parent[x]];  // x→上司→老大，關係相乘（差值題改成相加）\n    parent[x] = root;\n    return root;\n}\n```\n:::\n\n\n\n合併 `a`、`b` 且已知 `a/b = val` 時，把兩個老大接起來，並算出「新接上的老大」該帶的權值，讓不變式（每個點到老大的關係正確）繼續成立。查詢 `a/c` 時，若同夥就回傳 `w[a] / w[c]`（兩者都相對同一個老大，相除即得相對關係）。\n\n## 狀態／資料結構定義\n\n**權值群 $(G, \\oplus, e, \\ominus)$**：$\\oplus$ 為合成、$e$ 為單位元、$\\ominus$ 為逆元。常見選擇：\n\n| 關係 | $G$ | $\\oplus$ | $e$ | 逆元 |\n| --- | --- | --- | --- | --- |\n| 比值 $a/b$ | $(\\mathbb{R}^{+}, \\times)$ | 乘 | $1$ | 倒數 |\n| 差值 $a-b$ | $(\\mathbb{Z}, +)$ | 加 | $0$ | 取負 |\n| 奇偶／同／異 | $(\\mathbb{Z}_2, \\oplus)$ | XOR | $0$ | 自身 |\n| 模 $k$ 偏移 | $(\\mathbb{Z}_k, +)$ | 模加 | $0$ | $k - a \\bmod k$ |\n\n**交換律不是必要的**（合成順序在實作中是固定的），但實務上遇到的權值群幾乎都是交換群。\n\n**資料結構**（以加法群為例）：\n\n```cpp\nstruct WeightedDSU {\n    vector<int> fa, sz;\n    vector<long long> w;          // w[x] = rel(x, fa[x])，即 val[x] - val[fa[x]]\n    explicit WeightedDSU(int n) : fa(n), sz(n, 1), w(n, 0) {\n        iota(fa.begin(), fa.end(), 0);\n    }\n    int find(int x) {             // 遞迴版最不容易寫錯\n        if (fa[x] == x) return x;\n        int r = find(fa[x]);\n        w[x] += w[fa[x]];         // 先累積（此時 w[fa[x]] 已是「到根」）\n        fa[x] = r;                // 再改指標\n        return r;\n    }\n};\n```\n\n**`find` 裡那兩行的順序是本節最容易寫錯的地方。** `w[x] += w[fa[x]]` 必須在遞迴**之後**、改 `fa[x]` **之前**：遞迴保證了 `w[fa[x]]` 已經是「`fa[x]` 到根」的合成，加完 `w[x]` 才變成「$x$ 到根」；一旦先寫 `fa[x] = r`，`w[fa[x]]` 就變成 `w[r] = 0`，權值靜靜地歸零而程式照跑。**這個 bug 不會崩、不會警告，只會給出錯誤答案。**\n\n**合併**（記錄 $\\mathrm{rel}(x, y) = v$，即 $\\mathrm{val}(x) - \\mathrm{val}(y) = v$）：\n\n```cpp\n    // 回傳 false 表示矛盾\n    bool unite(int x, int y, long long v) {\n        int rx = find(x), ry = find(y);\n        if (rx == ry) return w[x] - w[y] == v;      // 一致性檢查\n        if (sz[rx] > sz[ry]) {                       // 按大小合併：需要逆元\n            fa[ry] = rx; w[ry] = w[x] - w[y] - v;    // 方向反轉 ⇒ 取負\n            sz[rx] += sz[ry];\n        } else {\n            fa[rx] = ry; w[rx] = v + w[y] - w[x];\n            sz[ry] += sz[rx];\n        }\n        return true;\n    }\n    // 查詢 rel(x, y)：同根才有意義\n    bool query(int x, int y, long long& res) {\n        if (find(x) != find(y)) return false;\n        res = w[x] - w[y];\n        return true;\n    }\n```\n\n**按大小合併與帶權完全相容**——只要群有逆元，把邊反向時取負（乘法群取倒數）即可。**不要因為「權值有方向」就放棄平衡策略**，那會白白讓樹退化。\n\n## 不變量或正確性證明\n\n以下用加法記號（$\\oplus = +$、$\\ominus = -$），乘法版把加改乘、減改除即可。設每個元素有一個**虛擬值** $\\mathrm{val}(\\cdot)$（實際上並不儲存）。\n\n### 定理 1（主不變量與 `find` 的正確性）\n\n**不變量 (W)**：任何時刻，對每個非根節點 $x$，\n$$w[x] = \\mathrm{val}(x) - \\mathrm{val}(\\texttt{fa}[x]);$$\n對每個根 $r$，$w[r] = 0$。\n\n**命題**：在 (W) 之下，`find(x)` 執行完畢後 $w[x] = \\mathrm{val}(x) - \\mathrm{val}(\\text{root}(x))$，且 (W) 仍然成立。\n\n*證明（對 $x$ 到根的距離 $d$ 歸納）*。\n- $d = 0$：$x$ 是根，`find` 直接回傳，$w[x] = 0 = \\mathrm{val}(x) - \\mathrm{val}(x)$ ✔。\n- $d \\ge 1$：令 $p = \\texttt{fa}[x]$，其距離為 $d-1$。歸納假設：遞迴呼叫後 $w[p] = \\mathrm{val}(p) - \\mathrm{val}(r)$，且 $\\texttt{fa}[p] = r$。此時\n  $$w[x] + w[p] = \\big(\\mathrm{val}(x) - \\mathrm{val}(p)\\big) + \\big(\\mathrm{val}(p) - \\mathrm{val}(r)\\big) = \\mathrm{val}(x) - \\mathrm{val}(r),$$\n  正是把 `fa[x]` 改成 $r$ 後 (W) 所要求的值 ✔。$\\square$\n\n**這裡有兩個前提，都必須點名：**\n\n1. **中間項 $\\mathrm{val}(p)$ 相消** —— 這是「相對關係可合成」的數學內容，也是群結合律的體現。**若權值不構成群（例如你想維護「兩點間的最大值」），這一步就不成立，帶權並查集就用不了。**\n2. **壓縮的目標必須是原本的祖先** —— 8.1 定理 1 已經指出這一點，本節把它變得更嚴苛：**普通並查集壓縮到祖先只影響效率，帶權並查集若壓縮到非祖先，(W) 直接失效**，因為 $w[x]$ 累積的是**沿原路徑**的和。這就是 8.1 中那句「8.6 的帶權並查集可能因為權值算錯而破壞這一點」的具體所指。\n\n**推論 1.1（查詢公式）**：若 $x, y$ 同根 $r$，則\n$$\\mathrm{val}(x) - \\mathrm{val}(y) = \\big(\\mathrm{val}(x) - \\mathrm{val}(r)\\big) - \\big(\\mathrm{val}(y) - \\mathrm{val}(r)\\big) = w[x] - w[y].$$\n**根的值 $\\mathrm{val}(r)$ 被消掉了** ——這既是「不需要絕對值」的原因，也是「無法得知絕對值」的原因。**同一枚硬幣的兩面。**\n\n### 定理 2（`unite` 的權值公式是唯一正確的選擇）\n\n*設定*：要記錄 $\\mathrm{val}(x) - \\mathrm{val}(y) = v$，且 $rx = \\texttt{find}(x) \\ne ry = \\texttt{find}(y)$。\n\n**命題**：令 $\\texttt{fa}[rx] = ry$，則 (W) 要求 $w[rx] = v + w[y] - w[x]$，且此值唯一。\n\n*證明*。(W) 對新邊要求 $w[rx] = \\mathrm{val}(rx) - \\mathrm{val}(ry)$。由定理 1，$\\mathrm{val}(x) = w[x] + \\mathrm{val}(rx)$、$\\mathrm{val}(y) = w[y] + \\mathrm{val}(ry)$。代入約束：\n$$v = \\mathrm{val}(x) - \\mathrm{val}(y) = \\big(w[x] + \\mathrm{val}(rx)\\big) - \\big(w[y] + \\mathrm{val}(ry)\\big),$$\n移項得 $\\mathrm{val}(rx) - \\mathrm{val}(ry) = v + w[y] - w[x]$ ✔。**表達式由 (W) 唯一決定，沒有選擇餘地。**\n\n反向掛（$\\texttt{fa}[ry] = rx$）時取逆元：$w[ry] = w[x] - w[y] - v$ ✔。$\\square$\n\n**推導這條公式的正確做法是「寫下 (W) 的要求，代入定理 1，移項」，不要憑感覺拼。** 帶權並查集的公式錯誤有一半來自方向拼錯，而方向拼錯在小測資上常常剛好對（例如所有權值都是 $1$ 或所有關係都對稱時）。**用一個非對稱的三元素例子手算驗證：$a-b=3$、$b-c=5$，查 $a-c$ 應得 $8$。**\n\n### 定理 3（399 除法求值：可答 $\\iff$ 同組）\n\n*問題*：給定若干 $\\mathrm{val}(a_i)/\\mathrm{val}(b_i) = v_i$，回答若干 $\\mathrm{val}(x)/\\mathrm{val}(y)$，無法確定則回 $-1$。\n\n**命題**：$\\mathrm{val}(x)/\\mathrm{val}(y)$ 被方程組唯一確定 $\\iff$ $x, y$ 出現過且在同一連通塊；此時答案 $= w[x]/w[y]$。\n\n*證明*。\n- **($\\Leftarrow$)** 同根時由推論 1.1（乘法版）得 $\\mathrm{val}(x)/\\mathrm{val}(y) = w[x]/w[y]$，而該式只依賴給定的方程 ✔。\n- **($\\Rightarrow$)** 設 $x, y$ 不同塊（或某一者從未出現）。取任一組滿足所有方程的賦值 $\\mathrm{val}$，把 $x$ 所在塊的所有值同乘一個常數 $t > 0$、其餘不動。**所有方程仍然成立**（每個方程的兩端在同一塊內，比值不變），但 $\\mathrm{val}(x)/\\mathrm{val}(y)$ 變成原值的 $t$ 倍。$t$ 任意 ⇒ 該比值未被確定 ✔。$\\square$\n\n**($\\Rightarrow$) 這一半才是重點**：它證明了「不同塊 ⇒ 真的無解」，而不只是「我們的演算法算不出來」。**論證方式是「造出兩組都滿足約束但答案不同的解」——這是所有『資訊不足』證明的標準形狀。**\n\n### 定理 4（2307 矛盾偵測：檢查每條非樹邊就夠了）\n\n*問題*：給定 $m$ 個方程 $\\mathrm{val}(a_i)/\\mathrm{val}(b_i) = v_i$，判斷是否存在矛盾。\n\n**演算法**：依序 `unite`；若某次 $rx = ry$ 且 $|w[x]/w[y] - v| > \\varepsilon$ 則回報矛盾。\n\n**命題**：方程組可滿足 $\\iff$ 演算法從未回報矛盾。\n\n*證明*。把每個方程看成一條無向邊。演算法對每條邊做一次判斷：$rx \\ne ry$ 的邊構成一棵**生成森林**（稱樹邊），其餘是**非樹邊**。\n\n- **($\\Rightarrow$ 可滿足則不回報)**：設有滿足解 $\\mathrm{val}$。由定理 1，任何時刻 $w[\\cdot]$ 都忠實反映 $\\mathrm{val}$ 的相對關係，故非樹邊的檢查 $w[x]/w[y]$ 恰等於 $\\mathrm{val}(x)/\\mathrm{val}(y) = v$ ✔。\n- **($\\Leftarrow$ 不回報則可滿足)**：**構造解**。對每棵樹的根任意指定 $\\mathrm{val}(r) = 1$，其餘節點取 $\\mathrm{val}(x) = w[x]$。則所有**樹邊**的方程依 (W) 自動成立；所有**非樹邊**的方程因為通過了檢查而成立 ✔。$\\square$\n\n**這個證明的價值在於它回答了「為什麼只檢查非樹邊就夠、不必檢查所有環」**：因為構造出的賦值讓**每一條給定的邊**都成立，而方程組的可滿足性就只關乎這些邊。（用圖論語言說：基本環構成環空間的一組基，檢查每條非樹邊等於檢查所有環——但上面的構造性證明更直接，也不需要環空間的語言。）\n\n**浮點細節**：$m \\le 10^5$、$v \\le 20$，連乘可能溢位或下溢到 $10^{\\pm 100}$ 以外。**用相對誤差比較**：`fabs(w[x] / w[y] - v) > 1e-5 * max(1.0, fabs(v))`；更穩健的做法是**改存 $\\log$，把乘法群換成加法群**（$\\log(a/b) = \\log a - \\log b$），連乘變連加，誤差不再相乘放大。\n\n### 定理 5（$\\mathbb{Z}_2$ 權值 $\\equiv$ 8.2 的擴展域並查集）\n\n*設定*：關係只有「同類」與「異類」（奇偶、敵友、二分圖判定）。\n\n**命題**：以 $G = \\mathbb{Z}_2$（$\\oplus$ 為 XOR）的帶權並查集，與 8.2 的擴展域並查集（為每個 $x$ 額外開一個節點 $x + n$ 表示「$x$ 的反面」）**判定能力完全相同**。\n\n*證明（雙向模擬）*。\n- **帶權 $\\to$ 擴展域**：若帶權判定 $x, y$ 同根且 $w[x] \\oplus w[y] = 0$，則路徑上「異類」邊為偶數條；在擴展域中沿同一路徑做 `unite(a, b)`／`unite(a, b+n)` 的對應合併，可得 $x$ 與 $y$ 同組 ✔。\n- **擴展域 $\\to$ 帶權**：擴展域中 $x$ 與 $y$ 同組表示存在一條交錯路徑，其「翻面」次數為偶數，對應帶權下 $w[x] \\oplus w[y] = 0$ ✔。$\\square$\n\n**兩者的取捨：**\n\n| | 帶權（$\\mathbb{Z}_2$） | 擴展域 |\n| --- | --- | --- |\n| 空間 | $2n$ 個 `int`（`fa` + `w`） | $2n$ 個 `int`（`fa`） |\n| 程式複雜度 | 要正確維護 `w`，易錯 | **只有普通 `unite`，幾乎不會錯** |\n| 推廣到 $k \\ge 3$ 類 | **改成 $\\mathbb{Z}_k$ 即可** | 要開 $kn$ 個節點，且「互斥」語意在 $k \\ge 3$ 時不再等價 |\n\n**結論很實際：只有兩類就用擴展域（更不容易錯）；三類以上或需要具體數值差就用帶權。** 這是 8.2 與本節的分界線。\n\n## 逐步演算法\n\n:::example 初始：自成一夥，且「自己相對自己」的權值是單位元（乘法用 1，加法用 0）\n**1. 狀態表示：`parent` + 權值 `w`。** 權值的運算要配合題意：除法用乘除、差值/同餘用加減。\n\n```cpp\n// 初始：自成一夥，且「自己相對自己」的權值是單位元（乘法用 1，加法用 0）\nvoid init(int n) {\n    parent.resize(n);\n    w.assign(n, 1.0);\n    for (int i = 0; i < n; ++i) {\n        parent[i] = i;\n    }\n}\n```\n:::\n\n\n\n:::example find：如上，路徑壓縮 + 權值相乘\n**2. find：壓縮路徑並同步累積權值。** 這一步是帶權並查集的靈魂，漏了權值就全錯。\n\n```cpp\n// find：如上，路徑壓縮 + 權值相乘\n// w[x] *= w[parent[x]] 之後再把 parent[x] 改成 root\n```\n:::\n\n\n\n:::example merge：已知 a/b = val，把 a 的老大接到 b 的老大底下\n**3. union：接上兩個老大，算出新邊權。** 由 `a→b` 的已知關係，推出「a 的老大」相對「b 的老大」的權值。\n\n```cpp\n// merge：已知 a/b = val，把 a 的老大接到 b 的老大底下\nvoid merge(int a, int b, double val) {\n    int ra = find(a), rb = find(b);\n    if (ra == rb) {\n        return;\n    }\n    parent[ra] = rb;\n    w[ra] = val * w[b] / w[a];  // 推導使 a/b 仍等於 val\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 查詢前要先 `find(a)`、`find(b)`：不同夥代表關係未知（回傳 -1）。\n- 矛盾偵測（如 2307「方程中的矛盾」）：合併已同夥的兩點時，檢查既有權值和新條件是否衝突。\n- 複雜度：和普通並查集同級，單次操作均攤近乎 `O(α(n))`。\n\n## C++17 模板\n\n**模板一：399 除法求值（乘法群，字串映射）**\n\n```cpp\nclass Solution {\n    vector<int> fa;\n    vector<double> w;                       // w[x] = val[x] / val[fa[x]]\n    int find(int x) {\n        if (fa[x] == x) return x;\n        int r = find(fa[x]);\n        w[x] *= w[fa[x]];                   // 先累積\n        fa[x] = r;                          // 再改指標\n        return r;\n    }\npublic:\n    vector<double> calcEquation(vector<vector<string>>& equations,\n                                vector<double>& values,\n                                vector<vector<string>>& queries) {\n        unordered_map<string, int> id;\n        auto getId = [&](const string& s) {\n            auto it = id.find(s);\n            if (it != id.end()) return it->second;\n            int k = id.size();\n            id[s] = k;\n            fa.push_back(k);\n            w.push_back(1.0);\n            return k;\n        };\n        for (int i = 0; i < (int)equations.size(); ++i) {\n            int a = getId(equations[i][0]), b = getId(equations[i][1]);\n            int ra = find(a), rb = find(b);\n            if (ra == rb) continue;         // 本題保證無矛盾\n            fa[ra] = rb;\n            w[ra] = values[i] * w[b] / w[a];   // 定理 2 的乘法版\n        }\n        vector<double> ans;\n        ans.reserve(queries.size());\n        for (auto& q : queries) {\n            auto ia = id.find(q[0]), ib = id.find(q[1]);\n            if (ia == id.end() || ib == id.end() ||\n                find(ia->second) != find(ib->second)) {\n                ans.push_back(-1.0);\n            } else {\n                ans.push_back(w[ia->second] / w[ib->second]);\n            }\n        }\n        return ans;\n    }\n};\n```\n\n（本題沒有按大小合併——變數數量 $\\le 40$，樹深可忽略。**規模大時務必加回按大小合併**，並記得反向掛時把權值取倒數。）\n\n**模板二：2307 檢查方程式中的矛盾（改存 $\\log$，把乘法變加法）**\n\n```cpp\nclass Solution {\n    vector<int> fa, sz;\n    vector<double> w;                       // w[x] = log(val[x]) - log(val[fa[x]])\n    int find(int x) {\n        if (fa[x] == x) return x;\n        int r = find(fa[x]);\n        w[x] += w[fa[x]];\n        fa[x] = r;\n        return r;\n    }\npublic:\n    bool checkContradictions(vector<vector<string>>& equations,\n                             vector<double>& values) {\n        unordered_map<string, int> id;\n        auto getId = [&](const string& s) {\n            auto it = id.find(s);\n            if (it != id.end()) return it->second;\n            int k = id.size();\n            id[s] = k;\n            fa.push_back(k);\n            sz.push_back(1);\n            w.push_back(0.0);\n            return k;\n        };\n        const double EPS = 1e-5;\n        for (int i = 0; i < (int)equations.size(); ++i) {\n            int a = getId(equations[i][0]), b = getId(equations[i][1]);\n            double v = log(values[i]);\n            int ra = find(a), rb = find(b);\n            if (ra == rb) {\n                if (fabs(w[a] - w[b] - v) > EPS) return true;   // 矛盾\n                continue;\n            }\n            if (sz[ra] > sz[rb]) {          // 按大小合併，反向掛要取負\n                fa[rb] = ra;\n                w[rb] = w[a] - w[b] - v;\n                sz[ra] += sz[rb];\n            } else {\n                fa[ra] = rb;\n                w[ra] = v + w[b] - w[a];\n                sz[rb] += sz[ra];\n            }\n        }\n        return false;\n    }\n};\n```\n\n**模板三：$\\mathbb{Z}_k$ 版通用骨架（奇偶、模 $k$ 偏移）**\n\n```cpp\ntemplate <int K>\nstruct ModDSU {\n    vector<int> fa, sz, w;                  // w[x] = (val[x] - val[fa[x]]) mod K\n    explicit ModDSU(int n) : fa(n), sz(n, 1), w(n, 0) {\n        iota(fa.begin(), fa.end(), 0);\n    }\n    int find(int x) {\n        if (fa[x] == x) return x;\n        int r = find(fa[x]);\n        w[x] = (w[x] + w[fa[x]]) % K;\n        fa[x] = r;\n        return r;\n    }\n    // 記錄 (val[x] - val[y]) mod K == v；回傳 false 表示矛盾\n    bool unite(int x, int y, int v) {\n        int rx = find(x), ry = find(y);\n        if (rx == ry) return ((w[x] - w[y]) % K + K) % K == v % K;\n        if (sz[rx] > sz[ry]) {\n            fa[ry] = rx;\n            w[ry] = ((w[x] - w[y] - v) % K + K) % K;\n            sz[rx] += sz[ry];\n        } else {\n            fa[rx] = ry;\n            w[rx] = ((v + w[y] - w[x]) % K + K) % K;\n            sz[ry] += sz[rx];\n        }\n        return true;\n    }\n};\n```\n\n（`K = 2` 就是奇偶／二分圖判定；**但如前所述，只有兩類時 8.2 的擴展域寫法更不容易錯。**）\n\n## 時間與空間複雜度\n\n| 操作 | 時間 |\n| --- | --- |\n| `find` | $O(\\alpha(n))$ 攤還 $\\times$ 群運算成本 |\n| `unite` | $O(\\alpha(n))$ 攤還 |\n| `query` | $O(\\alpha(n))$ 攤還 |\n\n**與 8.1 完全相同的漸近界**——按大小合併與路徑壓縮都保留了，只是每次跳躍多做一次群運算（一次加法或乘法，$O(1)$）。\n\n**空間**：$O(n)$，比普通並查集多一個權值陣列。權值型別要按群選：整數差用 `long long`（防溢位），比值用 `double`，模 $k$ 用 `int`。\n\n**遞迴深度**：本節推薦遞迴版 `find`（迭代版要兩趟，見下），深度上界即樹高。**有按大小合併時樹高 $\\le \\log_2 n$**（8.1 定理 2），$n = 10^5$ 時只有 17 層，遞迴完全安全。**沒有按大小合併時樹高可達 $O(n)$，就有爆堆疊風險——這是「帶權並查集一定要按大小合併」的另一個理由。**\n\n**迭代版**（需要時）：第一趟走到根並記錄路徑，第二趟從根往回累加權值並改指標。\n\n```cpp\n    int find(int x) {\n        int r = x;\n        while (fa[r] != r) r = fa[r];\n        long long acc = 0;                  // 從 r 往回走時累積的權值\n        while (fa[x] != r) {\n            int nxt = fa[x];\n            long long nw = w[x] + acc;      // 注意：這裡的累積方向要小心\n            // 兩趟寫法容易寫反，建議直接用遞迴版\n            acc = nw; x = nxt;\n        }\n        return r;\n    }\n```\n**兩趟版的累積方向極易寫反，而且錯了不會崩。除非確定會爆堆疊，否則用遞迴版。**\n\n## 常見錯誤與邊界條件\n\n- **路徑壓縮時忘記更新權值**。這是本節的頭號 bug。改了 `fa[x]` 卻不改 `w[x]`，(W) 立刻失效，答案錯得毫無規律。\n- **`find` 裡兩行順序寫反**。必須是「遞迴 → `w[x] ⊕= w[fa[x]]` → `fa[x] = r`」。先改指標會讓 `w[fa[x]]` 變成根的 $0$，**權值靜靜歸零**。\n- **`unite` 的權值公式方向拼錯**。**唯一可靠的辦法是照定理 2 推導**：寫下 (W) 的要求、代入 $\\mathrm{val}(x) = w[x] + \\mathrm{val}(rx)$、移項。用非對稱例子驗證：$a-b=3$、$b-c=5$ ⇒ $a-c=8$。\n- **按大小合併時忘記把權值取逆**。反向掛（`fa[ry] = rx`）的權值是正向的逆元，不是同一個值。\n- **查詢時忘記先 `find`**。`w[x]` 只有在 `find(x)` 之後才保證是「到根」的值。**`query` 一定要先呼叫兩次 `find`，再讀 `w`。**\n- **不同塊的查詢沒特判**。399 要回 $-1.0$；直接算 `w[x] / w[y]` 會得到一個看似合理的垃圾數字。\n- **變數未出現過**。399 的查詢可能含從未在方程中出現的變數，也要回 $-1.0$。**注意 `"x" / "x"` 若 `x` 未出現，答案仍是 $-1.0$ 而不是 $1.0$。**\n- **自環方程**。$a / a = v$ 且 $v \\ne 1$ 是矛盾；$v = 1$ 是廢話。一般的一致性檢查（$rx = ry$ 分支）會自動處理，**不需要特判，但要確認你的程式真的走進那個分支**。\n- **浮點誤差**。長鏈連乘會放大誤差。**用相對誤差比較，或改存 $\\log$ 把乘法變加法**（模板二）。整數差值群請用 `long long`。\n- **整數溢位**。差值群若 $|v| \\le 10^9$ 且鏈長 $10^5$，累積差可達 $10^{14}$，**必須 `long long`**。\n- **模 $k$ 群忘記處理負數取模**。C++ 的 `%` 對負數回傳負值，要寫 `((a % K) + K) % K`。\n- **想用它維護非群的量**。「路徑上的最大值」「路徑上的元素集合」都不是群（沒有逆元），**帶權並查集做不了**。這類需求要用 LCT 或樹上倍增。\n- **想問絕對值**。連通塊的根可以自由伸縮（定理 3 的 ($\\Rightarrow$) 方向），**絕對值本質上不存在**，除非題目另給錨點。\n\n## 與相似技巧的比較\n\n- **與 8.1 普通並查集**：普通版是本節的 $G = \\{e\\}$（平凡群）特例——所有權值都是單位元，(W) 退化成廢話。**反過來說，本節的所有複雜度分析可以直接沿用 8.1，不必重做。**\n- **與 8.2 擴展域並查集**：由定理 5，擴展域 $\\equiv$ $\\mathbb{Z}_2$ 帶權。**兩類用擴展域（不易錯），多類或需要數值差用帶權。**\n- **與「建圖 + DFS 賦值」**：399 也可以建無向圖，對每個連通塊做一次 DFS，把根設為 $1$ 並沿邊算出所有點的值，之後查詢 $O(1)$。**複雜度 $O(n + m + q)$，比帶權並查集還好，程式也不見得長。** 差別在於：DFS 版是**離線**的（要先收齊所有方程），帶權並查集支援**方程與查詢交錯到來的線上場景**，以及**邊加邊檢查矛盾**（2307）。**題目若是純離線，DFS 往往是更好的選擇——不要為了用工具而用工具。**\n- **與 Floyd 傳遞閉包**：399 的變數只有 $\\le 40$ 個，$O(V^3)$ 的 Floyd（把 $\\times$ 當作路徑合成）完全可行，而且十行寫完。**小規模時它是最省腦力的解。**\n- **與差分約束系統（SPFA／Bellman-Ford）**：差分約束處理的是**不等式** $\\mathrm{val}(x) - \\mathrm{val}(y) \\le c$，帶權並查集只能處理**等式**。**看到不等式就換工具**，那是圖論章的內容。\n- **與線段樹／樹狀陣列**：完全不同的問題域。本節維護的是「元素之間的關係」，不是「區間上的聚合量」。\n- **與 8.4／8.5**：那兩節的 `fa` 根本不表示等價類。**本節的 `fa` 是正統等價類，而且額外掛了權值——它是 8.1 的正向擴展，8.4／8.5 是側向借用。**\n\n## 代表例題\n\n**題號**：399、2307、3710\n\n把等價或連通關係合併到代表元；練習在 union 時同步維護集合資訊。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 399 | [除法求值](https://leetcode.cn/problems/evaluate-division) |  |\n| 2307 | [檢查方程中的矛盾之處](https://leetcode.cn/problems/check-for-contradictions-in-equations)（Premium） |  |\n| 3710 | [最大劃分因子](https://leetcode.cn/problems/maximum-partition-factor) | 2135 |\n\n## 本節重點速查\n\n- **適用形狀**：關係**可傳遞、可逆**（比值、差值、同餘、奇偶）⇒ 權值構成**群** ⇒ 每條 `fa` 邊掛一個群元素。\n- **主不變量 (W)**：$w[x] = \\mathrm{rel}(x, \\texttt{fa}[x])$；`find` 之後 $w[x] = \\mathrm{rel}(x, \\text{root})$。\n- **查詢公式**：同根時 $\\mathrm{rel}(x, y) = w[x] \\ominus w[y]$。**根的值被消掉**——這既是「不需要絕對值」也是「得不到絕對值」的原因。\n- **`find` 的兩行順序**：遞迴 → `w[x] ⊕= w[fa[x]]` → `fa[x] = r`。**寫反會讓權值靜靜歸零**，這是頭號 bug。\n- **`unite` 公式照定理 2 推**：寫下 (W) 的要求、代入 $\\mathrm{val}(x) = w[x] \\oplus \\mathrm{val}(rx)$、移項。**不要憑感覺拼方向**，用 $a-b=3,\\ b-c=5 \\Rightarrow a-c=8$ 驗證。\n- **按大小合併完全相容**，反向掛時取逆元。**不加平衡策略會讓遞迴 `find` 有爆堆疊風險。**\n- **矛盾偵測（2307）**：`unite` 遇到同根就檢查。**只檢查非樹邊就夠**——構造性證明：樹邊由 (W) 自動成立，非樹邊逐一驗過（定理 4）。\n- **399 的 $-1$ 情形**：不同塊、或變數未出現過。**($\\Rightarrow$) 方向要靠「整塊同乘 $t$」的構造證明資訊真的不足**，不是演算法弱。\n- **浮點就改存 $\\log$**，把乘法群換成加法群，誤差不再相乘放大；整數差用 `long long`。\n- **只有兩類就用 8.2 的擴展域**（定理 5 證明兩者等價，但擴展域不易錯）；**三類以上或要數值差才用帶權**。\n- **純離線的 399 用「建圖 + DFS 賦值」更好**（$O(n+m+q)$，且不必維護權值）。帶權並查集的真正價值在**線上**與**邊加邊檢查矛盾**。\n- **做不到的事**：絕對值、不等式（用差分約束）、非群的量如路徑最大值（用倍增或 LCT）。\n',
        },
      ],
    },
    {
      id: 60,
      title: "9. 樹狀陣列和線段樹",
      src: "https://leetcode.cn/circle/discuss/mOr1u6/",
      summary:
        "字首和只能應付「不會變的陣列」，差分只能應付「最後才查一次」。當**修改與查詢交錯出現**時，就輪到本章：樹狀陣列與線段樹把兩者都壓到 `O(log n)`。\n\n能用樹狀陣列解決的題目，也能用線段樹解決（反過來不一定）。但樹狀陣列實現簡單、程式碼短，所以這裡把適合樹狀陣列的題目分到樹狀陣列中，其餘分到線段樹中。\n\n**如何挑子單元**\n\n- **9.1 樹狀陣列**：只需要「單點修改 + 前綴查詢」，而且合併方式可以相減（總和、計數）。最短最好寫，能用就用。\n- **9.2 逆序對**：樹狀陣列最經典的應用。先把值域離散化，再從一端掃過去，邊查「已經看過幾個比我大的」邊插入。\n- **9.3 線段樹（無區間更新）**：合併方式**不能相減**時（max、min、gcd、區間最大子段和），樹狀陣列就不夠用了。\n- **9.4 Lazy 線段樹**：出現「把一整段都加上 v」「把一整段都設成 v」這類**區間修改**，就要用懶標記把修改暫存在節點上，等真正需要往下走時才下推。\n- **9.5 動態開點線段樹**：值域極大（如 `1e9`）但實際用到的點很少時，不預先建滿整棵樹，走到才建節點。也可先做離散化再用普通線段樹。\n- **9.6 可持久化線段樹**：要查詢**歷史版本**，或用「第 r 版減第 l−1 版」回答區間第 K 小。\n- **9.7 ST 表**：陣列完全不修改、但要大量查詢區間最值。預處理 `O(n log n)`，之後每次查詢 `O(1)`，比線段樹更快。\n\n**選擇順序**：先問「要不要區間修改」，再問「合併能不能相減」，最後才問「值域大小與是否需要歷史版本」。",
      children: [
        {
          id: 61,
          title: "9.1 樹狀陣列",
          summary:
            "## 這個技術解決什麼問題\n\n「單點修改」與「前綴查詢」這兩件事，各自都有 $O(1)$ 的做法，但**互相衝突**：\n\n| 表示法 | 單點修改 | 前綴和查詢 |\n| --- | --- | --- |\n| 原陣列 | $O(1)$ | $O(n)$ |\n| 前綴和陣列（第 2 章） | $O(n)$ | $O(1)$ |\n| **樹狀陣列** | $O(\\log n)$ | $O(\\log n)$ |\n\n前綴和之所以改得慢，是因為**一個位置的變動會影響它後面的每一個前綴和**——資訊被儲存了 $n$ 次。樹狀陣列的想法是：**只儲存 $O(\\log n)$ 個「精心挑選的區段和」，讓任何前綴都能由其中 $O(\\log n)$ 段拼出，而任何位置也只落在 $O(\\log n)$ 段裡面。**\n\n挑選的規則出奇簡單：**`tree[i]` 負責的區段長度就是 `lowbit(i) = i & -i`（$i$ 的最低位 1 所代表的權重）。** 整個資料結構就這一條規則，其餘都是它的推論。\n\n**它真正的價值不只是「可修改的前綴和」，更是「值域桶」**：把值離散化成下標，`add(v, 1)` 表示「見過一個 $v$」，`query(v-1)` 就是「已見過的數中有幾個小於 $v$」。**一邊掃陣列一邊查詢與插入，就把「對每個元素數前面有多少個比它小」這類 $O(n^2)$ 的計數壓成 $O(n\\log n)$**——9.2 整節都在講這件事，而 2519、1409、2179 等題都是它的變體。\n\n**辨識訊號**：\n- 單點改 + 前綴／區間**求和**（可差分的聚合）反覆交錯 ⇒ 307；\n- 「有多少個數比我小／大」的計數 ⇒ 離散化 + 值域桶；\n- 二維單點改、矩形查 ⇒ 308（兩層樹狀陣列）。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「樹狀陣列」想：\n\n- **需要「單點修改 + 前綴／區間求和」反覆交錯。** 如 307「區域和檢索 - 陣列可修改」，正是經典模板題。\n- **要統計「有多少個數比目前這個小／大」這類計數問題。** 把值離散化後當下標，用樹狀陣列當「桶」邊掃邊查，例如 2519「統計 K-Big 索引的數量」。\n- **二維版本的單點改、矩形查。** 308「二維區域和檢索」就是把一維樹狀陣列套兩層。\n\n:::example 嗅到味道的瞬間：你想做這兩件事，而且交錯很多次\n```cpp\n// 嗅到味道的瞬間：你想做這兩件事，而且交錯很多次\n// update(i, delta);   // 改一個位置\n// rangeSum(l, r);     // 問一段區間和\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要維護的「聚合值」是什麼？（總和、計數…）\n2. 我的下標是不是 1-based？值域要不要先離散化？\n3. 我的查詢能不能寫成「前綴(r) - 前綴(l-1)」？\n\n三題都答得出來，就套對了。\n\n## 核心想法與直覺\n\n本節要解決的場景是：有一個陣列，你需要一邊「把某個位置的數字加上一個值（單點更新）」，一邊「問前面一段的總和是多少（前綴和查詢）」，而且這兩種操作會交錯出現很多次。\n\n先看暴力做法為什麼不夠快。普通的「前綴和陣列」查詢只要 `O(1)`，但只要改動一個元素，後面所有前綴和都要重算，更新就變成 `O(n)`。樹狀陣列（Binary Indexed Tree，又叫 Fenwick Tree）讓更新和查詢都壓到 `O(log n)`。\n\n:::example lowbit：取出 i 在二進位裡「最低位的那個 1」所代表的數值\n核心想法只有一句話：**用每個位置 i 的「二進位最低位的 1」來決定它負責的區間長度。** 我們定義一個輔助運算 `lowbit`：\n\n```cpp\n// lowbit：取出 i 在二進位裡「最低位的那個 1」所代表的數值\nint lowbit(int i) {\n    return i & (-i);  // 例如 i=6(110)，lowbit=2(010)\n}\n```\n:::\n\n\n\n:::example 查詢前綴和：sum(1..i)，沿著 lowbit 一路往左跳\n樹狀陣列 `tree[i]` 不是存單一元素，而是存「以 i 結尾、長度為 `lowbit(i)` 的那一段區間總和」。例如 `tree[6]` 管 `[5, 6]`（長度 2），`tree[8]` 管 `[1, 8]`（長度 8）。這樣任何前綴和都能用「不斷減去 `lowbit` 跳格」拼出來，最多跳 `log n` 次：\n\n```cpp\n// 查詢前綴和：sum(1..i)，沿著 lowbit 一路往左跳\nint query(int i) {\n    int s = 0;\n    for (; i > 0; i -= lowbit(i)) {\n        s += tree[i];  // 每次跳掉一段，最多 log n 段\n    }\n    return s;\n}\n```\n:::\n\n\n\n之所以正確，是因為這些跳到的區間恰好不重不漏地覆蓋 `[1, i]`。整體 n 次操作合計 `O(n log n)`，注意樹狀陣列習慣用 **1-based 索引**（從 1 開始），因為 `lowbit(0)` 是 0 會卡住。\n\n## 狀態／資料結構定義\n\n**核心定義（一維，1-indexed）**：\n$$\\texttt{tree}[i] = \\sum_{j = i - \\mathrm{lowbit}(i) + 1}^{i} a[j], \\qquad \\mathrm{lowbit}(i) = i \\mathbin{\\&} (-i).$$\n即 `tree[i]` 存的是**左開右閉區間 $(\\,i - \\mathrm{lowbit}(i),\\ i\\,]$ 的和**。\n\n**下標必須從 1 開始。** $\\mathrm{lowbit}(0) = 0$，`0` 會讓查詢迴圈永遠停在原地或讓更新迴圈死循環。**這不是風格問題，是正確性問題。**\n\n```cpp\nstruct Fenwick {\n    int n;\n    vector<long long> t;\n    explicit Fenwick(int n) : n(n), t(n + 1, 0) {}\n\n    void add(int i, long long v) {            // a[i] += v\n        for (; i <= n; i += i & -i) t[i] += v;\n    }\n    long long sum(int i) const {              // a[1] + ... + a[i]\n        long long s = 0;\n        for (; i > 0; i -= i & -i) s += t[i];\n        return s;\n    }\n    long long range(int l, int r) const {     // a[l] + ... + a[r]\n        return sum(r) - sum(l - 1);\n    }\n};\n```\n\n**兩個迴圈的方向恰好相反，這不是巧合**：查詢是「把 $[1, i]$ 拆成幾段」（往下走），更新是「找出所有覆蓋位置 $i$ 的段」（往上走）。定理 2 與定理 3 分別證明兩者。\n\n**$O(n)$ 建樹**（比 $n$ 次 `add` 的 $O(n\\log n)$ 快，且程式一樣短）：\n\n```cpp\n    void build(const vector<long long>& a) {  // a 為 1-indexed\n        for (int i = 1; i <= n; ++i) {\n            t[i] += a[i];\n            int j = i + (i & -i);\n            if (j <= n) t[j] += t[i];\n        }\n    }\n```\n\n**三種常見變體（都是同一個結構換個解讀）：**\n\n| 需求 | 做法 |\n| --- | --- |\n| 單點改、前綴查 | 直接用（上面的定義） |\n| **區間改、單點查** | 樹狀陣列存**差分陣列** $d$；`add(l, v); add(r+1, -v)`，單點值 $= \\texttt{sum}(i)$ |\n| **區間改、區間查** | 開**兩個**樹狀陣列，維護 $\\sum d[i]$ 與 $\\sum i\\cdot d[i]$ |\n| 二維單點改、矩形查 | `tree[i][j]` 覆蓋 $(i-\\mathrm{lowbit}(i), i] \\times (j-\\mathrm{lowbit}(j), j]$，兩層迴圈 |\n\n**樹狀陣列上二分（求第 $k$ 小，$O(\\log n)$ 而非 $O(\\log^2 n)$）**——值域桶場景極常用：\n\n```cpp\n    int kth(long long k) const {              // 最小的 i 使 sum(i) >= k\n        int pos = 0;\n        for (int pw = 1 << __lg(max(n, 1)); pw > 0; pw >>= 1) {\n            if (pos + pw <= n && t[pos + pw] < k) {\n                pos += pw;\n                k -= t[pos];\n            }\n        }\n        return pos + 1;\n    }\n```\n**它之所以能work，是因為 `tree` 的下標結構恰好就是一棵完全二元樹的層次編號**：從高位往低位試著「往右跳」，每次跳躍讀的正好是一個完整段。\n\n## 不變量或正確性證明\n\n以下把 $i$ 的二進位展開寫成 $i = 2^{b_1} + 2^{b_2} + \\cdots + 2^{b_k}$，其中 $b_1 > b_2 > \\cdots > b_k \\ge 0$。於是 $\\mathrm{lowbit}(i) = 2^{b_k}$。\n\n### 定理 1（結構不變量）\n\n**不變量 (F)**：任何時刻，對每個 $i \\in [1, n]$，\n$$\\texttt{tree}[i] = \\sum_{j \\in (\\,i - \\mathrm{lowbit}(i),\\ i\\,]} a[j].$$\n\n`add` 與 `build` 的任務就是維護 (F)，`sum` 的正確性完全建立在 (F) 之上。**下面三個定理分別處理「拆得對」「改得全」「維護得住」。**\n\n### 定理 2（`sum` 的下標序列恰好切出 $[1, i]$ 的一個分割）\n\n*命題*：令 $i_0 = i$、$i_{m+1} = i_m - \\mathrm{lowbit}(i_m)$，直到 $i_k = 0$。則區間族\n$$\\big(i_1, i_0\\big],\\ \\big(i_2, i_1\\big],\\ \\dots,\\ \\big(i_k, i_{k-1}\\big] = \\big(0, i_{k-1}\\big]$$\n**兩兩不交，且聯集恰為 $(0, i]$**；而 `tree[i_m]` 覆蓋的正是 $(i_{m+1}, i_m]$。項數 $k = \\mathrm{popcount}(i)$。\n\n*證明*。\n- 「減去 lowbit」就是**清掉最低位的 1**，故 $i_m = 2^{b_1} + \\cdots + 2^{b_{k-m}}$，序列在 $k$ 步後歸零 ⇒ 項數 $= k = \\mathrm{popcount}(i)$ ✔。\n- 由定義，`tree[`$i_m$`]` 覆蓋 $(\\,i_m - \\mathrm{lowbit}(i_m),\\ i_m\\,] = (i_{m+1}, i_m]$ ✔。\n- 這些區間**首尾相接**（前一段的左端點就是下一段的右端點），故不交且**望遠鏡式地**併成 $(i_k, i_0] = (0, i]$ ✔。$\\square$\n\n**「首尾相接」這個觀察就是整個查詢迴圈的全部內容。** 不需要記憶任何位運算技巧——只要記得「減 lowbit 等於清掉最低位的 1」，分割性質自動成立。\n\n**推論 2.1**：$k = \\mathrm{popcount}(i) \\le \\lfloor\\log_2 i\\rfloor + 1$，故 `sum` 是 $O(\\log n)$。\n\n### 定理 3（`add` 的下標序列恰好是所有覆蓋 $x$ 的段）\n\n*命題*：位置 $x$ 被 `tree[j]` 覆蓋（即 $j - \\mathrm{lowbit}(j) < x \\le j$）**當且僅當** $j$ 出現在序列 $j_0 = x$、$j_{m+1} = j_m + \\mathrm{lowbit}(j_m)$ 中（且 $j \\le n$）。\n\n*證明*。分三步。\n\n**(a) $x$ 本身覆蓋 $x$**：$x - \\mathrm{lowbit}(x) < x \\le x$ ✔。\n\n**(b) 若 $j$ 覆蓋 $x$，則 $(j, j + \\mathrm{lowbit}(j))$ 之間沒有任何下標覆蓋 $x$。**\n設 $\\mathrm{lowbit}(j) = 2^b$，取 $j' = j + t$ 且 $0 < t < 2^b$。則 $\\mathrm{lowbit}(j') = \\mathrm{lowbit}(t) \\le t$，於是\n$$j' - \\mathrm{lowbit}(j') = j + \\big(t - \\mathrm{lowbit}(t)\\big) \\ge j \\ge x,$$\n不滿足「$<x$」，故 $j'$ 不覆蓋 $x$ ✔。（最後一步用到 $j \\ge x$，這由「$j$ 覆蓋 $x$」給出。）\n\n**(c) $j'' = j + \\mathrm{lowbit}(j)$ 覆蓋 $x$。**\n寫 $j = m\\cdot 2^b$（$m$ 為奇數，$2^b = \\mathrm{lowbit}(j)$），則 $j'' = (m+1)\\cdot 2^b$ 且 $m+1$ 為偶數，故 $\\mathrm{lowbit}(j'') \\ge 2^{b+1}$。於是\n$$j'' - \\mathrm{lowbit}(j'') \\le j'' - 2^{b+1} = j + 2^b - 2^{b+1} = j - 2^b = j - \\mathrm{lowbit}(j) < x,$$\n又 $j'' > j \\ge x$，兩個條件都滿足 ✔。\n\n由 (a)(b)(c)，從 $x$ 出發反覆「加 lowbit」，**不重不漏地列舉出所有覆蓋 $x$ 的下標**，直到超出 $n$ ✔。$\\square$\n\n**這個定理是樹狀陣列最容易被跳過、卻最值得看的一段。** 大多數教材只說「加 lowbit 就對了」；(b) 才是「不漏」的保證，(c) 才是「不重」與「連續」的保證。**沒有 (b)，你無法排除「中間某個下標也覆蓋了 $x$ 但被跳過」的可能。**\n\n**推論 3.1（`add` 是 $O(\\log n)$）**：由 (c) 的推導，$\\mathrm{lowbit}(j'') \\ge 2\\,\\mathrm{lowbit}(j)$——**每一步 lowbit 至少加倍**，故不超過 $\\lfloor\\log_2 n\\rfloor + 1$ 步 ✔。\n\n**推論 3.2（(F) 被 `add` 正確維護）**：`a[x] += v` 後，需要且僅需要把 $v$ 加到所有覆蓋 $x$ 的 `tree[j]` 上——正是定理 3 列舉的那些 ✔。\n\n### 定理 4（$O(n)$ 建樹的正確性）\n\n*命題*：`build` 執行完畢後 (F) 成立。\n\n*證明（對 $i$ 歸納）*。定義 `tree[i]` 的**直接子節點**為所有 $j < i$ 滿足 $j + \\mathrm{lowbit}(j) = i$。由定理 2 的分割性質，$(i - \\mathrm{lowbit}(i), i]$ 恰好被 $\\{a[i]\\} \\cup \\bigcup_{j \\text{ 為 } i \\text{ 的子節點}} (j - \\mathrm{lowbit}(j), j]$ 不交地覆蓋。\n\n迴圈按 $i$ 遞增執行，處理到 $i$ 時：所有子節點 $j < i$ 都已被處理完畢（歸納假設 ⇒ `t[j]` 已滿足 (F)），且在處理 $j$ 時已把 `t[j]` 累加進 `t[i]`。再加上本輪的 `a[i]`，`t[i]` 即滿足 (F) ✔。$\\square$\n\n**注意迴圈裡是 `t[j] += t[i]` 而不是 `t[j] += a[i]`——因為 `t[i]` 此刻已經是完整的段和。** 寫成 `a[i]` 是常見的抄錯。\n\n### 定理 5（樹狀陣列只能處理「可差分」的聚合）\n\n*命題*：`range(l, r) = sum(r) - sum(l - 1)` 成立的前提是聚合運算構成**群**（有逆元）。\n\n*證明*。$\\mathrm{sum}(r) = \\mathrm{sum}(l-1) \\oplus \\big(a[l] \\oplus \\cdots \\oplus a[r]\\big)$ 要能解出中間項，必須能對 $\\mathrm{sum}(l-1)$ 取逆 ✔。$\\square$\n\n**這條界線決定了整章的分工**：\n\n| 聚合 | 有逆元？ | 樹狀陣列 |\n| --- | --- | --- |\n| 加法、XOR | 是 | ✅ 可以 |\n| 乘法（非零） | 是 | ✅ 可以 |\n| **max / min / gcd** | **否** | ❌ **要用線段樹（9.3）** |\n\n**（技術補充：只做「單點改成更大的值 + 查前綴 max」時，樹狀陣列仍可用——因為那不需要差分，只需要 `tree[i] = max(...)` 且修改是單調的。但一旦有「改小」或要查任意區間 max，就必須換線段樹。這個特例在 LIS 類 DP 中很常見，別忘了它。）**\n\n## 逐步演算法\n\n樹狀陣列的實作非常短，記住「更新往右跳、查詢往左跳」即可。\n\n:::example 狀態表示：tree[i] 存以 i 結尾、長度 lowbit(i) 的區間和\n**1. 狀態表示：開一個 1-based 陣列。** 大小要比元素個數多 1，所有下標從 1 開始。\n\n```cpp\n// 狀態表示：tree[i] 存以 i 結尾、長度 lowbit(i) 的區間和\nvector<long long> tree;  // 1-based，tree[0] 不用\nint n;                   // 元素個數\n```\n:::\n\n\n\n:::example 單點更新：把第 i 個位置加上 delta，向右更新所有受影響區間\n**2. 單點更新：往右跳，沿途每段都加上 delta。** 把位置 i 加上 delta，會影響所有「包含 i」的區間，這些區間的下標是 `i += lowbit(i)`。\n\n```cpp\n// 單點更新：把第 i 個位置加上 delta，向右更新所有受影響區間\nvoid update(int i, long long delta) {\n    for (; i <= n; i += lowbit(i)) {\n        tree[i] += delta;\n    }\n}\n```\n:::\n\n\n\n:::example 區間查詢：閉區間 [l, r] 的和 = 前綴(r) - 前綴(l-1)\n**3. 前綴查詢：往左跳，累加沿途各段。** 前面已示範 `query(i)` 回傳 `sum(1..i)`。要查區間 `[l, r]` 就用 `query(r) - query(l - 1)`。\n\n```cpp\n// 區間查詢：閉區間 [l, r] 的和 = 前綴(r) - 前綴(l-1)\nlong long rangeSum(int l, int r) { return query(r) - query(l - 1); }\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 一定用 1-based；若原資料是 0-based，更新查詢時記得整體 +1。\n- 值域很大但元素不多時（例如值高達 1e9），先做**離散化**：把出現過的值排序去重，用「排名」當下標。\n- 累加可能溢位，計數和用 `long long`。\n- 複雜度：單次更新與查詢皆 `O(log n)`，建表 `O(n log n)`，空間 `O(n)`。\n\n## C++17 模板\n\n以下用本節代表題 307. 區域和檢索 - 陣列可修改 展示核心寫法。\n\n**完整題目**：設計可修改陣列，支援單點更新與區間和查詢。\n\n:::example 單點更新：在 1-indexed 位置 i+1 加上 delta，沿 lowbit 路徑…\n**線索**：題目同時有更新與區間/前綴查詢；若值域大，先離散化再用樹狀結構維護統計。\n\n```cpp\nclass NumArray {\n    int n;\n    vector<int> bit, nums;  // bit：樹狀陣列（1-indexed），nums：原始值\n    // 單點更新：在 1-indexed 位置 i+1 加上 delta，沿 lowbit 路徑向上更新\n    void add(int i, int delta) {\n            for (++i; i <= n; i += i & -i) {\n                    bit[i] += delta;\n            }\n    }\n    // 前綴和查詢：計算 [0, i] 的總和，沿 lowbit 路徑向下累加\n    int sumPrefix(int i) {\n            int s = 0;\n            for (++i; i > 0; i -= i & -i) {\n                    s += bit[i];\n            }\n            return s;\n    }\n\npublic:\n    NumArray(vector<int>& a) : n(a.size()), bit(n + 1), nums(a) {\n            for (int i = 0; i < n; ++i) {\n                    add(i, nums[i]);  // 建樹：逐一插入初始值\n            }\n    }\n    void update(int index, int val) {\n            add(index, val - nums[index]);  // 只加差值，避免重複計算\n            nums[index] = val;\n    }\n    int sumRange(int left, int right) {\n            return sumPrefix(right) - (left ? sumPrefix(left - 1) : 0);\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- 樹狀陣列維護前綴和差分；單點修改影響 O(log n) 個節點，區間和由兩個前綴和相減。\n- `add` 沿著 `i += i & -i` 更新所有包含該點的桶，`sum` 沿著 `i -= i & -i` 收集前綴。\n- 外部下標與內部 1-indexed 下標要分清，離散化後也不要混用原值。\n\n## 時間與空間複雜度\n\n| 操作 | 時間 | 說明 |\n| --- | --- | --- |\n| `add` | $O(\\log n)$ | 推論 3.1：lowbit 每步至少加倍 |\n| `sum` | $O(\\log n)$ | 推論 2.1：迭代次數 $=\\mathrm{popcount}(i)$ |\n| `range` | $O(\\log n)$ | 兩次 `sum` |\n| `kth`（樹上二分） | $O(\\log n)$ | **不是 $O(\\log^2 n)$** |\n| 建樹 | $O(n)$ | 定理 4 |\n| 空間 | $O(n)$ | **只有一個陣列**，無指標、無節點物件 |\n| 二維 `add`／`query` | $O(\\log n\\log m)$ | 空間 $O(nm)$ |\n\n**常數是樹狀陣列真正的賣點。** 與線段樹比較（同為 $O(\\log n)$）：\n\n| | 樹狀陣列 | 線段樹 |\n| --- | --- | --- |\n| 程式行數 | ~10 | ~60 |\n| 空間 | $n$ | $4n$（遞迴版） |\n| 實測速度 | **基準** | 約慢 2～4 倍 |\n| 每次操作的實際迭代 | $\\mathrm{popcount}(i)$，**平均只有 $\\log n / 2$** | 完整 $\\log n$ 層遞迴 |\n| 快取行為 | 好（單一陣列、下標局部性高） | 差（遞迴 + 跳躍） |\n\n**平均迭代次數是 $\\log_2 n$ 的一半**（隨機下標的 popcount 期望值），這是樹狀陣列常數小的主要來源之一。\n\n**各題的實際複雜度**：\n\n| 題目 | 時間 | 瓶頸 |\n| --- | --- | --- |\n| 307 區域和檢索 | $O(n + q\\log n)$ | — |\n| 308 二維區域和檢索 | $O(nm + q\\log n\\log m)$ | 二維 |\n| 2519 統計 K-Big 索引 | $O(n\\log n)$ | **兩個樹狀陣列各掃一遍** |\n| 1409 查詢帶鍵的排列 | $O((n+m)\\log(n+m))$ | 需預留前置空間 |\n\n**空間陷阱**：二維樹狀陣列是 $O(nm)$，$1000\\times1000$ 的 `long long` 就是 8 MB——**還可以，但 $5000\\times5000$ 就爆了**。值域桶則務必**先離散化**：值到 $10^9$ 直接開陣列必然 MLE。\n\n## 常見錯誤與邊界條件\n\n- 樹狀陣列常用 1-indexed。\n- `i += i & -i` 更新，`i -= i & -i` 查詢。\n- 線段樹懶標記下推時機要一致。\n- 離散化後不要混用原座標與壓縮座標。\n\n## 常見變形\n\n- 樹狀陣列：單點加、前綴和。\n- 線段樹：區間查詢、區間修改、懶標記。\n- 掃描線 + 樹狀陣列/線段樹統計二維偏序。\n\n## 與相似技巧的比較\n\n- **與前綴和（第 2 章）**：**陣列不變就用前綴和**，$O(1)$ 查詢，沒有理由用樹狀陣列。**只要出現一次修改，前綴和就崩了。** 判準只有一句：資料會不會變。\n- **與線段樹（9.3）**：分界線就是**定理 5**——聚合有沒有逆元。求和／XOR 用樹狀陣列（短、快、省），max／min／gcd 或「節點要維護複合資訊」（2213 那種前綴段／後綴段／最長段）只能用線段樹。**樹狀陣列能做的事線段樹都能做，反之不然；但能用樹狀陣列時就別寫線段樹。**\n- **與 Lazy 線段樹（9.4）**：樹狀陣列的「區間改」是靠差分硬湊出來的，**只支援區間加**（可差分的修改）；區間**賦值**（把一段全設為 $v$）樹狀陣列做不到，必須 lazy 線段樹。\n- **與平衡樹／`pb_ds` order-statistics tree**：後者直接支援 `order_of_key`／`find_by_order`，**不需要離散化**，且支援刪除任意元素。代價是常數大 3～5 倍。**值域已知且可離散化 ⇒ 樹狀陣列；值域動態或需要真正的有序集合操作 ⇒ 平衡樹。**\n- **與「離線 + 排序」**：很多需要樹狀陣列的線上問題，離線後按值排序就能用更簡單的手段解決（例如雙指標）。**先問一句「這題能不能離線」，常常省掉整個資料結構。**\n- **與歸併排序求逆序對（9.2）**：兩者複雜度相同。歸併版不需要離散化、常數更小；樹狀陣列版更容易改成各種變形（$a_i > 2a_j$、前綴和區間計數）。**9.2 會詳細比。**\n- **與分塊（第 11 章）**：分塊 $O(\\sqrt n)$ 比 $O(\\log n)$ 慢，但**能處理沒有結合律友善性質的怪異操作**。樹狀陣列做不到的、線段樹也做不到的，才輪到分塊。\n- **與差分（第 3 章）**：差分是「離線的、只能查一次最終狀態」的區間改；樹狀陣列 + 差分是「線上的、隨時可查」的區間改。**差分 $O(n)$ 更快，能用就用。**\n\n## 代表例題\n\n**題號**：60、307、308、1756、2519\n\n有動態更新與區間查詢；練習先離散化值域，再確認查詢/更新的閉開區間。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 60 | [排列序列](https://leetcode.cn/problems/permutation-sequence) |  |\n| 307 | [區域和檢索 - 陣列可修改](https://leetcode.cn/problems/range-sum-query-mutable) |  |\n| 308 | [二維區域和檢索 - 可變](https://leetcode.cn/problems/range-sum-query-2d-mutable)（Premium） |  |\n| 1756 | [設計最近使用（MRU）佇列](https://leetcode.cn/problems/design-most-recently-used-queue)（Premium） |  |\n| 2519 | [統計 K-Big 索引的數量](https://leetcode.cn/problems/count-the-number-of-k-big-indices)（Premium） |  |\n| 2613 | [美數對](https://leetcode.cn/problems/beautiful-pairs) |  |\n| 2921 | [價格遞增的最大利潤三元組 II](https://leetcode.cn/problems/maximum-profitable-triplets-with-increasing-prices-ii) |  |\n| 3109 | [查詢排列的下標](https://leetcode.cn/problems/find-the-index-of-permutation) |  |\n| LCP 05 | [發 LeetCoin](https://leetcode.cn/problems/coin-bonus) |  |\n| 1409 | [查詢帶鍵的排列](https://leetcode.cn/problems/queries-on-a-permutation-with-key) | 1335 |\n| 1395 | [統計作戰單位數](https://leetcode.cn/problems/count-number-of-teams) | 1344 |\n| 2653 | [滑動子陣列的美麗值](https://leetcode.cn/problems/sliding-subarray-beauty) | 1786 |\n| 2250 | [統計包含每個點的矩形數目](https://leetcode.cn/problems/count-number-of-rectangles-containing-each-point) | 1998 |\n| 3027 | [人員站位的方案數 II](https://leetcode.cn/problems/find-the-number-of-ways-to-place-people-ii) | 2020 |\n| 1626 | [無矛盾的最佳球隊](https://leetcode.cn/problems/best-team-with-no-conflicts) | 2027 |\n| 3072 | [將元素分配到兩個陣列中 II](https://leetcode.cn/problems/distribute-elements-into-two-arrays-ii) | 2053 |\n| 3624 | [位計數深度為 K 的整數數目 II](https://leetcode.cn/problems/number-of-integers-with-popcount-depth-equal-to-k-ii) | 2086 |\n| 3187 | [陣列中的峰值](https://leetcode.cn/problems/peaks-in-array) | 2154 |\n| 3777 | [使子字串變交替的最少刪除次數](https://leetcode.cn/problems/minimum-deletions-to-make-alternating-substring) | 2202 |\n| 1649 | [透過指令建立有序陣列](https://leetcode.cn/problems/create-sorted-array-through-instructions) | 2208 |\n| 2179 | [統計陣列中好三元組數目](https://leetcode.cn/problems/count-good-triplets-in-an-array) | 2272 |\n| 2659 | [將陣列清空](https://leetcode.cn/problems/make-array-empty) | 2282 |\n| 3515 | [帶權樹中的最短路徑](https://leetcode.cn/problems/shortest-path-in-a-weighted-tree) | 2312 |\n| 1505 | [最多 K 次交換相鄰數位後得到的最小整數](https://leetcode.cn/problems/minimum-possible-integer-after-at-most-k-adjacent-swaps-on-digits) | 2337 |\n| 2926 | [平衡子序列的最大和](https://leetcode.cn/problems/maximum-balanced-subsequence-sum) | 2448 |\n| 2736 | [最大和查詢](https://leetcode.cn/problems/maximum-sum-queries) | 2533 |\n| 3590 | [第 K 小的路徑異或和](https://leetcode.cn/problems/kth-smallest-path-xor-sum) | 2646 |\n| 3671 | [子序列美麗值求和](https://leetcode.cn/problems/sum-of-beautiful-subsequences) | 2647 |\n| 3382 | [用點構造面積最大的矩形 II](https://leetcode.cn/problems/maximum-area-rectangle-with-point-constraints-ii) | 2723 |\n| 3245 | [交替組 III](https://leetcode.cn/problems/alternating-groups-iii) | 3112 |\n\n**延伸閱讀**：[LeetCode 307：樹狀陣列的設計動機與證明](https://leetcode.cn/problems/range-sum-query-mutable/solution/dai-ni-fa-ming-shu-zhuang-shu-zu-fu-shu-lyfll/)，建議配合 `lowbit` 覆蓋區間圖理解。\n\n## 本節重點速查\n\n- **一句話定義**：`tree[i]` 存 $(\\,i - \\mathrm{lowbit}(i),\\ i\\,]$ 的和，$\\mathrm{lowbit}(i) = i \\,\\&\\, {-i}$。**整個結構只有這一條規則。**\n- **兩個迴圈方向相反**：查詢 `i -= i & -i`（清掉最低位 1，把 $[1,i]$ 切成 $\\mathrm{popcount}(i)$ 段，望遠鏡式相接——定理 2）；更新 `i += i & -i`（列舉所有覆蓋位置 $i$ 的段，不重不漏——定理 3）。\n- **定理 3 的 (b)(c) 才是重點**：(b) 保證中間沒有被跳過的覆蓋者，(c) 保證下一個確實覆蓋且 **lowbit 至少加倍** ⇒ $O(\\log n)$。\n- **下標必須 1-indexed**。$\\mathrm{lowbit}(0) = 0$ ⇒ 死迴圈或空轉。\n- **$O(n)$ 建樹**：`t[i] += a[i]; t[i + lowbit(i)] += t[i];`。注意加的是 **`t[i]` 不是 `a[i]`**。\n- **定理 5 是整章的分工線**：聚合**有逆元**（加法、XOR）⇒ 樹狀陣列；**沒有**（max/min/gcd）⇒ 線段樹。例外：只做「單點變大 + 查前綴 max」仍可用樹狀陣列（LIS 類 DP 常用）。\n- **最重要的用法不是前綴和，是值域桶**：離散化後 `add(v, 1)` + `query(v-1)` = 「比 $v$ 小的有幾個」，把 $O(n^2)$ 計數壓成 $O(n\\log n)$（9.2 全節）。\n- **三種變體**：區間改單點查（存差分）、區間改區間查（兩個樹狀陣列，維護 $\\sum d$ 與 $\\sum i\\,d$）、二維（兩層 lowbit）。\n- **樹上二分 `kth` 是 $O(\\log n)$**，不要寫成外層二分 + 內層 `sum` 的 $O(\\log^2 n)$。\n- **常數小是真正的賣點**：程式 10 行、空間 $n$、平均迭代只有 $\\log n / 2$，比線段樹快 2～4 倍。\n- **兩個空間陷阱**：值域桶一定要離散化；二維是 $O(nm)$。\n- **陣列不會變就用前綴和**——不要為了用資料結構而用資料結構。\n",
        },
        {
          id: 62,
          title: "9.2 逆序對",
          summary:
            "## 這個技術解決什麼問題\n\n一大類看似不同的題目，其實都是同一個問題：\n\n> **數出滿足「下標有序 + 數值有序」兩個條件的配對數量。**\n\n$$\\#\\{\\,(i, j) : i < j \\ \\text{且}\\ f(a_i, a_j)\\,\\}$$\n\n暴力是雙層迴圈 $O(n^2)$；$n = 10^5$ 就是 $10^{10}$ 次比較。\n\n**核心想法只有一句：兩個條件，一個交給掃描順序，另一個交給資料結構。**\n\n- **下標條件 $i < j$** ⟶ 由左往右掃描，掃到 $j$ 時「已插入資料結構的」恰好就是所有 $i < j$；\n- **數值條件** ⟶ 把值當成下標建**值域桶**（樹狀陣列），一次前綴查詢就得到答案。\n\n這個框架叫**二維偏序**。逆序對（$i<j$ 且 $a_i > a_j$）是它最小的實例，而 315、493、327、2426 都只是換了「數值條件」的寫法：\n\n| 題目 | 數值條件 | 化簡後 |\n| --- | --- | --- |\n| 經典逆序對 | $a_i > a_j$ | 直接查 |\n| 315 右側小於當前元素 | $a_j < a_i$（從右往左掃） | 直接查 |\n| 493 翻轉對 | $a_i > 2a_j$ | 值域要同時含 $a$ 與 $2a$ |\n| 327 區間和的個數 | $\\ell \\le S_j - S_i \\le r$ | $S_j - r \\le S_i \\le S_j - \\ell$ |\n| 2426 滿足不等式的數對 | $a_i - a_j \\le b_i - b_j + d$ | 令 $c = a - b$，查 $c_i \\le c_j + d$ |\n\n**看到「對每個元素，問前面（或後面）有多少個滿足某不等式」，先做的事永遠是移項——把 $i$ 的量搬到一邊、$j$ 的量搬到另一邊。** 移項成功之後，剩下的就是套模板。\n\n**另一個獨立的價值**：逆序對數 $=$ 用**相鄰交換**把序列排好序的最少次數（定理 4）。這讓 1850、2193 這類「最少相鄰交換」的題目瞬間變成計數題。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「逆序對 / 計數樹狀陣列」想：\n\n- **要數「前面比後面大」或「右側比我小」的配對數。** 如 315「計算右側小於當前元素的個數」，把陣列反過來掃就是逆序對的變形。\n- **題目本質是「對每個元素，問前面（或後面）有多少個滿足某不等式」。** 把值離散化成下標，用樹狀陣列邊掃邊查。\n- **閥值帶倍數或前綴和的版本**，如 493「翻轉對」、327「區間和的個數」——先把要比較的量算成可離散化的數列，再套同樣骨架。\n\n:::example 嗅到味道的瞬間：暴力是 O(n^2) 兩兩比較\n```cpp\n// 嗅到味道的瞬間：暴力是 O(n^2) 兩兩比較\n// for (i) for (j>i) if (a[i] > a[j]) ++ans;   // 太慢\n// 改成「離散化 + 邊掃邊用樹狀陣列計數」變 O(n log n)\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要數的「配對條件」寫成不等式長什麼樣？\n2. 我把哪個量離散化、當成桶的下標？\n3. 我掃描方向（左到右 / 右到左）和「先查再插」的順序對不對？\n\n三題都答得出來，就套對了。\n\n## 核心想法與直覺\n\n本節要解決的問題是「逆序對」：給一個陣列，數出有多少組 `(i, j)` 滿足 `i < j` 但 `a[i] > a[j]`——也就是「前面比後面大」的配對數。它是衡量「一個序列有多亂」的常見指標。\n\n暴力做法是兩層迴圈逐對比較，`O(n^2)`，n 一大就超時。關鍵的加速想法是：**從左往右掃，每處理到一個新元素 `a[j]`，只要知道「左邊已經出現過、而且比 `a[j]` 大的數有幾個」，就是以 j 結尾的逆序對數量。** 把每個 j 的答案累加起來就是總數。\n\n:::example 邊掃邊查：以 j 結尾的逆序對 = 已插入的數中比 a[j] 大的個數\n「左邊有幾個比我大」這種「動態插入 + 即時計數」正是樹狀陣列最擅長的。我們把樹狀陣列當成一個「桶」：下標代表值，`tree` 統計每個值出現的次數。掃到 `a[j]` 時，先問「目前桶裡有多少個值大於 `a[j]`」，再把 `a[j]` 自己放進桶。\n\n```cpp\n// 邊掃邊查：以 j 結尾的逆序對 = 已插入的數中比 a[j] 大的個數\n// 已插入總數 - (值 <= a[j] 的個數) = 比 a[j] 大的個數\nans += inserted - bit.query(rank_of(a[j]));  // query 回傳值 <= a[j] 的累計次數\nbit.update(rank_of(a[j]), 1);                // 把 a[j] 放進桶\n```\n:::\n\n\n\n因為值可能很大或有負數，不能直接拿值當下標，要先**離散化**：把所有值排序去重，用它的「排名」（第幾小）當下標。這樣下標就壓進 `[1, n]`，桶才開得下。\n\n另一條等價路線是用「歸併排序」：在合併兩個已排序的半邊時，若右半的元素比左半某些元素小，就一次數出跨越的逆序對。兩種做法都是 `O(n log n)`，本節以樹狀陣列計數法為主。\n\n## 狀態／資料結構定義\n\n**主線做法：離散化 + 樹狀陣列值域桶**\n\n```cpp\n// 通用骨架：計算嚴格逆序對數 #{ i < j : a[i] > a[j] }\nlong long countInversions(vector<int> a) {\n    int n = a.size();\n    vector<int> v(a);                                  // 離散化\n    sort(v.begin(), v.end());\n    v.erase(unique(v.begin(), v.end()), v.end());      // 去重不可省，見定理 3\n    auto rk = [&](int x) {                             // 1-indexed 排名\n        return int(lower_bound(v.begin(), v.end(), x) - v.begin()) + 1;\n    };\n\n    int m = v.size();\n    vector<int> t(m + 1, 0);\n    auto add = [&](int i) { for (; i <= m; i += i & -i) ++t[i]; };\n    auto sum = [&](int i) { int s = 0; for (; i > 0; i -= i & -i) s += t[i]; return s; };\n\n    long long ans = 0;\n    for (int j = 0; j < n; ++j) {\n        int r = rk(a[j]);\n        ans += (j - sum(r));        // 已插入的總數 - (值 <= a[j] 的個數) = 值 > a[j] 的個數\n        add(r);\n    }\n    return ans;\n}\n```\n\n**`ans += j - sum(r)` 這一行值得停下來看**：`j` 是目前已插入的元素個數，`sum(r)` 是其中值 $\\le a_j$ 的個數，相減就是值 $> a_j$ 的個數。**用「總數減補集」比直接查後綴更不容易寫錯**，因為樹狀陣列天生只會算前綴。\n\n**備援做法：歸併排序**\n\n```cpp\nlong long merge_count(vector<int>& a, vector<int>& buf, int l, int r) {\n    if (r - l <= 1) return 0;\n    int mid = (l + r) / 2;\n    long long res = merge_count(a, buf, l, mid) + merge_count(a, buf, mid, r);\n    int p = l, q = mid, k = l;\n    while (p < mid && q < r) {\n        if (a[p] <= a[q]) {                 // <= 保證「嚴格」逆序對；用 < 會多算相等對\n            buf[k++] = a[p++];\n        } else {\n            res += mid - p;                 // a[p..mid) 全都 > a[q]，且下標都更小\n            buf[k++] = a[q++];\n        }\n    }\n    while (p < mid) buf[k++] = a[p++];\n    while (q < r)   buf[k++] = a[q++];\n    copy(buf.begin() + l, buf.begin() + r, a.begin() + l);\n    return res;\n}\n```\n\n**兩者的選擇**：\n\n| | 樹狀陣列 | 歸併排序 |\n| --- | --- | --- |\n| 需要離散化 | **是** | 否 |\n| 常數 | 小 | 更小（無額外 $\\log$ 的 `lower_bound`） |\n| 改成 $a_i > 2a_j$（493） | 值域擴一倍即可 | 要**額外一趟**雙指標 |\n| 改成區間計數（327） | **直接改查詢範圍** | 要兩趟雙指標，繁瑣 |\n| 支援線上（邊插邊查） | **是** | 否（必須離線） |\n| 附帶產出 | 無 | **順便排好序** |\n\n**結論**：只求逆序對數 ⇒ 歸併更短更快；**條件一變複雜（帶倍數、帶區間、帶偏移）就用樹狀陣列**，因為只要改一行查詢範圍。\n\n## 不變量或正確性證明\n\n### 定理 1（掃描 + 值域桶：每個配對恰被計數一次）\n\n**迴圈不變量 (I)**：在處理下標 $j$（即執行 `ans += ...` 那一刻）之前，樹狀陣列中恰好包含集合 $\\{\\,a_i : i < j\\,\\}$（重複值計重）。\n\n*證明*。$j = 0$ 時桶為空 ✔。每輪結尾執行 `add(rk(a[j]))` 恰好把 $a_j$ 加入，故進入下一輪時桶為 $\\{a_i : i < j+1\\}$ ✔。$\\square$\n\n**命題**：`ans` 最終等於 $\\#\\{(i,j) : i<j,\\ a_i > a_j\\}$。\n\n*證明*。固定一對 $(i, j)$ 且 $i < j$。它只可能在處理下標 $j$ 的那一輪被計入（其他輪的查詢與 $j$ 無關）。由 (I)，該輪桶中含 $a_i$，且查詢統計的是「桶中值 $> a_j$ 的個數」，故 $(i,j)$ 被計入 $\\iff a_i > a_j$ ✔。**恰好一次，不重不漏。**$\\square$\n\n**這個證明看起來平凡，但它點出了整個框架賴以成立的前提：「下標條件必須能由掃描順序完全承擔」。** 一旦條件變成 $|i - j| \\le k$（有距離限制）或 $i < j$ 換成三維偏序 $i<j$ 且 $a_i<a_j$ 且 $b_i<b_j$，這個框架就不夠了——前者要加滑動視窗式的刪除，後者要 CDQ 分治。**先確認「掃描順序能不能吃掉一整個維度」，是套用模板前該問的第一句話。**\n\n### 定理 2（歸併排序法：每個逆序對恰在一層被計數一次）\n\n*命題*：`merge_count` 回傳嚴格逆序對數。\n\n*證明*。分兩步。\n\n**(a) 分層不重不漏。** 對任一逆序對 $(i, j)$（$i<j$，$a_i > a_j$），遞迴樹上存在**唯一**一個節點 $[l, r)$ 使得 $i, j$ 都在其中、但分屬左右兩半（即 $l \\le i < \\text{mid} \\le j < r$）——就是 $i$ 與 $j$ 的「最近公共區間」。在該節點以外的節點裡，兩者要嘛不同時出現，要嘛在同一半（此時計數交給更深的遞迴）✔。\n\n**(b) 單層計數正確。** 考慮某次 `merge`，此時左半 $a[l..\\text{mid})$ 與右半 $a[\\text{mid}..r)$ **各自已排序**（歸納假設）。當演算法因 `a[p] > a[q]` 而取走右半的 $a_q$ 時：\n- 左半剩餘的 $a[p], a[p+1], \\dots, a[\\text{mid}-1]$ **全部 $> a_q$**（左半已排序，$a[p]$ 是其中最小者）；\n- 它們的**原始下標全部 $<$ $a_q$ 的原始下標**（左半整體位於右半之前）。\n\n故這 $\\text{mid} - p$ 個配對全是逆序對，且每個都只在此刻被數一次 ✔。反之，被跳過的（`a[p] <= a[q]` 分支）確實不構成逆序對 ✔。$\\square$\n\n**(b) 有兩個前提，都必須點名**：\n1. **左右兩半各自已排序**——這由遞迴保證。若順序被破壞（例如你在 merge 中途又去讀原陣列），計數立刻失效。\n2. **「左半整體在右半之前」**——這由陣列的物理佈局保證。**這也是為什麼歸併版天生離線**：它靠位置關係表達下標順序，一旦允許插入新元素就崩了。\n\n**相等元素的處理**：`a[p] <= a[q]` 取左邊 ⇒ 相等對**不**計入（嚴格逆序對）。改成 `a[p] < a[q]` 會把相等對也算進去。**題目問「嚴格大於」還是「大於等於」，就差這一個字元。**\n\n### 定理 3（離散化保序，且必須去重）\n\n*命題*：設 $\\phi$ 為把值映到排名的映射。若 $\\phi$ **嚴格保序**（$x < y \\iff \\phi(x) < \\phi(y)$），則對 $\\phi(a)$ 計數與對 $a$ 計數結果相同。\n\n*證明*。逆序對的定義只用到值之間的「$>$」比較，而嚴格保序映射保持所有比較結果 ✔。$\\square$\n\n**推論（為什麼 `unique` 不能省）**：若用「排序後的位置」而非「去重後的排名」當作 $\\phi$，相同的值會得到**不同**的下標，$\\phi$ 就不再滿足 $x = y \\Rightarrow \\phi(x) = \\phi(y)$。此時 `sum(r)` 會漏掉部分相等元素，**相等對被誤判成逆序對**。\n\n具體反例：$a = [2, 2]$。正確答案 0。若把兩個 `2` 分別映成排名 1 和 2，掃到 $j=1$ 時查「值 $>$ 排名 2」得 0，看似沒事；但 $a = [2, 2, 1]$ 這種帶重複又帶比較的資料就會出錯。**養成 `sort` + `unique` + `lower_bound` 三件套的習慣，不要自己發明離散化。**\n\n### 定理 4（相鄰交換次數的下界與上界都等於逆序對數）\n\n*命題*：把序列 $a$ 用**相鄰元素交換**變成非遞減序列，最少需要 $\\mathrm{inv}(a)$ 次，其中 $\\mathrm{inv}$ 為逆序對數。\n\n*證明*。\n- **下界**：一次相鄰交換只改變**一對**元素的相對順序（其餘所有配對的相對順序不變），故 $\\mathrm{inv}$ 每次至多變化 $1$。目標狀態 $\\mathrm{inv} = 0$，故至少需要 $\\mathrm{inv}(a)$ 次 ✔。\n- **上界**：若 $\\mathrm{inv}(a) > 0$，則**必存在相鄰的逆序對**（否則 $a_1 \\le a_2 \\le \\cdots \\le a_n$，$\\mathrm{inv} = 0$，矛盾）。交換它使 $\\mathrm{inv}$ 恰好減 1。反覆執行（即氣泡排序）$\\mathrm{inv}(a)$ 步後到達有序 ✔。$\\square$\n\n**上下界都達到，等號成立。** 這個定理把一整類「最少相鄰交換」的題目（1850、2193）化成計數題。\n\n**注意「必存在相鄰逆序對」這一步**——它是上界論證的關鍵，而且是反證法。**很多人只證了下界就以為證完了；下界只說明「不可能更少」，不保證「真的做得到」。**\n\n**有重複元素時的推廣（1850、2193 都會遇到）**：目標序列若含相同字元，要把 $a$ 變成指定的目標串 $t$，**最少相鄰交換數 $=$「把 $a$ 的每個位置對應到 $t$ 中同字元位置」所有匹配方案裡逆序對最少的那個**；而**最優匹配一定是「同字元按出現順序一一對應」**（否則交換兩個同字元的對應可使逆序對不增），於是可以貪心地做。\n\n### 定理 5（493 翻轉對的兩個陷阱）\n\n*問題*：數 $\\#\\{(i,j) : i<j,\\ a_i > 2a_j\\}$。\n\n**陷阱一（樹狀陣列版）：值域必須同時包含 $\\{a_i\\}$ 與 $\\{2a_j\\}$。**\n查詢時要問「桶中有幾個值 $> 2a_j$」，而 $2a_j$ **不一定出現在原陣列中**。若只離散化 $\\{a_i\\}$，`lower_bound(2*a[j])` 會落在錯誤的排名上。**正確做法是把 $\\{a_i\\} \\cup \\{2a_i\\}$ 合併後一起離散化**（也可以只離散化 $\\{a_i\\}$，但查詢時用 `upper_bound(2*a[j])` 取得「第一個 $> 2a_j$ 的排名」——兩種寫法都對，混著寫就錯）。\n\n**陷阱二（歸併版）：計數必須在 merge 之前用獨立的雙指標完成。**\nmerge 的比較條件是 $a_p \\le a_q$，與 $a_p > 2a_q$ **不是同一個條件**，不能共用同一組指標。正確結構是：\n\n```cpp\n    // 先數，再併\n    for (int p = l, q = mid; q < r; ++q) {\n        while (p < mid && (long long)a[p] <= 2LL * a[q]) ++p;\n        res += mid - p;                    // a[p..mid) 都 > 2*a[q]\n    }\n    // 然後才做標準 merge\n```\n**這一趟是 $O(r - l)$ 的**：$q$ 遞增時 $2a_q$ 遞增（右半已排序），故 $p$ **單調不減**，兩個指標各只走一遍 ✔。**單調性來自「右半已排序」，這是雙指標合法的唯一前提。**\n\n**陷阱三（兩種版本共有）：溢位。** $a_i$ 可到 $2^{31}-1$，$2a_j$ 就超出 `int`。**`2LL * a[q]`，不是 `2 * a[q]`。**\n\n### 定理 6（327 區間和的個數：移項與 $S_0$）\n\n*問題*：數 $\\#\\{(i,j) : i \\le j,\\ \\ell \\le a_i + \\cdots + a_j \\le r\\}$。\n\n**化簡**：令 $S_0 = 0$、$S_k = a_1 + \\cdots + a_k$。則子陣列 $[i, j]$ 的和 $= S_j - S_{i-1}$，於是\n$$\\ell \\le S_j - S_{i-1} \\le r \\iff S_j - r \\le S_{i-1} \\le S_j - \\ell,$$\n問題變成：**對每個 $j$，數前綴和序列中下標 $< j$ 的項有幾個落在 $[S_j - r,\\ S_j - \\ell]$ 內。** 這正是定理 1 的框架，只是查詢從「前綴」變成「區間」（兩次前綴相減）。\n\n**兩個必踩的坑：**\n1. **$S_0 = 0$ 必須被插入桶中**。子陣列 $[1, j]$ 對應 $i - 1 = 0$；漏掉 $S_0$ 就會少算所有從頭開始的子陣列。**掃描迴圈要在處理 $S_1$ 之前先 `add(rk(0))`。**\n2. **離散化的值域要含 $\\{S_k\\} \\cup \\{S_k - \\ell\\} \\cup \\{S_k - r\\}$**（共 $3(n+1)$ 個），否則查詢邊界對不上。\n3. **`long long`**：$n \\le 10^5$、$|a_i| \\le 2^{31}-1$ ⇒ $|S_k|$ 可達 $2\\times10^{14}$。\n\n**「先移項、再確認值域、最後補哨兵」是這一類題目的固定順序。** 三步任一步漏掉，答案都只會差一點點——**而「差一點點」是最難 debug 的錯誤**。\n\n## 逐步演算法\n\n:::example 離散化：sorted 是去重排序後的值，rank_of 回傳 1-based 排名\n**1. 離散化：把值換成排名。** 排序去重後，用二分找出每個值的下標。\n\n```cpp\n// 離散化：sorted 是去重排序後的值，rank_of 回傳 1-based 排名\nvector<int> sorted(a);\nsort(sorted.begin(), sorted.end());\nsorted.erase(unique(sorted.begin(), sorted.end()), sorted.end());\nauto rank_of = [&](int x) {\n    return lower_bound(sorted.begin(), sorted.end(), x) - sorted.begin() + 1;\n};\n```\n:::\n\n\n\n:::example 計數桶：update(r,1) 表示插入一個排名 r 的值\n**2. 準備一個計數用的樹狀陣列。** `tree[r]` 代表「排名為 r 的值出現幾次」，支援單點 +1 與前綴計數。\n\n```cpp\n// 計數桶：update(r,1) 表示插入一個排名 r 的值\nvector<int> tree;  // 1-based\nint lowbit(int i) { return i & (-i); }\nvoid update(int i, int v) {\n    for (; i < (int)tree.size(); i += lowbit(i)) {\n        tree[i] += v;\n    }\n}\nint query(int i) {\n    int s = 0;\n    for (; i > 0; i -= lowbit(i)) {\n        s += tree[i];\n    }\n    return s;\n}\n```\n:::\n\n\n\n:::example 主流程：累加每個位置貢獻的逆序對\n**3. 從左到右掃描，邊累加答案邊插入。** 對每個元素，先數「比它大的已插入個數」，再把它插入。\n\n```cpp\n// 主流程：累加每個位置貢獻的逆序對\nlong long ans = 0;\nfor (int j = 0; j < (int)a.size(); ++j) {\n    int r = rank_of(a[j]);\n    ans += query((int)sorted.size()) - query(r);  // 已插入中排名 > r 的數量\n    update(r, 1);                                 // 插入 a[j]\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 注意「逆序對」是嚴格大於還是大於等於：若要嚴格 `a[i] > a[j]`，相等的值不算，用 `query(總數) - query(r)`（排除自己這檔）即可正確。\n- 變形題（如 493「翻轉對」要求 `a[i] > 2 * a[j]`）查詢的閥值不同，但「離散化 + 計數」的骨架一樣。\n- 計數可能很大，`ans` 用 `long long`。\n- 複雜度：離散化 `O(n log n)`，掃描每步 `O(log n)`，合計 `O(n log n)`，空間 `O(n)`。\n\n## C++17 模板\n\n以下用同型延伸示範題 307. 區域和檢索 - 陣列可修改 展示核心寫法。\n\n**完整題目**：設計可修改陣列，支援單點更新與區間和查詢。\n\n:::example 單點更新：在 1-indexed 位置 i+1 加上 delta，沿 lowbit 路徑…\n**線索**：題目同時有更新與區間/前綴查詢；若值域大，先離散化再用樹狀結構維護統計。\n\n```cpp\nclass NumArray {\n    int n;\n    vector<int> bit, nums;  // bit：樹狀陣列（1-indexed），nums：原始值\n    // 單點更新：在 1-indexed 位置 i+1 加上 delta，沿 lowbit 路徑向上更新\n    void add(int i, int delta) {\n            for (++i; i <= n; i += i & -i) {\n                    bit[i] += delta;\n            }\n    }\n    // 前綴和查詢：計算 [0, i] 的總和，沿 lowbit 路徑向下累加\n    int sumPrefix(int i) {\n            int s = 0;\n            for (++i; i > 0; i -= i & -i) {\n                    s += bit[i];\n            }\n            return s;\n    }\n\npublic:\n    NumArray(vector<int>& a) : n(a.size()), bit(n + 1), nums(a) {\n            for (int i = 0; i < n; ++i) {\n                    add(i, nums[i]);  // 建樹：逐一插入初始值\n            }\n    }\n    void update(int index, int val) {\n            add(index, val - nums[index]);  // 只加差值，避免重複計算\n            nums[index] = val;\n    }\n    int sumRange(int left, int right) {\n            return sumPrefix(right) - (left ? sumPrefix(left - 1) : 0);\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- 樹狀陣列維護前綴和差分；單點修改影響 O(log n) 個節點，區間和由兩個前綴和相減。\n- `add` 沿著 `i += i & -i` 更新所有包含該點的桶，`sum` 沿著 `i -= i & -i` 收集前綴。\n- 外部下標與內部 1-indexed 下標要分清，離散化後也不要混用原值。\n\n## 時間與空間複雜度\n\n| 做法 | 時間 | 空間 | 備註 |\n| --- | --- | --- | --- |\n| 離散化 + 樹狀陣列 | $O(n\\log n)$ | $O(n)$ | 排序 $O(n\\log n)$ + $n$ 次 $O(\\log n)$ 操作 |\n| 歸併排序 | $O(n\\log n)$ | $O(n)$ | 常數更小，**順便得到排序結果** |\n| 值域線段樹（不離散化） | $O(n\\log V)$ | $O(n\\log V)$ | 動態開點，$V$ 為值域 |\n| `pb_ds` 平衡樹 | $O(n\\log n)$ | $O(n)$ | 常數大 3～5 倍，**但不用離散化** |\n| 暴力 | $O(n^2)$ | $O(1)$ | $n \\le 5000$ 時仍是最省腦力的選擇 |\n\n**各題**：\n\n| 題目 | 時間 | 值域大小 | 注意 |\n| --- | --- | --- | --- |\n| 315 右側小於當前元素 | $O(n\\log n)$ | $n$ | 從右往左掃 |\n| 493 翻轉對 | $O(n\\log n)$ | $2n$ | `2LL *`，值域含 $2a$ |\n| 327 區間和的個數 | $O(n\\log n)$ | $3(n+1)$ | $S_0$、`long long` |\n| 2426 滿足不等式的數對 | $O(n\\log n)$ | $n$（或 $2n$） | 先令 $c = a - b$ |\n| 1850 最小相鄰交換 | $O(k\\cdot n + n^2)$ | — | $k$ 次 `next_permutation`，$n \\le 1000$ 故 $O(n^2)$ 貪心即可 |\n\n**常數比較（$n = 10^5$ 實測量級）**：歸併 $\\approx$ 樹狀陣列 $<$ 值域線段樹 $<$ `pb_ds`。**但「離散化的那次 `sort`」通常才是主要成本**——樹狀陣列本身只佔總時間的三分之一左右。\n\n**空間陷阱**：不離散化直接開值域桶。$|a_i| \\le 10^9$ ⇒ 需要 $2\\times10^9$ 個桶，必然 MLE。**看到值域大就離散化，或改用動態開點線段樹（9.5）。**\n\n## 常見錯誤與邊界條件\n\n- 樹狀陣列常用 1-indexed。\n- `i += i & -i` 更新，`i -= i & -i` 查詢。\n- 線段樹懶標記下推時機要一致。\n- 離散化後不要混用原座標與壓縮座標。\n\n## 常見變形\n\n- 樹狀陣列：單點加、前綴和。\n- 線段樹：區間查詢、區間修改、懶標記。\n- 掃描線 + 樹狀陣列/線段樹統計二維偏序。\n\n## 與相似技巧的比較\n\n- **樹狀陣列 vs 歸併排序**：如「狀態」一節的表格。**單純數逆序對用歸併（短、快、不用離散化）；條件一複雜就用樹狀陣列（改一行查詢範圍即可）。** 493 是分水嶺：歸併版要多寫一趟雙指標，樹狀陣列版只要把 `rk(a[j])` 換成 `rk(2*a[j])`。\n- **與 `pb_ds` order-statistics tree**：`tree<int, null_type, less<int>, rb_tree_tag, tree_order_statistics_node_update>` 直接提供 `order_of_key`，**完全不用離散化，而且支援刪除**。程式最短，常數最大。**面試或需要動態刪除時是好選擇。**\n- **與值域線段樹／動態開點（9.5）**：功能等同樹狀陣列值域桶，但**不必離散化**（線上處理值域到 $10^9$）。代價是常數與空間。**離線可離散化 ⇒ 樹狀陣列；線上且值域大 ⇒ 動態開點。**\n- **與 CDQ 分治**：本節處理**二維**偏序（下標 + 一個數值）。若條件是**三維**（$i<j$ 且 $a_i<a_j$ 且 $b_i<b_j$），一維交給分治、一維交給排序、一維交給樹狀陣列，就是 CDQ。**LeetCode 上罕見，但「掃描吃掉一維」的思路完全相同。**\n- **與 9.1 樹狀陣列**：9.1 講的是資料結構本身，本節講的是**它最重要的應用模式**。**「離散化 + 值域桶 + 邊掃邊查」這三件事的組合，比樹狀陣列的實作重要得多。**\n- **與「離線 + 排序」**：某些逆序對變形按值排序後可用雙指標解決，完全不需要資料結構。**先問「能不能離線」。**\n- **與 6.1／6.3 的堆**：「前 $k$ 大」用堆，「有幾個比我大」用樹狀陣列。**堆回答的是極值，桶回答的是計數——需求不同，別互換。**\n- **與 4.7 單調堆疊**：單調堆疊回答「左／右第一個比我大的」（**最近**的一個），樹狀陣列回答「有幾個比我大」（**計數**）。**兩者常在同一題的不同部分出現，看清楚問的是「哪一個」還是「有幾個」。**\n\n## 代表例題\n\n**題號**：LCR 170、315、327、493、1850\n\n有動態更新與區間查詢；練習先離散化值域，再確認查詢/更新的閉開區間。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| LCR 170 | [交易逆序對的總數](https://leetcode.cn/problems/shu-zu-zhong-de-ni-xu-dui-lcof)（Premium） |  |\n| 315 | [計算右側小於當前元素的個數](https://leetcode.cn/problems/count-of-smaller-numbers-after-self) |  |\n| 327 | [區間和的個數](https://leetcode.cn/problems/count-of-range-sum) |  |\n| 493 | [翻轉對](https://leetcode.cn/problems/reverse-pairs) |  |\n| 1850 | [鄰位交換的最小次數](https://leetcode.cn/problems/minimum-adjacent-swaps-to-reach-the-kth-smallest-number) | 2073 |\n| 1885 | [統計數對](https://leetcode.cn/problems/count-pairs-in-two-arrays) |  |\n| 2193 | [得到迴文串的最少操作次數](https://leetcode.cn/problems/minimum-number-of-moves-to-make-palindrome) | 2091 |\n| 2426 | [滿足不等式的數對數目](https://leetcode.cn/problems/number-of-pairs-satisfying-inequality) | 2030 |\n| 3768 | [固定長度子陣列中的最小逆序對數目](https://leetcode.cn/problems/minimum-inversion-count-in-subarrays-of-fixed-length) | 2158 |\n\n## 本節重點速查\n\n- **框架一句話**：兩個條件，**下標條件交給掃描順序，數值條件交給值域桶**（二維偏序）。\n- **看到「對每個元素數前面有多少個滿足不等式」，第一件事永遠是移項**——把 $i$ 的量搬一邊、$j$ 的量搬另一邊。2426 的 $c = a - b$、327 的 $S_j - r \\le S_{i-1} \\le S_j - \\ell$ 都是這一步。\n- **不重不漏的理由（定理 1）**：配對 $(i,j)$ 只可能在處理 $j$ 的那一輪被計入，而那一輪桶中恰為 $\\{a_i : i<j\\}$。**框架的前提是「掃描順序能吃掉一整個維度」**；有距離限制或三維偏序就不夠。\n- **`ans += j - sum(r)`**：用「總數減補集」算後綴，比直接查後綴不易錯。\n- **離散化三件套 `sort` + `unique` + `lower_bound`**。**`unique` 不能省**（定理 3）：不去重會讓相同值拿到不同排名，相等對被誤判成逆序對。\n- **歸併版的兩個前提（定理 2）**：左右各自已排序、左半整體在右半之前。**相等元素用 `<=` 取左邊 ⇒ 嚴格逆序對**，改成 `<` 就變成非嚴格。\n- **逆序對數 $=$ 最少相鄰交換次數（定理 4）**：下界靠「一次交換只改變一對的相對順序」，上界靠「$\\mathrm{inv}>0$ 必存在相鄰逆序對」。**只證下界不算證完。** 有重複元素時「同字元按出現順序一一對應」是最優匹配。\n- **493 的三個陷阱**：值域要含 $2a$（或改用 `upper_bound`）、歸併版要**額外一趟**雙指標（單調性來自右半已排序）、**`2LL *` 防溢位**。\n- **327 的三個陷阱**：**$S_0 = 0$ 必須先插入**、值域要含 $\\{S\\}\\cup\\{S-\\ell\\}\\cup\\{S-r\\}$、`long long`。\n- **選擇**：純逆序對 ⇒ 歸併；條件複雜 ⇒ 樹狀陣列；要線上或值域巨大 ⇒ 動態開點線段樹；懶得離散化又能忍常數 ⇒ `pb_ds`。\n- **值域大不離散化就是 MLE**——這是本節最常見的當機原因。\n",
        },
        {
          id: 63,
          title: "9.3 線段樹（無區間更新）",
          summary:
            "## 這個技術解決什麼問題\n\n9.1 的定理 5 劃下了一條界線：**樹狀陣列靠 `sum(r) - sum(l-1)` 回答區間查詢，所以聚合必須有逆元。** max、min、gcd 都沒有逆元——知道 $[1,r]$ 的最大值和 $[1,l-1]$ 的最大值，**無法**還原 $[l,r]$ 的最大值。\n\n線段樹換了一個思路：**不做減法，改做拼接。**\n\n> **把 $[1, n]$ 遞迴二分成一棵樹，每個節點存自己那段的聚合值。任何區間 $[l, r]$ 都能拆成樹上 $O(\\log n)$ 個節點的不交聯集，把它們的值合併起來就是答案。**\n\n於是對聚合的要求從「群」放寬成**幺半群（monoid）**：只需要**結合律**與**單位元**，不需要逆元，也不需要交換律。\n\n這個放寬帶來兩層能力：\n\n1. **max / min / gcd / 位元 OR** 之類沒有逆元的聚合，全部可以做；\n2. **更關鍵的**：節點裡可以存**複合資訊**。2213「由單個字元重複的最長子字串」的每個節點存「前綴最長同字元段、後綴最長同字元段、區間內最長同字元段、兩端字元」四樣東西，合併時靠邊界拼接算出跨界的段——**這種「答案不是簡單數值、而是一個需要精心設計合併規則的結構」，是線段樹真正不可替代的地方。**\n\n**辨識訊號**：\n- 單點改 + 區間查，但查的是 max / min / gcd 這類**不可差分**的聚合 ⇒ 2940、3161；\n- 節點需要**複合資訊**才能合併 ⇒ 2213；\n- 需要在樹上**二分**（找「第一個滿足某條件的位置」）⇒ 2940、2286；\n- 一連串「修改某位置 + 詢問某區間性質」的操作流。\n\n**本節只講單點修改。** 區間修改需要懶標記，那是 9.4。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「線段樹（無區間更新）」想：\n\n- **要「單點改 + 區間查」，但查的是 max/min/gcd 這類無法用前綴和相減還原的聚合。** 樹狀陣列做不到，線段樹可以。\n- **節點需要維護「複合資訊」才能合併。** 如 2213「由單個字元重複的最長子字串」，每個節點要存「前綴最長、後綴最長、區間最長同字元段」，靠 pushUp 巧妙合併——這是線段樹的強項。\n- **題目給的是一連串「修改某位置 + 詢問某區間性質」的操作流。**\n\n:::example 嗅到味道的瞬間：你想對區間求 max，又要支援單點修改\n```cpp\n// 嗅到味道的瞬間：你想對區間求 max，又要支援單點修改\n// 普通前綴和 / 樹狀陣列對 max 無法相減還原 → 用線段樹\n// tree[node] = max(tree[left], tree[right]);\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 每個節點要存哪些欄位，才足以合併出父節點？\n2. 我的合併（pushUp）函式與對應的單位元是什麼？\n3. 我的查詢能拆成「完全覆蓋 / 不相交 / 部分覆蓋」三種情況嗎？\n\n三題都答得出來，就套對了。\n\n## 核心想法與直覺\n\n本節介紹「線段樹（無區間更新版）」。它要解決的場景比樹狀陣列更通用：你有一個陣列，需要反覆做「單點修改」加上「區間查詢」，而且查詢的不只是求和，可能是區間最大值、最小值、最大子段和等各種聚合。\n\n:::example 每個節點存它負責區間的聚合值；這裡以「區間和」為例\n核心想法是**分治**：把整個區間 `[0, n - 1]` 不斷對半切，切到只剩一個元素為止，形成一棵二元樹。每個節點保存「它所負責的那段區間」的聚合結果。\n\n```cpp\n// 每個節點存它負責區間的聚合值；這裡以「區間和」為例\nstruct Node {\n    long long sum;\n};                  // 換成 max/min 就變 RMQ\nvector<Node> tree;  // 用陣列模擬樹，node 的左右孩子是 2*node, 2*node+1\n```\n:::\n\n\n\n:::example 區間查詢：完全覆蓋就直接用，否則往左右孩子拆\n為什麼這樣會快？因為**任何一段查詢區間 `[l, r]`，都能被 `O(log n)` 個「完整節點」拼出來**。查詢時從根往下遞迴：若當前節點區間完全落在 `[l, r]` 裡，直接回傳它的聚合值不再往下；若完全不相交就回傳「單位元」（求和用 0、求最大值用負無限大）；否則拆成左右孩子各查一次再合併。\n\n```cpp\n// 區間查詢：完全覆蓋就直接用，否則往左右孩子拆\nlong long query(int node, int lo, int hi, int l, int r) {\n    if (r < lo || hi < l) {\n        return 0;  // 不相交 → 單位元\n    }\n    if (l <= lo && hi <= r) {\n        return tree[node].sum;  // 完全覆蓋 → 直接回傳\n    }\n    int mid = (lo + hi) / 2;\n    return query(2 * node, lo, mid, l, r) +\n                  query(2 * node + 1, mid + 1, hi, l, r);  // 部分覆蓋 → 合併左右\n}\n```\n:::\n\n\n\n單點更新則是「從葉子改起、一路把父節點重算」：只有 `O(log n)` 個祖先受影響。比起樹狀陣列只能做可逆的聚合（如和），線段樹連 max/min 這種不可逆的也能做，這是它更通用的原因。整體 n 次操作 `O(n log n)`。\n\n## 狀態／資料結構定義\n\n**樹形結構**（遞迴版，堆式編號）：節點 $k$ 覆蓋區間 $[\\mathit{lo}_k, \\mathit{hi}_k]$，根是節點 $1$ 覆蓋 $[0, n-1]$；若 $\\mathit{lo} < \\mathit{hi}$，令 $\\mathit{mid} = \\lfloor(\\mathit{lo}+\\mathit{hi})/2\\rfloor$，左子 $2k$ 覆蓋 $[\\mathit{lo}, \\mathit{mid}]$、右子 $2k+1$ 覆蓋 $[\\mathit{mid}+1, \\mathit{hi}]$。\n\n**幺半群介面**（把「聚合」抽象出來，之後換題只要換這一段）：\n\n```cpp\nstruct Info {\n    long long val;                                   // 這段的聚合值\n    static Info id() { return Info{LLONG_MIN}; }     // 單位元：max 的單位元是 -inf\n    friend Info operator+(const Info& a, const Info& b) {\n        return Info{max(a.val, b.val)};              // 結合律成立、單位元正確\n    }\n};\n```\n\n**單位元不能亂填。** 求 max 用 `0` 當單位元，在全是負數的資料上立刻錯；求 gcd 的單位元是 `0`（因為 $\\gcd(0, x) = x$）；求 min 用 `LLONG_MAX`。**寫任何線段樹之前，先在紙上確認 `id() + x == x` 對所有 `x` 成立。**\n\n**合併不必交換，但順序不能反。** `operator+` 的兩個參數是「左段」和「右段」，2213 的合併規則會用到「左段的右端字元 vs 右段的左端字元」——**`pushUp` 寫成 `t[k] = t[2k+1] + t[2k]` 就全錯了，而且在 max/sum 這類交換的聚合上測不出來。**\n\n**節點陣列開 $4n$。** 理由見「時間與空間複雜度」。\n\n**2213 的複合資訊**（本節最值得看的 `Info` 設計）：\n\n```cpp\nstruct Info {\n    int len;            // 這段長度\n    int pre, suf, best; // 前綴最長同字元段、後綴最長同字元段、區間最長同字元段\n    char lc, rc;        // 這段最左、最右的字元\n};\nInfo merge(const Info& L, const Info& R) {\n    if (L.len == 0) return R;\n    if (R.len == 0) return L;\n    Info c;\n    c.len = L.len + R.len;\n    c.lc = L.lc; c.rc = R.rc;\n    bool join = (L.rc == R.lc);\n    c.pre = (L.pre == L.len && join) ? L.len + R.pre : L.pre;\n    c.suf = (R.suf == R.len && join) ? R.len + L.suf : R.suf;\n    c.best = max({L.best, R.best, join ? L.suf + R.pre : 0});\n    return c;\n}\n```\n\n**空段 `len == 0` 就是這個幺半群的單位元**，`merge` 的頭兩行負責它。\n\n## 不變量或正確性證明\n\n### 定理 1（結構不變量）\n\n**不變量 (S)**：任何時刻，對每個節點 $k$，\n$$t[k] = a[\\mathit{lo}_k] \\oplus a[\\mathit{lo}_k + 1] \\oplus \\cdots \\oplus a[\\mathit{hi}_k]$$\n（$\\oplus$ 為幺半群運算，按**下標遞增順序**折疊）。\n\n**維護**：`build` 自底向上一次填滿；單點修改只改一片葉子，然後沿**根路徑**逐層 `pushUp`。\n\n*正確性*：修改位置 $x$ 只影響覆蓋 $x$ 的節點，而它們恰好是從葉子 $x$ 到根的那條路徑（每層恰一個，因為同層節點不交）。**這與 9.1 定理 3 是同一件事的不同版本**——那裡「覆蓋 $x$ 的段」是 lowbit 鏈，這裡是根路徑，**兩者長度都是 $O(\\log n)$，這才是兩個結構複雜度相同的真正原因。**\n\n### 定理 2（區間分解定理：$[l,r]$ 拆成 $O(\\log n)$ 個節點）\n\n這是線段樹的核心定理，**也是 9.1 那條「拆成 popcount 段」性質在線段樹上的對應物**。\n\n*設定*：標準查詢遞迴為\n\n```\nquery(k):\n  若 [lo_k, hi_k] 與 [l, r] 不交      → 回傳單位元\n  若 [lo_k, hi_k] ⊆ [l, r]           → 回傳 t[k]      （稱為「典範節點」）\n  否則                                → 回傳 query(2k) ⊕ query(2k+1)\n```\n\n**引理 2.1（每層至多 2 個「部分相交」節點）**：稱節點 $k$ 為**部分相交**，若 $[\\mathit{lo}_k, \\mathit{hi}_k]$ 與 $[l,r]$ 相交但不被 $[l,r]$ 包含。則在遞迴樹的每一層，部分相交的節點至多 2 個。\n\n*證明*。同層節點兩兩不交且從左到右排列。設某層有三個部分相交節點 $N_1 < N_2 < N_3$（按位置）。因為 $N_1$ 與 $[l,r]$ 相交，$[l,r]$ 含有 $N_2$ 左側的點，故 $l < \\mathit{lo}_{N_2}$；因為 $N_3$ 與 $[l,r]$ 相交，$[l,r]$ 含有 $N_2$ 右側的點，故 $r > \\mathit{hi}_{N_2}$。**於是 $[l,r] \\supseteq N_2$，$N_2$ 不是部分相交**，矛盾 ✔。$\\square$\n\n**定理 2**：查詢遞迴訪問的節點數為 $O(\\log n)$，其中典範節點至多 $2\\lceil\\log_2 n\\rceil$ 個，且它們構成 $[l, r]$ 的一個**不交分割**。\n\n*證明*。\n- **訪問數**：只有部分相交的節點會繼續遞迴（不交與被包含都立即回傳）。由引理 2.1，每層至多 2 個節點遞迴，故每層至多產生 4 個子呼叫，訪問節點數 $\\le 4\\log_2 n = O(\\log n)$ ✔。\n- **典範節點數**：典範節點的父親必為部分相交（否則父親已被包含，遞迴不會下來），每個部分相交節點至多貢獻 2 個典範子節點，但**若兩個子節點都被包含，父親本身就被包含**——矛盾。故每個部分相交節點至多貢獻 **1** 個典範子節點 ⇒ 每層至多 2 個典範節點 ⇒ 總數 $\\le 2\\lceil\\log_2 n\\rceil$ ✔。\n- **分割性**：遞迴在每個節點要嘛整段回傳、要嘛把段切成兩半分別處理，**過程中沒有任何位置被丟棄或重複**（不交的部分本來就不在 $[l,r]$ 內）。故典範節點的區間不交且聯集恰為 $[l,r]$ ✔。$\\square$\n\n**「不交且聯集為 $[l,r]$」加上「$\\oplus$ 有結合律」，才推得出 `query` 的結果等於 $a_l \\oplus \\cdots \\oplus a_r$。** 兩個前提缺一不可：\n\n- **沒有分割性** ⇒ 元素被重複計入（對 max 無害，**對 sum 就是災難**）。這解釋了為什麼「不交」這件小事必須證。\n- **沒有結合律** ⇒ 「先合併哪兩段」會影響結果，而遞迴的合併順序是由樹形決定的。**結合律是線段樹存在的前提，不是可有可無的細節。**\n\n**推論 2.2**：交換律**不需要**。遞迴永遠以 `左 ⊕ 右` 的順序合併，與下標順序一致，故非交換的幺半群（如 2213 的 `Info`）完全合法。**但這也意味著任何一處把左右寫反都會出錯。**\n\n### 定理 3（2213 的合併規則正確）\n\n*命題*：設字串 $L$ 與 $R$ 相接成 $LR$，則 $LR$ 中最長的「單一字元重複段」長度為\n$$\\max\\Big(L.\\mathit{best},\\ R.\\mathit{best},\\ [\\,L.\\mathit{rc} = R.\\mathit{lc}\\,]\\cdot\\big(L.\\mathit{suf} + R.\\mathit{pre}\\big)\\Big).$$\n\n*證明*。取 $LR$ 中任一極長的同字元段 $B$。$B$ 是一段連續區間，三種情形窮盡：\n- $B$ 完全落在 $L$ 內 ⇒ $|B| \\le L.\\mathit{best}$ ✔；\n- $B$ 完全落在 $R$ 內 ⇒ $|B| \\le R.\\mathit{best}$ ✔；\n- $B$ **跨越邊界** ⇒ $B$ 同時含 $L$ 的最後一個字元與 $R$ 的第一個字元，故 $L.\\mathit{rc} = R.\\mathit{lc}$；且 $B \\cap L$ 是 $L$ 的一個同字元後綴、$B \\cap R$ 是 $R$ 的一個同字元前綴，於是 $|B| \\le L.\\mathit{suf} + R.\\mathit{pre}$ ✔。\n\n反向：三個候選值都對應 $LR$ 中真實存在的同字元段（第三個需要 $L.\\mathit{rc} = R.\\mathit{lc}$ 才成立，已用指示函數守住）✔。取最大即得等號。$\\square$\n\n**`pre` 的規則同理**：$LR$ 的前綴同字元段等於 $L$ 的前綴段，**除非 $L$ 整段都是同一個字元且與 $R$ 的首字元相同**，此時可以延伸進 $R$。`suf` 對稱。\n\n**結合律必須另外檢查。** `merge` 只是一個二元運算，**線段樹要求 $(A \\oplus B) \\oplus C = A \\oplus (B \\oplus C)$**。上述規則滿足結合律（兩邊都等於「對三段串接的直接計算」），但**自訂 `Info` 時這一步經常被跳過，然後在特定的分割形狀下才爆炸**。**實務檢查法：拿三個單字元葉子，手算兩種括號方式，確認結果相同。**\n\n### 定理 4（線段樹上二分：2940 的 $O(\\log n)$ 而非 $O(\\log^2 n)$）\n\n*問題*：2940 要對每組 $(i, j)$（設 $i < j$）找**最小的 $k > j$ 使 $h[k] > \\max(h[i], h[j])$**。\n\n*演算法*：在區間 $[j+1, n-1]$ 上找第一個值 $> x$ 的位置。\n\n```\ndescend(k):                    // 前提：t[k].mx > x\n  若 k 是葉子 → 回傳 lo_k\n  若 t[2k].mx > x → 回傳 descend(2k)\n  否則             → 回傳 descend(2k+1)\n```\n外層照定理 2 把 $[j+1, n-1]$ 分解成**由左到右**的典範節點序列，**掃到第一個 `mx > x` 的節點就 `descend` 進去，立即回傳。**\n\n**正確性**：\n- **剪枝安全**：`t[k].mx <= x` 表示該子樹內**每個**元素都 $\\le x$，跳過不會漏 ✔。\n- **回傳的是最小下標**：典範節點按下標遞增排列，第一個含合法元素的節點必包含答案；`descend` 內優先走左子樹，而「左子樹有合法元素」時答案必在左子樹（下標更小）✔。\n\n**複雜度 $O(\\log n)$**：分解出 $O(\\log n)$ 個典範節點，每個只做 $O(1)$ 的 `mx > x` 判斷；**`descend` 只會被呼叫一次**，且深度 $O(\\log n)$ ✔。\n\n**不要寫成「外層二分答案 + 內層區間查 max」**——那是 $O(\\log^2 n)$，而且在 $q = 10^5$ 時差距明顯。**「把二分做進樹裡」是線段樹相對於樹狀陣列與 ST 表的一個獨門優勢。**\n\n### 定理 5（2286 的 `scatter` 為什麼是攤還 $O(\\log n)$）\n\n*問題*：$n$ 排、每排 $m$ 個座位。`gather(k, maxRow)` 找最小的排使剩餘座位 $\\ge k$；`scatter(k, maxRow)` 從最小的排開始依序分配 $k$ 個座位。\n\n*結構*：線段樹每個節點同時維護該段的**剩餘座位最大值** `mx`（供 `gather` 做定理 4 的樹上二分）與**剩餘座位總和** `sum`（供 `scatter` 判斷可行性）。\n\n**`scatter` 的實作是遞迴下降，遇到「本排剩餘 $\\le$ 待分配量」就把整排填滿並繼續。** 單次最壞是 $O(n\\log n)$（填滿很多排），但——\n\n**命題**：$q$ 次操作的**總**成本為 $O\\big((n + q)\\log n\\big)$。\n\n*證明（攤還）*。把 `scatter` 遞迴訪問的葉子分成兩類：\n- **被填滿的排**：該排從此剩餘 0，**永遠不會再被 `scatter` 訪問**（訪問前會先檢查剩餘 $>0$）。全程至多 $n$ 次 ✔。\n- **未被填滿的排**：每次 `scatter` 至多一個（$k$ 用完就停）。全程至多 $q$ 次 ✔。\n\n每次葉子訪問伴隨 $O(\\log n)$ 的路徑代價，總計 $O((n+q)\\log n)$ ✔。$\\square$\n\n**又一次是「昂貴的迭代對應不可逆的狀態變化」**——與 8.5 定理 2、4.7 單調堆疊、6.6 懶刪除同形。**這個攤還模式在整個資料結構章出現了至少五次，值得當成一個獨立的思考工具記住：先問「這個昂貴的步驟消耗了什麼不可再生的資源」。**\n\n## 逐步演算法\n\n:::example 狀態表示：tree 開 4n；node=1 是根，孩子是 2*node 與 2*node…\n**1. 狀態表示：用 4n 大小的陣列模擬樹。** 線段樹節點數最多到 `4n`，習慣開這麼大避免越界。\n\n```cpp\n// 狀態表示：tree 開 4n；node=1 是根，孩子是 2*node 與 2*node+1\nvector<long long> tree;  // 以區間和為例\nvoid init(int n) { tree.assign(4 * n, 0); }\n```\n:::\n\n\n\n:::example 建樹：到葉子寫入元素，回程把左右孩子合併到父節點\n**2. 建樹：遞迴到葉子填值，回程合併（pushUp）。** 葉子放原始元素，內部節點由兩個孩子合併而得。\n\n```cpp\n// 建樹：到葉子寫入元素，回程把左右孩子合併到父節點\nvoid build(int node, int lo, int hi, vector<int>& a) {\n    if (lo == hi) {\n        tree[node] = a[lo];\n        return;\n    }\n    int mid = (lo + hi) / 2;\n    build(2 * node, lo, mid, a);\n    build(2 * node + 1, mid + 1, hi, a);\n    tree[node] = tree[2 * node] + tree[2 * node + 1];  // pushUp：合併\n}\n```\n:::\n\n\n\n:::example 單點更新：把位置 pos 設為 val\n**3. 單點更新：改葉子，回程重算沿途父節點。** 找到目標葉子改值，回來時每個祖先重新 pushUp。\n\n```cpp\n// 單點更新：把位置 pos 設為 val\nvoid update(int node, int lo, int hi, int pos, int val) {\n    if (lo == hi) {\n        tree[node] = val;\n        return;\n    }\n    int mid = (lo + hi) / 2;\n    if (pos <= mid) {\n        update(2 * node, lo, mid, pos, val);\n    } else {\n        update(2 * node + 1, mid + 1, hi, pos, val);\n    }\n    tree[node] = tree[2 * node] + tree[2 * node + 1];  // 重算父節點\n}\n```\n:::\n\n\n\n**4. 答案讀取與邊界、複雜度。** 區間查詢用前面示範的 `query`。注意三件事：\n\n- **合併函式與單位元要一致**：求和用 `+`／單位元 0；求最大值用 `max`／單位元極小值。寫錯單位元會在「不相交」分支汙染答案。\n- 索引閉開區間要前後統一，遞迴傳的 `[lo, hi]` 與查詢的 `[l, r]` 都用閉區間最不易錯。\n- 複雜度：建樹 `O(n)`，單點更新與區間查詢皆 `O(log n)`，空間 `O(n)`。\n\n## C++17 模板\n\n**模板一：通用單點改 + 區間查（把 `Info` 換掉就能重用）**\n\n```cpp\ntemplate <class Info>\nstruct SegTree {\n    int n;\n    vector<Info> t;\n    explicit SegTree(const vector<Info>& a) : n(a.size()), t(4 * a.size()) {\n        build(1, 0, n - 1, a);\n    }\n    void build(int k, int lo, int hi, const vector<Info>& a) {\n        if (lo == hi) { t[k] = a[lo]; return; }\n        int mid = (lo + hi) >> 1;\n        build(2 * k, lo, mid, a);\n        build(2 * k + 1, mid + 1, hi, a);\n        t[k] = t[2 * k] + t[2 * k + 1];          // 順序不可反\n    }\n    void update(int k, int lo, int hi, int pos, const Info& v) {\n        if (lo == hi) { t[k] = v; return; }\n        int mid = (lo + hi) >> 1;\n        if (pos <= mid) update(2 * k, lo, mid, pos, v);\n        else            update(2 * k + 1, mid + 1, hi, pos, v);\n        t[k] = t[2 * k] + t[2 * k + 1];\n    }\n    Info query(int k, int lo, int hi, int l, int r) const {\n        if (r < lo || hi < l) return Info::id();  // 不交\n        if (l <= lo && hi <= r) return t[k];      // 典範節點\n        int mid = (lo + hi) >> 1;\n        return query(2 * k, lo, mid, l, r) + query(2 * k + 1, mid + 1, hi, l, r);\n    }\n    // 對外介面\n    void update(int pos, const Info& v) { update(1, 0, n - 1, pos, v); }\n    Info query(int l, int r) const {\n        if (l > r) return Info::id();\n        return query(1, 0, n - 1, l, r);\n    }\n};\n```\n\n**模板二：2213 由單個字元重複的最長子字串（複合 `Info`）**\n\n```cpp\nstruct Info {\n    int len = 0, pre = 0, suf = 0, best = 0;\n    char lc = 0, rc = 0;\n    static Info id() { return Info{}; }           // len == 0 即單位元\n    friend Info operator+(const Info& L, const Info& R) {\n        if (L.len == 0) return R;\n        if (R.len == 0) return L;\n        Info c;\n        c.len = L.len + R.len;\n        c.lc = L.lc; c.rc = R.rc;\n        bool join = (L.rc == R.lc);\n        c.pre = (L.pre == L.len && join) ? L.len + R.pre : L.pre;\n        c.suf = (R.suf == R.len && join) ? R.len + L.suf : R.suf;\n        c.best = max({L.best, R.best, join ? L.suf + R.pre : 0});\n        return c;\n    }\n};\n\nclass Solution {\npublic:\n    vector<int> longestRepeating(string s, string queryCharacters,\n                                 vector<int>& queryIndices) {\n        int n = s.size();\n        vector<Info> a(n);\n        for (int i = 0; i < n; ++i) a[i] = Info{1, 1, 1, 1, s[i], s[i]};\n        SegTree<Info> st(a);\n        vector<int> ans;\n        ans.reserve(queryIndices.size());\n        for (int q = 0; q < (int)queryIndices.size(); ++q) {\n            char c = queryCharacters[q];\n            int i = queryIndices[q];\n            st.update(i, Info{1, 1, 1, 1, c, c});\n            ans.push_back(st.query(0, n - 1).best);\n        }\n        return ans;\n    }\n};\n```\n\n**模板三：線段樹上二分——區間內第一個 $> x$ 的下標（定理 4）**\n\n```cpp\nstruct MaxSeg {\n    int n;\n    vector<int> mx;\n    explicit MaxSeg(const vector<int>& a) : n(a.size()), mx(4 * a.size(), INT_MIN) {\n        build(1, 0, n - 1, a);\n    }\n    void build(int k, int lo, int hi, const vector<int>& a) {\n        if (lo == hi) { mx[k] = a[lo]; return; }\n        int mid = (lo + hi) >> 1;\n        build(2 * k, lo, mid, a);\n        build(2 * k + 1, mid + 1, hi, a);\n        mx[k] = max(mx[2 * k], mx[2 * k + 1]);\n    }\n    // 在 [l, n-1] 中找第一個 > x 的下標；不存在回傳 -1\n    int firstGreater(int k, int lo, int hi, int l, int x) const {\n        if (hi < l || mx[k] <= x) return -1;      // 剪枝：整段都 <= x\n        if (lo == hi) return lo;\n        int mid = (lo + hi) >> 1;\n        int res = firstGreater(2 * k, lo, mid, l, x);\n        if (res != -1) return res;                // 左子樹優先 ⇒ 下標最小\n        return firstGreater(2 * k + 1, mid + 1, hi, l, x);\n    }\n    int firstGreater(int l, int x) const {\n        if (l >= n) return -1;\n        return firstGreater(1, 0, n - 1, l, x);\n    }\n};\n```\n\n**`mx[k] <= x` 這個剪枝是整段程式的靈魂**：沒有它就是 $O(n)$ 的暴力遞迴，有了它就是 $O(\\log n)$。\n\n## 時間與空間複雜度\n\n| 操作 | 時間 | 說明 |\n| --- | --- | --- |\n| `build` | $O(n)$ | 每個節點 $O(1)$，節點總數 $O(n)$ |\n| 單點修改 | $O(\\log n)$ | 根路徑長度 |\n| 區間查詢 | $O(\\log n)$ | 定理 2：至多 $4\\log_2 n$ 個節點 |\n| 樹上二分 | $O(\\log n)$ | 定理 4，**不是 $O(\\log^2 n)$** |\n| 空間 | $\\mathbf{4n}$ 個節點 | 見下 |\n\n**為什麼是 $4n$ 而不是 $2n$**：堆式編號要求陣列大小為「完全二元樹的節點數」。當 $n$ 不是 2 的冪時，樹的最底層會有空洞，最壞需要 $2 \\cdot 2^{\\lceil\\log_2 n\\rceil} \\le 4n$。例如 $n = 5$ 時最大節點編號是 $13 > 2n$。**開 $2n$ 是最常見的線段樹越界原因，而且往往只在特定 $n$ 上才炸。** 想省空間就用非遞迴（zkw）版或動態開點（9.5）。\n\n**與樹狀陣列的實測比較**（$n = 10^5$、$q = 10^5$）：\n\n| | 樹狀陣列 | 線段樹（遞迴） | 線段樹（非遞迴） |\n| --- | --- | --- | --- |\n| 程式行數 | ~10 | ~60 | ~30 |\n| 空間 | $n$ | $4n$ | $2n$ |\n| 相對耗時 | 1.0 | 2～4 | 1.3～2 |\n| 支援 max/min/gcd | ❌ | ✅ | ✅ |\n| 支援複合 `Info` | ❌ | ✅ | ✅（合併非交換時要小心） |\n\n**各題**：\n\n| 題目 | 時間 | 節點資訊 |\n| --- | --- | --- |\n| 2213 最長重複子字串 | $O((n + q)\\log n)$ | `len/pre/suf/best/lc/rc` |\n| 2940 Alice 和 Bob 相遇 | $O((n + q)\\log n)$ | `max` + 樹上二分 |\n| 2286 團體訂票 | $O((n + q)\\log n)$ **攤還** | `max` + `sum`，定理 5 |\n| 3161 放置方塊查詢 | $O((n+q)\\log n)$ | `max`（區段長）+ 樹上二分 |\n\n**注意 2940 也有 $O(n + q)$ 的離線做法**（把查詢按 $j$ 分組 + 單調堆疊）。**線段樹是通用解，不一定是最優解——寫之前值得花三十秒想想有沒有離線的捷徑。**\n\n## 常見錯誤與邊界條件\n\n- **節點陣列只開 $2n$**。必須 $4n$（或用 `2 << (32 - __builtin_clz(n))`）。**這是線段樹第一大坑。**\n- **`pushUp` 的左右順序寫反**。`t[k] = t[2k] + t[2k+1]`，不是反過來。**在 sum/max 上測不出來，換到 2213 這種非交換的 `Info` 立刻錯。**\n- **忘記 `pushUp`**。單點修改後只改葉子不往上更新，查詢會讀到舊值。\n- **單位元填錯**。max 用 `0`（全負資料錯）、min 用 `0`（全正資料錯）、gcd 的單位元是 `0` 不是 `1`。**寫之前先驗證 `id() + x == x`。**\n- **空區間查詢沒處理**。$l > r$ 時要回傳單位元。**2940 的 $j = n-1$、327 的邊界都會觸發。**\n- **不交判斷寫錯**。`if (r < lo || hi < l) return id();` 少一個等號就會漏或重。\n- **`mid` 的計算與子區間劃分不一致**。左子是 $[\\mathit{lo}, \\mathit{mid}]$、右子是 $[\\mathit{mid}+1, \\mathit{hi}]$。**寫成 $[\\mathit{lo}, \\mathit{mid}-1]$／$[\\mathit{mid}, \\mathit{hi}]$ 會在 $\\mathit{lo}+1 = \\mathit{hi}$ 時無窮遞迴。**\n- **0-indexed 與 1-indexed 混用**。整份程式選一種並貫徹。本節模板一律 0-indexed 資料 + 1-indexed 節點編號。\n- **`Info` 沒驗證結合律**。自訂複合資訊時**一定要用三個葉子手算兩種括號方式**。\n- **溢位**。求和用 `long long`；`(lo + hi) >> 1` 在 $n$ 不到 $10^9$ 時安全，但別在超大值域的動態開點上照抄。\n- **樹上二分寫成外層二分**。$O(\\log^2 n)$ 在 $q = 10^5$ 時可能 TLE，而且程式並沒有比較短。\n- **2286 忘記 `maxRow` 的限制**。`gather`／`scatter` 都只在 $[0, \\mathit{maxRow}]$ 內操作，樹上二分的範圍要跟著限制。\n- **遞迴深度**。$n = 10^5$ 時深度只有 17，**遞迴版完全安全**，不必為此改寫成非遞迴。\n\n## 與相似技巧的比較\n\n- **與 9.1 樹狀陣列**：分界線是**定理 5（9.1）**——聚合有沒有逆元。**能用樹狀陣列就用**（短 6 倍、快 2～4 倍、省 4 倍空間）。max/min/gcd、複合資訊、樹上二分才輪到線段樹。\n- **與 9.4 Lazy 線段樹**：本節只做**單點**修改，所以不需要懶標記——修改只影響一條根路徑，沿路 `pushUp` 即可。**一旦出現「把一整段全加／全設為某值」就必須加 lazy**，那是另一套心智負擔（`pushDown`、標記合併、標記與查詢的順序）。**不需要區間修改時不要寫 lazy 版，多出來的每一行都是 bug 的機會。**\n- **與 9.5 動態開點**：本節假設值域 $= [0, n-1]$ 且 $n$ 不大。值域到 $10^9$ 且稀疏時，改成動態開點（用指標或左右子下標，只在需要時建節點）。**結構與定理 2 完全相同，只是節點按需分配。**\n- **與 9.7 ST 表**：ST 表 $O(1)$ 查詢、$O(n\\log n)$ 預處理，**但完全不支援修改**，且只適用**可重複貢獻**的聚合（max/min/gcd，因為區間可以重疊）。**判準只有一句：資料會不會改。** 不會改 + 大量區間 max 查詢 ⇒ ST 表完勝。\n- **與 4.7 單調堆疊**：單調堆疊 $O(n)$ 解決「每個位置左／右第一個比它大的」，**線段樹二分 $O(n\\log n)$ 也能做同一件事**。**能離線就用單調堆疊**；要線上、要帶修改、要任意起點才用線段樹（2940 的線上版本）。\n- **與分塊（第 11 章）**：分塊 $O(\\sqrt n)$ 更慢，但**不要求結合律**，能處理「沒有好的合併規則」的怪操作。**線段樹寫不出 `merge` 時，才考慮分塊。**\n- **與 8.5 區間並查集**：那是「每格只處理一次」的一次性標記，$O((n+q)\\alpha)$ 遠快於線段樹。**只要前提成立就用它**；有撤銷、有累加、有中途查詢才升級到線段樹。\n\n## 代表例題\n\n**題號**：LCP 81、2213、2286、2940、3161\n\n有動態更新與區間查詢；練習先離散化值域，再確認查詢/更新的閉開區間。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| LCP 81 | [與非的謎題](https://leetcode.cn/problems/ryfUiz)（Premium） |  |\n| 2213 | [由單個字元重複的最長子字串](https://leetcode.cn/problems/longest-substring-of-one-repeating-character) | 2629 |\n| 2286 | [以組為單位訂音樂會的門票](https://leetcode.cn/problems/booking-concert-tickets-in-groups) | 2470 |\n| 2940 | [找到 Alice 和 Bob 可以相遇的建築](https://leetcode.cn/problems/find-building-where-alice-and-bob-can-meet) | 2327 |\n| 3161 | [物塊放置查詢](https://leetcode.cn/problems/block-placement-queries) | 2513 |\n| 3165 | [不包含相鄰元素的子序列的最大和](https://leetcode.cn/problems/maximum-sum-of-subsequence-with-non-adjacent-elements) | 2698 |\n| 3410 | [刪除所有值為某個元素後的最大子陣列和](https://leetcode.cn/problems/maximize-subarray-sum-after-removing-all-occurrences-of-one-element) | 2844 |\n| 3479 | [水果成籃 III](https://leetcode.cn/problems/fruits-into-baskets-iii) | 2178 |\n| 3501 | [操作後最大活躍區段數 II](https://leetcode.cn/problems/maximize-active-section-with-trade-ii) | 2941 |\n| 3525 | [求出陣列的 X 值 II](https://leetcode.cn/problems/find-x-value-of-array-ii) | 2645 |\n\n**延伸閱讀**：通用、已驗證的介面可參考 [AtCoder Library `segtree.hpp`](https://github.com/atcoder/ac-library/blob/master/atcoder/segtree.hpp)；重點是合併運算需滿足結合律並提供單位元。\n\n## 本節重點速查\n\n- **存在理由**：樹狀陣列靠差分，需要**逆元**；線段樹靠拼接，只需要**幺半群**（結合律 + 單位元），**不需要交換律**。\n- **兩個能力**：(1) max/min/gcd 這類不可差分的聚合；(2) **節點存複合資訊**（2213 的 `len/pre/suf/best/lc/rc`）——後者才是線段樹不可替代的地方。\n- **定理 2 是核心**：每層至多 **2 個部分相交節點**（三個會導致中間那個被完全包含，矛盾）⇒ 訪問 $O(\\log n)$ 個節點、典範節點 $\\le 2\\log_2 n$ 個、且**不交且聯集為 $[l,r]$**。\n- **「不交」與「結合律」缺一不可**：沒有前者 sum 會重複計入，沒有後者合併順序會改變答案。\n- **單位元要驗證**：max ⇒ $-\\infty$、min ⇒ $+\\infty$、**gcd ⇒ 0**、sum ⇒ 0。寫之前先確認 `id() + x == x`。\n- **`pushUp` 順序 `t[2k] + t[2k+1]` 不可反**——在 sum/max 上測不出來，在 2213 上必錯。\n- **自訂 `Info` 要手動驗結合律**：三個葉子、兩種括號方式，結果必須相同。\n- **節點陣列開 $4n$**，不是 $2n$。這是第一大坑。\n- **樹上二分是獨門優勢（定理 4）**：`mx <= x` 剪枝 + 左子優先 ⇒ **$O(\\log n)$ 找第一個滿足條件的位置**。不要寫成外層二分的 $O(\\log^2 n)$。\n- **2286 的攤還（定理 5）**：填滿的排永不再訪（$\\le n$ 次）+ 每次至多一個未填滿的排（$\\le q$ 次）。**「昂貴迭代對應不可逆狀態變化」在本章已出現五次**（4.7、6.6、8.1、8.5、本節）。\n- **選擇順序**：能前綴和就前綴和 → 能樹狀陣列就樹狀陣列 → 不可差分或要複合資訊才線段樹 → 要區間改才 lazy（9.4）→ 值域大才動態開點（9.5）→ 不改資料且只查 max/min/gcd 就 ST 表（9.7）。\n",
        },
        {
          id: 64,
          title: "9.4 Lazy 線段樹（有區間更新）",
          summary:
            "## 這個技術解決什麼問題\n\n9.3 的線段樹只能**單點**修改：改一個位置，沿根路徑往上 `pushUp`，$O(\\log n)$。\n\n但如果要「把 $[l, r]$ 全部加 $v$」呢？照 9.3 的做法要改 $r - l + 1$ 片葉子，單次 $O(n\\log n)$——比暴力還糟。\n\n**懶標記的想法只有一句：**\n\n> **改到「整段都被包含」的節點就停下來，直接更新這個節點的聚合值，並在節點上記一筆「我欠子孫一個修改」的帳。只有真的要往下走時，才把帳結清。**\n\n由 9.3 定理 2，$[l, r]$ 只會拆出 $O(\\log n)$ 個典範節點，**在每個典範節點打一個標記就走人**，於是區間修改也是 $O(\\log n)$。\n\n**但這個「欠帳」的機制對聚合提出了三個新要求**（9.3 的幺半群不夠了）：\n\n1. **能整段套用**：給定標記 $f$、段長 $\\mathit{len}$、原聚合值 $x$，必須能在 $O(1)$ 算出 $\\mathrm{apply}(x, f, \\mathit{len})$。區間加求和 ⇒ $x + v\\cdot \\mathit{len}$ ✔；**區間加求 max ⇒ $x + v$ ✔；區間加求「不同值的個數」⇒ 算不出來 ❌。**\n2. **標記能合成**：一個節點可能連續欠好幾筆帳，必須能把它們壓成一筆。\n3. **套用與合併可交換**（分配律）：$\\mathrm{apply}(a \\oplus b, f) = \\mathrm{apply}(a, f) \\oplus \\mathrm{apply}(b, f)$。**這條保證了「在父節點打標記」與「在兩個子節點各打一次標記」等價**——沒有它，懶標記整套機制不成立。\n\n**滿足這三條的，數學上叫「幺半群上的作用（monoid action）」。** 本節的所有細節都是這三條的實作。\n\n**辨識訊號**：\n- 「把一整段全部加 $v$ / 全部設為 $v$」+ 區間聚合查詢 ⇒ 2502、LCP 52；\n- **掃描線**：每條邊讓一段的「覆蓋次數 $\\pm1$」，再查「目前被覆蓋的總長度」⇒ 850；\n- **DP 轉移的批次更新**：$dp$ 的候選值在加入新元素時整段同時變化 ⇒ 2547；\n- 全域的加、乘、查詢 ⇒ 1622（**這題是標記代數的極簡形式，見「C++17 模板」的說明**）。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「Lazy 線段樹」想：\n\n- **出現「把一整段全部加 / 全部設為某值」並且還要查詢區間聚合。** 如 1622「奇妙序列」要對全體做加、乘、查詢，正是 lazy 的舞台。\n- **掃描線類問題**：如 850「矩形面積 II」用掃描線時，每條邊讓某段「覆蓋次數 +1 / -1」，再查「目前被覆蓋的總長度」——典型區間更新 + 區間查詢。\n- **題目操作數很大、且每次操作影響一整段而非單點。**\n\n:::example 嗅到味道的瞬間：你想一次更新一整段，又不想 O(n) 改到底\n```cpp\n// 嗅到味道的瞬間：你想一次更新一整段，又不想 O(n) 改到底\n// update(l, r, +v);   // 整段加 v\n// 普通線段樹只能單點改 → 改用 lazy 打標延遲下放\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的 lazy 代表「欠孩子的什麼更新」？怎麼累積？\n2. apply / pushDown / pushUp 三者規則一致嗎？\n3. 每條走進孩子的路徑，我都先 pushDown 了嗎？\n\n三題都答得出來，就套對了。\n\n## 核心想法與直覺\n\n本節是線段樹的升級版：除了單點改，還要支援「一次把一整段區間全部加上某值」或「整段設為某值」這類**區間更新**。\n\n:::example 每個節點除了聚合值，多一個 lazy：尚未下放給孩子的待辦更新\n問題在哪？如果區間更新還是老老實實改到每個葉子，一次更新就 `O(n)`，跟暴力一樣慢。解法是「Lazy 懶標記（lazy tag）」，核心想法一句話：**更新到某個節點、而這節點的區間被完全覆蓋時，就先在它身上貼一張「欠你一筆」的便利貼，更新它自己的聚合值，但暫時不往下傳給孩子。** 等將來真的需要走進它的孩子時，再把這筆「下放」出去。\n\n```cpp\n// 每個節點除了聚合值，多一個 lazy：尚未下放給孩子的待辦更新\nstruct Node {\n    long long sum;  // 本區間目前的聚合值（已含自己的 lazy 效果）\n    long long lazy;  // 欠孩子的：之後要套到孩子上的增量\n};\n```\n:::\n\n\n\n:::example push down：把父的待辦更新下放給左右孩子，再清空\n「完全覆蓋就打標、不往下走」省下了大量遞迴。但有個必須遵守的規則：**任何時候要往孩子遞迴前，一定先把自己的 lazy 下放（push down），否則孩子讀到的是過時資料。** push down 做兩件事：把待辦套到兩個孩子的聚合值與它們的 lazy 上，然後清空自己的 lazy。\n\n```cpp\n// push down：把父的待辦更新下放給左右孩子，再清空\nvoid pushDown(int node, int leftLen, int rightLen) {\n    if (tree[node].lazy == 0) {\n        return;\n    }\n    long long t = tree[node].lazy;\n    apply(2 * node, t, leftLen);  // 套到左孩子（含更新它的 sum 與 lazy）\n    apply(2 * node + 1, t, rightLen);  // 套到右孩子\n    tree[node].lazy = 0;               // 自己的待辦已交付\n}\n```\n:::\n\n\n\n有了「打標 + 延遲下放」，區間更新與區間查詢都維持 `O(log n)`，因為每次仍只碰到 `O(log n)` 個節點。這就是「Lazy 線段樹」之所以快又通用的關鍵。\n\n## 狀態／資料結構定義\n\n每個節點多存一個**懶標記** `lz[k]`，語意是：\n\n> **`t[k]` 已經反映了 `lz[k]`；但 $k$ 的兩個子節點（及其子樹）還沒有。**\n\n這句話就是全部的約定，**寫錯任何一行 lazy 程式碼，本質上都是違反了它**。\n\n**三個必須成對定義的原語：**\n\n```cpp\n// 1) 把標記 f 套到「一個長度為 len、聚合值為 x 的段」上\nvoid applyTag(int k, int len, Tag f);\n// 2) 把標記 g 疊到節點已有的標記上（先 lz[k]，後 g）\nTag composeTag(Tag old, Tag g);\n// 3) 把 lz[k] 下推給兩個子節點，然後清空\nvoid pushDown(int k, int lLen, int rLen);\n```\n\n**「區間加、區間求和」的具體版本：**\n\n```cpp\nstruct LazySeg {\n    int n;\n    vector<long long> t, lz;                 // lz[k]：整段每個元素還欠加多少\n    explicit LazySeg(const vector<long long>& a)\n        : n(a.size()), t(4 * a.size(), 0), lz(4 * a.size(), 0) {\n        build(1, 0, n - 1, a);\n    }\n    void build(int k, int lo, int hi, const vector<long long>& a) {\n        if (lo == hi) { t[k] = a[lo]; return; }\n        int mid = (lo + hi) >> 1;\n        build(2 * k, lo, mid, a);\n        build(2 * k + 1, mid + 1, hi, a);\n        t[k] = t[2 * k] + t[2 * k + 1];\n    }\n    void applyTag(int k, int len, long long v) {\n        t[k] += v * len;                     // 整段套用：需要段長\n        lz[k] += v;                          // 標記合成：加法標記直接相加\n    }\n    void pushDown(int k, int lo, int mid, int hi) {\n        if (lz[k] == 0) return;\n        applyTag(2 * k, mid - lo + 1, lz[k]);\n        applyTag(2 * k + 1, hi - mid, lz[k]);\n        lz[k] = 0;                           // 清帳，不可漏\n    }\n    void update(int k, int lo, int hi, int l, int r, long long v) {\n        if (r < lo || hi < l) return;\n        if (l <= lo && hi <= r) { applyTag(k, hi - lo + 1, v); return; }\n        int mid = (lo + hi) >> 1;\n        pushDown(k, lo, mid, hi);\n        update(2 * k, lo, mid, l, r, v);\n        update(2 * k + 1, mid + 1, hi, l, r, v);\n        t[k] = t[2 * k] + t[2 * k + 1];\n    }\n    long long query(int k, int lo, int hi, int l, int r) {\n        if (r < lo || hi < l) return 0;\n        if (l <= lo && hi <= r) return t[k];\n        int mid = (lo + hi) >> 1;\n        pushDown(k, lo, mid, hi);            // 查詢也要 pushDown！\n        return query(2 * k, lo, mid, l, r) + query(2 * k + 1, mid + 1, hi, l, r);\n    }\n};\n```\n\n**`query` 也要 `pushDown`**——這是初學者第一個踩的坑。查詢會走進節點內部，內部節點的子樹還欠著帳，不結清就讀到舊值。（副作用是 `query` **不能宣告成 `const`**。）\n\n**常見標記代數一覽：**\n\n| 修改 | 標記 $f$ | 合成（先 $f_1$ 後 $f_2$） | 套用（段長 $\\mathit{len}$） |\n| --- | --- | --- | --- |\n| 區間加，查和 | $v$ | $v_1 + v_2$ | $x + v\\cdot \\mathit{len}$ |\n| 區間加，查 max | $v$ | $v_1 + v_2$ | $x + v$ |\n| **區間賦值**，查和 | $c$ | **$c_2$（後者吃掉前者）** | $c \\cdot \\mathit{len}$ |\n| **加 + 乘**（仿射） | $(m, b):\\ x\\mapsto mx+b$ | $(m_1m_2,\\ b_1m_2 + b_2)$ | $m\\cdot x + b\\cdot \\mathit{len}$ |\n| 賦值 + 加（混用） | `(hasAssign, c, v)` | **賦值會清掉待加的量** | 分兩種情形 |\n\n**「賦值 + 加」混用是最容易寫錯的組合。** 約定成 $x \\mapsto (\\text{有賦值} ? c : x) + v$，則疊上新標記時：新的是賦值 ⇒ 整個舊標記作廢，換成 `(true, c_new, 0)`；新的是加 ⇒ `v += v_new`（`hasAssign` 與 `c` 不動）。**先在紙上寫清楚語意，再寫程式。**\n\n## 不變量或正確性證明\n\n### 定理 1（懶標記的主不變量）\n\n令 $a[\\cdot]$ 為陣列的**真實**當前值。對節點 $k$，令 $A_k$ 為從根到 $k$ 的**嚴格祖先**的標記依序合成後的作用。\n\n**不變量 (L)**：任何時刻，\n$$A_k\\big(t[k]\\big) = \\bigoplus_{i \\in [\\mathit{lo}_k,\\, \\mathit{hi}_k]} a[i],$$\n且 `lz[k]` 已經反映在 `t[k]` 中、尚未反映在子樹中。\n\n換一種說法：**`t[k]` 是「假裝所有祖先的欠帳都不存在」時，這一段的正確聚合值。** 根節點沒有祖先，故 $t[1]$ 永遠是全域的真實聚合值。\n\n**維護的四個地方：**\n\n- **`applyTag(k, len, f)`（節點 $k$ 被整段修改）**：$t[k] \\leftarrow \\mathrm{apply}(t[k], f, \\mathit{len})$、$\\mathit{lz}[k] \\leftarrow \\mathrm{compose}(\\mathit{lz}[k], f)$。$k$ 的真實聚合值確實被 $f$ 作用了一次，$t[k]$ 跟著變 ✔；子孫的 (L) 不變，因為它們的祖先鏈剛好多了一個 $f$，而 $A$ 也跟著多了一個 $f$ ✔。**這一步的合法性正是「要求 1（能整段套用）」。**\n- **`pushDown(k)`**：把 $\\mathit{lz}[k]$ 套到兩個子節點再清空。子節點的 $t$ 被 $f$ 作用一次，而它們的 $A$ 少了一個 $f$，**兩者恰好抵消** ✔。$k$ 自己的 $t[k]$ 不變、$A_k$ 不變 ✔。\n- **`pushUp(k)`**：$t[k] \\leftarrow t[2k] \\oplus t[2k+1]$。此處要求 $A_{2k} = A_{2k+1} = A_k \\circ \\mathit{lz}[k]$，而 `pushUp` 只在 `pushDown` 之後（$\\mathit{lz}[k] = \\text{id}$）呼叫，故 $A_{2k} = A_k$，兩子的 $t$ 直接合併就是 $k$ 的 $t$ ✔。**這解釋了為什麼「先 `pushDown` 再遞迴、回來再 `pushUp`」的順序不能動。**\n- **`query` 走進內部節點**：必須先 `pushDown`，否則讀到的子節點 $t$ 少算了 $\\mathit{lz}[k]$ ✔。\n\n$\\square$\n\n**(L) 點名的關鍵前提是「$A_k$ 對子樹的每個位置一視同仁」**——祖先的標記作用在整段上，不會只作用在半段。**這正是「分配律（要求 3）」在證明中的角色**：$A_k(t[2k] \\oplus t[2k+1]) = A_k(t[2k]) \\oplus A_k(t[2k+1])$。若沒有分配律，「在父節點打一個標記」與「在兩個子節點各打一次」就不等價，(L) 的第二條 bullet 立刻失效。\n\n### 定理 2（區間修改是 $O(\\log n)$）\n\n*證明*。修改遞迴與 9.3 的查詢遞迴**形狀完全相同**（不交則返回、被包含則處理、否則分兩半）。由 9.3 定理 2，訪問節點數 $O(\\log n)$。每個節點額外做 $O(1)$ 的 `pushDown` / `applyTag` / `pushUp` ✔。$\\square$\n\n**注意這是最壞情況界，不是攤還界。** `pushDown` 不會累積債務：每次訪問一個節點就結清它的帳，而訪問總數已被定理 2 限制住。\n\n### 定理 3（仿射標記的合成公式）\n\n*設定*：標記 $(m, b)$ 表示 $x \\mapsto mx + b$（**先乘後加**）。\n\n*命題*：先套 $(m_1, b_1)$ 再套 $(m_2, b_2)$，等於套 $(m_1 m_2,\\ b_1 m_2 + b_2)$。\n\n*證明*：$m_2\\big(m_1 x + b_1\\big) + b_2 = (m_1m_2)x + (b_1m_2 + b_2)$ ✔。$\\square$\n\n*套用到「長度 $\\mathit{len}$ 的段和」*：$\\sum (m x_i + b) = m\\sum x_i + b\\cdot \\mathit{len}$ ✔——**分配律成立，所以仿射變換可以當懶標記。**\n\n**「先乘後加」是一個約定，不是定理。** 若你定義成 $x \\mapsto m(x + b)$，合成公式就變成 $(m_1m_2,\\ b_1 + b_2/m_1)$——**需要除法，在模運算下要求逆元存在**。**選 $x \\mapsto mx + b$，永遠。**\n\n**推論（`pushDown` 的順序）**：把父標記 $(m, b)$ 下傳給已有標記 $(m', b')$ 的子節點，正確結果是 $(m'm,\\ b'm + b)$——**先乘再加，順序寫反是本節最常見的 WA。**\n\n### 定理 4（區間賦值的標記吸收律）\n\n*命題*：賦值標記 $C_c : x \\mapsto c$ 滿足 $g \\circ C_c = C_{g(c)}$ 且 $C_c \\circ g = C_c$（後套的賦值吃掉一切）。\n\n*證明*：$C_c(g(x)) = c$，故 $C_c \\circ g = C_c$ ✔；$g(C_c(x)) = g(c)$，為常數映射 ✔。$\\square$\n\n**實作意涵**：疊上賦值標記時，**必須把節點上待處理的加法標記整個丟棄**（而不是保留）。忘記丟棄就會出現「先賦值成 5，再被之前欠的 +3 補上變成 8」的幽靈 bug。**在紙上寫清楚「我的標記代表哪個函數」，然後套定理 4 推導合成規則——不要憑感覺拼。**\n\n### 定理 5（850 掃描線：為什麼**不需要**也**不能**下傳標記）\n\n這是本節最值得單獨拿出來講的一題。\n\n*設定*：矩形面積並。把 $y$ 座標離散化成 $m$ 個值，線段樹的葉子代表**相鄰兩個 $y$ 之間的縫隙**（不是點）。掃描 $x$ 時，矩形的左邊讓 $[y_1, y_2)$ 的「覆蓋次數 $+1$」、右邊 $-1$。每次要查**根**的「被覆蓋總長度」。\n\n*結構*：節點存 `cnt[k]`（**恰好完整覆蓋 $\\mathrm{seg}(k)$ 的區間有幾個**，即打在 $k$ 上的次數淨值）與 `len[k]`（$\\mathrm{seg}(k)$ 中被覆蓋的長度）。\n\n```cpp\nvoid pushUp(int k, int lo, int hi) {\n    if (cnt[k] > 0)      len[k] = ys[hi + 1] - ys[lo];      // 整段被蓋滿\n    else if (lo == hi)   len[k] = 0;                        // 葉子且無覆蓋\n    else                 len[k] = len[2 * k] + len[2 * k + 1];\n}\n```\n\n**不變量 (C)**：$\\mathit{cnt}[k]$ 等於「典範分解**恰好包含節點 $k$**」的活躍區間數量；\n$$\\mathit{len}[k] = \\big|\\{\\,y \\in \\mathrm{seg}(k) : y \\text{ 被某個典範分解落在 } k \\text{ 子樹內的活躍區間覆蓋}\\,\\}\\big|.$$\n\n*證明（對節點高度歸納）*。\n- $\\mathit{cnt}[k] > 0$ ⇒ 有區間完整覆蓋 $\\mathrm{seg}(k)$ ⇒ 整段都被覆蓋 ⇒ $\\mathit{len}[k] = |\\mathrm{seg}(k)|$ ✔（**注意此時不能寫成 $\\mathit{len}[2k] + \\mathit{len}[2k+1]$，因為子樹並不知道父親被蓋滿了**）。\n- $\\mathit{cnt}[k] = 0$ ⇒ 沒有區間的典範分解正好是 $k$ ⇒ 落在 $k$ 子樹內的典範節點都在兩個子樹裡，由歸納假設兩邊各自正確且**不交**，相加即得 ✔。\n- 葉子且 $\\mathit{cnt} = 0$ ⇒ $0$ ✔。$\\square$\n\n**根的正確性**：任一活躍區間的典範分解**不重不漏**地落在某些節點上（9.3 定理 2），故 $\\mathit{len}[1]$ 恰為被至少一個活躍區間覆蓋的總長度 ✔。\n\n**為什麼不能 `pushDown`**：修改是**成對的 $+1/-1$**，$\\mathit{cnt}$ 可以減回 $0$。**若把 $+1$ 下傳給子節點，後來的 $-1$ 打在哪裡就變成一筆糊塗帳**（$-1$ 的典範分解可能與 $+1$ 的不同——實際上在本題中相同，但一旦下傳過，節點上的資訊就對不起來了）。**上面的 `pushUp` 之所以能只靠 `cnt[k] > 0` 的三分支就恢復正確值，正是因為標記從不離開它被打上的那個節點。**\n\n**這是一個重要的一般教訓：懶標記不是唯一的區間修改手法。當標記「可撤銷」且「只查詢根」時，『標記永駐 + 特殊 pushUp』往往更簡單、更安全。** 掃描線類問題（矩形面積並、矩形周長並、天際線）幾乎都用這一招。\n\n### 定理 6（2547：DP 轉移的區間批次更新）\n\n*問題*：把陣列分成若干段，每段代價 $= k + (\\text{段長} - \\text{段內恰好出現一次的元素個數})$，求最小總代價。\n\n令 $\\mathrm{tr}(l, r) = \\sum_{v:\\ \\mathrm{cnt}_v \\ge 2} \\mathrm{cnt}_v$（段內出現 $\\ge 2$ 次的元素的總個數），則 $dp[i] = \\min_{0 \\le j < i}\\big(dp[j] + k + \\mathrm{tr}(j+1, i)\\big)$。\n\n**命題**：每把一個新元素 $a_i$ 併進來，函數 $g(j) = dp[j] + \\mathrm{tr}(j+1, i)$ 只在 $O(1)$ 段區間上變化，且變化量是常數。\n\n*證明*。令 $p_1$ 為 $a_i$ 在 $[0, i-1]$ 的最後一次出現位置，$p_2$ 為倒數第二次（不存在則設為 $-\\infty$ 對應的空區間）。對每個 $j$，考慮 $a_i$ 在 $[j+1, i-1]$ 中的出現次數 $c_j$：\n- $j \\le p_2 - 1$ ⇒ $c_j \\ge 2$ ⇒ 加入第 $c_j+1$ 個 ⇒ $\\mathrm{tr}$ **$+1$**；\n- $p_2 \\le j \\le p_1 - 1$ ⇒ $c_j = 1$ ⇒ 該值從「不計入」變成「計入 2 個」 ⇒ $\\mathrm{tr}$ **$+2$**；\n- $j \\ge p_1$ ⇒ $c_j = 0$ ⇒ 該值仍只出現一次 ⇒ $\\mathrm{tr}$ **$+0$** ✔。\n\n三段互不重疊且覆蓋 $[0, i-1]$ ✔。$\\square$\n\n**於是每一步只要兩次「區間加」，再查一次「全域最小」，$O(n\\log n)$ 完成。** $dp[i] = k + \\min_{j} g(j)$，把 $dp[i]$ 寫回線段樹的位置 $i$。\n\n**這個模式（「DP 的候選值隨著狀態推進而整段平移，用 lazy 線段樹維護 min」）在 DP 章會反覆出現。** 本節提供的是機制，**辨識的關鍵是「新元素只讓 $O(1)$ 段候選值發生常數變化」——先驗證這件事，再決定要不要上線段樹。**\n\n## 逐步演算法\n\n:::example apply：把「整段加 v」套到一個長度 len 的節點上\n**1. 狀態表示：每個節點同時存聚合值與 lazy。** 並寫一個 `apply`，把一筆更新套到「單一節點」上（同時改 sum 與累積 lazy）。\n\n```cpp\n// apply：把「整段加 v」套到一個長度 len 的節點上\nvoid apply(int node, long long v, int len) {\n    tree[node].sum += v * len;  // 區間和：每個元素都加 v\n    tree[node].lazy += v;       // 把這筆待辦累積到該節點\n}\n```\n:::\n\n\n\n:::example 區間更新：把 [l,r] 整段加上 v\n**2. 區間更新：完全覆蓋就打標返回；部分覆蓋先 pushDown 再遞迴。**\n\n```cpp\n// 區間更新：把 [l,r] 整段加上 v\nvoid update(int node, int lo, int hi, int l, int r, long long v) {\n    if (r < lo || hi < l) {\n        return;  // 不相交\n    }\n    if (l <= lo && hi <= r) {\n        apply(node, v, hi - lo + 1);\n        return;\n    }  // 完全覆蓋 → 打標\n    int mid = (lo + hi) / 2;\n    pushDown(node, mid - lo + 1, hi - mid);  // 往下前先下放\n    update(2 * node, lo, mid, l, r, v);\n    update(2 * node + 1, mid + 1, hi, l, r, v);\n    tree[node].sum = tree[2 * node].sum + tree[2 * node + 1].sum;  // pushUp\n}\n```\n:::\n\n\n\n:::example 區間查詢：[l,r] 的和\n**3. 區間查詢：同樣在往孩子走之前先 pushDown。**\n\n```cpp\n// 區間查詢：[l,r] 的和\nlong long query(int node, int lo, int hi, int l, int r) {\n    if (r < lo || hi < l) {\n        return 0;\n    }\n    if (l <= lo && hi <= r) {\n        return tree[node].sum;\n    }\n    int mid = (lo + hi) / 2;\n    pushDown(node, mid - lo + 1, hi - mid);  // 讀孩子前先下放\n    return query(2 * node, lo, mid, l, r) +\n                  query(2 * node + 1, mid + 1, hi, l, r);\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- **`apply`、`pushDown`、`pushUp` 三者必須前後一致**：apply 怎麼套，pushDown 就怎麼下放，pushUp 怎麼合併。三者其一寫錯，答案就會錯。\n- 「區間加」與「區間賦值」的 lazy 合成規則不同：加是累加，賦值是覆蓋（且賦值的 lazy 要用一個「無效值」標記是否有設定）。\n- 別忘了任何進入孩子的路徑（更新或查詢）都要先 pushDown，漏一處就讀到舊值。\n- 複雜度：區間更新與查詢皆 `O(log n)`，空間 `O(n)`。\n\n## C++17 模板\n\n**模板一：850 矩形面積 II（掃描線 + 標記永駐）**\n\n```cpp\nclass Solution {\n    vector<int> ys;                 // 離散化後的 y 座標\n    vector<int> cnt;                // 完整覆蓋本節點的活躍區間數\n    vector<long long> cov;          // 本節點內被覆蓋的長度\n    // 葉子 k 代表縫隙 [ys[lo], ys[lo+1])\n    void pushUp(int k, int lo, int hi) {\n        if (cnt[k] > 0)      cov[k] = ys[hi + 1] - ys[lo];\n        else if (lo == hi)   cov[k] = 0;\n        else                 cov[k] = cov[2 * k] + cov[2 * k + 1];\n    }\n    void update(int k, int lo, int hi, int l, int r, int d) {\n        if (r < lo || hi < l) return;\n        if (l <= lo && hi <= r) { cnt[k] += d; pushUp(k, lo, hi); return; }\n        int mid = (lo + hi) >> 1;\n        update(2 * k, lo, mid, l, r, d);\n        update(2 * k + 1, mid + 1, hi, l, r, d);\n        pushUp(k, lo, hi);          // 沒有 pushDown，只有 pushUp\n    }\npublic:\n    int rectangleArea(vector<vector<int>>& rectangles) {\n        const long long MOD = 1'000'000'007LL;\n        for (auto& r : rectangles) { ys.push_back(r[1]); ys.push_back(r[3]); }\n        sort(ys.begin(), ys.end());\n        ys.erase(unique(ys.begin(), ys.end()), ys.end());\n        int m = ys.size() - 1;                       // 縫隙數\n        if (m <= 0) return 0;\n        cnt.assign(4 * m, 0);\n        cov.assign(4 * m, 0);\n\n        // 事件：(x, y1, y2, +1/-1)\n        vector<array<int, 4>> ev;\n        ev.reserve(rectangles.size() * 2);\n        for (auto& r : rectangles) {\n            ev.push_back({r[0], r[1], r[3], 1});\n            ev.push_back({r[2], r[1], r[3], -1});\n        }\n        sort(ev.begin(), ev.end(),\n             [](const array<int, 4>& a, const array<int, 4>& b) { return a[0] < b[0]; });\n\n        long long area = 0;\n        int prevX = ev[0][0];\n        for (auto& e : ev) {\n            area = (area + cov[1] % MOD * ((e[0] - prevX) % MOD)) % MOD;\n            prevX = e[0];\n            int l = lower_bound(ys.begin(), ys.end(), e[1]) - ys.begin();\n            int r = int(lower_bound(ys.begin(), ys.end(), e[2]) - ys.begin()) - 1;\n            update(1, 0, m - 1, l, r, e[3]);\n        }\n        return int(area % MOD);\n    }\n};\n```\n\n**注意 `r` 要減 1**：$y$ 區間 $[y_1, y_2)$ 對應的是縫隙下標 $[\\,l,\\ r-1\\,]$。**這個 off-by-one 是掃描線的頭號錯誤。**\n\n**模板二：區間賦值 + 區間求和（LCP 52 的正序解法）**\n\n```cpp\nstruct AssignSeg {\n    int n;\n    vector<long long> sum;\n    vector<int> tag;                 // -1 表示無標記，否則表示「整段賦值為 tag」\n    explicit AssignSeg(int n) : n(n), sum(4 * n, 0), tag(4 * n, -1) {}\n    void applyTag(int k, int len, int c) {\n        sum[k] = 1LL * c * len;      // 定理 4：整段套用\n        tag[k] = c;                  // 後者吃掉前者\n    }\n    void pushDown(int k, int lo, int mid, int hi) {\n        if (tag[k] == -1) return;\n        applyTag(2 * k, mid - lo + 1, tag[k]);\n        applyTag(2 * k + 1, hi - mid, tag[k]);\n        tag[k] = -1;\n    }\n    void assign(int k, int lo, int hi, int l, int r, int c) {\n        if (r < lo || hi < l) return;\n        if (l <= lo && hi <= r) { applyTag(k, hi - lo + 1, c); return; }\n        int mid = (lo + hi) >> 1;\n        pushDown(k, lo, mid, hi);\n        assign(2 * k, lo, mid, l, r, c);\n        assign(2 * k + 1, mid + 1, hi, l, r, c);\n        sum[k] = sum[2 * k] + sum[2 * k + 1];\n    }\n    long long total() const { return sum[1]; }\n};\n```\n\n（**對照 8.5 定理 3**：LCP 52 也可以**倒序 + `set`** 二十行解決，複雜度更好。**線段樹是通用解，倒序是這題的巧解——兩個都該會，並知道各自的適用邊界：中途要查詢就只能用線段樹。**）\n\n**模板三：1622 奇妙序列——只有標記，沒有樹**\n\n1622 的三種操作（`addAll`、`multAll`）**都作用在整個序列上**。既然永遠是「整段」，樹就退化成一個節點：**只要維護一個全域仿射變換 $(m, b)$，把 `append(v)` 存成「原像」即可。**\n\n```cpp\nclass Fancy {\n    static constexpr long long MOD = 1'000'000'007LL;\n    vector<long long> raw;                       // 存的是「還原到初始變換下」的值\n    long long m = 1, b = 0;                      // 全域仿射：x -> m*x + b\n    static long long qpow(long long a, long long e) {\n        long long r = 1;\n        for (a %= MOD; e; e >>= 1, a = a * a % MOD)\n            if (e & 1) r = r * a % MOD;\n        return r;\n    }\npublic:\n    void append(int val) {\n        long long inv = qpow(m, MOD - 2);        // m 在模質數下的逆元\n        raw.push_back((val - b % MOD + MOD) % MOD * inv % MOD);\n    }\n    void addAll(int inc)  { b = (b + inc) % MOD; }\n    void multAll(int mul) { m = m * mul % MOD; b = b * mul % MOD; }   // 定理 3\n    int getIndex(int idx) {\n        if (idx >= (int)raw.size()) return -1;\n        return int((m * raw[idx] + b) % MOD);\n    }\n};\n```\n\n**`multAll` 的兩行就是定理 3 的合成公式**（把 $(m, b)$ 再套上 $(\\mathit{mul}, 0)$，得 $(m\\cdot \\mathit{mul},\\ b\\cdot \\mathit{mul})$）。**這題的價值在於它把懶標記的代數剝到只剩骨頭：標記合成、標記套用、逆元還原——沒有樹的干擾。搞懂它，`pushDown` 就不再神秘。**\n\n## 時間與空間複雜度\n\n| 操作 | 時間 | 說明 |\n| --- | --- | --- |\n| `build` | $O(n)$ | |\n| 區間修改 | $O(\\log n)$ **最壞** | 定理 2，不是攤還 |\n| 區間查詢 | $O(\\log n)$ | 含 `pushDown` |\n| 空間 | $\\mathbf{8n}$ | `t` 與 `lz` 各 $4n$ |\n\n**常數比 9.3 的無 lazy 版明顯大**：每個內部節點多了一次 `pushDown`（讀寫兩個子節點）。實測約慢 1.5～2 倍。**不需要區間修改就不要寫 lazy 版**——多出來的每一行都是 bug 的機會。\n\n**各題**：\n\n| 題目 | 時間 | 備註 |\n| --- | --- | --- |\n| 850 矩形面積 II | $O(n\\log n)$ | 排序 $2n$ 個事件 + $2n$ 次 $O(\\log n)$ 修改 |\n| LCP 52 二叉搜尋樹染色 | $O((n+q)\\log n)$ | **倒序 + `set` 是 $O(n\\log n)$，更好** |\n| 2547 拆分陣列的最小代價 | $O(n\\log n)$ | 每步兩次區間加 + 一次全域 min |\n| 2502 設計記憶體分配器 | $O(q\\log n)$ | $n, q \\le 1000$，**暴力 $O(nq)$ 也能過** |\n| 1622 奇妙序列 | $O(\\log \\mathit{MOD})$ / 次 | **不需要樹**，瓶頸是求逆元的快速冪 |\n\n**掃描線的空間**：離散化後縫隙數 $m \\le 2n$，線段樹 $4m = 8n$ 個節點。$n = 200$（850 的規模）完全不是問題，但推廣到 $n = 10^5$ 的天際線類問題時要留意。\n\n**1622 的優化**：`append` 每次呼叫快速冪是 $O(\\log \\mathit{MOD}) \\approx 30$ 次乘法。**可以改成「預先累積 $m$ 的前綴積並最後一次性求逆」或「維護 $m^{-1}$ 隨 `multAll` 同步更新」**，把 `append` 降到 $O(1)$。\n\n## 常見錯誤與邊界條件\n\n- **`query` 忘記 `pushDown`**。查詢走進內部節點就必須先結清欠帳。**第一大坑。** 副作用：`query` 不能是 `const`。\n- **`pushDown` 後忘記清空標記**。帳結了兩次，值翻倍。\n- **`applyTag` 忘記乘段長**。區間加求「和」時是 $x + v\\cdot \\mathit{len}$；求 max 時才是 $x + v$。**兩者的公式不同，抄錯模板必錯。**\n- **標記合成順序寫反**。仿射標記下傳給子節點是 $(m'm,\\ b'm + b)$——**先乘後加**（定理 3 推論）。\n- **賦值標記沒有吃掉待加標記**（定理 4）。混用 assign 與 add 時，賦值必須清空待加量。\n- **賦值標記用 `0` 表示「無標記」**。若題目允許賦值成 `0`，這就撞號了。**用 `-1` 或另開一個 `bool`。**\n- **掃描線的區間端點沒減 1**。$[y_1, y_2)$ 對應縫隙下標 $[l, r-1]$。\n- **掃描線寫了 `pushDown`**（定理 5）。**標記必須永駐**；下傳會讓成對的 $-1$ 無處可扣。\n- **掃描線的 `pushUp` 三分支寫成兩分支**。`cnt[k] > 0` 時**不能**用 `cov[2k] + cov[2k+1]`。\n- **葉子代表點而不是縫隙**。掃描線的線段樹必須以「相鄰座標之間的縫隙」為葉子，否則長度算不出來。\n- **標記型別溢位**。區間加的標記累積後可能超出 `int`；求和更要 `long long`。\n- **模運算的負數**。減法後要 `(x % MOD + MOD) % MOD`。\n- **`build` 忘記初始化 `lz`**。`vector` 預設歸零可以救加法標記，但賦值標記的「無標記」是 `-1`，必須顯式 `assign`。\n- **在 `pushDown` 之前呼叫 `pushUp`**。順序永遠是「`pushDown` → 遞迴 → `pushUp`」。\n- **空區間**。$l > r$（例如掃描線裡 $y_1 = y_2$ 的退化矩形）要直接返回，否則 `update(1, 0, m-1, l, r)` 的邊界判斷可能出錯。\n\n## 與相似技巧的比較\n\n- **與 9.3 無 lazy 線段樹**：只有**單點**修改就不需要 lazy——修改只影響一條根路徑，沿路 `pushUp` 即可。**判準只有一句：修改是點還是段。** 是點就別寫 lazy（少一半程式、少一半 bug、快 1.5 倍）。\n- **與 3. 差分**：「區間加、最後一次性查全部」用差分 $O(n + q)$，比什麼都快。**差分不支援中途查詢、不支援區間賦值。** 能用就用。\n- **與 9.1 樹狀陣列 + 差分**：「區間加、單點查」一個樹狀陣列；「區間加、區間和」兩個樹狀陣列。**比 lazy 線段樹短得多、快 2～3 倍。** 但**只能做區間加**（可差分的修改），**做不了區間賦值**。**這是選擇的分界線。**\n- **與 8.5 區間並查集**：「每格只需被處理一次」時 $O((n+q)\\alpha)$，遠快於 lazy。**先檢查 8.5 的前提是否成立**（一次性標記、無撤銷、無累加）；成立就別上線段樹。LCP 52 就是這樣。\n- **與珂朵莉樹（ODT）**：`map` 維護「值相同的極長區間」，區間賦值 $O(\\log n)$ 攤還（隨機資料下），**程式短得多，而且能直接遍歷區間**。缺點是複雜度依賴資料隨機性，**被針對性構造的資料可以卡到 $O(n)$**。LeetCode 上通常安全，競賽中要小心。\n- **與 Segment Tree Beats**：處理「區間取 min／max 並查詢和」這類**標記不滿足分配律**的操作。**它是本節三個要求被打破後的補救方案**，複雜度 $O(n\\log^2 n)$，實作複雜度高一個檔次。LeetCode 上極少需要。\n- **與分塊（第 11 章）**：$O(\\sqrt n)$，更慢但**不要求分配律、不要求標記可合成**。**當你發現「標記無法 $O(1)$ 套用到整段」時，分塊往往是唯一出路。**\n- **與掃描線 + 「標記永駐」（定理 5）**：**當修改成對可撤銷且只查根時，這個寫法比一般 lazy 更簡單也更安全。** 矩形面積並／周長並／天際線都用它。**不要看到區間修改就反射性地寫 `pushDown`。**\n\n## 代表例題\n\n**題號**：LCP 52、850、1622、2502、2547\n\n有動態更新與區間查詢；練習先離散化值域，再確認查詢/更新的閉開區間。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| LCP 52 | [二叉搜索樹染色](https://leetcode.cn/problems/QO5KpG)（Premium） |  |\n| 850 | [矩形面積 II](https://leetcode.cn/problems/rectangle-area-ii) | 2236 |\n| 1622 | [奇妙序列](https://leetcode.cn/problems/fancy-sequence) | 2476 |\n| 2502 | [設計記憶體分配器](https://leetcode.cn/problems/design-memory-allocator) | 1746 |\n| 2547 | [拆分陣列的最小代價](https://leetcode.cn/problems/minimum-cost-to-split-an-array) | 2020 |\n| 2569 | [更新陣列後處理求和查詢](https://leetcode.cn/problems/handling-sum-queries-after-update) | 2398 |\n| 2589 | [完成所有任務的最少時間](https://leetcode.cn/problems/minimum-time-to-complete-all-tasks) | 2381 |\n| 2916 | [子陣列不同元素數目的平方和 II](https://leetcode.cn/problems/subarrays-distinct-element-sum-of-squares-ii) | 2816 |\n| 3454 | [分割正方形 II](https://leetcode.cn/problems/separate-squares-ii) | 2671 |\n| 3569 | [分割陣列後不同質數的最大數目](https://leetcode.cn/problems/maximize-count-of-distinct-primes-after-split) | 2697 |\n| 3721 | [最長平衡子陣列 II](https://leetcode.cn/problems/longest-balanced-subarray-ii) | 2724 |\n\n**延伸閱讀**：通用懶標記介面可參考 [AtCoder Library `lazysegtree.hpp`](https://github.com/atcoder/ac-library/blob/master/atcoder/lazysegtree.hpp)；使用前要明確定義映射、標記合成順序與單位標記。\n\n## 本節重點速查\n\n- **一句話約定**：**`t[k]` 已含 `lz[k]`，子樹尚未含**。所有 lazy 的 bug 都是違反這句話。\n- **三個代數要求**：(1) 能用「段長 + 標記」$O(1)$ 整段套用；(2) 標記能合成；(3) **分配律** $\\mathrm{apply}(a \\oplus b, f) = \\mathrm{apply}(a,f)\\oplus \\mathrm{apply}(b,f)$。**第三條是「父節點打一個標記 $\\equiv$ 兩個子節點各打一次」的保證，缺了它整套機制不成立。**\n- **順序永遠是「`pushDown` → 遞迴 → `pushUp`」**，而且 **`query` 也要 `pushDown`**（所以不能 `const`）。\n- **`applyTag` 求和要乘段長，求 max 不乘**。抄錯模板必錯。\n- **仿射標記約定 $x \\mapsto mx + b$**，合成 $(m_1m_2,\\ b_1m_2+b_2)$（定理 3）。**先乘後加**，寫反是頭號 WA。定義成 $m(x+b)$ 會需要除法。\n- **賦值標記吃掉一切**（定理 4）：$C_c \\circ g = C_c$。混用 assign+add 時賦值必須清空待加量。**「無標記」不要用 `0` 表示。**\n- **掃描線用「標記永駐 + 三分支 `pushUp`」，不要 `pushDown`（定理 5）**：`cnt[k] > 0` ⇒ 整段長；葉子且 `cnt == 0` ⇒ 0；否則兩子相加。**葉子代表縫隙不是點；$[y_1,y_2)$ 對應縫隙下標 $[l, r-1]$。**\n- **2547 的模式**：新元素只讓 $O(1)$ 段候選值發生常數變化（$+1$／$+2$／$+0$，由 $a_i$ 的最後兩次出現位置切分）⇒ 區間加 + 全域 min。**上線段樹之前先驗證這個「$O(1)$ 段」的性質。**\n- **1622 沒有樹**：全域仿射 + 存原像 + 模逆元。**它是懶標記代數的極簡形式，搞懂它 `pushDown` 就不神秘。**\n- **空間 $8n$**（`t` 與 `lz` 各 $4n$），常數比無 lazy 版慢 1.5～2 倍。\n- **選擇順序**：差分（離線、只查一次）→ 樹狀陣列 + 差分（只有區間**加**）→ 8.5 區間並查集（每格只處理一次）→ ODT（區間賦值、資料不刁鑽）→ **lazy 線段樹（通用）** → Segment Tree Beats / 分塊（分配律被打破）。\n",
        },
        {
          id: 65,
          title: "9.5 動態開點線段樹",
          summary:
            "## 這個技術解決什麼問題\n\n9.3、9.4 的線段樹都假設「值域就是陣列下標 $[0, n-1]$」。但日程表系列（729／731／732）的時間座標到 $10^9$，掉落方塊（699）的位置也一樣——**開一個 $4\\times10^9$ 的陣列是不可能的。**\n\n關鍵觀察是：**值域很大，但真正被碰到的點很稀疏。** $q$ 次操作最多只涉及 $2q$ 個端點，而線段樹只需要「真的被走過」的那些節點。\n\n於是有三條路：\n\n| 做法 | 前提 | 代價 |\n| --- | --- | --- |\n| **離散化 + 靜態線段樹** | **離線**（所有操作事先已知） | 最省，$O(q)$ 節點 |\n| **動態開點線段樹** | 無 | $O(q\\log V)$ 節點，**支援線上** |\n| `map` / `set` 維護不交區間 | 操作是「整段賦值」 | 程式最短，複雜度依情形 |\n\n**動態開點的想法只有一句：**\n\n> **保持完整值域 $[0, V)$ 的邏輯樹形，但節點按需分配——沒有被建立的節點，代表一整段「什麼都沒發生」。**\n\n於是 `lc`／`rc` 從「$2k$／$2k+1$」變成兩個存下來的下標，`0` 表示「還不存在」。\n\n**為什麼必須是「線上」才值得用它**：日程表系列每次 `book` 都要**立刻**回答能不能訂，你**沒辦法**先收齊所有預訂再離散化。**離線可離散化時，靜態線段樹又快又省，不要為了用工具而用工具。**\n\n**辨識訊號**：\n- 座標／時間值域到 $10^9$，但操作次數只有 $10^4 \\sim 10^5$；\n- **邊加邊問**，無法預先收集所有端點（729／731／732）；\n- 大值域上的區間取 max 與查詢（699）；\n- 值域巨大的計數桶（2276 統計區間中的整數數目、2770）。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「動態開點線段樹」想：\n\n- **值域（座標 / 時間）很大，但實際用到的點稀疏。** 如 715「Range 模組」、729「我的日程安排表 I」，座標範圍動輒到 `10^9`，整段標記「被佔用 / 空閒」。\n- **線上（online）加入區間並即時查詢有無衝突。** 日程表系列 729/731/732 都要邊加邊問「這段能不能訂」，無法事先離散化所有區間。\n- **「掉落方塊」這種座標連續且大的疊放問題**（699），需要在大值域上做區間取 max 與查詢。\n\n:::example 嗅到味道的瞬間：座標到 1e9，但只會碰到少數幾段\n```cpp\n// 嗅到味道的瞬間：座標到 1e9，但只會碰到少數幾段\n// 普通線段樹開 4 * 1e9 → 記憶體爆炸\n// 改用動態開點：走到哪、開點到哪\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的值域有多大？實際會碰到的點是不是很稀疏？\n2. 我能不能離散化？（能就未必需要動態開點）\n3. 我在「用到孩子」時有沒有正確按需開點？\n\n三題都答得出來，就套對了。\n\n## 核心想法與直覺\n\n本節介紹「動態開點線段樹」。它要解決一個很實際的麻煩：有時候**值域（座標範圍）非常大**，例如座標可以到 `10^9`，但你實際只會碰到少少幾個位置。如果照普通線段樹開 `4n` 大小的陣列，n 高達十億，記憶體直接爆掉。\n\n核心想法一句話：**樹的節點不要一開始就全部建好，而是「走到哪、用到哪，才動態 new 出來」。** 沒被碰過的區間，根本不分配節點。\n\n:::example 動態開點：節點顯式記錄左右孩子；0 代表「還沒建」\n實作上不再用 `2 * node / 2 * node + 1` 這種固定下標，而是每個節點明確存「左右孩子的編號」，沒孩子時是 0（代表尚未建立）：\n\n```cpp\n// 動態開點：節點顯式記錄左右孩子；0 代表「還沒建」\nstruct Node {\n    long long sum;            // 本區間聚合值\n    int left = 0, right = 0;  // 左右孩子在 pool 裡的下標，0 = 不存在\n};\nvector<Node> pool(1);  // pool[0] 當作「空節點」，根從 pool[1] 開始\n```\n:::\n\n\n\n:::example 用到孩子才開點：若 left 為 0，就新增一個節點並記下編號\n關鍵動作是「需要往孩子走、但孩子還不存在時，才現場建立它」：\n\n```cpp\n// 用到孩子才開點：若 left 為 0，就新增一個節點並記下編號\nint leftChild(int node) {\n    if (pool[node].left == 0) {\n        pool.push_back(Node());             // 新增空節點\n        pool[node].left = pool.size() - 1;  // 連上去\n    }\n    return pool[node].left;\n}\n```\n:::\n\n\n\n因為只有被真正觸及的路徑才建節點，總節點數約是「操作數 × log(值域)」，而不是值域大小。這讓我們在不離散化的情況下，也能直接在 `[0, 10^9]` 上做區間更新與查詢，單次操作仍是 `O(log(值域))`。它常與 lazy 標記合用，支援區間更新。\n\n## 狀態／資料結構定義\n\n**節點池寫法**（比指標版好——沒有 `new`／`delete`，快取友善，容易 `reserve`）：\n\n```cpp\nstruct DynSeg {\n    struct Node { int lc = 0, rc = 0; int mx = 0; int add = 0; };\n    vector<Node> t;\n    int V;                                   // 值域為 [0, V)\n    explicit DynSeg(int V) : V(V) {\n        t.reserve(1 << 20);                  // 先 reserve，避免反覆搬移\n        t.emplace_back();                    // t[0] 是「不存在」的哨兵，永遠不用\n        t.emplace_back();                    // t[1] 是根\n    }\n    int newNode() { t.emplace_back(); return int(t.size()) - 1; }\n};\n```\n\n**核心不變量（見定理 1）：`lc == 0` 表示「左子樹整段都還是初始狀態」。** 所有讀取子節點的地方都必須容忍 `0`，並把它當成單位元。\n\n**`mid` 必須寫成 `lo + (hi - lo) / 2`。** 值域到 $10^9$ 時 `lo + hi` 會溢位 `int`——**這是動態開點獨有的坑，靜態版因為 $n$ 小從來不會遇到。**\n\n**兩種標記策略：**\n\n| | 需要建點嗎 | 適用 |\n| --- | --- | --- |\n| **一般 lazy**（`pushDown`） | **要**：下傳前必須先把子節點建出來 | 所有情形，含區間賦值 |\n| **標記永久化**（標記永駐，查詢時沿路累加） | **不要** | 標記**可交換**且與查詢順序無關（區間加 ✔、區間賦值 ✘） |\n\n**標記永久化省下大量節點，是動態開點的標準搭配**，但它有嚴格的適用條件（定理 3）。**區間賦值不能永久化**，因為「後蓋前」的順序資訊會遺失。\n\n## 不變量或正確性證明\n\n以下記值域為 $[0, V)$，$L = \\lceil\\log_2 V\\rceil$。\n\n### 定理 1（空節點不變量）\n\n**不變量 (D)**：若某節點的 `lc == 0`（或 `rc == 0`），則對應的整個子區間**從未被任何修改觸及**，其聚合值為單位元。\n\n*維護*：節點只在「修改遞迴真的要走進去」時才被建立；查詢遇到 `0` 直接回傳單位元，不建立節點 ✔。\n\n**推論 1.1（查詢不應建點）**：`query` 若不小心呼叫了 `newNode`，節點數會從 $O(q_{\\text{改}}\\log V)$ 膨脹到 $O(q_{\\text{總}}\\log V)$。**在只讀多寫少的場景下這是數倍的記憶體差距。** 寫 `query` 時檢查一遍：有沒有任何一條路徑會建點。\n\n### 定理 2（節點數上界 $O(q\\log V)$）\n\n*命題*：$q$ 次區間修改後，節點總數不超過 $4q\\lceil\\log_2 V\\rceil + O(1)$。\n\n*證明*。單次修改的遞迴形狀與 9.3 完全相同，由 9.3 定理 2，**訪問的節點數 $\\le 4L$**。新建的節點必定在訪問集合之內（沒被訪問就不會被建立），故單次至多新建 $4L$ 個 ✔。$\\square$\n\n**這個界很鬆但夠用。實際上更精確的界是 $2L$ 左右**（每層至多 2 個部分相交節點，每個至多新建 2 個子節點，但其中至多 1 個會繼續遞迴）。\n\n**記憶體是真正的風險，必須算清楚**：$V = 10^9$ ⇒ $L = 30$；$q = 10^5$ ⇒ 節點數可達 $6\\times10^6$。若每個節點是 `{int lc, rc; long long sum, add;}` = 24 位元組，就是 **144 MB——直接 MLE**。\n\n**三個省記憶體的手段**（按效果排序）：\n1. **能離線就離散化**，節點數降到 $O(q)$；\n2. **標記永久化**（省掉為了 `pushDown` 而建的節點）；\n3. **縮小節點型別**（`int` 而非 `long long`，能省一半）。\n\n### 定理 3（標記永久化的正確性與適用條件）\n\n*設定*：「區間加、區間求和」。不 `pushDown`，而是在每個典範節點上把 `add[k] += v` 記著，**永不下傳**；同時仍然更新沿途所有祖先的 `sum`。\n\n*查詢* $[l, r]$ 的實作：遞迴下降，每經過一個節點 $k$ 就累加 $\\mathit{add}[k] \\times \\big|[l,r] \\cap \\mathrm{seg}(k)\\big|$，走到典範節點時再加上 $\\mathit{sum}[k]$。\n\n**不變量 (P)**：$\\mathit{sum}[k]$ 等於 $\\mathrm{seg}(k)$ 的真實總和**扣掉所有嚴格祖先的 `add` 貢獻**；$\\mathit{add}[k]$ 記錄「整段 $\\mathrm{seg}(k)$ 被加了多少」。\n\n*正確性*。任一次區間加操作 $(\\,[l', r'], v\\,)$ 對查詢答案的貢獻是 $v \\times |[l,r]\\cap[l',r']|$。該操作把 $v$ 記在若干典範節點上，而這些節點的區間不交、聯集為 $[l', r']$（9.3 定理 2）。查詢時：\n- 若典範節點 $k'$ **在查詢的下降路徑上**（是查詢典範節點的祖先或本身），$v$ 由「沿途累加 $\\mathit{add}[k'] \\times |\\text{交集}|$」計入 ✔；\n- 若 $k'$ **在查詢的某個典範節點之下**，$v$ 已被計入 $\\mathit{sum}$（修改時更新了所有祖先的 `sum`）✔；\n- 若 $k'$ 與查詢區間**不交**，交集為空，貢獻 $0$ ✔。\n\n三種情形窮盡且不重疊 ⇒ 恰好計入一次 ✔。$\\square$\n\n**適用條件（必須點名）**：\n1. **標記可交換**：查詢時是「把沿路所有 `add` 加起來」，**順序無關**。區間加滿足；**區間賦值不滿足**——「先賦值 5 再賦值 3」與「先 3 再 5」結果不同，而永久化丟失了順序資訊。\n2. **能用「交集長度」$O(1)$ 算貢獻**：區間加求和 ✔（乘交集長度）、區間加求 max ✔（直接加）。\n\n**要做區間賦值就老實用 `pushDown`，並在下傳前把子節點建出來。**\n\n### 定理 4（日程表系列：$k$ 重預訂 $\\iff$ 覆蓋計數的 max $\\ge k$）\n\n*命題*：一組半開區間 $\\{[s_i, e_i)\\}$ 存在 $k$ 個兩兩有共同內點的區間，**當且僅當**覆蓋計數函數 $c(x) = \\#\\{i : x \\in [s_i, e_i)\\}$ 的最大值 $\\ge k$。\n\n*證明*。\n- **($\\Leftarrow$)** 若某點 $x_0$ 滿足 $c(x_0) \\ge k$，則覆蓋 $x_0$ 的那 $k$ 個區間都含 $x_0$，兩兩相交 ✔。\n- **($\\Rightarrow$)** 設 $I_1, \\dots, I_k$ 兩兩相交。**區間具有 Helly 性質**（一維：兩兩相交的區間族必有公共點）：取 $s^* = \\max_j s_j$、$e^* = \\min_j e_j$，兩兩相交蘊含 $s^* < e^*$，故 $s^*$ 被全部 $k$ 個區間覆蓋，$c(s^*) \\ge k$ ✔。$\\square$\n\n**於是三題化成同一個資料結構操作**：\n\n| 題目 | 操作 |\n| --- | --- |\n| 729 我的日程安排表 I | 訂前查 $\\max_{[s,e)} c$，$\\ge 1$ 就拒絕 |\n| 731 我的日程安排表 II | 訂前查 $\\max_{[s,e)} c$，$\\ge 2$ 就拒絕 |\n| 732 我的日程安排表 III | 訂後回傳**全域** $\\max c$（即根節點的 `mx`） |\n\n**都是「區間加 1 + 區間查 max」，一份程式碼三題通吃。** 而「區間加 + 區間 max」的標記恰好滿足定理 3 的兩個條件，**可以標記永久化，不必 `pushDown`。**\n\n**($\\Rightarrow$) 用到的 Helly 性質不是廢話**：如果區間換成二維矩形，「兩兩相交」**不**蘊含「有公共點」（三個互相重疊的矩形可以沒有共同區域），這個化簡就失效了。**點名前提，才知道推廣的邊界在哪裡。**\n\n### 定理 5（715 Range 模組：`map` 維護不交區間的攤還分析）\n\n715 也可以用線段樹（區間賦值 0/1 + 查「整段是否全 1」），但**用 `map` 維護「不交且互不相鄰的極長 1 段」程式短得多**。值得證的是它的複雜度。\n\n*結構*：`map<int,int> m`，鍵為區間左端、值為右端，保證區間**兩兩不交且互不相鄰**（相鄰的會被合併）。\n\n*`addRange(l, r)`*：找出所有與 $[l, r)$ 相交或相鄰的區間，**全部刪除**，插入它們與 $[l,r)$ 的聯集（必為單一區間）。\n\n**命題**：$q$ 次操作的總時間為 $O(q\\log q)$。\n\n*證明（攤還）*。令勢能 $\\Phi = $ 目前 `map` 中的區間數。\n- 一次 `addRange` 刪除 $d$ 個區間、插入 1 個 ⇒ $\\Delta\\Phi = 1 - d$，實際成本 $O((d+1)\\log q)$。攤還成本 $O((d+1)\\log q) + (1-d)\\cdot c\\log q = O(\\log q)$（取 $c$ 足夠大）✔。\n- 一次 `removeRange` 至多**新增 2 個**區間（左右各切出一截）⇒ $\\Delta\\Phi \\le 2$，攤還 $O(\\log q)$ ✔。\n- $\\Phi \\ge 0$ 且初始為 $0$ ⇒ 總攤還成本即為總實際成本的上界 ✔。$\\square$\n\n**「刪除的數量由勢能付帳」是勢能法最典型的用法**，與 4.7 單調堆疊、6.6 懶刪除堆同構。**本章第七次出現這個模式了。**\n\n### 定理 6（699 掉落方塊：為什麼是「賦值」而不是「取 max」）\n\n*問題*：方塊依序落在 $[l, l + \\mathit{sz})$ 上，落到「該區間現有最大高度」之上，回傳每次落下後的**全域**最大高度。\n\n**命題**：設落下前 $h^* = \\max_{x\\in[l,\\,l+\\mathit{sz})} h(x)$，則落下後該區間的高度**處處等於** $h^* + \\mathit{sz}$，即這是一次**區間賦值**而非「區間取 max」。\n\n*證明*。方塊是剛體且底面是平的，落到 $h^*$ 就停住（再低會穿透最高的那一點），其上表面高度處處為 $h^* + \\mathit{sz}$。而對區間內任一點 $x$，原高度 $h(x) \\le h^* < h^* + \\mathit{sz}$，故新高度 $= h^* + \\mathit{sz}$ **嚴格大於**原值，賦值不會覆蓋掉更高的東西 ✔。$\\square$\n\n**「賦值合法」依賴 $\\mathit{sz} > 0$**（方塊有厚度）。若允許厚度 0，就必須改成 `chmax`，而 `chmax` 的標記不滿足定理 3 的條件，也不滿足 9.4 的分配律——**得用 Segment Tree Beats。一個小前提，決定了兩個檔次的實作難度。**\n\n**邊界**：題目規定「只在邊緣接觸不算重疊」，所以區間要用**半開** $[l, l+\\mathit{sz})$；若用閉區間 $[l, l+\\mathit{sz}]$，相鄰的兩個方塊會被誤判成重疊。**699 的規模只有 $n \\le 1000$，$O(n^2)$ 暴力完全能過——但它是理解「大值域區間賦值」的好練習。**\n\n## 逐步演算法\n\n:::example 狀態表示：節點池 + 邏輯值域邊界\n**1. 狀態表示：用一個節點池（pool）+ 顯式左右孩子。** 並記住整個邏輯區間的上下界（如 `[0, 1e9]`）。\n\n```cpp\n// 狀態表示：節點池 + 邏輯值域邊界\nvector<Node> pool{Node()};   // pool[0] 為空節點\nint root = 1;                // 根節點\nconst int LO = 0, HI = 1e9;  // 邏輯區間，不預先展開\n```\n:::\n\n\n\n:::example 區間更新：把 [l,r] 加上 v；走到孩子前先 push down\n**2. 更新：沿路徑「按需開點」，完全覆蓋就打 lazy。** 與 lazy 線段樹相同，只是孩子是現場 new 出來的。\n\n```cpp\n// 區間更新：把 [l,r] 加上 v；走到孩子前先 push down\nvoid update(int node, int lo, int hi, int l, int r, long long v) {\n    if (r < lo || hi < l) {\n        return;\n    }\n    if (l <= lo && hi <= r) {\n        apply(node, v, hi - lo + 1);\n        return;\n    }\n    int mid = lo + (hi - lo) / 2;\n    pushDown(node, mid - lo + 1, hi - mid);  // 下放（內部也會開點）\n    update(leftChild(node), lo, mid, l, r, v);\n    update(rightChild(node), mid + 1, hi, l, r, v);\n    pushUp(node);\n}\n```\n:::\n\n\n\n:::example 區間查詢：碰到不存在的節點代表該段全是初始值\n**3. 查詢：未建立的子樹視為「空 / 單位元」。** 查到尚未開點的區間，直接回傳 0（求和）或對應單位元，不必硬建節點。\n\n```cpp\n// 區間查詢：碰到不存在的節點代表該段全是初始值\nlong long query(int node, int lo, int hi, int l, int r) {\n    if (node == 0 || r < lo || hi < l) {\n        return 0;  // 空節點 → 單位元\n    }\n    if (l <= lo && hi <= r) {\n        return pool[node].sum;\n    }\n    int mid = lo + (hi - lo) / 2;\n    pushDown(node, mid - lo + 1, hi - mid);\n    return query(pool[node].left, lo, mid, l, r) +\n                  query(pool[node].right, mid + 1, hi, l, r);\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 用 `mid = lo + (hi - lo) / 2` 而非 `(lo + hi) / 2`，避免值域大時相加溢位。\n- pushDown 內部需要孩子時，要用 `leftChild/rightChild` 確保節點已建立，否則 lazy 無處可放。\n- 若值域雖大但操作不多，**離散化 + 普通線段樹**往往更省事；動態開點適合「無法或不便離散化」（如線上、查詢座標未知）的場景。\n- 複雜度：每次操作 `O(log(值域))`，總空間約 `O(操作數 × log(值域))`。\n\n## C++17 模板\n\n**模板一：動態開點 + 標記永久化（區間加 1、區間查 max）——729／731／732 通用**\n\n```cpp\nclass MyCalendarThree {\n    struct Node { int lc = 0, rc = 0, mx = 0, add = 0; };\n    vector<Node> t;\n    static constexpr int V = 1'000'000'001;      // 值域 [0, V)\n    int newNode() { t.emplace_back(); return int(t.size()) - 1; }\n\n    // 區間 [l, r) 加 v；標記永久化，不 pushDown\n    void update(int k, int lo, int hi, int l, int r, int v) {\n        if (r <= lo || hi <= l) return;\n        if (l <= lo && hi <= r) { t[k].add += v; t[k].mx += v; return; }\n        int mid = lo + (hi - lo) / 2;             // 不可寫 (lo + hi) / 2\n        if (l < mid) { if (!t[k].lc) t[k].lc = newNode();\n                       update(t[k].lc, lo, mid, l, r, v); }\n        if (mid < r) { if (!t[k].rc) t[k].rc = newNode();\n                       update(t[k].rc, mid, hi, l, r, v); }\n        int L = t[k].lc ? t[t[k].lc].mx : 0;\n        int R = t[k].rc ? t[t[k].rc].mx : 0;\n        t[k].mx = t[k].add + max(L, R);           // 自己的永久標記要加回來\n    }\n    int query(int k, int lo, int hi, int l, int r) const {\n        if (k == 0 || r <= lo || hi <= l) return 0;    // 空節點回傳單位元\n        if (l <= lo && hi <= r) return t[k].mx;\n        int mid = lo + (hi - lo) / 2;\n        int res = max(query(t[k].lc, lo, mid, l, r),\n                      query(t[k].rc, mid, hi, l, r));\n        return res + t[k].add;                    // 沿路累加永久標記\n    }\npublic:\n    MyCalendarThree() { t.reserve(1 << 18); t.emplace_back(); t.emplace_back(); }\n    int book(int startTime, int endTime) {\n        update(1, 0, V, startTime, endTime, 1);\n        return t[1].mx;                           // 732：全域最大覆蓋數\n    }\n};\n```\n\n**`t[k].mx = t[k].add + max(L, R)` 與 `return res + t[k].add` 這兩處，就是永久化的全部。** 前者維護 (P)，後者在查詢時把祖先的帳補回來。\n\n**729／731 只要改 `book`：**\n\n```cpp\n    bool book(int s, int e) {                     // 729：k = 1；731：改成 >= 2\n        if (query(1, 0, V, s, e) >= 1) return false;\n        update(1, 0, V, s, e, 1);\n        return true;\n    }\n```\n\n**模板二：一般 lazy 版（支援區間賦值，`pushDown` 前必須建點）**\n\n```cpp\n    void pushDown(int k, int lo, int mid, int hi) {\n        if (t[k].tag == NONE) return;\n        if (!t[k].lc) t[k].lc = newNode();        // 沒有子節點就先建\n        if (!t[k].rc) t[k].rc = newNode();\n        applyTag(t[k].lc, mid - lo, t[k].tag);\n        applyTag(t[k].rc, hi - mid, t[k].tag);\n        t[k].tag = NONE;\n    }\n```\n\n**「下傳前先建點」是動態開點 lazy 版與靜態版唯一的差別，但漏掉就是空指標般的邏輯錯誤（標記寫進 `t[0]` 這個哨兵，被後續操作讀到）。**\n\n**模板三：715 Range 模組（`map` 維護不交區間，最短解）**\n\n```cpp\nclass RangeModule {\n    map<int, int> m;                              // left -> right，半開 [left, right)\npublic:\n    void addRange(int left, int right) {\n        auto it = m.upper_bound(left);            // 第一個 left' > left\n        if (it != m.begin() && prev(it)->second >= left) --it;   // 可能與前一段相鄰／相交\n        while (it != m.end() && it->first <= right) {\n            left  = min(left, it->first);\n            right = max(right, it->second);\n            it = m.erase(it);\n        }\n        m[left] = right;\n    }\n    bool queryRange(int left, int right) {\n        auto it = m.upper_bound(left);\n        if (it == m.begin()) return false;\n        --it;\n        return it->second >= right;               // 單一段就能蓋住才算 true\n    }\n    void removeRange(int left, int right) {\n        auto it = m.upper_bound(left);\n        if (it != m.begin() && prev(it)->second > left) --it;\n        while (it != m.end() && it->first < right) {\n            int l = it->first, r = it->second;\n            it = m.erase(it);\n            if (l < left)   m[l] = left;          // 左邊切一截回去\n            if (r > right) { m[right] = r; break; }   // 右邊切一截，之後必無交集\n        }\n    }\n};\n```\n\n**`queryRange` 只檢查「單一段能否蓋住」是對的**——因為不變量保證區間互不相鄰，兩段之間必有空隙。**這個不變量是 `addRange` 用 `it->first <= right`（而不是 `<`）合併相鄰段換來的。**\n\n## 時間與空間複雜度\n\n令 $V$ 為值域大小、$L = \\lceil\\log_2 V\\rceil$、$q$ 為操作數。\n\n| | 時間 | 節點數 |\n| --- | --- | --- |\n| 動態開點（永久化） | $O(q\\log V)$ | $O(q\\log V)$，實測約 $2L$／次 |\n| 動態開點（一般 lazy） | $O(q\\log V)$ | $O(q\\log V)$，**常數更大**（`pushDown` 會建點） |\n| 離散化 + 靜態線段樹 | $O(q\\log q)$ | $O(q)$ |\n| `map` 維護不交區間 | $O(q\\log q)$ **攤還**（定理 5） | $O(q)$ |\n\n**$V = 10^9$ 時 $L = 30$，$\\log q$（$q = 10^5$）只有 17——離散化不只省記憶體，還快將近一倍。** 能離線就離線。\n\n**記憶體試算（必做）**：\n\n| 節點欄位 | 每節點 | $q = 10^5$、$L = 30$、係數 2 | 結論 |\n| --- | --- | --- | --- |\n| `int lc, rc, mx, add` | 16 B | $6\\times10^6 \\times 16$ = **96 MB** | **危險** |\n| `int lc, rc, mx` | 12 B | 72 MB | 危險 |\n| 離散化後 $4q$ 個節點 | 16 B | 6.4 MB | 安全 |\n\n**LeetCode 的記憶體上限通常是 256 MB，但 `vector` 擴容時會同時持有新舊兩份**——`reserve` 到位可以避開這個峰值。**動態開點的第一大死因是 MLE，不是 TLE。**\n\n**各題**：\n\n| 題目 | 值域 | 操作數 | 建議 |\n| --- | --- | --- | --- |\n| 729／731 日程表 I／II | $10^9$ | $\\le 1000$ | **`set` 維護不交區間或暴力都行**，動態開點是殺雞用牛刀 |\n| 732 日程表 III | $10^9$ | $\\le 400$ | 同上，但動態開點最直觀 |\n| 715 Range 模組 | $10^9$ | $\\le 10^4$ | **`map` 版最短** |\n| 699 掉落方塊 | $10^8$ | $\\le 1000$ | $O(n^2)$ 暴力即可 |\n| 2276 統計區間中的整數數目 | $10^9$ | $\\le 10^5$ | **`map` 維護不交區間**（動態開點也行） |\n\n**誠實的結論：本節列出的 LeetCode 題目規模都不大，`map` / `set` 維護不交區間往往是更短更快的解。** 動態開點的真正價值在於**它是通用的**——當操作不只是「整段賦值」（例如區間加、區間求和、區間 max 混用）時，`map` 就無能為力了。\n\n## 常見錯誤與邊界條件\n\n- **`mid = (lo + hi) / 2` 溢位**。值域到 $10^9$ 時 `lo + hi` 超出 `int`。**必須寫 `lo + (hi - lo) / 2`。這是動態開點獨有的坑。**\n- **查詢時建點**（推論 1.1）。`query` 遇到 `0` 應直接回傳單位元，**絕不呼叫 `newNode`**。\n- **忘記 `reserve`**。`vector` 反覆擴容既慢又會製造記憶體峰值。**先估好節點數再 `reserve`。**\n- **`pushDown` 前沒建子節點**。標記被寫進哨兵 `t[0]`，之後所有讀到 `t[0]` 的地方都被污染。\n- **對區間賦值使用標記永久化**（定理 3）。賦值不可交換，永久化會丟失順序。**只有可交換的標記（加法）能永久化。**\n- **永久化版忘記在查詢時累加沿路 `add`**，或忘記在 `pushUp` 時加回自己的 `add`。**兩處缺一都錯。**\n- **半開／閉區間混用**。日程表是 $[s, e)$、掉落方塊是 $[l, l+\\mathit{sz})$。**「邊緣接觸不算重疊」就必須用半開區間。**\n- **根區間寫成 $[0, V]$ 而不是 $[0, V)$**。與 `update` 的邊界判斷 `r <= lo || hi <= l` 要一致。\n- **值域下界不是 0**。若座標可為負，要平移或把根區間設成 $[-V, V)$（此時 `mid` 的溢位風險更大）。\n- **`long long` vs `int`**。求和類要 `long long`，但那會讓記憶體翻倍——**先確認真的會溢位再改。**\n- **715 的 `queryRange` 檢查了多段拼接**。不變量保證區間互不相鄰，**必須是單一段蓋住**才回 `true`；寫成「累加多段長度」是錯的。\n- **715 的 `addRange` 用 `<` 而不是 `<=` 判斷合併**。相鄰（`it->first == right`）的段也要合併，否則不變量破壞，`queryRange` 就會誤判。\n- **699 用閉區間**。相鄰方塊會被誤判成重疊。\n\n## 與相似技巧的比較\n\n- **與離散化 + 靜態線段樹（9.3／9.4）**：**只要能離線，離散化永遠更好**——節點 $O(q)$ 而非 $O(q\\log V)$，$\\log q$ 而非 $\\log V$，程式也更短。**動態開點的唯一理由是「線上」。** 拿到題目先問：所有操作是不是事先都知道？\n- **與 `map` / `set` 維護不交區間（ODT 風格）**：當操作是「整段賦值／整段標記」時，`map` 版程式短一半、常數小、記憶體 $O(q)$，而且攤還複雜度有證明（定理 5）。**715、2276、729 都建議先寫 `map` 版。** 但它做不了「區間加 + 區間求和」這類需要聚合的操作。\n- **與 9.6 可持久化線段樹**：可持久化也是「按需建節點」，但目的不同——**動態開點是為了省空間，可持久化是為了保留歷史版本。** 兩者常常同時使用（主席樹 = 動態開點 + 版本共享）。\n- **與 9.1 樹狀陣列**：樹狀陣列**必須有連續的小下標**，值域大就只能離散化。**沒有「動態開點樹狀陣列」這種東西**（有 `map` 版但常數極差）。\n- **與平衡樹（`pb_ds` / 手寫 Treap）**：平衡樹的節點數是 $O(q)$（只存真正存在的元素），比動態開點省。**但它不支援「區間加、區間 max」這類需要區間分解的操作**，除非寫成 Treap 按位置分裂——那已經是另一個檔次的實作。\n- **與掃描線（9.4 定理 5）**：離線的區間覆蓋問題（矩形面積並）用掃描線 + 靜態線段樹，比動態開點好。**再一次：能離線就離線。**\n- **與暴力**：729／699 的 $n \\le 1000$，$O(n^2)$ 只有 $10^6$ 次操作。**面試中先寫暴力並說明可以升級成動態開點，往往比直接寫 200 行更好。**\n\n## 代表例題\n\n**題號**：699、715、729、731、732\n\n有動態更新與區間查詢；練習先離散化值域，再確認查詢/更新的閉開區間。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 699 | [掉落的方塊](https://leetcode.cn/problems/falling-squares) |  |\n| 715 | [Range 模組](https://leetcode.cn/problems/range-module) |  |\n| 729 | [我的日程安排表 I](https://leetcode.cn/problems/my-calendar-i) |  |\n| 731 | [我的日程安排表 II](https://leetcode.cn/problems/my-calendar-ii) |  |\n| 732 | [我的日程安排表 III](https://leetcode.cn/problems/my-calendar-iii) |  |\n| 2276 | [統計區間中的整數數目](https://leetcode.cn/problems/count-integers-in-intervals) | 2222 |\n| 2770 | [達到末尾下標所需的最大跳躍次數](https://leetcode.cn/problems/maximum-number-of-jumps-to-reach-the-last-index) | 1533 |\n| 3590 | [第 K 小的路徑異或和](https://leetcode.cn/problems/kth-smallest-path-xor-sum) | 2646 |\n\n## 本節重點速查\n\n- **存在理由**：值域 $10^9$ 但操作只有 $10^5$，且**必須線上**。**能離線就離散化——那更快更省。**\n- **核心不變量 (D)**：`lc == 0` 表示「整段從未被觸及」，讀到就回傳單位元。**`query` 絕不建點**，否則節點數從 $O(q_{\\text{改}}\\log V)$ 膨脹到 $O(q_{\\text{總}}\\log V)$。\n- **`mid = lo + (hi - lo) / 2`**。`(lo + hi) / 2` 在 $10^9$ 值域下溢位——**動態開點獨有的坑**。\n- **節點數 $O(q\\log V)$（定理 2）**，$V=10^9$、$q=10^5$ ⇒ 可達 $6\\times10^6$ 個 ⇒ **96 MB，第一大死因是 MLE 不是 TLE**。務必 `reserve` 並算一次記憶體。\n- **標記永久化（定理 3）**：不 `pushDown`，修改時在典範節點記 `add`、查詢時沿路累加。**條件：標記可交換（區間加 ✔、區間賦值 ✘）且能用交集長度 $O(1)$ 算貢獻。** 省下大量節點。\n- **一般 lazy 版的唯一差別是「`pushDown` 前先建子節點」**，漏掉就把標記寫進哨兵。\n- **日程表三題同一份程式（定理 4）**：$k$ 重預訂 $\\iff$ 覆蓋計數 max $\\ge k$，靠一維區間的 **Helly 性質**（推廣到矩形就失效）。729 查 $\\ge1$、731 查 $\\ge2$、732 回傳根的 `mx`。\n- **699 是「區間賦值」不是「區間取 max」（定理 6）**：方塊有厚度 ⇒ 新高度嚴格大於區間內任何舊值。**厚度可為 0 就得用 Segment Tree Beats。** 用半開區間，否則相鄰方塊誤判重疊。\n- **715 用 `map` 維護不交且不相鄰的區間**，攤還 $O(q\\log q)$（勢能法，定理 5——本章第七次出現這個攤還模式）。**`queryRange` 必須單一段蓋住**；`addRange` 要用 `<=` 把相鄰段也合併。\n- **選擇順序**：暴力（$n\\le1000$）→ 離散化 + 靜態線段樹（離線）→ `map` 維護不交區間（只有整段賦值）→ **動態開點 + 永久化（線上、可交換標記）** → 動態開點 + lazy（線上、要區間賦值）。\n",
        },
        {
          id: 66,
          title: "9.6 可持久化線段樹",
          summary:
            "## 這個技術解決什麼問題\n\n到目前為止的線段樹都只有**一個「現在」**：修改之後，舊的狀態就沒了。但有些問題要問**歷史**：\n\n- 「陣列在第 $t$ 次操作之後長什麼樣？」\n- 「子陣列 $[l, r]$ 的第 $k$ 小是多少？」\n\n第二個問題乍看與歷史無關，但它其實是同一件事。**關鍵的轉換是**：\n\n> 令版本 $i$ 表示「只放入前 $i$ 個元素的值域計數線段樹」。那麼**版本 $r$ 減版本 $l-1$**，就是「只含 $a_l \\dots a_r$」的值域計數線段樹。\n\n在這棵「差」上做值域二分，就能 $O(\\log n)$ 求出區間第 $k$ 小。這就是**主席樹**（可持久化權值線段樹）最經典的用法。\n\n**但天真的做法是不可行的**：存 $n$ 個完整版本要 $O(n^2)$ 空間。**可持久化的全部想法只有一句：**\n\n> **相鄰兩個版本只差一次單點修改，而單點修改只改動根到葉的一條路徑（$O(\\log n)$ 個節點）。把這條路徑複製一份，其餘節點兩個版本共用。**\n\n於是每個版本只多花 $O(\\log n)$ 空間，總共 $O(n\\log n)$。\n\n**辨識訊號**：\n- 需要查詢「歷史版本」或保留每一步的狀態；\n- **任意**子陣列 $[l, r]$ 的第 $k$ 小／值域計數（**任意**是重點——若只問全域第 $k$ 小，排序就夠了）；\n- 多次查詢彼此獨立、針對不同時間切片的資料（3762）。\n\n**先問一個問題：能離線嗎？** 若所有查詢事先已知，**整體二分**或「按值排序後用樹狀陣列掃描」通常更短、常數更小、空間 $O(n)$。**主席樹的不可替代之處在於線上查詢，以及真的要保留歷史版本。**\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「可持久化線段樹」想：\n\n- **需要查詢「歷史版本」或「保留每一步的狀態」。** 一般線段樹改了就回不去，可持久化能凍結每個版本。\n- **「任意子陣列 `[l, r]` 的第 k 小 / 值域計數」。** 對前綴各存一版，用「版本 r 減版本 l-1」在值域上二分，是主席樹最經典的用法。\n- **多次查詢彼此獨立、針對不同時間切片的資料**，如 3762「使陣列元素相等的最小操作次數」這類需對多個子區間做值域統計的題。\n\n:::example 嗅到味道的瞬間：你想查「某個歷史時刻」的區間資訊\n```cpp\n// 嗅到味道的瞬間：你想查「某個歷史時刻」的區間資訊\n// 普通線段樹被覆蓋後回不去 → 用可持久化保留每個版本的根\n// long long ans = query(roots[版本r], ...) - query(roots[版本l-1], ...);\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我為什麼需要保留多個版本，而不是只用最新狀態？\n2. 我每個版本的根存在哪？怎麼用版本相減回答子區間查詢？\n3. 我的更新有沒有保持「節點不可變、只新建路徑」？\n\n三題都答得出來，就套對了。\n\n## 核心想法與直覺\n\n本節介紹「可持久化線段樹」（又稱主席樹）。它要解決的場景是：你不只想查「現在這個版本」的資料，還想**回頭查任何一個歷史版本**，例如「第 3 次修改之後、那時候的某段區間和是多少」。\n\n最笨的做法是每改一次就完整複製一棵線段樹存起來，但一棵樹有 `O(n)` 個節點，存 m 個版本就 `O(n·m)`，太耗空間。\n\n:::example 節點不可變；每個版本有自己的根。孩子用編號指向 pool，可跨版本共用\n核心想法一句話：**每次修改只會影響從根到某葉子的那一條路徑（`O(log n)` 個節點），所以新版本只要「新建這條路徑上的節點」，其餘沒變的子樹直接「共用」舊版本的節點。** 這叫「路徑複製（path copying）」。\n\n```cpp\n// 節點不可變；每個版本有自己的根。孩子用編號指向 pool，可跨版本共用\nstruct Node {\n    long long sum;\n    int left, right;\n};\nvector<Node> pool;  // 所有版本共用的節點池\nvector<int> roots;  // roots[v] = 第 v 版的根節點編號\n```\n:::\n\n\n\n:::example 可持久化單點更新：回傳新版本這條路徑的新節點，未變側共用舊孩子\n更新時，沿著要改的路徑「複製一份新節點」，未走到的那一側孩子**直接指回舊節點**，於是新舊版本共享了大量子樹：\n\n```cpp\n// 可持久化單點更新：回傳新版本這條路徑的新節點，未變側共用舊孩子\nint update(int prev, int lo, int hi, int pos, int val) {\n    int cur = newNode(prev);  // 複製舊節點作為新版本的這個節點\n    if (lo == hi) {\n        pool[cur].sum += val;\n        return cur;\n    }\n    int mid = (lo + hi) / 2;\n    if (pos <= mid) {\n        pool[cur].left = update(pool[prev].left, lo, mid, pos, val);\n    } else {\n        pool[cur].right = update(pool[prev].right, mid + 1, hi, pos, val);\n    }\n    // 沒被改的那一側維持指向舊孩子（在 newNode 複製時已帶過來）\n    pool[cur].sum = pool[pool[cur].left].sum + pool[pool[cur].right].sum;\n    return cur;\n}\n```\n:::\n\n\n\n每個版本只多花 `O(log n)` 個節點，m 次修改總空間 `O((n + m) log n)`。一個經典應用是「主席樹查區間第 k 小」：對前綴各建一個版本，用「版本 r 減版本 l-1」就能對任意子陣列做值域上的計數查詢。\n\n## 狀態／資料結構定義\n\n```cpp\nstruct PersistentSeg {\n    struct Node { int lc = 0, rc = 0, cnt = 0; };\n    vector<Node> t;\n    vector<int> root;                 // root[i] = 第 i 個版本的根\n    int m;                            // 離散化後的值域大小 [0, m)\n\n    explicit PersistentSeg(int m, int n) : m(m) {\n        t.reserve(size_t(n) * (__lg(max(m, 1)) + 2) + 2);   // 節點池要一次開夠\n        t.emplace_back();             // t[0]：空節點，代表「整段計數為 0」\n        root.push_back(0);            // 版本 0 = 空樹\n    }\n};\n```\n\n三件事值得注意：\n\n1. **`t[0]` 同時扮演「空節點」與「空樹」。** 這與 9.5 的空節點不變量是同一個約定：讀到 `0` 就當成單位元（計數 0）。**版本 0 的根直接就是 `0`，不需要建一棵全零的樹。**\n2. **`root` 是版本表。** 版本之間**不是**父子關係，只是共享子樹的一堆 DAG 根。\n3. **節點池必須一次 `reserve` 足夠**。$n$ 次插入、每次 $\\lceil\\log_2 m\\rceil + 1$ 個新節點，寫成 `n * (log2(m) + 2)` 留餘裕。**這是可持久化最常見的 RE／MLE 來源。**\n\n**最重要的約定（定理 1 會證明它為什麼必要）：**\n\n> **節點一旦建立，內容永不改變（immutable）。** 任何「就地修改」都會同時污染所有共用該節點的舊版本。\n\n**這條約定直接封殺了 `pushDown`。** 詳見定理 5。\n\n## 不變量或正確性證明\n\n以下記離散化後值域為 $[0, m)$，$L = \\lceil\\log_2 m\\rceil$。\n\n### 定理 1（可持久化不變量：版本互不干擾）\n\n**不變量 (V)**：對每個版本 $i$，從 `root[i]` 出發的整棵樹，**語義上等同於**「把前 $i$ 個元素依序插入一棵獨立的值域計數線段樹」的結果。\n\n*證明（對 $i$ 歸納）*。\n\n**基底** $i = 0$：`root[0] = 0`，所有計數為 0 ✔。\n\n**歸納步**：設 (V) 對版本 $i-1$ 成立。插入 $a_i$ 時，我們沿著「$a_i$ 所在葉子」的根到葉路徑 $P$ 走，**為 $P$ 上每個節點建立一份新副本**，副本的兩個子指標中，**路徑方向的那個指向新建的副本，另一個原封不動指向舊版本的節點**。\n\n- 對 $P$ 上的節點：新副本的 `cnt` = 舊值 + 1 ✔（$a_i$ 落在它的值域區間內）。\n- 對不在 $P$ 上、但被新版本可達的節點 $u$：$u$ 是某個 $P$ 上節點的「非路徑方向」子樹的根。$u$ 從未被寫入，其子樹在新舊版本中**是同一份記憶體**，而 $a_i$ 不落在 $\\mathrm{seg}(u)$ 內，所以「舊版本的正確值」也正是「新版本應有的值」✔。\n- 舊版本 `root[i-1]` 可達的節點**一個都沒被寫**（我們只寫了剛 `emplace_back` 出來的新節點）⇒ 版本 $i-1$ 的 (V) 仍然成立 ✔。$\\square$\n\n**這個證明依賴兩個前提，必須點名**：\n\n1. **修改只影響一條根到葉的路徑。** 這對**單點修改**成立。區間修改會觸及 $O(\\log m)$ 個典範節點及其祖先——仍是 $O(\\log m)$ 個節點，複製它們也行；**但 `pushDown` 不行**（定理 5）。\n2. **新版本只寫新節點。** 只要有一行寫進了舊節點，所有共享它的版本一起壞掉，而且**症狀是「某個舊版本的查詢答案莫名其妙變了」，極難除錯。**\n\n**推論 1.1**：任兩個版本可以自由地同時查詢、互相比較，因為它們是各自完整的樹。**定理 3 就建立在這一點上。**\n\n### 定理 2（空間與時間：$O(n\\log m)$）\n\n*命題*：$n$ 次單點插入後，節點總數 $\\le n(L+1) + 1$。\n\n*證明*。根到葉的路徑長為 $L+1$（含根與葉），每次插入恰好複製這條路徑上的每個節點各一次 ✔。$\\square$\n\n**試算**：$n = 10^5$、$m = 10^5$ ⇒ $L = 17$ ⇒ 節點數 $\\approx 1.8\\times10^6$。每節點 `{int lc, rc, cnt}` = 12 B ⇒ **約 22 MB，安全。**\n\n**與 9.5 對照**：9.5 是「一個版本、大值域、按需建點」，這裡是「多個版本、小值域（先離散化）、按需複製」。**兩者都是 $O(q\\log V)$ 個節點，但目的完全不同**——9.5 為了省空間，本節為了保留歷史。（**兩者可以疊加**：若值域大又要持久化，就不離散化、直接在 $[0, 10^9)$ 上可持久化，節點數變成 $O(n\\log V)$。）\n\n### 定理 3（主席樹的可減性）\n\n*命題*：設 $\\mathrm{cnt}_i(u)$ 為版本 $i$ 中節點 $u$ 所在值域區間的元素個數。則對任意值域區間 $S$，\n\n$$\\#\\{\\,j \\in [l, r] : a_j \\in S\\,\\} \\;=\\; \\mathrm{cnt}_r(S) - \\mathrm{cnt}_{l-1}(S).$$\n\n*證明*。版本 $i$ 恰好裝入 $a_1, \\dots, a_i$，故 $\\mathrm{cnt}_i(S) = \\#\\{j \\le i : a_j \\in S\\}$。兩式相減，$j \\le l-1$ 的項全數抵消，剩下 $l \\le j \\le r$ ✔。$\\square$\n\n**這裡點名的前提，是整章反覆出現的同一件事：計數在加法下構成一個群，有反元素，所以「相減」有意義。** 9.1 定理 5 說樹狀陣列的區間查詢需要群、9.3 說線段樹只需要么半群；**主席樹的「版本相減」也需要群。**\n\n**推論 3.1（主席樹的能力邊界）**：**主席樹能做「區間第 $k$ 小」「區間內值在 $[x,y]$ 的個數」「區間不同值個數（配合另一種建版本方式）」，但不能做「區間 max」**——最大值沒有反元素，$\\max_r - \\max_{l-1}$ 毫無意義。**要區間 max 就用 9.7 的 ST 表或 9.3 的線段樹。**\n\n**注意這裡「相減」的具體形式**：我們**不**真的建一棵差樹，而是**兩個指標同步下降**——每一步都同時走版本 $r$ 與版本 $l-1$ 的對應節點，用兩者的 `cnt` 相減。**這也是為什麼推論 1.1 重要：兩棵樹的形狀完全相同（都是完整值域的邏輯樹形），才能這樣配對下降。**\n\n### 定理 4（值域二分求第 $k$ 小的正確性）\n\n*演算法*：從 $(u, v) = (\\mathrm{root}[l-1], \\mathrm{root}[r])$、值域區間 $[0, m)$ 出發。令 $c = \\mathrm{cnt}(v.\\mathit{lc}) - \\mathrm{cnt}(u.\\mathit{lc})$（左半值域在 $[l,r]$ 中的元素個數）。若 $k \\le c$ 就往左走，否則往右走並令 $k \\gets k - c$。走到葉子時回傳該葉子的值。\n\n**迴圈不變量 (K)**：*當前值域區間 $[\\mathit{lo}, \\mathit{hi})$ 內恰有 $\\ge k$ 個 $[l,r]$ 中的元素，且**原問題的答案**就是「$[l,r]$ 中落在 $[\\mathit{lo}, \\mathit{hi})$ 內的元素的第 $k$ 小」。*\n\n*證明*。\n\n**初始**：$[\\mathit{lo}, \\mathit{hi}) = [0, m)$ 包含全部元素，$k$ 未改動 ✔。\n\n**維持**。設 (K) 在 $[\\mathit{lo}, \\mathit{hi})$ 成立，中點 $\\mathit{mid}$，左半含 $c$ 個元素。\n\n- 若 $k \\le c$：左半的所有元素都**嚴格小於**右半的所有元素（值域切分），所以「$[\\mathit{lo},\\mathit{hi})$ 內的第 $k$ 小」= 「$[\\mathit{lo},\\mathit{mid})$ 內的第 $k$ 小」，且左半確實有 $\\ge k$ 個元素 ✔。\n- 若 $k > c$：$[\\mathit{lo},\\mathit{hi})$ 內最小的 $c$ 個元素全在左半，故第 $k$ 小 = 右半的第 $k - c$ 小；由 (K) 有 $\\ge k$ 個元素，扣掉左半的 $c$ 個，右半有 $\\ge k - c$ 個 ✔。\n\n**終止**：值域區間長度每次減半，$L$ 步後成為單點 $[\\mathit{lo}, \\mathit{lo}+1)$。此時 (K) 說「該值出現 $\\ge k$ 次且答案是其中第 $k$ 小」——同一個值的第 $k$ 小就是它自己 ✔。$\\square$\n\n**兩個容易忽略的點**：\n- **「左半嚴格小於右半」是值域線段樹的性質，不是下標線段樹的性質。** 這個證明放在下標線段樹上完全不成立。**先確認你建的是權值線段樹。**\n- **$k$ 必須滿足 $1 \\le k \\le r-l+1$。** 初始不變量要求「$\\ge k$ 個元素」，$k$ 越界時整個論證失效（實作上會走到葉子回傳一個錯誤的值，而不是報錯）。**呼叫前檢查。**\n\n### 定理 5（為什麼可持久化不能 `pushDown`，以及標記永久化為什麼可以）\n\n*命題*：在可持久化線段樹上執行 9.4 式的 `pushDown` 會破壞不變量 (V)。\n\n*證明（給出破壞的實例）*。`pushDown(k)` 做兩件事：把 `t[k].tag` 套用到兩個子節點、再把 `t[k].tag` 清空。**這兩個寫入都落在既有節點上。** 若 $k$ 的某個子節點 $u$ 同時被舊版本 $i'$ 可達（共享），那麼 $u$ 的值被改了，而版本 $i'$ 的根到 $u$ 的路徑上並沒有相應的標記被移除——版本 $i'$ 的查詢會把這個標記算兩次 ✔（破壞了 (V)）。$\\square$\n\n**正確的做法是標記永久化（9.5 定理 3）**：標記寫在典範節點上、**永不下傳**，查詢時沿路累加。**因為修改時這些節點本來就要複製，寫入落在新副本上，舊版本毫髮無傷。**\n\n**於是可持久化與標記永久化是天生一對**——這也解釋了為什麼競賽程式碼裡「可持久化 + lazy」幾乎總是寫成永久化的形式。**代價是繼承 9.5 定理 3 的限制：標記必須可交換，區間賦值做不到。**\n\n**推論 5.1**：一切「就地修改」都要重新檢查。例如想在主席樹上「刪掉一個元素」，不能改 `cnt`，只能**建一個新版本**。\n\n### 定理 6（版本鏈不必是線性的）\n\n*觀察*：`root` 只是一個指標陣列，**版本 $i$ 的父版本可以是任意舊版本**，不必是 $i-1$。\n\n於是可持久化天然支援：\n- **可回溯**：`root[i] = root[j]`（$j < i-1$）就是「回到版本 $j$」，$O(1)$；\n- **版本樹**：從同一個版本分岔出多條修改線（可持久化並查集、可持久化平衡樹皆賴此）。\n\n**但定理 3 的「相減」只在版本鏈是線性前綴時才有意義。** 若版本形成一棵樹，$\\mathrm{cnt}_r - \\mathrm{cnt}_{l-1}$ 對應的不再是連續子陣列，而是「樹上路徑」——那是「樹上主席樹」的做法：對每個節點建立「根到它」的版本，查詢路徑 $(u,v)$ 時用 $\\mathrm{cnt}_u + \\mathrm{cnt}_v - \\mathrm{cnt}_{\\mathrm{lca}} - \\mathrm{cnt}_{\\mathrm{fa}(\\mathrm{lca})}$。**四個版本一起下降，原理完全相同——都只依賴「計數可加減」這一個前提。**\n\n## 逐步演算法\n\n:::example 狀態表示：pool 存所有節點，roots 記錄各版本的根\n**1. 狀態表示：共用節點池 + 每版一個根。** 節點一旦建立不再修改（不可變）。\n\n```cpp\n// 狀態表示：pool 存所有節點，roots 記錄各版本的根\nvector<Node> pool;\nvector<int> roots;       // roots.back() 是最新版本\nint newNode(int from) {  // 複製一份 from 作為新節點\n    pool.push_back(pool[from]);\n    return pool.size() - 1;\n}\n```\n:::\n\n\n\n:::example 建空樹作為版本 0\n**2. 建初始版本（版本 0）。** 像普通線段樹一樣建一棵空樹，記下它的根為 `roots[0]`。\n\n```cpp\n// 建空樹作為版本 0\nint build(int lo, int hi) {\n    int cur = (int)pool.size();\n    pool.push_back({0, 0, 0});\n    if (lo == hi) {\n        return cur;\n    }\n    int mid = (lo + hi) / 2;\n    pool[cur].left = build(lo, mid);\n    pool[cur].right = build(mid + 1, hi);\n    return cur;\n}\n```\n:::\n\n\n\n:::example 以最新版為基礎，新增一個修改後的版本\n**3. 產生新版本：基於某舊版本做路徑複製更新。** 不覆蓋舊根，而是把新根 push 進 `roots`。\n\n```cpp\n// 以最新版為基礎，新增一個修改後的版本\nvoid addVersion(int pos, int val) {\n    int newRoot = update(roots.back(), 0, n - 1, pos, val);\n    roots.push_back(newRoot);  // 舊版本仍可用 roots[舊編號] 查到\n}\n```\n:::\n\n\n\n:::example 在某版本上查前綴和：傳入該版本的根\n**4. 查詢與邊界、複雜度。** 查某版本就用它的根遞迴；「子陣列值域計數」用兩個版本相減。\n\n```cpp\n// 在某版本上查前綴和：傳入該版本的根\nlong long query(int node, int lo, int hi, int l, int r) {\n    if (node == 0 || r < lo || hi < l) {\n        return 0;\n    }\n    if (l <= lo && hi <= r) {\n        return pool[node].sum;\n    }\n    int mid = (lo + hi) / 2;\n    return query(pool[node].left, lo, mid, l, r) +\n                  query(pool[node].right, mid + 1, hi, l, r);\n}\n```\n:::\n\n\n\n- 節點必須**不可變**：更新時務必新建節點，絕不能改到舊節點，否則會汙染歷史版本。\n- 值域大時先離散化再建樹。\n- 複雜度：每次更新與查詢 `O(log n)`，總空間 `O((n + m) log n)`。\n\n## C++17 模板\n\n**模板一：主席樹求區間第 $k$ 小（最常用的形態）**\n\n```cpp\nstruct KthSeg {\n    struct Node { int lc = 0, rc = 0, cnt = 0; };\n    vector<Node> t;\n    vector<int> root, vals;           // vals：離散化後的值\n    int m = 0;\n\n    // 插入 pos（值域下標），基於版本 prev，回傳新節點下標\n    int insert(int prev, int lo, int hi, int pos) {\n        int cur = int(t.size());\n        t.push_back(t[prev]);         // 複製整個節點（含兩個子指標）\n        t[cur].cnt++;\n        if (hi - lo == 1) return cur;\n        int mid = lo + (hi - lo) / 2;\n        if (pos < mid) t[cur].lc = insert(t[prev].lc, lo, mid, pos);\n        else           t[cur].rc = insert(t[prev].rc, mid, hi, pos);\n        return cur;\n    }\n\n    explicit KthSeg(const vector<int>& a) {\n        vals = a;\n        sort(vals.begin(), vals.end());\n        vals.erase(unique(vals.begin(), vals.end()), vals.end());\n        m = max<int>(vals.size(), 1);\n        t.reserve(a.size() * (__lg(m) + 2) + 2);\n        t.emplace_back();             // t[0]：空節點\n        root.assign(1, 0);            // 版本 0：空樹\n        for (int x : a) {\n            int p = int(lower_bound(vals.begin(), vals.end(), x) - vals.begin());\n            root.push_back(insert(root.back(), 0, m, p));\n        }\n    }\n\n    // a[l..r]（0-indexed，皆含）的第 k 小，k 從 1 起算\n    int kth(int l, int r, int k) const {\n        int u = root[l], v = root[r + 1];        // 版本 l-1 與版本 r（前綴長度）\n        int lo = 0, hi = m;\n        while (hi - lo > 1) {\n            int c = t[t[v].lc].cnt - t[t[u].lc].cnt;\n            int mid = lo + (hi - lo) / 2;\n            if (k <= c) { u = t[u].lc; v = t[v].lc; hi = mid; }\n            else        { k -= c; u = t[u].rc; v = t[v].rc; lo = mid; }\n        }\n        return vals[lo];\n    }\n};\n```\n\n**`t.push_back(t[prev])` 這一行是整份程式碼的心臟**：先整份複製（所以「非路徑方向」的子指標自動被共用），再只改動需要改的部分。**注意不能寫成 `t.push_back(t[prev]); ...` 之後又用 `t[prev]` 的參考——`push_back` 可能觸發擴容使參考失效。上面的寫法用的是下標，安全。**\n\n**`t[0].cnt == 0` 讓 `t[t[v].lc].cnt` 在 `lc == 0` 時自動回傳 0**，省去所有空指標判斷。**這是 `t[0]` 哨兵最漂亮的地方。**\n\n**模板二：查詢區間內值落在 $[x, y]$ 的個數**\n\n```cpp\n    // 版本差 (u, v) 中，值域下標落在 [ql, qr) 的個數\n    int count(int u, int v, int lo, int hi, int ql, int qr) const {\n        if (qr <= lo || hi <= ql) return 0;\n        if (ql <= lo && hi <= qr) return t[v].cnt - t[u].cnt;\n        int mid = lo + (hi - lo) / 2;\n        return count(t[u].lc, t[v].lc, lo, mid, ql, qr)\n             + count(t[u].rc, t[v].rc, mid, hi, ql, qr);\n    }\n```\n\n**與 9.3 的區間查詢一模一樣，只是每個節點都拖著兩個版本同步走。** 由 9.3 定理 2，訪問 $O(\\log m)$ 個節點對。\n\n**模板三：區間不同值的個數（換一種建版本的方式）**\n\n```cpp\n    // pre[i]：a[i] 上一次出現的下標（沒有則 -1）\n    // 版本 i 在「下標」值域上，於位置 pre[i] 減 1、於位置 i 加 1\n    // 查詢 [l, r] 的不同值個數 = 版本 r 中下標落在 [l, r] 的計數和\n```\n\n**這說明可持久化的威力不在「第 $k$ 小」這個特例，而在「任意可加減的前綴資訊都能被線上區間化」。** 換個插入規則，就換一類問題。\n\n## 時間與空間複雜度\n\n令 $n$ 為元素數、$m$ 為離散化後值域、$q$ 為查詢數、$L = \\lceil\\log_2 m\\rceil$。\n\n| 操作 | 時間 | 空間 |\n| --- | --- | --- |\n| 建樹（$n$ 次插入） | $O(n\\log m)$ | $O(n\\log m)$ 節點 |\n| 區間第 $k$ 小 | $O(\\log m)$ | $O(1)$（迴圈版，非遞迴） |\n| 區間值域計數 | $O(\\log m)$ | $O(\\log m)$ 遞迴堆疊 |\n| 離散化 | $O(n\\log n)$ | $O(n)$ |\n\n**總計 $O((n+q)\\log n)$ 時間、$O(n\\log n)$ 空間。**\n\n**空間試算**：$n = m = 10^5$、$L = 17$ ⇒ $10^5 \\times 19 \\approx 1.9\\times10^6$ 節點 $\\times$ 12 B $\\approx$ **23 MB**。$n = 10^6$ 就變成 230 MB——**規模上去就要考慮離線做法。**\n\n**與替代方案的複雜度對照（區間第 $k$ 小）**：\n\n| 做法 | 時間 | 空間 | 線上？ |\n| --- | --- | --- | --- |\n| 主席樹 | $O((n+q)\\log n)$ | $O(n\\log n)$ | **✔** |\n| 整體二分 | $O((n+q)\\log n\\log V)$ | $O(n)$ | ✘ |\n| 莫隊 + 值域分塊 | $O(n\\sqrt q + q\\sqrt n)$ | $O(n)$ | ✘ |\n| 排序 + 樹狀陣列（離線掃描） | $O((n+q)\\log n)$ | $O(n)$ | ✘ |\n| 暴力排序每個查詢 | $O(qn\\log n)$ | $O(n)$ | ✔ |\n\n**主席樹是唯一同時做到「線上」與 $O(\\log n)$ 單次查詢的。空間是它付出的代價。**\n\n## 常見錯誤與邊界條件\n\n- **節點池開太小**。$n(L+2)+2$ 是安全值。**開太小是 RE，開太大是 MLE——先算一次。** 用 `reserve` 而非固定大小陣列，可以避免猜錯上界。\n- **`push_back` 後仍持有舊參考**。`t.push_back(t[prev])` 若寫成 `Node& p = t[prev]; t.push_back(p);` 會在擴容時讀到失效參考。**全程用下標，不要用參考。**\n- **版本編號差一**。`root[i]` 通常定義為「放入前 $i$ 個元素」，查 $[l, r]$（0-indexed）要用 `root[l]` 與 `root[r+1]`。**這個 off-by-one 是本節最高頻的錯誤，寫註解標明 `root` 的語義。**\n- **忘記離散化，或離散化後忘記 `unique`**。重複值不去重會讓值域變大（不致錯，但浪費），而**答案要用 `vals[lo]` 還原回原值**——直接回傳值域下標是錯的。\n- **`pushDown`**（定理 5）。可持久化上絕不能下傳標記，只能永久化。\n- **就地修改既有節點**（推論 5.1）。任何 `t[x].cnt++`（$x$ 不是剛建的節點）都會污染舊版本。**症狀是「舊版本的答案變了」，而不是崩潰——極難除錯。建議把修改集中在 `insert` 一個函式裡。**\n- **`k` 越界**（定理 4）。$k > r-l+1$ 時演算法不會報錯，只會回傳垃圾。\n- **值域寫成閉區間**。本節模板一律用半開 $[lo, hi)$，遞迴終止條件是 `hi - lo == 1`。**混用閉區間會讓葉子判斷錯位。**\n- **`mid = (lo + hi) / 2`**。離散化後 $m \\le n$ 很小，這裡不會溢位；**但若跳過離散化直接在 $[0, 10^9)$ 上可持久化，就回到 9.5 的坑，必須寫 `lo + (hi - lo) / 2`。** 模板統一用安全寫法。\n- **想拿它做區間 max**（推論 3.1）。沒有反元素，版本相減沒有意義。\n\n## 與相似技巧的比較\n\n- **與 9.5 動態開點**：兩者都「按需建節點」，但 9.5 是**一棵樹**在大值域上省空間，本節是**多棵樹**共享子樹以保留歷史。**技術上可以疊加**（不離散化的可持久化 = 動態開點 + 持久化），而且**兩者都被迫使用標記永久化**——9.5 是為了省節點，本節是為了不破壞舊版本。\n- **與 9.3／9.4 一般線段樹**：唯一結構差異是「改節點」變成「複製節點」。**9.3 定理 2 的區間分解、定理 1 的覆蓋路徑，在這裡逐字成立**——所以查詢的複雜度分析可以整段搬過來。\n- **與離線做法（整體二分、莫隊、排序+樹狀陣列）**：**能離線就先考慮它們**，空間 $O(n)$ 且常數小。整體二分尤其值得學——它用 $O(n)$ 空間換一個 $\\log$，思路是「對所有查詢同時二分答案」。**主席樹換來的是線上與更好的單次查詢複雜度。**\n- **與可持久化 Trie**：同一個想法套在 01-Trie 上，用來做「區間內與 $x$ 異或最大」。**結構更簡單（固定 30 層、無需離散化），值得作為理解可持久化的第一個例子。**\n- **與 wavelet tree**：也能做區間第 $k$ 小，空間 $O(n\\log \\sigma)$ 位元（比主席樹省很多），但實作較繁、修改困難。**競賽中主席樹的性價比更高。**\n- **與 9.7 ST 表**：ST 表是「不修改、$O(1)$ 查詢、只支援可重複貢獻的聚合」；主席樹是「不修改原陣列、$O(\\log n)$ 查詢、支援可減的計數」。**兩者的能力邊界剛好互補：ST 表能做 max 不能做第 $k$ 小，主席樹反之。**\n- **與 8 節並查集**：可持久化並查集用同樣的手法（可持久化陣列 = 可持久化線段樹的葉子），但**必須放棄路徑壓縮**（壓縮是就地修改，違反推論 5.1），只保留按秩合併，複雜度退化到 $O(\\log n)$。**又一次「不變量決定了哪些優化可用」。**\n\n## 代表例題\n\n**題號**：3762\n\n有動態更新與區間查詢；練習先離散化值域，再確認查詢/更新的閉開區間。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 3762 | [使陣列元素相等的最小操作次數](https://leetcode.cn/problems/minimum-operations-to-equalize-subarrays) | 2497 |\n\n## 本節重點速查\n\n- **核心想法**：相鄰版本只差一條根到葉的路徑 ⇒ **複製路徑、共享其餘**，每個版本只多花 $O(\\log m)$ 空間。\n- **不變量 (V)（定理 1）**：每個 `root[i]` 都是一棵語義完整的樹。**依賴兩個前提：修改只碰一條路徑；只寫新節點、絕不寫舊節點。**\n- **空間 $O(n\\log m)$（定理 2）**。節點池 `reserve(n * (log2(m) + 2) + 2)`，**開太小 RE、開太大 MLE**。\n- **主席樹 = 前綴版本 + 版本相減（定理 3）**：$\\mathrm{cnt}_r - \\mathrm{cnt}_{l-1}$。**前提是計數構成群（有反元素）——和 9.1 定理 5 是同一個條件。** 所以能做第 $k$ 小、值域計數，**不能做區間 max**（推論 3.1）。\n- **實作上不建差樹，而是兩個指標同步下降。**\n- **值域二分（定理 4）**：$c$ = 左半計數差，$k\\le c$ 往左、否則 $k \\gets k-c$ 往右。**不變量：答案是當前值域區間內的第 $k$ 小。** 正確性依賴「值域線段樹的左半嚴格小於右半」——下標線段樹上不成立。\n- **絕不 `pushDown`（定理 5）**：下傳會寫進共享節點，污染舊版本。**用標記永久化（9.5 定理 3），代價是標記必須可交換。**\n- **版本鏈可以是樹（定理 6）**：$O(1)$ 回溯、可持久化並查集、樹上主席樹（四版本同步下降）。**但「相減」只在線性前綴或樹上路徑差分時有意義。**\n- **最高頻錯誤**：`root` 的 off-by-one（查 $[l,r]$ 要用 `root[l]` 與 `root[r+1]`）；`push_back` 後持有失效參考；忘了用 `vals[lo]` 還原原值。\n- **先問能不能離線**：整體二分／排序+樹狀陣列空間 $O(n)$、常數更小。**主席樹的不可替代之處是線上。**\n",
        },
        {
          id: 67,
          title: "9.7 ST 表（Sparse Table）",
          summary:
            "## 這個技術解決什麼問題\n\n線段樹的區間查詢是 $O(\\log n)$。當查詢次數遠多於元素數——例如 $n = 10^5$ 但要在某個雙層迴圈裡問 $10^7$ 次區間 max——那個 $\\log$ 就是瓶頸。\n\n**但如果陣列從頭到尾不會被修改**，我們可以用預處理換掉這個 $\\log$：\n\n> **ST 表：$O(n\\log n)$ 預處理，$O(1)$ 回答區間 min／max／gcd。**\n\n代價是**完全不支援修改**（改一個元素要重建整張表），而且**只適用於「可重複貢獻」的運算**（定理 2 會說清楚這是什麼意思，以及為什麼求和用不了）。\n\n**辨識訊號**：\n- **陣列固定不變**，但要回答極多次區間 min／max／gcd 查詢。**「不需修改」是關鍵前提——一旦要改就改用線段樹。**\n- 演算法的內層反覆需要「某段的最值」，且該段隨著指標滑動（3691 最大子陣列總值 II、3501 操作後最大活躍區段數 II）；\n- 查詢次數 $q \\gg n$，$O(q\\log n)$ 被卡；\n- 需要在區間最值上再做二分／遞迴分治（例如笛卡爾樹式的分治，每層要 $O(1)$ 取區間最大值的位置）。\n\n**反過來說，看到「修改」兩個字就不要用 ST 表。** 這不是效率問題，是**做不到**——ST 表沒有任何增量更新的辦法。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「ST 表」想：\n\n- **陣列固定不變，但要回答超多次區間 min/max/gcd 查詢。** 不需要修改是關鍵前提——一旦要改，就改用線段樹。\n- **演算法中內層反覆需要「某段的最值」，且這段隨指標滑動。** 如 3691「最大子陣列總值 II」、3501「操作後最大活躍區段數 II」，把某些前綴量先算好後對區間取最值，用 ST 表把每次查詢壓到 `O(1)`。\n- **查詢次數遠多於元素數**，預處理一次後分攤極划算。\n\n:::example 嗅到味道的瞬間：靜態陣列 + 海量區間 max 查詢\n```cpp\n// 嗅到味道的瞬間：靜態陣列 + 海量區間 max 查詢\n// 線段樹查詢 O(log n)；若不需修改，ST 表查詢直接 O(1)\n// int ans = max(st[k][l], st[k][r - (1 << k) + 1]);\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的陣列在查詢期間真的完全不變嗎？（會變就別用 ST 表）\n2. 我的聚合是冪等的嗎（max/min/gcd）？還是求和（不適用）？\n3. 我的查詢次數值不值得花 `O(n log n)` 預處理換 `O(1)` 查詢？\n\n三題都答得出來，就套對了。\n\n## 核心想法與直覺\n\n本節介紹「ST 表（Sparse Table，稀疏表）」。它專門解決一種特定但常見的需求：陣列**不會被修改**，但要對它做**大量區間查詢**，而且查的是「冪等」的聚合，例如區間最大值、最小值、gcd。這類「靜態陣列上的區間最值查詢」叫 RMQ（Range Minimum/Maximum Query）。\n\n什麼是「冪等」？就是同一份資料重複算幾次都不影響結果，例如 `max(x, x) = x`、`gcd(x, x) = x`。求和不冪等（重複加會變大），所以 ST 表不適用於求和。\n\n:::example st[k][i]：從 i 開始、長度 2^k 的區間的最大值\n核心想法一句話：**預先算好「每個起點、長度為 2 的冪」的區間答案；查任意區間時，用兩個可重疊、但合起來剛好覆蓋整段的 2 冪區間拼出答案。** 因為冪等，重疊的部分被算兩次也沒關係。\n\n```cpp\n// st[k][i]：從 i 開始、長度 2^k 的區間的最大值\nvector<vector<int>> st;  // 第一維是 k（長度的指數），第二維是起點 i\n```\n:::\n\n\n\n:::example 預處理遞推：長度 2^k 由兩個長度 2^(k-1) 的相鄰段合併\n預處理靠一條漂亮的遞推：長度 `2^k` 的區間 = 兩個相鄰、長度 `2^(k - 1)` 的區間合併：\n\n```cpp\n// 預處理遞推：長度 2^k 由兩個長度 2^(k-1) 的相鄰段合併\nfor (int k = 1; (1 << k) <= n; ++k) {\n    for (int i = 0; i + (1 << k) <= n; ++i) {\n        st[k][i] = max(st[k - 1][i], st[k - 1][i + (1 << (k - 1))]);\n    }\n}\n```\n:::\n\n\n\n:::example 查詢 [l, r] 的最大值：兩段長 2^k 的區間覆蓋整段\n查詢 `[l, r]` 時，取最大的 k 使 `2^k <= 區間長度`，再把「從 l 起的 `2^k` 段」和「到 r 為止的 `2^k` 段」取 max——這兩段一定覆蓋整個 `[l, r]`（中間可能重疊，但冪等不怕）：\n\n```cpp\n// 查詢 [l, r] 的最大值：兩段長 2^k 的區間覆蓋整段\nint k = log2_floor[r - l + 1];  // 預先算好的整數 log2\nint ans = max(st[k][l], st[k][r - (1 << k) + 1]);\n```\n:::\n\n\n\n預處理 `O(n log n)`、空間 `O(n log n)`，但每次查詢只要 `O(1)`，非常適合「建一次、查很多次」。\n\n## 狀態／資料結構定義\n\n**狀態定義（整節只有這一個式子）：**\n\n$$\\mathit{st}[k][i] \\;=\\; \\mathrm{op}\\big(a_i,\\, a_{i+1},\\, \\dots,\\, a_{i + 2^k - 1}\\big),$$\n\n也就是**從 $i$ 開始、長度恰為 $2^k$ 的區間**的聚合值。合法範圍是 $0 \\le i$ 且 $i + 2^k \\le n$。\n\n```cpp\ntemplate <class T, class Op>\nstruct SparseTable {\n    vector<vector<T>> st;\n    Op op;\n    explicit SparseTable(const vector<T>& a, Op op = Op()) : op(op) {\n        int n = int(a.size()), K = (n > 1 ? __lg(n) : 0) + 1;\n        st.assign(K, vector<T>(n));\n        st[0] = a;\n        for (int k = 1; k < K; k++)\n            for (int i = 0; i + (1 << k) <= n; i++)\n                st[k][i] = op(st[k - 1][i], st[k - 1][i + (1 << (k - 1))]);\n    }\n    T query(int l, int r) const {            // [l, r] 皆含，需 l <= r\n        int k = __lg(r - l + 1);\n        return op(st[k][l], st[k][r - (1 << k) + 1]);\n    }\n};\n```\n\n**兩層迴圈的順序不能換**：$\\mathit{st}[k]$ 依賴 $\\mathit{st}[k-1]$，必須先把整層 $k-1$ 算完。\n\n**`query` 只有兩行，而且沒有迴圈——這就是 $O(1)$。** 它取兩段長度都是 $2^k$ 的區間：一段貼左端、一段貼右端。**這兩段一定會重疊**（除非 $r-l+1$ 恰好是 2 的冪），而**「重疊沒關係」正是 ST 表最強的假設，也是它最大的限制。**\n\n**運算 $\\mathrm{op}$ 必須滿足三條性質（缺一不可）：**\n\n| 性質 | 意思 | `max` | `gcd` | `+` |\n| --- | --- | --- | --- | --- |\n| 結合律 | $(x\\,\\mathrm{op}\\,y)\\,\\mathrm{op}\\,z = x\\,\\mathrm{op}\\,(y\\,\\mathrm{op}\\,z)$ | ✔ | ✔ | ✔ |\n| 交換律 | $x\\,\\mathrm{op}\\,y = y\\,\\mathrm{op}\\,x$ | ✔ | ✔ | ✔ |\n| **冪等（可重複貢獻）** | $x\\,\\mathrm{op}\\,x = x$ | ✔ | ✔ | **✘** |\n\n**滿足三者的運算構成一個半格（semilattice）**：`min`、`max`、`gcd`、`lcm`、按位 `&`、按位 `|`。**求和、乘積、XOR 都不冪等，ST 表對它們無效**（$x \\oplus x = 0 \\ne x$）。\n\n## 不變量或正確性證明\n\n### 定理 1（預處理的正確性）\n\n**不變量 (T)**：迴圈結束後，對所有合法的 $(k, i)$，$\\mathit{st}[k][i] = \\mathrm{op}(a_i, \\dots, a_{i+2^k-1})$。\n\n*證明（對 $k$ 歸納）*。\n\n**基底** $k = 0$：$\\mathit{st}[0][i] = a_i$，區間長度 $2^0 = 1$ ✔。\n\n**歸納步**：設 (T) 對 $k-1$ 成立。長度 $2^k$ 的區間 $[i,\\, i+2^k-1]$ 可**恰好**切成兩段長度 $2^{k-1}$ 的區間 $[i,\\, i+2^{k-1}-1]$ 與 $[i+2^{k-1},\\, i+2^k-1]$，**不重不漏**。由歸納假設兩段的值分別是 $\\mathit{st}[k-1][i]$ 與 $\\mathit{st}[k-1][i+2^{k-1}]$，由**結合律**可以先算兩段再合併 ✔。$\\square$\n\n**注意這裡只用了結合律**——預處理階段兩段不重疊，還不需要冪等。**冪等是查詢階段才用到的。**\n\n### 定理 2（$O(1)$ 查詢的正確性）\n\n*命題*：設 $\\mathit{len} = r - l + 1 \\ge 1$，$k = \\lfloor\\log_2 \\mathit{len}\\rfloor$。則\n\n$$\\mathrm{op}\\big(\\mathit{st}[k][l],\\; \\mathit{st}[k][\\,r - 2^k + 1\\,]\\big) \\;=\\; \\mathrm{op}(a_l, \\dots, a_r).$$\n\n*證明*。令 $S_1 = [\\,l,\\; l+2^k-1\\,]$、$S_2 = [\\,r-2^k+1,\\; r\\,]$。\n\n**(a) 兩段都落在 $[l, r]$ 內。** 由 $k = \\lfloor\\log_2 \\mathit{len}\\rfloor$ 得 $2^k \\le \\mathit{len}$，故 $S_1$ 的右端 $l + 2^k - 1 \\le l + \\mathit{len} - 1 = r$ ✔；同理 $S_2$ 的左端 $r - 2^k + 1 \\ge r - \\mathit{len} + 1 = l$ ✔。**這保證不會把區間外的元素算進來。**\n\n**(b) 兩段的聯集覆蓋 $[l, r]$。** 由 $k = \\lfloor\\log_2 \\mathit{len}\\rfloor$ 得 $\\mathit{len} < 2^{k+1} = 2\\cdot 2^k$，即 $2^k > \\mathit{len}/2$。於是 $S_1$ 的右端 $\\ge l + \\mathit{len}/2 - 1$，而 $S_2$ 的左端 $\\le r - \\mathit{len}/2 + 1$；具體地，$S_2$ 的左端 $-\\, S_1$ 的右端 $= (r - 2^k + 1) - (l + 2^k - 1) = \\mathit{len} + 1 - 2^{k+1} \\le 0$，所以 $S_2$ 的左端 $\\le S_1$ 的右端 $+\\,1$——**兩段首尾相接或重疊，中間沒有空隙** ✔。**這保證不會漏掉元素。**\n\n**(c) 重疊部分被算兩次也無妨。** 由 (a)(b)，$S_1 \\cup S_2 = [l, r]$，但作為**多重集** $S_1 \\uplus S_2$ 中，$S_1 \\cap S_2$ 的元素出現兩次。由**交換律與結合律**，我們可以把結果重排成 $\\mathrm{op}$ 依序作用於 $S_1\\cap S_2$ 的元素各兩次、其餘各一次；再由**冪等律** $x\\,\\mathrm{op}\\,x = x$，反覆消去重複項，得到與「每個元素恰一次」相同的結果 ✔。$\\square$\n\n**三條性質各自負責證明的一個部分**——(a)(b) 是純算術，(c) 才動用代數。**這就是為什麼求和用不了：對 `+`，重疊部分被加了兩次，答案直接錯。**\n\n**推論 2.1（唯一需要檢查的事）**：拿到一個新的 $\\mathrm{op}$，只需驗證「$x\\,\\mathrm{op}\\,x = x$」。結合律與交換律通常一眼可見，冪等律才是真正的分水嶺。$\\gcd(x,x)=x$ ✔、$\\mathrm{lcm}(x,x)=x$ ✔、$x\\,\\&\\,x = x$ ✔、$x \\oplus x = 0$ ✘。\n\n**推論 2.2（區間長度為 2 的冪時兩段重合）**：此時 $2^k = \\mathit{len}$，$S_1 = S_2$，計算的是 $\\mathrm{op}(v, v) = v$。**這是冪等律的極端情形，也順帶說明程式碼不必特判。**\n\n### 定理 3（$k$ 的計算必須是**下**取整）\n\n*命題*：定理 2 的 (a) 依賴 $2^k \\le \\mathit{len}$，(b) 依賴 $2^{k+1} > \\mathit{len}$。**兩者合起來唯一確定 $k = \\lfloor\\log_2 \\mathit{len}\\rfloor$。**\n\n- 若取大了（$2^k > \\mathit{len}$）：(a) 失效，$S_1$ 會伸出 $r$ 之外，**答案包含不該有的元素**，而且 $\\mathit{st}[k][l]$ 甚至可能越界。\n- 若取小了（$2^{k+1} \\le \\mathit{len}$）：(b) 失效，中間出現空隙，**漏算元素**。\n\n**實作上有兩種算法**：\n- `__lg(x)`（GCC 內建，$O(1)$，`x` 必須 $> 0$）；\n- 預處理 `lg[1] = 0; lg[i] = lg[i/2] + 1;`（可攜、無分支、快取友善）。\n\n**`__lg(0)` 是未定義行為**（底層是 `31 - __builtin_clz(0)`，而 `__builtin_clz(0)` 未定義）。**所以 `query` 必須保證 $l \\le r$。空區間要由呼叫端處理，不要指望 `query` 回傳單位元。**\n\n### 定理 4（非冪等運算怎麼辦：Disjoint Sparse Table）\n\n冪等律不是天生必要的——**它只是「允許重疊」這個偷懶做法的代價。** 若改成「保證兩段不重疊」，就只需要結合律。\n\n*Disjoint Sparse Table 的構造*：對第 $k$ 層，把陣列按 $2^{k+1}$ 分塊；在每塊內以塊中點為界，**向左做後綴聚合、向右做前綴聚合**，都存進 $\\mathit{dst}[k]$。\n\n*查詢 $[l, r]$*：取 $k$ 使得 $l$ 與 $r$ 在第 $k$ 層**首次被同一塊的中點分開**——即 $k = \\lfloor\\log_2(l \\oplus r)\\rfloor$。此時 $\\mathit{dst}[k][l]$ 是「$l$ 到中點」的後綴、$\\mathit{dst}[k][r]$ 是「中點到 $r$」的前綴，**兩段不重不漏**，合併即得答案。\n\n*正確性的關鍵*：$l \\oplus r$ 的最高位 $k$，恰好是 $l$ 與 $r$ 在二進位上首次分歧的位置，也就是「以 $2^{k+1}$ 分塊時，$l$ 與 $r$ 同塊但被中點分開」的那一層 ✔。\n\n**所以：$O(n\\log n)$ 預處理、$O(1)$ 查詢、只需結合律——連求和、矩陣乘法都能做。** 代價是實作較繁、常數較大、且 $l = r$ 要特判（$l\\oplus r = 0$）。\n\n**但要誠實**：**區間和用前綴和就好（$O(n)$ 空間、$O(1)$ 查詢），DST 是為了「不可減的結合運算」而生**——例如區間矩陣乘積、區間最大子段和的 `Info` 合併（9.3 定理 3 的那種）。**LeetCode 上罕見，知道有這個東西即可。**\n\n### 定理 5（gcd 的 ST 表：複雜度與一個常用性質）\n\n**複雜度不一樣**：`gcd` 本身是 $O(\\log V)$，所以預處理是 $O(n\\log n\\log V)$、查詢是 $O(\\log V)$ 而非 $O(1)$。**寫「$O(1)$ 查詢」的時候心裡要清楚這是對 `min`／`max` 說的。**\n\n**一個常被用到的性質**：*固定左端點 $l$，序列 $g_r = \\gcd(a_l, \\dots, a_r)$ 隨 $r$ 增大而單調不增（在整除意義下），且**至多只有 $O(\\log V)$ 個不同值**。*\n\n*證明*。$g_{r+1} \\mid g_r$。若 $g_{r+1} \\ne g_r$，則 $g_{r+1} \\le g_r / 2$（真因數至多是一半）。從 $g_l = a_l \\le V$ 開始，最多減半 $\\log_2 V$ 次就到 1 ✔。$\\square$\n\n**這個性質讓「枚舉所有不同的區間 gcd」變成 $O(n\\log V)$ 而非 $O(n^2)$**，配上 ST 表的二分（對固定 $l$ 二分出每段 gcd 相同的極大區間）就能高效處理「有多少子陣列的 gcd 等於 $x$」這類問題。**同樣的論證對 `按位 &`、`按位 |` 也成立**（每次變化至少改變一個位元，故至多 $O(\\log V)$ 種值）——**這是「ST 表 + 值段合併」這一族解法的共同基礎。**\n\n### 定理 6（ST 表上的二分：$O(\\log n)$ 找最遠邊界）\n\n*問題*：固定 $l$，求最大的 $r$ 使得 $\\max(a_l, \\dots, a_r) \\le x$。\n\n由於 $\\max$ 隨 $r$ 單調不減，可以直接二分 + `query`，$O(\\log n)$ 且每步 $O(1)$。**但也可以用「倍增」做成一次掃描**：從 $k = \\lfloor\\log_2 n\\rfloor$ 往下，若 $\\mathit{st}[k][\\mathit{pos}] \\le x$ 就 $\\mathit{pos} \\mathrel{+}= 2^k$。**正確性與 LCA 倍增完全同構**——不變量是「$\\mathit{pos}$ 始終是合法的右端界之後一格」，每步嘗試盡量大的跳躍。\n\n**與 9.3 定理 4 的線段樹上二分對照**：線段樹二分是 $O(\\log n)$ 且**支援修改**；ST 表二分也是 $O(\\log n)$ 但**不支援修改**。**單看這個操作，ST 表沒有優勢——它的優勢只在單次 $O(1)$ 的區間查詢。**\n\n## 逐步演算法\n\n:::example log2 表：log2_floor[x] = floor(log2(x))\n**1. 預先算好整數 log2 表，加速查詢。** 查詢時要快速得到 `floor(log2(len))`。\n\n```cpp\n// log2 表：log2_floor[x] = floor(log2(x))\nvector<int> log2_floor(n + 1);\nfor (int i = 2; i <= n; ++i) {\n    log2_floor[i] = log2_floor[i / 2] + 1;\n}\n```\n:::\n\n\n\n:::example 第 0 層：長度 1 的區間就是元素自己\n**2. 狀態表示與初始化第 0 層。** `st[0][i]` 就是元素本身（長度 `2^0 = 1`）。\n\n```cpp\n// 第 0 層：長度 1 的區間就是元素自己\nint K = log2_floor[n] + 1;\nst.assign(K, vector<int>(n));\nfor (int i = 0; i < n; ++i) {\n    st[0][i] = a[i];\n}\n```\n:::\n\n\n\n:::example 填表：每層由下一層兩段合併\n**3. 由低層往高層遞推填表（pushUp）。** 用前面示範的雙層迴圈把 `st[k][i]` 全部算出。\n\n```cpp\n// 填表：每層由下一層兩段合併\nfor (int k = 1; k < K; ++k) {\n    for (int i = 0; i + (1 << k) <= n; ++i) {\n        st[k][i] = max(st[k - 1][i], st[k - 1][i + (1 << (k - 1))]);\n    }\n}\n```\n:::\n\n\n\n**4. O(1) 查詢與邊界、複雜度。** 用前面的查詢式。注意：\n\n- ST 表**不支援修改**：任何元素一變，整張表幾乎都要重算，這時該改用線段樹。\n- 只有冪等聚合（max / min / gcd / 按位 and、or）能用「重疊兩段」的 `O(1)` 查法；求和、區間積這類**不能**，需改用前綴和或線段樹。\n- 區間長度為 0 要特判；下標別越界（`r - (1 << k) + 1` 必須 `>= l`，由 k 的取法保證）。\n- 複雜度：預處理 `O(n log n)`、空間 `O(n log n)`，查詢 `O(1)`。\n\n## C++17 模板\n\n**模板一：泛型 ST 表（最常用的形態）**\n\n```cpp\ntemplate <class T, class Op>\nstruct SparseTable {\n    vector<vector<T>> st;\n    Op op;\n    SparseTable() = default;\n    SparseTable(const vector<T>& a, Op op = Op()) : op(op) {\n        int n = int(a.size());\n        if (n == 0) return;\n        int K = __lg(n) + 1;\n        st.assign(K, vector<T>(n));\n        st[0] = a;\n        for (int k = 1; k < K; k++)\n            for (int i = 0; i + (1 << k) <= n; i++)\n                st[k][i] = op(st[k - 1][i], st[k - 1][i + (1 << (k - 1))]);\n    }\n    // [l, r] 兩端皆含；呼叫端必須保證 0 <= l <= r < n\n    T query(int l, int r) const {\n        int k = __lg(r - l + 1);\n        return op(st[k][l], st[k][r - (1 << k) + 1]);\n    }\n};\n\n// 用法\nSparseTable<int, function<int(int, int)>> mx(a, [](int x, int y) { return max(x, y); });\nSparseTable<int, function<int(int, int)>> g (a, [](int x, int y) { return gcd(x, y); });\n```\n\n**用 `function` 方便但有間接呼叫開銷；效能敏感時改用無狀態的 functor：**\n\n```cpp\nstruct MaxOp { int operator()(int x, int y) const { return x > y ? x : y; } };\nSparseTable<int, MaxOp> mx(a);          // 可完全內聯\n```\n\n**模板二：一維扁平化（省記憶體、更快）**\n\n```cpp\nstruct MaxST {\n    int n, K;\n    vector<int> st, lg;                  // st 攤平成 K * n\n    explicit MaxST(const vector<int>& a) : n(int(a.size())) {\n        lg.assign(n + 1, 0);\n        for (int i = 2; i <= n; i++) lg[i] = lg[i >> 1] + 1;\n        K = lg[max(n, 1)] + 1;\n        st.assign(size_t(K) * n, INT_MIN);\n        copy(a.begin(), a.end(), st.begin());\n        for (int k = 1; k < K; k++)\n            for (int i = 0; i + (1 << k) <= n; i++)\n                st[size_t(k) * n + i] = max(st[size_t(k - 1) * n + i],\n                                            st[size_t(k - 1) * n + i + (1 << (k - 1))]);\n    }\n    int query(int l, int r) const {\n        int k = lg[r - l + 1];\n        return max(st[size_t(k) * n + l], st[size_t(k) * n + r - (1 << k) + 1]);\n    }\n};\n```\n\n**自建 `lg` 表而不用 `__lg`，順帶避開了 `__lg(0)` 的未定義行為（`lg[0] = 0`），也不依賴 GCC 擴充。**\n\n**模板三：同時要最值與其位置**\n\n```cpp\n    // 存下標而非值，比較時查原陣列——這樣就能回傳「最大值在哪」\n    int better(int i, int j) const { return a[i] >= a[j] ? i : j; }\n```\n\n**「回傳位置」比「回傳值」更通用**（有了位置就有值，反之不然），而且是笛卡爾樹式分治的必要條件。**唯一要小心的是相等時的取捨要一致**，否則同一個查詢可能回傳不同下標。\n\n## 時間與空間複雜度\n\n| | 預處理 | 單次查詢 | 空間 |\n| --- | --- | --- | --- |\n| ST 表（min／max／&／\\|） | $O(n\\log n)$ | $O(1)$ | $O(n\\log n)$ |\n| ST 表（gcd／lcm） | $O(n\\log n\\log V)$ | $O(\\log V)$ | $O(n\\log n)$ |\n| Disjoint ST 表（任意結合運算） | $O(n\\log n)$ | $O(1)$ | $O(n\\log n)$ |\n| 線段樹 | $O(n)$ | $O(\\log n)$ | $O(n)$ |\n| 前綴和（僅可減運算） | $O(n)$ | $O(1)$ | $O(n)$ |\n\n**$q$ 次查詢的總時間**：ST 表 $O(n\\log n + q)$、線段樹 $O(n + q\\log n)$。**交叉點大約在 $q \\approx n$**：$q \\ll n$ 時線段樹（甚至暴力）更好，$q \\gg n$ 時 ST 表明顯勝出。\n\n**空間是 ST 表唯一真正的弱點，必須試算**：\n\n| $n$ | $K = \\log_2 n + 1$ | `int` 表大小 | 結論 |\n| --- | --- | --- | --- |\n| $10^4$ | 15 | 0.6 MB | 安全 |\n| $10^5$ | 18 | 7.2 MB | 安全 |\n| $10^6$ | 21 | **84 MB** | 危險 |\n| $10^7$ | 25 | 1000 MB | 不可行 |\n\n**$n \\ge 10^6$ 就要改用「分塊 + 塊內 ST 表」的 $O(n)$–$O(1)$ 方案，或直接退回線段樹。** LeetCode 的 $n$ 通常 $\\le 10^5$，ST 表夠用。\n\n**另外**：`vector<vector<T>>` 的每一層是獨立配置，查詢時兩次間接定址。**模板二的扁平化版本在熱迴圈裡通常快 20%–40%。**\n\n## 常見錯誤與邊界條件\n\n- **拿去做區間和／XOR**。不冪等，重疊部分被算兩次（定理 2(c)）。**這是最根本的誤用，而且測資小時可能剛好蒙對——因為兩段不重疊時答案是對的。務必用長度非 2 的冪的區間測試。**\n- **`__lg(0)`**。$l > r$ 時 `r - l + 1 <= 0`，`__lg` 未定義。**呼叫端保證 $l \\le r$，或改用自建 `lg` 表。**\n- **`k` 用了上取整**（定理 3）。`__lg` 本身就是下取整，但若手寫成 `ceil(log2(len))` 就錯了；**`log2` 的浮點誤差也會在 $\\mathit{len}$ 恰為 2 的冪時出錯**（可能得到 $k$ 或 $k-1$）。**不要用浮點 `log2`。**\n- **$K$ 開太小**。$K$ 必須 $\\ge \\lfloor\\log_2 n\\rfloor + 1$。**寫成 `__lg(n)` 少一層，最長的那個查詢就越界。**\n- **內層迴圈的邊界寫成 `i < n`**。必須是 `i + (1 << k) <= n`，否則讀到未初始化的元素。\n- **兩層迴圈順序顛倒**。必須 $k$ 在外、$i$ 在內。\n- **建表後修改了原陣列**。ST 表不會跟著變，**而且不會報錯**——只會安靜地回答舊資料。**若模板三存的是下標且參考原陣列，這一點尤其危險。**\n- **`min`／`max` 的初始值**。扁平化版把未使用的格子填 `INT_MIN`（對 `max`）；若拿去做 `min` 要改成 `INT_MAX`。**單位元選錯，越界讀取就會安靜地污染答案。**\n- **`gcd` 沒處理 0**。$\\gcd(0, x) = x$ 是對的（0 是 `gcd` 的單位元），但若題目語義上 0 代表「空」，就要另外約定。\n- **記憶體**。$n \\ge 10^6$ 時 $O(n\\log n)$ 會炸（見上表）。\n- **回傳位置時相等的取捨不一致**（模板三），導致同一查詢的兩段給出不同下標。\n\n## 與相似技巧的比較\n\n- **與 9.3／9.4 線段樹**：**能不能修改，是唯一的分水嶺。** 線段樹 $O(n)$ 空間、$O(\\log n)$ 查詢、支援修改；ST 表 $O(n\\log n)$ 空間、$O(1)$ 查詢、**完全不支援修改**。而且線段樹只需要么半群（9.3），ST 表還要求冪等——**ST 表能做的事嚴格少於線段樹，換來的只有那個常數級的查詢。** $q \\gg n$ 時這個交換才划算。\n- **與 9.1 樹狀陣列**：樹狀陣列要群（9.1 定理 5），ST 表要半格，**兩者的代數要求不相容**：`sum` 有反元素但不冪等（BIT ✔ / ST ✘），`max` 冪等但沒反元素（BIT ✘ / ST ✔）。**「這個運算有沒有反元素／是不是冪等」決定了你該用哪個工具——這是第 9 節從頭到尾的主線。**\n- **與前綴和（2 節）**：前綴和是 $O(n)$–$O(1)$ 的最佳解，**但只對可減運算成立**。ST 表可以視為「不可減運算的前綴和替代品」，代價是 $\\log n$ 倍空間。\n- **與單調隊列（5 節）**：若查詢的是**固定長度**的滑動窗口最值，單調隊列 $O(n)$ 時間 $O(n)$ 空間，全面優於 ST 表。**只有窗口長度會變時才需要 ST 表。**\n- **與離線 + 排序掃描**：若所有查詢事先已知，把查詢按右端點排序 + 單調棧，往往 $O(n\\log n)$ 就能解，空間 $O(n)$。**又是那句話：能離線就先想離線。**\n- **與 Disjoint Sparse Table（定理 4）**：放棄「重疊」換掉冪等要求，能處理任意結合運算。**LeetCode 罕見，但它清楚地說明了「冪等」在 ST 表裡到底扮演什麼角色——它只是為了讓兩段可以重疊。**\n- **與 LCA 的倍增表（trees 章）**：$\\mathit{st}[k][i]$ 與 $\\mathit{up}[k][v]$ 是同一個「倍增」骨架——**狀態定義都是「從 $x$ 出發跳 $2^k$ 步」，遞推都是「兩個 $2^{k-1}$ 拼成一個 $2^k$」。** 差別在 LCA 的跳躍必須不重疊（所以是 $O(\\log n)$ 查詢），而 ST 表允許重疊（所以是 $O(1)$）。**認出這是同一個骨架，兩個東西就只需要記一次。**\n- **與 9.6 主席樹**：能力互補——ST 表做 max 不能做第 $k$ 小，主席樹做第 $k$ 小不能做 max（推論 3.1）。**兩者都不支援修改。**\n- **與 $O(n)$–$O(1)$ RMQ**：把陣列切成大小 $\\log n$ 的塊，塊間用 ST 表（$O(n/\\log n \\cdot \\log n) = O(n)$ 空間），塊內用笛卡爾樹的位元遮罩。**理論最優但常數大、實作複雜，競賽中幾乎不需要。** 知道 $O(n\\log n)$ 不是下界就好。\n\n## 代表例題\n\n**題號**：3501、3691\n\n有動態更新與區間查詢；練習先離散化值域，再確認查詢/更新的閉開區間。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 3501 | [操作後最大活躍區段數 II](https://leetcode.cn/problems/maximize-active-section-with-trade-ii) | 2941 |\n| 3691 | [最大子陣列總值 II](https://leetcode.cn/problems/maximum-total-subarray-value-ii) | 2469 |\n\n**延伸閱讀**：[OI Wiki：Sparse Table](https://oi-wiki.org/ds/sparse-table/)。ST 表適合靜態、冪等的 RMQ；若運算不冪等，重疊兩塊會重複計入。\n\n## 本節重點速查\n\n- **存在理由**：**陣列不變 + 查詢極多**。$O(n\\log n)$ 預處理換 $O(1)$ 查詢。**看到「修改」就換線段樹——ST 表沒有增量更新的辦法。**\n- **狀態**：$\\mathit{st}[k][i] = \\mathrm{op}(a_i \\dots a_{i+2^k-1})$。遞推 $\\mathit{st}[k][i] = \\mathrm{op}(\\mathit{st}[k-1][i],\\, \\mathit{st}[k-1][i+2^{k-1}])$，**$k$ 在外層、$i$ 在內層，邊界是 `i + (1<<k) <= n`。**\n- **查詢兩行**：$k = \\lfloor\\log_2(r-l+1)\\rfloor$，$\\mathrm{op}(\\mathit{st}[k][l],\\, \\mathit{st}[k][r-2^k+1])$。**兩段重疊，靠冪等律消掉重複。**\n- **三條性質（定理 2）**：結合、交換、**冪等**。(a) $2^k\\le \\mathit{len}$ ⇒ 不超界；(b) $\\mathit{len} < 2^{k+1}$ ⇒ 不漏；(c) 冪等 ⇒ 重疊無害。**唯一要檢查的是 $x\\,\\mathrm{op}\\,x = x$**（推論 2.1）：`min`／`max`／`gcd`／`lcm`／`&`／`|` ✔，`+`／`*`／`^` ✘。\n- **$k$ 必須下取整（定理 3）**，用 `__lg` 或自建 `lg` 表，**絕不用浮點 `log2`**；`__lg(0)` 未定義，呼叫端保證 $l\\le r$。\n- **`gcd` 版查詢是 $O(\\log V)$ 不是 $O(1)$（定理 5）**；附帶性質：固定左端點，區間 gcd 至多 $O(\\log V)$ 種值（`&`、`|` 同理）——這是「值段合併」解法的基礎。\n- **空間 $O(n\\log n)$ 是唯一硬傷**：$n=10^5$ ⇒ 7 MB 安全，$n=10^6$ ⇒ 84 MB 危險。**扁平成一維陣列可快 20%–40%。**\n- **Disjoint ST（定理 4）**：改成不重疊分段，只需結合律，可做求和／矩陣乘；$k = \\lfloor\\log_2(l\\oplus r)\\rfloor$。\n- **與倍增 LCA 是同一個骨架**：狀態都是「跳 $2^k$」，差別只在允不允許重疊。\n- **選擇順序**：可減 ⇒ 前綴和；固定窗口 ⇒ 單調隊列；要修改 ⇒ 線段樹；能離線 ⇒ 排序掃描；**其餘（不變、多查、冪等）才輪到 ST 表。**\n",
        },
      ],
    },
    {
      id: 68,
      title: "10. 伸展樹（Splay 樹）",
      src: "https://leetcode.cn/circle/discuss/mOr1u6/",
      summary:
        "平衡樹的一種，支援分裂、合併與區間反轉；常作為線段樹難以表達的動態序列底層。\n\n**取捨**：實作量大，賽場上優先確認是否可用 simpler 結構替代。",
      children: [
        {
          id: 69,
          title: "10.1 基礎",
          summary:
            "## 這個技術解決什麼問題\n\n線段樹（9 節）能對**固定長度**的序列做區間修改與區間查詢，但它有一個根本限制：**下標是寫死的。** 一旦要在中間插入或刪除元素，後面所有元素的下標都會平移，線段樹只能重建。\n\n而「設計一個文字編輯器」（2296）恰恰要在游標處反覆插字、刪字；更一般的序列問題還要求「把第 $l$ 到第 $r$ 個元素整段反轉」。**這類「在任意位置插刪、並對任意一段做操作」的需求，需要一棵能維護序列的平衡樹。**\n\n**伸展樹（Splay tree）的核心轉念只有一句：**\n\n> **不要把樹當作「有序集合」，而是把它的中序走訪序列當作「陣列」。** 節點的比較鍵不是元素的值，而是它在序列中的**位置**——而位置由子樹大小 `sz` 隱含地決定，不需要真的存下來。\n\n於是：\n- 「取第 $k$ 個」= 按 `sz` 在樹上下降；\n- 「在第 $k$ 個之後插入」= 找到位置後掛上新節點；\n- 「對 $[l, r]$ 做事」= 把這段**整體提取成一棵子樹**（定理 5），然後像線段樹一樣打標記。\n\n**伸展樹的獨特之處在於它不維護任何平衡資訊**——沒有顏色、沒有高度、沒有隨機優先級。它只做一件事：**每次訪問完一個節點，就把它旋轉到根（splay）。** 這個看似隨意的動作，能保證**攤還** $O(\\log n)$（定理 3）。\n\n**辨識訊號**：\n- 資料是一個序列，要在**任意位置**插入／刪除，並查詢第 $k$ 個或某段的聚合（2296 設計文字編輯器）；\n- 需要對一整段做**反轉、平移**這類「重排」操作，而且次數很多；\n- 暴力是每次操作搬 $O(n)$ 個元素，$n$ 與操作數都在 $10^5$ 等級。\n\n**先說一句誠實話**：**LeetCode 上真的非寫伸展樹不可的題目極少。** 2296 用兩個堆疊就能 $O(1)$ 每次操作（見「與相似技巧的比較」）。學它的價值在於**「把序列裝進平衡樹」這個想法**，以及**勢能法攤還分析的一個經典範例**——這兩樣東西在別處反覆出現。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，可以往伸展樹（或同類的平衡樹）想：\n\n- **資料是一個「序列」，而且要在任意位置插入 / 刪除，並查詢第 k 個或某段的聚合。** 像「設計一個文字編輯器」（2296）要在游標處插字、刪字、左右移動，正是序列的中間插刪。\n- **需要對「一整段區間」做反轉、平移之類的操作，而且操作很多次。** 普通陣列做不到 `O(log n)`，這時平衡樹才划算。\n- **暴力是每次操作都搬動 `O(n)` 個元素；** 伸展樹把它壓到均攤 `O(log n)`。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我把序列的「順序」對應到樹的什麼遍歷？（中序）\n2. 我要查的東西（第 k 個 / 區間聚合）靠哪個欄位定位？（`size`）\n3. 我的區間操作怎麼「孤立」成一棵子樹來打標記？\n\n## 核心想法與直覺\n\n本節要解決的場景是：我們有一個「序列」（一排有順序的元素），而且要對它做很多花樣的操作——在中間插入、刪除一段、把某一段整個反轉、查詢第 k 個位置的值。如果用普通陣列，插入或刪除中間元素要搬動後面全部，一次就要 `O(n)`，太慢。\n\n伸展樹（Splay 樹）是一種會「自我整理」的二元搜尋樹。它的招牌動作叫 splay（伸展）：每次只要碰到某個節點，就用一連串「旋轉」把它一路轉到樹根。\n\n:::example 右旋：把 x 轉上去取代它的父親 p（保持中序順序不變）\n先看旋轉是什麼。旋轉就是把一條父子邊「翻過來」，讓兒子變成父親，但**保持中序遍歷（由小到大走訪）的順序不變**。這點很關鍵：順序不變，代表序列本身沒被破壞，只是樹的形狀改變了。\n\n```cpp\n// 右旋：把 x 轉上去取代它的父親 p（保持中序順序不變）\nvoid rotate(int x) {\n    int p = fa[x], g = fa[p];\n    int side = (ch[p][1] == x);  // x 是 p 的左兒子還是右兒子\n    ch[p][side] = ch[x][!side];  // 把 x 的內側子樹過繼給 p\n    ch[x][!side] = p;            // p 變成 x 的兒子\n    // ...接上 g、修正父指標、最後 pushUp(p), pushUp(x)\n}\n```\n:::\n\n\n\n為什麼要把節點轉到根？因為「最近碰過的東西很可能馬上又碰」，把它放到根附近，下次找它就快。splay 的均攤效果讓每個操作平均只花 `O(log n)`。\n\n:::example 每個節點要維護的資訊\n為了支援「查第 k 個」「反轉一段」，每個節點還要多存兩樣東西：子樹大小 `size`（這棵子樹底下有幾個元素），以及反轉懶標記 `rev`。`size` 讓我們能像翻書找頁碼一樣定位第 k 個元素。\n\n```cpp\n// 每個節點要維護的資訊\nint ch[N][2];  // 左右兒子\nint fa[N];     // 父指標\nint sz[N];     // 子樹大小（這棵子樹有幾個元素）\nbool rev[N];   // 反轉懶標記：這棵子樹是否「待反轉」\n```\n:::\n\n## 狀態／資料結構定義\n\n```cpp\nstruct Splay {\n    struct Node {\n        int ch[2] = {0, 0};      // 左右子\n        int fa = 0;              // 父節點（伸展樹必須存父指標）\n        int sz = 0;              // 子樹大小\n        int val = 0;             // 元素值\n        bool rev = false;        // 區間反轉的懶標記\n    };\n    vector<Node> t;\n    int root = 0;\n};\n```\n\n**四個約定，整節都靠它們：**\n\n1. **`t[0]` 是空節點，`sz = 0`。** 與 9.5／9.6 的哨兵是同一個手法——讀到 `0` 自動回傳單位元，省掉所有空指標判斷。\n2. **中序走訪序列 = 使用者看到的陣列。** 樹上完全沒有「下標」這個欄位；**第 $k$ 個元素是靠 `sz` 現算出來的。**\n3. **必須存父指標 `fa`。** 這是伸展樹與 Treap 的關鍵差異：splay 要從 $x$ 一路往上轉，沒有 `fa` 走不了。**代價是每次旋轉要更新三個 `fa`，這是最高頻的 bug 來源。**\n4. **序列頭尾各加一個哨兵節點。** 於是「第 $i$ 個元素」在樹中是「中序第 $i+1$ 個」。**這個偏移不是為了美觀，是定理 5 的前提**——沒有哨兵，$[1, n]$ 這種貼邊的區間就提取不出來。\n\n**兩個基本操作：**\n\n```cpp\n    void pushUp(int x) { t[x].sz = t[t[x].ch[0]].sz + t[t[x].ch[1]].sz + 1; }\n\n    void pushDown(int x) {\n        if (!t[x].rev) return;\n        swap(t[x].ch[0], t[x].ch[1]);\n        if (t[x].ch[0]) t[t[x].ch[0]].rev ^= 1;\n        if (t[x].ch[1]) t[t[x].ch[1]].rev ^= 1;\n        t[x].rev = false;\n    }\n```\n\n**`rev` 的語義沿用 9.4 的約定**：「`x` 自己已經反轉好了，但兩個子樹還沒有。」**所以 `swap` 發生在 `pushDown` 的當下，而不是打標記的當下。**\n\n## 不變量或正確性證明\n\n以下記 $s(x)$ 為 $x$ 的子樹大小，$r(x) = \\log_2 s(x)$（稱為 $x$ 的**秩**）。\n\n### 定理 1（旋轉保持中序序列）\n\n**這是整棵樹的根本不變量**：無論做多少次旋轉，**中序走訪序列不變**。\n\n*證明*。設 $x$ 是 $p$ 的左子（右子對稱），$A = x$ 的左子樹、$B = x$ 的右子樹、$C = p$ 的右子樹。旋轉前，以 $p$ 為根的子樹的中序序列是\n\n$$A,\\; x,\\; B,\\; p,\\; C.$$\n\n右旋後 $x$ 成為父、$p$ 成為 $x$ 的右子、$B$ 成為 $p$ 的左子，中序序列是\n\n$$A,\\; x,\\; \\underbrace{B,\\; p,\\; C}_{p\\text{ 的子樹}}$$\n\n——**逐字相同** ✔。而該子樹在整棵樹中的位置沒變（$x$ 接替了 $p$ 原來的位置），故全樹中序序列不變 ✔。$\\square$\n\n**推論 1.1**：既然中序序列不變，**任何由中序位置定義的查詢（第 $k$ 個、區間 $[l,r]$）在旋轉前後都指向同一批元素。** splay 可以在任意時刻執行而不影響語義——這是後面所有操作的許可證。\n\n**推論 1.2（維護順序）**：旋轉時 `pushUp` 必須**先算 $p$ 再算 $x$**（$p$ 變成了 $x$ 的子節點）。順序寫反，`sz` 就永遠差一截。\n\n### 定理 2（splay 的正確性）\n\n*操作*：`splay(x, goal)` 反覆旋轉，把 $x$ 移到 `goal` 的子節點位置（`goal = 0` 表示移到根）。每一步依 $x$、父 $p$、祖父 $g$ 的相對位置分三種情形：\n\n| 情形 | 條件 | 動作 |\n| --- | --- | --- |\n| **zig** | $p$ 就是 `goal` 的子（$g$ 是 `goal`） | 轉 $x$ 一次 |\n| **zig-zig** | $x$ 與 $p$ **同側** | **先轉 $p$，再轉 $x$** |\n| **zig-zag** | $x$ 與 $p$ **異側** | 轉 $x$ 兩次 |\n\n*正確性*：每一步之後 $x$ 的深度嚴格減少（zig 減 1，另兩者減 2），故有限步後 $x$ 到達目標位置 ✔；由定理 1，中序序列全程不變 ✔。$\\square$\n\n**關鍵前提：splay 之前必須把根到 $x$ 的路徑上所有標記 `pushDown` 完畢。** 否則旋轉會把「尚未生效的標記」搬到錯誤的子樹上。**實作上有兩種寫法**：往下找 $x$ 的過程中順手 `pushDown`（推薦），或在 splay 開始前用一個堆疊自頂向下推一遍。\n\n### 定理 3（攤還 $O(\\log n)$：Access Lemma）\n\n**這是伸展樹存在的全部理由，也是本節最值得讀的一段。**\n\n*勢能函數*：$\\Phi = \\sum_{x} r(x) = \\sum_x \\log_2 s(x)$。（$n$ 個節點時 $0 \\le \\Phi \\le n\\log_2 n$。）\n\n**引理（Access Lemma）**：一次 `splay(x)` 到根的**攤還成本**至多為 $3\\big(r(\\mathrm{root}) - r(x)\\big) + 1$，其中 $r$ 取操作**前**的值。\n\n*證明*。逐步分析。記某一步之前的秩為 $r$、之後為 $r'$。\n\n**zig-zig**（實際成本 2 次旋轉）。旋轉後 $x$ 佔據 $g$ 原來的位置，故 $s'(x) = s(g)$，即 $r'(x) = r(g)$。勢能只有 $x, p, g$ 三點改變：\n\n$$\\Delta\\Phi = r'(x)+r'(p)+r'(g) - r(x)-r(p)-r(g) = r'(p)+r'(g)-r(x)-r(p).$$\n\n由樹的包含關係 $r'(p) \\le r'(x)$ 且 $r(p) \\ge r(x)$，得\n\n$$\\Delta\\Phi \\;\\le\\; r'(x) + r'(g) - 2r(x).$$\n\n**現在是關鍵一步。** 旋轉後 $x$ 的子樹包含了「$x$ 原來的子樹」與「$g$ 旋轉後的子樹」這兩塊互不相交的部分（外加 $p$ 本身），所以\n\n$$s(x) + s'(g) \\;\\le\\; s'(x).$$\n\n由 $\\log$ 的**凹性**（$\\log a + \\log b \\le 2\\log\\frac{a+b}{2}$）：\n\n$$r(x) + r'(g) \\;\\le\\; 2\\log_2\\frac{s(x)+s'(g)}{2} \\;\\le\\; 2\\log_2\\frac{s'(x)}{2} \\;=\\; 2r'(x) - 2.$$\n\n於是攤還成本\n\n$$2 + \\Delta\\Phi \\;\\le\\; 2 + r'(x)+r'(g)-2r(x) \\;=\\; 2 + \\underbrace{\\big(r(x)+r'(g)-2r'(x)\\big)}_{\\le\\,-2} + 3\\big(r'(x)-r(x)\\big) \\;\\le\\; 3\\big(r'(x)-r(x)\\big).$$\n\n**zig-zag**（實際成本 2）。同樣有 $r'(x) = r(g)$，且旋轉後 $p$ 與 $g$ 的子樹互不相交、聯集不超過 $x$ 的新子樹，故 $s'(p)+s'(g) \\le s'(x)$，由凹性 $r'(p)+r'(g) \\le 2r'(x)-2$。因此\n\n$$2 + \\Delta\\Phi = 2 + r'(p)+r'(g)-r(x)-r(p) \\le 2 + 2r'(x) - 2 - 2r(x) = 2\\big(r'(x)-r(x)\\big) \\le 3\\big(r'(x)-r(x)\\big).$$\n\n（用了 $r(p) \\ge r(x)$。）\n\n**zig**（實際成本 1，整個 splay 中至多發生一次）。$r'(p) \\le r(p)$ 且 $r'(x) = r(\\mathrm{root})$，故\n\n$$1 + \\Delta\\Phi = 1 + r'(x)+r'(p)-r(x)-r(p) \\le 1 + r'(x)-r(x) \\le 1 + 3\\big(r'(x)-r(x)\\big).$$\n\n**把所有步驟相加**：右側的 $3(r'(x)-r(x))$ 首尾相消（望遠鏡求和），只剩第一步的 $r(x)$ 與最後一步的 $r'(x) = r(\\mathrm{root})$，再加上至多一次 zig 的 $+1$：\n\n$$\\text{總攤還成本} \\;\\le\\; 3\\big(r(\\mathrm{root}) - r(x)\\big) + 1 \\;\\le\\; 3\\log_2 n + 1. \\qquad\\square$$\n\n**由於 $\\Phi \\ge 0$ 且初始 $\\Phi \\le n\\log_2 n$，$m$ 次操作的總實際成本 $\\le O\\big((n+m)\\log n\\big)$** ✔。\n\n**這個證明依賴的前提，每一條都要點名**：\n1. **勢能函數選 $\\sum\\log s(x)$**——換成「節點數」或「高度」都證不出來。**$\\log$ 的凹性是整個證明的引擎。**\n2. **zig-zig 必須先轉 $p$ 再轉 $x$**（定理 4 說明為什麼）。\n3. **這是攤還界，不是最壞界。** 單次操作可以是 $\\Theta(n)$（例如對一條單鏈的最深節點做第一次 splay）。**題目若要求「每次操作都必須快」，伸展樹不合適。**\n\n### 定理 4（為什麼 zig-zig 不能拆成兩次 zig）\n\n*反例*。把 $n$ 個節點排成一條向左的單鏈（每個節點只有左子），對最深的節點 $x$ 做 splay。\n\n- **正確的 zig-zig 做法**：每做一次 zig-zig，**整條鏈的深度大致減半**（$x$ 上方的路徑被摺疊）。$O(\\log n)$ 次之後樹的高度就降到 $O(\\log n)$，勢能大幅下降，付掉了這次昂貴的操作。\n- **拆成兩次 zig（自底向上逐次旋轉）**：這等價於「把 $x$ 一路提到根」，**其餘節點的相對深度幾乎不變**——原來深度 $d$ 的節點變成深度 $d-1$ 或 $d$。樹仍然是一條長鏈。於是**下一次對新的最深節點 splay 又要 $\\Theta(n)$**，反覆進行，總成本 $\\Theta(mn)$。\n\n**形式上**：定理 3 的 zig-zig 分析靠 $s(x) + s'(g) \\le s'(x)$ 得到 $-2$ 的常數，正是「摺疊」帶來的勢能下降；兩次 zig 得不到這個不等式 ✔。$\\square$\n\n**這是伸展樹最違反直覺的地方**：一個看起來只是「換個旋轉順序」的細節，決定了 $O(\\log n)$ 與 $O(n)$ 的差別。**寫的時候務必照著三種情形分類，不要「簡化」。**\n\n### 定理 5（區間提取：把 $[l, r]$ 變成一棵子樹）\n\n*操作*：`split(l, r)`——\n1. 把**中序第 $l-1$ 個**節點（即哨兵偏移後的「$l$ 左邊那一個」）splay 到根；\n2. 把**中序第 $r+1$ 個**節點 splay 到「根的子節點」位置（`splay(y, root)`）。\n\n**命題**：此時 `t[t[root].ch[1]].ch[0]` 這棵子樹的中序序列，**恰好是原序列的 $[l, r]$**。\n\n*證明*。設 $u$ = 第 $l-1$ 個、$v$ = 第 $r+1$ 個。步驟 1 後 $u$ 是根，由 BST（此處指「中序 = 序列」）性質，**所有中序位置 $> l-1$ 的節點都在 $u$ 的右子樹**。步驟 2 把 $v$ splay 到 $u$ 的右子（`splay(v, u)` 保證 $v$ 是 $u$ 的直接子節點，且仍在右側，因為 $v$ 的中序位置 $> u$）。於是 $v$ 的左子樹 = 「中序位置嚴格介於 $u$ 與 $v$ 之間」的所有節點 = 位置 $l$ 到 $r$ ✔。$\\square$\n\n**兩個哨兵是必要的，而且是這個定理的前提**：查 $[1, n]$ 時 $u$ 是「第 0 個」、$v$ 是「第 $n+1$ 個」，**若沒有哨兵，這兩個節點不存在，整個提取就失效。** 加了哨兵之後，`split` 對所有 $1 \\le l \\le r \\le n$ 一律成立，**不需要任何特判**——這正是哨兵的價值。\n\n**推論 5.1（提取之後就是線段樹）**：拿到子樹根 $z$ 後，\n- **區間反轉** = `t[z].rev ^= 1`；\n- **區間聚合** = 讀 $z$ 上維護的欄位；\n- **區間刪除** = `t[v].ch[0] = 0; pushUp(v); pushUp(u);`；\n- **區間插入** = 建好一棵樹掛到 `t[v].ch[0]`。\n\n**全部 $O(1)$（不計 splay 的攤還成本）。這就是「序列平衡樹」的全部威力。**\n\n### 定理 6（反轉標記的合法性）\n\n*命題*：對一棵子樹打 `rev` 標記，等價於把它的中序序列整段反轉；且 `pushDown` 的「交換左右子 + 標記下傳」是正確的實作。\n\n*證明*。設子樹根為 $z$，中序序列為 $L, z, R$（$L$、$R$ 分別是左右子樹的中序序列）。反轉後應為 $\\mathrm{rev}(R),\\, z,\\, \\mathrm{rev}(L)$。\n\n而 `pushDown` 做的事是：把左右子**交換**（於是中序變成 $R, z, L$），再對兩棵子樹各打一個 `rev`（於是變成 $\\mathrm{rev}(R), z, \\mathrm{rev}(L)$）✔ —— **兩者一致。** $\\square$\n\n**這是 9.4 那個「分配律」條件的具體實例**：$\\mathrm{rev}$ 對序列拼接滿足 $\\mathrm{rev}(A \\cdot B) = \\mathrm{rev}(B)\\cdot\\mathrm{rev}(A)$。**注意它是反序的**——所以 `pushDown` 必須交換左右子，只下傳標記是不夠的。\n\n**標記合成**：`rev ^= 1`，兩次反轉抵消 ✔（$\\mathbb{Z}_2$，與 8.6 的 $\\mathbb{Z}_2$ 帶權並查集是同一個群）。\n\n**警告**：若子樹上還維護了**不對稱**的聚合（例如「最大前綴和」`pre` 與「最大後綴和」`suf`，見 9.3 定理 3），**`pushDown` 交換左右子時必須同時交換 `pre` 與 `suf`**。忘了這件事，反轉之後聚合值就悄悄錯了。\n\n## 逐步演算法\n\n伸展樹題目可拆成下面幾步，逐一搭配程式碼：\n\n:::example pushUp：用兒子的 size 重算自己的 size\n**1. 維護 size：每次改動後往上更新。** 任何旋轉或接子樹後，父節點的 `size` 等於左右子樹大小加自己。\n\n```cpp\n// pushUp：用兒子的 size 重算自己的 size\nvoid pushUp(int x) { sz[x] = sz[ch[x][0]] + sz[ch[x][1]] + 1; }\n```\n:::\n\n\n\n:::example splay：把 x 一路旋轉到根\n**2. splay：把目標節點轉到根（或某個指定父親之下）。** 反覆旋轉直到 x 成為根。\n\n```cpp\n// splay：把 x 一路旋轉到根\nvoid splay(int x) {\n    while (fa[x]) {\n        // （標準寫法會看祖父決定先轉誰，這裡簡化）\n        rotate(x);\n    }\n    root = x;\n}\n```\n:::\n\n\n\n:::example 找中序第 k 個節點（記得邊走邊 pushDown 懶標記）\n**3. 用「第 k 名」定位節點。** 靠 `size` 往左或往右走：左子樹大小決定第 k 個在哪一邊。\n\n```cpp\n// 找中序第 k 個節點（記得邊走邊 pushDown 懶標記）\nint kth(int x, int k) {\n    pushDown(x);\n    int leftSize = sz[ch[x][0]];\n    if (k <= leftSize) {\n        return kth(ch[x][0], k);\n    }\n    if (k == leftSize + 1) {\n        return x;\n    }\n    return kth(ch[x][1], k - leftSize - 1);\n}\n```\n:::\n\n\n\n:::example 反轉一段：對孤立出來的子樹打反轉標記（O(1)）\n**4. 區間操作：把區間「孤立」成一棵子樹。** 想對 `[l, r]` 做事，先把第 `l - 1` 個 splay 到根、第 `r + 1` 個 splay 到根的右兒子，此時根的右兒子的左子樹正好就是 `[l, r]`，對它整棵打標記即可。\n\n```cpp\n// 反轉一段：對孤立出來的子樹打反轉標記（O(1)）\nvoid applyReverse(int x) {\n    if (!x) {\n        return;\n    }\n    swap(ch[x][0], ch[x][1]);  // 交換左右兒子 = 反轉\n    rev[x] ^= 1;  // 標記留給子孫，之後 pushDown 才真正往下傳\n}\n```\n:::\n\n\n\n**5. 邊界與複雜度。**\n\n- 往下走之前一定要先 `pushDown` 把懶標記傳給兒子，否則會走錯方向。\n- 旋轉後 `fa`、`size` 都要同步更新，漏一個整棵樹就亂掉。\n- 複雜度：每個操作均攤 `O(log n)`，總共 m 次操作為 `O(m log n)`。\n\n## C++17 模板\n\n**完整的序列版伸展樹（支援：第 $k$ 個、任意位置插刪、區間反轉）：**\n\n```cpp\nstruct SplaySeq {\n    struct Node { int ch[2] = {0,0}, fa = 0, sz = 0, val = 0; bool rev = false; };\n    vector<Node> t;\n    int root = 0;\n\n    int newNode(int v) { t.push_back({{0,0}, 0, 1, v, false}); return int(t.size()) - 1; }\n    void pushUp(int x) { t[x].sz = t[t[x].ch[0]].sz + t[t[x].ch[1]].sz + 1; }\n    void pushDown(int x) {\n        if (!t[x].rev) return;\n        swap(t[x].ch[0], t[x].ch[1]);\n        for (int c : {t[x].ch[0], t[x].ch[1]}) if (c) t[c].rev = !t[c].rev;\n        t[x].rev = false;\n    }\n    int side(int x) const { return t[t[x].fa].ch[1] == x; }   // x 是父的哪一側\n\n    void rotate(int x) {\n        int p = t[x].fa, g = t[p].fa, k = side(x), c = t[x].ch[k ^ 1];\n        if (g) t[g].ch[side(p)] = x;\n        t[x].fa = g;\n        t[x].ch[k ^ 1] = p;  t[p].fa = x;\n        t[p].ch[k] = c;      if (c) t[c].fa = p;\n        pushUp(p); pushUp(x);                                  // 先 p 後 x（推論 1.2）\n    }\n    // 把 x 轉到 goal 的子節點；goal = 0 表示轉到根\n    void splay(int x, int goal = 0) {\n        while (t[x].fa != goal) {\n            int p = t[x].fa, g = t[p].fa;\n            if (g != goal) rotate(side(x) == side(p) ? p : x); // zig-zig 先轉 p（定理 4）\n            rotate(x);\n        }\n        if (goal == 0) root = x;\n    }\n    // 中序第 k 個（1-indexed，含哨兵）\n    int kth(int k) {\n        int x = root;\n        while (true) {\n            pushDown(x);                                       // 下降途中順手下傳（定理 2 前提）\n            int ls = t[t[x].ch[0]].sz;\n            if (k <= ls) x = t[x].ch[0];\n            else if (k == ls + 1) return x;\n            else { k -= ls + 1; x = t[x].ch[1]; }\n        }\n    }\n    // 提取原序列的 [l, r]（1-indexed，不含哨兵），回傳該子樹的根\n    int split(int l, int r) {\n        int u = kth(l), v = kth(r + 2);                        // 哨兵偏移：+0 與 +2\n        splay(u); splay(v, u);\n        return t[v].ch[0];\n    }\n\n    // 由陣列建樹（含頭尾哨兵），O(n)\n    int build(const vector<int>& a, int lo, int hi) {          // 建 a[lo..hi]\n        if (lo > hi) return 0;\n        int mid = lo + (hi - lo) / 2, x = newNode(a[mid]);\n        t[x].ch[0] = build(a, lo, mid - 1);\n        t[x].ch[1] = build(a, mid + 1, hi);\n        for (int c : {t[x].ch[0], t[x].ch[1]}) if (c) t[c].fa = x;\n        pushUp(x);\n        return x;\n    }\n    explicit SplaySeq(const vector<int>& a) {\n        t.reserve(a.size() + 4);\n        t.emplace_back();                                      // t[0] 空節點，sz = 0\n        vector<int> b;\n        b.reserve(a.size() + 2);\n        b.push_back(0);                                        // 前哨兵\n        b.insert(b.end(), a.begin(), a.end());\n        b.push_back(0);                                        // 後哨兵\n        root = build(b, 0, int(b.size()) - 1);\n    }\n\n    void reverse(int l, int r) { int z = split(l, r); t[z].rev = !t[z].rev; }\n    int at(int k) { int x = kth(k + 1); splay(x); return t[x].val; }\n    void insertAfter(int pos, int v) {                         // 在第 pos 個之後插入\n        int u = kth(pos + 1), w = kth(pos + 2);\n        splay(u); splay(w, u);\n        int x = newNode(v);\n        t[w].ch[0] = x; t[x].fa = w;\n        pushUp(w); pushUp(u);\n    }\n    void erase(int l, int r) {                                 // 刪除 [l, r]\n        split(l, r);\n        int v = t[root].ch[1];\n        t[v].ch[0] = 0;\n        pushUp(v); pushUp(root);\n    }\n};\n```\n\n**四個「照抄就對、改了就錯」的細節**：\n- `rotate` 裡 **`pushUp(p)` 在 `pushUp(x)` 之前**；\n- `splay` 裡 zig-zig 判斷用 `side(x) == side(p)` 並**先轉 `p`**；\n- `kth` **每一步都 `pushDown`**；\n- `split` 的下標偏移是 **`kth(l)` 與 `kth(r+2)`**（因為前哨兵佔了中序第 1 位，原序列第 $i$ 個是中序第 $i+1$ 個，所以「$l$ 的前一個」是中序第 $l$、「$r$ 的後一個」是中序第 $r+2$）。\n\n**`at()` 之後為什麼要 `splay(x)`？** 不 splay 也能得到正確答案，但**定理 3 的攤還保證就沒了**——勢能不會下降，樹會保持退化。**「訪問後必須 splay」是伸展樹的紀律，不是可選的優化。**\n\n## 時間與空間複雜度\n\n| 操作 | 複雜度 |\n| --- | --- |\n| `build`（由陣列建樹） | $O(n)$，樹高 $O(\\log n)$ |\n| `kth` / `at` / `insertAfter` / `erase` / `reverse` | **攤還** $O(\\log n)$ |\n| 單次操作的最壞情形 | $O(n)$ |\n| 空間 | $O(n)$ |\n\n**「攤還」三個字必須認真對待**（定理 3 的第三個前提）。$m$ 次操作總計 $O((n+m)\\log n)$，但**任何單次操作都可能是 $\\Theta(n)$**。互動式、有時限的線上系統要小心；競賽題只看總時間，所以無妨。\n\n**常數很大**：每次操作要走一遍路徑再轉一遍回來，`pushDown`／`pushUp` 反覆呼叫，指標跳躍對快取極不友善。**實測常常比同複雜度的線段樹慢 3–5 倍。**\n\n**各題的實際規模**：\n\n| 題目 | 規模 | 實際建議 |\n| --- | --- | --- |\n| 2296 設計文字編輯器 | $\\le 2\\times10^4$ 次呼叫 | **兩個堆疊，$O(1)$ 攤還**——見下節 |\n| 3526 | — | 先確認是否真的需要中間插刪 |\n\n**誠實的結論：本節的兩題都不需要伸展樹。** 學它是為了「序列平衡樹」這個想法與勢能法的範例；**真要在競賽中寫平衡樹，FHQ Treap 是更好的選擇**（見下節）。\n\n## 常見錯誤與邊界條件\n\n- **`fa` 沒更新完整**。`rotate` 要改動三處父指標（$x \\to g$、$p \\to x$、$c \\to p$），**加上 $g$ 的子指標**。漏任何一處，樹就斷了，而且症狀往往是無窮迴圈而非崩潰。\n- **`rotate` 裡 `pushUp` 順序寫反**（推論 1.2）。必須先 $p$ 後 $x$。\n- **zig-zig 拆成兩次 zig**（定理 4）。功能正確，複雜度退化到 $O(n)$ 攤還。**測資大才會暴露，最陰險的一種錯。**\n- **splay 前沒 `pushDown`**（定理 2 前提）。標記被旋轉搬到錯誤的子樹上，答案錯得毫無規律。**在 `kth` 的下降迴圈裡 `pushDown` 是最省事的寫法。**\n- **忘了哨兵，或哨兵偏移算錯**（定理 5）。`split(1, n)` 會直接越界。**寫個註解標明「原序列第 $i$ 個 = 中序第 $i+1$ 個」。**\n- **哨兵被算進聚合**。哨兵的 `val` 要設成單位元（求和設 0、求 max 設 $-\\infty$），否則區間聚合被污染。**本節模板的 `sz` 也包含哨兵，所以 `split` 依賴的是中序位置而非「有效元素個數」——兩者不要混用。**\n- **`reverse` 之後忘了交換不對稱的聚合欄位**（定理 6 的警告）。`pre`／`suf`、「最左的某某」這類欄位都要在 `pushDown` 交換左右子時一起換。\n- **訪問後不 splay**。攤還保證失效。\n- **`t.push_back` 之後持有舊參考**。與 9.6 同一個坑：`vector` 擴容會使參考失效。**全程用下標。**\n- **`erase` 之後節點沒回收**。競賽中通常不回收（空間 $O(\\text{總插入數})$ 即可）；但若插刪次數極多，要維護一個空閒節點池。\n- **遞迴 `build` 的深度**。$O(\\log n)$，安全；但若改成「逐個 `insert` 建樹」就是 $O(n\\log n)$ 且更慢。**能一次建好就一次建好。**\n\n## 與相似技巧的比較\n\n- **與 FHQ Treap（無旋 Treap）**：**同樣的能力，程式碼短一半，而且好懂得多。** 它用 `split`／`merge` 兩個函式取代 splay 的三種情形，不需要父指標，複雜度是**期望** $O(\\log n)$（隨機優先級）而非攤還。**唯一的取捨：FHQ 是遞迴的，常數略大；而伸展樹能做「可持久化以外」的一些花招（LCT 的基礎）。實務上先寫 FHQ Treap。**\n- **與 9.3／9.4 線段樹**：**線段樹的下標是固定的，伸展樹的位置是動態的。** 只要序列長度不變、不需要插刪與重排，**線段樹在每個維度都更好**：常數小、非攤還的 $O(\\log n)$、易寫易除錯。**看到「插入／刪除／反轉」才考慮平衡樹。**\n- **與 9.1 樹狀陣列 + 二分**：若「刪除」可以用「標記為已刪」代替（元素不真的消失，只是不計入），那麼「第 $k$ 個未刪元素」可以用樹狀陣列上二分 $O(\\log n)$ 解決（9.1 的 `kth`）。**這覆蓋了相當多看似需要平衡樹的題目，而且快得多。先問：真的需要插入嗎，還是只需要刪除？**\n- **與兩個堆疊（2296 的正解）**：文字編輯器的游標只會**左右移動一格**、在游標處插刪。用 `left`、`right` 兩個堆疊表示游標兩側的字元，「左移」= 從 `left` 彈出推入 `right`，插入 = 推入 `left`。**每次操作攤還 $O(1)$，程式十行。** **這是「認清問題的真正約束」勝過「搬出強大工具」的典型例子——游標局部移動這個限制，把平衡樹問題降級成了堆疊問題。**\n- **與 `std::rope`（GCC 的 `__gnu_cxx::rope`）**：內部就是平衡樹，支援 $O(\\log n)$ 的字串插刪與拼接。**能用就用，但 LeetCode 上不保證可用，且不支援自訂聚合。**\n- **與 8 節並查集的攤還分析**：兩者都用勢能法，但 DSU 的勢能是「秩」而伸展樹的是「$\\log$ 子樹大小」。**共同點是那個反覆出現的模式：昂貴的操作必然伴隨結構的不可逆改善，而改善的總量有上界。** 本章第八次見到它了。\n- **與 LCT（Link-Cut Tree）**：LCT 就是「一堆伸展樹 + 虛實邊」，用來維護動態樹。**伸展樹的攤還分析（定理 3）是 LCT 複雜度證明的基石**——這是學伸展樹最實在的長期回報。\n\n## 代表例題\n\n**題號**：2296、3526\n\n要把區間量改寫成前綴狀態之差；若是計數題，重點是查找過去需要的前綴值。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 2296 | [設計一個文字編輯器](https://leetcode.cn/problems/design-a-text-editor) | 1912 |\n| 3526 | [範圍異或查詢與子陣列反轉](https://leetcode.cn/problems/range-xor-queries-with-subarray-reversals)（Premium） |  |\n\n## 本節重點速查\n\n- **核心轉念**：**中序走訪序列 = 陣列。** 鍵不是值而是位置，位置由 `sz` 隱含決定。\n- **四個約定**：`t[0]` 是 `sz=0` 的哨兵；必須存 `fa`；頭尾各加一個哨兵節點（定理 5 的前提）；`rev` 的語義是「自己好了、子樹還沒」。\n- **定理 1**：旋轉保持中序序列 ⇒ splay 隨時可做。**`rotate` 裡先 `pushUp(p)` 再 `pushUp(x)`。**\n- **定理 3（Access Lemma）**：勢能 $\\Phi=\\sum\\log_2 s(x)$，一次 splay 攤還 $\\le 3(r(\\mathrm{root})-r(x))+1 \\le 3\\log_2 n + 1$。**引擎是 $\\log$ 的凹性**：zig-zig 由 $s(x)+s'(g)\\le s'(x)$ 擠出 $-2$。**這是攤還界，單次可以是 $\\Theta(n)$。**\n- **定理 4**：**zig-zig 必須先轉父再轉自己。** 拆成兩次 zig 會讓單鏈永遠是單鏈，退化成 $O(n)$ 攤還——功能正確、複雜度爆炸，最陰險的錯。\n- **定理 5（區間提取）**：`splay(第 l-1 個)` 到根、`splay(第 r+1 個)` 到根的子 ⇒ 後者的左子樹恰為 $[l,r]$。**哨兵讓這件事對所有 $l,r$ 無特判成立。** 提取後打標記、刪除、插入全是 $O(1)$。\n- **定理 6（反轉標記）**：$\\mathrm{rev}(A\\cdot B)=\\mathrm{rev}(B)\\cdot\\mathrm{rev}(A)$ ⇒ `pushDown` 必須**交換左右子**再下傳；標記在 $\\mathbb{Z}_2$ 上合成。**不對稱的聚合欄位（`pre`／`suf`）也要一起交換。**\n- **splay 前必須 `pushDown` 整條路徑；訪問後必須 splay**（否則攤還保證失效）。\n- **實務選擇**：不需插刪 ⇒ 線段樹；只需刪除 ⇒ 樹狀陣列上二分；真要平衡樹 ⇒ **FHQ Treap 更短更好懂**；2296 這種游標局部移動 ⇒ **兩個堆疊，$O(1)$**。**學伸展樹主要是為了 LCT 與這個勢能法範例。**\n",
        },
      ],
    },
    {
      id: 70,
      title: "11. 根號演算法",
      src: "https://leetcode.cn/circle/discuss/mOr1u6/",
      summary:
        "分塊、莫隊、根號分治：以 $O(n\\sqrt n)$ 或 $O((n+q)\\sqrt n)$ 換取較鬆的常數與思維難度。\n\n**辨識**：離線詢問、區間眾數、可加不可減資訊。",
      children: [
        {
          id: 71,
          title: "11.1 根號分解（Sqrt Decomposition）",
          summary:
            "## 這個技術解決什麼問題\n\n線段樹（9 節）很強，但它有一個硬性代價：**你維護的摘要必須可合併**——兩個子區間的摘要要能 $O(1)$ 併成父區間的摘要（9.3 要求么半群）。\n\n可是很多摘要根本不可合併：\n\n- 「區間內小於 $x$ 的元素有幾個？」——左右兩半各自的答案沒辦法合併成整體的答案；\n- 「區間內出現次數最多的元素是哪個？」——眾數不可合併（左半的眾數 + 右半的眾數 $\\ne$ 整體的眾數）；\n- 「區間內有幾個不同的值？」——同樣不可合併。\n\n**根號分解（分塊）用一句話繞開這個限制：**\n\n> **把序列切成大小約 $\\sqrt n$ 的塊。整塊有預先算好的摘要，零頭直接暴力。**\n\n於是一次區間操作只碰 $O(\\sqrt n)$ 個整塊與 $O(\\sqrt n)$ 個零頭元素，總計 $O(\\sqrt n)$。\n\n**它換到了什麼？** 關鍵洞察是：\n\n> **線段樹要求「摘要之間可合併」；分塊只要求「單塊的摘要能回答關於這一塊的問題」。** 摘要彼此不必有任何代數關係——**每塊各自排序、各自開一張頻次表都行。**\n\n**這是分塊真正的價值，也是它值得學的唯一理由。** 光是「區間和」，線段樹在每個維度都比分塊好。\n\n**辨識訊號**：\n- 同時有修改與區間查詢，但**要維護的摘要很怪，線段樹不好寫，塊內卻可以暴力**（每塊排序、每塊一張頻次表）；\n- $n$、$q$ 都在 $10^5$ 等級，$O(nq)$ 會超時而 $O((n+q)\\sqrt n)$（約 $3\\times10^7$）剛好能過；\n- 題目帶了一個**參數**，該參數大時暴力就快、小時可以預處理（1714 特殊等間距元素和——見定理 3）；\n- 需要區間操作又要查詢的怪組合（3655 區間乘法查詢後的異或 II、3400、LCP 16）。\n\n**「$O(\\sqrt n)$ 剛好能過」不是巧合**：出題者若想卡掉 $O(n\\log n)$ 又不想卡死暴力，$n = 10^5$ 這個規模正是分塊的舒適區。**看到 $n, q \\le 10^5$ 而題目摘要又很怪，就該想到分塊。**\n\n## 辨識題型的訊號\n\n看到下面這些特徵，可以往根號分解想：\n\n- **同時有「更新」和「區間查詢」，而且查詢的東西很普通（區間和、最大值、出現次數）。** 像「區間乘法查詢後的異或 II」（3655）這種帶區間操作又要查詢的題目。\n- **要維護的摘要比較怪，線段樹不好寫，但塊內可以暴力。** 例如每塊維護排序後陣列、或頻次表來回答「區間內小於 x 的個數」。\n- **資料規模 n、查詢數 m 都在十萬等級，`O(n·m)` 暴力會超時，但 `O((n+m)·sqrt(n))` 剛好過。**\n\n確認自己有沒有套對，問三個問題：\n\n1. 我每塊存的「摘要」是什麼？它能合併嗎？\n2. 單點 / 整塊更新時，我有沒有同步維護或重建摘要？\n3. 查詢時，我有沒有分清「整塊讀摘要」和「零頭暴力」兩段？\n\n## 核心想法與直覺\n\n本節要解決的場景是：有一個很長的陣列，會被反覆「更新某些值」和「查詢某一段的總和／最大值／某數出現幾次」。如果每次查詢都從頭掃到尾，一次 `O(n)`，查很多次就太慢。\n\n根號分解（Sqrt Decomposition）的想法很直覺：**把整排資料切成一塊一塊，每塊大約 `sqrt(n)` 個元素，並且替每塊先算好一份「摘要」**（例如這塊的總和、最大值、或一張數字出現次數表）。\n\n:::example 把陣列切塊：每塊大小 len ≈ sqrt(n)，預先算好每塊的總和\n為什麼切成 `sqrt(n)`？因為這樣「塊的數量」也大約是 `sqrt(n)` 個。查詢一段區間時：中間蓋滿的整塊直接拿摘要（最多 `sqrt(n)` 塊），左右兩端不滿一塊的零碎部分才逐一暴力掃（最多 `sqrt(n)` 個元素）。兩邊都是 `sqrt(n)` 等級，合起來每次查詢就是 `O(sqrt(n))`，比 `O(n)` 快得多。\n\n```cpp\n// 把陣列切塊：每塊大小 len ≈ sqrt(n)，預先算好每塊的總和\nint len = (int)sqrt(n) + 1;  // 塊大小\nvector<long long> blockSum(n / len + 1, 0);\nfor (int i = 0; i < n; ++i) {\n    blockSum[i / len] += a[i];  // i / len 就是 i 屬於第幾塊\n}\n```\n:::\n\n\n\n:::example 查詢區間總和 [l, r]：整塊讀摘要，邊界散塊逐個加\n關鍵就一句口訣：**整塊看摘要，零頭用暴力。**\n\n```cpp\n// 查詢區間總和 [l, r]：整塊讀摘要，邊界散塊逐個加\nlong long query(int l, int r) {\n    long long s = 0;\n    while (l <= r && l % len != 0) {\n        s += a[l++];  // 左邊零頭\n    }\n    while (l + len - 1 <= r) {\n        s += blockSum[l / len];\n        l += len;\n    }  // 整塊\n    while (l <= r) {\n        s += a[l++];  // 右邊零頭\n    }\n    return s;\n}\n```\n:::\n\n\n\n它沒有線段樹快（線段樹是 `O(log n)`），但好寫、好改，遇到「奇怪的摘要」（例如要維護每塊排序後的陣列、或頻次表）時特別靈活。\n\n## 狀態／資料結構定義\n\n**三個座標換算函式，整節都靠它們：**\n\n```cpp\nint s;                              // 塊長\nint nb;                             // 塊數 = (n + s - 1) / s\nint bel(int i) const { return i / s; }               // 元素 i 屬於哪一塊\nint lo(int b) const { return b * s; }                // 第 b 塊的左界（含）\nint hi(int b) const { return min(n, (b + 1) * s); }  // 第 b 塊的右界（不含）\n```\n\n**`hi` 必須夾住 `n`**——最後一塊通常不滿。**這是分塊最高頻的越界來源。**\n\n**每塊維護什麼，因題而異。** 三種最常見的形態：\n\n| 要回答的問題 | 每塊維護 | 整塊查詢 | 單點修改後的重建 |\n| --- | --- | --- | --- |\n| 區間和（帶區間加） | `sum[b]`、`add[b]` | $O(1)$ | $O(1)$ |\n| 區間內 $< x$ 的個數 | **塊內排序副本** `srt[b]` | $O(\\log s)$ 二分 | $O(s)$ 或 $O(\\log s)$（插入排序） |\n| 區間眾數／不同值個數 | **塊內頻次表** | $O(1)$ | $O(1)$ |\n\n**帶區間加時的核心不變量（定理 4）：**\n\n> **元素 $i$ 的真實值 $= \\mathit{raw}[i] + \\mathit{add}[\\mathit{bel}(i)]$。**\n\n**整塊修改只動 `add[b]`（$O(1)$）；零頭修改直接動 `raw[i]`（不碰 `add`）。** 這樣就永遠不需要「下傳標記」——**分塊沒有 `pushDown`，因為標記只有一層。**\n\n## 不變量或正確性證明\n\n以下記序列長度 $n$、塊長 $s$、塊數 $B = \\lceil n/s\\rceil$。\n\n### 定理 1（塊長的最優選擇：$\\sqrt n$ 只是特例）\n\n*設定*：設單次操作的代價為\n\n$$T(s) \\;=\\; a\\cdot\\frac{n}{s} \\;+\\; b\\cdot s,$$\n\n其中 $a$ 是「處理一個整塊」的代價、$b$ 是「處理一個零頭元素」的代價。\n\n*命題*：$T$ 在 $s^\\star = \\sqrt{an/b}$ 取最小值 $2\\sqrt{abn}$。\n\n*證明*。由 AM–GM，$\\frac{an}{s} + bs \\ge 2\\sqrt{\\frac{an}{s}\\cdot bs} = 2\\sqrt{abn}$，等號成立當且僅當 $\\frac{an}{s} = bs$，即 $s = \\sqrt{an/b}$ ✔。$\\square$\n\n**$a = b$ 時才得到 $s = \\sqrt n$。而 $a \\ne b$ 的情形非常常見**：\n\n**情形一：修改與查詢的代價不對稱。** 設有 $q_u$ 次修改（每次 $O(s)$，例如塊內重新排序）與 $q_v$ 次查詢（每次 $O(n/s)$）。總代價\n\n$$q_u \\cdot s + q_v \\cdot \\frac{n}{s} \\quad\\Longrightarrow\\quad s^\\star = \\sqrt{\\frac{q_v\\, n}{q_u}}.$$\n\n**修改多、查詢少 ⇒ 塊要小；查詢多、修改少 ⇒ 塊要大。** 盲目寫 `s = sqrt(n)` 在極端比例下會慢好幾倍。\n\n**情形二：整塊操作帶 $\\log$。** 塊內排序後二分是 $O(\\log s)$，於是 $T(s) = \\frac{n}{s}\\log s + s$，最優塊長略大於 $\\sqrt n$（約 $\\sqrt{n\\log n}$）。\n\n**實務建議**：`s = max(1, (int)sqrt(n))` 起手，**若 TLE 就把 `s` 當成可調參數試 $\\sqrt{n}/2$、$2\\sqrt n$**。這不是玄學——定理 1 說明了 $T(s)$ 在最小值附近是平緩的（$T(2s^\\star) = 1.25\\,T(s^\\star)$），所以粗調就夠，但差一個數量級就會有感。\n\n### 定理 2（區間分解：與線段樹的對照）\n\n*命題*：任意區間 $[l, r]$ 可**不重不漏**地分解成\n- 至多 2 段零頭（$l$ 所在塊的一部分、$r$ 所在塊的一部分），共 $< 2s$ 個元素；\n- 至多 $B$ 個完整塊。\n\n*證明*。設 $b_l = \\mathit{bel}(l)$、$b_r = \\mathit{bel}(r)$。\n- 若 $b_l = b_r$：整段落在同一塊內，$< s$ 個元素，**必須特判為單一段暴力** ✔（這是分塊最經典的邊界，見「常見錯誤」）。\n- 否則：$[l,\\,\\mathit{hi}(b_l))$ 是左零頭、$[\\mathit{lo}(b_r),\\, r]$ 是右零頭、中間的塊 $b_l+1 \\dots b_r-1$ 是完整塊。三部分兩兩不交、聯集為 $[l,r]$ ✔。$\\square$\n\n**與 9.3 定理 2 的對照，是理解分塊的關鍵：**\n\n| | 段數 | 每段的摘要要求 |\n| --- | --- | --- |\n| 線段樹 | $O(\\log n)$ | **必須可合併**（么半群） |\n| 分塊 | $O(\\sqrt n)$ | **只要能回答關於自己的子問題** |\n\n**分塊用更多的段數，換來對摘要形態的完全自由。** 這個交換在「摘要可合併」時是純虧本，在「摘要不可合併」時是唯一出路。**每次考慮分塊，先問自己：我的摘要真的不可合併嗎？**\n\n### 定理 3（1714：對「參數」分塊，而非對陣列分塊）\n\n*問題*：給定陣列 $\\mathit{nums}$（長度 $n$）與 $q$ 個查詢 $(x, y)$，求 $\\mathit{nums}[x] + \\mathit{nums}[x+y] + \\mathit{nums}[x+2y] + \\cdots$（下標 $< n$），對 $10^9+7$ 取模。\n\n*天真做法*：每個查詢 $O(n/y)$。**$y = 1$ 時是 $O(n)$，$q$ 個這種查詢就是 $O(nq) = 10^{10}$。**\n\n**根號的想法**：**分界點放在 $y$ 上，而不是放在陣列上。**\n\n- **若 $y \\ge \\sqrt n$**：直接暴力，迴圈次數 $\\le n/y \\le \\sqrt n$ ✔。\n- **若 $y < \\sqrt n$**：預處理 $f[y][x] = \\mathit{nums}[x] + f[y][x+y]$（由 $x$ 從大到小遞推），查詢 $O(1)$。\n\n*複雜度*。預處理只對 $y = 1 \\dots \\lfloor\\sqrt n\\rfloor - 1$ 做，每個 $y$ 花 $O(n)$，共 $O(n\\sqrt n)$ 時間與空間。查詢共 $O(q\\sqrt n)$。**總計 $O((n+q)\\sqrt n)$** ✔。\n\n*為什麼分界點在 $\\sqrt n$*：暴力側的代價是 $n/y$（$y$ 越大越便宜），預處理側的代價是 $y_{\\max}\\cdot n$（$y_{\\max}$ 越小越便宜）。令 $n/y_{\\max} = y_{\\max}$ 得 $y_{\\max} = \\sqrt n$ ✔——**這就是定理 1 的同一個平衡，只是套在不同的變數上。**\n\n**這個形態值得單獨記住**：\n\n> **「小的情形預處理，大的情形暴力，分界點取在兩側代價相等處。」**\n\n**它不需要「塊」這個概念，只需要一個參數與一個單調的取捨。** 調和級數型的問題（枚舉倍數）、「出現次數 $> \\sqrt n$ 的元素至多 $\\sqrt n$ 種」（重元素／輕元素分治）都屬於這一族。\n\n**空間要小心**：$f$ 是 $\\sqrt n \\times n$ 的表，$n = 5\\times10^4$ 時約 $223 \\times 5\\times10^4 \\approx 1.1\\times10^7$ 個 `int` = **45 MB**。**這是 1714 的真正瓶頸，不是時間。** 可以只對 $y < \\sqrt{n}/2$ 預處理來換空間（定理 1 說代價只上升常數倍）。\n\n### 定理 4（區間加、區間求和的不變量）\n\n**不變量 (R)**：*對每個 $i$，元素的真實值為 $\\mathit{raw}[i] + \\mathit{add}[\\mathit{bel}(i)]$；且對每一塊 $b$，$\\mathit{sum}[b] = \\sum_{i \\in b} \\mathit{raw}[i]$。*\n\n（注意 $\\mathit{sum}[b]$ **不含** `add`——真實塊和是 $\\mathit{sum}[b] + \\mathit{add}[b]\\cdot|b|$。這個選擇讓零頭修改不必碰 `add`。）\n\n*維護*。區間加 $(\\,[l,r],\\,v\\,)$：\n- **整塊 $b$**：`add[b] += v`。$\\mathit{raw}$ 不變 ⇒ (R) 的第二式成立 ✔；每個 $i \\in b$ 的真實值都增加了 $v$ ✔。\n- **零頭 $i$**：`raw[i] += v; sum[bel(i)] += v;`。第二式同步更新 ✔；真實值 $= \\mathit{raw}[i]+v+\\mathit{add}[\\cdot]$，正確增加 $v$ ✔。**注意這裡沒有動 `add[bel(i)]`，所以同塊其他元素不受影響** ✔。\n\n*區間和查詢*：整塊貢獻 $\\mathit{sum}[b] + \\mathit{add}[b]\\cdot(\\mathit{hi}(b)-\\mathit{lo}(b))$，零頭貢獻 $\\mathit{raw}[i]+\\mathit{add}[\\mathit{bel}(i)]$。由 (R) 與定理 2 的不重不漏，總和正確 ✔。$\\square$\n\n**「標記只有一層，所以不需要 `pushDown`」是分塊相對線段樹最舒服的地方。** 9.4 的分配律、標記合成、下傳順序在這裡全部消失——**代價只是把 $\\log n$ 換成 $\\sqrt n$。** 寫錯機率大幅下降。\n\n**推論 4.1**：這個不變量對任何「整塊 $O(1)$ 可套用、零頭 $O(1)$ 可直接改」的標記都成立（區間加、區間賦值、區間乘）。**區間賦值時要小心**：`assign[b]` 生效後，塊內 `raw` 的個別值就失去意義了，讀零頭前必須先把 `assign[b]` **實際展開**到 `raw`（$O(s)$）——**這是分塊版的 `pushDown`，而且因為只有一層，它一定終止。**\n\n### 定理 5（塊內排序：不可合併摘要的典型）\n\n*問題*：帶單點修改，查詢「區間 $[l,r]$ 內 $< x$ 的元素個數」。\n\n*結構*：每塊額外維護一份**排序後的副本** `srt[b]`。\n\n*查詢*：整塊用 `lower_bound(srt[b], x)` 得 $O(\\log s)$；零頭逐個比較 $O(s)$。總計 $O\\big(\\frac{n}{s}\\log s + s\\big)$。\n\n*修改* $a[i] \\gets v$：塊 $\\mathit{bel}(i)$ 的排序副本要更新。$O(s)$ 重建，或用「刪一個插一個」的 $O(s)$ 搬移（`std::rotate`，常數極小）。\n\n*正確性*：由定理 2，$[l,r]$ 被不重不漏地覆蓋；每塊內的計數與零頭的計數相加即為總數 ✔——**這裡完全沒有「合併兩個答案」這件事，只有「把互不相交的計數加起來」。** 這正是分塊能做而線段樹難做的原因：**計數本身是可加的，但「小於 $x$ 的個數」這個摘要在合併時需要知道 $x$，而 $x$ 是查詢時才給的。**\n\n**這句話值得展開**：線段樹**也**能做這題——每個節點掛一個排序陣列（歸併樹），或用 9.6 的主席樹。**但歸併樹的空間是 $O(n\\log n)$、不支援修改；主席樹也不支援修改。分塊是三者中唯一支援修改的。**\n\n*塊長選擇*：$T(s) = \\frac{n}{s}\\log s + s$（查詢）與 $s$（修改）。**若修改遠多於查詢，就把 $s$ 調小**（定理 1 情形一）。\n\n### 定理 6（定期重建：讓「插入」也能分塊）\n\n*問題*：分塊假設下標固定，插入會讓後面所有元素移位。\n\n*做法*：改用 `vector<vector<int>> blocks`——每塊是一個獨立的 `vector`。插入時找到目標塊，在塊內 `insert`（$O(s)$）；**當某塊的大小超過 $2s$ 時，把它裂成兩塊**；**每進行 $\\Theta(s)$ 次操作就全域重建一次**（把所有塊攤平再重新均分，$O(n)$）。\n\n*攤還分析*：全域重建每 $\\Theta(s)$ 次操作發生一次，攤還成本 $O(n/s)$；單次插入 $O(s)$（塊內搬移）加上找塊的 $O(n/s)$。取 $s = \\sqrt n$ 得**每次操作攤還 $O(\\sqrt n)$** ✔。\n\n**這與 10.1 的平衡樹解決同一個問題**（序列的中間插刪），但**分塊版好寫十倍、常數小、且天然支援「塊內任意暴力」**。代價是 $O(\\sqrt n)$ 而非 $O(\\log n)$。**$n \\le 10^5$ 時這個交換通常划算。**\n\n**「定期重建」是根號思想的第三種形態**（前兩種是「對陣列分塊」與「對參數分塊」）：**把昂貴的整理工作攤到 $\\sqrt n$ 次操作上。** 它與 4.7 單調堆疊、6.6 懶刪除堆、8.5 區間並查集的攤還是同一個家族——**昂貴的動作對應一個有上界的總量。本章第九次。**\n\n## 逐步演算法\n\n根號分解題目可拆成下面幾步，逐一搭配程式碼：\n\n:::example 狀態：a 是真值，blockSum 是每塊摘要（可換成 max、頻次表…）\n**1. 狀態表示：原陣列 + 每塊摘要。** 原陣列存單點真實值，另開一份每塊的摘要。\n\n```cpp\n// 狀態：a 是真值，blockSum 是每塊摘要（可換成 max、頻次表…）\nvector<int> a;               // 原陣列，單點真實值\nvector<long long> blockSum;  // 每塊的摘要\n```\n:::\n\n\n\n:::example 單點更新 a[i] = v：先扣舊值再加新值維護摘要\n**2. 單點更新：改真值，同步修這塊的摘要。** 因為只動一個值，只要修它所在那一塊的摘要即可。\n\n```cpp\n// 單點更新 a[i] = v：先扣舊值再加新值維護摘要\nvoid update(int i, int v) {\n    blockSum[i / len] += v - a[i];  // 摘要同步調整\n    a[i] = v;                       // 再改真值\n}\n```\n:::\n\n\n\n**3. 區間查詢：整塊讀摘要、零頭暴力。** 就是上面那段 `query`：兩端不滿一塊的逐個處理，中間整塊直接取摘要。\n\n:::example 重建第 b 塊的摘要（散塊改過值後一定要呼叫）\n**4. 區間更新就用「整塊重建」。** 若更新涉及一整塊，散塊逐元素改完後，把受影響的塊摘要重新算一遍。\n\n```cpp\n// 重建第 b 塊的摘要（散塊改過值後一定要呼叫）\nvoid rebuild(int b) {\n    blockSum[b] = 0;\n    for (int i = b * len; i < min(n, (b + 1) * len); ++i) {\n        blockSum[b] += a[i];\n    }\n}\n```\n:::\n\n\n\n**5. 邊界與複雜度。**\n\n- 塊大小可依操作種類微調，但 `sqrt(n)` 是安全預設。\n- 散塊改值後**忘記重建該塊摘要**，後續查詢就會錯，這是最常見的 bug。\n- 複雜度：每次更新與查詢都是 `O(sqrt(n))`，m 次操作共 `O((n + m) sqrt(n))`。\n\n## C++17 模板\n\n**模板一：區間加、區間求和（定理 4 的不變量）**\n\n```cpp\nstruct BlockSum {\n    int n, s, nb;\n    vector<long long> raw, sum, add;\n    explicit BlockSum(const vector<int>& a) : n(int(a.size())) {\n        s  = max(1, int(sqrt(double(n))));\n        nb = (n + s - 1) / s;\n        raw.assign(a.begin(), a.end());\n        sum.assign(nb, 0);\n        add.assign(nb, 0);\n        for (int i = 0; i < n; i++) sum[i / s] += raw[i];\n    }\n    int lo(int b) const { return b * s; }\n    int hi(int b) const { return min(n, (b + 1) * s); }\n\n    void update(int l, int r, long long v) {            // [l, r] 皆含\n        int bl = l / s, br = r / s;\n        if (bl == br) {                                  // 同塊：全部當零頭（定理 2）\n            for (int i = l; i <= r; i++) raw[i] += v;\n            sum[bl] += v * (r - l + 1);\n            return;\n        }\n        for (int i = l; i < hi(bl); i++) raw[i] += v;\n        sum[bl] += v * (hi(bl) - l);\n        for (int i = lo(br); i <= r; i++) raw[i] += v;\n        sum[br] += v * (r - lo(br) + 1);\n        for (int b = bl + 1; b < br; b++) add[b] += v;   // 整塊：O(1)\n    }\n    long long query(int l, int r) const {\n        int bl = l / s, br = r / s;\n        long long res = 0;\n        if (bl == br) {\n            for (int i = l; i <= r; i++) res += raw[i];\n            return res + add[bl] * (r - l + 1);\n        }\n        for (int i = l; i < hi(bl); i++) res += raw[i];\n        res += add[bl] * (hi(bl) - l);\n        for (int i = lo(br); i <= r; i++) res += raw[i];\n        res += add[br] * (r - lo(br) + 1);\n        for (int b = bl + 1; b < br; b++) res += sum[b] + add[b] * (hi(b) - lo(b));\n        return res;\n    }\n};\n```\n\n**`bl == br` 的特判不可省。** 不特判的話，左零頭會跑到 `hi(bl)`（超過 $r$），右零頭又從 `lo(br)` 開始（小於 $l$）——**同一批元素被算兩次，而且還多算了區間外的。**\n\n**模板二：塊內排序，查區間內 $< x$ 的個數（定理 5）**\n\n```cpp\nstruct BlockSorted {\n    int n, s, nb;\n    vector<int> a;\n    vector<vector<int>> srt;\n    explicit BlockSorted(vector<int> a_) : n(int(a_.size())), a(move(a_)) {\n        s  = max(1, int(sqrt(double(n))));\n        nb = (n + s - 1) / s;\n        srt.resize(nb);\n        for (int b = 0; b < nb; b++) {\n            srt[b].assign(a.begin() + b * s, a.begin() + min(n, (b + 1) * s));\n            sort(srt[b].begin(), srt[b].end());\n        }\n    }\n    void set(int i, int v) {                             // 單點賦值，O(s)\n        int b = i / s;\n        auto& v2 = srt[b];\n        v2.erase(lower_bound(v2.begin(), v2.end(), a[i]));\n        v2.insert(lower_bound(v2.begin(), v2.end(), v), v);\n        a[i] = v;\n    }\n    int countLess(int l, int r, int x) const {\n        int bl = l / s, br = r / s, res = 0;\n        auto brute = [&](int lo2, int hi2) {\n            for (int i = lo2; i <= hi2; i++) res += (a[i] < x);\n        };\n        if (bl == br) { brute(l, r); return res; }\n        brute(l, min(n, (bl + 1) * s) - 1);\n        brute(br * s, r);\n        for (int b = bl + 1; b < br; b++)\n            res += int(lower_bound(srt[b].begin(), srt[b].end(), x) - srt[b].begin());\n        return res;\n    }\n};\n```\n\n**`set` 用 `erase` + `insert` 而非重新 `sort`**：兩者都是 $O(s)$，但前者只做記憶體搬移，常數小一個數量級。\n\n**模板三：對參數分塊（1714，定理 3）**\n\n```cpp\nclass Solution {\npublic:\n    vector<int> solve(vector<int>& nums, vector<vector<int>>& queries) {\n        const long long MOD = 1'000'000'007;\n        int n = int(nums.size());\n        int S = max(1, int(sqrt(double(n))));            // 分界點\n        // f[y][x] = nums[x] + nums[x+y] + ...，只對 y < S 預處理\n        vector<vector<int>> f(S, vector<int>(n + 1, 0));\n        for (int y = 1; y < S; y++)\n            for (int x = n - 1; x >= 0; x--)\n                f[y][x] = int((nums[x] + (x + y < n ? f[y][x + y] : 0)) % MOD);\n        vector<int> ans;\n        ans.reserve(queries.size());\n        for (auto& q : queries) {\n            int x = q[0], y = q[1];\n            if (y < S) { ans.push_back(f[y][x]); continue; }\n            long long t = 0;\n            for (int i = x; i < n; i += y) t += nums[i];  // 至多 n/y <= S 次\n            ans.push_back(int(t % MOD));\n        }\n        return ans;\n    }\n};\n```\n\n**`f` 的第一維是 `S` 而非 `n`——這是空間能否過關的關鍵**（定理 3 的空間試算）。\n\n## 時間與空間複雜度\n\n令塊長 $s$、塊數 $B = \\lceil n/s\\rceil$。\n\n| 操作 | 一般式 | 取 $s=\\sqrt n$ |\n| --- | --- | --- |\n| 區間加 / 區間和 | $O(n/s + s)$ | $O(\\sqrt n)$ |\n| 區間內 $<x$ 的個數 | $O\\big(\\frac{n}{s}\\log s + s\\big)$ | $O(\\sqrt n\\log n)$ |\n| 單點修改（帶塊內排序） | $O(s)$ | $O(\\sqrt n)$ |\n| 帶插入的序列（定理 6） | 攤還 $O(n/s + s)$ | 攤還 $O(\\sqrt n)$ |\n| 建表 | $O(n)$ 或 $O(n\\log s)$（要排序） | — |\n| 空間 | $O(n + B)$ | $O(n)$ |\n\n**$n = q = 10^5$ 時 $O((n+q)\\sqrt n) \\approx 6.3\\times10^7$ 次基本運算**——在 1 秒的限制內，只要常數小（連續記憶體、無指標跳躍）就能過。**分塊的常數通常比線段樹小，這部分抵消了複雜度的劣勢。**\n\n**與線段樹的實際交叉點**：$\\sqrt n$ vs $\\log n$ 在 $n = 10^5$ 是 316 vs 17，**理論上差 18 倍，實測常常只差 3–5 倍**（快取友善 + 無遞迴）。**但 $n$ 上到 $10^6$ 就是 1000 vs 20，分塊必敗。**\n\n**1714 的空間**（定理 3）：$S \\times n$ 個 `int`，$n = 5\\times10^4$ ⇒ 約 45 MB。**這題的瓶頸在空間，不在時間。**\n\n## 常見錯誤與邊界條件\n\n- **忘了 `bl == br` 的特判**（定理 2）。這是分塊的頭號錯誤：左右零頭會重疊並越界。**每個分塊函式的第一行就寫這個特判。**\n- **`hi(b)` 沒有夾住 `n`**。最後一塊不滿，`(b+1)*s` 會超過 `n`。**寫成 `min(n, (b+1)*s)`。**\n- **`s = 0`**。$n = 0$ 或 $n$ 很小時 `sqrt(n)` 可能是 0，除以 0 直接崩潰。**一律 `s = max(1, ...)`。**\n- **塊數算成 `n / s`**。必須是 `(n + s - 1) / s`，否則最後一塊被漏掉。\n- **`sum[b]` 的語義沒定清楚**（定理 4）。本節約定 $\\mathit{sum}[b]$ **不含** `add`；若改成「含」，則零頭修改也要動 `add`，兩種寫法不能混。**在結構體上方寫一行註解說明語義。**\n- **區間賦值忘了先展開標記**（推論 4.1）。`assign[b]` 生效後直接讀 `raw[i]` 會讀到過期的值。\n- **修改後忘了維護塊內摘要**。改 `a[i]` 卻沒更新 `srt[bel(i)]` 或頻次表，之後所有整塊查詢都錯。**把「改元素」封裝成唯一入口，不要讓外部直接寫 `a[i]`。**\n- **盲目用 `s = sqrt(n)`**（定理 1）。修改與查詢代價不對稱時最優塊長不是 $\\sqrt n$。**TLE 時第一件事就是調 `s`。**\n- **`long long` 溢位**。$n = 10^5$、值域 $10^9$、區間加 $10^5$ 次 ⇒ 塊和可達 $10^{19}$，**`long long` 也可能不夠**，必要時取模或用 `__int128`。\n- **1714 的預處理表開成 `n × n`**。空間直接爆。**第一維必須是 $\\sqrt n$。**\n- **對參數分塊時分界點兩側漏掉一個**（定理 3）。若預處理 $y < S$、暴力 $y \\ge S$，兩者必須恰好覆蓋所有 $y \\ge 1$。**寫成 `if (y < S) 查表; else 暴力;`，不要用 `<=` 與 `>` 這種容易寫錯的組合。**\n\n## 與相似技巧的比較\n\n- **與 9.3／9.4 線段樹**：**摘要可合併就用線段樹**——$O(\\log n)$ 完勝 $O(\\sqrt n)$，而且空間更小。**分塊只在摘要不可合併時才有存在意義**（定理 2 的對照表）。實務上的判準：如果你能寫出 `Info operator+(Info, Info)`，就別用分塊。\n- **與 11.2 莫隊**：兩者都是根號，但**莫隊要求離線**、代價是 $O((n+q)\\sqrt n)$ 的**總**時間；分塊支援線上、每次操作 $O(\\sqrt n)$。**莫隊處理「大量區間查詢、答案靠增量維護」；分塊處理「線上、摘要不可合併」。** 莫隊的排序本身也用到分塊（按左端點所在塊排序）——**它是分塊思想的另一種應用，不是替代品。**\n- **與 9.7 ST 表**：ST 表 $O(1)$ 查詢但完全不能改；分塊 $O(\\sqrt n)$ 查詢但能改，且摘要形態自由。**兩者的定位幾乎不重疊。**\n- **與 9.1 樹狀陣列**：樹狀陣列做「區間加、區間和」是 $O(\\log n)$ 且程式極短（9.1 的兩個 BIT）。**單論這個功能，分塊沒有任何優勢。** 本節模板一的價值在於它是後續複雜摘要的骨架。\n- **與 Segment Tree Beats**：處理「區間 chmin／chmax + 區間和」這種標記不滿足分配律的問題。**它是線段樹的擴展，複雜度 $O(n\\log^2 n)$；分塊做同樣的事是 $O(n\\sqrt n)$ 但好寫得多。** 面試或時間緊迫時分塊是務實選擇。\n- **與 10.1 伸展樹**：處理「序列中間插刪」時，**定理 6 的定期重建分塊 $O(\\sqrt n)$ 攤還，比平衡樹的 $O(\\log n)$ 慢但好寫十倍。** $n \\le 10^5$ 時先寫分塊。\n- **與「重元素／輕元素」分治**：「出現次數 $> \\sqrt n$ 的元素至多 $\\sqrt n$ 種」——對重元素各自暴力掃一遍（$\\sqrt n$ 種 $\\times$ $O(n)$），對輕元素用另一套方法。**這是定理 3 的同一個平衡，套在「出現次數」這個參數上。**\n- **與調和級數枚舉**：$\\sum_{d=1}^{n} n/d = O(n\\log n)$ 這類枚舉倍數的技巧，本質上也是「小的多做、大的少做」。**根號分解是同一種思維，只是分界點取在 $\\sqrt n$ 而非讓兩側自然平衡。**\n\n## 代表例題\n\n**題號**：LCP 16、1714、3400、3655\n\n要把區間量改寫成前綴狀態之差；若是計數題，重點是查找過去需要的前綴值。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| LCP 16 | [遊樂園的遊覽計劃](https://leetcode.cn/problems/you-le-yuan-de-you-lan-ji-hua)（Premium） |  |\n| 1714 | [陣列中特殊等間距元素的和](https://leetcode.cn/problems/sum-of-special-evenly-spaced-elements-in-array)（Premium） |  |\n| 3400 | [右移後的最大匹配索引數](https://leetcode.cn/problems/maximum-number-of-matching-indices-after-right-shifts)（Premium） |  |\n| 3655 | [區間乘法查詢後的異或 II](https://leetcode.cn/problems/xor-after-range-multiplication-queries-ii) | 2454 |\n\n## 本節重點速查\n\n- **存在理由**：**線段樹要求摘要可合併，分塊不要求。** 每塊可以各自排序、各自開頻次表——**摘要形態完全自由，代價是 $O(\\log n) \\to O(\\sqrt n)$。** 能寫出 `Info operator+` 就別用分塊。\n- **三個座標函式**：`bel(i) = i/s`、`lo(b) = b*s`、**`hi(b) = min(n, (b+1)*s)`（必須夾 `n`）**。塊數 `(n+s-1)/s`，`s = max(1, sqrt(n))`。\n- **定理 1（塊長）**：$T(s)=a\\frac{n}{s}+bs$ 在 $s^\\star=\\sqrt{an/b}$ 最小。**$\\sqrt n$ 只是 $a=b$ 的特例**；修改多就把 $s$ 調小，查詢多就調大。**TLE 時第一件事是調 `s`。**\n- **定理 2（分解）**：$\\le 2$ 段零頭 + $\\le B$ 個整塊，不重不漏。**`bl == br` 必須特判——頭號錯誤。**\n- **定理 3（對參數分塊）**：**「小的預處理、大的暴力，分界點取在兩側代價相等處」**。1714 以 $y=\\sqrt n$ 為界，$O((n+q)\\sqrt n)$；**預處理表第一維必須是 $\\sqrt n$，否則空間爆。**\n- **定理 4（不變量 (R)）**：真實值 $=\\mathit{raw}[i]+\\mathit{add}[\\mathit{bel}(i)]$，$\\mathit{sum}[b]$ 不含 `add`。**整塊改 `add`、零頭改 `raw`——標記只有一層，所以沒有 `pushDown`。** 這是分塊比線段樹好寫的根本原因。區間賦值例外，讀零頭前要展開標記。\n- **定理 5（塊內排序）**：查「$<x$ 的個數」整塊二分 $O(\\log s)$、零頭暴力。**它是三種做法（歸併樹／主席樹／分塊）中唯一支援修改的。**\n- **定理 6（定期重建）**：每塊一個 `vector`，塊超過 $2s$ 就裂開，每 $\\Theta(s)$ 次操作全域重建 ⇒ 插入也能攤還 $O(\\sqrt n)$。**根號思想的第三種形態，與本章反覆出現的攤還模式同源。**\n- **規模感**：$n=q=10^5$ ⇒ $6\\times10^7$，常數小就能過；$n=10^6$ 分塊必敗，回線段樹。\n",
        },
        {
          id: 72,
          title: "11.2 莫隊演算法",
          summary:
            "## 這個技術解決什麼問題\n\n有一類區間查詢，摘要既**不可合併**（線段樹無效）、**也不可減**（前綴和與主席樹無效）：\n\n- 「區間內有幾種不同的值？」\n- 「區間內出現次數最多的元素出現幾次？」\n- 「區間內第 $k$ 小？」（可減，但要線上就得主席樹；離線有更省的辦法）\n\n但它們有一個共同的好性質：**如果我已經知道 $[l, r]$ 的答案，那麼 $[l, r+1]$、$[l-1, r]$、$[l+1, r]$、$[l, r-1]$ 的答案都能 $O(1)$ 算出來**——只要維護一張頻次表。\n\n**莫隊演算法把這件事推到極致：**\n\n> **既然答案可以「移動一格」地更新，那就把所有查詢重新排序，讓兩個指標走過的總距離最短。** 排序得當時，總移動量是 $O(n\\sqrt q)$ 而非 $O(nq)$。\n\n**它的代價是必須離線**——要先看到所有查詢才能排序。\n\n**辨識訊號**：\n- 大量區間查詢，問的是**種類數／眾數／頻次統計**這類沒有簡單區間合併公式的東西（3636 查詢超過閾值頻率最高元素、3590 第 K 小的路徑異或和）；\n- **查詢可以離線**（一次給你全部查詢，中間沒有修改）；\n- **相鄰區間的答案能用「加一個元素／減一個元素」$O(1)$ 更新**——**這是莫隊能用的前提，不是可選項**（定理 4）。\n\n**三個條件缺一不可。** 少了離線就用主席樹（9.6）；少了 $O(1)$ 增量就要重新設計狀態，否則那個 $\\log$ 會被乘上 $O(n\\sqrt q)$ 次而爆掉（定理 6）。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，可以往莫隊想：\n\n- **大量區間查詢，問的是「種類數 / 眾數 / 第 k 大」這類靠頻次的統計，沒有簡單的區間合併公式。** 像「查詢超過閾值頻率最高元素」（3636）、「第 K 小的路徑異或和」（3590）。\n- **查詢可以離線**（題目把所有查詢一次給你，且沒有夾雜修改）。\n- **相鄰區間的答案能用「加一個元素 / 減一個元素」`O(1)` 更新。** 這是莫隊能用的前提。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的答案能不能用 `add` / `remove` 一格一格地維護？\n2. 查詢能離線嗎？中間有沒有修改操作？\n3. 我有沒有按「L 的塊號、再按 R」排序，並記住原 id 回填？\n\n## 核心想法與直覺\n\n本節要解決的場景是：有很多個區間查詢，每個問「`[L, R]` 這段裡有幾個不同的數」「出現最多的數的次數是多少」這類**靠頻次統計**的問題。這種查詢通常沒有現成的「區間合併」公式，線段樹不好套。\n\n莫隊演算法的核心觀察是：**如果我已經算好了區間 `[L, R]` 的答案，那麼算 `[L, R + 1]`、`[L, R - 1]`、`[L - 1, R]`、`[L + 1, R]` 只差「加一個」或「減一個」元素。** 也就是說，相鄰的區間之間，答案可以靠一次 `add` 或 `remove` 小步調整出來。\n\n:::example 維護目前區間 [L,R] 的頻次與答案，靠 add/remove 小步調整\n所以我們維護兩個指標 L、R 代表「目前算到的區間」，要回答新查詢時，就把 L、R 一步步挪到目標位置，每挪一格就更新頻次與答案。\n\n```cpp\n// 維護目前區間 [L,R] 的頻次與答案，靠 add/remove 小步調整\nint cnt[MAXV];  // 每個數值目前出現幾次\nint cur = 0;    // 目前區間的答案（例：不同數的個數）\n\nvoid add(int x) {\n    if (cnt[x]++ == 0) {\n        ++cur;\n    }\n}  // 新出現一種數\nvoid remove(int x) {\n    if (--cnt[x] == 0) {\n        cur--;\n    }\n}  // 一種數歸零\n```\n:::\n\n\n\n:::example 莫隊排序：先按 L 的塊號，同塊再按 R\n但如果查詢順序很亂，指標可能來回大幅跳動，退化成 `O(n)` 每次。莫隊的聰明之處是**離線排序**：把所有查詢先收集起來，按「L 屬於哪一塊」分組，同塊內再按 R 排序。這樣指標移動的總距離被壓到 `O(n sqrt(n))`，平均每次查詢約 `O(sqrt(n))`。\n\n```cpp\n// 莫隊排序：先按 L 的塊號，同塊再按 R\nint len = (int)sqrt(n) + 1;  // 塊大小\nsort(queries.begin(), queries.end(), [&](auto& a, auto& b) {\n    if (a.l / len != b.l / len) {\n        return a.l / len < b.l / len;\n    }\n    return a.r < b.r;\n});\n```\n:::\n\n\n\n「離線」的意思是：我們不必照題目給的順序回答，可以先重排、算完再按原本的 id 填回去。代價是查詢之間不能有會改變陣列的修改操作。\n\n## 狀態／資料結構定義\n\n**三樣東西：**\n\n```cpp\nstruct Query { int l, r, id; };      // id 用來把答案放回原順序\nint curL = 0, curR = -1;             // 當前窗口 [curL, curR]，初始為空\nvector<int> cnt;                     // 頻次表：值 v 在當前窗口出現幾次\nint cur = 0;                         // 當前窗口的答案\n```\n\n**`curL = 0, curR = -1` 表示空窗口。** 這個初始值不是隨便選的——它讓 `curR - curL + 1 == 0`，且第一次 `curR++` 之後就指向元素 0，**所有移動邏輯不需要特判空窗口。**\n\n**兩個增量函式，是整個演算法唯一與題目相關的部分：**\n\n```cpp\nvoid add(int i)    { if (cnt[a[i]]++ == 0) cur++; }   // 例：不同值個數\nvoid remove(int i) { if (--cnt[a[i]] == 0) cur--; }\n```\n\n**排序鍵（定理 2 會證明為什麼是這個）：**\n\n$$\\text{按}\\;\\big(\\mathit{bel}(l),\\; r\\big)\\;\\text{排序，其中}\\;\\mathit{bel}(l) = l / s.$$\n\n**加上奇偶優化（定理 3）**：偶數塊 $r$ 遞增、奇數塊 $r$ 遞減。\n\n**塊長 $s$ 的最優值是 $n/\\sqrt q$ 而不是 $\\sqrt n$**（定理 2）。$q \\approx n$ 時兩者一致，但 $q \\ll n$ 時差距很大。\n\n## 不變量或正確性證明\n\n以下記序列長度 $n$、查詢數 $q$、塊長 $s$、塊數 $B = \\lceil n/s\\rceil$。\n\n### 定理 1（指標移動的正確性與順序）\n\n**不變量 (M)**：*在處理每個查詢之前與之後，`cnt` 恰好是子陣列 $a[\\mathit{curL} \\dots \\mathit{curR}]$ 的頻次表，且 `cur` 是該子陣列的答案。*\n\n*維護*：四種移動各自 $O(1)$ 地維持 (M)：`curR++` 前 `add(curR+1)`、`curR--` 後 `remove(curR+1)`、`curL--` 前 `add(curL-1)`、`curL++` 後 `remove(curL)` ✔。\n\n**但移動的順序不能任意。** 標準寫法是：\n\n```cpp\nwhile (curR < q.r) add(++curR);       // 先擴張\nwhile (curL > q.l) add(--curL);       // 先擴張\nwhile (curR > q.r) remove(curR--);    // 再收縮\nwhile (curL < q.l) remove(curL++);    // 再收縮\n```\n\n*為什麼必須「先擴張、再收縮」*。設當前窗口是 $[5, 5]$（一個元素），目標是 $[8, 8]$。\n\n- **先收縮**：`while (curL < 8) remove(curL++)` 會走到 $\\mathit{curL} = 8$，而 $\\mathit{curR}$ 還是 5——**窗口變成 $[8, 5]$，`curL > curR + 1`，中間的 6、7 被「移除」了兩次**（一次沒被加入過），`cnt` 出現負值，(M) 崩壞 ✔（這就是反例）。\n- **先擴張**：`curR` 先走到 8（依序 add 6、7、8），窗口成為 $[5, 8]$；再收縮 `curL` 到 8。全程 $\\mathit{curL} \\le \\mathit{curR}+1$，每個元素的 add／remove 恰好配對 ✔。\n\n**形式上，先擴張保證了「窗口在任何中間時刻都是一個合法的（可能為空的）區間」**，於是 (M) 在每一步之後都成立。$\\square$\n\n**這是莫隊最常見的錯誤，而且症狀是「大部分測資對、少數錯」**——只有當相鄰兩個查詢的區間**不相交**時才會觸發。**照抄上面四行的順序。**\n\n### 定理 2（複雜度 $O(n\\sqrt q)$，以及最優塊長）\n\n*命題*：按 $(\\mathit{bel}(l),\\, r)$ 排序後，兩個指標的總移動量為\n\n$$O\\!\\left(\\frac{n^2}{s} + qs\\right),$$\n\n取 $s = n/\\sqrt q$ 得 $O(n\\sqrt q)$。\n\n*證明*。分別計算兩個指標。\n\n**右指標 $r$**。在**同一塊內**，所有查詢按 $r$ 遞增排序，故 $r$ **單調不減**，該塊內的總移動量 $\\le n$。共 $B = n/s$ 塊 ⇒ 總移動量 $\\le nB = n^2/s$ ✔。（跨塊時 $r$ 可能從 $n$ 跳回小值，一次 $O(n)$，共 $B$ 次，已含在上式中。）\n\n**左指標 $l$**。在同一塊內，所有查詢的 $l$ 都落在同一個長度 $s$ 的塊裡，故相鄰兩個查詢之間 $l$ 至多移動 $s$；共 $q$ 個查詢 ⇒ 總移動量 $\\le qs$ ✔。（跨塊時 $l$ 至多移動 $2s$，共 $B$ 次，$O(Bs) = O(n)$，可忽略。）\n\n**求最優 $s$**：由 AM–GM（與 11.1 定理 1 同一個計算），$\\frac{n^2}{s} + qs$ 在 $s = n/\\sqrt q$ 取最小值 $2n\\sqrt q$ ✔。$\\square$\n\n**兩個實務推論**：\n\n- **$q \\approx n$ 時 $s = \\sqrt n$，總複雜度 $O(n\\sqrt n)$。** 這是最常見的情形，也是「莫隊 = 根號」這個印象的來源。\n- **$q \\ll n$ 時 $s = n/\\sqrt q > \\sqrt n$，塊要開大。** 例如 $n = 10^5$、$q = 10^3$ ⇒ $s \\approx 3163$（而不是 316），總移動量 $2n\\sqrt q \\approx 6.3\\times10^6$ 而非 $\\sqrt{}$ 版的 $3\\times10^7$。**塊長寫死 `sqrt(n)` 在這種比例下慢 5 倍。**\n\n**這個證明依賴的前提要點名**：「$r$ 在塊內單調」完全來自排序鍵的第二維；「$l$ 在塊內移動 $\\le s$」完全來自第一維的分塊。**兩維各負責一個指標——這就是排序鍵的設計理由，不是湊出來的。**\n\n### 定理 3（奇偶排序優化）\n\n*做法*：比較器改成\n\n```cpp\nsort(qs.begin(), qs.end(), [&](const Query& x, const Query& y) {\n    int bx = x.l / s, by = y.l / s;\n    if (bx != by) return bx < by;\n    return (bx & 1) ? x.r > y.r : x.r < y.r;   // 奇數塊反向\n});\n```\n\n*正確性*：排序只改變處理順序，而由定理 1，**任何處理順序都會得到正確答案**（(M) 與順序無關）✔。\n\n*複雜度不變*：塊內 $r$ 仍然單調（只是方向不同），總移動量的上界 $n^2/s$ 不變 ✔。\n\n*收益*：原本每跨一塊，$r$ 都要從 $n$ 附近「歸零」重跑一次，浪費 $O(n)$；奇偶交替後，**跨塊時 $r$ 停在哪就從哪繼續**，這 $B$ 次共 $O(n)$ 的浪費被完全省下。**實測常數約減半，一兩行的改動，值得永遠加上。** $\\square$\n\n### 定理 4（`add` / `remove` 必須 $O(1)$ 且可逆——以及做不到時怎麼辦）\n\n*為什麼是硬前提*：由定理 2，`add`／`remove` 被呼叫 $O(n\\sqrt q)$ 次。$n = q = 10^5$ 時是 $3\\times10^7$ 次。**每次多一個 $\\log$ 就是 $5\\times10^8$，必然超時。**\n\n**但「可逆」這個條件比想像中嚴格。** 考慮**區間眾數的出現次數**：\n\n- `add`：`cnt[v]++; cur = max(cur, cnt[v]);` —— $O(1)$ ✔\n- `remove`：`cnt[v]--;` 之後 `cur` 該變成多少？**如果 $v$ 剛好是唯一的眾數，`cur` 要減 1；否則不變。判斷這件事需要另一張「出現次數的出現次數」表，而且即使有了也只能處理「恰好減 1」的情形。** 一般而言 **max 不可撤銷** ✘。\n\n**解法：回滾莫隊（只增不減莫隊）。**\n\n*做法*：按 $(\\mathit{bel}(l), r)$ 排序後，**對每一塊單獨處理**：\n1. 令 $R_b = \\mathit{hi}(b)$（塊右界）。**右指標 $r$ 從 $R_b$ 開始，在本塊內單調遞增，全程只 `add`。**\n2. 對每個查詢，先把 $r$ 推到 $q.r$（只 `add`）；再**從 $R_b$ 出發把左指標臨時推到 $q.l$**（只 `add`），記錄答案；然後**把這些臨時的 `add` 全部回滾**（恢復 `cnt` 與 `cur` 的快照）。\n3. 若查詢**整段落在同一塊內**（$q.r < R_b$），直接暴力 $O(s)$。\n\n*正確性*：每個查詢被回答時，窗口恰為 $[q.l, q.r]$ ✔；回滾把狀態還原到「只含 $[R_b, r]$」，供下一個查詢使用 ✔。\n\n*複雜度*：$r$ 每塊單調 ⇒ $O(n B) = O(n^2/s)$；每個查詢的臨時左擴 $\\le s$ 且回滾同樣 $\\le s$ ⇒ $O(qs)$。**與定理 2 完全相同的 $O(n\\sqrt q)$** ✔。$\\square$\n\n**回滾的實作要點**：`cur` 只要存一個變數快照；`cnt` 的還原要**逐個撤銷剛才 `add` 過的位置**（不能整表清空，那是 $O(V)$）。**因為只 `add` 過 $\\le s$ 個位置，撤銷也是 $O(s)$。**\n\n**對偶的情形是「只減不增莫隊」**（`remove` 好寫、`add` 難寫），做法對稱。**先問哪一邊好寫，再決定用哪一種。**\n\n### 定理 5（適用邊界與兩個擴展）\n\n**莫隊要求三件事：離線、無修改、$O(1)$ 可逆增量。** 放寬前兩者各有代價：\n\n**帶修莫隊**：查詢加上第三維「時間」$t$（前面有幾次修改），按 $(\\mathit{bel}(l), \\mathit{bel}(r), t)$ 排序，塊長取 $n^{2/3}$。\n\n*複雜度*：$l$ 移動 $O(qs)$、$r$ 移動 $O(q\\cdot n^2/s^2 \\cdot s)$… 標準結果是 $O(n^{5/3})$（取 $s = n^{2/3}$）。**$n = 10^5$ 時 $n^{5/3} \\approx 2\\times10^8$——能過但很緊，通常有更好的做法。**\n\n**樹上莫隊**：把樹展開成**歐拉序**（每個節點出現兩次），路徑 $(u,v)$ 對應歐拉序上的一段區間；出現兩次的節點視為「不在路徑上」（用一個 `vis` 標記做 XOR 式的 add／remove），LCA 要另外補上。**核心技巧是「把樹上路徑轉成序列區間」，轉完之後就是普通莫隊。** 3590 這類「路徑上的統計」屬於這一族。\n\n**兩者都只是換排序鍵與換增量函式，主體邏輯不變。** 這說明莫隊是一個**框架**而非單一演算法。\n\n### 定理 6（值域分塊：為什麼不能用 `set` 或樹狀陣列）\n\n*情境*：莫隊維護頻次表之後，查詢還需要一個「全域統計」——例如「當前窗口的第 $k$ 小」「當前窗口中出現次數 $\\ge$ 閾值的最大值」。\n\n*天真做法*：用樹狀陣列維護值域計數，`add`／`remove` 是 $O(\\log V)$、查詢是 $O(\\log V)$。\n\n*為什麼不行*：`add`／`remove` 被呼叫 $O(n\\sqrt q)$ 次、查詢只有 $q$ 次。**代價完全不對稱——應該讓 `add`／`remove` 快，查詢慢。**\n\n$$\\underbrace{n\\sqrt q \\cdot \\log V}_{3\\times10^7 \\times 20 = 6\\times10^8\\;\\text{不可行}} \\quad\\text{vs}\\quad \\underbrace{n\\sqrt q \\cdot 1 + q\\sqrt V}_{3\\times10^7 + 10^5\\times316 = 6\\times10^7\\;\\checkmark}$$\n\n*正確做法：**值域分塊***。把值域切成 $\\sqrt V$ 塊，維護 `cnt[v]` 與 `blockCnt[b]`：\n- `add`／`remove`：兩個 $O(1)$ 的增減 ✔；\n- 查詢第 $k$ 小：先掃 $\\sqrt V$ 個塊找到答案所在塊，再掃塊內 $\\sqrt V$ 個值 ⇒ $O(\\sqrt V)$ ✔。\n\n**這是「$O(1)$ 修改 / $O(\\sqrt n)$ 查詢」的資料結構，恰好與樹狀陣列的 $O(\\log n)$/$O(\\log n)$ 互補。**\n\n**這條原則值得單獨記住**：\n\n> **選資料結構時看的不是單次複雜度，而是「操作次數 × 單次代價」的總和。** 莫隊裡修改次數遠多於查詢次數，所以要挑修改快的那個。\n\n**這也回答了 11.1 遺留的問題「$O(\\sqrt n)$ 的資料結構有什麼用」——它的用處正是在這種操作次數極度不對稱的場合。**\n\n## 逐步演算法\n\n莫隊題目可拆成下面幾步，逐一搭配程式碼：\n\n:::example 每個查詢記住左右端點與原本的順序 id\n**1. 收集查詢並記住原 id。** 因為要重排，得記得每個查詢原本是第幾個，最後才填得回去。\n\n```cpp\n// 每個查詢記住左右端點與原本的順序 id\nstruct Query {\n    int l, r, id;\n};\nvector<Query> queries;  // 收集全部查詢\n```\n:::\n\n\n\n**2. 按莫隊順序排序。** 就是上面那段 `sort`：先 L 的塊號，同塊按 R。\n\n:::example 把 [L,R] 挪到查詢的 [ql,qr]，邊挪邊更新答案\n**3. 移動指標，每移一格就 add / remove。** 注意「先擴張再收縮」可避免一瞬間區間變空出錯。\n\n```cpp\n// 把 [L,R] 挪到查詢的 [ql,qr]，邊挪邊更新答案\nwhile (R < qr) {\n    add(a[++R]);\n}\nwhile (L > ql) {\n    add(a[--L]);\n}\nwhile (R > qr) {\n    remove(a[R--]);\n}\nwhile (L < ql) {\n    remove(a[L++]);\n}\nans[q.id] = cur;  // 區間對齊後，cur 就是答案\n```\n:::\n\n\n\n**4. 按原 id 回填答案。** `ans` 開成原查詢數量大小，用 `q.id` 當索引存放，輸出時就是原順序。\n\n**5. 邊界與複雜度。**\n\n- 只有「沒有線上修改」或「可離線」的查詢適用；中途會改陣列就不能直接用。\n- `add` 和 `remove` 必須是彼此的反操作，且和答案統計方式一致，否則 `cur` 會算錯。\n- 複雜度：指標總移動 `O(n sqrt(n))`，q 個查詢整體約 `O((n + q) sqrt(n))`。\n\n## C++17 模板\n\n**模板一：基礎莫隊（區間不同值個數）**\n\n```cpp\nstruct Mo {\n    int n, s;\n    vector<int> a, cnt;\n    int cur = 0;\n\n    void add(int i)    { if (cnt[a[i]]++ == 0) cur++; }\n    void remove(int i) { if (--cnt[a[i]] == 0) cur--; }\n\n    // queries: {l, r}，皆為 0-indexed 且兩端皆含\n    vector<int> solve(vector<int> a_, vector<array<int,2>> queries) {\n        a = move(a_);\n        n = int(a.size());\n        int q = int(queries.size());\n        if (q == 0) return {};\n        // 離散化，讓 cnt 的大小是 O(n)\n        {\n            vector<int> v = a;\n            sort(v.begin(), v.end());\n            v.erase(unique(v.begin(), v.end()), v.end());\n            for (int& x : a) x = int(lower_bound(v.begin(), v.end(), x) - v.begin());\n            cnt.assign(v.size(), 0);\n        }\n        s = max(1, int(n / max(1.0, sqrt(double(q)))));      // 定理 2：n / sqrt(q)\n\n        vector<int> ord(q);\n        iota(ord.begin(), ord.end(), 0);\n        sort(ord.begin(), ord.end(), [&](int x, int y) {\n            int bx = queries[x][0] / s, by = queries[y][0] / s;\n            if (bx != by) return bx < by;\n            return (bx & 1) ? queries[x][1] > queries[y][1]   // 定理 3：奇偶優化\n                            : queries[x][1] < queries[y][1];\n        });\n\n        vector<int> ans(q);\n        int curL = 0, curR = -1;\n        for (int id : ord) {\n            int l = queries[id][0], r = queries[id][1];\n            while (curR < r) add(++curR);        // 先擴張（定理 1）\n            while (curL > l) add(--curL);\n            while (curR > r) remove(curR--);     // 再收縮\n            while (curL < l) remove(curL++);\n            ans[id] = cur;\n        }\n        return ans;\n    }\n};\n```\n\n**四行指標移動的順序是硬性的**（定理 1）；**塊長是 `n / sqrt(q)` 而非 `sqrt(n)`**（定理 2）；**離散化讓 `cnt` 的大小是 $O(n)$**，否則值域 $10^9$ 開不出陣列。\n\n**模板二：回滾莫隊骨架（`remove` 無法 $O(1)$ 時，定理 4）**\n\n```cpp\n    // 依塊處理；本塊右界 R = min(n, (b+1)*s)\n    for (int b = 0; b * s < n; b++) {\n        int R = min(n, (b + 1) * s);\n        // 每塊重置：只 add 過的位置逐個清掉，不要整表 memset\n        resetCounters();\n        int curR = R - 1, snapshot = 0;          // 窗口 [R, R-1] 為空\n        for (int id : queriesInBlock[b]) {\n            int l = queries[id][0], r = queries[id][1];\n            if (r < R) { ans[id] = bruteForce(l, r); continue; }   // 同塊，暴力\n            while (curR < r) add(++curR);        // 只 add，塊內單調\n            snapshot = cur;\n            int tmpL = R;\n            while (tmpL > l) add(--tmpL);        // 臨時左擴\n            ans[id] = cur;\n            while (tmpL < R) rollback(tmpL++);   // 回滾：撤銷剛才那些 add\n            cur = snapshot;\n        }\n    }\n```\n\n**`rollback` 與 `remove` 的差別**：`rollback` 只需要還原 `cnt`（`cnt[a[i]]--`），**不需要維護 `cur`**——`cur` 直接用快照覆蓋。**這正是回滾莫隊能繞開「max 不可撤銷」的原因。**\n\n**模板三：值域分塊（$O(1)$ 修改 / $O(\\sqrt V)$ 查詢，定理 6）**\n\n```cpp\nstruct ValueBlocks {\n    int V, s, nb;\n    vector<int> cnt, blk;\n    explicit ValueBlocks(int V) : V(V) {\n        s = max(1, int(sqrt(double(V))));\n        nb = (V + s - 1) / s;\n        cnt.assign(V, 0);\n        blk.assign(nb, 0);\n    }\n    void ins(int v) { cnt[v]++; blk[v / s]++; }      // O(1)\n    void del(int v) { cnt[v]--; blk[v / s]--; }      // O(1)\n    int kth(int k) const {                            // 第 k 小，k 從 1 起算；O(sqrt V)\n        int b = 0;\n        while (b < nb && k > blk[b]) k -= blk[b], b++;\n        for (int v = b * s; ; v++) { k -= cnt[v]; if (k <= 0) return v; }\n    }\n};\n```\n\n## 時間與空間複雜度\n\n| | 時間 | 空間 |\n| --- | --- | --- |\n| 排序查詢 | $O(q\\log q)$ | $O(q)$ |\n| 指標移動（定理 2） | $O(n\\sqrt q)$ 次 `add`／`remove` | — |\n| 基礎莫隊總計（$q\\approx n$） | $O(n\\sqrt n)$ | $O(n)$ |\n| 回滾莫隊（定理 4） | $O(n\\sqrt q)$ | $O(n)$ |\n| 帶修莫隊 | $O(n^{5/3})$ | $O(n)$ |\n| 樹上莫隊 | $O(n\\sqrt q)$ | $O(n)$ |\n| 莫隊 + 值域分塊（定理 6） | $O(n\\sqrt q + q\\sqrt V)$ | $O(n + V)$ |\n| 莫隊 + 樹狀陣列（**錯誤示範**） | $O(n\\sqrt q\\log V)$ | $O(n+V)$ |\n\n**規模感**：$n = q = 10^5$ ⇒ $n\\sqrt q \\approx 3.2\\times10^7$ 次 `add`／`remove`。**每次必須是幾條指令的等級**（一次陣列讀寫加一個比較），否則過不了。**`add` 裡出現 `map`、`set`、`vector::push_back` 都是紅燈。**\n\n**最優塊長的實測影響**（$n = 10^5$）：\n\n| $q$ | $s = \\sqrt n = 316$ | $s = n/\\sqrt q$ | 總移動量比 |\n| --- | --- | --- | --- |\n| $10^5$ | 316 | 316 | 1.0× |\n| $10^4$ | 316 | 1000 | 約 1.6× |\n| $10^3$ | 316 | 3163 | 約 5× |\n\n**查詢少時一定要調大塊長。**\n\n**空間**：`cnt` 的大小是離散化後的值域 $O(n)$；查詢陣列 $O(q)$。**莫隊的空間一向不是問題。**\n\n## 常見錯誤與邊界條件\n\n- **指標移動順序寫成「先收縮」**（定理 1）。窗口出現 `curL > curR + 1` 的非法狀態，`cnt` 變負。**只有相鄰查詢不相交時才觸發，測資小時可能矇混過關。照抄「兩個 add 在前、兩個 remove 在後」。**\n- **`curL`／`curR` 初始值錯**。必須是 `curL = 0, curR = -1`（空窗口）。寫成 `curR = 0` 就漏加了元素 0。\n- **忘了離散化**。值域 $10^9$ 時 `cnt` 開不出來。**離散化後 `cnt.assign(m, 0)`。**\n- **塊長寫死 `sqrt(n)`**（定理 2）。$q \\ll n$ 時慢數倍。**用 `n / sqrt(q)`，並注意 $q = 0$ 的除零。**\n- **`add` / `remove` 不對稱**。兩者必須嚴格互逆：`add` 裡是 `cnt[v]++ == 0`（後置），`remove` 裡是 `--cnt[v] == 0`（前置）。**寫成同一種前後置就錯了，而且錯得很隱晦。**\n- **在 `add` 裡做了 $O(\\log)$ 的事**（定理 6）。`set::insert`、`map` 查找、BIT 更新——**全部會把總複雜度乘上 $\\log$。改用值域分塊或陣列。**\n- **答案沒有放回原順序**。排序後要用 `id` 寫回 `ans[id]`，不能按處理順序輸出。\n- **`max` 型答案用了普通莫隊**（定理 4）。眾數、最大頻次這類不可撤銷的答案**必須用回滾莫隊**。症狀是答案偏小且無規律。\n- **回滾莫隊沒有正確重置**。每塊開始時要清空 `cnt`，但**不能 `memset` 整張表**（$O(V)$ × $B$ 次會爆）——**逐個撤銷本塊 add 過的位置。**\n- **回滾莫隊漏了「同塊查詢暴力處理」的分支**。$q.r < R_b$ 時右指標無法從 $R_b$ 遞增到 $q.r$，必須單獨暴力 $O(s)$。\n- **奇偶優化寫錯方向判斷**。`(bx & 1)` 用的是**塊編號**的奇偶，不是 `l` 的奇偶。\n- **查詢區間的閉合約定不一致**。本節一律 $[l, r]$ 兩端皆含、0-indexed。**題目若給 1-indexed 要先轉換。**\n\n## 與相似技巧的比較\n\n- **與 11.1 分塊**：兩者都是根號，但**分塊是「對資料分塊」、莫隊是「對查詢排序」**。分塊支援線上、每次操作 $O(\\sqrt n)$；莫隊必須離線、但總複雜度更好（$O(n\\sqrt q)$ 對比 $O(q\\sqrt n)$，$q < n$ 時莫隊贏）。**莫隊的排序鍵用到分塊——它是分塊思想的應用，不是競爭者。** 而定理 6 更是直接把值域分塊嵌進莫隊裡。\n- **與 9.6 主席樹**：**離線 ⇒ 莫隊（空間 $O(n)$）；線上 ⇒ 主席樹（空間 $O(n\\log n)$）。** 對「區間第 $k$ 小」兩者都能做，主席樹是 $O(\\log n)$ 單次查詢、莫隊是 $O(\\sqrt n)$——**但莫隊能做主席樹做不了的「眾數」「種類數」，因為那些不可減。**\n- **與 12.1 離線 + 排序掃描**：若查詢的**單調性**允許「只加不刪」（例如按門檻排序後元素只增不減），那麼一次線性掃描 + 樹狀陣列就是 $O((n+q)\\log n)$，**遠優於莫隊。** **拿到離線題先問：能不能只用一個指標？能的話別上莫隊。** 莫隊是「兩個指標都要來回移動」時的最後手段。\n- **與 CDQ 分治／整體二分**：處理三維偏序、帶修改的離線問題，$O(n\\log^2 n)$。**複雜度比帶修莫隊的 $O(n^{5/3})$ 好，但實作更難。** 帶修莫隊的價值是「不用重新思考，只加一維排序鍵」。\n- **與 9.2 樹狀陣列求逆序對**：那也是離線的區間統計，但用的是「掃描順序承擔一個維度」——**單指標、$O(n\\log n)$。** 與莫隊的雙指標是完全不同的路子。**能用單指標就別用雙指標，這是效率上的一整個檔次。**\n- **與滑動窗口（sliding_window 章）**：滑動窗口是「一個指標單調前進、另一個單調跟隨」的 $O(n)$ 特例。**莫隊可以看成「放棄單調性、改用排序把總移動量壓到 $O(n\\sqrt q)$」的推廣。** 認出這個關係，就知道莫隊本質上是在為「無序的查詢集合」重建一種近似的單調性。\n\n## 代表例題\n\n**題號**：3590、3636\n\n要把區間量改寫成前綴狀態之差；若是計數題，重點是查找過去需要的前綴值。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 3590 | [第 K 小的路徑異或和](https://leetcode.cn/problems/kth-smallest-path-xor-sum) | 2646 |\n| 3636 | [查詢超過閾值頻率最高元素](https://leetcode.cn/problems/threshold-majority-queries) | 2451 |\n\n## 本節重點速查\n\n- **三個硬前提**：**離線、無修改、$O(1)$ 可逆增量。** 缺一就換工具（線上 ⇒ 主席樹；有修改 ⇒ 帶修莫隊 $O(n^{5/3})$；不可撤銷 ⇒ 回滾莫隊）。\n- **排序鍵 $(\\mathit{bel}(l), r)$ + 奇偶優化**：第一維讓 $l$ 塊內只移動 $s$，第二維讓 $r$ 塊內單調。**兩維各負責一個指標。** 奇偶交替（偶塊 $r$ 遞增、奇塊遞減）省下跨塊時 $r$ 的歸零，常數約減半。\n- **指標移動順序（定理 1）**：**兩個 `add` 在前、兩個 `remove` 在後。** 先收縮會產生 `curL > curR+1` 的非法窗口。初始 `curL = 0, curR = -1`。\n- **複雜度 $O(n^2/s + qs)$，最優 $s = n/\\sqrt q$，總 $O(n\\sqrt q)$（定理 2）。** $q\\approx n$ 時退化成 $\\sqrt n$；**$q \\ll n$ 時塊要開大，寫死 `sqrt(n)` 會慢數倍。**\n- **回滾莫隊（定理 4）**：`max` 型答案不可撤銷 ⇒ 每塊內 $r$ 只增、$l$ 臨時左擴後回滾（`cur` 用快照還原，`cnt` 逐個撤銷）；同塊查詢暴力。**複雜度不變。**\n- **值域分塊而非樹狀陣列（定理 6）**：`add`／`remove` 被呼叫 $O(n\\sqrt q)$ 次、查詢只有 $q$ 次 ⇒ **要挑「修改 $O(1)$、查詢 $O(\\sqrt V)$」的結構。** 一般原則：**看「次數 × 單次代價」的總和，不是單次複雜度。**\n- **擴展只是換排序鍵**：帶修莫隊加時間維、塊長 $n^{2/3}$；樹上莫隊用歐拉序把路徑轉成區間。**主體邏輯不變——莫隊是框架。**\n- **先問能不能單指標**：離線 + 門檻單調 ⇒ 排序掃描 $O((n+q)\\log n)$，遠勝莫隊。**莫隊是兩個指標都必須來回時的最後手段。**\n",
        },
        {
          id: 73,
          title: "11.3 其他",
          summary:
            "## 這個技術解決什麼問題\n\n11.1 把**陣列**切成 $\\sqrt n$ 塊，11.2 把**查詢**按 $\\sqrt n$ 分組。但根號的用處遠不止「切塊」。\n\n**這一節收集的是另一類根號**：不切任何東西，而是**證明「某個量本身就不超過 $\\sqrt n$ 種」**，於是可以放心地枚舉它。\n\n> **核心引理族**：若兩個非負量 $x$、$y$ 滿足 $x \\cdot y \\le n$（或 $x^2 \\le n$），則**其中至少一個不超過 $\\sqrt n$**。\n\n這句廢話一樣的算術，撐起了一整族技巧：\n\n| 場景 | 受限的量 | 上界 |\n| --- | --- | --- |\n| 子字串中 $\\text{ones} \\ge \\text{zeros}^2$（3234） | 零的個數 | $\\sqrt n$ |\n| 出現次數 $> \\sqrt n$ 的元素 | 這種元素的種類數 | $\\sqrt n$ |\n| $\\lfloor n/i \\rfloor$ 隨 $i$ 變化 | 不同值的個數 | $2\\sqrt n$ |\n| $\\sum a_i = S$ 時 $a_i$ 的不同值 | 種類數 | $\\sqrt{2S}$ |\n| $n$ 的因數 | $\\le\\sqrt n$ 或配對的 $>\\sqrt n$ | 各一半 |\n\n**辨識訊號**：\n- 問的是某段裡「符合某種條件的數量」，而這條件**沒有好用的區間合併公式**（3234 統計 1 顯著的子字串的數量）；\n- 線段樹／樹狀陣列很難維護你要的摘要，但「每塊各自統計 + 零頭暴力」就能回答；\n- $n$、$q$ 在十萬等級，$O((n+q)\\sqrt n)$ 可過而 $O(nq)$ 會超時；\n- **題目條件裡出現了平方、乘積或總和的限制**——這往往就是根號的入口。\n\n**最後一條是最有用的辨識法**：**看到「$x^2 \\le$ 某個線性量」或「總和 $\\le S$」，先算一下它蘊含了什麼上界。** 很多題目的關鍵不是資料結構，而是這個上界。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，可以往「根號其他應用」想：\n\n- **問的是某段裡「符合某種條件的數量」，而這條件沒有好用的區間合併公式。** 像「統計 1 顯著的子字串的數量」（3234）這種需要對子區間做特殊計數的題。\n- **線段樹 / 樹狀陣列很難維護你要的摘要，但「每塊各自統計 + 零頭暴力」就能回答。**\n- **資料規模允許 `O((n+q) sqrt(n))`**（十萬等級通常可過），而純暴力 `O(n·q)` 會超時。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要統計的量，能不能塞進「每塊一份摘要」裡？\n2. 整塊能直接從摘要讀答案、零頭能逐元素暴力嗎？\n3. 我有沒有確認線段樹真的不好做，根號才是較簡單的選擇？\n\n## 核心想法與直覺\n\n本節是根號思想的「其他應用」：同一個「切成 `sqrt(n)` 塊、整塊看摘要、零頭暴力」的點子，不一定用在區間和上，也能用來統計更彈性的東西。本節要解決的，是那種**摘要不只是一個數字，而是一整張表**的情況。\n\n:::example 塊的摘要可以是一張表，而不只是一個數\n先回顧根號分解的骨架：把長度 n 的資料切成約 `sqrt(n)` 塊，每塊先存一份摘要。不同的是，這裡「摘要」可能是一張頻次表、或塊內排序後的陣列，用來回答比較複雜的問題（例如「這段裡有幾個值小於 x」「某種前綴狀態各出現幾次」）。\n\n```cpp\n// 塊的摘要可以是一張表，而不只是一個數\nint len = (int)sqrt(n) + 1;\n// 例：每塊維護「塊內某種狀態值 -> 出現次數」的頻次表\nvector<unordered_map<int, int>> blockCnt(n / len + 1);\n```\n:::\n\n\n\n:::example 查詢時：整塊問摘要、零頭逐個檢查（與基礎版同骨架）\n為什麼這招在「奇怪統計」上特別好用？因為線段樹要求資訊能「左右合併」，但很多統計（如「等於某值的個數」「小於 x 的個數」）合併起來很麻煩。根號分解不要求合併公式——它只要求**每塊能各自回答、零頭能暴力**，限制鬆得多。\n\n```cpp\n// 查詢時：整塊問摘要、零頭逐個檢查（與基礎版同骨架）\nlong long ans = 0;\n// 1) 左零頭：逐元素判斷是否符合條件，符合就累加\n// 2) 中間整塊：直接查 blockCnt[b] 這張表拿答案\n// 3) 右零頭：同左零頭\n```\n:::\n\n\n\n代價同樣是每次 `O(sqrt(n))`，比線段樹的 `O(log n)` 慢，但換來「想統計什麼都能塞進塊摘要」的彈性。許多看似要高級資料結構、實則用根號暴力就能過的題，都屬於本節。\n\n## 狀態／資料結構定義\n\n這一節沒有單一的資料結構，而是四個可以獨立使用的骨架。\n\n**骨架一：受限量枚舉（3234 型）**\n\n```cpp\n// 對每個右端點 r，枚舉「零的個數」z = 0 .. sqrt(n)\n// 對每個 z，合法的左端點 l 落在一個「連續區間」上，O(1) 計數\n```\n\n需要的輔助狀態只有：**零的位置陣列 `pos`**（`pos[j]` = 第 $j$ 個零的下標，$j$ 從 1 起算，`pos[0] = -1`）。\n\n**骨架二：整除分塊（數論分塊）**\n\n```cpp\nfor (int i = 1, j; i <= n; i = j + 1) {\n    j = n / (n / i);                 // [i, j] 內 n/i 的值都相同\n    // 用 (j - i + 1) 個相同的 n/i 做事\n}\n```\n\n**骨架三：重／輕元素分治**\n\n```cpp\n// 出現次數 > sqrt(n) 的元素稱為「重元素」，至多 sqrt(n) 種\n// 重元素：每種各自 O(n) 掃一遍  ⇒ O(n sqrt n)\n// 輕元素：出現次數 <= sqrt(n)，用另一套（通常是暴力枚舉出現位置對）\n```\n\n**骨架四：操作分塊（對時間軸分塊）**\n\n```cpp\n// 把 q 個操作按每 sqrt(q) 個分成一批\n// 每批開始時：O(n) 重建一個「不含本批修改」的靜態結構\n// 批內查詢：靜態結構的答案 + 本批 O(sqrt q) 個修改的暴力修正\n```\n\n**四個骨架的共同點**：**它們都把「一次做全部」拆成「大的少做、小的多做」，並讓兩側的代價相等。** 這就是 11.1 定理 1 的同一個平衡，只是套在不同的量上。\n\n## 不變量或正確性證明\n\n### 定理 1（平方受限引理）\n\n*命題*：設子字串包含 $z$ 個 0 與 $o$ 個 1，且滿足 $o \\ge z^2$。若字串總長為 $n$，則 $z \\le \\lfloor\\sqrt n\\rfloor$。\n\n*證明*。$z^2 \\le o \\le z + o \\le n$，故 $z \\le \\sqrt n$ ✔。$\\square$\n\n**看起來平凡，但它把「枚舉所有 $O(n^2)$ 個子字串」變成了「枚舉 $O(n\\sqrt n)$ 個 $(r, z)$ 組合」——這是本節全部技巧的原型。**\n\n**推論 1.1（同型的常用上界）**：\n- $\\sum_{i} a_i = S$ 且 $a_i$ 為相異正整數 ⇒ 至多 $O(\\sqrt S)$ 個（因為 $1+2+\\cdots+k = k(k+1)/2 \\le S$）。**多重背包的「相同物品分組」與「不同重量至多 $\\sqrt S$ 種」都靠這一條。**\n- 陣列中出現次數 $> \\sqrt n$ 的元素 ⇒ 至多 $\\sqrt n$ 種（否則總長超過 $n$）。\n- $n$ 的因數必定成對出現 $(d, n/d)$，其中至少一個 $\\le \\sqrt n$ ⇒ **枚舉因數只需 $O(\\sqrt n)$。**\n\n### 定理 2（3234 的完整解法與正確性）\n\n*問題*：給定 01 字串 $s$（長度 $n$），求有多少子字串滿足「1 的個數 $\\ge$ 0 的個數的平方」。\n\n*記號*：設 $s$ 中零的位置由小到大為 $p_1 < p_2 < \\cdots < p_m$，並約定 $p_0 = -1$。對右端點 $r$，令 $t = t(r)$ 為 $[0, r]$ 內零的個數。\n\n**命題**：固定 $r$ 與 $z \\ge 1$，子字串 $[l, r]$ 恰含 $z$ 個零，**當且僅當**\n\n$$p_{t-z} + 1 \\;\\le\\; l \\;\\le\\; p_{t-z+1}\\qquad(\\text{需 } 1 \\le z \\le t).$$\n\n*證明*。$[l, r]$ 內的零的個數 $= t - \\#\\{j : p_j < l\\}$。此數等於 $z$ $\\iff$ 恰有 $t-z$ 個零位置嚴格小於 $l$ $\\iff$ $p_{t-z} < l \\le p_{t-z+1}$ ✔（$p$ 嚴格遞增，故「前 $t-z$ 個在左、第 $t-z+1$ 個在右」等價於這個區間條件）。$\\square$\n\n**命題（1 的個數條件）**：$[l, r]$ 的長度為 $r-l+1$，其中 $z$ 個零 ⇒ 1 的個數 $o = r-l+1-z$。條件 $o \\ge z^2$ 等價於\n\n$$l \\;\\le\\; r + 1 - z - z^2.$$\n\n**兩個條件都是「$l$ 的區間限制」，交集仍是區間 ⇒ 合法 $l$ 的個數可以 $O(1)$ 算出**：\n\n$$\\mathrm{cnt}(r, z) \\;=\\; \\max\\Big(0,\\; \\min\\big(p_{t-z+1},\\; r+1-z-z^2\\big) \\;-\\; \\big(p_{t-z}+1\\big) \\;+\\; 1\\Big).$$\n\n**$z = 0$ 的情形單獨處理**：$[l, r]$ 全為 1 $\\iff$ $l > p_t$，且條件 $o \\ge 0$ 恆成立 ⇒ 個數 $= r - p_t$（當 $t = 0$ 時 $p_0 = -1$，個數 $= r+1$ ✔）。\n\n**總答案** $= \\sum_{r=0}^{n-1}\\sum_{z=0}^{\\min(t(r),\\,\\lfloor\\sqrt n\\rfloor)} \\mathrm{cnt}(r, z)$。\n\n*不重不漏*：每個子字串 $[l, r]$ 有唯一的右端點 $r$ 與唯一的零個數 $z$，故恰好在 $(r, z)$ 這一項被計數一次 ✔。由定理 1，$z > \\sqrt n$ 的項全部為 0，跳過它們不影響答案 ✔。\n\n*複雜度*：$O(n\\sqrt n)$。$n = 4\\times10^4$ ⇒ 約 $8\\times10^6$，輕鬆通過 ✔。$\\square$\n\n**這個證明的結構值得記住**：**「把條件逐一翻譯成 $l$ 的區間限制，取交集，$O(1)$ 計數。」** 只要每個條件都是 $l$ 的單調條件，這招就成立——**這也是 sliding_window 章「雙指標」與本節的共同祖先。**\n\n**一個容易寫錯的地方**：$z$ 的上界要同時受 $t(r)$（真的有這麼多零）與 $\\lfloor\\sqrt n\\rfloor$（定理 1）限制。**只寫其中一個，不是漏算就是越界讀 `pos`。**\n\n### 定理 3（整除分塊：$\\lfloor n/i \\rfloor$ 只有 $O(\\sqrt n)$ 種值）\n\n*命題 (a)*：集合 $\\{\\lfloor n/i \\rfloor : 1 \\le i \\le n\\}$ 的大小不超過 $2\\lfloor\\sqrt n\\rfloor$。\n\n*證明*。分兩段：\n- $i \\le \\sqrt n$：至多 $\\lfloor\\sqrt n\\rfloor$ 個 $i$，故至多這麼多個值 ✔。\n- $i > \\sqrt n$：此時 $\\lfloor n/i\\rfloor < \\sqrt n$，取值落在 $\\{0, 1, \\dots, \\lfloor\\sqrt n\\rfloor\\}$ 中，至多 $\\lfloor\\sqrt n\\rfloor + 1$ 個 ✔。\n\n合計 $\\le 2\\lfloor\\sqrt n\\rfloor + 1$ ✔。$\\square$\n\n*命題 (b)（塊的右端點公式）*：設 $q = \\lfloor n/i\\rfloor$，則使 $\\lfloor n/j\\rfloor = q$ 的最大 $j$ 為 $j = \\lfloor n/q \\rfloor$。\n\n*證明*。\n- **$\\lfloor n/\\lfloor n/q\\rfloor\\rfloor = q$**：令 $j = \\lfloor n/q\\rfloor$。由 $q = \\lfloor n/i\\rfloor$ 知 $q \\le n/i$，故 $j = \\lfloor n/q\\rfloor \\ge i$，於是 $\\lfloor n/j\\rfloor \\le \\lfloor n/i\\rfloor = q$。另一方面 $j \\le n/q$ 蘊含 $n/j \\ge q$，故 $\\lfloor n/j\\rfloor \\ge q$。兩者夾出 $\\lfloor n/j\\rfloor = q$ ✔。\n- **$j$ 是最大的**：對 $j' > j = \\lfloor n/q\\rfloor$ 有 $j' \\ge \\lfloor n/q\\rfloor + 1 > n/q$，故 $n/j' < q$，即 $\\lfloor n/j'\\rfloor < q$ ✔。$\\square$\n\n**於是 `for (i = 1; i <= n; i = j + 1) { j = n / (n / i); ... }` 恰好走過每一個「值相同的極大區間」，共 $O(\\sqrt n)$ 次迭代。**\n\n**典型用途**：$\\sum_{i=1}^{n}\\lfloor n/i\\rfloor$、莫比烏斯反演的求和、「有多少個 $i$ 使得 $\\lfloor n/i\\rfloor = k$」。**LeetCode 上直接考的不多，但它是「值只有 $O(\\sqrt n)$ 種」這個模式最乾淨的例子。**\n\n### 定理 4（重／輕元素分治）\n\n*命題*：陣列長度 $n$，稱出現次數 $> \\sqrt n$ 的元素為**重元素**，則重元素至多 $\\lfloor\\sqrt n\\rfloor$ 種。\n\n*證明*。若有 $k$ 種重元素，總出現次數 $> k\\sqrt n$，而總長為 $n$，故 $k < \\sqrt n$ ✔。$\\square$\n\n**這條引理的價值在於它允許「對每種重元素各做一次 $O(n)$ 的處理」**，總計 $O(n\\sqrt n)$。\n\n*典型應用（區間眾數，無修改，離線或線上）*：\n- **重元素**：至多 $\\sqrt n$ 種。對每種 $v$ 預處理前綴出現次數 $c_v[i]$，查詢時 $O(1)$ 得到 $[l,r]$ 內 $v$ 的次數 ⇒ 每個查詢掃過所有重元素 $O(\\sqrt n)$。\n- **輕元素**：出現次數 $\\le \\sqrt n$。若答案是輕元素，則答案的出現次數 $\\le \\sqrt n$。**這一半通常需要別的技巧**（例如「答案要嘛是重元素，要嘛出現次數不超過 $\\sqrt n$，可以用出現位置的列表二分」）。\n\n**誠實的說明**：重／輕分治的「輕」那一半往往不像「重」那一半那麼乾淨，**很多題目只有重元素這一半是根號，輕元素得靠題目的額外性質。** 動手前先確認兩半都有辦法。\n\n**空間代價**：對每種重元素存一份長度 $n$ 的前綴計數，是 $O(n\\sqrt n)$ 的記憶體。$n = 10^5$ ⇒ $3\\times10^7$ 個 `int` = **120 MB，通常不可行**。**實務上改成「重元素的出現位置列表 + 二分」，空間降到 $O(n)$，查詢多一個 $\\log$。**\n\n### 定理 5（操作分塊：把「難以增量維護」的結構定期重建）\n\n*設定*：有一個結構 $D$，重建要 $O(n)$、查詢要 $O(1)$，但**單次修改無法增量維護**（只能重建）。共 $q$ 次操作混合修改與查詢。\n\n*做法*：把操作序列按每 $b$ 個分成一批。**每批開始時重建 $D$（只反映本批之前的所有修改）**；批內的查詢 = $D$ 的答案 + 「本批已發生的 $\\le b$ 個修改」的暴力修正。\n\n*複雜度*：重建 $\\frac{q}{b}\\cdot O(n)$，批內修正 $q \\cdot O(b)$ ⇒ 總 $O\\big(\\frac{qn}{b} + qb\\big)$，取 $b = \\sqrt n$ 得 $O(q\\sqrt n)$ ✔。\n\n*正確性的關鍵前提*：**「$D$ 的答案」與「本批修改的影響」必須能疊加。** 若修改之間會互相干擾（例如區間賦值的後蓋前），暴力修正就不是簡單相加，這招失效。**點名這個前提，才知道什麼時候不能用。**\n\n**這與 11.1 定理 6（定期重建分塊）是同一個想法，只是這裡分塊的對象是「時間」而不是「空間」。** 兩者都屬於本章反覆出現的那個模式：**昂貴的整理工作被攤到 $\\sqrt{\\cdot}$ 次操作上。**\n\n### 定理 6（為什麼分界點永遠在「兩側代價相等處」）\n\n*命題*：設把某個參數 $k$ 分成「$k \\le K$ 用方法 A、$k > K$ 用方法 B」，且方法 A 的代價隨 $K$ 遞增為 $f(K)$、方法 B 的代價隨 $K$ 遞減為 $g(K)$，兩者皆連續單調。則總代價 $\\max\\big(f(K), g(K)\\big)$（或 $f+g$）在 $f(K^\\star) = g(K^\\star)$ 處達到最小。\n\n*證明*。$\\max(f,g)$ 在 $K < K^\\star$ 時等於 $g$（遞減），在 $K > K^\\star$ 時等於 $f$（遞增），故 $K^\\star$ 是唯一極小點 ✔。對於 $f+g$，若兩者形如 $aK$ 與 $b/K$，AM–GM 給出同一個結論（11.1 定理 1）✔。$\\square$\n\n**這條「元定理」統一了本章所有的根號**：\n\n| 技巧 | 方法 A（小） | 方法 B（大） | 平衡點 |\n| --- | --- | --- | --- |\n| 陣列分塊（11.1） | 零頭暴力 $O(s)$ | 整塊摘要 $O(n/s)$ | $s = \\sqrt n$ |\n| 1714 對參數分塊 | 預處理 $O(yn)$ | 暴力 $O(n/y)$ | $y = \\sqrt n$ |\n| 莫隊（11.2） | $l$ 移動 $O(qs)$ | $r$ 移動 $O(n^2/s)$ | $s = n/\\sqrt q$ |\n| 重／輕元素（定理 4） | 重元素各掃一次 | 輕元素次數有限 | $\\sqrt n$ |\n| 操作分塊（定理 5） | 批內修正 $O(qb)$ | 重建 $O(qn/b)$ | $b = \\sqrt n$ |\n\n**看到「一個參數，兩側代價一升一降」，就令它們相等解出分界點。$\\sqrt n$ 只是最常見的解，不是唯一的答案。**\n\n## 逐步演算法\n\n這類題目可拆成下面幾步，逐一搭配程式碼：\n\n:::example 例：要回答「區間內前綴狀態 = v 的個數」\n**1. 想清楚要統計什麼，決定塊摘要的形態。** 是頻次表？排序陣列？前綴狀態計數？這一步決定後面全部。\n\n```cpp\n// 例：要回答「區間內前綴狀態 = v 的個數」\n// 塊摘要就用 value -> count 的頻次表\nvector<unordered_map<int, int>> blockCnt;  // 每塊一張表\n```\n:::\n\n\n\n:::example 建立每塊摘要：i / len 是 i 屬於第幾塊\n**2. 預處理：掃一遍原資料，把每個元素登記進它所屬塊的摘要。**\n\n```cpp\n// 建立每塊摘要：i / len 是 i 屬於第幾塊\nfor (int i = 0; i < n; ++i) {\n    blockCnt[i / len][state[i]]++;\n}\n```\n:::\n\n\n\n:::example 中間整塊：直接從摘要表拿「值為 v 的個數」\n**3. 查詢：整塊查表、零頭暴力。** 兩端不滿一塊的逐元素檢查，中間整塊直接從那張表讀。\n\n```cpp\n// 中間整塊：直接從摘要表拿「值為 v 的個數」\nlong long ans = 0;\nfor (int b = lb; b <= rb; ++b) {\n    auto it = blockCnt[b].find(v);\n    if (it != blockCnt[b].end()) {\n        ans += it->second;\n    }\n}\n// 左右零頭再逐元素補上\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 塊大小仍以 `sqrt(n)` 為預設；摘要太重時可調小塊數來平衡。\n- 零頭一定要逐元素算，別只靠整塊摘要而漏掉邊界。\n- 複雜度：每次查詢 `O(sqrt(n))`（乘上查表常數），整體 `O((n + q) sqrt(n))`。\n\n## C++17 模板\n\n**模板一：3234 統計 1 顯著的子字串（定理 2）**\n\n```cpp\nclass Solution {\npublic:\n    int numberOfSubstrings(string s) {\n        int n = int(s.size());\n        int Z = int(sqrt(double(n))) + 1;          // 零的個數上界（定理 1）\n        vector<int> pos;                            // pos[0] = -1，之後是零的下標\n        pos.push_back(-1);\n        long long ans = 0;\n        for (int r = 0; r < n; r++) {\n            if (s[r] == '0') pos.push_back(r);\n            int t = int(pos.size()) - 1;            // [0, r] 內零的個數\n            // z = 0：全為 1 的子字串\n            ans += r - pos[t];\n            // z >= 1\n            for (int z = 1; z <= min(t, Z); z++) {\n                long long need = 1LL * z * z;\n                long long hi = min<long long>(pos[t - z + 1], r + 1 - z - need);\n                long long lo = pos[t - z] + 1;\n                if (hi >= lo) ans += hi - lo + 1;\n            }\n        }\n        return int(ans);\n    }\n};\n```\n\n**`z` 的上界是 `min(t, Z)`——兩個限制缺一不可**（真的有這麼多零 ∧ 定理 1 的上界）。**`hi` 用 `long long` 是因為 `r + 1 - z - z*z` 可能為負，而它要跟 `pos[...]` 比較。**\n\n**模板二：整除分塊（定理 3）**\n\n```cpp\nlong long sumOfFloorDiv(int n) {                    // 計算 sum_{i=1}^{n} floor(n / i)\n    long long res = 0;\n    for (int i = 1, j; i <= n; i = j + 1) {\n        int v = n / i;\n        j = n / v;                                  // [i, j] 內 n/i 恆為 v（定理 3(b)）\n        res += 1LL * v * (j - i + 1);\n    }\n    return res;\n}\n```\n\n**`j = n / (n / i)` 這一行就是全部。** 迴圈次數 $O(\\sqrt n)$。\n\n**模板三：重元素預處理（定理 4，空間友善版）**\n\n```cpp\nstruct HeavyLight {\n    int n, TH;\n    vector<int> heavyId;                            // 值 -> 重元素編號，非重為 -1\n    vector<vector<int>> occ;                        // 每個重元素的出現位置（遞增）\n\n    explicit HeavyLight(const vector<int>& a, int m) : n(int(a.size())) {\n        TH = max(1, int(sqrt(double(n))));\n        vector<int> cnt(m, 0);\n        for (int x : a) cnt[x]++;\n        heavyId.assign(m, -1);\n        for (int v = 0; v < m; v++)\n            if (cnt[v] > TH) { heavyId[v] = int(occ.size()); occ.emplace_back(); }\n        for (int i = 0; i < n; i++)\n            if (heavyId[a[i]] >= 0) occ[heavyId[a[i]]].push_back(i);\n        // occ.size() <= sqrt(n)（定理 4），總空間 O(n)\n    }\n    // 重元素 v 在 [l, r] 內出現幾次：O(log n)\n    int countIn(int v, int l, int r) const {\n        int id = heavyId[v];\n        if (id < 0) return -1;                      // 不是重元素\n        const auto& o = occ[id];\n        return int(upper_bound(o.begin(), o.end(), r) - lower_bound(o.begin(), o.end(), l));\n    }\n};\n```\n\n**用「出現位置 + 二分」而非「$\\sqrt n$ 份前綴計數」，把空間從 $O(n\\sqrt n)$ 降到 $O(n)$**（定理 4 的空間說明）。**這幾乎總是正確的取捨。**\n\n**模板四：操作分塊骨架（定理 5）**\n\n```cpp\n    const int B = max(1, int(sqrt(double(n))));\n    for (int st = 0; st < q; st += B) {\n        int en = min(q, st + B);\n        rebuild();                                  // O(n)，反映 [0, st) 的所有修改\n        vector<Op> pending;                         // 本批已發生的修改\n        for (int i = st; i < en; i++) {\n            if (ops[i].isUpdate) { pending.push_back(ops[i]); apply(ops[i]); }\n            else {\n                long long base = staticQuery(ops[i]);      // O(1) 或 O(log n)\n                for (auto& u : pending) base += correct(u, ops[i]);   // O(|pending|)\n                ans[ops[i].id] = base;\n            }\n        }\n    }\n```\n\n## 時間與空間複雜度\n\n| 技巧 | 時間 | 空間 |\n| --- | --- | --- |\n| 3234（定理 2） | $O(n\\sqrt n)$ | $O(n)$ |\n| 整除分塊（定理 3） | $O(\\sqrt n)$ 次迭代 | $O(1)$ |\n| 重元素（前綴計數版） | 預處理 $O(n\\sqrt n)$、查詢 $O(\\sqrt n)$ | **$O(n\\sqrt n)$，通常爆** |\n| 重元素（位置 + 二分版） | 預處理 $O(n)$、查詢 $O(\\sqrt n\\log n)$ | $O(n)$ |\n| 操作分塊（定理 5） | $O(q\\sqrt n)$ | $O(n)$ |\n\n**規模感**：\n- $n = 4\\times10^4$（3234）⇒ $n\\sqrt n = 8\\times10^6$，**極寬鬆**；\n- $n = 10^5$ ⇒ $n\\sqrt n = 3.2\\times10^7$，**常數要小**；\n- $n = 10^6$ ⇒ $10^9$，**根號全線失效，必須找 $O(n\\log n)$**。\n\n**「$n\\sqrt n$ 的臨界點在 $n \\approx 10^5$」值得記住**——看到 $n$ 超過這個量級，根號就不再是候選方案。\n\n**空間永遠先算一遍**（定理 4 的教訓）：**「對 $\\sqrt n$ 個東西各存一個長度 $n$ 的陣列」是 $O(n\\sqrt n)$，$n = 10^5$ 時就是 120 MB。** 幾乎總要換成「位置列表 + 二分」。\n\n## 常見錯誤與邊界條件\n\n- **忘了受限量的上界要取兩個限制的最小值**（定理 2）。3234 的 `z` 上界是 `min(t, sqrt(n))`——只寫 `sqrt(n)` 會越界讀 `pos`，只寫 `t` 會退化成 $O(n^2)$。\n- **`pos[0] = -1` 這個哨兵沒放**。$z = t$（子字串含全部零）時要用到 `pos[0]`，沒有哨兵就特判滿天飛。**與 8.4 的 `fa[n] = n+1`、10.1 的序列哨兵是同一個手法。**\n- **負數與型別**。`r + 1 - z - z*z` 可能為負；`z*z` 在 `int` 下當 $z$ 大時也可能溢位（雖然 $z \\le \\sqrt n$ 時安全，但寫成 `1LL * z * z` 更保險）。\n- **整除分塊寫成 `j = n / (n / i) + 1` 或忘了 `i = j + 1`**。前者跳過一個值，後者死迴圈。**照抄 `for (i = 1, j; i <= n; i = j + 1) { j = n / (n / i); }`。**\n- **整除分塊在 `n / i == 0` 時除零**。只有 $i > n$ 才會發生，而迴圈條件是 `i <= n`，所以安全；**但若把上界改成 $m > n$ 就會炸，要寫成 `j = (v ? min(m, n / v) : m)`。**\n- **重元素的空間**（定理 4）。$O(n\\sqrt n)$ 的前綴計數表通常爆記憶體。**改用出現位置 + 二分。**\n- **重／輕分治只想清楚一半**。「重」那半通常很順，**「輕」那半才是真正的難點**。動手前先確認兩半都有辦法。\n- **操作分塊的疊加前提沒檢查**（定理 5）。修改之間互相干擾（區間賦值的後蓋前）時，「靜態答案 + 暴力修正」不成立。\n- **分界點取錯**（定理 6）。$\\sqrt n$ 不是萬用值——**兩側代價不對稱時要重新解方程。** 莫隊的 $n/\\sqrt q$ 就是最好的反例。\n- **在 $n \\ge 10^6$ 時還想用根號**。$n\\sqrt n = 10^9$，沒有救。\n\n## 與相似技巧的比較\n\n- **與 11.1 陣列分塊**：11.1 是「切資料」，本節是「證明某個量本身就 $\\le\\sqrt n$」。**後者不需要任何額外的資料結構，只需要一個算術論證，因此往往更快也更好寫。** 拿到題目先找有沒有這種上界，找不到再考慮切塊。\n- **與 11.2 莫隊**：莫隊是「切查詢」。**三節合起來就是根號的三種對象：資料、查詢、以及「量本身」。**\n- **與 9.7 ST 表的 gcd 性質**：「固定左端點，區間 gcd 至多 $O(\\log V)$ 種值」與本節的「至多 $O(\\sqrt n)$ 種」是同一類論證——**都是「找出被枚舉量的真實上界」**，只是一個是對數、一個是根號。**這類論證的通用問法：這個量每次變化至少付出多少代價？總代價的上界是多少？**\n- **與 2 節前綴和**：3234 的解法本質上是「前綴計數 + 區間交集」，只是多了一層 $z$ 的枚舉。**如果你能把所有條件都翻譯成「$l$ 的區間限制」，剩下的就是 $O(1)$ 算交集。** 這是本節最可遷移的技能。\n- **與 sliding_window 的雙指標**：雙指標處理「條件對 $l$ 單調」的情形，$O(n)$。**本節處理的是「條件對 $l$ 單調，但還有第二個參數 $z$ 要枚舉」的情形**，代價是多一個 $\\sqrt n$。**先問：能不能只用雙指標？**\n- **與 dynamic_programming 的多重背包**：「$\\sum a_i = S$ ⇒ 不同重量至多 $O(\\sqrt S)$」（推論 1.1）是多重背包能被優化的根據之一。**同一條引理，跨章出現。**\n- **與位元壓縮（bitset）**：另一種常見的「常數優化到能過」的手段，$O(n^2/64)$。**與根號的定位相似（都是把不可行的暴力壓進時限），但 bitset 依賴的是機器字長而非數學上界。** $n \\le 5\\times10^4$ 時 $n^2/64 \\approx 4\\times10^7$，與 $n\\sqrt n$ 是同一個量級——**兩者常常是同一題的兩種解法。**\n\n## 代表例題\n\n**題號**：3234\n\n要把區間量改寫成前綴狀態之差；若是計數題，重點是查找過去需要的前綴值。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 3234 | [統計 1 顯著的字串的數量](https://leetcode.cn/problems/count-the-number-of-substrings-with-dominant-ones) | 2557 |\n\n## 本節重點速查\n\n- **核心引理（定理 1）**：$x^2 \\le n$ 或 $xy \\le n$ ⇒ 至少一邊 $\\le \\sqrt n$。**看到題目條件裡的平方、乘積、總和限制，先算它蘊含什麼上界——那往往就是解法。**\n- **四個常用上界（推論 1.1）**：$\\sum a_i = S$ 的相異正整數至多 $O(\\sqrt S)$ 種；出現次數 $>\\sqrt n$ 的元素至多 $\\sqrt n$ 種；因數成對出現故枚舉 $O(\\sqrt n)$；$\\lfloor n/i\\rfloor$ 至多 $2\\sqrt n$ 種值。\n- **3234（定理 2）**：$o \\ge z^2$ ⇒ $z \\le \\sqrt n$。固定 $(r, z)$，**兩個條件都是 $l$ 的區間限制**（恰含 $z$ 個零 ⇒ $l \\in (p_{t-z},\\, p_{t-z+1}]$；$o\\ge z^2$ ⇒ $l \\le r+1-z-z^2$），取交集 $O(1)$ 計數。**$z$ 上界 `min(t, sqrt(n))`，`pos[0] = -1` 哨兵，`long long`。**\n- **整除分塊（定理 3）**：`for (i = 1, j; i <= n; i = j + 1) j = n / (n / i);`。塊右端 $= \\lfloor n/\\lfloor n/i\\rfloor\\rfloor$，$O(\\sqrt n)$ 次迭代。\n- **重／輕分治（定理 4）**：重元素至多 $\\sqrt n$ 種 ⇒ 各掃一遍 $O(n\\sqrt n)$。**空間務必用「出現位置 + 二分」（$O(n)$）而非「$\\sqrt n$ 份前綴計數」（$O(n\\sqrt n)$，會爆）。** 「輕」那一半通常才是難點。\n- **操作分塊（定理 5）**：每 $\\sqrt n$ 個操作重建一次靜態結構，批內查詢 = 靜態答案 + $O(\\sqrt n)$ 個修改的暴力修正。**前提是兩者可疊加**（區間賦值不行）。\n- **元定理（定理 6）**：**一個參數、兩側代價一升一降 ⇒ 令兩者相等解出分界點。** $\\sqrt n$ 只是最常見的解；莫隊的 $n/\\sqrt q$ 說明它不是唯一。\n- **規模感**：$n\\sqrt n$ 在 $n = 10^5$ 是 $3\\times10^7$（要小常數）、$n = 10^6$ 是 $10^9$（**放棄根號**）。**空間永遠先算一遍。**\n",
        },
      ],
    },
    {
      id: 74,
      title: "12. 專題：離線演算法",
      src: "https://leetcode.cn/circle/discuss/mOr1u6/",
      summary:
        "透過改變回答詢問的順序，使問題更容易處理。\n\n> 相應的，**線上演算法**就是按照 queries 的順序一個一個處理。",
      children: [
        {
          id: 75,
          title: "12.1 基礎",
          summary:
            "## 這個技術解決什麼問題\n\n一般我們預設「查詢來一個、答一個」。但很多題目**一次把所有查詢都給你**——這時你沒有義務按原順序回答。\n\n**離線演算法的全部想法只有一句：**\n\n> **先讀完所有查詢，重新排序，讓「回答查詢」與「建立資料」共用同一次掃描。**\n\n最典型的形態是**帶門檻的查詢**：\n\n- 「所有邊權 $<$ `limit` 時 $u$、$v$ 連通嗎？」（1697）\n- 「價格 $\\le$ `q` 的物品中最大的美麗值？」（2070）\n- 「大小 $\\ge$ `minSize` 的房間中，編號離 `preferred` 最近的？」（1847）\n\n**天真做法是每個查詢重掃一遍資料，$O(nq)$。** 但注意：**門檻越寬，合法的元素只增不減。** 於是把查詢按門檻排序，一邊放寬門檻一邊往資料結構裡加元素——**每個元素只會被加入一次**，總計 $O((n+q)\\log n)$。\n\n**辨識訊號**：\n- 每個查詢帶一個門檻，且**查詢之間互不影響、可以任意順序回答**（1697、1847、2070）；\n- **門檻具單調性**：門檻越寬，合法元素只增不減 ⇒ **可以「只加不刪」**；\n- 暴力是每個查詢重新篩一遍（$O(nq)$），而排序後漸進加入降到 $O((n+q)\\log n)$；\n- 題目一次給你整個 `queries` 陣列（**而不是一個 `query()` 介面**）。\n\n**最後一條是最實用的辨識法。** 題目給的是陣列還是介面，直接決定了離線是否可用——**看到 `vector<vector<int>>& queries` 就該問：排序它會不會讓事情變簡單？**\n\n## 辨識題型的訊號\n\n看到下面這些特徵，可以往「離線按門檻掃描」想：\n\n- **每個查詢帶一個門檻，且查詢之間互不影響、可以任意順序回答。** 像「檢查邊長度限制的路徑是否存在」（1697）、「每一個查詢的最大美麗值」（2070）、「最近的房間」（1847）。\n- **門檻具單調性：門檻越寬，合法元素只增不減，** 因此可以「只加不刪」。\n- **暴力是每個查詢都重新篩一遍元素（`O(n·q)`）；** 排序後漸進加入把它降到 `O((n+q) log n)`。\n\n確認自己有沒有套對，問三個問題：\n\n1. 每個查詢的「門檻」是什麼？它能拿來排序嗎？\n2. 門檻變寬時，合法元素是不是只增不減（可以只加不刪）？\n3. 我有沒有記住原查詢 id，最後把答案回填到原順序？\n\n## 核心想法與直覺\n\n本節要解決的場景是：有一堆查詢，每個查詢都附帶一個「門檻」——例如「只看權重不超過 w 的邊」「只看大小至少 k 的房間」「只看價格不超過 p 的商品」。如果每個查詢都重新把符合門檻的元素找一遍再回答，會做很多重複工作。\n\n離線演算法（這裡指「按門檻排序」這一類）的核心想法是：**既然查詢可以不照順序回答，那就把查詢按門檻排序，再從寬到嚴（或從嚴到寬）掃過去；每個元素只在「第一次變得合法」的那一刻加入資料結構一次，之後就一直留著。**\n\n:::example 把元素和查詢都按門檻排序，用一個指標漸進加入元素\n關鍵在於門檻的「單調性」：如果查詢按門檻由小到大處理，那麼門檻越大，合法的元素只會越來越多、不會變少。於是我們用一個指標，把「已經合法」的元素逐步加進來，永遠不必移除。\n\n```cpp\n// 把元素和查詢都按門檻排序，用一個指標漸進加入元素\nsort(items.begin(), items.end());     // 元素按門檻（如價格）升冪\nsort(queries.begin(), queries.end(),  // 查詢也按門檻升冪\n          [](auto& a, auto& b) { return a.limit < b.limit; });\n```\n:::\n\n\n\n:::example 雙指標：先把所有合法元素加入，再回答當前查詢\n處理每個查詢前，先把所有「門檻已達標」的元素都加進資料結構，然後直接讀答案——因為此刻結構裡裝的，正好就是這個查詢看得到的全部候選。\n\n```cpp\n// 雙指標：先把所有合法元素加入，再回答當前查詢\nint j = 0;\nfor (auto& q : queries) {  // 查詢已按門檻升冪\n    while (j < items.size() && items[j].key <= q.limit) {\n        ds.add(items[j]);  // 第一次合法就加入，不再移除\n        ++j;\n    }\n    ans[q.id] = ds.query(q);  // 結構內容 = 此查詢的候選\n}\n```\n:::\n\n\n\n因為每個元素一生只被加入一次，總工作量大幅下降；若資料結構操作是 `O(log n)`，整體就是 `O((n + q) log n)`，遠快於每查詢都重掃的 `O(n·q)`。\n\n## 狀態／資料結構定義\n\n**離線的骨架永遠是這三步：**\n\n```cpp\n// 1. 給每個查詢記住原始編號\nvector<int> ord(q);\niota(ord.begin(), ord.end(), 0);\n\n// 2. 查詢與資料各自按「同一個維度」排序\nsort(ord.begin(), ord.end(), [&](int x, int y) { return key(queries[x]) < key(queries[y]); });\nsort(items.begin(), items.end(), [](const Item& a, const Item& b) { return a.key < b.key; });\n\n// 3. 雙指標：一邊放寬門檻、一邊把新合法的元素加進結構\nvector<int> ans(q);\nint p = 0;\nfor (int id : ord) {\n    while (p < n && items[p].key <= key(queries[id])) { structure.add(items[p]); p++; }\n    ans[id] = structure.query(queries[id]);       // 一定要寫回 ans[id]，不是 push_back\n}\nreturn ans;\n```\n\n**三件事各有陷阱：**\n\n| 步驟 | 陷阱 |\n| --- | --- |\n| 記 `id` | 忘了記，排序後就無法還原順序 |\n| 同維度排序 | 資料與查詢的排序鍵必須是**同一個量**，且方向一致 |\n| 雙指標 | `<=` 還是 `<`，取決於門檻是否嚴格（見「常見錯誤」） |\n\n**資料結構的選擇取決於「只加不刪」還是「要加要刪」**（定理 2 vs 定理 6）：\n\n| 情境 | 可用的結構 |\n| --- | --- |\n| **只加不刪**（門檻單調） | **並查集**、前綴 max、單調棧、樹狀陣列、`set` |\n| **要加要刪**（DFS 上的離線） | 可撤銷並查集、計數陣列、字典樹（計數版） |\n\n**「只加不刪」是離線給的最大紅利**——並查集根本不支援刪除，而它恰好是 1697 這類題目的最佳工具。\n\n## 不變量或正確性證明\n\n### 定理 1（重排查詢的合法性）\n\n**不變量 (O)**：*若查詢之間沒有依賴——即第 $i$ 個查詢的答案不依賴第 $j$ 個查詢的答案（$j < i$），且查詢本身不修改資料——則以任意順序回答，再按 `id` 寫回，結果與按原順序回答相同。*\n\n*證明*。每個查詢的答案是「輸入資料」的函數，而輸入資料在整個過程中不變（查詢是唯讀的）⇒ 答案與計算時機無關 ✔。$\\square$\n\n**看似顯然，但它有兩個真實的失效情形，必須點名**：\n\n1. **強制線上（forced online）**：題目要求「第 $i$ 個查詢的參數要與第 $i-1$ 個答案做 XOR 解碼」。**這是出題者刻意封殺離線的手段**，此時只能上主席樹（9.6）、Kruskal 重構樹（定理 4）這類線上結構。\n2. **查詢與修改交錯**：例如「查詢 → 修改 → 查詢」，重排就會讓查詢看到錯誤的資料版本。**此時要麼把「時間」也納入排序鍵（帶修莫隊、CDQ 分治），要麼放棄離線。**\n\n**LeetCode 上絕大多數 `queries` 陣列型的題目都滿足 (O)。** 但看到「上一個答案」四個字就要警覺。\n\n### 定理 2（單調門檻 ⇒ 只加不刪）\n\n*設定*：每個查詢 $Q_i$ 帶門檻 $\\theta_i$，合法元素集合為 $S(\\theta) = \\{\\,e : \\mathrm{key}(e) \\le \\theta\\,\\}$。\n\n*命題*：$\\theta \\le \\theta' \\Rightarrow S(\\theta) \\subseteq S(\\theta')$。因此若把查詢按 $\\theta$ **遞增**排序，資料結構**只需要支援插入**，且每個元素**恰好被插入一次**。\n\n*證明*。$S$ 的單調性由定義直接得出 ✔。處理第 $k$ 個（排序後的）查詢時，結構中的元素恰為 $S(\\theta_{(k)})$；處理下一個時只需補上 $S(\\theta_{(k+1)}) \\setminus S(\\theta_{(k)})$——**這些元素從未被插入過**（因為 key 落在 $(\\theta_{(k)}, \\theta_{(k+1)}]$，之前的門檻都不夠寬）✔。故雙指標 `p` 單調不減，總插入次數 $= n$ ✔。$\\square$\n\n**這個推論的價值遠超複雜度**：\n\n> **「只加不刪」讓一批不支援刪除的結構變得可用。**\n\n- **並查集**：合併不可逆（8 節），但離線後根本不需要拆開 ✔；\n- **前綴 max / 前綴和**：只能追加 ✔；\n- **單調棧**：彈出即永久丟棄（4.7），與只加不刪相容 ✔。\n\n**這正是「昂貴的迭代對應一個不可逆的狀態變化」那個模式的另一面**——離線排序保證了狀態變化永遠朝同一個方向，於是不可逆反而成了優點。**本章第十次遇到這個主題了。**\n\n### 定理 3（1697：瓶頸路徑 $\\iff$ 子圖連通）\n\n*問題*：給定無向帶權圖與查詢 $(u, v, \\mathit{limit})$，問是否存在 $u \\to v$ 的路徑，**使得路徑上每條邊的權重都嚴格小於 $\\mathit{limit}$**。\n\n**命題**：這樣的路徑存在 $\\iff$ 在只保留「權重 $< \\mathit{limit}$」的邊所構成的子圖 $G_{<\\mathit{limit}}$ 中，$u$ 與 $v$ 連通。\n\n*證明*。\n- **($\\Rightarrow$)** 該路徑的每條邊權重都 $< \\mathit{limit}$，故整條路徑都在 $G_{<\\mathit{limit}}$ 中，$u$、$v$ 連通 ✔。\n- **($\\Leftarrow$)** 若在 $G_{<\\mathit{limit}}$ 中連通，取任一條連接 $u$、$v$ 的路徑；它的每條邊都屬於 $G_{<\\mathit{limit}}$，即權重 $< \\mathit{limit}$ ✔。$\\square$\n\n**這個等價依賴的前提必須點名**：**條件是「路徑上的**每一條**邊都滿足」，而不是「路徑的**總長**滿足」。**\n\n- **每條邊都要滿足**（瓶頸型）⇒ 邊的合法性與路徑無關 ⇒ 可以逐條加入 ⇒ 並查集 ✔；\n- **總長要滿足**（最短路型）⇒ 一條邊是否「該用」取決於整條路徑 ⇒ **這個等價完全失效**，得用 Dijkstra 之類的方法 ✘。\n\n**這是離線題最常見的誤判：看到「limit」就以為能排序加邊。先確認限制是加在「每個元素」上，還是加在「整體」上。**\n\n*演算法*：邊按權重遞增排序、查詢按 $\\mathit{limit}$ 遞增排序，雙指標把權重 $< \\mathit{limit}$ 的邊逐一 `union`，然後 `find(u) == find(v)` ✔。由定理 2，每條邊只加一次，總計 $O(m\\log m + q\\log q + (m+q)\\alpha)$。\n\n**嚴格 vs 非嚴格**：1697 要求「嚴格小於」，所以加邊的條件是 `edges[p][2] < limit`（不是 `<=`）。**這一個字元就是 WA 與 AC 的差別。**\n\n### 定理 4（同一題的線上解法：Kruskal 重構樹）\n\n*命題（瓶頸路徑定理）*：在連通無向圖中，$u$ 到 $v$ 的所有路徑裡，**「路徑上最大邊權」的最小值**，等於**最小生成樹上 $u$ 到 $v$ 唯一路徑的最大邊權**。\n\n*證明*。記 MST 上 $u$–$v$ 路徑的最大邊為 $e$，權重 $w(e)$。\n\n- **($\\le$)** MST 上的那條路徑本身就是一條路徑，其最大邊權為 $w(e)$，故最小值 $\\le w(e)$ ✔。\n- **($\\ge$)** 反設存在一條路徑 $P$，其所有邊權 $< w(e)$。把 $e$ 從 MST 中刪去，MST 分成兩個連通塊 $A \\ni u$、$B \\ni v$（$e$ 在 $u$–$v$ 路徑上，故 $u$、$v$ 被分開）。路徑 $P$ 從 $u$ 走到 $v$，必然有一條邊 $f$ 跨越 $A$ 與 $B$，且 $w(f) < w(e)$。那麼把 MST 中的 $e$ 換成 $f$ 仍是生成樹，且總權重更小——**與 MST 的最小性矛盾** ✔。$\\square$\n\n**於是 1697 也能線上回答**：建 Kruskal 重構樹（按邊權遞增合併，每次合併新建一個權重為 $w$ 的內部節點），則 $u$、$v$ 的瓶頸值 $=$ 它們在重構樹上的 LCA 的權重，查詢 $O(\\log n)$。\n\n**為什麼還是推薦離線？** 離線版只要「排序 + 並查集」，十行；重構樹版要建樹 + 倍增 LCA，五十行。**除非題目強制線上（定理 1 的失效情形），否則離線總是更划算。**\n\n**但知道線上解法的存在很重要**：它告訴你「離線不是唯一出路」，也讓你在遇到強制線上時不至於束手無策。\n\n### 定理 5（離線的一般框架：把一個維度變成時間）\n\n**觀察**：定理 3 的模式可以抽象成——\n\n> **查詢有兩個條件 $(A, B)$。把 $A$ 交給掃描順序（時間），把 $B$ 交給資料結構。**\n\n| 題目 | 交給掃描順序的 $A$ | 交給資料結構的 $B$ |\n| --- | --- | --- |\n| 1697 | 邊權 $<$ limit | $u$、$v$ 是否連通（並查集） |\n| 2070 | 價格 $\\le$ query | 美麗值的最大值（前綴 max） |\n| 1847 | 房間大小 $\\ge$ minSize | 編號離 preferred 最近（`set`） |\n| 9.2 逆序對 | 下標 $j < i$ | 值 $a_j > a_i$（樹狀陣列） |\n| 2940 | 建築高度的單調關係 | 最小的合法下標（堆／線段樹二分） |\n\n**這與 9.2 定理 1 的「二維偏序」是同一件事，一字不差**：*「兩個條件，一個交給掃描順序，另一個交給資料結構。」*\n\n**框架的正確性**：處理排序後的第 $k$ 個查詢時，結構中恰好是「滿足條件 $A$」的全部元素（由定理 2 的單調性），故對結構做關於 $B$ 的查詢，得到的正是「同時滿足 $A$ 與 $B$」的答案 ✔。\n\n**推論 5.1（維度不夠用的時候）**：**三個條件就需要 CDQ 分治或樹套樹**——一個維度給排序、一個給分治、一個給資料結構。**看到三維偏序，離線的單指標框架就不夠了。**\n\n**推論 5.2（排序方向）**：$B$ 的形式決定了 $A$ 該遞增還是遞減排序。「$\\ge$ 門檻」就遞減（1847 的房間大小），「$\\le$ 門檻」就遞增（2070 的價格）。**寫之前先想清楚「放寬門檻」是往哪個方向走。**\n\n### 定理 6（DFS 上的離線：需要撤銷的情形）\n\n*設定*（1938 型）：查詢掛在樹的節點上，答案取決於「根到該節點的路徑上有哪些值」。\n\n*做法*：對樹做 DFS，**進入節點時把它的值加入結構、離開時移除**；在節點處回答所有掛在它上面的查詢。\n\n**不變量 (P)**：*在 DFS 訪問節點 $x$ 的時刻，結構中恰好是「根到 $x$ 的路徑」上所有節點的值。*\n\n*證明（對遞迴深度歸納）*。進入根時結構為空加上根的值 ✔。設進入 $x$ 前結構為「根到 $\\mathrm{fa}(x)$ 的路徑」，加入 $x$ 的值後即為「根到 $x$」✔；遞迴處理每個子節點，由歸納假設它們各自維持 (P)，且**每個子節點離開時都把自己的貢獻撤銷了**，故回到 $x$ 時結構恢復為「根到 $x$」✔；最後離開 $x$ 前移除 $x$ 的值，恢復為「根到 $\\mathrm{fa}(x)$」✔。$\\square$\n\n**這裡與定理 2 的「只加不刪」形成鮮明對比**：\n\n| | 門檻型離線（定理 2） | DFS 型離線（定理 6） |\n| --- | --- | --- |\n| 操作 | 只加 | 加 + **撤銷** |\n| 每個元素被操作 | 1 次 | 2 次（進入、離開） |\n| 可用結構 | 並查集、單調棧 | 計數陣列、可撤銷並查集（**不能路徑壓縮**） |\n\n**「可撤銷」這個要求會排除掉一些結構。** 特別是並查集：**可撤銷版必須放棄路徑壓縮**（壓縮改動了無法還原的資訊），只保留按秩合併，複雜度變成 $O(\\log n)$——**這與 9.6 推論 5.1 的可持久化並查集是同一個限制。**\n\n**Tarjan 離線 LCA 也是這個模式**：DFS 時用並查集把已完成的子樹指向父節點，離開時回答掛在該節點的 LCA 查詢。**認出「DFS + 進出維護 + 就地回答」這個骨架，一整族題目就通了。**\n\n## 逐步演算法\n\n離線演算法題目可拆成下面幾步，逐一搭配程式碼：\n\n:::example 元素與查詢都記住原 id，方便最後回填\n**1. 找出「門檻」並決定掃描方向。** 看清楚查詢限制的是什麼（邊權上限、尺寸下限、價格上限…），它就是排序鍵。\n\n```cpp\n// 元素與查詢都記住原 id，方便最後回填\nstruct Item {\n    int key, val;\n};\nstruct Query {\n    int limit, id;\n};\n```\n:::\n\n\n\n**2. 雙排序：元素與查詢都按門檻排。** 注意方向要一致：若查詢是「上限」就都升冪，「下限」就都降冪。\n\n:::example 例：用 set 維護已加入的房間 size，回答「最接近 preferred」\n**3. 用指標漸進加入元素，再回答查詢。** 就是上面那段 while + query：先補齊所有合法元素，再讀答案。\n\n```cpp\n// 例：用 set 維護已加入的房間 size，回答「最接近 preferred」\nmultiset<int> alive;\n// add: alive.insert(room.size);\n// query: 用 lower_bound 找最接近 q.preferred 的值\n```\n:::\n\n\n\n:::example 回填：用原 id 還原題目要求的輸出順序\n**4. 按原 id 回填答案。** 因為排序打亂了順序，最後要用 `q.id` 把答案放回原位置。\n\n```cpp\n// 回填：用原 id 還原題目要求的輸出順序\nvector<int> ans(q_count);\n// 處理時：ans[q.id] = 該查詢的答案;\n```\n:::\n\n\n\n**5. 邊界與複雜度。**\n\n- 離線排序不能改變「有時間先後依賴」的語意；若查詢必須看「截至某時刻」的狀態，要把時間也當成一個維度一起排。\n- 門檻方向弄反會把該加的元素漏掉，務必確認排序方向與加入條件一致。\n- 複雜度：每元素加入一次、每查詢回答一次，整體 `O((n + q) log n)`。\n\n## C++17 模板\n\n**模板一：門檻型離線的通用骨架**\n\n```cpp\n// items：資料，按 key 遞增排序；queries：查詢，帶門檻 theta\nvector<int> offline(vector<Item> items, vector<Query> queries) {\n    int n = int(items.size()), q = int(queries.size());\n    sort(items.begin(), items.end(),\n         [](const Item& a, const Item& b) { return a.key < b.key; });\n    vector<int> ord(q);\n    iota(ord.begin(), ord.end(), 0);\n    sort(ord.begin(), ord.end(),\n         [&](int x, int y) { return queries[x].theta < queries[y].theta; });\n\n    Structure st;\n    vector<int> ans(q);\n    int p = 0;\n    for (int id : ord) {\n        while (p < n && items[p].key <= queries[id].theta) st.add(items[p++]);\n        ans[id] = st.query(queries[id]);          // 寫回 ans[id]，不是 push_back\n    }\n    return ans;\n}\n```\n\n**模板二：1697 檢查邊長度限制的路徑是否存在（定理 3）**\n\n```cpp\nclass Solution {\n    vector<int> fa;\n    int find(int x) { while (fa[x] != x) x = fa[x] = fa[fa[x]]; return x; }\npublic:\n    vector<bool> distanceLimitedPathsExist(int n, vector<vector<int>>& edgeList,\n                                           vector<vector<int>>& queries) {\n        sort(edgeList.begin(), edgeList.end(),\n             [](const vector<int>& a, const vector<int>& b) { return a[2] < b[2]; });\n        int q = int(queries.size());\n        vector<int> ord(q);\n        iota(ord.begin(), ord.end(), 0);\n        sort(ord.begin(), ord.end(),\n             [&](int x, int y) { return queries[x][2] < queries[y][2]; });\n\n        fa.resize(n);\n        iota(fa.begin(), fa.end(), 0);\n        vector<bool> ans(q);\n        int p = 0;\n        for (int id : ord) {\n            int limit = queries[id][2];\n            while (p < int(edgeList.size()) && edgeList[p][2] < limit) {   // 嚴格小於\n                fa[find(edgeList[p][0])] = find(edgeList[p][1]);\n                p++;\n            }\n            ans[id] = (find(queries[id][0]) == find(queries[id][1]));\n        }\n        return ans;\n    }\n};\n```\n\n**`edgeList[p][2] < limit` 的 `<` 對應題目的「嚴格小於」。** 若題目改成「不超過」就要換成 `<=`——**這是本節最高頻的 off-by-one。**\n\n**模板三：2070 每一個查詢的最大美麗值（前綴 max，最輕量的離線）**\n\n```cpp\nclass Solution {\npublic:\n    vector<int> maximumBeauty(vector<vector<int>>& items, vector<int>& queries) {\n        sort(items.begin(), items.end(),\n             [](const vector<int>& a, const vector<int>& b) { return a[0] < b[0]; });\n        for (size_t i = 1; i < items.size(); i++)                 // 前綴 max\n            items[i][1] = max(items[i][1], items[i - 1][1]);\n\n        int q = int(queries.size());\n        vector<int> ord(q), ans(q);\n        iota(ord.begin(), ord.end(), 0);\n        sort(ord.begin(), ord.end(), [&](int x, int y) { return queries[x] < queries[y]; });\n\n        int p = 0, best = 0;\n        for (int id : ord) {\n            while (p < int(items.size()) && items[p][0] <= queries[id]) best = items[p++][1];\n            ans[id] = best;\n        }\n        return ans;\n    }\n};\n```\n\n**這題也能不排序查詢，直接對每個查詢二分前綴 max 陣列（$O(q\\log n)$）。** **當「資料結構」退化成一個變數或一個陣列時，離線排序的收益就只剩常數——此時二分往往更好寫。**\n\n**模板四：DFS 型離線的骨架（定理 6）**\n\n```cpp\n    vector<vector<int>> g, qs;      // qs[x]：掛在節點 x 上的查詢編號\n    void dfs(int x) {\n        st.add(val[x]);                          // 進入：加入\n        for (int id : qs[x]) ans[id] = st.query(qryParam[id]);\n        for (int y : g[x]) dfs(y);\n        st.remove(val[x]);                       // 離開：撤銷（不變量 (P)）\n    }\n```\n\n**`add` 與 `remove` 必須嚴格互逆**（與 11.2 莫隊的要求一模一樣）。**字典樹用「每個節點掛一個計數」而非「刪除節點」來實現撤銷。**\n\n## 時間與空間複雜度\n\n| 步驟 | 時間 |\n| --- | --- |\n| 排序資料 | $O(n\\log n)$ |\n| 排序查詢 | $O(q\\log q)$ |\n| 雙指標插入（定理 2） | 每個元素恰一次，共 $O(n \\cdot C_{\\text{add}})$ |\n| 回答查詢 | $O(q \\cdot C_{\\text{query}})$ |\n| **總計** | $O\\big((n+q)\\log(n+q)\\big)$（結構為並查集／BIT 時） |\n\n**與暴力的對比**：$O(nq)$ vs $O((n+q)\\log)$。$n = q = 10^5$ ⇒ $10^{10}$ vs $3.4\\times10^6$，**四個數量級。**\n\n**各題的結構與複雜度**：\n\n| 題目 | 結構 | 複雜度 |\n| --- | --- | --- |\n| 1697 | 並查集 | $O((m+q)\\log + m\\,\\alpha)$ |\n| 2070 | 前綴 max | $O((n+q)\\log)$ |\n| 1847 | `set`（按編號） | $O((n+q)\\log n)$ |\n| 1851 | 小根堆（按區間長度） | $O((n+q)\\log n)$ |\n| 1938 | 01-字典樹（計數版）+ DFS | $O((n+q)\\cdot 18)$ |\n\n**空間**：離線一定要額外存下所有查詢與答案，$O(q)$。**這是離線唯一的固定代價，通常無關緊要。**\n\n**排序是瓶頸**：實務上 $O((n+q)\\log)$ 裡的 $\\log$ 大多來自兩次排序，而不是資料結構。**若門檻是小整數，用計數排序可以把 $\\log$ 拿掉。**\n\n## 常見錯誤與邊界條件\n\n- **忘了記原始 `id`**。排序後就再也還不回去。**先寫 `iota(ord.begin(), ord.end(), 0)`，再排序 `ord` 而不是排序 `queries` 本身**——這樣 `queries` 的下標仍然是原始編號。\n- **答案用 `push_back` 而非 `ans[id] =`**。順序全亂，而且不會報錯。\n- **嚴格與非嚴格搞混**（定理 3）。`< limit` vs `<= limit` 是 WA 與 AC 的差別。**把題目的原文抄到註解裡。**\n- **資料與查詢的排序方向不一致**（推論 5.2）。「$\\ge$ 門檻」要遞減排序，「$\\le$ 門檻」要遞增。**寫錯的症狀是「答案總是 0 或總是最大值」。**\n- **相同門檻時的處理順序**。若門檻相等，**必須先把元素全部加完再回答查詢**。用 `while (p < n && key[p] <= theta)` 這種寫法自然滿足；但若寫成「先答再加」就漏了邊界上的元素。\n- **雙指標寫成每次從頭掃**。`p` 必須是外層變數，不能在迴圈內重置——**否則複雜度退回 $O(nq)$，而答案是對的**（最陰險的效能錯誤）。\n- **把「總長限制」當成「每邊限制」**（定理 3 的前提）。看到 limit 就排序加邊，但題目其實問的是路徑總權重——**等價性失效，答案完全錯。**\n- **強制線上卻用了離線**（定理 1）。題目說「查詢參數要與上一個答案 XOR」時，離線不可用。\n- **DFS 型離線忘了撤銷**（定理 6）。`remove` 漏寫或與 `add` 不互逆，兄弟子樹之間就會互相污染。\n- **可撤銷並查集用了路徑壓縮**。壓縮不可還原。**要撤銷就只能按秩合併 + 記錄操作棧。**\n- **遞迴 DFS 爆棧**。樹退化成鏈且 $n = 10^5$ 時要改迭代或手動開棧。\n- **離線後忘了處理「沒有任何元素合法」的查詢**。例如 2070 中所有物品都比查詢貴 ⇒ 答案是 0 而非未定義。**結構要有合理的初始值。**\n\n## 與相似技巧的比較\n\n- **與 9.6 主席樹**：**主席樹是「離線技巧的線上化」**——它把「按前綴建版本」這件事持久化下來，於是不需要排序查詢。**能離線就離線：空間 $O(n)$ 對比 $O(n\\log n)$，程式碼短得多。**\n- **與定理 4 的 Kruskal 重構樹**：同一題的線上解法。**離線 = 排序 + 並查集（10 行）；線上 = 重構樹 + 倍增 LCA（50 行）。** 只有強制線上時才值得。\n- **與 11.2 莫隊**：莫隊也是離線，但它處理的是「兩個指標都要來回移動」的情形，$O(n\\sqrt q)$。**本節的門檻型離線只需要一個單調前進的指標，$O((n+q)\\log n)$——快一個檔次。** **拿到離線題先問：能不能只用一個指標？能的話絕不上莫隊。**\n- **與 9.2 逆序對**：9.2 定理 1 的「兩個條件，一個交給掃描順序，另一個交給資料結構」就是本節定理 5 的原型。**逆序對是離線思想最早出現的地方，只是那裡的「查詢」是隱含的（每個下標都是一個查詢）。**\n- **與 CDQ 分治／整體二分**：處理三維偏序或帶修改的離線問題（推論 5.1）。**本節的單指標框架處理二維；三維就要多一層分治。** 整體二分則是「對所有查詢同時二分答案」，空間 $O(n)$，是主席樹的離線替代品。\n- **與掃描線（9.4 定理 5）**：掃描線就是離線——把二維問題的一維交給掃描順序。**矩形面積並、區間覆蓋計數都是這個框架，只是資料結構換成了線段樹。**\n- **與 8.5 定理 4（1851 的區間並查集解法）**：1851 有兩種離線解法——「查詢按值排序 + 小根堆」與「排序後用區間並查集」。**兩者都靠排序製造單調性，差別只在資料結構。** 這說明離線是一個框架，結構可以替換。\n- **與 9.3 定理 4（2940 的線段樹二分）**：2940 既可以離線（按高度排序 + 單調棧／堆），也可以線上（線段樹上二分）。**又一組「離線更短、線上更通用」的對照。**\n- **與貪心的排序**：很多貪心也以排序開場，但**貪心的排序是為了證明交換論證，離線的排序是為了複用計算。** 兩者的正確性論證完全不同——**別把「排序了就對」當成證明。**\n\n## 代表例題\n\n**題號**：1697、1847、1851、1938、2070\n\n要把區間量改寫成前綴狀態之差；若是計數題，重點是查找過去需要的前綴值。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1697 | [檢查邊長度限制的路徑是否存在](https://leetcode.cn/problems/checking-existence-of-edge-length-limited-paths) | 2300 |\n| 1847 | [最近的房間](https://leetcode.cn/problems/closest-room) | 2082 |\n| 1851 | [包含每個查詢的最小區間](https://leetcode.cn/problems/minimum-interval-to-include-each-query) | 2286 |\n| 1938 | [查詢最大基因差](https://leetcode.cn/problems/maximum-genetic-difference-query) | 2503 |\n| 2070 | [每一個查詢的最大美麗值](https://leetcode.cn/problems/most-beautiful-item-for-each-query) | 1724 |\n| 2343 | [裁剪數字後查詢第 K 小的數字](https://leetcode.cn/problems/query-kth-smallest-trimmed-number) | 1652 |\n| 2503 | [矩陣查詢可獲得的最大分數](https://leetcode.cn/problems/maximum-number-of-points-from-grid-queries) | 2196 |\n| 2736 | [最大和查詢](https://leetcode.cn/problems/maximum-sum-queries) | 2533 |\n| 2747 | [統計沒有收到請求的伺服器數目](https://leetcode.cn/problems/count-zero-request-servers) | 2405 |\n| 2940 | [找到 Alice 和 Bob 可以相遇的建築](https://leetcode.cn/problems/find-building-where-alice-and-bob-can-meet) | 2327 |\n| 3382 | [用點構造面積最大的矩形 II](https://leetcode.cn/problems/maximum-area-rectangle-with-point-constraints-ii) | 2723 |\n| 3590 | [第 K 小的路徑異或和](https://leetcode.cn/problems/kth-smallest-path-xor-sum) | 2646 |\n| 3607 | [電網維護](https://leetcode.cn/problems/power-grid-maintenance) | 1700 |\n\n## 本節重點速查\n\n- **三步骨架**：**記 `id` → 資料與查詢按同一維度排序 → 雙指標邊放寬門檻邊插入 → `ans[id] =` 寫回。**\n- **定理 1（合法性）**：查詢唯讀且互不依賴 ⇒ 任意順序回答。**兩個失效情形：強制線上（參數與上一個答案 XOR）、查詢與修改交錯。**\n- **定理 2（最大紅利）**：門檻單調 ⇒ **只加不刪**，每個元素恰插入一次。**這讓並查集、單調棧、前綴 max 這些不支援刪除的結構變得可用。**\n- **定理 3（1697）**：「路徑上每條邊 $<$ limit」$\\iff$「子圖 $G_{<limit}$ 中連通」。**前提是限制加在每條邊上，不是加在路徑總長上——搞混就全錯。** 嚴格小於就寫 `<`。\n- **定理 4（瓶頸路徑）**：最小瓶頸路徑值 $=$ MST 上該路徑的最大邊權（用割性質反證）。**線上解法 = Kruskal 重構樹 + LCA；離線短十倍，除非強制線上。**\n- **定理 5（一般框架）**：**兩個條件，一個交給掃描順序、一個交給資料結構**——與 9.2 的二維偏序一字不差。**三個條件就要 CDQ／樹套樹。** 排序方向由門檻是 $\\le$ 還是 $\\ge$ 決定。\n- **定理 6（DFS 型）**：進入加入、離開撤銷，不變量是「結構中恰為根到當前節點的路徑」。**`add` 與 `remove` 必須嚴格互逆；可撤銷並查集不能路徑壓縮。** Tarjan LCA 同骨架。\n- **最高頻錯誤**：忘了記 `id`；`push_back` 而非 `ans[id]=`；`<` 與 `<=` 搞混；雙指標在迴圈內被重置（答案對但退化成 $O(nq)$）；排序方向反了。\n- **判斷順序**：題目給 `queries` 陣列 ⇒ 想離線；一個單調指標夠嗎 ⇒ 夠就用本節（$O((n+q)\\log n)$）；兩個指標要來回 ⇒ 莫隊（$O(n\\sqrt q)$）；強制線上 ⇒ 主席樹／重構樹。\n",
        },
      ],
    },
    {
      id: 76,
      title: "13. 專題：比較複雜的題目",
      src: "https://leetcode.cn/circle/discuss/mOr1u6/",
      summary:
        "整章為高難度模擬與大實作：常同時考線段樹分治、可持久化結構、分塊或離線轉化。\n\n**建議**：先讀題拆解子任務，寫清每部分的時間界再動手；可與題單第 12 章離線技巧對照。",
      children: [
        {
          id: 77,
          title: "13.1 Part A",
          summary:
            "## 這個技術解決什麼問題\n\n前面每一節都對應一個明確的工具：看到「區間最值 + 修改」就用線段樹、看到「離線門檻」就排序加元素。**但有一類題目不吃這一套**——它把好幾個概念綁在一起，你認得出每一塊零件，卻不知道怎麼拼。\n\n**辨識訊號**：\n- 題目把多個概念綁在一起（圖 + 模擬 + 狀態、棋盤 + 搜尋），**無法一眼套模板**。像尋寶（LCP 13）、追逐遊戲（LCP 21）、貓和老鼠（913、1728）這類題；\n- 資料量明顯會變、又要反覆查詢，提示你需要**設計專屬的狀態與更新規則**，而不是沿用上層分類的通用模板；\n- **暴力解寫得出來但會超時**——這反而是好消息，它給了你「優化哪裡」的明確起點。\n\n**這一節不教新的資料結構，教的是拆解的方法。** 複合題的解法幾乎總是三層：\n\n> **第一層：狀態空間**（把局面編碼成什麼？）\n> **第二層：轉移規則**（狀態之間怎麼走？代價多少？）\n> **第三層：搜尋策略**（怎麼把最優值算出來？DP／BFS／逆向推理）\n\n**每一層都是你已經會的東西。難的是切在哪裡。**\n\n**而切得對不對，有一個可驗證的判準（定理 1）——這是本節唯一真正需要證明的東西。** 一旦狀態設計正確，剩下的往往就是照抄模板。\n\n## 辨識題型的訊號\n\n這類題的「線索」反而是它**不像任何單一套路**：\n\n- **題目把多個概念綁在一起**（圖 + 模擬 + 狀態、或棋盤 + 搜尋），無法一眼套模板。像尋寶、追逐遊戲、無限棋局這類題。\n- **資料量明顯會變、又要反覆查詢**，提示你需要設計專屬的狀態與更新規則，而非沿用上層分類的通用模板。\n- **暴力解雖然寫得出來但會超時**，給了你「優化哪裡」的明確起點。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的「狀態」每個欄位代表什麼？對應的不變式是哪一句？\n2. 每次更新只動局部、且維持了不變式嗎？\n3. 我的答案是從哪個狀態、在哪個時機讀出來的？\n\n## 核心想法與直覺\n\n本節收的是「比較複雜、不容易歸到單一套路」的題目（Part A，多為設計感強的綜合題）。這類題沒有現成模板可抄，重點不在記某個資料結構，而在**學會一套拆題的通用流程**：先把問題講清楚，再決定要維護什麼。\n\n:::example 拆題的第一步：把「會變的量」和「要快查的量」分開存\n核心方法只有三步：**狀態表示 → 更新規則 → 答案讀取。** 先問「輸入裡哪些量會變、哪些量需要被快速查詢」，把它們存進合適的結構；接著定義每一步怎麼更新這些狀態；最後想清楚答案是從哪個狀態讀出來的。\n\n```cpp\n// 拆題的第一步：把「會變的量」和「要快查的量」分開存\n// 例：某設計題裡，位置會變(state)、某些值要 O(1) 查(lookup)\nstruct State {\n    /* 會隨操作改變的量 */\n};\nunordered_map<int, int> lookup;  // 需要被快速查詢的量\n```\n:::\n\n\n\n:::example 每一步只更新「與當前元素相關」的局部狀態，維持不變式\n第二個關鍵概念是「不變式（invariant）」：一句永遠成立的話，描述你的狀態代表什麼。例如「`dist[v]` 永遠是從起點到 v 的最短距離」。每次更新都要**維持這句話成立**，答案才可信。不變式說不清楚，往往就是還沒真正理解題目。\n\n```cpp\n// 每一步只更新「與當前元素相關」的局部狀態，維持不變式\nfor (auto& e : events) {  // 按題目自然順序或排序後順序處理\n    apply(e);               // 只動受 e 影響的部分，別整體重算\n}\n// 迴圈結束時，不變式成立 → 直接讀答案\n```\n:::\n\n\n\n對這種難題，務必先寫得出**暴力解**：暴力解定義了「正確答案長什麼樣」。有了它，再去找暴力裡「被重複算很多次」的部分，把那部分換成可維護的狀態或資料結構，就是優化的方向。很多綜合題（如尋寶、追逐遊戲這類圖上模擬）本質是「最短路 / BFS / 狀態壓縮」的組合，看清楚要維護的狀態後就拆得開。\n\n## 狀態／資料結構定義\n\n**設計狀態時，逐條檢查這三件事：**\n\n| 檢查項 | 問題 | 失敗的後果 |\n| --- | --- | --- |\n| **完備性**（定理 1） | 兩個狀態相同的局面，後續最優值一定相同嗎？ | 答案錯，且錯得沒有規律 |\n| **最小性** | 有沒有哪個分量其實推不出任何區別？ | 狀態數爆炸，TLE／MLE |\n| **可編碼性** | 能不能塞進一個 `int` 或陣列下標？ | 只能用 `map`，常數暴增 |\n\n**三個最常見的狀態骨架：**\n\n```cpp\n// 骨架 A：集合狀態壓縮（元素 <= 20 個，順序不重要但「哪些做完了」重要）\n//   dp[mask][i] = 已完成 mask 這組任務、目前停在 i，的最小代價\nvector<vector<long long>> dp(1 << m, vector<long long>(m, LLONG_MAX / 2));\n\n// 骨架 B：多主體位置狀態（博弈、追逐）\n//   id = (mouse * n + cat) * 2 + turn\nint enc(int mouse, int cat, int turn, int n) { return (mouse * n + cat) * 2 + turn; }\n\n// 骨架 C：事件驅動模擬（時間推進由「下一個事件」決定，而非逐秒遞增）\n//   四個優先佇列分別代表四種身分，時間跳到最早的事件時刻\npriority_queue<pair<long long,int>, vector<pair<long long,int>>, greater<>> workL, workR;\npriority_queue<int> waitL, waitR;      // 依題目規則排序（例如效率最低者優先）\n```\n\n**骨架 A 的關鍵是「$m \\le 20$」**：$2^{20} = 10^6$，乘上 $m^2$ 還在可算範圍。**看到「至多 16 個機關」「至多 20 個城市」，狀態壓縮基本上就是答案。**\n\n**骨架 B 的關鍵是編碼要緊湊**：$n = 50$ 時 $(50\\times50)\\times2 = 5000$ 個狀態，可以開陣列；若還要加上「已走幾步」這一維就變成 $10^6$——**能不加就不加**（定理 4 說明為什麼可以不加）。\n\n## 不變量或正確性證明\n\n### 定理 1（狀態完備性判準）\n\n**定義**：狀態映射 $\\sigma$（把局面映到狀態）是**完備的**，若對任意兩個局面 $P$、$P'$，\n\n$$\\sigma(P) = \\sigma(P') \\;\\Longrightarrow\\; \\mathrm{opt}(P) = \\mathrm{opt}(P'),$$\n\n其中 $\\mathrm{opt}$ 是「從該局面出發能達到的最優值」。\n\n**命題**：若 $\\sigma$ 完備，則「以 $\\sigma$ 的像為節點、以合法動作為邊」建圖，在圖上做 DP／BFS 得到的結果正確。\n\n*證明*。完備性保證了 $\\mathrm{opt}$ 可以寫成狀態的函數 $F(\\sigma(P))$。轉移方程 $F(s) = \\bigoplus_{s \\to s'} \\big(\\mathrm{cost}(s,s') \\otimes F(s')\\big)$ 中，右側只依賴狀態而不依賴具體局面 ✔——這正是遞迴式良定義（well-defined）的條件。$\\square$\n\n**這就是「無後效性」的形式化，也是複合題唯一真正的難點。** 實務上怎麼檢查？\n\n> **試著構造兩個狀態相同但後續不同的局面。構造得出來，狀態就不完備。**\n\n**兩個典型的不完備例子**：\n- **貓和老鼠只記 `(mouse, cat)` 而不記 `turn`**：同樣的位置，輪到貓走和輪到老鼠走的勝負可以完全相反 ✘。\n- **尋寶只記「已觸發幾個機關」而不記「是哪些」**：同樣觸發了 3 個，剩下的距離完全不同 ✘。\n\n**反過來，最小性的檢查是**：拿掉某一維，還能不能構造出反例？**構造不出來，那一維就是多餘的。** 例如貓和老鼠若加上「已走幾步」這一維，狀態數乘上 $2n$——**而定理 4 會證明這一維可以拿掉。**\n\n### 定理 2（LCP 13 尋寶：把「路徑」與「順序」分離）\n\n*問題*：$N\\times M$ 的棋盤上有起點 `S`、終點 `T`、$m$ 個機關 `M`、若干石堆 `O`、障礙 `#`。**要觸發一個機關，必須先到某個石堆拿一塊石頭，再帶到該機關**（石堆的石頭無限）。求觸發所有機關後到達 `T` 的最短總步數。\n\n**分解命題**：最優路徑必定形如\n\n$$S \\;\\to\\; O_{i_1} \\;\\to\\; M_{j_1} \\;\\to\\; O_{i_2} \\;\\to\\; M_{j_2} \\;\\to\\;\\cdots\\;\\to\\; O_{i_m} \\;\\to\\; M_{j_m} \\;\\to\\; T,$$\n\n其中 $(j_1,\\dots,j_m)$ 是機關的一個排列。因此定義\n\n$$c(j, j') \\;=\\; \\min_{O}\\Big(d(M_j,\\, O) + d(O,\\, M_{j'})\\Big),\\qquad\nc_0(j) \\;=\\; \\min_{O}\\Big(d(S,\\, O) + d(O,\\, M_j)\\Big),$$\n\n則答案 $= \\min_{\\text{排列}} \\Big( c_0(j_1) + \\sum_{k=2}^{m} c(j_{k-1}, j_k) + d(M_{j_m},\\, T) \\Big)$。\n\n*證明*。\n- **(路徑形態)** 每個機關恰需一塊石頭，而石頭只能從石堆取得且**一次只能帶一塊**（帶到機關就消耗掉）。故在觸發第 $k$ 個機關之前，必須有一次「造訪石堆」的動作，且該次造訪之後直接前往該機關（中途繞路只會更長）✔。\n- **(相鄰段可獨立最小化)** 從機關 $j$ 出發、經某石堆、到達機關 $j'$ 的最短代價，只取決於 $(j, j')$——**與之前觸發了哪些機關無關**（棋盤不會改變，石堆無限）✔。這正是定理 1 的完備性：狀態只需要「已觸發的機關集合 + 當前所在機關」。\n- **(不需要考慮「先拿石頭再繞路」)** 由於石堆的石頭無限且每次只帶一塊，任何「提前繞去石堆」的策略，其代價都不小於 $\\min_O(d(M_j,O)+d(O,M_{j'}))$ ✔。$\\square$\n\n**於是問題化成 TSP**：\n\n$$\\mathit{dp}[\\mathit{mask}][j] = \\min_{j' \\in \\mathit{mask},\\, j'\\ne j} \\Big(\\mathit{dp}[\\mathit{mask}\\setminus\\{j\\}][j'] + c(j', j)\\Big).$$\n\n*複雜度*：$m+2$ 次 BFS 求出所有 $d$（$O((m+2)NM)$）、$O(m^2 \\cdot |O|)$ 算 $c$、$O(2^m m^2)$ 做 TSP。**$m \\le 16$ ⇒ $2^{16}\\times16^2 \\approx 1.7\\times10^7$** ✔。\n\n**這個分解值得記住的地方**：**「棋盤上的最短路」與「訪問順序的最優化」是兩個完全獨立的子問題，用 BFS 預處理把前者壓成一張 $m\\times m$ 的代價表，後者就退化成標準 TSP。** 複合題的解法幾乎都長這樣——**找到那條把兩個子問題切開的縫。**\n\n**要點名的前提**：「相鄰段可獨立最小化」依賴**石堆的石頭無限、棋盤不變**。若石堆每個只有一塊石頭，$c(j,j')$ 就會依賴「哪些石堆已用完」，狀態必須加一維石堆集合——**問題立刻變成不可解的規模。** 讀題時要確認這一點。\n\n### 定理 3（913 貓和老鼠：為什麼必須逆向推理）\n\n*問題*：無向圖上，老鼠從節點 1、貓從節點 2 出發，洞在節點 0。老鼠先動，兩者輪流沿邊移動（**貓不能進洞**）。老鼠進洞則老鼠贏、貓抓到老鼠則貓贏、局面無限循環則平局。求最優對弈的結果。\n\n**為什麼正向記憶化搜尋會壞掉**：狀態圖**有環**（雙方都可以來回走），`dfs(state)` 會呼叫到自己，記憶化陣列裡是「計算中」的標記——**這不是簡單的重複子問題，而是真正的循環依賴。** 隨手把「計算中」當成平局也不對：那會把「其實有必勝走法但需要繞路」的狀態誤判。\n\n**正確做法：逆向 BFS（retrograde analysis）。**\n\n*設定*：狀態 $s = (\\mathit{mouse}, \\mathit{cat}, \\mathit{turn})$。定義 $\\mathrm{deg}(s)$ 為該狀態下**輪到走的一方**的合法走法數。\n\n*演算法*：\n1. 把所有**終局**（$\\mathit{mouse}=0$ ⇒ 老鼠贏；$\\mathit{mouse}=\\mathit{cat}$ ⇒ 貓贏）加入佇列，標記結果。\n2. 取出一個已確定結果的狀態 $s$，枚舉它的**前驅** $p$（上一步走到 $s$ 的狀態）：\n   - 若 $s$ 的結果**對 $p$ 的行動方有利**（例如 $p$ 輪到老鼠、而 $s$ 是老鼠贏）⇒ $p$ 直接確定為「行動方贏」，入佇列；\n   - 否則 `deg(p)--`；**若 `deg(p)` 降到 0**（所有走法都對行動方不利）⇒ $p$ 確定為「行動方輸」，入佇列。\n3. 佇列空後，**所有未被標記的狀態就是平局**。\n\n**正確性（三部分）**：\n\n- **(必勝的傳播)** 若某個後繼對行動方有利，行動方選它即可獲勝 ✔——這是「存在」語意，一個後繼就夠。\n- **(必敗的傳播)** 若**所有**後繼都對行動方不利，則無論怎麼走都輸 ✔——這是「全稱」語意，所以要用 `deg` 計數到 0。\n- **(平局的刻畫)** 未被標記的狀態，其每個後繼要嘛未被標記、要嘛對行動方不利，且**至少有一個未被標記的後繼**（否則 `deg` 會降到 0 被標記）。因此行動方可以永遠走進未標記狀態，遊戲無限進行 ⇒ 平局 ✔。**反過來，被標記的狀態確實能在有限步內分出勝負**，因為標記是從終局出發、按 BFS 層次傳播的，每個標記都附帶一條有限長的取勝路線 ✔。$\\square$\n\n**這三段對應的是博弈論的標準結論：「必勝 = 存在一個必敗後繼」「必敗 = 所有後繼都必勝」「其餘 = 平局」。** 逆向 BFS 只是把這個定義**按拓撲序**實現出來——**環的問題被「未標記即平局」自然吸收了。**\n\n**這是「有環的博弈 DP 必須逆向」的標準論證。** 記住它的觸發條件：**狀態圖有環 + 存在「無限循環」這種第三種結果。** 若題目規定了步數上限（1728 貓和老鼠 II 就有），環就被步數維切開了，**正向記憶化又變得可行**——這是兩題的根本差異。\n\n### 定理 4（為什麼不需要「已走步數」這一維：抽屜原理）\n\n*命題*：在 913 中，若雙方最優對弈下經過 $2n(n-1)$ 步仍未分勝負，則必為平局。\n\n*證明*。狀態 $(\\mathit{mouse}, \\mathit{cat}, \\mathit{turn})$ 的總數為 $n \\times (n-1) \\times 2$（貓不能在洞裡）。**若對弈超過這麼多步，由抽屜原理必有某個狀態重複出現。** 雙方採取的都是「只依賴當前狀態」的最優策略（由定理 1，最優值是狀態的函數），故從重複狀態起，之後的走法會與上一次完全相同 ⇒ **進入週期循環，永遠不會結束** ⇒ 平局 ✔。$\\square$\n\n**於是「已走步數」這一維可以完全省略**——狀態數從 $O(n^3)$ 降到 $O(n^2)$。**這是最小性檢查（見「狀態定義」表）帶來的實際收益。**\n\n**兩點誠實的補充**：\n- 已知有更緊的界（$2n$ 步），但那需要更細緻的論證；**$2n^2$ 這個界用抽屜原理三行就能證，而且對演算法設計完全夠用**（我們根本不記步數）。\n- **這個論證依賴「最優策略只依賴狀態」。** 若題目允許隨機策略或有其他隱藏資訊，論證失效。\n\n### 定理 5（2532 過橋：模擬題的不變量與時間推進）\n\n*問題*：$k$ 個工人在橋兩側搬箱子，每人有四個時間參數，規則明確規定了「橋空閒時誰先過」（效率最低者優先）。求最後一個工人回到左岸的時刻。\n\n**這不是貪心題，是模擬題**——策略由題目直接規定，**不需要證明任何交換論證**。正確性完全在於「忠實實現規則」與「正確推進時間」。\n\n**不變量 (W)**：*任何時刻，四個容器（左岸等待、右岸等待、正在左岸工作、正在右岸工作）的併集恰為全部 $k$ 個工人，且互不重疊。*\n\n**這條不變量是模擬題的除錯利器**：**每次事件處理後 `assert` 四個容器的總大小等於 $k$，絕大多數狀態轉移的 bug 會當場暴露。**\n\n**時間推進的正確性**：\n\n*命題*：當兩側的等待區都為空時，把當前時間直接跳到「兩個工作佇列中最早的完成時刻」，不會遺漏任何事件。\n\n*證明*。等待區為空 ⇒ 沒有工人能立刻過橋 ⇒ 在最早完成時刻之前，系統狀態不會改變 ✔。$\\square$\n\n**這就是「事件驅動模擬」與「逐秒模擬」的分野**：時間上限可能是 $10^9$，**逐秒必然 TLE；事件驅動的迭代次數是 $O(k)$**（每個工人的過橋次數有限）。\n\n**推論 5.1（模擬題的通用做法）**：\n1. 列出所有「事件類型」；\n2. 用優先佇列按時間排序；\n3. **每次跳到下一個事件時刻，而不是 `time++`**；\n4. 用不變量 (W) 型的斷言自我檢查。\n\n**優先佇列的比較器必須逐字對應題目的規則**（2532 是「效率最低者優先，相同則編號大者優先」）。**模擬題的 WA 十有八九出在比較器上，把題目原文抄成註解放在比較器旁邊。**\n\n### 定理 6（先估算狀態數，再決定要不要優化）\n\n**複合題最常見的失誤是「過早優化」**——花兩小時設計精巧的資料結構，其實暴力就能過。\n\n*判準*：估算 **狀態數 × 每個狀態的轉移數**。\n\n| 量級 | 判斷 |\n| --- | --- |\n| $\\le 10^7$ | **直接暴力／記憶化搜尋**，不要優化 |\n| $10^7 \\sim 10^8$ | 需要小常數（陣列而非 `map`、避免遞迴） |\n| $> 10^8$ | 必須換演算法或加剪枝 |\n\n**2056 這類「枚舉所有走法組合」的題目正是靠這個估算才敢寫暴力**——棋子數與可走方向都很小，乘起來還在 $10^7$ 以內，於是「枚舉每顆棋子的移動方向與步數 + 逐時刻檢查碰撞」直接可行。**先算一次，再決定。**\n\n**估算時要算的是「不同狀態的個數」，不是「所有路徑的條數」。** 記憶化搜尋的複雜度是前者——**這是暴力搜尋與記憶化搜尋在複雜度上的分水嶺，也是很多人高估難度的原因。**\n\n## 逐步演算法\n\n面對這類複雜題，按下面步驟逐一推進：\n\n:::example 寫清楚每個欄位的語意，避免之後自己也忘了它代表什麼\n**1. 狀態表示：列出會變的量與要快查的量。** 寫下來、給每個量一個明確語意。\n\n```cpp\n// 寫清楚每個欄位的語意，避免之後自己也忘了它代表什麼\nint dist[N];      // 起點到各點目前已知的最短距離\nbool visited[N];  // 是否已確定最短距離\n```\n:::\n\n\n\n:::example 例：BFS 一步只擴展當前節點的鄰居（局部更新）\n**2. 更新流程：按自然或排序後的順序，逐步局部更新。** 每處理一個元素，只動和它相關的狀態。\n\n```cpp\n// 例：BFS 一步只擴展當前節點的鄰居（局部更新）\nfor (int nxt : graph[cur]) {\n    if (!visited[nxt]) {\n        dist[nxt] = dist[cur] + 1;\n        q.push(nxt);\n    }\n}\n```\n:::\n\n\n\n:::example 不變式成立後，答案直接從狀態讀（例：終點的 dist）\n**3. 答案讀取：在不變式成立的位置讀出答案。** 明確說出答案來自哪個狀態。\n\n```cpp\n// 不變式成立後，答案直接從狀態讀（例：終點的 dist）\nreturn dist[target];\n```\n:::\n\n\n\n:::example 先寫對、再寫快：暴力解是正確性的基準與優化的起點\n**4. 卡住時的退路：回到暴力解。** 若狀態語意與更新時機說不清，先寫對的暴力解，再從中找可優化的重複計算。\n\n```cpp\n// 先寫對、再寫快：暴力解是正確性的基準與優化的起點\n// （例：先 O(n^2) 跑通，再找哪段可用結構加速）\n```\n:::\n\n\n\n**5. 邊界與複雜度。**\n\n- 綜合題常有多種特殊輸入（空、極值、單元素），逐一想過。\n- 每加一個狀態，都要重新檢查「它在每次更新後是否仍維持不變式」。\n- 複雜度取決於主導步驟（如圖題常是 `O(V + E)` 或 `O(V log V)`），確認沒有藏著一層 `O(n)` 重算。\n\n## C++17 模板\n\n**模板一：狀態壓縮 TSP（LCP 13 骨架，定理 2）**\n\n```cpp\n// d[i][j]：預先用 BFS 算好的兩點最短距離；INF 表示不可達\n// c[j][j2]：經由某個石堆從機關 j 走到機關 j2 的最小代價\nint minimalSteps(vector<string>& maze) {\n    // 1. 找出 S、T、機關 M、石堆 O，對 S 與每個 M 各做一次 BFS\n    // 2. 算 c0[j] = min_O(d[S][O] + d[O][M_j])，c[j][j2] = min_O(d[M_j][O] + d[O][M_j2])\n    int m = /* 機關數 */ 0;\n    const long long INF = LLONG_MAX / 4;\n    vector<vector<long long>> dp(1 << m, vector<long long>(m, INF));\n    for (int j = 0; j < m; j++) dp[1 << j][j] = c0[j];\n    for (int mask = 1; mask < (1 << m); mask++)\n        for (int j = 0; j < m; j++) {\n            if (!(mask >> j & 1) || dp[mask][j] == INF) continue;\n            for (int j2 = 0; j2 < m; j2++) {\n                if (mask >> j2 & 1) continue;\n                long long nv = dp[mask][j] + c[j][j2];\n                dp[mask | (1 << j2)][j2] = min(dp[mask | (1 << j2)][j2], nv);\n            }\n        }\n    long long ans = INF;\n    for (int j = 0; j < m; j++)\n        if (dp[(1 << m) - 1][j] < INF && dToT[j] < INF)\n            ans = min(ans, dp[(1 << m) - 1][j] + dToT[j]);\n    return ans >= INF ? -1 : int(ans);\n}\n```\n\n**`mask >> j & 1` 的優先級**：`>>` 高於 `&`，所以這樣寫是對的；但 `mask & 1 << j` 也對（`<<` 高於 `&`）。**混寫容易出錯，選一種風格貫徹到底。**\n\n**推式（push）比拉式（pull）好寫**：外層 `mask` 遞增枚舉，保證 `dp[mask][j]` 在被用來更新更大的 mask 之前已經定案 ✔。\n\n**模板二：逆向 BFS 博弈（913 骨架，定理 3）**\n\n```cpp\nclass Solution {\npublic:\n    int catMouseGame(vector<vector<int>>& graph) {\n        int n = int(graph.size());\n        // color[m][c][t]：0 未定（平局）、1 老鼠贏、2 貓贏；t = 0 老鼠走、1 貓走\n        vector color(n, vector(n, array<int,2>{0, 0}));\n        vector deg(n, vector(n, array<int,2>{0, 0}));\n        for (int m = 0; m < n; m++)\n            for (int c = 1; c < n; c++) {\n                deg[m][c][0] = int(graph[m].size());\n                deg[m][c][1] = int(graph[c].size());\n                for (int x : graph[c]) if (x == 0) { deg[m][c][1]--; break; }  // 貓不能進洞\n            }\n        queue<array<int,3>> q;\n        for (int c = 1; c < n; c++)\n            for (int t = 0; t < 2; t++) {\n                color[0][c][t] = 1; q.push({0, c, t});            // 老鼠進洞\n                color[c][c][t] = 2; q.push({c, c, t});            // 貓抓到老鼠\n            }\n        while (!q.empty()) {\n            auto [m, c, t] = q.front(); q.pop();\n            int win = color[m][c][t];\n            // 枚舉前驅：上一步是誰走的？t=0 表示現在輪老鼠，故上一步是貓走的\n            int pt = t ^ 1;\n            vector<array<int,2>> pre;\n            if (pt == 0) { for (int pm : graph[m]) pre.push_back({pm, c}); }\n            else { for (int pc : graph[c]) if (pc != 0) pre.push_back({m, pc}); }\n            for (auto [pm, pc] : pre) {\n                if (color[pm][pc][pt] != 0) continue;              // 已定案\n                if ((pt == 0 && win == 1) || (pt == 1 && win == 2)) {\n                    color[pm][pc][pt] = win;                       // 存在有利後繼 ⇒ 必勝\n                    q.push({pm, pc, pt});\n                } else if (--deg[pm][pc][pt] == 0) {\n                    color[pm][pc][pt] = win;                       // 所有後繼都不利 ⇒ 必敗\n                    q.push({pm, pc, pt});\n                }\n            }\n        }\n        return color[1][2][0];\n    }\n}; \n```\n\n**兩個判斷分支對應定理 3 的「存在」與「全稱」語意，順序不能顛倒**——先檢查「有利後繼」，沒有才 `deg--`。\n\n**`pt = t ^ 1`**：前驅狀態輪到的是**另一方**。**這個方向搞反是最常見的錯，寫註解標明。**\n\n**模板三：事件驅動模擬（2532 骨架，定理 5）**\n\n```cpp\n    // waitL / waitR：等待過橋（按題目規則排序，堆頂是「下一個該走的人」）\n    // workL / workR：正在搬箱（按完成時刻排序）\n    long long cur = 0;\n    while (/* 還有箱子或還有人在右岸 */) {\n        // 1. 把所有「已完成」的工人搬到對應的等待區\n        while (!workL.empty() && workL.top().first <= cur) { waitL.push(...); workL.pop(); }\n        while (!workR.empty() && workR.top().first <= cur) { waitR.push(...); workR.pop(); }\n        // 2. 依規則決定誰過橋（右岸優先）\n        if (!waitR.empty())      { /* 過橋回左岸 */ }\n        else if (!waitL.empty() && /* 還有箱子 */) { /* 過橋去右岸 */ }\n        else {\n            // 3. 兩邊都沒人能動 ⇒ 時間跳到最早完成時刻（定理 5）\n            long long nxt = LLONG_MAX;\n            if (!workL.empty()) nxt = min(nxt, workL.top().first);\n            if (!workR.empty()) nxt = min(nxt, workR.top().first);\n            if (nxt == LLONG_MAX) break;              // 防死迴圈\n            cur = nxt;\n        }\n    }\n```\n\n**`if (nxt == LLONG_MAX) break;` 是防死迴圈的保險絲。** 模擬題若邏輯有漏洞，最常見的症狀是無窮迴圈——**主動加一道防線，能把「TLE」變成「WA」，而 WA 好除錯得多。**\n\n## 時間與空間複雜度\n\n| 題型 | 狀態數 | 轉移 | 總計 |\n| --- | --- | --- | --- |\n| LCP 13 尋寶（定理 2） | $2^m \\cdot m$ | $O(m)$ | $O(2^m m^2 + (m+2)NM)$ |\n| 913 貓和老鼠（定理 3） | $2n^2$ | $O(\\deg)$ | $O(n^3)$（邊數 $\\times$ 狀態） |\n| 1728 貓和老鼠 II | $2n^2 \\cdot T$（$T$ = 步數上限） | $O(\\text{可走格數})$ | 有步數維 ⇒ **正向記憶化可行** |\n| 2532 過橋（定理 5） | 事件數 $O(k + \\text{箱數})$ | $O(\\log k)$ | $O((k+B)\\log k)$ |\n| 2056 枚舉組合（定理 6） | 組合數 | $O(1)$ 檢查 | 先估算，$\\le 10^7$ 就直接寫 |\n\n**規模感速查（定理 6）**：\n\n| $m$（狀態壓縮） | $2^m$ | $2^m m^2$ |\n| --- | --- | --- |\n| 12 | 4096 | $5.9\\times10^5$ |\n| 16 | 65536 | $1.7\\times10^7$ |\n| 20 | $10^6$ | $4.2\\times10^8$（**太大，要優化轉移**） |\n\n**$m \\le 16$ 是狀態壓縮 + TSP 的舒適區；$m = 20$ 就只能做 $O(2^m m)$ 的轉移。**\n\n**空間**：$\\mathit{dp}[2^{16}][16]$ 的 `long long` 是 8 MB ✔；$\\mathit{dp}[2^{20}][20]$ 是 160 MB ✘——**狀態壓縮的空間常常比時間先爆，先算一次。**\n\n## 常見錯誤與邊界條件\n\n- **狀態不完備**（定理 1）。忘了記 `turn`、忘了記「是哪些」而只記「幾個」。**症狀是答案在小測資對、大測資錯，且找不到規律。除錯法：構造兩個同狀態但答案不同的局面。**\n- **狀態不最小**。多帶了一維（例如 913 的步數），狀態數翻 $n$ 倍導致 MLE。**先問：拿掉這一維，能構造出反例嗎？**\n- **有環的博弈用了正向記憶化**（定理 3）。無窮遞迴或把「計算中」誤判成平局。**判斷條件：狀態圖有環 + 有「平局」這第三種結果 ⇒ 必須逆向 BFS。**\n- **逆向 BFS 的前驅方向搞反**。`pt = t ^ 1`——**前驅輪到的是另一方。**\n- **`deg` 初始化沒扣掉非法走法**。913 中貓不能進洞，`deg[·][·][1]` 要減 1。**`deg` 算多了，「必敗」永遠傳播不出來。**\n- **BFS 起點漏了**（LCP 13）。要對 `S` 與**每個機關**各做一次 BFS；只對 `S` 做會拿不到 $c(j,j')$。\n- **不可達的處理**。棋盤有障礙時 $d$ 可能是 $\\infty$，**相加會溢位**。用 `INF = LLONG_MAX / 4` 並在相加前檢查。\n- **位元運算優先級**。`mask >> j & 1` 對、`mask & 1 << j` 對，`mask & 1 >> j` 錯。**選一種寫法貫徹到底。**\n- **狀態壓縮的初始狀態沒設**。`dp[1 << j][j] = c0[j]`——漏了就全是 INF。\n- **逐秒模擬**（定理 5）。時間上限 $10^9$ 時必然 TLE。**改事件驅動。**\n- **模擬題的比較器與題目規則不符**。**把題目原文抄成註解放在比較器旁邊。**\n- **模擬迴圈沒有防死迴圈的出口**。加 `if (nxt == LLONG_MAX) break;`。\n- **過早優化**（定理 6）。先估算狀態數，$\\le 10^7$ 就直接寫。\n\n## 與相似技巧的比較\n\n- **與純 DP（dynamic_programming 章）**：複合題的第三層通常就是 DP，**差別在於狀態要自己設計而不是套模板。** 定理 1 的完備性判準就是 DP「無後效性」的另一種說法——**這一節與 DP 章共用同一個核心概念。**\n- **與純圖論（graph 章）**：LCP 13 的第一層是多源 BFS、第三層是 TSP。**複合題常常是「圖論預處理 + DP 主體」，切開之後兩邊都是模板。** 找那條縫，是本節唯一需要創造力的地方。\n- **與 8 節並查集**：LCP 21 追逐遊戲這類「$n$ 點 $n$ 邊」的基環樹問題，第一步是找環（拓撲剝葉或並查集），之後才是距離比較。**又是「先化簡結構，再套模板」。**\n- **與搜尋剪枝（IDA*、A*）**：當狀態數超過 $10^8$（定理 6），只能靠剪枝把實際訪問的狀態壓下來。**但剪枝的效果無法事先估算，是最後手段。** 先確認真的沒有多項式解法。\n- **與 11 節根號演算法**：複合題若卡在「$O(nq)$ 太慢、$O(n\\log n)$ 想不出來」，**根號往往是那個中間選項**。$n \\le 10^5$ 時 $O(n\\sqrt n)$ 可過。\n- **與 12 節離線**：複合題中若查詢彼此獨立，**先問能不能離線**——這常常把一個維度直接消掉。\n- **與模擬題**：模擬題（2532）與演算法題的根本差異是**「策略由題目給定，不需要證明最優性」**。**不要在模擬題上尋找貪心證明，該做的是把規則抄準、把時間推進寫對。** 反過來，也不要把貪心題當模擬寫——那會漏掉最優性的驗證。\n\n## 代表例題\n\n**題號**：LCP 13、LCP 21、LCP 48、LCP 58、LCP 69\n\n要把區間量改寫成前綴狀態之差；若是計數題，重點是查找過去需要的前綴值。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| LCP 13 | [尋寶](https://leetcode.cn/problems/xun-bao)（Premium） |  |\n| LCP 21 | [追逐遊戲](https://leetcode.cn/problems/Za25hA)（Premium） |  |\n| LCP 48 | [無限棋局](https://leetcode.cn/problems/fsa7oZ)（Premium） |  |\n| LCP 58 | [積木拼接](https://leetcode.cn/problems/De4qBB)（Premium） |  |\n| LCP 69 | [Hello LeetCode!](https://leetcode.cn/problems/rMeRt2)（Premium） |  |\n| LCP 76 | [魔法棋盤](https://leetcode.cn/problems/1ybDKD) |  |\n| LCP 82 | [萬靈之樹](https://leetcode.cn/problems/cnHoX6) |  |\n| 913 | [貓和老鼠](https://leetcode.cn/problems/cat-and-mouse) | 2567 |\n| 1728 | [貓和老鼠 II](https://leetcode.cn/problems/cat-and-mouse-ii) | 2849 |\n| 1900 | [最佳運動員的比拼回合](https://leetcode.cn/problems/the-earliest-and-latest-rounds-where-players-compete) | 2455 |\n| 2056 | [棋盤上有效移動組合的數目](https://leetcode.cn/problems/number-of-valid-move-combinations-on-chessboard) | 2611 |\n| 2532 | [過橋的時間](https://leetcode.cn/problems/time-to-cross-a-bridge) | 2589 |\n| 3197 | [包含所有 1 的最小矩形面積 II](https://leetcode.cn/problems/find-the-minimum-area-to-cover-all-ones-ii) | 2541 |\n\n## 本節重點速查\n\n- **三層拆解**：**狀態空間 → 轉移規則 → 搜尋策略。** 每一層都是已知模板，難的是切在哪裡。\n- **定理 1（完備性判準）**：$\\sigma(P)=\\sigma(P') \\Rightarrow \\mathrm{opt}(P)=\\mathrm{opt}(P')$。**檢查法：試著構造兩個同狀態但後續不同的局面——構造得出來，狀態就不完備。** 最小性則反過來問：拿掉這一維還有反例嗎？\n- **定理 2（LCP 13）**：**把「棋盤最短路」與「訪問順序」切開**——BFS 預處理成 $m\\times m$ 的代價表 $c(j,j')=\\min_O(d(M_j,O)+d(O,M_{j'}))$，剩下是標準 TSP，$O(2^m m^2)$。**前提：石堆無限、棋盤不變**（否則狀態要加石堆集合，直接爆）。\n- **定理 3（913）**：**有環的博弈 DP 必須逆向 BFS。** 「存在有利後繼 ⇒ 必勝」（一個就夠）、「所有後繼都不利 ⇒ 必敗」（用 `deg` 計數到 0）、「其餘 ⇒ 平局」。**觸發條件：狀態圖有環 + 有第三種結果「平局」。** 有步數上限（1728）就切開了環，正向記憶化又可行。\n- **定理 4（抽屜原理）**：狀態數 $2n(n-1)$ ⇒ 超過就必有重複 ⇒ 循環 ⇒ 平局。**所以「已走步數」這一維可以省掉**，狀態從 $O(n^3)$ 降到 $O(n^2)$。依賴「最優策略只依賴狀態」。\n- **定理 5（模擬題）**：**策略由題目規定，不需要交換論證。** 不變量 (W)：四個容器的併集恆為全部工人（**拿來寫 `assert`**）。**事件驅動而非逐秒**：等待區皆空時直接跳到最早完成時刻。比較器逐字對應題目原文。**加防死迴圈的 `break`。**\n- **定理 6（先估算）**：狀態數 × 轉移數 $\\le 10^7$ ⇒ 直接暴力，別優化。**算的是「不同狀態數」而非「路徑條數」。** $2^{16}m^2 = 1.7\\times10^7$ 可行、$2^{20}m^2 = 4\\times10^8$ 不可行；**空間常常比時間先爆。**\n",
        },
        {
          id: 78,
          title: "13.2 Part B",
          summary:
            '## 這個技術解決什麼問題\n\n有一類題目**沒有演算法**——它的難不在「想出聰明做法」，而在「把一長串規則做對」。\n\n**辨識訊號**：\n- 題目描述本身就是一長串規則（合法格式、轉換對照、排版要求），重點是**照規則做對**，而非想出聰明演算法。像 8 字串轉整數（atoi）、65 有效數字、68 文字左右對齊；\n- 輸入是字串或數字，要**轉成另一種表示**，且有大量邊界要處理（12/13 羅馬數字、273 整數轉英文）；\n- **沒有明顯的最優化目標**（不是求最大／最小／計數），而是「忠實還原規則」。\n\n**這類題的失分點與演算法題完全不同**：不是 TLE，而是**規則漏了一條**。所以本節的核心方法不是複雜度分析，而是：\n\n> **把規則寫成一個「完整劃分」——輸入空間的每一種情況都恰好落入一條規則。**\n\n**完整劃分是可以驗證的**（定理 1），這就是本節的主軸。**同時，這類題目裡也藏著兩種真正需要證明的東西**：**結構不變量**（146 LRU、460 LFU 的資料結構同步，定理 3、4）與**被規則偽裝的最優化**（420 強密碼、2468 分割訊息，定理 5、6）——**後兩題看起來是模擬題，實際上必須做貪心論證與不動點分析，這是本節最容易低估的地方。**\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就知道是模擬 / 解析題：\n\n- **題目描述本身就是一長串規則**（合法格式、轉換對照、排版要求），重點是「照規則做對」，而非想出聰明演算法。像 atoi、有效數字、文字左右對齊。\n- **輸入是字串或數字，要轉成另一種表示**，且有大量邊界要處理。\n- **沒有明顯的最優化目標**（不是求最大 / 最小 / 計數），而是「忠實還原規則」。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我有沒有把所有規則與邊界先列成一張清單？\n2. 我的指標掃描中，每一步的狀態與前進條件清楚嗎？\n3. 我有沒有針對每個邊界情況（空、極值、特殊符號）各想一個測資？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 8 | 字串轉換整數 (atoi) | N/A | 要把區間量改寫成前綴狀態之差；若是計數題，重點是查找過去需要的前綴值。 |\n| 12 | 整數轉羅馬數字 | N/A | 要把區間量改寫成前綴狀態之差；若是計數題，重點是查找過去需要的前綴值。 |\n| 13 | 羅馬數字轉整數 | N/A | 要把區間量改寫成前綴狀態之差；若是計數題，重點是查找過去需要的前綴值。 |\n| 65 | 有效數字 | N/A | 要把區間量改寫成前綴狀態之差；若是計數題，重點是查找過去需要的前綴值。 |\n| 68 | 文字左右對齊 | N/A | 要把區間量改寫成前綴狀態之差；若是計數題，重點是查找過去需要的前綴值。 |\n\n## 核心想法與直覺\n\n本節收的是 Part B 的綜合題，看代表例題就懂它的共同氣味：字串轉整數（atoi，8）、整數轉羅馬數字（12）、羅馬數字轉整數（13）、有效數字判斷（65）、文字左右對齊（68）。它們不是難在演算法，而是難在**規則又多又雜的「模擬與解析」**——你得照題目的規則，一步一步把輸入轉成輸出，而且邊界特別多。\n\n:::example 解析類的骨架：一個指標 i 掃過輸入，逐字元更新狀態\n這類題沒有漂亮的數學技巧，核心方法就是把規則拆成**狀態表示 → 更新規則 → 答案讀取**，並用一個指標老老實實掃過輸入。最常見的骨架是「線性掃描 + 維護一個小狀態」。\n\n```cpp\n// 解析類的骨架：一個指標 i 掃過輸入，逐字元更新狀態\nint i = 0, n = s.size();\nwhile (i < n) {\n    char c = s[i];\n    // 依目前狀態 + 當前字元，決定怎麼更新狀態並前進 i\n    ++i;\n}\n```\n:::\n\n\n\n:::example atoi 片段：略過空白 -> 取符號 -> 累積數字（含溢位保護）\n以「字串轉整數 atoi」為例，規則是：先略過前導空白，再讀一個可選的正負號，接著讀連續數字，遇到非數字就停，還要處理溢位。每條規則對應一段處理，順序不能亂。\n\n```cpp\n// atoi 片段：略過空白 -> 取符號 -> 累積數字（含溢位保護）\nwhile (i < n && s[i] == \' \') {\n    ++i;  // 1) 前導空白\n}\nint sign = 1;\nif (i < n && (s[i] == \'+\' || s[i] == \'-\')) {  // 2) 可選符號\n    sign = (s[i++] == \'-\') ? -1 : 1;\n}\nlong long num = 0;\nwhile (i < n && isdigit(s[i])) {  // 3) 連續數字\n    num = num * 10 + (s[i++] - \'0\');\n    if (sign * num < INT_MIN) {\n        return INT_MIN;\n    }\n    if (sign * num > INT_MAX) {\n        return INT_MAX;  // 溢位夾住\n    }\n}\n```\n:::\n\n\n\n關鍵心法：**先把規則一條條列成清單（含每個邊界情況），再對照著寫程式碼。** 模擬題的 bug 幾乎都來自漏掉某條規則或某個邊界（空字串、全是空白、只有符號、最大最小值…），而不是邏輯太難。\n\n## 狀態／資料結構定義\n\n**四種骨架，對應四類規則題。**\n\n**骨架 A：DFA 狀態表（格式驗證，65、8）**\n\n```cpp\n// 把「合法字串」的規則畫成有限狀態機：state x charClass -> nextState（-1 = 拒絕）\nenum CharClass { DIGIT, SIGN, DOT, EXP, OTHER };\n// 每一格都必須填，包含明確的「拒絕」。留空 = 漏規則（定理 1）\nstatic const int TRANS[9][5] = { /* ... */ };\nstatic const bool ACCEPT[9] = { /* 哪些狀態可以作為結尾 */ };\n```\n\n**骨架 B：雜湊表 + 侵入式鏈結串列（LRU，146）**\n\n```cpp\nlist<pair<int,int>> lst;                       // 前端 = 最近使用\nunordered_map<int, list<pair<int,int>>::iterator> pos;   // key -> 節點迭代器\n// 提升：lst.splice(lst.begin(), lst, it);    // O(1)，且迭代器仍然有效（定理 3）\n```\n\n**骨架 C：頻率桶 + `minFreq`（LFU，460）**\n\n```cpp\nunordered_map<int, list<pair<int,int>>> bucket;      // freq -> 該頻率的 key 串列（前端最新）\nunordered_map<int, pair<int, list<pair<int,int>>::iterator>> pos;  // key -> (freq, 迭代器)\nint minFreq = 0;                                     // 不變量見定理 4\n```\n\n**骨架 D：分組轉換（273、12/13）**\n\n```cpp\n// 273：每三位一組，組內轉換是一個對 [0,999] 全域定義的函式\nstatic const string SCALE[] = {"", " Thousand", " Million", " Billion"};\n// 12：13 個值由大到小；貪心等價於逐位查表（定理 2）\nstatic const int  VAL[13] = {1000,900,500,400,100,90,50,40,10,9,5,4,1};\nstatic const char* SYM[13] = {"M","CM","D","CD","C","XC","L","XL","X","IX","V","IV","I"};\n```\n\n**選骨架的判準**：\n\n| 規則的形狀 | 骨架 |\n| --- | --- |\n| 「這個字串合不合法」 | **A（DFA）**——把規則變成表格 |\n| 「維護一個有淘汰策略的容器」 | **B / C**——雜湊表指向串列節點 |\n| 「把 X 表示成 Y」 | **D**——找出可獨立處理的「組」 |\n| 「求最少操作次數」 | **不是模擬題**，見定理 5、6 |\n\n## 不變量或正確性證明\n\n### 定理 1（規則實作的正確性判準：完整劃分）\n\n**命題**：設輸入空間為 $\\mathcal{I}$，程式的分支條件為 $C_1,\\dots,C_k$。程式正確的**必要條件**是\n\n$$\\bigcup_{i=1}^{k} C_i = \\mathcal{I} \\quad\\text{(不漏)}\\qquad\\text{且}\\qquad C_i \\cap C_j = \\varnothing\\ (i\\ne j) \\quad\\text{(不重)}.$$\n\n*為什麼這是可驗證的*。「不漏」失敗 ⇒ 存在輸入落不進任何分支，程式行為未定義（通常是回傳初始值）。「不重」失敗 ⇒ 行為取決於 `if` 的書寫順序，**規格與實作脫鉤**，改動順序就會改變答案。$\\square$\n\n**這聽起來像廢話，但它給了一個具體的除錯流程**：\n\n> **把所有分支條件列成一張表，逐一問「這個輸入落在哪一格」。找不到格子 ⇒ 漏規則；找到兩格 ⇒ 重疊。**\n\n**DFA 是這張表的極致形式**：`TRANS[state][charClass]` 的每一格都必須填值（包含明確的 $-1$ 表示拒絕）。**表格中留空的格子，就是漏掉的規則。** 這正是骨架 A 優於一連串 `if-else` 的原因——**表格的「空格」看得見，`if-else` 的「漏掉」看不見。**\n\n**以 65 有效數字為例**，DFA 接受的語言恰為\n\n$$\\texttt{[+-]?}\\;\\big(\\;\\texttt{d+}\\;\\texttt{.?}\\;\\texttt{d*}\\;\\big|\\;\\texttt{.}\\,\\texttt{d+}\\;\\big)\\;\\big(\\texttt{[eE]}\\;\\texttt{[+-]?}\\;\\texttt{d+}\\big)\\texttt{?}$$\n\n**寫完 DFA 後的驗收方式**：把這個正規表示式與轉移表逐項對照，**確認每個接受狀態都對應到表示式的一個合法結尾**（整數部分結尾、小數部分結尾、指數部分結尾），且**「只有小數點」「只有 e」「e 後面沒有數字」都不是接受狀態** ✔。\n\n**要點名的前提**：DFA 只驗證**格式**，不驗證**數值範圍**。8 atoi 要求截斷到 `int` 範圍——**那是另一條規則，不屬於 DFA。** 把兩者混在同一張表裡是常見的設計錯誤。\n\n### 定理 2（12 羅馬數字：貪心等價於逐位查表）\n\n*命題*：對 $1 \\le n \\le 3999$，用 13 個值由大到小的貪心，產生的字串等於標準羅馬數字表示。\n\n*證明*。把 13 個值分成四組（依數量級）：\n\n$$\\{1000\\},\\quad \\{900,500,400,100\\},\\quad \\{90,50,40,10\\},\\quad \\{9,5,4,1\\}.$$\n\n- **(組間不干擾)** 貪心處理完 $\\{1000\\}$ 後餘數 $< 1000$；處理完百位組後餘數 $< 100$；依此類推 ✔。因此**每一組只看到自己那一位數字**。\n- **(組內正確)** 對單一組 $\\{9u, 5u, 4u, u\\}$ 與任意數字 $h \\in [0,9]$，貪心產生的符號串就是標準寫法。這是**有限的 10 個情況**，逐一驗證：$0\\to\\varepsilon$、$1\\to u$、$2\\to uu$、$3\\to uuu$、$4\\to 4u$、$5\\to 5u$、$6\\to 5u\\,u$、$7\\to 5u\\,uu$、$8\\to 5u\\,uuu$、$9\\to 9u$ ✔。\n\n兩者合起來即得結論。$\\square$\n\n**這個證明的價值在於它是「有限驗證」**——不需要巧妙的交換論證，只要確認 10 個情況。**遇到「查表 + 貪心」的轉換題，先問：能不能把它切成互不干擾的組，讓組內只剩有限個情況？**\n\n**反方向（13 羅馬數字轉整數）的不變量**：*由左至右掃描，若 $\\mathrm{val}(s_i) < \\mathrm{val}(s_{i+1})$ 則減、否則加。*\n\n*正確性依賴的前提*：**在合法的羅馬數字中，「小值在大值之前」恰好只出現在 6 個減法組合（IV, IX, XL, XC, CD, CM）中。** 這條前提由題目保證輸入合法 ✔。**若輸入可能非法，這個掃描法會靜默地算出一個數字而不報錯**——這時要改用定理 1 的完整劃分先做驗證。\n\n### 定理 3（146 LRU：迭代器有效性是不變量的前提）\n\n**不變量 (L)**：*對每個 key $k$ 在容器中，$\\mathit{pos}[k]$ 指向 `lst` 中唯一那個 first 等於 $k$ 的節點；且 `lst` 由前到後恰為「最近使用」到「最久未使用」的順序。*\n\n*維持 (L) 的關鍵操作*：`lst.splice(lst.begin(), lst, it)`。\n\n*為什麼 (L) 在 splice 後仍成立*——**這裡有一條必須點名的前提**：\n\n> **`std::list::splice` 保證被移動的元素其迭代器、指標、參考全部保持有效**，只是所屬位置改變（若跨容器則歸屬也改變）。\n\n因此 `pos[k]` 不需要更新 ✔。**這正是選 `std::list` 而不是 `std::vector` 或 `std::deque` 的唯一理由**——後兩者的任何插入刪除都可能使迭代器失效，不變量 (L) 立刻崩潰。\n\n**推論 3.1**：`get` 與 `put` 都是 $O(1)$，**因為 splice 是 $O(1)$ 的指標改接，不是元素搬移** ✔。\n\n**推論 3.2（淘汰的順序不能反）**：`put` 一個新 key 且已滿時，**必須先刪 `lst.back()` 對應的 `pos` 條目，再 `lst.pop_back()`**。反過來寫會先讓迭代器懸空，然後拿它去查 `pos` ✘。**這是 LRU 最常見的 UB。**\n\n### 定理 4（460 LFU：`minFreq` 為什麼只需要 +1）\n\n**不變量 (F)**：*`minFreq` 恰等於當前容器中所有 key 的頻率最小值（容器非空時）。*\n\n*證明（對操作數歸納）*。能改變頻率集合的操作只有三種：\n\n1. **插入新 key**：其頻率為 1，是可能的最小值 ⇒ 令 `minFreq = 1` ✔。\n2. **淘汰**：從 `bucket[minFreq]` 移除一個 key。若該桶變空，則接下來**一定緊接著插入新 key**（淘汰只發生在容量已滿的 `put` 中），由情況 1 重設 `minFreq = 1` ✔。\n3. **存取既有 key**（`get` 或覆寫的 `put`）：把它從 `bucket[f]` 移到 `bucket[f+1]`。**這是整條不變量的核心：**\n   - 若 $f > \\mathit{minFreq}$：最小值不變 ✔；\n   - 若 $f = \\mathit{minFreq}$ 且 `bucket[f]` 移除後**非空**：仍有 key 的頻率為 $f$，最小值不變 ✔；\n   - 若 $f = \\mathit{minFreq}$ 且 `bucket[f]` 變空：所有 key 的頻率都 $> f$。而剛才那個 key 的新頻率是 $f+1$，**所以最小值恰為 $f+1$** ⇒ `minFreq++` ✔。\n\n$\\square$\n\n**最後一條分支是整個 LFU 能做到 $O(1)$ 的原因**，而它依賴兩個前提：\n\n> **前提一：頻率一次只 +1（不會跳號）。**\n> **前提二：一次操作只改變一個 key 的頻率。**\n\n**兩者缺一，`minFreq++` 就不成立，必須重掃所有桶找最小值（$O(\\text{桶數})$）。** 若題目改成「存取一次頻率 +2」或「批次操作」，這個 $O(1)$ 立刻失效——**這是「不變量依賴什麼前提」的教科書例子。**\n\n**桶內的順序**：同頻率時淘汰最久未使用者 ⇒ 每個桶自己是一個 LRU 串列（定理 3 的結構原封不動套用）✔。**LFU = LRU 再加一層頻率索引。**\n\n### 定理 5（420 強密碼檢驗器：偽裝成模擬的貪心）\n\n*問題*：合法密碼需滿足 (i) $6 \\le \\mathrm{len} \\le 20$、(ii) 含大寫、小寫、數字、(iii) 無連續 3 個相同字元。求最少操作（插入／刪除／替換）次數。\n\n**這不是模擬題。** 三種操作可以互相替代，必須做最優化論證。設 $t \\in [0,3]$ 為缺少的字元種類數。\n\n**情況 A（$\\mathrm{len} < 6$）**：答案 $= \\max(6 - \\mathrm{len},\\; t)$。\n\n*證明*。$\\ge$ 方向：長度至少要補到 6，且每種缺少的字元至少需要一次操作 ✔。$\\le$ 方向：$\\mathrm{len} \\le 5$ ⇒ 最長連續段 $\\le 5$ ⇒ 至多需要打斷一處。**每次插入都可以同時（a）增加長度、（b）補一個缺少的種類、（c）插在連續段中間打斷它**——三件事一次做完 ✔。$\\square$\n\n**情況 B（$6 \\le \\mathrm{len} \\le 20$）**：答案 $= \\max(r,\\; t)$，其中 $r = \\sum_{\\text{連續段}} \\lfloor L/3 \\rfloor$。\n\n*證明*。$r$ 是打斷所有連續段所需的最少替換數（長度 $L$ 的段每 3 個必須改一個）✔。**每次替換都可以順便補一個缺少的種類**（把被改的字元選成缺少的那類）⇒ 兩個需求可以合併，取 $\\max$ ✔。$\\square$\n\n**情況 C（$\\mathrm{len} > 20$）**：必須刪 $d = \\mathrm{len} - 20$ 個字元。答案 $= d + \\max(r\',\\; t)$，$r\'$ 是刪除後剩餘的替換數。**這裡才是真正的貪心。**\n\n*關鍵觀察*：對一個長度 $L \\ge 3$ 的連續段，刪 $x$ 個字元後替換數變成 $\\lfloor (L-x)/3 \\rfloor$。因此\n\n| $L \\bmod 3$ | 少一次替換所需的刪除數 |\n| --- | --- |\n| $0$ | **1** |\n| $1$ | **2** |\n| $2$ | **3** |\n\n*貪心*：把 $d$ 次刪除**優先用在 $L \\bmod 3 = 0$ 的段（每次刪除換 1 次替換），其次 $L \\bmod 3 = 1$，最後其餘。**\n\n*交換論證*。任何一次刪除最多讓總替換數減少 1（因為 $\\lfloor (L-1)/3 \\rfloor \\ge \\lfloor L/3 \\rfloor - 1$）✔。$L \\bmod 3 = 0$ 的段達到了這個上界（1 次刪除換 1 次替換），是可能的最佳效率。設某個最優解沒有把第一次刪除用在某個 $L \\bmod 3 = 0$ 的段上，把它換成用在該段，總替換數的減少量不減、刪除數不變 ⇒ 仍為最優 ✔。對後續刪除歸納即得。$\\square$\n\n**要點名的前提**：**刪除總數 $d$ 是固定的**（由長度上限決定），所以只需要最小化「刪除後的替換數」這一個目標——**若 $d$ 也是自由變數，這個貪心就不成立。** 另外「刪除不會製造新的連續段」在此成立，因為刪的是段內字元；**若允許刪段外字元把兩個同字元的段接起來，論證要重寫**（本題不會更優，因為那樣既沒減少替換又浪費了刪除）。\n\n### 定理 6（2468 分割訊息：為什麼不能二分搜尋 $b$）\n\n*問題*：把長度 $n$ 的訊息切成 $b$ 段，第 $i$ 段結尾必須附上 `<i/b>`，每段（含後綴）長度不超過 `limit`。求最小的 $b$。\n\n*容量函式*：第 $i$ 段能裝的內容長度為\n\n$$\\mathrm{cap}_i(b) = \\mathrm{limit} - 3 - |i| - |b|,\\qquad |x| \\coloneqq x \\text{ 的十進位位數},$$\n\n（$3$ 來自 `<`、`/`、`>`）。$b$ 可行 $\\iff$ **所有** $\\mathrm{cap}_i(b) \\ge 1$ 且 $\\sum_{i=1}^{b} \\mathrm{cap}_i(b) \\ge n$。\n\n**這裡的陷阱：$b$ 同時出現在「段數」與「每段後綴長度」裡——這是一個自我指涉。**\n\n*命題*：$C(b) \\coloneqq \\sum_{i=1}^{b} \\mathrm{cap}_i(b)$ **不是 $b$ 的單調遞增函式**，因此**不能對 $b$ 二分搜尋可行性**。\n\n*反例*。取 $\\mathrm{limit} = 10$：\n\n$$C(9) = \\sum_{i=1}^{9}(10 - 3 - 1 - 1) = 9 \\times 5 = 45,$$\n$$C(10) = \\sum_{i=1}^{9}(10 - 3 - 1 - 2) \\;+\\; (10 - 3 - 2 - 2) = 9 \\times 4 + 3 = 39.$$\n\n$C(10) = 39 < 45 = C(9)$ ✘。$\\square$\n\n**原因**：$b$ 從 9 跳到 10 時，$|b|$ 由 1 變 2，**每一段都損失一個字元**；新增的那一段補回來的容量不足以抵銷。**一般地，$b$ 跨過 $10^k$ 時 $C$ 會下降 $\\approx b - \\mathrm{limit}$。**\n\n**推論 6.1（正確做法）**：**從 $b = 1$ 遞增枚舉到 $b = n$，第一個可行的就是答案。** $n \\le 10^4$ ⇒ 直接枚舉完全可行；用「按位數分組」（$b \\in [1,9], [10,99], \\dots$）可讓每組內 $|b|$ 固定、$\\sum |i|$ 用前綴和 $O(1)$ 取得，總計 $O(n)$。\n\n**推論 6.2**：$\\mathrm{cap}_i(b) \\ge 1$ 這個逐段條件**不能省略**。只檢查總和 $\\sum \\mathrm{cap}_i(b) \\ge n$，會接受「某一段裝不下任何內容」的 $b$ ✘——**這正是定理 1 的「漏規則」在數值題中的樣子。**\n\n**這一題值得單獨記住的教訓**：**看到「參數自我指涉」（答案的位數影響答案），先驗證單調性再談二分。** 大多數人會直覺地二分，而這題的單調性剛好不成立。\n\n## 逐步演算法\n\n模擬 / 解析題可拆成下面幾步，逐一搭配程式碼：\n\n:::example 動手前先列規則（以註解形式釘住每一步）\n**1. 把規則列成有序清單。** 寫在註解或紙上：依什麼順序處理、各自的條件是什麼、邊界有哪些。\n\n```cpp\n// 動手前先列規則（以註解形式釘住每一步）\n// 1) 略過前導空白  2) 讀符號  3) 讀數字  4) 溢位處理\n```\n:::\n\n\n\n:::example 整數轉羅馬：把可用的值與符號由大到小排成表，貪心減去\n**2. 用查表代替一長串 if。** 像羅馬數字這種「固定對照」的題，用表格最清楚。\n\n```cpp\n// 整數轉羅馬：把可用的值與符號由大到小排成表，貪心減去\nint val[] = {1000, 900, 500, 400, 100, 90, 50, 40, 10, 9, 5, 4, 1};\nstring sym[] = {"M",  "CM", "D",  "CD", "C",  "XC", "L",\n        "XL", "X",  "IX", "V",  "IV", "I"};\n```\n:::\n\n\n\n:::example 羅馬轉整數：右邊比左邊大就代表是「減法」組合（如 IV = 4）\n**3. 線性掃描，逐步更新狀態。** 一個指標走到底，每步只根據目前字元與狀態做事。\n\n```cpp\n// 羅馬轉整數：右邊比左邊大就代表是「減法」組合（如 IV = 4）\nint total = 0;\nfor (int i = 0; i < s.size(); ++i) {\n    if (i + 1 < s.size() && rank[s[i]] < rank[s[i + 1]]) {\n        total -= rank[s[i]];  // 小的在大的左邊 → 減\n    } else {\n        total += rank[s[i]];  // 否則 → 加\n    }\n}\n```\n:::\n\n\n\n**4. 答案讀取：掃描結束後組裝結果。** 結果可能是一個數、一個字串、或一行行排好的文字（如文字左右對齊要算每行該補幾個空格）。\n\n**5. 邊界與複雜度。**\n\n- 把所有邊界當測資逐一驗：空輸入、全空白、只有符號、極大極小值、單一字元等。\n- 規則之間的「順序」是正確性核心，順序錯了結果就錯。\n- 複雜度通常是 `O(n)`（掃一遍輸入），空間 `O(1)` 或 `O(n)`（取決於要不要建結果字串）。\n\n## C++17 模板\n\n以下用同型延伸示範題 560. 和為 K 的子陣列 展示核心寫法。\n\n**完整題目**：給定整數陣列與 `k`，計算和為 `k` 的連續子陣列數量。\n\n:::example 前綴和 + 雜湊表：統計和為 k 的子陣列數量\n**線索**：題目反覆詢問連續區間量；若區間值能由兩個前綴狀態相減，就應優先想到前綴和。\n\n```cpp\nclass Solution {\npublic:\n    // 前綴和 + 雜湊表：統計和為 k 的子陣列數量\n    int subarraySum(vector<int>& nums, int k) {\n            unordered_map<int, int> cnt;  // cnt[pre] = 前綴和為 pre 的出現次數\n            cnt[0] = 1;                   // 空前綴，處理從頭開始的子陣列\n            int pre = 0, ans = 0;\n            for (int x : nums) {\n                    pre += x;             // 更新前綴和\n                    ans += cnt[pre - k];  // 若 pre - k 曾出現，代表存在一段子陣列和為 k\n                    cnt[pre]++;\n            }\n            return ans;\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- 若當前前綴和為 `pre`，要找和為 k 的左端點，就需要之前出現過 `pre - k`。\n- 前綴狀態先保存空前綴，讓從下標 0 開始的區間也能統一處理。\n- 查詢區間時只做兩個前綴狀態相減；計數題則查找歷史中需要的前綴值。\n\n## 時間與空間複雜度\n\n| 題目 | 時間 | 空間 | 備註 |\n| --- | --- | --- | --- |\n| 8 atoi / 65 有效數字 | $O(n)$ | $O(1)$（DFA 表是常數） | 單次掃描，每字元一次表格查詢 |\n| 12 整數轉羅馬 | $O(\\log n)$ | $O(1)$ | 至多 13 個值、輸出長度 $\\le 15$ |\n| 13 羅馬轉整數 | $O(n)$ | $O(1)$ | 單次掃描 |\n| 68 文字左右對齊 | $O(\\sum \\lvert w_i \\rvert)$ | $O(\\text{輸出})$ | 每個單字被讀 $O(1)$ 次 |\n| 146 LRU | **$O(1)$ 均攤**（定理 3） | $O(\\mathrm{capacity})$ | splice 是指標改接 |\n| 273 整數轉英文 | $O(\\log n)$ | $O(1)$ | 至多 4 組 |\n| 420 強密碼（定理 5） | $O(n)$ | $O(n/3)$ 存段長 | 掃一次求段長 + 一次貪心分配 |\n| 460 LFU | **$O(1)$ 均攤**（定理 4） | $O(\\mathrm{capacity})$ | `minFreq` 只 +1 或重設 |\n| 2468 分割訊息（定理 6） | $O(n)$ | $O(n)$ 輸出 | 按位數分組枚舉 $b$ |\n\n**兩點值得強調**：\n\n- **146／460 的 $O(1)$ 是真 $O(1)$，不是「$O(\\log n)$ 但常數小」。** 它靠的是「雜湊表 $O(1)$ 定位 + 串列 $O(1)$ 改接」的組合。**若把 `list` 換成 `set` 或平衡樹，就退化成 $O(\\log n)$**——面試中這是常被追問的一點。\n- **這一類題的實際瓶頸幾乎不是複雜度，而是常數與程式碼長度。** 68 文字對齊的 $O(n)$ 毫無爭議，難的是把「最後一行左對齊」「單字間空格盡量平均、多的放左邊」「只有一個單字時右填空格」三條規則同時做對。**寫這類題時，把時間花在列規則表上，不要花在優化上。**\n\n## 常見錯誤與邊界條件\n\n- 前綴陣列常開 n+1，讓空前綴為 0。\n- 區間 `[l, r]` 對應 `pre[r + 1] - pre[l]`。\n- 含負數時不能用滑動視窗替代前綴和。\n- 和可能溢位時用 long long。\n\n## 常見變形\n\n- 一維前綴和、二維前綴和。\n- 前綴和 + hash map 統計子陣列數。\n- 前綴和 + 有序集合處理上下界或最大不超過 k。\n\n## 與相似技巧的比較\n\n- **與 13.1 綜合題**：13.1 的難點是**設計狀態**（要創造），13.2 的難點是**窮盡規則**（要完備）。**兩者的檢查方式相反**：13.1 靠「構造反例」證明狀態不完備（定理 1 of 13.1），13.2 靠「列表格找空格」證明規則不完整（定理 1）。**一個是找漏洞，一個是填格子。**\n- **與 6 節堆（優先佇列）**：LRU／LFU 看起來像「取最小」，很容易想用堆。**但堆的 `decrease-key` 需要 $O(\\log n)$ 且標準 `priority_queue` 不支援**；LFU 靠「頻率只 +1」這個前提（定理 4 前提一）把它降成 $O(1)$ 的桶操作。**當鍵值的變化量有界且很小時，桶（counting）永遠打敗堆。**\n- **與 2 節字首和**：2468 用位數分組 + 前綴和把 $\\sum_{i=1}^{b}|i|$ 壓成 $O(1)$ 查詢。**規則題裡也常常需要這種小優化，但那是最後 5% 的事。**\n- **與貪心（greedy 章）**：420 與 2468 是**偽裝成模擬題的最優化題**。**判準：題目問「最少操作次數」或「最小的 $b$」⇒ 需要證明，不是模擬。** 反過來，2532 過橋（13.1 定理 5）問的是「最後時刻」而策略由題目給定 ⇒ 是模擬，不需要證明。**看題目問的是「最少」還是「結果」。**\n- **與正規表示式**：65 可以用 `std::regex` 一行解決，但**面試與競賽都會要求手寫 DFA**——因為 DFA 讓「規則是否完整」變成可檢查的表格（定理 1），而正規表示式把它藏起來了。**手寫 DFA 也是 $O(n)$，`std::regex` 的回溯反而可能更慢。**\n- **與設計題（18 節）**：146／460 屬於設計題，差別在於**設計題通常要你自己選資料結構組合**，而本節強調的是**選定之後那條不變量為什麼成立**（定理 3、4）。**能說出「splice 保證迭代器有效」與「minFreq 只會 +1」的人，和只會背模板的人，在追問下差別立現。**\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 8 | [字串轉換整數 (atoi)](https://leetcode.cn/problems/string-to-integer-atoi) |  |\n| 12 | [整數轉羅馬數字](https://leetcode.cn/problems/integer-to-roman) |  |\n| 13 | [羅馬數字轉整數](https://leetcode.cn/problems/roman-to-integer) |  |\n| 65 | [有效數字](https://leetcode.cn/problems/valid-number) |  |\n| 68 | [文字左右對齊](https://leetcode.cn/problems/text-justification) |  |\n| 146 | [LRU 快取](https://leetcode.cn/problems/lru-cache) |  |\n| 273 | [整數轉換英文表示](https://leetcode.cn/problems/integer-to-english-words) |  |\n| 420 | [強密碼檢驗器](https://leetcode.cn/problems/strong-password-checker) |  |\n| 460 | [LFU 快取](https://leetcode.cn/problems/lfu-cache) |  |\n| 2468 | [根據限制分割訊息](https://leetcode.cn/problems/split-message-based-on-limit) | 2382 |\n\n## 本節重點速查\n\n- **定理 1（完整劃分）**：分支條件必須**不重不漏**地覆蓋輸入空間。**除錯流程：列成表格，逐一問「這個輸入落在哪一格」。** DFA 之所以優於 `if-else` 串，是因為**表格的空格看得見**。前提：DFA 只管格式，數值範圍（atoi 的溢位截斷）是另一條規則。\n- **定理 2（12 羅馬數字）**：貪心 = 逐位查表。**證明靠兩步：組間不干擾（餘數逐級變小）+ 組內只有 10 個情況（有限驗證）。** 13 的反向掃描依賴「小值在前只出現在 6 個減法組合」——**這條由題目保證輸入合法。**\n- **定理 3（146 LRU）**：不變量 (L) = `pos[k]` 指向 `lst` 中唯一的 $k$ 節點，`lst` 前端最新。**成立的前提是 `std::list::splice` 保證迭代器有效**——這是選 `list` 而非 `vector`／`deque` 的唯一理由。**淘汰時先刪 `pos` 條目再 `pop_back`，反了就是 UB。**\n- **定理 4（460 LFU）**：不變量 (F) = `minFreq` 恆為當前最小頻率。**桶變空時只需 `minFreq++`，依賴兩個前提：頻率一次只 +1、一次只改一個 key。** 缺一就得重掃所有桶。**LFU = LRU 再加一層頻率索引。**\n- **定理 5（420 強密碼）**：**這是貪心不是模擬。** $\\mathrm{len}<6 \\Rightarrow \\max(6-\\mathrm{len},t)$；$6\\le\\mathrm{len}\\le20 \\Rightarrow \\max(r,t)$；$\\mathrm{len}>20 \\Rightarrow d + \\max(r\',t)$，刪除**優先給 $L\\bmod3=0$ 的段（1 刪換 1 替換），再 $L\\bmod3=1$（2 刪），再其餘（3 刪）**。交換論證的上界：**一次刪除最多省 1 次替換。** 前提：$d$ 是固定的。\n- **定理 6（2468 分割訊息）**：容量 $C(b)=\\sum_i(\\mathrm{limit}-3-|i|-|b|)$ **對 $b$ 不單調**（$\\mathrm{limit}=10$ 時 $C(10)=39<45=C(9)$）⇒ **不能二分，要從小到大枚舉 $b$。** 逐段條件 $\\mathrm{cap}_i(b)\\ge1$ 不能只用總和取代。**教訓：參數自我指涉時，先驗證單調性再談二分。**\n- **判別「模擬」與「最優化」**：**題目問「最少／最小」⇒ 要證明；問「結果是什麼」且策略由題目給定 ⇒ 忠實實作即可。**\n',
        },
      ],
    },
    {
      id: 9006,
      title: "14. 常見面試模式",
      summary:
        "常見面試題往往不是考某個冷僻演算法，而是考你能否從題目敘述中**辨認出模式**，並套用對應的核心做法。本節整理最常出現的幾種模式，幫你在讀完題目後快速對號入座。\n\n辨認模式的關鍵是抓「訊號」：例如看到「連續子陣列的和」就想到前綴和，看到「前 K 大／第 K 小」就想到堆積，看到「動態合併集合」就想到並查集。把訊號和做法對應起來，解題速度會大幅提升。\n\n| 模式 | 訊號 | 核心做法 | 代表題 |\n| --- | --- | --- | --- |\n| 前綴和 + 雜湊 | 連續子陣列的和、餘數、可整除 | 前綴和邊掃邊用雜湊記錄出現次數，查互補前綴 | [560 和為 K 的子陣列](https://leetcode.cn/problems/subarray-sum-equals-k/) |\n| 堆積 Top-K | 前 K 大／第 K 小、資料流中的中位數 | 用大小為 K 的堆維護候選，或雙堆 | [215 陣列中的第 K 個最大元素](https://leetcode.cn/problems/kth-largest-element-in-an-array/) |\n| 並查集 | 動態合併群組、判斷連通、判環 | 路徑壓縮 + 按秩合併，近 `O(1)` 合併查詢 | [547 省份數量](https://leetcode.cn/problems/number-of-provinces/) |\n| 單點更新 + 前綴查詢 | 邊改單點、邊查前綴和／區間和 | 樹狀陣列（BIT），`O(log n)` 更新查詢 | [307 區域和檢索 - 陣列可修改](https://leetcode.cn/problems/range-sum-query-mutable/) |\n| 區間更新 + 區間查詢 | 區間整段加值、區間查詢最值或和 | 線段樹 + 懶標記（lazy），`O(log n)` | [307 區域和檢索 - 陣列可修改](https://leetcode.cn/problems/range-sum-query-mutable/) |\n| 雜湊 + 鏈結串列設計 | O(1) 取用且要淘汰最舊／最少用 | 雜湊表定位 + 雙向鏈結串列維護順序 | [146 LRU 快取](https://leetcode.cn/problems/lru-cache/) |\n| 快慢指針 | 鏈結串列找環、找中點、找倒數第 K | 兩指針不同速度前進 | [141 環形鏈結串列](https://leetcode.cn/problems/linked-list-cycle/) |\n| 掃描線 | 區間重疊計數、會議室、天際線 | 把端點排序後依序掃過，用差分或堆維護當前狀態 | [253 會議室 II](https://leetcode.cn/problems/meeting-rooms-ii/) |\n\n**小提醒**\n\n- 同一題常常可以用多種模式解，先想最直接的訊號，再考慮複雜度需求是否要升級資料結構。\n- 「訊號」是關鍵字觸發器，但最終要看資料規模：`n` 很小時暴力即可，`n` 很大（如 $10^5$ 以上）才需要 `O(n log n)` 或更佳的結構。\n- 前綴和、堆積、雜湊這三招覆蓋面最廣，務必練到反射動作。",
    },
    {
      id: 9106,
      title: "18. 設計題（Design）",
      summary:
        '設計題考的是「組合多種資料結構達到指定複雜度」的能力。題目通常規定每個操作要 `O(1)` 或 `O(log n)`，你得選對結構並把它們黏合起來。最常見的組合是「雜湊定位 + 另一種結構維護順序」。\n\n**雜湊 + 雙向鏈結串列（LRU）**\n\n:::example LC 146 LRU 快取：unordered_map 定位 + std::list 維…\nLC 146 LRU 快取：要求 `get` 與 `put` 都 `O(1)`，並能淘汰「最久未使用」的項目。做法是用雜湊表做 `key -> 節點` 的 `O(1)` 定位，用雙向鏈結串列維護使用順序（最近用的放頭、最久的放尾）。C++ 可直接用 `std::list` + `unordered_map` 黏合。\n\n```cpp\n// LC 146 LRU 快取：unordered_map 定位 + std::list 維護順序\nclass LRUCache {\n    int cap;\n    list<pair<int, int>> dll;  // 前端=最近使用，存 (key, value)\n    unordered_map<int, list<pair<int, int>>::iterator>\n                    mp;  // key -> 串列節點迭代器\npublic:\n    LRUCache(int capacity) : cap(capacity) {}\n    int get(int key) {\n            auto it = mp.find(key);\n            if (it == mp.end()) return -1;\n            dll.splice(dll.begin(), dll, it->second);  // 移到前端：O(1)\n            return it->second->second;\n    }\n    void put(int key, int value) {\n            auto it = mp.find(key);\n            if (it != mp.end()) {\n                    it->second->second = value;\n                    dll.splice(dll.begin(), dll, it->second);\n                    return;\n            }\n            if ((int)dll.size() == cap) {  // 滿了：淘汰尾端\n                    mp.erase(dll.back().first);\n                    dll.pop_back();\n            }\n            dll.push_front({key, value});\n            mp[key] = dll.begin();\n    }\n};\n```\n:::\n\n\n\nLFU 快取（LC 460）更進一步：再加一層「頻率桶」，每個頻率對應一條鏈結串列。\n\n**輔助堆疊（最小堆疊）**\n\n:::example LC 155 最小堆疊：主堆疊 + 輔助堆疊同步維護最小值\nLC 155 最小堆疊：要在 `O(1)` 內回傳當前最小值。用一個輔助堆疊同步記錄「到目前為止的最小值」。\n\n```cpp\n// LC 155 最小堆疊：主堆疊 + 輔助堆疊同步維護最小值\nclass MinStack {\n    stack<int> st, mn;  // mn.top() 永遠是當前最小\npublic:\n    void push(int x) {\n            st.push(x);\n            mn.push(mn.empty() ? x : min(mn.top(), x));\n    }\n    void pop() {\n            st.pop();\n            mn.pop();\n    }\n    int top() { return st.top(); }\n    int getMin() { return mn.top(); }\n};\n```\n:::\n\n\n\n**陣列 + 雜湊 O(1) 隨機**\n\n:::example LC 380 O(1) 插入/刪除/隨機：vector + 雜湊定位，刪除靠尾端交換\nLC 380 常數時間插入、刪除和取隨機：用 `vector` 存值、`unordered_map` 存 `值 -> 索引`。刪除時把目標和最後一個元素交換再 `pop_back`，達到 `O(1)`。\n\n```cpp\n// LC 380 O(1) 插入/刪除/隨機：vector + 雜湊定位，刪除靠尾端交換\nclass RandomizedSet {\n    vector<int> vals;\n    unordered_map<int, int> idx;  // 值 -> 在 vals 的索引\npublic:\n    bool insert(int v) {\n            if (idx.count(v)) return false;\n            idx[v] = vals.size();\n            vals.push_back(v);\n            return true;\n    }\n    bool remove(int v) {\n            auto it = idx.find(v);\n            if (it == idx.end()) return false;\n            int i = it->second, last = vals.back();\n            vals[i] = last;\n            idx[last] = i;  // 把尾端搬到被刪位置\n            vals.pop_back();\n            idx.erase(it);\n            return true;\n    }\n    int getRandom() { return vals[rand() % vals.size()]; }\n};\n```\n:::\n\n\n\n**版本二分（時間鍵值）**\n\n:::example LC 981 基於時間的鍵值儲存：每個 key 存有序的 (timestamp, val…\nLC 981 基於時間的鍵值儲存：每個 key 對應一串「(時間戳, 值)」且時間遞增，查詢「不超過某時間的最新值」用二分搜尋，`get` 是 `O(log n)`。\n\n```cpp\n// LC 981 基於時間的鍵值儲存：每個 key 存有序的 (timestamp, value)，查詢用二分\nclass TimeMap {\n    unordered_map<string, vector<pair<int, string>>>\n                    mp;  // key -> 依時間遞增的紀錄\npublic:\n    void set(string key, string value, int timestamp) {\n            mp[key].push_back({timestamp, value});  // timestamp 嚴格遞增，自動有序\n    }\n    string get(string key, int timestamp) {\n            auto& v = mp[key];\n            // 找最後一個 timestamp <= 查詢值\n            int lo = 0, hi = v.size();\n            while (lo < hi) {\n                    int mid = (lo + hi) / 2;\n                    if (v[mid].first <= timestamp)\n                            lo = mid + 1;\n                    else\n                            hi = mid;\n            }\n            return lo == 0 ? "" : v[lo - 1].second;\n    }\n};\n```\n:::\n\n\n\n**模式表**\n\n| 模式 | 訊號 | 核心做法 | 代表題 |\n| --- | --- | --- | --- |\n| 雜湊 + 雙向鏈結串列 | O(1) 取用且要淘汰 | map 定位 + list 維護順序 | [146 LRU 快取](https://leetcode.cn/problems/lru-cache/) |\n| 輔助堆疊 | O(1) 查當前最值 | 同步維護一個最值堆疊 | [155 最小堆疊](https://leetcode.cn/problems/min-stack/) |\n| 陣列 + 雜湊 | O(1) 插入/刪除/隨機 | 尾端交換刪除 | [380 O(1) 時間插入刪除取隨機](https://leetcode.cn/problems/insert-delete-getrandom-o1/) |\n| 版本二分 | 依時間查最新值 | 有序紀錄 + 二分 | [981 基於時間的鍵值儲存](https://leetcode.cn/problems/time-based-key-value-store/) |\n\n**搭配練習**\n\n| ID | Problem | Rating | Labels |\n| --- | --- | --- | --- |\n| 146 | [LRU Cache](https://leetcode.cn/problems/lru-cache/) | N/A | 雜湊 + 雙向鏈結串列 |\n| 460 | [LFU Cache](https://leetcode.cn/problems/lfu-cache/) | N/A | 頻率桶 + 雙向鏈結串列 |\n| 155 | [Min Stack](https://leetcode.cn/problems/min-stack/) | N/A | 輔助堆疊 |\n| 295 | [Find Median from Data Stream](https://leetcode.cn/problems/find-median-from-data-stream/) | N/A | 雙堆 |\n| 355 | [Design Twitter](https://leetcode.cn/problems/design-twitter/) | N/A | 雜湊 + 堆合併 |\n| 380 | [Insert Delete GetRandom O(1)](https://leetcode.cn/problems/insert-delete-getrandom-o1/) | N/A | 陣列 + 雜湊 |\n| 622 | [Design Circular Queue](https://leetcode.cn/problems/design-circular-queue/) | N/A | 環形緩衝區 |\n| 705 | [Design HashSet](https://leetcode.cn/problems/design-hashset/) | N/A | 分桶雜湊 |\n| 706 | [Design HashMap](https://leetcode.cn/problems/design-hashmap/) | N/A | 分桶雜湊 |\n| 981 | [Time Based Key-Value Store](https://leetcode.cn/problems/time-based-key-value-store/) | 1575 | 版本紀錄 + 二分 |\n| 1472 | [Design Browser History](https://leetcode.cn/problems/design-browser-history/) | 1454 | 陣列游標 / 雙棧 |\n| 1912 | [Design Movie Rental System](https://leetcode.cn/problems/design-movie-rental-system/) | 2182 | 多索引有序集合 |\n| 2102 | [Sequentially Ordinal Rank Tracker](https://leetcode.cn/problems/sequentially-ordinal-rank-tracker/) | 2159 | 排名查詢 / 雙堆 |\n| 2353 | [Design a Food Rating System](https://leetcode.cn/problems/design-a-food-rating-system/) | 1782 | 多索引有序集合 |\n| 3408 | [Design Task Manager](https://leetcode.cn/problems/design-task-manager/) | 1807 | 任務索引 / 懶刪除堆 |\n| 3484 | [Design Spreadsheet](https://leetcode.cn/problems/design-spreadsheet/) | 1524 | 表格狀態解析 |\n| 3508 | [Implement Router](https://leetcode.cn/problems/implement-router/) | 1851 | 固定容量佇列 + 有序索引 |\n| 3709 | [Design Exam Scores Tracker](https://leetcode.cn/problems/design-exam-scores-tracker/) | 1648 | 分數追蹤 / 前綴索引 |\n\n**小提醒**\n\n- 設計題先看每個操作的目標複雜度，複雜度直接決定了要選哪些結構。\n- 「O(1) 取用 + 維護順序」幾乎都是雜湊 + 鏈結串列的組合，背起來。\n- 用 `std::list::splice` 可以 `O(1)` 把節點移到串列任意位置，是 LRU 的關鍵；不要用搬移元素的容器（如 `vector`）來維護順序。',
    },
  ],
} as TutorialData.Root;
