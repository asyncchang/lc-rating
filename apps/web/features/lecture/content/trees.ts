import type { TutorialData } from "@/types";

export const trees = {
  id: 1,
  title: "鏈結串列、二元樹與回溯（前後指標/快慢指標/DFS/BFS/直徑/LCA/一般樹）",
  src: "https://leetcode.cn/circle/discuss/K0n2gO/",
  last_update: "2025-11-07T23:47:26.098808+00:00",
  summary:
    "本題單涵蓋連結串列、二叉樹、一般樹、回溯與分治五大主題，系統性地整理各類經典演算法與資料結構操作。\n\n**本題單涵蓋**：\n- 前後指標\n- 快慢指標\n- DFS\n- BFS\n- 直徑\n- LCA\n- 一般樹\n\n:::example 反轉連結串列（迭代）\n**C++ 模板（反轉連結串列）**：\n\n```cpp\n// 反轉連結串列（迭代）\nListNode* reverseList(ListNode* head) {\n    ListNode* prev = nullptr;\n    ListNode* curr = head;\n    while (curr) {\n        ListNode* next = curr->next;\n        curr->next = prev;  // 反轉指向\n        prev = curr;\n        curr = next;\n    }\n    return prev;\n}\n```\n:::\n",
  children: [
    {
      id: 2,
      title: "1. 連結串列",
      summary:
        "涵蓋連結串列的遍歷、刪除、插入、反轉、前後指標、快慢指標、雙指標、合併、分治及綜合應用，包含會員題與經典題型。\n\n:::example 遍歷連結串列\n**C++ 模板（遍歷與反轉）**：\n\n```cpp\n// 遍歷連結串列\nvoid traverse(ListNode* head) {\n    for (ListNode* cur = head; cur; cur = cur->next) {\n        // 處理 cur->val\n    }\n}\n\n// 反轉連結串列（迭代）\nListNode* reverseList(ListNode* head) {\n    ListNode* prev = nullptr;\n    ListNode* curr = head;\n    while (curr) {\n        ListNode* next = curr->next;\n        curr->next = prev;\n        prev = curr;\n        curr = next;\n    }\n    return prev;\n}\n```\n:::",
      children: [
        {
          id: 3,
          title: "1.1 遍歷連結串列",
          summary:
            "## 這個技術解決什麼問題\n\n連結串列沒有隨機存取：想拿第 $i$ 個節點只能從 `head` 走 $i$ 步。若照著陣列的思路寫「對每個 $i$ 取出第 $i$ 個節點再處理」，等於做了 $0+1+\\dots+(n-1)$ 步，退化成 $O(n^2)$。\n\n遍歷的槓桿在於**把答案改寫成一個可以邊走邊更新的摘要**：只保留一個游標與一份累積量，走一趟就得到答案，$O(n)$ 時間、$O(1)$ 額外空間。代價是失去回頭的能力——凡是需要「往後看」的資訊，都得換成前後指標或兩趟掃描。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就往「遍歷連結串列」想：\n\n- **輸入是一條連結串列，而你要的答案只要「看過每個節點一次」就能算出。** 例如數節點、求總和、把節點值拼成一個數（1290）、找出符合某條件的節點位置（2058）。\n- **答案可以表示成一個邊走邊維護的摘要**，不需要回頭、也不需要隨機跳到第 k 個節點。\n- **你發現自己想寫 `list[i]` 卻發現連結串列沒有這種隨機存取**——這正提醒你只能老老實實沿著 `next` 走一遍。\n\n:::example 嗅到味道的瞬間：你只需要一個游標，從頭順順走到尾\n```cpp\n// 嗅到味道的瞬間：你只需要一個游標，從頭順順走到尾\n// for (ListNode* cur = head; cur; cur = cur->next) { ... }\n// 沒有回頭、沒有跳格，就是純遍歷\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我邊走邊要維護的「摘要」是什麼？（計數器？累積值？上一個節點？）\n2. 我在走到每個節點時，要怎麼用它更新摘要？\n3. 走到 `nullptr` 時，摘要是不是就等於我要的答案？\n\n三題都答得出來，這個技巧就套對了。\n\n## 核心想法與直覺\n\n本節要解決的場景是：資料不像陣列那樣排成一整排存在記憶體裡，而是被切成一個個「節點（node）」，每個節點除了存自己的值，還存一個指向「下一個節點」的箭頭（指標）。這種結構叫做「連結串列（linked list）」。\n\n:::example 連結串列節點：值 val + 指向下一個節點的指標 next\n先看清楚一個節點長什麼樣，以及它們怎麼串起來：\n\n```cpp\n// 連結串列節點：值 val + 指向下一個節點的指標 next\nstruct ListNode {\n    int val;\n    ListNode* next;  // 指向下一個節點；最後一個節點的 next 是 nullptr（空）\n};\n```\n:::\n\n\n\n:::example 遍歷：從頭走到尾，把每個節點都看過一遍\n因為節點之間只靠 `next` 箭頭連著，你沒辦法像陣列那樣用 `a[i]` 直接跳到第 5 個。想看到所有資料，唯一的辦法就是**從頭節點出發，沿著 `next` 一個一個往下走，直到走到 `nullptr`（串列結尾）為止**。這個「沿著箭頭走完整條串列」的動作就是「遍歷（traverse）」。\n\n```cpp\n// 遍歷：從頭走到尾，把每個節點都看過一遍\nfor (ListNode* cur = head; cur != nullptr; cur = cur->next) {\n    // 這裡可以讀 cur->val，或一邊走一邊累積某個摘要\n}\n```\n:::\n\n\n\n為什麼這樣就夠用？因為很多題目要的答案，其實只要「走一趟、邊走邊記東西」就能算出來：例如數有幾個節點、把每個位元拼成一個整數、或記住上一個節點的值來和現在比較。關鍵心法是：**選一個你從頭掃到尾就能維護的「摘要」**（一個計數器、一個累積值、上一個節點等），讓答案在走到結尾時自然成形，而不必來回重掃。整趟遍歷只走一遍，時間是 `O(n)`。\n\n## 狀態／資料結構定義\n\n- `cur`：游標，語意是「下一個尚未併入摘要的節點」。`cur == nullptr` 表示已走完。\n- `acc`：摘要，型別與語意由題目決定（計數器、累加值、拼出的整數、目前最佳節點）。\n- 摘要必須帶有一個**單位元**（計數器為 `0`、乘積為 `1`、最佳值為哨兵），代表「空前綴的答案」。\n\n關鍵約束：`acc` 必須能由「舊的 `acc`」與「`cur` 這一個節點」單獨算出，不得依賴尚未走到的節點。\n\n## 不變量或正確性證明\n\n迴圈不變量：**每次檢查迴圈條件時，`acc` 恰等於 `head` 到 `cur` 前一個節點這段前綴的答案。**\n\n- 初始化：`cur = head` 時前綴為空，`acc` 設為單位元，成立。\n- 保持：假設進入迴圈本體前 `acc` 是前綴 $P$ 的答案。本體用 `cur` 更新 `acc`，再令 `cur = cur->next`；此時前綴變成 $P \\cup \\{cur_{\\text{舊}}\\}$，而更新規則正是「前綴答案 ⊕ 單一節點」，故不變量維持。\n- 終止：`cur == nullptr` 時前綴就是整條串列，因此 `acc` 是全串列的答案。\n\n這段論證依賴一個前提：摘要可增量合併，也就是答案是前綴的函數。若答案依賴後綴（例如「每個節點右邊的最大值」），前綴不變量根本無法表述該答案，此時必須反向遍歷、先反轉、或改用兩趟掃描——不是實作細節沒寫好，而是不變量本身不成立。\n\n另一個隱含前提是串列無環。有環時 `cur` 永遠不會變成 `nullptr`，終止性論證失效，需先用快慢指標判環。\n\n## 逐步演算法\n\n把遍歷類題目拆成三件固定的事，逐一搭配程式碼。\n\n:::example 狀態表示：以「把二進位拼成整數」為例（LeetCode 1290）\n**1. 狀態表示：想清楚「邊走邊要記什麼」。** 這就是你的摘要，常見的有計數器、累積值、或「上一個節點」。\n\n```cpp\n// 狀態表示：以「把二進位拼成整數」為例（LeetCode 1290）\nint result = 0;  // 邊走邊把目前累積的數字記在這裡\n```\n:::\n\n\n\n:::example 更新：每走到一個節點，就把它的位元接到結果的最低位（1290）\n**2. 更新流程：固定一個游標 `cur`，每步只更新摘要再前進。** 游標每次往 `cur->next` 移動一格，移動前先用目前節點更新摘要。\n\n```cpp\n// 更新：每走到一個節點，就把它的位元接到結果的最低位（1290）\nfor (ListNode* cur = head; cur; cur = cur->next) {\n    result = result * 2 + cur->val;  // 左移一位再加上目前位元\n}\n```\n:::\n\n\n\n:::example 答案讀取：迴圈結束時，摘要就是要回傳的東西\n**3. 答案讀取：走到 `nullptr` 後，摘要本身就是答案。** 例如 LeetCode 2058「臨界點」是邊走邊比較「前一個、現在、後一個」三者，記下臨界點位置；2181「合併零之間的節點」則是邊走邊把零之間的值加起來。\n\n```cpp\n// 答案讀取：迴圈結束時，摘要就是要回傳的東西\nreturn result;  // 整條串列已遍歷完畢，result 即為答案\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 空串列（`head == nullptr`）要先處理，否則一開始就解參考 `nullptr` 會當掉。\n- 需要比較相鄰節點時（如 2058），要小心 `cur->next` 是否存在，避免讀到空指標。\n- 複雜度：只走一趟，時間 `O(n)`、額外空間 `O(1)`（n 為節點數）。\n\n## C++17 模板\n\n以下用同型延伸示範題 206. 反轉連結串列 展示核心寫法。\n\n**完整題目**：給定單向連結串列的頭節點，反轉整條串列並回傳新的頭節點。\n\n:::example 迭代反轉：prev 指向已反轉部分的頭節點，head 為待處理節點\n**線索**：題目中的「題目要求刪除、插入、反轉、合併或找第 k 個節點。」是主要線索；先把它翻成狀態、候選集合或判斷函式，再套用本節 pattern。\n\n```cpp\nclass Solution {\npublic:\n    ListNode* reverseList(ListNode* head) {\n            // 迭代反轉：prev 指向已反轉部分的頭節點，head 為待處理節點\n            ListNode* prev = nullptr;\n            while (head) {\n                    ListNode* nxt = head->next;  // 暫存下一節點，避免斷鏈後遺失\n                    head->next = prev;           // 將當前節點指向已反轉部分\n                    prev = head;                 // prev 前移\n                    head = nxt;                  // head 移至下一節點\n            }\n            return prev;  // prev 即新鏈結串列的頭節點\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- 每步先保存下一個節點，再反轉當前指標；連結串列題的核心是不要丟失後續鏈。\n- `dp` 變數代表已處理前綴或子問題的答案，不是模糊的目前最佳值。\n- 迴圈順序必須保證轉移來源已經算好；壓縮空間時更要確認讀到的是舊值還是新值。\n\n## 時間與空間複雜度\n\n每個節點恰被 `cur` 指到一次，單次更新為 $O(1)$，故時間 $O(n)$，$n$ 是**節點數**而非值域。空間 $O(1)$：只有游標與摘要，遞迴寫法則是 $O(n)$ 堆疊，長串列會爆堆疊，這是偏好迴圈的實際理由。\n\n## 常見錯誤與邊界條件\n\n- 改 `next` 前先保存後繼。\n- dummy 節點可避免特判 head。\n- 循環條件用 `node && node->next` 時先確認訪問順序。\n- 返回值可能是新 head，不一定是原 head。\n\n## 常見變形\n\n- 快慢指標找中點或倒數第 k。\n- dummy head 處理刪除頭節點。\n- 分治合併多條有序鏈。\n\n## 與相似技巧的比較\n\n- 對照陣列遍歷：陣列能雙向走、能隨機跳，連結串列只能單向前進一次，所以「能否寫成前綴摘要」變成能不能用這招的分水嶺。\n- 要「倒數第 $k$ 個」不必先數長度，用前後指標（1.5）一趟完成。\n- 要判環或找中點，用快慢指標（1.6）。\n- 要修改結構而非只讀取，改用刪除（1.2）、插入（1.3）的改箭頭手法。\n\n## 代表例題\n\n**題號**：725、817、1290、2058、2181\n\n放在「遍歷連結串列」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 725 | [分隔連結串列](https://leetcode.cn/problems/split-linked-list-in-parts/) |  |\n| 817 | [連結串列元件](https://leetcode.cn/problems/linked-list-components/) |  |\n| 1290 | [二進位制連結串列轉整數](https://leetcode.cn/problems/convert-binary-number-in-a-linked-list-to-integer/) | 1151 |\n| 2058 | [找出臨界點之間的最小和最大距離](https://leetcode.cn/problems/find-the-minimum-and-maximum-number-of-nodes-between-critical-points/) | 1311 |\n| 2181 | [合併零之間的節點](https://leetcode.cn/problems/merge-nodes-in-between-zeros/) | 1333 |\n| 3062 | [連結串列遊戲的獲勝者](https://leetcode.cn/problems/winner-of-the-linked-list-game/) |  |\n| 3063 | [連結串列頻率](https://leetcode.cn/problems/linked-list-frequency/) |  |\n| 3263 | [將雙鏈錶轉換為陣列 I](https://leetcode.cn/problems/convert-doubly-linked-list-to-array-i/) |  |\n| 3294 | [將雙鏈錶轉換為陣列 II](https://leetcode.cn/problems/convert-doubly-linked-list-to-array-ii/) |  |\n\n## 本節重點速查\n\n- 一個游標 `cur` 加一份摘要 `acc`；\n- 不變量是「`acc` = 已走過前綴的答案」；\n- 只有當答案能寫成前綴的函數時才適用，否則改用雙指標或兩趟掃描。",
        },
        {
          id: 4,
          title: "1.2 刪除節點",
          summary:
            "## 這個技術解決什麼問題\n\n從陣列刪一個元素要把後面全部往前搬，$O(n)$；刪 $k$ 個就是 $O(nk)$。連結串列的節點只靠 `next` 相連，**刪除等於改一條邊**：讓前一個節點跳過被刪者，$O(1)$ 完成，走一趟就能刪掉所有符合條件的節點，總計 $O(n)$。\n\n真正的麻煩不是刪除本身，而是「頭節點也可能被刪」——頭沒有前一個節點，於是程式碼被迫分裂成兩套邏輯。dummy head 就是為了消掉這個特例。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就往「刪除節點」想：\n\n- **題目要你從連結串列中拿掉符合某條件的節點**：等於某值（203）、重複（83、82）、或指定位置。\n- **你發現「改箭頭」就能達成，不需要搬資料。** 這正是連結串列刪除的特徵——`O(1)` 改一條邊即可。\n- **頭節點可能也要被刪，讓你開始想寫特例**——這就是該拿出 dummy head 的訊號：\n\n:::example 嗅到味道的瞬間：頭可能被刪，於是先架一個 dummy\n```cpp\n// 嗅到味道的瞬間：頭可能被刪，於是先架一個 dummy\n// ListNode dummy; dummy.next = head;  prev = &dummy;\n// 之後所有節點都有「前一個」，不必為頭寫特例\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 改箭頭之前，我有沒有先抓住「下一個節點」、避免弄丟後面的鏈？\n2. 我需不需要 dummy head 來統一處理「頭也被刪」的情況？\n3. 該刪的時候 `prev` 是不是「不前進」，該留的時候才前進？\n\n三題都答得出來，這個技巧就套對了。\n\n## 核心想法與直覺\n\n**延伸講解**：[刪除鏈表重複節點【基礎演算法精講 08】](https://www.bilibili.com/video/BV1VP4y1Q71e/)\n\n本節要解決的場景是：在一條連結串列裡「刪掉某個節點」。和陣列不同，連結串列刪節點不用搬動一大堆元素，只要**把箭頭改個方向**——讓被刪節點的前一個節點，直接指向被刪節點的後一個節點，那個被「跳過」的節點就等於從串列裡消失了。\n\n:::example 刪除核心：讓 prev 跳過 cur，直接指向 cur 的下一個\n先看刪除的核心動作：\n\n```cpp\n// 刪除核心：讓 prev 跳過 cur，直接指向 cur 的下一個\nprev->next = cur->next;  // cur 被跳過，從串列裡消失\n                                                  // delete cur;            // 若需要釋放記憶體可在此進行\n```\n:::\n\n\n\n這個技巧的精髓只有一句話：**改箭頭之前，一定要先抓住「下一個節點」，否則一改就把後面整條鏈弄丟了。** 因為連結串列只能單向往前，一旦 `prev->next` 被改掉、又沒人記得原本的下一個是誰，後面的節點就再也找不到了。\n\n:::example 虛擬頭節點：讓頭節點也有「前一個」，統一處理\n第二個關鍵是**虛擬頭節點（dummy head）**。如果要刪的剛好是第一個節點，那它沒有「前一個」可以改箭頭，會變成特例。解法是在真正的頭前面接一個假節點 `dummy`，讓每個節點（包含原本的頭）都保證有「前一個」，程式就不必為頭節點寫特殊分支。\n\n```cpp\n// 虛擬頭節點：讓頭節點也有「前一個」，統一處理\nListNode dummy;\ndummy.next = head;  // dummy 之後才是真正的串列\n          // 最後回傳 dummy.next（可能新的頭已經不是原本的 head 了）\n```\n:::\n\n\n\n像 LeetCode 203「移除連結串列元素」就是邊走邊把值等於目標的節點用上面的方式跳過；83 / 82「刪除排序串列中的重複元素」則是利用「已排序、重複的會相鄰」，比較相鄰節點來決定是否跳過。整趟只走一遍，時間 `O(n)`。\n\n## 狀態／資料結構定義\n\n- `ListNode dummy; dummy.next = head;`：哨兵節點，語意是「一個保證不會被刪、且排在真正頭節點之前的位置」。\n- `prev`：指向**已確定保留**的最後一個節點，初始為 `&dummy`。\n- `prev->next`：待判定的節點。\n- 回傳值一律是 `dummy.next`，而不是原本的 `head`——`head` 可能已被刪掉。\n\n不變量所依賴的語意：`dummy -> ... -> prev` 這段是最終答案的前綴，且已經定案。\n\n## 不變量或正確性證明\n\n迴圈不變量：**每次檢查迴圈條件時，從 `dummy` 到 `prev` 的這條鏈，恰好是「原串列前綴中所有應保留節點」依原順序串成的鏈，且 `prev->next` 指向原串列中下一個待判定的節點。**\n\n- 初始化：`prev = &dummy`，保留鏈為空，`prev->next = head` 是第一個待判定者，成立。\n- 保持：設 `cur = prev->next`。若 `cur` 該刪，執行 `prev->next = cur->next`；保留鏈不變（仍以 `prev` 結尾），而待判定者變成原串列中 `cur` 的後繼，兩個條件都維持。若 `cur` 該留，執行 `prev = cur`；保留鏈末端接上 `cur`（它確實該留），待判定者變成 `cur->next`，同樣維持。\n- 終止：`prev->next == nullptr` 時待判定集合為空，保留鏈就是所有應保留節點，故 `dummy.next` 是答案。\n\n這裡有兩個容易被略過的前提。其一，刪除判定只能看 `cur` 本身（或已走過的資訊）；像 82 題「重複的全刪」需要看後繼，就得把判定條件改成對 `prev->next` 這一整段等值區塊做，不能逐點判。其二，`prev` 永不為 `nullptr`——這正是 dummy 保證的，也是不必為頭節點寫特例的原因。\n\n至於 237 題那種「只給你要刪的節點、拿不到 `prev`」的變形，做法是把後繼的值複製到自己身上再刪後繼；它的正確性依賴「被刪者不是尾節點」，因為尾節點沒有後繼可以借。\n\n## 逐步演算法\n\n把刪除類題目拆成幾個固定步驟，逐一搭配程式碼。\n\n:::example 狀態表示：dummy + prev/cur 兩個游標（以 203 為例）\n**1. 狀態表示：準備 dummy 與兩個游標 `prev`、`cur`。** `prev` 永遠停在「上一個保留下來的節點」，`cur` 是正在檢查的節點。\n\n```cpp\n// 狀態表示：dummy + prev/cur 兩個游標（以 203 為例）\nListNode dummy;\ndummy.next = head;\nListNode* prev = &dummy;  // 已保留的最後一個節點\nListNode* cur = head;     // 正在檢查的節點\n```\n:::\n\n\n\n:::example 更新：值等於 val 就跳過（刪除），否則 prev 前進\n**2. 更新流程：要刪就改 prev 的箭頭，要留就讓 prev 跟上。** 這是刪除的主迴圈，注意改箭頭前已先用 `cur->next` 決定下一步往哪走。\n\n```cpp\n// 更新：值等於 val 就跳過（刪除），否則 prev 前進\nwhile (cur) {\n    if (cur->val == val) {\n        prev->next = cur->next;  // 刪：跳過 cur\n    } else {\n        prev = cur;  // 留：prev 跟上\n    }\n    cur = cur->next;  // cur 永遠前進\n}\n```\n:::\n\n\n\n:::example 答案讀取：dummy.next 才是更新後的新頭\n**3. 答案讀取：回傳 `dummy.next`。** 因為頭節點也可能被刪掉，所以不要回傳原本的 `head`，要回傳 dummy 之後的節點——那才是更新後真正的頭。\n\n```cpp\n// 答案讀取：dummy.next 才是更新後的新頭\nreturn dummy.next;\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 連續多個都要刪時（如 82 要刪掉「所有出現過重複的值」），`prev` 在確定刪除前不能往前移，否則會漏。\n- 特例 237「刪除連結串列中的節點」拿不到 `prev`：作法是把「下一個節點的值複製過來、再刪掉下一個」，等效於刪掉自己。\n- 複雜度：單趟掃描，時間 `O(n)`、額外空間 `O(1)`。\n\n## C++17 模板\n\n```cpp\n// 刪除所有值等於 val 的節點。\nListNode* removeElements(ListNode* head, int val) {\n  ListNode dummy;            // 哨兵：保證每個節點都有「前一個」\n  dummy.next = head;\n\n  ListNode* prev = &dummy;   // 已確定保留的最後一個節點\n  while (prev->next) {\n    if (prev->next->val == val) {\n      prev->next = prev->next->next;  // 跳過：改一條邊即完成刪除\n    } else {\n      prev = prev->next;              // 保留：前緣往前推\n    }\n  }\n  return dummy.next;         // 不能回傳 head，它可能已被刪掉\n}\n```\n\n刪除後若需自行管理記憶體，先用暫存指標抓住被刪節點再改邊，否則改完就找不到它了。\n\n## 時間與空間複雜度\n\n`prev` 單向前進、從不回頭，每個節點最多被判定一次，故時間 $O(n)$。空間 $O(1)$：只有一個哨兵與一個指標，與刪除幾個節點無關。相對地，用陣列做同樣的事需要 $O(n)$ 搬移或額外 $O(n)$ 空間。\n\n## 常見錯誤與邊界條件\n\n- 回傳 `head` 而非 `dummy.next`（頭被刪時就錯）。\n- 刪除後仍執行 `prev = prev->next`，導致連續的目標節點被跳過只刪掉一個。\n- 忘記 `prev->next` 可能為 `nullptr` 就存取 `prev->next->val`。\n- 82 題把「刪重複」寫成逐點比對，只刪掉重複區塊的一部分。\n- `dummy` 宣告成區域變數卻回傳指向它的指標（回傳 `dummy.next` 沒問題，回傳 `&dummy` 就是懸空）。\n- 空串列輸入。\n\n## 與相似技巧的比較\n\n- 插入（1.3）與刪除是同一套「改箭頭」手法的正反面，都靠 dummy 消除頭部特例。\n- 需要刪的是「倒數第 $k$ 個」時，配合前後指標（1.5）一趟定位再刪。\n- 若要刪除的是子串列或需重排順序，改用雙指標拆鏈（1.7）比逐點刪乾淨。\n- 陣列的對應技巧是原地雙指標覆寫；差別在陣列搬資料、連結串列搬指標。\n\n## 代表例題\n\n**題號**：82、83、203、237、1669\n\n放在「刪除節點」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 82 | [刪除排序連結串列中的重複元素 II](https://leetcode.cn/problems/remove-duplicates-from-sorted-list-ii/) |  |\n| 83 | [刪除排序連結串列中的重複元素](https://leetcode.cn/problems/remove-duplicates-from-sorted-list/) |  |\n| 203 | [移除連結串列元素](https://leetcode.cn/problems/remove-linked-list-elements/) |  |\n| 237 | [刪除連結串列中的節點](https://leetcode.cn/problems/delete-node-in-a-linked-list/) |  |\n| 1669 | [合併兩個連結串列](https://leetcode.cn/problems/merge-in-between-linked-lists/) | 1428 |\n| 1836 | [從未排序的連結串列中移除重複元素](https://leetcode.cn/problems/remove-duplicates-from-an-unsorted-linked-list/) |  |\n| 2487 | [從連結串列中移除節點](https://leetcode.cn/problems/remove-nodes-from-linked-list/) | 1455 |\n| 3217 | [從連結串列中移除在陣列中存在的節點](https://leetcode.cn/problems/delete-nodes-from-linked-list-present-in-array/) | 1342 |\n\n## 本節重點速查\n\n- 一律先架 dummy；\n- `prev` 只在「保留」時前進，「刪除」時原地不動；\n- 回傳 `dummy.next`。\n- 不變量是「`dummy` 到 `prev` 已是定案的答案前綴」。\n",
        },
        {
          id: 5,
          title: "1.3 插入節點",
          summary:
            "## 這個技術解決什麼問題\n\n陣列插入要把後面全部往後推，$O(n)$。連結串列插入只要改兩條邊：新節點指向後繼、前驅指向新節點，$O(1)$。這讓「維護一條有序鏈並不斷插入」變成可行——插入排序（147）用陣列是 $O(n^2)$ 次搬移，用連結串列則是 $O(n^2)$ 次比較但每次只改常數條邊。\n\n槓桿在於：**只要能拿到「插入位置的前驅」，插入本身就是常數時間**。因此所有插入題的難點都被壓縮成同一件事——把前驅找出來。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就往「插入節點」想：\n\n- **題目要你把新節點放進串列的某個位置**：依大小排序插入（147）、在每對相鄰節點間插入（2807）、或維持迴圈串列的有序性（708）。\n- **你發現只要改兩條箭頭就能完成**，不像陣列插入要搬動後面一整排。\n- **新節點可能要插在最前面**，讓你想寫特例——這就是該用 dummy head 的訊號：\n\n:::example 嗅到味道的瞬間：插在最前面沒有「前一個」，於是架 dummy\n```cpp\n// 嗅到味道的瞬間：插在最前面沒有「前一個」，於是架 dummy\n// ListNode dummy; dummy.next = head; prev = &dummy;\n// 之後不論插哪裡都有 prev，邏輯統一\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我接箭頭的順序對嗎？（先讓新節點接住後面，前一個才交棒）\n2. 我的 `prev` 怎麼停到「正確的插入位置之前」？\n3. 插完之後游標要不要跳過新節點，避免重複處理？\n\n三題都答得出來，這個技巧就套對了。\n\n## 核心想法與直覺\n\n本節要解決的場景是：在一條連結串列裡「插入一個新節點」。和刪除一樣，插入也只是**改箭頭**——找到要插入的位置，讓前一個節點指向新節點、新節點再指向原本的後一個節點。整個動作只動兩條邊，不必搬移任何資料。\n\n:::example 插入核心：把 node 接在 prev 和 prev->next 之間\n先看插入的核心動作。注意箭頭的設定**順序很重要**：\n\n```cpp\n// 插入核心：把 node 接在 prev 和 prev->next 之間\nnode->next = prev->next;  // 1) 先讓新節點指向「後面那段」\nprev->next = node;        // 2) 再讓 prev 指向新節點\n```\n:::\n\n\n\n順序不能反。如果先做 `prev->next = node`，那原本 `prev->next` 指的「後面那段」就被覆蓋、再也找不到了。所以心法是：**新節點要先接住後面，前一個節點才放手交棒給新節點。** 這跟刪除「改箭頭前先抓住下一個」是同一個道理——永遠別把還用得到的鏈弄丟。\n\n:::example 虛擬頭節點：讓「插在最前面」也能用同一套邏輯\n同樣地，如果新節點要插在最前面（變成新的頭），它就沒有「前一個」可以改箭頭，會變成特例。用**虛擬頭節點（dummy head）**可以讓頭也有「前一個」，免去特殊分支：\n\n```cpp\n// 虛擬頭節點：讓「插在最前面」也能用同一套邏輯\nListNode dummy;\ndummy.next = head;\n// 找到插入點的前一個節點 prev（可能就是 &dummy），再接上新節點\n// 最後回傳 dummy.next\n```\n:::\n\n\n\n像 LeetCode 147「對連結串列進行插入排序」就是每次取一個節點，從頭找到它該插入的位置再接進去；2807「在連結串列中插入最大公約數」則是邊走邊在每對相鄰節點之間，插入一個值為兩者最大公約數的新節點。\n\n## 狀態／資料結構定義\n\n- `dummy`：哨兵，讓「插在最前面」與「插在中間」共用同一段程式碼。\n- `prev`：插入點的前驅，語意是「新節點應該接在它後面」。\n- 插入的兩行是有順序的：`node->next = prev->next;` 必須先於 `prev->next = node;`。\n- 有序插入時，`prev` 的判定條件是「`prev->next` 為空，或 `prev->next->val` 已不小於待插值」——找的是**第一個不小於待插值的位置之前驅**。\n\n環狀串列（708）多一個語意：沒有 `nullptr` 當終點，得靠「走回起點」判定走完一圈。\n\n## 不變量或正確性證明\n\n先證單次插入的正確性。設插入前鏈為 $\\dots \\to prev \\to succ \\to \\dots$，記 `succ = prev->next`。執行 `node->next = succ` 後 `node` 指向 `succ`，且此時 `prev->next` 仍是 `succ`，鏈未斷；再執行 `prev->next = node`，鏈變成 $\\dots \\to prev \\to node \\to succ \\to \\dots$。**兩行的順序不能交換**：若先寫 `prev->next = node`，`succ` 的唯一入邊就被覆蓋，之後 `node->next = prev->next` 只會讓 `node` 指向自己，整條後綴洩漏。這不是風格問題，是唯一入邊被覆寫後資訊不可復原。\n\n再證有序插入（147）的迴圈不變量：**每輪開始時，`dummy` 之後的鏈是「已處理過的節點」的有序排列，且原鏈剩餘部分完好。**\n\n- 初始化：已處理集合為空，有序成立。\n- 保持：取出剩餘鏈的第一個節點 `cur`（先存好 `cur->next` 再改動 `cur`，否則會弄丟剩餘鏈）。從 `dummy` 往後找到第一個滿足 `prev->next == nullptr || prev->next->val >= cur->val` 的 `prev`，把 `cur` 插在其後。由於掃描在「第一個不小於」處停下，`prev->val <= cur->val <= prev->next->val`，插入後整段仍有序；已處理集合多了 `cur`。\n- 終止：剩餘鏈為空時，已處理集合是全部節點且有序，故 `dummy.next` 是答案。\n\n穩定性也由此得到：掃描停在第一個**嚴格大於**還是**不小於**決定相等元素的相對次序；用 `>` 停會把新元素排在相等者之後，即穩定。\n\n環狀串列插入的正確性另有前提：當所有值相等或待插值落在最大與最小之間（跨越接縫）時，找不到「前小後大」的位置，必須額外用「走滿一圈就插在任意接縫」收尾，否則會無限迴圈。\n\n## 逐步演算法\n\n把插入類題目拆成幾個固定步驟，逐一搭配程式碼。\n\n:::example 狀態表示：dummy + prev 游標\n**1. 狀態表示：準備 dummy 與游標 `prev`。** `prev` 用來停在「新節點要插在它後面」的那個節點。\n\n```cpp\n// 狀態表示：dummy + prev 游標\nListNode dummy;\ndummy.next = head;\nListNode* prev = &dummy;  // 新節點將被插在 prev 之後\n```\n:::\n\n\n\n:::example 找插入點：prev 停在「下一個就比 val 大」的地方\n**2. 找插入點：沿著串列走到正確位置。** 以插入排序（147）為例，要往後走到第一個「比待插入值大」的節點之前停下。\n\n```cpp\n// 找插入點：prev 停在「下一個就比 val 大」的地方\nwhile (prev->next && prev->next->val < val) {\n    prev = prev->next;\n}\n```\n:::\n\n\n\n:::example 接上：node 先指向後段，prev 再指向 node\n**3. 接上新節點：先接後面，再交棒。** 這是插入的關鍵兩步，順序不可顛倒。\n\n```cpp\n// 接上：node 先指向後段，prev 再指向 node\nnode->next = prev->next;\nprev->next = node;\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 在「每對相鄰節點之間」插入時（如 2807），插完要記得讓游標**跳過剛插入的新節點**，否則可能無限插入或位置錯亂。\n- 迴圈型串列的插入（708）要小心找不到邊界，需處理「整圈值都相同」等特例。\n- 複雜度：每次找位置最壞 `O(n)`，插入本身 `O(1)`；插入排序整體最壞 `O(n^2)`。\n\n## C++17 模板\n\n```cpp\n// 對連結串列做插入排序（LeetCode 147）。\nListNode* insertionSortList(ListNode* head) {\n  ListNode dummy;\n  dummy.next = nullptr;\n\n  ListNode* cur = head;\n  while (cur) {\n    ListNode* nxt = cur->next;   // 先抓住剩餘鏈，等下要改 cur->next\n\n    ListNode* prev = &dummy;     // 找第一個「下一個 >= cur」的位置之前驅\n    while (prev->next && prev->next->val < cur->val) { prev = prev->next; }\n\n    cur->next = prev->next;      // 順序不可交換：先接後繼\n    prev->next = cur;            // 再改前驅\n\n    cur = nxt;\n  }\n  return dummy.next;\n}\n```\n\n單純「在 `prev` 後插入一個新值」則是三行：`ListNode* node = new ListNode(v); node->next = prev->next; prev->next = node;`。\n\n## 時間與空間複雜度\n\n單次插入 $O(1)$，但**找前驅是 $O(n)$**——連結串列不能二分，即使鏈已有序也得線性掃。插入排序總計最壞 $O(n^2)$ 時間、$O(1)$ 額外空間；已接近有序時每次掃描很短，實務上遠快於最壞值。這也說明為何連結串列排序不用插入排序而用歸併（1.9）：後者靠分治把找位置的成本攤成 $O(n \\log n)$。\n\n## 常見錯誤與邊界條件\n\n- 先改 `prev->next` 再改 `node->next`，導致自環並洩漏後綴。\n- 忘記先存 `cur->next` 就改動 `cur`，弄丟剩餘鏈。\n- 插入排序結束時忘記把最後一個節點的 `next` 設為 `nullptr`（若沿用原鏈的 `next` 會殘留舊指向而成環）。\n- 環狀串列（708）漏掉「全部相等」與「跨越最大最小接縫」兩種情形而死迴圈。\n- 空串列或插在頭部時未用 dummy 而寫特例。\n\n## 與相似技巧的比較\n\n- 刪除（1.2）是同一套改箭頭手法的反面，同樣靠 dummy 消特例；插入要小心兩行順序，刪除則要小心先抓住被刪節點。\n- 需要 $O(\\log n)$ 定位就不能用連結串列，得換成平衡樹或跳表——這正是 1.10 那些設計題選擇雜湊表加雙向鏈的原因。\n- 陣列的有序插入可用二分找位置但搬移 $O(n)$；連結串列剛好相反，找位置 $O(n)$ 但接上去 $O(1)$。\n\n## 代表例題\n\n**題號**：147、708、2046、2807、4046\n\n放在「插入節點」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 147 | [對連結串列進行插入排序](https://leetcode.cn/problems/insertion-sort-list/) |  |\n| 708 | [迴圈有序列表的插入](https://leetcode.cn/problems/insert-into-a-sorted-circular-linked-list/)（Premium） |  |\n| 2046 | [給按照絕對值排序的連結串列排序](https://leetcode.cn/problems/sort-linked-list-already-sorted-using-absolute-values/)（Premium） |  |\n| 2807 | [在連結串列中插入最大公約數](https://leetcode.cn/problems/insert-greatest-common-divisors-in-linked-list/) | 1279 |\n| 4046 | [LCR 029. 迴圈有序列表的插入](https://leetcode.cn/problems/4ueAj6/) |  |\n\n## 本節重點速查\n\n- 插入只有兩行，順序固定：先 `node->next = prev->next`，再 `prev->next = node`。\n- 難點永遠在找前驅；\n- 改動節點前先存好剩餘鏈；\n- 用 dummy 消掉插在頭部的特例。\n",
        },
        {
          id: 6,
          title: "1.4 反轉連結串列",
          summary:
            "## 這個技術解決什麼問題\n\n連結串列只能單向走，「取得倒數第 $i$ 個」「由後往前輸出」都做不到。把所有值倒進陣列再處理要 $O(n)$ 額外空間；反轉則是**原地把每條邊掉頭**，$O(n)$ 時間、$O(1)$ 空間，之後就能順著新方向走。\n\n它同時是很多題的零件：判回文（234）先反轉後半段再對比、K 個一組反轉（25）、反轉區間（92）、兩兩交換（24）都是同一套邊掉頭的手法套在不同範圍上。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就往「反轉連結串列」想：\n\n- **題目要你把整條或某一段串列的順序倒過來**（206、92），或**成組地翻轉**（24 兩兩交換、25 每 k 個一組）。\n- **你發現只要把 `next` 箭頭逐一掉頭就能完成**，不需要額外開陣列存值再重建。\n- **看到「掉頭」就想到要先存下一個**——這是反轉最容易踩的雷：\n\n:::example 嗅到味道的瞬間：你正要寫 cur->next = prev\n```cpp\n// 嗅到味道的瞬間：你正要寫 cur->next = prev\n// 但這會蓋掉原本的下一個，所以前一行一定先 nxt = cur->next;\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我有沒有在掉頭前先把 `nxt = cur->next` 存起來？\n2. 反轉結束後，新的頭是不是 `prev`？\n3. 若是局部反轉，我有沒有記住段的「前一個」和「原段頭」來接回剩下的鏈？\n\n三題都答得出來，這個技巧就套對了。\n\n## 核心想法與直覺\n\n**延伸講解**：[反轉鏈表【基礎演算法精講 06】](https://www.bilibili.com/video/BV1sd4y1x7KN/)\n\n本節要解決的場景是：把一條連結串列「反轉」——原本 `1 -> 2 -> 3` 變成 `3 -> 2 -> 1`。重點是，我們不搬移節點本身，而是**把每個節點的 `next` 箭頭反過來指**：原本指向後一個，改成指向前一個。\n\n:::example 反轉一步：先存下一個，再把 cur 的箭頭掉頭，最後兩個游標一起前進\n反轉的核心是同時握住三個游標：`prev`（已反轉好的那段的頭）、`cur`（正在處理的節點）、`nxt`（先存起來的下一個節點）。每一步做四件事：\n\n```cpp\n// 反轉一步：先存下一個，再把 cur 的箭頭掉頭，最後兩個游標一起前進\nListNode* nxt = cur->next;  // 1) 先記住下一個，否則改箭頭後就丟了\ncur->next = prev;           // 2) 把 cur 的箭頭掉頭，指向前面\nprev = cur;                 // 3) prev 前進到 cur\ncur = nxt;                  // 4) cur 前進到原本的下一個\n```\n:::\n\n\n\n第 1 步是整個技巧的命門：**改箭頭之前一定要先把「下一個」存起來。** 因為第 2 步 `cur->next = prev` 會覆蓋掉原本的下一個，如果沒先存，後面整條鏈就斷了、找不回來。\n\n:::example 完整反轉：cur 掃完整條串列後，prev 就是新的頭（LeetCode 206）\n把這四步放進迴圈，從頭走到尾，`prev` 就會一路收集反轉好的節點，最後它指向的就是新的頭：\n\n```cpp\n// 完整反轉：cur 掃完整條串列後，prev 就是新的頭（LeetCode 206）\nListNode *prev = nullptr, *cur = head;\nwhile (cur) {\n    ListNode* nxt = cur->next;\n    cur->next = prev;\n    prev = cur;\n    cur = nxt;\n}\nreturn prev;  // 新的頭\n```\n:::\n\n\n\n像 LeetCode 206「反轉連結串列」就是上面這段；92「反轉連結串列 II」是只反轉中間某一段；25「K 個一組翻轉」是每湊滿 k 個就反轉一次；24「兩兩交換」是 k=2 的特例。整趟只走一遍，時間 `O(n)`、額外空間 `O(1)`。\n\n## 狀態／資料結構定義\n\n三個指標，語意要分清楚：\n\n- `prev`：**已反轉部分的頭**（也就是原前綴的最後一個節點）。初始 `nullptr`，因為原本的頭反轉後會變成尾，其 `next` 必須是 `nullptr`。\n- `cur`：待處理的第一個節點，即**未反轉部分的頭**。初始 `head`。\n- `nxt`：`cur->next` 的暫存。必要，因為 `cur->next` 馬上要被覆寫，覆寫後就再也找不到後面。\n\n區間反轉（92）多兩個：`p0` 指向待反轉區間的前驅（用 dummy 保證存在），反轉完後 `p0->next` 是區間原本的頭、現在的尾，接回剩餘鏈。\n\n## 不變量或正確性證明\n\n迴圈不變量：**每輪開始時，原串列被 `cur` 切成兩段；前段（`head` 到 `cur` 之前）已完成反轉且 `prev` 是它的頭，後段（`cur` 起）維持原樣且 `cur` 是它的頭。兩段之間沒有任何指標相連。**\n\n- 初始化：`prev = nullptr`、`cur = head`。前段為空（頭為 `nullptr`，符合），後段是整條原鏈，成立。\n- 保持：本體做四件事——`nxt = cur->next` 記住後段的第二個節點；`cur->next = prev` 把 `cur` 的邊掉頭接上已反轉段的頭；`prev = cur` 讓 `cur` 成為新的已反轉段頭；`cur = nxt` 前進。執行後前段多了 `cur`（且它在最前面，正是反轉該有的位置），後段少了第一個節點，不變量維持。\n- 終止：`cur == nullptr` 時後段為空，前段是整條串列且已反轉，故 `prev` 是新的頭。\n\n四行的順序由「唯一入邊」約束決定：`cur->next` 是通往後段的唯一通路，覆寫它之前必須先存進 `nxt`；`prev = cur` 必須在 `cur` 前進之前。少存 `nxt` 就會在改完 `cur->next` 後失去整條後段——不是會走錯，是資訊已經不存在。\n\n初始值 `prev = nullptr` 也不是隨意選的：反轉後原本的頭變成尾，尾的 `next` 必須是 `nullptr`；把 `prev` 初始成 `head` 之類的值會直接造出環。\n\nK 個一組（25）的正確性再加一層：先數夠 $k$ 個才反轉，不足 $k$ 就原樣保留。每組反轉後，該組原本的頭變成組尾，必須接上下一組反轉後的頭——所以要保存「上一組的尾」作為下一輪的 `p0`。\n\n## 逐步演算法\n\n把反轉類題目拆成幾個固定步驟，逐一搭配程式碼。\n\n:::example 狀態表示：prev 從空開始，cur 從頭開始\n**1. 狀態表示：三個游標 `prev`、`cur`、`nxt`。** `prev` 一開始是 `nullptr`（反轉後它會變成新尾巴的 next，剛好是空）。\n\n```cpp\n// 狀態表示：prev 從空開始，cur 從頭開始\nListNode* prev = nullptr;\nListNode* cur = head;\nListNode* nxt = nullptr;  // 每步暫存「下一個」\n```\n:::\n\n\n\n:::example 更新：存下一個 -> 掉頭 -> 兩游標前進\n**2. 更新流程：四步一循環，掉頭再前進。** 這就是反轉的主迴圈，務必照順序。\n\n```cpp\n// 更新：存下一個 -> 掉頭 -> 兩游標前進\nwhile (cur) {\n    nxt = cur->next;   // 存\n    cur->next = prev;  // 掉頭\n    prev = cur;        // prev 前進\n    cur = nxt;         // cur 前進\n}\n```\n:::\n\n\n\n:::example 局部反轉的接線：以反轉 [a..b] 為例\n**3. 局部反轉要記住「邊界節點」。** 若只反轉中間一段（92、25），反轉前要先記住「這段的前一個節點」和「這段的第一個節點」——反轉後，前者要接到新段頭，後者會變成新段尾，要接回剩下的鏈。\n\n```cpp\n// 局部反轉的接線：以反轉 [a..b] 為例\n// before -> a -> ... -> b -> after  反轉成  before -> b -> ... -> a -> after\nbefore->next = newHead;  // newHead 是反轉後的段頭（原本的 b）\na->next = after;         // 原本的段頭 a 變成段尾，接回 after\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 空串列或只有一個節點時，直接回傳 `head` 即可，反轉迴圈也能自然處理。\n- 分組反轉（25）若最後不滿 k 個，依題意通常「保持原樣不反轉」，要先數夠 k 個再動手。\n- 複雜度：每個節點被處理一次，時間 `O(n)`、額外空間 `O(1)`。\n\n## C++17 模板\n\n以下用本節代表題 206. 反轉連結串列 展示核心寫法。\n\n**完整題目**：給定單向連結串列的頭節點，反轉整條串列並回傳新的頭節點。\n\n:::example 迭代反轉：prev 指向已反轉部分的頭節點，head 為待處理節點\n**線索**：題目中的「題目要求刪除、插入、反轉、合併或找第 k 個節點。」是主要線索；先把它翻成狀態、候選集合或判斷函式，再套用本節 pattern。\n\n```cpp\nclass Solution {\npublic:\n    ListNode* reverseList(ListNode* head) {\n            // 迭代反轉：prev 指向已反轉部分的頭節點，head 為待處理節點\n            ListNode* prev = nullptr;\n            while (head) {\n                    ListNode* nxt = head->next;  // 暫存下一節點，避免斷鏈後遺失\n                    head->next = prev;           // 將當前節點指向已反轉部分\n                    prev = head;                 // prev 前移\n                    head = nxt;                  // head 移至下一節點\n            }\n            return prev;  // prev 即新鏈結串列的頭節點\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- 每步先保存下一個節點，再反轉當前指標；連結串列題的核心是不要丟失後續鏈。\n- `dp` 變數代表已處理前綴或子問題的答案，不是模糊的目前最佳值。\n- 迴圈順序必須保證轉移來源已經算好；壓縮空間時更要確認讀到的是舊值還是新值。\n\n## 時間與空間複雜度\n\n每條邊恰被掉頭一次，時間 $O(n)$；只用三個指標，空間 $O(1)$。遞迴寫法同樣 $O(n)$ 時間但 $O(n)$ 堆疊，長串列會爆。\n\nK 個一組看似對每組重複掃描，但各組互不重疊，總掃描量仍是 $O(n)$；若加上「先數長度」那一趟則是 $2n$ 次存取，仍為 $O(n)$。\n\n## 常見錯誤與邊界條件\n\n- 改 `next` 前先保存後繼。\n- dummy 節點可避免特判 head。\n- 循環條件用 `node && node->next` 時先確認訪問順序。\n- 返回值可能是新 head，不一定是原 head。\n\n## 常見變形\n\n- 快慢指標找中點或倒數第 k。\n- dummy head 處理刪除頭節點。\n- 分治合併多條有序鏈。\n\n## 與相似技巧的比較\n\n- 用堆疊反向輸出同樣 $O(n)$ 時間，但要 $O(n)$ 空間，且沒有真的改變結構；只讀不改時堆疊寫起來更快，需要回傳新鏈就得原地反轉。\n- 遞迴反轉程式碼短但吃堆疊，面試常被追問迭代版。\n- 「兩兩交換」（24）可視為 $k=2$ 的特例；直接改邊比套通用模板更短，但通用模板不易錯。\n- 回文判定（234）用快慢指標（1.6）找中點、反轉後半、再對比，是本節與 1.6 的典型組合。\n\n## 代表例題\n\n**題號**：24、25、92、206、2074\n\n放在「反轉連結串列」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 24 | [兩兩交換連結串列中的節點](https://leetcode.cn/problems/swap-nodes-in-pairs/) |  |\n| 25 | [K 個一組翻轉連結串列](https://leetcode.cn/problems/reverse-nodes-in-k-group/) |  |\n| 92 | [反轉連結串列 II](https://leetcode.cn/problems/reverse-linked-list-ii/) |  |\n| 206 | [反轉連結串列](https://leetcode.cn/problems/reverse-linked-list/) |  |\n| 2074 | [反轉偶數長度組的節點](https://leetcode.cn/problems/reverse-nodes-in-even-length-groups/) | 1685 |\n\n## 本節重點速查\n\n- 三指標四行：`nxt = cur->next`、`cur->next = prev`、`prev = cur`、`cur = nxt`，順序不可動。\n- `prev` 初始必須是 `nullptr`。\n- 不變量是「`prev` 是已反轉段的頭，`cur` 是未動段的頭」；\n- 區間反轉再多存一個 `p0` 把兩段接回去。",
        },
        {
          id: 7,
          title: "1.5 前後指標",
          summary:
            "## 這個技術解決什麼問題\n\n「刪除倒數第 $n$ 個節點」（19）這種需求，直覺做法是先走一趟數長度 $L$，再走一趟到第 $L-n$ 個。兩趟都是 $O(n)$，總體沒變差，但題目常追問「能不能一趟完成」，而且在串流情境下你根本無法回到頭再走第二次。\n\n前後指標的槓桿是：**用兩個相距固定 $k$ 步的指標，把「倒數第 $k$」轉換成「前指標到終點時，後指標所在位置」**。距離差一旦建立就自動維持，不需要知道總長度。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就往「前後指標」想：\n\n- **題目出現「倒數第 N 個」「距離結尾 K」「相隔固定間隔」這類描述**（19 刪除倒數第 N 個、1721 交換串列中第 k 個與倒數第 k 個）。\n- **你只想掃一遍串列就定位，不想先數長度再走第二次。** 固定間隔雙指標正是把「兩趟」壓成「一趟」。\n- **你需要在某個位置「停在它的前一個」來改箭頭**，這時讓 `slow` 落後一格即可：\n\n:::example 嗅到味道的瞬間：要找「倒數第 N 個」卻不想數兩遍\n```cpp\n// 嗅到味道的瞬間：要找「倒數第 N 個」卻不想數兩遍\n// 讓 fast 先走 N 步，再一起走到底，slow 就停在那裡\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我讓前指標先走幾步？這個間隔對應題目要的「倒數第幾個」嗎？\n2. 結束條件用 `fast` 還是 `fast->next`？（決定 slow 停在目標還是目標前一個）\n3. 我需不需要 dummy head 來處理「目標就是頭節點」的情況？\n\n三題都答得出來，這個技巧就套對了。\n\n## 核心想法與直覺\n\n**延伸講解**：[刪除鏈表重複節點【基礎演算法精講 08】](https://www.bilibili.com/video/BV1VP4y1Q71e/)\n\n本節要解決的場景是：在連結串列上做「倒數第 N 個」這類事情，例如刪除倒數第 N 個節點。難點在於連結串列只能從頭往前走、不能像陣列那樣回頭，也不能直接知道總長度。「前後指標（也叫間隔雙指標）」就是專門解這個的技巧。\n\n:::example 第一步：讓 fast 先走 N 步，拉開固定間隔 N\n核心想法只有一句話：**讓兩個游標保持固定的間隔一起往前走。** 先讓「前指標 `fast`」單獨往前走 N 步，於是 `fast` 和「後指標 `slow`」之間相差恰好 N 個節點。接著兩個一起走，當 `fast` 走到結尾時，`slow` 自然就停在「倒數第 N 個」的位置——因為它們的間隔一直是 N。\n\n```cpp\n// 第一步：讓 fast 先走 N 步，拉開固定間隔 N\nListNode* fast = head;\nfor (int i = 0; i < n; ++i) {\n    fast = fast->next;\n}\n// 現在 fast 領先 slow（在 head）剛好 N 個節點\n```\n:::\n\n\n\n:::example 第二步：兩指標同速前進，fast 到尾時 slow 正好在倒數第 N 個\n```cpp\n// 第二步：兩指標同速前進，fast 到尾時 slow 正好在倒數第 N 個\nListNode* slow = head;\nwhile (fast) {\n    fast = fast->next;\n    slow = slow->next;\n}\n// slow 此刻就是「倒數第 N 個」節點\n```\n:::\n\n\n\n為什麼這樣就對？因為兩指標永遠相差 N 步，這個間隔在整個過程中不變（這就是「不變式」）。`fast` 走到 `nullptr`（總共走了「長度」步），`slow` 因為落後 N 步，所以停在「長度 − N」的位置，正是倒數第 N 個。整個過程**只走一遍串列**就找到位置，不必先數一次長度再走第二次，時間 `O(n)`、空間 `O(1)`。\n\n如果要刪除倒數第 N 個，技巧是讓 `slow` 停在它的**前一個**節點（一樣靠 dummy head 統一處理頭被刪的情況），這樣才好改箭頭。\n\n## 狀態／資料結構定義\n\n- `fast`：前指標，先走 $k$ 步。\n- `slow`：後指標，從 `dummy`（而非 `head`）出發——因為刪除需要的是**目標的前驅**，從 dummy 起跑剛好讓 `slow` 停在前驅上，也順帶處理「刪的就是頭節點」。\n- 間距 $k$：兩指標之間的邊數，是整個技巧的核心量，必須在迴圈中保持不變。\n\n旋轉串列（61）的變體：先數長度 $L$，把 $k$ 取模成 $k \\bmod L$，再用間距 $L - k \\bmod L$ 找到新尾。這裡取模是必要的，否則 $k$ 可能遠大於 $L$。\n\n## 不變量或正確性證明\n\n不變量：**建立間距之後，每輪開始時 `fast` 與 `slow` 之間恰隔 $k$ 條邊。**\n\n- 初始化：讓 `fast` 單獨走 $k$ 步，`slow` 不動，間距為 $k$。\n- 保持：迴圈本體同時執行 `fast = fast->next` 與 `slow = slow->next`，兩者各前進一步，差值不變。\n- 終止：以 `fast == nullptr` 為條件退出時，`fast` 走過的總步數等於串列長度 $L$（從 `head` 起算需 $L$ 步才到 `nullptr`）。因此 `slow` 從 `dummy` 起走了 $L - k$ 步，停在第 $L-k$ 個節點上（`dummy` 記為第 0 個），它的後繼是第 $L-k+1$ 個節點，也就是**倒數第 $k$ 個**。於是 `slow->next` 正是待刪節點，`slow` 是其前驅。\n\n正確性依賴兩個前提。其一，$k \\le L$，否則初始那 $k$ 步就會走出串列外而解參考空指標；題目保證 $n \\le L$ 時可省略檢查，否則要顯式判斷。其二，退出條件與起跑點必須配套：`slow` 從 `dummy` 起跑、`fast` 從 `head` 起跑並以 `fast == nullptr` 退出，三者搭配才會落在前驅；把 `slow` 改成從 `head` 起跑就會停在待刪節點自己，拿不到前驅。這組偏移量是最常寫錯的地方，建議用 $L = k$（刪頭）與 $k = 1$（刪尾）兩個極端案例各驗一次。\n\n## 逐步演算法\n\n把前後指標類題目拆成幾個固定步驟，逐一搭配程式碼。\n\n:::example 狀態表示：dummy 統一邊界，fast/slow 都從 dummy 出發（LeetCo…\n**1. 狀態表示：dummy + 兩個游標 `fast`、`slow`。** 用 dummy 讓「刪到頭節點」也不必寫特例。\n\n```cpp\n// 狀態表示：dummy 統一邊界，fast/slow 都從 dummy 出發（LeetCode 19）\nListNode dummy;\ndummy.next = head;\nListNode* fast = &dummy;\nListNode* slow = &dummy;\n```\n:::\n\n\n\n:::example 拉開間隔：fast 先行 n 步\n**2. 拉開間隔：fast 先走 N 步。** 走完後 `fast` 與 `slow` 相差 N。\n\n```cpp\n// 拉開間隔：fast 先行 n 步\nfor (int i = 0; i < n; ++i) {\n    fast = fast->next;\n}\n```\n:::\n\n\n\n:::example 同速前進：fast 到尾時，slow 停在待刪節點的前一個\n**3. 同速前進：直到 fast 走到最後一個節點。** 因為要刪除，所以條件用 `fast->next`，讓 `slow` 停在「待刪節點的前一個」。\n\n```cpp\n// 同速前進：fast 到尾時，slow 停在待刪節點的前一個\nwhile (fast->next) {\n    fast = fast->next;\n    slow = slow->next;\n}\nslow->next = slow->next->next;  // 跳過倒數第 N 個，完成刪除\n```\n:::\n\n\n\n**4. 答案讀取與複雜度。** 回傳 `dummy.next`（頭可能已被刪）。\n\n- 旋轉串列（61）也用間隔思想：先數長度、取 `k % len`，找到斷點再接成新頭。\n- N 可能等於整條長度（要刪的就是頭），dummy 讓這種情況自然成立。\n- 複雜度：單趟掃描，時間 `O(n)`、額外空間 `O(1)`。\n\n## C++17 模板\n\n```cpp\n// 刪除倒數第 n 個節點（LeetCode 19），一趟完成。\nListNode* removeNthFromEnd(ListNode* head, int n) {\n  ListNode dummy;\n  dummy.next = head;\n\n  ListNode* fast = head;      // 前指標從 head 起跑\n  for (int i = 0; i < n; ++i) { fast = fast->next; }  // 先拉開 n 條邊的間距\n\n  ListNode* slow = &dummy;    // 後指標從 dummy 起跑，才會停在「前驅」\n  while (fast) {              // 同步前進，間距恆為 n\n    fast = fast->next;\n    slow = slow->next;\n  }\n\n  slow->next = slow->next->next;   // slow 正是倒數第 n 個的前驅\n  return dummy.next;\n}\n```\n\n## 時間與空間複雜度\n\n`fast` 總共走 $L$ 步、`slow` 走 $L-k$ 步，時間 $O(n)$ 且**只掃一趟**；空間 $O(1)$。與「先數長度再走」的兩趟版相比漸近同階，差別在常數（$L$ 對 $2L$）與能否處理不可重讀的串流輸入。\n\n## 常見錯誤與邊界條件\n\n- `slow` 從 `head` 而非 `dummy` 起跑，結果停在待刪節點本身而拿不到前驅。\n- 初始間距多走或少走一步（用刪頭、刪尾兩個極端案例驗證）。\n- `n` 大於長度時初始迴圈解參考空指標。\n- 刪除頭節點後回傳 `head` 而非 `dummy.next`。\n- 旋轉串列忘記 `k %= L` 而白繞很多圈，或 `L` 為 0 時取模除以零。\n- 只有一個節點的串列。\n\n## 與相似技巧的比較\n\n- 快慢指標（1.6）是**速度比**不同（一次一步對一次兩步），本節是**固定間距**、速度相同；前者用來找中點與判環，後者用來定位倒數第 $k$。\n- 陣列裡「倒數第 $k$」直接用 `a[n-k]`，不需要這個技巧——它存在的唯一理由就是連結串列不能隨機存取。\n- 若要同時取得長度與位置，兩趟寫法更直白，程式碼也更難寫錯；一趟版主要是面試要求與串流場景。\n\n## 代表例題\n\n**題號**：19、61、1474、1721\n\n放在「前後指標」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 19 | [刪除連結串列的倒數第 N 個結點](https://leetcode.cn/problems/remove-nth-node-from-end-of-list/) |  |\n| 61 | [旋轉連結串列](https://leetcode.cn/problems/rotate-list/) |  |\n| 1474 | [刪除連結串列 M 個節點之後的 N 個節點](https://leetcode.cn/problems/delete-n-nodes-after-m-nodes-of-a-linked-list/)（Premium） |  |\n| 1721 | [交換連結串列中的節點](https://leetcode.cn/problems/swapping-nodes-in-a-linked-list/) | 1387 |\n\n## 本節重點速查\n\n- 先讓 `fast` 走 $k$ 步拉開間距，再兩者同步前進；\n- `fast` 到 `nullptr` 時 `slow` 就位。\n- `slow` 從 `dummy` 起跑才能停在前驅。\n- 不變量是「間距恆為 $k$」，偏移量用刪頭與刪尾兩個極端案例校準。\n",
        },
        {
          id: 8,
          title: "1.6 快慢指標",
          summary:
            "## 這個技術解決什麼問題\n\n「這條串列有沒有環」「環的入口在哪」「中點是哪個節點」——用雜湊表記錄走過的節點當然可以，但要 $O(n)$ 空間。快慢指標把這些問題全部壓到 $O(1)$ 空間：**兩個速度不同的指標在有環結構裡必定相遇，而相遇的位置藏著環長與入口的資訊。**\n\n它的適用範圍其實比連結串列大得多：任何「狀態轉移是確定性函數」的系統（快樂數、287 題把陣列視為函數圖）都能套用。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就往「快慢指標」想：\n\n- **題目要判斷有沒有環、找環的入口**（141、142），或把問題轉化成「找隱式串列裡的環」（287 尋找重複數）。\n- **題目要找「正中間」的節點**，當作後續操作的分界（234 迴文、143 重排）。\n- **你想用 `O(1)` 空間達成上面的事**，不想開一個 `set` 記錄走過的節點：\n\n:::example 嗅到味道的瞬間：要判環或找中點，又想省空間\n```cpp\n// 嗅到味道的瞬間：要判環或找中點，又想省空間\n// 兩個游標一快一慢；相遇=有環，fast 到尾=slow 在中間\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的快、慢指標各走幾格？（找中點與判環都是 1 比 2）\n2. 迴圈條件有沒有同時檢查 `fast` 和 `fast->next`，避免空指標？\n3. 我要的是「相遇與否」（判環）還是「相遇位置」（環入口、中點）？\n\n三題都答得出來，這個技巧就套對了。\n\n## 核心想法與直覺\n\n**延伸講解**：[環形鏈表 II【基礎演算法精講 07】](https://www.bilibili.com/video/BV1KG4y1G7cu/)\n\n本節要解決的場景是：在連結串列上判斷「有沒有環」、找「正中間的節點」、或偵測「重複」這類問題。共同工具叫「快慢指標（也叫 Floyd 龜兔算法）」——讓兩個游標**用不同的速度**在串列上跑，一步一個（慢，slow）和一步兩個（快，fast）。\n\n:::example 找中點：fast 走兩步、slow 走一步，fast 到尾時 slow 在中間\n先看最常見的兩個用法。其一是**找中點**：快指標一次走兩格、慢指標一次走一格，快指標到尾時，慢指標剛好在正中間（因為它只走了一半的步數）。\n\n```cpp\n// 找中點：fast 走兩步、slow 走一步，fast 到尾時 slow 在中間\nListNode *slow = head, *fast = head;\nwhile (fast && fast->next) {\n    slow = slow->next;        // 慢：一次一格\n    fast = fast->next->next;  // 快：一次兩格\n}\n// slow 現在是中間節點\n```\n:::\n\n\n\n:::example 判環：若 fast 追上 slow（相遇），就有環（LeetCode 141）\n其二是**判斷有沒有環**。如果串列是一條直線，快指標遲早走到 `nullptr`；但如果有環，快指標會在環裡繞圈，最終一定會「從後面追上」慢指標，兩者相遇。這就像操場上跑得快的人遲早會套圈追上跑得慢的人。\n\n```cpp\n// 判環：若 fast 追上 slow（相遇），就有環（LeetCode 141）\nwhile (fast && fast->next) {\n    slow = slow->next;\n    fast = fast->next->next;\n    if (slow == fast) {\n        return true;  // 相遇 = 有環\n    }\n}\nreturn false;  // fast 走到底 = 無環\n```\n:::\n\n\n\n為什麼有環就一定會相遇？因為進入環之後，快指標每步比慢指標多走一格，兩者的「距離」每步縮短 1，環長有限，所以距離一定會減到 0（相遇）。這個「不同速度造成距離單調變化」的不變式，就是快慢指標能成立的原因。整個過程只用兩個游標、走線性步數，時間 `O(n)`、空間 `O(1)`。\n\n## 狀態／資料結構定義\n\n- `slow`：每次走一步。\n- `fast`：每次走兩步。\n- 兩者同時從 `head` 出發（判環、找中點）。\n- 判環的退出條件是 `fast == nullptr || fast->next == nullptr`（無環）或 `slow == fast`（有環）。**必須同時檢查 `fast` 與 `fast->next`**，因為一次走兩步會跨過兩個可能為空的位置。\n\n找中點時的語意選擇：以 `while (fast && fast->next)` 退出，偶數長度會停在**後中點**；以 `while (fast->next && fast->next->next)` 則停在**前中點**。反轉後半段（234）需要前中點，這個差別會直接決定程式對錯。\n\n## 不變量或正確性證明\n\n**相遇性。** 設串列從 `head` 走 $\\mu$ 步進入環，環長 $\\lambda$。當 `slow` 首次進入環時（走了 $\\mu$ 步），`fast` 已走 $2\\mu$ 步、也在環內。此後兩者都在環中，考慮 `fast` 相對 `slow` 的位置差（模 $\\lambda$）：每輪 `fast` 多走一步，差值每輪恰減一（或加一，視方向定義）。差值在 $\\{0,1,\\dots,\\lambda-1\\}$ 中每輪變動 1，故至多 $\\lambda$ 輪後必為 0，即相遇。**這裡的關鍵是速度差恰為 1**：若改成一次走三步、一次走一步，速度差為 2，當 $\\lambda$ 為偶數且初始差為奇數時差值永遠是奇數，可能**永不相遇**。所以「兩倍速」不是隨手挑的常數。\n\n無環時 `fast` 每輪前進兩步，至多 $\\lceil n/2 \\rceil$ 輪就會走到 `nullptr`，終止。\n\n**入口定位（Floyd 判圈）。** 設相遇時 `slow` 走了 $s$ 步、`fast` 走了 $2s$ 步。兩者都在環上，故步數差是環長的整數倍：$2s - s = s = k\\lambda$。又 `slow` 走了 $s = \\mu + t$ 步（$t$ 是它在環內走的距離）。從相遇點再走 $\\mu$ 步，總距離為 $s + \\mu = k\\lambda + \\mu$；由於在環內繞 $k\\lambda$ 步等於原地不動，這等價於從 `head` 走 $\\mu$ 步——正是環的入口。因此把一個指標重置到 `head`，兩者**各走一步**同步前進，相遇處即入口。注意這一步的速度必須改成相同，沿用兩倍速就不成立，因為上述推導要求兩者走過的距離相等。\n\n**中點。** 無環時，`fast` 走的步數恆為 `slow` 的兩倍，故 `fast` 到終點時 `slow` 恰在中間。這是「距離比」而非「距離差」，與前後指標（1.5）的機制不同。\n\n## 逐步演算法\n\n把快慢指標類題目拆成幾個固定步驟，逐一搭配程式碼。\n\n:::example 狀態表示：slow 慢、fast 快，都從頭開始\n**1. 狀態表示：兩個游標 `slow`、`fast`，通常都從 `head` 出發。**\n\n```cpp\n// 狀態表示：slow 慢、fast 快，都從頭開始\nListNode* slow = head;\nListNode* fast = head;\n```\n:::\n\n\n\n:::example 更新：注意條件，否則 fast->next->next 會解參考 nullptr\n**2. 更新流程：每步 slow 走一格、fast 走兩格。** 迴圈條件要同時檢查 `fast` 和 `fast->next`，避免走兩格時踩到空指標。\n\n```cpp\n// 更新：注意條件，否則 fast->next->next 會解參考 nullptr\nwhile (fast && fast->next) {\n    slow = slow->next;\n    fast = fast->next->next;\n}\n```\n:::\n\n\n\n:::example 找環入口：相遇後，ptr 從頭、slow 從相遇點，同速走到再次相遇\n**3. 找環的入口（142）：相遇後，用第二個游標從頭重走。** 數學上可證：相遇點與頭節點到「環入口」的距離相等，所以一個從頭、一個從相遇點，同速前進，再次相遇處就是入口。\n\n```cpp\n// 找環入口：相遇後，ptr 從頭、slow 從相遇點，同速走到再次相遇\nListNode* ptr = head;\nwhile (ptr != slow) {\n    ptr = ptr->next;\n    slow = slow->next;\n}\nreturn ptr;  // 環的入口節點\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 287「尋找重複數」可把陣列看成「值即下一個位置」的隱式串列，重複數對應到環入口，正好套用 142 的解法。\n- 234「迴文串列」常用快慢指標找中點，再反轉後半段比對。\n- 複雜度：時間 `O(n)`、額外空間 `O(1)`。\n\n## C++17 模板\n\n```cpp\n// 判環並回傳入口，無環回傳 nullptr（LeetCode 141 / 142）。\nListNode* detectCycle(ListNode* head) {\n  ListNode* slow = head;\n  ListNode* fast = head;\n\n  while (fast && fast->next) {      // 兩個都要檢查：一次跨兩步\n    slow = slow->next;\n    fast = fast->next->next;\n    if (slow == fast) {             // 相遇，確定有環\n      ListNode* p = head;\n      while (p != slow) {           // 改成同速各走一步\n        p = p->next;\n        slow = slow->next;\n      }\n      return p;                     // 相遇處即環的入口\n    }\n  }\n  return nullptr;                   // fast 走到底，無環\n}\n```\n\n找中點只保留 `while` 那三行，退出後 `slow` 即中點（偶數長度取後中點）。\n\n## 時間與空間複雜度\n\n判環：`slow` 進環需 $\\mu$ 步，進環後至多 $\\lambda$ 輪相遇，故 $O(\\mu + \\lambda) = O(n)$。找入口再走 $\\mu$ 步，仍是 $O(n)$。空間 $O(1)$——這正是它勝過雜湊表法的地方，後者同為 $O(n)$ 時間但需 $O(n)$ 空間。\n\n找中點 $O(n)$ 時間、$O(1)$ 空間。回文判定（234）是找中點 $O(n)$ 加反轉 $O(n)$ 加對比 $O(n)$，仍為 $O(n)$ 時間、$O(1)$ 空間。\n\n## 常見錯誤與邊界條件\n\n- `while` 條件只檢查 `fast` 而漏掉 `fast->next`，偶數長度時解參考空指標。\n- 找入口時沿用兩倍速（必須改同速）。\n- 前中點與後中點搞混，導致 234 題反轉後多一個或少一個節點。\n- `fast` 與 `slow` 起點不一致（判環兩者都從 `head`，某些寫法讓 `fast` 從 `head->next` 起跑，則需在最後對相遇點做偏移修正）。\n- 空串列或單節點。\n- 287 題忘記「把陣列視為 `i -> a[i]` 的函數圖」需要保證入口存在（值域在 $[1,n]$ 而長度 $n+1$）。\n\n## 與相似技巧的比較\n\n- 前後指標（1.5）維持**固定差**，用來定位倒數第 $k$；本節維持**固定比**，用來找中點與判環。看到「倒數」想差，看到「中點／環」想比。\n- 雜湊表判環更直白也更容易推廣到「記錄第一次出現」，但空間 $O(n)$；面試要求 $O(1)$ 空間時只能用 Floyd。\n- Brent 判圈演算法同樣 $O(1)$ 空間，比較次數更少，但實作較繁；競賽外少用。\n- 找中點若之後還要反轉後半，記得 234 題結束前應把串列復原，否則呼叫端看到的資料已被破壞。\n\n## 代表例題\n\n**題號**：141、142、143、234、287\n\n放在「快慢指標」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 141 | [環形連結串列](https://leetcode.cn/problems/linked-list-cycle/) |  |\n| 142 | [環形連結串列 II](https://leetcode.cn/problems/linked-list-cycle-ii/) |  |\n| 143 | [重排連結串列](https://leetcode.cn/problems/reorder-list/) |  |\n| 234 | [迴文連結串列](https://leetcode.cn/problems/palindrome-linked-list/) |  |\n| 287 | [尋找重複數](https://leetcode.cn/problems/find-the-duplicate-number/) |  |\n| 457 | [環形陣列是否存在迴圈](https://leetcode.cn/problems/circular-array-loop/) |  |\n| 2674 | [拆分迴圈連結串列](https://leetcode.cn/problems/split-a-circular-linked-list/) |  |\n| 876 | [連結串列的中間結點](https://leetcode.cn/problems/middle-of-the-linked-list/) | 1232 |\n| 2130 | [連結串列最大孿生和](https://leetcode.cn/problems/maximum-twin-sum-of-a-linked-list/) | 1318 |\n| 2095 | [刪除連結串列的中間節點](https://leetcode.cn/problems/delete-the-middle-node-of-a-linked-list/) | 1324 |\n| 3790 | [最小全 1 倍數](https://leetcode.cn/problems/smallest-all-ones-multiple/) | 1593 |\n| 1015 | [可被 K 整除的最小整數](https://leetcode.cn/problems/smallest-integer-divisible-by-k/) | 1875 |\n\n## 本節重點速查\n\n- 一步配兩步；\n- `while (fast && fast->next)` 兩個都要檢查。\n- 相遇證明依賴速度差恰為 1。\n- 找入口：相遇後把一個指標放回 `head`，**改成同速**同步走，再相遇即入口。\n- 找中點注意前中點與後中點的差別。\n",
        },
        {
          id: 9,
          title: "1.7 雙指標",
          summary:
            "## 這個技術解決什麼問題\n\n有些題目要把一條串列**按規則重新分配**：小於 $x$ 的排前面、大於等於的排後面（86），或奇數位一組、偶數位一組（328）。若在原鏈上邊走邊搬節點，前驅與後繼會互相牽動，很容易寫成一團漿糊。\n\n這裡的槓桿是**拆成多條鏈再接起來**：準備若干條獨立的尾指標，掃描原鏈時把每個節點「摘下來」丟進對應的鏈，最後首尾相接。每條鏈各自維護，互不干擾，程式碼從糾纏的指標操作變成幾條平行的追加動作。\n\n相交串列（160）則是另一種雙指標：兩條鏈長度不同，用「走完自己換走對方」補齊長度差。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就往「雙指標（分流／對齊）」想：\n\n- **題目要按某條件把串列「拆成兩組」再重新拼接**（86 分隔串列、328 奇偶串列）。\n- **題目要比較或找兩條串列的關係**，例如交點（160），而兩條長度可能不同需要「對齊節奏」。\n- **你發現一個游標不夠用**，需要同時維護「目前在收哪一條」的兩個尾巴：\n\n:::example 嗅到味道的瞬間：要分兩堆或比兩條\n```cpp\n// 嗅到味道的瞬間：要分兩堆或比兩條\n// 開兩個 dummy + 兩個游標，分頭收，最後接起來\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我需要幾條子串列？每條的 dummy 和游標都備好了嗎？\n2. 走訪時，每個節點依什麼條件被分到哪一條？\n3. 收尾接線時，我有沒有封住最後一條的 `nullptr`，避免斷鏈或成環？\n\n三題都答得出來，這個技巧就套對了。\n\n## 核心想法與直覺\n\n本節要解決的場景是：在連結串列上同時維護**兩個（或多個）獨立前進的游標**，各自負責收集一部分節點，最後再把它們接起來。和「快慢指標」不同速、「前後指標」固定間隔不一樣，這裡的兩個指標通常是**各自獨立地往前走**，分頭做事。\n\n:::example 分流：兩個 dummy 各帶一個游標，分頭收節點（LeetCode 86）\n最典型的用法是「分流」：走訪原串列時，依條件把節點分到兩條新的子串列，最後再首尾相接。例如 LeetCode 86「分隔串列」要把小於 x 的節點排前面、其餘排後面——就開兩條子串列，一條收小的、一條收大的：\n\n```cpp\n// 分流：兩個 dummy 各帶一個游標，分頭收節點（LeetCode 86）\nListNode small, large;                // 兩條子串列的虛擬頭\nListNode *sp = &small, *lp = &large;  // 兩個游標分別收尾\n```\n:::\n\n\n\n:::example 收尾：每個節點依條件掛到對應子串列，最後兩條相接\n走訪時，每個節點依條件被接到對應游標後面，游標再前進；最後把「小的那條」尾巴接上「大的那條」的頭：\n\n```cpp\n// 收尾：每個節點依條件掛到對應子串列，最後兩條相接\nfor (ListNode* cur = head; cur; cur = cur->next) {\n    if (cur->val < x) {\n        sp->next = cur;\n        sp = sp->next;\n    } else {\n        lp->next = cur;\n        lp = lp->next;\n    }\n}\nlp->next = nullptr;     // 大的那條結尾封住\nsp->next = large.next;  // 小的尾巴接上大的頭\n```\n:::\n\n\n\n:::example 對齊兩條串列：走完自己就接到對方起點，最終相遇於交點（160）\n另一種用法是「對齊」兩條不同長度的串列。LeetCode 160「相交串列」讓兩個游標分別走 A、B，走到底就跳到對方的起點繼續走；因為 `a + b == b + a`，兩個游標走過的總長度相同，會在交點相遇（沒有交點則同時變 `nullptr`）。心法是：**用兩個指標把「不對齊」的兩段資料拉到同一個節奏上比較。**\n\n```cpp\n// 對齊兩條串列：走完自己就接到對方起點，最終相遇於交點（160）\nListNode *a = headA, *b = headB;\nwhile (a != b) {\n    a = a ? a->next : headB;\n    b = b ? b->next : headA;\n}\nreturn a;  // 交點，或同時為 nullptr\n```\n:::\n\n\n\n整類做法都只走常數趟，時間 `O(n + m)`、空間 `O(1)`。\n\n## 狀態／資料結構定義\n\n- 每條目標鏈一組 `dummy` 與 `tail`：`dummy` 是該鏈的哨兵頭，`tail` 是目前的尾。追加動作固定是 `tail->next = node; tail = node;`。\n- `cur`：掃描原鏈的游標。摘下節點前必須先存好 `cur->next`（或改為在追加後才前進）。\n- 收尾：最後一條鏈的 `tail->next` **必須顯式設為 `nullptr`**，否則會殘留原鏈的舊指向，形成環。\n\n相交串列的狀態則是 `pA`、`pB` 兩個游標，各自走到底後跳到對方的頭。\n\n## 不變量或正確性證明\n\n**分鏈的正確性。** 不變量：每輪開始時，原鏈已掃描的節點恰好被分配到各條目標鏈中的一條且僅一條，各鏈內部維持這些節點在原鏈中的相對次序，`cur` 指向下一個待分配節點。\n\n- 初始化：各鏈為空，`cur = head`，成立。\n- 保持：判定 `cur` 屬於哪條鏈，用 `tail->next = cur; tail = cur;` 追加。因為只追加到尾端且掃描順序即原鏈順序，該鏈內部次序與原鏈一致；每個節點只被判定一次，故不重不漏。\n- 終止：`cur == nullptr` 時全部節點分配完畢，依序接上各鏈即得答案。\n\n**穩定性**由「只追加到尾端 + 依原序掃描」共同保證，86 題明確要求保留相對次序，靠的就是這一點。若改成插入到頭端，相對次序會反轉。\n\n必須把最後一條鏈的 `tail->next` 清成 `nullptr`：被摘下的節點仍保有原本的 `next`，若不清除，接起來後最後一段會指回原鏈中間，形成環。這是本節最常見的無窮迴圈來源。\n\n**相交串列（160）的正確性。** 設兩鏈獨有部分長度為 $a$、$b$，共同尾段長 $c$。`pA` 走完 A（$a+c$ 步）後跳到 B 頭，再走 $b$ 步；`pB` 走完 B（$b+c$ 步）後跳到 A 頭，再走 $a$ 步。兩者在各走了 $a+b+c$ 步時同時抵達交點。若不相交（$c=0$），兩者在各走 $a+b$ 步後同時變成 `nullptr`，迴圈以相等結束並回傳 `nullptr`——**這正是要用 `nullptr` 而非 `head` 作為換軌時機的原因**：若在到達尾節點時就換軌，不相交的情形下兩指標永遠差 $a-b$ 步，會無限繞行。\n\n## 逐步演算法\n\n把雙指標分流／對齊類題目拆成幾個固定步驟，逐一搭配程式碼。\n\n:::example 狀態表示：奇偶分流為例（LeetCode 328）\n**1. 狀態表示：為每條子串列各備一個 dummy 與游標。** dummy 讓「每條子串列的頭」不必特判。\n\n```cpp\n// 狀態表示：奇偶分流為例（LeetCode 328）\nListNode oddDummy, evenDummy;\nListNode *odd = &oddDummy, *even = &evenDummy;\n```\n:::\n\n\n\n:::example 更新：奇數位掛 odd、偶數位掛 even（328）\n**2. 更新流程：走訪原串列，依條件把節點掛到對應游標後。** 每掛一個就讓對應游標前進。\n\n```cpp\n// 更新：奇數位掛 odd、偶數位掛 even（328）\nfor (ListNode* cur = head; cur;) {\n    odd->next = cur;\n    odd = cur;\n    cur = cur->next;  // 奇數位\n    if (cur) {\n        even->next = cur;\n        even = cur;\n        cur = cur->next;\n    }  // 偶數位\n}\n```\n:::\n\n\n\n:::example 收尾：偶串列結尾封住，奇串列尾接偶串列頭（328）\n**3. 收尾接線：封住結尾，再把子串列接成最終答案。** 漏接或漏封 `nullptr` 會造成斷鏈或成環。\n\n```cpp\n// 收尾：偶串列結尾封住，奇串列尾接偶串列頭（328）\neven->next = nullptr;\nodd->next = evenDummy.next;\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 收尾時務必把最後一條子串列的 `next` 設為 `nullptr`，否則可能殘留舊箭頭而成環。\n- 對齊型（160）要確認「沒有交點」時兩游標會同時變 `nullptr` 而正常結束，不會死迴圈。\n- 複雜度：常數趟掃描，時間 `O(n + m)`、額外空間 `O(1)`。\n\n## C++17 模板\n\n```cpp\n// 分隔串列：小於 x 的在前，其餘在後，各自保持相對次序（LeetCode 86）。\nListNode* partition(ListNode* head, int x) {\n  ListNode lo, hi;                 // 兩條鏈的哨兵頭\n  ListNode* lt = &lo;              // 小於 x 的鏈尾\n  ListNode* ge = &hi;              // 大於等於 x 的鏈尾\n\n  for (ListNode* cur = head; cur; cur = cur->next) {\n    if (cur->val < x) { lt->next = cur; lt = cur; }\n    else              { ge->next = cur; ge = cur; }\n  }\n\n  ge->next = nullptr;              // 必須清乾淨，否則殘留舊指向而成環\n  lt->next = hi.next;              // 兩鏈相接\n  return lo.next;\n}\n```\n\n奇偶鏈（328）結構相同，判定條件換成位置的奇偶。\n\n## 時間與空間複雜度\n\n每個節點恰被掃描並追加一次，時間 $O(n)$；只用常數個 dummy 與 tail，空間 $O(1)$（節點是搬移不是複製）。相交串列同樣 $O(a+b+c)$ 時間、$O(1)$ 空間，優於用雜湊表記錄 A 的所有節點那種 $O(n)$ 空間解法。\n\n## 常見錯誤與邊界條件\n\n- 忘記把最後一條鏈的 `tail->next` 設為 `nullptr`，造成環與無窮迴圈。\n- 在 `for` 的更新式已寫 `cur = cur->next` 的情況下又在本體改動 `cur->next`，導致游標跳錯（追加時 `cur->next` 會被下一次追加覆寫，此處因先取 `cur->next` 再改而安全，改寫成 `while` 版時要特別小心）。\n- 相交串列在尾節點換軌而非 `nullptr` 換軌，不相交時死迴圈。\n- 某一條鏈為空時直接存取 `dummy.next->val`。\n- 空串列輸入。\n\n## 與相似技巧的比較\n\n- 與 1.2 刪除、1.3 插入相比，本節不改動節點內容也不新增節點，只是**重新分組**；當要保持相對次序時，分鏈比原地交換好寫得多。\n- 與陣列的雙指標（快慢覆寫）相比，陣列是搬值、連結串列是搬邊；陣列的原地分割可以不穩定（如快排 partition），連結串列分鏈天生穩定。\n- 需要三組以上（如荷蘭國旗）就開三條鏈，模式完全一樣，這是分鏈法比原地交換更容易推廣的地方。\n\n## 代表例題\n\n**題號**：86、160、328\n\n放在「雙指標」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 86 | [分隔連結串列](https://leetcode.cn/problems/partition-list/) |  |\n| 160 | [相交連結串列](https://leetcode.cn/problems/intersection-of-two-linked-lists/) |  |\n| 328 | [奇偶連結串列](https://leetcode.cn/problems/odd-even-linked-list/) |  |\n\n## 本節重點速查\n\n- 每組一個 `dummy` 加一個 `tail`，追加固定兩行；\n- 掃描順序即原序，故天生穩定；\n- **收尾一定要把最後一條鏈的 `next` 清成 `nullptr`**。\n- 相交串列用「走完自己換走對方」補長度差，換軌時機是 `nullptr` 不是尾節點。\n",
        },
        {
          id: 10,
          title: "1.8 合併連結串列",
          summary:
            "## 這個技術解決什麼問題\n\n把兩條有序串列併成一條有序串列。用陣列做要先倒出來、合併、再建鏈，$O(n)$ 額外空間；直接在鏈上合併則只是**反覆從兩個頭中挑較小者接到結果尾端**，$O(1)$ 額外空間，且節點是搬移不是複製。\n\n同一套「同步推進兩個游標」的骨架也支撐鏈上加法（2、445）：兩條數字鏈逐位相加並維護進位。差別只在「挑一個」變成「合兩個再進位」。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就往「合併連結串列」想：\n\n- **輸入是一條或多條連結串列，要把它們接成一條新串列。** 例如第 21 題合併兩條有序串列、第 2／445 題把兩個數字逐位相加成新串列。\n- **答案的頭節點不固定、或第一個節點可能被刪改**，需要 dummy 來統一處理：\n\n:::example 嗅到味道的瞬間：新頭是誰要比較後才知道 → 用 dummy\n```cpp\n// 嗅到味道的瞬間：新頭是誰要比較後才知道 → 用 dummy\nListNode dummy;  // 先放假頭，最後 return dummy.next\nListNode* tail = &dummy;\n```\n:::\n\n\n\n- **你發現只要「改幾條指標」就能完成，不必真的搬移或複製資料。** 這正是連結串列題的價值：插入、刪除都是 `O(1)` 的局部改接。\n- **暴力做法是把節點值倒進陣列再排序重建（一次 `O(n log n)` 或多用一份空間）；** 直接改指標可做到 `O(n)` 且原地完成。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我有沒有用 dummy 來避開「第一個節點」的特判？\n2. 改某個節點的 `next` 之前，後面那串我有先存起來嗎？\n3. 迴圈結束後，還沒接完的那一段我有接上去嗎？\n\n## 核心想法與直覺\n\n本節要解決的場景是：把一條或多條「連結串列（linked list）」接在一起，或在運算過程中重新組合節點。連結串列是由一個個節點串成的鏈，每個節點記住自己的值，再用一個指標 `next` 指向下一個節點；最後一個節點的 `next` 是空（`nullptr`）。\n\n合併連結串列的核心想法是：**不要搬資料，只要改指標。** 想像兩條繩子各打了一排結，要把它們交錯接成一條，你只需要解開幾個接點重新綁，而不必把每個結重新做一遍。在程式裡「重新綁」就是改 `node->next`。\n\n:::example 連結串列節點長這樣\n這裡有一個新手最容易踩的陷阱：一旦你把某個節點的 `next` 改指到別處，原本它後面那一整串就「找不到了」。所以鐵律是：**改指標之前，先用一個變數把下一個節點存起來。**\n\n```cpp\n// 連結串列節點長這樣\nstruct ListNode {\n    int val;\n    ListNode* next;\n};\n```\n:::\n\n\n\n:::example 用 dummy 統一處理「新頭節點」，最後回傳 dummy.next\n第二個關鍵工具是「啞節點（dummy head）」。合併時新串列的「頭」是誰，往往要比較兩條串列才知道，寫起來很囉嗦。我們先做一個假的、不存資料的節點放在最前面，所有節點都接在它後面，最後回傳 `dummy.next` 即可，省去一堆「第一個怎麼辦」的特判。\n\n```cpp\n// 用 dummy 統一處理「新頭節點」，最後回傳 dummy.next\nListNode dummy;           // 假節點，不存實際資料\nListNode* tail = &dummy;  // tail 永遠指向已接好部分的最後一個\n// ... 把節點一個個接到 tail 後面 ...\nreturn dummy.next;  // 真正的頭就是 dummy 後面那個\n```\n:::\n\n\n\n像第 21 題「合併兩個有序連結串列」就是：兩條已排序的串列，每次比較兩個頭，把較小的接到 `tail` 後面。第 2、445 題「兩數相加」則是逐位相加、把結果一位位串成新串列。整個過程每個節點只走訪一次，是 `O(n)`。\n\n## 狀態／資料結構定義\n\n- `dummy` 與 `tail`：結果鏈的哨兵頭與尾。dummy 讓「第一個節點來自哪條鏈」不必寫特例。\n- `p1`、`p2`：兩條輸入鏈當前未處理的頭。\n- 迴圈條件 `while (p1 && p2)`，退出後把非空的那條**整段接上**（不必逐點搬，因為剩下的本來就有序且都不小於已接入的元素）。\n\n鏈上加法多一個狀態 `carry`，且迴圈條件放寬成 `while (p1 || p2 || carry)`——**進位必須算進終止條件**，否則最高位的進位會被丟掉（如 `5 + 5` 應為 `01`）。\n\n445 題數字最高位在鏈頭，需先反轉（1.4）或改用堆疊，因為加法必須從低位開始。\n\n## 不變量或正確性證明\n\n迴圈不變量：**每輪開始時，結果鏈是「兩條輸入鏈已消耗部分」的有序合併，且結果鏈中每個值都不大於 `p1->val` 與 `p2->val`。**\n\n- 初始化：結果鏈為空，兩個條件空虛成立。\n- 保持：不失一般性設 `p1->val <= p2->val`，把 `p1` 接到 `tail` 後並前進 `p1`。需要驗證兩件事。其一，接上後仍有序：由不變量，`tail` 的值不大於 `p1->val`，故追加後非遞減。其二，新的結果鏈中每個值仍不大於兩個新頭——新加入的 `p1->val` 不大於 `p2->val`（分支條件），也不大於新的 `p1->next->val`（輸入鏈有序）。故不變量維持。\n- 終止：某條鏈耗盡時，結果鏈是另一條鏈已消耗部分與整條耗盡鏈的合併。由不變量，結果鏈所有值都不大於剩餘鏈的頭，且剩餘鏈本身有序，故**直接整段接上**即得完整有序鏈。\n\n這裡有兩個前提值得點名。第一，**輸入必須各自有序**；否則「挑較小者」無法保證後面不會冒出更小的值，不變量的第二個條件立刻崩潰。第二，分支用 `<=` 而非 `<` 才穩定——相等時優先取 `p1`，保證來自第一條鏈的元素排在前面；歸併排序（1.9）依賴這個穩定性。\n\n鏈上加法的不變量則是：處理完第 $i$ 位後，結果鏈的低 $i$ 位等於兩數低 $i$ 位之和的低 $i$ 位，且 `carry` 等於其進位。歸納步驟就是 `sum = v1 + v2 + carry; carry = sum / 10; 接上 sum % 10;`。終止條件必須包含 `carry`，理由如上。\n\n## 逐步演算法\n\n合併類題目可拆成三個固定步驟，逐一配程式碼：\n\n:::example 步驟一：準備 dummy 與 tail\n**1. 建立 dummy 與 tail 指標。** `tail` 代表「目前已接好串列的尾巴」，新節點一律接在它後面，再把 `tail` 往後移。\n\n```cpp\n// 步驟一：準備 dummy 與 tail\nListNode dummy;\nListNode* tail = &dummy;\n```\n:::\n\n\n\n:::example 步驟二：每次挑較小的接上，再前進\n**2. 逐一比較 / 計算，接到 tail 後面。** 以合併兩條有序串列為例，每輪挑出較小的節點接上去，並把那條串列往前推進。\n\n```cpp\n// 步驟二：每次挑較小的接上，再前進\nwhile (l1 && l2) {\n    if (l1->val <= l2->val) {\n        tail->next = l1;\n        l1 = l1->next;\n    } else {\n        tail->next = l2;\n        l2 = l2->next;\n    }\n    tail = tail->next;  // tail 永遠跟到最尾端\n}\n```\n:::\n\n\n\n:::example 步驟三：接上剩餘的一條，回傳真正的頭\n**3. 收尾並讀答案。** 把還沒接完的那條整串直接接上（剩下的本來就有序），最後回傳 `dummy.next`。\n\n```cpp\n// 步驟三：接上剩餘的一條，回傳真正的頭\ntail->next = l1 ? l1 : l2;  // 其中一條可能還有剩\nreturn dummy.next;\n```\n:::\n\n\n\n邊界與複雜度：\n\n- 改 `next` 前若還會用到原本的後續節點，務必先用 `ListNode* next = cur->next;` 存起來。\n- 頭節點可能被換掉或刪除，用 dummy 就不必特判「第一個」。\n- 時間 `O(n + m)`（兩條長度相加），只用了幾個指標，額外空間 `O(1)`。\n\n## C++17 模板\n\n```cpp\n// 合併兩條有序串列（LeetCode 21）。\nListNode* mergeTwoLists(ListNode* a, ListNode* b) {\n  ListNode dummy;\n  ListNode* tail = &dummy;\n\n  while (a && b) {\n    if (a->val <= b->val) { tail->next = a; a = a->next; }  // <= 保證穩定\n    else                  { tail->next = b; b = b->next; }\n    tail = tail->next;\n  }\n  tail->next = a ? a : b;        // 剩下那段整條接上，不必逐點搬\n  return dummy.next;\n}\n```\n\n鏈上加法（LeetCode 2）：\n\n```cpp\nListNode* addTwoNumbers(ListNode* a, ListNode* b) {\n  ListNode dummy;\n  ListNode* tail = &dummy;\n  int carry = 0;\n\n  while (a || b || carry) {      // carry 必須進終止條件\n    int sum = carry;\n    if (a) { sum += a->val; a = a->next; }\n    if (b) { sum += b->val; b = b->next; }\n    carry = sum / 10;\n    tail->next = new ListNode(sum % 10);\n    tail = tail->next;\n  }\n  return dummy.next;\n}\n```\n\n## 時間與空間複雜度\n\n合併：每輪至少消耗一個節點，故至多 $n_1 + n_2$ 輪，時間 $O(n_1 + n_2)$；只用常數指標，額外空間 $O(1)$（結果重用原節點）。收尾的「整段接上」是 $O(1)$，不是 $O(n)$——這一點常被誤算。\n\n鏈上加法時間 $O(\\max(n_1, n_2))$，空間 $O(\\max(n_1,n_2))$ 用於新節點（結果無法重用輸入節點的值）。445 用堆疊則額外 $O(n_1+n_2)$。\n\n## 常見錯誤與邊界條件\n\n- 收尾忘記接上剩餘鏈，答案被截斷。\n- 用 `<` 而非 `<=` 導致不穩定（單獨看答案仍有序，但當作歸併排序的零件時就錯了）。\n- 鏈上加法漏掉最後的 `carry`。\n- 445 忘記數字方向相反、直接從頭相加。\n- 某條輸入鏈為空。\n- 合併後回傳 `a` 或 `b` 而非 `dummy.next`。\n- `new` 出來的節點在多次呼叫下未釋放（面試環境通常不追究，但要能說明）。\n\n## 與相似技巧的比較\n\n- 合併 $k$ 條鏈（23）不要兩兩串接（$O(kn)$），改用分治兩兩配對或小根堆，見 1.9。\n- 陣列的雙序列合併邏輯完全相同，差別在陣列需要 $O(n)$ 目標空間而鏈可原地重接。\n- 與 1.7 的分鏈相比，本節是「多合一」、分鏈是「一分多」，兩者共用 `dummy` 加 `tail` 的追加骨架。\n\n## 代表例題\n\n**題號**：2、21、369、445、1634\n\n放在「合併連結串列」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 2 | [兩數相加](https://leetcode.cn/problems/add-two-numbers/) |  |\n| 21 | [合併兩個有序連結串列](https://leetcode.cn/problems/merge-two-sorted-lists/) |  |\n| 369 | [給單連結串列加一](https://leetcode.cn/problems/plus-one-linked-list/)（Premium） |  |\n| 445 | [兩數相加 II](https://leetcode.cn/problems/add-two-numbers-ii/) |  |\n| 1634 | [求兩個多項式連結串列的和](https://leetcode.cn/problems/add-two-polynomials-represented-as-linked-lists/)（Premium） |  |\n| 2816 | [翻倍以連結串列形式表示的數字](https://leetcode.cn/problems/double-a-number-represented-as-a-linked-list/) | 1394 |\n\n## 本節重點速查\n\n- `dummy` 加 `tail`，每輪挑較小者接上；\n- 用 `<=` 保持穩定；\n- 退出後把剩餘鏈**整段**接上。\n- 前提是兩條輸入各自有序。\n- 鏈上加法把終止條件寫成 `p1 || p2 || carry`。\n",
        },
        {
          id: 11,
          title: "1.9 分治",
          summary:
            "## 這個技術解決什麼問題\n\n合併 $k$ 條有序鏈（23），若用「結果鏈依序與第 $i$ 條合併」，第 $i$ 次合併的結果鏈長度已達 $O(in/k)$，總成本 $\\sum_i O(in/k) = O(kn)$。分治兩兩配對把它降到 $O(n \\log k)$。\n\n連結串列排序（148）也一樣：快排需要隨機存取取中位數，鏈上做不到；歸併排序卻天然適合鏈——**合併是 $O(1)$ 額外空間（1.8），分割靠快慢指標（1.6）**，於是能達成 $O(n \\log n)$ 時間、$O(\\log n)$ 空間（僅遞迴堆疊），這是陣列歸併排序做不到的（陣列版需 $O(n)$ 輔助空間）。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就往「分治」想：\n\n- **問題可以「對半切」，兩半的解法和原問題一模一樣。** 例如第 148 題排序串列、第 23 題合併 K 條串列，切一半之後子問題仍是「排序 / 合併」。\n- **合併兩個子答案很容易做。** 分治划不划算，往往取決於「合」這一步便不便宜：\n\n:::example 嗅到味道的瞬間：子問題同型、且「合」很便宜\n```cpp\n// 嗅到味道的瞬間：子問題同型、且「合」很便宜\n// 子答案是兩條有序串列 -> 合併只要 O(長度) -> 適合分治\n```\n:::\n\n\n\n- **暴力做法層層相依、規模沒被有效縮小（如逐條串接 K 個串列是 `O(N·K)`）；** 改成對半遞迴可把因子降成 `log`。\n- **資料天然有「中點 / 中心」可切**，例如用快慢指標就能切連結串列。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的 base case 是什麼？遞迴一定會停嗎？\n2. 我把問題拆成的子問題，是不是和原問題同型？\n3. 兩個子答案怎麼「合」回去？這步是不是夠便宜？\n\n## 核心想法與直覺\n\n本節談的是「分治（divide and conquer）」在連結串列上的應用。分治的精神是一句老話：**「大問題拆成幾個一樣的小問題，分別解完再合起來。」** 當小問題小到不能再拆（例如只剩一個節點），答案就直接看得出來，這叫「遞迴的終點（base case）」。\n\n:::example 分治三步驟：拆 -> 遞迴解 -> 合併\n為什麼分治會比較快？關鍵在「合併」這一步很便宜，而「拆」每次把規模砍半，於是只需要拆 `log n` 層。以第 148 題「排序連結串列」為例，做法是歸併排序（merge sort）：把串列從中間切兩半，各自排好序，再用前一節「合併兩條有序串列」的技巧把它們併回去。\n\n```cpp\n// 分治三步驟：拆 -> 遞迴解 -> 合併\nListNode* sortList(ListNode* head) {\n    if (!head || !head->next) {\n        return head;  // 0 或 1 個節點，已排序\n    }\n    ListNode* mid = split(head);   // 1) 從中間切成兩半\n    ListNode* l = sortList(head);  // 2) 各自遞迴排序\n    ListNode* r = sortList(mid);\n    return merge(l, r);  // 3) 合併兩條有序串列\n}\n```\n:::\n\n\n\n:::example 快慢指標找中點，並把串列斷成兩半\n「從中間切開」用的是經典的快慢指標：快指標一次走兩步、慢指標一次走一步，快指標到底時，慢指標剛好在中間。\n\n```cpp\n// 快慢指標找中點，並把串列斷成兩半\nListNode* split(ListNode* head) {\n    ListNode* slow = head;\n    ListNode* fast = head->next;\n    while (fast && fast->next) {\n        slow = slow->next;\n        fast = fast->next->next;\n    }\n    ListNode* mid = slow->next;\n    slow->next = nullptr;  // 從中間切斷，前半結束\n    return mid;            // 回傳後半的頭\n}\n```\n:::\n\n\n\n每一層要把所有節點掃過一遍合併（`O(n)`），總共 `log n` 層，所以整體是 `O(n log n)`，比兩兩比較的 `O(n^2)` 快得多。\n\n## 狀態／資料結構定義\n\n- 子問題以「一段鏈」表示。由於鏈無法用下標界定範圍，分割時必須**真的把鏈斷開**：找到前中點後執行 `mid = slow->next; slow->next = nullptr;`，否則兩半仍相連，遞迴會無限展開。\n- 合併 $k$ 條：子問題是區間 $[l, r]$ 上的鏈陣列，遞迴分成 $[l, m]$ 與 $[m+1, r]$。\n- 遞迴出口：`head == nullptr || head->next == nullptr`（0 或 1 個節點必定有序）。\n\n找中點必須取**前中點**（`while (fast->next && fast->next->next)`），理由見下。\n\n## 不變量或正確性證明\n\n**歸併排序的正確性**用強歸納法。命題：`sortList(h)` 回傳的鏈由 `h` 起始的所有節點組成，且非遞減排列。\n\n- 基底：長度 0 或 1 時直接回傳，平凡成立。\n- 歸納：長度 $n \\ge 2$ 時分成兩段，長度分別為 $\\lceil n/2 \\rceil$ 與 $\\lfloor n/2 \\rfloor$，都嚴格小於 $n$，故由歸納假設兩段各自被正確排序。再由 1.8 的合併不變量，合併兩條有序鏈得到的鏈有序且含全部節點。故命題成立。\n\n**終止性**是這裡真正容易出錯的地方，而且它依賴兩件事。其一，分割必須真的斷開；不斷開的話「兩段」其實都是整條鏈，遞迴深度無限。其二，**必須取前中點**：長度為 2 時，後中點會切成「0 個 + 2 個」，子問題與原問題同大，遞迴不下降；前中點切成「1 個 + 1 個」，嚴格下降。所以 `while (fast->next && fast->next->next)` 這個寫法不是風格偏好，是終止性的必要條件。\n\n**穩定性**：分割時前半在原鏈中確實排在後半之前，而 1.8 的合併在相等時優先取第一條鏈，故歸併排序穩定。\n\n**合併 $k$ 條的複雜度論證**：遞迴樹共 $\\log k$ 層，每層所有合併加起來恰好掃過全部 $n$ 個節點一次（各子問題的節點集合互斥），故每層 $O(n)$，總計 $O(n \\log k)$。相對地，順序串接法的每一層都要重掃已合併的長鏈，這正是它退化成 $O(kn)$ 的原因。\n\n## 逐步演算法\n\n分治題目可拆成三個固定步驟，逐一配程式碼：\n\n:::example 步驟一：base case，0 或 1 個元素已是答案\n**1. 寫終點（base case）。** 規模小到不能再拆時直接回傳，否則遞迴會無止境拆下去。\n\n```cpp\n// 步驟一：base case，0 或 1 個元素已是答案\nif (!head || !head->next) {\n    return head;\n}\n```\n:::\n\n\n\n:::example 步驟二：拆成兩半各自遞迴（以 K 條串列為例，分一半）\n**2. 拆成子問題並遞迴。** 連結串列從中點切兩半；若是第 23 題「合併 K 個串列」，則是把 K 條兩兩配對遞迴合併。\n\n```cpp\n// 步驟二：拆成兩半各自遞迴（以 K 條串列為例，分一半）\nListNode* mergeKLists(vector<ListNode*>& lists, int lo, int hi) {\n    if (lo == hi) {\n        return lists[lo];  // 只剩一條\n    }\n    int mid = (lo + hi) / 2;\n    ListNode* l = mergeKLists(lists, lo, mid);\n    ListNode* r = mergeKLists(lists, mid + 1, hi);\n    return merge(l, r);  // 合併兩條有序串列\n}\n```\n:::\n\n\n\n:::example 步驟三：合併兩條有序串列（分治的「合」）\n**3. 合併子答案。** 連結串列題的合併就是前一節「合併兩條有序串列」：比較兩個頭，較小的接上去。\n\n```cpp\n// 步驟三：合併兩條有序串列（分治的「合」）\nListNode* merge(ListNode* a, ListNode* b) {\n    ListNode dummy;\n    ListNode* t = &dummy;\n    while (a && b) {\n        if (a->val <= b->val) {\n            t->next = a;\n            a = a->next;\n        } else {\n            t->next = b;\n            b = b->next;\n        }\n        t = t->next;\n    }\n    t->next = a ? a : b;\n    return dummy.next;\n}\n```\n:::\n\n\n\n邊界與複雜度：\n\n- 一定要寫 base case，否則無限遞迴會堆爆呼叫堆疊（stack overflow）。\n- 切串列後務必把前半的尾端 `next` 設成 `nullptr`，否則兩半會還黏在一起。\n- 第 148 題 `O(n log n)`；第 23 題 K 條共 N 個節點，分治版是 `O(N log K)`，比一條條串接的 `O(N·K)` 快。\n\n## C++17 模板\n\n```cpp\n// 連結串列歸併排序（LeetCode 148）：O(n log n) 時間、O(log n) 堆疊。\nListNode* sortList(ListNode* head) {\n  if (!head || !head->next) { return head; }   // 出口：0 或 1 個節點\n\n  ListNode* slow = head;                       // 取「前中點」，長度為 2 時才會 1+1\n  ListNode* fast = head;\n  while (fast->next && fast->next->next) {\n    slow = slow->next;\n    fast = fast->next->next;\n  }\n  ListNode* mid = slow->next;\n  slow->next = nullptr;                        // 必須真的斷開，否則無限遞迴\n\n  return mergeTwoLists(sortList(head), sortList(mid));   // 合併見 1.8\n}\n```\n\n合併 $k$ 條鏈同樣分治：`merge(l, r)` 回傳 `mergeTwoLists(merge(l, m), merge(m + 1, r))`，出口是 `l == r` 時回傳該條鏈、`l > r` 時回傳 `nullptr`。\n\n## 時間與空間複雜度\n\n歸併排序：遞迴式 $T(n) = 2T(n/2) + O(n)$，其中 $O(n)$ 來自找中點與合併，解得 $O(n \\log n)$。空間只有遞迴堆疊 $O(\\log n)$——合併本身是原地重接指標，不需輔助陣列。自底向上寫法可把堆疊也消掉，達成真正的 $O(1)$ 空間，但程式碼繁瑣許多。\n\n合併 $k$ 條：$O(n \\log k)$ 時間（$n$ 為總節點數），$O(\\log k)$ 堆疊。用小根堆的替代解法同為 $O(n \\log k)$ 時間但需 $O(k)$ 空間，寫起來較短，兩者都可接受。\n\n## 常見錯誤與邊界條件\n\n- 分割後忘記 `slow->next = nullptr`，導致堆疊溢位。\n- 取後中點導致長度為 2 時無限遞迴（最典型的當機原因）。\n- 遞迴出口漏掉 `!head->next`。\n- 合併 $k$ 條時 `l > r`（$k=0$）未處理。\n- 順序串接 $k$ 條而超時。\n- 誤以為鏈上可以寫快排（能寫，但取樞紐只能取頭或隨機走 $O(n)$ 步，最壞 $O(n^2)$ 且不穩定）。\n\n## 與相似技巧的比較\n\n- 對照陣列歸併排序：邏輯相同，但陣列版需 $O(n)$ 輔助空間、鏈版只需 $O(\\log n)$；反過來陣列可用快排達成 $O(1)$ 空間而鏈不行。\n- 合併 $k$ 條的小根堆解法（見 data_structure 的堆一節）在 $k$ 遠小於 $n$ 時常數更小、程式碼更短；分治版不需額外資料結構。\n- 兩兩配對的迭代寫法（每輪把相鄰兩條合併，輪數 $\\lceil \\log k \\rceil$）與遞迴版複雜度相同，可避免遞迴。\n\n## 代表例題\n\n**題號**：23、148\n\n放在「分治」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 23 | [合併 K 個升序連結串列](https://leetcode.cn/problems/merge-k-sorted-lists/) |  |\n| 148 | [排序連結串列](https://leetcode.cn/problems/sort-list/) |  |\n\n## 本節重點速查\n\n- 鏈上排序用歸併不用快排。\n- 三步：快慢指標取**前中點**、`slow->next = nullptr` **真的斷開**、遞迴後用 1.8 合併。\n- 合併 $k$ 條用分治或小根堆得 $O(n \\log k)$，不要順序串接。\n",
        },
        {
          id: 12,
          title: "1.10 綜合應用",
          summary:
            "## 這個技術解決什麼問題\n\nLRU（146）、LFU（460）、快取與迭代器設計題要求**每個操作都是 $O(1)$**。單用雜湊表能 $O(1)$ 查值，但找不到「最久沒用的是誰」；單用鏈能 $O(1)$ 移動節點，但找不到指定 key 在哪。\n\n槓桿在於**組合**：雜湊表存 `key -> 節點指標`，雙向鏈維護使用次序。查詢用雜湊表定位，重排用鏈的改邊；兩者各自補上對方的短板，於是插入、查詢、淘汰全部 $O(1)$。這是「用空間換操作時間、用兩種結構換單一結構做不到的組合能力」的典型例子。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就往「連結串列綜合應用」想：\n\n- **題目要你「設計一個資料結構」，並要求多數操作做到 `O(1)`。** 例如第 146 題 LRU、第 460 題 LFU、第 432 題全 `O(1)` 結構。\n- **需要頻繁「依某種順序」搬動元素**（最近用的提到前面、最久沒用的淘汰），同時又要能用鍵快速找到元素：\n\n:::example 嗅到味道的瞬間：既要 O(1) 查找、又要 O(1) 從中間搬移\n```cpp\n// 嗅到味道的瞬間：既要 O(1) 查找、又要 O(1) 從中間搬移\n// 查找 -> unordered_map；中間插刪 -> 雙向連結串列\n```\n:::\n\n\n\n- **單用陣列會在插刪時 `O(n)` 搬移、單用雜湊表又記不住順序。** 兩者合體才能同時滿足「查得快」與「搬得快」。\n- **第 707 題直接要你實作連結串列本身**，是練習這些指標改接基本功的入門。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我用什麼結構做到 `O(1)` 查找？又用什麼做到 `O(1)` 從中間插刪？\n2. 我有沒有加頭尾哨兵來省掉邊界特判？\n3. 淘汰一個節點時，雜湊表裡對應的鍵我有一起刪掉嗎？\n\n## 核心想法與直覺\n\n本節是連結串列的「綜合應用」：把連結串列當成一塊積木，和雜湊表（hash map）等結構搭在一起，設計出有特定行為的資料結構。代表題目是第 146 題「LRU 快取」、第 460 題「LFU 快取」、第 707 題「設計連結串列」、第 432 題「全 `O(1)` 的資料結構」。\n\n先說為什麼要用連結串列。陣列的麻煩在於：要在中間插入或刪除元素，後面全部得搬位子，是 `O(n)`。連結串列剛好相反：**只要手上握有某個節點，把它從鏈中拔掉或插進去，都只是改幾條指標，是 `O(1)`。** 這正是設計「需要頻繁搬動元素」的快取時最需要的能力。\n\n:::example 招牌組合：哈希表(鍵 -> 節點) + 雙向連結串列\n但連結串列也有弱點：**它不能像陣列那樣用索引一步跳到某個節點，要找東西得從頭一個個走。** 解法是搭配一個雜湊表，把「鍵」直接對應到「節點指標」，這樣就能 `O(1)` 找到節點，再 `O(1)` 把它搬走。這個「雜湊表 + 雙向連結串列」的組合是本節的招牌。\n\n```cpp\n// 招牌組合：哈希表(鍵 -> 節點) + 雙向連結串列\nstruct Node {\n    int key, val;\n    Node *prev, *next;\n};\nunordered_map<int, Node *>\n        mp;  // O(1) 由鍵找到節點\n                  // 雙向：每個節點同時有 prev 與 next，方便從中間拔除\n```\n:::\n\n\n\n:::example O(1) 從雙向串列中拔除一個節點\n為什麼要「雙向」連結串列？因為要把中間某個節點拔掉，必須知道它「前一個」是誰才能接合。單向串列只有 `next`，找前驅得從頭掃；雙向串列多存一個 `prev`，拔除就真的是 `O(1)`。\n\n```cpp\n// O(1) 從雙向串列中拔除一個節點\nvoid remove(Node* x) {\n    x->prev->next = x->next;  // 前一個直接指向後一個\n    x->next->prev = x->prev;  // 後一個的 prev 也接回去\n}\n```\n:::\n\n\n\n以 LRU 為例：把「最近用過」的放鏈頭、「最久沒用」的留在鏈尾。每次存取就把該節點搬到頭；容量滿了就刪鏈尾。每個操作都是 `O(1)`。\n\n## 狀態／資料結構定義\n\n- `unordered_map<int, Node*> mp`：key 到節點的直接指標。存指標而非索引，才能 $O(1)$ 拿到節點。\n- 雙向鏈 `Node { key, val, prev, next }`：**必須雙向**。單向鏈刪除節點需要前驅，而從雜湊表拿到的是節點本身，找前驅要 $O(n)$，$O(1)$ 就破功了。\n- 節點必須存 `key`：淘汰尾節點時要用它從雜湊表中 `erase`，否則有節點沒 key 可刪，雜湊表會殘留失效指標。\n- 兩個哨兵 `head`、`tail` 首尾相連：讓所有插入刪除都在「有前驅也有後繼」的情形下進行，完全免去空鏈與頭尾特例。約定 `head->next` 是最近使用、`tail->prev` 是最久未用。\n\nLFU 再加一層：`freq -> 該頻次的雙向鏈`，以及 `minFreq`。\n\n## 不變量或正確性證明\n\nLRU 的資料結構不變量有三條，每個操作前後都必須成立：\n\n1. `mp` 的鍵集合恰等於鏈中節點的 key 集合，且 `mp[k]` 指向鏈中那個節點（無失效指標、無遺漏）。\n2. 鏈從 `head->next` 到 `tail->prev` 依「最近使用時間」由新到舊排列。\n3. 鏈長（不含哨兵）不超過 `capacity`。\n\n驗證 `get(k)`：若 `k` 不在 `mp` 回傳 $-1$，三條不變量未被觸碰。若在，取出節點、從原位置摘除、插到 `head` 之後。摘除與插入都不改變節點集合，故條件 1、3 維持；條件 2 成立是因為該節點剛被使用，理應排最前，其餘節點相對次序不變。\n\n驗證 `put(k, v)`：若 `k` 已存在，更新值並移到最前，同上。若不存在，先建節點插到最前並登記到 `mp`（條件 1、2 維持，鏈長加一）；若此時超過容量，摘除 `tail->prev` 並用**該節點存的 key** 從 `mp` 中 erase，鏈長回到容量內，條件 1、3 恢復。\n\n**淘汰的正確性**：由條件 2，`tail->prev` 是所有節點中最久未被使用者，正是 LRU 定義要淘汰的對象。這個推論完全依賴條件 2，而條件 2 又依賴「每次存取都把節點移到最前」——這就是為什麼 `get` 命中時也必須移動，只讀不移是最常見的錯誤。\n\n順序也有講究：**必須先插入再檢查淘汰**。若先淘汰再插入，當容量為 1 且插入的是新 key 時仍正確，但若寫成「先淘汰」且新 key 恰是被淘汰者，就會出現先刪後加的多餘工作；更嚴重的是若容量檢查用 `>=` 而非 `>`，容量未滿時也會誤淘汰。\n\nLFU 的額外不變量是 `minFreq` 等於所有節點頻次的最小值。它只在兩處可能改變：插入新節點時必然重置為 1；某節點頻次從 `minFreq` 升上去且該頻次鏈變空時，`minFreq` 加一即可——**不需要重新掃描**，因為頻次每次只加 1，最小值最多也只上升 1。這個攤還論證是 LFU 能做到 $O(1)$ 的關鍵。\n\n## 逐步演算法\n\n設計這類結構可拆成四個步驟，逐一配程式碼：\n\n:::example 步驟一：頭尾哨兵，串列永遠非空，省去邊界特判\n**1. 選資料表示：雙向串列 + 雜湊表，並加頭尾哨兵。** 頭尾各放一個不存資料的哨兵節點（sentinel），這樣任何節點都保證有 `prev` 和 `next`，插刪不必特判邊界。\n\n```cpp\n// 步驟一：頭尾哨兵，串列永遠非空，省去邊界特判\nNode* head = new Node();  // 哨兵頭\nNode* tail = new Node();\nhead->next = tail;\ntail->prev = head;\n```\n:::\n\n\n\n:::example 步驟二：插到頭部（代表「最新使用」）\n**2. 寫好兩個 O(1) 基本動作：插到頭、從中間拔除。** 所有高階操作都由這兩個拼出來。\n\n```cpp\n// 步驟二：插到頭部（代表「最新使用」）\nvoid pushFront(Node* x) {\n    x->prev = head;\n    x->next = head->next;\n    head->next->prev = x;\n    head->next = x;\n}\n```\n:::\n\n\n\n:::example 步驟三：get = 找到 + 搬到頭 + 回傳值\n**3. 用雜湊表把鍵接到節點，組出 get / put。** `get` 找到節點、搬到頭並回傳值；`put` 若已存在就更新並搬到頭，不存在就新建、必要時淘汰鏈尾。\n\n```cpp\n// 步驟三：get = 找到 + 搬到頭 + 回傳值\nint get(int key) {\n    if (!mp.count(key)) {\n        return -1;\n    }\n    Node* x = mp[key];\n    remove(x);\n    pushFront(x);  // 標記為最近使用\n    return x->val;\n}\n```\n:::\n\n\n\n:::example 步驟四：容量滿，淘汰最久未用（尾端）\n**4. 處理容量上限與淘汰。** 超過容量時，刪掉哨兵尾巴前一個節點，並從雜湊表移除對應的鍵。\n\n```cpp\n// 步驟四：容量滿，淘汰最久未用（尾端）\nif (mp.size() > capacity) {\n    Node* lru = tail->prev;\n    remove(lru);\n    mp.erase(lru->key);\n    delete lru;\n}\n```\n:::\n\n\n\n邊界與複雜度：\n\n- 哨兵頭尾能避免「鏈為空 / 只有一個節點」的特判。\n- 拔除節點後別忘了同步從雜湊表 `erase` 鍵，否則會殘留懸空指標。\n- 每個操作 `O(1)`；空間 `O(容量)`。LFU（第 460 題）多一層「按使用次數分組」，但每組內仍是同一套雙向串列搬移。\n\n## C++17 模板\n\n```cpp\n// LRU 快取（LeetCode 146）：雜湊表 + 雙向鏈，get/put 皆 O(1)。\nclass LRUCache {\n  struct Node {\n    int key, val;\n    Node *prev = nullptr, *next = nullptr;\n    Node(int k, int v) : key(k), val(v) {}\n  };\n\n  int cap_;\n  Node* head_;                       // 哨兵：head_->next 是最近使用\n  Node* tail_;                       // 哨兵：tail_->prev 是最久未用\n  std::unordered_map<int, Node*> mp_;\n\n  void remove(Node* n) {             // 從鏈上摘除；雙向鏈才能 O(1)\n    n->prev->next = n->next;\n    n->next->prev = n->prev;\n  }\n  void pushFront(Node* n) {          // 插到最前，代表剛被使用\n    n->next = head_->next;\n    n->prev = head_;\n    head_->next->prev = n;\n    head_->next = n;\n  }\n\n public:\n  explicit LRUCache(int capacity) : cap_(capacity) {\n    head_ = new Node(0, 0);\n    tail_ = new Node(0, 0);\n    head_->next = tail_;             // 兩個哨兵首尾相連，免去所有邊界特例\n    tail_->prev = head_;\n  }\n\n  int get(int key) {\n    auto it = mp_.find(key);\n    if (it == mp_.end()) { return -1; }\n    remove(it->second);\n    pushFront(it->second);           // 命中也要移到最前，否則不變量 2 崩壞\n    return it->second->val;\n  }\n\n  void put(int key, int value) {\n    if (auto it = mp_.find(key); it != mp_.end()) {\n      it->second->val = value;\n      remove(it->second);\n      pushFront(it->second);\n      return;\n    }\n    Node* n = new Node(key, value);\n    mp_[key] = n;\n    pushFront(n);\n    if (static_cast<int>(mp_.size()) > cap_) {   // 先插入再淘汰\n      Node* victim = tail_->prev;\n      remove(victim);\n      mp_.erase(victim->key);        // 節點必須存 key，才刪得掉雜湊表項\n      delete victim;\n    }\n  }\n};\n```\n\nC++ 也可直接用 `std::list` 加 `unordered_map<int, list<...>::iterator>`，`splice` 能 $O(1)$ 移動節點且不使迭代器失效。\n\n## 時間與空間複雜度\n\n`get` 與 `put` 都是 $O(1)$ **均攤**：雜湊表查找均攤 $O(1)$（最壞 $O(n)$，發生在大量雜湊碰撞時），鏈上的摘除與插入是最壞 $O(1)$。空間 $O(\\text{capacity})$，雜湊表與鏈各存一份指標。\n\nLFU 同樣是均攤 $O(1)$，關鍵在 `minFreq` 的維護只需常數次更新，如前述攤還論證。\n\n## 常見錯誤與邊界條件\n\n- `get` 命中時忘記把節點移到最前（測資稍長就會露餡）。\n- 用單向鏈導致刪除需找前驅而退化成 $O(n)$。\n- 節點不存 `key`，淘汰時無法從雜湊表刪除，留下失效指標。\n- 容量檢查用 `>=` 而在未滿時誤淘汰。\n- `capacity` 為 0 的情形。\n- `put` 已存在的 key 時又插入一個新節點造成重複。\n- 忘記 `delete` 被淘汰節點（記憶體洩漏）。\n- LFU 忘記在插入新節點時把 `minFreq` 重設為 1。\n\n## 與相似技巧的比較\n\n- 只需要「先進先出」淘汰用單純佇列即可，不必雙向鏈；LRU 之所以要雙向鏈，是因為需要把**中間**的節點移到最前。\n- 若允許 $O(\\log n)$，用平衡樹依時間戳排序也能實作，程式碼較短但更慢；面試明確要求 $O(1)$ 時只能用本節的組合。\n- 設計題（707 設計鏈、432 全 $O(1)$ 資料結構、1019 鏈中下一個更大元素）的共同套路都是「先問清楚每個操作的目標複雜度，再反推需要哪幾種結構組合」。1019 則是把鏈倒成陣列後用單調堆疊，屬於換結構而非組合結構。\n\n## 代表例題\n\n**題號**：146、432、460、707、1019\n\n放在「綜合應用」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 146 | [LRU 快取](https://leetcode.cn/problems/lru-cache/) |  |\n| 432 | [全 $O(1)$ 的資料結構](https://leetcode.cn/problems/all-oone-data-structure/) |  |\n| 460 | [LFU 快取](https://leetcode.cn/problems/lfu-cache/) |  |\n| 707 | [設計連結串列](https://leetcode.cn/problems/design-linked-list/) |  |\n| 1019 | [連結串列中的下一個更大節點](https://leetcode.cn/problems/next-greater-node-in-linked-list/) | 1571 |\n| 1171 | [從連結串列中刪去總和值為零的連續節點](https://leetcode.cn/problems/remove-zero-sum-consecutive-nodes-from-linked-list/) | 1782 |\n| 1206 | [設計跳錶](https://leetcode.cn/problems/design-skiplist/) |  |\n\n## 本節重點速查\n\n- 雜湊表管定位、雙向鏈管次序，兩者組合達成 $O(1)$。\n- 必用雙向鏈與雙哨兵；\n- 節點要存 key 以便反向刪雜湊表項；\n- `get` 命中也要移到最前；\n- 先插入再淘汰。\n- 核心不變量是「鏈序即最近使用序」。\n",
        },
        {
          id: 13,
          title: "1.11 其他",
          summary:
            "## 這個技術解決什麼問題\n\n這一節收攏幾個不屬於前面任一模式、但各自有代表性的鏈表題型：深拷貝帶隨機指標的鏈（138）、扁平化多層雙向鏈（430）、隨機取樣（382）、以及以鏈為底層的迭代器設計（1265）。\n\n它們的共同處是：**原本的「一趟掃描」不夠用，因為節點之間存在第二種連結關係**（`random` 指標、`child` 指標）或**輸入長度未知**（串流取樣）。解法各自不同，但都圍繞「如何在不能回頭、不能隨機存取的限制下建立節點之間的對應」。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就往本節的「其他技巧」想：\n\n- **節點上有「非線性」的額外指標**，例如 `random`（第 138 題）或 `child`（第 430 題），單純沿 `next` 走無法解決。\n- **要建立「節點到副本／到某資訊」的對應，而對應對象當下還沒生出來** —— 先建雜湊表，分趟處理：\n\n:::example 嗅到味道的瞬間：現在要接的指標，指向的東西還沒準備好\n```cpp\n// 嗅到味道的瞬間：現在要接的指標，指向的東西還沒準備好\n// 解法：先把所有對應記進 unordered_map，第二趟再接\n```\n:::\n\n\n\n- **不知道串列長度、又限制只走一趟或 `O(1)` 空間做隨機** —— 這是水塘抽樣的招牌場景（第 382 題）。\n- **巢狀 / 多層結構要攤平** —— 想到 DFS，把子層就地插入主鏈（第 430 題）。\n\n確認自己有沒有套對，問三個問題：\n\n1. 這題卡住，是不是因為「現在要用的資訊還沒準備好」？要不要先建表分趟做？\n2. 節點上除了 `next`，還有沒有別的指標（`random`、`child`）需要一起處理？\n3. 若要隨機或限制空間，我能不能用水塘抽樣只走一趟？\n\n## 核心想法與直覺\n\n本節收錄連結串列的「其他」技巧，這些題目不靠單一招式，而是各有巧思。代表題目有第 138 題「隨機連結串列的複製」、第 382 題「連結串列隨機節點」、第 430 題「扁平化多級雙向連結串列」、第 1265 題「逆序列印不可變連結串列」。它們共同的精神是：**面對連結串列，先想清楚「我需要什麼額外資訊」，再決定要不要多走幾趟、或借一份輔助結構。**\n\n:::example 第 138 題：用哈希表記住 原節點 -> 新節點 的對應\n以第 138 題為例。每個節點除了 `next`，還有一個 `random` 指標可指向任意節點。複製時的難點是：當你正在做某節點的副本，它的 `random` 指向的那個節點可能還沒被複製出來，你不知道副本是誰。解法是借一個雜湊表，先把「原節點 -> 新節點」的對應全部記下來，再回頭接指標。\n\n```cpp\n// 第 138 題：用哈希表記住 原節點 -> 新節點 的對應\nunordered_map<Node*, Node*> mp;\nfor (Node* p = head; p; p = p->next) {\n    mp[p] = new Node(p->val);  // 第一趟：先把所有節點複製出來\n}\n```\n:::\n\n\n\n:::example 第二趟：副本的 next / random 都靠查表接上\n```cpp\n// 第二趟：副本的 next / random 都靠查表接上\nfor (Node* p = head; p; p = p->next) {\n    mp[p]->next = mp[p->next];  // p->next 可能是 nullptr，查表得 nullptr\n    mp[p]->random = mp[p->random];  // 此時對應的副本一定已存在\n}\n```\n:::\n\n\n\n第 430 題「扁平化多級串列」則是另一種思路：每個節點除了 `next` 還有一個 `child` 子串列，要把它攤平成一條。遇到有 `child` 的節點，就先把子串列整段插進「目前節點和它原本的 next 之間」，再繼續往下，本質是深度優先（DFS）。第 382 題用到「水塘抽樣（reservoir sampling）」，可在不知道長度、只走一趟的情況下等機率隨機選一個節點。\n\n## 狀態／資料結構定義\n\n- 深拷貝（138）：需要一組「原節點 → 新節點」的對應。兩種做法——用 `unordered_map<Node*, Node*>` 明確存，或用**交錯法**把新節點暫時插在原節點之後，讓對應關係隱含在 `cur->next` 裡。\n- 扁平化（430）：用堆疊存「待接回的後繼」，或遞迴處理 `child` 後回傳該子鏈的尾。核心狀態是「這一層還沒接回的 `next`」。\n- 隨機取樣（382）：狀態只有 `chosen` 與已看過的個數 `i`。\n- 迭代器（1265）：狀態是遞迴堆疊或顯式堆疊。\n\n## 不變量或正確性證明\n\n**交錯法深拷貝的不變量。** 分三趟，每趟結束時都有明確的結構性質：\n\n1. 第一趟後，鏈形如 $A \\to A' \\to B \\to B' \\to \\dots$，即每個原節點 $X$ 之後緊跟其複本 $X'$。此時**「$X$ 的複本」可以 $O(1)$ 取得，就是 `X->next`**——這正是交錯法用結構替代雜湊表的地方。\n2. 第二趟設定隨機指標：`X'->random = X->random->next`。正確性直接來自性質 1：`X->random` 是某個原節點 $Y$，而 $Y$ 的複本正是 `Y->next`。必須先完成整個第一趟才能做第二趟，因為 `X->random` 可能指向鏈中任意位置（包括尚未走到的後方），只有全部複本都就位後這個等式才對每個 $X$ 都成立。這是「為什麼不能一趟做完」的實質理由。\n3. 第三趟拆鏈，把原鏈與新鏈的 `next` 各自還原。若題目要求不修改輸入，這一趟不可省。\n\n`X->random` 可能為 `nullptr`，此時 `X'->random` 也要是 `nullptr`，不能寫成 `X->random->next` 而解參考空指標。\n\n**扁平化（430）的正確性**用結構歸納：對節點 `cur`，若無 `child` 則直接前進；若有 `child`，遞迴扁平化該子鏈得到一條無 `child` 的雙向鏈及其尾節點 `t`，然後把 `cur->next` 暫存為 `nxt`，接成 `cur -> child... -> t -> nxt`，並清空 `cur->child`。歸納假設保證子鏈已無巢狀，故接回後 `cur` 到 `t` 這段也無巢狀。**必須先存 `nxt` 再改 `cur->next`**，否則後半鏈遺失；且**必須清空 `child`**，否則題目要求的「不再有 child」不成立。雙向鏈還要同步設定 `prev`，漏掉時單向遍歷看似正確、反向遍歷才會出錯。\n\n**蓄水池取樣（382）的正確性。** 演算法：看到第 $i$ 個節點（從 1 起算）時，以機率 $1/i$ 用它取代目前選中的值。宣稱：掃完 $n$ 個節點後，每個節點被選中的機率都是 $1/n$。\n\n歸納證明。$n = 1$ 時必選第一個，機率 1，成立。設對長度 $n-1$ 成立，考慮長度 $n$：第 $n$ 個節點被選中的機率依規則為 $1/n$。對 $j < n$ 的節點，它要留到最後，必須先在前 $n-1$ 步中被選中（歸納假設，機率 $\\frac{1}{n-1}$），且第 $n$ 步沒有被取代（機率 $1 - \\frac1n = \\frac{n-1}{n}$）。兩者獨立，故機率為 $\\frac{1}{n-1} \\cdot \\frac{n-1}{n} = \\frac1n$。所有節點機率相同，得證。\n\n這個證明的價值在於它解釋了**為何機率必須恰為 $1/i$ 而非某個固定值**：正是 $\\frac{1}{i-1} \\cdot \\frac{i-1}{i}$ 的伸縮相消讓每一步都把機率重新攤平。也解釋了為何這個方法不需要事先知道 $n$——每一步的規則只用到 $i$。\n\n## 逐步演算法\n\n這類題沒有單一模板，但有幾個反覆出現的招式，逐一配程式碼：\n\n:::example 招式一：兩趟法 + 哈希表（第 138 題核心）\n**1. 需要「之後才知道的對應關係」時，先建雜湊表。** 像複製帶 `random` 指標的串列，分兩趟：第一趟造節點建表，第二趟接指標。\n\n```cpp\n// 招式一：兩趟法 + 哈希表（第 138 題核心）\nfor (Node* p = head; p; p = p->next) {\n    mp[p] = new Node(p->val);\n}\nfor (Node* p = head; p; p = p->next) {\n    mp[p]->next = mp[p->next];\n    mp[p]->random = mp[p->random];\n}\n```\n:::\n\n\n\n:::example 招式二：扁平化，把 child 串列插到 cur 與 cur->next 之間（第 43…\n**2. 遇到「多層 / 巢狀」結構，用 DFS 把子串列就地插入。** 把 `child` 那段攤開接到目前節點後面，再把原本的 `next` 接到子串列尾端。\n\n```cpp\n// 招式二：扁平化，把 child 串列插到 cur 與 cur->next 之間（第 430 題）\nif (cur->child) {\n    Node* nxt = cur->next;\n    cur->next = cur->child;\n    cur->child->prev = cur;\n    cur->child = nullptr;  // 攤平後 child 要清空\n    Node* tail = cur->next;\n    while (tail->next) {\n        tail = tail->next;  // 找到子串列尾\n    }\n    tail->next = nxt;\n    if (nxt) {\n        nxt->prev = tail;\n    }\n}\n```\n:::\n\n\n\n:::example 招式三：水塘抽樣，O(1) 空間、一趟取等機率隨機節點（第 382 題）\n**3. 只能走一趟又要隨機，用水塘抽樣。** 走到第 `i` 個節點時，以 `1/i` 的機率用它替換目前答案，最終每個節點被選中的機率都相等。\n\n```cpp\n// 招式三：水塘抽樣，O(1) 空間、一趟取等機率隨機節點（第 382 題）\nint res = 0, i = 0;\nfor (Node* p = head; p; p = p->next) {\n    if (rand() % (++i) == 0) {\n        res = p->val;  // 第 i 個以 1/i 機率被選中\n    }\n}\nreturn res;\n```\n:::\n\n\n\n邊界與複雜度：\n\n- 查表接指標時，`nullptr` 也要能查到 `nullptr`；用 `unordered_map` 時 `mp[nullptr]` 預設就是 `nullptr`，剛好正確。\n- 扁平化插入子串列後，記得把 `child` 清成 `nullptr`，並維護好雙向的 `prev`。\n- 第 138／430 題時間 `O(n)`；雜湊表法空間 `O(n)`，扁平化與水塘抽樣可做到 `O(1)` 額外空間。\n\n## C++17 模板\n\n```cpp\n// 深拷貝帶隨機指標的鏈（LeetCode 138），交錯法，O(1) 額外空間。\nNode* copyRandomList(Node* head) {\n  if (!head) { return nullptr; }\n\n  for (Node* cur = head; cur; cur = cur->next->next) {   // 1. 複本插在原節點之後\n    Node* copy = new Node(cur->val);\n    copy->next = cur->next;\n    cur->next = copy;\n  }\n\n  for (Node* cur = head; cur; cur = cur->next->next) {   // 2. 靠交錯結構設隨機指標\n    if (cur->random) { cur->next->random = cur->random->next; }\n  }\n\n  Node dummy;                                            // 3. 拆開兩條鏈\n  Node* tail = &dummy;\n  for (Node* cur = head; cur; cur = cur->next) {\n    Node* copy = cur->next;\n    cur->next = copy->next;      // 還原原鏈\n    tail->next = copy;\n    tail = copy;\n  }\n  return dummy.next;\n}\n```\n\n蓄水池取樣（LeetCode 382）：\n\n```cpp\nint getRandom() {\n  int chosen = 0, i = 0;\n  for (ListNode* cur = head_; cur; cur = cur->next) {\n    if (rand() % (++i) == 0) { chosen = cur->val; }   // 第 i 個以 1/i 取代\n  }\n  return chosen;\n}\n```\n\n## 時間與空間複雜度\n\n深拷貝：交錯法三趟各 $O(n)$，總計 $O(n)$ 時間、$O(1)$ 額外空間（不計新節點本身）；雜湊表法同為 $O(n)$ 時間但額外 $O(n)$ 空間，程式碼短很多。\n\n扁平化：每個節點恰被走訪一次，$O(n)$ 時間；遞迴版空間 $O(d)$（$d$ 為巢狀深度，最壞 $O(n)$），堆疊版相同。\n\n蓄水池取樣：每次 `getRandom` 都要走完整條鏈，$O(n)$ 時間、$O(1)$ 空間。若鏈長固定且允許預處理，倒成陣列後可 $O(1)$ 查詢但需 $O(n)$ 空間——蓄水池的優勢在於**長度未知或資料為串流**時仍然可用。\n\n## 常見錯誤與邊界條件\n\n- 深拷貝把三趟合成一趟，導致 `X->random` 指向後方尚未建立複本的節點時取到錯的值。\n- `random` 為 `nullptr` 時解參考。\n- 第三趟忘記還原原鏈的 `next`（題目通常要求輸入不被破壞）。\n- 扁平化忘記清空 `child` 或忘記設定 `prev`。\n- 扁平化改 `cur->next` 前沒先存後半鏈。\n- 蓄水池用 `rand() % i` 卻從 $i=0$ 起算而除以零。\n- 空鏈輸入。\n\n## 與相似技巧的比較\n\n- 交錯法與雜湊表法的取捨很典型：前者 $O(1)$ 空間但要三趟且會暫時破壞輸入結構，後者好寫但吃 $O(n)$ 空間。面試若限定 $O(1)$ 空間，答案就是交錯法。\n- 扁平化與二元樹的前序展開（114，見 2.14）是同一個問題的兩種外觀，都是「把巢狀結構壓成單鏈，注意先存後繼再改指標」。\n- 蓄水池取樣可推廣到取 $k$ 個：前 $k$ 個直接放入，第 $i > k$ 個以 $k/i$ 的機率替換其中隨機一個，證明方式與上面完全平行。\n\n## 代表例題\n\n**題號**：138、382、430、1265\n\n放在「其他」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 138 | [隨機連結串列的複製](https://leetcode.cn/problems/copy-list-with-random-pointer/) |  |\n| 382 | [連結串列隨機節點](https://leetcode.cn/problems/linked-list-random-node/) |  |\n| 430 | [扁平化多級雙向連結串列](https://leetcode.cn/problems/flatten-a-multilevel-doubly-linked-list/) |  |\n| 1265 | [逆序列印不可變連結串列](https://leetcode.cn/problems/print-immutable-linked-list-in-reverse/)（Premium） |  |\n\n## 本節重點速查\n\n- 深拷貝用交錯法：插複本、設 random、拆鏈，三趟不能合併。\n- 扁平化先存後繼再接子鏈，記得清 `child` 補 `prev`。\n- 蓄水池取樣第 $i$ 個以 $1/i$ 取代，$1/i$ 這個值由歸納證明中的伸縮相消決定，不能改。\n",
        },
      ],
    },
    {
      id: 14,
      title: "2. 二叉樹",
      summary:
        "涵蓋二叉樹的遍歷、DFS/BFS、LCA、直徑、BST、N叉樹、樹形DP、連結串列+二叉樹等，適合系統學習與刷題。\n\n:::example 二叉樹 DFS（前序遍歷）\n**C++ 模板（二叉樹 DFS）**：\n\n```cpp\n// 二叉樹 DFS（前序遍歷）\nvoid dfs(TreeNode* root) {\n    if (!root) {\n        return;\n    }\n    // 處理 root->val（前序）\n    dfs(root->left);\n    dfs(root->right);\n}\n\n// 求二叉樹最大深度（後序遍歷）\nint maxDepth(TreeNode* root) {\n    if (!root) {\n        return 0;\n    }\n    return max(maxDepth(root->left), maxDepth(root->right)) + 1;\n}\n```\n:::",
      children: [
        {
          id: 15,
          title: "2.1 遍歷二叉樹",
          summary:
            "## 這個技術解決什麼問題\n\n樹是遞迴定義的結構：一個節點加上左右兩棵子樹。要對每個節點做一次處理，最自然的方式就是讓程式碼的形狀跟著資料的形狀走——**遞迴到左子樹、處理自己、遞迴到右子樹**。這就是遍歷。\n\n它本身不是「技巧」而是**基礎設施**：後面所有樹題（DFS 求值、樹形 DP、LCA、建樹）都是在遍歷的骨架上塞進不同的處理內容。搞清楚前序、中序、後序三者的差別，等於掌握了「在什麼時機點你手上有什麼資訊」。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就往「遍歷二叉樹」想：\n\n- **輸入是一棵二叉樹，要求「對每個節點做點事」或收集所有節點的某種資訊。** 例如第 94／144／145 題就是按特定順序輸出所有節點值。\n- **答案可以邊走邊累積。** 走訪時順手更新一個變數即可：\n\n:::example 嗅到味道的瞬間：每個節點都要看一眼、順手記點東西\n```cpp\n// 嗅到味道的瞬間：每個節點都要看一眼、順手記點東西\n// -> 遞迴 dfs，挑前 / 中 / 後序，邊走邊更新答案\n```\n:::\n\n\n\n- **題目暗示了順序需求。** 「按由小到大」往往是中序（二叉搜尋樹中序即有序）；「先處理父再處理子」是前序。\n- **暴力地對每個節點都重新從根找一遍是 `O(n^2)`；** 一次遍歷把每個節點只看一次就是 `O(n)`。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的 base case 是不是「節點為空就 return」？\n2. 我該在「走左 / 走右」之前還是之後處理自己（前 / 中 / 後序）？\n3. 答案是用哪個變數邊走邊累積、遍歷結束後讀出來的？\n\n## 核心想法與直覺\n\n本節要學的是「遍歷二叉樹（traverse a binary tree）」：把一棵樹上每個節點都不重不漏地走訪一次。先講清楚名詞。二叉樹是一種分叉的結構，每個節點最多有左、右兩個子節點；最上面那個叫「根（root）」，沒有子節點的叫「葉（leaf）」。\n\n樹和陣列最大的不同是：**陣列是一條直線，從頭走到尾就好；樹有分叉，走到一個節點時你有兩條路（左、右）要走。** 所以遍歷的關鍵是定義「先走哪邊、什麼時候處理自己」。最自然的寫法是遞迴：對每個節點，就「處理自己、遞迴左、遞迴右」，三者的先後順序不同，就得到三種遍歷：\n\n- 前序（preorder）：自己 -> 左 -> 右（第 144 題）\n- 中序（inorder）：左 -> 自己 -> 右（第 94 題）\n- 後序（postorder）：左 -> 右 -> 自己（第 145 題）\n\n:::example 二叉樹節點與「中序遍歷」：左 -> 自己 -> 右\n```cpp\n// 二叉樹節點與「中序遍歷」：左 -> 自己 -> 右\nstruct TreeNode {\n    int val;\n    TreeNode *left, *right;\n};\nvoid inorder(TreeNode* root, vector<int>& out) {\n    if (!root) {\n        return;  // 走到空節點就回頭（base case）\n    }\n    inorder(root->left, out);   // 先把左子樹全部走完\n    out.push_back(root->val);   // 再處理自己\n    inorder(root->right, out);  // 最後走右子樹\n}\n```\n:::\n\n\n\n為什麼這樣就能不重不漏？因為每個節點都恰好被它的父節點呼叫一次，而 `if (!root) return;` 保證走到底就回頭，不會無限繞。整棵樹有 n 個節點，每個處理一次，所以是 `O(n)`。\n\n:::example 「前序遍歷」：把「處理自己」提到最前 -> 自己 -> 左 -> 右\n換個順序就換一種遍歷。例如前序只要把「處理自己」這行移到最前面：\n\n```cpp\n// 「前序遍歷」：把「處理自己」提到最前 -> 自己 -> 左 -> 右\nvoid preorder(TreeNode* root, vector<int>& out) {\n    if (!root) {\n        return;\n    }\n    out.push_back(root->val);  // 先處理自己\n    preorder(root->left, out);\n    preorder(root->right, out);\n}\n```\n:::\n\n\n\n順序的選擇要看題目要的資訊何時可用。例如第 671 題「二叉樹中第二小的節點」、第 404 題「左葉子之和」，都是在遍歷過程中順手累積一個答案。\n\n## 狀態／資料結構定義\n\n- `TreeNode { int val; TreeNode *left, *right; }`，空子樹用 `nullptr` 表示。\n- 遞迴函式 `dfs(node)`：語意是「處理以 `node` 為根的整棵子樹」。出口固定是 `if (!node) return;`。\n- 三種順序的差別只在**處理自己這一行放在哪裡**：\n  - 前序（144）：處理自己 → 左 → 右。進入節點時處理。\n  - 中序（94）：左 → 處理自己 → 右。左子樹做完時處理。\n  - 後序（145）：左 → 右 → 處理自己。兩棵子樹都做完時處理。\n- 迭代版需要顯式堆疊，因為要手動保存「回來之後該做什麼」。\n\n## 不變量或正確性證明\n\n**每個節點恰被訪問一次。** 用結構歸納證明 `dfs(node)` 恰好訪問以 `node` 為根的子樹中的每個節點各一次。基底：`node == nullptr` 時訪問 0 個節點，成立。歸納：`dfs(node)` 訪問 `node` 自己一次，並呼叫 `dfs(node->left)` 與 `dfs(node->right)`；由歸納假設，兩者分別恰訪左右子樹各節點一次。由於樹的結構保證**左子樹、右子樹、根三者的節點集合兩兩不交且並集為整棵子樹**，故總計恰為每個節點一次。\n\n這裡點名兩個前提。其一，輸入必須真的是樹——無環且每個節點只有一個父節點。若有環（例如題目給的是圖），遞迴不會終止；若某節點有兩個父節點（DAG），它會被訪問多次，上面的「不交」條件失效。其二，遞迴深度等於樹高，斜樹會退化成 $O(n)$ 深。\n\n**中序遍歷與 BST 的關係。** 若樹是二元搜尋樹，中序遍歷輸出遞增序列。證明同樣用結構歸納：由 BST 性質，左子樹所有值 < 根 < 右子樹所有值；由歸納假設，左子樹中序輸出遞增、右子樹中序輸出遞增；中序把三者依「左、根、右」串接，故整體遞增。這條性質是 98、230、99、二元搜尋樹一整節（2.9）的基礎，值得單獨記住。\n\n**前序與後序的資訊時機。** 進入節點時，你手上有的是「從根到此節點這條路徑」的資訊（自上而下累積），所以前序適合傳遞路徑和、深度、路徑上的最大值（2.2）。離開節點時，你手上有的是「兩棵子樹回傳的彙總」，所以後序適合求高度、子樹和、子樹節點數（2.3）。**選錯時機，需要的資訊根本還不存在**——這是所有樹 DFS 題最核心的判斷。\n\n**迭代中序的不變量。** 堆疊中自底向上存的是「當前節點的祖先鏈中，右子樹尚未處理的那些節點」。每次先把左鏈全部壓入，彈出時該節點的左子樹必然已處理完畢，故此時輸出它是正確的中序位置。\n\n## 逐步演算法\n\n遍歷題可拆成三個步驟，逐一配程式碼：\n\n:::example 步驟一：空節點直接返回\n**1. 想清楚 base case。** 走到空節點（`nullptr`）就回頭，這是遞迴一定會停下來的保證。\n\n```cpp\n// 步驟一：空節點直接返回\nif (!root) {\n    return;\n}\n```\n:::\n\n\n\n:::example 步驟二：以「左葉子之和」為例，遍歷時順手累加（第 404 題）\n**2. 決定處理自己的時機（前 / 中 / 後序）。** 依題目需要選順序：要照大小順序輸出二叉搜尋樹用中序；要先看父再看子用前序；要先彙整子樹資訊再算自己用後序。\n\n```cpp\n// 步驟二：以「左葉子之和」為例，遍歷時順手累加（第 404 題）\nvoid dfs(TreeNode* root, int& sum) {\n    if (!root) {\n        return;\n    }\n    if (root->left && !root->left->left && !root->left->right) {\n        sum += root->left->val;  // 左子節點且它是葉 -> 累加\n    }\n    dfs(root->left, sum);\n    dfs(root->right, sum);\n}\n```\n:::\n\n\n\n:::example 步驟三：呼叫端拿到結果\n**3. 從累積的狀態讀出答案。** 用一個外部變數（或回傳值）邊走邊更新，遍歷結束後它就是答案。\n\n```cpp\n// 步驟三：呼叫端拿到結果\nint sumOfLeftLeaves(TreeNode* root) {\n    int sum = 0;\n    dfs(root, sum);\n    return sum;\n}\n```\n:::\n\n\n\n邊界與複雜度：\n\n- 一定要先寫 `if (!root) return;`，否則對空指標取 `->left` 會崩潰。\n- 累積資訊用「傳參考」或「回傳值」都行，新手用 `int&` 傳參考最直觀。\n- 時間 `O(n)`（每節點一次）；遞迴會用到呼叫堆疊，空間 `O(樹高)`，最壞（退化成鏈）是 `O(n)`。\n\n## C++17 模板\n\n以下用同型延伸示範題 104. 二叉樹的最大深度 展示核心寫法。\n\n**完整題目**：給定二叉樹根節點，回傳從根到最深葉節點的節點數。\n\n:::example 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n**線索**：題目資料本身是樹，或答案可由子樹合併/祖先下傳；DFS 的回傳值與參數就是核心線索。\n\n```cpp\nclass Solution {\npublic:\n    // 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n    int maxDepth(TreeNode* root) {\n            if (!root) {\n                    return 0;\n            }\n            return 1 + max(maxDepth(root->left), maxDepth(root->right));\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- 後序 DFS 從子樹回傳資訊，父節點只負責合併左右答案；樹形 DP 也遵循這個資料流。\n- 遞迴函式回傳可被父節點合併的資訊，空節點返回值要和合併式相容。\n- 若需要全域答案，通常在回溯合併左右子樹時更新，而不是只看根節點。\n\n## 時間與空間複雜度\n\n三種遍歷都是 $O(n)$ 時間——每個節點常數次操作，由上面的歸納論證直接得出。\n\n空間 $O(h)$，$h$ 為樹高：遞迴版是呼叫堆疊，迭代版是顯式堆疊（存的正是祖先鏈）。平衡樹 $h = O(\\log n)$，斜樹 $h = n$，故最壞 $O(n)$。若輸出要收集所有節點值則另計 $O(n)$。\n\nMorris 遍歷可做到 $O(1)$ 空間，代價是暫時修改樹結構（利用葉節點的空右指標當回程線索），$O(n)$ 時間不變。\n\n## 常見錯誤與邊界條件\n\n- 空節點的返回值要與合併式相容。\n- 全域答案在遞迴中更新時注意初始化。\n- 一般樹 DFS 要跳過 parent。\n- 深樹可能遞迴溢位，必要時改迭代。\n\n## 常見變形\n\n- 直徑題回傳向下最大鏈，順手更新全域答案。\n- LCA 可用遞迴、倍增或 Tarjan 離線。\n- 換根 DP 需要先下後上兩次遍歷。\n\n## 與相似技巧的比較\n\n- BFS（2.13）逐層訪問，空間是 $O(w)$（最大層寬）而非 $O(h)$；求「最短層數」「層平均」時用 BFS，求「路徑」「子樹彙總」時用 DFS。\n- 前序遍歷的訪問順序與 BFS 都能重建結構嗎？不行——單靠前序無法唯一確定樹，需要搭配中序（見 2.10），或改用含空節點標記的序列化（297）。\n- 一般樹（3.1）的遍歷沒有「中序」的概念（子節點不只兩個），只剩「進入時處理」與「離開時處理」兩種時機。\n\n## 代表例題\n\n**題號**：94、144、145、404、671\n\n放在「遍歷二叉樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 94 | [二叉樹的中序遍歷](https://leetcode.cn/problems/binary-tree-inorder-traversal/) |  |\n| 144 | [二叉樹的前序遍歷](https://leetcode.cn/problems/binary-tree-preorder-traversal/) |  |\n| 145 | [二叉樹的後序遍歷](https://leetcode.cn/problems/binary-tree-postorder-traversal/) |  |\n| 404 | [左葉子之和](https://leetcode.cn/problems/sum-of-left-leaves/) |  |\n| 671 | [二叉樹中第二小的節點](https://leetcode.cn/problems/second-minimum-node-in-a-binary-tree/) |  |\n| 872 | [葉子相似的樹](https://leetcode.cn/problems/leaf-similar-trees/) | 1288 |\n| 1214 | [查詢兩棵二叉搜索樹之和](https://leetcode.cn/problems/two-sum-bsts/) | 1389 |\n| 1469 | [尋找所有的獨生節點](https://leetcode.cn/problems/find-all-the-lonely-nodes/) |  |\n| 2764 | [陣列是否表示某二叉樹的前序遍歷](https://leetcode.cn/problems/is-array-a-preorder-of-some-binary-tree/) |  |\n| 4046 | [LCP 44. 開幕式焰火](https://leetcode.cn/problems/sZ59z6/) |  |\n\n## 本節重點速查\n\n- 出口 `if (!node) return;`，然後左、右兩次遞迴；\n- 三種順序只差「處理自己」放哪一行。\n- **前序手上有根到此的路徑資訊，後序手上有子樹的彙總資訊**，選錯時機資訊就不存在。\n- 中序走 BST 得遞增序列。\n- 時間 $O(n)$、空間 $O(h)$。",
        },
        {
          id: 16,
          title: "2.2 自頂向下 DFS（先序遍歷）",
          summary:
            "## 這個技術解決什麼問題\n\n有一類樹題，答案取決於**從根走到當前節點這條路徑**：目前深度是多少（104、111）、路徑上累積的和（112、129）、路徑上出現過的最大值（1448）。這些資訊在「進入節點的當下」就已經確定，不需要等子樹回來。\n\n自頂向下 DFS 的做法是**把路徑資訊當作參數往下傳**。相對於「每次到葉節點再回頭沿父指標算一遍」（$O(nh)$），參數傳遞讓每個節點只做常數工作，總計 $O(n)$。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就往「自頂向下 DFS」想：\n\n- **答案取決於「從根到目前節點」這一路的累積資訊。** 例如深度（第 104／111 題）、路徑數字（第 129 題）、路徑和是否達標（第 112 題）。\n- **你會想「在進入子節點前，先把某個值算好交給它」。** 這就是自頂向下、用參數往下帶：\n\n:::example 嗅到味道的瞬間：子節點需要知道「父輩一路帶下來的東西」\n```cpp\n// 嗅到味道的瞬間：子節點需要知道「父輩一路帶下來的東西」\n// -> 把那個東西當參數，先序時更新後再遞迴下去\n```\n:::\n\n\n\n- **常在葉子節點結算。** 「到葉子時……」這種敘述，多半是自頂向下累積到底再判斷。\n- **暴力對每個節點都從根重算一次路徑是 `O(n^2)`；** 自頂向下把累積值順手帶下去，一趟 `O(n)` 解決。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我需要往子節點「帶下去」的資訊是什麼？（深度？路徑和？）\n2. 我有沒有在「遞迴左右之前」就先更新好這個資訊（先序）？\n3. 我在哪裡結算答案——是不是在葉子節點？\n\n## 核心想法與直覺\n\n本節是「自頂向下 DFS（先序遍歷）」。DFS 是「深度優先搜尋（depth-first search）」，意思是沿著一條路一直往深處走到底，再回頭走旁邊的路。「自頂向下」指的是資訊的流向：**從根節點往下傳，父節點先把它沿途累積的資訊（例如目前深度、從根到這裡的路徑和）算好，當作參數交給子節點。**\n\n這正好對應「先序」：先處理自己（更新要往下傳的資訊），再遞迴左右子樹。和上一節單純遍歷的差別在於：這裡我們會多帶一個「沿路帶下去的參數」。\n\n:::example 第 104 題：自頂向下，把「目前深度」當參數往下傳\n舉第 104 題「二叉樹的最大深度」為例。每往下走一層，深度就 +1，把這個深度當參數傳給子節點；走到葉子時，目前的深度就是一條從根到底的長度，拿來更新答案。\n\n```cpp\n// 第 104 題：自頂向下，把「目前深度」當參數往下傳\nvoid dfs(TreeNode* root, int depth, int& ans) {\n    if (!root) {\n        return;\n    }\n    ++depth;                      // 自己這層，深度 +1（先處理自己）\n    ans = max(ans, depth);        // 用目前累積資訊更新答案\n    dfs(root->left, depth, ans);  // 把累積好的 depth 交給子節點\n    dfs(root->right, depth, ans);\n}\n```\n:::\n\n\n\n:::example 第 129 題：把根到目前節點組成的數字往下傳\n再看第 129 題「求根到葉節點數字之和」：從根往下走，把一路上的數字串成一個數（每下一層就 `cur = cur * 10 + 節點值`），到葉子時這個數就完整了，加進答案。這個「沿路累積、傳給子節點」就是自頂向下的精髓。\n\n```cpp\n// 第 129 題：把根到目前節點組成的數字往下傳\nvoid dfs(TreeNode* root, int cur, int& sum) {\n    if (!root) {\n        return;\n    }\n    cur = cur * 10 + root->val;         // 先把自己接到數字尾端\n    if (!root->left && !root->right) {  // 到葉子，一條完整數字\n        sum += cur;\n    }\n    dfs(root->left, cur, sum);\n    dfs(root->right, cur, sum);\n}\n```\n:::\n\n\n\n每個節點仍只走訪一次，是 `O(n)`。\n\n## 狀態／資料結構定義\n\n- `dfs(node, acc)`：`acc` 是「從根到 `node` 的父節點為止」累積的資訊，或「到 `node` 為止」——**兩種語意都可行，但必須從頭到尾一致**，混用是本節錯誤的主要來源。本文採後者：進入函式後第一件事就是把 `node` 併入 `acc`。\n- `acc` 的型別隨題目而定：深度是 `int`、路徑和是 `long long`（避免溢位）、路徑數字是 `acc * 10 + node->val`。\n- 答案通常寫進外部變數 `ans`，或由回傳值層層向上傳。\n- 葉節點的判定是 `!node->left && !node->right`，**不是 `!node`**。這兩者的差別在 111 題（最小深度）會直接造成錯誤答案。\n\n## 不變量或正確性證明\n\n不變量：**每次呼叫 `dfs(node, acc)` 時，`acc` 恰等於根到 `node` 的父節點這條路徑所定義的累積量。**\n\n- 初始化：`dfs(root, e)`，其中 `e` 是空路徑的單位元（深度為 0、和為 0、數字為 0），成立。\n- 保持：函式內計算 `acc' = combine(acc, node)`，然後呼叫 `dfs(node->left, acc')` 與 `dfs(node->right, acc')`。對子節點而言，根到其父（即 `node`）的累積量正是 `acc'`，故不變量維持。\n- 結論：對任一節點 $v$，`dfs(v, ·)` 被呼叫時參數必為根到 $v$ 之父的累積量；特別地，葉節點處拿到的就是完整的根到葉路徑資訊。\n\n這條不變量成立的前提是 **`combine` 只依賴路徑本身，不依賴子樹**。深度、路徑和、路徑最大值都滿足；但「子樹節點數」「子樹高度」不滿足，那些必須用後序（2.3）。判斷用哪一種的依據就是這裡：**所需資訊是路徑函數還是子樹函數。**\n\n還有一個實作層面的正確性條件：若 `acc` 是可變的共享狀態（例如傳參考的 `vector` 路徑），必須在遞迴返回後**復原**，否則兄弟子樹會看到彼此的殘留——這就是回溯（2.7）。若 `acc` 按值傳遞（`int`、`long long`），各分支各有一份副本，天然隔離，不需要復原。這個區別決定了要不要寫 `path.pop_back()`。\n\n**最小深度（111）為何必須特判單邊。** 若寫成 `1 + min(dfs(left), dfs(right))`，對只有左子的節點會取到 `dfs(right) = 0`，得出深度 1，但該節點不是葉節點，不構成合法的根到葉路徑。正確做法是：只有一邊為空時取另一邊，兩邊皆空時（真葉節點）回傳 1。最大深度（104）沒有這個問題，因為空子樹回傳 0 在取 `max` 時自然被忽略。**這是「葉節點」與「空節點」語意混淆的典型後果。**\n\n## 逐步演算法\n\n自頂向下 DFS 可拆成三個步驟，逐一配程式碼：\n\n:::example 步驟一：以「路徑總和」第 112 題為例，往下傳「還差多少」\n**1. 設計「要往下傳的參數」。** 想清楚子節點需要知道父輩累積了什麼：深度、路徑和、目前組成的數字、剩餘目標值等。\n\n```cpp\n// 步驟一：以「路徑總和」第 112 題為例，往下傳「還差多少」\nbool dfs(TreeNode* root, int remain) {  // remain = 目標 - 已走過的和\n    if (!root) {\n        return false;\n    }\n    remain -= root->val;  // 先扣掉自己（先序：先處理自己）\n                                                // ...\n}\n```\n:::\n\n\n\n:::example 步驟二：到葉子時檢查是否剛好湊到目標\n**2. 在「處理自己」時更新參數並判斷答案。** 這一步要放在遞迴左右之前（這就是先序）。到葉子節點時通常是檢查或結算的時機。\n\n```cpp\n// 步驟二：到葉子時檢查是否剛好湊到目標\nif (!root->left && !root->right) {\n    return remain == 0;  // 葉子且剛好扣到 0 -> 找到一條路徑\n}\n```\n:::\n\n\n\n:::example 步驟三：把新的 remain 交給左右子樹\n**3. 把更新後的參數遞迴給左右子節點。** 左右只要有一邊成立（或依題意彙整）即可。\n\n```cpp\n// 步驟三：把新的 remain 交給左右子樹\nreturn dfs(root->left, remain) || dfs(root->right, remain);\n```\n:::\n\n\n\n邊界與複雜度：\n\n- 「先處理自己再往下傳」是先序的關鍵；順序顛倒會把尚未更新的值傳下去而出錯。\n- 參數用「傳值」傳下去（如 `int depth`），這樣每條路徑各有自己的副本，回溯時不互相污染。\n- 像第 199 題「右檢視」可在進入每層時，把「該層第一個被訪問到的節點」記下，配合「先右後左」的順序即得每層最右節點。\n- 時間 `O(n)`，空間 `O(樹高)`。\n\n## C++17 模板\n\n以下用本節代表題 104. 二叉樹的最大深度 展示核心寫法。\n\n**完整題目**：給定二叉樹根節點，回傳從根到最深葉節點的節點數。\n\n:::example 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n**線索**：題目資料本身是樹，或答案可由子樹合併/祖先下傳；DFS 的回傳值與參數就是核心線索。\n\n```cpp\nclass Solution {\npublic:\n    // 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n    int maxDepth(TreeNode* root) {\n            if (!root) {\n                    return 0;\n            }\n            return 1 + max(maxDepth(root->left), maxDepth(root->right));\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- 後序 DFS 從子樹回傳資訊，父節點只負責合併左右答案；樹形 DP 也遵循這個資料流。\n- 遞迴函式回傳可被父節點合併的資訊，空節點返回值要和合併式相容。\n- 若需要全域答案，通常在回溯合併左右子樹時更新，而不是只看根節點。\n\n## 時間與空間複雜度\n\n每個節點被訪問一次，每次做 $O(1)$ 的 `combine`，故 $O(n)$ 時間。若 `acc` 是路徑陣列且每層複製一份，則退化成 $O(nh)$ 時間與空間——這是把回溯寫成值傳遞時的常見效能陷阱。\n\n空間 $O(h)$ 的遞迴堆疊；`acc` 為純量時不增加額外空間。$h$ 最壞為 $n$（斜樹），此時遞迴可能爆堆疊，需改迭代加顯式堆疊。\n\n## 常見錯誤與邊界條件\n\n- 空節點的返回值要與合併式相容。\n- 全域答案在遞迴中更新時注意初始化。\n- 一般樹 DFS 要跳過 parent。\n- 深樹可能遞迴溢位，必要時改迭代。\n\n## 常見變形\n\n- 直徑題回傳向下最大鏈，順手更新全域答案。\n- LCA 可用遞迴、倍增或 Tarjan 離線。\n- 換根 DP 需要先下後上兩次遍歷。\n\n## 與相似技巧的比較\n\n- 自底向上（2.3）用回傳值彙總子樹資訊，適合高度、子樹和、平衡判定；本節用參數往下傳路徑資訊。**看題目要的是「從根到我」還是「我以下」**，就知道選哪個。\n- 兩者可以同時使用（2.5「有遞有歸」）：往下傳路徑資訊，往上收子樹資訊，例如 1080 題需要先知道路徑和才能決定是否刪點，刪點的效果又要往上傳。\n- 回溯（2.7）是本節的特例：`acc` 是可變的路徑容器，需要顯式復原。\n- BFS（2.13）也能求深度且對斜樹不會爆堆疊，但拿不到「路徑」本身。\n\n## 代表例題\n\n**題號**：104、111、112、129、199\n\n放在「自頂向下 DFS（先序遍歷）」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 104 | [二叉樹的最大深度](https://leetcode.cn/problems/maximum-depth-of-binary-tree/) |  |\n| 111 | [二叉樹的最小深度](https://leetcode.cn/problems/minimum-depth-of-binary-tree/) |  |\n| 112 | [路徑總和](https://leetcode.cn/problems/path-sum/) |  |\n| 129 | [求根節點到葉節點數字之和](https://leetcode.cn/problems/sum-root-to-leaf-numbers/) |  |\n| 199 | [二叉樹的右檢視](https://leetcode.cn/problems/binary-tree-right-side-view/) |  |\n| 298 | [二叉樹最長連續序列](https://leetcode.cn/problems/binary-tree-longest-consecutive-sequence/) |  |\n| 545 | [二叉樹的邊界](https://leetcode.cn/problems/boundary-of-binary-tree/) |  |\n| 623 | [在二叉樹中增加一行](https://leetcode.cn/problems/add-one-row-to-tree/) |  |\n| 971 | [翻轉二叉樹以匹配先序遍歷](https://leetcode.cn/problems/flip-binary-tree-to-match-preorder-traversal/) | 1787 |\n| 988 | [從葉結點開始的最小字串](https://leetcode.cn/problems/smallest-string-starting-from-leaf/) | 1429 |\n| 1022 | [從根到葉的二進位制數之和](https://leetcode.cn/problems/sum-of-root-to-leaf-binary-numbers/) | 1462 |\n| 1026 | [節點與其祖先之間的最大差值](https://leetcode.cn/problems/maximum-difference-between-node-and-ancestor/) | 1446 |\n| 1315 | [祖父節點值為偶數的節點和](https://leetcode.cn/problems/sum-of-nodes-with-even-valued-grandparent/) | 1427 |\n| 1372 | [二叉樹中的最長交錯路徑](https://leetcode.cn/problems/longest-zigzag-path-in-a-binary-tree/) | 1713 |\n| 1430 | [判斷給定的序列是否是二叉樹從根到葉的路徑](https://leetcode.cn/problems/check-if-a-string-is-a-valid-sequence-from-root-to-leaves-path-in-a-binary-tree/) |  |\n| 1448 | [統計二叉樹中好節點的數目](https://leetcode.cn/problems/count-good-nodes-in-binary-tree/) | 1360 |\n| 2689 | [從 Rope 樹中提取第 K 個字元](https://leetcode.cn/problems/extract-kth-character-from-the-rope-tree/) |  |\n\n## 本節重點速查\n\n- 把根到當前節點的資訊當**參數往下傳**，不變量是「`acc` 等於根到父的累積量」。\n- 適用條件：所需資訊是路徑的函數。\n- 純量按值傳遞不需復原，容器傳參考就必須回溯。\n- 葉節點是 `!left && !right`，不是 `!node`——111 題靠這個區別。",
        },
        {
          id: 17,
          title: "2.3 自底向上 DFS（後序遍歷）",
          summary:
            "## 這個技術解決什麼問題\n\n另一類樹題，答案取決於**以當前節點為根的整棵子樹**：這棵子樹有多高（104）、有幾個節點（222）、是否平衡（110）、子樹和是多少。這些資訊在進入節點時還不存在，必須等兩棵子樹都算完才能合成。\n\n若對每個節點各跑一次子樹掃描，成本是 $O(nh)$（斜樹達 $O(n^2)$）。自底向上 DFS 讓**每個節點只算一次、結果沿著遞迴回傳往上傳**，總計 $O(n)$。這是「一次遍歷解決全部節點的子樹查詢」的標準手法，也是樹形 DP（2.12）的雛形。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就往「自底向上 DFS」想：\n\n- **要判斷或計算「整棵子樹」的某種性質**：高度、是否平衡（第 110 題）、兩樹是否相同（第 100 題）/ 對稱（第 101 題）、子樹和等。\n- **自己的答案需要先知道左右子樹的答案才能算。** 這就是由下往上、靠回傳值合成：\n\n:::example 嗅到味道的瞬間：算自己之前，得先有左右子樹的結果\n```cpp\n// 嗅到味道的瞬間：算自己之前，得先有左右子樹的結果\n// -> 後序：先遞迴左右，拿回傳值再合成自己\n```\n:::\n\n\n\n- **可以一邊遞迴一邊「短路」傳壞消息**（如第 110 題用 -1 表示已不平衡），避免重複計算。\n- **暴力對每個節點都重新往下掃一遍子樹是 `O(n^2)`；** 自底向上讓每節點只回傳一次資訊，一趟 `O(n)` 完成。\n\n確認自己有沒有套對，問三個問題：\n\n1. 每個節點該「回傳」什麼，才夠父節點算出自己的答案？\n2. 空樹（base case）應該回傳什麼中性值，才讓合併公式對葉子也成立？\n3. 我是不是先遞迴完左右，最後才合成自己（後序）？\n\n## 核心想法與直覺\n\n**延伸講解**：[如何靈活運用遞迴？【基礎演算法精講 10】](https://www.bilibili.com/video/BV18M411z7bb/)\n\n本節是「自底向上 DFS（後序遍歷）」。和上一節相反，這裡資訊的流向是**從下往上彙整：先把左、右子樹的答案各自算出來，再用這兩個結果合成「自己這棵子樹」的答案，往上交給父節點。** 這正對應「後序」：先遞迴左、遞迴右，最後才處理自己。\n\n關鍵差別在於這次我們善用「回傳值」。每次遞迴回傳「以這個節點為根的子樹」的某個資訊（高度、是否平衡、是否相同……），父節點拿到左右兩份回傳值後做合併。\n\n:::example 第 104 題（自底向上）：先拿左右深度，再合成自己的\n以第 104 題「最大深度」的自底向上寫法為例：一棵子樹的深度 = 左右子樹深度的較大者 + 1。葉子以下是空樹，深度 0。\n\n```cpp\n// 第 104 題（自底向上）：先拿左右深度，再合成自己的\nint maxDepth(TreeNode* root) {\n    if (!root) {\n        return 0;  // 空樹深度 0（base case）\n    }\n    int l = maxDepth(root->left);   // 先算左子樹答案\n    int r = maxDepth(root->right);  // 再算右子樹答案\n    return max(l, r) + 1;           // 合成：較深的一邊 +1\n}\n```\n:::\n\n\n\n:::example 第 110 題：回傳高度；用 -1 代表「已偵測到不平衡」\n很多「整棵樹的某性質」都能這樣由下而上判斷。第 110 題「平衡二叉樹」要求每個節點的左右高度差 ≤ 1：我們讓遞迴回傳高度，順便檢查；一旦發現不平衡就回傳一個哨兵值（例如 -1）一路往上傳，代表「整棵已經不平衡」。\n\n```cpp\n// 第 110 題：回傳高度；用 -1 代表「已偵測到不平衡」\nint height(TreeNode* root) {\n    if (!root) {\n        return 0;\n    }\n    int l = height(root->left);\n    if (l == -1) {\n        return -1;\n    }\n    int r = height(root->right);\n    if (r == -1) {\n        return -1;\n    }\n    if (abs(l - r) > 1) {\n        return -1;  // 自己這裡就不平衡\n    }\n    return max(l, r) + 1;  // 否則回傳正常高度\n}\n```\n:::\n\n\n\n這種「一次走訪就回傳兩種意義（高度 + 是否合格）」的技巧，比每個節點都重算高度的暴力 `O(n^2)` 快，整體是 `O(n)`。\n\n## 狀態／資料結構定義\n\n- `dfs(node)` 的**回傳值**就是狀態：以 `node` 為根的子樹的彙總量。定義必須精確到能讓合併規則寫得出來。\n- 空子樹的回傳值是**單位元**，且必須讓合併規則對它成立：高度取 0、節點數取 0、子樹和取 0、最小值取 `INT_MAX`。單位元選錯是本節最常見的錯誤。\n- 合併規則 `f(node) = combine(f(left), f(right), node)`，只用兩個子樹的彙總值與節點自身。\n- 若彙總量不只一個（例如同時要高度與是否平衡），回傳 `pair`/`struct`，或用哨兵值編碼（110 題常用 $-1$ 表示「已失衡」）。\n\n## 不變量或正確性證明\n\n命題（結構歸納）：**對每個節點 $v$，`dfs(v)` 回傳的值等於以 $v$ 為根之子樹的目標彙總量。**\n\n- 基底：$v$ 為空，回傳單位元。這要求單位元的定義與「空子樹的彙總量」一致——高度 0、節點數 0 都對；但若求「子樹最小值」而回傳 0，遇到全正數的樹就會錯，必須用 `INT_MAX`。基底不是形式步驟，它是實際的正確性條件。\n- 歸納：假設 `dfs(left)`、`dfs(right)` 各自正確。由於左子樹、右子樹、$v$ 三者的節點集合構成整棵子樹的一個劃分，只要 `combine` 忠實反映「彙總量如何由這個劃分組合而成」，`dfs(v)` 即正確。例如高度 $= 1 + \\max(h_L, h_R)$，因為最長的根到葉路徑必經左或右其中一棵；節點數 $= 1 + n_L + n_R$，因為劃分不重不漏。\n- 結論：對根成立，得證。\n\n這條論證要求彙總量**可由子樹的彙總量組合而成**（即具有最優子結構／可分解性）。若某個量不具此性質——例如「子樹中距離為 3 的節點對數」無法只由兩個子樹的同類數字算出——就得擴充狀態（多回傳幾個量），這正是樹形 DP 的思路。判斷「狀態夠不夠」的標準就是：**歸納步驟能不能只用子樹的回傳值寫出來。**\n\n**110 平衡二元樹的提前退出。** 定義 `dfs` 回傳高度，但一旦發現失衡就回傳 $-1$ 並讓 $-1$ 向上傳播。正確性在於 $-1$ 不可能是合法高度（高度非負），故它是無歧義的哨兵；且任何節點只要有一棵子樹回傳 $-1$ 就立刻回傳 $-1$，不再計算，於是「失衡」這個事實一路傳到根。這使複雜度從樸素的「對每個節點各算一次高度」$O(n^2)$ 降到 $O(n)$——**關鍵是把「高度」與「是否平衡」合併進同一次遍歷，而不是分兩次算。**\n\n**100 相同樹、101 對稱樹**同屬此類：狀態是「這兩棵子樹是否相同」，合併規則是「根值相等且對應的兩對子樹都相同」。對稱樹要注意配對方式是 $(L.left, R.right)$ 與 $(L.right, R.left)$，寫成同側配對就變成判斷相同而非對稱。\n\n## 逐步演算法\n\n自底向上 DFS 可拆成三個步驟，逐一配程式碼：\n\n:::example 步驟一：base case 通常是空樹回傳「中性值」\n**1. 想清楚「每個節點要回傳什麼」。** 這是整個方法的核心：回傳的資訊必須足以讓父節點合成自己的答案（高度、子樹和、節點數、是否相同……）。\n\n```cpp\n// 步驟一：base case 通常是空樹回傳「中性值」\nif (!root) {\n    return 0;  // 高度 0 / 和 0 / 計數 0 等\n}\n```\n:::\n\n\n\n:::example 步驟二：先取得左右子樹的回傳值\n**2. 先遞迴左右，拿到兩份子答案。** 一定先把子問題解完（後序：左、右在前）。\n\n```cpp\n// 步驟二：先取得左右子樹的回傳值\nint l = dfs(root->left);\nint r = dfs(root->right);\n```\n:::\n\n\n\n:::example 步驟三：以「相同的樹」第 100 題為例，兩棵同時往下比\n**3. 用左右結果合成自己的答案並回傳。** 合併規則由題意決定；若要同時維護一個全域答案（如最大路徑和），就在這一步用 `l + r + 自己` 更新它。\n\n```cpp\n// 步驟三：以「相同的樹」第 100 題為例，兩棵同時往下比\nbool isSame(TreeNode* a, TreeNode* b) {\n    if (!a && !b) {\n        return true;  // 同時為空 -> 相同\n    }\n    if (!a || !b || a->val != b->val) {\n        return false;\n    }\n    return isSame(a->left, b->left)  // 左右子樹都要相同\n                  && isSame(a->right, b->right);\n}\n```\n:::\n\n\n\n邊界與複雜度：\n\n- base case 要回傳「中性值」（高度 0、和 0），讓合併公式對葉子也成立。\n- 若要邊算邊更新一個全域答案（如最長路徑），通常回傳「單邊鏈的長度」，但更新答案時用「左 + 右」。第 101 題「對稱二叉樹」則是把左右子樹鏡像比對。\n- 時間 `O(n)`，每節點處理一次；空間 `O(樹高)`。\n\n## C++17 模板\n\n```cpp\n// 判斷平衡二元樹（LeetCode 110）：回傳高度，失衡則回傳 -1 並向上傳播。\nint height(TreeNode* node) {\n  if (!node) { return 0; }             // 單位元：空子樹高度為 0\n\n  int l = height(node->left);\n  if (l == -1) { return -1; }          // 已失衡，提前退出\n  int r = height(node->right);\n  if (r == -1) { return -1; }\n\n  if (std::abs(l - r) > 1) { return -1; }\n  return 1 + std::max(l, r);           // 合併：由兩棵子樹的高度組合\n}\n\nbool isBalanced(TreeNode* root) { return height(root) != -1; }\n```\n\n需要回傳多個量時改用 `struct`：\n\n```cpp\nstruct Info { int size; long long sum; int mx; };\n\nInfo dfs(TreeNode* node) {\n  if (!node) { return {0, 0, INT_MIN}; }        // 單位元要讓 combine 成立\n  Info l = dfs(node->left), r = dfs(node->right);\n  return {1 + l.size + r.size,\n          node->val + l.sum + r.sum,\n          std::max({node->val, l.mx, r.mx})};\n}\n```\n\n## 時間與空間複雜度\n\n每個節點恰呼叫一次 `dfs`，每次做 $O(1)$ 的合併，故 $O(n)$ 時間。這正是「一次遍歷取代 $n$ 次子樹掃描」的收穫：樸素做法 $O(nh)$，最壞 $O(n^2)$。\n\n空間 $O(h)$ 遞迴堆疊。回傳 `struct` 不改變漸近空間（每層一份，仍是 $O(h)$）。斜樹時 $h = n$，深度上萬的測資需改迭代版（後序迭代較繁瑣，通常用顯式堆疊記錄「是否已處理過右子」）。\n\n## 常見錯誤與邊界條件\n\n- 空子樹回傳的單位元選錯（求最小值回傳 0、求最大值回傳 0 而樹中有負數）。\n- 110 題對每個節點各算一次高度而超時。\n- 101 對稱樹配對方向寫成同側。\n- 把需要路徑資訊的題目硬用後序寫（資訊在後序時已遺失）。\n- `sum` 用 `int` 溢位。\n- 斜樹爆堆疊。\n- 忘記 `dfs(node->right)` 而只算左邊。\n\n## 與相似技巧的比較\n\n- 自頂向下（2.2）傳路徑資訊、自底向上傳子樹資訊。**問「我以下如何」用後序，問「從根到我如何」用前序**，這是選擇的唯一依據。\n- 兩者結合就是 2.5「有遞有歸」，以及 2.6 直徑那類「在每個節點處用左右子樹資訊更新全域答案」的模式。\n- 樹形 DP（2.12）是本節的推廣：當單一彙總量不足以支撐歸納步驟時，把狀態擴充成多維（例如「選這個節點」與「不選」兩種情形各一個值）。\n- 一般樹的自底向上（3.3）完全同構，只是把「左右兩個子樹」換成「對所有子節點迴圈累積」。\n\n## 代表例題\n\n**題號**：67、100、101、104、110\n\n放在「自底向上 DFS（後序遍歷）」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 67 | [LCP 67. 裝飾樹](https://leetcode.cn/problems/KnLfVT/) |  |\n| 100 | [相同的樹](https://leetcode.cn/problems/same-tree/) |  |\n| 101 | [對稱二叉樹](https://leetcode.cn/problems/symmetric-tree/) |  |\n| 104 | [二叉樹的最大深度](https://leetcode.cn/problems/maximum-depth-of-binary-tree/) |  |\n| 110 | [平衡二叉樹](https://leetcode.cn/problems/balanced-binary-tree/) |  |\n| 111 | [二叉樹的最小深度](https://leetcode.cn/problems/minimum-depth-of-binary-tree/) |  |\n| 156 | [上下翻轉二叉樹](https://leetcode.cn/problems/binary-tree-upside-down/) |  |\n| 226 | [翻轉二叉樹](https://leetcode.cn/problems/invert-binary-tree/) |  |\n| 250 | [統計同值子樹](https://leetcode.cn/problems/count-univalue-subtrees/) |  |\n| 298 | [二叉樹最長連續序列](https://leetcode.cn/problems/binary-tree-longest-consecutive-sequence/) |  |\n| 333 | [最大二叉搜尋子樹](https://leetcode.cn/problems/largest-bst-subtree/) |  |\n| 366 | [尋找二叉樹的葉子節點](https://leetcode.cn/problems/find-leaves-of-binary-tree/) |  |\n| 508 | [出現次數最多的子樹元素和](https://leetcode.cn/problems/most-frequent-subtree-sum/) |  |\n| 563 | [二叉樹的坡度](https://leetcode.cn/problems/binary-tree-tilt/) |  |\n| 572 | [另一棵樹的子樹](https://leetcode.cn/problems/subtree-of-another-tree/) |  |\n| 606 | [根據二叉樹建立字串](https://leetcode.cn/problems/construct-string-from-binary-tree/) |  |\n| 617 | [合併二叉樹](https://leetcode.cn/problems/merge-two-binary-trees/) |  |\n| 663 | [均勻樹劃分](https://leetcode.cn/problems/equal-tree-partition/) |  |\n| 951 | [翻轉等價二叉樹](https://leetcode.cn/problems/flip-equivalent-binary-trees/) | 1477 |\n| 965 | [單值二叉樹](https://leetcode.cn/problems/univalued-binary-tree/) | 1178 |\n| 1026 | [節點與其祖先之間的最大差值](https://leetcode.cn/problems/maximum-difference-between-node-and-ancestor/) | 1446 |\n| 1120 | [子樹的最大平均值](https://leetcode.cn/problems/maximum-average-subtree/) | 1362 |\n| 1145 | [二叉樹著色遊戲](https://leetcode.cn/problems/binary-tree-coloring-game/) | 1741 |\n| 1339 | [分裂二叉樹的最大乘積](https://leetcode.cn/problems/maximum-product-of-splitted-binary-tree/) | 1675 |\n| 1372 | [二叉樹中的最長交錯路徑](https://leetcode.cn/problems/longest-zigzag-path-in-a-binary-tree/) | 1713 |\n| 1379 | [找出克隆二叉樹中的相同節點](https://leetcode.cn/problems/find-a-corresponding-node-of-a-binary-tree-in-a-clone-of-that-tree/) |  |\n| 1530 | [好葉子節點對的數量](https://leetcode.cn/problems/number-of-good-leaf-nodes-pairs/) | 1746 |\n| 1612 | [檢查兩棵二叉表示式樹是否等價](https://leetcode.cn/problems/check-if-two-expression-trees-are-equivalent/) |  |\n| 1973 | [值等於子節點值之和的節點數量](https://leetcode.cn/problems/count-nodes-equal-to-sum-of-descendants/) |  |\n| 2265 | [統計值等於子樹平均值的節點數](https://leetcode.cn/problems/count-nodes-equal-to-average-of-subtree/) | 1473 |\n| 2331 | [計算布林二叉樹的值](https://leetcode.cn/problems/evaluate-boolean-binary-tree/) | 1304 |\n| 2792 | [計算足夠大的節點數](https://leetcode.cn/problems/count-nodes-that-are-great-enough/) |  |\n| 3319 | [第 K 大的完美二叉子樹的大小](https://leetcode.cn/problems/k-th-largest-perfect-subtree-size-in-binary-tree/) | 1603 |\n\n## 本節重點速查\n\n- 用**回傳值**把子樹彙總往上傳；\n- 空子樹回傳單位元，且單位元必須讓合併規則成立。\n- 正確性是結構歸納，關鍵條件是「彙總量可由子樹彙總量組合而成」；\n- 組合不出來就擴充狀態。\n- 時間 $O(n)$、空間 $O(h)$。\n",
        },
        {
          id: 18,
          title: "2.4 自底向上 DFS：刪點",
          summary:
            "## 這個技術解決什麼問題\n\n「刪掉所有不含 1 的子樹」（814）、「刪掉所有不含指定目標的分支」（1325 反覆刪葉），這類題目要**修改樹的結構**而非只計算數值。\n\n難點在於刪除的判定有連鎖效應：刪掉一片葉子後，它的父節點可能變成新的葉子而也該被刪。若自頂向下判斷，你在看父節點時還不知道子樹會不會被清空，於是只能反覆掃描直到不再變化，成本 $O(nh)$。\n\n自底向上剪枝的槓桿是：**先遞迴處理子樹，回來時子樹已經是最終形態**，此時判斷自己去留所依據的資訊就是完整且最終的，一趟 $O(n)$ 解決連鎖。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就往「自底向上 DFS 刪點」想：\n\n- **某個節點刪不刪，取決於它整棵子樹的狀態**（子樹有沒有 1、是不是全空、葉子值是多少），而不是只看節點自己。\n- **刪除會往上連鎖**：刪了葉子，父親可能變新葉子也要刪——例如 1325。普通「掃一遍刪葉子」做不到連鎖，得反覆掃很多次，很慢。\n- **題目要回傳「整理後的樹」**：返回值是樹根或森林（如 1110 刪點成林，刪掉某些點後把斷開的子樹收集成多棵樹）。\n- 暴力做法常是「每刪一個就重掃整棵樹看誰又該刪」，`O(n^2)` 起跳；自底向上一次 DFS 就 `O(n)` 解決。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的 DFS「回傳值」代表什麼？（通常是整理後的子樹根，`nullptr` 表示刪光）\n2. 我有沒有「先遞迴左右、接回結果」，再判斷自己？順序對嗎？\n3. 「刪一個節點」這件事，是不是就等於「讓父親把指標接成 `nullptr` 或新根」？\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n本節要解決的場景是：給你一棵二元樹，你想根據「整棵子樹的情況」來決定要不要刪掉某個節點。例如題目 814「二叉樹剪枝」，要把所有「裡面完全沒有 1」的子樹整片砍掉；或 1325「刪除給定值的葉子節點」，刪掉一個葉子之後它的父親可能也變成葉子、也要被刪。\n\n關鍵難點是：一個節點該不該刪，往往**取決於它底下的小孩處理完之後的結果**，而不是只看它自己。葉子是否被刪 → 影響父親是否變葉子 → 再影響祖父……這是一條「由下往上傳遞」的連鎖。\n\n:::example 自底向上的骨架：先處理小孩，再決定自己\n這正是「自底向上 DFS（深度優先搜尋）」的拿手好戲。所謂自底向上，就是**先把左右子樹遞迴處理完、拿到它們回傳的結果，再回頭處理自己**。每個節點對外回傳一個值，代表「我這棵子樹整理後變成什麼樣」（通常是整理後的子樹根，沒了就回 `nullptr`）。\n\n```cpp\n// 自底向上的骨架：先處理小孩，再決定自己\nTreeNode* dfs(TreeNode* node) {\n    if (!node) {\n        return nullptr;  // 空樹：沒東西可整理\n    }\n    node->left = dfs(node->left);    // 先把左子樹整理好，接回來\n    node->right = dfs(node->right);  // 先把右子樹整理好，接回來\n    // 到這裡，左右小孩都「已經是最終結果」了，才輪到判斷自己\n    return node;\n}\n```\n:::\n\n\n\n:::example 刪一個節點 = 父親把指向它的指標改掉\n「刪點」用這個骨架特別自然：刪掉一個節點，只要讓它的父親把對應指標接成 `nullptr`（或接成「孩子整理後的新根」）即可。因為小孩先處理完，父親拿到的永遠是最新狀態，連鎖刪除就自動完成了。\n\n```cpp\n// 刪一個節點 = 父親把指向它的指標改掉\nnode->left = dfs(node->left);  // 若 dfs 回傳 nullptr，等於把左孩子刪了\n```\n:::\n\n\n\n整棵樹每個節點只被走訪一次，時間複雜度 `O(n)`，`n` 是節點數。\n\n## 狀態／資料結構定義\n\n- `dfs(node)` **回傳修剪後的子樹根**，可能是 `nullptr`（整棵被刪）也可能是 `node` 自己。這個「回傳新根」的介面是關鍵：它讓父節點用 `node->left = dfs(node->left);` 一行就完成「接上修剪結果」，不必區分「有刪」與「沒刪」兩種情況。\n- 判定順序固定：先遞迴左右並**把回傳值接回去**，再判斷自己。\n- 「是否為葉」在判定時必須用**修剪後**的狀態：`!node->left && !node->right`，此時兩者已是遞迴回傳的結果。\n\n1325 題（刪除值為 target 的葉節點）的判定條件是「修剪後成為葉節點且值等於 target」；814 題是「修剪後兩子皆空且自身值為 0」。\n\n## 不變量或正確性證明\n\n命題（結構歸納）：**`dfs(v)` 回傳的子樹，等於對以 $v$ 為根的原子樹反覆套用刪除規則直到穩定所得的結果。**\n\n- 基底：$v$ 為空，回傳空，平凡成立。\n- 歸納：假設 `dfs(v->left)`、`dfs(v->right)` 回傳的分別是左右子樹的**穩定形態**。把它們接回 $v$ 後，以 $v$ 為根的樹中除了 $v$ 自己，其餘部分都已穩定（不存在任何還該被刪的節點）。因此只需再判斷 $v$ 一次：若 $v$ 滿足刪除條件則回傳 `nullptr`，否則回傳 $v$。無論哪種情形，結果都已穩定。\n\n論證的核心是這一句：**修剪只會讓節點消失，不會產生新節點；而某節點是否該刪只依賴其子樹的最終狀態。** 因此後序處理過的部分不會因為後續操作而重新變得「該刪」——單調性保證了一趟就到不動點，不需要迭代。\n\n反過來看為什麼自頂向下不行：在 $v$ 處判斷時子樹尚未修剪，「$v$ 是否為葉」的答案可能與最終狀態不同。814 題若先判 $v$ 再遞迴，一棵值全為 0 的鏈只會刪掉最底層一個節點，需再掃一趟才刪下一層，共 $h$ 趟。\n\n**這個論證依賴刪除規則的「向上單調」性質**：刪掉子節點只可能讓父節點更接近被刪的條件，不會讓已經確定保留的節點變回該刪。若題目的規則是「子樹被刪則父節點必須保留」這種非單調規則，一趟後序就不成立，得另尋方法。\n\n另外注意 `node->left = dfs(node->left);` 這個寫法本身就是不變量的實作：它保證「父節點看到的永遠是修剪後的子樹」。寫成 `dfs(node->left);` 而不接回傳值，是本節最常見的錯誤——遞迴確實跑了，但結果沒有被接上，樹完全沒變。\n\n## 逐步演算法\n\n把刪點題拆成三步，逐一搭配程式碼：\n\n:::example 回傳值的語意：整理後這棵子樹的新根（nullptr 代表整片沒了）\n**1. 想清楚每個節點要「回傳什麼」。** 通常回傳「整理後的子樹根」：保留就回自己，刪掉就回 `nullptr`（或回某個替代孩子）。這個回傳值就是父親接回去的東西。\n\n```cpp\n// 回傳值的語意：整理後這棵子樹的新根（nullptr 代表整片沒了）\nTreeNode* dfs(TreeNode* node);\n```\n:::\n\n\n\n:::example 814 二叉樹剪枝：砍掉不含 1 的子樹\n**2. 先遞迴左右、把結果接回來，再判斷自己。** 順序千萬不能反——一定要等小孩變成最終狀態，自己的判斷才正確。\n\n```cpp\n// 814 二叉樹剪枝：砍掉不含 1 的子樹\nTreeNode* prune(TreeNode* node) {\n    if (!node) {\n        return nullptr;\n    }\n    node->left = prune(node->left);    // 左子樹剪完\n    node->right = prune(node->right);  // 右子樹剪完\n    // 自己是 0、且左右都被剪光了 → 整片砍掉\n    if (node->val == 0 && !node->left && !node->right) {\n        return nullptr;\n    }\n    return node;  // 否則保留\n}\n```\n:::\n\n\n\n:::example 1325 刪除值為 target 的葉子（可能連鎖）\n**3. 處理「刪除引發連鎖」的題目。** 像 1325，刪葉子後父親可能變新葉子。因為是自底向上，孩子先被刪、父親再判斷，連鎖自然發生，不必額外迴圈。\n\n```cpp\n// 1325 刪除值為 target 的葉子（可能連鎖）\nTreeNode* removeLeaf(TreeNode* node, int target) {\n    if (!node) {\n        return nullptr;\n    }\n    node->left = removeLeaf(node->left, target);\n    node->right = removeLeaf(node->right, target);\n    if (!node->left && !node->right && node->val == target) {\n        return nullptr;  // 現在是葉子且值符合 → 刪\n    }\n    return node;\n}\n```\n:::\n\n\n\n**邊界與複雜度：**\n\n- 別忘了根節點本身也可能被刪，所以最外層要用 `root = dfs(root)` 接回傳值。\n- 遞迴深度等於樹高，極端歪斜的樹可能逼近 `O(n)`、有爆 stack 風險。\n- 整體時間 `O(n)`、額外空間 `O(h)`（`h` 為樹高，遞迴堆疊用掉的）。\n\n## C++17 模板\n\n```cpp\n// 刪除所有不含 1 的子樹（LeetCode 814）。\nTreeNode* pruneTree(TreeNode* root) {\n  if (!root) { return nullptr; }\n\n  root->left  = pruneTree(root->left);    // 先修剪子樹，並把結果接回去\n  root->right = pruneTree(root->right);   // 漏掉賦值 = 樹完全沒變\n\n  if (!root->left && !root->right && root->val == 0) {\n    return nullptr;                       // 修剪後成為葉且值為 0，刪掉自己\n  }\n  return root;\n}\n```\n\n1325 題（反覆刪除值為 target 的葉節點）只需把最後的判定換成 `!root->left && !root->right && root->val == target`；連鎖刪除由後序自動完成，不需外層迴圈。\n\n## 時間與空間複雜度\n\n每個節點恰被訪問一次、判定一次，$O(n)$ 時間。這是相對於「反覆掃描直到不變」$O(nh)$（最壞 $O(n^2)$）的主要收穫，而且省下的不只是常數——一趟到不動點是由上述單調性論證保證的，不是經驗。\n\n空間 $O(h)$ 遞迴堆疊。若需要釋放被刪節點的記憶體，在回傳 `nullptr` 前 `delete` 即可，不改變複雜度。\n\n## 常見錯誤與邊界條件\n\n- 呼叫 `dfs(root->left)` 卻沒把回傳值賦回 `root->left`，樹完全沒被修剪。\n- 判定「是否為葉」時用了修剪前的子樹（例如先判自己再遞迴）。\n- 1325 題外面再包一層 `while (changed)` 迴圈，多餘且容易寫錯終止條件。\n- 整棵樹被刪光時回傳 `nullptr`，呼叫端未處理。\n- 只判 `root->val == 0` 而忘了同時要求兩子為空，把中間節點也刪掉。\n- 被刪節點未釋放。\n\n## 與相似技巧的比較\n\n- 與 2.3 一般的自底向上相比，本節回傳的是**節點指標**而非數值，因此能直接改動結構；兩者的歸納骨架完全相同。\n- 與 2.11 的 BST 刪除節點相比：本節是「依條件批次刪除」，2.11 是「刪除指定的單一節點並維持 BST 性質」，後者需要用前驅或後繼補位，複雜得多。\n- 一般樹上的對應題型見 3.3；圖上「反覆刪度數為 1 的點」則是拓撲排序剝葉（3.6），那裡因為沒有天然的父子方向，必須用佇列顯式維護待刪集合。\n\n## 代表例題\n\n**題號**：814、1110、1325\n\n放在「自底向上 DFS：刪點」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 814 | [二叉樹剪枝](https://leetcode.cn/problems/binary-tree-pruning/) |  |\n| 1110 | [刪點成林](https://leetcode.cn/problems/delete-nodes-and-return-forest/) | 1511 |\n| 1325 | [刪除給定值的葉子節點](https://leetcode.cn/problems/delete-leaves-with-a-given-value/) | 1407 |\n\n## 本節重點速查\n\n- 先遞迴、**把回傳值接回 `node->left` 與 `node->right`**、再判自己，這個順序保證判定時看到的是子樹的最終形態。\n- 單調性（刪子只會讓父更該刪）保證一趟 $O(n)$ 到不動點，不需要外層迴圈。\n",
        },
        {
          id: 19,
          title: "2.5 有遞有歸",
          summary:
            "## 這個技術解決什麼問題\n\n有些題目同時需要兩個方向的資訊：處理某節點時，既要知道「從根到我累積了什麼」，也要知道「我的子樹回傳了什麼」。538／1038（把 BST 轉成累加樹）就是典型——每個節點要加上**所有比它大的節點之和**，而那些節點分佈在它的右子樹與若干祖先的右子樹裡。\n\n一趟前序拿不到子樹資訊，一趟後序拿不到路徑資訊。「有遞有歸」的做法是在**同一次遍歷**中兩者兼顧：往下時傳遞參數，往上時回傳彙總，或者更巧妙地——**挑一個讓所需資訊恰好按順序出現的遍歷次序**。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就往「有遞有歸」想：\n\n- **需要沿路攜帶祖先資訊**（深度、路徑前綴和、目前到根的限制值）——這要靠 DFS 參數一路往下帶。\n- **答案要同時用到「上面帶下來的」和「下面回上來的」兩種資訊**：例如 1080 既要往下帶 limit，又要往上回報是否被刪空。\n- **走訪順序本身就是解法**：像 538 / 1038 改成反中序，由大到小造訪，邊走邊累加。\n- 暴力做法常是「對每個節點都重新往上爬一次祖先、或重新算一次子樹」，造成 `O(n^2)`；一次有遞有歸的 DFS 把它壓成 `O(n)`。\n\n確認自己有沒有套對，問三個問題：\n\n1. 哪些資訊要「往下帶」（放參數）、哪些要「往上回」（放回傳值或全域）？\n2. 我的動作該寫在遞迴呼叫「之前（遞）」還是「之後（歸）」？\n3. 換個走訪順序（例如反中序）會不會讓問題瞬間變簡單？\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n本節要解決的場景是：在樹上走訪時，光靠「自底向上彙總子樹」還不夠，有些資訊必須**從上往下帶**，或是要在同一次 DFS 裡「去的時候做一件事、回來的時候再做另一件事」。這就是「有遞有歸」——「遞」是往下深入（進入子節點前），「歸」是往上返回（離開子節點後）。\n\n:::example 反中序：右 → 自己 → 左，會由大到小造訪 BST\n先看一個最能說明問題的例子：538 / 1038「把二元搜尋樹轉成累加樹」。要把每個節點的值改成「所有 ≥ 它的值之和」。二元搜尋樹的中序走訪是由小到大；若我們**反過來走（右 → 自己 → 左）**，就會由大到小遇到每個節點，於是維護一個「目前累加和」變數，邊走邊加即可。\n\n```cpp\n// 反中序：右 → 自己 → 左，會由大到小造訪 BST\nint sum = 0;  // 一路累加「比目前大的值之和」\nvoid dfs(TreeNode* node) {\n    if (!node) {\n        return;\n    }\n    dfs(node->right);  // 先走比自己大的那邊\n    sum += node->val;  // 「歸」的時機：此刻 sum 已含所有更大的值\n    node->val = sum;   // 改寫成累加值\n    dfs(node->left);   // 再走比自己小的那邊\n}\n```\n:::\n\n\n\n:::example 一個 DFS 同時有「遞」與「歸」的動作\n重點在於：`sum` 是一個**沿途攜帶、隨走訪推進的狀態**。「遞」與「歸」之間插入動作的時機決定了答案。把動作寫在遞迴呼叫「之前」是下行時做（自頂向下攜帶資訊）；寫在「之後」是上行時做（自底向上彙總）。同一個函式裡兩者都可以有。\n\n```cpp\n// 一個 DFS 同時有「遞」與「歸」的動作\nvoid dfs(TreeNode* node, int depth) {\n    if (!node) {\n        return;\n    }\n    enter(node, depth);  // 遞：下行時帶著 depth 等祖先資訊\n    dfs(node->left, depth + 1);\n    dfs(node->right, depth + 1);\n    leave(node);  // 歸：上行時彙總子樹結果\n}\n```\n:::\n\n\n\n每個節點進出各一次，時間 `O(n)`。\n\n## 狀態／資料結構定義\n\n以 538 為例，關鍵洞察是：BST 的**反中序**（右 → 根 → 左）恰好給出由大到小的節點序列。於是狀態退化成一個變數：\n\n- `sum`：目前為止（即所有比當前節點大的節點）的累加和，用引用或成員變數跨遞迴共享。\n- 訪問順序：先遞迴右子樹，再處理自己（`sum += node->val; node->val = sum;`），最後遞迴左子樹。\n\n一般形式則是：\n\n- 往下傳的參數 `down`（路徑資訊）。\n- 往上回的回傳值 `up`（子樹彙總）。\n- 節點處理放在「兩者都齊備」的時機點。\n\n865／1080 需要真正的雙向：1080 要先自頂向下算出根到當前節點的路徑和，才能判斷「經過此節點的最大路徑和是否不足 limit」；而刪除的效果又必須自底向上傳遞（子樹全刪則自己也要刪）。\n\n## 不變量或正確性證明\n\n**538 反中序累加的不變量：** 在對節點 $v$ 執行 `sum += v->val` 之前，`sum` 恰等於樹中所有**嚴格大於** $v->val$ 的節點值之和。\n\n證明分兩部分，都依賴 BST 性質。\n\n其一，反中序序列是節點值的遞減排列。由 BST 定義，左子樹全部小於根、右子樹全部大於根；反中序輸出「右子樹、根、左子樹」，由結構歸納，右子樹遞減、左子樹遞減，且右子樹的值全大於根、根大於左子樹全部，故串接後整體遞減。\n\n其二，既然序列遞減，處理到 $v$ 時已處理過的節點恰是所有大於 $v$ 的節點；`sum` 是它們的和，正是不變量所述。因此 `sum += v->val` 後 `sum` 成為「大於等於 $v$」之和，賦給 `v->val` 即為所求。\n\n**這裡點名關鍵前提：輸入必須是 BST，且值互不相同。** 若有重複值，「嚴格大於」與「大於等於」的界線落在重複區塊內部，反中序的相對次序決定結果，題目須另行約定。若不是 BST，反中序給不出遞減序列，整個論證崩潰，只能退回「先收集所有值排序」的 $O(n \\log n)$ 做法。\n\n**一般雙向模式的正確性**由兩條不變量疊加：往下的參數滿足 2.2 的路徑不變量（呼叫 `dfs(v, down)` 時 `down` 是根到 $v$ 之父的累積量），往上的回傳值滿足 2.3 的子樹不變量（`dfs(v, ·)` 的回傳值是 $v$ 子樹的彙總量）。兩者互不干擾——**前提是往下傳的參數不被子樹修改**。若 `down` 是傳參考的可變容器，遞迴回來後必須復原，否則兄弟子樹拿到的路徑資訊是錯的（見 2.7 回溯）。\n\n1080 的刪除規則正確性：節點 $v$ 應被刪，若且唯若所有經過 $v$ 的根到葉路徑和都小於 limit。等價地，$v$ 的左右子樹在遞迴後都回傳 `nullptr`（各自沒有合格路徑），或 $v$ 是葉且路徑和不足。這個等價需要「合格路徑必然延伸到葉」這個前提，故葉節點的判定必須用原始的葉，而路徑和的累積必須自頂向下——兩個方向缺一不可。\n\n## 逐步演算法\n\n把「有遞有歸」的題目拆成三步：\n\n:::example 往下帶用參數，往上回用回傳值\n**1. 狀態表示：分清楚哪些資訊要「往下帶」、哪些要「往上回」。** 往下帶的（如深度、路徑前綴、限制範圍）當成 DFS 參數；往上回的（如子樹高度、是否合法）當成回傳值或全域變數。\n\n```cpp\n// 往下帶用參數，往上回用回傳值\nReturnInfo dfs(TreeNode* node, DownInfo carry);\n```\n:::\n\n\n\n:::example 1080 根到葉路徑上的不足節點：往下帶「剩餘 limit」，往上回「是否被刪光」\n**2. 更新流程：選對「插入動作的時機」。** 下行才知道的事，寫在遞迴呼叫前；要等小孩算完才知道的事，寫在呼叫後。\n\n```cpp\n// 1080 根到葉路徑上的不足節點：往下帶「剩餘 limit」，往上回「是否被刪光」\nTreeNode* dfs(TreeNode* node, int limit) {\n    if (!node) {\n        return nullptr;\n    }\n    if (!node->left && !node->right) {  // 葉子：路徑和不足就刪\n        return node->val < limit ? nullptr : node;\n    }\n    node->left = dfs(node->left, limit - node->val);  // 遞：扣掉自己的值往下帶\n    node->right = dfs(node->right, limit - node->val);\n    return (!node->left && !node->right) ? nullptr\n                                                                              : node;  // 歸：兩邊都沒了就刪自己\n}\n```\n:::\n\n\n\n:::example 865：歸的時候同時回傳「深度」與「答案候選」\n**3. 答案讀取：依題目落點不同。** 可能在根的回傳值、全域變數，或在「歸」的時刻順手記錄。865「含所有最深節點的最小子樹」就是在「歸」時回傳 `{子樹最大深度, 該答案候選}` 一路往上合併。\n\n```cpp\n// 865：歸的時候同時回傳「深度」與「答案候選」\npair<int, TreeNode*> dfs(TreeNode* node) {\n    if (!node) {\n        return {0, nullptr};\n    }\n    auto [ld, ln] = dfs(node->left);\n    auto [rd, rn] = dfs(node->right);\n    if (ld == rd) {\n        return {ld + 1, node};  // 左右一樣深 → 自己是 LCA\n    }\n    return ld > rd ? pair{ld + 1, ln} : pair{rd + 1, rn};\n}\n```\n:::\n\n\n\n**邊界與複雜度：** 想清楚每個變數是「下行有效」還是「全程有效」；全域變數記得在多筆測資間重置。時間 `O(n)`、空間 `O(h)`（樹高）。\n\n## C++17 模板\n\n```cpp\n// BST 轉累加樹（LeetCode 538 / 1038）：反中序，一個共享變數即可。\nclass Solution {\n  int sum_ = 0;\n public:\n  TreeNode* convertBST(TreeNode* root) {\n    if (!root) { return nullptr; }\n    convertBST(root->right);       // 先走大的一側\n    sum_ += root->val;             // 此時 sum_ 是所有大於 root 的節點之和\n    root->val = sum_;\n    convertBST(root->left);        // 再走小的一側\n    return root;\n  }\n};\n```\n\n真正雙向的骨架（以 1080 為例）：\n\n```cpp\n// 往下傳路徑和 down，往上回「修剪後的子樹根」。\nTreeNode* sufficientSubset(TreeNode* root, int limit) {\n  if (!root) { return nullptr; }\n  int down = limit - root->val;                     // 往下：扣掉自己\n  if (!root->left && !root->right) {                // 葉：路徑和是否達標\n    return down > 0 ? nullptr : root;\n  }\n  root->left  = sufficientSubset(root->left, down);   // 往上：接回修剪結果\n  root->right = sufficientSubset(root->right, down);\n  return (root->left || root->right) ? root : nullptr;\n}\n```\n\n## 時間與空間複雜度\n\n兩者都是 $O(n)$ 時間——每個節點仍只訪問一次，「有遞有歸」指的是在同一次訪問中利用兩個時機點，不是走兩趟。空間 $O(h)$ 遞迴堆疊。\n\n538 的替代解法「先中序收集再反向前綴和」同為 $O(n)$ 時間但需 $O(n)$ 額外空間；反中序版只用一個變數，這是選它的理由。\n\n## 常見錯誤與邊界條件\n\n- 538 寫成正中序（左 → 根 → 右），累加的變成比它小的節點。\n- `sum` 宣告成區域變數而非跨遞迴共享，每層各算各的。\n- 把往下傳的可變容器在遞迴後忘記復原，污染兄弟子樹。\n- 1080 在非葉節點就用路徑和判定（必須延伸到葉才算數）。\n- 1080 忘記把遞迴回傳值接回 `root->left`／`root->right`。\n- 空樹。\n- 整棵樹被刪光時回傳 `nullptr` 未被呼叫端處理。\n\n## 與相似技巧的比較\n\n- 純自頂向下（2.2）與純自底向上（2.3）各用一個方向；本節在需要兩者時才用，不要無腦套用——多傳一個沒用到的參數只會增加出錯面。\n- 2.6 直徑那類「在每個節點用左右子樹回傳值更新全域答案」嚴格說只用了向上方向，但因為答案寫在全域變數，形式上很像雙向，要能區分。\n- 一般樹的對應是 3.4；換根 DP（樹形 DP 的進階，見 2.12 與 3.4）是雙向的極致——第一趟後序算子樹資訊，第二趟前序把「子樹外」的資訊傳下去。\n\n## 代表例題\n\n**題號**：538、865、1038、1080\n\n放在「有遞有歸」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 538 | [把二叉搜索樹轉換為累加樹](https://leetcode.cn/problems/convert-bst-to-greater-tree/) |  |\n| 865 | [具有所有最深節點的最小子樹](https://leetcode.cn/problems/smallest-subtree-with-all-the-deepest-nodes/) | 1534 |\n| 1038 | [從二叉搜索樹到更大和樹](https://leetcode.cn/problems/binary-search-tree-to-greater-sum-tree/) | 1375 |\n| 1080 | [根到葉路徑上的不足節點](https://leetcode.cn/problems/insufficient-nodes-in-root-to-leaf-paths/) | 1805 |\n\n## 本節重點速查\n\n- 往下傳路徑參數、往上回子樹彙總，兩個時機點在同一次遍歷中各取所需。\n- BST 的反中序給出遞減序列，538 靠這條性質把雙向需求壓成一個共享變數。\n- 往下傳可變容器時務必復原；\n- 往上回節點指標時務必接回賦值。\n",
        },
        {
          id: 20,
          title: "2.6 二叉樹的直徑",
          summary:
            "## 這個技術解決什麼問題\n\n樹的直徑是「任意兩節點間最長路徑的長度」（543 數邊數，124 求最大路徑和）。樸素做法是對每個節點各跑一次 DFS 求最遠距離，$O(n^2)$。\n\n槓桿在於一個結構性觀察：**每條路徑都有唯一的最高點**（離根最近的那個節點）。於是「枚舉所有路徑」可以轉成「枚舉最高點」——對每個節點 $v$，計算「以 $v$ 為最高點的最長路徑」，取全域最大值。而後者只需要 $v$ 的左右子樹各自的「向下最長鏈」，一次後序遍歷就能全部算完，$O(n)$。\n\n這個「枚舉最高點」的分解方式是樹上路徑問題的通用武器，遠不只用在直徑。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就往「樹的直徑 / 轉折點路徑」想：\n\n- **求一條路徑的最長 / 最大，且路徑可以「拐彎」**：從某節點往左下又往右下，不必經過根——這就是要找每個節點當轉折點。\n- **答案是「左邊貢獻 + 右邊貢獻」，但往上傳遞只能給一邊**：543、124、687 全是這個結構。\n- **題目雖然字面不叫直徑，本質卻一樣**：例如 2385「感染二元樹需要的總時間」，把樹從某點當作起點，所求其實是「離起點最遠的距離」，也用同樣的「左右臂 + 上傳單邊」框架（需先建父指標或換根）。\n- 暴力做法是「對每個節點都重新往下量左右最長」，重複計算成 `O(n^2)`；一次 DFS 把臂長記下來就 `O(n)`。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的 DFS「回傳值」是不是「單邊臂長」（給父親接）？\n2. 我更新答案時，是不是用「左臂 + 右臂」（在轉折點合併）？\n3. 臂能不能接上來，有沒有額外條件（同值？非負？）需要判斷？\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n**延伸講解**：[樹形 DP：樹的直徑【基礎演算法精講 23】](https://www.bilibili.com/video/BV17o4y187h1/)\n\n本節要解決的場景是：在一棵樹上找「一條穿過某個節點、左右各往下延伸的最長（或最大）路徑」。最經典的就是 543「二元樹的直徑」——直徑是任意兩節點間最長路徑的邊數；以及 124「二元樹中的最大路徑和」——找一條路徑使節點值總和最大。\n\n:::example 以 node 為轉折點的路徑長 = 左臂 + 右臂\n關鍵觀察：任何一條這樣的路徑，一定有一個**最高點（轉折點）**。以這個最高點來看，路徑就是「從它往左子樹一路向下」接上「從它往右子樹一路向下」。所以只要對**每個節點**都算一次「以它為轉折點時，左臂 + 右臂能有多長」，取全部的最大值，就是答案。\n\n```cpp\n// 以 node 為轉折點的路徑長 = 左臂 + 右臂\n// 左臂 = 左子樹往下能延伸的最長長度；右臂同理\nint through = leftArm + rightArm;\n```\n:::\n\n\n\n:::example 核心二分：回傳給父親的是「單邊臂長」，但更新答案時用「左+右」\n難點在於：每個節點需要知道「我往下單邊能延伸多長」，而這要靠子樹算出來。於是用自底向上 DFS：DFS 回傳的是「**單邊**往下的最長延伸」（只能選左或右一條，因為回傳值是要給父親接著往上用的，不能左右都要），但在 DFS 內部、計算答案時，可以「左右都要」更新全域最大值。\n\n```cpp\n// 核心二分：回傳給父親的是「單邊臂長」，但更新答案時用「左+右」\nint dfs(TreeNode* node) {\n    if (!node) {\n        return 0;\n    }\n    int L = dfs(node->left);   // 左子樹單邊最長\n    int R = dfs(node->right);  // 右子樹單邊最長\n    ans = max(ans, L + R);     // 答案：以 node 轉折，左右都用上\n    return max(L, R) + 1;      // 回傳：給父親只能選一條臂 + 自己\n}\n```\n:::\n\n\n\n這個「回傳單邊、更新雙邊」的不對稱，是整個技巧的靈魂，務必分清楚。\n\n## 狀態／資料結構定義\n\n必須嚴格區分兩個量，混用是本節唯一的大坑：\n\n- **回傳值** `down(v)`：從 $v$ 出發、**向下走到某個後代**的最長鏈。它必須是「單邊」的，因為父節點要拿它接上自己再往上延伸，路徑不能有分叉。空節點回傳 0（543 數邊）或 0（124 因為可捨棄負貢獻）。\n- **全域答案** `ans`：在每個節點處用 `down(left) + down(right) + (自身貢獻)` 更新，這是「以 $v$ 為最高點」的最長路徑，允許左右分叉。\n\n543 的兩者：`down(v) = 1 + max(down(L), down(R))`（邊數），`ans = max(ans, down(L) + down(R))`。\n\n124 的兩者：`down(v) = v->val + max(0, max(down(L), down(R)))`，`ans = max(ans, v->val + max(0, down(L)) + max(0, down(R)))`。`max(0, ·)` 的意思是負貢獻的子樹乾脆不接。\n\n## 不變量或正確性證明\n\n**分解的完備性。** 命題：任一條路徑 $P$ 恰有一個節點是 $P$ 上深度最小的節點（即最高點）。存在性顯然（有限集合有最小值）。唯一性：假設有兩個節點 $a \\ne b$ 深度同為最小，路徑 $P$ 上 $a$ 到 $b$ 這段必經過它們的 LCA，而 LCA 的深度嚴格小於兩者（$a$、$b$ 互不為祖先，否則深度不等），與最小性矛盾。故最高點唯一。\n\n因此「所有路徑」被最高點劃分成 $n$ 個互斥類別，取各類別最大值的最大值即為全域最大——**沒有路徑被漏掉，也沒有被重複計算**。這就是為什麼在每個節點更新一次 `ans` 就足夠。\n\n**每個類別內的最大值。** 以 $v$ 為最高點的路徑，形狀必定是「從左子樹某後代向上到 $v$，再向下到右子樹某後代」（其中一側可為空）。要最大化，兩側各自獨立取最長向下鏈即可——**兩側的選擇互不影響**，因為它們位於不相交的子樹中。這個獨立性正是能拆成 `down(L) + down(R)` 的依據；若路徑有「不能重複用某資源」之類的耦合約束，這一步就不成立。\n\n**回傳值的正確性（結構歸納）。** `down(v)` 回傳從 $v$ 向下的最長鏈。基底：空節點回傳 0。歸納：從 $v$ 向下的鏈，若非空則必先走到左子或右子，之後就是從該子節點向下的鏈；由歸納假設兩者為 `down(L)`、`down(R)`，取較大者加上 $v$ 到子的那條邊即可。\n\n**為什麼回傳值必須是單邊。** 若 `down(v)` 回傳了分叉後的值（即把 `ans` 那個公式當回傳值），父節點接上它會得到一條有分叉的「路徑」，不是合法路徑。這是本節最常見的錯誤，症狀是答案偏大。\n\n**124 為何要 `max(0, ·)`。** 節點值可為負，接上一棵總和為負的子樹只會讓路徑和變小，故可以選擇不接。合法性在於「單一節點」本身就是合法路徑，所以捨棄某一側不會導致無路徑可選。若題目要求路徑必須含至少兩個節點，這個裁剪就要調整。相對地，543 數的是邊數且邊數非負，不需要裁剪。\n\n## 逐步演算法\n\n把直徑類問題拆成三步：\n\n:::example 兩個量分開：一個回傳、一個全域累計\n**1. 想清楚 DFS「回傳值」與「答案」的差別。** 回傳值＝單邊向下延伸（給父親用）；答案＝在某節點左右臂合起來的最大值（用全域變數記錄，不回傳）。\n\n```cpp\n// 兩個量分開：一個回傳、一個全域累計\nint ans = 0;              // 全域答案：最佳的「左臂 + 右臂」\nint dfs(TreeNode* node);  // 回傳：單邊臂長\n```\n:::\n\n\n\n:::example 687 最長同值路徑：值相同才能把臂接上來\n**2. 在「歸」的時候同時做兩件事：更新答案、回傳單邊。** 543 求邊數，臂長就是子樹高度；687「最長同值路徑」則多一個條件：只有子值等於自己時臂才能接。\n\n```cpp\n// 687 最長同值路徑：值相同才能把臂接上來\nint dfs(TreeNode* node) {\n    if (!node) {\n        return 0;\n    }\n    int L = dfs(node->left), R = dfs(node->right);\n    int la = (node->left && node->left->val == node->val) ? L + 1 : 0;\n    int ra = (node->right && node->right->val == node->val) ? R + 1 : 0;\n    ans = max(ans, la + ra);  // 左右同值臂合併\n    return max(la, ra);       // 回傳單邊\n}\n```\n:::\n\n\n\n:::example 124 最大路徑和：負臂寧可捨棄（與 0 取大）\n**3. 處理「值可能為負」的變形。** 124 的路徑和裡，若某條臂是負的，不如不要，所以延伸時和 0 取大。\n\n```cpp\n// 124 最大路徑和：負臂寧可捨棄（與 0 取大）\nint dfs(TreeNode* node) {\n    if (!node) {\n        return 0;\n    }\n    int L = max(0, dfs(node->left));  // 負的就當 0，不接\n    int R = max(0, dfs(node->right));\n    ans = max(ans, node->val + L + R);  // 以 node 為頂的整條路徑和\n    return node->val + max(L, R);       // 回傳：自己 + 較大的單邊\n}\n```\n:::\n\n\n\n**邊界與複雜度：** 答案初值要設安全值（求和題用很小的負數，避免全負樹算錯）；分清楚「邊數」與「節點數」（直徑常以邊數計，臂長相加即是）。時間 `O(n)`、空間 `O(h)`（樹高）。\n\n## C++17 模板\n\n```cpp\n// 二元樹的直徑（LeetCode 543）：回傳向下最長鏈，全域答案允許分叉。\nclass Solution {\n  int ans_ = 0;\n\n  int down(TreeNode* node) {              // 從 node 向下的最長鏈（邊數）\n    if (!node) { return 0; }\n    int l = down(node->left);\n    int r = down(node->right);\n    ans_ = std::max(ans_, l + r);         // 以 node 為最高點：允許左右分叉\n    return 1 + std::max(l, r);            // 回傳給父節點：必須單邊\n  }\n\n public:\n  int diameterOfBinaryTree(TreeNode* root) {\n    down(root);\n    return ans_;\n  }\n};\n```\n\n最大路徑和（LeetCode 124）同一骨架，加上負貢獻裁剪：\n\n```cpp\nlong long ans = LLONG_MIN;\n\nlong long down(TreeNode* node) {\n  if (!node) { return 0; }\n  long long l = std::max(0LL, down(node->left));    // 負的就不接\n  long long r = std::max(0LL, down(node->right));\n  ans = std::max(ans, node->val + l + r);           // 最高點在此\n  return node->val + std::max(l, r);                // 單邊上傳\n}\n```\n\n## 時間與空間複雜度\n\n每個節點恰計算一次 `down` 並更新一次 `ans`，均為 $O(1)$ 工作，故 $O(n)$ 時間。相對於「對每個節點各跑一次 DFS」的 $O(n^2)$，收穫來自最高點分解讓每個節點只需負責自己那一類。\n\n空間 $O(h)$ 遞迴堆疊，斜樹最壞 $O(n)$。\n\n## 常見錯誤與邊界條件\n\n- 把回傳值寫成 `l + r`（分叉值），父節點接上後路徑不合法，答案偏大。\n- 124 忘記 `max(0, ·)`，被負值子樹拖累。\n- 124 答案初始值設為 0 而全樹皆負時輸出 0（應設為 `LLONG_MIN` 或第一個節點值）。\n- 543 混淆邊數與節點數（邊數 = 節點數 $-$ 1，題目要哪個要看清楚）。\n- `ans` 用 `int` 在 124 溢位。\n- 只有一個節點時直徑為 0（邊數）。\n- 空樹。\n\n## 與相似技巧的比較\n\n- 一般樹的直徑（3.5）除了同樣的 DFS 解法，還有「兩次 BFS」的經典解法：從任意點出發找最遠點 $u$，再從 $u$ 出發找最遠點 $v$，$u$ 到 $v$ 即直徑。那個解法的正確性另需一套論證（且**只對無權或正權樹成立**），見 3.5。\n- 687（最長同值路徑）、2385（感染時間）都是同一個最高點分解骨架，只是 `down` 的定義換成「向下最長同值鏈」或「向下最長距離」。\n- 549（最長連續序列路徑）需要回傳兩個量（遞增鏈長與遞減鏈長），是狀態擴充的例子，思路同 2.3 末尾所述。\n- 與樹形 DP（2.12）的界線：直徑類問題的狀態只有一維（向下最長鏈），樹形 DP 通常有多維狀態（選／不選）。\n\n## 代表例題\n\n**題號**：124、543、549、687、2385\n\n放在「二叉樹的直徑」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 124 | [二叉樹中的最大路徑和](https://leetcode.cn/problems/binary-tree-maximum-path-sum/) |  |\n| 543 | [二叉樹的直徑](https://leetcode.cn/problems/diameter-of-binary-tree/) |  |\n| 549 | [二叉樹最長連續序列 II](https://leetcode.cn/problems/binary-tree-longest-consecutive-sequence-ii/)（Premium） |  |\n| 687 | [最長同值路徑](https://leetcode.cn/problems/longest-univalue-path/) |  |\n| 2385 | [感染二叉樹需要的總時間](https://leetcode.cn/problems/amount-of-time-for-binary-tree-to-be-infected/) | 1711 |\n\n## 本節重點速查\n\n- **枚舉最高點**：每條路徑有唯一最高點，故在每個節點更新一次全域答案就不重不漏。\n- 回傳值是**單邊**向下最長鏈（給父節點延伸用），全域答案才允許**左右分叉**——兩者絕不可混用。\n- 124 用 `max(0, ·)` 捨棄負貢獻，答案初值要能容納全負。\n",
        },
        {
          id: 21,
          title: "2.7 回溯",
          summary:
            "## 這個技術解決什麼問題\n\n當答案是「所有從根到葉、和為目標值的路徑」（113）或「所有根到葉路徑的字串」（257）時，你需要的不只是路徑的**摘要**（和、長度），而是路徑**本身**。\n\n把路徑存成容器往下傳，若每層複製一份，$n$ 個節點各持有長度 $O(h)$ 的副本，光是複製就 $O(nh)$。回溯的槓桿是**全程共用同一個容器**：進入節點時 `push_back`，離開節點前 `pop_back`。每次進出各 $O(1)$，總計 $O(n)$ 的維護成本，只有在真正記錄答案時才付一次複製。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就往「樹上回溯」想：\n\n- **要枚舉 / 收集「一條條路徑」**（根到葉、或任意起訖），而不是只回傳單一數值。\n- **沿途需要記錄「目前走過的節點序列」**，且走完一個分支要回到原狀去走另一個分支——這就是 push / pop 的撤銷。\n- **答案出現在「葉子」或某個合法終點**：抵達時把當前 path 收下來。\n- 暴力做法常是「對每個節點都重新從根找一遍路徑」或反覆重算路徑和，造成大量重複；回溯靠「邊走邊維護 path」一次走訪就枚舉完。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我維護的 `path` 不變式是什麼？（任何時刻它就是「根 → 當前節點」）\n2. 我每個 `push_back` 是否都有對稱的 `pop_back`？順序對嗎？\n3. 收答案時我存的是 path 的「拷貝」還是「參考」？（必須是拷貝）\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n本節要解決的場景是：在樹上「枚舉所有從根往下的路徑」，並在過程中收集或統計符合條件的路徑。例如 257「二元樹的所有路徑」要列出每條根到葉的路徑；113「路徑總和 II」要找出所有和等於目標的根到葉路徑。\n\n這類問題的共通點：**答案是一條條路徑**，而路徑會隨著走訪「邊走邊長、回頭邊縮」。我們需要一個容器（通常叫 `path`）記錄「目前從根走到這裡經過了哪些節點」。這正是「回溯（backtracking）」：\n\n> 進入一個節點前「做選擇」（把它加進 path），遞迴探索它的子樹，**離開時「撤銷選擇」（把它從 path 移除）**，讓 path 回到進來之前的樣子。\n\n:::example 回溯三部曲：選擇 → 遞迴 → 撤銷\n```cpp\n// 回溯三部曲：選擇 → 遞迴 → 撤銷\npath.push_back(node->val);  // 1) 做選擇：把自己加入路徑\ndfs(node->left);            // 2) 遞迴探索\ndfs(node->right);\npath.pop_back();  // 3) 撤銷選擇：離開時還原，不污染兄弟分支\n```\n:::\n\n\n\n:::example 不變式：任何時刻 path 就是「根 → 當前節點」這條路徑\n為什麼一定要撤銷？因為左右子樹、以及更上層的其他分支共用同一個 `path`。若不撤銷，走完左子樹後 path 還殘留左邊的節點，再去走右子樹就會把不相干的節點混進來。撤銷的動作保證「每進入一個分支時，path 都恰好是從根到當前位置的正確狀態」——這就是回溯要維持的**不變式**。\n\n```cpp\n// 不變式：任何時刻 path 就是「根 → 當前節點」這條路徑\n// push 後成立，pop 後也回到呼叫前成立\n```\n:::\n\n\n\n樹上回溯每個節點進出各一次，框架本身是 `O(n)`；若每到葉子要複製整條路徑收進答案，總成本和「路徑數 × 路徑長」有關。\n\n## 狀態／資料結構定義\n\n- `path`：共享的可變容器（`vector<int>`），語意是「從根到當前節點的路徑」。**必須以引用傳遞或設為成員變數**，按值傳遞就退化成複製法。\n- 三段式骨架，順序固定：\n  1. `path.push_back(node->val);` — 進入，把自己加入路徑。\n  2. 遞迴左右子樹（中間可能記錄答案）。\n  3. `path.pop_back();` — 離開，撤銷。\n- 記錄答案的時機在葉節點：`if (!node->left && !node->right && rest == 0) ans.push_back(path);` — 這裡的複製是必要的，因為 `path` 之後會被改動。\n- 437（路徑和 III，路徑不必從根開始）改用另一種狀態：前綴和計數雜湊表 `cnt[prefix]`，同樣需要在離開時撤銷。\n\n## 不變量或正確性證明\n\n不變量：**每次呼叫 `dfs(node)` 進入函式時，`path` 恰等於根到 `node` 之父的路徑；每次 `dfs(node)` 返回時，`path` 已被還原成呼叫前的內容。**\n\n- 初始化：`dfs(root)` 前 `path` 為空，而根之父不存在（空路徑），成立。\n- 保持與還原：函式體 `push_back(node)` 使 `path` 成為根到 `node` 的路徑，故對子節點呼叫 `dfs(child)` 時，`path` 正是根到 child 之父（即 `node`）的路徑，不變量的前半對子呼叫成立。由歸納假設，每個子呼叫返回時 `path` 已還原（仍是根到 `node`）。最後 `pop_back()` 移除 `node`，`path` 回到進入前的狀態，後半成立。\n- 結論：對任一葉節點 $\\ell$，在其函式體內 `push_back` 之後，`path` 恰為根到 $\\ell$ 的完整路徑。\n\n**這個「返回時已還原」的性質是整套方法的核心，也是它為什麼叫「回溯」。** 它保證兄弟子樹之間完全隔離：右子樹開始時看到的 `path` 與左子樹開始時一模一樣。少寫一行 `pop_back()`，路徑就會不斷累積，右子樹看到的是「左子樹殘留 + 自己」，答案錯得離譜卻不會崩潰——這正是它難以除錯的原因。\n\n**push 與 pop 必須嚴格配對**：函式的每一條返回路徑（包括提前 `return`）之前都要 pop。因此若在中途寫 `if (...) return;`，就漏了一次 pop。穩妥的寫法是把出口統一在函式末尾，或改用 RAII 守衛。\n\n**437 前綴和法的正確性。** 定義 `pre(v)` 為根到 $v$ 的路徑和。以 $v$ 結尾的向下路徑中，和為 `target` 的條數，等於祖先鏈上（含根之前的虛擬起點）滿足 `pre(u) == pre(v) - target` 的 $u$ 的個數。用雜湊表 `cnt` 記錄**當前祖先鏈**上各前綴和出現的次數即可 $O(1)$ 查詢。\n\n關鍵前提：`cnt` 必須恰好反映「當前祖先鏈」，不能包含已經離開的分支。因此進入時 `cnt[pre(v)]++`、離開前 `cnt[pre(v)]--`，與 `path` 的 push/pop 同構。少了這個撤銷，左子樹的前綴和會被誤算成右子樹的祖先，產生根本不存在的路徑。另需初始化 `cnt[0] = 1`，代表「空前綴」，否則從根開始的合格路徑會漏算。\n\n## 逐步演算法\n\n把樹上回溯題拆成三步：\n\n:::example 狀態：path 記路徑；remain 記離目標還差多少\n**1. 狀態表示：用一個 `path` 記錄目前路徑，需要時再加輔助量。** 求和題可額外帶「剩餘目標」`remain`，省得每次重算。\n\n```cpp\n// 狀態：path 記路徑；remain 記離目標還差多少\nvector<int> path;\n// dfs(node, remain)：remain 是 target 扣掉沿途值後剩餘\n```\n:::\n\n\n\n:::example 113 路徑總和 II：收集所有「根到葉、和為 target」的路徑\n**2. 更新流程：標準三部曲，撤銷一定要對稱。** push 幾個就 pop 幾個，順序相反。\n\n```cpp\n// 113 路徑總和 II：收集所有「根到葉、和為 target」的路徑\nvoid dfs(TreeNode* node, int remain) {\n    if (!node) {\n        return;\n    }\n    path.push_back(node->val);\n    remain -= node->val;\n    if (!node->left && !node->right && remain == 0) {\n        ans.push_back(path);  // 抵達葉子且剛好用完 → 收一條\n    }\n    dfs(node->left, remain);\n    dfs(node->right, remain);\n    path.pop_back();  // 撤銷，還原 path\n}\n```\n:::\n\n\n\n:::example 257：收集字串形式的路徑（同樣在葉子處收）\n**3. 答案讀取：到合法位置就「複製」一份收進答案。** 注意收的是 `path` 的**拷貝**（`ans.push_back(path)`），不能存指標／參考，否則後續 pop 會把它改掉。\n\n```cpp\n// 257：收集字串形式的路徑（同樣在葉子處收）\nif (!node->left && !node->right) {\n    ans.push_back(cur);  // cur 為當前拼好的字串\n}\n```\n:::\n\n\n\n**邊界與複雜度：**\n\n- 437「路徑總和 III」允許路徑「不必從根開始」，純回溯會 `O(n^2)`；常改用「前綴和 + 雜湊表」邊走邊回溯計數，降到 `O(n)`。\n- 1457「偽迴文路徑」收集的是「各數字出現次數的奇偶」，回溯時用一個位元遮罩翻轉，葉子處檢查最多一個奇數。\n- 空樹、單邊樹、目標為負都要能正確處理；時間視路徑數而定，空間 `O(h)`（path 與遞迴深度）。\n\n## C++17 模板\n\n以下用同型延伸示範題 46. 全排列 展示核心寫法。\n\n**完整題目**：給定不重複整數陣列，列出所有可能排列。\n\n:::example 回溯 DFS：每層從未使用的元素中選一個加入 path\n**線索**：題目要求列出所有方案、排列、組合或路徑；需要逐步選擇並在返回時撤銷。\n\n```cpp\nclass Solution {\npublic:\n    vector<vector<int>> permute(vector<int>& nums) {\n            vector<vector<int>> ans;\n            vector<int> path;                // 當前排列路徑\n            vector<char> used(nums.size());  // 標記各元素是否已加入路徑\n            // 回溯 DFS：每層從未使用的元素中選一個加入 path\n            function<void()> dfs = [&]() {\n                    if (path.size() == nums.size()) {\n                            ans.push_back(path);  // 路徑長度等於 nums 時得到完整排列\n                            return;\n                    }\n                    for (int i = 0; i < (int)nums.size(); ++i) {\n                            if (used[i]) {\n                                    continue;  // 已使用的元素跳過\n                            }\n                            used[i] = true;\n                            path.push_back(nums[i]);\n                            dfs();\n                            // 回溯：撤銷選擇，讓後續元素有機會被選入\n                            path.pop_back();\n                            used[i] = false;\n                    }\n            };\n            dfs();\n            return ans;\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- 回溯維護路徑與可選集合；遞迴返回前恢復現場，確保下一個分支看到同樣的狀態。\n- `path` 保存目前已做的選擇，`used` 或 `start` 控制下一步可選集合。\n- 每次遞迴後都要撤銷選擇，讓下一個分支看到相同的現場。\n\n## 時間與空間複雜度\n\n113：遍歷 $O(n)$，但**記錄答案的複製不可省**。最壞情況（完全二元樹，全部路徑合格）有 $O(n)$ 條路徑、每條長 $O(h)$，故輸出規模 $O(nh)$，總時間 $O(nh)$。這是輸出下界決定的，不是演算法的浪費。空間除輸出外為 $O(h)$。\n\n437：每個節點常數次雜湊操作，$O(n)$ 時間、$O(h)$ 空間。相對於「對每個節點各往下掃一次」的 $O(n^2)$（斜樹）或 $O(n \\log n)$（平衡），前綴和法對任何形狀都是 $O(n)$。\n\n## 常見錯誤與邊界條件\n\n- push 後必須 pop，標記後必須復原。\n- 排列與組合的 start/used 語意不同。\n- 剪枝條件要保證不漏解。\n- 答案要拷貝 path，而不是存引用。\n\n## 常見變形\n\n- 排序後用 `i > start && nums[i] == nums[i - 1]` 去重。\n- 用 bitmask 表示已選集合。\n- 可與 DFS、DP 記憶化結合剪枝。\n\n## 與相似技巧的比較\n\n- 與 2.2 自頂向下相比：純量摘要按值傳遞、天然隔離、不需撤銷；只有當狀態是**共享的可變結構**時才需要回溯。判斷標準就這一條。\n- 與 4.x 的組合／排列回溯相比，樹上回溯的「選擇分支」由樹結構給定（就是左右子），而組合問題的分支要自己枚舉；撤銷的機制完全相同。\n- 437 的前綴和加雜湊表撤銷，與陣列上「和為 K 的子陣列」（560）是同一招，差別只在陣列的前綴是線性的、樹的前綴是祖先鏈，因此需要 pop。\n- 若只要「是否存在」一條合格路徑（112），不需要回溯，用 2.2 傳純量即可。\n\n## 代表例題\n\n**題號**：113、257、437、1457\n\n放在「回溯」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 113 | [路徑總和 II](https://leetcode.cn/problems/path-sum-ii/) |  |\n| 257 | [二叉樹的所有路徑](https://leetcode.cn/problems/binary-tree-paths/) |  |\n| 437 | [路徑總和 III](https://leetcode.cn/problems/path-sum-iii/) |  |\n| 1457 | [二叉樹中的偽迴文路徑](https://leetcode.cn/problems/pseudo-palindromic-paths-in-a-binary-tree/) | 1405 |\n\n## 本節重點速查\n\n- 三段式：進入 push、遞迴、離開 pop，每條返回路徑都要配對撤銷。\n- 不變量是「函式返回時狀態已還原」，它保證兄弟子樹互不污染。\n- 只有共享可變狀態才需要回溯，純量傳值不需要。\n- 437 用前綴和計數表，撤銷機制相同，別忘 `cnt[0] = 1`。",
        },
        {
          id: 22,
          title: "2.8 最近公共祖先",
          summary:
            "## 這個技術解決什麼問題\n\n最近公共祖先（LCA）是「同時是 $p$ 與 $q$ 的祖先，且深度最大的那個節點」。它是樹上距離、路徑查詢的基石：$p$ 到 $q$ 的路徑必經 LCA，距離為 $d(p) + d(q) - 2d(\\text{LCA})$。\n\n樸素做法是求出根到 $p$、根到 $q$ 的兩條路徑，再找最後一個公共節點，需要 $O(h)$ 空間存路徑。二元樹的遞迴解法把它壓成一次後序遍歷、$O(1)$ 額外空間（不計堆疊），而且程式碼只有幾行——代價是那幾行的正確性並不顯然，值得仔細論證。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就往「最近公共祖先」想：\n\n- **問兩個（或多個）節點「最近的共同祖先」**，或等價的「兩條根到節點路徑第一次分岔的地方」。\n- **要找「兩個目標分屬左右子樹」的那個轉折節點**：236 的左右都非空即是。\n- **若是 BST**，立刻想到用「值落在 `p`、`q` 之間」一路往下定位（235），比一般樹更快。\n- 暴力做法是「分別求出 `p`、`q` 到根的兩條路徑，再比對最後一個共同節點」，需要額外存路徑；自底向上 DFS 一趟就能合併判斷，省下儲存。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的 DFS 回傳值代表什麼？（「子樹裡找到的目標 / LCA 候選」）\n2. 「左右子樹都回報找到」時，是不是就代表當前節點是 LCA？\n3. 這是不是 BST？若是，能不能改用「值比較往下走」省成 `O(h)`？\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n**延伸講解**：[二叉樹的最近公共祖先【基礎演算法精講 12】](https://www.bilibili.com/video/BV1W44y1Z7AR/)\n\n本節要解決的場景是：給樹上兩個節點 `p`、`q`，找它們的「最近公共祖先（Lowest Common Ancestor, LCA）」——也就是同時是 `p` 和 `q` 祖先、且**深度最深**的那個節點。代表題是 236「二元樹的最近公共祖先」、235「BST 的最近公共祖先」。\n\n先建立直覺：兩個節點的所有公共祖先排成一條從根往下的鏈，最近公共祖先就是這條鏈最底下那個。換句話說，LCA 是「`p` 與 `q` 第一次分道揚鑣的那個節點」——在它這裡，一個往左子樹、另一個往右子樹（或其中一個就是它本身）。\n\n:::example 236：回傳「這棵子樹裡找到的 LCA 候選」，沒找到回 nullptr\n對一般二元樹（236），用自底向上 DFS：每個節點回報「我這棵子樹裡有沒有見到 `p` 或 `q`」。\n\n```cpp\n// 236：回傳「這棵子樹裡找到的 LCA 候選」，沒找到回 nullptr\nTreeNode* dfs(TreeNode* node, TreeNode* p, TreeNode* q) {\n    if (!node || node == p || node == q) {\n        return node;  // 撞到目標或空就回報\n    }\n    TreeNode* L = dfs(node->left, p, q);\n    TreeNode* R = dfs(node->right, p, q);\n    if (L && R) {\n        return node;  // 左右各找到一個 → node 就是分岔點 = LCA\n    }\n    return L ? L : R;  // 否則把找到的那邊往上傳\n}\n```\n:::\n\n\n\n精妙之處：若左子樹回傳非空、右子樹也非空，代表 `p`、`q` 分別在兩側，當前節點就是它們的分岔點，也就是 LCA；若只有一側非空，把那一側往上傳（可能是 LCA，也可能只是其中一個目標，交給更上層判斷）。\n\n:::example 235 BST：靠值的大小決定往哪走，第一個「夾在中間」的就是 LCA\n對 BST（235）更簡單：利用「左小右大」的性質，從根往下，只要 `p`、`q` 都比目前小就往左、都比目前大就往右，**一旦目前節點落在兩者之間（或等於某一個），它就是 LCA**。\n\n```cpp\n// 235 BST：靠值的大小決定往哪走，第一個「夾在中間」的就是 LCA\nwhile (node) {\n    if (p->val < node->val && q->val < node->val) {\n        node = node->left;\n    } else if (p->val > node->val && q->val > node->val) {\n        node = node->right;\n    } else {\n        return node;  // p、q 分屬兩側或等於 node → LCA\n    }\n}\n```\n:::\n\n\n\n兩種做法都是 `O(n)`（235 更精確是 `O(h)`，`h` 為樹高）。\n\n## 狀態／資料結構定義\n\n遞迴函式 `lca(node, p, q)` 的回傳值語意必須precise，這是理解本節的全部關鍵：\n\n> **若以 `node` 為根的子樹中同時含有 $p$ 與 $q$，回傳它們的 LCA；若只含其中一個，回傳那一個；若都不含，回傳 `nullptr`。**\n\n注意這是一個**三合一**的語意，而不是「回傳 LCA」。正因為它把「找到了一個」與「找到了答案」用同一個回傳值編碼，程式碼才能這麼短。\n\nBST 版本（235）的狀態更簡單：只需當前節點，靠值的大小關係決定往哪走。\n\n## 不變量或正確性證明\n\n**先證回傳值語意（結構歸納）。** 設遞迴依 `l = lca(node->left)`、`r = lca(node->right)` 展開。\n\n- 基底：`node == nullptr` 回傳 `nullptr`，符合「都不含」。\n- `node` 是 $p$ 或 $q$ 時直接回傳 `node`。要說明這不會漏掉「另一個在 `node` 的子樹裡」的情形：若 $q$ 在 $p$ 的子樹中，則 LCA 就是 $p$ 本身，回傳 `node`（$=p$）正確。若 $q$ 不在其中，則此子樹只含 $p$，依語意也該回傳 $p$。兩種情形都對，故可以提前返回、不必再往下找。**這一步依賴「$p$ 與 $q$ 都確實存在於樹中」的前提**；若某個節點可能不存在，這個提前返回會給出錯誤答案（回傳 $p$ 卻宣稱是 LCA），必須改成完整遍歷並額外回報是否兩者都找到。\n- 遞迴情形：由歸納假設，`l`、`r` 各自符合語意。\n  - `l` 與 `r` 皆非空：表示左子樹含其中一個、右子樹含另一個（各自不可能同時含兩個，否則另一邊就會是空）。故 `node` 同時是兩者的祖先，且沒有更深的公共祖先——任何更深的節點只會落在單側，不可能同時是兩者的祖先。回傳 `node` 正確。\n  - 恰一非空：整棵子樹中只在該側找到東西。若該側回傳的是 LCA（兩者都在該側），向上傳遞不改變答案；若該側只含一個，則本子樹也只含一個。兩種情形都該回傳那個非空值，與語意一致。\n  - 皆空：本子樹不含 $p$ 也不含 $q$，回傳 `nullptr`。\n- 對根呼叫時，由於兩者都在樹中，落入「皆非空」或「node 自己是其中一個」的情形，回傳的必是真正的 LCA。\n\n上面「恰一非空」的分支之所以能不加區分地向上傳遞，正是三合一語意的價值：呼叫者不需要知道那個非空值到底是「LCA」還是「其中一個」，因為兩種情形下的正確動作相同。\n\n**BST 版（235）的正確性。** 利用 BST 性質：若 $p$、$q$ 的值都小於 `node->val`，兩者必在左子樹（BST 定義），故 LCA 也在左子樹，可以直接往左走而不必搜右邊；都大則往右；否則（一小一大，或其中之一等於 `node->val`）當前節點就是分歧點，即 LCA。這裡的推論「兩者都在左子樹 ⟹ LCA 在左子樹」成立，是因為 LCA 必是兩者的公共祖先，而左子樹外的節點若是兩者祖先則 `node` 更近或相等——嚴格說，`node` 是公共祖先但深度較小，故 LCA 必在左子樹內。這使複雜度從 $O(n)$ 降到 $O(h)$。\n\n## 逐步演算法\n\n把 LCA 題拆成三步：\n\n:::example 一般樹：回傳值的語意就是「這棵子樹貢獻的 LCA 候選」\n**1. 狀態表示：DFS 回傳「子樹裡找到的目標 / 候選」。** 一般樹用「回傳找到的節點，沒找到回 `nullptr`」；BST 則靠值比較直接定位，連回傳都省了。\n\n```cpp\n// 一般樹：回傳值的語意就是「這棵子樹貢獻的 LCA 候選」\nTreeNode* dfs(TreeNode* node, TreeNode* p, TreeNode* q);\n```\n:::\n\n\n\n:::example 合併規則（236 的核心兩行）\n**2. 更新流程：自底向上合併左右回報。** 左右都非空 → 自己是 LCA；否則把非空那邊上傳。這套邏輯對 236 直接適用。\n\n```cpp\n// 合併規則（236 的核心兩行）\nif (L && R) {\n    return node;  // 分岔點\n}\nreturn L ? L : R;  // 單側上傳\n```\n:::\n\n\n\n:::example 1123：歸時回傳 {子樹最大深度, 該深度的 LCA}\n**3. 處理變形題。** 1123「最深葉節點的最近公共祖先」需要同時回傳「子樹深度」與「該深度下的 LCA」，在「歸」時依左右深度合併（這其實和直徑類的雙回傳一脈相承）。1644 / 1650 是 Premium 變形：1644 要求 `p`、`q` 不一定都存在（需先確認兩者都在），1650 給了父指標（可從兩節點各自往上爬、用「兩指針相遇」找交點）。\n\n```cpp\n// 1123：歸時回傳 {子樹最大深度, 該深度的 LCA}\npair<int, TreeNode*> dfs(TreeNode* node) {\n    if (!node) {\n        return {0, nullptr};\n    }\n    auto [ld, ln] = dfs(node->left);\n    auto [rd, rn] = dfs(node->right);\n    if (ld == rd) {\n        return {ld + 1, node};  // 左右等深 → 自己是 LCA\n    }\n    return ld > rd ? pair{ld + 1, ln} : pair{rd + 1, rn};\n}\n```\n:::\n\n\n\n**邊界與複雜度：** 注意「一個節點是否可為另一個的祖先」（題目通常允許 LCA 等於 `p` 或 `q` 本身）；1644 要先確認兩節點都存在才回傳。時間 `O(n)`（235 為 `O(h)`），空間 `O(h)`。\n\n## C++17 模板\n\n```cpp\n// 二元樹的 LCA（LeetCode 236）。前提：p 與 q 都存在於樹中。\nTreeNode* lowestCommonAncestor(TreeNode* root, TreeNode* p, TreeNode* q) {\n  if (!root || root == p || root == q) { return root; }   // 找到一個就上傳\n\n  TreeNode* l = lowestCommonAncestor(root->left,  p, q);\n  TreeNode* r = lowestCommonAncestor(root->right, p, q);\n\n  if (l && r) { return root; }    // 分居兩側，當前節點即 LCA\n  return l ? l : r;               // 只有一側有，原樣上傳\n}\n```\n\nBST 版（LeetCode 235），$O(h)$：\n\n```cpp\nTreeNode* lowestCommonAncestorBST(TreeNode* root, TreeNode* p, TreeNode* q) {\n  while (root) {\n    if (p->val < root->val && q->val < root->val)      { root = root->left; }\n    else if (p->val > root->val && q->val > root->val) { root = root->right; }\n    else { return root; }         // 分歧點（含其中之一等於 root）即 LCA\n  }\n  return nullptr;\n}\n```\n\n## 時間與空間複雜度\n\n二元樹版：最壞需訪問每個節點一次，$O(n)$ 時間、$O(h)$ 堆疊空間。有提前返回，實際常更快，但最壞界不變。\n\nBST 版：每步下降一層，$O(h)$ 時間；迭代寫法 $O(1)$ 空間。平衡時 $h = O(\\log n)$。\n\n若要**多次查詢**，$O(n)$ 每次就太慢，應改用倍增（3.8）預處理 $O(n \\log n)$、查詢 $O(\\log n)$，或歐拉序加稀疏表達成 $O(n \\log n)$ 預處理、$O(1)$ 查詢。單次查詢用本節，多次查詢用 3.8——這是選擇的分水嶺。\n\n## 常見錯誤與邊界條件\n\n- 假設 $p$、$q$ 必定存在卻用在可能不存在的題目上（1644 明確要求處理不存在的情形，必須改成統計找到幾個）。\n- 比較節點時用 `->val` 而樹中有重複值（應比指標）。\n- `l && r` 的分支寫成 `l || r`。\n- BST 版忘記「其中之一等於 `node->val`」也算分歧點而繼續下降，越過真正的 LCA。\n- 1650 給的是帶父指標的節點，此時應改用「兩指標走完自己換走對方」的技巧（與 160 相交串列同構），不要套本節模板。\n- $p == q$ 的情形（LCA 是自己）。\n- 空樹。\n\n## 與相似技巧的比較\n\n- 1650（帶父指標）等價於兩條「向上到根」的鏈求交點，用 160 的雙指標換軌法 $O(h)$ 時間、$O(1)$ 空間，比本節更簡單——**有父指標時不要用遞迴版**。\n- 1123（最深葉節點的 LCA）需要同時回傳深度與 LCA，是狀態擴充的例子：回傳 `pair<深度, 節點>`，深度較大的一側直接上傳，相等時當前節點即答案。\n- 倍增 LCA（3.8）處理多次查詢；Tarjan 離線 LCA 用並查集可做到近線性，但只能離線。\n- 236 的三合一語意與 2.3 一般的後序彙總同構，只是彙總的是「指標」而非數值。\n\n## 代表例題\n\n**題號**：235、236、1123、1644、1650\n\n放在「最近公共祖先」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 235 | [二叉搜索樹的最近公共祖先](https://leetcode.cn/problems/lowest-common-ancestor-of-a-binary-search-tree/) |  |\n| 236 | [二叉樹的最近公共祖先](https://leetcode.cn/problems/lowest-common-ancestor-of-a-binary-tree/) |  |\n| 1123 | [最深葉節點的最近公共祖先](https://leetcode.cn/problems/lowest-common-ancestor-of-deepest-leaves/) | 1607 |\n| 1644 | [二叉樹的最近公共祖先 II](https://leetcode.cn/problems/lowest-common-ancestor-of-a-binary-tree-ii/)（Premium） |  |\n| 1650 | [二叉樹的最近公共祖先 III](https://leetcode.cn/problems/lowest-common-ancestor-of-a-binary-tree-iii/)（Premium） |  |\n| 1676 | [二叉樹的最近公共祖先 IV](https://leetcode.cn/problems/lowest-common-ancestor-of-a-binary-tree-iv/) |  |\n| 1740 | [找到二叉樹中的距離](https://leetcode.cn/problems/find-distance-in-a-binary-tree/) |  |\n| 2096 | [從二叉樹一個節點到另一個節點每一步的方向](https://leetcode.cn/problems/step-by-step-directions-from-a-binary-tree-node-to-another/) | 1805 |\n\n## 本節重點速查\n\n- 回傳值是**三合一語意**：同時含兩者回傳 LCA、只含一個回傳那一個、都不含回傳空。\n- 左右都非空則自己是 LCA，否則原樣上傳。\n- 提前返回依賴「$p$、$q$ 都存在」這個前提。\n- BST 版靠值的大小關係一路下降到分歧點，$O(h)$。\n- 多次查詢改用倍增（3.8）。\n",
        },
        {
          id: 23,
          title: "2.9 二叉搜索樹",
          summary:
            "## 這個技術解決什麼問題\n\n二元搜尋樹（BST）用結構把「有序」編碼進樹裡：**左子樹全部小於根，右子樹全部大於根**（對整棵子樹成立，不只對直接子節點）。於是查找、插入、刪除都能沿著一條路徑完成，$O(h)$ 而非 $O(n)$。\n\n本節處理的是「利用 BST 性質」的題型：驗證是否為 BST（98）、找第 $k$ 小（230）、修復被交換的兩個節點（99）、找最接近的值（270）。它們的共同槓桿是同一條性質的兩種等價表述——**遞迴的區間約束**，與**中序遍歷遞增**。選對表述，題目就化簡了。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就往「二元搜尋樹」想：\n\n- **題目明說是 BST，或保證「左小右大」**——立刻聯想「中序＝升序」與「往下定位」兩大武器。\n- **要按值的大小順序處理**（第 k 小、累加和、驗證遞增、找逆序對）→ 中序走訪 + 維護「上一個值」。\n- **要找某個值、前驅 / 後繼、或最接近的值** → 從根往下、每步排除半棵樹，`O(h)` 解決。\n- 暴力做法常是「把所有節點值倒進陣列再排序」`O(n log n)`，或「對每個查詢都全樹掃描」；善用 BST 性質可降到 `O(n)`（中序）或 `O(h)`（定位），且常常不必額外開陣列。\n\n確認自己有沒有套對，問三個問題：\n\n1. 這棵樹保證是 BST 嗎？我能不能用上「中序＝升序」？\n2. 我該「按順序走全部節點（中序）」還是「往下定位單一目標（比大小）」？\n3. 若用中序，我是不是只要記「上一個值」就夠，不必存整個陣列？\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n**延伸講解**：[驗證二叉搜尋樹【基礎演算法精講 11】](https://www.bilibili.com/video/BV14G411P7C1/)\n\n本節要解決的場景是：處理「二元搜尋樹（Binary Search Tree, BST）」。BST 是一種有特殊規則的二元樹——**每個節點，左子樹所有值都比它小，右子樹所有值都比它大**。這條規則看似簡單，卻帶來一個威力強大的性質：\n\n> 對 BST 做「中序走訪（左 → 自己 → 右）」，得到的值序列一定是**由小到大嚴格遞增**的。\n\n:::example BST 的中序走訪 → 值由小到大冒出來\n幾乎所有 BST 題目，都是在反覆利用「左小右大」或「中序＝升序」這兩件事。例如 98「驗證二元搜尋樹」要檢查一棵樹是不是合法 BST；230「BST 中第 K 小的元素」要找第 k 小；270「最接近的 BST 值」要找離目標最近的值。\n\n```cpp\n// BST 的中序走訪 → 值由小到大冒出來\nvoid inorder(TreeNode* node) {\n    if (!node) {\n        return;\n    }\n    inorder(node->left);  // 先把更小的都走完\n    visit(node->val);     // 此刻拿到的就是「下一個最小值」\n    inorder(node->right);\n}\n```\n:::\n\n\n\n:::example 98 驗證 BST：中序時若沒有嚴格遞增，就不是 BST\n有了「中序＝升序」，很多題目立刻變簡單：第 k 小，就是中序走訪數到第 k 個；驗證 BST，就是檢查中序序列是否嚴格遞增（只要比對「上一個訪問到的值」即可，不必存整個陣列）。\n\n```cpp\n// 98 驗證 BST：中序時若沒有嚴格遞增，就不是 BST\nlong prev = LONG_MIN;  // 上一個中序訪問到的值\nbool ok = true;\nvoid check(TreeNode* node) {\n    if (!node || !ok) {\n        return;\n    }\n    check(node->left);\n    if (node->val <= prev) {\n        ok = false;  // 沒比前一個大 → 違規\n    }\n    prev = node->val;\n    check(node->right);\n}\n```\n:::\n\n\n\n另一個常用招式是「往下定位」：要找某個值或最接近的值，從根出發，比目前小就往左、比目前大就往右，每一步排除半棵樹，`O(h)` 到位（`h` 為樹高）。\n\n## 狀態／資料結構定義\n\n兩種表述各自對應一組狀態：\n\n**區間約束法**（適合 98、255）：`dfs(node, lo, hi)`，語意是「以 `node` 為根的子樹，所有值必須嚴格落在開區間 $(lo, hi)$ 內」。往左走時上界收緊為 `node->val`，往右走時下界收緊為 `node->val`。根的初始區間是 $(-\\infty, +\\infty)$，用 `long long` 的極值或 `optional` 表示，**不能用 `INT_MIN`／`INT_MAX`**，因為節點值本身可能等於它們。\n\n**中序遞增法**（適合 98、230、99）：只需一個變數 `prev`，記錄中序序列中前一個訪問到的節點（初始為空）。訪問到 `node` 時檢查 `prev->val < node->val`。230 則再加一個計數器。\n\n## 不變量或正確性證明\n\n**兩種表述的等價性。** BST 定義（每棵子樹的左邊全小於根、右邊全大於根）等價於「中序遍歷嚴格遞增」。\n\n（⟹）由 2.1 已證：中序輸出「左子樹、根、右子樹」，由歸納左右各自遞增，又左子樹全小於根、根小於右子樹全部，故串接後嚴格遞增。\n\n（⟸）反之，設中序嚴格遞增。對任一節點 $v$，其左子樹的節點在中序中全部排在 $v$ 之前，故全部小於 $v$；右子樹全部排在之後，故全部大於 $v$。這對每個 $v$ 成立，即 BST 定義。\n\n這個等價性是本節的核心工具：**看到「BST」就可以自由切換成「一個遞增序列」來思考**。230（第 $k$ 小）在序列觀點下就是「取第 $k$ 個」，中序走到第 $k$ 個即可停止，根本不需要想樹。\n\n**98 的常見錯誤與區間約束的必要性。** 只檢查 `node->left->val < node->val < node->right->val` 是不夠的——這只保證了直接子節點的關係。反例：根為 5，左子為 1，左子的右子為 6。每一對父子關係都成立，但 6 在根的左子樹中卻大於 5，不是 BST。區間約束法正是為了修這個漏洞：**約束必須從所有祖先繼承下來，而非只看父親**。不變量表述為：呼叫 `dfs(v, lo, hi)` 時，$(lo, hi)$ 恰是 $v$ 的所有祖先施加的約束之交集。歸納保持：往左遞迴時新上界是 `v->val`（因為左子樹必須小於 $v$），與繼承的 `lo` 合起來就是子節點的完整約束。\n\n**99（修復兩個被交換的節點）的正確性。** 對正確的 BST 做中序遍歷得嚴格遞增序列；交換其中兩個元素後，序列中出現「逆序對」。關鍵論證是：\n\n- 若被交換的兩個位置**不相鄰**，恰好產生兩處相鄰逆序（`prev > cur`）。第一處的 `prev` 是第一個錯位元素，第二處的 `cur` 是第二個錯位元素。\n- 若**相鄰**，只產生一處逆序，此時第一個錯位是該處的 `prev`、第二個是該處的 `cur`。\n\n因此演算法是：中序掃描，第一次遇到 `prev > cur` 時記下 `first = prev` 與 `second = cur`；之後每次再遇到就更新 `second = cur`。最後交換 `first` 與 `second` 的值。這個「第一次記兩個、之後只更新第二個」的寫法同時涵蓋了相鄰與不相鄰兩種情形——**這正是它為什麼長成那樣，不是隨手寫的**。\n\n**230 的提前終止**：中序訪問到第 $k$ 個時即可回傳，不必走完，故實際成本是 $O(h + k)$。若題目允許預處理且會多次查詢，改在每個節點存「子樹大小」，查詢降到 $O(h)$。\n\n## 逐步演算法\n\n把 BST 題拆成三步：\n\n:::example 兩條主路線\n**1. 先問：這題該用「中序升序」還是「往下定位」？** 要「按大小順序處理全部節點」（驗證、第 k 小、累加）→ 中序；要「找單一目標 / 最接近值」→ 往下定位。\n\n```cpp\n// 兩條主路線\n// (A) 中序走訪：按升序逐一處理節點\n// (B) 從根往下：比大小決定 left / right，O(h) 定位\n```\n:::\n\n\n\n:::example 230 第 K 小：中序計數，數到 k 就是答案\n**2. 中序路線：用「上一個值 prev」省去存陣列。** 230 數到第 k 個就停；98 檢查嚴格遞增；99「恢復 BST」找出中序裡兩個「逆序對」對應的被交換節點。\n\n```cpp\n// 230 第 K 小：中序計數，數到 k 就是答案\nint cnt = 0, ans = -1;\nvoid kth(TreeNode* node, int k) {\n    if (!node || ans != -1) {\n        return;\n    }\n    kth(node->left, k);\n    if (++cnt == k) {\n        ans = node->val;\n        return;\n    }  // 第 k 個冒出來的\n    kth(node->right, k);\n}\n```\n:::\n\n\n\n:::example 270 最接近的 BST 值：往下走，沿途更新最接近者\n**3. 定位路線：每步排除半棵樹。** 270 沿途記錄「目前最接近 target 的值」。\n\n```cpp\n// 270 最接近的 BST 值：往下走，沿途更新最接近者\nint closest(TreeNode* node, double target) {\n    int best = node->val;\n    while (node) {\n        if (abs(node->val - target) < abs(best - target)) {\n            best = node->val;\n        }\n        node = target < node->val ? node->left : node->right;  // 排除半棵\n    }\n    return best;\n}\n```\n:::\n\n\n\n**邊界與複雜度：** 驗證 BST 要小心「只比父子不夠」，必須用「整體上下界」或「中序前一個值」來判斷（左子樹的每個值都要小於祖先，不只小於直接父親）；數值可能到 `INT_MIN/MAX`，prev 用更寬的型別。中序為 `O(n)`，定位為 `O(h)`，空間 `O(h)`。255「驗證前序序列」則用單調堆疊維護「下界」。\n\n## C++17 模板\n\n```cpp\n// 驗證 BST（LeetCode 98）：區間約束法，約束從所有祖先繼承。\nbool isValidBST(TreeNode* node,\n                long long lo = LLONG_MIN, long long hi = LLONG_MAX) {\n  if (!node) { return true; }\n  if (node->val <= lo || node->val >= hi) { return false; }   // 嚴格開區間\n  return isValidBST(node->left,  lo, node->val)               // 往左收緊上界\n      && isValidBST(node->right, node->val, hi);              // 往右收緊下界\n}\n```\n\n中序法（同時可用於 230、99）：\n\n```cpp\n// 第 k 小（LeetCode 230）：中序走到第 k 個就停。\nclass Solution {\n  int cnt_ = 0, ans_ = 0;\n  void dfs(TreeNode* node, int k) {\n    if (!node || cnt_ >= k) { return; }\n    dfs(node->left, k);\n    if (++cnt_ == k) { ans_ = node->val; return; }\n    dfs(node->right, k);\n  }\n public:\n  int kthSmallest(TreeNode* root, int k) { dfs(root, k); return ans_; }\n};\n```\n\n## 時間與空間複雜度\n\n98、99：需檢查每個節點，$O(n)$ 時間、$O(h)$ 空間。\n\n230：$O(h + k)$ 時間（走到第 $k$ 個即停），$O(h)$ 空間。若預先在節點存子樹大小，單次查詢 $O(h)$，但插入刪除時要維護大小。\n\n270（最接近的值）：沿著比較結果單向下降，$O(h)$ 時間、$O(1)$ 空間（迭代版）。\n\n所有 $O(h)$ 在平衡時是 $O(\\log n)$、退化成鏈時是 $O(n)$——**BST 的所有優勢都建立在「樹是平衡的」這個前提上**，這也是實務上要用紅黑樹或 AVL 的原因。單純的 BST 若按有序資料依序插入會退化成鏈。\n\n## 常見錯誤與邊界條件\n\n- 98 只比較直接父子而漏掉跨層違規（上文反例）。\n- 區間邊界用 `INT_MIN`／`INT_MAX` 而節點值恰好等於它們（用 `long long` 或 `optional`）。\n- 比較用 `<=` 或 `>=` 與題目對重複值的約定不符（多數題目要求嚴格遞增，不允許重複）。\n- 99 只記錄一處逆序而漏掉相鄰交換的情形。\n- 230 忘記提前終止導致大樹超時。\n- 中序法的 `prev` 宣告成區域變數而非跨遞迴共享。\n- 空樹（通常視為合法 BST）。\n\n## 與相似技巧的比較\n\n- 區間約束法與中序法都能解 98；前者是自頂向下傳約束（2.2 的模式），後者是「有遞有歸」共享一個 `prev`（2.5 的模式）。中序法對 99、230 更自然，區間法對 255（驗證前序序列）更自然。\n- BST 的 LCA（235，見 2.8）同樣靠值的大小關係下降，$O(h)$，比一般二元樹的 $O(n)$ 快。\n- 需要動態插入刪除並保持平衡時，競賽用 `std::set`／`std::map`（紅黑樹）或平衡樹模板；LeetCode 的 BST 題通常不要求自己實作平衡。\n- 「BST 中序即有序陣列」這條對應關係反過來也有用：108 題由有序陣列建平衡 BST（見 2.10），就是把中序序列切成「中點當根、左右遞迴」。\n\n## 代表例題\n\n**題號**：98、99、230、255、270\n\n放在「二叉搜索樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 98 | [驗證二叉搜索樹](https://leetcode.cn/problems/validate-binary-search-tree/) |  |\n| 99 | [恢復二叉搜索樹](https://leetcode.cn/problems/recover-binary-search-tree/) |  |\n| 230 | [二叉搜索樹中第 K 小的元素](https://leetcode.cn/problems/kth-smallest-element-in-a-bst/) |  |\n| 255 | [驗證二叉搜索樹的前序遍歷序列](https://leetcode.cn/problems/verify-preorder-sequence-in-binary-search-tree/)（Premium） |  |\n| 270 | [最接近的二叉搜索樹值](https://leetcode.cn/problems/closest-binary-search-tree-value/)（Premium） |  |\n| 272 | [最接近的二叉搜索樹值 II](https://leetcode.cn/problems/closest-binary-search-tree-value-ii/) |  |\n| 285 | [二叉搜索樹中的中序後繼](https://leetcode.cn/problems/inorder-successor-in-bst/) |  |\n| 501 | [二叉搜索樹中的眾數](https://leetcode.cn/problems/find-mode-in-binary-search-tree/) |  |\n| 510 | [二叉搜索樹中的中序後繼 II](https://leetcode.cn/problems/inorder-successor-in-bst-ii/) |  |\n| 530 | [二叉搜索樹的最小絕對差](https://leetcode.cn/problems/minimum-absolute-difference-in-bst/) |  |\n| 653 | [兩數之和 IV - 輸入二叉搜索樹](https://leetcode.cn/problems/two-sum-iv-input-is-a-bst/) |  |\n| 700 | [二叉搜索樹中的搜尋](https://leetcode.cn/problems/search-in-a-binary-search-tree/) |  |\n| 783 | [二叉搜索樹節點最小距離](https://leetcode.cn/problems/minimum-distance-between-bst-nodes/) |  |\n| 897 | [遞增順序搜尋樹](https://leetcode.cn/problems/increasing-order-search-tree/) | 1473 |\n| 938 | [二叉搜索樹的範圍和](https://leetcode.cn/problems/range-sum-of-bst/) | 1335 |\n| 1305 | [兩棵二叉搜索樹中的所有元素](https://leetcode.cn/problems/all-elements-in-two-binary-search-trees/) | 1260 |\n| 1373 | [二叉搜尋子樹的最大鍵值和](https://leetcode.cn/problems/maximum-sum-bst-in-binary-tree/) | 1914 |\n| 1902 | [給定二叉搜索樹的插入順序求深度](https://leetcode.cn/problems/depth-of-bst-given-insertion-order/) |  |\n| 1932 | [合併多棵二叉搜索樹](https://leetcode.cn/problems/merge-bsts-to-create-single-bst/) | 2484 |\n| 2476 | [二叉搜索樹最近節點查詢](https://leetcode.cn/problems/closest-nodes-queries-in-a-binary-search-tree/) | 1597 |\n\n## 本節重點速查\n\n- BST 的兩種等價表述：**遞迴區間約束**與**中序嚴格遞增**，選好用的那個。\n- 98 必須用從祖先繼承的區間（只比父子會漏），邊界用 `long long`。\n- 230 中序走 $k$ 步即停。\n- 99 靠「第一次記兩個、之後只更新第二個」同時涵蓋相鄰與不相鄰交換。\n- 所有 $O(h)$ 保證都依賴樹是平衡的。\n",
        },
        {
          id: 24,
          title: "2.10 建立二叉樹",
          summary:
            "## 這個技術解決什麼問題\n\n給定遍歷序列還原樹（105 前序＋中序、106 後序＋中序）、由有序陣列建平衡 BST（108）、由陣列建最大二元樹（654）。這些題的共同結構是**分治**：想辦法在序列中找出「根」與「左右兩段」，然後遞迴。\n\n難點有二。其一，單一種遍歷序列**無法唯一確定一棵樹**——前序 `[1,2]` 既可能是 1 的左子為 2，也可能是右子。必須有第二種序列提供切分資訊。其二，若每層都用線性搜尋找根在中序中的位置，總成本 $O(n^2)$；用雜湊表把位置預先建好，降到 $O(n)$。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就往「建立二元樹」想：\n\n- **題目給的是「描述」而非現成的樹**：兩種走訪序列、有序陣列、含括號的字串、或一段數列要你「按某規則造樹」。\n- **能在資料裡定出「誰是根」並把剩餘切成左右兩塊**——這就是分治建樹的核心。\n- **想建出「平衡」或「有特定結構」的樹**：如 108 取中點保平衡、654 取最大值定根。\n- 暴力做法常是「每層都 substring / 複製子陣列」造成 `O(n^2)` 甚至更糟；改用邊界索引 + 雜湊表能壓到 `O(n)`。\n\n確認自己有沒有套對，問三個問題：\n\n1. 在這段資料裡，「根」要怎麼一眼定出來（最前？最後？中間？最大？）？\n2. 定了根之後，左子樹的料和右子樹的料各是哪一段（邊界索引算對了嗎）？\n3. 我有沒有避免「每層複製陣列」，改用區間下標（必要時配雜湊表）？\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n本節要解決的場景是：給你某種「描述樹的資料」，要你**把樹真正建出來**（產生一堆節點、接好左右指標）。代表題有 105「從前序與中序建二元樹」、106「從中序與後序建」、108「把有序陣列轉成 BST」、654「最大二元樹」。\n\n:::example 建樹通用骨架：定根 → 切左右 → 遞迴 → 接上\n所有建樹題的共同骨架是「**遞迴分治**」：先在資料裡找到「誰是這一段的根」，再把剩下的資料切成「左子樹的料」和「右子樹的料」，分別遞迴建好後，掛到根的左右指標上。難點只有一個——**怎麼從給定資料判斷出根、以及如何切分左右**。\n\n```cpp\n// 建樹通用骨架：定根 → 切左右 → 遞迴 → 接上\nTreeNode* build(/* 某段資料 */) {\n    if (/* 這段空了 */) {\n        return nullptr;  // 沒料 → 空子樹\n    }\n    TreeNode* root = new TreeNode(/* 找出的根值 */);\n    root->left = build(/* 左半段料 */);   // 遞迴建左子樹\n    root->right = build(/* 右半段料 */);  // 遞迴建右子樹\n    return root;\n}\n```\n:::\n\n\n\n不同題目的差別，只在「根怎麼找」「左右怎麼切」：\n\n- 105：**前序的第一個**就是根；拿這個根值去**中序**裡找位置，位置左邊是左子樹、右邊是右子樹（中序的特性：根把序列切成左右兩段）。\n- 106：**後序的最後一個**是根，其餘同理用中序切分。\n- 108：有序陣列**正中間**當根（這樣左右一樣多，樹才平衡），左半段、右半段遞迴。\n- 654：**區間裡的最大值**當根，最大值左邊、右邊各自遞迴。\n\n:::example 108：取中點當根，自然建出平衡 BST\n```cpp\n// 108：取中點當根，自然建出平衡 BST\nTreeNode* build(vector<int>& a, int lo, int hi) {\n    if (lo > hi) {\n        return nullptr;\n    }\n    int mid = (lo + hi) / 2;  // 中點當根 → 左右節點數最接近\n    TreeNode* root = new TreeNode(a[mid]);\n    root->left = build(a, lo, mid - 1);\n    root->right = build(a, mid + 1, hi);\n    return root;\n}\n```\n:::\n\n## 狀態／資料結構定義\n\n以 105（前序＋中序）為例，子問題用**四個下標**界定，這是本節最容易寫錯的地方：\n\n- `build(preL, preR, inL, inR)`：前序區間 $[preL, preR]$ 與中序區間 $[inL, inR]$ 描述**同一棵子樹**，兩段長度必然相等。\n- `pos`：`unordered_map<int, int>`，中序中每個值的下標，用來 $O(1)$ 定位根。**前提是節點值互不相同**，題目若允許重複就不能這樣做（事實上有重複值時樹本身就無法唯一還原）。\n- 切分：根是 `preorder[preL]`；設 `k = pos[根]`，則左子樹在中序中佔 $[inL, k-1]$，長度 `sz = k - inL`；左子樹在前序中佔 $[preL+1, preL+sz]$，右子樹佔 $[preL+sz+1, preR]$。\n\n108 的狀態只需 $[l, r]$；654 需要在區間內找最大值的位置（可用單調堆疊優化成 $O(n)$）。\n\n## 不變量或正確性證明\n\n**唯一性：前序＋中序可唯一確定二元樹（值互異時）。** 用對節點數的強歸納。\n\n- 基底：空序列對應空樹，唯一。\n- 歸納：前序的第一個元素必為根（前序定義為「根、左、右」）。設它在中序中的位置為 $k$（值互異保證位置唯一）。由中序定義（「左、根、右」），中序中位於 $k$ 左邊的元素恰為左子樹的所有節點，右邊恰為右子樹的所有節點。於是左子樹的節點集合被唯一確定，其大小 `sz` 也隨之確定。再由前序定義，前序中緊接根之後的 `sz` 個元素恰為左子樹的前序、其餘為右子樹的前序。因此兩棵子樹各自的（前序，中序）配對被唯一確定，由歸納假設各自唯一，故整棵樹唯一。\n\n**這個論證明確依賴「值互異」**。若有重複值，$k$ 的選擇不唯一，不同選擇會給出不同的樹——不是演算法沒寫好，是問題本身無解。\n\n同理，後序＋中序可唯一確定（後序的**最後**一個元素是根）。但**前序＋後序無法唯一確定**：對只有一個子節點的節點，前序與後序都無法區分它是左子還是右子。這是為什麼 889 題只要求「回傳任意一棵符合的樹」。\n\n**下標推導的驗證。** 兩段長度必須始終相等：初始 $preR - preL = inR - inL$。左子呼叫用 $[preL+1, preL+sz]$ 與 $[inL, k-1]$，長度分別為 $sz - 1$ 與 $k - 1 - inL = sz - 1$，相等。右子呼叫用 $[preL+sz+1, preR]$ 與 $[k+1, inR]$，長度分別為 $preR - preL - sz - 1$ 與 $inR - k - 1$；代入 $k = inL + sz$ 與初始等式即得兩者相等。這個檢查是除錯下標最快的方法：**任何一步若兩段長度不等，該步的推導就錯了**。\n\n**108 建平衡 BST 的正確性與平衡性。** 取區間中點作根，左半遞迴為左子樹、右半為右子樹。BST 性質成立：左半全部小於中點、右半全部大於中點（輸入有序），且遞迴保證子樹內部也滿足。平衡性：每次把長度 $m$ 切成 $\\lfloor (m-1)/2 \\rfloor$ 與 $\\lceil (m-1)/2 \\rceil$，兩者相差至多 1，故深度 $T(m) = 1 + T(m/2) = O(\\log m)$，高度差至多 1。中點取 `l + (r-l)/2` 或 `l + (r-l+1)/2` 都可以（產生不同但都合法的樹），題目通常接受任一。\n\n**654 最大二元樹**：區間內最大值為根，左右兩段遞迴。樸素找最大值是 $O(n)$ 每層，最壞 $O(n^2)$（輸入遞增時退化成鏈）；用單調遞減堆疊可 $O(n)$ 建成，原理是「每個元素的父親是它左右兩側第一個比它大的元素中較小的那個」。\n\n## 逐步演算法\n\n把建樹題拆成三步：\n\n:::example 105：前序首元素即為根值\n**1. 想清楚「根」藏在資料的哪裡。** 前序看最前、後序看最後、有序陣列看中間、最大樹看最大值。\n\n```cpp\n// 105：前序首元素即為根值\nint rootVal = preorder[preStart];\n```\n:::\n\n\n\n:::example 105：用 inorder 值→索引的表，O(1) 找到根在中序的位置\n**2. 用「邊界索引」遞迴，避免狂複製陣列。** 不要每層都切出新 vector（很慢又耗記憶體），改用 `[lo, hi]` 區間下標。105 / 106 還要先用一張雜湊表記「中序每個值在哪個位置」，才能 `O(1)` 切分。\n\n```cpp\n// 105：用 inorder 值→索引的表，O(1) 找到根在中序的位置\nunordered_map<int, int> pos;  // 預先：inorder[i] -> i\nTreeNode* build(int preL, int preR, int inL, int inR) {\n    if (preL > preR) {\n        return nullptr;\n    }\n    int rootVal = preorder[preL];\n    int k = pos[rootVal];    // 根在中序的位置\n    int leftSize = k - inL;  // 左子樹有幾個節點\n    TreeNode* root = new TreeNode(rootVal);\n    root->left = build(preL + 1, preL + leftSize, inL, k - 1);\n    root->right = build(preL + leftSize + 1, preR, k + 1, inR);\n    return root;\n}\n```\n:::\n\n\n\n:::example 654：區間最大值當根，左右遞迴\n**3. 算對左右兩段的邊界。** 最容易錯的就是 105 / 106 裡前序 / 後序那一段的切點：要用「左子樹節點數 `leftSize`」去推前序的分界（如上 `preL + leftSize`）。畫個小例子手動對一遍最保險。\n\n```cpp\n// 654：區間最大值當根，左右遞迴\nTreeNode* build(vector<int>& a, int lo, int hi) {\n    if (lo > hi) {\n        return nullptr;\n    }\n    int m = lo;\n    for (int i = lo + 1; i <= hi; ++i) {\n        if (a[i] > a[m]) {\n            m = i;  // 找最大值位置\n        }\n    }\n    TreeNode* root = new TreeNode(a[m]);\n    root->left = build(a, lo, m - 1);\n    root->right = build(a, m + 1, hi);\n    return root;\n}\n```\n:::\n\n\n\n**邊界與複雜度：** 空區間（`lo > hi`）一定回 `nullptr`；題目通常保證值不重複，否則「用值去中序找位置」會出錯。105 / 106 加了雜湊表後是 `O(n)`；108 是 `O(n)`；654 樸素找最大是 `O(n^2)`（可用單調堆疊優化）。空間 `O(h)` 遞迴 + 必要的索引表。536（從字串生成二元樹，Premium）則是先解析括號結構再遞迴建。\n\n## C++17 模板\n\n```cpp\n// 由前序與中序還原二元樹（LeetCode 105）。前提：節點值互不相同。\nclass Solution {\n  std::vector<int> pre_, in_;\n  std::unordered_map<int, int> pos_;      // 中序值 -> 下標，O(1) 定位根\n\n  TreeNode* build(int preL, int preR, int inL, int inR) {\n    if (preL > preR) { return nullptr; }              // 空區間\n\n    int rootVal = pre_[preL];                         // 前序首元素是根\n    int k = pos_[rootVal];                            // 它在中序中的位置\n    int sz = k - inL;                                 // 左子樹的節點數\n\n    TreeNode* root = new TreeNode(rootVal);\n    root->left  = build(preL + 1,      preL + sz, inL,     k - 1);\n    root->right = build(preL + sz + 1, preR,      k + 1,   inR);\n    return root;\n  }\n\n public:\n  TreeNode* buildTree(std::vector<int>& preorder, std::vector<int>& inorder) {\n    pre_ = preorder;\n    in_ = inorder;\n    for (int i = 0; i < static_cast<int>(in_.size()); ++i) { pos_[in_[i]] = i; }\n    return build(0, pre_.size() - 1, 0, in_.size() - 1);\n  }\n};\n```\n\n有序陣列建平衡 BST（LeetCode 108）：\n\n```cpp\nTreeNode* build(std::vector<int>& a, int l, int r) {\n  if (l > r) { return nullptr; }\n  int m = l + (r - l) / 2;                 // 取中點保證高度差不超過 1\n  TreeNode* root = new TreeNode(a[m]);\n  root->left  = build(a, l, m - 1);\n  root->right = build(a, m + 1, r);\n  return root;\n}\n```\n\n## 時間與空間複雜度\n\n105／106：每個節點建立一次、雜湊表查一次，$O(n)$ 時間；雜湊表 $O(n)$ 空間加遞迴堆疊 $O(h)$。若改用線性搜尋找根，退化成 $O(n^2)$（斜樹時每層都掃全長）。\n\n108：$T(n) = 2T(n/2) + O(1) = O(n)$ 時間，$O(\\log n)$ 堆疊。\n\n654：樸素 $O(n^2)$ 最壞、$O(n \\log n)$ 平均；單調堆疊版 $O(n)$。\n\n536（由字串還原）需解析括號，$O(n)$ 時間。\n\n## 常見錯誤與邊界條件\n\n- 四個下標推導錯誤（用「兩段長度必須相等」自檢）。\n- 忘記空區間出口 `preL > preR`。\n- 有重複值時仍用雜湊表定位（此時題目本身無唯一解）。\n- 106 誤以為後序的第一個是根（是最後一個）。\n- 把左右子樹的前序區間切反。\n- 108 對空陣列呼叫 `build(a, 0, -1)` 未處理。\n- 654 用樸素找最大值而在遞增輸入下超時。\n- 雜湊表在每層遞迴重建而非預先建好。\n- 未釋放建樹失敗時的部分節點。\n\n## 與相似技巧的比較\n\n- 297（序列化與反序列化）走的是另一條路：在序列中**顯式標記空節點**，於是單一種遍歷就足以還原，不需要第二種序列。這說明「需要兩種序列」的根本原因是空節點資訊遺失，補上它就不必了。\n- 889（前序＋後序）只能回傳任意合法解，理由如上——單子節點的左右歸屬無法區分。\n- 108 與 109（有序鏈建 BST）思路相同，但鏈無法 $O(1)$ 取中點；可用快慢指標每層找中點（$O(n \\log n)$），或用「中序模擬」的巧解達成 $O(n)$，見 2.14。\n- 分治的骨架與 1.9 的歸併排序同構：都是切分、遞迴、合併，差別在這裡的「合併」是建立父子關係而非合併有序鏈。\n\n## 代表例題\n\n**題號**：105、106、108、536、654\n\n放在「建立二叉樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 105 | [從前序與中序遍歷序列構造二叉樹](https://leetcode.cn/problems/construct-binary-tree-from-preorder-and-inorder-traversal/) |  |\n| 106 | [從中序與後序遍歷序列構造二叉樹](https://leetcode.cn/problems/construct-binary-tree-from-inorder-and-postorder-traversal/) |  |\n| 108 | [將有序陣列轉換為二叉搜索樹](https://leetcode.cn/problems/convert-sorted-array-to-binary-search-tree/) |  |\n| 536 | [從字串生成二叉樹](https://leetcode.cn/problems/construct-binary-tree-from-string/)（Premium） |  |\n| 654 | [最大二叉樹](https://leetcode.cn/problems/maximum-binary-tree/) |  |\n| 889 | [根據前序和後序遍歷構造二叉樹](https://leetcode.cn/problems/construct-binary-tree-from-preorder-and-postorder-traversal/) | 1732 |\n| 998 | [最大二叉樹 II](https://leetcode.cn/problems/maximum-binary-tree-ii/) | 1498 |\n| 1008 | [前序遍歷構造二叉搜索樹](https://leetcode.cn/problems/construct-binary-search-tree-from-preorder-traversal/) | 1563 |\n| 1028 | [從先序遍歷還原二叉樹](https://leetcode.cn/problems/recover-a-tree-from-preorder-traversal/) | 1797 |\n| 1382 | [將二叉搜索樹變平衡](https://leetcode.cn/problems/balance-a-binary-search-tree/) | 1541 |\n| 1597 | [根據中綴表示式構造二叉表示式樹](https://leetcode.cn/problems/build-binary-expression-tree-from-infix-expression/) |  |\n| 1628 | [設計帶解析函式的表示式樹](https://leetcode.cn/problems/design-an-expression-tree-with-evaluate-function/) |  |\n| 2196 | [根據描述建立二叉樹](https://leetcode.cn/problems/create-binary-tree-from-descriptions/) | 1644 |\n\n## 本節重點速查\n\n- 分治三步：定根、切分、遞迴。\n- 前序＋中序（或後序＋中序）在**值互異**時唯一確定樹；\n- 前序＋後序不行。\n- 用雜湊表 $O(1)$ 定位根，否則退化 $O(n^2)$。\n- 四個下標用「兩段長度必相等」自檢。\n- 108 取中點建樹自然平衡。\n",
        },
        {
          id: 25,
          title: "2.11 插入/刪除節點",
          summary:
            "## 這個技術解決什麼問題\n\n在 BST 上插入（701）、刪除（450）、修剪到指定值域（669）。這些操作都必須在改動結構之後**仍然是一棵 BST**——這才是難點所在。插入相對容易（總能掛在某個空位），刪除則棘手：拿掉一個有兩個子節點的內部節點後，會留下一個必須被「合法填補」的洞。\n\n槓桿在於 BST 的中序即有序序列（2.9）：**刪除一個節點後，只要用它在中序序列中的鄰居（前驅或後繼）補位，序列的有序性就自動維持**，因此 BST 性質也維持。這把一個看似要重建的問題化簡成一次替換。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「BST 插入/刪除」想：\n\n- **題目明說是二叉搜尋樹，要求加入、移除、修剪某個值或某個範圍**，而且改完還要維持 BST 性質。Leetcode 450、669、701 都是。\n- **你發現「比大小決定左右走向」能省下大量比較**——這正是 BST 存在的理由：\n\n:::example 嗅到味道的瞬間：能用「大小」直接決定方向，不必整棵掃\n```cpp\n// 嗅到味道的瞬間：能用「大小」直接決定方向，不必整棵掃\n// if (val < root->val) 往左; else 往右;   // 每層砍掉一半\n```\n:::\n\n\n\n- **刪除有兩個小孩讓你卡住**，這就是要用「右子樹最小值頂替」的訊號。\n- **把 BST 拆開或合併**（如 Leetcode 776 拆分、1666 改變根節點），本質還是局部改幾條指標、其餘子樹整段搬移。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我每個遞迴函式回傳的是什麼？（應該是「修改後子樹的根」）\n2. 刪除有兩個小孩時，我的替身是誰？\n3. 我有沒有靠「比大小」決定往左還往右，而不是整棵樹掃一遍？\n\n三題都答得出來，就套對了。\n\n## 核心想法與直覺\n\n本節要解決的場景是：在「二叉搜尋樹（BST）」上插入或刪除一個節點，而且改完之後它還要繼續是一棵合法的 BST。\n\n:::example BST 的查找：比目標小就往右，比目標大就往左\n先複習 BST 是什麼。BST 是一棵二叉樹，它有一條鐵律：**任何節點的左子樹全部比它小，右子樹全部比它大**。這條規律有個好處——找一個值時，只要比較大小就知道往左走還是往右走，平均只要 `O(log n)` 步。\n\n```cpp\n// BST 的查找：比目標小就往右，比目標大就往左\nTreeNode* find(TreeNode* root, int target) {\n    while (root && root->val != target) {\n        root = target < root->val ? root->left : root->right;\n    }\n    return root;  // 找到就回傳該節點，找不到回傳 nullptr\n}\n```\n:::\n\n\n\n:::example 插入：照大小往下走，空位就掛上新節點\n插入很單純：照上面的規則一路往下走，走到底（遇到空位）就把新節點掛在那裡，這個位置一定不會破壞 BST 規律。\n\n```cpp\n// 插入：照大小往下走，空位就掛上新節點\nTreeNode* insert(TreeNode* root, int val) {\n    if (!root) {\n        return new TreeNode(val);  // 找到空位\n    }\n    if (val < root->val) {\n        root->left = insert(root->left, val);\n    } else {\n        root->right = insert(root->right, val);\n    }\n    return root;  // 回傳本層的根，串回上層\n}\n```\n:::\n\n\n\n刪除比較麻煩。如果被刪節點有兩個小孩，不能直接拔掉，否則樹會斷成兩半。解法是找一個「替身」來頂替它：右子樹裡最小的那個節點（它剛好比左子樹全大、又比右子樹其餘全小），把替身的值搬上來，再去右子樹刪掉那個替身即可。Leetcode 450（刪除 BST 中的節點）就是這個。\n\n## 狀態／資料結構定義\n\n一律採用「**遞迴回傳新的子樹根**」的介面（與 2.4 剪枝相同）：\n\n- `insert(node, key)` / `remove(node, key)` 回傳操作後該子樹的根，呼叫端用 `node->left = remove(node->left, key);` 接回去。這個介面讓「根本身被刪除」不必寫特例。\n- 刪除時的三種情形：無子（直接回傳 `nullptr`）、單子（回傳那個子節點）、雙子（找替身）。\n- 替身的定義：**後繼**是右子樹中的最小節點（一路往左走到底），**前驅**是左子樹中的最大節點（一路往右走到底）。\n- 669 修剪的狀態只有 `[low, high]` 兩個界。\n\n## 不變量或正確性證明\n\n**插入的正確性。** 沿著 BST 的比較路徑下降，最終抵達一個空位並在該處建立新節點。要證明「掛在這個空位上仍是 BST」：下降過程中每一步都由比較決定方向，因此對路徑上每個祖先 $u$，新值與 $u$ 的大小關係恰好與「新節點最終落在 $u$ 的哪一側」一致。而非祖先的節點與新節點不存在祖先—後代關係，BST 性質對它們無約束。故整棵樹仍是 BST。注意新節點必定成為**葉節點**，這是這個做法最簡單的地方。\n\n**刪除的正確性（雙子情形）。** 設要刪的節點為 $v$，取其後繼 $s$（右子樹最小者）。宣稱把 $v$ 的值換成 $s$ 的值、再從右子樹中刪除 $s$，結果仍是 BST。\n\n論證分兩步。第一步，$s$ 是中序序列中緊接 $v$ 之後的節點，故 $s$ 的值大於 $v$ 的左子樹全部（它們在中序中都排在 $v$ 之前），也小於 $v$ 的右子樹中除 $s$ 外的全部（它們在中序中都排在 $s$ 之後）。因此把 $v$ 的值替換成 $s$ 的值，對 $v$ 位置上的 BST 約束仍然成立。第二步，$s$ 是右子樹的最小節點，故**它必定沒有左子**（若有，那個左子更小，與最小性矛盾）。於是遞迴刪除 $s$ 時落入「無子」或「單子（只有右子）」的簡單情形，不會無限遞迴。\n\n這第二點是整個做法能終止的關鍵：**雙子情形被化簡成至多單子的情形，遞迴深度嚴格下降**。若隨便選一個右子樹的節點當替身，這個保證就沒了。\n\n**669 修剪的正確性。** 對節點 $v$：若 `v->val < low`，則 $v$ 及其**整棵左子樹**都小於 `low`（BST 性質），全部該刪，故直接回傳 `trim(v->right)`；若 `v->val > high`，對稱地回傳 `trim(v->left)`；否則保留 $v$，並遞迴修剪左右。\n\n這裡最值得點名的是「可以整棵子樹一起丟」——它直接來自 BST 性質，是 $O(h)$ 級別剪枝而非逐點判斷。同時要注意**不能在丟掉左子樹後就停止**：`v->val < low` 時右子樹中仍可能有合格節點，必須繼續遞迴，不能直接回傳 `nullptr`。這是 669 最常見的錯誤。\n\n修剪後結果仍是 BST：保留的節點集合是原集合對值域的一個「區間篩選」，而中序序列的子序列仍然遞增，故仍是 BST（由 2.9 的等價性）。\n\n## 逐步演算法\n\n這類題目可拆成四個固定步驟，逐一搭配程式碼：\n\n:::example 統一介面：每個遞迴回傳「修改後子樹的根」\n**1. 狀態表示：用「回傳新的子樹根」串接修改。** 不要直接去改父節點的指標，而是讓每個遞迴函式回傳「處理後這棵子樹的新根」，父層用 `root->left = f(root->left)` 接回去。這樣插入、刪除、修剪都能統一寫法。\n\n```cpp\n// 統一介面：每個遞迴回傳「修改後子樹的根」\n// root->left  = modify(root->left);\n// root->right = modify(root->right);\n```\n:::\n\n\n\n**2. 插入流程：往下找空位。** 如上面的 `insert`，遞迴到 `nullptr` 就 new 一個節點接上（Leetcode 701）。\n\n:::example 刪除：核心是「兩個小孩」用右子樹最小值頂替\n**3. 刪除流程：分三種情況。** 沒有小孩 → 直接刪；只有一個小孩 → 用那個小孩頂替；有兩個小孩 → 用右子樹最小值當替身。\n\n```cpp\n// 刪除：核心是「兩個小孩」用右子樹最小值頂替\nTreeNode* del(TreeNode* root, int key) {\n    if (!root) {\n        return nullptr;\n    }\n    if (key < root->val) {\n        root->left = del(root->left, key);\n    } else if (key > root->val) {\n        root->right = del(root->right, key);\n    } else {\n        if (!root->left) {\n            return root->right;  // 0 或 1 個小孩\n        }\n        if (!root->right) {\n            return root->left;\n        }\n        TreeNode* s = root->right;  // 找右子樹最小值\n        while (s->left) {\n            s = s->left;\n        }\n        root->val = s->val;                      // 搬替身的值上來\n        root->right = del(root->right, s->val);  // 再刪掉替身\n    }\n    return root;\n}\n```\n:::\n\n\n\n**4. 修剪與邊界。** 修剪 BST（Leetcode 669）是把超出 `[low, high]` 的節點整段砍掉：若節點太小，整個左子樹都太小，直接回傳處理後的右子樹；太大則對稱處理。複雜度都是樹高級別，平均 `O(log n)`、最壞 `O(n)`（退化成鏈）。注意遞迴深度過大可能爆 stack。\n\n## C++17 模板\n\n```cpp\n// BST 刪除節點（LeetCode 450）：回傳新的子樹根。\nTreeNode* deleteNode(TreeNode* root, int key) {\n  if (!root) { return nullptr; }\n\n  if (key < root->val)      { root->left  = deleteNode(root->left,  key); }\n  else if (key > root->val) { root->right = deleteNode(root->right, key); }\n  else {\n    if (!root->left)  { TreeNode* r = root->right; delete root; return r; }\n    if (!root->right) { TreeNode* l = root->left;  delete root; return l; }\n\n    TreeNode* s = root->right;              // 後繼：右子樹最小者\n    while (s->left) { s = s->left; }        // 它必定沒有左子\n    root->val = s->val;                     // 用後繼補位，中序序列仍遞增\n    root->right = deleteNode(root->right, s->val);   // 遞迴刪除，至多單子情形\n  }\n  return root;\n}\n```\n\n修剪到值域（LeetCode 669）：\n\n```cpp\nTreeNode* trimBST(TreeNode* root, int low, int high) {\n  if (!root) { return nullptr; }\n  if (root->val < low)  { return trimBST(root->right, low, high); }  // 左子樹整棵丟\n  if (root->val > high) { return trimBST(root->left,  low, high); }\n  root->left  = trimBST(root->left,  low, high);\n  root->right = trimBST(root->right, low, high);\n  return root;\n}\n```\n\n插入（LeetCode 701）：沿比較路徑下降，`if (!root) return new TreeNode(key);`，其餘接回 `root->left`／`root->right`。\n\n## 時間與空間複雜度\n\n插入、刪除、修剪都是 $O(h)$：插入下降一條路徑；刪除下降一條路徑後再往下找後繼（後繼在右子樹中，兩段合計仍是 $O(h)$）；修剪最壞訪問每個節點 $O(n)$，但實際只會走到值域邊界附近的路徑，若大量節點被整段丟棄則遠快於 $O(n)$。\n\n空間 $O(h)$ 遞迴堆疊，迭代版插入可做到 $O(1)$。\n\n平衡時 $h = O(\\log n)$，退化成鏈時 $O(n)$。純 BST **不保證平衡**：依序插入有序資料會建出一條鏈。要保證 $O(\\log n)$ 必須用 AVL、紅黑樹或 Treap，那需要額外的旋轉操作，超出本節範圍。\n\n## 常見錯誤與邊界條件\n\n- 遞迴後忘記把回傳值接回 `root->left`／`root->right`，樹沒有變化。\n- 669 在 `val < low` 時直接回傳 `nullptr` 而丟掉右子樹中的合格節點。\n- 刪除雙子節點時選了非最小／最大的替身，導致遞迴不終止或破壞 BST。\n- 替換值後忘記從子樹中刪除替身（該值出現兩次）。\n- 刪除根節點後回傳 `root` 而非新根。\n- `delete` 節點後仍存取其欄位（要先取出 `root->right` 再 delete）。\n- 樹中不存在 `key` 時應原樣回傳。\n- 空樹。\n\n## 與相似技巧的比較\n\n- 與 2.4 的批次剪枝相同的是「回傳新根」介面與後序接回；不同的是本節要維持 BST 性質，因此刪除需要替身而非單純移除。\n- `std::set` / `std::map` 提供的就是平衡版本的這些操作，競賽中除非題目要求手寫，一律直接用標準庫。\n- 776（拆分 BST）是修剪的變體：不是丟掉界外節點而是分成兩棵樹，同樣利用「整棵子樹可一起歸類」的性質。\n- 1666（改變二元樹的根）不涉及 BST 性質，是純結構重連，要注意父子關係反轉時原本的父節點要先斷開。\n\n## 代表例題\n\n**題號**：450、669、701、776、1666\n\n放在「插入/刪除節點」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 450 | [刪除二叉搜索樹中的節點](https://leetcode.cn/problems/delete-node-in-a-bst/) |  |\n| 669 | [修剪二叉搜索樹](https://leetcode.cn/problems/trim-a-binary-search-tree/) |  |\n| 701 | [二叉搜索樹中的插入操作](https://leetcode.cn/problems/insert-into-a-binary-search-tree/) |  |\n| 776 | [拆分二叉搜索樹](https://leetcode.cn/problems/split-bst/)（Premium） |  |\n| 1666 | [改變二叉樹的根節點](https://leetcode.cn/problems/change-the-root-of-a-binary-tree/)（Premium） |  |\n\n## 本節重點速查\n\n- 一律用「遞迴回傳新根 + 呼叫端接回賦值」的介面。\n- 刪除雙子節點時用**後繼（右子樹最小者）**補位——它必定無左子，故遞迴化簡成至多單子情形而終止。\n- 669 修剪時可整棵子樹丟棄，但界外節點的**另一側仍要遞迴**。\n- 所有 $O(h)$ 都不保證平衡。\n",
        },
        {
          id: 26,
          title: "2.12 樹形 DP",
          summary:
            "## 這個技術解決什麼問題\n\n2.3 的自底向上處理的是「單一彙總量可由子樹彙總量算出」的情形。但很多題目不滿足這個條件：打家劫舍 III（337）中，一棵子樹能貢獻的最大金額**取決於它的根有沒有被選**——只回傳一個「最大值」不足以支撐父節點的決策，因為父節點需要知道「不選根時的最大值」才能在選自己時使用。\n\n樹形 DP 的槓桿就是**擴充狀態**：讓每個節點回傳一組值（通常是「選自己」與「不選自己」兩種情形下的最優解），使得歸納步驟能夠完成。這是 2.3 末尾「組合不出來就擴充狀態」那句話的正式展開。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「樹形 DP」想：\n\n- **資料是一棵樹，要求最大/最小/方案數，而每個節點都要做「選或不選」之類的決策**，且這決策會牽動相鄰節點。337 打家劫舍 III、968 監控二叉樹、3400 二叉樹染色都是。\n- **你發現只回報一個總數不夠**，父節點還想知道「你這個節點到底有沒有被選」——這就是要多回報幾個狀態的訊號：\n\n:::example 嗅到味道的瞬間：父層的決策依賴「我被選了沒」\n```cpp\n// 嗅到味道的瞬間：父層的決策依賴「我被選了沒」\n// 於是回傳就不能只給一個總和，要拆成 {選我, 不選我}\n```\n:::\n\n\n\n- **暴力做法會把同一棵子樹反覆重算**（指數級），樹形 DP 讓每節點只算一次，壓成 `O(n)`。\n- **題目能自然描述成「子樹的答案由小孩子樹拼出來」**，這就是後序遞迴的標誌。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我每個節點回報哪幾種狀態？為什麼是這幾種？\n2. 我的轉移公式怎麼從小孩的狀態組出自己的？\n3. 最終答案在根節點的哪個狀態（或哪幾個的極值）？\n\n三題都答得出來，就套對了。\n\n## 核心想法與直覺\n\n本節要解決的場景是：答案跟「整棵樹」有關，而每個節點要做的決策（選或不選、放或不放）又會影響它的小孩。這類問題用「樹形 DP（樹上動態規劃）」來解。\n\n先說 DP 是什麼。DP 就是「把一個大問題拆成小問題，算過的小答案先記起來，之後直接拿來用，不重算」。在樹上，小問題天然就是「子樹」：一棵子樹的答案，可以由它兩個小孩子樹的答案拼出來。\n\n:::example 每個節點回報兩種狀態：{偷自己, 不偷自己} 的子樹最大值\n關鍵想法：**每個節點向上回報「以我為根的子樹」算出來的結果，而且通常要回報好幾種狀態**。例如「打家劫舍 III」（Leetcode 337）裡，房子排成樹，相鄰不能同時偷。對每個節點，我們回報兩個數字：偷這個節點時子樹能拿的最大金額、不偷時的最大金額。\n\n```cpp\n// 每個節點回報兩種狀態：{偷自己, 不偷自己} 的子樹最大值\nstruct R {\n    int rob;\n    int skip;\n};  // rob：偷此節點；skip：不偷此節點\n```\n:::\n\n\n\n:::example 合併小孩：偷自己=自己的錢+兩個小孩都不偷；不偷自己=小孩各取較大者\n為什麼要回報兩種？因為「父節點要不要偷」取決於「小孩有沒有被偷」。只回報一個總和不夠，必須把「自己被選 / 沒被選」分開記，父層才能正確接續。\n\n```cpp\n// 合併小孩：偷自己=自己的錢+兩個小孩都不偷；不偷自己=小孩各取較大者\nR merge(int val, R l, R r) {\n    int rob = val + l.skip + r.skip;\n    int skip = max(l.rob, l.skip) + max(r.rob, r.skip);\n    return {rob, skip};\n}\n```\n:::\n\n\n\n走訪順序一定是「後序」——先把兩個小孩算完，才能算自己，因為自己依賴小孩的結果。最後在根節點取 `max(rob, skip)` 就是答案。整體每個節點只算一次，複雜度 `O(n)`。\n\n## 狀態／資料結構定義\n\n以 337 為例：\n\n- `dfs(v)` 回傳 `pair<long long, long long>`，記為 $(\\text{rob}_v, \\text{skip}_v)$。\n  - $\\text{rob}_v$：**選了 $v$** 的前提下，$v$ 子樹能取得的最大金額。\n  - $\\text{skip}_v$：**不選 $v$** 的前提下，$v$ 子樹能取得的最大金額。\n- 空節點回傳 $(0, 0)$。\n- 轉移：\n  $$\\text{rob}_v = val_v + \\text{skip}_L + \\text{skip}_R, \\qquad \\text{skip}_v = \\max(\\text{rob}_L, \\text{skip}_L) + \\max(\\text{rob}_R, \\text{skip}_R)$$\n- 答案是 $\\max(\\text{rob}_{root}, \\text{skip}_{root})$。\n\n968（監控二元樹）需要三個狀態（未被覆蓋／已被覆蓋但無攝影機／有攝影機），2313（翻轉使根為目標值的最少翻轉次數）需要「結果為 true」與「結果為 false」兩個狀態。狀態的維度由「父節點需要知道什麼」決定。\n\n## 不變量或正確性證明\n\n**狀態定義的完備性。** 命題：對每個節點 $v$，`dfs(v)` 回傳的兩個值分別正確。\n\n用結構歸納。基底：空子樹兩種情形都取 0，成立（沒有節點可選）。\n\n歸納步驟需要驗證兩條轉移。\n\n$\\text{rob}_v$：既然選了 $v$，題目約束「不能選相鄰節點」意味著 $L$ 與 $R$ 都不能選。在此前提下，左子樹能貢獻的最大值恰為 $\\text{skip}_L$（由歸納假設，這正是它的定義），右子樹同理。且**左右子樹的選擇互不影響**——它們的節點集合不相交，約束也不跨子樹。故三者可獨立相加。\n\n$\\text{skip}_v$：不選 $v$ 時，$L$ 與 $R$ 各自選或不選都合法，故各取自己的兩種情形之最大值。同樣由子樹不相交得到可加性。\n\n由歸納，對根成立。\n\n**這個證明實際依賴三個前提，每個都值得檢查：**\n\n1. **無後效性**：$v$ 的決策只透過「$v$ 選或不選」這一位資訊影響子樹，不會有更遠的耦合。若題目改成「不能選距離 $\\le 2$ 的節點」，狀態就要擴充成「子樹根的最近被選深度」，兩個狀態不夠。\n2. **子樹獨立性**：左右子樹的節點集合不相交，故最優解可分別取。若題目有「全樹最多選 $k$ 個」這種全域約束，子樹就不獨立了，必須把「用了幾個」也放進狀態（變成樹上背包，複雜度上升）。\n3. **狀態涵蓋所有情形**：$\\{選, 不選\\}$ 對每個節點是完備且互斥的劃分，故取兩者最大值不會漏解也不會重複計數。\n\n**為什麼不能只回傳一個最大值。** 假設 `dfs(v)` 只回傳 $\\max(\\text{rob}_v, \\text{skip}_v)$。父節點在選自己時需要「$v$ 不被選」的最大值，但單一回傳值可能來自「選了 $v$」的方案，直接使用會產生非法解（父子同選）。資訊已在回傳時遺失，無法補救——這就是狀態不完備的具體後果。\n\n**968 的三狀態為何必要。** 一個節點對父節點的影響有三種質性不同的情況：它需要父節點裝攝影機來覆蓋（狀態 0）、它已被覆蓋但沒有攝影機因此無法覆蓋父節點（狀態 1）、它有攝影機因此能順帶覆蓋父節點（狀態 2）。這三種對父節點的決策後果各不相同，任兩者合併都會遺失資訊。**狀態維度的正確判斷法就是問：有哪些情形，父節點必須區別對待？**\n\n## 逐步演算法\n\n樹形 DP 題目可拆成四個固定步驟：\n\n:::example 狀態定義範例（監控二叉樹 Leetcode 968）：每節點三態\n**1. 狀態定義：想清楚每個節點要回報哪幾個值。** 這是最難也最關鍵的一步。問自己：「父節點要做決策時，需要知道我的哪些情況？」需要幾種情況，就回報幾個值。\n\n```cpp\n// 狀態定義範例（監控二叉樹 Leetcode 968）：每節點三態\n// 0：自己有攝影機  1：自己被別人看著  2：自己沒被看著（待父層處理）\n```\n:::\n\n\n\n:::example 後序：先算小孩，再算自己\n**2. 設計遞迴：後序回傳狀態。** 先遞迴左右小孩拿到它們的狀態，再用它們組出自己的狀態回傳。\n\n```cpp\n// 後序：先算小孩，再算自己\nR dfs(TreeNode* node) {\n    if (!node) {\n        return baseCase();  // 空節點的初始狀態\n    }\n    R l = dfs(node->left);\n    R r = dfs(node->right);\n    return combine(node, l, r);  // 用小孩的狀態組出自己的\n}\n```\n:::\n\n\n\n:::example 監控二叉樹的轉移骨架（示意）\n**3. 寫轉移：把「決策」翻成幾條 max/min 公式。** 列舉自己每一種狀態，分別算出在該狀態下子樹的最佳值。轉移寫對的前提是狀態定義清楚。\n\n```cpp\n// 監控二叉樹的轉移骨架（示意）\n// 放攝影機  = 1 + min(各小孩三態的最小)\n// 被看著    = min(左放+右任意, 右放+左任意)\n// 沒被看著  = 左被看著 + 右被看著\n```\n:::\n\n\n\n**4. 讀答案與邊界。** 答案通常在根節點的回傳值裡取極值（如 968 還要處理「根自己沒被看著就得補一台攝影機」）。空節點的初始值要設得不破壞 min/max（例如不可能的狀態設成極大值）。複雜度 `O(n)`，但遞迴深度大時小心爆 stack。\n\n## C++17 模板\n\n```cpp\n// 打家劫舍 III（LeetCode 337）：每個節點回傳「選」與「不選」兩個最優值。\nclass Solution {\n  // first = 選了 node，second = 沒選 node\n  std::pair<long long, long long> dfs(TreeNode* node) {\n    if (!node) { return {0, 0}; }\n\n    auto [robL, skipL] = dfs(node->left);\n    auto [robR, skipR] = dfs(node->right);\n\n    long long rob  = node->val + skipL + skipR;                  // 選了自己，兩子必不選\n    long long skip = std::max(robL, skipL) + std::max(robR, skipR);  // 沒選，兩子自由\n    return {rob, skip};\n  }\n\n public:\n  int rob(TreeNode* root) {\n    auto [a, b] = dfs(root);\n    return static_cast<int>(std::max(a, b));\n  }\n};\n```\n\n三狀態的骨架（968）同理，回傳 `array<int, 3>`，轉移時對每種情形分別寫出「兩子必須處於哪些狀態」的組合並取最小。\n\n## 時間與空間複雜度\n\n每個節點恰計算一次，每次做常數次（狀態數平方級，但狀態數是常數）的組合運算，故 $O(n \\cdot S)$ 時間，$S$ 為狀態數（337 是 2，968 是 3），即 $O(n)$。\n\n空間 $O(h)$ 遞迴堆疊，加上每層 $O(S)$ 的回傳值，仍是 $O(h)$。\n\n若狀態中含有一個範圍為 $k$ 的維度（樹上背包，例如「子樹中選 $j$ 個」），複雜度會變成 $O(nk)$ 或 $O(n k^2)$；後者在「合併時枚舉左右各分配多少」的寫法下，透過經典的「按子樹大小上界枚舉」分析其實是 $O(n^2)$ 而非 $O(n k^2)$，這是樹上背包的著名結論。\n\n## 常見錯誤與邊界條件\n\n- 只回傳單一最大值導致狀態不完備，產生父子同選的非法解。\n- 空節點的初始狀態設錯（968 中空節點應視為「已被覆蓋」而非「需要覆蓋」，否則會給葉節點的父親裝多餘的攝影機）。\n- 用 `int` 累加而溢位。\n- 把左右子樹的狀態組合寫漏一種。\n- 誤以為可以貪心（樹形 DP 題常有看似合理的貪心，337 對某些測資會失敗）。\n- 斜樹爆堆疊。\n- 只有一個節點的樹。\n\n## 與相似技巧的比較\n\n- 與 2.3 的界線：能用單一彙總量寫出歸納步驟就用 2.3，寫不出來才擴充成樹形 DP。多開狀態沒有壞處以外的代價，但無謂的狀態會讓轉移變複雜、更容易寫錯。\n- 與線性 DP（dynamic_programming 主題）的關係：樹形 DP 就是把「按下標順序」的轉移換成「按後序順序」，無後效性由樹的層次結構天然保證——**子樹的答案不依賴子樹外的資訊**，這正是遞迴能奏效的原因。\n- 換根 DP（3.4）處理「以每個節點為根各求一次答案」，做法是第一趟後序算子樹資訊、第二趟前序把子樹外的資訊傳下去，$O(n)$ 完成 $n$ 次查詢。\n- 樹上背包（子樹中選 $k$ 個）是狀態帶容量維度的版本，複雜度分析與本節不同，需另行討論。\n\n## 代表例題\n\n**題號**：337、968、1000、2313、3400\n\n放在「樹形 DP」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 337 | [打家劫舍 III](https://leetcode.cn/problems/house-robber-iii/) |  |\n| 968 | [監控二叉樹](https://leetcode.cn/problems/binary-tree-cameras/) | 2124 |\n| 1000 | [LCP 10. 二叉樹任務排程](https://leetcode.cn/problems/er-cha-shu-ren-wu-diao-du/) | 2423 |\n| 2313 | [二叉樹中得到結果所需的最少翻轉次數](https://leetcode.cn/problems/minimum-flips-in-binary-tree-to-get-result/)（Premium） |  |\n| 3400 | [LCP 34. 二叉樹染色](https://leetcode.cn/problems/er-cha-shu-ran-se-UGC/) |  |\n| 6400 | [LCP 64. 二叉樹燈飾](https://leetcode.cn/problems/U7WvvU/) |  |\n\n## 本節重點速查\n\n- 狀態擴充的判斷準則：**父節點需要區別對待哪些情形，就開幾個狀態**。\n- 337 用「選／不選」兩個；\n- 968 用三個。\n- 轉移的正確性依賴無後效性、子樹獨立性、狀態劃分完備三個前提。\n- 空節點的初始狀態要與轉移一致。\n- 只回傳單一最大值會遺失資訊而產生非法解。\n",
        },
        {
          id: 27,
          title: "2.13 二叉樹 BFS",
          summary:
            "## 這個技術解決什麼問題\n\n有一類問題的答案是**按層組織**的：逐層輸出（102）、鋸齒形輸出（103）、每層最右邊的節點（199）、最底層最左邊的值（513）。DFS 能訪問所有節點，但訪問順序打散了層次，要靠額外記錄深度再重組。\n\nBFS 直接按層推進，天然給出層次結構。更重要的是在**無權圖／樹**上，BFS 第一次到達某節點時經過的邊數就是最短距離——這條性質讓「最小深度」「最少步數」類問題有了 $O(n)$ 解，而且不必像 DFS 那樣搜完所有分支。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「二叉樹 BFS」想：\n\n- **題目跟「層」「深度」「每一層」「最底層」「某層最左/最右」有關。** 102、103、107、199、513 全都是按層在問問題。\n- **你發現需要「同一層的節點一起看」**，而 DFS 一條路走到底不好控制層界線：\n\n:::example 嗅到味道的瞬間：你想「一次看完一整層」\n```cpp\n// 嗅到味道的瞬間：你想「一次看完一整層」\n// DFS 會跨層亂跑；改用佇列 + 每圈先抓 q.size()，層界線就清楚了\n```\n:::\n\n\n\n- **要找「離根最近且滿足某條件」的節點**（最短層數），BFS 一層層擴散天然就是由近到遠。\n- **暴力對每層重新從根掃一遍太慢**，BFS 讓每個節點只進出佇列一次，`O(n)` 解決。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我用什麼結構存「待處理節點」？（應該是 queue）\n2. 我怎麼切出每一層的邊界？（每圈先記 `q.size()`）\n3. 我對「每一層」要做的事是什麼（收集？取最後一個？反轉？）\n\n三題都答得出來，就套對了。\n\n## 核心想法與直覺\n\n**延伸講解**：[二叉樹的層序遍歷【基礎演算法精講 13】](https://www.bilibili.com/video/BV1hG4y1277i/)\n\n本節要解決的場景是：要按「一層一層」的順序走訪二叉樹——先處理離根最近的所有節點，再處理下一層。這叫「BFS（廣度優先搜尋）」或「層序遍歷」。\n\n:::example 佇列：先進先出，是 BFS 的核心工具\n對比一下：平常遞迴走樹是 DFS，會「一條路走到底」再回頭。BFS 不一樣，它像水波擴散，先把同一層全部看完，再往下一層。要做到這件事，靠的是一個「佇列（queue）」——先進先出的排隊結構：先排進去的節點先被處理，剛好讓上層先於下層。\n\n```cpp\n// 佇列：先進先出，是 BFS 的核心工具\nqueue<TreeNode*> q;\nq.push(root);               // 根先排隊\nTreeNode* cur = q.front();  // 看最前面的\nq.pop();                    // 取出最前面的，O(1)\n```\n:::\n\n\n\n:::example 一層的處理：先記住當層大小 n，只處理這 n 個\n關鍵技巧：**怎麼知道「這一層」到哪裡結束？** 答案是——在開始處理某層前，先記下此刻佇列裡有幾個節點（`int n = q.size()`），這個 `n` 就是當層節點數。接著只處理這 `n` 個，並把它們的小孩排進佇列（成為下一層）。\n\n```cpp\n// 一層的處理：先記住當層大小 n，只處理這 n 個\nint n = q.size();  // 當層有幾個節點\nfor (int i = 0; i < n; ++i) {\n    TreeNode* cur = q.front();\n    q.pop();\n    // ... 處理 cur ...\n    if (cur->left) {\n        q.push(cur->left);  // 小孩進下一層\n    }\n    if (cur->right) {\n        q.push(cur->right);\n    }\n}\n```\n:::\n\n\n\n有了「能切出每一層」這個能力，很多題目就迎刃而解：把每層收集成一個陣列（Leetcode 102 層序遍歷）、把奇數層反向（103 鋸齒形）、取每層最後一個（199 右檢視）、取最底層最左邊（513）。它們的骨架完全一樣，只是「對每層做什麼」不同。每個節點進出佇列各一次，複雜度 `O(n)`。\n\n## 狀態／資料結構定義\n\n- `queue<TreeNode*> q`：待處理節點，初始放入 `root`（**必須先判 `root` 非空**）。\n- **分層的關鍵技巧**：進入每一輪迴圈時先記錄 `int sz = q.size();`，然後恰好處理 `sz` 個節點。這 `sz` 個就是當前層的全部。\n- 若不記 `sz` 而直接 `while (!q.empty())` 逐個彈出，節點順序仍然正確，但**層與層之間的界線消失**，無法輸出巢狀結果。這是 102 與「單純 BFS」的唯一差別。\n- 103 鋸齒形：不要反轉佇列（會破壞後續層的順序），而是照常 BFS，只在**輸出該層的結果陣列時**依層號決定是否反轉，或用 `deque` 從兩端插入。\n\n## 不變量或正確性證明\n\n**分層不變量：每輪迴圈開始時，佇列中恰好包含第 $d$ 層的所有節點，且順序為由左至右。**\n\n- 初始化：$d = 0$，佇列只有根，成立。\n- 保持：記下 `sz = q.size()`（第 $d$ 層的節點數）。依序彈出這 `sz` 個節點，對每個節點依「先左子後右子」的順序把非空子節點入隊。彈出時佇列前 `sz` 個是第 $d$ 層，後面新加入的都在其後（FIFO），故這一輪不會誤處理到第 $d+1$ 層的節點。處理完後，佇列中恰是第 $d$ 層所有節點的子節點——由於樹中每個第 $d+1$ 層節點的父節點唯一且必在第 $d$ 層，這正是第 $d+1$ 層的全部，不重不漏。左右順序也維持：父節點按左至右處理，每個父節點先加左子再加右子，故子節點整體仍是左至右。\n- 終止：佇列為空時所有層都已處理完。\n\n`sz` 必須在**彈出任何節點之前**取得。若在迴圈中途重新讀 `q.size()`，該值已被本輪新加入的子節點污染，分層就錯了。\n\n**最短距離性質。** 由不變量，節點 $v$ 在第 $d$ 輪被彈出，當且僅當 $v$ 位於第 $d$ 層，即根到 $v$ 的邊數為 $d$。因此 BFS 依距離遞增的順序訪問節點，**第一次遇到某節點時的層號就是最短距離**。這條性質在樹上顯得平凡（樹中路徑唯一），但推廣到圖時就是 BFS 求無權最短路的基礎，前提是**每條邊的權重相同**——邊權不等時 BFS 不再給出最短路，必須改用 Dijkstra。\n\n**513（最底層最左）的巧解**：把入隊順序改成「先右子後左子」，則最後一個被彈出的節點必是最底層最左者。理由：由不變量的對稱版本，每層的彈出順序變成由右至左，故每層最後彈出的是該層最左者；而最後一層最後彈出的節點就是全域最後一個。這省去了記錄層號與比較的步驟。\n\n**199（右視圖）**：每層的最後一個彈出者即該層最右節點，用 `i == sz - 1` 判斷即可。\n\n## 逐步演算法\n\n層序遍歷題目可拆成四個固定步驟：\n\n:::example 初始化：根入隊，並準備收集結果\n**1. 狀態表示：用佇列存「待處理的節點」。** 初始只放根節點。佇列裡隨時都是「接下來要展開的前線」。\n\n```cpp\n// 初始化：根入隊，並準備收集結果\nqueue<TreeNode*> q;\nif (root) {\n    q.push(root);\n}\nvector<vector<int>> ans;  // 每個元素是一層\n```\n:::\n\n\n\n:::example 外層：佇列非空就還有層要處理\n**2. 外層迴圈：每轉一圈處理一整層。** 用 `while (!q.empty())` 包住，每圈一開始先抓 `n = q.size()` 鎖定當層。\n\n```cpp\n// 外層：佇列非空就還有層要處理\nwhile (!q.empty()) {\n    int n = q.size();\n    vector<int> level;  // 收集這一層\n    // ... 內層處理 n 個 ...\n    ans.push_back(level);\n}\n```\n:::\n\n\n\n:::example 內層：取 n 個、記錄值、推入小孩\n**3. 內層迴圈：處理當層 n 個，並把小孩送進下一層。**\n\n```cpp\n// 內層：取 n 個、記錄值、推入小孩\nfor (int i = 0; i < n; ++i) {\n    TreeNode* cur = q.front();\n    q.pop();\n    level.push_back(cur->val);\n    if (cur->left) {\n        q.push(cur->left);\n    }\n    if (cur->right) {\n        q.push(cur->right);\n    }\n}\n```\n:::\n\n\n\n**4. 變化與邊界。** 「鋸齒形」只要對偶數層把 `level` 反轉；「右檢視」取每層 `i == n - 1` 的那個；「層序 II」把結果整個反轉。空樹要先判 `root == nullptr`。複雜度時間 `O(n)`、空間最壞 `O(n)`（最寬那層的節點數）。\n\n## C++17 模板\n\n```cpp\n// 逐層輸出（LeetCode 102）：用 sz 切出層界線。\nstd::vector<std::vector<int>> levelOrder(TreeNode* root) {\n  std::vector<std::vector<int>> ans;\n  if (!root) { return ans; }               // 空樹必須先擋掉\n\n  std::queue<TreeNode*> q;\n  q.push(root);\n  while (!q.empty()) {\n    int sz = q.size();                     // 必須在彈出前取得，否則被子節點污染\n    std::vector<int> level;\n    level.reserve(sz);\n    for (int i = 0; i < sz; ++i) {         // 恰好處理當前層的 sz 個\n      TreeNode* node = q.front();\n      q.pop();\n      level.push_back(node->val);\n      if (node->left)  { q.push(node->left); }\n      if (node->right) { q.push(node->right); }\n    }\n    ans.push_back(std::move(level));\n  }\n  return ans;\n}\n```\n\n鋸齒形（103）在 `ans.push_back` 前依 `ans.size() % 2` 決定是否 `std::reverse(level.begin(), level.end())`；右視圖（199）只取 `i == sz - 1` 的節點。\n\n## 時間與空間複雜度\n\n每個節點恰入隊一次、出隊一次，各做 $O(1)$ 工作，故 $O(n)$ 時間。\n\n空間 $O(w)$，$w$ 是**最大層寬**。這與 DFS 的 $O(h)$ 是互補的：完全二元樹中 $w = O(n)$ 而 $h = O(\\log n)$，此時 DFS 省空間；斜樹中 $w = 1$ 而 $h = n$，此時 BFS 省空間且不會爆堆疊。**選 DFS 還是 BFS，空間上要看樹的形狀。**\n\n輸出所有層的結果本身需要 $O(n)$ 空間，不可省。\n\n## 常見錯誤與邊界條件\n\n- 忘記在入隊前判空樹，`q.push(nullptr)` 後解參考崩潰。\n- 在迴圈中途重新讀 `q.size()` 導致分層錯亂。\n- 把子節點入隊時沒判空，佇列混入 `nullptr`。\n- 103 直接反轉佇列而破壞後續層次（應只反轉輸出）。\n- 107（自底向上分層）忘記最後反轉整個結果，或用 `insert(begin())` 造成 $O(n^2)$。\n- 只有根節點的樹。\n- 深度很大但每層很窄時誤以為 BFS 省空間（此時反而是 DFS 省）。\n\n## 與相似技巧的比較\n\n- DFS（2.1、2.2）能拿到「路徑」，BFS 拿不到（要拿到得額外記父節點回溯）；反過來 BFS 天然給出層次與最短距離，DFS 要額外傳深度。**問「路徑」用 DFS，問「層」或「最少步數」用 BFS。**\n- 空間上 $O(h)$ 對 $O(w)$，如上所述取決於形狀。\n- 最小深度（111）用 BFS 遇到第一個葉節點即可回傳，比 DFS 需搜完所有分支更快；最大深度（104）則兩者都要走完全樹，用 DFS 較短。\n- 圖上的 BFS（graph 主題）多了 `visited` 集合，因為圖可能有環且節點可從多條路徑到達；樹上不需要 `visited`，這是樹 BFS 較簡單的原因。\n- 多源 BFS（同時把多個起點入隊）是常見變體，2385（感染時間）可用它，也可用 2.6 的直徑分解。\n\n## 代表例題\n\n**題號**：102、103、107、199、513\n\n放在「二叉樹 BFS」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 102 | [二叉樹的層序遍歷](https://leetcode.cn/problems/binary-tree-level-order-traversal/) |  |\n| 103 | [二叉樹的鋸齒形層序遍歷](https://leetcode.cn/problems/binary-tree-zigzag-level-order-traversal/) |  |\n| 107 | [二叉樹的層序遍歷 II](https://leetcode.cn/problems/binary-tree-level-order-traversal-ii/) |  |\n| 199 | [二叉樹的右檢視](https://leetcode.cn/problems/binary-tree-right-side-view/) |  |\n| 513 | [找樹左下角的值](https://leetcode.cn/problems/find-bottom-left-tree-value/) |  |\n| 515 | [在每個樹行中找最大值](https://leetcode.cn/problems/find-largest-value-in-each-tree-row/) |  |\n| 623 | [在二叉樹中增加一行](https://leetcode.cn/problems/add-one-row-to-tree/) |  |\n| 637 | [二叉樹的層平均值](https://leetcode.cn/problems/average-of-levels-in-binary-tree/) |  |\n| 662 | [二叉樹最大寬度](https://leetcode.cn/problems/maximum-width-of-binary-tree/) |  |\n| 742 | [二叉樹最近的葉節點](https://leetcode.cn/problems/closest-leaf-in-a-binary-tree/) |  |\n| 1602 | [找到二叉樹中最近的右側節點](https://leetcode.cn/problems/find-nearest-right-node-in-binary-tree/) |  |\n| 1660 | [糾正二叉樹](https://leetcode.cn/problems/correct-a-binary-tree/) |  |\n| 3157 | [找到具有最小和的樹的層數](https://leetcode.cn/problems/find-the-level-of-tree-with-minimum-sum/) |  |\n| 3831 | [二叉搜尋樹某一層的中位數](https://leetcode.cn/problems/median-of-a-binary-search-tree-level/) |  |\n| 1161 | [最大層內元素和](https://leetcode.cn/problems/maximum-level-sum-of-a-binary-tree/) | 1250 |\n| 993 | [二叉樹的堂兄弟節點](https://leetcode.cn/problems/cousins-in-binary-tree/) | 1288 |\n| 2583 | [二叉樹中的第 K 大層和](https://leetcode.cn/problems/kth-largest-sum-in-a-binary-tree/) | 1374 |\n| 1302 | [層數最深葉子節點的和](https://leetcode.cn/problems/deepest-leaves-sum/) | 1388 |\n| 2415 | [反轉二叉樹的奇數層](https://leetcode.cn/problems/reverse-odd-levels-of-binary-tree/) | 1431 |\n| 1609 | [奇偶樹](https://leetcode.cn/problems/even-odd-tree/) | 1438 |\n| 2471 | [逐層排序二叉樹所需的最少運算元目](https://leetcode.cn/problems/minimum-number-of-operations-to-sort-a-binary-tree-by-level/) | 1635 |\n| 863 | [二叉樹中所有距離為 K 的結點](https://leetcode.cn/problems/all-nodes-distance-k-in-binary-tree/) | 1663 |\n| 2641 | [二叉樹的堂兄弟節點 II](https://leetcode.cn/problems/cousins-in-binary-tree-ii/) | 1677 |\n| 919 | [完全二叉樹插入器](https://leetcode.cn/problems/complete-binary-tree-inserter/) | 1691 |\n| 958 | [二叉樹的完全性檢驗](https://leetcode.cn/problems/check-completeness-of-a-binary-tree/) | 1703 |\n\n## 本節重點速查\n\n- `while (!q.empty())` 外層加 `int sz = q.size()` 內層迴圈，這一行就是分層的全部；\n- `sz` 必須在彈出前取得。\n- 不變量是「每輪開始時佇列恰為第 $d$ 層、由左至右」。\n- BFS 的層號即最短距離（前提：邊權相同）。\n- 空間 $O(w)$ 對 DFS 的 $O(h)$，依樹形選擇。\n",
        },
        {
          id: 28,
          title: "2.14 連結串列+二叉樹",
          summary:
            "## 這個技術解決什麼問題\n\n這一節處理樹與鏈之間的相互轉換：把有序鏈建成平衡 BST（109）、把二元樹展平成單鏈（114）、把 BST 轉成有序雙向循環鏈（426）、在每層節點間串上 `next` 指標（116、117）。\n\n它們的共同挑戰是**兩種結構的訪問模式不相容**：樹允許遞迴分治但鏈只能順序前進；鏈無法 $O(1)$ 取中點但樹的分治需要它。解法通常是「找到一個讓兩者對齊的遍歷順序」——多半是中序（對 BST）或前序（對展平）。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「連結串列 + 二叉樹」想：\n\n- **題目要在「樹」和「鏈」之間轉換**，或要在樹節點間「拉一條橫向的鏈」。109、114、116、117、426 都是。\n- **你發現解法就是「小心地改幾條指標」**，而一不小心就會把後面整段弄丟：\n\n:::example 嗅到味道的瞬間：你正要改某個 next/left/right，但後面還有用\n```cpp\n// 嗅到味道的瞬間：你正要改某個 next/left/right，但後面還有用\n// 反射動作：先 ListNode* nxt = cur->next; 存起來再改\n```\n:::\n\n\n\n- **看到「有序」+「要建平衡樹」**，馬上想到「中點當根」。\n- **要把同一層連起來**，想到沿著上一層已建好的 `next` 橫向走。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我需要哪幾個指標（prev / cur / next / dummy）？\n2. 我每次改指標前，有沒有先把後繼存起來？\n3. 如果是鏈轉樹，我的「樹根」對應鏈的哪個位置（中點？）\n\n三題都答得出來，就套對了。\n\n## 核心想法與直覺\n\n本節要解決的場景是：題目同時牽涉「連結串列（linked list）」和「二叉樹」，常見任務是把樹「攤平」成一條鏈、或反過來把有序鏈「建」成一棵樹、又或者在樹的節點間「拉線」連成鏈。\n\n:::example 連結串列的鐵則：改 next 前先存下一個，免得鏈斷掉\n先複習連結串列：它是一串節點，每個節點存一個值和一個指向「下一個」的指標。它沒有索引，只能順著指標走。操作它的精髓是**改指標前先把下一個存起來，否則一改就找不到後面**。\n\n```cpp\n// 連結串列的鐵則：改 next 前先存下一個，免得鏈斷掉\nListNode* nxt = cur->next;  // 1) 先存後繼\ncur->next = somethingNew;   // 2) 再改指標，原本的後繼不會遺失\n```\n:::\n\n\n\n:::example 114 的核心一步：把左子樹接到右邊，左指標清空\n樹和鏈的共同點是「都靠指標連節點」，所以很多操作其實就是「小心地改幾條指標」。例如「二叉樹展開為連結串列」（Leetcode 114）：要把整棵樹照前序順序壓成一條只用右指標的鏈。做法是對每個節點，把它的左子樹整段插到右子樹前面。\n\n```cpp\n// 114 的核心一步：把左子樹接到右邊，左指標清空\nTreeNode *l = node->left, *r = node->right;\nnode->left = nullptr;  // 攤平後不再用左指標\nnode->right = l;       // 左子樹接到右側\n                  // 再把原本的右子樹 r 接到「新右鏈的尾端」\n```\n:::\n\n\n\n反方向的「有序鏈轉 BST」（Leetcode 109）：鏈是排好序的，鏈的「中點」就是樹根（左半變左子樹、右半變右子樹），遞迴下去就得到一棵高度平衡的 BST。核心觀念是**有序序列的中點 = 平衡樹的根**。\n\n還有「填充右側 next 指標」（116、117）：本質是在樹的同一層節點之間「拉一條鏈」，把同層相鄰節點用 `next` 串起來。\n\n## 狀態／資料結構定義\n\n- **109（有序鏈建平衡 BST）**：樸素做法每層用快慢指標找中點（$O(n \\log n)$）。更好的做法是**中序模擬**：先數出長度 $n$，然後 `build(l, r)` 依中序順序遞迴——先建左子樹，此時全域游標 `cur` 恰好停在應作為根的節點上，取它的值後把 `cur` 前進，再建右子樹。狀態只有一個全域游標 `cur` 與區間 $[l, r]$。\n- **114（展平成前序單鏈）**：用「反前序」（右 → 左 → 根）遍歷，全域 `prev` 記錄上一個處理過的節點，處理當前節點時執行 `node->right = prev; node->left = nullptr; prev = node;`。\n- **426（BST 轉雙向循環鏈）**：中序遍歷，`prev` 記錄中序前驅，訪問時互相接上 `prev->right = cur; cur->left = prev;`，最後把頭尾相連。\n- **116／117（連接 next 指標）**：116 是完美二元樹，可用 `next` 指標本身當作「當前層的鏈」逐層下推，$O(1)$ 空間；117 是任意二元樹，需用 dummy 加 tail 逐層串接（1.7 的分鏈手法）。\n\n## 不變量或正確性證明\n\n**109 中序模擬的正確性。** 關鍵洞察：由 2.9 的等價性，一棵 BST 的中序序列就是有序序列。反過來，若我們**先決定樹的形狀**（由 `build(l, r)` 的區間切分決定），再按中序順序把有序鏈的值依次填入，得到的必然是一棵 BST。\n\n不變量：**呼叫 `build(l, r)` 時，全域游標 `cur` 恰指向鏈中第 $l$ 個節點（0 起算）；返回時 `cur` 指向第 $r+1$ 個。**\n\n- 基底：$l > r$ 時不消耗任何節點，直接返回，成立。\n- 保持：設 `m = (l + r) / 2`。先呼叫 `build(l, m - 1)`，由歸納假設它消耗第 $l$ 到 $m-1$ 個節點，返回時 `cur` 指向第 $m$ 個。此時取 `cur->val` 作為當前子樹的根值，並把 `cur` 前進一格（指向第 $m+1$ 個）。再呼叫 `build(m + 1, r)`，由歸納假設消耗第 $m+1$ 到 $r$ 個，返回時指向第 $r+1$ 個。整段恰消耗 $[l, r]$，不變量維持。\n- 由於填入順序嚴格按中序、鏈本身遞增，故中序序列遞增，由 2.9 得結果是 BST。平衡性則由「每次取中點切分」保證，論證同 108。\n\n**這個做法的價值在於把「找中點」從 $O(n)$ 降到 $O(1)$**——形狀由下標算術決定，值由順序游標供給，兩者解耦。這是本節最值得學的一招。\n\n**114 為何用反前序。** 目標是把樹改成一條「按前序排列、全部掛在右指標上」的鏈。若用正前序處理，改動 `node->right` 時會弄丟原本的右子樹（它還沒被處理）。反前序（右 → 左 → 根）則保證：處理到 `node` 時，它的左右子樹都已經被展平並串好，而 `prev` 正指向**前序序列中緊接 `node` 之後的那個節點**。\n\n為什麼 `prev` 恰是前序後繼？反前序的訪問序列是前序序列的完全逆序（前序是「根、左、右」，反前序是「右、左、根」，兩者互為逆序，可用結構歸納證明）。因此在反前序中「上一個被處理的節點」，在前序中就是「下一個節點」。於是 `node->right = prev` 直接接對，`node->left = nullptr` 清掉左指標。\n\n**426 的中序串接**同理：中序序列即有序序列，`prev` 是中序前驅，雙向接上即可。收尾要把最後一個節點與第一個節點相連成環，且**必須在遞迴全部結束後做**，因為在遞迴中無法得知誰是最後一個。\n\n**116 為何能 $O(1)$ 空間。** 完美二元樹中每個節點要嘛有兩個子、要嘛沒有。處理第 $d$ 層時，該層的 `next` 指標已全部串好，於是可以沿著這條鏈走訪本層所有節點，並為第 $d+1$ 層建立連接：`node->left->next = node->right;`（同一父親），`node->right->next = node->next ? node->next->left : nullptr;`（跨父親，靠本層的 `next` 找到右邊的兄弟）。不變量是「處理第 $d+1$ 層時，第 $d$ 層的 `next` 已完整」，歸納即得。**這條論證依賴完美二元樹的性質**：跨父親連接時 `node->next->left` 必定存在。117 中它可能為空，必須逐個往右找第一個有子節點的兄弟，故改用 dummy 加 tail 更穩妥。\n\n## 逐步演算法\n\n這類題目可拆成四個固定步驟：\n\n:::example dummy 頭：讓「接第一個節點」和「接後面節點」寫法一致\n**1. 狀態表示：盯緊幾個關鍵指標。** 通常需要 `prev`、`cur`、`next`，以及一個「dummy 頭節點」當作鏈的固定起點，省去處理「第一個節點」的特例。\n\n```cpp\n// dummy 頭：讓「接第一個節點」和「接後面節點」寫法一致\nListNode dummy(0);\nListNode* tail = &dummy;  // tail 永遠指向已建好鏈的最後一個\n```\n:::\n\n\n\n:::example 通用：先存，再改，最後前進\n**2. 改邊流程：每動一條指標前先存後繼。** 不論是攤平、插入還是反轉，都只改局部幾條指標，但順序錯了鏈就斷。\n\n```cpp\n// 通用：先存，再改，最後前進\nListNode* nxt = cur->next;\ncur->next = tail;  // 接到目標位置\ntail = cur;        // 推進已建鏈的尾端\ncur = nxt;         // 前進到原本的後繼\n```\n:::\n\n\n\n:::example 快慢指標找鏈中點：slow 每次一步、fast 每次兩步\n**3. 樹鏈互轉：抓住「中點」或「同層相鄰」。** 鏈轉樹用快慢指標找中點當根（109）；同層拉線（116/117）則用上一層已連好的 `next` 鏈，橫向走訪這一層、把它們的小孩串起來。\n\n```cpp\n// 快慢指標找鏈中點：slow 每次一步、fast 每次兩步\nListNode *slow = head, *fast = head, *prev = nullptr;\nwhile (fast && fast->next) {\n    prev = slow;\n    slow = slow->next;\n    fast = fast->next->next;\n}\n// 迴圈結束 slow 在中點，prev 用來把左半截斷\n```\n:::\n\n\n\n**4. 邊界與複雜度。** 頭節點可能被改動 → 用 dummy 統一；空鏈、單節點要單獨確認。攤平/拉線多為 `O(n)`；鏈轉 BST 若每次重走找中點是 `O(n log n)`，改用「中序模擬」可優化到 `O(n)`。\n\n## C++17 模板\n\n```cpp\n// 有序鏈建平衡 BST（LeetCode 109）：中序模擬，O(n) 時間。\nclass Solution {\n  ListNode* cur_ = nullptr;\n\n  TreeNode* build(int l, int r) {\n    if (l > r) { return nullptr; }\n    int m = l + (r - l) / 2;\n\n    TreeNode* left = build(l, m - 1);      // 先建左子樹，cur_ 隨之推進\n    TreeNode* root = new TreeNode(cur_->val);   // 此刻 cur_ 恰是本子樹的根\n    cur_ = cur_->next;\n    root->left = left;\n    root->right = build(m + 1, r);         // 再建右子樹\n    return root;\n  }\n\n public:\n  TreeNode* sortedListToBST(ListNode* head) {\n    int n = 0;\n    for (ListNode* p = head; p; p = p->next) { ++n; }\n    cur_ = head;\n    return build(0, n - 1);\n  }\n};\n```\n\n展平二元樹（LeetCode 114）：\n\n```cpp\nclass Solution {\n  TreeNode* prev_ = nullptr;\n public:\n  void flatten(TreeNode* root) {\n    if (!root) { return; }\n    flatten(root->right);        // 反前序：右 -> 左 -> 根\n    flatten(root->left);\n    root->right = prev_;         // prev_ 正是前序中的後繼\n    root->left = nullptr;\n    prev_ = root;\n  }\n};\n```\n\n## 時間與空間複雜度\n\n109 中序模擬：數長度 $O(n)$，建樹每個節點 $O(1)$，總計 $O(n)$ 時間、$O(\\log n)$ 堆疊。樸素的「每層快慢指標找中點」是 $T(n) = 2T(n/2) + O(n) = O(n \\log n)$——差別正是找中點的成本。\n\n114、426：$O(n)$ 時間、$O(h)$ 堆疊。114 有 $O(1)$ 空間的 Morris 式解法（利用左子樹最右節點接上原右子樹），值得一提但較繁瑣。\n\n116：$O(n)$ 時間、$O(1)$ 空間（利用已建好的 `next` 當作層鏈）。117 用 dummy 加 tail 同樣 $O(1)$ 空間。用 BFS 做則是 $O(w)$ 空間。\n\n## 常見錯誤與邊界條件\n\n- 109 用樸素找中點而在大測資超時，或切分時忘記斷開鏈導致無限遞迴。\n- 109 的中序模擬中，把 `build(l, m-1)` 的結果先賦給 `root->left` 再取 `cur_`（求值順序不保證，必須先存進區域變數）。\n- 114 用正前序而弄丟右子樹。\n- 114 忘記把 `left` 設為 `nullptr`。\n- 426 忘記最後首尾相連，或在遞迴中途就嘗試連環。\n- 117 誤用 116 的解法而在缺子節點處連錯。\n- 空輸入。\n\n## 與相似技巧的比較\n\n- 109 與 108（有序陣列建 BST，見 2.10）目標相同，差別只在資料來源能否隨機存取；中序模擬正是為了彌補這個差距。\n- 114 與 430（扁平化多層雙向鏈，見 1.11）是同一個「壓平巢狀結構」的問題，都要注意先保存後繼再改指標；114 用反向遍歷繞開了這個問題，430 用堆疊或遞迴回傳尾節點。\n- 116／117 的逐層串接與 1.7 的分鏈手法同構，都是 dummy 加 tail。\n- 426 與 2.9 的中序法共用「一個 `prev` 變數」的骨架，差別只在做的事情是比較還是接指標。\n\n## 代表例題\n\n**題號**：109、114、116、117、426\n\n放在「連結串列+二叉樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 109 | [有序連結串列轉換二叉搜索樹](https://leetcode.cn/problems/convert-sorted-list-to-binary-search-tree/) |  |\n| 114 | [二叉樹展開為連結串列](https://leetcode.cn/problems/flatten-binary-tree-to-linked-list/) |  |\n| 116 | [填充每個節點的下一個右側節點指標](https://leetcode.cn/problems/populating-next-right-pointers-in-each-node/) |  |\n| 117 | [填充每個節點的下一個右側節點指標 II](https://leetcode.cn/problems/populating-next-right-pointers-in-each-node-ii/) |  |\n| 426 | [將二叉搜索樹轉化為排序的雙向連結串列](https://leetcode.cn/problems/convert-binary-search-tree-to-sorted-doubly-linked-list/)（Premium） |  |\n| 1367 | [二叉樹中的連結串列](https://leetcode.cn/problems/linked-list-in-binary-tree/) | 1650 |\n\n## 本節重點速查\n\n- 樹與鏈的轉換靠「找到讓兩者對齊的遍歷順序」。\n- 109 用**中序模擬**：形狀由下標切分決定、值由順序游標供給，把找中點從 $O(n)$ 降到 $O(1)$。\n- 114 用**反前序**（右→左→根），`prev` 恰是前序後繼。\n- 426 用中序加 `prev`，最後才連環。\n- 116 靠完美樹性質用 `next` 當層鏈達成 $O(1)$ 空間，117 不行，改用 dummy 加 tail。\n",
        },
        {
          id: 29,
          title: "2.15 N 叉樹",
          summary:
            "## 這個技術解決什麼問題\n\nN 叉樹的每個節點有一個子節點串列 `vector<Node*> children`，而非固定的 `left`／`right`。它是二元樹的直接推廣，也是一般樹（3.x）的具體表示。\n\n本節的重點不是新技巧，而是**辨認哪些二元樹的手法能無痛遷移、哪些會失效**。答案很乾脆：所有基於「前序／後序」的技巧照搬即可，把兩次遞迴換成對 `children` 的迴圈；**只有中序遍歷失去意義**——子節點超過兩個時，「根該插在第幾個子樹之後」沒有標準答案。這也連帶讓所有依賴「中序即有序」的 BST 技巧（2.9）在 N 叉樹上無對應。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「N 叉樹」想：\n\n- **節點的小孩數量不固定**，題目直接給你 `children` 陣列，或描述成四叉樹。427、428、429、558、559 都是。\n- **你發現「二叉樹會做的事我也想做，只是小孩變多了」**——這就是把 `left/right` 換成 for 迴圈的訊號：\n\n:::example 嗅到味道的瞬間：本來想寫 dfs(left); dfs(right);\n```cpp\n// 嗅到味道的瞬間：本來想寫 dfs(left); dfs(right);\n// 但小孩不止兩個，於是改成 for (auto c : children) dfs(c);\n```\n:::\n\n\n\n- **要彙總整棵子樹的資訊**（深度、總數、序列化），本質仍是後序遞迴 + 合併小孩結果。\n- **四叉樹的「同值整塊」可合併**，遇到不一致才切四塊往下遞迴。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我有沒有把「處理 left、處理 right」換成「for 迴圈跑所有小孩」？\n2. 每個節點向上回報的摘要是什麼？怎麼由小孩合併出來？\n3. 如果要序列化，我怎麼記錄「每個節點有幾個小孩」？\n\n三題都答得出來，就套對了。\n\n## 核心想法與直覺\n\n本節要解決的場景是：樹的每個節點不再固定只有「左、右」兩個小孩，而是可以有任意多個小孩——這叫「N 叉樹（N-ary tree）」。四叉樹（每節點 0 或 4 個小孩）是它的特例。\n\n:::example N 叉樹節點：用一個 vector 裝所有小孩\n二叉樹的小孩寫成 `left`、`right` 兩個欄位；N 叉樹改用一個「小孩陣列」來裝不定數量的小孩：\n\n```cpp\n// N 叉樹節點：用一個 vector 裝所有小孩\nstruct Node {\n    int val;\n    vector<Node*> children;  // 想有幾個小孩就放幾個\n};\n```\n:::\n\n\n\n好消息是：**二叉樹的那套走訪思路完全沿用，只是把「處理 left、處理 right」換成「用一個 for 迴圈跑過所有小孩」**。自底向上彙總子樹、自頂向下傳遞資訊，全都一樣。\n\n:::example N 叉樹最大深度：1 + 所有小孩深度的最大值\n例如「N 叉樹的最大深度」（Leetcode 559）：一個節點的深度 = 1 + 所有小孩深度的最大值。二叉樹是 `1 + max(左, 右)`，N 叉樹只是把 max 攤開到所有小孩：\n\n```cpp\n// N 叉樹最大深度：1 + 所有小孩深度的最大值\nint maxDepth(Node* root) {\n    if (!root) {\n        return 0;\n    }\n    int best = 0;\n    for (Node* c : root->children) {  // 遍歷每個小孩\n        best = max(best, maxDepth(c));\n    }\n    return best + 1;  // 加上自己這一層\n}\n```\n:::\n\n\n\n層序遍歷（Leetcode 429）也一樣，只是把小孩入隊時從「push 左、push 右」改成「for 迴圈把所有小孩 push 進去」。四叉樹題（427 建立四叉樹、558 四叉樹交集）則是「能合併成一塊就合併、否則切四塊遞迴」，本質仍是遞迴處理每個小孩再彙總。\n\n## 狀態／資料結構定義\n\n- `struct Node { int val; vector<Node*> children; };`\n- 前序 DFS：處理自己，再 `for (Node* c : node->children) dfs(c);`\n- 後序 DFS：先迴圈遞迴，再處理自己；彙總量用累加或取最值的方式合併所有子節點的回傳值，**初始值必須是該運算的單位元**（求和用 0，求最大值用 `INT_MIN` 或第一個子節點的值）。\n- BFS（429）：與 2.13 完全相同，只是入隊時改成迴圈把所有子節點推入。\n- 序列化（428）：因為子節點數不固定，序列中必須額外編碼「這個節點有幾個子」或使用括號、哨兵標記。\n\n## 不變量或正確性證明\n\n**遍歷的完備性**與 2.1 的論證同構，只是劃分從「左子樹、右子樹、根」三塊變成「$k$ 棵子樹、根」共 $k+1$ 塊。歸納步驟仍然成立，因為 N 叉樹的關鍵性質未變：**各子樹的節點集合兩兩不交，且與根的並集是整棵子樹**。這條性質是所有樹上遞迴的根基，它與子節點數目無關，這正是二元樹技巧能整批遷移的理由。\n\n**後序彙總的正確性**同樣是結構歸納，合併規則從 `combine(f(L), f(R), v)` 變成 $\\text{fold}$：\n\n$$f(v) = g\\big(v,\\ \\bigoplus_{c \\in \\text{children}(v)} f(c)\\big)$$\n\n要求 $\\oplus$ 是**可結合的**（結合律），否則對 `children` 的迴圈順序會影響結果。求和、取最大值、取最小值都滿足；「減法」「除法」不滿足，遇到這類彙總要重新設計。這是二元樹版本裡看不出來的隱含條件——只有兩個運算元時結合律不會被觸發。\n\n**N 叉樹直徑的推廣。** 2.6 的「枚舉最高點」分解在 N 叉樹上完全成立（該證明只用到「路徑的最高點唯一」，與分支數無關）。差別在於：以 $v$ 為最高點的最長路徑，是從 $v$ 的所有子節點中取**向下最長鏈的前兩大**相加，而不是簡單的「左 + 右」。\n\n實作上不需要排序：邊迴圈邊維護 `max1 >= max2` 兩個變數即可，$O(k)$ 而非 $O(k \\log k)$。更新順序要小心——**必須先用當前子節點的值嘗試更新 `max1`，若被擠下來的舊 `max1` 再成為 `max2`**，寫成兩個獨立的 `if` 會漏掉「新值同時大於兩者」的情形。\n\n**N 叉樹的最大深度（559）**：$h(v) = 1 + \\max_{c} h(c)$，葉節點（`children` 為空）的 $\\max$ 為空集合，必須特別處理——回傳 1 而非 $1 + (-\\infty)$。這正是「單位元選錯」在 N 叉樹上的具體表現：對 `max` 運算，空集合的單位元是 $-\\infty$，但這裡的語意是「沒有子節點時深度為 1」，用 0 作為初始值（代表「空子樹高度 0」）才對。\n\n**428 序列化的唯一性。** 二元樹靠固定的兩個位置（含空標記）就能還原；N 叉樹不行，因為讀到一串子節點時不知道何時該停。必須顯式編碼子節點數量，或用配對的括號／終止哨兵界定範圍。這是「N 叉」相對「二元」真正多出來的困難，不是遍歷順序的問題。\n\n## 逐步演算法\n\nN 叉樹題目可拆成四個固定步驟：\n\n:::example 摘要由「自己」加上「所有小孩的摘要」合併而成\n**1. 狀態表示：節點回傳子樹摘要。** 跟二叉樹一樣，每個節點向上回報「以我為根的子樹」算出的東西（深度、節點數、序列化字串等）。\n\n```cpp\n// 摘要由「自己」加上「所有小孩的摘要」合併而成\n// summary(node) = combine(node->val, summary(c1), summary(c2), ...)\n```\n:::\n\n\n\n:::example 通用 DFS 骨架：遞迴每個小孩，再合併\n**2. 走訪流程：用 for 迴圈跑過所有小孩。** 這是 N 叉樹和二叉樹唯一的寫法差別。\n\n```cpp\n// 通用 DFS 骨架：遞迴每個小孩，再合併\nvoid dfs(Node* node) {\n    if (!node) {\n        return;\n    }\n    // ... 進入時的處理（前序）...\n    for (Node* c : node->children) {\n        dfs(c);\n    }\n    // ... 離開時的處理（後序，彙總小孩結果）...\n}\n```\n:::\n\n\n\n:::example N 叉層序：取出節點後，把它所有小孩排進佇列\n**3. BFS 版本：入隊時把整個 children 推進去。**\n\n```cpp\n// N 叉層序：取出節點後，把它所有小孩排進佇列\nint n = q.size();\nfor (int i = 0; i < n; ++i) {\n    Node* cur = q.front();\n    q.pop();\n    for (Node* c : cur->children) {\n        q.push(c);  // 一次推所有小孩\n    }\n}\n```\n:::\n\n\n\n**4. 序列化與邊界。** 序列化 N 叉樹（428）因小孩數不定，要額外記「每個節點有幾個小孩」或用特殊分隔符，反序列化才知道讀幾個。四叉樹合併（427/558）的邊界是「整塊同值就停止細分」。複雜度多為 `O(n)`（每節點訪一次）；遞迴深時注意 stack。\n\n## C++17 模板\n\n```cpp\n// N 叉樹的最大深度（LeetCode 559）與後序彙總骨架。\nint maxDepth(Node* root) {\n  if (!root) { return 0; }             // 單位元：空子樹深度 0\n  int best = 0;\n  for (Node* c : root->children) {     // 兩次遞迴換成對 children 的迴圈\n    best = std::max(best, maxDepth(c));\n  }\n  return 1 + best;                     // children 為空時自然回傳 1\n}\n```\n\nN 叉樹直徑（取前兩大向下鏈）：\n\n```cpp\nclass Solution {\n  int ans_ = 0;\n\n  int down(Node* node) {               // 從 node 向下的最長鏈（邊數）\n    int max1 = 0, max2 = 0;            // 最大與次大，初值 0 代表「不接任何子」\n    for (Node* c : node->children) {\n      int d = 1 + down(c);\n      if (d > max1) { max2 = max1; max1 = d; }   // 擠下來的舊 max1 成為 max2\n      else if (d > max2) { max2 = d; }\n    }\n    ans_ = std::max(ans_, max1 + max2);          // 以 node 為最高點，可分叉\n    return max1;                                 // 上傳必須單邊\n  }\n public:\n  int diameter(Node* root) { down(root); return ans_; }\n};\n```\n\n## 時間與空間複雜度\n\n設節點數為 $n$、總邊數為 $n - 1$。對 `children` 的迴圈總共執行 $\\sum_v |\\text{children}(v)| = n - 1$ 次，故所有遍歷仍是 $O(n)$ 時間——**不是 $O(nk)$**，因為 $k$ 是每個節點各自的分支數，總和有界。這個「所有分支數之和等於邊數」的觀察在分析樹上演算法時反覆出現，值得記住。\n\n空間 $O(h)$ 遞迴堆疊（DFS）或 $O(w)$ 佇列（BFS）。直徑的前兩大維護是 $O(k)$ 而非 $O(k \\log k)$，故不影響總複雜度。\n\n## 常見錯誤與邊界條件\n\n- 後序彙總的初始值選錯（559 用 `INT_MIN` 導致葉節點回傳錯誤，應用 0）。\n- 直徑的前兩大更新寫成兩個獨立 `if`，漏掉新值同時大於兩者的情形。\n- 上傳時回傳 `max1 + max2`（分叉值）而非 `max1`。\n- `children` 中含有 `nullptr`（多數題目保證沒有，但序列化還原時可能產生）。\n- 428 序列化未編碼子節點數量而無法還原。\n- 空樹。\n- 只有根節點（`children` 為空）。\n\n## 與相似技巧的比較\n\n- 與二元樹（2.x）的對應關係如開頭所述：前序、後序、BFS、直徑、樹形 DP 全部照搬；中序與 BST 的一整套技巧沒有對應。\n- 與一般樹（3.x）的差別只在表示法：3.x 用鄰接表加父節點編號（因為輸入常是邊集合，且需要避免走回父節點），2.15 用 `children` 指標（父子關係已由結構給定，不會走回頭）。**這個差別導致 3.x 的 DFS 必須傳入 `fa` 參數，本節不需要。**\n- 「左子右兄弟」表示法可把 N 叉樹編碼成二元樹（`left` 指第一個子、`right` 指下一個兄弟），此時 N 叉樹的前序等於該二元樹的前序、N 叉樹的後序等於該二元樹的中序——這是一個有趣的對應，偶爾能把 N 叉問題轉成二元問題。\n- 589／590（N 叉樹前序／後序遍歷）的迭代版要注意：用堆疊時子節點需**逆序入棧**才能維持由左至右的訪問順序。\n\n## 代表例題\n\n**題號**：427、428、429、558、559\n\n放在「N 叉樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 427 | [建立四叉樹](https://leetcode.cn/problems/construct-quad-tree/) |  |\n| 428 | [序列化和反序列化 N 叉樹](https://leetcode.cn/problems/serialize-and-deserialize-n-ary-tree/)（Premium） |  |\n| 429 | [N 叉樹的層序遍歷](https://leetcode.cn/problems/n-ary-tree-level-order-traversal/) |  |\n| 558 | [四叉樹交集](https://leetcode.cn/problems/logical-or-of-two-binary-grids-represented-as-quad-trees/) |  |\n| 559 | [N 叉樹的最大深度](https://leetcode.cn/problems/maximum-depth-of-n-ary-tree/) |  |\n| 589 | [N 叉樹的前序遍歷](https://leetcode.cn/problems/n-ary-tree-preorder-traversal/) |  |\n| 590 | [N 叉樹的後序遍歷](https://leetcode.cn/problems/n-ary-tree-postorder-traversal/) |  |\n| 1490 | [克隆 N 叉樹](https://leetcode.cn/problems/clone-n-ary-tree/) |  |\n| 1506 | [找到 N 叉樹的根節點](https://leetcode.cn/problems/find-root-of-n-ary-tree/) |  |\n| 1516 | [移動 N 叉樹的子樹](https://leetcode.cn/problems/move-sub-tree-of-n-ary-tree/) |  |\n| 1522 | [N 叉樹的直徑](https://leetcode.cn/problems/diameter-of-n-ary-tree/) |  |\n\n## 本節重點速查\n\n- 兩次遞迴換成對 `children` 的迴圈，其餘照搬二元樹；\n- **只有中序與 BST 那一套沒有對應**。\n- 後序彙總要求運算可結合，且單位元要與「無子節點」的語意一致。\n- 直徑取前兩大向下鏈，更新時記得把被擠下的舊最大值降為次大。\n- 總複雜度仍是 $O(n)$，因為所有分支數之和等於邊數。\n",
        },
        {
          id: 30,
          title: "2.16 其他",
          summary:
            '## 這個技術解決什麼問題\n\n這一節收攏幾類不屬於前面單一模式的二元樹題：序列化與反序列化（297）、BST 迭代器（173）、完全二元樹的節點計數（222）、樹的垂直遍歷（314）、以及排列序號類的組合題（60）。\n\n它們各自的槓桿不同，但有一個共同主題值得先點出：**當樹具有額外的結構性保證（完全二元樹、BST）時，樸素的 $O(n)$ 遍歷往往可以被壓到 $O(\\log^2 n)$ 或 $O(h)$**。看到「完全」「滿」「搜尋樹」這些字眼，就該問「這個保證能省下什麼」。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「二叉樹綜合走訪」想：\n\n- **題目是二叉樹上的某種操作**：序列化、計數、做成迭代器、特殊順序輸出。297、222、173、314 都是。\n- **你發現答案能由「子樹結果彙總」或「從根帶下狀態」得到**——這就是套 DFS/BFS 骨架的訊號：\n\n:::example 嗅到味道的瞬間：每個節點都在做同一件事，只是位置不同\n```cpp\n// 嗅到味道的瞬間：每個節點都在做同一件事，只是位置不同\n// 反射動作：套上 dfs(node) 骨架，想清楚「回報什麼/攜帶什麼」\n```\n:::\n\n\n\n- **看到「中序 = 由小到大」「完全二叉樹」「按層」這些結構特性**，往往能用它加速，別硬掃整棵樹。\n- **暴力反覆重算子樹資訊**，改成一次後序遍歷就把重複工作省掉。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我用哪種走訪（前序 / 中序 / 後序 / BFS）？為什麼？\n2. 節點向上回報什麼、或向下攜帶什麼？\n3. 有沒有可利用的結構特性（BST 有序、完全二叉樹）能加速？\n\n三題都答得出來，就套對了。\n\n## 核心想法與直覺\n\n本節是二叉樹的「其他」綜合題，共同精神只有一句：**把資訊用 DFS/BFS 在樹上傳遞——自底向上彙總子樹，或自頂向下攜帶狀態**。同一套走訪框架，套不同的「在節點上做什麼」，就能解一票看似不相干的題目。\n\n:::example 二叉樹遞迴萬用骨架\n先抓住二叉樹遞迴的萬用骨架。每個節點做三件事：進來時可以先做點事（前序）、遞迴左右小孩、回來後彙總小孩的結果（後序）。\n\n```cpp\n// 二叉樹遞迴萬用骨架\nvoid dfs(TreeNode* node) {\n    if (!node) {\n        return;  // 邊界：空節點\n    }\n    // 前序：進入時的處理\n    dfs(node->left);\n    dfs(node->right);\n    // 後序：彙總左右小孩的結果\n}\n```\n:::\n\n\n\n:::example 297 序列化：前序輸出，空節點寫成 \'#\'\n舉本節幾個例子看它如何變形。「序列化與反序列化」（Leetcode 297）：序列化就是前序把每個節點（含空節點用特殊符號）寫成字串；反序列化就是照同樣順序讀回、遞迴建樹。\n\n```cpp\n// 297 序列化：前序輸出，空節點寫成 \'#\'\nvoid ser(TreeNode* n, string& s) {\n    if (!n) {\n        s += "#,";\n                return;\n    }  // 空節點也要記，才能還原結構\n    s += to_string(n->val) + ",";\n              ser(n->left, s);\n    ser(n->right, s);\n}\n```\n:::\n\n\n\n「完全二叉樹的節點個數」（Leetcode 222）則利用完全二叉樹的特性：若左右子樹高度相同，左子樹是滿的，可用公式 `2^h - 1` 直接算，不必一個個數，把 `O(n)` 壓到 `O(log² n)`。「BST 迭代器」（173）則用一個堆疊模擬中序走訪，做到「每次呼叫回傳下一個最小值」。\n\n共同點：**先確定「節點要回報什麼 / 攜帶什麼」，再套 DFS 或 BFS 骨架。**\n\n## 狀態／資料結構定義\n\n- **297 序列化**：選一種遍歷（通常前序），並**顯式輸出空節點標記**（如 `#`）。還原時用一個游標依同樣順序消耗 token。狀態是「輸入串流的當前位置」。\n- **173 BST 迭代器**：用堆疊存「當前節點的祖先鏈中，右子樹尚未處理的節點」——與 2.1 迭代中序的堆疊語意完全相同。建構時把根的左鏈全部壓入；`next()` 彈出棧頂、輸出、再把其右子的左鏈壓入。\n- **222 完全二元樹計數**：狀態是當前子樹的根與其「左鏈深度」`hl` 與「右鏈深度」`hr`（沿最左／最右一路走到底的深度）。\n- **314 垂直遍歷**：給每個節點一個座標 $(col, row)$，根為 $(0,0)$，左子 $(col-1, row+1)$、右子 $(col+1, row+1)$；用 `map<int, vector<pair<int,int>>>` 按 `col` 分組。\n\n## 不變量或正確性證明\n\n**297 為何加上空節點標記就足以還原。** 2.10 已說明單一種遍歷無法唯一確定樹，根本原因是「單子節點的左右歸屬不明」。加上空標記後，前序序列變成對**擴充二元樹**（每個空位補一個外部節點）的前序遍歷，而擴充二元樹中每個內部節點都恰有兩個子節點。\n\n還原的正確性用歸納：讀到一個 token，若是 `#` 則回傳 `nullptr`（消耗一個 token）；否則建立節點，遞迴建左子樹、再遞迴建右子樹。**不變量是「每次呼叫 `deserialize()` 恰好消耗完整描述一棵子樹的那段 token，並回傳其根」**。基底是 `#`（消耗 1 個）。歸納步驟中，左子樹的遞迴消耗完自己那段後，游標恰好停在右子樹描述的開頭——這正是前序序列的定義所保證的。故整個過程無歧義。\n\n這解釋了 2.10 提到的「需要兩種序列的根本原因是空節點資訊遺失」：補回這個資訊，一種序列就夠了。\n\n**222 完全二元樹計數的複雜度論證。** 完全二元樹的定義是「除最後一層外都填滿，且最後一層節點靠左連續」。演算法：對當前子樹，計算最左鏈深度 `hl` 與最右鏈深度 `hr`。\n\n- 若 `hl == hr`，這棵子樹是**滿二元樹**，節點數為 $2^{hl} - 1$，$O(1)$ 得出。要說明這個推論成立：完全二元樹中若最左路徑與最右路徑等長，表示最後一層已填滿（否則最右鏈會短一層），故是滿的。\n- 否則遞迴計算 `1 + count(left) + count(right)`。\n\n複雜度的關鍵論證：**在完全二元樹中，任一節點的左右兩棵子樹中，至少有一棵是滿的**。理由：最後一層的節點靠左連續，所以「缺口」只出現在一條從根往下的路徑上；不在這條路徑上的子樹要嘛全滿、要嘛全空。因此遞迴每層只會有**一個**分支真正展開，另一個 $O(1)$ 解決。遞迴深度 $O(\\log n)$，每層計算 `hl`、`hr` 各需 $O(\\log n)$，總計 $O(\\log^2 n)$——遠優於樸素遍歷的 $O(n)$。\n\n**173 迭代器的攤還分析。** 單次 `next()` 最壞需要壓入一整條左鏈，$O(h)$。但每個節點在整個迭代過程中**恰被壓入一次、彈出一次**，故 $n$ 次 `next()` 的總成本是 $O(n)$，**攤還 $O(1)$**。空間 $O(h)$，這正是題目要求「不能預先把所有值倒進陣列」的原因——後者是 $O(n)$ 空間。\n\n**314 垂直遍歷的排序規則。** 同一列（相同 `col`）內，先按 `row` 由小到大；`row` 也相同時，題目通常要求按值排序（987）或按輸入順序（314）。若用 BFS 產生節點，同一 `(col, row)` 的節點天然按由左至右的順序出現，可省去一次排序——這是選 BFS 而非 DFS 的實質理由。\n\n## 逐步演算法\n\n這類題目可拆成四個固定步驟：\n\n:::example 兩種方向：選一個\n**1. 狀態表示：決定節點回報或攜帶什麼。** 是回報子樹摘要（高度、節點數），還是攜帶從根帶下來的資訊（路徑、深度、座標）？\n\n```cpp\n// 兩種方向：選一個\n// 自底向上：int dfs(node) 回傳子樹摘要\n// 自頂向下：void dfs(node, 累積狀態) 把狀態帶給小孩\n```\n:::\n\n\n\n:::example 中序：對 BST 會由小到大輸出（173 迭代器的基礎）\n**2. 套走訪骨架：DFS 或 BFS。** 需要層 / 距離概念用 BFS；需要彙總子樹用 DFS（後序）；需要按值有序輸出用中序。\n\n```cpp\n// 中序：對 BST 會由小到大輸出（173 迭代器的基礎）\nvoid inorder(TreeNode* n) {\n    if (!n) {\n        return;\n    }\n    inorder(n->left);\n    // 處理 n（此時 n 是「目前最小的未輸出值」）\n    inorder(n->right);\n}\n```\n:::\n\n\n\n:::example 222：左右子樹等高 → 左子樹是滿的，直接用公式\n**3. 用結構特性加速。** 像 222 利用「完全二叉樹左滿則公式直算」、垂直遍歷（314）用一個 map 依「列座標」分桶收集。\n\n```cpp\n// 222：左右子樹等高 → 左子樹是滿的，直接用公式\nint lh = leftHeight(node), rh = rightHeight(node);\nif (lh == rh) {\n    return (1 << lh) - 1;  // 2^lh - 1，不必逐一數\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。** 序列化要妥善處理空節點（漏了就無法還原）；迭代器要用堆疊保存「待回」路徑；遞迴深易爆 stack。複雜度視題而定，多為 `O(n)`，能用結構特性的可更快（如 222 的 `O(log² n)`）。\n\n## C++17 模板\n\n```cpp\n// 序列化與反序列化（LeetCode 297）：前序 + 空節點標記。\nclass Codec {\n public:\n  std::string serialize(TreeNode* root) {\n    if (!root) { return "#,"; }              // 空節點也要輸出\n    return std::to_string(root->val) + "," +\n           serialize(root->left) + serialize(root->right);\n  }\n\n  TreeNode* deserialize(const std::string& data) {\n    size_t pos = 0;\n    return build(data, pos);\n  }\n\n private:\n  TreeNode* build(const std::string& s, size_t& pos) {   // pos 是共享游標\n    size_t comma = s.find(\',\', pos);\n    std::string token = s.substr(pos, comma - pos);\n    pos = comma + 1;\n\n    if (token == "#") { return nullptr; }\n    TreeNode* node = new TreeNode(std::stoi(token));\n    node->left  = build(s, pos);             // 前序保證游標順序正確\n    node->right = build(s, pos);\n    return node;\n  }\n};\n```\n\n完全二元樹計數（LeetCode 222），$O(\\log^2 n)$：\n\n```cpp\nint countNodes(TreeNode* root) {\n  if (!root) { return 0; }\n\n  int hl = 0, hr = 0;\n  for (TreeNode* p = root; p; p = p->left)  { ++hl; }    // 最左鏈深度\n  for (TreeNode* p = root; p; p = p->right) { ++hr; }    // 最右鏈深度\n\n  if (hl == hr) { return (1 << hl) - 1; }                // 滿樹，O(1) 算出\n  return 1 + countNodes(root->left) + countNodes(root->right);\n}\n```\n\n## 時間與空間複雜度\n\n297：序列化與反序列化各 $O(n)$ 時間，序列長度 $O(n)$。遞迴 $O(h)$ 堆疊。\n\n173：建構 $O(h)$，`next()` 與 `hasNext()` 攤還 $O(1)$（最壞單次 $O(h)$），空間 $O(h)$。\n\n222：$O(\\log^2 n)$ 時間、$O(\\log n)$ 堆疊，依據上面的「每層只有一個分支展開」論證。\n\n314／987：$O(n \\log n)$ 時間（排序或 `map` 的開銷），$O(n)$ 空間。\n\n## 常見錯誤與邊界條件\n\n- 297 用 `int` 解析而節點值為負或超出範圍。\n- 297 的游標未以引用傳遞，各層各自從頭讀。\n- 297 序列化格式的分隔符與節點值衝突。\n- 222 直接用 $O(n)$ 遍歷（能過但沒抓到題目給「完全二元樹」的用意）。\n- 222 的 `1 << hl` 在 `hl` 較大時溢位（用 `1LL`）。\n- 173 在建構時就把所有值倒進陣列（違反空間要求）。\n- 314 忘記處理同一格多個節點的次序規則。\n- 空樹在各題的回傳約定。\n\n## 與相似技巧的比較\n\n- 297 與 2.10 的「兩種序列建樹」互為對照：前者補上空節點資訊所以一種序列就夠，後者靠第二種序列提供切分點。428（N 叉樹序列化）則需編碼子節點數量，見 2.15。\n- 173 的堆疊與 2.1 迭代中序完全相同，只是把「一次跑完」拆成「每次吐一個」；這種把遞迴改寫成可暫停的迭代器，是「顯式堆疊」最有價值的用途。\n- 222 的「利用結構保證省下遍歷」與 BST 的 $O(h)$ 查找同屬一類思路：額外的結構保證換取複雜度。\n- 60（排列序號）其實與樹的關係是概念性的——它在「排列的決策樹」上直接定位第 $k$ 個葉節點，靠的是「以某個數字開頭的排列恰有 $(n-1)!$ 個」這條計數性質，一路除法定位而不必真的建樹。這是「用計數代替搜尋」的典型，與 4.x 的回溯形成對比。\n\n## 代表例題\n\n**題號**：60、173、222、297、314\n\n放在「其他」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 60 | [LCP 60. 力扣泡泡龍](https://leetcode.cn/problems/WInSav/) |  |\n| 173 | [二叉搜索樹迭代器](https://leetcode.cn/problems/binary-search-tree-iterator/) |  |\n| 222 | [完全二叉樹的節點個數](https://leetcode.cn/problems/count-complete-tree-nodes/) |  |\n| 297 | [二叉樹的序列化與反序列化](https://leetcode.cn/problems/serialize-and-deserialize-binary-tree/) |  |\n| 314 | [二叉樹的垂直遍歷](https://leetcode.cn/problems/binary-tree-vertical-order-traversal/)（Premium） |  |\n| 331 | [驗證二叉樹的前序序列化](https://leetcode.cn/problems/verify-preorder-serialization-of-a-binary-tree/) |  |\n| 431 | [將 N 叉樹編碼為二叉樹](https://leetcode.cn/problems/encode-n-ary-tree-to-binary-tree/) |  |\n| 449 | [序列化和反序列化二叉搜索樹](https://leetcode.cn/problems/serialize-and-deserialize-bst/) |  |\n| 652 | [尋找重複的子樹](https://leetcode.cn/problems/find-duplicate-subtrees/) |  |\n| 655 | [輸出二叉樹](https://leetcode.cn/problems/print-binary-tree/) |  |\n| 666 | [路徑總和 IV](https://leetcode.cn/problems/path-sum-iv/) |  |\n| 979 | [在二叉樹中分配硬幣](https://leetcode.cn/problems/distribute-coins-in-binary-tree/) | 1709 |\n| 987 | [二叉樹的垂序遍歷](https://leetcode.cn/problems/vertical-order-traversal-of-a-binary-tree/) | 1676 |\n| 1104 | [二叉樹尋路](https://leetcode.cn/problems/path-in-zigzag-labelled-binary-tree/) | 1545 |\n| 1261 | [在受汙染的二叉樹中查詢元素](https://leetcode.cn/problems/find-elements-in-a-contaminated-binary-tree/) | 1440 |\n| 1485 | [克隆含隨機指標的二叉樹](https://leetcode.cn/problems/clone-binary-tree-with-random-pointer/) |  |\n| 1586 | [二叉搜索樹迭代器 II](https://leetcode.cn/problems/binary-search-tree-iterator-ii/) |  |\n| 2005 | [斐波那契樹的移除子樹遊戲](https://leetcode.cn/problems/subtree-removal-game-with-fibonacci-tree/) |  |\n| 2049 | [統計最高分的節點數目](https://leetcode.cn/problems/count-nodes-with-the-highest-score/) | 1912 |\n| 2445 | [值為 1 的節點數](https://leetcode.cn/problems/number-of-nodes-with-value-one/) |  |\n| 2458 | [移除子樹後的二叉樹高度](https://leetcode.cn/problems/height-of-binary-tree-after-subtree-removal-queries/) | 2299 |\n| 2509 | [查詢樹中環的長度](https://leetcode.cn/problems/cycle-length-queries-in-a-tree/) | 1948 |\n| LCP 26 | [LCP 26. 導航裝置](https://leetcode.cn/problems/hSRGyL) |  |\n| 2673 | [使二叉樹所有路徑值相等的最小代價](https://leetcode.cn/problems/make-costs-of-paths-equal-in-a-binary-tree/) | 1917 |\n| 2773 | [特殊二叉樹的高度](https://leetcode.cn/problems/height-of-special-binary-tree/) |  |\n\n## 本節重點速查\n\n- 看到「完全二元樹」「BST」這類結構保證，先問能省下什麼——222 靠「至少一棵子樹是滿的」把 $O(n)$ 壓到 $O(\\log^2 n)$。\n- 297 補上空節點標記後單一種前序即可唯一還原，反序列化的游標必須跨遞迴共享。\n- 173 用堆疊實作可暫停的中序，攤還 $O(1)$、空間 $O(h)$。\n',
        },
      ],
    },
    {
      id: 31,
      title: "3. 一般樹",
      summary:
        "涵蓋一般樹的遍歷、DFS、LCA、直徑、拓撲排序、時間戳、點分治等，適合進階學習。\n\n:::example 一般樹 DFS（鄰接表）\n**C++ 模板（一般樹 DFS）**：\n\n```cpp\n// 一般樹 DFS（鄰接表）\nvoid dfs(int u, int fa, vector<vector<int>>& adj) {\n    // 處理節點 u\n    for (int v : adj[u]) {\n        if (v != fa) {\n            dfs(v, u, adj);\n        }\n    }\n}\n```\n:::",
      children: [
        {
          id: 32,
          title: "3.1 遍歷",
          summary:
            "## 這個技術解決什麼問題\n\n一般樹的輸入通常不是節點指標，而是**邊的集合**（`edges[i] = {u, v}`）或**父節點陣列**（`parent[i]`）。這帶來兩個二元樹沒有的問題：邊是無向的，所以從 $u$ 走到 $v$ 之後，$v$ 的鄰居裡還有 $u$；如果不擋住，DFS 會在兩個節點之間來回震盪，永不終止。\n\n本節建立一般樹的基本工作方式：**建鄰接表 + DFS 時傳入父節點以避免回頭**。這是 3.x 所有內容的地基，也是與 2.x 的主要介面差異。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「圖遍歷」想：\n\n- **資料是「點 + 邊」**（城市與道路、節點與連線），問「能到哪些點」「有幾個連通塊」「最少步數」。1466、2368、582 都是這型。\n- **你發現可能繞圈或重複走到同一點**——這就是要加 visited 的訊號：\n\n:::example 嗅到味道的瞬間：同一個點會被多條路走到\n```cpp\n// 嗅到味道的瞬間：同一個點會被多條路走到\n// 反射動作：準備 visited，進點先檢查再標記，保證每點只處理一次\n```\n:::\n\n\n\n- **要「從某點擴散到所有相連的點」**，DFS / BFS 走一遍就是答案的骨架。\n- **暴力對每個查詢都重走整張圖太慢**，一次遍歷把每點每邊只看一次，壓成 `O(V + E)`。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我怎麼把題目的邊存成鄰接表？是有向還是無向？\n2. 我的 visited 在哪裡檢查、哪裡標記？能保證每點只處理一次嗎？\n3. 答案是在走訪過程中累加（可達數 / 反轉數），還是走完才統計？\n\n三題都答得出來，就套對了。\n\n## 核心想法與直覺\n\n本節進入「圖（graph）」的世界，主題是最基本也最重要的能力：**走遍整張圖**。圖就是「一堆點，加上點與點之間的連線（邊）」。樹其實是圖的特例（無環、連通），所以樹會的 DFS/BFS，在圖上一樣能用，只是多了一個必須注意的點。\n\n:::example 圖走訪的鐵則：每個點只處理一次，靠 visited 防重複/防繞圈\n那個關鍵差別是：**圖可能有環，同一個點可能從好幾條路走到**。如果不做記號，就會無限繞圈或重複處理。所以圖走訪一定要有一個「visited（去過了沒）」標記。\n\n```cpp\n// 圖走訪的鐵則：每個點只處理一次，靠 visited 防重複/防繞圈\nvector<bool> visited(n, false);\n// 進入一個點時：先檢查、再標記\nif (visited[u]) {\n    return;  // 去過就別再進\n}\nvisited[u] = true;\n```\n:::\n\n\n\n:::example 鄰接表 + DFS：標記自己，再遞迴每個沒去過的鄰居\n圖通常用「鄰接表（adjacency list）」存：對每個點，記下它能直接到的鄰居清單。走訪就是「從某點出發，沿著鄰居一個個展開，直到全圖看完」。\n\n```cpp\n// 鄰接表 + DFS：標記自己，再遞迴每個沒去過的鄰居\nvector<vector<int>> g(n);  // g[u] 是 u 的所有鄰居\nvoid dfs(int u) {\n    visited[u] = true;\n    for (int v : g[u]) {\n        if (!visited[v]) {\n            dfs(v);  // 只走沒去過的鄰居\n        }\n    }\n}\n```\n:::\n\n\n\n有了「走遍全圖」這個基本功，很多題目就成立了。例如「受限條件下可到達節點的數目」（Leetcode 2368）：把被限制的點先標成 visited（等於封路），再從 0 號點 DFS，能走到幾個點就是答案。又如「重新規劃路線」（Leetcode 1466）：把每條邊同時記下「原方向」，走訪時若方向是「背離 0 號城市」就需要反轉，邊走邊累加答案。每個點、每條邊各處理一次，複雜度 `O(點 + 邊)`。\n\n## 狀態／資料結構定義\n\n- `vector<vector<int>> g(n)`：鄰接表。無向邊要**雙向加入**：`g[u].push_back(v); g[v].push_back(u);`\n- `dfs(u, fa)`：`u` 是當前節點，`fa` 是它的父節點（根的父節點用 $-1$ 表示，因為節點編號從 0 起算）。迴圈中 `if (v == fa) continue;` 就是防止回頭的全部機制。\n- 若題目未指定根，任選一個節點（通常是 0）當根即可——樹的許多性質（節點數、邊數、直徑）與選誰當根無關，但「子樹」「深度」「祖先」這些概念會隨根改變，所以題目要求這些量時必須用指定的根。\n- 父節點陣列 `parent[i]` 的輸入（1466、2368）可直接建有向鄰接表，此時不需要 `fa` 參數。\n\n## 不變量或正確性證明\n\n**為何 `v == fa` 就足以防止重複訪問。** 在一般圖上，DFS 必須用 `visited` 陣列；但在**樹**上，只要擋住父節點就夠了。理由是樹的定義性質：任兩節點之間恰有一條簡單路徑，等價地，樹中無環。\n\n嚴格論證：假設 DFS 從根出發，遞迴到 $u$ 且來自 $fa$。若某個鄰居 $v \\ne fa$ 已經被訪問過，則存在一條從 $v$ 到 $u$ 的路徑（沿著 DFS 的呼叫鏈），再加上邊 $(u, v)$ 就構成一個環。這與樹無環矛盾。故 $u$ 的所有非父鄰居都尚未被訪問，`v == fa` 這一個判斷就涵蓋了全部重複情形。\n\n**這條論證的前提是輸入真的是樹**——$n$ 個節點、$n-1$ 條邊、且連通。若題目給的其實是有環的圖，或給了重邊（同一對節點之間兩條邊），`v == fa` 就不夠了：重邊的情形下，第二條 $u$–$fa$ 邊會通過檢查，造成無限遞迴。此時必須改用 `visited` 陣列，或用邊的編號而非節點編號來判斷「來時路」。**看到「保證是樹」才能用 `fa` 判斷，這不是可以省略的細節。**\n\n**每個節點恰訪問一次。** 由上，DFS 從根出發能訪問到所有節點（連通性保證），且每個節點只被其唯一的父節點訪問一次（樹中除根外每個節點恰有一個父節點）。故總訪問次數為 $n$。\n\n**兩種時機**與 2.1 相同：進入 `u` 時手上有「根到 `u` 的路徑」資訊（前序，見 3.2），離開時手上有「`u` 的子樹」彙總（後序，見 3.3）。一般樹沒有中序的概念——子節點順序沒有「左右」的語意區分，就算有多個子節點也無法定義根該插在哪裡。\n\n## 逐步演算法\n\n圖遍歷題目可拆成四個固定步驟：\n\n:::example 建圖：把邊清單轉成鄰接表（這裡是無向圖，兩邊都加）\n**1. 狀態表示：建鄰接表 + visited 陣列。** 先把題目給的邊轉成「每個點的鄰居清單」，再準備一個 visited 防重複。\n\n```cpp\n// 建圖：把邊清單轉成鄰接表（這裡是無向圖，兩邊都加）\nvector<vector<int>> g(n);\nfor (auto& e : edges) {\n    g[e[0]].push_back(e[1]);\n    g[e[1]].push_back(e[0]);\n}\n```\n:::\n\n\n\n:::example BFS 版走遍：從 src 出發一層層擴散\n**2. 選 DFS 或 BFS 走訪。** 兩者都能走遍全圖：DFS 用遞迴/堆疊一條路深入；BFS 用佇列一圈圈擴散（要算「最短步數」時選 BFS）。\n\n```cpp\n// BFS 版走遍：從 src 出發一層層擴散\nqueue<int> q;\nq.push(src);\nvisited[src] = true;\nwhile (!q.empty()) {\n    int u = q.front();\n    q.pop();\n    for (int v : g[u]) {\n        if (!visited[v]) {\n            visited[v] = true;\n            q.push(v);\n        }\n    }\n}\n```\n:::\n\n\n\n:::example 2368 思路：先把受限點當成「已訪」封死，再從 0 出發數可達點\n**3. 在走訪過程中順手算答案。** 走到一個點就 `count++`（數可達點數）、或檢查這條邊的方向是否要反轉（累加反轉次數）。把要算的東西嵌進走訪迴圈即可。\n\n```cpp\n// 2368 思路：先把受限點當成「已訪」封死，再從 0 出發數可達點\nfor (int x : restricted) {\n    visited[x] = true;\n}\n// 之後 dfs(0)，過程中每進一個新點就 ++count\n```\n:::\n\n\n\n**4. 邊界與複雜度。** 圖不一定連通，要遍歷「所有點」找答案時，需對每個沒去過的點都起一次走訪（數連通塊就是這樣）；有向圖建圖只加單向。複雜度時間 `O(V + E)`（點數加邊數），空間 `O(V + E)`；DFS 遞迴深時小心爆 stack，可改 BFS。\n\n## C++17 模板\n\n```cpp\n// 一般樹的基本 DFS：建鄰接表，用 fa 擋住回頭路。\nclass Solution {\n  std::vector<std::vector<int>> g_;\n\n  void dfs(int u, int fa) {\n    // 進入 u：此時「根到 u 的路徑」資訊已備妥（前序時機）\n    for (int v : g_[u]) {\n      if (v == fa) { continue; }     // 唯一需要的防重複機制（前提：輸入是樹）\n      dfs(v, u);\n    }\n    // 離開 u：此時所有子樹的彙總已備妥（後序時機）\n  }\n\n public:\n  void solve(int n, const std::vector<std::vector<int>>& edges) {\n    g_.assign(n, {});\n    for (const auto& e : edges) {\n      g_[e[0]].push_back(e[1]);      // 無向邊必須雙向加入\n      g_[e[1]].push_back(e[0]);\n    }\n    dfs(0, -1);                      // 任選 0 當根；根的父節點記為 -1\n  }\n};\n```\n\n節點數較大（$n$ 上看 $10^5$）時遞迴深度可能達 $n$，斜樹會爆堆疊。改成顯式堆疊的迭代版，或在支援的環境下加大堆疊。\n\n## 時間與空間複雜度\n\n建圖 $O(n)$（$n-1$ 條邊各加入兩次）。DFS 中，對 `g[u]` 的迴圈總共執行 $\\sum_u \\deg(u) = 2(n-1)$ 次，故遍歷是 $O(n)$ 時間——**不是 $O(n \\cdot \\text{maxdeg})$**，理由與 2.15 相同：所有度數之和等於邊數的兩倍。\n\n空間：鄰接表 $O(n)$，遞迴堆疊 $O(h)$，最壞 $O(n)$（鏈狀樹）。\n\n用 `unordered_map` 存鄰接表會慢很多且沒必要——節點編號通常是 $0$ 到 $n-1$ 的連續整數，直接用 `vector` 索引。\n\n## 常見錯誤與邊界條件\n\n- 無向邊只加一個方向，導致樹被切斷、DFS 只走到部分節點。\n- 忘記傳 `fa` 或忘記 `v == fa` 的檢查，無限遞迴至爆堆疊。\n- 根的 `fa` 用 0 而非 $-1$（若根恰為 0 號節點，會誤擋掉真正的鄰居 0）。\n- 輸入其實是圖或含重邊時仍只用 `fa` 判斷。\n- $n = 1$（沒有邊，鄰接表全空）。\n- 節點編號從 1 起算而陣列開 $n$ 大小。\n- 深度大時爆堆疊。\n\n## 與相似技巧的比較\n\n- 與 2.15 的 N 叉樹相比：N 叉樹的 `children` 已經是有向的父到子關係，不會走回頭，所以不需要 `fa`。**輸入形式決定要不要傳 `fa`**，這是兩者唯一的實質差異。\n- 與圖的 DFS（graph 主題）相比：圖需要 `visited` 陣列，因為可能有環且節點可從多條路徑到達。樹省下這個是因為無環性質，如上所證。\n- 父節點陣列輸入（1466）可直接建立有向邊，此時問題往往退化成「沿著父指標往上走」或「按拓撲序處理」，見 3.6。\n- BFS 在一般樹上同樣可用，防重複機制相同（擋住父節點），適合求「層」與「距離」，見 3.5 的兩次 BFS 求直徑。\n\n## 代表例題\n\n**題號**：582、1466、2368\n\n放在「遍歷」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 582 | [殺掉程式](https://leetcode.cn/problems/kill-process/)（Premium） |  |\n| 1466 | [重新規劃路線](https://leetcode.cn/problems/reorder-routes-to-make-all-paths-lead-to-the-city-zero/) | 1634 |\n| 2368 | [受限條件下可到達節點的數目](https://leetcode.cn/problems/reachable-nodes-with-restrictions/) | 1477 |\n\n## 本節重點速查\n\n- 一般樹的輸入是邊集合，先建**雙向**鄰接表。\n- DFS 傳 `dfs(u, fa)`，用 `v == fa` 擋回頭——這在樹上（無環、無重邊）就足夠，在圖上不夠。\n- 根的 `fa` 用 $-1$。\n- 遍歷 $O(n)$，因為度數總和是 $2(n-1)$。\n- 沒有中序的概念，只有進入與離開兩個時機。\n",
        },
        {
          id: 33,
          title: "3.2 自頂向下 DFS",
          summary:
            "## 這個技術解決什麼問題\n\n與 2.2 相同的需求，換到一般樹上：答案取決於**從根走到當前節點這條路徑**——深度、路徑上的權重和、路徑上的最大邊權、路徑上某種狀態的累積（1376 通知員工所需時間、1443 收集蘋果的最短時間中的一部分）。\n\n單獨對每個節點沿父指標往上算一遍是 $O(nh)$；把路徑資訊當參數往下傳，每個節點只做常數工作，$O(n)$。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就往「自頂向下 DFS」想：\n\n- **答案和「從根到該節點的路徑」有關**：到根距離、路徑累加值、沿途收集到的東西。資訊天然是「由上往下流」。\n- **子節點的答案可以由父節點的答案直接推出**：像 1376 的「下屬收到通知時刻 = 上司時刻 + 上司耗時」，1443 把開銷從根往各子節點傳。一旦發現「父算完，子就能算」，就是這個模式。\n- **暴力做法是對每個節點都重新從根爬一次**（一次 `O(深度)`，全部 `O(n²)`）；自頂向下一次 DFS 把累積值順手帶下去，攤平成 `O(n)`。\n- 嗅到味道的瞬間：\n\n:::example 你發現「要算 u 的答案，只需要父親 fa 已算好的值 + 一條邊」\n```cpp\n// 你發現「要算 u 的答案，只需要父親 fa 已算好的值 + 一條邊」\n// ans[u] = ans[fa] + something;   // 就把 something 當參數往下傳\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要往下帶的「狀態」是什麼？（深度？路徑和？）\n2. 從父進到子時，這個狀態怎麼更新？\n3. 答案是在「進入節點當下」就能寫好，還是其實得等子樹算完（那就該用自底向上）？\n\n三題都答得清楚，這個模型就套對了。\n\n## 核心想法與直覺\n\n本節要解決的場景是：資料長成一棵「樹」（每個節點有一個父節點、若干子節點，沒有環），我們要算的答案和「從根往下、一路累積的資訊」有關。例如「某個節點到根的距離」「從根走到這裡經過的累加值」。這類問題的共同點是：**子節點需要的資訊，父節點都已經算好了，只要往下傳即可。**\n\n這就是「自頂向下 DFS」。DFS（深度優先搜尋）就是「從根出發，先一路鑽到底，再回頭走另一條分支」的走法。所謂「自頂向下」，是指我們在「往下走、進入子節點之前」就把一份**從祖先累積下來的狀態**算好，當作參數傳給子節點。\n\n:::example 樹 DFS 的最基本骨架：fa 記住父節點，避免走回頭\n先看最基本的樹 DFS 骨架。樹用「鄰接表」表示：`g[u]` 是 `u` 的所有鄰居。因為樹沒有環，只要記得「從哪來」就不會走回頭路：\n\n```cpp\n// 樹 DFS 的最基本骨架：fa 記住父節點，避免走回頭\nvoid dfs(int u, int fa) {\n    for (int v : g[u]) {\n        if (v == fa) {\n            continue;  // v 是父節點就跳過\n        }\n        dfs(v, u);  // 往下遞迴，u 變成 v 的父\n    }\n}\n```\n:::\n\n\n\n:::example 自頂向下：depth 由父節點 +1 傳給子節點\n「自頂向下」的關鍵，是多帶一個參數，把「從根累積到目前」的狀態一路往下傳。例如算每個節點到根的深度：\n\n```cpp\n// 自頂向下：depth 由父節點 +1 傳給子節點\nvoid dfs(int u, int fa, int depth) {\n    dep[u] = depth;  // 進入 u 時就確定它的深度\n    for (int v : g[u]) {\n        if (v == fa) {\n            continue;\n        }\n        dfs(v, u, depth + 1);  // 把「父的深度+1」帶給子\n    }\n}\n```\n:::\n\n\n\n重點在於：答案在「下行（遞）」的途中就決定了，不必等子樹回傳。像 1376「通知所有員工所需的時間」，每個員工的「收到通知時刻」＝主管的時刻＋主管的通知耗時，正是把時間從上司一路往下加；1443 把訊息（或開銷）從根帶往各節點，也是同一個模式。整體只走每個節點一次，複雜度 `O(n)`。\n\n## 狀態／資料結構定義\n\n- `dfs(u, fa, acc)`：`acc` 是根到 `u` 之父的累積量（或到 `u` 為止，兩種語意擇一並貫徹）。\n- 常見的 `acc` 型別：深度 `int`、路徑和 `long long`（邊權相加容易溢位）、路徑上的最大值、位元遮罩（記錄路徑上出現過的字元集合）。\n- 若邊帶權，鄰接表要存 `pair<int, int>`（鄰居, 邊權）或 `vector<array<int,2>>`。\n- 答案通常存進全域陣列 `ans[u]`（每個節點各有一個答案）或單一全域變數（取全樹最值）。\n\n## 不變量或正確性證明\n\n不變量：**每次呼叫 `dfs(u, fa, acc)` 時，`acc` 恰等於根到 `u` 之父這條路徑所定義的累積量。**\n\n- 初始化：`dfs(root, -1, e)`，$e$ 是空路徑的單位元。\n- 保持：計算 `acc' = combine(acc, u)`（或含邊權 `combine(acc, w(fa,u))`），對每個非父鄰居 `v` 呼叫 `dfs(v, u, acc')`。對 `v` 而言根到其父（即 `u`）的累積量正是 `acc'`，不變量維持。\n- 結論：每個節點拿到的參數都正確。\n\n適用前提與 2.2 相同：**所需資訊必須是路徑的函數，不能依賴子樹**。此外一般樹多一層考量——由於子節點不只兩個，若 `acc` 是可變的共享容器，**每個子節點遞迴後都要撤銷**（回溯），不是只在最後撤銷一次。這比二元樹更容易寫錯，因為迴圈裡的 push/pop 配對不像兩行遞迴那樣顯眼。\n\n**1376（通知所有員工所需時間）的正確性。** 每個員工 $u$ 收到通知的時刻等於根到 $u$ 路徑上所有上司的 `informTime` 之和。答案是所有葉節點時刻的最大值。這裡值得點名的是**為何只需看葉節點**：通知時間沿路徑單調不減（`informTime` 非負），故任一節點的時刻不超過其某個後代葉節點的時刻。因此取全體節點的最大值與只取葉節點的最大值相同，實作上兩種寫法都對。若 `informTime` 可為負，單調性失效，就必須取全體節點的最大值。\n\n**路徑資訊為何不能事後補算。** 有人會想「先做一次 DFS 記下每個節點的父親，再對每個節點沿父指標往上累加」。這是對的但成本是 $O(nh)$，斜樹時 $O(n^2)$。參數傳遞把「重複走祖先鏈」的部分共享掉了——每條祖先鏈的公共前綴只算一次。這個共享正是自頂向下的價值。\n\n## 逐步演算法\n\n把任何「自頂向下」題目拆成三件事：狀態怎麼帶、怎麼往下傳、答案在哪讀。\n\n:::example 狀態就是 DFS 的參數：這裡帶「到根的路徑和」\n**1. 決定要往下帶的狀態。** 問自己：「子節點要算它的答案，需要知道哪些『從祖先來的』資訊？」常見是深度、到根的路徑和、或一個累積的集合。把它做成 DFS 的參數。\n\n```cpp\n// 狀態就是 DFS 的參數：這裡帶「到根的路徑和」\nvoid dfs(int u, int fa, long long sum) {\n    sum += val[u];     // 加上目前節點的值\n    rootSum[u] = sum;  // 記下 u 到根的累積和\n                                          // ... 往下遞迴\n}\n```\n:::\n\n\n\n:::example 更新後再下傳：把「父的狀態 + 這條邊的貢獻」交給子\n**2. 進入子節點前更新狀態，再遞迴。** 「自頂向下」的精神就是：先把狀態更新成「子節點看到的版本」，再傳下去。\n\n```cpp\n// 更新後再下傳：把「父的狀態 + 這條邊的貢獻」交給子\nfor (int v : g[u]) {\n    if (v == fa) {\n        continue;\n    }\n    dfs(v, u, sum + w[u][v]);  // w 是邊權；沒有邊權時就傳 sum\n}\n```\n:::\n\n\n\n:::example 答案：每個節點進來時就寫好，最後直接讀陣列\n**3. 讀答案。** 自頂向下的答案通常「進入節點當下就確定」，所以可以邊走邊寫進一個全域陣列，或在進入葉節點時收集。\n\n```cpp\n// 答案：每個節點進來時就寫好，最後直接讀陣列\n// dfs(root, -1, 起始狀態);\n// for (int i = 0; i < n; ++i) cout << rootSum[i];\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 一定要傳 `fa`（或用 visited 標記）擋住回頭路，否則父子互相遞迴會無限迴圈。\n- 樹很深（接近一條鏈）時遞迴層數可能上萬，有爆 stack 風險；必要時改成用自己的堆疊模擬，或確認題目 n 的上限。\n- 每個節點、每條邊各處理一次，時間 `O(n)`，遞迴額外空間 `O(樹高)`。\n\n## C++17 模板\n\n```cpp\n// 一般樹自頂向下：計算每個節點到根的距離（帶權）。\nclass Solution {\n  std::vector<std::vector<std::pair<int, int>>> g_;   // {鄰居, 邊權}\n  std::vector<long long> dist_;\n\n  void dfs(int u, int fa, long long acc) {\n    dist_[u] = acc;                       // 進入時，acc 已是根到 u 的距離\n    for (auto [v, w] : g_[u]) {\n      if (v == fa) { continue; }\n      dfs(v, u, acc + w);                 // 純量按值傳遞，天然隔離，不需撤銷\n    }\n  }\n\n public:\n  std::vector<long long> solve(int n, const std::vector<std::vector<int>>& edges) {\n    g_.assign(n, {});\n    for (const auto& e : edges) {\n      g_[e[0]].push_back({e[1], e[2]});\n      g_[e[1]].push_back({e[0], e[2]});\n    }\n    dist_.assign(n, 0);\n    dfs(0, -1, 0);\n    return dist_;\n  }\n};\n```\n\n若 `acc` 是共享容器（例如記錄路徑上的節點），迴圈裡要配對撤銷：\n\n```cpp\npath.push_back(u);\nfor (int v : g_[u]) {\n  if (v == fa) { continue; }\n  dfs(v, u);                 // 每個子節點都在同一份 path 上遞迴\n}\npath.pop_back();             // 離開 u 時撤銷一次即可（push 也只有一次）\n```\n\n## 時間與空間複雜度\n\n每個節點訪問一次、每條邊掃描兩次，$O(n)$ 時間。純量 `acc` 不增加空間；共享容器 `acc` 佔 $O(h)$。遞迴堆疊 $O(h)$，最壞 $O(n)$。\n\n相對於「對每個節點各自往上追溯」的 $O(nh)$，收穫來自祖先鏈公共前綴的共享。\n\n## 常見錯誤與邊界條件\n\n- `acc` 的語意（到 `u` 還是到 `u` 之父）中途改變。\n- 共享容器忘記撤銷，或在迴圈內部誤加了多餘的 push/pop。\n- 邊權和用 `int` 溢位。\n- 忘記 `v == fa` 而無限遞迴。\n- 根的初始 `acc` 沒設成單位元。\n- 1376 中 `informTime` 為 0 的葉節點也要納入計算。\n- $n = 1$ 時答案為 0。\n- 深度大時爆堆疊。\n\n## 與相似技巧的比較\n\n- 自底向上（3.3）傳子樹彙總；本節傳路徑資訊。判斷依據仍是「所需資訊是路徑的函數還是子樹的函數」。\n- 兩者結合是 3.4，換根 DP 更是把兩個方向的資訊都用滿。\n- 與 2.2 的唯一差異是要傳 `fa` 且子節點用迴圈；不變量與適用條件完全相同。\n- BFS 也能自頂向下傳遞資訊（按層推進），且不會爆堆疊，在深度很大時是實用的替代；但 BFS 拿不到「當前路徑」本身，需要路徑時仍得用 DFS。\n- 3.7 的 DFS 時間戳是自頂向下的一個特化用途：進入與離開時各記一個時間，用來 $O(1)$ 判斷祖先關係。\n\n## 代表例題\n\n**題號**：3535、1376、3528、1443、3820\n\n放在「自頂向下 DFS」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 3535 | [單位轉換 II](https://leetcode.cn/problems/unit-conversion-ii/)（Premium） |  |\n| 1376 | [通知所有員工所需的時間](https://leetcode.cn/problems/time-needed-to-inform-all-employees/) | 1561 |\n| 3528 | [單位轉換 I](https://leetcode.cn/problems/unit-conversion-i/) | 1580 |\n| 1443 | [收集樹上所有蘋果的最少時間](https://leetcode.cn/problems/minimum-time-to-collect-all-apples-in-a-tree/) | 1683 |\n| 3820 | [樹上的勾股距離節點](https://leetcode.cn/problems/pythagorean-distance-nodes-in-a-tree/) | 1725 |\n| 1377 | [T 秒後青蛙的位置](https://leetcode.cn/problems/frog-position-after-t-seconds/) | 1824 |\n| 3067 | [在帶權樹網路中統計可連線伺服器對數目](https://leetcode.cn/problems/count-pairs-of-connectable-servers-in-a-weighted-tree-network/) | 1909 |\n| 3372 | [連線兩棵樹後最大目標節點數目 I](https://leetcode.cn/problems/maximize-the-number-of-target-nodes-after-connecting-trees-i/) | 1927 |\n| 2467 | [樹上最大得分和路徑](https://leetcode.cn/problems/most-profitable-path-in-a-tree/) | 2053 |\n| 3373 | [連線兩棵樹後最大目標節點數目 II](https://leetcode.cn/problems/maximize-the-number-of-target-nodes-after-connecting-trees-ii/) | 2162 |\n| 1766 | [互質樹](https://leetcode.cn/problems/tree-of-coprimes/) | 2232 |\n| 2791 | [樹中可以形成迴文的路徑數](https://leetcode.cn/problems/count-paths-that-can-form-a-palindrome-in-a-tree/) | 2677 |\n\n## 本節重點速查\n\n- 把根到當前節點的資訊當**參數往下傳**，不變量是「`acc` 等於根到父的累積量」。\n- 純量按值傳遞天然隔離；\n- 共享容器必須配對撤銷，一般樹的迴圈結構讓這件事比二元樹更容易寫錯。\n- 適用前提：所需資訊是路徑的函數。\n- $O(n)$ 時間，勝過逐點追溯祖先鏈的 $O(nh)$。\n",
        },
        {
          id: 34,
          title: "3.3 自底向上 DFS",
          summary:
            "## 這個技術解決什麼問題\n\n與 2.3 相同的需求，換到一般樹上：答案取決於**以當前節點為根的整棵子樹**——子樹大小、子樹權重和、子樹中的最值、子樹是否滿足某條件（1519 統計子樹中相同標籤的節點數、3004 最大星形和）。\n\n一次後序遍歷同時算出**所有節點**的子樹彙總，$O(n)$；相對於「對每個節點各跑一次子樹掃描」的 $O(n \\cdot \\text{subtree size})$ 總和（最壞 $O(n^2)$）。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就往「自底向上 DFS」想：\n\n- **要算某節點的答案，必須先知道它整棵子樹的資訊**：子樹大小、子樹和、子樹裡某種東西的數量。資訊天然「由下往上」彙總，像 690「員工的重要性」要把下屬的重要度全部加上來。\n- **答案是「每棵子樹各算一份，再合併」**：3249 要比較各子樹大小是否一致，3004「相同顏色的最大子樹」要回傳「子樹是否同色＋大小」，1519 由子往上累加字母計數。一看到「合併子結果」就是它。\n- **暴力做法是對每個節點都重新遍歷它整棵子樹**（一次最壞 `O(n)`，全部 `O(n²)`）；自底向上一次 DFS 讓每棵子樹只算一次，攤平成 `O(n)`。\n- 嗅到味道的瞬間：\n\n:::example 你發現「u 的答案 = 合併(各個子節點 v 的子樹結果)」\n```cpp\n// 你發現「u 的答案 = 合併(各個子節點 v 的子樹結果)」\n// 就讓 dfs 回傳那個結果，在迴圈裡收齊後再算 u\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的 dfs 要回傳什麼「子樹摘要」？型別是數字還是表？\n2. 拿到每棵子樹的摘要後，我用什麼規則合併成自己的？\n3. 最終答案在根的回傳值，還是每個節點各一份？\n\n三題都答得清楚，這個模型就套對了。\n\n## 核心想法與直覺\n\n本節要解決的場景是：答案和「整棵子樹的彙總資訊」有關。也就是說，要算某個節點的結果，你得先知道它**所有子節點各自的子樹算出了什麼**，再把這些結果合併起來。例如「以某節點為根的子樹有多少節點」「子樹節點值的總和」「子樹高度」。\n\n這就是「自底向上 DFS」。它和自頂向下相反：自頂向下是「進入子節點前」就把祖先資訊算好傳下去；自底向上則是「子節點全部遞迴完、回來之後」才用子樹的結果算自己。換句話說，答案是在「回程（歸）」產生的——遞迴先一路鑽到葉子，再從葉子一層層往上彙總。\n\n:::example 自底向上：dfs 回傳「以 u 為根的子樹大小」\n先看骨架。和一般樹 DFS 一樣記住父節點防回頭，但這次讓 `dfs` **回傳一個值**——代表「這棵子樹的摘要」：\n\n```cpp\n// 自底向上：dfs 回傳「以 u 為根的子樹大小」\nint dfs(int u, int fa) {\n    int size = 1;  // 自己算 1 個\n    for (int v : g[u]) {\n        if (v == fa) {\n            continue;\n        }\n        size += dfs(v, u);  // 先算完子樹，回來再加上去\n    }\n    return size;  // 把彙總結果交給父節點\n}\n```\n:::\n\n\n\n關鍵在最後一行：節點 `u` 必須等迴圈裡每個 `dfs(v, u)` 都回來，拿到所有子樹的摘要，才能算出自己的答案再回傳。這就是「自底向上」——資訊由葉往根流。\n\n:::example 合併不只是加：這裡把子樹的「字母計數表」累加進來\n合併時不一定只是相加，也可能取最大、計數、或更新一個全域答案。例如 3249「統計好節點的數目」要看「子樹中每個子節點的子樹大小是否都相同」，1519「子樹中標籤相同的節點數」要回傳一張「各字母出現次數」的表，由子往上累加：\n\n```cpp\n// 合併不只是加：這裡把子樹的「字母計數表」累加進來\narray<int, 26> dfs(int u, int fa) {\n    array<int, 26> cnt{};\n    cnt[label[u]]++;  // 算上自己這個字母\n    for (int v : g[u]) {\n        if (v == fa) {\n            continue;\n        }\n        auto sub = dfs(v, u);  // 拿到子樹的計數表\n        for (int i = 0; i < 26; ++i) {\n            cnt[i] += sub[i];\n        }\n    }\n    ans[u] = cnt[label[u]];  // 答案：和自己同字母的數量\n    return cnt;\n}\n```\n:::\n\n\n\n每個節點只被遞迴一次，合併成本通常是常數或固定大小（如 26），整體 `O(n)`。\n\n## 狀態／資料結構定義\n\n- `dfs(u, fa)` 的回傳值是 `u` 子樹的彙總量；或改用全域陣列 `size[u]`、`sum[u]` 在後序時機填寫，兩種寫法等價。\n- **合併方式從「兩個運算元」變成「對子節點迴圈折疊」**：初始值必須是運算的單位元，然後 `for (v : children) acc = combine(acc, dfs(v, u));`，最後併入 `u` 自己。\n- 單位元的選擇：求和用 0、求最大值用 $-\\infty$（或第一個子的值）、計數用 0、子樹大小的初始值是 1（把自己算進去）。\n- 若彙總量有多個分量，回傳 `struct`；若彙總量是集合（1519 的標籤計數），要注意合併的成本，見下面的複雜度討論。\n\n## 不變量或正確性證明\n\n命題（結構歸納）：**對每個節點 $u$，`dfs(u, fa)` 回傳以 $u$ 為根之子樹的目標彙總量。**\n\n- 基底：葉節點（除父節點外無其他鄰居），迴圈不執行，回傳「只有 $u$ 自己」的彙總量。這要求初始值設定正確——子樹大小是 1、子樹和是 $val_u$。\n- 歸納：假設各子節點的回傳值正確。由於 $u$ 的各棵子樹的節點集合**兩兩不交**，且與 $\\{u\\}$ 的並集恰為 $u$ 的子樹（樹無環的直接後果），只要 `combine` 忠實反映這個劃分，結果即正確。\n\n**「兩兩不交」這條在一般樹上仍然成立，是因為樹中任兩節點間路徑唯一**——若某節點同時在 $u$ 的兩棵子樹中，就會有兩條到 $u$ 的路徑，構成環。這是所有樹上後序彙總的共同基礎，值得明確點出，因為在 DAG 上它就不成立（同一節點可被多個父節點的子樹包含），此時後序累加會重複計數，必須改用記憶化。\n\n**合併運算需滿足結合律**（理由同 2.15）：子節點的迭代順序不應影響結果。\n\n**1519（統計子樹中同標籤節點數）的複雜度陷阱。** 直觀寫法是每個節點回傳一個大小 26 的計數陣列，父節點把所有子節點的陣列加起來。這是 $O(26n)$，可以接受。但若標籤種類是 $O(n)$（例如用 `map` 存），樸素合併會退化成 $O(n^2)$——每個節點都要複製一份 $O(n)$ 大小的 map。\n\n解法是**啟發式合併（小併大）**：把子節點的 map 併入時，總是把小的併進大的，並直接**移動**（`std::move` 或交換指標）而非複製最大的那個。這樣每個元素被移動的次數是 $O(\\log n)$，總計 $O(n \\log n)$。這個技巧的完整論證見 3.11。\n\n**3004（最大星形和）**：對每個節點 $u$，取其所有鄰居中權值為正的前 $k$ 大相加，再加上 $u$ 自己。注意這裡「鄰居」包含父節點，所以不能只在後序時機用子節點的資訊——這是一個**看起來像後序、實則需要全部鄰居**的例子。做法是直接對每個節點掃描其鄰接表（總計 $O(n)$ 次掃描 $+$ 排序），不必用樹的遞迴結構。**辨認出這一點可以省下大量無謂的換根推導。**\n\n## 逐步演算法\n\n把任何「自底向上」題目拆成三件事：子樹摘要是什麼、怎麼合併、答案在哪裡。\n\n:::example 摘要 = 子樹節點值總和（回傳一個 long long）\n**1. 決定「子樹摘要」——也就是 dfs 的回傳型別。** 問自己：「父節點要算它的答案，需要從每棵子樹拿到什麼？」是一個數字（大小、和、高度），還是一張表（計數）？這決定回傳型別。\n\n```cpp\n// 摘要 = 子樹節點值總和（回傳一個 long long）\nlong long dfs(int u, int fa) {\n    long long s = val[u];\n    // ... 遞迴累加子樹的和\n    return s;\n}\n```\n:::\n\n\n\n:::example 先收齊子樹結果，再合併\n**2. 遞迴完所有子節點後再合併。** 順序很重要：必須先把 `dfs(v, u)` 全部叫完、拿到回傳值，才能算自己。\n\n```cpp\n// 先收齊子樹結果，再合併\nfor (int v : g[u]) {\n    if (v == fa) {\n        continue;\n    }\n    long long sub = dfs(v, u);  // 子樹算好了\n    s += sub;                   // 合併進自己的摘要\n}\n```\n:::\n\n\n\n:::example 答案寫在 return 前；最終答案常在根：ans = dfs(root, -1);\n**3. 在「回來」的時機讀／寫答案。** 答案可能就是根的回傳值（如整棵樹的總和），也可能是「每個節點各自一個答案」——那就在合併完、return 之前寫進全域陣列。\n\n```cpp\n// 答案寫在 return 前；最終答案常在根：ans = dfs(root, -1);\nans[u] = computeFrom(s);  // 用彙總好的 s 算出 u 的答案\nreturn s;\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 葉節點是遞迴終點：它沒有子節點，迴圈不執行，直接回傳「只含自己」的摘要——這就是 base case，不需特別寫 if。\n- 別忘了傳 `fa` 防回頭。\n- 回傳型別若是大物件（如 vector），每次複製會變慢；可改成傳參考、或回傳固定大小的 `array`。\n- 時間 `O(n × 單次合併成本)`，遞迴空間 `O(樹高)`。\n\n## C++17 模板\n\n```cpp\n// 一般樹自底向上：一次算出所有節點的子樹大小與子樹權重和。\nclass Solution {\n  std::vector<std::vector<int>> g_;\n  std::vector<int> val_, size_;\n  std::vector<long long> sum_;\n\n  void dfs(int u, int fa) {\n    size_[u] = 1;                    // 單位元：先算自己\n    sum_[u] = val_[u];\n    for (int v : g_[u]) {\n      if (v == fa) { continue; }\n      dfs(v, u);                     // 先遞迴，回來時子樹資訊已備妥\n      size_[u] += size_[v];          // 折疊：各子樹節點集合兩兩不交，可直接相加\n      sum_[u]  += sum_[v];\n    }\n  }\n};\n```\n\n回傳 `struct` 的寫法（需要多個分量時）：\n\n```cpp\nstruct Info { int size; long long sum; int mx; };\n\nInfo dfs(int u, int fa) {\n  Info cur{1, val[u], val[u]};                 // 單位元 = 只有自己\n  for (int v : g[u]) {\n    if (v == fa) { continue; }\n    Info sub = dfs(v, u);\n    cur.size += sub.size;\n    cur.sum  += sub.sum;\n    cur.mx    = std::max(cur.mx, sub.mx);\n  }\n  return cur;\n}\n```\n\n## 時間與空間複雜度\n\n每個節點訪問一次、每條邊掃描兩次，若 `combine` 是 $O(1)$ 則總計 $O(n)$ 時間。這是相對於樸素「對每個節點各掃一次子樹」的主要收穫，後者總成本是 $\\sum_u |\\text{subtree}(u)|$，鏈狀樹時達 $O(n^2)$。\n\n若 `combine` 的成本與彙總量大小成正比（合併集合、合併計數表），樸素做法是 $O(n \\cdot m)$（$m$ 為值域）或最壞 $O(n^2)$；用小併大可降到 $O(n \\log n)$（3.11）。**判斷關鍵是問「合併一次要多久」，不能一律套 $O(n)$。**\n\n空間：鄰接表 $O(n)$，遞迴堆疊 $O(h)$，回傳 `struct` 每層一份仍是 $O(h)$。\n\n## 常見錯誤與邊界條件\n\n- 單位元設錯（子樹大小忘記從 1 開始、求最大值用 0 而樹中權值為負）。\n- 忘記 `v == fa` 而無限遞迴。\n- 把彙總量宣告成全域變數卻在遞迴中被子節點覆寫（應用陣列按節點索引，或用回傳值）。\n- 集合型彙總量樸素合併而超時。\n- `sum` 用 `int` 溢位。\n- 3004 誤以為只能用子節點而漏掉父節點方向的鄰居。\n- $n = 1$。\n- 深度大時爆堆疊。\n\n## 與相似技巧的比較\n\n- 自頂向下（3.2）傳路徑資訊、自底向上傳子樹資訊。**問「我以下如何」用後序，問「從根到我如何」用前序。**\n- 若題目要「以每個節點為根各求一次答案」（而不是以固定根求每棵子樹的答案），單靠後序不夠——子樹外的部分沒被算進去。這時要用換根 DP（3.4）。**這是本節與 3.4 的分界線，也是最容易誤判的地方。**\n- 與 2.3 的差異只有介面（傳 `fa`、迴圈折疊）；歸納骨架、單位元的要求、狀態擴充的判準完全相同。\n- 樹形 DP 在一般樹上的形式見 2.12 的討論，狀態擴充的判準（父節點需要區別對待哪些情形）不因分支數改變。\n\n## 代表例題\n\n**題號**：690、3004、3249、1273、1519\n\n放在「自底向上 DFS」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 690 | [員工的重要性](https://leetcode.cn/problems/employee-importance/) |  |\n| 3004 | [相同顏色的最大子樹](https://leetcode.cn/problems/maximum-subtree-of-the-same-color/)（Premium） |  |\n| 3249 | [統計好節點的數目](https://leetcode.cn/problems/count-the-number-of-good-nodes/) | 1566 |\n| 1273 | [刪除樹節點](https://leetcode.cn/problems/delete-tree-nodes/)（Premium） | 1733 |\n| 1519 | [子樹中標籤相同的節點數](https://leetcode.cn/problems/number-of-nodes-in-the-sub-tree-with-the-same-label/) | 1809 |\n| 3558 | [給邊賦權值的方案數 I](https://leetcode.cn/problems/number-of-ways-to-assign-edge-weights-i/) | 1845 |\n| 3593 | [使葉子路徑成本相等的最小增量](https://leetcode.cn/problems/minimum-increments-to-equalize-leaf-paths/) | 1959 |\n| 2872 | [可以被 K 整除連通塊的最大數目](https://leetcode.cn/problems/maximum-number-of-k-divisible-components/) | 1968 |\n| 2477 | [到達首都的最少油耗](https://leetcode.cn/problems/minimum-fuel-cost-to-report-to-the-capital/) | 2012 |\n| 3812 | [翻轉樹上最少邊](https://leetcode.cn/problems/minimum-edge-toggles-on-a-tree/) | 2179 |\n| 2973 | [樹中每個節點放置的金幣數目](https://leetcode.cn/problems/find-number-of-coins-to-place-in-tree-nodes/) | 2277 |\n| 2440 | [建立價值相同的連通塊](https://leetcode.cn/problems/create-components-with-same-value/) | 2460 |\n\n## 本節重點速查\n\n- 先遞迴所有非父鄰居，回來後**對子節點迴圈折疊**，單位元要與「只有自己」的語意一致。\n- 正確性依賴「各子樹節點集合兩兩不交」，這來自樹無環。\n- 合併若非 $O(1)$，複雜度要重算——集合型彙總用小併大（3.11）。\n- 要「以每個節點為根」時本節不夠，改用換根（3.4）。\n",
        },
        {
          id: 35,
          title: "3.4 有遞有歸",
          summary:
            "## 這個技術解決什麼問題\n\n3.3 能一次算出「以固定根 $r$ 為根時，每棵子樹的彙總量」。但很多題目問的是另一件事：**以每個節點為根時，全樹的某個量各是多少**（例如「每個節點到所有其他節點的距離和」，834）。\n\n對每個節點各跑一次 $O(n)$ 的 DFS 是 $O(n^2)$，$n$ 上看 $10^5$ 時不可行。換根 DP（也叫二次掃描法）用兩趟 DFS 做到 $O(n)$：**第一趟後序算出「子樹內」的資訊，第二趟前序把「子樹外」的資訊傳下去**，兩者一合就是以該節點為根的完整答案。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就往「有遞有歸」想：\n\n- **算一個節點同時要用到「上面（祖先）」和「下面（子樹）」的資訊**：只缺一邊用前兩節就夠，兩邊都缺才需要把遞、歸都用上。\n- **題目同時出現「沿路徑往下累積」和「對齊／彙總各子分支」兩種操作**：像 3593 要先有「往下的路徑成本」，又要在每點「讓各子分支成本相等」；3331 要先往上認新父親，又要往下統計子樹大小。\n- **暴力做法把「找祖先」和「掃子樹」拆成多次獨立遍歷**（容易變成 `O(n²)`）；有遞有歸用一次 DFS、在進出兩個時機各做一件事，攤平成 `O(n)`。\n- 嗅到味道的瞬間：\n\n:::example 你發現有些事「進節點時就要做」（要祖先），\n```cpp\n// 你發現有些事「進節點時就要做」（要祖先），\n// 有些事「子樹算完才能做」（要子結果）——一次 DFS 兩個時機分別處理\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我往下帶（遞）的祖先資訊是什麼？在哪裡更新它？\n2. 我往上回（歸）的子樹摘要是什麼？怎麼合併？\n3. 每段程式碼，到底該放在「遞迴呼叫之前」還「之後」？\n\n三題都答得清楚，這個模型就套對了。\n\n## 核心想法與直覺\n\n本節要解決的場景是：一次 DFS 同時需要「往下帶的祖先資訊」**和**「往上彙總的子樹資訊」。前一節（自頂向下）只用下行的「遞」，再前一節（自底向上）只用回程的「歸」。但有些題目兩邊都要用到——進節點時要看祖先的狀態，出節點時又要把子樹結果交還給父。這種「下行帶一份、回程還一份」的寫法，就叫「有遞有歸」。\n\n:::example 一次呼叫的兩個時機：遞（往下前）與 歸（往下回來後）\n理解的關鍵，是看清一次遞迴呼叫其實有「兩個時間點」：**進入節點（遞）**和**離開節點（歸）**。同一個 `dfs(u)` 函式裡，遞迴呼叫子節點「之前」的程式碼，是在「遞」的時候跑；遞迴呼叫「之後」的程式碼，是在「歸」的時候跑：\n\n```cpp\n// 一次呼叫的兩個時機：遞（往下前）與 歸（往下回來後）\nvoid dfs(int u, int fa) {\n    // —— 這裡是「遞」：可用祖先資訊、做進入時的處理 ——\n    for (int v : g[u]) {\n        if (v == fa) {\n            continue;\n        }\n        dfs(v, u);\n    }\n    // —— 這裡是「歸」：子樹都算完了，可彙總子樹、回填答案 ——\n}\n```\n:::\n\n\n\n:::example 有遞有歸：down 往下傳，回傳值往上彙總\n把這兩個時機都用上，就能做到「自頂向下傳一個值，自底向上回一個值」。例如 3331「修改後子樹的大小」：先在「遞」的時候沿著鏈往上找到「最近的非葉祖先」當作新父親（祖先資訊），再在「歸」的時候統計每棵子樹的大小（子樹彙總）。下面示意同一函式裡兩件事各自發生在哪：\n\n```cpp\n// 有遞有歸：down 往下傳，回傳值往上彙總\nint dfs(int u, int fa, int down) {  // down：從祖先帶來的狀態\n    int size = 1;                     // 「歸」要用的子樹大小\n    for (int v : g[u]) {\n        if (v == fa) {\n            continue;\n        }\n        size += dfs(v, u, nextDown(down));  // 遞：更新後傳下；歸：累加回來\n    }\n    use(u, down);  // 用祖先資訊處理 u\n    return size;   // 把子樹摘要交還父節點\n}\n```\n:::\n\n\n\n每個節點仍只進出一次，時間 `O(n)`。\n\n## 狀態／資料結構定義\n\n以 834（樹中距離和）為例，$n$ 個節點，`ans[u]` 表示 $u$ 到所有節點的距離和。\n\n- 第一趟（後序，以 0 為根）：\n  - `size[u]`：$u$ 子樹的節點數。\n  - `down[u]`：$u$ 到**其子樹內**所有節點的距離和。\n  - 轉移：$\\text{down}[u] = \\sum_{v \\in \\text{child}(u)} (\\text{down}[v] + \\text{size}[v])$。加 `size[v]` 是因為子樹 $v$ 中每個節點到 $u$ 都比到 $v$ 多走一條邊。\n- 第二趟（前序）：\n  - `ans[root] = down[root]`（根的子樹就是全樹）。\n  - 從 $u$ 換根到子節點 $v$ 時：$v$ 子樹內的 `size[v]` 個節點各**少走**一條邊，其餘 $n - \\text{size}[v]$ 個節點各**多走**一條邊。故\n    $$\\text{ans}[v] = \\text{ans}[u] - \\text{size}[v] + (n - \\text{size}[v])$$\n\n一般形式的兩個狀態就是「子樹內」與「子樹外」，第二趟負責把後者從父節點推導給子節點。\n\n## 不變量或正確性證明\n\n**第一趟的不變量**就是 3.3 的後序歸納，此處不重複。\n\n**第二趟的不變量：進入 `reroot(u, fa)` 時，`ans[u]` 已經是「以 $u$ 為根時的完整答案」。**\n\n- 初始化：對根 $r$，全樹就是 $r$ 的子樹，故 `ans[r] = down[r]`，成立。\n- 保持：假設 `ans[u]` 正確。要推導 `ans[v]`（$v$ 是 $u$ 的子節點）。關鍵在於**把全部節點按「在不在 $v$ 的子樹裡」分成兩類**，這個劃分是不重不漏的（樹的子樹劃分性質）：\n  - $v$ 子樹內的 $\\text{size}[v]$ 個節點：從 $u$ 出發要經過邊 $(u,v)$，從 $v$ 出發不用，故每個少 1。\n  - 其餘 $n - \\text{size}[v]$ 個節點：從 $v$ 出發必須先走到 $u$，故每個多 1。\n  - 兩類節點到 $u$ 與到 $v$ 的距離差恰好各是 $\\mp 1$，沒有第三類。因此 $\\text{ans}[v] = \\text{ans}[u] - \\text{size}[v] + (n - \\text{size}[v])$。\n- 結論：遞迴地，每個節點的 `ans` 都正確。\n\n**這個推導依賴兩個前提，換一題就要重新檢查：**\n\n1. **從 $u$ 換到相鄰的 $v$ 時，答案的變化量必須能由 $O(1)$ 個已知量算出。** 距離和滿足（只需 `size[v]` 與 $n$）。若題目的量是「到所有節點距離的最大值」，換根時的變化就不是簡單的加減，必須改存「向下最長鏈」與「向上最長鏈」等更多資訊。\n2. **換根操作必須可逆或可分解。** 上式其實是「把 $v$ 子樹的貢獻從 $u$ 的答案中扣掉、再把其餘部分加上」，這要求彙總運算有逆元（加法有減法）。若彙總是「取最大值」，就**沒有逆元**——你無法從最大值中「移除」某個貢獻。這時的標準做法是維護**前綴最大值與後綴最大值**（對每個節點的子節點列表），或維護「最大值與次大值」，換根時若要排除的恰是最大值就改用次大值。\n\n第二點是換根 DP 最常見的難點，也是判斷題目難度的分水嶺：加法類（距離和、計數）直接套公式；最值類（3313、最遠距離）必須額外維護前二大或前後綴。\n\n## 逐步演算法\n\n把「有遞有歸」題目拆成：往下帶什麼、往上回什麼、各自在哪個時機處理。\n\n:::example 參數 down = 祖先資訊；回傳值 = 子樹摘要（例如路徑成本）\n**1. 同時定義「下傳參數」和「上傳回傳值」。** 下傳的放參數（祖先資訊），上傳的放回傳值（子樹摘要）。\n\n```cpp\n// 參數 down = 祖先資訊；回傳值 = 子樹摘要（例如路徑成本）\nlong long dfs(int u, int fa, long long down) {\n    // ...\n}\n```\n:::\n\n\n\n:::example 遞：進入子節點前更新下傳狀態\n**2.「遞」的時機：先用祖先資訊更新，再下傳。** 在 for 迴圈呼叫子節點之前，把 `down` 更新成子節點該看到的版本。\n\n```cpp\n// 遞：進入子節點前更新下傳狀態\nfor (int v : g[u]) {\n    if (v == fa) {\n        continue;\n    }\n    long long childAns = dfs(v, u, down + cost[u]);  // 更新後下傳\n    // childAns 是「歸」回來的結果，下一步合併\n}\n```\n:::\n\n\n\n:::example 歸：彙總子樹，算自己的答案後回傳\n**3.「歸」的時機：合併子樹結果、回填答案、再回傳。** 子節點都回來後，把它們的回傳值合併，算出自己的答案，最後 return 給父節點。\n\n```cpp\n// 歸：彙總子樹，算自己的答案後回傳\nlong long best = 0;\nfor (...) {\n    best = max(best, childAns);  // 合併各子樹\n}\nans[u] = best;          // 回填這個節點的答案\nreturn best + cost[u];  // 把摘要交還父節點\n```\n:::\n\n\n\n像 3593「使葉子路徑成本相等的最小增量」：遞時把「到目前的路徑成本」往下帶，歸時讓每個節點對齊各子分支的最大成本、累加需要的增量——正是兩個時機各做一件事。\n\n**4. 邊界與複雜度。**\n\n- 想清楚「這段程式該放在遞迴呼叫之前還之後」：放錯時機，要嘛拿不到祖先資訊，要嘛拿不到子樹結果。\n- 葉節點同時是「遞」的終點和「歸」的起點，記得它的回傳值要有正確的初始意義。\n- 一樣傳 `fa` 防回頭；時間 `O(n)`，空間 `O(樹高)`。\n\n## C++17 模板\n\n```cpp\n// 樹中距離和（LeetCode 834）：兩趟 DFS，O(n)。\nclass Solution {\n  std::vector<std::vector<int>> g_;\n  std::vector<long long> ans_;\n  std::vector<int> size_;\n  int n_ = 0;\n\n  void down(int u, int fa) {              // 第一趟：後序，算子樹內\n    size_[u] = 1;\n    for (int v : g_[u]) {\n      if (v == fa) { continue; }\n      down(v, u);\n      size_[u] += size_[v];\n      ans_[u]  += ans_[v] + size_[v];     // 子樹 v 每個節點多走一條邊\n    }\n  }\n\n  void reroot(int u, int fa) {            // 第二趟：前序，把子樹外的資訊推下去\n    for (int v : g_[u]) {\n      if (v == fa) { continue; }\n      ans_[v] = ans_[u] - size_[v] + (n_ - size_[v]);   // size_[v] 個少 1，其餘多 1\n      reroot(v, u);                       // 必須先算好 ans_[v] 再遞迴\n    }\n  }\n\n public:\n  std::vector<long long> sumOfDistancesInTree(\n      int n, const std::vector<std::vector<int>>& edges) {\n    n_ = n;\n    g_.assign(n, {});\n    for (const auto& e : edges) {\n      g_[e[0]].push_back(e[1]);\n      g_[e[1]].push_back(e[0]);\n    }\n    ans_.assign(n, 0);\n    size_.assign(n, 0);\n    down(0, -1);        // ans_[0] 此時已是以 0 為根的完整答案\n    reroot(0, -1);\n    return ans_;\n  }\n};\n```\n\n## 時間與空間複雜度\n\n兩趟 DFS 各 $O(n)$，總計 $O(n)$ 時間——這是相對於「對每個節點各跑一次 DFS」的 $O(n^2)$ 的全部收穫。\n\n空間：鄰接表 $O(n)$，各狀態陣列 $O(n)$，遞迴堆疊 $O(h)$。\n\n若換根時需要維護前後綴最值（最值類問題），每個節點要對其子節點列表做兩次掃描，總成本仍是 $\\sum_u \\deg(u) = O(n)$，複雜度不變，只是常數與程式碼量增加。\n\n## 常見錯誤與邊界條件\n\n- 第二趟中先遞迴再計算 `ans[v]`（順序反了，此時 `ans[u]` 可能已被後續操作改動，或 `ans[v]` 尚未算好就被使用）。\n- 把 `ans[root] = down[root]` 這一步漏掉（第一趟結束時根的答案已經對，但若用了不同陣列存則要顯式複製）。\n- 對最值類問題直接套加減公式（沒有逆元，答案錯）。\n- `size` 與 `n - size` 寫反。\n- 距離和用 `int` 溢位（$n = 10^5$ 時距離和可達 $10^{10}$）。\n- $n = 1$ 時答案為 0。\n- 忘記 `v == fa`。\n\n## 與相似技巧的比較\n\n- 與 3.3 的分界線：**「以固定根求每棵子樹的答案」用 3.3；「以每個節點為根求全樹的答案」用換根。** 誤判這一點會導致答案漏掉子樹外的貢獻。\n- 與 2.5「有遞有歸」的關係：換根是它的系統化版本——第一趟純後序、第二趟純前序，兩趟分工明確，而非在同一次遍歷中兼顧。\n- 3.5 的樹直徑若要「對每個節點求最遠距離」，就是最值類換根的典型，需維護向下最長與次長鏈；也可用「直徑的兩個端點」的性質繞開（見 3.5）。\n- 換根的思路在圖上沒有直接對應——它本質上依賴「移除一條邊就把樹分成兩塊」這條樹特有的性質。\n\n## 代表例題\n\n**題號**：3331、3593\n\n放在「有遞有歸」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 3331 | [修改後子樹的大小](https://leetcode.cn/problems/find-subtree-sizes-after-changes/) | 2046 |\n| 3593 | [使葉子路徑成本相等的最小增量](https://leetcode.cn/problems/minimum-increments-to-equalize-leaf-paths/) | 1959 |\n\n## 本節重點速查\n\n- 兩趟：後序算「子樹內」，前序把「子樹外」推下去。\n- 核心是**從 $u$ 換到 $v$ 時，全部節點按「在不在 $v$ 子樹裡」二分，兩類的變化量各自 $O(1)$ 可算**。\n- 加法類直接加減；\n- **最值類沒有逆元**，必須維護前二大或前後綴最值。\n- 第二趟務必先算 `ans[v]` 再遞迴。\n",
        },
        {
          id: 36,
          title: "3.5 樹的直徑",
          summary:
            "## 這個技術解決什麼問題\n\n一般樹的直徑：最遠的兩個節點之間的距離（1245、2246 的變體、3313）。它是很多題目的核心量——「最少幾輪能傳遍全樹」「樹的中心在哪」都要先求直徑。\n\n有兩套 $O(n)$ 解法，各有適用範圍，選錯會出錯：\n\n- **DFS 枚舉最高點**（同 2.6）：對每個節點取其子樹方向的前兩長鏈相加。**對任意邊權都成立，包括負權。**\n- **兩次 BFS/DFS**：從任意點出發找最遠點 $u$，再從 $u$ 出發找最遠點 $v$，$u$–$v$ 即直徑。**只在非負邊權（含無權）時成立。**\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就往「樹的直徑」想：\n\n- **問「樹上最遠兩點的距離」或「最長的一條路徑」**：1245「樹的直徑」是裸題，2246 是「最長路徑」加上字元限制的變形。\n- **路徑可以是任意兩點、不一定經過根**：這正是要用「每個節點都當一次最高點、拼最長＋次長兩條鏈」的理由。\n- **暴力做法是枚舉所有點對、各自 BFS 求距離**（`O(n²)` 甚至更糟）；直徑技巧用一次 DFS 攤平成 `O(n)`。\n- 嗅到味道的瞬間：\n\n:::example 你發現「最長路徑 = 某節點往下最長的兩條鏈接起來」\n```cpp\n// 你發現「最長路徑 = 某節點往下最長的兩條鏈接起來」\n// 就在每個節點維護 max1、max2，ans = max(ans, max1 + max2)\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的「往下鏈」怎麼定義？葉子回傳 0 還是 1？帶不帶權？\n2. 在每個節點，我有沒有同時維護「最長」與「次長」兩條鏈？\n3. 為什麼往上只能回傳「一條」鏈，而更新答案卻用「兩條相加」？\n\n三題都答得清楚，這個模型就套對了。\n\n## 核心想法與直覺\n\n**延伸講解**：[樹形 DP：樹的直徑【基礎演算法精講 23】](https://www.bilibili.com/video/BV17o4y187h1/)\n\n本節要解決的場景是：求一棵樹的「直徑」——也就是**樹上任意兩個節點之間最長的那條路徑**（長度可以算邊數，或邊權總和）。例如一個公司組織圖、一張道路樹狀網，問「相隔最遠的兩點距離是多少」，就是直徑問題。\n\n直觀但慢的做法是枚舉所有點對算距離，太貴。聰明的做法只要**一次自底向上 DFS**。關鍵觀察是：任何一條最長路徑，一定有一個「最高點」——路徑上離根最近的那個節點。在那個最高點，這條路徑是「往左子分支下去最長 + 往右（另一）子分支下去最長」拼起來的。所以我們只要在每個節點，算出「從它往下能延伸的最長兩條鏈」，把它們相加，就是「以該節點為最高點的最長路徑」；對所有節點取最大，就是直徑。\n\n:::example 往下最長鏈：u 朝子樹方向能延伸的最大長度（以邊數計）\n先定義「往下最長鏈」：節點 `u` 往子樹方向，最遠能走多長。它等於「子節點裡最大的往下鏈 + 1」：\n\n```cpp\n// 往下最長鏈：u 朝子樹方向能延伸的最大長度（以邊數計）\nint down(int u, int fa) {\n    int best = 0;  // 沒有子節點就是 0\n    for (int v : g[u]) {\n        if (v == fa) {\n            continue;\n        }\n        best = max(best, down(v, u) + 1);\n    }\n    return best;\n}\n```\n:::\n\n\n\n:::example 在每個節點，把最長 + 次長兩條往下鏈拼成一條穿過 u 的路徑\n但只算「最長一條」不夠，直徑要「最長兩條接起來」。所以遍歷子節點時，順手記住「最長」和「次長」兩條往下鏈，相加更新全域答案：\n\n```cpp\n// 在每個節點，把最長 + 次長兩條往下鏈拼成一條穿過 u 的路徑\nint ans = 0;  // 全域：目前最大直徑（邊數）\nint dfs(int u, int fa) {\n    int max1 = 0, max2 = 0;  // 最長、次長往下鏈\n    for (int v : g[u]) {\n        if (v == fa) {\n            continue;\n        }\n        int d = dfs(v, u) + 1;\n        if (d > max1) {\n            max2 = max1, max1 = d;\n        } else if (d > max2) {\n            max2 = d;\n        }\n    }\n    ans = max(ans, max1 + max2);  // 穿過 u 的最長路徑\n    return max1;                  // 往上只回傳「一條」最長鏈\n}\n```\n:::\n\n\n\n注意回傳值只回「一條」（`max1`），因為對父節點而言，只能接續一條往下鏈；但更新答案時用「兩條相加」。整體一次 DFS，`O(n)`。\n\n## 狀態／資料結構定義\n\n**方法一（枚舉最高點）：**\n- `down(u, fa)` 回傳從 $u$ 向下的最長鏈長度（邊數或權重和）。\n- 迴圈中維護 `max1 >= max2` 兩個變數（不必排序），代表所有子節點方向中最長與次長的鏈。\n- 全域 `ans = max(ans, max1 + max2)`；回傳 `max1`（**單邊**）。\n- 初始 `max1 = max2 = 0`，代表「不往任何子節點延伸」，這對無權樹正確；帶負權時要允許取更小的值，見下。\n\n**方法二（兩次 BFS）：**\n- 第一趟從節點 0 出發，記錄距離陣列，取距離最大者為 $u$。\n- 第二趟從 $u$ 出發，最大距離即直徑，對應節點為 $v$。\n- 用 BFS（無權）或 DFS/Dijkstra（帶權）皆可。\n\n## 不變量或正確性證明\n\n**方法一**的正確性與 2.6 完全相同：每條路徑有唯一最高點，故枚舉最高點不重不漏；以 $u$ 為最高點的最長路徑是其各子節點方向的前兩長鏈之和（各方向獨立，因為子樹不相交）。這套論證只用到「路徑最高點唯一」與「子樹不相交」，**與邊權正負無關**，所以它是通用的。\n\n**方法二的正確性論證（且看清它的前提）。** 宣稱：從任意節點 $s$ 出發的最遠點 $u$，必定是某條直徑的一個端點。\n\n證明。設 $(a, b)$ 是一條直徑。分兩種情形。\n\n情形一：$s$ 在 $a$–$b$ 路徑上。此時 $d(a,b) = d(a,s) + d(s,b)$。設 $u$ 是離 $s$ 最遠的點，不失一般性設 $d(s,a) \\ge d(s,b)$。因 $u$ 最遠，$d(s,u) \\ge d(s,a)$。設 $m$ 是 $s$–$u$ 與 $s$–$b$ 兩條路徑的分歧點，則\n$$d(u,b) = d(s,u) + d(s,b) - 2\\,d(s,m).$$\n若 $m = s$（兩條路徑一離開 $s$ 就分開），則 $d(u,b) = d(s,u) + d(s,b) \\ge d(s,a) + d(s,b) = d(a,b)$，故 $(u,b)$ 也是直徑。若 $m \\ne s$，表示 $u$ 與 $b$ 同側，那就改與 $a$ 配對：$s$–$u$ 與 $s$–$a$ 必定在 $s$ 就分歧（因為 $a$、$b$ 分居 $s$ 兩側），同樣得 $d(u,a) \\ge d(a,b)$。兩種情形都找到一條不短於直徑的路徑，而直徑已是最大，故等號成立。\n\n情形二：$s$ 不在 $a$–$b$ 路徑上。設 $s$ 到該路徑的接入點為 $t$。若 $u$ 到 $a$–$b$ 路徑的接入點也在路徑上，類似地可證 $d(u,a)$ 或 $d(u,b)$ 達到直徑長度。反之若 $d(s,u) > d(s,t) + \\max(d(t,a), d(t,b))$，則把 $a$（或 $b$）換成 $u$ 會得到比 $d(a,b)$ 更長的路徑，矛盾。\n\n**這個證明的每一步都用到「距離非負」——具體來說，用到「路徑的一段不會比整條長」這個單調性。** 若邊權可為負，$d(s,u)$ 最大不再蘊含 $u$ 是好的端點：一條含大量負權邊的長路徑可能距離很小，而真正的直徑端點反而在第一趟中沒被選中。此時方法二直接失效，必須用方法一。\n\n**方法一在負權下的調整**：`max1`、`max2` 的初值不能是 0（那等於「可以選擇不延伸」，隱含了「不延伸不會更差」的非負假設）。若題目定義路徑必須至少含一條邊，初值應設為 $-\\infty$ 並小心處理葉節點。若定義允許單點路徑（長度 0），初值 0 才對。**先問清楚題目對「路徑」的定義**。\n\n## 逐步演算法\n\n把任何直徑題拆成：往下鏈是什麼、怎麼在每點拼兩條、答案在哪。\n\n:::example 帶權版：往下鏈加的是邊權 w，而非固定 +1\n**1.「往下鏈」的定義與回傳。** dfs 回傳「以 u 為頂、往子樹方向的最長鏈」。無權樹就是「子節點最長鏈 +1」；帶權樹則加上邊權。\n\n```cpp\n// 帶權版：往下鏈加的是邊權 w，而非固定 +1\nlong long d = dfs(v, u) + w;  // w 是邊 (u,v) 的權重\n```\n:::\n\n\n\n:::example 維護最長 max1、次長 max2\n**2. 每個節點記「最長＋次長」並更新答案。** 直徑＝穿過某節點的最長路徑＝該節點兩條最長往下鏈之和。逐一比較維護 max1、max2。\n\n```cpp\n// 維護最長 max1、次長 max2\nif (d > max1) {\n    max2 = max1;\n    max1 = d;\n} else if (d > max2) {\n    max2 = d;\n}\nans = max(ans, max1 + max2);  // 拼起來嘗試刷新直徑\n```\n:::\n\n\n\n:::example 給父節點的，是單條最長往下鏈\n**3. 往上只回傳一條鏈。** 對父節點，u 只能貢獻「一條」往下鏈（不能同時用兩條，否則路徑會分岔），所以 `return max1`。\n\n```cpp\n// 給父節點的，是單條最長往下鏈\nreturn max1;\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 直徑用「邊數」還是「節點數」要對齊題意：邊數版葉子回傳 0，節點數版葉子回傳 1，差一要小心。\n- 把全域 `ans` 初始化好（0），單節點樹答案就是 0。\n- 像 2246「相鄰字元不同的最長路徑」是直徑的變形：只有「子節點字元 ≠ 自己」時那條往下鏈才能接續，否則視為 0；3203「合併兩棵樹後的最小直徑」要先各自求直徑再分析合併點。\n- 時間 `O(n)`，空間 `O(樹高)`。\n\n## C++17 模板\n\n```cpp\n// 樹的直徑（方法一：枚舉最高點）。對任意邊權都正確。\nclass Solution {\n  std::vector<std::vector<std::pair<int, int>>> g_;   // {鄰居, 邊權}\n  long long ans_ = 0;\n\n  long long down(int u, int fa) {          // 從 u 向下的最長鏈\n    long long max1 = 0, max2 = 0;          // 最大與次大（非負邊權時初值 0 代表不延伸）\n    for (auto [v, w] : g_[u]) {\n      if (v == fa) { continue; }\n      long long d = down(v, u) + w;\n      if (d > max1) { max2 = max1; max1 = d; }   // 舊 max1 降為 max2\n      else if (d > max2) { max2 = d; }\n    }\n    ans_ = std::max(ans_, max1 + max2);    // 以 u 為最高點，可分叉\n    return max1;                           // 上傳單邊\n  }\n\n public:\n  long long diameter(int n, const std::vector<std::vector<int>>& edges) {\n    g_.assign(n, {});\n    for (const auto& e : edges) {\n      g_[e[0]].push_back({e[1], 1});\n      g_[e[1]].push_back({e[0], 1});\n    }\n    down(0, -1);\n    return ans_;\n  }\n};\n```\n\n方法二（無權樹，兩次 BFS）：跑一次 BFS 取最遠點 `u`，從 `u` 再跑一次 BFS，最大距離即直徑。程式碼更短，但只適用非負邊權。\n\n## 時間與空間複雜度\n\n方法一：一次 DFS，$O(n)$ 時間；維護前兩大是 $O(\\deg u)$ 而非排序的 $O(\\deg u \\log \\deg u)$，總計仍是 $O(n)$。空間 $O(n)$ 鄰接表加 $O(h)$ 堆疊。\n\n方法二：兩次遍歷各 $O(n)$，總計 $O(n)$。用 BFS 時空間 $O(n)$ 且**不會爆堆疊**——這是它在深度極大的鏈狀樹上的實際優勢。\n\n兩者同階，選擇依據是：**邊權可能為負就必須用方法一；否則方法二程式碼更短、且 BFS 版沒有堆疊風險。**\n\n## 常見錯誤與邊界條件\n\n- 在可能有負權的題目上使用兩次 BFS（最常見的錯誤，測資弱時還會過）。\n- 方法一的回傳值寫成 `max1 + max2`（分叉值不是合法的向上延伸鏈）。\n- 前兩大的更新寫成兩個獨立 `if`，漏掉新值同時大於兩者的情形。\n- 混淆直徑的「邊數」與「節點數」（相差 1）。\n- 帶權時用 `int` 累加而溢位。\n- `max1`／`max2` 初值與題目對路徑定義（是否允許單點）不一致。\n- $n = 1$ 時直徑為 0。\n- 忘記 `v == fa`。\n\n## 與相似技巧的比較\n\n- 與 2.6 的二元樹直徑：分解思路完全相同，差別只在「左右兩個」變成「所有子節點取前兩大」。\n- 2246（相鄰字元不同的最長路徑）、687（最長同值路徑）都是同一骨架，只是 `down` 的定義加上「只在字元不同時才延伸」的條件。\n- 3313（樹中最遠節點）若要對**每個節點**求最遠距離，那是換根 DP（3.4）的最值類問題，需維護向下最長與次長、以及向上最長；也可用「答案必是到直徑兩端點之一的距離」這條性質繞開——後者的正確性同樣依賴非負邊權。\n- 樹的中心（最小化到最遠點的距離）位於直徑的中點，這是直徑的直接推論，常用於 1245 的變體。\n\n## 代表例題\n\n**題號**：3313、3787、1245、2246、3203\n\n放在「樹的直徑」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 3313 | [查詢樹中最後標記的節點](https://leetcode.cn/problems/find-the-last-marked-nodes-in-tree/)（Premium） |  |\n| 3787 | [查詢樹的直徑端點](https://leetcode.cn/problems/find-diameter-endpoints-of-a-tree/)（Premium） |  |\n| 1245 | [樹的直徑](https://leetcode.cn/problems/tree-diameter/)（Premium） | 1792 |\n| 2246 | [相鄰字元不同的最長路徑](https://leetcode.cn/problems/longest-path-with-different-adjacent-characters/) | 2126 |\n| 3203 | [合併兩棵樹後的最小直徑](https://leetcode.cn/problems/find-minimum-diameter-after-merging-two-trees/) | 2266 |\n| 1617 | [統計子樹中城市之間最大距離](https://leetcode.cn/problems/count-subtrees-with-max-distance-between-cities/) | 2309 |\n| 2538 | [最大價值和與最小价值和的差值](https://leetcode.cn/problems/difference-between-maximum-and-minimum-price-sum/) | 2398 |\n\n## 本節重點速查\n\n- 兩套解法：**枚舉最高點（取前兩長鏈相加）對任意邊權都對；\n- 兩次 BFS 只在非負邊權下成立**，因為它的證明用到「路徑的一段不會比整條長」。\n- 回傳值必須是單邊 `max1`，全域答案才用 `max1 + max2`。\n- 前兩大的更新要記得把被擠下的舊最大降為次大。\n",
        },
        {
          id: 37,
          title: "3.6 樹的拓撲排序",
          summary:
            "## 這個技術解決什麼問題\n\n有些樹題沒有指定根，而是要求「反覆剝掉葉節點」直到剩下核心——最小高度樹（310）就是要找出使樹高最小的根。若對每個節點各跑一次 BFS 求高度，$O(n^2)$；剝葉法一趟 $O(n)$。\n\n剝葉法本質上是**樹上的拓撲排序**：把度數為 1 的節點視為「入度為 0」，用佇列逐層剝除。它同時也是處理「父子關係未給定、只能從邊界往內推」這類題目的通用手法（2603 收集硬幣、3812 類的變體）。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就往「樹的拓撲排序（剝葉子）」想：\n\n- **要從「最外層葉子」往內逐層處理**，或要找「樹的中心 / 最小高度根」：310 是裸題，剝到最後剩的就是中心。\n- **題目允許『先把對答案沒影響的葉子刪掉』來化簡**：像 2603 先移除無金幣的葉子，3812「翻轉樹上最少邊」也常從葉子端開始定序處理。\n- **暴力做法是『以每個節點為根各跑一次 DFS 求高度』**（`O(n²)`）；剝葉子用一次層序剝除攤平成 `O(n)`。\n- 嗅到味道的瞬間：\n\n:::example 你發現「度數為 1 的節點最好先處理，處理完鄰居度數會變」\n```cpp\n// 你發現「度數為 1 的節點最好先處理，處理完鄰居度數會變」\n// 就用佇列裝葉子，--deg[v]==1 時把新葉子加進來\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的「葉子」條件是什麼？（度數 == 1）初始葉子怎麼找？\n2. 剝掉一個葉子後，要怎麼更新鄰居、判斷誰變成新葉子？\n3. 停止條件是什麼？（剩 ≤ 2 個？剝完所有可剝的？）邊界 n=1、n=2 處理了嗎？\n\n三題都答得清楚，這個模型就套對了。\n\n## 核心想法與直覺\n\n本節要解決的場景是：題目給的是「無根樹」（節點和邊，沒指定誰是根），而我們想從「最外圈」一層層往內處理。最典型的例子是 310「最小高度樹」：要找哪些節點當根時整棵樹最矮——答案就是把葉子一圈圈剝掉後，最後剩下的 1～2 個中心點。\n\n這裡借用了圖論裡「拓撲排序」的想法。一般拓撲排序處理「有向圖、按入度為 0 的順序逐個移除」。在無根樹上，我們關注的是**度數為 1 的節點，也就是葉子**：葉子只連一條邊，是最容易先處理掉的；剝掉一個葉子，它的鄰居度數會減 1，可能變成新的葉子。如此一層層往內收，就像剝洋蔥。\n\n:::example 算度數，把所有葉子（度為 1）放進佇列當起點\n先看怎麼算「度數」並找出初始葉子。度數 = 一個節點連了幾條邊；度數為 1 的就是葉子：\n\n```cpp\n// 算度數，把所有葉子（度為 1）放進佇列當起點\nvector<int> deg(n, 0);\nfor (auto& e : edges) {\n    deg[e[0]]++;\n    deg[e[1]]++;\n}\nqueue<int> q;\nfor (int i = 0; i < n; ++i) {\n    if (deg[i] == 1) {\n        q.push(i);  // 一開始的最外圈葉子\n    }\n}\n```\n:::\n\n\n\n:::example 一層層剝葉子：把當前所有葉子同時移除，產生下一圈葉子\n接著像 BFS 一樣，一整層一整層地剝。每剝掉一個葉子，就把它鄰居的度數減 1；鄰居度數掉到 1，代表它變成新葉子，加入下一層：\n\n```cpp\n// 一層層剝葉子：把當前所有葉子同時移除，產生下一圈葉子\nwhile (!q.empty()) {\n    int sz = q.size();  // 這一層的葉子數\n    for (int i = 0; i < sz; ++i) {\n        int u = q.front();\n        q.pop();\n        for (int v : g[u]) {\n            if (--deg[v] == 1) {  // 鄰居度數降到 1 → 新葉子\n                q.push(v);\n            }\n        }\n    }\n}\n```\n:::\n\n\n\n每個節點、每條邊各被處理一次，複雜度 `O(n)`。\n\n## 狀態／資料結構定義\n\n- `deg[u]`：節點 $u$ 的度數（鄰居數）。初始由建圖時統計。\n- `queue<int> q`：當前所有度數為 1 的節點（葉節點）。\n- `remain`：尚未被剝除的節點數，初始為 $n$。\n- 分層剝除：與 2.13 相同，用 `int sz = q.size()` 切出「這一輪剝掉的葉節點」。\n- 310 的終止條件：`remain > 2` 時繼續剝；剩下 1 或 2 個節點時，它們就是答案（最小高度樹的根）。\n\n注意「度數為 1」而非「度數為 0」——樹的葉節點有一條連向父親的邊。$n = 1$ 時唯一節點的度數是 0，必須特判。\n\n## 不變量或正確性證明\n\n**不變量：每輪剝除後，剩下的節點集合構成原樹的一棵子樹（連通），且它恰是「距離原樹葉節點超過 $k$ 層」的節點集合（$k$ 為已剝輪數）。**\n\n- 初始化：$k = 0$，剩下全部節點，成立。\n- 保持：剝掉當前所有度數為 1 的節點後，剩餘部分仍連通——移除葉節點不會斷開樹（葉節點不在任何兩個其他節點的路徑上）。且新的葉節點恰是「原本距離葉節點 $k+1$ 層」的那些。\n\n**310 的正確性：最後剩下的 1 或 2 個節點就是最小高度樹的根。**\n\n論證分兩步。\n\n第一步，**最小高度樹的根必定是直徑的中點**。設直徑為 $a$–$b$，長度（邊數）為 $L$。對任意節點 $r$，以 $r$ 為根的樹高至少是 $\\max(d(r,a), d(r,b)) \\ge \\lceil L/2 \\rceil$——因為 $d(r,a) + d(r,b) \\ge d(a,b) = L$（三角不等式），故兩者中較大者不小於 $\\lceil L/2 \\rceil$。取 $r$ 為 $a$–$b$ 路徑的中點時，$\\max(d(r,a), d(r,b)) = \\lceil L/2 \\rceil$，且**不存在比 $\\lceil L/2 \\rceil$ 更遠的節點**（否則會有比直徑更長的路徑）。故中點達到下界，是最小高度樹的根。\n\n第二步，**剝葉法最後剩下的正是直徑的中點**。每剝一輪，所有從葉節點出發的路徑都縮短 1，特別是直徑的兩端各縮進一格，直徑長度每輪減 2。因此經過 $\\lfloor L/2 \\rfloor$ 輪後：若 $L$ 為偶數，剩下 1 個節點（中點）；若 $L$ 為奇數，剩下 2 個相鄰節點（兩個中點）。這解釋了為何答案至多兩個——**這不是經驗規則，是直徑奇偶性的直接後果**。\n\n**為何 `remain > 2` 是終止條件而非 `q` 為空**：若一直剝到佇列空，會把整棵樹剝光。必須在剩下 1 或 2 個節點時停手。\n\n**與圖的拓撲排序的關係。** 這裡的「度數為 1 就入隊」對應拓撲排序的「入度為 0 就入隊」。剝除節點 $u$ 時，對每個尚未剝除的鄰居 $v$ 執行 `if (--deg[v] == 1) q.push(v);`——注意判斷的是**恰好等於 1**，不是「小於等於 1」。用 `<= 1` 會讓同一個節點被重複入隊。這與拓撲排序中 `if (--indeg[v] == 0)` 的寫法同構，原因也相同：每個節點只在度數「跨過」臨界值的那一刻入隊一次。\n\n## 逐步演算法\n\n把「樹的拓撲排序（剝葉子）」拆成：建圖算度、找初始葉、逐層剝除、讀答案。\n\n:::example 建圖 + 度數\n**1. 建鄰接表並統計度數。** 用 `g[u]` 存鄰居，用 `deg[u]` 存度數，兩者同步建立。\n\n```cpp\n// 建圖 + 度數\nvector<vector<int>> g(n);\nfor (auto& e : edges) {\n    g[e[0]].push_back(e[1]);\n    g[e[1]].push_back(e[0]);\n    deg[e[0]]++;\n    deg[e[1]]++;\n}\n```\n:::\n\n\n\n:::example 初始最外圈\n**2. 把所有初始葉子（度為 1）入列。** 這是「最外圈」，剝洋蔥的第一層。\n\n```cpp\n// 初始最外圈\nfor (int i = 0; i < n; ++i) {\n    if (deg[i] == 1) {\n        q.push(i);\n    }\n}\n```\n:::\n\n\n\n:::example 逐層剝除，remaining 追蹤還沒被剝掉的節點數\n**3. 一層一層剝，並記住「還剩幾個節點」。** 每剝一層就更新剩餘節點數；當剩餘 ≤ 2 時，剩下的就是中心。\n\n```cpp\n// 逐層剝除，remaining 追蹤還沒被剝掉的節點數\nint remaining = n;\nwhile (remaining > 2) {\n    int sz = q.size();\n    remaining -= sz;  // 這一層整層剝掉\n    for (int i = 0; i < sz; ++i) {\n        int u = q.front();\n        q.pop();\n        for (int v : g[u]) {\n            if (--deg[v] == 1) {\n                q.push(v);\n            }\n        }\n    }\n}\n```\n:::\n\n\n\n:::example 剩在佇列裡的就是中心（1 或 2 個）\n**4. 讀答案、處理邊界。** 佇列裡最後剩下的就是答案（最小高度樹的根）。\n\n```cpp\n// 剩在佇列裡的就是中心（1 或 2 個）\nvector<int> ans;\nwhile (!q.empty()) {\n    ans.push_back(q.front());\n    q.pop();\n}\n```\n:::\n\n\n\n- 邊界：`n == 1` 時沒有邊，唯一節點自己就是答案；`n == 2` 時兩個都是答案——記得先處理，否則迴圈條件可能漏掉。\n- 一定要「整層一起剝」（記 `sz`），不能一個一個剝到底，否則分不出洋蔥的層次。\n- 像 2603「收集樹中金幣」也用同樣手法先剝掉「沒有金幣的葉子」化簡樹，再處理核心。時間 `O(n)`，空間 `O(n)`。\n\n## C++17 模板\n\n```cpp\n// 最小高度樹（LeetCode 310）：剝葉法，O(n)。\nstd::vector<int> findMinHeightTrees(int n, const std::vector<std::vector<int>>& edges) {\n  if (n == 1) { return {0}; }              // 唯一節點度數為 0，必須特判\n\n  std::vector<std::vector<int>> g(n);\n  std::vector<int> deg(n, 0);\n  for (const auto& e : edges) {\n    g[e[0]].push_back(e[1]);\n    g[e[1]].push_back(e[0]);\n    ++deg[e[0]];\n    ++deg[e[1]];\n  }\n\n  std::queue<int> q;\n  for (int i = 0; i < n; ++i) {\n    if (deg[i] == 1) { q.push(i); }        // 葉節點的度數是 1，不是 0\n  }\n\n  int remain = n;\n  while (remain > 2) {                     // 剩 1 或 2 個就停，不能剝光\n    int sz = q.size();                     // 分層：這一輪剝掉的葉節點\n    remain -= sz;\n    for (int i = 0; i < sz; ++i) {\n      int u = q.front();\n      q.pop();\n      for (int v : g[u]) {\n        if (--deg[v] == 1) { q.push(v); }  // 恰好等於 1 才入隊，避免重複\n      }\n    }\n  }\n\n  std::vector<int> ans;\n  while (!q.empty()) { ans.push_back(q.front()); q.pop(); }\n  return ans;\n}\n```\n\n## 時間與空間複雜度\n\n每個節點入隊、出隊各一次；每條邊在其兩個端點被剝除時各被掃描一次，總計 $\\sum \\deg = 2(n-1)$。故 $O(n)$ 時間。\n\n空間：鄰接表 $O(n)$、度數陣列 $O(n)$、佇列 $O(n)$。\n\n相對於「對每個節點各跑一次 BFS 求高度」的 $O(n^2)$，收穫來自「答案只可能是直徑中點」這條結構性質——剝葉法等於用 $O(n)$ 直接定位它。\n\n## 常見錯誤與邊界條件\n\n- $n = 1$ 未特判（此時唯一節點度數為 0，不會入隊，迴圈直接結束但答案應為 `{0}`）。\n- $n = 2$（兩個節點度數都是 1，`remain > 2` 立刻為假，直接回傳兩者，正確）。\n- 把葉節點的判定寫成 `deg[i] == 0`。\n- 更新時用 `if (deg[v] <= 1)` 導致重複入隊。\n- 剝到佇列為空而把樹剝光。\n- 忘記 `remain -= sz`。\n- 忘記分層（不記 `sz` 的話 `remain` 的扣減與輪次對不上）。\n\n## 與相似技巧的比較\n\n- 與圖的拓撲排序（graph 主題）同構：入度為 0 換成度數為 1，`--indeg[v] == 0` 換成 `--deg[v] == 1`。差別在於樹是無向的，所以用度數而非入度，且必須避免剝光。\n- 與 2.4 的自底向上剪枝相比：剪枝有天然的父子方向可依循，一次後序即可；剝葉法用在**沒有指定根**的場合，必須從外圍往內收，因此需要顯式的佇列。\n- 求直徑（3.5）與本節密切相關——剝葉法的輪數就是 $\\lfloor L/2 \\rfloor$，因此也能用來求直徑長度，但通常直接用 3.5 的方法更直接。\n- 2603（收集樹上硬幣）是剝葉法的進階應用：先剝掉沒有硬幣的葉子分支，再剝固定輪數，剩下的部分決定答案；它的關鍵同樣是「從邊界往內收」。\n\n## 代表例題\n\n**題號**：310、3812、2603\n\n放在「樹的拓撲排序」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 310 | [最小高度樹](https://leetcode.cn/problems/minimum-height-trees/) |  |\n| 3812 | [翻轉樹上最少邊](https://leetcode.cn/problems/minimum-edge-toggles-on-a-tree/) | 2179 |\n| 2603 | [收集樹中金幣](https://leetcode.cn/problems/collect-coins-in-a-tree/) | 2712 |\n\n## 本節重點速查\n\n- 樹上拓撲排序＝剝葉法：葉節點的判定是 **`deg == 1`**，更新時用 **`--deg[v] == 1`** 恰好觸發一次。\n- 分層剝除用 `sz = q.size()`。\n- 310 的答案至多兩個，因為它們是直徑中點，而直徑長度的奇偶決定 1 個還是 2 個。\n- 終止條件是 `remain > 2`，不能剝光；\n- $n = 1$ 要特判。\n",
        },
        {
          id: 38,
          title: "3.7 DFS 時間戳",
          summary:
            "## 這個技術解決什麼問題\n\n很多樹上問題需要反覆回答「$u$ 是不是 $v$ 的祖先」，或者「把 $u$ 的整棵子樹加上某個值」。樸素做法是每次都往上走一遍（$O(h)$）或遞迴整棵子樹（$O(\\text{size})$），查詢多時就爆了。\n\nDFS 時間戳的槓桿是**把樹的子樹結構壓平成陣列上的連續區間**：對每個節點記錄進入時間 `tin[u]` 與離開時間 `tout[u]`，則「$u$ 的子樹」恰好對應區間 $[\\text{tin}[u], \\text{tout}[u]]$。於是祖先判定變成 $O(1)$ 的區間包含檢查，子樹修改變成**區間**修改——可以直接套線段樹或樹狀陣列。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就往「DFS 時間戳」想：\n\n- **反覆問「某節點是否在某子樹內」或「某兩節點誰是誰的祖先」**：用 in/out 區間包含一次判斷，2322 判子樹互含就是典型。\n- **要對「整棵子樹」做求和、加值、計數等操作**：把子樹轉成連續區間後，直接套樹狀陣列／線段樹，如 3515 的路徑（子樹）更新。\n- **暴力做法是每次查詢都重新 DFS 掃一遍子樹**（每次 `O(n)`）；時間戳把子樹變區間，查詢攤平成 `O(1)` 或 `O(log n)`。\n- 嗅到味道的瞬間：\n\n:::example 你發現「子樹」一直被當作整體反覆操作／查詢\n```cpp\n// 你發現「子樹」一直被當作整體反覆操作／查詢\n// 就打 in/out 時間戳，把子樹變成陣列上的 [in[u], out[u]]\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我有沒有用同一個計數器正確記下每個節點的 in 與 out？\n2. 子樹 u 對應到陣列上的哪一段區間？端點怎麼取（`<=` 還是 `<`）？\n3. 子樹操作要接什麼結構？（純包含判斷？還是要區間求和／加值？）\n\n三題都答得清楚，這個模型就套對了。\n\n## 核心想法與直覺\n\n本節要解決的場景是：常常要問「節點 `x` 是不是在以 `y` 為根的子樹裡？」或「把整棵子樹當成一個整體去做區間操作（求和、加值）」。如果每次都重新 DFS 一遍子樹去判斷，太慢。DFS 時間戳（也叫歐拉序、DFS 序）能把「子樹」這個概念，變成陣列上的一段**連續區間**，於是子樹問題就轉化成大家熟悉的區間問題。\n\n核心想法：用一個全域計數器 `clock`，在 DFS **進入**每個節點時記下「進入時間」`in[u]`，在**離開**（子樹全部走完）時記下「離開時間」`out[u]`。因為 DFS 是「進去後一定把整棵子樹走完才出來」，所以一棵子樹裡所有節點的進入時間，必定落在 `[in[u], out[u]]` 這個區間內——子樹和區間於是一一對應。\n\n:::example DFS 時間戳：進入記 in，離開記 out\n先看怎麼打時間戳：\n\n```cpp\n// DFS 時間戳：進入記 in，離開記 out\nint clk = 0;\nvoid dfs(int u, int fa) {\n    in[u] = ++clk;  // 進入 u 的時刻\n    for (int v : g[u]) {\n        if (v == fa) {\n            continue;\n        }\n        dfs(v, u);\n    }\n    out[u] = clk;  // 子樹走完，離開時刻\n}\n```\n:::\n\n\n\n:::example x 是否在 y 的子樹中：看 x 的進入時間是否落在 y 的區間內\n打完戳後，「x 在 y 的子樹裡」就只是一個區間包含判斷，`O(1)`：\n\n```cpp\n// x 是否在 y 的子樹中：看 x 的進入時間是否落在 y 的區間內\nbool inSubtree(int x, int y) { return in[y] <= in[x] && in[x] <= out[y]; }\n```\n:::\n\n\n\n更強的是：如果每個節點還有一個值，按 `in` 順序把值排成一個陣列，那「子樹 y 的所有值」就是這個陣列上的連續區間 `[in[y], out[y]]`。要「子樹求和」「子樹整體加值」，就接上樹狀陣列或線段樹，把樹問題完全變成區間問題。一次 DFS 打戳 `O(n)`，之後每次查詢 `O(1)` 或 `O(log n)`。\n\n## 狀態／資料結構定義\n\n- 全域計數器 `timer`，DFS 進入節點時 `tin[u] = ++timer`，離開前 `tout[u] = timer`。\n- 這個版本（不在離開時遞增）給出的是「**歐拉序中的節點區間**」：`tin[u]` 是 $u$ 自己的位置，`tout[u]` 是其子樹中最後一個被訪問節點的位置。子樹恰對應 $[\\text{tin}[u], \\text{tout}[u]]$，長度等於子樹大小。\n- 另一種常見版本是進入與離開都遞增（`tin[u] = ++timer; ... tout[u] = ++timer;`），得到長度 $2n$ 的括號序列。兩種都能判祖先，但**只有第一種能讓子樹對應長度等於子樹大小的區間**，因此配合資料結構做子樹修改時要用第一種。\n- 祖先判定：$u$ 是 $v$ 的祖先 $\\iff \\text{tin}[u] \\le \\text{tin}[v] \\le \\text{tout}[u]$。\n\n## 不變量或正確性證明\n\n**核心性質：$u$ 的子樹中的節點，其 `tin` 恰好構成連續區間 $[\\text{tin}[u], \\text{tout}[u]]$。**\n\n證明用結構歸納。DFS 進入 $u$ 時把 `tin[u]` 設為當時的計數器值。接著依序遞迴各子節點；由歸納假設，第 $i$ 個子節點 $c_i$ 佔用連續區間 $[\\text{tin}[c_i], \\text{tout}[c_i]]$，且長度等於其子樹大小。由於 DFS 在完成 $c_i$ 的整棵子樹之前不會離開它，各子樹佔用的區間**依序首尾相接、互不重疊**。離開 $u$ 前把 `tout[u]` 設為當時的計數器，它等於最後一個子樹的 `tout`。故 $u$ 的子樹佔用 $[\\text{tin}[u], \\text{tout}[u]]$，長度為 $1 + \\sum_i |\\text{subtree}(c_i)| = |\\text{subtree}(u)|$。\n\n**這裡關鍵的一句是「DFS 在完成一棵子樹之前不會離開它」**——這正是深度優先的定義，也是為什麼廣度優先的訪問序**不具備**這個性質（BFS 中同一棵子樹的節點會被其他子樹的節點打散）。想用時間戳就必須用 DFS。\n\n**祖先判定的正確性。** ($\\Rightarrow$) 若 $u$ 是 $v$ 的祖先，則 $v$ 在 $u$ 的子樹中，由上述性質 $\\text{tin}[v] \\in [\\text{tin}[u], \\text{tout}[u]]$。($\\Leftarrow$) 若 $\\text{tin}[v]$ 落在該區間內，則 $v$ 屬於 $u$ 的子樹（區間與子樹一一對應），故 $u$ 是 $v$ 的祖先（或 $u = v$，視題目是否把自己算作祖先而定，需明確約定）。\n\n另一個等價寫法是用**括號序列版本**：$u$ 是 $v$ 的祖先 $\\iff$ $u$ 的括號對包含 $v$ 的括號對。兩種版本在祖先判定上等價。\n\n**為何這使子樹修改變成區間修改。** 若把節點按 `tin` 排成陣列 `a`（即 `a[tin[u]] = val[u]`），則「對 $u$ 的子樹全體加 $x$」等價於「對 `a` 的區間 $[\\text{tin}[u], \\text{tout}[u]]$ 全體加 $x$」。這一步把樹上問題**完全轉成序列問題**，之後線段樹、樹狀陣列、差分陣列全部可用。這是時間戳最有價值的用途，也是樹鏈剖分等進階技巧的第一步。\n\n**注意這只對「子樹」有效，對「路徑」無效**——樹上路徑在 `tin` 序中並不連續。路徑查詢要用 LCA（3.8）搭配前綴和，或用樹鏈剖分把路徑拆成 $O(\\log n)$ 個連續區間。\n\n## 逐步演算法\n\n把任何「DFS 時間戳」題拆成：打戳、子樹轉區間、接資料結構、讀答案。\n\n:::example in[u] 是子樹區間左端，out[u] 是右端\n**1. 一次 DFS 打進入／離開時間戳。** 用 `in[u]`、`out[u]` 把每個子樹對應到一段連續區間。\n\n```cpp\n// in[u] 是子樹區間左端，out[u] 是右端\nin[u] = ++clk;\nfor (int v : g[u]) {\n    if (v != fa) {\n        dfs(v, u);\n    }\n}\nout[u] = clk;\n```\n:::\n\n\n\n:::example 攤平：base[in[u]] 對應節點 u 的值\n**2. 把節點值按 in 順序攤平成陣列。** 第 `in[u]` 個位置放節點 u 的值，於是子樹 = 陣列上的 `[in[u], out[u]]`。\n\n```cpp\n// 攤平：base[in[u]] 對應節點 u 的值\nvector<long long> base(n + 1);\nfor (int u = 0; u < n; ++u) {\n    base[in[u]] = val[u];\n}\n```\n:::\n\n\n\n:::example 子樹求和 = 區間求和（fen 為樹狀陣列）\n**3. 接上區間資料結構處理子樹操作。** 子樹求和 / 子樹加值，就用樹狀陣列或線段樹在區間 `[in[u], out[u]]` 上做。\n\n```cpp\n// 子樹求和 = 區間求和（fen 為樹狀陣列）\nlong long subtreeSum(int u) {\n    return fen.query(in[u], out[u]);  // 整棵子樹一次搞定\n}\n```\n:::\n\n\n\n**4. 子樹包含判斷與邊界。** 「x 在 y 子樹中」用區間包含 `O(1)`；判斷祖先關係、做某些 LCA 也可借時間戳。\n\n- `in`/`out` 用同一個 `clk`：本寫法 `out[u]` 取「子樹最後一個進入時間」，故用 `<=` 判包含；若改成「離開時 ++clk」則邊界用法不同，要前後一致。\n- 像 2322「從樹中刪除邊的最小分數」用時間戳快速判「兩刪邊形成的子樹是否互相包含」；3515「帶權樹中的最短路徑」把路徑更新轉成子樹區間更新。\n- 打戳 `O(n)`；查詢視所接結構為 `O(1)`～`O(log n)`。\n\n## C++17 模板\n\n```cpp\n// DFS 時間戳：把子樹壓成連續區間。\nclass TreeTimer {\n  std::vector<std::vector<int>> g_;\n  std::vector<int> tin_, tout_;\n  int timer_ = 0;\n\n  void dfs(int u, int fa) {\n    tin_[u] = ++timer_;                // 進入：佔一個位置\n    for (int v : g_[u]) {\n      if (v == fa) { continue; }\n      dfs(v, u);                       // 子樹的區間首尾相接\n    }\n    tout_[u] = timer_;                 // 離開：子樹最後一個節點的位置\n  }\n\n public:\n  void build(int n, const std::vector<std::vector<int>>& edges) {\n    g_.assign(n, {});\n    for (const auto& e : edges) {\n      g_[e[0]].push_back(e[1]);\n      g_[e[1]].push_back(e[0]);\n    }\n    tin_.assign(n, 0);\n    tout_.assign(n, 0);\n    dfs(0, -1);\n  }\n\n  // u 是 v 的祖先嗎（含 u == v）\n  bool isAncestor(int u, int v) const {\n    return tin_[u] <= tin_[v] && tin_[v] <= tout_[u];\n  }\n\n  // u 的子樹對應區間 [tin_[u], tout_[u]]，長度即子樹大小\n  std::pair<int, int> subtreeRange(int u) const { return {tin_[u], tout_[u]}; }\n};\n```\n\n## 時間與空間複雜度\n\n預處理一次 DFS，$O(n)$ 時間、$O(n)$ 空間（兩個陣列）加 $O(h)$ 堆疊。\n\n單次祖先判定 $O(1)$——這是相對於「沿父指標往上走」$O(h)$ 的收穫，在 $q$ 次查詢時把 $O(qh)$ 降到 $O(n + q)$。\n\n子樹加值／子樹求和：配合樹狀陣列或線段樹，單次 $O(\\log n)$。相對於樸素的 $O(\\text{subtree size})$（最壞 $O(n)$），$q$ 次操作從 $O(qn)$ 降到 $O((n + q)\\log n)$。\n\n## 常見錯誤與邊界條件\n\n- `tout[u]` 在離開時又遞增一次（變成括號序列版），卻仍用「區間長度等於子樹大小」的假設去做子樹修改。\n- 祖先判定寫成 `tin[u] < tin[v]`（漏掉 $u = v$）或 `tin[v] < tout[u]`（漏掉子樹最後一個節點）。\n- `timer` 從 0 起算但樹狀陣列要求下標從 1 起算（用 `++timer` 而非 `timer++` 可避免）。\n- 用 BFS 產生時間戳（不具備連續區間性質）。\n- 試圖用時間戳處理**路徑**查詢（不連續，必須用 LCA 或樹鏈剖分）。\n- 忘記 `v == fa`。\n- 深度大時爆堆疊。\n\n## 與相似技巧的比較\n\n- 與 LCA（3.8）互補：時間戳處理**子樹**（連續區間），LCA 處理**路徑**。事實上時間戳提供的祖先判定是倍增 LCA 中判斷「是否已跳過頭」的常用工具，兩者常一起出現。\n- 歐拉序（每次進入與返回都記錄，長度 $2n-1$）搭配稀疏表可做到 $O(1)$ 查詢 LCA，那是時間戳的另一種用法。\n- 與 3.11 的 DSU on tree 相關：後者也是利用「子樹在 DFS 序中連續」這個性質，只是換一種方式利用。\n- 樹鏈剖分把「路徑」也拆成 $O(\\log n)$ 段連續區間，是時間戳思路的完整推廣，超出本節範圍。\n- 3515、3590 這類「查詢子樹內滿足某條件的節點」的題目，標準套路就是時間戳加離線排序或可持久化資料結構。\n\n## 代表例題\n\n**題號**：5、2322、3327、3515、3590\n\n放在「DFS 時間戳」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 5 | [LCP 05. 發 LeetCoin](https://leetcode.cn/problems/coin-bonus/) |  |\n| 2322 | [從樹中刪除邊的最小分數](https://leetcode.cn/problems/minimum-score-after-removals-on-a-tree/) | 2392 |\n| 3327 | [判斷 DFS 字串是否是迴文串](https://leetcode.cn/problems/check-if-dfs-strings-are-palindromes/) | 2454 |\n| 3515 | [帶權樹中的最短路徑](https://leetcode.cn/problems/shortest-path-in-a-weighted-tree/) | 2312 |\n| 3590 | [第 K 小的路徑異或和](https://leetcode.cn/problems/kth-smallest-path-xor-sum/) | 2646 |\n\n## 本節重點速查\n\n- 進入記 `tin`、離開記 `tout`，則**子樹恰對應連續區間 $[\\text{tin}[u], \\text{tout}[u]]$**，長度等於子樹大小。\n- 祖先判定 $O(1)$：`tin[u] <= tin[v] && tin[v] <= tout[u]`。\n- 這把子樹修改轉成區間修改，線段樹樹狀陣列全部可用。\n- **只對子樹有效，路徑不連續**。\n- 必須用 DFS，BFS 沒有這個性質。\n",
        },
        {
          id: 39,
          title: "3.8 最近公共祖先（LCA）、倍增演算法",
          summary:
            "## 這個技術解決什麼問題\n\n2.8 的遞迴 LCA 每次查詢 $O(n)$，$q$ 次查詢就是 $O(qn)$。當 $n, q$ 都上看 $10^5$ 時不可行。\n\n倍增（binary lifting）的槓桿是**預先算出每個節點往上跳 $2^k$ 步到達哪裡**，於是「往上跳任意 $d$ 步」可以按 $d$ 的二進位展開，用 $O(\\log n)$ 次跳躍完成。預處理 $O(n \\log n)$，之後每次查詢 $O(\\log n)$。\n\n同一套表格還能回答「$u$ 的第 $k$ 個祖先」（1483）、「$u$ 到 $v$ 路徑上的最大邊權」（把最值也一起倍增）等問題，用途遠不只 LCA。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就往「LCA / 倍增」想：\n\n- **反覆查「兩點的最近公共祖先」或「某點的第 k 個祖先」**：1483 是「第 K 祖先」裸題，許多路徑題要先用 LCA 找交會點。\n- **要把「樹上 x 到 y 的路徑」拆解**：x→y 的路徑 = x→LCA 加 LCA→y，2646、3553、3559 都靠這個拆法統計或求權。\n- **暴力做法是每次查詢都一步步往上爬**（每查 `O(n)`，查詢多就爆）；倍增預處理一次後每查 `O(log n)`。\n- 嗅到味道的瞬間：\n\n:::example 你發現「要往上跳很多步、而且要跳很多次」\n```cpp\n// 你發現「要往上跳很多步、而且要跳很多次」\n// 就預存 2^j 步跳表，把任意步數拆成二進位來跳\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我有沒有先用一次 DFS 求出每點的 parent 與 depth？\n2. 我的跳表 `up[u][j]` 遞推對不對？LOG 夠大嗎？跳到根的 -1 有處理嗎？\n3. 求 LCA 時，我有沒有「先補平深度，再一起跳到不相遇為止」？\n\n三題都答得清楚，這個模型就套對了。\n\n## 核心想法與直覺\n\n本節要解決的場景是：在一棵有根樹上反覆問「節點 `x` 和節點 `y` 的最近公共祖先（LCA）是誰？」或「x 往上走第 `k` 步到哪個祖先？」。LCA 就是「同時是 x 和 y 祖先、而且離它們最近的那個節點」——直觀講，是兩個節點往上走、最先碰頭的地方。\n\n最樸素的做法是讓較深的點先往上爬，直到兩點同深度，再一起一步步往上走到相遇。但「一步步走」最壞要 `O(n)`，查詢多了就太慢。「倍增（binary lifting）」的核心想法是：**預先記下每個節點往上 1、2、4、8…步（2 的次方步）分別到哪**，這樣任何往上 k 步都能拆成幾個 2 的次方相加，用跳的，`O(log n)` 完成。\n\n:::example 倍增預處理：up[u][j] = u 往上 2^j 步的祖先\n先看怎麼預處理這張「祖先跳表」。`up[u][j]` 表示「u 往上跳 2^j 步到達的節點」。關鍵遞推：跳 2^j 步 = 先跳 2^(j-1) 步，再從那裡跳 2^(j-1) 步：\n\n```cpp\n// 倍增預處理：up[u][j] = u 往上 2^j 步的祖先\n// up[u][0] = parent[u]（先由一次 DFS 求出 parent 與 depth）\nfor (int j = 1; j < LOG; ++j) {\n    for (int u = 0; u < n; ++u) {\n        int mid = up[u][j - 1];                      // 先跳 2^(j-1)\n        up[u][j] = (mid < 0) ? -1 : up[mid][j - 1];  // 再跳 2^(j-1)\n    }\n}\n```\n:::\n\n\n\n:::example 往上 k 步：按 k 的二進位逐位跳\n有了跳表，「往上 k 步」就把 k 寫成二進位，哪一位是 1 就跳對應的 2^j 步。1483「樹節點的第 K 個祖先」正是直接用這招：\n\n```cpp\n// 往上 k 步：按 k 的二進位逐位跳\nint kthAncestor(int u, int k) {\n    for (int j = 0; j < LOG && u >= 0; ++j) {\n        if (k >> j & 1) {\n            u = up[u][j];  // 第 j 位是 1 就跳 2^j\n        }\n    }\n    return u;\n}\n```\n:::\n\n\n\n求 LCA 就分兩步：先把深的點跳到和淺的點同深度，再讓兩點一起「能跳就跳、但不跳過頭」，最後它們的父節點就是 LCA。預處理 `O(n log n)`，每次查詢 `O(log n)`。\n\n## 狀態／資料結構定義\n\n- `LOG`：滿足 $2^{LOG} \\ge n$ 的最小整數，通常取 $\\lceil \\log_2 n \\rceil + 1$。\n- `up[k][u]`：從 $u$ 往上跳 $2^k$ 步到達的節點；跳出根則記為 $-1$（或記為根自己，兩種約定都可行但要貫徹）。\n- `dep[u]`：$u$ 的深度，根為 0。\n- 遞推式：$\\text{up}[0][u] = \\text{parent}(u)$，且\n  $$\\text{up}[k][u] = \\text{up}[k-1][\\,\\text{up}[k-1][u]\\,]$$\n  意思是「跳 $2^k$ 步 = 先跳 $2^{k-1}$ 步，再跳 $2^{k-1}$ 步」。\n- 預處理順序很重要：必須**先把所有節點的第 $k-1$ 層算完，才能算第 $k$ 層**，所以外層迴圈是 $k$、內層是 $u$。若寫成外層 $u$、內層 $k$，`up[k-1][up[k-1][u]]` 可能還沒算好。\n\n## 不變量或正確性證明\n\n**遞推式的正確性。** 從 $u$ 往上跳 $2^k$ 步，等於先跳 $2^{k-1}$ 步到達某節點 $w = \\text{up}[k-1][u]$，再從 $w$ 跳 $2^{k-1}$ 步。因為 $2^{k-1} + 2^{k-1} = 2^k$，且**祖先鏈是一條唯一的路徑**（每個節點恰有一個父節點），所以「跳 $a$ 步再跳 $b$ 步」與「跳 $a+b$ 步」結果相同。這個結合性正是倍增能成立的根本——它在一般圖上不成立（往上的路徑不唯一），只在樹（或更廣義的函數圖）上成立。\n\n**任意步數可分解。** 任何 $d \\ge 0$ 都能唯一寫成 $\\sum_{k \\in S} 2^k$（二進位表示），故往上跳 $d$ 步可以拆成 $|S| \\le \\log_2 d + 1$ 次跳躍。實作上枚舉 $d$ 的每個為 1 的位元即可。\n\n**LCA 演算法的正確性。** 分兩階段。\n\n*階段一：把較深的節點提到與較淺者同深度。* 設 $\\text{dep}[u] \\ge \\text{dep}[v]$，把 $u$ 往上跳 $\\text{dep}[u] - \\text{dep}[v]$ 步。跳完後 $u' $ 是 $u$ 的祖先且與 $v$ 同深度。此時若 $u' = v$，則 $v$ 本來就是 $u$ 的祖先，LCA 即 $v$，直接回傳。\n\n*階段二：兩者同時往上跳，找到「最深的仍然不同」的位置。* 對 $k$ 從大到小枚舉，若 $\\text{up}[k][u] \\ne \\text{up}[k][v]$ 就同時跳。\n\n這一步的不變量是：**每次跳躍後，$u$ 與 $v$ 仍然不相等，且它們的 LCA 保持不變。**\n\n- 「仍不相等」由跳躍條件直接保證。\n- 「LCA 不變」需要論證：若 $\\text{up}[k][u] \\ne \\text{up}[k][v]$，表示往上跳 $2^k$ 步還沒到達 LCA（因為一旦到達或超過 LCA，兩者會落在同一條祖先鏈上而可能相等——更精確地說，設 $L$ 是 LCA，若 $2^k \\ge \\text{dep}[u] - \\text{dep}[L]$，則 $\\text{up}[k][u]$ 是 $L$ 或 $L$ 的祖先，同理 $\\text{up}[k][v]$ 也是，且兩者深度相同，故必相等）。既然沒到達 LCA，跳過去之後 $u$、$v$ 仍在 LCA 的兩棵不同子樹中，LCA 不變。\n- 由 $k$ 從大到小枚舉，這是標準的貪心二進位逼近：結束時 $u$ 與 $v$ 恰好停在 LCA 的兩個不同子節點上（再往上一步就相同）。故 $\\text{up}[0][u]$ 即為 LCA。\n\n**為何 $k$ 必須從大到小。** 從小到大枚舉會失去「每一位獨立決策」的性質：跳了小步之後，剩餘距離的二進位表示會改變，無法保證用剩下的位元湊出正確結果。從大到小則等價於貪心地決定二進位表示的每一位——與二進位轉換的標準算法一致。**這個順序不是慣例，是正確性要求。**\n\n## 逐步演算法\n\n把任何 LCA / 倍增題拆成：求 parent 與 depth、建跳表、跳同深度、一起跳到 LCA。\n\n:::example DFS 求 parent（即 up[u][0]）與 depth\n**1. 一次 DFS 求每點的父節點與深度。** 這是倍增的基礎資料。\n\n```cpp\n// DFS 求 parent（即 up[u][0]）與 depth\nvoid dfs(int u, int fa, int d) {\n    up[u][0] = fa;\n    depth[u] = d;\n    for (int v : g[u]) {\n        if (v != fa) {\n            dfs(v, u, d + 1);\n        }\n    }\n}\n```\n:::\n\n\n\n:::example 由小到大填每個 2^j 級別\n**2. 用遞推填滿整張跳表。** 如前述：先跳一半，再跳一半。\n\n```cpp\n// 由小到大填每個 2^j 級別\nfor (int j = 1; j < LOG; ++j) {\n    for (int u = 0; u < n; ++u) {\n        up[u][j] = up[u][j - 1] < 0 ? -1 : up[up[u][j - 1]][j - 1];\n    }\n}\n```\n:::\n\n\n\n:::example 把 u 往上抬到與目標同深度\n**3. 查詢時先把較深的點提到同深度。** 用「往上 k 步」把深度差補平。\n\n```cpp\n// 把 u 往上抬到與目標同深度\nint lift(int u, int k) {  // k = depth 差\n    for (int j = 0; j < LOG; ++j) {\n        if (k >> j & 1) {\n            u = up[u][j];\n        }\n    }\n    return u;\n}\n```\n:::\n\n\n\n:::example 同深度後一起跳：能不相遇就跳，最後父節點是 LCA\n**4. 兩點一起往上跳，求 LCA。** 從大步到小步，若兩點跳完仍不同才跳（避免跳過頭）；結束時父節點即 LCA。\n\n```cpp\n// 同深度後一起跳：能不相遇就跳，最後父節點是 LCA\nint lca(int a, int b) {\n    if (depth[a] < depth[b]) {\n        swap(a, b);\n    }\n    a = lift(a, depth[a] - depth[b]);\n    if (a == b) {\n        return a;\n    }\n    for (int j = LOG - 1; j >= 0; j--) {\n        if (up[a][j] != up[b][j]) {\n            a = up[a][j];\n            b = up[b][j];\n        }\n    }\n    return up[a][0];\n}\n```\n:::\n\n\n\n- `LOG` 要夠大：滿足 `2^LOG >= n`（取 `ceil(log2(n)) + 1`），否則跳不夠高。\n- 跳到根時 parent 用 `-1` 表示，跳轉前要判，避免越界。\n- 像 2646「最小化旅行的價格總和」用 LCA 拆解每條路徑、統計節點被經過次數；3553「包含給定路徑的最小帶權子樹 II」也靠 LCA 定位路徑交點。預處理 `O(n log n)`，每查 `O(log n)`。\n\n## C++17 模板\n\n```cpp\n// 倍增 LCA：預處理 O(n log n)，查詢 O(log n)。\nclass LCA {\n  int n_, LOG_;\n  std::vector<std::vector<int>> g_, up_;   // up_[k][u] = u 往上跳 2^k 步\n  std::vector<int> dep_;\n\n  void dfs(int u, int fa) {\n    up_[0][u] = fa;\n    for (int v : g_[u]) {\n      if (v == fa) { continue; }\n      dep_[v] = dep_[u] + 1;\n      dfs(v, u);\n    }\n  }\n\n public:\n  LCA(int n, const std::vector<std::vector<int>>& edges) : n_(n) {\n    LOG_ = 1;\n    while ((1 << LOG_) < n_) { ++LOG_; }\n    ++LOG_;\n\n    g_.assign(n_, {});\n    for (const auto& e : edges) {\n      g_[e[0]].push_back(e[1]);\n      g_[e[1]].push_back(e[0]);\n    }\n    up_.assign(LOG_, std::vector<int>(n_, -1));\n    dep_.assign(n_, 0);\n    dfs(0, -1);\n\n    for (int k = 1; k < LOG_; ++k) {          // 外層必須是 k：先算完第 k-1 層\n      for (int u = 0; u < n_; ++u) {\n        int mid = up_[k - 1][u];\n        up_[k][u] = (mid == -1) ? -1 : up_[k - 1][mid];\n      }\n    }\n  }\n\n  int kthAncestor(int u, int k) const {       // 第 k 個祖先（LeetCode 1483）\n    for (int i = 0; i < LOG_ && u != -1; ++i) {\n      if (k >> i & 1) { u = up_[i][u]; }\n    }\n    return u;\n  }\n\n  int lca(int u, int v) const {\n    if (dep_[u] < dep_[v]) { std::swap(u, v); }\n    u = kthAncestor(u, dep_[u] - dep_[v]);    // 階段一：提到同深度\n    if (u == v) { return u; }                 // v 本來就是 u 的祖先\n\n    for (int k = LOG_ - 1; k >= 0; --k) {     // 階段二：k 從大到小\n      if (up_[k][u] != up_[k][v]) {           // 還沒跳到 LCA，安全地跳\n        u = up_[k][u];\n        v = up_[k][v];\n      }\n    }\n    return up_[0][u];                         // 此刻兩者停在 LCA 的兩個子節點\n  }\n\n  int dist(int u, int v) const {              // 樹上距離\n    return dep_[u] + dep_[v] - 2 * dep_[lca(u, v)];\n  }\n};\n```\n\n## 時間與空間複雜度\n\n預處理：一次 DFS $O(n)$，填表雙重迴圈 $O(n \\log n)$。\n\n查詢：階段一至多 $\\log n$ 次跳躍，階段二枚舉 $\\log n$ 個位元，故 $O(\\log n)$。\n\n空間 $O(n \\log n)$——這是倍增的主要代價。$n = 10^5$、$\\text{LOG} = 18$ 時約 $1.8 \\times 10^6$ 個 `int`，約 7 MB，通常可接受但要留意題目的記憶體限制。\n\n替代方案的取捨：歐拉序加稀疏表是 $O(n \\log n)$ 預處理、$O(1)$ 查詢，空間同樣 $O(n \\log n)$，查詢更快但實作稍繁；Tarjan 離線 LCA 是近線性時間與 $O(n)$ 空間，但**必須離線**（所有查詢預先給定）。**單次查詢用 2.8 的遞迴版就好，不要為了一次查詢建表。**\n\n## 常見錯誤與邊界條件\n\n- 填表時外層迴圈寫成節點、內層寫成 $k$，導致依賴的值尚未算好。\n- 階段二的 $k$ 從小到大枚舉。\n- 階段一忘記先判 `u == v` 就進入階段二（此時 `up[k][u] != up[k][v]` 恆為假，會直接回傳 `up[0][u]`，即 $v$ 的父節點，答案偏高一層）。\n- `LOG` 取太小（$2^{LOG} < n$ 時跳不夠遠）。\n- 跳出根時的約定不一致（有時記 $-1$、有時記根，混用會解參考 $-1$）。\n- `kthAncestor` 未處理 $k$ 大於深度的情形（應回傳 $-1$）。\n- 森林（非連通）時要對每個連通分量各跑一次 DFS。\n- 深度大時 DFS 爆堆疊（可改迭代或用 BFS 建 `dep` 與 `up[0]`）。\n\n## 與相似技巧的比較\n\n- 與 2.8 的遞迴 LCA：**單次查詢用遞迴（$O(n)$ 但零預處理），多次查詢用倍增。** 這是唯一的選擇依據。\n- 與 3.7 的時間戳：時間戳處理子樹、LCA 處理路徑，兩者互補。時間戳的祖先判定常被用在 LCA 的實作或驗證中。\n- 倍增不只能存「祖先是誰」——把 `up[k][u]` 擴充成同時存「這 $2^k$ 步中的最大邊權／和」，就能 $O(\\log n)$ 回答路徑最值與路徑和查詢（合併時取 `max` 或相加）。這是倍增最有價值的推廣，前提是該運算**可結合**。\n- 樹上距離 $d(u,v) = \\text{dep}[u] + \\text{dep}[v] - 2\\,\\text{dep}[\\text{LCA}]$，這條公式讓 LCA 直接支撐所有樹上距離查詢（2277、2646 等）。\n\n## 代表例題\n\n**題號**：2277、1483、3559、2646、3553\n\n放在「最近公共祖先（LCA）、倍增演算法」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 2277 | [樹中最接近路徑的節點](https://leetcode.cn/problems/closest-node-to-path-in-tree/)（Premium） |  |\n| 1483 | [樹節點的第 K 個祖先](https://leetcode.cn/problems/kth-ancestor-of-a-tree-node/) | 2115 |\n| 3559 | [給邊賦權值的方案數 II](https://leetcode.cn/problems/number-of-ways-to-assign-edge-weights-ii/) | 2146 |\n| 2646 | [最小化旅行的價格總和](https://leetcode.cn/problems/minimize-the-total-price-of-the-trips/) | 2238 |\n| 3553 | [包含給定路徑的最小帶權子樹 II](https://leetcode.cn/problems/minimum-weighted-subgraph-with-the-required-paths-ii/) | 2411 |\n| 3585 | [樹中找到帶權中位節點](https://leetcode.cn/problems/find-weighted-median-node-in-tree/) | 2429 |\n| 3534 | [針對圖的路徑存在性查詢 II](https://leetcode.cn/problems/path-existence-queries-in-a-graph-ii/) | 2507 |\n| 2846 | [邊權重均等查詢](https://leetcode.cn/problems/minimum-edge-weight-equilibrium-queries-in-a-tree/) | 2508 |\n| 2836 | [在傳球遊戲中最大化函式值](https://leetcode.cn/problems/maximize-value-of-function-in-a-ball-passing-game/) | 2769 |\n| 3464 | [正方形上的點之間的最大距離](https://leetcode.cn/problems/maximize-the-distance-between-points-on-a-square/) | 2806 |\n\n## 本節重點速查\n\n- `up[k][u] = up[k-1][up[k-1][u]]`，**填表外層必須是 $k$**。\n- 查詢兩階段：先把深的提到同深度（若相等則淺者即 LCA），再讓 $k$ **從大到小**同時跳（只在 `up[k][u] != up[k][v]` 時跳），最後回傳 `up[0][u]`。\n- $k$ 從大到小是正確性要求，不是慣例。\n- 預處理 $O(n \\log n)$、查詢 $O(\\log n)$、空間 $O(n \\log n)$。\n- 單次查詢別建表。\n",
        },
        {
          id: 55,
          title: "3.9 虛樹",
          summary:
            "## 這個技術解決什麼問題\n\n樹有 $n$ 個節點，但每次詢問只關心其中 $k$ 個關鍵點，且 $\\sum k$ 遠小於 $qn$。逐次掃全樹是 $O(qn)$，太慢。\n\n虛樹的槓桿是**把「與答案無關的過路點」壓縮掉**：只保留關鍵點與它們之間的岔路口（LCA），重新連成一棵節點數 $O(k)$ 的小樹，在小樹上跑原本的樹形 DP。單次成本從 $O(n)$ 降到 $O(k \\log k)$。\n\n它的價值不在於某一題，而在於它把「大樹＋小關鍵點集」這個常見結構，化約成「小樹上的標準樹形 DP」——你原本會寫的 DP 一行都不用改。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，可以往虛樹想：\n\n- **樹很大，但每次詢問只牽涉一小撮特殊節點**，而且所有詢問的節點數加起來不算太多。這是虛樹最典型的觸發點。\n- **答案只跟「關鍵點之間的路徑、它們的 LCA」有關**，路徑上其他普通點只是「過路」，不影響結果。\n- **暴力是每次詢問都掃整棵樹**（一次 `O(n)`），詢問一多就爆；虛樹把每次壓到關鍵點規模。\n- 題目把節點「分組」，要對每組各算一次（如 3786），各組合起來節點總數可控——非常適合對每組建虛樹。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的「關鍵點」是誰？所有詢問的關鍵點總數是否遠小於 n？\n2. 我的答案是不是只依賴關鍵點與它們的 LCA，普通點可以被壓縮掉？\n3. 多次查詢時，我有沒有只清空虛樹用到的點，而不是整棵樹？\n\n三題都答得出來，就套對了。\n\n## 核心想法與直覺\n\n本節要解決的場景是：一棵很大的樹（節點上萬、上百萬），但每次題目其實只「關心」其中少數幾個特殊節點（稱為關鍵點）。如果每次查詢都把整棵樹掃一遍，會慢到無法接受。「虛樹（virtual tree）」這個技巧的目的，就是只用這些關鍵點和它們之間的「轉折點」，臨時搭出一棵很小的樹，在小樹上算答案。\n\n先想清楚：兩個關鍵點之間的路徑，會在某個「最近公共祖先（LCA，兩個節點往上走第一次相遇的點）」匯合。所有關鍵點之間的 LCA，就是路徑上真正重要的「岔路口」。虛樹的點集合 = 所有關鍵點 + 它們兩兩之間的 LCA。\n\n:::example 虛樹的點集合 = 關鍵點 + 它們在 DFS 序下相鄰兩個的 LCA\n關鍵的事實是：**就算原樹有上百萬個點，虛樹的點數最多只有關鍵點數量的兩倍。** 所以原本 `O(n)` 的處理，可以縮到 `O(k)`（k 是關鍵點數），快非常多。\n\n```cpp\n// 虛樹的點集合 = 關鍵點 + 它們在 DFS 序下相鄰兩個的 LCA\nsort(key.begin(), key.end(), [&](int a, int b) {\n    return dfn[a] < dfn[b];  // dfn：DFS 進入順序（時間戳）\n});\nfor (int i = 0; i + 1 < key.size(); ++i) {\n    key.push_back(lca(key[i], key[i + 1]));  // 補進轉折點\n}\n```\n:::\n\n\n\n:::example 排序去重後，剩下的就是虛樹所有節點\n為什麼要照 DFS 序排？因為在 DFS 序裡相鄰的兩個關鍵點，它們的 LCA 已經涵蓋所有需要的岔路口，不必兩兩都算，省下大量計算。\n\n```cpp\n// 排序去重後，剩下的就是虛樹所有節點\nsort(key.begin(), key.end(), [&](int a, int b) { return dfn[a] < dfn[b]; });\nkey.erase(unique(key.begin(), key.end()), key.end());\n// 之後用單調棧把這些點按祖先關係連成一棵小樹\n```\n:::\n\n\n\n像 3786「樹組的互動代價總和」這種題，分組後每組只在乎自己那幾個節點，虛樹讓我們對每組獨立、快速地計算。\n\n## 狀態／資料結構定義\n\n- 原樹預處理：`dfn[u]`（DFS 進入時間戳，見 3.7）、`dep[u]`、倍增表 `up[k][u]`（見 3.8）。這兩節是虛樹的前置。\n- 關鍵點陣列 `key`，按 `dfn` 遞增排序。\n- **虛樹點集** $V' = \\text{key} \\cup \\{\\,\\mathrm{lca}(\\text{key}_i, \\text{key}_{i+1}) : 1 \\le i < k\\,\\}$，排序去重後即為最終點集。\n- 虛樹鄰接表 `vt`：只存 $V'$ 中的點，**每次用完只清 $V'$ 中的點**，不可 `memset` 整棵原樹。\n- 建樹用的單調棧 `stk`：棧內由底到頂是一條深度嚴格遞增的祖先鏈。\n- 虛樹的邊代表原樹上的一段路徑，若題目在意路徑長度，邊權取 $\\text{dep}[v] - \\text{dep}[u]$（或路徑權重和）。\n\n## 不變量或正確性證明\n\n**引理（為何只需相鄰兩點的 LCA）。** 設關鍵點按 `dfn` 排序為 $a_1, \\dots, a_k$。則\n$$\\{\\,\\mathrm{lca}(a_i, a_j) : i < j\\,\\} = \\{\\,\\mathrm{lca}(a_t, a_{t+1}) : 1 \\le t < k\\,\\}.$$\n\n證明。$\\supseteq$ 顯然。證 $\\subseteq$：固定 $i < j$，令 $L = \\mathrm{lca}(a_i, a_j)$。由 3.7，$L$ 的子樹在 `dfn` 上是連續區間，而 $\\mathrm{dfn}(a_i)$ 與 $\\mathrm{dfn}(a_j)$ 都在其中，故 $a_i, a_{i+1}, \\dots, a_j$ **全部**落在 $L$ 的子樹內。把這些點按「屬於 $L$ 的哪個子節點的子樹」分組——由於 `dfn` 連續區間性質，同組的點在排序後是連續的一段。$a_i$ 與 $a_j$ 不在同一組（否則它們的 LCA 會比 $L$ 更深）。因此存在某個 $t \\in [i, j)$ 使 $a_t$ 與 $a_{t+1}$ 分屬不同組（或其中之一就是 $L$ 本身），此時 $\\mathrm{lca}(a_t, a_{t+1}) = L$。故 $L$ 在右側集合中。$\\square$\n\n這條引理把要算的 LCA 從 $\\binom{k}{2}$ 個降到 $k-1$ 個，是虛樹能做到 $O(k \\log k)$ 的根本原因。**它整個建立在 3.7 的「子樹在 dfn 上連續」之上**——若不按 `dfn` 排序，引理不成立，必須真的兩兩求 LCA。\n\n**點數上界：$|V'| \\le 2k - 1$。** 虛樹中的每個非關鍵點都是某兩個關鍵點的 LCA，因此在虛樹中至少有兩個子節點（否則它就不是分岔點，可被壓縮掉）。一棵有根樹中「子節點數 $\\ge 2$ 的節點數」嚴格小於「葉節點數」，而葉節點必為關鍵點（非關鍵點都是分岔點），故葉數 $\\le k$，分岔點數 $\\le k - 1$，總計 $\\le 2k - 1$。**這個上界保證虛樹的規模與 $n$ 無關**，才有意義。\n\n**單調棧的不變量：處理完前 $i$ 個點後，棧內由底到頂是一條深度嚴格遞增的鏈，且棧中每個點都是 $a_i$ 的祖先（或 $a_i$ 自己）。**\n\n- 初始化：棧內只有 $a_1$，成立。\n- 保持：處理 $a_{i+1}$ 時令 $l = \\mathrm{lca}(\\text{stk.top}, a_{i+1})$。由於 `dfn` 遞增，$a_{i+1}$ 不在棧頂的子樹內時，棧中所有深度大於 $\\mathrm{dep}(l)$ 的點的子樹**已經完全處理完畢**（它們的 dfn 區間已被走過），可以安全彈出並連邊。彈到深度 $\\le \\mathrm{dep}(l)$ 後，若棧頂不是 $l$ 就把 $l$ 補進去（它是新的分岔點），再壓入 $a_{i+1}$。不變量恢復。\n- 終止：最後把棧清空、逐一連邊，得到完整虛樹。\n\n「已完全處理完畢」這一句依賴的前提是**輸入按 `dfn` 遞增**——這也是為什麼排序不能省，且不能改用別的順序。\n\n## 逐步演算法\n\n虛樹的建構是固定套路，拆成幾步：\n\n:::example 預處理：記錄 dfn、深度、倍增祖先表\n**1. 預處理：DFS 序 + LCA。** 先對原樹做一次 DFS，記下每個點的進入時間戳 `dfn` 與深度，並建好可以 `O(log n)` 查 LCA 的倍增表（倍增：預存「往上跳 1, 2, 4, 8… 步」的祖先，加快往上找）。\n\n```cpp\n// 預處理：記錄 dfn、深度、倍增祖先表\nvoid dfs(int u, int fa) {\n    dfn[u] = ++clk;\n    dep[u] = dep[fa] + 1;\n    up[u][0] = fa;\n    for (int k = 1; k < LOG; ++k) {\n        up[u][k] = up[up[u][k - 1]][k - 1];\n    }\n    for (int v : g[u]) {\n        if (v != fa) {\n            dfs(v, u);\n        }\n    }\n}\n```\n:::\n\n\n\n**2. 收集點集合。** 把關鍵點按 `dfn` 排序，相鄰兩個求 LCA 加進來，再排序去重（如上方原理的程式碼）。\n\n:::example 單調棧建虛樹：棧裡是當前的一條祖先鏈\n**3. 用單調棧連邊。** 依 `dfn` 順序處理每個點，維護一個「目前這條根到當前點的鏈」的棧。新點若不在棧頂的子樹內，就把棧頂彈出並連邊，直到能掛上去。\n\n```cpp\n// 單調棧建虛樹：棧裡是當前的一條祖先鏈\nstk[top = 0] = key[0];\nfor (int i = 1; i < key.size(); ++i) {\n    int u = key[i], l = lca(stk[top], u);\n    while (top && dep[stk[top - 1]] >= dep[l]) {  // 彈出比 l 還深的\n        vt[stk[top - 1]].push_back(stk[top]);\n        top--;  // 連虛樹邊\n    }\n    if (stk[top] != l) {\n        vt[l].push_back(stk[top]);\n        stk[top] = l;\n    }\n    stk[++top] = u;\n}\nwhile (top) {\n    vt[stk[top - 1]].push_back(stk[top]);\n    top--;\n}\n```\n:::\n\n\n\n**4. 在虛樹上跑你的演算法、再清空。** 虛樹建好後，就在它上面做樹形 DP 或彙總。多次查詢時務必只清空用到的點（不要 memset 整棵原樹），否則複雜度又退回 `O(n)`。\n\n複雜度：每次查詢 `O(k log k)`（排序 + LCA），總和遠小於對整棵樹硬做。\n\n## C++17 模板\n\n```cpp\n// 虛樹建構。前置：dfn / dep / 倍增 LCA（見 3.7、3.8）。\n// key 為關鍵點，回傳虛樹的根；vt 為虛樹鄰接表。\nstd::vector<std::vector<int>> vt;   // 只在 V' 上有內容\nstd::vector<int> dfn, dep;\n\nint buildVirtualTree(std::vector<int>& key) {\n  std::sort(key.begin(), key.end(),\n            [](int a, int b) { return dfn[a] < dfn[b]; });   // 必須按 dfn\n\n  int k = key.size();\n  for (int i = 0; i + 1 < k; ++i) {\n    key.push_back(lca(key[i], key[i + 1]));                  // 只需相鄰的 LCA\n  }\n  std::sort(key.begin(), key.end(),\n            [](int a, int b) { return dfn[a] < dfn[b]; });\n  key.erase(std::unique(key.begin(), key.end()), key.end());\n\n  for (int u : key) { vt[u].clear(); }                       // 只清 V'，不清全樹\n\n  std::vector<int> stk;\n  stk.push_back(key[0]);\n  for (size_t i = 1; i < key.size(); ++i) {\n    int u = key[i];\n    int l = lca(stk.back(), u);\n    while (stk.size() >= 2 && dep[stk[stk.size() - 2]] >= dep[l]) {\n      vt[stk[stk.size() - 2]].push_back(stk.back());         // 子樹已處理完，連邊\n      stk.pop_back();\n    }\n    if (stk.back() != l) {                                   // l 是新的分岔點\n      vt[l].push_back(stk.back());\n      stk.back() = l;\n    }\n    stk.push_back(u);\n  }\n  while (stk.size() >= 2) {\n    vt[stk[stk.size() - 2]].push_back(stk.back());\n    stk.pop_back();\n  }\n  return stk[0];                                             // 虛樹的根\n}\n```\n\n## 時間與空間複雜度\n\n單次建虛樹：排序 $O(k \\log k)$，$k-1$ 次 LCA 各 $O(\\log n)$，單調棧 $O(k)$。合計 $O(k \\log k + k \\log n)$。\n\n之後在虛樹上跑樹形 DP 是 $O(|V'|) = O(k)$。\n\n全部 $q$ 次詢問：$O\\!\\left(n \\log n + \\sum_i k_i (\\log k_i + \\log n)\\right)$，其中 $n \\log n$ 是一次性的倍增預處理。相對於暴力的 $O(qn)$，當 $\\sum k_i \\ll qn$ 時收穫巨大。\n\n空間：倍增表 $O(n \\log n)$，虛樹鄰接表 $O(n)$（但每次只用到 $O(k)$ 個位置）。\n\n**清空的成本必須算進去。** 若每次詢問都 `memset` 整個 `vt` 陣列，單次就是 $O(n)$，總計 $O(qn)$——虛樹的全部好處被抵銷。這不是常數層級的疏忽，是複雜度層級的錯誤。\n\n## 常見錯誤與邊界條件\n\n- 沒有按 `dfn` 排序就取相鄰 LCA（引理不成立，會漏掉分岔點）。\n- 只排一次序、加入 LCA 後忘記重新排序去重。\n- 用 `memset` 清空整棵原樹的鄰接表（複雜度退化）。\n- 忘記去重導致同一點在虛樹中出現兩次。\n- 單調棧彈出條件寫成 `>` 而非 `>=`（$l$ 恰好等於棧中某點時會漏連邊）。\n- 虛樹的根不是原樹的根（是所有關鍵點的 LCA），DP 時不要誤用原根。\n- 只有一個關鍵點時虛樹只有一個節點（迴圈都不執行，要確認 DP 能處理）。\n- 虛樹邊權若要用原樹路徑長，記得用深度差而非 1。\n- 虛樹上的「子節點」與原樹的子節點不同，別把原樹的鄰接表混進來。\n\n## 與相似技巧的比較\n\n- 與 3.7、3.8：虛樹是這兩節的**組合應用**，兩者都是硬前置。不熟 dfn 的連續區間性質就無法理解為何只取相鄰 LCA。\n- 與 3.11 的 DSU on tree：兩者都在對付「每次都掃子樹太慢」。差別在於 DSU on tree 要求**每個節點都要答案**（無法壓縮），虛樹則是**只有少數點被關心**（可以壓縮）。關鍵點多到接近 $n$ 時虛樹沒有好處。\n- 與 3.12 的點分治：點分治處理「任意點對的路徑統計」，虛樹處理「指定點集上的樹形 DP」。前者是分治，後者是壓縮，思路正交。\n- 樹鏈剖分也能處理多次路徑查詢，但它的長處在「路徑修改／查詢」，不在「對點集跑 DP」。\n- 3786 這類「把節點分組、對每組各算一次」的題目是虛樹的教科書場景：各組節點數總和有界，逐組建虛樹即可。\n\n## 代表例題\n\n**題號**：3786\n\n放在「虛樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 3786 | [樹組的互動代價總和](https://leetcode.cn/problems/total-sum-of-interaction-cost-in-tree-groups/) | 2139 |\n\n## 本節重點速查\n\n- 虛樹點集 = 關鍵點 ∪ **按 `dfn` 排序後相鄰兩點的 LCA**，這條引理靠的是 3.7 的「子樹在 dfn 上連續」。\n- 點數上界 $2k - 1$，與 $n$ 無關。\n- 單調棧內始終是一條深度遞增的祖先鏈，`dfn` 遞增保證彈出的點其子樹已處理完。\n- **清空只清虛樹用到的點**，`memset` 全樹會讓複雜度退回暴力。\n",
        },
        {
          id: 40,
          title: "3.10 樹上滑動視窗",
          summary:
            "## 這個技術解決什麼問題\n\n陣列上的滑動視窗解決「最長／最短的連續子陣列滿足某條件」。樹上的對應問題是「最長的**由上往下路徑**滿足某條件」——例如路徑上顏色不重複（3425）、最多允許一種顏色重複（3486）。\n\n暴力枚舉每對祖先-後代再驗證是 $O(n^2)$ 或更差。樹上滑動視窗的槓桿是：**DFS 進行時，「根到當前節點」的那條鏈天然就是一個右端不斷擴張的視窗**，只要把陣列版的「左指標」改成「深度下界」，整套增量更新原封不動搬過來，一次 DFS $O(n)$。\n\n## 辨識題型的訊號\n\n看到這些特徵，往樹上滑動視窗想：\n\n- **答案是「一條由上往下的路徑」**，且路徑要滿足某個對端點單調的條件（元素不重複、和不超過上限、種類數受限）。\n- **這個條件在陣列上就是經典滑動視窗**，只是把「連續子陣列」換成「樹上的祖先-後代鏈」。\n- **暴力是枚舉每對祖先-後代再驗證**（`O(n^2)` 起跳）；本節用一次 DFS + 窗口更新壓到接近 `O(n)`。\n- 題目常見「特殊路徑」「不重複顏色路徑」「最長/最短向下路徑」等字眼（如 3425 / 3486）。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的「窗口」是不是就是當前 DFS 的那條根到節點的鏈？\n2. 條件是否對端點移動具單調性，能像陣列那樣縮左端？\n3. 我有沒有在回溯時還原本次的修改，避免污染兄弟子樹？\n\n三題都答得出來，就套對了。\n\n## 核心想法與直覺\n\n本節把熟悉的「滑動視窗」搬到樹上。先回顧陣列版的滑動視窗：用左右兩個指標框住一段連續區間 `[left, right]`，右指標往右擴張納入新元素，當區間違反條件時左指標往右縮，整段移動過程中答案可以「增量更新」而不必重算。它快的關鍵是條件對端點移動有單調性。\n\n「樹上滑動視窗」要處理的是：視窗不再是陣列上的連續區間，而是**樹上一條由上往下的路徑**（從某個祖先到某個後代）。我們在 DFS 往下走時，這條路徑就是「當前根到當前節點」的那一段；節點進入路徑 = 右指標擴張，節點離開（回溯）= 左指標的概念。\n\n:::example 樹上滑動視窗：窗口 = 根到當前節點的那條鏈，沿 DFS 移動\n以 3425「最長特殊路徑」為例：要找一條由上往下、路徑上節點顏色都不重複的最長路徑。這正是「視窗內元素不可重複」這個經典滑動窗口條件，只是窗口長在樹上。\n\n```cpp\n// 樹上滑動視窗：窗口 = 根到當前節點的那條鏈，沿 DFS 移動\n// top 代表窗口左端（祖先方向）的深度界線，往下走時動態抬高\nvoid dfs(int u, int fa, int topDep) {\n    // topDep：窗口上界；u 加入後若有衝突，就把上界往下壓\n}\n```\n:::\n\n\n\n:::example 用 lastDepth[color] 記某顏色「上次出現的深度」當作左端依據\n核心想法：DFS 沿著一條鏈往下，相當於右指標一直擴張；當某個顏色重複，就把「左端」（窗口頂端的深度）往下壓到讓衝突消失為止——這跟陣列版「左指標前進、移除舊元素」一模一樣。回溯時要把這次加入造成的改動還原，這樣兄弟子樹才不會互相污染。\n\n```cpp\n// 用 lastDepth[color] 記某顏色「上次出現的深度」當作左端依據\nint prev = lastDepth[color[u]];  // 同色上次的深度\nint newTop = max(topDep, prev + 1);  // 衝突 → 把窗口上界壓到它下面一格\nans = max(ans, dis[u] - dis[newTop祖先]);  // 用合法窗口更新答案\n```\n:::\n\n## 狀態／資料結構定義\n\n以 3425（最長特殊路徑：路徑上顏色互異）為例。\n\n- `dis[d]`：根到深度 $d$ 的節點的路徑權重前綴和（用一個隨 DFS 增減的堆疊陣列維護）。有了它，視窗 $[\\text{top}, \\text{dep}(u)]$ 的長度就是 $\\text{dis}[\\text{dep}(u)] - \\text{dis}[\\text{top}]$。\n- `lastDepth[c]`：**當前這條鏈上**顏色 $c$ 最近一次出現的深度；不存在記 $-1$。這是全域結構，回溯時必須還原。\n- `top`：視窗的深度下界，意義是「合法路徑的最淺端點所在深度」。它**以參數形式往下傳**，不是全域變數。\n- 進入 $u$ 時：`saved = lastDepth[color[u]]`，`top = max(parentTop, saved + 1)`，`lastDepth[color[u]] = dep[u]`。\n- 離開 $u$ 前：`lastDepth[color[u]] = saved`。\n\n`top` 傳參、`lastDepth` 全域加還原——這個分工是本節的實作核心，兩者混淆就會錯。\n\n## 不變量或正確性證明\n\n**不變量一（`lastDepth` 的意義）：在處理節點 $u$ 的期間，對每個顏色 $c$，`lastDepth[c]` 等於「根到 $u$ 這條鏈上顏色 $c$ 最深的出現位置」（不存在則為 $-1$）。**\n\n由結構歸納。進入 $u$ 時把 `lastDepth[color[u]]` 更新為 $\\mathrm{dep}(u)$，而 $u$ 是鏈上最深的節點，故成立；其他顏色不受影響。離開 $u$ 前還原成進入時的值，於是父節點的視角恢復原狀，兄弟子樹看到的是「不含 $u$ 的鏈」。**還原這一步不是清理習慣，是不變量的一部分**——少了它，兄弟子樹會誤以為 $u$ 的顏色在自己的鏈上出現過。\n\n**不變量二（`top` 的意義）：`top` 是使區間 $[\\text{top}, \\mathrm{dep}(u)]$ 對應的鏈段顏色互異的最小深度。**\n\n- 初始化：根節點，$\\text{top} = 0$（單點必然互異）。\n- 保持：設父節點 $p$ 的下界為 $\\text{ptop}$，即 $[\\text{ptop}, \\mathrm{dep}(p)]$ 互異且 $\\text{ptop}$ 最小。加入 $u$ 後，新的衝突**只可能來自 $u$ 自己的顏色**（其他顏色的互異性沒被改動）。設該顏色在鏈上前一次出現於深度 $s = \\text{saved}$：\n  - 若 $s < \\text{ptop}$，那次出現已在視窗外，無新衝突，下界仍為 $\\text{ptop}$。\n  - 若 $s \\ge \\text{ptop}$，必須把下界推到 $s + 1$ 才能排除它。\n  - 兩者合起來就是 $\\text{top} = \\max(\\text{ptop}, s+1)$，且這是**最小**的可行值（再小一格就會含入某個衝突）。\n\n這裡有一個容易被忽略的前提：**只需檢查「最近一次出現」就夠了**。因為若顏色 $c$ 在深度 $s' < s$ 也出現過，把下界推到 $s+1$ 之後 $s'$ 自動被排除；反之若只推到 $s'+1$，深度 $s$ 的那次仍在視窗內。故「最近」是唯一需要記錄的資訊，`lastDepth` 只存一個值就夠。\n\n**不變量三（單調性，也是「滑動視窗」三個字的來源）：沿著任一條根到葉的鏈往下走，`top` 單調不減。**\n\n由 $\\text{top} = \\max(\\text{ptop}, s+1) \\ge \\text{ptop}$ 直接得到。這正是陣列版滑動視窗中「左指標只右移不左移」的樹上對應，也是總複雜度 $O(n)$ 的來源：下界在每條鏈上只往下移，不回頭。\n\n**注意單調性只沿著「一條鏈」成立，不是全域成立。** 兄弟子樹的 `top` 各自從父節點的值出發，彼此獨立。這正是為什麼 `top` 必須用參數傳遞——若寫成全域變數並在回溯時忘記還原，第二棵子樹會繼承第一棵子樹壓下去的界線，答案偏小。\n\n**答案的完整性。** 對每個節點 $u$，$[\\text{top}, \\mathrm{dep}(u)]$ 是以 $u$ 為下端點的最長合法路徑。每條合法向下路徑都有唯一的下端點，故枚舉所有 $u$ 不重不漏。\n\n## 逐步演算法\n\n把樹上滑動視窗拆成幾步，每步對照陣列版的影子：\n\n:::example 狀態：dis 是根到各點的距離前綴；lastDepth 記每色最後出現深度\n**1. 狀態表示：窗口 = 當前 DFS 路徑。** 維護「根到當前點」這條鏈的摘要：路徑長度前綴和、每種顏色最近出現的深度等可增刪的資訊。\n\n```cpp\n// 狀態：dis 是根到各點的距離前綴；lastDepth 記每色最後出現深度\nvector<int> dis;                    // dis[u] = 根到 u 的路徑長\nunordered_map<int, int> lastDepth;  // color -> 該色最近出現的深度\n```\n:::\n\n\n\n:::example 進入 u：納入窗口，必要時把窗口上界往下壓\n**2. 右端擴張：DFS 進入節點。** 進入 `u` 時把它納入窗口，並依條件（如顏色衝突）更新左端界線。\n\n```cpp\n// 進入 u：納入窗口，必要時把窗口上界往下壓\nint saved = lastDepth.count(color[u]) ? lastDepth[color[u]] : -1;\nint top = max(parentTop, saved + 1);  // 新的窗口上界（深度）\nlastDepth[color[u]] = dep[u];         // 記錄本色最新深度\n```\n:::\n\n\n\n:::example 答案：窗口 [top, u] 的長度\n**3. 答案讀取：每個合法窗口都更新。** 走到每個節點時，用「當前深度的前綴和 − 窗口上界的前綴和」得到合法路徑長，更新最長答案。\n\n```cpp\n// 答案：窗口 [top, u] 的長度\nans = max(ans, dis[u] - dis_at_depth(top));\n```\n:::\n\n\n\n:::example 回溯：還原，避免污染兄弟子樹\n**4. 回溯：撤銷本次修改（最關鍵）。** 離開 `u` 前，把 `lastDepth[color[u]]` 還原成進入前的值，否則會影響別的分支。\n\n```cpp\n// 回溯：還原，避免污染兄弟子樹\nlastDepth[color[u]] = saved;  // 還原成進入前的狀態\n```\n:::\n\n\n\n複雜度：每個節點進出窗口各一次，整體 `O(n)`（用雜湊或陣列維護顏色深度）；3486「最長特殊路徑 II」放寬到「最多一個顏色可重複」，本質仍是同一個窗口框架加一點條件。\n\n## C++17 模板\n\n```cpp\n// 最長特殊路徑（LeetCode 3425）：樹上滑動視窗，一次 DFS O(n)。\nclass Solution {\n  std::vector<std::vector<std::pair<int, int>>> g_;   // {鄰居, 邊權}\n  std::vector<int> color_, lastDepth_;                // lastDepth_: 顏色 -> 鏈上最深出現\n  std::vector<long long> dis_;                        // dis_[d]: 根到深度 d 的前綴和\n  long long bestLen_ = 0;\n  int bestNodes_ = 0;\n\n  void dfs(int u, int fa, int d, int top) {           // top 以參數傳遞，不是全域\n    int saved = lastDepth_[color_[u]];                // 記下進入前的值\n    top = std::max(top, saved + 1);                   // 只需管最近一次出現\n    lastDepth_[color_[u]] = d;                        // 更新為當前深度\n\n    long long len = dis_[d] - dis_[top];              // 視窗 [top, d] 的長度\n    int nodes = d - top + 1;\n    if (len > bestLen_ || (len == bestLen_ && nodes < bestNodes_)) {\n      bestLen_ = len;\n      bestNodes_ = nodes;\n    }\n\n    for (auto [v, w] : g_[u]) {\n      if (v == fa) { continue; }\n      dis_[d + 1] = dis_[d] + w;                      // 前綴和往下延伸\n      dfs(v, u, d + 1, top);                          // 兄弟各自從同一個 top 出發\n    }\n\n    lastDepth_[color_[u]] = saved;                    // 回溯：還原，否則污染兄弟\n  }\n\n public:\n  std::vector<int> longestSpecialPath(\n      const std::vector<std::vector<int>>& edges, const std::vector<int>& nums) {\n    int n = nums.size();\n    g_.assign(n, {});\n    for (const auto& e : edges) {\n      g_[e[0]].push_back({e[1], e[2]});\n      g_[e[1]].push_back({e[0], e[2]});\n    }\n    color_ = nums;\n    lastDepth_.assign(*std::max_element(nums.begin(), nums.end()) + 1, -1);\n    dis_.assign(n + 1, 0);\n    bestNodes_ = 1;\n    dfs(0, -1, 0, 0);\n    return {static_cast<int>(bestLen_), bestNodes_};\n  }\n};\n```\n\n## 時間與空間複雜度\n\n每個節點進入、離開各一次，每次做 $O(1)$ 的更新與還原（顏色用陣列索引；若值域大則離散化或用雜湊表，後者是 $O(1)$ 期望）。總計 $O(n)$ 時間。\n\n空間：鄰接表 $O(n)$、`dis` $O(n)$、`lastDepth` $O(值域)$、遞迴堆疊 $O(h)$。\n\n相對於「枚舉每對祖先-後代」的 $O(n^2)$，收穫全部來自不變量三的單調性——下界不回頭，所以不需要對每個上端點重算。\n\n3486（最多允許一種顏色重複）把 `top` 換成「維護最近兩次出現」的版本，複雜度不變，仍是 $O(n)$。\n\n## 常見錯誤與邊界條件\n\n- 回溯時忘記還原 `lastDepth`（最常見，兄弟子樹被污染，答案偏小）。\n- 把 `top` 寫成全域變數並在回溯時未還原（同樣污染，但更隱蔽，因為單條鏈上測試會過）。\n- 還原時寫成 `lastDepth[color[u]] = -1` 而非進入前的 `saved`（祖先鏈上同色的資訊被抹掉）。\n- `dis` 陣列用 `d` 索引但忘記在遞迴前寫入 `dis[d+1]`。\n- 視窗長度算成 `dis[d] - dis[top-1]`（下界的定義是「合法端點所在深度」，前綴和要取 `dis[top]`）。\n- 節點數與權重和混淆（3425 要求權重和最大、節點數最少，兩個都要更新）。\n- 顏色值域大時未離散化導致 `lastDepth` 開不下。\n- 單點路徑是否合法要看題目定義。\n\n## 與相似技巧的比較\n\n- 與陣列滑動視窗：條件判定、`left = max(left, last[c] + 1)` 的更新式完全一樣。唯一的差別是**視窗沿著 DFS 的鏈移動，因此需要回溯還原**——陣列版沒有分岔，不需要。\n- 與 2.7 的回溯：還原的機制相同（進入時記錄、離開時復原），本節可視為回溯技巧與滑動視窗的合成。\n- 與 3.2 的自頂向下 DFS：`top` 走的是自頂向下的傳遞路線（路徑資訊），`lastDepth` 則是需要撤銷的全域狀態。3.2 提到的「純量傳值不用撤銷、容器全域要撤銷」在這裡體現得最清楚。\n- 與 3.11 的 DSU on tree：後者也維護一個全域計數器並要求清乾淨，但它處理的是**子樹**統計，本節處理的是**鏈**上的視窗，適用場景不重疊。\n- 本節只能處理「由上往下」的路徑。若路徑可以在某節點分叉（經過 LCA 的一般路徑），視窗性質不成立，要改用 2.6／3.5 的枚舉最高點，或 3.12 的點分治。\n\n## 代表例題\n\n**題號**：3425、3486\n\n放在「樹上滑動視窗」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 3425 | [最長特殊路徑](https://leetcode.cn/problems/longest-special-path/) | 2435 |\n| 3486 | [最長特殊路徑 II](https://leetcode.cn/problems/longest-special-path-ii/) | 2925 |\n\n## 本節重點速查\n\n- 視窗 = 根到當前節點的鏈。\n- `top = max(parentTop, lastDepth[color[u]] + 1)`，**只需看最近一次出現**。\n- 三個不變量：`lastDepth` 描述當前鏈、`top` 是最小合法下界、`top` 沿鏈單調不減（$O(n)$ 的來源）。\n- **`top` 傳參、`lastDepth` 全域且回溯必須還原**。\n- 只適用「由上往下」的路徑，會分叉的路徑要改用枚舉最高點或點分治。\n",
        },
        {
          id: 41,
          title: "3.11 樹上啟發式合併",
          summary:
            "## 這個技術解決什麼問題\n\n一類題目要對**每一棵子樹**各給一個答案，而答案由子樹內元素的統計量決定（有幾種顏色？眾數是誰？缺失的最小值是多少？）。對每個節點各掃一次它的子樹是 $O(n^2)$。\n\n樹上啟發式合併（DSU on tree，small-to-large）的槓桿是：**保留最大子樹的統計結果，只把較小的子樹搬進來。** 搬動總量從 $O(n^2)$ 降到 $O(n \\log n)$，而且不需要任何高深的資料結構——只要一個全域計數器與 add／remove 兩個函式。\n\n## 辨識題型的訊號\n\n看到這些特徵，往樹上啟發式合併想：\n\n- **要對「每棵子樹」各回答一個問題**（每個節點一個答案），而且答案可由子樹內元素的統計（顏色數、眾數、是否出現某值…）得到。\n- **這個統計可以用 add / remove 單點增量維護**，加入一個點、移除一個點都好寫。\n- **暴力是對每個節點重掃整棵子樹**（`O(n^2)`）；本節用「保留重兒子」把它降到 `O(n log n)`。\n- 題目沒有「修改/刪除歷史」需求，只是純粹的子樹彙總（如 2003 / 3575 / 3590）。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要的答案是不是「每棵子樹各一個」，且能用單點 add/remove 維護？\n2. 我有沒有正確標出重兒子，並只保留它、清空輕兒子？\n3. 清空時我有沒有把不保留的子樹清得乾乾淨淨，避免污染兄弟？\n\n三題都答得出來，就套對了。\n\n## 核心想法與直覺\n\n本節要解決一類「對每棵子樹各問一個問題」的題目，例如：每棵子樹裡出現了哪些顏色？子樹內某個統計量是多少？最直覺的做法是對每個節點都重新掃一遍它的整棵子樹，但這樣會 `O(n^2)`，n 一大就爆。\n\n「樹上啟發式合併（DSU on tree，又叫 small-to-large）」是一個聰明的省力技巧。核心觀察是：一個節點的答案來自它所有孩子的子樹資訊合併。合併時，與其把所有孩子的資訊都拷貝一份，不如**保留最大的那個孩子的資訊（重兒子），只把其他較小孩子的資訊逐一搬進來**。\n\n:::example 重兒子：子樹最大的那個孩子，它的資訊保留不搬\n為什麼這樣會快？因為「啟發式合併」有個著名結論：每個元素被搬動的次數最多 `O(log n)` 次。直覺是：一個元素只有在「它所在的集合被併進一個更大的集合」時才被搬，而集合每被搬一次大小至少翻倍，翻倍最多 `log n` 次。所以總搬動量是 `O(n log n)`，遠優於暴力。\n\n```cpp\n// 重兒子：子樹最大的那個孩子，它的資訊保留不搬\nvoid dfsSize(int u, int fa) {\n    sz[u] = 1;\n    int mx = 0;\n    for (int v : g[u]) {\n        if (v != fa) {\n            dfsSize(v, u);\n            sz[u] += sz[v];\n            if (sz[v] > mx) {\n                mx = sz[v];\n                big[u] = v;\n            }  // big[u] = 重兒子\n        }\n    }\n}\n```\n:::\n\n\n\n:::example small-to-large 的精神：保留重兒子，輕兒子重新加入\n整個流程像這樣：先遞迴算完所有「輕兒子」（較小的孩子）並清掉它們留下的資訊，最後算重兒子並**保留**它的資訊，再把輕兒子的子樹一個個重新加回來，湊出當前節點的完整答案。\n\n```cpp\n// small-to-large 的精神：保留重兒子，輕兒子重新加入\nadd(u);  // 把當前節點自己算進來\nfor (輕兒子 v) {\n    addSubtree(v);  // 把每個輕兒子整棵加回 cnt\n}\nans[u] = 查詢當前統計();  // 此刻 cnt 就是 u 子樹的完整資訊\n```\n:::\n\n\n\n像 2003「每棵子樹內缺失的最小基因值」、3575「最大好子樹分數」，都是「每棵子樹要一個答案、且子樹資訊可增量維護」的典型。\n\n## 狀態／資料結構定義\n\n- `sz[u]`：$u$ 的子樹大小；`big[u]`：$u$ 的**重兒子**，即子樹最大的那個子節點（無子節點時記 $-1$）。由第一次 DFS 求出。\n- 全域統計結構，例如 `cnt[value]` 加上若干輔助量（不同值的個數、當前眾數等）。它描述的是**「目前已加入集合」的元素多重集**，而不是任何特定子樹——這點要想清楚，否則會弄不懂何時該清空。\n- `add(u)` / `remove(u)`：把單一節點的貢獻加入／移出全域統計。\n- `addSubtree(v)` / `removeSubtree(v)`：對子樹 $v$ 的每個節點呼叫 `add` / `remove`。\n- `query()`：從全域統計讀出答案。\n- 主遞迴 `dfs(u, fa, keep)`：`keep` 表示「回傳時是否保留 $u$ 子樹的貢獻在全域統計中」。\n\n**輕邊**指 $u \\to v$ 且 $v \\ne \\text{big}[u]$；**重邊**指 $u \\to \\text{big}[u]$。\n\n## 不變量或正確性證明\n\n**不變量（呼叫契約）：`dfs(u, fa, keep)` 被呼叫時，全域統計為空（不含 $u$ 子樹外的任何節點，也不含 $u$ 子樹內的節點）；回傳時，若 `keep == true` 則統計恰為 $u$ 的整棵子樹，若 `keep == false` 則統計恢復為空。**\n\n先確認遞迴體維持了這個契約：\n\n1. 對每個輕兒子 $v$ 呼叫 `dfs(v, u, false)`。由歸納假設，每次呼叫前後統計都是空的。\n2. 對重兒子呼叫 `dfs(big[u], u, true)`。回傳後統計恰為重兒子的整棵子樹。\n3. `add(u)`，再對每個輕兒子 $v$ 執行 `addSubtree(v)`。此時統計 = 重兒子子樹 ∪ $\\{u\\}$ ∪ 所有輕兒子子樹 = **$u$ 的整棵子樹**，不多不少（子樹的劃分不重不漏）。\n4. `ans[u] = query()`。契約中「統計恰為 $u$ 子樹」正是 `query()` 能給出正確答案的前提。\n5. 若 `keep == false`，把整棵 $u$ 子樹移除，統計歸空。\n\n歸納成立。**注意步驟 1 的「呼叫前統計為空」依賴步驟 2 尚未執行**——這就是為什麼**輕兒子必須先遞迴、重兒子必須最後遞迴**。若順序反過來，遞迴輕兒子時全域統計裡還留著重兒子的資料，`query()` 會讀到別人的東西。這不是效率問題，是正確性問題。\n\n**複雜度的攤還論證。** 成本集中在 `addSubtree` / `removeSubtree`，也就是「節點 $x$ 被逐點搬動」的總次數。\n\n節點 $x$ 在處理祖先 $u$ 時被搬動，當且僅當 $x$ 位於 $u$ 的某個**輕兒子**的子樹中。也就是說：\n\n$$x \\text{ 被搬動的次數} = \\text{根到 } x \\text{ 的路徑上輕邊的條數}.$$\n\n現在證明這個數不超過 $\\log_2 n$。設 $p \\to v$ 是一條輕邊，$h = \\text{big}[p]$ 是重兒子。由重兒子的定義 $\\text{sz}[h] \\ge \\text{sz}[v]$，於是\n$$\\text{sz}[p] \\ge 1 + \\text{sz}[h] + \\text{sz}[v] \\ge 1 + 2\\,\\text{sz}[v] > 2\\,\\text{sz}[v].$$\n即**每往上跨過一條輕邊，子樹大小至少翻倍**。從 $x$（大小 $\\ge 1$）往上到根（大小 $n$），翻倍最多 $\\log_2 n$ 次，故輕邊不超過 $\\log_2 n$ 條。\n\n因此總搬動次數 $\\le n \\log_2 n$，每次 $O(1)$（假設 add/remove 是 $O(1)$），總計 $O(n \\log n)$。\n\n**這個論證點名的前提有兩個，換題時要各自檢查：**\n\n1. **`add` 與 `remove` 必須是 $O(1)$（或至少常數小）。** 若 `add` 內部帶了一個 $O(\\log n)$ 的資料結構操作，總複雜度變成 $O(n \\log^2 n)$——通常仍可接受，但要算清楚。\n2. **統計量必須可逆（能 `remove`）。** 「有幾種不同顏色」可逆（計數歸零時遞減種類數）；「歷史上出現過的最大值」不可逆。不可逆的統計量無法用本節的框架，得改用別的方法（例如 2003 的「缺失最小基因值」就不走通用 DSU on tree，而是用「只有一條鏈上的節點答案非 1」這條性質特判）。\n\n**為何 `keep` 這個參數不能省。** 它的作用是把「清空成本」推給呼叫者中真正需要清空的那一方。重兒子回傳時不清（省下的正是這 $O(n \\log n)$ 與 $O(n^2)$ 的差距），輕兒子回傳時清（維持契約）。把 `keep` 恆設為 `true` 會導致統計被污染；恆設為 `false` 則退化成暴力 $O(n^2)$。\n\n## 逐步演算法\n\n固定模板，分幾步：\n\n**1. 第一次 DFS：算子樹大小、標記重兒子。**（見上方 `dfsSize`。）重兒子是子樹最大的孩子，它的貢獻將被保留。\n\n:::example add / remove：把單一節點的貢獻加入或移出全域統計\n**2. 設計 add / remove：單點加入、移除對統計的影響。** 用一個全域計數結構（如 `cnt[]`）記錄當前「已加入集合」的狀態。\n\n```cpp\n// add / remove：把單一節點的貢獻加入或移出全域統計\nint cnt[MAXV];\nvoid add(int u) { cnt[val[u]]++; /* 視題目更新答案輔助量 */ }\nvoid remove(int u) { cnt[val[u]]--; }\n```\n:::\n\n\n\n:::example 主流程：輕兒子算完即清空，重兒子保留，再補回輕兒子\n**3. 主 DFS：先輕後重，重兒子資訊保留。** 先遞迴輕兒子並用 `remove` 把它們清乾淨（`keep = false`）；再遞迴重兒子且保留（`keep = true`）；接著把自己和所有輕兒子子樹重新 `add` 回來。\n\n```cpp\n// 主流程：輕兒子算完即清空，重兒子保留，再補回輕兒子\nvoid dfs(int u, int fa, bool keep) {\n    for (int v : g[u]) {\n        if (v != fa && v != big[u]) {\n            dfs(v, u, false);  // 輕：不留\n        }\n    }\n    if (big[u]) {\n        dfs(big[u], u, true);  // 重：保留\n    }\n    add(u);\n    for (int v : g[u]) {\n        if (v != fa && v != big[u]) {\n            addSubtree(v);  // 補回輕兒子\n        }\n    }\n    ans[u] = query();  // u 的答案\n    if (!keep) {\n        for (int v : g[u]) {\n            if (v != fa) {\n                removeSubtree(v), remove(u);\n            }\n        }\n    }\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n- `addSubtree` 要遍歷整棵子樹逐點 `add`，不能用快取（那是重兒子才有的待遇）。\n- 清空只清「不保留」的子樹，且務必清乾淨，否則兄弟之間會互相污染。\n- 複雜度 `O(n log n)`：每個點被 add/remove 最多 `O(log n)` 次。\n\n## C++17 模板\n\n```cpp\n// DSU on tree：對每棵子樹各求一個答案，O(n log n)。\nclass DsuOnTree {\n  std::vector<std::vector<int>> g_;\n  std::vector<int> sz_, big_, val_, ans_;\n  std::vector<int> cnt_;          // 全域統計：目前已加入集合的元素分佈\n  int distinct_ = 0;              // 輔助量：不同值的個數\n\n  void dfsSize(int u, int fa) {\n    sz_[u] = 1;\n    big_[u] = -1;\n    for (int v : g_[u]) {\n      if (v == fa) { continue; }\n      dfsSize(v, u);\n      sz_[u] += sz_[v];\n      if (big_[u] == -1 || sz_[v] > sz_[big_[u]]) { big_[u] = v; }\n    }\n  }\n\n  void add(int u) { if (cnt_[val_[u]]++ == 0) { ++distinct_; } }\n  void remove(int u) { if (--cnt_[val_[u]] == 0) { --distinct_; } }\n\n  void addSubtree(int u, int fa) {           // 輕兒子只能逐點搬，沒有快取待遇\n    add(u);\n    for (int v : g_[u]) {\n      if (v != fa) { addSubtree(v, u); }\n    }\n  }\n\n  void removeSubtree(int u, int fa) {\n    remove(u);\n    for (int v : g_[u]) {\n      if (v != fa) { removeSubtree(v, u); }\n    }\n  }\n\n  void dfs(int u, int fa, bool keep) {\n    for (int v : g_[u]) {                    // 1. 輕兒子先做，做完清空\n      if (v != fa && v != big_[u]) { dfs(v, u, false); }\n    }\n    if (big_[u] != -1) { dfs(big_[u], u, true); }   // 2. 重兒子最後做，保留\n\n    add(u);                                  // 3. 補上自己\n    for (int v : g_[u]) {                    //    與所有輕兒子子樹\n      if (v != fa && v != big_[u]) { addSubtree(v, u); }\n    }\n\n    ans_[u] = distinct_;                     // 4. 此刻統計恰為 u 的整棵子樹\n\n    if (!keep) { removeSubtree(u, fa); }     // 5. 契約：不保留就清乾淨\n  }\n\n public:\n  std::vector<int> solve(int n, const std::vector<std::vector<int>>& edges,\n                         const std::vector<int>& val, int maxVal) {\n    g_.assign(n, {});\n    for (const auto& e : edges) {\n      g_[e[0]].push_back(e[1]);\n      g_[e[1]].push_back(e[0]);\n    }\n    val_ = val;\n    sz_.assign(n, 0);\n    big_.assign(n, -1);\n    ans_.assign(n, 0);\n    cnt_.assign(maxVal + 1, 0);\n    dfsSize(0, -1);\n    dfs(0, -1, true);\n    return ans_;\n  }\n};\n```\n\n## 時間與空間複雜度\n\n第一次 DFS：$O(n)$。\n\n主遞迴：每個節點被 `add`/`remove` 的次數 = 其到根路徑上的輕邊數 $+\\,O(1)$，總計 $O(n \\log n)$ 次，每次 $O(1)$，故 $O(n \\log n)$。\n\n空間：鄰接表與各陣列 $O(n)$，全域計數器 $O(值域)$，遞迴堆疊 $O(h)$。\n\n與其他做法比較：用 3.7 的 dfn 把子樹轉成區間、再離線用莫隊是 $O(n\\sqrt n)$，通常更慢；用可合併的資料結構（線段樹合併）也是 $O(n \\log n)$，但空間常數大、實作長。**DSU on tree 的優勢是實作短、常數小，代價是統計量必須可逆。**\n\n## 常見錯誤與邊界條件\n\n- 重兒子先遞迴、輕兒子後遞迴（違反契約，`query()` 讀到重兒子殘留的資料）。\n- `keep == false` 時清空不徹底（只 `remove(u)` 而沒清子樹，或漏掉重兒子），導致兄弟之間互相污染。\n- 把 `addSubtree` 用在重兒子上（複雜度退化成 $O(n^2)$，答案仍對，只是慢）。\n- `big[u]` 在葉節點時未初始化為 $-1$，被當成節點 0。\n- 統計量不可逆卻硬套（`remove` 寫不出正確的反向操作）。\n- `cnt` 陣列開的大小小於值域。\n- 根呼叫時 `keep` 給 `false`（會白白多清一次，不影響正確性但浪費）。\n- 深度大時遞迴爆堆疊。\n- $n = 1$。\n\n## 與相似技巧的比較\n\n- 與 3.3 的自底向上 DFS：3.3 要求合併是 $O(1)$ 的（回傳一個純量）。**當合併需要整個集合（而非摘要）時就落到本節**——3.3 那節提過的「合併不是 $O(1)$ 時複雜度必須重算」，答案就在這裡。\n- 與線段樹合併：兩者都是 $O(n \\log n)$ 解同一類問題。線段樹合併支援「查詢子樹內第 $k$ 大」這類需要有序結構的問題，且統計量不必可逆；DSU on tree 實作短得多。先試 DSU on tree。\n- 與 3.9 的虛樹：虛樹適用「只有少數點被關心」，DSU on tree 適用「每個點都要答案」。判準是**答案的個數**。\n- 與一般的 small-to-large（例如用 `set` 啟發式合併，每次 `swap` 大小容器）：那是同一條攤還論證的另一種實作，複雜度也是 $O(n \\log n)$（或帶 `set` 的 $O(n \\log^2 n)$）。DSU on tree 用「重兒子不搬」取代「容器 swap」，省下了容器本身的開銷。\n- 3575、3590 這類題的難點通常不在框架，而在設計出一個**可逆且 $O(1)$** 的統計量。\n\n## 代表例題\n\n**題號**：2003、3575、3590\n\n放在「樹上啟發式合併」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 2003 | [每棵子樹內缺失的最小基因值](https://leetcode.cn/problems/smallest-missing-genetic-value-in-each-subtree/) | 2415 |\n| 3575 | [最大好子樹分數](https://leetcode.cn/problems/maximum-good-subtree-score/) | 2360 |\n| 3590 | [第 K 小的路徑異或和](https://leetcode.cn/problems/kth-smallest-path-xor-sum/) | 2646 |\n\n## 本節重點速查\n\n- **輕兒子先做並清空、重兒子最後做並保留**，然後 `add(u)` 加所有輕子樹，此刻全域統計恰為 $u$ 的子樹。\n- 攤還的核心：節點被搬動的次數 = 到根路徑上的輕邊數，而**每跨一條輕邊子樹至少翻倍**，故不超過 $\\log_2 n$，總計 $O(n \\log n)$。\n- 兩個前提：`add`/`remove` 要 $O(1)$，統計量必須**可逆**。\n- `keep` 參數不能省。\n",
        },
        {
          id: 42,
          title: "3.12 點分治",
          summary:
            "## 這個技術解決什麼問題\n\n一類題目要統計**所有點對之間的路徑**是否滿足某條件：長度恰為 $K$、權重和在某範圍、異或為 0……路徑共有 $\\binom{n}{2}$ 條，逐條檢查是 $O(n^2)$。\n\n點分治的槓桿是一個劃分：**任一條路徑，要嘛經過選定的中心點 $c$，要嘛完全落在移除 $c$ 後的某個連通塊內。** 前者可以用「兩段半路徑配對」在 $O(\\text{塊大小})$ 或 $O(\\text{塊大小} \\log)$ 內一次算完，後者遞迴處理。若每次都選**重心**當中心，遞迴深度只有 $O(\\log n)$，總計 $O(n \\log n)$ 到 $O(n \\log^2 n)$。\n\n## 辨識題型的訊號\n\n看到這些特徵，往點分治想：\n\n- **要統計「樹上所有點對之間的路徑」是否滿足某條件**（路徑長 = K、和、異或、距離分佈…），且路徑可任意起終點。\n- **路徑可以自然拆成「經過某中心點的兩段半路徑」來配對計算**。\n- **暴力是枚舉所有點對**（`O(n^2)`），n 上萬就爆；點分治用重心把它降到 `O(n log n)` 級。\n- 題目強調「距離」「路徑數量」且和具體子樹結構無關，只關乎點到點關係（如 3372 的目標節點數隨距離奇偶變化）。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要統計的東西是不是「跨任意兩點的路徑」，能拆成經過中心的兩段？\n2. 我每層是不是都選重心當中心，確保分治只有 `O(log n)` 層？\n3. 統計時我有沒有用容斥扣掉「同子樹內部、其實沒經過重心」的配對？\n\n三題都答得出來，就套對了。\n\n## 核心想法與直覺\n\n本節要解決的是「跟樹上路徑有關的統計」題，例如：有多少條路徑長度等於 K？有多少條路徑滿足某個條件？路徑可以從任何點到任何點，所以總共有 `O(n^2)` 條，硬枚舉太慢。「點分治（centroid decomposition，重心分治）」用分治的思想把它壓下來。\n\n關鍵觀察：任何一條路徑，要嘛**經過某個我們選定的中心點**，要嘛**完全落在它移除後分裂出的某棵子樹裡**。於是我們可以：先把「經過中心點的路徑」全部算掉，再對每個分裂出的子樹遞迴做同樣的事。這樣不重不漏地涵蓋所有路徑。\n\n:::example 找重心：移除某點後，最大塊也不超過總數一半的那個點\n但分治要快，每次必須把樹砍得夠平均，否則退化成一條鏈會變 `O(n^2)`。解法是每次選「重心（centroid）」當中心：重心是「移除它後，剩下最大子樹的大小最小」的那個點。數學上保證最大子樹不超過原樹的一半，所以分治層數只有 `O(log n)`。\n\n```cpp\n// 找重心：移除某點後，最大塊也不超過總數一半的那個點\nvoid findCentroid(int u, int fa, int total) {\n    int mx = total - sz[u];  // 往父方向那一塊的大小\n    for (int v : g[u]) {\n        if (v != fa && !done[v]) {\n            mx = max(mx, sz[v]);  // 各子樹塊\n        }\n    }\n    if (mx <= total / 2) {\n        centroid = u;  // 最大塊 ≤ 一半 → 它就是重心\n    }\n}\n```\n:::\n\n\n\n:::example 經過重心的路徑 = 兩條從重心出發的半路徑拼起來\n每一層分治，我們從重心出發 DFS，量出每個點到重心的距離（或其他要統計的量），再用這些「半路徑」湊出「經過重心的整條路徑」的數量。\n\n```cpp\n// 經過重心的路徑 = 兩條從重心出發的半路徑拼起來\n// 例：統計長度為 K 的路徑，就找 dist[a] + dist[b] == K 的配對\n```\n:::\n\n\n\n像 3372「連線兩棵樹後最大目標節點數目」這類，需要對「每個點考慮它到其他點的距離分佈」的題目，點分治能把它組織成 `O(n log n)` 的分治流程。\n\n## 狀態／資料結構定義\n\n- `sz[u]`：當前連通塊中以 $u$ 為根的子樹大小（每層重算，因為塊在變）。\n- `done[u]`：$u$ 是否已被選為某層的重心並「封住」。所有遍歷都必須跳過 `done` 的點——這是「當前連通塊」的唯一定義方式。\n- **重心**：使「移除它後最大連通塊的大小」最小的點。等價刻畫：所有塊的大小都 $\\le \\lfloor N/2 \\rfloor$（$N$ 為當前塊的節點數）。\n- 每層從重心 $c$ 出發 DFS，收集每個點的「半路徑量」`dist[x]`（到 $c$ 的距離／權重和／異或值）。\n- 統計函式 `calc(起點, 初始量)`：把從某點出發收集到的半路徑兩兩配對，回傳滿足條件的配對數。\n\n## 不變量或正確性證明\n\n**引理一（重心存在，且最大塊 $\\le \\lfloor N/2 \\rfloor$）。**\n\n設 $c$ 是使 $f(u) = \\max_{\\text{塊}} |\\text{塊}|$ 最小的點。反證：若 $f(c) > N/2$，設達到最大的塊是經由鄰居 $v$ 的那一塊 $B$，$|B| > N/2$。考慮改選 $v$：移除 $v$ 後，「往 $c$ 方向」的那一塊大小是 $N - |B| < N/2$；其餘各塊都是 $B$ 的真子集去掉 $v$ 後的部分，大小 $\\le |B| - 1$。故 $f(v) \\le \\max(N - |B|,\\ |B| - 1) < f(c)$（因為 $|B| > N/2 > N - |B|$，且 $|B| - 1 < |B| = f(c)$）。與 $c$ 的最小性矛盾。故 $f(c) \\le \\lfloor N/2 \\rfloor$。$\\square$\n\n**引理二（遞迴深度 $O(\\log n)$）。** 由引理一，每往下遞迴一層，連通塊大小至少減半。從 $n$ 減半到 1 最多 $\\log_2 n$ 層。\n\n進一步，**每一層所有連通塊的節點總數 $\\le n$**（它們互不相交），所以若每層對每塊做 $O(\\text{塊大小})$ 的工作，每層總計 $O(n)$，全部 $O(n \\log n)$。**這個「每層總和有界」的觀察，而不是「每塊很小」，才是複雜度的真正來源。**\n\n**引理三（不重不漏）：每條路徑恰好在一層、被恰好一個重心統計一次。**\n\n設路徑 $P = (a, b)$。在最上層，$a, b$ 都在同一個連通塊中（整棵樹）。歸納地假設在某層 $a, b$ 同屬當前塊 $T$，塊的重心為 $c$：\n\n- 若 $c$ 在 $P$ 上（含 $c = a$ 或 $c = b$），這一層就統計到它。\n- 若 $c$ 不在 $P$ 上，則 $P$ 上所有點都不是 $c$，故移除 $c$ 後 $a$ 與 $b$ 仍由 $P$ 連通，落在**同一個**新塊中，遞迴時繼續。\n\n因為塊嚴格變小，遞迴必定終止，而終止時塊只剩一個點，此時 $a = b = c$，路徑退化。故每條路徑在「第一次遇到 $c \\in P$」的那一層被統計，且該層只有一個重心，統計恰一次。$\\square$\n\n這條引理是點分治不重不漏的全部依據。它只用到「移除不在路徑上的點不會斷開該路徑」——樹上路徑唯一，這在圖上不成立，所以點分治是樹專屬技巧。\n\n**容斥的正確性。** 從 $c$ 出發 DFS 得到所有點的 `dist`（含 $c$ 自己，`dist = 0`）。把這些值兩兩配對得到的「$\\text{dist}[x] + \\text{dist}[y]$ 滿足條件」的對數，包含了兩類：\n\n- $x, y$ 在 $c$ 的不同子樹（或其一為 $c$）：$x \\to c \\to y$ 確實是一條簡單路徑，長度就是 $\\text{dist}[x] + \\text{dist}[y]$。**應該算**。\n- $x, y$ 在 $c$ 的**同一棵**子樹：$x \\to c \\to y$ 不是簡單路徑（會重複走 $c$ 到分岔點那一段），真實距離比 $\\text{dist}[x] + \\text{dist}[y]$ 小。**不該算**。\n\n因此扣掉每棵子樹內部的配對數即可。實作上是對每個子節點 $v$，以 $v$ 為起點、初始量為邊權 $w(c,v)$ 再算一次 `calc`——這樣算出的正是「同一棵子樹內、且用 $\\text{dist}$ 相加的錯誤配對數」，減去它就對了。\n\n**注意容斥的初始量必須是 $w(c,v)$ 而不是 0**，否則扣掉的量與加進去的量不是同一批（前者用的是到 $v$ 的距離，後者用的是到 $c$ 的距離），會多扣或少扣。\n\n## 逐步演算法\n\n點分治的骨架很固定，分幾步：\n\n:::example 算大小（已被標記 done 的重心不再進入）\n**1. 算子樹大小、找重心。** 每進入一塊（連通分量），先 DFS 算大小，再找重心當這層的中心。\n\n```cpp\n// 算大小（已被標記 done 的重心不再進入）\nvoid getSize(int u, int fa) {\n    sz[u] = 1;\n    for (int v : g[u]) {\n        if (v != fa && !done[v]) {\n            getSize(v, u);\n            sz[u] += sz[v];\n        }\n    }\n}\n```\n:::\n\n\n\n:::example 容斥：全部配對 − 同子樹內部配對 = 真正經過重心的路徑\n**2. 統計「經過重心」的路徑。** 從重心出發收集每個點的距離，常用「容斥」：先算所有半路徑兩兩配對，再扣掉「同一子樹內部的配對」（那些其實沒真的經過重心，會繞回去）。\n\n```cpp\n// 容斥：全部配對 − 同子樹內部配對 = 真正經過重心的路徑\nlong long ans = calc(centroid, 0);  // 以重心為起點，全部\nfor (int v : g[centroid]) {\n    if (!done[v]) {\n        ans -= calc(v, edgeLen(centroid, v));  // 扣掉同一子樹內部的\n    }\n}\n```\n:::\n\n\n\n:::example 分治下去：重心用過就封住，遞迴各子樹\n**3. 標記重心、遞迴每個子樹。** 把重心標成 `done`（之後不再跨過它），對它分裂出的每塊遞迴重複步驟 1–3。\n\n```cpp\n// 分治下去：重心用過就封住，遞迴各子樹\nvoid solve(int u) {\n    getSize(u, 0);\n    centroid = -1;\n    findCentroid(u, 0, sz[u]);\n    int c = centroid;\n    done[c] = true;\n    countThrough(c);  // 統計經過 c 的路徑\n    for (int v : g[c]) {\n        if (!done[v]) {\n            solve(v);  // 對每塊遞迴\n        }\n    }\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n- 收集距離時務必用 `done` 擋住已封住的重心，否則會跨塊重複計算。\n- 容斥那步若漏扣「同子樹內部」，會把不經過重心的路徑算進去。\n- 複雜度：分治 `O(log n)` 層，每層所有塊合計 `O(n)`（或加上排序的 `O(n log n)`），總計 `O(n log n)` 到 `O(n log^2 n)`。\n\n## C++17 模板\n\n```cpp\n// 點分治骨架：統計滿足條件的路徑數，O(n log n) ~ O(n log^2 n)。\nclass CentroidDecomposition {\n  std::vector<std::vector<std::pair<int, int>>> g_;   // {鄰居, 邊權}\n  std::vector<int> sz_;\n  std::vector<bool> done_;                            // 已被選為重心 → 封住\n  std::vector<long long> buf_;                        // 收集半路徑量\n  long long ans_ = 0;\n  long long kLimit_ = 0;                              // 題目給的門檻，視題目而定\n\n  void getSize(int u, int fa) {                       // done_ 的點視為不存在\n    sz_[u] = 1;\n    for (auto [v, w] : g_[u]) {\n      if (v == fa || done_[v]) { continue; }\n      getSize(v, u);\n      sz_[u] += sz_[v];\n    }\n  }\n\n  int findCentroid(int u, int fa, int total) {\n    for (auto [v, w] : g_[u]) {\n      if (v == fa || done_[v]) { continue; }\n      if (sz_[v] > total / 2) { return findCentroid(v, u, total); }  // 往大塊走\n    }\n    return u;                                         // 所有塊都 <= total/2\n  }\n\n  void collect(int u, int fa, long long d) {\n    buf_.push_back(d);\n    for (auto [v, w] : g_[u]) {\n      if (v == fa || done_[v]) { continue; }\n      collect(v, u, d + w);\n    }\n  }\n\n  long long calc(int u, int fa, long long init) {     // 兩兩配對，視題目實作\n    buf_.clear();\n    collect(u, fa, init);\n    std::sort(buf_.begin(), buf_.end());\n    long long res = 0;\n    // 例：統計 buf_[i] + buf_[j] <= K 的對數（雙指標）\n    for (int i = 0, j = buf_.size() - 1; i < j;) {\n      if (buf_[i] + buf_[j] <= kLimit_) { res += j - i; ++i; }\n      else { --j; }\n    }\n    return res;\n  }\n\n  void solve(int entry) {\n    getSize(entry, -1);\n    int c = findCentroid(entry, -1, sz_[entry]);\n    done_[c] = true;                                  // 先封住，之後的遍歷會自動繞開\n\n    ans_ += calc(c, -1, 0);                           // 所有半路徑兩兩配對\n    for (auto [v, w] : g_[c]) {\n      if (done_[v]) { continue; }\n      ans_ -= calc(v, c, w);                          // 容斥：初始量必須是 w\n    }\n\n    for (auto [v, w] : g_[c]) {\n      if (!done_[v]) { solve(v); }                    // 對每個新塊遞迴\n    }\n  }\n};\n```\n\n## 時間與空間複雜度\n\n層數 $O(\\log n)$（引理二）；每層所有塊的節點總數 $\\le n$。\n\n- 若 `calc` 是 $O(m)$（例如用桶計數統計「距離恰為 $K$」），總計 $O(n \\log n)$。\n- 若 `calc` 需要排序或雙指標前先排序，是 $O(m \\log m)$，總計 $O(n \\log^2 n)$。\n\n`getSize` 與 `findCentroid` 每層合計 $O(n)$，不影響階數。\n\n空間：鄰接表 $O(n)$、`sz`/`done` $O(n)$、`buf` $O(n)$、遞迴堆疊 $O(\\log n)$ 層 $\\times$ 單層 DFS 深度 $O(n)$ 最壞——**堆疊深度仍可能是 $O(n)$**（單層的 DFS 走的是原樹的鏈），鏈狀樹上要留意。\n\n## 常見錯誤與邊界條件\n\n- 遍歷時忘記檢查 `done_`（會跨到已封住的塊，路徑被重複統計）。\n- `done_[c] = true` 設定的時機在容斥之後（此時 `calc` 會走過 $c$，把它算成普通點）。\n- 容斥時初始量給 0 而非邊權 $w(c,v)$（扣錯量）。\n- `findCentroid` 的判斷寫成 `sz_[v] >= total / 2`（邊界差一，可能無限遞迴）。\n- `getSize` 每層都要重算，沿用上一層的值會全錯。\n- 忘記路徑「一端是重心」的情形（`collect` 從 $c$ 出發、`dist = 0` 已涵蓋，但若把 $c$ 排除就會漏）。\n- 單點路徑是否計入要看題目定義。\n- 帶權時用 `int` 累加溢位。\n- 每層 `buf_` 忘記清空。\n\n## 與相似技巧的比較\n\n- 與 2.6／3.5 的「枚舉最高點」：兩者都是「每條路徑有唯一的代表點」這個劃分思路。差別在於枚舉最高點是**固定根之後由樹結構決定代表點**，只能處理「代表點處可 $O(1)$ 合併」的問題（最長鏈相加）；點分治**自己選代表點**並允許在代表點處做 $O(m \\log m)$ 的配對統計，因此能處理「恰為 $K$」「不超過 $K$」這類需要查表配對的條件。**問法是「最長／最大」通常用枚舉最高點，問法是「有幾條」通常用點分治。**\n- 與 3.10 的樹上滑動視窗：後者只處理「由上往下」的鏈，本節處理任意點對的路徑。\n- 與 3.11 的 DSU on tree：DSU on tree 答的是「每棵子樹一個答案」，點分治答的是「全樹的路徑統計總量」。輸出形狀不同。\n- 與 3.9 的虛樹：虛樹是壓縮點集，點分治是分解結構，兩者可以疊加使用（在虛樹上跑點分治），但那已超出本節。\n- 若題目在分治過程中還要支援修改，就要用**動態點分治（點分樹）**，即把每層重心的父子關係建成一棵深度 $O(\\log n)$ 的輔助樹。那是本節的自然延伸。\n\n## 代表例題\n\n**題號**：3372\n\n放在「點分治」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 3372 | [連線兩棵樹後最大目標節點數目 I](https://leetcode.cn/problems/maximize-the-number-of-target-nodes-after-connecting-trees-i/) | 1927 |\n\n## 本節重點速查\n\n- 劃分：路徑要嘛經過重心，要嘛整條落在移除重心後的某一塊——**不重不漏**（引理三，依賴樹上路徑唯一）。\n- 選重心保證每塊 $\\le \\lfloor N/2 \\rfloor$，故只有 $O(\\log n)$ 層，而**每層節點總數 $\\le n$** 才是複雜度的來源。\n- 統計用容斥：全部配對減去同子樹內部配對，且**容斥的初始量必須是邊權 $w(c,v)$**。\n- 所有遍歷都要跳過 `done_`。\n",
        },
        {
          id: 43,
          title: "3.13 其他",
          summary:
            "## 這個技術解決什麼問題\n\n這一節收的是不屬於前面任何一個固定模板的樹題。它們沒有共同的名字，但有共同的**降本手法**：把「每次都要重算的東西」壓進「一次遍歷的回傳值」或「並查集合併的那一瞬間」。\n\n兩種主要形態：\n\n- **子樹摘要型**（2479）：定義一個可向上合併的回傳值，一次後序 DFS 就把所有子樹的資訊算完，避免對每個節點重掃子樹。\n- **合併時計數型**（2421、2867）：把邊或節點**按某個順序**加入並查集，在兩團合併的那一刻，用兩團的大小相乘算出「這一步新增了多少條跨團路徑」。這樣把 $O(n^2)$ 條路徑的枚舉，攤成 $n-1$ 次合併。\n\n## 辨識題型的訊號\n\n這些題沒有統一名字，但有共同氣味：\n\n- **資料是一棵樹，答案跟「子樹彙總」或「路徑/連通性」有關**，但又不完全是某個標準模板。\n- **可以定義一個向上合併的不變量**（子樹摘要），或**可以「排序後逐邊加入並查集、在合併時統計」**（如 2421 / 2867）。\n- **暴力會反覆重算子樹或重掃所有路徑**；本節把重複工作壓進「一次遍歷的回報值」或「並查集的合併瞬間」。\n- 題目要你「數路徑」「判斷能否重構」「在子樹間找極值」，且 n 很大不允許 `O(n^2)`（如 1719 / 2479）。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我能不能用一句話說出「每個節點向上回報什麼」？\n2. 我需要的只是子樹資訊，還是也要祖先資訊（決定要遍歷幾次）？\n3. 若是路徑計數，我能不能「排序後逐邊合併、在合併瞬間統計」？\n\n三題都答得出來，就套對了。\n\n## 核心想法與直覺\n\n本節是「一般樹」的綜合題集，收的是不容易歸到單一名詞、但都圍繞同一個核心心法的題目：**把樹上的資訊用一兩次 DFS/BFS 傳遞，自底向上彙總子樹、自頂向下攜帶祖先資訊，再搭配排序、並查集（DSU）或位元運算把答案湊出來。** 這類題的價值在於訓練你「先想清楚狀態，再決定怎麼遍歷」。\n\n:::example 樹形 DFS：節點回傳子樹摘要（這裡示範回傳子樹大小）\n第一個常用工具是「樹上 DFS 回傳子樹摘要」。每個節點向上回報一個總結值（高度、節點數、最大鏈長、某種 DP 值），父節點合併孩子的回報得到自己的。\n\n```cpp\n// 樹形 DFS：節點回傳子樹摘要（這裡示範回傳子樹大小）\nint dfs(int u, int fa) {\n    int size = 1;\n    for (int v : g[u]) {\n        if (v != fa) {\n            size += dfs(v, u);  // 合併孩子\n        }\n    }\n    return size;  // 向上回報\n}\n```\n:::\n\n\n\n:::example 並查集核心：找代表 + 合併兩團\n第二個常用工具是「並查集 + 按某種順序加邊」。像 2421「好路徑的數目」、2867「統計樹中的合法路徑數目」，技巧是**先排序（按點權或質數性質），再依序把邊加入並查集**，在合併的瞬間統計新產生的合法路徑。並查集是一種「快速判斷兩點是否已連通、並把兩團合併」的結構。\n\n```cpp\n// 並查集核心：找代表 + 合併兩團\nint find(int x) { return fa[x] == x ? x : fa[x] = find(fa[x]); }  // 路徑壓縮\nvoid merge(int a, int b) { fa[find(a)] = find(b); }\n```\n:::\n\n\n\n心法是：很多「其他」樹題看似花俏，骨架都是「定義一個能向上合併、或能在合併時統計的不變量」。例如 2867 把「合法路徑」拆成「路徑上只有端點是質數，或全不是質數」，用篩法 + 並查集分塊統計；1719「重構一棵樹的方案數」則是分析祖先包含關係。先抓住「狀態是什麼、怎麼合併」，再選遍歷或資料結構。\n\n## 狀態／資料結構定義\n\n**合併時計數型**（以 2421 好路徑數目為例，`vals[u]` 是節點值）：\n\n- 並查集 `fa[]`，附帶按大小合併與路徑壓縮。\n- `cntMax[r]`：連通塊 $r$ 中，**值等於該塊最大值**的節點個數。這是本題唯一需要維護的額外量。\n- 邊按 $\\max(\\text{vals}[a], \\text{vals}[b])$ 遞增排序，逐條加入。\n\n**子樹摘要型**：回傳值的型別就是狀態定義。可能是純量（最長鏈、子樹大小）、`pair`（最大與次大）、或整個容器（各顏色計數；此時要注意合併成本，見 3.11）。\n\n## 不變量或正確性證明\n\n**合併時計數型的不變量（2421）：處理完所有「$\\max$ 值 $\\le v$」的邊之後，並查集中每個連通塊恰好是「僅使用值 $\\le v$ 的節點所能連通的極大集合」，且 `cntMax[r]` 是塊 $r$ 中值等於塊內最大值的節點數。**\n\n- 初始化：每個節點自成一塊，`cntMax = 1`，塊內最大值就是自己的值。成立。\n- 保持：加入一條 $\\max = v$ 的邊 $(a,b)$ 時，$a$、$b$ 所在塊的最大值都 $\\le v$。合併後新塊的最大值是 $v$。因此新的 `cntMax` 只該累計「原本最大值就等於 $v$」的那些塊的計數：若某塊最大值 $< v$，它的 `cntMax` 對新塊沒有貢獻（那些節點不是新塊的最大值）。\n\n現在論證答案。**好路徑的定義是：兩端點值相等，且路徑上所有節點的值都不超過端點值。** 設端點值為 $v$。這等價於「兩端點都在『只用值 $\\le v$ 的節點』所形成的同一個連通塊中，且兩端點的值都等於 $v$」——而由不變量，在處理完所有 $\\max \\le v$ 的邊之後，這樣的塊正是並查集中的塊，塊內值為 $v$ 的節點數就是 `cntMax`。\n\n因此，在合併兩塊 $A$、$B$（兩者最大值皆為 $v$）時，新增的好路徑數恰為 $\\text{cntMax}[A] \\times \\text{cntMax}[B]$：一端取自 $A$、一端取自 $B$，跨團配對。**同團內部的配對在更早的合併中已經算過，不會重複**——這是「按值排序後逐步合併」能不重不漏的關鍵。單點路徑（$a = b$）另計 $n$ 條。\n\n**這條論證點名的前提：邊必須按 $\\max(\\text{vals}[a], \\text{vals}[b])$ 排序。** 若按邊權或任意順序加入，塊就不再是「值 $\\le v$ 的極大連通集合」，`cntMax` 的意義崩潰，乘法計數會把「路徑上有更大值」的非法路徑算進去。\n\n**為何乘法計數對「跨團」成立而對「同團」不成立。** 兩團合併後，任取 $x \\in A$、$y \\in B$，$x$–$y$ 的路徑必定經過新加的邊，因此這些路徑在此之前**不存在於任何一個塊內**，都是新的。反之同團內的路徑早已存在。這個「新舊劃分」是不重不漏的，乘法原理才適用。\n\n**子樹摘要型的不變量**就是 3.3 的後序歸納：`dfs(u)` 回傳的是「僅由 $u$ 子樹決定」的摘要。要檢查的兩點與 3.3 相同——摘要是否對父節點**足夠**（完整性），以及合併是否 $O(1)$（否則複雜度要重算，見 3.11）。\n\n## 逐步演算法\n\n雖然題目雜，但解題流程可固定成幾步：\n\n:::example 例：回傳「以 u 為頂、向下的最長鏈」，順便更新全域直徑類答案\n**1. 定狀態：節點要向上回報什麼？** 高度、子樹大小、最長鏈、某顏色計數、DP 值——這決定 DFS 的回傳型別。\n\n```cpp\n// 例：回傳「以 u 為頂、向下的最長鏈」，順便更新全域直徑類答案\nint dfs(int u, int fa) {\n    int best = 0;\n    for (int v : g[u]) {\n        if (v != fa) {\n            best = max(best, dfs(v, u) + 1);\n        }\n    }\n    return best;\n}\n```\n:::\n\n\n\n**2. 選傳遞方向。** 只需子樹資訊就一次 DFS 自底向上；需要祖先/父側資訊就再做一次自頂向下，或用倍增預處理 LCA。\n\n:::example 合併時統計：兩團相連，新增「一端在 A、一端在 B」的路徑數\n**3. 配合排序或並查集做合併統計。** 路徑計數型常「排序後逐邊合併」，在 `merge` 前用兩團的大小相乘統計跨團新路徑。\n\n```cpp\n// 合併時統計：兩團相連，新增「一端在 A、一端在 B」的路徑數\nlong long cntA = size[find(a)], cntB = size[find(b)];\nans += cntA * cntB;  // 跨團配對數\nmerge(a, b);         // 再真正合併\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n- 遞迴可能很深，必要時改成顯式堆疊或 BFS 反向序，避免爆 stack。\n- 並查集記得路徑壓縮 + 按秩/大小合併，單次近似 `O(1)`。\n- 多數此類題整體 `O(n log n)`（含排序），純 DFS 部分 `O(n)`。\n\n## C++17 模板\n\n```cpp\n// 好路徑的數目（LeetCode 2421）：按值排序 + 並查集合併時計數。\nclass Solution {\n  std::vector<int> fa_, cntMax_;\n\n  int find(int x) { return fa_[x] == x ? x : fa_[x] = find(fa_[x]); }\n\n public:\n  int numberOfGoodPaths(const std::vector<int>& vals,\n                        const std::vector<std::vector<int>>& edges) {\n    int n = vals.size();\n    fa_.resize(n);\n    std::iota(fa_.begin(), fa_.end(), 0);\n    cntMax_.assign(n, 1);                    // 每個點自成一塊，塊內最大值即自己\n\n    std::vector<std::vector<int>> es(edges);\n    std::sort(es.begin(), es.end(), [&](const auto& p, const auto& q) {\n      return std::max(vals[p[0]], vals[p[1]]) < std::max(vals[q[0]], vals[q[1]]);\n    });                                      // 必須按 max(端點值) 遞增\n\n    long long ans = n;                       // 單點路徑\n    for (const auto& e : es) {\n      int x = find(e[0]), y = find(e[1]);\n      int vx = vals[x], vy = vals[y];        // 路徑壓縮後代表元不一定是最大值節點，\n      // 實作上通常把「塊內最大值」也存在代表元上；此處用 vals[代表元] 需保證\n      // 合併時把代表元設為最大值較大的那一方。\n      if (vx == vy) {\n        ans += 1LL * cntMax_[x] * cntMax_[y];   // 跨團配對\n        fa_[y] = x;\n        cntMax_[x] += cntMax_[y];\n      } else if (vx > vy) {\n        fa_[y] = x;                          // 較小的塊最大值被吞併，cntMax_ 不變\n      } else {\n        fa_[x] = y;\n      }\n    }\n    return static_cast<int>(ans);\n  }\n};\n```\n\n上面的寫法讓「代表元」始終是塊內值最大的節點之一，因此 `vals[find(u)]` 就是塊內最大值。這個約定必須貫徹：合併時一定把最大值較小的那一方掛到較大的那一方底下。\n\n## 時間與空間複雜度\n\n**2421 型**：排序 $O(n \\log n)$ 主導；$n-1$ 次並查集操作在路徑壓縮下是 $O(n\\,\\alpha(n))$，可視為近線性。總計 $O(n \\log n)$，空間 $O(n)$。\n\n相對於「枚舉所有點對再驗證路徑」的 $O(n^3)$、或「對每個值各跑一次連通性」的 $O(n^2)$，收穫來自把「所有值的門檻」用一次排序串成單調過程，每條邊只被處理一次。\n\n**子樹摘要型**：一次 DFS $O(n)$；若回傳值是容器且合併成本非 $O(1)$，必須把合併成本乘進去（例如逐一插入是 $O(n^2)$，改用 small-to-large 才是 $O(n \\log n)$）。\n\n遞迴深度：鏈狀樹上 $O(n)$，必要時改成顯式堆疊，或用 BFS 的反序當作後序。\n\n## 常見錯誤與邊界條件\n\n- 2421 型：邊沒有按 $\\max(\\text{端點值})$ 排序。\n- `cntMax` 在最大值不等時被誤加（只有兩塊最大值相同才累加）。\n- 忘記代表元必須是最大值較大的一方，導致 `vals[find(u)]` 取到錯誤的塊最大值。\n- 答案用 `int` 溢位（$n = 3\\times10^4$ 時 `cntMax` 相乘可達 $10^9$ 級）。\n- 漏掉 $n$ 條單點路徑。\n- 重複邊或自環未處理。\n\n- 子樹摘要型：摘要不完整（父節點需要的資訊沒被回傳，見 2.12 的狀態完整性）。\n- 合併成本被忽略導致複雜度誤判。\n- 空子樹的單位元選錯（見 2.3）。\n- 忘記 `v == fa`。\n- 遞迴過深。\n\n2867（合法路徑：路徑上恰有一個質數）則是另一種形態：把質數節點當作「分隔點」，先對非質數節點做並查集，再枚舉每個質數節點、用其各鄰接塊的大小做乘法計數——同樣是「合併時計數」的思路，只是分組的依據換成質數與否。\n\n## 與相似技巧的比較\n\n- 與 3.3：子樹摘要型就是 3.3，本節只是提醒「摘要要完整、合併成本要算」。\n- 與 3.12 的點分治：兩者都在數路徑。點分治適用「路徑條件與距離／權重和有關」；並查集合併計數適用「路徑條件可以表達成**單調的門檻**」（值 $\\le v$、只用某類節點），因為它依賴「按門檻遞增逐步加入」這個單調過程。**條件不單調就用不了並查集。**\n- 與 graph 主題的 Kruskal 重構樹：2421 的做法本質上就是 Kruskal 重構樹的簡化版——按權排序合併、在合併時統計。若題目要支援查詢而非只求總數，就該真的建出重構樹。\n- 與 3.11 的 DSU on tree：名字都有 DSU 但完全是兩回事。本節的並查集是真的並查集（維護連通性）；DSU on tree 只是借用了 small-to-large 的攤還思想，並不使用並查集結構。**不要混淆。**\n\n## 代表例題\n\n**題號**：2479、2867、2421、1719\n\n放在「其他」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 2479 | [兩個不重疊子樹的最大異或值](https://leetcode.cn/problems/maximum-xor-of-two-non-overlapping-subtrees/)（Premium） |  |\n| 2867 | [統計樹中的合法路徑數目](https://leetcode.cn/problems/count-valid-paths-in-a-tree/) | 2428 |\n| 2421 | [好路徑的數目](https://leetcode.cn/problems/number-of-good-paths/) | 2445 |\n| 1719 | [重構一棵樹的方案數](https://leetcode.cn/problems/number-of-ways-to-reconstruct-a-tree/) | 3018 |\n\n## 本節重點速查\n\n- 兩種降本手法：**子樹摘要**（一次後序把重算壓成回傳值）與**合併時計數**（把 $O(n^2)$ 條路徑攤成 $n-1$ 次合併，每次用兩團大小相乘）。\n- 2421 的關鍵不變量是「處理完 $\\max \\le v$ 的邊後，塊 = 值 $\\le v$ 的極大連通集合，`cntMax` = 塊內取到最大值的節點數」，而它**依賴邊按 $\\max(\\text{端點值})$ 排序**。\n- 乘法只對「跨團」成立，同團的配對早已算過。\n- 這裡的 DSU 與 3.11 的 DSU on tree 沒有關係。\n",
        },
      ],
    },
    {
      id: 44,
      title: "4. 回溯",
      summary:
        "涵蓋回溯的入門、子集、劃分、組合、排列、有重複元素、搜尋與折半列舉，適合系統學習。\n\n:::example 回溯框架（子集型：選或不選）\n**C++ 模板（回溯框架）**：\n\n```cpp\n// 回溯框架（子集型：選或不選）\nvector<vector<int>> result;\nvector<int> path;\nvoid backtrack(vector<int>& nums, int i) {\n    if (i == (int)nums.size()) {\n        result.push_back(path);\n        return;\n    }\n    // 不選 nums[i]\n    backtrack(nums, i + 1);\n    // 選 nums[i]\n    path.push_back(nums[i]);\n    backtrack(nums, i + 1);\n    path.pop_back();  // 回溯\n}\n```\n:::",
      children: [
        {
          id: 45,
          title: "4.1 入門回溯",
          summary:
            '## 這個技術解決什麼問題\n\n有一類題目要求的不是「一個最佳答案」，而是**把所有合法方案列出來**：所有字母組合（17）、所有子集、所有排列、所有分割方式。這些答案的個數本身就是指數級的，沒有多項式解法可言，問題變成「如何不重不漏地枚舉，且不要浪費時間在注定失敗的分支上」。\n\n回溯的槓桿有兩個。第一，**把「所有方案」組織成一棵遞迴樹**，讓「根到葉的路徑」與「方案」一一對應——有了這個雙射，不重不漏就是結構性的，不需要額外去重。第二，**共用前綴**：長度為 $n$ 的方案有指數多個，但它們共用大量前綴，回溯只把每個前綴構造一次，並在原地修改同一個 `path` 陣列，避免每條路徑各複製一份。\n\n## 辨識題型的訊號\n\n看到這些特徵，往入門回溯想：\n\n- **要「列出所有」組合 / 排列 / 路徑 / 選法**，或要在所有可能中找最值，且資料規模很小。\n- **每一步是一個獨立的選擇**，做完一個選擇問題就縮小成「同樣形式的子問題」。\n- **暴力多層巢狀迴圈寫不出來**（層數不固定、由輸入決定）——這正是遞迴回溯的舞台。\n- 題目像 17 那樣，答案的長度/結構由輸入動態決定，需要逐步構造。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的決策樹「每一層」代表什麼？「每個分支」是哪些候選？\n2. 我的終止條件是什麼？什麼時候算湊出一個完整解？\n3. 我有沒有把「做選擇」和「撤銷」嚴格成對寫出來？\n\n三題都答得出來，就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 17 | 電話號碼的字母組合 | N/A | 放在「入門回溯」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n## 核心想法與直覺\n\n**延伸講解**：[回溯算法套路①子集型回溯【基礎演算法精講 14】](https://www.bilibili.com/video/BV1mG4y1A7Gu/)\n\n本節介紹「回溯（backtracking）」的入門。很多題目要你「列出所有可能的組合 / 排列 / 選法」，例如 17「電話號碼的字母組合」：輸入 "23"，2 對應 abc、3 對應 def，要列出 ad, ae, af, bd, …, cf 全部 9 種。這種「窮舉所有可能」最自然的寫法就是回溯。\n\n:::example 回溯三件事：做選擇 → 遞迴 → 撤銷選擇\n回溯的本質是「在一棵想像中的決策樹上走遍每條路」。每一步你做一個選擇（這位數字選哪個字母），往下遞迴；走到底（所有位數都選完）就收集一個答案；然後**退回上一步、撤銷剛才的選擇，換下一個分支**。這個「撤銷、換下一個」就是「回溯」這個名字的由來。\n\n```cpp\n// 回溯三件事：做選擇 → 遞迴 → 撤銷選擇\npath.push_back(choice);  // 做選擇\nbacktrack(next);         // 往下走\npath.pop_back();         // 撤銷，換下一個分支試\n```\n:::\n\n\n\n為什麼要「撤銷」？因為 `path` 是一條被所有分支共用的路徑紀錄。如果選了不還原，下一個分支會帶著上一個分支的殘留，答案就錯了。撤銷保證「每條路徑都是乾淨地從當前狀態出發」。\n\n:::example 電話字母組合：第 i 位選一個字母，遞迴到下一位\n以 17 為例，決策樹的第 i 層代表第 i 個數字，分支數 = 該數字對應的字母數；走到第 n 層就湊滿一個字串：\n\n```cpp\n// 電話字母組合：第 i 位選一個字母，遞迴到下一位\nvoid dfs(int i) {\n    if (i == digits.size()) {\n        ans.push_back(path);\n        return;\n    }                                    // 收集\n    for (char c : mapping[digits[i]]) {  // 枚舉本位所有候選\n        path.push_back(c);\n        dfs(i + 1);\n        path.pop_back();  // 撤銷\n    }\n}\n```\n:::\n\n\n\n回溯的時間複雜度通常是「分支數的連乘」，例如全是 3 個字母的 n 位數就是 `O(3^n * n)`（最後乘 n 是複製字串的成本）。它本來就是窮舉，慢是正常的；能用是因為題目規模小或有剪枝。\n\n## 狀態／資料結構定義\n\n以 17（電話號碼的字母組合）為例：\n\n- `path`：當前已做出的選擇序列，長度等於當前遞迴深度。它是**全域可變**的，靠進入時 push、離開時 pop 維持正確。\n- `i`：當前要決定第幾個位置。這是**唯一的狀態變數**——因為「還沒決定的部分」完全由 $i$ 決定。\n- `ans`：收集完整方案的容器。\n\n回溯函式的簽名 `void dfs(int i)` 意思是：「`path[0..i-1]` 已經定好，請把所有以它為前綴的完整方案都加進 `ans`」。**把這句話寫下來，是設計回溯的第一步**，因為它同時決定了遞迴的參數、終止條件與迴圈範圍。\n\n一般化的三要素：\n\n1. **狀態**：描述「已做的決策」與「還要決策什麼」的最小資訊。\n2. **選擇列表**：在當前狀態下，下一步有哪些合法選項。\n3. **終止條件**：什麼時候 `path` 已是一個完整方案。\n\n## 不變量或正確性證明\n\n**不變量：進入 `dfs(i)` 時，`path` 恰好是長度為 $i$ 的一個合法前綴；離開 `dfs(i)` 時，`path` 與進入時完全相同。**\n\n第二句是「回溯」二字的全部內容。它由「每個 `path.push_back` 都配對一個 `path.pop_back`，且中間沒有其他路徑能跳出函式」保證。有了它，**兄弟分支之間互不干擾**：`dfs(i)` 的第二個選項開始執行時，看到的 `path` 與第一個選項開始時一模一樣。若少了 pop，第二個分支會繼承第一個分支的殘留，答案全錯。\n\n**不重不漏的證明（方案 ↔ 根到葉路徑的雙射）。**\n\n- *不漏*：對任一合法方案 $c_0 c_1 \\cdots c_{n-1}$，由不變量遞迴地看：`dfs(0)` 的迴圈會枚舉第 0 位的所有合法選項，其中包含 $c_0$；進入該分支後 `dfs(1)` 會枚舉包含 $c_1$ 的所有選項……如此下去必定到達 $i = n$，把該方案加入 `ans`。前提是**每層的選擇列表涵蓋了該位置所有可能的取值**——這是設計時要自己確認的，程式不會提醒你。\n- *不重*：兩條不同的根到葉路徑，必在某一層 $i$ 選了不同的值 $c_i \\ne c\'_i$，因此對應的方案在第 $i$ 位不同，是兩個不同方案。反過來說，同一個方案只可能沿著唯一一條路徑被生成，因為每層的選項互不相同（同一層的 `for` 迴圈枚舉的是**互異**的選項）。\n\n**這條「不重」的論證點名了一個容易被違反的前提：同一層的選項必須兩兩互異。** 若輸入含重複元素（例如 `[1,1,2]` 求子集），同一層會出現兩個相同的選項，雙射就破了，必須額外去重——那正是 4.6 要處理的事。\n\n**終止條件與「答案在哪裡收集」。** 17 這種「方案長度固定」的題目在 $i = n$ 時收集，答案只在葉節點。但**這不是通則**：4.2 的子集型在每個節點都要收集，因為每個前綴本身就是一個合法方案。收集的位置由「什麼是完整方案」決定，不由習慣決定。\n\n## 逐步演算法\n\n入門回溯有固定骨架，分幾步：\n\n**1. 想清楚「決策樹」：每層選什麼、有幾個分支。** 第幾層代表什麼、候選有哪些，直接決定迴圈與遞迴參數。\n\n:::example 狀態：path 是當前部分解，i 是進度\n**2. 設計狀態：path + 進度索引。** `path` 記目前選到的東西，一個索引（如 `i`）記「現在輪到第幾步」。\n\n```cpp\n// 狀態：path 是當前部分解，i 是進度\nstring path;  // 已經選好的前幾位\n              // dfs(int i)：表示正在決定第 i 步\n```\n:::\n\n\n\n:::example 終止：選滿了就收集一個完整解\n**3. 寫終止條件與收集。** 走到底（索引到頭）就把 `path` 存進答案，然後 `return`。\n\n```cpp\n// 終止：選滿了就收集一個完整解\nif (i == n) {\n    ans.push_back(path);\n    return;\n}\n```\n:::\n\n\n\n:::example 心臟：選 → 遞迴 → 還原，缺一不可\n**4. 枚舉候選：做選擇 → 遞迴 → 撤銷。** 這是回溯的心臟，三步缺一不可（見原理的程式碼）。撤銷一定要和「做選擇」嚴格成對。\n\n```cpp\n// 心臟：選 → 遞迴 → 還原，缺一不可\nfor (auto& c : candidates(i)) {\n    path.push_back(c);  // 做選擇\n    dfs(i + 1);         // 往下\n    path.pop_back();    // 撤銷\n}\n```\n:::\n\n\n\n**5. 邊界與複雜度。**\n- 空輸入要特判（如 digits 為空時答案是空陣列而非含一個空字串）。\n- 撤銷與選擇必須對稱：push 配 pop、加旗標配清旗標。\n- 複雜度多為指數級（如 `O(3^n)`），只適用小規模或有強剪枝。\n\n## C++17 模板\n\n以下用同型延伸示範題 46. 全排列 展示核心寫法。\n\n**完整題目**：給定不重複整數陣列，列出所有可能排列。\n\n:::example 回溯 DFS：每層從未使用的元素中選一個加入 path\n**線索**：題目要求列出所有方案、排列、組合或路徑；需要逐步選擇並在返回時撤銷。\n\n```cpp\nclass Solution {\npublic:\n    vector<vector<int>> permute(vector<int>& nums) {\n            vector<vector<int>> ans;\n            vector<int> path;                // 當前排列路徑\n            vector<char> used(nums.size());  // 標記各元素是否已加入路徑\n            // 回溯 DFS：每層從未使用的元素中選一個加入 path\n            function<void()> dfs = [&]() {\n                    if (path.size() == nums.size()) {\n                            ans.push_back(path);  // 路徑長度等於 nums 時得到完整排列\n                            return;\n                    }\n                    for (int i = 0; i < (int)nums.size(); ++i) {\n                            if (used[i]) {\n                                    continue;  // 已使用的元素跳過\n                            }\n                            used[i] = true;\n                            path.push_back(nums[i]);\n                            dfs();\n                            // 回溯：撤銷選擇，讓後續元素有機會被選入\n                            path.pop_back();\n                            used[i] = false;\n                    }\n            };\n            dfs();\n            return ans;\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- 回溯維護路徑與可選集合；遞迴返回前恢復現場，確保下一個分支看到同樣的狀態。\n- `path` 保存目前已做的選擇，`used` 或 `start` 控制下一步可選集合。\n- 每次遞迴後都要撤銷選擇，讓下一個分支看到相同的現場。\n\n## 時間與空間複雜度\n\n回溯的複雜度 = **遞迴樹的節點數 × 每個節點的工作量**，不是「深度乘以分支數」這種直覺式的估算。\n\n17 的遞迴樹：深度 $n$（數字個數），每層分支數是該數字對應的字母數 $b_i \\in \\{3, 4\\}$。葉節點數為 $\\prod_i b_i \\le 4^n$，內部節點數比葉節點少，同階。每個葉節點要複製一份長度 $n$ 的字串，故時間 $O(n \\cdot 4^n)$。\n\n空間：`path` 佔 $O(n)$，遞迴堆疊 $O(n)$，**不計輸出**。若計入輸出則是 $O(n \\cdot 4^n)$。回答複雜度時說清楚是否含輸出，否則對不上。\n\n一般規則：\n\n- **方案數本身就是指數級時，複雜度的下界就是方案數**，任何演算法都逃不掉。回溯的優化空間全在「減少無效分支」（剪枝），不在改變數量級。\n- 「每個節點的工作量」常被忽略。若在每個節點都做一次 $O(n)$ 的檢查（例如判回文），複雜度要多乘一個 $n$。\n\n## 常見錯誤與邊界條件\n\n- push 後必須 pop，標記後必須復原。\n- 排列與組合的 start/used 語意不同。\n- 剪枝條件要保證不漏解。\n- 答案要拷貝 path，而不是存引用。\n\n## 常見變形\n\n- 排序後用 `i > start && nums[i] == nums[i - 1]` 去重。\n- 用 bitmask 表示已選集合。\n- 可與 DFS、DP 記憶化結合剪枝。\n\n## 與相似技巧的比較\n\n- 與 2.7 的二元樹回溯：機制完全相同（進入時記錄、離開時還原），差別只在「樹」是顯式給定的還是由選擇列表隱式生成。**回溯就是在一棵不存在的樹上做 DFS。**\n- 與 DFS 的關係：回溯 = DFS + 狀態還原。若狀態是傳值的參數（純量），不需要顯式還原；若是共用的容器或全域陣列，就必須還原。這與 3.2 提過的判準一致。\n- 與動態規劃：兩者都在遞迴樹上工作。**DP 適用於「不同前綴會導向相同子問題」而只需要一個最佳值的場合**（可以記憶化）；回溯適用於「要列出所有方案」的場合——方案本身依賴完整路徑，無法合併，記憶化沒有意義。這是選擇兩者的唯一判準。\n- 與 BFS：回溯用 DFS 是因為「共用前綴」在深度優先下天然成立（同一個 `path` 反覆改），BFS 則必須為佇列中每個狀態各存一份路徑，空間爆炸。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 17 | [電話號碼的字母組合](https://leetcode.cn/problems//letter-combinations-of-a-phone-number/) |  |\n\n## 本節重點速查\n\n- 先把契約寫成一句話：「`dfs(i)` 表示前 $i$ 位已定，請枚舉所有以它為前綴的完整方案」。\n- 三要素是**狀態、選擇列表、終止條件**。\n- 不變量是「離開時 `path` 與進入時相同」，靠 push/pop 嚴格配對，這才讓兄弟分支互不干擾。\n- 不重不漏來自「方案 ↔ 根到葉路徑」的雙射，其前提是**同一層的選項兩兩互異**（違反時見 4.6）。\n- 複雜度 = 遞迴樹節點數 × 每節點工作量。',
        },
        {
          id: 46,
          title: "4.2 子集型回溯",
          summary:
            "## 這個技術解決什麼問題\n\n「把所有子集列出來」是回溯最基本的形狀，也是 39、78、1239、2597、1601 這一大類題的共同骨架：每個元素獨立地「選或不選」，答案是所有選法。\n\n它與 4.1 的差別在於**方案長度不固定**——子集可以是空集，也可以是全集。這帶來兩個結構性問題：答案該在哪裡收集？如何保證每個子集只被生成一次？本節給出兩種標準視角（選／不選、枚舉下一個選誰），並說明它們各自的不變量與適用場合。\n\n## 辨識題型的訊號\n\n看到這些特徵，往子集型回溯想：\n\n- **要列舉「所有子集 / 所有組合 / 所有選法」**，或在其中找滿足條件者（如和等於目標）。\n- **每個元素的核心決定是「選或不選」**，元素之間相對獨立、不講順序。\n- **暴力無法用固定層數的巢狀迴圈枚舉**（子集數隨 n 指數成長），需要遞迴。\n- 題目像 78 / 39 / 254：輸出一堆「子集合 / 組合」，常伴隨「不可重複」「可重複選」「湊目標」等條件。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的每個元素是不是在做「選 / 不選」或「從 start 往後挑」的決定？\n2. 我該在「每個節點」收集（子集），還是「達標時」收集（組合）？\n3. 有重複元素時，我有沒有先排序、再做同層去重？\n\n三題都答得出來，就套對了。\n\n## 核心想法與直覺\n\n**延伸講解**：[回溯算法套路①子集型回溯【基礎演算法精講 14】](https://www.bilibili.com/video/BV1mG4y1A7Gu/)\n\n本節聚焦「子集型回溯」。它處理的題目共通點是：**對每個元素獨立做「選 / 不選」的決定**，最後把所有可能的選法列出來，或在其中找符合條件的。例如 78「子集」：給 [1,2,3]，要列出全部 8 個子集（含空集）。每個數字都有「要 / 不要」兩種選擇，這就是「子集型」這個名字的由來。\n\n回顧回溯的精神：在決策樹上做選擇、遞迴、再撤銷。子集型的決策樹特別規律——它有兩種等價的長法：\n\n:::example 子集：對第 i 個元素「不選」和「選」各遞迴一次\n**長法一：對每個元素二選一（選 / 不選）。** 第 i 層決定「第 i 個元素要不要」，走到底就是一個子集。\n\n```cpp\n// 子集：對第 i 個元素「不選」和「選」各遞迴一次\nvoid dfs(int i) {\n    if (i == nums.size()) {\n        ans.push_back(path);\n        return;\n    }\n    dfs(i + 1);               // 不選 nums[i]\n    path.push_back(nums[i]);  // 選 nums[i]\n    dfs(i + 1);\n    path.pop_back();  // 撤銷\n}\n```\n:::\n\n\n\n:::example 子集（起始下標版）：每到一個節點就收集，再往後延伸\n**長法二：用「起始下標」枚舉。** 每進一層就從 `start` 往後挑一個元素加入，挑誰就從誰的下一位繼續，天然避免重複組合（避免 {1,2} 和 {2,1} 都出現）。\n\n```cpp\n// 子集（起始下標版）：每到一個節點就收集，再往後延伸\nvoid dfs(int start) {\n    ans.push_back(path);  // 每個節點都是一個合法子集\n    for (int i = start; i < nums.size(); ++i) {\n        path.push_back(nums[i]);\n        dfs(i + 1);  // 從 i+1 繼續，不回頭\n        path.pop_back();\n    }\n}\n```\n:::\n\n\n\n:::example 組合總和：可重複選同一元素，故遞迴傳 i（不是 i+1）\n像 39「組合總和」（可重複選、湊出目標和）、254「因子的組合」、320「單詞縮寫」，本質都是「每個候選選或不選 / 從某處往後挑」的變形。組合總和因為同一個數可重複用，遞迴時傳 `i` 而不是 `i + 1`：\n\n```cpp\n// 組合總和：可重複選同一元素，故遞迴傳 i（不是 i+1）\npath.push_back(candidates[i]);\ndfs(i, target - candidates[i]);  // 還能再選一次 i\npath.pop_back();\n```\n:::\n\n\n\n複雜度：子集共有 `2^n` 個，列出全部就是 `O(2^n * n)`（乘 n 是複製每個子集的成本）。\n\n## 狀態／資料結構定義\n\n**視角一：選或不選（輸入的角度）**\n\n- `dfs(i)`：決定第 $i$ 個元素選不選。\n- 遞迴樹是一棵**滿二元樹**，深度恰為 $n$，每個節點兩個分支。\n- 葉節點（$i = n$）與子集一一對應，答案只在葉節點收集。\n\n**視角二：枚舉下一個選誰（答案的角度）**\n\n- `dfs(start)`：`path` 已經是一個合法子集，接下來只能從下標 $\\ge \\text{start}$ 的元素中挑「下一個要加入的」。\n- 遞迴樹的分支數逐層遞減（$n - \\text{start}$ 個選項），深度至多 $n$。\n- **每個節點都是一個合法子集**，答案在每個節點收集，包括根節點（空集）。\n\n兩種視角都用同一個 `path` 與 pop/push 配對。差別只在「遞迴樹長什麼樣」與「答案在哪裡收集」。\n\n## 不變量或正確性證明\n\n**視角一的雙射。** 一條根到葉的路徑是一個長度 $n$ 的二元決策序列 $(d_0, \\dots, d_{n-1})$，$d_i \\in \\{\\text{選}, \\text{不選}\\}$。這與 $\\{0,1\\}^n$ 一一對應，而 $\\{0,1\\}^n$ 與 $\\{0,\\dots,n-1\\}$ 的子集族一一對應（特徵向量）。故葉節點與子集一一對應，共 $2^n$ 個，**不重不漏是雙射的直接後果**，不需要任何額外論證。\n\n**視角二的雙射。** 每個子集 $S$ 有唯一的**遞增下標表示** $i_1 < i_2 < \\cdots < i_m$。視角二的遞迴恰好枚舉所有遞增下標序列：`dfs(start)` 的迴圈取 $j \\ge \\text{start}$，然後遞迴 `dfs(j + 1)`——下一個選的下標嚴格大於 $j$。因此根到任一節點的路徑對應一個嚴格遞增序列，且每個遞增序列對應唯一一條路徑。故節點與子集一一對應。\n\n**這條論證點名了一個必須遵守的細節：遞迴時傳的是 `j + 1`，不是 `start + 1`，也不是 `i + 1`。**\n\n- 傳 `start + 1`：下一層仍可選到 $j$ 以外但小於 $j$ 的下標，遞增性被破壞，同一個子集會以多種順序被生成（重複）。\n- 傳 `j`（不加一）：允許重複選同一個元素——這在 39（組合總和，元素可重複使用）中**正是想要的**。39 與 78 的唯一差別就是這裡加不加一。把這一點看清楚，兩題就是同一份程式碼。\n\n**答案收集位置的推導，而不是記憶。** 問「什麼樣的 `path` 是一個完整方案」：\n\n- 視角一：只有在所有元素都表態之後 `path` 才確定，故在 $i = n$ 收集。\n- 視角二：`path` 在任何時刻都已經是一個合法子集（它就是已選元素的集合），故在**每個節點**收集。\n\n若在視角二只在葉節點收集，會漏掉所有非極大的子集；若在視角一每個節點都收集，會把同一個子集收集多次（前綴相同的中途狀態）。**兩種錯誤都源自沒有先回答「什麼是完整方案」。**\n\n**剪枝的合法性。** 加剪枝時要證明「被剪掉的分支確實不含任何答案」，這是必要條件而非充分條件的檢查。例如 39（組合總和，正整數、和為 `target`）中，若當前和已超過 `target`，由於所有元素為正，繼續往下只會更大，故該分支必無解——**這一步用到「元素全為正」這個前提**。若允許負數，剪枝立刻失效，這也是 39 明確要求正整數的原因。\n\n2597（美麗子集：任兩元素差不等於 $k$）的剪枝則要在加入前檢查 `cnt[x - k]` 與 `cnt[x + k]` 是否為 0，並在回溯時遞減——這是「用計數器維護可行性」的標準做法，計數器的增減必須與 push/pop 同樣嚴格配對。\n\n## 逐步演算法\n\n子集型回溯的固定步驟：\n\n**1. 選一種決策樹寫法。** 二選一版直觀；起始下標版更容易處理「組合不計順序」與去重，建議優先掌握。\n\n:::example 狀態：path 為當前子集，start 為可挑選的起點\n**2. 狀態：path + start（或進度 i）。** `path` 是當前部分解，`start` 控制「只能往後挑」以避免重複。\n\n```cpp\n// 狀態：path 為當前子集，start 為可挑選的起點\nvector<int> path;\n// dfs(int start)：只從 nums[start..] 裡挑\n```\n:::\n\n\n\n:::example 收集：子集每節點都收；組合總和則在和達標時收\n**3. 收集時機。** 子集題「每個節點都收集」（含空集）；組合/目標和題則在「達成條件時」才收集。\n\n```cpp\n// 收集：子集每節點都收；組合總和則在和達標時收\nif (target == 0) {\n    ans.push_back(path);\n    return;\n}\n```\n:::\n\n\n\n:::example 同層去重：排序後，跳過同一層重複的值\n**4. 含重複元素要「同層去重」。** 先排序，遞迴迴圈裡若當前元素和前一個相同、且前一個沒被選，就跳過，避免產生重複子集。\n\n```cpp\n// 同層去重：排序後，跳過同一層重複的值\nfor (int i = start; i < nums.size(); ++i) {\n    if (i > start && nums[i] == nums[i - 1]) {\n        continue;  // 同層相同 → 跳\n    }\n    path.push_back(nums[i]);\n    dfs(i + 1);\n    path.pop_back();\n}\n```\n:::\n\n\n\n**5. 邊界與複雜度。**\n- 「選或不選」「可否重複選」「計不計順序」三件事決定傳 `i` 還是 `i + 1`、要不要去重。\n- 去重前一定要先排序，否則相同元素不相鄰，去重失效。\n- 複雜度子集 `O(2^n)`、組合視剪枝而定，皆為指數級，適用小規模。\n\n## C++17 模板\n\n```cpp\n// 子集（LeetCode 78）：兩種視角，任選一種。\nclass Solution {\n  std::vector<int> nums_, path_;\n  std::vector<std::vector<int>> ans_;\n\n  // 視角一：選或不選。深度固定 n，只在葉節點收集。\n  void pickOrNot(int i) {\n    if (i == static_cast<int>(nums_.size())) {\n      ans_.push_back(path_);\n      return;\n    }\n    pickOrNot(i + 1);              // 不選 nums_[i]\n    path_.push_back(nums_[i]);     // 選 nums_[i]\n    pickOrNot(i + 1);\n    path_.pop_back();\n  }\n\n  // 視角二：枚舉下一個選誰。每個節點都是一個合法子集，都要收集。\n  void pickNext(int start) {\n    ans_.push_back(path_);         // 包含根節點的空集\n    for (int j = start; j < static_cast<int>(nums_.size()); ++j) {\n      path_.push_back(nums_[j]);\n      pickNext(j + 1);             // j + 1：保證下標嚴格遞增\n      path_.pop_back();\n    }\n  }\n\n public:\n  std::vector<std::vector<int>> subsets(std::vector<int>& nums) {\n    nums_ = nums;\n    pickNext(0);\n    return ans_;\n  }\n};\n```\n\n## 時間與空間複雜度\n\n視角一的遞迴樹有 $2^{n+1} - 1$ 個節點，$2^n$ 個葉節點；每個葉節點複製一份長度至多 $n$ 的 `path`。時間 $O(n \\cdot 2^n)$。\n\n視角二的節點數恰為 $2^n$（與子集一一對應），每個節點複製一次 `path`。時間同樣 $O(n \\cdot 2^n)$。**兩者同階**，選擇依據是哪一種的剪枝比較好寫：\n\n- 條件與「某個元素選不選」直接掛鉤（1863 求所有子集異或和）→ 視角一。\n- 條件與「已選集合」有關且需要提早中止（39 的和超過 target、2597 的差值衝突）→ 視角二，因為每個節點都是完整狀態，容易判斷。\n\n空間：`path` 與遞迴堆疊 $O(n)$，不計輸出。\n\n$n$ 的規模上限：$2^n$ 在 $n = 20$ 時約 $10^6$，$n = 25$ 時約 $3\\times10^7$。**看到 $n \\le 20$ 幾乎就是在暗示指數枚舉**；$n$ 到 40 左右則要考慮 4.8 的折半列舉。\n\n## 常見錯誤與邊界條件\n\n- 視角二漏掉在節點處收集（只收葉節點，答案少一大半）。\n- 視角二傳 `start + 1` 或 `i + 1` 而非 `j + 1`（產生重複）。\n- 39 這種可重複選的題誤傳 `j + 1`（漏解）。\n- 視角一在每個節點收集（重複）。\n- 剪枝條件未驗證「被剪的分支確實無解」（例如有負數時仍用「和超過就剪」）。\n- 輔助計數器的增減沒有與 push/pop 配對。\n- 空集是否算合法子集要看題目。\n- `path` 存的是值還是下標要一致。\n- 輸入含重複元素時本節的雙射失效，須用 4.6 的去重規則。\n\n## 與相似技巧的比較\n\n- 與 4.4 的組合型：組合是「固定大小的子集」，骨架相同，多一個「長度到了就收集並回傳」的終止條件與長度剪枝。\n- 與 4.5 的排列型：子集不在乎順序（用 `start` 保證下標遞增），排列在乎順序（每層都要從全部未使用的元素中挑）。**`start` 這個參數在不在，就是子集與排列的全部差別。**\n- 與位元枚舉（`for (int mask = 0; mask < (1 << n); ++mask)`）：兩者同階且位元版更短、常數更小。**能用位元枚舉就用位元枚舉**；回溯的優勢在於可以剪枝（位元枚舉沒得剪），以及元素可重複選、方案長度無上界（39）這類位元表示不了的情形。\n- 與 4.8 的折半列舉：當 $n$ 大到 $2^n$ 不可行但 $2^{n/2}$ 可行時（$n \\approx 40$），把子集枚舉拆成兩半。前提是目標函數可分解。\n- 與狀壓 DP：同樣是 $2^n$ 個狀態，但 DP 會**合併**到達同一狀態的不同路徑（只留最佳值），子集枚舉則要全部列出。要「所有方案」用回溯，要「最佳值」優先考慮狀壓 DP。\n\n## 代表例題\n\n**題號**：39、51、78、254、320\n\n放在「子集型回溯」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 39 | [組合總和](https://leetcode.cn/problems/combination-sum/) |  |\n| 51 | [LCP 51. 烹飪料理](https://leetcode.cn/problems/UEcfPD/) |  |\n| 78 | [子集](https://leetcode.cn/problems/subsets/) |  |\n| 254 | [因子的組合](https://leetcode.cn/problems/factor-combinations/)（Premium） |  |\n| 320 | [列舉單詞的全部縮寫](https://leetcode.cn/problems/generalized-abbreviation/)（Premium） |  |\n| 784 | [字母大小寫全排列](https://leetcode.cn/problems/letter-case-permutation/) |  |\n| 2174 | [透過翻轉行或列來去除所有的 1 II](https://leetcode.cn/problems/remove-all-ones-with-row-and-column-flips-ii/) |  |\n| 1863 | [找出所有子集的異或總和再求和](https://leetcode.cn/problems/sum-of-all-subset-xor-totals/) | 1372 |\n| 3566 | [等積子集的劃分方案](https://leetcode.cn/problems/partition-array-into-two-equal-product-subsets/) | 1459 |\n| 2044 | [統計按位或能得到最大值的子集數目](https://leetcode.cn/problems/count-number-of-maximum-bitwise-or-subsets/) | 1568 |\n| 2397 | [被列覆蓋的最多行數](https://leetcode.cn/problems/maximum-rows-covered-by-columns/) | 1719 |\n| 1239 | [串聯字串的最大長度](https://leetcode.cn/problems/maximum-length-of-a-concatenated-string-with-unique-characters/) | 1720 |\n| 2212 | [射箭比賽中的最大得分](https://leetcode.cn/problems/maximum-points-in-an-archery-competition/) | 1869 |\n| 1255 | [得分最高的單詞集合](https://leetcode.cn/problems/maximum-score-words-formed-by-letters/) | 1882 |\n| 2151 | [基於陳述統計最多好人數](https://leetcode.cn/problems/maximum-good-people-based-on-statements/) | 1980 |\n| 2597 | [美麗子集的數目](https://leetcode.cn/problems/the-number-of-beautiful-subsets/) | 2023 |\n| 2959 | [關閉分部的可行集合數目](https://leetcode.cn/problems/number-of-possible-sets-of-closing-branches/) | 2077 |\n| 1601 | [最多可達成的換樓請求數目](https://leetcode.cn/problems/maximum-number-of-achievable-transfer-requests/) | 2119 |\n| 1617 | [統計子樹中城市之間最大距離](https://leetcode.cn/problems/count-subtrees-with-max-distance-between-cities/) | 2309 |\n\n## 本節重點速查\n\n- 兩種視角：**選／不選**（滿二元樹、深度 $n$、只在葉收集）與**枚舉下一個選誰**（每個節點都是合法子集、都要收集）。\n- 後者的關鍵是遞迴傳 **`j + 1`** 以保證下標嚴格遞增，這正是不重不漏的雙射依據；\n- 傳 `j`（不加一）就變成「元素可重複使用」的 39。\n- 答案收集的位置由「什麼是完整方案」推導，不靠記憶。\n- 剪枝必須先證明被剪的分支無解（39 用到「元素全為正」）。\n- $n \\le 20$ 是指數枚舉的訊號。\n",
        },
        {
          id: 47,
          title: "4.3 劃分型回溯",
          summary:
            '## 這個技術解決什麼問題\n\n劃分型回溯處理的是：**把一個序列切成若干連續段，每段要滿足某條件，列出所有切法**。分割回文串（131）、復原 IP 位址（93）、單詞拆分 II（140）、拆分字串使唯一子字串最多（1593）都是這個形狀。\n\n它看起來像子集型，其實不同：子集型選的是「哪些元素」，劃分型選的是「在哪裡下刀」。把問題轉譯成「選切點」之後，$n$ 個字元的序列有 $n-1$ 個可能的切點位置，每個切點獨立地切或不切——**劃分型本質上是切點集合的子集枚舉**，$2^{n-1}$ 種切法。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「劃分型回溯」想：\n\n- **要把一個字串或陣列切成「連續的好幾段」，並列出所有切法**，例如 93「復原 IP 地址」、131「分割回文串」、140「單詞拆分 II」、291「單詞規律 II」。\n- **段與段之間不重疊、剛好拼回原本**——這正是用單一 `start` 下標就能描述的訊號：\n\n:::example 嗅到味道的瞬間：答案是「把整體切成連續段」的所有方案\n```cpp\n// 嗅到味道的瞬間：答案是「把整體切成連續段」的所有方案\n// 不是選哪些元素，而是「刀切在哪些縫隙」\n```\n:::\n\n\n\n- **每段有獨立的合法條件**（是回文嗎？是合法 IP 段嗎？在字典裡嗎？）——這天生適合「切一段、驗一段、再遞迴」。\n- **3566「拆分字串使唯一子字串的數目最大」** 這類求「最多能切幾段」的，也是同一個骨架，只是把「收集所有方案」換成「更新全局最大段數」。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的「未處理起點」`start` 代表什麼？切完的判斷條件是什麼？\n2. 每枚舉一刀，我取出的那一段如何檢查合法？\n3. 遞迴回來後，我有沒有把剛加進 `path` 的段撤銷掉？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 93 | 復原 IP 地址 | N/A | 放在「劃分型回溯」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 131 | 分割回文串 | N/A | 放在「劃分型回溯」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 140 | 單詞拆分 II | N/A | 放在「劃分型回溯」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 291 | 單詞規律 II（Premium） | N/A | 放在「劃分型回溯」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 1593 | [拆分字串使唯一子字串的數目最大](https://leetcode.cn/problems/split-a-string-into-the-max-number-of-unique-substrings/) | 1740 | 放在「劃分型回溯」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n## 核心想法與直覺\n\n**延伸講解**：[回溯算法套路①子集型回溯【基礎演算法精講 14】](https://www.bilibili.com/video/BV1mG4y1A7Gu/)\n\n本節要解決的場景是：一個字串（或一段陣列）要被「切成連續的好幾段」，而且每一段都要滿足某種規則，我們想枚舉所有合法的切法。例如把 `"25525511135"` 切成四段合法 IP，或把 `"aab"` 切成每段都是回文的片段。\n\n這類問題的共通點是：**邊界只能切在元素「之間」，而且每一段都連著、不重疊、不漏掉。** 我們稱它為「劃分型回溯」——回溯（backtracking）就是「先試一種選擇，做下去；走不通或試完了，就退回來把剛才那步收回，再試下一種」。\n\n:::example 劃分的骨架：start 是還沒切的起點，往後枚舉每一刀的位置\n核心想法是：用一個下標 `start` 代表「目前還沒被切的部分從哪裡開始」。我們從 `start` 往後枚舉「下一刀切在哪」，每切一刀就得到一段 `[start, i]`，檢查它合不合法，合法就遞迴去切剩下的 `[i + 1, ...]`。\n\n```cpp\n// 劃分的骨架：start 是還沒切的起點，往後枚舉每一刀的位置\nvoid dfs(int start) {\n    if (start == s.size()) {\n        record();\n        return;\n    }  // 整個字串切完了\n    for (int i = start; i < s.size(); ++i) {\n        // 這一段是 s[start..i]\n    }\n}\n```\n:::\n\n\n\n:::example 做選擇 → 遞迴 → 撤銷選擇（恢復現場）\n為什麼「退回來」很重要？因為一段路試完之後，我們必須讓現場回到試之前的樣子，下一個選擇才不會被汙染。這就是「遞迴前做選擇、遞迴後撤銷」：\n\n```cpp\n// 做選擇 → 遞迴 → 撤銷選擇（恢復現場）\npath.push_back(seg);  // 選：把這一段加進路徑\ndfs(i + 1);           // 遞迴：去切剩下的部分\npath.pop_back();  // 撤銷：拿掉這一段，回到原狀，準備試下一刀\n```\n:::\n\n\n\n只要每段都連續、起點接著上一段的終點，就保證切出來的片段拼回去剛好是原字串，不重不漏。整體會走遍所有切法，是指數級，所以要靠「不合法就提前停」來剪枝。\n\n## 狀態／資料結構定義\n\n- `dfs(i)`：`s[0..i-1]` 已經被完整切好且每段合法，`path` 存著這些段；請枚舉 `s[i..n-1]` 的所有合法切法。\n- **唯一的狀態變數是 $i$**（下一段的起點）。這是劃分型與子集型的關鍵區別：子集型的 `start` 是「候選元素的下界」，劃分型的 $i$ 是「已消耗的前綴長度」，兩者含義不同但都恰好一維。\n- 迴圈枚舉這一段的**結束位置** $j \\in [i, n)$，即取子串 `s[i..j]`。\n- 終止條件：$i = n$，表示整個字串剛好用完，`path` 是一個完整劃分。\n- 93 這種段數固定的題目多一個維度：`dfs(i, seg)`，`seg` 是已切出的段數；終止條件是 $i = n$ **且** $\\text{seg} = 4$。\n\n## 不變量或正確性證明\n\n**不變量：進入 `dfs(i)` 時，`path` 中各段依序連接恰好等於 `s[0..i-1]`，且每段都滿足題目條件。**\n\n- 初始化：`dfs(0)`，`path` 為空，連接起來是空串 = `s[0..-1]`，成立。\n- 保持：在 `dfs(i)` 中取子串 `s[i..j]`（先檢查它合法），push 進 `path` 後遞迴 `dfs(j + 1)`。此時 `path` 各段連接為 `s[0..i-1] + s[i..j] = s[0..j]`，正是 `dfs(j+1)` 所要求的。回溯時 pop，恢復原狀。\n- 終止：$i = n$ 時各段連接恰為整個 `s`，故 `path` 是一個合法劃分。\n\n**不重不漏（劃分 ↔ 切點集合的雙射）。** 一個劃分完全由它的切點集合決定：段的邊界是 $0 = b_0 < b_1 < \\cdots < b_m = n$，中間的 $b_1, \\dots, b_{m-1}$ 就是切點，取自 $\\{1, \\dots, n-1\\}$。\n\n- *不漏*：對任一合法劃分，其第一段是 `s[0..b_1-1]`，這個 $j = b_1 - 1$ 必定被 `dfs(0)` 的迴圈枚舉到（迴圈範圍 $j \\in [0, n)$ 涵蓋所有可能的第一段結束位置）；遞迴地，每一段都被枚舉到。\n- *不重*：兩條不同路徑必在某一層取了不同的 $j$，於是對應劃分的該段長度不同，是兩個不同的劃分。反之同一個劃分只對應唯一一條路徑，因為各段的結束位置是它的固有屬性。\n\n因此路徑與劃分一一對應，至多 $2^{n-1}$ 條。\n\n**「合法性檢查放在哪裡」是本節唯一真正的設計決策。** 兩種寫法：\n\n1. **在 push 之前檢查**（提前剪枝）：`if (!ok(i, j)) continue;`。不合法的段直接跳過，整棵子樹不展開。\n2. **走到底再檢查**：把所有切法列出來再過濾。\n\n兩者答案相同，但第一種的剪枝是**指數級**的收穫，不是常數優化。以 131 為例，若 `s = "aaaa...a"` 之外的一般字串，非回文的段往往在很淺的層就出現，提前剪枝直接砍掉整棵子樹。**設計時應該永遠選第一種**，並確認 `ok(i, j)` 判斷的是「這一段本身合法」而不涉及後續——若條件跨段（例如 1593 要求所有段互異），就得把「已用過的段」放進一個集合並在回溯時移除。\n\n**93 的長度剪枝的正確性。** 剩下 `n - i` 個字元、還要切 `4 - seg` 段，每段長度在 $[1, 3]$。若 $n - i > 3(4 - \\text{seg})$ 或 $n - i < 4 - \\text{seg}$，則無論怎麼切都湊不出來，可直接剪枝。**這是一個必要條件的檢查：不滿足它一定無解，滿足它未必有解**——剪枝只要求前者，這就夠了。這類「剩餘量 vs 剩餘容量」的剪枝在劃分型與組合型中反覆出現。\n\n**回文預處理不改變雙射，只改變每個節點的工作量。** 131 若每次現算 `isPalindrome(i, j)` 是 $O(n)$，總複雜度多乘一個 $n$；先用 $O(n^2)$ 的 DP 預處理 `pal[i][j]` 後，每次判斷 $O(1)$。這是「每節點工作量」這一項的優化，與剪枝是兩件不同的事，別混為一談。\n\n## 逐步演算法\n\n把劃分型回溯拆成四步，逐一對應程式碼：\n\n:::example 狀態：start 是當前未處理起點，path 是已切好的片段\n**1. 狀態表示：一個起點下標 + 一條已選路徑。** `start` 記錄還沒切的部分從哪開始，`path` 存目前已經切好的段。\n\n```cpp\n// 狀態：start 是當前未處理起點，path 是已切好的片段\nstring s;\nvector<string> path;  // 已經切出來的段\nvector<vector<string>> ans;\n```\n:::\n\n\n\n:::example 枚舉這一段的結尾 i，合法就深入，回來撤銷\n**2. 更新流程：枚舉這一刀、檢查、遞迴、撤銷。** 從 `start` 到結尾枚舉切點 `i`，取出子段 `s[start..i]`，合法才往下走，回來一定要 `pop_back`。\n\n```cpp\n// 枚舉這一段的結尾 i，合法就深入，回來撤銷\nfor (int i = start; i < s.size(); ++i) {\n    string seg = s.substr(start, i - start + 1);\n    if (!valid(seg)) {\n        continue;  // 剪枝：這段不合法就跳過\n    }\n    path.push_back(seg);\n    dfs(i + 1);       // 從 i+1 繼續切剩下的\n    path.pop_back();  // 恢復現場\n}\n```\n:::\n\n\n\n:::example 切到字串尾端 → path 是一組完整劃分\n**3. 答案讀取：切到底就收集。** 當 `start` 走到字串結尾，代表整個字串剛好被切完，這時 `path` 就是一組合法劃分（IP 題還要額外檢查「剛好切成四段」）。\n\n```cpp\n// 切到字串尾端 → path 是一組完整劃分\nif (start == s.size()) {\n    ans.push_back(path);\n    return;\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 每段的合法性檢查要早做（例如 IP 段不能有前導零、數值 ≤ 255），越早剪枝越快。\n- 段數有限制時（如 IP 固定四段），用一個 `count` 一起傳，超過就回頭。\n- 複雜度是指數級，但被合法性剪枝大幅壓縮；像 IP 這種長度固定的題目實際分支極少。\n\n## C++17 模板\n\n以下用同型延伸示範題 46. 全排列 展示核心寫法。\n\n**完整題目**：給定不重複整數陣列，列出所有可能排列。\n\n:::example 回溯 DFS：每層從未使用的元素中選一個加入 path\n**線索**：題目要求列出所有方案、排列、組合或路徑；需要逐步選擇並在返回時撤銷。\n\n```cpp\nclass Solution {\npublic:\n    vector<vector<int>> permute(vector<int>& nums) {\n            vector<vector<int>> ans;\n            vector<int> path;                // 當前排列路徑\n            vector<char> used(nums.size());  // 標記各元素是否已加入路徑\n            // 回溯 DFS：每層從未使用的元素中選一個加入 path\n            function<void()> dfs = [&]() {\n                    if (path.size() == nums.size()) {\n                            ans.push_back(path);  // 路徑長度等於 nums 時得到完整排列\n                            return;\n                    }\n                    for (int i = 0; i < (int)nums.size(); ++i) {\n                            if (used[i]) {\n                                    continue;  // 已使用的元素跳過\n                            }\n                            used[i] = true;\n                            path.push_back(nums[i]);\n                            dfs();\n                            // 回溯：撤銷選擇，讓後續元素有機會被選入\n                            path.pop_back();\n                            used[i] = false;\n                    }\n            };\n            dfs();\n            return ans;\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- 回溯維護路徑與可選集合；遞迴返回前恢復現場，確保下一個分支看到同樣的狀態。\n- `path` 保存目前已做的選擇，`used` 或 `start` 控制下一步可選集合。\n- 每次遞迴後都要撤銷選擇，讓下一個分支看到相同的現場。\n\n## 時間與空間複雜度\n\n遞迴樹的葉節點與合法劃分一一對應，至多 $2^{n-1}$ 個；內部節點同階。\n\n131：每個節點的迴圈是 $O(n)$，回文判定 $O(1)$（預處理後），葉節點複製 `path` 是 $O(n)$。總計 $O(n \\cdot 2^n)$，預處理 $O(n^2)$ 被吸收。若不預處理，回文判定 $O(n)$ 使總時間變成 $O(n^2 \\cdot 2^n)$。\n\n93：段數固定為 4，切點只有 $\\binom{n-1}{3}$ 種組合，且每段長度 $\\le 3$，實際搜尋空間是 $O(3^4) = O(1)$ 級——**段數固定時複雜度與 $n$ 幾乎無關**，這與段數自由的 131 是完全不同的量級。看到「固定切成 $k$ 段」就該意識到指數退化成 $O(\\binom{n}{k})$。\n\n空間：`path` $O(n)$、`pal` $O(n^2)$、遞迴堆疊 $O(n)$，不計輸出。\n\n## 常見錯誤與邊界條件\n\n- push 後必須 pop，標記後必須復原。\n- 排列與組合的 start/used 語意不同。\n- 剪枝條件要保證不漏解。\n- 答案要拷貝 path，而不是存引用。\n\n## 常見變形\n\n- 排序後用 `i > start && nums[i] == nums[i - 1]` 去重。\n- 用 bitmask 表示已選集合。\n- 可與 DFS、DP 記憶化結合剪枝。\n\n## 與相似技巧的比較\n\n- 與 4.2 的子集型：劃分型 = 切點集合的子集枚舉，所以複雜度是 $2^{n-1}$ 而非 $2^n$。骨架相同，但**狀態的含義不同**——子集型的 `start` 是候選下界，劃分型的 $i$ 是已消耗的前綴長度。\n- 與 4.4 的組合型：93 這種「固定切成 $k$ 段」的劃分，就是「從 $n-1$ 個切點中選 $k-1$ 個」的組合型，兩者的長度剪枝形式一模一樣。\n- 與 DP：140（單詞拆分 II）要列出所有拆法，只能回溯；139（單詞拆分，只問能不能）用 DP $O(n^2)$。**「要所有方案」與「要一個布林／最佳值」的分界線在這裡最清晰。** 若題目問「最少切幾段」，用 DP；問「有哪些切法」，用回溯。\n- 140 可以用「記憶化 + 回溯」的混合：先用 DP 算出 `canBreak[i]`（後綴 $i$ 能否被拆分），在回溯時用它剪枝。這樣能避免在注定失敗的後綴上浪費指數時間，是劃分型最有效的剪枝形式之一。\n- 與 string 主題的回文相關演算法（Manacher）：131 的 $O(n^2)$ 回文預處理已足夠（它被指數項吸收），不需要更快的演算法。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 93 | [復原 IP 地址](https://leetcode.cn/problems//restore-ip-addresses/) |  |\n| 131 | [分割回文串](https://leetcode.cn/problems//palindrome-partitioning/) |  |\n| 140 | [單詞拆分 II](https://leetcode.cn/problems//word-break-ii/) |  |\n| 291 | [單詞規律 II](https://leetcode.cn/problems//word-pattern-ii/) |  |\n| 1593 | [拆分字串使唯一子字串的數目最大](https://leetcode.cn/problems//split-a-string-into-the-max-number-of-unique-substrings/) | 1740 |\n| 1849 | [將字串拆分為遞減的連續值](https://leetcode.cn/problems//splitting-a-string-into-descending-consecutive-values/) | 1747 |\n| 2698 | [求一個整數的懲罰數](https://leetcode.cn/problems//find-the-punishment-number-of-an-integer/) | 1679 |\n\n## 本節重點速查\n\n- 劃分型 = **選切點**，故方案數是 $2^{n-1}$。\n- 契約：「`dfs(i)` 表示 `s[0..i-1]` 已切好」，迴圈枚舉這一段的**結束位置** $j$，遞迴傳 **`j + 1`**。\n- 不變量是「`path` 各段連接 = `s[0..i-1]`」。\n- 合法性檢查必須放在 push **之前**（指數級剪枝），而非事後過濾。\n- 段數固定時（93）要同時檢查「用完字串」與「段數正確」，並用「剩餘字元 vs 剩餘段數×段長上下界」剪枝。\n- 要所有方案用回溯，要最佳值用 DP。',
        },
        {
          id: 48,
          title: "4.4 組合型回溯",
          summary:
            "## 這個技術解決什麼問題\n\n組合型回溯處理「**從 $n$ 個候選中選出恰好 $k$ 個**」以及它的近親「選出若干個使某個量恰好達標」：77（組合）、216（組合總和 III）、22（括號生成）、301（刪除無效的括號）、1286（字母組合迭代器）。\n\n它與 4.2 子集型的骨架相同，多了一個**大小約束**。這個約束不只是終止條件，更是**剪枝的來源**：當「還需要選的個數」超過「還剩下的候選個數」時，整棵子樹必然無解，可以直接砍掉。$\\binom{n}{k}$ 相對於 $2^n$ 可以小非常多（$n = 20, k = 3$ 時 1140 對上 $10^6$），這個剪枝把差距真的兌現出來。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「組合型回溯」想：\n\n- **要「挑出一組」而且 {1,2} 與 {2,1} 視為相同**，例如 77「組合」、216「組合總和 III」、1286「字母組合迭代器」。順序不重要，就是組合的訊號。\n- **你發現需要避免「同一組被算兩次」**——解法就是用 `start` 只往後挑：\n\n:::example 嗅到味道的瞬間：你想「不重複地列出所有子集合 / 固定大小的組合」\n```cpp\n// 嗅到味道的瞬間：你想「不重複地列出所有子集合 / 固定大小的組合」\n// dfs(i + 1) 而非 dfs(start)，就是「不回頭」的具體寫法\n```\n:::\n\n\n\n- **每一步是「二選一或多選一」的合法構造**，像 22「括號生成」「放左還是放右」、301「刪除無效的括號」「這個字元留還是刪」，本質都是組合式枚舉加合法性剪枝。\n- **暴力列出所有子集再篩選太慢**；組合回溯邊構造邊剪枝，省掉大量不可能分支。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我這題在意順序嗎？（不在意 → 組合，要用 `start` 防重複）\n2. 遞迴時我傳的是 `i + 1` 還是 `start`？（組合不重選通常傳 `i + 1`）\n3. 我的終止與收集條件是什麼？有沒有可以提早 `break` 的剪枝？\n\n## 核心想法與直覺\n\n**延伸講解**：[回溯算法套路②組合型回溯＋剪枝【基礎演算法精講 15】](https://www.bilibili.com/video/BV1xG4y1F7nC/)\n\n本節要解決的場景是：從一堆候選裡「挑出一組」，只在乎「挑了哪些」、不在乎「挑的先後順序」。例如從 1~n 裡挑 k 個數（77 組合）、挑出加總等於某值的數（216 組合總和 III）。這種「只問選哪些、不問順序」的問題，就是「組合型回溯」。\n\n:::example 組合的關鍵：用 start 限制「只往後挑」，自動避免 {1,2}/{2,1} 重複\n回溯（backtracking）的意思是「先試一種選擇做下去，試完或走不通就退回來、收回剛才那步、再試別的」。組合和排列最大的差別在這裡：**組合裡 {1,2} 和 {2,1} 算同一組，所以我們必須避免重複。** 做法是規定「只能往後挑，不能回頭」——用一個 `start` 下標記住「下一個可以挑的最小位置」。\n\n```cpp\n// 組合的關鍵：用 start 限制「只往後挑」，自動避免 {1,2}/{2,1} 重複\nvoid dfs(int start) {\n    // 從 start 開始往後選，永遠不回頭去選比 start 小的\n    for (int i = start; i <= n; ++i) { /* 選 i，然後遞迴到 i+1 */\n    }\n}\n```\n:::\n\n\n\n:::example 做選擇 → 遞迴（從 i+1 繼續）→ 撤銷\n核心動作同樣是「遞迴前做選擇、遞迴後撤銷」：把候選加進路徑、往下遞迴、回來時把它拿掉，讓現場恢復原狀，下一個選擇才不會被汙染。\n\n```cpp\n// 做選擇 → 遞迴（從 i+1 繼續）→ 撤銷\npath.push_back(i);\ndfs(i + 1);  // 注意是 i+1：下一個從更後面挑，避免重複也避免重選自己\npath.pop_back();  // 恢復現場\n```\n:::\n\n\n\n像 22「括號生成」雖然不是從陣列挑數，但本質一樣：每一步「放左括號或放右括號」也是一種組合式的選擇，只要在合法範圍內枚舉即可。整體是指數級枚舉，靠剪枝（提早停掉不可能的分支）加速。\n\n## 狀態／資料結構定義\n\n**77（從 $1..n$ 中選 $k$ 個）：**\n\n- `dfs(start)`：`path` 已選好若干個且下標嚴格遞增，下一個只能從 `start` 開始挑。\n- 終止條件：`path.size() == k` 時收集並**立即回傳**（不再往下）。\n- 剪枝：剩餘候選數 `n - j + 1` 必須 $\\ge$ 還需要的個數 `k - path.size()`。\n\n**22（括號生成）：** 狀態不是「選哪些」而是兩個計數器。\n\n- `dfs(open, close)`：已放 `open` 個左括號、`close` 個右括號。\n- 選擇列表：放左括號（條件 `open < n`）、放右括號（條件 `close < open`）。\n- 終止條件：`open == close == n`。\n\n兩者看起來不同，其實都是「在約束下枚舉固定長度的序列」。**22 的狀態之所以是 `(open, close)` 而不是位置下標，是因為合法性只取決於這兩個計數，與具體排列無關**——認出這一點是解 22 的關鍵。\n\n## 不變量或正確性證明\n\n**77 的剪枝正確性。** 在 `dfs(start)` 中枚舉 $j$，若選了 $j$，則剩下的必須從 $\\{j+1, \\dots, n\\}$ 中選出 $k - |\\text{path}| - 1$ 個。可用候選數是 $n - j$。故有解的**必要條件**是\n$$n - j \\ge k - |\\text{path}| - 1 \\quad\\Longleftrightarrow\\quad j \\le n - (k - |\\text{path}|) + 1 .$$\n超過這個上界的 $j$ 一定湊不滿 $k$ 個，可以整批跳過——直接把迴圈上界設成 $n - (k - |\\text{path}|) + 1$。\n\n注意這是必要條件而非充分條件：滿足它未必真的有解（例如 216 還有和的限制）。**剪枝只需要必要條件**，這一點在所有搜尋問題中通用：剪掉的必須確定無解，留下的不保證有解。\n\n這個剪枝把複雜度從「遍歷子集型的部分遞迴樹」壓成「恰好 $\\binom{n}{k}$ 個葉節點加上同階的內部節點」。沒有剪枝時，程式會展開大量最終長度不足 $k$ 的分支，白白浪費。\n\n**216 的雙向剪枝。** 從 $1..9$ 中選 $k$ 個不同數字使和為 $n$。除了上面的個數剪枝，還有和的剪枝：\n\n- 若當前和已 $>$ 目標，剪（依賴**所有候選為正**）。\n- 若「當前和 + 剩下能取的最大值之和」$<$ 目標，剪。\n- 若「當前和 + 剩下能取的最小值之和」$>$ 目標，剪。\n\n三條都是必要條件的檢查，且第一條的正確性明確依賴正數前提，與 4.2 中 39 的情形相同。\n\n**22 的不變量：任何時刻 `open >= close`，且最終序列的每個前綴都滿足「左括號數 $\\ge$ 右括號數」。**\n\n這正是括號序列合法的充要條件（配合總數相等）。證明合法性判定的充要性：\n\n- ($\\Rightarrow$) 合法序列中，任一前綴的每個右括號都必須有一個在它之前的左括號與之配對，且配對互不重複，故左括號數 $\\ge$ 右括號數。\n- ($\\Leftarrow$) 若每個前綴都滿足此條件且總數相等，則用「棧」掃描時每遇到右括號棧必非空，掃完棧必空，故合法。\n\n因此把「放右括號」的條件寫成 `close < open`，恰好維持了這個不變量。**不重不漏**：每個合法序列對應唯一一條路徑（第 $t$ 步放的是哪種括號是序列的固有屬性），每條路徑對應唯一一個合法序列。\n\n這個做法的精髓是**「只生成合法的」而非「生成全部再過濾」**：後者要枚舉 $\\binom{2n}{n}$ 個序列，前者只走 $C_n = \\frac{1}{n+1}\\binom{2n}{n}$ 個葉節點（卡塔蘭數）。兩者相差 $n+1$ 倍——不算驚人，但更重要的是它示範了「把可行性條件編進選擇列表」這個通用技巧，在別的題目上能省下指數級的工作。\n\n**301（刪除無效的括號）的層級搜尋。** 要刪最少的字元使括號合法。做法是先掃一遍算出必須刪的左、右括號數 $(l, r)$，再回溯枚舉刪哪些，只在恰好刪掉 $l + r$ 個且結果合法時收集。這裡的剪枝關鍵是「剩餘字元數 $<$ 還要刪的字元數就返回」，以及對連續相同字元只在第一個位置嘗試刪除（否則會生成重複結果，理由與 4.6 的去重完全相同）。\n\n## 逐步演算法\n\n把組合型回溯拆成四步，逐一對應程式碼：\n\n:::example 狀態：path 是已挑出的元素，start 控制只往後選\n**1. 狀態表示：一條已選路徑 + 一個起點下標。** `path` 存目前挑了哪些，`start` 限制接下來只能往後挑。\n\n```cpp\n// 狀態：path 是已挑出的元素，start 控制只往後選\nint n, k;\nvector<int> path;\nvector<vector<int>> ans;\n```\n:::\n\n\n\n:::example 從 start 往後枚舉，選 i 後遞迴到 i+1，回來撤銷\n**2. 更新流程：枚舉、選、遞迴、撤銷。** 從 `start` 往後枚舉候選 `i`，選進 `path`，遞迴時傳 `i + 1`（下一個更後面），回來 `pop_back`。\n\n```cpp\n// 從 start 往後枚舉，選 i 後遞迴到 i+1，回來撤銷\nfor (int i = start; i <= n; ++i) {\n    path.push_back(i);\n    dfs(i + 1);\n    path.pop_back();\n}\n```\n:::\n\n\n\n:::example 路徑長度達到 k → 找到一組合法組合\n**3. 答案讀取：路徑湊滿條件就收集。** 組合通常有「選滿 k 個」或「加總達標」的終止條件，達成就把 `path` 收進答案。\n\n```cpp\n// 路徑長度達到 k → 找到一組合法組合\nif (path.size() == k) {\n    ans.push_back(path);\n    return;\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 強力剪枝：剩下的候選數量不夠湊滿 k 個時，直接停。`for` 上界可寫成 `i <= n - (k - path.size()) + 1`。\n- 求和題（216）可在加總已超標時提前 `break`（若候選有序）。\n- 複雜度為組合數量級 `O(C(n, k) * k)`，剪枝能把實際分支大幅砍掉。\n\n## C++17 模板\n\n```cpp\n// 組合（LeetCode 77）：從 1..n 選 k 個，帶個數剪枝。\nclass Solution {\n  int n_ = 0, k_ = 0;\n  std::vector<int> path_;\n  std::vector<std::vector<int>> ans_;\n\n  void dfs(int start) {\n    int need = k_ - static_cast<int>(path_.size());   // 還需要選幾個\n    if (need == 0) {\n      ans_.push_back(path_);\n      return;                                          // 湊滿就停，不再往下\n    }\n    // 剪枝：j 太大時剩餘候選 n_ - j 不足以湊滿 need - 1 個\n    for (int j = start; j <= n_ - need + 1; ++j) {\n      path_.push_back(j);\n      dfs(j + 1);                                      // 保證嚴格遞增\n      path_.pop_back();\n    }\n  }\n\n public:\n  std::vector<std::vector<int>> combine(int n, int k) {\n    n_ = n;\n    k_ = k;\n    path_.reserve(k);\n    dfs(1);\n    return ans_;\n  }\n};\n```\n\n```cpp\n// 括號生成（LeetCode 22）：把合法性編進選擇列表，只生成合法序列。\nclass Solution {\n  int n_ = 0;\n  std::string path_;\n  std::vector<std::string> ans_;\n\n  void dfs(int open, int close) {\n    if (close == n_) {                 // 右括號放滿 → 左括號也必然放滿\n      ans_.push_back(path_);\n      return;\n    }\n    if (open < n_) {                   // 還能放左括號\n      path_.push_back('(');\n      dfs(open + 1, close);\n      path_.pop_back();\n    }\n    if (close < open) {                // 不變量：任何前綴 open >= close\n      path_.push_back(')');\n      dfs(open, close + 1);\n      path_.pop_back();\n    }\n  }\n\n public:\n  std::vector<std::string> generateParenthesis(int n) {\n    n_ = n;\n    path_.reserve(2 * n);\n    dfs(0, 0);\n    return ans_;\n  }\n};\n```\n\n## 時間與空間複雜度\n\n77：葉節點數 $\\binom{n}{k}$，內部節點同階（有剪枝時，每個內部節點至少能長出一個葉節點）。每個葉節點複製長度 $k$ 的 `path`。時間 $O\\!\\left(k \\binom{n}{k}\\right)$，空間 $O(k)$ 不計輸出。\n\n**沒有剪枝時**內部節點數退化到 $O(2^n)$ 級（會展開大量長度不足的分支），差距在 $k \\ll n$ 時極大。這是本節剪枝的實際價值。\n\n216：候選只有 $1..9$，搜尋空間上界 $\\binom{9}{k} \\le 126$，實際更小。\n\n22：葉節點數為卡塔蘭數 $C_n = \\frac{1}{n+1}\\binom{2n}{n} = O(4^n / n^{1.5})$，每個葉節點複製長度 $2n$ 的字串，時間 $O\\!\\left(\\frac{4^n}{\\sqrt n}\\right)$。\n\n301：最壞 $O(2^n)$（每個字元刪或不刪），但「必須刪的字元數固定」與去重剪枝讓實際規模小很多。\n\n## 常見錯誤與邊界條件\n\n- 77 的迴圈上界寫成 `n`（沒剪枝，正確但慢）或寫錯成 `n - need`（少一個，漏解）。\n- 湊滿 $k$ 個後忘記 `return`（會繼續往下，多做無用功甚至越界）。\n- 遞迴傳 `start + 1` 而非 `j + 1`（重複）。\n- 22 的右括號條件寫成 `close < n`（生成非法序列）。\n- 22 的終止條件用 `path.size() == 2 * n` 也對，但用 `close == n` 更直接。\n- 216 忘記「數字不重複」或「範圍 $1..9$」。\n- 剪枝條件用了「和超過就剪」卻沒確認候選全為正。\n- 301 對連續相同字元未去重（產生重複答案）。\n- $k = 0$ 時應回傳一個空組合。\n- $k > n$ 時應回傳空。\n\n## 與相似技巧的比較\n\n- 與 4.2 的子集型：組合型 = 子集型 + 大小約束。**約束帶來的剪枝是主要價值**，別只把它當成一個 `if`。\n- 與 4.3 的劃分型：93（固定切成 4 段）就是組合型——從 $n-1$ 個切點中選 3 個，剪枝形式相同。\n- 與 4.5 的排列型：組合不計順序（用 `start` 保證遞增），排列計順序。同樣是「`start` 在不在」的差別。\n- 與 4.6：若候選中有重複元素（40 組合總和 II），要加上 4.6 的去重規則。\n- 與數學公式：只問「有幾種組合」而不問是什麼，直接算 $\\binom{n}{k}$，不要枚舉。**回溯只在需要列出方案時使用。**\n- 1286（字母組合迭代器）要求「按字典序逐個吐出」，可以預先回溯生成全部再逐個回傳（簡單，空間 $O(\\binom{n}{k})$），也可以維護當前組合並實作「下一個組合」的迭代（空間 $O(k)$）。後者本質上是把遞迴展開成迭代，值得練。\n\n## 代表例題\n\n**題號**：22、77、216、301、1286\n\n放在「組合型回溯」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 22 | [括號生成](https://leetcode.cn/problems/generate-parentheses/) |  |\n| 77 | [組合](https://leetcode.cn/problems/combinations/) |  |\n| 216 | [組合總和 III](https://leetcode.cn/problems/combination-sum-iii/) |  |\n| 301 | [刪除無效的括號](https://leetcode.cn/problems/remove-invalid-parentheses/) |  |\n| 1286 | [字母組合迭代器](https://leetcode.cn/problems/iterator-for-combination/) | 1591 |\n\n## 本節重點速查\n\n- 組合型 = 子集型 + 大小約束，方案數 $\\binom{n}{k}$。\n- **核心剪枝：迴圈上界取 `n - need + 1`**，其中 `need = k - path.size()`——這是「剩餘候選不足以湊滿」的必要條件。\n- 剪枝只需必要條件，剪掉的必須確定無解。\n- 22 示範另一種形態：**把可行性條件編進選擇列表**（右括號的條件是 `close < open`），只生成合法方案，其依據是「合法括號序列 ⟺ 每個前綴左 $\\ge$ 右且總數相等」。\n- 湊滿之後記得 `return`。\n",
        },
        {
          id: 49,
          title: "4.5 排列型回溯",
          summary:
            "## 這個技術解決什麼問題\n\n排列型回溯枚舉「**所有元素的所有排列順序**」：46（全排列）、51/52（N 皇后）、37（解數獨）、1718、1307。\n\n與子集／組合的差別只有一句話：**順序有意義**。子集用 `start` 強制下標遞增，把 $k!$ 種順序壓成一種；排列不能用 `start`，每一層都必須從**所有還沒用過的元素**中挑，因此需要一個 `used` 標記。\n\n排列型的方案數是 $n!$，比 $2^n$ 增長得快得多（$n = 10$ 時 $10! \\approx 3.6\\times10^6$，$n = 12$ 時已近 $5\\times10^8$）。因此**排列型題目幾乎都以剪枝為主戰場**——N 皇后與數獨的全部難度都在「如何快速判斷衝突並提早放棄」。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「排列型回溯」想：\n\n- **順序有意義、要列出所有「排法」或「放法」**，例如 46「全排列」、51/52「N 皇后」、267「迴文排列 II」。\n- **每一步要「從還沒用過的元素裡挑一個」，且能回頭挑前面的**——這就是改用 `used` 而非 `start` 的訊號：\n\n:::example 嗅到味道的瞬間：{1,2} 和 {2,1} 是不同答案，且每個元素恰好用一次\n```cpp\n// 嗅到味道的瞬間：{1,2} 和 {2,1} 是不同答案，且每個元素恰好用一次\n// 於是每層從 0 掃起 + used 過濾，而不是 dfs(i+1)\n```\n:::\n\n\n\n- **「把東西一一就位」型問題**，如 N 皇后「每列放一個皇后」、數獨「每格填一個數」，本質都是排列式的逐位賦值 + 合法性剪枝。\n- **暴力生成所有序列再驗證太慢**；排列回溯邊放邊檢查衝突，提早砍掉死路。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我這題在意順序嗎？（在意 → 排列，用 `used` 而不是 `start`）\n2. 撤銷時我有沒有把 `used[i]` 也設回 `false`？（只還原 path 會出錯）\n3. 我有沒有可剪枝的合法性條件，能在放下去之前就擋掉死路？\n\n## 核心想法與直覺\n\n**延伸講解**：[回溯算法套路③排列型回溯＋N 皇后【基礎演算法精講 16】](https://www.bilibili.com/video/BV1mY411D7f6/)\n\n本節要解決的場景是：把一堆元素「排成一個順序」，列出所有可能的排列。例如把 `[1, 2, 3]` 排成 `[1, 2, 3]、[1, 3, 2]、[2, 1, 3]…` 全部六種（46 全排列）；或在棋盤上放皇后讓彼此不攻擊（51 N 皇后），其實也是「每一列選一個欄位、組成一個放法」。這種「順序有意義、要枚舉所有排法」的問題，就是「排列型回溯」。\n\n:::example 排列的關鍵：每層都從頭掃，靠 used 跳過已用過的元素\n回溯（backtracking）就是「先試一種選擇做下去，試完或走不通就退回來、收回剛才那步、再試別的」。排列和組合最大的差別：**排列在意順序，{1,2} 和 {2,1} 是兩個不同答案，所以每一步都要「能回頭挑前面還沒用過的元素」。** 既然能回頭，就不能再用 `start` 限制方向，改用一個 `used` 旗標陣列記住「哪些元素已經在路徑裡了」。\n\n```cpp\n// 排列的關鍵：每層都從頭掃，靠 used 跳過已用過的元素\nfor (int i = 0; i < n; ++i) {\n    if (used[i]) {\n        continue;  // 已經放進 path 的不能再選\n                              // 選 nums[i] ...\n    }\n}\n```\n:::\n\n\n\n:::example 做選擇（標記+入路徑）→ 遞迴 → 撤銷（兩者都還原）\n核心動作仍是「遞迴前做選擇、遞迴後撤銷」，但這次選擇包含「標記 used、加入 path」，撤銷就要把兩者都還原：\n\n```cpp\n// 做選擇（標記+入路徑）→ 遞迴 → 撤銷（兩者都還原）\nused[i] = true;\npath.push_back(nums[i]);\ndfs();\npath.pop_back();\nused[i] = false;  // 現場完全恢復，才能正確試下一個\n```\n:::\n\n\n\n當 `path` 的長度等於元素總數，就湊成了一個完整排列。整體是 `n!` 級別的枚舉，靠剪枝（例如 N 皇后用「同欄、同對角線是否被佔」提前停）大幅加速。\n\n## 狀態／資料結構定義\n\n**46（全排列）：**\n\n- `path`：當前已排好的前綴。\n- `used[i]`：元素 $i$ 是否已在 `path` 中。這是取代 `start` 的東西。\n- `dfs(i)`：已填好前 $i$ 個位置，請枚舉剩下的。終止條件 $i = n$。\n\n**51（N 皇后）：** 一列一列地放，本質上是在枚舉「列 → 行」的排列。\n\n- `col[r]`：第 $r$ 行是否已有皇后。\n- `diag1[r + c]`：主對角線（左上到右下）佔用標記，長度 $2n - 1$。\n- `diag2[r - c + n - 1]`：副對角線佔用標記，長度 $2n - 1$。\n- `dfs(c)`：前 $c$ 列已放好，請放第 $c$ 列。\n\n**37（解數獨）：** 三組標記 `row[9][9]`、`col[9][9]`、`box[9][9]`，配合「找下一個空格」的遞迴。它與 46/51 的差別是**只要找到一組解就回傳**，因此遞迴函式回傳 `bool`，一旦成功就沿途 `return true`，不再回溯。\n\n## 不變量或正確性證明\n\n**46 的不變量：進入 `dfs(i)` 時，`path` 的長度為 $i$，且 `used` 恰好標記 `path` 中出現的那些元素——不多不少。**\n\n「不多不少」是雙向的：`used[j] == true` $\\iff$ 元素 $j$ 在 `path` 中。這由 push 時設 `true`、pop 時設 `false` 的嚴格配對保證。少了任一半都會出錯：漏設 `true` 會產生重複使用同一元素的非法「排列」；漏設 `false` 會讓兄弟分支誤以為元素已用完，漏解。\n\n**不重不漏（排列 ↔ 根到葉路徑的雙射）。** 一條根到葉的路徑記錄了「第 0 位填誰、第 1 位填誰、……」，這正是一個排列的定義。不同路徑在某一層填了不同元素，故是不同排列；同一個排列只對應唯一一條路徑。葉節點數為 $n \\times (n-1) \\times \\cdots \\times 1 = n!$。\n\n**為什麼不能用 `start`。** 若用 `start` 限制「只能挑下標 $\\ge \\text{start}$ 的元素」，則 `[2,1]` 這個排列永遠生成不出來（選了下標 1 之後不能回頭選下標 0）。`start` 的作用正是「消除順序」，那與排列的目標相反。這也解釋了為什麼排列型的分支數是 $n, n-1, \\dots$ 而不是遞減到某個下界——每層都要掃全部 $n$ 個元素（其中已用的被 `used` 跳過）。\n\n**51 的對角線編號為何是 `r + c` 與 `r - c`。**\n\n兩個格子 $(r_1, c_1)$、$(r_2, c_2)$ 在同一條主對角線（往右下走，行與列同增）$\\iff r_1 - c_1 = r_2 - c_2$；在同一條副對角線（往右上走，行減列增）$\\iff r_1 + c_1 = r_2 + c_2$。\n\n證明：沿主對角線移動一步是 $(r, c) \\to (r+1, c+1)$，$r - c$ 不變；反之若 $r_1 - c_1 = r_2 - c_2$，令 $d = r_2 - r_1 = c_2 - c_1$，則從 $(r_1,c_1)$ 走 $d$ 步斜線可達 $(r_2,c_2)$。副對角線同理。\n\n$r + c$ 的範圍是 $[0, 2n-2]$，可直接當下標；$r - c$ 的範圍是 $[-(n-1), n-1]$，**必須加上偏移量 $n-1$** 才能當下標。這個偏移是實作上最常見的越界來源。\n\n有了這三組標記，「這格能不能放」是 $O(1)$ 判斷，取代了 $O(n)$ 的掃描——這是把 $O(n! \\cdot n)$ 降到 $O(n!)$ 的關鍵。\n\n**逐列放置為何不漏解。** 每列恰有一個皇后（否則某兩個皇后同列，衝突），所以「逐列放置、每列選一行」涵蓋了所有合法佈局。這一步把搜尋空間從 $\\binom{n^2}{n}$ 降到 $n^n$，再由 `col` 標記降到 $n!$。**這三個數字的落差全部來自把約束編進枚舉方式，而不是編進事後檢查**——與 4.4 的括號生成是同一個道理。\n\n**37 的「回傳 bool」與純枚舉的差別。** 數獨只要一組解，遞迴應寫成 `bool dfs()`：找到解時 `return true`，呼叫端收到 `true` 就立刻 `return true`，**不執行撤銷**（因為棋盤上的解要保留）。若寫成 `void` 並繼續搜尋，會把已找到的解撤銷掉。這是排列型裡最容易寫錯的一處：**「找一組解」與「列出所有解」的回溯骨架不同，撤銷的時機也不同。**\n\n## 逐步演算法\n\n把排列型回溯拆成四步，逐一對應程式碼：\n\n:::example 狀態：path 是當前排列前綴，used 記錄哪些已用\n**1. 狀態表示：一條路徑 + 一組 used 旗標。** `path` 是目前排好的前綴，`used[i]` 表示第 i 個元素是否已被放入。\n\n```cpp\n// 狀態：path 是當前排列前綴，used 記錄哪些已用\nvector<int> nums, path;\nvector<bool> used;\nvector<vector<int>> ans;\n```\n:::\n\n\n\n:::example 每層從 0 掃起，未用過才選；回來把 used 與 path 都還原\n**2. 更新流程：每層從頭掃、跳過已用、選、遞迴、撤銷。** 注意排列每一層都從 `0` 開始掃（不像組合用 `start`），靠 `used` 過濾。\n\n```cpp\n// 每層從 0 掃起，未用過才選；回來把 used 與 path 都還原\nfor (int i = 0; i < n; ++i) {\n    if (used[i]) {\n        continue;\n    }\n    used[i] = true;\n    path.push_back(nums[i]);\n    dfs();\n    path.pop_back();\n    used[i] = false;\n}\n```\n:::\n\n\n\n:::example path 排滿全部元素 → 一個完整排列\n**3. 答案讀取：路徑排滿就收集。** 長度達到 `n` 代表一個完整排列產生。\n\n```cpp\n// path 排滿全部元素 → 一個完整排列\nif (path.size() == n) {\n    ans.push_back(path);\n    return;\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- N 皇后類問題用「每列放一個」天然避免列衝突，再用三個布林陣列記欄、兩條對角線是否被佔，衝突就剪枝。\n- 解數獨（37）是同型骨架：每個空格枚舉 1~9，檢查行/列/宮是否合法，不合法就回頭。\n- 複雜度最壞 `O(n! * n)`，但好的剪枝（如 N 皇后）能把實際搜尋空間砍得很小。\n\n## C++17 模板\n\n以下用本節代表題 46. 全排列 展示核心寫法。\n\n**完整題目**：給定不重複整數陣列，列出所有可能排列。\n\n:::example 回溯 DFS：每層從未使用的元素中選一個加入 path\n**線索**：題目要求列出所有方案、排列、組合或路徑；需要逐步選擇並在返回時撤銷。\n\n```cpp\nclass Solution {\npublic:\n    vector<vector<int>> permute(vector<int>& nums) {\n            vector<vector<int>> ans;\n            vector<int> path;                // 當前排列路徑\n            vector<char> used(nums.size());  // 標記各元素是否已加入路徑\n            // 回溯 DFS：每層從未使用的元素中選一個加入 path\n            function<void()> dfs = [&]() {\n                    if (path.size() == nums.size()) {\n                            ans.push_back(path);  // 路徑長度等於 nums 時得到完整排列\n                            return;\n                    }\n                    for (int i = 0; i < (int)nums.size(); ++i) {\n                            if (used[i]) {\n                                    continue;  // 已使用的元素跳過\n                            }\n                            used[i] = true;\n                            path.push_back(nums[i]);\n                            dfs();\n                            // 回溯：撤銷選擇，讓後續元素有機會被選入\n                            path.pop_back();\n                            used[i] = false;\n                    }\n            };\n            dfs();\n            return ans;\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- 回溯維護路徑與可選集合；遞迴返回前恢復現場，確保下一個分支看到同樣的狀態。\n- `path` 保存目前已做的選擇，`used` 或 `start` 控制下一步可選集合。\n- 每次遞迴後都要撤銷選擇，讓下一個分支看到相同的現場。\n\n## 時間與空間複雜度\n\n46：葉節點 $n!$，每個葉節點複製長度 $n$ 的 `path`，內部節點的迴圈總量同階。時間 $O(n \\cdot n!)$，空間 $O(n)$ 不計輸出。\n\n51：搜尋樹的上界是 $n!$（每列選一行、行不重複），但對角線約束砍掉極多分支，實際節點數遠小於 $n!$（$n = 8$ 時只有約 2000 個節點，而 $8! = 40320$）。每個葉節點構造棋盤是 $O(n^2)$。**這是「剪枝改變的是常數與實際規模，不改變理論上界」的典型例子**——寫複雜度時應說「$O(n!)$ 上界，實際遠低」。\n\n52 只數個數，省掉 $O(n^2)$ 的棋盤構造，快一個數量級。\n\n37：理論上界是指數級（每格 9 種填法），實際靠三組標記與「優先填候選最少的格子」（最小剩餘值啟發式）在毫秒級完成。\n\n$n$ 的規模：$n! $ 在 $n = 10$ 時 $3.6\\times10^6$（可行），$n = 12$ 時 $4.8\\times10^8$（勉強），$n = 13$ 起不可行。**看到 $n \\le 10$ 左右且要求「所有順序」，就是排列型的訊號。**\n\n## 常見錯誤與邊界條件\n\n- push 後必須 pop，標記後必須復原。\n- 排列與組合的 start/used 語意不同。\n- 剪枝條件要保證不漏解。\n- 答案要拷貝 path，而不是存引用。\n\n## 常見變形\n\n- 排序後用 `i > start && nums[i] == nums[i - 1]` 去重。\n- 用 bitmask 表示已選集合。\n- 可與 DFS、DP 記憶化結合剪枝。\n\n## 與相似技巧的比較\n\n- 與 4.2／4.4：唯一差別是「有沒有 `start`」。有 `start` → 不計順序（子集／組合）；用 `used` → 計順序（排列）。\n- 交換法（`swap(nums[i], nums[j])` 後遞迴再換回）是排列的另一種寫法，省掉 `used` 陣列與 `path`，常數更小。但它的**生成順序不是字典序**，且在有重複元素時去重規則完全不同（不能沿用 4.6 的寫法），初學建議用 `used` 版。\n- 與 `std::next_permutation`：若只需按字典序逐個產生排列，標準庫的迭代版更短且空間 $O(1)$。**能用它就用它**，回溯的價值在於可以剪枝。\n- 與狀壓 DP：$n!$ 個排列中，許多不同前綴會導向「已用集合相同」的等價狀態。若題目只問**最佳值或方案數**而非列出所有排列，就該把狀態壓成 `mask`（已用集合）並記憶化，$O(n! ) \\to O(2^n \\cdot n)$。2850（石頭分散）、1307 都可以這樣優化。**這是排列型最重要的一條升級路徑。**\n- 與 3376、1718 這類「按某順序貪心 + 回溯驗證」的題目：搜尋順序（先試哪個分支）會大幅影響實際速度。優先嘗試「最可能成功」或「選項最少」的分支，是搜尋題的通用加速手段，見 4.7。\n\n## 代表例題\n\n**題號**：37、46、51、52、267\n\n放在「排列型回溯」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 37 | [解數獨](https://leetcode.cn/problems/sudoku-solver/) |  |\n| 46 | [全排列](https://leetcode.cn/problems/permutations/) |  |\n| 51 | [N 皇后](https://leetcode.cn/problems/n-queens/) |  |\n| 52 | [N 皇后 II](https://leetcode.cn/problems/n-queens-ii/) |  |\n| 267 | [迴文排列 II](https://leetcode.cn/problems/palindrome-permutation-ii/)（Premium） |  |\n| 3437 | [全排列 III](https://leetcode.cn/problems/permutations-iii/) |  |\n| 3799 | [單詞方塊 II](https://leetcode.cn/problems/word-squares-ii/) | 1606 |\n| 3376 | [破解鎖的最少時間 I](https://leetcode.cn/problems/minimum-time-to-break-locks-i/) | 1793 |\n| 2850 | [將石頭分散到網格圖的最少移動次數](https://leetcode.cn/problems/minimum-moves-to-spread-stones-over-grid/) | 2001 |\n| 1718 | [構建字典序最大的可行序列](https://leetcode.cn/problems/construct-the-lexicographically-largest-valid-sequence/) | 2080 |\n| 1307 | [口算難題](https://leetcode.cn/problems/verbal-arithmetic-puzzle/) | 2250 |\n\n## 本節重點速查\n\n- 排列 = 沒有 `start`、改用 **`used`**，不變量是「`used` 恰好標記 `path` 中的元素，不多不少」，靠 push/pop 與 true/false 嚴格配對。\n- 方案數 $n!$，$n \\le 10$ 左右才可行。\n- N 皇后逐列放置，同一主對角線 $\\iff r - c$ 相同、同一副對角線 $\\iff r + c$ 相同，**`r - c` 當下標要加偏移 $n-1$**。\n- 把約束編進枚舉方式（逐列、三組標記）而非事後檢查，是效率的來源。\n- **「找一組解」要用 `bool` 回傳並在成功時不撤銷**，與「列出所有解」的骨架不同。\n- 只問最佳值時改用狀壓 DP。",
        },
        {
          id: 50,
          title: "4.6 有重複元素的回溯",
          summary:
            "## 這個技術解決什麼問題\n\n前面幾節的不重不漏，全部建立在「同一層的選項兩兩互異」這個前提上。一旦輸入含重複元素（`[1,1,2]`），前提破裂：同一層會出現兩個相同的選項，於是同一個答案被生成多次。40（組合總和 II）、47（全排列 II）、90（子集 II）、491（非遞減子序列）、1079（活字印刷）都是這個形狀。\n\n最省事的做法是「全部生成後用 `set` 去重」，但那要付出「生成重複答案」的指數級浪費加上比較的開銷。本節要做的是**在搜尋時就不生成重複**——代價只是一次排序加一行 `if`。\n\n思路統一成一句話：**為每個答案指定一個唯一的「標準生成方式」，只允許沿著標準方式走，其餘剪掉。**\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「有重複元素的回溯」想：\n\n- **是組合 / 排列 / 子集題，但輸入「含有重複元素」，且要求結果「不能有重複答案」**，例如 40「組合總和 II」、47「全排列 II」、90「子集 II」、1079「活字印刷」。\n- **你發現普通回溯產生了兩個一模一樣的答案**——這就是要加同層去重的訊號：\n\n:::example 嗅到味道的瞬間：輸出裡冒出重複的 [1,2,2] / [2,2]\n```cpp\n// 嗅到味道的瞬間：輸出裡冒出重複的 [1,2,2] / [2,2]\n// 對策：先 sort，再在 for 裡加「i>start && nums[i]==nums[i-1] → continue」\n```\n:::\n\n\n\n- **題目強調「結果集中不含重複」或「子集 / 排列 II」這種帶 II 的進階版**，幾乎都在考這個去重技巧。\n- **491「非遞減子序列」這種「不能排序」的變形**：仍是同層去重，只是改用「本層 set」記錄用過的值。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我有沒有先 `sort`（或用本層 set）讓相同值可被偵測？\n2. 我的去重條件針對的是「同一層」嗎？（不是把所有相同值都砍掉）\n3. 排列題我用的是 `!used[i - 1]` 的條件，組合題用的是 `i > start` 的條件嗎？\n\n## 核心想法與直覺\n\n本節要解決的場景是：做組合 / 排列 / 子集的回溯，但**輸入裡有重複的元素**，例如 `[1, 2, 2]`。如果照普通寫法枚舉，你會得到兩個一模一樣的 `[1, 2]`，因為兩個 `2` 被當成不同的東西各選了一次。本節「有重複元素的回溯」就是專門處理「如何不產生重複答案」。\n\n先理解問題在哪。重複答案來自「在同一個位置（同一層遞迴），用兩個值相同的元素各試了一次」。例如第一層既選了第一個 `2`、又選了第二個 `2`，兩條分支長得完全一樣。關鍵洞察：**重複只發生在「同一層」選了相同的值；不同層選到相同值是合法的（例如答案真的需要兩個 2）。**\n\n:::example 第一步：排序，讓相同的值相鄰，後面才好判斷「同層重複」\n解法分兩步。第一步先「排序」，把相同的值聚在一起，方便偵測。\n\n```cpp\n// 第一步：排序，讓相同的值相鄰，後面才好判斷「同層重複」\nsort(nums.begin(), nums.end());  // [2,1,2] -> [1,2,2]\n```\n:::\n\n\n\n:::example 同層去重：i>start 且和左鄰相同 → 這個值這一層已試過，跳過\n第二步是「同層去重」：在 `for` 迴圈裡，如果目前這個值和「同一層剛剛試過的前一個值」相同，就跳過它。\n\n```cpp\n// 同層去重：i>start 且和左鄰相同 → 這個值這一層已試過，跳過\nif (i > start && nums[i] == nums[i - 1]) {\n    continue;\n}\n```\n:::\n\n\n\n`i > start` 很重要：它代表「這不是本層第一個候選」。本層第一次遇到某個值要正常使用；只有「同一層第二次以後遇到相同值」才跳過。這樣每個不同的值在同一層只會被當作分支一次，重複答案就消失了。其餘骨架（做選擇、遞迴、撤銷）和一般回溯完全相同。\n\n## 狀態／資料結構定義\n\n前提：**先排序**，讓相等元素在下標上相鄰。排序是所有去重規則的共同基礎。\n\n**子集／組合型（90、40）：**\n\n- 沿用 4.2 視角二的 `dfs(start)`。\n- 去重規則：`if (j > start && a[j] == a[j - 1]) continue;`\n- 條件是 `j > start`，**不是 `j > 0`**。這個差別是本節的核心。\n\n**排列型（47）：**\n\n- 沿用 4.5 的 `used[]`。\n- 去重規則：`if (j > 0 && a[j] == a[j - 1] && !used[j - 1]) continue;`\n\n**491（非遞減子序列，不可排序）：**\n\n- 題目要求保持原順序，**不能排序**，所以上面的規則失效。\n- 改用「同一層用過的值集合」：在每個 `dfs` 內開一個區域的 `unordered_set<int> seen`，同一層遇到用過的值就跳過。這是最通用但常數最大的做法。\n\n## 不變量或正確性證明\n\n**子集型規則的正確性。** 設排序後有一段相等元素 $a_p = a_{p+1} = \\cdots = a_q$。從這段裡選 $t$ 個，不同的下標選法有 $\\binom{q-p+1}{t}$ 種，但它們對應**同一個多重集**，因此只該保留一種。\n\n規則 `j > start && a[j] == a[j-1] → skip` 的效果是：在同一層（同一個 `start`）中，這段相等元素只有**第一個**（$j = \\text{start}$，或前一個元素與它不等的那個 $j$）會被選；後續的 $j$ 全部跳過。而遞迴進入 `dfs(j + 1)` 之後 `start` 變成 $j+1$，此時 $j+1$ 位置的元素若仍相等，它的 `j > start` 為假，**可以被選**。\n\n因此被允許的選法恰是「**從這段中取最前面的 $t$ 個**」，這是每個多重集的唯一標準表示。故每個答案生成恰一次：\n\n- *不重*：兩條不同路徑對應的下標序列不同；由於只允許取前綴式的下標，不同下標序列必對應不同多重集。\n- *不漏*：任一多重集都有其標準表示（各段取最前面若干個），而該表示對應的路徑沒有被規則剪掉。\n\n**為什麼是 `j > start` 而不是 `j > 0`。** 用 `j > 0` 會把「$j$ 恰為 `start` 且 $a_j = a_{j-1}$」的情形也剪掉——但那個 $a_{j-1}$ 是**上一層剛選走的元素**，不是同一層的兄弟。剪掉它就無法選出含兩個相同元素的答案，`[1,1]` 這種解會消失。**`j > start` 精確地表達「同一層的第二個及之後的相同選項」**，一個字元之差就是漏解與正確的差別。\n\n**排列型規則的正確性。** 對相等元素，我們指定的標準生成方式是「**按下標由小到大依次使用**」：即 $a_{j-1}$ 必須在 $a_j$ 之前被放入 `path`。\n\n- 條件 `a[j] == a[j-1] && !used[j-1]` 表示「要放 $a_j$，但與它相等的前一個 $a_{j-1}$ 還沒被用」——這違反標準順序，剪掉。\n- 反之若 `used[j-1]` 為真，表示 $a_{j-1}$ 已在 `path` 中，現在放 $a_j$ 符合標準順序，允許。\n\n*不重*：每個排列（作為值序列）在標準順序下只有唯一一種下標指派方式（相等元素按下標遞增依次填入各自出現的位置），故只被生成一次。\n*不漏*：任一值序列的標準下標指派都不會被剪掉。\n\n**`!used[j-1]` 與 `used[j-1]` 都能去重，但前者更快。** 若寫成 `used[j-1]` 為剪枝條件，等於規定「相等元素按下標由大到小使用」，同樣是一種標準順序，答案也對。差別在於剪枝發生的時機：`!used[j-1]` 在**這一段相等元素的第一個之後就立刻剪**，被剪的子樹更靠上、更大；`used[j-1]` 則要等到已經深入才剪。實測差距明顯。**兩者都對，但預設寫 `!used[j-1]`。**\n\n**491 為何不能排序。** 題目要求子序列保持原陣列順序，排序會改變答案本身。此時「相等元素相鄰」的前提不成立，前述規則失效。改用「同一層 `seen` 集合」的規則，其正確性依據是：同一層中選了值相同的兩個不同下標，會生成完全相同的後續子樹（因為後續只依賴「已選序列的值」與「起始下標」——注意這裡起始下標其實不同，所以嚴格說是「後者的子樹是前者的子集」），故只保留第一個即可，不會漏解。**這條論證比排序版弱，成立的原因是「更靠後的起始下標所能生成的答案是更靠前者的子集」**，遇到不符合這個性質的題目就不能照搬。\n\n## 逐步演算法\n\n把有重複元素的回溯拆成四步，逐一對應程式碼：\n\n:::example 狀態：先排序的 nums、當前 path；組合用 start，排列額外用 used\n**1. 狀態表示：排好序的輸入 + 路徑（組合題加 start，排列題加 used）。** 排序是這類題的前置動作。\n\n```cpp\n// 狀態：先排序的 nums、當前 path；組合用 start，排列額外用 used\nvector<int> nums, path;  // sort 過\nvector<vector<int>> ans;\n```\n:::\n\n\n\n:::example 組合/子集：i>start 且與左鄰相同 → 同層重複，跳過\n**2. 更新流程（組合 / 子集版）：同層相同值跳過。** 用 `start` 控制只往後挑，再加一行同層去重。\n\n```cpp\n// 組合/子集：i>start 且與左鄰相同 → 同層重複，跳過\nfor (int i = start; i < nums.size(); ++i) {\n    if (i > start && nums[i] == nums[i - 1]) {\n        continue;\n    }\n    path.push_back(nums[i]);\n    dfs(i + 1);\n    path.pop_back();\n}\n```\n:::\n\n\n\n:::example 排列：左鄰相同且左鄰本層未用(used[i-1]==false) → 跳過，避免重複排列\n**3. 更新流程（排列版）：用 used 判斷同層重複。** 排列每層從 0 掃，去重條件要看「左鄰那個相同值是否本層尚未使用」。\n\n```cpp\n// 排列：左鄰相同且左鄰本層未用(used[i-1]==false) → 跳過，避免重複排列\nif (i > 0 && nums[i] == nums[i - 1] && !used[i - 1]) {\n    continue;\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 一定要先 `sort`，去重條件才成立（它依賴「相同值相鄰」）。\n- 491「非遞減子序列」不能排序（會破壞原順序），這時改用「本層一個 `unordered_set` 記錄這層用過哪些值」來去重。\n- 複雜度仍是指數級，但去重把重複分支砍掉，實際輸出與搜尋量都明顯變小。\n\n## C++17 模板\n\n```cpp\n// 子集 II（LeetCode 90）：排序 + 同層跳過重複。\nclass Solution {\n  std::vector<int> nums_, path_;\n  std::vector<std::vector<int>> ans_;\n\n  void dfs(int start) {\n    ans_.push_back(path_);\n    for (int j = start; j < static_cast<int>(nums_.size()); ++j) {\n      // j > start：只跳過「同一層」的第二個及之後的相同選項\n      if (j > start && nums_[j] == nums_[j - 1]) { continue; }\n      path_.push_back(nums_[j]);\n      dfs(j + 1);\n      path_.pop_back();\n    }\n  }\n\n public:\n  std::vector<std::vector<int>> subsetsWithDup(std::vector<int>& nums) {\n    nums_ = nums;\n    std::sort(nums_.begin(), nums_.end());   // 去重的前提\n    dfs(0);\n    return ans_;\n  }\n};\n```\n\n```cpp\n// 全排列 II（LeetCode 47）：排序 + 相等元素按下標遞增使用。\nclass Solution {\n  std::vector<int> nums_, path_;\n  std::vector<bool> used_;\n  std::vector<std::vector<int>> ans_;\n\n  void dfs() {\n    if (path_.size() == nums_.size()) {\n      ans_.push_back(path_);\n      return;\n    }\n    for (int j = 0; j < static_cast<int>(nums_.size()); ++j) {\n      if (used_[j]) { continue; }\n      // 與前一個相等且前一個還沒用 → 違反「按下標遞增使用」的標準順序\n      if (j > 0 && nums_[j] == nums_[j - 1] && !used_[j - 1]) { continue; }\n      used_[j] = true;\n      path_.push_back(nums_[j]);\n      dfs();\n      path_.pop_back();\n      used_[j] = false;\n    }\n  }\n\n public:\n  std::vector<std::vector<int>> permuteUnique(std::vector<int>& nums) {\n    nums_ = nums;\n    std::sort(nums_.begin(), nums_.end());\n    used_.assign(nums_.size(), false);\n    dfs();\n    return ans_;\n  }\n};\n```\n\n## 時間與空間複雜度\n\n排序 $O(n \\log n)$，被後面的指數項吸收。\n\n90：不同子集的個數是 $\\prod_i (c_i + 1)$，其中 $c_i$ 是第 $i$ 種值的出現次數。全部互異時退化為 $2^n$；全部相同時只有 $n+1$ 個。時間 $O(n \\cdot \\prod_i (c_i + 1))$。\n\n47：不同排列的個數是多重集排列數 $\\dfrac{n!}{\\prod_i c_i!}$。時間 $O\\!\\left(n \\cdot \\dfrac{n!}{\\prod_i c_i!}\\right)$。\n\n**去重規則的價值就是把複雜度從「所有下標選法」降到「所有不同答案」。** 以 `[1,1,1,1,1,1,1,1]`（8 個 1）為例，$8! = 40320$ 對上 1 個——這不是常數優化。\n\n491 的 `seen` 版每層多一個雜湊表，常數較大；若值域小可改用固定大小的布林陣列或 `bitset`。\n\n空間：$O(n)$ 不計輸出。\n\n## 常見錯誤與邊界條件\n\n- **忘記排序**（規則完全失效，仍會產生重複）。\n- 子集型寫成 `j > 0`（漏掉含重複元素的答案，例如 `[1,1]`）。\n- 排列型寫成 `!used[j]`（拼字錯，變成恆真）。\n- 排列型漏掉 `j > 0` 的邊界檢查（`nums_[-1]` 越界）。\n- 491 誤加排序（改變題意）。\n- 491 的 `seen` 宣告成全域或成員（跨層污染，會漏解——它必須是**每層各一份**的區域變數）。\n- 40 的「每個元素只能用一次」與 39 的「可重複使用」混淆（前者傳 `j + 1`，後者傳 `j`）。\n- 1079 需要的是「不同排列的個數」而非列出，用計數版回溯或多重集排列公式更快。\n- 輸入為空。\n\n## 與相似技巧的比較\n\n- 與 4.2／4.4／4.5：本節是它們三個的補丁，骨架完全不變，只多一行 `if` 與一次排序。**先把無重複版寫對，再套規則。**\n- **「換一種枚舉方式」常常比去重規則更清爽**：把輸入整理成「值 → 出現次數」的計數表，然後對每個不同的值枚舉「用幾個」（$0$ 到 $c_i$）。這樣同一層的選項天然互異，完全不需要去重規則。1079（活字印刷）用計數版寫出來比用 47 的規則短且快。遇到重複元素多的題目，優先考慮這個角度。\n- 與事後 `set` 去重：正確但慢。它的問題不只是比較開銷，而是**仍然生成了指數多的重複答案**——複雜度沒有降。只有在去重規則實在寫不出來時才用。\n- 與 3646（下一個特殊迴文數）：這類「按位構造且各數字有使用次數上限」的題目，計數版枚舉幾乎是唯一合理的寫法。\n- 與 2014（重複 K 次的最長子序列）：去重之外還要配合「候選子序列按長度與字典序降序枚舉，第一個可行的就是答案」的搜尋順序設計，屬於 4.7 的範疇。\n\n## 代表例題\n\n**題號**：40、47、90、491、1079\n\n放在「有重複元素的回溯」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 40 | [組合總和 II](https://leetcode.cn/problems/combination-sum-ii/) |  |\n| 47 | [全排列 II](https://leetcode.cn/problems/permutations-ii/) |  |\n| 90 | [子集 II](https://leetcode.cn/problems/subsets-ii/) |  |\n| 491 | [非遞減子序列](https://leetcode.cn/problems/non-decreasing-subsequences/) |  |\n| 1079 | [活字印刷](https://leetcode.cn/problems/letter-tile-possibilities/) | 1741 |\n| 2014 | [重複 K 次的最長子序列](https://leetcode.cn/problems/longest-subsequence-repeated-k-times/) | 2558 |\n| 3646 | [下一個特殊迴文數](https://leetcode.cn/problems/next-special-palindrome-number/) | 2445 |\n\n## 本節重點速查\n\n- 統一思路：**為每個答案指定唯一的標準生成方式，只走標準路徑**。\n- 前提是**先排序**。\n- 子集／組合：`if (j > start && a[j] == a[j-1]) continue;`——**是 `j > start` 不是 `j > 0`**，因為要剪的是同層兄弟，不是上一層剛選走的元素。\n- 排列：`if (j > 0 && a[j] == a[j-1] && !used[j-1]) continue;`——規定相等元素按下標遞增使用；\n- `used[j-1]` 版也對但剪得晚。\n- 不能排序時（491）改用**每層一份**的 `seen` 集合。\n- 重複元素多時，改用「值 → 用幾個」的計數式枚舉，天然無重複，通常更好寫。\n",
        },
        {
          id: 51,
          title: "4.7 搜尋",
          summary:
            "## 這個技術解決什麼問題\n\n前面幾節的回溯有固定的形狀（子集、劃分、組合、排列）。本節收的是**沒有固定形狀的搜尋**：在網格上找單詞（79、212）、把火柴分成四堆（473）、給表達式加運算子（282）、24 點（679）、貼紙拼詞（691）。\n\n它們的共同點是：狀態不是一個簡單的下標，而是「棋盤／剩餘資源／已用集合」；搜尋空間巨大到必須靠剪枝才能通過。因此本節的重點不在骨架（骨架就是 4.1 的回溯），而在**四件讓搜尋真的跑得完的事**：狀態設計、可行性剪枝、搜尋順序、記憶化。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「搜尋」想：\n\n- **要在網格 / 圖 / 構造空間裡「找一條路徑或一個解」，可能要試很多分岔**，例如 79「單詞搜尋」、212「單詞搜尋 II」、126「單詞接龍 II」、247「中心對稱數 II」、LCP 58「積木拼接」。\n- **走某一步會「佔用資源」，走完要釋放**——這就是「進來標記、出去還原」的訊號：\n\n:::example 嗅到味道的瞬間：你需要「這格暫時不能再走」，但回頭後又要放開\n```cpp\n// 嗅到味道的瞬間：你需要「這格暫時不能再走」，但回頭後又要放開\n// visited[x][y]=true ... 遞迴 ... visited[x][y]=false\n```\n:::\n\n\n\n- **每一步只能在「相鄰 / 合法的下一狀態」中選擇**（上下左右、字典裡差一個字母、下一塊積木的接法），是典型的圖上 DFS。\n- **暴力列出所有路徑再篩選會爆炸**；搜尋型回溯邊走邊用條件剪枝，提早砍掉死路。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的「目前局面」由哪些變數描述？（座標？進度？已訪問集合？）\n2. 從一個局面能走到哪些「下一個局面」？合法條件是什麼？\n3. 我有沒有「進來標記、回去還原」，避免一條路把資源佔死？\n\n## 核心想法與直覺\n\n本節要解決的場景是：答案藏在一個「會分岔的局面」裡，你必須一步步往下試、走到底看看通不通，不通就退回來換條路。這種「在格子上找一條路徑」或「沿著狀態圖深入探索」的問題，就是本節的「搜尋」——通常指 DFS（深度優先搜尋）型的回溯。\n\n:::example 四方向探索的骨架：對每個鄰格遞迴\n典型例子是 79「單詞搜尋」：在字母網格裡找一條相鄰路徑拼出指定單字。從某格出發，往上下左右四個方向各試一次，每走一格就比對單字的下一個字母，對不上就退回。回溯（backtracking）的精髓在這：**走進一格時把它標記成「已訪問」避免重複踩；退出時把標記清掉，讓別條路徑還能用這格。**\n\n```cpp\n// 四方向探索的骨架：對每個鄰格遞迴\nint dx[4] = {0, 0, 1, -1}, dy[4] = {1, -1, 0, 0};\nfor (int d = 0; d < 4; ++d) {\n    int nx = x + dx[d], ny = y + dy[d];\n    // 若 (nx,ny) 在界內、未訪問、且符合條件 → 往那走\n}\n```\n:::\n\n\n\n:::example 進格子前標記，遞迴探索，回來時還原（這格能被其他路徑再用）\n最關鍵的就是「進來標記、出去還原」這一對動作，否則一條路會把格子佔死，別條路就走不了：\n\n```cpp\n// 進格子前標記，遞迴探索，回來時還原（這格能被其他路徑再用）\nvisited[x][y] = true;\nbool found = dfs(x, y, k + 1);  // 嘗試比對單字第 k+1 個字母\nvisited[x][y] = false;          // 恢復現場\n```\n:::\n\n\n\n走到「拼完整個單字」就是成功；四個方向都失敗就回頭。這是指數級的探索，靠「字母不符就立刻停」這種剪枝大幅縮短。126「單詞接龍 II」、247「中心對稱數 II」也都是同一套「深入—碰壁—回退」的搜尋骨架，只是局面從網格換成了字典圖或數字構造。\n\n## 狀態／資料結構定義\n\n**79（單詞搜尋）：**\n- `dfs(r, c, k)`：當前站在格子 $(r, c)$，要匹配 `word[k]`。\n- `vis[r][c]`：**當前路徑**上已走過的格子。注意它的語意是「路徑」而不是「曾經訪問過」——這是搜尋與 BFS/連通性 DFS 的分水嶺。\n- 也可以不開 `vis`，改成把 `board[r][c]` 暫時改成 `'#'`，回溯時改回來（省一個陣列）。\n\n**473（火柴拼正方形）：**\n- `side[4]`：四條邊當前的長度。\n- `dfs(i)`：前 $i$ 根火柴已分配，請分配第 $i$ 根。\n- 這是「把物品分到 $k$ 個箱子」的通用形狀，狀態是**箱子的當前狀態**而非「選了哪些物品」。\n\n**691（貼紙拼詞）／1601（換樓請求）：**\n- 狀態壓縮：`mask` 表示目標字串中哪些位置已被覆蓋。狀態數 $2^n$，適合記憶化。\n\n**212（單詞搜尋 II）：**\n- 把所有待搜單詞建成 Trie，搜尋時同步在 Trie 上走。**狀態多一維「當前 Trie 節點」**，讓「所有單詞一起搜」成為可能。\n\n## 不變量或正確性證明\n\n**79 的不變量：進入 `dfs(r, c, k)` 時，`vis` 恰好標記了從起點到 $(r,c)$ 的這條路徑上的所有格子（含 $(r,c)$），且這條路徑對應 `word[0..k]`。**\n\n「恰好」再次是雙向的，靠設 `true` 與設 `false` 的嚴格配對維持。**與圖的連通性 DFS 的差別在這裡最關鍵**：連通性 DFS 的 `vis` 一旦設 `true` 就永不還原（每個節點只訪問一次，$O(V+E)$）；路徑搜尋的 `vis` 必須還原，因為**同一個格子可以出現在不同的路徑上**。把兩者搞混是這類題最常見的錯誤：不還原會漏解，而且錯得很隱蔽（部分測資會過）。\n\n這也解釋了複雜度的落差：連通性 DFS 是多項式的，路徑搜尋是指數的。**`vis` 還不還原，決定了演算法是多項式還是指數。**\n\n**剪枝的通用要求：被剪的分支必須確定無解。** 這是必要條件，不是充分條件。逐一檢查本節的常見剪枝：\n\n- 79 的 `board[r][c] != word[k]` → 這條路徑已經不匹配，往下不可能匹配。**成立**。\n- 473 的「單根火柴長度 > 邊長」→ 它無論放哪條邊都超長。**成立**。\n- 473 的「總和不能被 4 整除」→ 顯然無解。**成立**。\n- 473 的「當前邊加上這根會超長就跳過」→ 該分支無解。**成立**（依賴火柴長度為正）。\n- 473 的「若把火柴放進一條空邊失敗，就不必再試其他空邊」→ **這條需要論證**：所有空邊是對稱的（狀態完全相同），放進任一條空邊得到的子問題同構，故一條失敗則全部失敗。這是**對稱性剪枝**，威力極大但必須確認對稱性真的成立。\n- 473 的「若某條邊剛好填滿後續失敗，直接回傳」→ 這條**不成立**，不能亂加。\n\n**搜尋順序不影響正確性，但影響數量級。** 473 先把火柴**由大到小排序**再搜：大火柴的可放位置少，衝突提早暴露，錯誤分支在很淺的層就被砍掉。這不改變答案，只改變遞迴樹的形狀。同理 79 可以先統計 `word` 的首尾字元在棋盤上的出現次數，**若尾字元更少就把 `word` 反轉再搜**——起點少一個數量級，總時間就少一個數量級。\n\n一般原則：**優先展開「選項最少」或「最可能失敗」的分支**（最小剩餘值啟發式）。這在數獨、N 皇后、火柴、24 點上都適用。\n\n**何時該加記憶化。** 回溯與記憶化搜尋的界線是：**不同的路徑是否會到達「後續行為完全相同」的狀態**。\n\n- 79：不會。狀態含整條路徑（`vis`），兩條不同路徑即使走到同一格，可用的後續格子也不同。**不能記憶化**。\n- 691：會。狀態只有 `mask`（已覆蓋哪些位置），到達同一個 `mask` 之後的最少貼紙數與怎麼到達無關。**應該記憶化**，$O(2^n \\cdot m \\cdot n)$。\n- 1601：`dfs(i, mask...)`，同理。\n\n判準寫成一句話：**若「未來」只依賴一個小的狀態摘要，而不依賴完整路徑，就記憶化；否則就是純回溯。** 這與 4.1 提到的「DP vs 回溯」是同一條線，只是這裡的狀態通常需要壓縮才夠小。\n\n**212 用 Trie 的收穫。** 樸素做法對每個單詞各跑一次 79，是 $O(W \\cdot mn \\cdot 4^L)$。建 Trie 後只跑一次 DFS，在每個格子同時推進所有單詞的匹配：若當前 Trie 節點沒有對應子節點，**所有以此為前綴的單詞一起被剪掉**。這是把「$W$ 次獨立搜尋」合併成「一次搜尋，共享前綴」——與回溯本身「共享前綴」的精神一致，只是共享的對象從路徑換成了模式串。實作上還要記得**找到一個單詞後把它從 Trie 上摘掉**（或標記），避免重複輸出。\n\n## 逐步演算法\n\n把搜尋型回溯拆成四步，逐一對應程式碼：\n\n:::example 狀態：座標 (x,y)、比對進度 k、訪問標記 visited\n**1. 狀態表示：目前位置 + 進度 + 訪問標記。** 在網格題裡是 `(x, y)` 座標、已比對到單字第幾個字母、以及 `visited` 陣列。\n\n```cpp\n// 狀態：座標 (x,y)、比對進度 k、訪問標記 visited\nvector<vector<char>> board;\nstring word;\nvector<vector<bool>> visited;\n```\n:::\n\n\n\n:::example 標記當前格 → 探索四鄰 → 還原標記\n**2. 更新流程：標記、向鄰居遞迴、還原。** 從當前格往四方向試，符合條件才深入；無論成功與否，回來都要清掉標記。\n\n```cpp\n// 標記當前格 → 探索四鄰 → 還原標記\nvisited[x][y] = true;\nfor (int d = 0; d < 4; ++d) {\n    int nx = x + dx[d], ny = y + dy[d];\n    if (ok(nx, ny) && board[nx][ny] == word[k + 1] && dfs(nx, ny, k + 1)) {\n        return true;\n    }\n}\nvisited[x][y] = false;  // 恢復現場，讓其他路徑可用此格\n```\n:::\n\n\n\n:::example 比對到單字最後一個字母 → 找到一條合法路徑\n**3. 答案讀取：抵達終止狀態即成功。** 比對到單字最後一個字母（或到達目標局面），就回傳成功 / 收集這條路徑。\n\n```cpp\n// 比對到單字最後一個字母 → 找到一條合法路徑\nif (k == (int)word.size() - 1) {\n    return true;\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 越界、已訪問、字母不符都要在「踏進去之前」擋掉，這是主要剪枝。\n- 212「單詞搜尋 II」要同時找很多單字，會把字典建成 Trie（字典樹）一邊走網格一邊匹配，避免每個單字各掃一遍。\n- 複雜度最壞是指數級（每格約四個分支），但字母 / 邊界剪枝讓實際分支遠少於此。\n\n## C++17 模板\n\n```cpp\n// 單詞搜尋（LeetCode 79）：路徑搜尋，vis 必須還原。\nclass Solution {\n  std::vector<std::vector<char>> board_;\n  std::string word_;\n  int m_ = 0, n_ = 0;\n\n  bool dfs(int r, int c, int k) {\n    if (r < 0 || r >= m_ || c < 0 || c >= n_) { return false; }\n    if (board_[r][c] != word_[k]) { return false; }        // 剪枝：已不匹配\n    if (k + 1 == static_cast<int>(word_.size())) { return true; }\n\n    char saved = board_[r][c];\n    board_[r][c] = '#';                                    // 標記在「當前路徑」上\n    static constexpr int kDir[4][2] = {{0, 1}, {0, -1}, {1, 0}, {-1, 0}};\n    for (auto [dr, dc] : kDir) {\n      if (dfs(r + dr, c + dc, k + 1)) { return true; }      // 找到就一路回傳\n    }\n    board_[r][c] = saved;                                  // 還原：換一條路徑還能再走\n    return false;\n  }\n\n public:\n  bool exist(std::vector<std::vector<char>>& board, std::string word) {\n    board_ = board;\n    m_ = board_.size();\n    n_ = board_[0].size();\n    // 搜尋順序優化：若尾字元比首字元罕見，反轉 word 可大幅減少起點數\n    long long headCnt = 0, tailCnt = 0;\n    for (const auto& row : board_) {\n      for (char ch : row) {\n        headCnt += (ch == word.front());\n        tailCnt += (ch == word.back());\n      }\n    }\n    word_ = (tailCnt < headCnt) ? std::string(word.rbegin(), word.rend()) : word;\n\n    for (int r = 0; r < m_; ++r) {\n      for (int c = 0; c < n_; ++c) {\n        if (dfs(r, c, 0)) { return true; }\n      }\n    }\n    return false;\n  }\n};\n```\n\n```cpp\n// 火柴拼正方形（LeetCode 473）：分箱搜尋 + 三種剪枝。\nclass Solution {\n  std::vector<int> nums_;\n  int side_ = 0;\n  int bucket_[4] = {0, 0, 0, 0};\n\n  bool dfs(int i) {\n    if (i == static_cast<int>(nums_.size())) { return true; }   // 全部分配完\n    for (int b = 0; b < 4; ++b) {\n      if (bucket_[b] + nums_[i] > side_) { continue; }          // 超長，剪\n      if (b > 0 && bucket_[b] == bucket_[b - 1]) { continue; }  // 對稱性：同狀態的箱子只試一次\n      bucket_[b] += nums_[i];\n      if (dfs(i + 1)) { return true; }\n      bucket_[b] -= nums_[i];\n    }\n    return false;\n  }\n\n public:\n  bool makesquare(std::vector<int>& matchsticks) {\n    int total = std::accumulate(matchsticks.begin(), matchsticks.end(), 0);\n    if (total % 4 != 0) { return false; }\n    side_ = total / 4;\n    nums_ = matchsticks;\n    std::sort(nums_.rbegin(), nums_.rend());                    // 大的先放，早暴露衝突\n    if (nums_[0] > side_) { return false; }\n    return dfs(0);\n  }\n};\n```\n\n## 時間與空間複雜度\n\n79：從每個格子出發，每步至多 3 個新方向（不能走回頭路），路徑長 $L$。上界 $O(mn \\cdot 3^L)$，空間 $O(L)$。**剪枝與搜尋順序不改變這個上界，但能把實際運行時間降幾個數量級**——回答複雜度時要把兩者分開講。\n\n473：上界 $O(4^n)$；加上對稱性剪枝與由大到小排序後，實際規模遠低於此。$n \\le 16$。\n\n691／1601：記憶化後 $O(2^n \\times \\text{每個狀態的轉移數})$，是真正的多項式（相對於狀態數），與純回溯的指數不同。\n\n212：Trie 建構 $O(\\sum |w_i|)$；搜尋上界 $O(mn \\cdot 4^{L})$，但 Trie 剪枝讓實際只走「棋盤上存在的前綴」，通常快幾個數量級。空間 $O(\\sum |w_i|)$。\n\n**這類題的複雜度分析必須誠實：上界通常是指數，能過是因為剪枝。** 不要把「加了剪枝」寫成「複雜度變成多項式」——除非真的做了記憶化。\n\n## 常見錯誤與邊界條件\n\n- `vis` 忘記還原（把路徑搜尋寫成連通性 DFS，漏解）。\n- 還原的位置放在 `return true` 之前（找到解後把棋盤改回去通常無妨，但若題目要求保留解就錯了，例如 37 解數獨）。\n- 邊界檢查放在遞迴之後而非函式開頭（越界）。\n- 剪枝條件沒有驗證「被剪的必定無解」（例如 473 若允許長度為 0 的火柴，「超長就跳過」仍對，但「一根超過邊長就回傳 false」需要火柴為正）。\n- 對稱性剪枝套在不對稱的狀態上（漏解）。\n- 該記憶化卻沒記憶化（超時）。\n- 不該記憶化卻記憶化（狀態不完整，答案錯——這是更危險的一種）。\n- 212 找到單詞後未從 Trie 摘除（重複輸出）。\n- 搜尋順序未優化導致 TLE。\n- 遞迴過深。\n\n## 與相似技巧的比較\n\n- 與 4.1–4.6：骨架相同，差別在狀態的複雜度與剪枝的必要性。**前面幾節可以「照抄模板」，本節不行——每題的剪枝都要自己論證。**\n- 與 BFS：若問「最少幾步」且每步代價相同（126 單詞接龍 II），BFS 分層後再回溯構造路徑，比純 DFS 好得多。**問最短用 BFS，問所有／存在性用 DFS。** 126 正是兩者的結合：BFS 建層次圖，再在層次圖上回溯列出所有最短路徑。\n- 與記憶化搜尋／DP：判準是「未來是否只依賴一個小狀態摘要」。691、1601、638（大禮包）都該記憶化；79、212 不該。\n- 與 grid 主題的連通性 DFS：唯一但關鍵的差別是 `vis` 還不還原。\n- 與 4.8 的折半列舉：當搜尋空間是 $2^n$ 且 $n \\approx 40$，且目標可分解成兩半，折半列舉把 $2^n$ 變成 $2^{n/2}$。2386（第 K 大和）就走這條路。\n- 與 IDA*／A*：更進階的搜尋框架，用估價函數引導。LeetCode 上少見，但 1240（鋪瓷磚）這類題用「當前已用塊數 + 樂觀下界 $\\ge$ 已知最優就剪」的思路，本質上就是分支限界。\n\n## 代表例題\n\n**題號**：58、79、126、212、247\n\n放在「搜尋」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 58 | [LCP 58. 積木拼接](https://leetcode.cn/problems/De4qBB/) |  |\n| 79 | [單詞搜尋](https://leetcode.cn/problems/word-search/) |  |\n| 126 | [單詞接龍 II](https://leetcode.cn/problems/word-ladder-ii/) |  |\n| 212 | [單詞搜尋 II](https://leetcode.cn/problems/word-search-ii/) |  |\n| 247 | [中心對稱數 II](https://leetcode.cn/problems/strobogrammatic-number-ii/)（Premium） |  |\n| 248 | [中心對稱數 III](https://leetcode.cn/problems/strobogrammatic-number-iii/) |  |\n| 282 | [給表示式新增運算子](https://leetcode.cn/problems/expression-add-operators/) |  |\n| 411 | [最短獨佔單詞縮寫](https://leetcode.cn/problems/minimum-unique-word-abbreviation/) |  |\n| 473 | [火柴拼正方形](https://leetcode.cn/problems/matchsticks-to-square/) |  |\n| 488 | [祖瑪遊戲](https://leetcode.cn/problems/zuma-game/) |  |\n| 638 | [大禮包](https://leetcode.cn/problems/shopping-offers/) |  |\n| 679 | [點遊戲](https://leetcode.cn/problems/24-game/) |  |\n| 691 | [貼紙拼詞](https://leetcode.cn/problems/stickers-to-spell-word/) |  |\n| 756 | [金字塔轉換矩陣](https://leetcode.cn/problems/pyramid-transition-matrix/) |  |\n| 2664 | [巡邏的騎士](https://leetcode.cn/problems/the-knights-tour/) |  |\n| 3565 | [順序網格路徑覆蓋](https://leetcode.cn/problems/sequential-grid-path-cover/) |  |\n| 1725 | [面試題 17.25. 單詞矩陣](https://leetcode.cn/problems/word-rectangle-lcci/) | 1229 |\n| 3211 | [生成不含相鄰零的二進位制字串](https://leetcode.cn/problems/generate-binary-strings-without-adjacent-zeros/) | 1353 |\n| 967 | [連續差相同的數字](https://leetcode.cn/problems/numbers-with-same-consecutive-differences/) | 1433 |\n| 2094 | [找出 3 位偶數](https://leetcode.cn/problems/finding-3-digit-even-numbers/) | 1455 |\n| 1415 | [長度為 n 的開心字串中字典序第 k 小的字串](https://leetcode.cn/problems/the-k-th-lexicographical-string-of-all-happy-strings-of-length-n/) | 1576 |\n| 1219 | [黃金礦工](https://leetcode.cn/problems/path-with-maximum-gold/) | 1663 |\n| 980 | [不同路徑 III](https://leetcode.cn/problems/unique-paths-iii/) | 1830 |\n| 2002 | [兩個迴文子序列長度的最大乘積](https://leetcode.cn/problems/maximum-product-of-the-length-of-two-palindromic-subsequences/) | 1869 |\n| 1255 | [得分最高的單詞集合](https://leetcode.cn/problems/maximum-score-words-formed-by-letters/) | 1882 |\n| 3669 | [K 因數分解](https://leetcode.cn/problems/balanced-k-factor-decomposition/) | 1917 |\n| 1088 | [易混淆數 II](https://leetcode.cn/problems/confusing-number-ii/) | 2077 |\n| 1240 | [鋪瓷磚](https://leetcode.cn/problems/tiling-a-rectangle-with-the-fewest-squares/) | 2242 |\n| 2056 | [棋盤上有效移動組合的數目](https://leetcode.cn/problems/number-of-valid-move-combinations-on-chessboard/) | 2611 |\n| 2386 | [找出陣列的第 K 大和](https://leetcode.cn/problems/find-the-k-sum-of-an-array/) | 2648 |\n| 3509 | [最大化交錯和為 K 的子序列乘積](https://leetcode.cn/problems/maximum-product-of-subsequences-with-an-alternating-sum-equal-to-k/) | 2703 |\n\n## 本節重點速查\n\n- 骨架是 4.1，難點是四件事：**狀態設計、可行性剪枝、搜尋順序、記憶化**。\n- 路徑搜尋的 `vis` **必須還原**——不還原就變成連通性 DFS，會漏解；\n- 還不還原決定了演算法是指數還是多項式。\n- 每條剪枝都要證明「被剪的分支確定無解」；\n- 對稱性剪枝（473 跳過狀態相同的空箱）威力最大，但要先確認對稱性成立。\n- 搜尋順序（大的先放、罕見端點當起點、選項最少的先展開）不影響正確性，影響數量級。\n- 記憶化的判準：**未來只依賴小的狀態摘要就記憶化**（691、1601），依賴完整路徑就不行（79、212）。\n",
        },
        {
          id: 52,
          title: "4.8 折半列舉",
          summary:
            "## 這個技術解決什麼問題\n\n$2^n$ 的枚舉在 $n \\le 20$ 時可行，$n = 40$ 時（$10^{12}$）不可行。但 $2^{20} \\approx 10^6$ 很輕鬆。折半列舉（meet in the middle）就是利用這個落差：**把 $n$ 個元素切成兩半各自枚舉，再把兩邊的結果配對**，總成本從 $O(2^n)$ 降到 $O(2^{n/2} \\cdot \\text{配對成本})$。\n\n適用題目：1755（最接近目標值的子序列和）、2035（分成兩陣列最小化和差）、805（陣列均值分割）、3267、2386、494 的大範圍變體。**看到 $n \\le 40$ 且題目要對「子集」做某種最佳化，就該想到折半。** 這個上限值本身就是出題者留下的訊號。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「折半列舉」想：\n\n- **每個元素「選 / 不選」或「正號 / 負號」，要枚舉所有組合，而 n 大約落在 30~45**，例如 494「目標和」、805「陣列的均值分割」、1755「最接近目標值的子序列和」、3566「等積子集的劃分方案」、LCP 82「萬靈之樹」。\n- **`2^n` 會超時，但 `2^(n/2)` 算得動**——這個數字感覺就是折半的訊號：\n\n:::example 嗅到味道的瞬間：n≈40，2^40 太大但 2^20≈1e6 可接受\n```cpp\n// 嗅到味道的瞬間：n≈40，2^40 太大但 2^20≈1e6 可接受\n// → 切兩半各枚舉 2^20，再拼起來\n```\n:::\n\n\n\n- **答案能拆成「左半貢獻 + 右半貢獻」並用簡單條件配對**（和相等、和最接近、乘積相等），就能左右分開算再合併。\n- **暴力 `2^n` 枚舉超時、又不是 DP / 貪心能解的子集問題**，折半列舉常是突破口。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我能把元素切成兩半、各自獨立枚舉嗎？每半的「摘要」是什麼（和？乘積？）\n2. 兩半要用什麼條件拼接？（相等？最接近？）配對能不能靠排序 + 二分加速？\n3. n 的大小是否落在「`2^n` 太大但 `2^(n/2)` 可行」的甜蜜區？\n\n## 核心想法與直覺\n\n本節要解決的場景是：每個元素都要在「選 / 不選」之間二選一（或從幾種值裡挑一個），要枚舉所有組合。直接枚舉是 `2^n` 種，n 一到 40 就是一兆級別，跑不動。但如果 n 大約是 40，`2^40` 太大、`2^20` 卻只有一百萬——本節的「折半列舉（meet in the middle，又叫雙向搜尋）」就是把指數的「指數部分砍一半」的技巧。\n\n:::example 步驟一：左半枚舉所有子集和，存進一個容器\n核心想法只有一句：**把元素切成左右兩半，各自暴力枚舉所有子集（各 `2^(n/2)` 種），再想辦法把兩半「拼起來」湊出完整答案。** 例如 494「目標和」要選正負號讓總和等於 target：左半枚舉出所有可能的部分和、右半也枚舉出所有部分和，那麼「左半和 + 右半和 = target」就是答案，等價於「對每個左半和 L，去右半找有沒有值等於 `target - L`」。\n\n```cpp\n// 步驟一：左半枚舉所有子集和，存進一個容器\nvector<long long> leftSums;  // 大小約 2^(n/2)\nfor (int mask = 0; mask < (1 << half); ++mask) {\n    leftSums.push_back(subsetSum(left, mask));\n}\n```\n:::\n\n\n\n:::example 步驟二：右半排序後，對每個左半和用二分查 target - L 是否存在\n關鍵在「拼接」要快。如果對每個左半值都線性掃右半，又退回 `2^n`。所以右半要先排序或放進雜湊表，讓查詢變成 `O(log m)` 或 `O(1)`：\n\n```cpp\n// 步驟二：右半排序後，對每個左半和用二分查 target - L 是否存在\nsort(rightSums.begin(), rightSums.end());\nfor (long long L : leftSums) {\n    cnt += countEqual(rightSums, target - L);  // 二分找區間\n}\n```\n:::\n\n\n\n這樣總時間是 `O(2^(n/2) * (n/2))` 加上排序與查詢，足以把 n≈40 的題目壓進可接受範圍。它的本質是「用空間換時間」：多存了兩半的枚舉結果，換掉了一半的指數。\n\n## 狀態／資料結構定義\n\n- 把陣列切成左半 $L$（前 $\\lceil n/2 \\rceil$ 個）與右半 $R$（其餘）。\n- `sumL`：$L$ 的所有 $2^{|L|}$ 個子集和；`sumR`：$R$ 的所有 $2^{|R|}$ 個子集和。用位元枚舉或回溯生成。\n- 配對結構：把 `sumR` **排序**後用二分搜尋，或放進雜湊表（等值查詢），或用雙指標（單調配對）。\n- 若目標函數需要更多維度（2035 要求「左右各取恰好 $n/2$ 個」），就按「取了幾個」分桶：`sumL[k]` 存「左半取 $k$ 個」的所有和，配對時只讓 `sumL[k]` 對上 `sumR[n/2 - k]`。\n\n**維度的判準：配對時需要對齊什麼，就要按什麼分桶。** 只要求和最接近 → 不分桶；還要求個數 → 按個數分桶；還要求乘積或其他量 → 那一維也要進桶。\n\n## 不變量或正確性證明\n\n**核心引理（唯一分解）。** 對全集 $S = L \\sqcup R$（不交併），映射\n$$T \\;\\longmapsto\\; (T \\cap L,\\; T \\cap R)$$\n是 $2^S$ 與 $2^L \\times 2^R$ 之間的**雙射**。\n\n證明。單射：若 $T_1 \\cap L = T_2 \\cap L$ 且 $T_1 \\cap R = T_2 \\cap R$，則 $T_1 = (T_1 \\cap L) \\cup (T_1 \\cap R) = (T_2 \\cap L) \\cup (T_2 \\cap R) = T_2$（用到 $L \\cup R = S$）。滿射：給定 $(A, B) \\in 2^L \\times 2^R$，取 $T = A \\cup B$ 即可，且 $T \\cap L = A$（用到 $A \\subseteq L$、$B \\cap L = \\varnothing$，即 $L$ 與 $R$ **不交**）。$\\square$\n\n因此「枚舉左半的所有子集 × 枚舉右半的所有子集」與「枚舉全集的所有子集」是同一件事，**不重不漏**。$|2^L| \\cdot |2^R| = 2^{|L|+|R|} = 2^n$——注意這說明**組合的總數並沒有減少**，減少的是「生成」的成本（從 $2^n$ 降到 $2^{|L|} + 2^{|R|}$）。折半列舉能贏，靠的是**不逐一檢查所有 $2^n$ 對，而是用排序／雜湊在對數時間內批次回答「右半有沒有／有幾個滿足條件的」**。\n\n**這條論證點名了折半列舉的核心前提：目標函數必須可分解。** 具體地，要能寫成\n$$f(T) = g(T \\cap L) \\;\\oplus\\; h(T \\cap R)$$\n其中 $\\oplus$ 是一個「可查詢」的運算——通常是加法（配對成「和為定值／最接近定值」）、異或（雜湊表等值查詢）、或乘法（取對數後變加法）。\n\n- 和／異或／乘積：可分解。✔\n- 「子集中相鄰兩元素之差不超過 $k$」：**不可分解**，因為它涉及跨越切點的相鄰關係。✘\n- 「子集的最大值減最小值」：可分解（各半的 max、min 再合併）。✔\n\n**判斷一題能不能折半，就是判斷目標能不能寫成這個形式。** 寫不出來就別硬套。\n\n**1755（最接近 `goal` 的子集和）的配對正確性。** 對每個 $a \\in \\text{sumL}$，我們要找 $b \\in \\text{sumR}$ 使 $|a + b - \\text{goal}|$ 最小，即 $b$ 最接近 $\\text{goal} - a$。把 `sumR` 排序後用 `lower_bound` 找到第一個 $\\ge \\text{goal} - a$ 的元素，**答案必在它與它的前一個之中**——因為排序後的序列裡，離某個值最近的元素只可能是「第一個不小於它的」或「最後一個小於它的」。這一步用到的是有序性，不是別的性質；漏掉「前一個」是最常見的錯誤。\n\n**2035（左右各取恰好 $n/2$ 個）為何必須按個數分桶。** 題目要求把 $2n$ 個數分成兩組各 $n$ 個。設左半取 $k$ 個進第一組，則右半必須取 $n - k$ 個。若不分桶而任意配對，會得到大小不等於 $n$ 的分組，答案非法。**分桶不是優化，是正確性要求。**\n\n**805 的均值轉換。** 「兩組平均值相等」等價於「第一組的和 × 全部個數 = 全部的和 × 第一組個數」，即\n$$\\text{sum}_A \\cdot n = \\text{sum} \\cdot |A| .$$\n為了避免浮點與分數，通常把每個元素替換成 $n \\cdot a_i - \\text{sum}$，則條件變成「該組替換後的和為 0」。**這個變換把「平均值」這個不可分解的量，轉成了「和」這個可分解的量**——折半列舉常常需要先做這種代數整理，這一步往往比折半本身更難想。\n\n## 逐步演算法\n\n把折半列舉拆成四步，逐一對應程式碼：\n\n:::example 狀態：左右兩半的所有子集和\n**1. 狀態表示：左右兩半各自的枚舉結果。** 把 n 個元素對半分，分別準備兩個容器存它們所有子集的摘要（通常是子集和）。\n\n```cpp\n// 狀態：左右兩半的所有子集和\nint n;\nvector<int> a;\nint half = n / 2;  // 左半 [0,half)，右半 [half,n)\nvector<long long> leftSums, rightSums;\n```\n:::\n\n\n\n:::example 用 mask 的每個 bit 決定該元素選不選，累出這個子集的和\n**2. 枚舉一半：用位元遮罩列出所有子集。** `mask` 的每一個 bit 代表對應元素「選或不選」，跑遍 `0..2^k - 1` 就枚舉了所有子集。\n\n```cpp\n// 用 mask 的每個 bit 決定該元素選不選，累出這個子集的和\nlong long subsetSum(int lo, int hi, int mask) {\n    long long s = 0;\n    for (int i = lo; i < hi; ++i) {\n        if (mask >> (i - lo) & 1) {\n            s += a[i];\n        }\n    }\n    return s;\n}\n```\n:::\n\n\n\n:::example 右半排序後二分查 target - L，累計符合的數量\n**3. 拼接兩半：排序 + 二分（或雜湊）快速配對。** 把右半排好序，對每個左半值用二分查「需要的另一半」是否存在、有幾個。\n\n```cpp\n// 右半排序後二分查 target - L，累計符合的數量\nsort(rightSums.begin(), rightSums.end());\nfor (long long L : leftSums) {\n    auto lo = lower_bound(rightSums.begin(), rightSums.end(), target - L);\n    auto hi = upper_bound(rightSums.begin(), rightSums.end(), target - L);\n    ans += hi - lo;\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 拼接條件依題而變：求「剛好等於」用二分定位；求「最接近 target」（1755）對每個 L 找右半中最接近 `target - L` 的值。\n- 注意溢位：部分和可能很大，用 `long long`。\n- 複雜度 `O(2^(n/2) * (n + log))`；空間 `O(2^(n/2))` 存兩半結果。只有在 `2^n` 太大、`2^(n/2)` 可接受時才划算。\n\n## C++17 模板\n\n```cpp\n// 最接近目標值的子序列和（LeetCode 1755）：折半 + 排序 + 二分。\nclass Solution {\n  static std::vector<long long> subsetSums(const std::vector<int>& a) {\n    int m = a.size();\n    std::vector<long long> sums(1 << m, 0);\n    for (int mask = 1; mask < (1 << m); ++mask) {\n      int low = mask & -mask;                       // 最低位的 1\n      int idx = __builtin_ctz(mask);\n      sums[mask] = sums[mask ^ low] + a[idx];       // 由子問題遞推，總計 O(2^m)\n    }\n    return sums;\n  }\n\n public:\n  int minAbsDifference(std::vector<int>& nums, int goal) {\n    int n = nums.size(), half = n / 2;\n    std::vector<int> left(nums.begin(), nums.begin() + half);\n    std::vector<int> right(nums.begin() + half, nums.end());\n\n    std::vector<long long> sl = subsetSums(left);\n    std::vector<long long> sr = subsetSums(right);\n    std::sort(sr.begin(), sr.end());\n\n    long long best = std::abs(static_cast<long long>(goal));\n    for (long long a : sl) {\n      long long need = goal - a;\n      auto it = std::lower_bound(sr.begin(), sr.end(), need);\n      if (it != sr.end()) {                          // 第一個 >= need\n        best = std::min(best, std::abs(a + *it - goal));\n      }\n      if (it != sr.begin()) {                        // 最後一個 < need，不能漏\n        best = std::min(best, std::abs(a + *std::prev(it) - goal));\n      }\n    }\n    return static_cast<int>(best);\n  }\n};\n```\n\n`subsetSums` 用 `sums[mask] = sums[mask ^ lowbit] + a[idx]` 遞推，每個 `mask` $O(1)$，總計 $O(2^m)$——比「對每個 `mask` 掃 $m$ 位」的 $O(m \\cdot 2^m)$ 快一個 $m$ 倍，在 $m = 20$ 時是 20 倍的差距，值得養成習慣。\n\n## 時間與空間複雜度\n\n生成兩半的子集和：$O(2^{n/2})$（用 lowbit 遞推）或 $O(n \\cdot 2^{n/2})$（逐位累加）。\n\n配對：排序 $O(2^{n/2} \\cdot \\frac{n}{2})$，對每個左半元素二分 $O(\\frac{n}{2})$，合計 $O(n \\cdot 2^{n/2})$。\n\n總計 $O(n \\cdot 2^{n/2})$ 時間、$O(2^{n/2})$ 空間。$n = 40$ 時 $2^{20} \\approx 10^6$，乘上 $n$ 約 $4\\times10^7$，可過。\n\n與 $O(2^n) \\approx 10^{12}$ 相比是決定性的差距。\n\n**空間是折半列舉的真正瓶頸。** $2^{20}$ 個 `long long` 是 8 MB，兩半就 16 MB，加上排序的臨時空間可能吃緊。$n = 44$ 時 $2^{22}$ 已是 32 MB 一半，通常超限。**所以 $n$ 的上限不只由時間決定，也由空間決定**，這與一般的指數演算法不同，要特別留意。\n\n若兩邊不等長，讓**較小的那半用來排序／建表**能省空間；或改用雙指標（兩邊都排序後從兩端夾逼）避免二分的常數。\n\n## 常見錯誤與邊界條件\n\n- 二分後只檢查 `lower_bound` 的結果而漏掉前一個（1755 的經典錯誤）。\n- `it == sr.end()` 或 `it == sr.begin()` 未檢查（解參考越界）。\n- 需要按「取了幾個」分桶卻沒分（2035，答案非法）。\n- 和用 `int` 溢位（$n = 40$、$|a_i| \\le 10^7$ 時和可達 $4\\times10^8$，配上 `goal` 相減後仍要 `long long`）。\n- 忘記空子集（和為 0）也是合法選擇，導致漏解。\n- 805 未做「$n \\cdot a_i - \\text{sum}$」的變換而直接比較平均值（浮點誤差）。\n- 805 要排除「兩組其中一組為空」的情形。\n- 切分點取 `n / 2` 但陣列長度為奇數時兩邊長度差 1（正確，但空間分配要按較長那半算）。\n- 目標函數其實不可分解卻硬套。\n\n## 與相似技巧的比較\n\n- 與 4.2 的子集型回溯：折半是它在 $n$ 太大時的救援。$n \\le 20$ 直接枚舉，$20 < n \\le 40$ 折半，$n > 40$ 兩者都不行，得找多項式解法或 DP。\n- 與 494（目標和）：$n \\le 20$ 時可以直接 DP（背包，$O(n \\cdot \\text{sum})$）。**當和的範圍小時，背包 DP 比折半好**；當元素值很大（和的範圍達 $10^9$）而 $n$ 小時，折半才是唯一出路。**判準是「$n$ 小還是值域小」。**\n- 與雙向 BFS：同一個「兩邊夾擊」的思想用在最短路徑上，把 $b^d$ 降到 $2 b^{d/2}$。原理相同（狀態空間的平方根縮減），場景不同。\n- 與 2386（第 K 大和）：那題結合折半與堆，先取全部正數的和當上界，再用堆逐步生成第 $k$ 大——是折半思想的變形。\n- 與 3267（統計近似相等數對 II）：把「至多兩次交換」的所有變體枚舉出來再配對，也是「枚舉一半的可能性 + 查表」的形態，只是切分的維度不是元素而是操作。\n\n## 代表例題\n\n**題號**：82、494、805、3566、1755\n\n放在「折半列舉」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 82 | [LCP 82. 萬靈之樹](https://leetcode.cn/problems/cnHoX6/) |  |\n| 494 | [目標和](https://leetcode.cn/problems/target-sum/) |  |\n| 805 | [陣列的均值分割](https://leetcode.cn/problems/split-array-with-same-average/) |  |\n| 3566 | [等積子集的劃分方案](https://leetcode.cn/problems/partition-array-into-two-equal-product-subsets/) | 1459 |\n| 1755 | [最接近目標值的子序列和](https://leetcode.cn/problems/closest-subsequence-sum/) | 2364 |\n| 956 | [最高的廣告牌](https://leetcode.cn/problems/tallest-billboard/) | 2381 |\n| 3801 | [合併有序列表的最小成本](https://leetcode.cn/problems/minimum-cost-to-merge-sorted-lists/) | 2399 |\n| 2035 | [將陣列分成兩個陣列並最小化陣列和的差](https://leetcode.cn/problems/partition-array-into-two-arrays-to-minimize-sum-difference/) | 2490 |\n| 3267 | [統計近似相等數對 II](https://leetcode.cn/problems/count-almost-equal-pairs-ii/) | 2545 |\n\n## 本節重點速查\n\n- $n \\le 40$ 且對子集做最佳化 → 折半列舉。\n- 依據是**唯一分解引理**：$T \\mapsto (T \\cap L, T \\cap R)$ 是雙射，故不重不漏。\n- **核心前提：目標必須寫成 $f(T) = g(T\\cap L) \\oplus h(T \\cap R)$ 且 $\\oplus$ 可查詢**（和、異或、乘積可以；\n- 跨切點的相鄰關係不行）。\n- 配對時**需要對齊什麼就按什麼分桶**（2035 要按個數分桶，這是正確性而非優化）。\n- 二分後**別漏掉前一個元素**。\n- 子集和用 `sums[mask] = sums[mask ^ lowbit] + a[idx]` 遞推可省一個 $n$ 倍。\n- 空間 $O(2^{n/2})$ 常是真正的瓶頸。\n",
        },
      ],
    },
    {
      id: 53,
      title: "5. 其他遞迴/分治",
      summary:
        "分治與遞迴在鏈表上的應用：merge sort 鏈表、分治反轉；注意斷鏈與歸併後頭節點返回。\n\n**複雜度**：找中點 $O(n)$，總 $O(n\\log n)$。",
      children: [
        {
          id: 54,
          title: "5.1 應用題",
          summary:
            "## 這個技術解決什麼問題\n\n本節收的是不屬於樹、也不屬於回溯枚舉的遞迴／分治應用。它們的共同槓桿是同一句話：**把大問題化約成同型的小問題，使總工作量從 $O(n^2)$ 掉到 $O(n \\log n)$，或從「$O(\\text{結構大小})$」掉到 $O(\\log \\text{結構大小})$。**\n\n三種形態，收穫的來源各不相同：\n\n- **分治**（912 排序陣列）：兩邊都要解，合併成本 $O(n)$，遞迴式 $T(n) = 2T(n/2) + O(n) \\Rightarrow O(n \\log n)$。收穫來自「合併兩段有序序列比從頭排序便宜」。\n- **減治**（215 第 K 大）：只往一邊遞迴，$T(n) = T(n/2) + O(n) \\Rightarrow O(n)$。收穫來自**丟掉另一半**。\n- **公式遞迴**（1545、880、3537）：結構大到無法生成，但「第 $n$ 層由第 $n-1$ 層按固定規則產生」，於是把查詢位置往上一層折算。收穫是把 $O(2^n)$ 的結構壓成 $O(n)$ 次折算。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「分治 / 遞迴應用」想：\n\n- **大問題能自然切成「同型的小問題」再合併**，例如 912「排序陣列」（合併排序）、215「第 K 大」（快速選擇）。\n- **第 n 層的結構由第 n-1 層按固定規則生成**，要查某個位置卻不可能把整個結構造出來——往「公式遞迴 / 折算」想：\n\n:::example 嗅到味道的瞬間：字串 / 網格大到無法真的生成，但有「上一層 → 這一層」的規律\n```cpp\n// 嗅到味道的瞬間：字串 / 網格大到無法真的生成，但有「上一層 → 這一層」的規律\n// 880「解碼字串」、1545「第 N 個二進位字串」、3537「填充特殊網格」都是這型\n```\n:::\n\n\n\n- **只需要答案的「一部分」（第 K 個、某一位）而非全部**——常可只遞迴一邊（減治），省掉另一半。\n- **暴力把整個結構建出來會爆記憶體 / 超時**；分治用遞迴關係跳過大部分計算。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的大問題能拆成哪幾個「同型小問題」？合併規則是什麼？\n2. 基底情形（最小、可直接回答的情況）是什麼？遞迴會收斂到它嗎？\n3. 我是否只需要答案的一部分，能不能只遞迴一邊、或用公式折算而不真的建出結構？\n\n## 核心想法與直覺\n\n本節是「其他遞迴 / 分治」的應用題集合。分治（divide and conquer）的核心想法是：**把一個大問題切成幾個同型的小問題，分別解決，再把小答案合併成大答案。** 遞迴（recursion）則是它的實現手段——一個函式「呼叫自己」去處理更小的子問題，小到不能再小（基底情形）就直接回答。\n\n:::example 分治三部曲：切一半 → 遞迴各排各的 → 合併兩段有序結果\n最經典的例子是排序。912「排序陣列」用合併排序：把陣列切兩半、各自排好、再合併兩條有序序列。\n\n```cpp\n// 分治三部曲：切一半 → 遞迴各排各的 → 合併兩段有序結果\nvoid mergeSort(int l, int r) {\n    if (l >= r) {\n        return;  // 基底情形：只剩 0 或 1 個元素，本就有序\n    }\n    int m = (l + r) / 2;\n    mergeSort(l, m);\n    mergeSort(m + 1, r);\n    merge(l, m, r);  // 合併兩段\n}\n```\n:::\n\n\n\n:::example 減治：分區後只往「答案所在的那半」遞迴，另一半丟掉\n另一種「分治」不需要兩邊都解，只要選一邊深入——這叫「減治」。215「陣列中的第 K 個最大元素」用快速選擇（quickselect）：隨機選一個基準把陣列分成「大的一堆 / 小的一堆」，依照 K 落在哪一堆，只遞迴那一邊，平均 `O(n)`。\n\n```cpp\n// 減治：分區後只往「答案所在的那半」遞迴，另一半丟掉\nint p = partition(l, r);  // 基準歸位，左小右大\nif (k == p) {\n    return a[p];\n}\nreturn (k < p) ? quickSelect(l, p - 1, k) : quickSelect(p + 1, r, k);\n```\n:::\n\n\n\n還有一類完全不碰原始資料，而是用遞迴「公式」直接算出第幾個位置的答案，例如 1545「找第 N 個二進位字串的第 K 位」、880「索引處的解碼字串」——它們利用「第 n 層由第 n-1 層按規則生成」的結構，把 K 不斷往前一層折算，避免真的把整個巨大字串造出來。共通精神都是：**找到「大問題 = 小問題 + 合併規則」的遞迴關係。**\n\n## 狀態／資料結構定義\n\n**912（合併排序）：** 狀態是區間 $[l, r]$。輔助陣列 `tmp` 用來合併（就地合併很難寫對，用輔助陣列是標準做法）。\n\n**215（快速選擇）：** 狀態是區間 $[l, r]$ 加上目標下標 $k$（**全域下標，不是區間內的相對下標**——這是最容易搞錯的地方）。分區後基準落在 $p$，比較 $k$ 與 $p$ 決定往哪一邊。\n\n**1545（第 N 個二進位字串的第 K 位）：** 狀態是 $(n, k)$。關鍵量是第 $n$ 個字串的長度 $\\ell_n = 2^n - 1$ 與正中位置 $m_n = 2^{n-1}$（1-indexed）。\n\n**880（索引處的解碼字串）：** 狀態是「解碼後的長度」與目標下標 $k$。由後往前掃描，遇到數字就把長度除回去、$k$ 對長度取模。\n\n## 不變量或正確性證明\n\n**912 的正確性（強歸納）。** 命題：`mergeSort(l, r)` 使 `a[l..r]` 有序，且是原多重集的一個排列。\n\n- 基底：$l \\ge r$，長度 $\\le 1$，本就有序。\n- 歸納：設 `mergeSort(l, m)` 與 `mergeSort(m+1, r)` 都正確（子區間嚴格更短，因為 $l \\le m < r$）。`merge` 每次從兩段的當前頭部取較小者，故輸出遞增；每個元素恰被取一次，故是排列。\n\n**穩定性的來源**：合併時相等取左邊（`a[i] <= a[j]` 用左邊），保證原本在前的元素仍在前。寫成 `<` 就不穩定了——這個字元在單獨排序時看不出差別，但當合併排序被當成別的演算法的元件時（例如 1.9 的連結串列排序、或需要穩定性的多關鍵字排序）就是錯的。\n\n**必須取前中點。** $m = \\lfloor (l+r)/2 \\rfloor$ 保證 $l \\le m < r$，兩個子區間都嚴格短於原區間，遞迴收斂。若取 $m = \\lceil (l+r)/2 \\rceil$，長度為 2 時會得到 $m = r$，右半是空、左半是原區間，**無限遞迴**。這與 1.9 中連結串列取前中點的理由完全相同。\n\n**215 快速選擇的正確性不變量：「答案位於當前區間 $[l, r]$ 內」始終成立。**\n\n- 初始化：$k \\in [0, n-1]$，區間是全陣列，成立。\n- 保持：分區後基準歸位到下標 $p$，滿足 `a[l..p-1] <= a[p] <= a[p+1..r]`。於是排序後第 $p$ 小的元素就是 `a[p]`——**這一步是快速選擇的全部**：分區之後，基準的位置就是它在最終有序陣列中的位置，這個事實與其他元素的順序無關。因此：\n  - $k = p$：答案就是 `a[p]`，回傳。\n  - $k < p$：答案在左半，且左半是 $[l, p-1]$，不變量保持。\n  - $k > p$：答案在右半，同理。\n- 終止：區間長度每次至少減 1（基準被排除），必定終止。\n\n**期望 $O(n)$ 的論證。** 若每次基準隨機選取，期望上分區點均勻分佈，遞迴子區間的期望長度是原長的 $3/4$（更精確：期望上有一半的機會落在中間一半內）。於是期望總工作量\n$$T(n) \\le n + T(3n/4) \\Rightarrow T(n) \\le n \\left(1 + \\tfrac34 + \\tfrac9{16} + \\cdots\\right) = 4n = O(n).$$\n\n**這裡「隨機」不是可有可無的裝飾。** 若固定取 `a[r]` 當基準，在已排序或逆序輸入上每次只能剝掉一個元素，退化成 $O(n^2)$——而 LeetCode 215 的測資裡就有這種輸入。必須隨機化（或用三數取中）。**這是「平均情形分析成立的前提是輸入分佈假設，而隨機化把假設從輸入身上轉移到演算法身上」的標準例子。**\n\n陣列含大量重複元素時，兩路分區也會退化；改用三路分區（`< pivot` / `== pivot` / `> pivot`）可解決。\n\n**1545 的折算不變量：`solve(n, k)` 回傳第 $n$ 個字串的第 $k$ 位（1-indexed），且始終有 $1 \\le k \\le 2^n - 1$。**\n\n構造規則是 $S_n = S_{n-1} + \\texttt{\"1\"} + \\text{invert}(\\text{reverse}(S_{n-1}))$，長度 $\\ell_n = 2\\ell_{n-1} + 1 = 2^n - 1$，正中位置 $m = 2^{n-1}$。\n\n- 若 $k = m$：該位是 `'1'`，直接回傳。\n- 若 $k < m$：落在前半 $S_{n-1}$ 中，答案是 `solve(n-1, k)`。\n- 若 $k > m$：落在後半。後半是「$S_{n-1}$ 先反轉再取反」，其第 $j$ 位（$j = k - m$）等於 $S_{n-1}$ 第 $\\ell_{n-1} - j + 1$ 位取反。故答案是 `invert(solve(n-1, ell_{n-1} - j + 1))`。\n\n每次遞迴 $n$ 減 1，故 $O(n)$ 次折算、$O(1)$ 空間（改成迴圈時）。**這裡真正的收穫是：字串長度 $2^n - 1$ 在 $n = 20$ 時已達百萬，$n$ 再大就無法生成，但折算完全不受影響。**\n\n「反轉再取反」這個對應關係必須寫對，$\\ell_{n-1} - j + 1$ 的 $+1$ 來自 1-indexed。**下標基準（0 還是 1）在這類公式遞迴中是最大的錯誤來源**，動筆前先寫清楚採用哪一種並貫徹。\n\n## 逐步演算法\n\n把這類分治 / 遞迴題拆成四步，逐一對應程式碼：\n\n:::example 狀態：子問題的範圍或層次參數\n**1. 狀態表示：用「範圍」或「層數 / 位置」描述子問題。** 排序類用區間 `[l, r]`；公式類用「第幾層、第幾位」。\n\n```cpp\n// 狀態：子問題的範圍或層次參數\nvector<int> a;\n// mergeSort(l, r) / quickSelect(l, r, k) / solve(n, k) ...\n```\n:::\n\n\n\n:::example 公式類：第 n 層長度 = 上一層的 2 倍 +1，據此把 k 折回上一層\n**2. 切分：把問題拆成更小的同型子問題。** 排序對半切；快速選擇用基準分區；公式類往「上一層」折算。\n\n```cpp\n// 公式類：第 n 層長度 = 上一層的 2 倍 +1，據此把 k 折回上一層\nlong long len = (1LL << n) - 1;  // 第 n 個字串長度\nlong long mid = len / 2 + 1;     // 正中間那位\nif (k == mid) {\n    return '1';  // 基底\n                              // 否則把 k 對映回第 n-1 層的相對位置，再遞迴\n}\n```\n:::\n\n\n\n:::example 合併兩段有序區間（合併排序的核心）\n**3. 合併 / 選擇：把子答案組回大答案。** 合併排序要 `merge` 兩段；快速選擇只取一邊；公式類直接由折算結果回答。\n\n```cpp\n// 合併兩段有序區間（合併排序的核心）\nvoid merge(int l, int m, int r) {\n    vector<int> tmp;\n    int i = l, j = m + 1;\n    while (i <= m && j <= r) {\n        tmp.push_back(a[i] <= a[j] ? a[i++] : a[j++]);\n    }\n    while (i <= m) {\n        tmp.push_back(a[i++]);\n    }\n    while (j <= r) {\n        tmp.push_back(a[j++]);\n    }\n    for (int k = 0; k < (int)tmp.size(); ++k) {\n        a[l + k] = tmp[k];\n    }\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 一定要有清楚的基底情形（區間長度 ≤ 1、或折到第 0/1 層），否則遞迴不收斂。\n- 合併排序穩定 `O(n log n)`；快速選擇平均 `O(n)`、最壞 `O(n^2)`（基準隨機化可避免）。\n- 公式類（1545、880、3537）關鍵是「不要真的建出整個結構」，只把查詢位置往前折算，通常 `O(log)` 或 `O(層數)`。\n\n## C++17 模板\n\n```cpp\n// 快速選擇（LeetCode 215）：隨機化分區，期望 O(n)。\nclass Solution {\n  std::mt19937 rng_{std::random_device{}()};\n\n  // 回傳 a[l..r] 中第 k 小的元素（k 為全域下標）\n  int quickSelect(std::vector<int>& a, int l, int r, int k) {\n    if (l == r) { return a[l]; }\n\n    int pick = l + static_cast<int>(rng_() % (r - l + 1));   // 隨機基準，不可省\n    std::swap(a[pick], a[r]);\n    int pivot = a[r];\n\n    int p = l;                                   // 分區：a[l..p-1] < pivot\n    for (int i = l; i < r; ++i) {\n      if (a[i] < pivot) { std::swap(a[i], a[p++]); }\n    }\n    std::swap(a[p], a[r]);                       // 基準歸位到 p\n\n    if (k == p) { return a[p]; }                 // 分區後 p 就是最終位置\n    return (k < p) ? quickSelect(a, l, p - 1, k)\n                   : quickSelect(a, p + 1, r, k);\n  }\n\n public:\n  int findKthLargest(std::vector<int>& nums, int k) {\n    int n = nums.size();\n    return quickSelect(nums, 0, n - 1, n - k);   // 第 k 大 = 第 n-k 小（0-indexed）\n  }\n};\n```\n\n```cpp\n// 合併排序（LeetCode 912）：穩定，O(n log n)。\nclass Solution {\n  std::vector<int> tmp_;\n\n  void mergeSort(std::vector<int>& a, int l, int r) {\n    if (l >= r) { return; }                      // 基底：長度 <= 1\n    int m = l + (r - l) / 2;                     // 前中點，保證 l <= m < r\n    mergeSort(a, l, m);\n    mergeSort(a, m + 1, r);\n\n    int i = l, j = m + 1, t = l;\n    while (i <= m && j <= r) {\n      tmp_[t++] = (a[i] <= a[j]) ? a[i++] : a[j++];   // <= 才穩定\n    }\n    while (i <= m) { tmp_[t++] = a[i++]; }\n    while (j <= r) { tmp_[t++] = a[j++]; }\n    std::copy(tmp_.begin() + l, tmp_.begin() + r + 1, a.begin() + l);\n  }\n\n public:\n  std::vector<int> sortArray(std::vector<int>& nums) {\n    tmp_.resize(nums.size());\n    mergeSort(nums, 0, nums.size() - 1);\n    return nums;\n  }\n};\n```\n\n## 時間與空間複雜度\n\n**912**：$T(n) = 2T(n/2) + O(n)$，由主定理得 $O(n \\log n)$，最壞情形也是（與快速排序不同，合併排序沒有壞輸入）。空間 $O(n)$ 輔助陣列加 $O(\\log n)$ 堆疊。\n\n**215**：期望 $O(n)$，最壞 $O(n^2)$（隨機化後機率極低）。空間 $O(1)$（改成迴圈）或 $O(\\log n)$ 期望堆疊。\n若改用大小為 $k$ 的堆，是 $O(n \\log k)$——**當 $k$ 很小時堆更好，且能處理串流輸入**；$k$ 接近 $n$ 時快速選擇更好。若要求最壞 $O(n)$，用中位數的中位數（BFPRT），常數大，競賽中少用。\n\n**1545／880**：$O(n)$ 次折算（$n$ 是層數），$O(1)$ 額外空間。相對於「真的把字串建出來」的 $O(2^n)$ 時間與空間，這是**在時間與空間上同時**降到對數級——公式遞迴的典型收穫。\n\n**主定理速記**：$T(n) = aT(n/b) + O(n^d)$ 時，$d > \\log_b a$ 給 $O(n^d)$；$d = \\log_b a$ 給 $O(n^d \\log n)$；$d < \\log_b a$ 給 $O(n^{\\log_b a})$。合併排序是 $a=2, b=2, d=1$，落在第二種。快速選擇是 $a=1, b=2, d=1$，落在第一種，得 $O(n)$。**「兩邊都遞迴」與「只遞迴一邊」的差別，在主定理裡就是 $a=2$ 與 $a=1$，結果差一個 $\\log$。**\n\n## 常見錯誤與邊界條件\n\n- 中點取後中點導致無限遞迴。\n- 合併時用 `<` 而非 `<=` 破壞穩定性。\n- `tmp` 每次遞迴都重新配置（正確但慢，應在外層開好一份）。\n- 合併後忘記把 `tmp` 複製回 `a`。\n- 215 把 `k` 當成區間內的相對下標（應為全域下標）。\n- 215 用固定基準在有序輸入上退化成 $O(n^2)$。\n- 215 的「第 $k$ 大」與「第 $k$ 小」轉換寫錯（$n - k$ 對應 0-indexed）。\n- 分區迴圈的邊界寫成 `i <= r`（把基準也比進去，基準歸位錯誤）。\n- 重複元素多時兩路分區退化（改三路）。\n- 公式遞迴的下標基準（0 或 1）混用。\n- `1 << n` 在 $n \\ge 31$ 時溢位（用 `1LL << n`）。\n- 880 的長度累乘可能爆 `long long`（超過 $k$ 就可以停止累乘）。\n- 遞迴過深。\n\n## 與相似技巧的比較\n\n- 與 1.9 的連結串列分治：完全同源。差別是連結串列沒有隨機存取，找中點要用快慢指標，且**必須真的切斷**（`slow->next = nullptr`）；陣列版靠下標天然分割。\n- 與 sorting 主題：合併排序在「求逆序對」「歸併過程中統計」這類題目上是不可替代的——因為合併的那一刻，左右兩段各自有序，可以 $O(n)$ 完成跨段統計。\n- 與 binary_search：二分搜尋是減治的極致（每次丟掉一半，且合併成本 $O(1)$，故 $O(\\log n)$）。快速選擇則是「每次丟掉一半，但分區成本 $O(n)$」，故 $O(n)$。**兩者的差別就在合併／分割的成本。**\n- 與 4.7 的搜尋：搜尋也是遞迴，但它的子問題**不會變小到可控**（指數級分支），靠剪枝而非分治結構取勝。分治的前提是「子問題規模按固定比例縮小」。\n- 與 heap／priority_queue：215 的替代解法，取捨見上。\n- 與 3537（填充特殊網格）：$4^n$ 大小的網格無法生成，但每個 $2^k \\times 2^k$ 的區塊由四個 $2^{k-1} \\times 2^{k-1}$ 區塊按固定順序拼成——這是二維版的公式遞迴，折算的是「座標落在哪個象限」以及「該象限的起始編號」。思路與 1545 同構。\n\n## 代表例題\n\n**題號**：215、912、1545、3537、880\n\n放在「應用題」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 215 | [陣列中的第K個最大元素](https://leetcode.cn/problems/kth-largest-element-in-an-array/) |  |\n| 912 | [排序陣列](https://leetcode.cn/problems/sort-an-array/) |  |\n| 1545 | [找出第 N 個二進位制字串中的第 K 位](https://leetcode.cn/problems/find-kth-bit-in-nth-binary-string/) | 1479 |\n| 3537 | [填充特殊網格](https://leetcode.cn/problems/fill-a-special-grid/) | 1542 |\n| 880 | [索引處的解碼字串](https://leetcode.cn/problems/decoded-string-at-index/) | 2011 |\n| 3614 | [用特殊操作處理字串 II](https://leetcode.cn/problems/process-string-with-special-operations-ii/) | 2011 |\n| 3307 | [找出第 K 個字元 II](https://leetcode.cn/problems/find-the-k-th-character-in-string-game-ii/) | 2232 |\n| 932 | [漂亮陣列](https://leetcode.cn/problems/beautiful-array/) | 2294 |\n\n## 本節重點速查\n\n- 三種形態：**分治**（兩邊都遞迴，$T(n)=2T(n/2)+O(n) \\Rightarrow O(n\\log n)$）、**減治**（只遞迴一邊，$\\Rightarrow O(n)$）、**公式遞迴**（結構太大不生成，把查詢位置往上一層折算，$O(\\log \\text{size})$）。\n- 合併排序：**取前中點**（否則無限遞迴）、合併時**相等取左邊**（穩定性）。\n- 快速選擇的不變量是「答案始終在當前區間內」，核心事實是**分區後基準的位置就是它的最終位置**；\n- **基準必須隨機化**，否則有序輸入退化 $O(n^2)$。\n- 公式遞迴最大的坑是下標基準（0 或 1）與「反轉後對應到哪一位」。\n- 主定理裡「兩邊遞迴」與「一邊遞迴」就是 $a=2$ 與 $a=1$，差一個 $\\log$。\n",
        },
      ],
    },
    {
      id: 9015,
      title: "6. 常見面試模式",
      summary:
        "樹題的核心是選對走訪順序與在遞迴中傳遞/回傳的資訊。先辨認下表訊號，再決定後序聚合、附帶狀態、利用 BST 性質或層序展開。\n\n| 模式 | 訊號 | 核心做法 | 代表題 |\n| --- | --- | --- | --- |\n| 後序樹形 DP | 答案由左右子樹結果合併 | 後序遞迴，子樹回傳聚合值，於每個節點更新全域答案 | [二叉樹中的最大路徑和](https://leetcode.cn/problems/binary-tree-maximum-path-sum) |\n| 帶狀態樹 DP | 相鄰節點互斥/選或不選 | 每節點回傳「選/不選」兩種狀態，父層據此取最優 | [打家劫舍 III](https://leetcode.cn/problems/house-robber-iii) |\n| BST 中序 | 與排序、第 `k` 小、區間有關 | 利用 BST 中序走訪即遞增序列的性質 | [驗證二叉搜尋樹](https://leetcode.cn/problems/validate-binary-search-tree) |\n| 層序 BFS | 按層處理、求最右/每層極值 | 佇列逐層展開，記錄當前層大小一次處理一層 | [二叉樹的層序遍歷](https://leetcode.cn/problems/binary-tree-level-order-traversal) |\n| 最近公共祖先 | 求兩節點的 LCA | 後序遞迴，左右子樹各找到一個目標時當前節點即為 LCA | [二叉樹的最近公共祖先](https://leetcode.cn/problems/lowest-common-ancestor-of-a-binary-tree) |\n| 由走訪重建 | 給定前/中/後序還原樹 | 用前(後)序定根、中序分左右子樹，遞迴建樹 | [從前序與中序遍歷序列構造二叉樹](https://leetcode.cn/problems/construct-binary-tree-from-preorder-and-inorder-traversal) |\n| 換根 DP | 求以每個節點為根的某指標 | 先一次 DFS 算出某根答案，再一次 DFS 將答案由父推子，`O(n)` | [樹中距離之和](https://leetcode.cn/problems/sum-of-distances-in-tree) |\n\n**小提醒**\n- 想清楚遞迴函式「回傳什麼」與「順帶更新什麼全域答案」，兩者常常不同。\n- 後序適合自底向上聚合；前序適合自頂向下傳遞路徑資訊。\n- 換根的關鍵是寫出由父節點答案推導子節點答案的轉移式，避免每個根都重算。",
    },
  ],
} as TutorialData.Root;
