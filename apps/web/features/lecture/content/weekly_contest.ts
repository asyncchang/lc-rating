import type { TutorialData } from "@/types";

/**
 * 週賽 AK 之路：以「一場週賽的解剖」為主軸的原創學習路線。
 * 節點 id 與 public/studyplan/weekly_contest.json 一一對應，
 * 讓題單頁能依 id 取得對應章節的講解。
 */
export const weeklyContest = {
  id: 1,
  title: "週賽 AK 之路",
  description:
    "以一場週賽的解剖為主軸：五個階段從 Q1/Q2 手速、Q3 建模練到 Q4 深度武器，再用三場全真模擬檢驗實戰節奏與罰時控制。",
  src: "",
  last_update: "2026-07-04T20:00:00.000000+00:00",
  summary:
    "這條路線不按演算法教科書的目錄走，而是按「一場週賽的解剖」來設計。每場週賽固定四題：Q1/Q2 考手速與細節、Q3 考建模、Q4 考深度。想拿好名次甚至 AK（All Kill，四題全解），需要的不是把所有演算法學一遍，而是把每個位置需要的能力分開練到位。\n\n**五個階段的分工：**\n- **速度關**：Q1 三分鐘、Q2 八分鐘內一次通過，名次的地板由此決定\n- **建模關**：Q3 最常見的六種模型，練到「看到訊號就想到工具」\n- **攻堅關**：資料結構選型與 DP 狀態設計，把 Q3 變成穩定得分、偶爾偷到 Q4\n- **天花板**：Q4 的高階武器，每解出一題就是一次大漲分\n- **實戰關**：把技巧帶進 90 分鐘的計時賽場，練節奏、控罰時、做復盤\n\n**一條貫穿全程的暗線：貪心思維。** 週賽裡最能拉開名次的能力之一，就是「先想有沒有一個不反悔的局部最優選擇」。它不是某一階段的專屬技巧，而是一種一路都要帶著的直覺：Q1/Q2 常常是「排序後取兩端」「能配就配」的貪心；Q3 的「最少操作」「最大收益」十有八九先試貪心，不行才升級成 DP；Q4 的關鍵觀察也常是「發現某個貪心序在此題成立」。所以每遇到最值題，養成三連問——**排序鍵是什麼？能不能用交換論證說明不會更差？想不出證明時 60 秒能不能造出反例？** 這套反射練熟了，等於在每一題都多開一條最短路。\n\n**使用建議：**\n- 每週安排 4-5 天練當前階段的題，週末打一場真實或 Virtual 週賽\n- 每個階段的題目都按難度遞增排列，從第一題開始按順序做\n- 練速度的題（第一階段）要計時；練深度的題（第四階段）允許最多 90 分鐘再看題解\n- 每道最值題先問「這是不是貪心」，把成立/不成立的判斷理由記進自己的清單\n- 每道卡住的題記下：卡在哪一步、缺什麼訊號、下次怎麼更快想到\n- 打完每場比賽，未解出的題必須在 48 小時內補完，這是漲分最快的一件事",
  children: [
    {
      id: 2,
      title: "1. 速度關：Q1/Q2 十分鐘雙殺（~1400）",
      summary:
        "週賽名次的地板是由 Q1/Q2 的速度決定的：同樣四題全對，快 10 分鐘可能差幾百名；更殘酷的是，一次 WA 罰 5 分鐘，兩次手滑就抵掉一題 Q3 的優勢。這一階段的目標不是「會做」——這些題你本來就會——而是「又快又零罰時」。\n\n**練法：**\n- 每題計時：Q1 等級（<1200）目標 3 分鐘內、Q2 等級（1200-1400）目標 8 分鐘內提交且一次通過\n- 提交前固定過一遍檢查清單：空輸入、單元素、重複值、負數、整數溢位\n- 寫錯一次就記錄原因，統計自己最常見的手滑類型（下標偏移、邊界、讀錯題）\n- 這一階段的題適合當每日熱身，每天 2-3 題維持手感",
      children: [
        {
          id: 3,
          title: "1.1 模擬與枚舉：把題面直譯成程式",
          summary:
            "**這一節在練什麼**\n\n模擬題與枚舉題沒有演算法門檻，考的是「把題目的規則一步不漏地翻譯成程式」的能力。週賽 Q1 幾乎都是這類題，Q2 也常出現「規則稍微複雜一點的模擬」。這一類題失分的原因幾乎從不是不會寫，而是三種情況之一：漏看一條規則、資料範圍估錯導致方法選錯、或下標算錯。\n\n**概念說明：模擬 vs. 枚舉**\n\n- 模擬（Simulation）：題目描述一個隨時間或步驟演化的過程（時鐘走動、佇列排隊、機器人移動、遊戲回合），要求照規則一步步跑完整個過程，回傳最終狀態或某個統計量。\n- 枚舉（Brute-force enumeration）：題目要你檢查所有候選答案（所有子陣列、所有配對、所有排列），資料範圍小到可以直接暴力跑過一遍。\n\n兩者的共同點：解法本身沒有「巧思」，正確率完全取決於程式碼是否精準對應題意。\n\n**解題步驟**\n\n1. 讀題時把規則抄成條列（輸入是什麼、每一步做什麼、什麼時候停、要輸出什麼），寫程式時逐條對照，不要嘗試在腦中一次消化整段敘述\n2. 估算資料範圍：`n <= 100` 或 `n <= 1000` 的雙重迴圈直接寫，`n <= 10` 甚至可以三重迴圈或枚舉排列——週賽比的是總完成時間，不是漂亮解法\n3. 選資料結構前先問「這一步之後，我還需要用到目前的中間結果嗎？」需要就存下來，不要每一步重算\n4. 寫完後不要立刻交，先手動跑一次題目給的範例，逐行對照輸出\n\n**完整實作範例**\n\n模擬題的代表模式是「事件按時間推進，維護一個或多個狀態變數」。以下是「服務台依序處理任務，任務來了就交給服務台，但要等它有空」的通用骨架：\n\n```cpp\n// n 位顧客依序抵達，arrival[i] 是到達時間，duration[i] 是服務所需時間，\n// 服務台一次只能服務一位，求總等待時間（等待 = 開始服務時間 - 到達時間）\nlong long totalWaitingTime(vector<int>& arrival, vector<int>& duration) {\n    long long freeAt = 0;   // 服務台下一次有空的時間\n    long long totalWait = 0;\n    for (int i = 0; i < (int)arrival.size(); ++i) {\n        long long start = max((long long)arrival[i], freeAt); // 實際開始服務時間\n        totalWait += start - arrival[i];\n        freeAt = start + duration[i];                          // 更新服務台狀態\n    }\n    return totalWait;\n}\n```\n\n**逐步解法：以「LC 1701 Average Waiting Time」為例**\n\n題意：顧客按到達順序排隊，廚房一次只能做一份餐，必須依序製作，求平均等待時間。\n\n- 條列規則：① 顧客按輸入順序抵達（已排序）；② 廚房同時間只能做一份；③ 廚房仍在忙時顧客要等到有空才開始計時\n- 狀態變數：`freeAt`（廚房下一次有空的時間點）\n- 逐步：對每位顧客，實際開始時間 = `max(到達時間, freeAt)`；等待時間 = 開始時間 - 到達時間；做完後更新 `freeAt = 開始時間 + 備餐時間`\n- 這正是上面模板的翻版：一個變數描述「資源何時釋放」，逐一推進即可，O(n) 解決\n\n**常見翻車點**\n\n- 讀錯題是這個等級最大的罰時來源。特別注意「恰好/至少/至多」「子陣列（連續）/子序列（可不連續）」「回傳下標/回傳值」這三組字眼\n- 字串與下標處理寧可多寫幾個變數，也不要在一行裡塞三個 `+1/-1`\n- 模擬會不會進入無窮迴圈（狀態重複出現）沒有事先想清楚，是進階模擬題最常見的超時原因；狀態空間有限時，先想清楚要不要偵測循環節\n- 平均值要不要無條件捨去（`long long` 整除）還是要精確到小數，題目通常會明講，型別寫錯會整題錯",
        },
        {
          id: 4,
          title: "1.2 雜湊與計數：O(n) 的第一直覺",
          summary:
            "**這一節在練什麼**\n\n看到「出現次數」「是否存在」「配對」，第一反應就應該是雜湊表或計數陣列，這是 Q1/Q2 最高頻的工具，也是後面滑動視窗、前綴和技巧的地基——它們幾乎都要靠雜湊表把「我需要的東西是否出現過」這件事壓到 O(1) 查詢。\n\n**概念說明**\n\n雜湊表（`unordered_map` / Python `dict`）與計數陣列本質上做同一件事：把「值 → 出現次數（或其他附加資訊）」的對應關係存起來，用空間換時間，把原本要 O(n) 掃描才能回答的問題壓到 O(1) 查詢。差別只在於：\n- 值域小且連續（例如 0-100、小寫字母 a-z）→ 用陣列，常數更小、沒有雜湊碰撞問題\n- 值域大或稀疏（任意整數、字串）→ 用雜湊表\n\n**解題步驟**\n\n1. 先問：「這題需要知道『某個值出現過幾次』還是『某個值是否出現過』？」前者要計數，後者布林即可，兩者實作幾乎一樣但語意不同，想清楚能省下除錯時間\n2. 決定「邊掃邊查」還是「先掃完整個陣列再查」：找配對的題（兩數之和型）通常邊掃邊查；全域統計後再排序/篩選的題（找出現次數最多的元素）通常先掃完整個陣列\n3. 若查詢目標和被計數的東西是同一層（例如兩數加起來等於 target），直接用值本身當 key；若題目要求對次數本身再統計（例如「有多少種出現次數」），代表要對計數結果再套一層雜湊表或排序，先想清楚是哪一層\n\n**核心習慣**\n\n- 值域小（例如 `0-100` 或小寫字母）用陣列計數，比雜湊表更快也更不容易寫錯\n- 「兩數之和」型的配對問題，邊掃邊查「我需要的另一半是否已出現」，一次遍歷完成\n- 「每個數字的出現次數」統計完之後，常常還要對次數再做一次統計或排序——先想清楚題目要的是哪一層\n\n**完整實作範例**\n\n邊掃邊查的兩數之和骨架：\n\n```cpp\n// 找出陣列中兩個數字之和等於 target 的下標\nvector<int> twoSum(vector<int>& nums, int target) {\n    unordered_map<int, int> seen; // 值 -> 下標\n    for (int i = 0; i < (int)nums.size(); ++i) {\n        int need = target - nums[i];\n        auto it = seen.find(need);\n        if (it != seen.end()) {\n            return {it->second, i};\n        }\n        seen[nums[i]] = i; // 先查再存，避免同一元素配對自己\n    }\n    return {};\n}\n```\n\n值域小、用陣列計數的骨架：\n\n```cpp\nint freq[26] = {};              // 值域為小寫字母 a-z\nfor (char c : s) {\n    ++freq[c - 'a'];\n}\n```\n\n**逐步解法：以「LC 2260 Minimum Consecutive Cards to Pick Up」為例**\n\n題意：找出最短的連續子陣列，使其中至少有兩張牌數字相同。\n\n- 訊號：「某個值上一次出現的位置」——雜湊表存「值 → 最近一次出現的下標」\n- 逐步：邊掃邊查 `lastSeen[nums[i]]` 是否存在；存在的話候選答案是 `i - lastSeen[nums[i]] + 1`；不論是否命中，都要更新 `lastSeen[nums[i]] = i`\n- 這正是「邊掃邊查 + 立即更新」的雜湊表標準用法，一次遍歷 O(n) 解決\n\n**常見翻車點**\n\n計數陣列開太小（沒看清值域上限）、忘記處理「自己和自己配對」的情況（`twoSum` 若不先查後存，會把自己算進去）、把「元素」和「次數」兩層搞混（明明要找次數最多的元素是誰，卻回傳次數本身）。",
        },
        {
          id: 5,
          title: "1.3 排序與雙指標：先排序再思考",
          summary:
            "**這一節在練什麼**\n\n很多題排序之後結構就變得一目了然：最值在兩端、相同元素相鄰、配對可以貪心。排序是 Q2 最常見的第一步，也是絕大多數貪心題的地基——「排序 + 從一端掃」就是最基礎的貪心骨架。\n\n**概念說明：為什麼排序常常是第一步**\n\n未排序的陣列裡，「最大的兩個數在哪」這種問題必須整個掃過一遍才能回答；一旦排序，這類問題就變成「頭尾」「相鄰」這種可以直接讀出來的結構。雙指標正是靠著排序後的單調性，把原本 O(n²) 的兩兩比較壓到 O(n)：兩根指標各自只往一個方向移動，總移動次數不超過 2n，因此均攤是線性時間。雙指標有兩種常見形態：\n\n1. **對撞指標**：一頭一尾各一根，往中間夾，用在「找配對」「判斷能不能兩兩湊成某條件」\n2. **快慢指標／同向雙指標**：兩根指標同方向移動，維護一個可伸縮的區間，這其實就是下一節滑動視窗的雛形\n\n**解題步驟**\n\n1. 拿到陣列題先自問：「排序會不會破壞答案？」不需要保留原順序的題，排序幾乎總是簡化問題\n2. 需要保留原下標時，排「(值, 下標)」的配對而不是值本身\n3. 決定指標移動的條件，並在心裡跑一次交換論證：為什麼這樣移動不會漏解？想不清楚就代表移動條件錯了\n4. 排序後的配對/收縮問題用雙指標：一端各放一個指標，根據條件移動其中一個\n\n**核心習慣**\n\n- 拿到陣列題先自問：「排序會不會破壞答案？」不需要保留原順序的題，排序幾乎總是簡化問題\n- 需要保留原下標時，排「(值, 下標)」的配對而不是值本身\n- 排序後的配對/收縮問題用雙指標：一端各放一個指標，根據條件移動其中一個，每步都要說得出「為什麼這樣移動不會漏解」——這句「為什麼不會漏解」其實就是貪心的交換論證\n\n**貪心味的雙指標範例**\n\n```cpp\n// 頭尾配對：最小配最大，讓「最大的一對和」盡量小\nsort(a.begin(), a.end());\nint n = a.size(), best = 0;\nfor (int i = 0; i < n / 2; ++i) {\n    best = max(best, a[i] + a[n - 1 - i]);  // 兩端往中間夾\n}\n```\n\n為什麼安全？若最優解把兩個大的配在一起，那一對和會更大；拆開讓它們各自去配小的，最大對和只會變小或不變。這就是最典型的交換論證。\n\n**逐步解法：以「LC 15 3Sum」為例**\n\n題意：找出陣列中所有相加為 0 的三元組（去重）。\n\n- 先排序。固定最小的一個數 `a[i]`，問題退化成「在 `i+1..n-1` 範圍內找兩數之和等於 `-a[i]`」——這就是排序後的對撞雙指標\n- 去重技巧：外層 `i` 與內層 `left/right` 在移動時都要跳過與前一個相同的值，否則會產生重複三元組\n\n```cpp\nvector<vector<int>> threeSum(vector<int>& a) {\n    sort(a.begin(), a.end());\n    int n = a.size();\n    vector<vector<int>> ans;\n    for (int i = 0; i < n - 2; ++i) {\n        if (i > 0 && a[i] == a[i - 1]) continue; // 跳過重複的第一個數\n        int left = i + 1, right = n - 1;\n        while (left < right) {\n            long long sum = (long long)a[i] + a[left] + a[right];\n            if (sum < 0) {\n                ++left;\n            } else if (sum > 0) {\n                --right;\n            } else {\n                ans.push_back({a[i], a[left], a[right]});\n                while (left < right && a[left] == a[left + 1]) ++left; // 跳過重複\n                while (left < right && a[right] == a[right - 1]) --right;\n                ++left;\n                --right;\n            }\n        }\n    }\n    return ans;\n}\n```\n\n**常見翻車點**\n\n自訂比較器寫反方向、排序後忘了題目要求輸出原始下標、雙指標移動條件用 `<` 還是 `<=` 沒想清楚導致漏掉相等的情況、3Sum 忘記跳過重複值導致答案重複。",
        },
        {
          id: 6,
          title: "1.4 前綴和與差分：區間統計標配",
          summary:
            "**這一節在練什麼**\n\n「子陣列的和」「區間內的數量」這類措辭出現時，前綴和能把每次區間查詢從 O(n) 壓到 O(1)；反過來「對一段區間整體加值」則是差分陣列的訊號。\n\n**概念說明：前綴和與差分互為逆運算**\n\n前綴和解決「查詢」：給定陣列，多次問「某段區間的和是多少」，先花 O(n) 預處理，之後每次查詢 O(1)。差分解決「修改」：多次對一段區間整體加值，最後才需要知道每個位置的值，用差分把每次修改壓到 O(1)，最後一次前綴和把差分還原成真正的陣列，總複雜度 O(n+m)。兩者是同一個工具的正反面：前綴和是「陣列 → 區間查詢」，差分是「區間修改 → 陣列」。\n\n**核心模板**\n\n```cpp\n// 前綴和：pre[i] = a[0..i) 之和，區間 [l, r] 的和 = pre[r+1] - pre[l]\nvector<long long> pre(n + 1);\nfor (int i = 0; i < n; ++i) {\n    pre[i + 1] = pre[i] + a[i];\n}\n```\n\n二維版本同理：矩陣前綴和讓任意子矩陣的和變成四項加減。\n\n**解題步驟**\n\n1. 看到「子陣列的和」「區間內的數量」先建前綴和陣列\n2. 看到「對一段區間整體加值」「多次區間更新，最後才需要結果」先建差分陣列\n3. 若同時要「邊查詢邊修改」，前綴和/差分就不夠了（它們是靜態或離線工具），要換成樹狀陣列或線段樹（見 3.6、4.6 節）\n4. 前綴和陣列一律開 `long long`，下標從 1 開始存，公式固定用 `pre[r+1] - pre[l]`，不要每次重新推導\n\n**核心習慣**\n\n- 前綴和陣列開 `n+1` 格、從 1 開始存，可以避開一堆邊界判斷\n- 和可能超過 `int`，前綴和一律用 64 位整數\n- 「有多少個子陣列滿足…」常常是前綴和 + 雜湊表：邊掃邊查「需要的前綴值出現過幾次」\n\n**差分陣列完整範例：LC 1094 Car Pooling**\n\n題意：多輛行程 `[numPassengers, from, to)`，同一輛車依序接載乘客，求任何時刻車上人數是否曾超過容量 `capacity`。\n\n- 每個行程對「人數變化量」做一次區間加值：上車地點 `+numPassengers`，下車地點 `-numPassengers`——這正是差分的定義：對區間 `[from, to)` 整體加 `numPassengers`\n- 最後對差分陣列做一次前綴和，得到每個位置的即時人數，逐一檢查是否超過 capacity\n\n```cpp\nbool carPooling(vector<vector<int>>& trips, int capacity) {\n    vector<int> diff(1001, 0); // 位置範圍 0-1000\n    for (auto& t : trips) {\n        int passengers = t[0], from = t[1], to = t[2];\n        diff[from] += passengers;\n        diff[to] -= passengers;\n    }\n    int cur = 0;\n    for (int d : diff) {\n        cur += d;\n        if (cur > capacity) return false;\n    }\n    return true;\n}\n```\n\n**「子陣列和」+ 雜湊表範例：LC 560 Subarray Sum Equals K**\n\n- 訊號：「有多少個子陣列的和等於 k」——把子陣列和用前綴和表示：`pre[r+1] - pre[l] = k`，也就是「對每個右端點 r，找有多少個左端點 l 使得 `pre[l] = pre[r+1] - k`」\n- 邊掃邊查：維護「前綴值 → 出現次數」的雜湊表，掃到每個位置時先查 `pre[i] - k` 出現過幾次，再把 `pre[i]` 本身計入雜湊表\n\n```cpp\nint subarraySum(vector<int>& nums, int k) {\n    unordered_map<long long, int> cnt;\n    cnt[0] = 1; // 空前綴\n    long long pre = 0;\n    int ans = 0;\n    for (int x : nums) {\n        pre += x;\n        auto it = cnt.find(pre - k);\n        if (it != cnt.end()) ans += it->second;\n        ++cnt[pre];\n    }\n    return ans;\n}\n```\n\n**常見翻車點**\n\n下標偏移（`pre[r+1] - pre[l]` 寫成 `pre[r] - pre[l]`）、忘記開 long long、以及忘記把空前綴 `cnt[0] = 1` 放進雜湊表（會漏掉「從頭開始就滿足條件」的子陣列），是這類題最常見的死法。",
        },
        {
          id: 27,
          title: "1.5 字串操作與正規表示式：週賽 Q1/Q2 的文字題",
          summary:
            "**這一節在練什麼**\n\n週賽 Q1/Q2 常出現「對字串做某種變形、統計、比對」的題。它們不需要高級演算法，但字元陣列的索引操作、邊界反轉、子字串提取特別容易手滑——是 Q2 最常見的罰時來源之一。\n\n**概念說明**\n\n字串題的兩個地雷來源：① 索引運算（反轉、旋轉、分段時的下標公式）；② 語言本身的字串可變性（Java/Python 字串不可變，頻繁修改會導致隱藏的 O(n) 甚至 O(n²)）。週賽的字串題很少需要 KMP 這類進階演算法（那是 Q4 的範疇，見 4.5 節），Q1/Q2 的字串題考的是「精準模擬」。\n\n**解題步驟**\n\n1. 涉及「反轉」「刪除」「替換」時，先在紙上畫出字元陣列的索引變化，算出 start 與 end 的對應關係再動手\n2. 決定要不要先把字串轉成可變的陣列/列表（Java `char[]`、Python `list(s)`），需要多次原地修改時這一步能省下大量麻煩\n3. 子字串提取前先確認題目的區間是半開 `[l, r)` 還是閉區間 `[l, r]`，並對照所用語言的 API 語意\n4. 統計字元種類/次數的題，用計數陣列（見 1.2 節）而不是重複掃描字串\n\n**核心習慣**\n\n- 涉及「反轉」「刪除」「替換」時，先畫出字元陣列的索引變化，算出 start 與 end 的對應關係再動手\n- 子字串提取注意半開區間還是閉區間；C++ `substr(pos, len)` 與 Python `s[start:end]` 的語意不同，現場寫錯會 WA\n- 正規表示式在週賽裡幾乎用不到，但「紀錄每一種字元的個數」這種計數思維（等價於陣列版的 regex character class）是這類題的標配\n\n**完整實作範例：LC 541 Reverse String II**\n\n題意：字串每 `2k` 個字元一組，每組的前 `k` 個字元反轉，其餘不變；剩不到 `k` 個就整段反轉，剩超過 `k` 但不到 `2k` 就只反轉前 `k` 個。\n\n```cpp\nstring reverseStr(string s, int k) {\n    int n = s.size();\n    for (int start = 0; start < n; start += 2 * k) {\n        int left = start;\n        int right = min(start + k, n) - 1; // 這一組要反轉的右邊界\n        while (left < right) {\n            swap(s[left++], s[right--]);\n        }\n    }\n    return s;\n}\n```\n\n- 逐步：外層迴圈以 `2k` 為步伐跳著處理，內層只反轉 `[start, min(start+k, n)-1]`，`min` 負責處理「剩不到 k 個」的邊界\n- 這題的關鍵不是演算法，而是把「每 2k 個一組，反轉前 k 個」這句話精準翻成迴圈邊界——典型的模擬＋索引運算組合\n\n**常見翻車點**\n\n- 反轉後下標換算錯：反轉 `[l, r]` 後，`i` 會對應到 `l + r - i`，不是簡單的 `n - 1 - i`\n- 字串是不可變物件時（如 Java/Python），頻繁做中間插入會變成 O(n²)；先轉成陣列或 list 再操作\n- 空字串與長度為 1 的邊界常常漏掉，提交前必須單獨測試",
        },
      ],
    },
    {
      id: 7,
      title: "2. 建模關：Q3 的常客套路（1400-1700）",
      summary:
        "Q3 的本質是「把敘述翻譯成已知模型」。題目不會告訴你該用什麼演算法，但訊號都藏在措辭裡：「連續子陣列」指向滑動視窗、「最大化最小值」指向二分答案、「最少操作次數 / 最大收益」先想貪心、貪心給不出理由才是 DP。這一階段練六個出現率最高的模型，目標是把「辨認訊號 → 套用模型」的時間壓進 5 分鐘。\n\n**先想貪心的習慣：** 遇到最值題，第一反應不是「這要用什麼高級演算法」，而是「有沒有一個排序 + 從一端掃的貪心？」貪心能解就最省時間；判斷不了時用 60 秒構造反例，造不出來就先按貪心寫，否則升級到 DP。這條判斷分支練熟了，Q3 的建模速度會明顯變快。\n\n**練法：**\n- 每題動手前先寫下：我認為這是什麼模型、依據是哪個訊號\n- 最值題先問「能不能貪心」，寫下排序鍵與交換論證，再決定要不要改 DP\n- 做完對照題解：模型判斷錯的題，把正確訊號補進自己的判斷清單\n- 單題上限 40 分鐘，超時就看提示——這一階段的目的是積累模型，不是硬磕",
      children: [
        {
          id: 8,
          title: "2.1 滑動視窗：連續子陣列的預設解法",
          summary:
            "**訊號**\n\n「最長/最短的連續子陣列（子字串），滿足某條件」，且條件具有單調性：視窗擴大時條件只會朝一個方向變化（例如和變大、種類數變多）。\n\n**概念說明：滑動視窗為什麼是 O(n)**\n\n滑動視窗本質上是「同向雙指標」的特例：右指標負責擴張視窗、左指標負責收縮視窗，兩者都只往同一個方向移動，從不回頭。因為每根指標的移動總次數都不超過 n，整個演算法是 O(n)——雖然外觀是雙層迴圈，但內層 `while` 的總執行次數被外層 `for` 的次數上限住，這叫「均攤分析」（amortized analysis）。\n\n視窗能這樣維護的前提是「單調性」：視窗擴大時，判斷條件只會朝一個方向變化（例如和變大、不同字元數變多），不會忽好忽壞。少了這個前提，收縮左端點時無法保證「移動後條件一定變好」，演算法就不成立。\n\n**核心模板**\n\n```cpp\n// 求最長的滿足條件的視窗\nint left = 0;\nfor (int right = 0; right < n; ++right) {\n    add(a[right]);               // 把右端點納入視窗\n    while (!ok()) {              // 條件被破壞就收縮左端\n        remove(a[left++]);\n    }\n    ans = max(ans, right - left + 1);\n}\n```\n\n**解題步驟**\n\n1. 先驗證單調性：視窗擴大後條件會不會「壞了又好」？涉及負數、乘積、異或的題通常沒有單調性，要換工具（前綴和、單調佇列）\n2. 決定視窗是「定長」還是「可變長」：定長視窗右進左出，維護視窗內統計量；可變長視窗右進、觸發條件才收縮左端\n3. 想清楚 `add`/`remove` 兩個操作各要更新哪些狀態變數（總和、計數、種類數）\n4. 「恰好 K 種/K 個」型的計數常用「至多 K 減去至多 K-1」的轉化\n\n**思考重點**\n\n- 先驗證單調性：如果視窗擴大後條件可能「壞了又好」（例如涉及乘積為負、異或），滑動視窗不適用，要改前綴和或其他工具\n- 定長視窗是簡化版：右進左出，維護視窗內統計量即可\n- 「恰好 K 種/K 個」型的計數常用「至多 K 減去至多 K-1」的轉化\n\n**逐步解法：以「LC 3 Longest Substring Without Repeating Characters」為例**\n\n題意：找出不含重複字元的最長子字串長度。\n\n- 驗證單調性：視窗內出現重複字元後，只要不移出重複的那個字元，重複狀態就不會消失——左指標要一路收縮到重複字元被移出為止\n- 維護狀態：用雜湊表記錄「字元 → 最近出現位置」；當 `s[right]` 已在視窗內出現過，把 `left` 直接跳到「該字元上次出現位置 + 1」（比逐格收縮更快，邏輯等價）\n\n```cpp\nint lengthOfLongestSubstring(string s) {\n    unordered_map<char, int> lastSeen;\n    int left = 0, ans = 0;\n    for (int right = 0; right < (int)s.size(); ++right) {\n        if (lastSeen.count(s[right]) && lastSeen[s[right]] >= left) {\n            left = lastSeen[s[right]] + 1;  // 直接跳過重複字元\n        }\n        lastSeen[s[right]] = right;\n        ans = max(ans, right - left + 1);\n    }\n    return ans;\n}\n```\n\n**常見翻車點**\n\n- 用「逐格收縮」寫法時忘記在 `while` 迴圈內同步更新統計量，導致視窗狀態與實際不一致\n- 定長視窗題把「先加入右端」和「視窗大小超過 k 就先移除左端」的順序寫反\n- 「至多 K」與「恰好 K」搞混：恰好 K 通常要用「至多K」減「至多K-1」，不能直接寫等於條件的收縮邏輯\n\n**時間目標**\n\n模型認出來之後，這類題應該在 15 分鐘內寫完；認不出來才是真正的瓶頸。",
        },
        {
          id: 9,
          title: "2.2 二分搜尋與二分答案",
          summary:
            "**訊號**\n\n「最大化最小值」「最小化最大值」「至少需要多少才能滿足」——答案本身有單調性：如果 x 可行，比 x 更寬鬆的值也可行。此外，有序結構上的查詢（第一個 ≥ x 的位置）是二分的直接形式。\n\n**概念說明：二分搜尋的正確性來自單調的可行性函式**\n\n二分搜尋能work的唯一條件是：存在一個判斷函式 `check(x)`，它在答案的定義域上是單調的——要求「找最小可行值」時，`check(x)` 必須滿足「一旦為真，所有比 x 大的值也為真」（反之找最大可行值則反過來）。二分搜尋做的事，其實是在這個單調的布林序列裡，用 O(log n) 找到「從假變真」的那個分界點，而不是真的在「猜答案」。\n\n二分搜尋分成兩種用途，週賽裡常混用：\n1. **在有序陣列上查值**（std::lower_bound / upper_bound）：找第一個 ≥ x 的位置，屬於「資料結構上的二分」\n2. **二分答案**：答案本身不是陣列裡的元素，而是一個滿足單調可行性的數值，透過 `check` 函式驗證某個候選答案是否可行\n\n**核心模板**\n\n```cpp\n// 二分答案：找最小的可行值\nlong long lo = minAns, hi = maxAns;\nwhile (lo < hi) {\n    long long mid = lo + (hi - lo) / 2;\n    if (check(mid)) {\n        hi = mid;      // mid 可行，往更小找\n    } else {\n        lo = mid + 1;  // mid 不可行，必須更大\n    }\n}\n// lo 即為答案\n```\n\n**解題步驟**\n\n1. 先確認答案存在單調性：把可行值畫成一條數線，是否呈現「一段不可行接一段可行」的形狀？想不清楚就先手算兩三個小範例驗證\n2. 明確定義 `check(x)` 的語意（例如「x 天內能不能完成所有任務」），並想清楚它要怎麼實作、複雜度多少\n3. 決定二分邊界 `lo`、`hi` 的初始範圍，寧可給寬一點，只要 `check` 在邊界上邏輯正確\n4. 用「找最小可行」或「找最大可行」兩個固定模板寫，不要每次現場推導 `mid` 的更新方向\n\n**思考重點**\n\n- 把力氣花在 `check` 上：它通常是一個貪心或線性掃描，先在紙上確認它是 O(n) 或 O(n log n)\n- 明確寫下「check(x) 為真代表什麼」，並驗證單調方向，方向想反是最常見的錯\n- 二分邊界用「找最小可行」或「找最大可行」兩個模板之一，不要每次現場推導\n\n**逐步解法：以「LC 875 Koko Eating Bananas」為例**\n\n題意：Koko 每小時吃 `speed` 根香蕉（吃某一堆時，若這堆不足 `speed` 根就吃完這堆並結束這小時），求在 `h` 小時內吃完所有香蕉的最小 `speed`。\n\n- 單調性：`speed` 越大，花的小時數越少；`speed` 越小，花的小時數越多——存在一個分界點，右邊都可行，左邊都不可行\n- `check(speed)`：算出以這個速度吃完所有堆需要的總小時數，看是否 `<= h`\n\n```cpp\nint minEatingSpeed(vector<int>& piles, int h) {\n    auto check = [&](long long speed) {\n        long long hours = 0;\n        for (int p : piles) {\n            hours += (p + speed - 1) / speed;  // 向上取整\n        }\n        return hours <= h;\n    };\n    long long lo = 1, hi = *max_element(piles.begin(), piles.end());\n    while (lo < hi) {\n        long long mid = lo + (hi - lo) / 2;\n        if (check(mid)) hi = mid; else lo = mid + 1;\n    }\n    return (int)lo;\n}\n```\n\n- 逐步：`check` 是 O(n) 的線性掃描；外層二分 O(log(max speed))，總複雜度 O(n log(max speed))\n\n**常見翻車點**\n\n- `check` 的方向想反，導致二分結果收斂到錯誤的一端——寫完先手動驗證 `check(lo)` 與 `check(hi)` 是否符合預期\n- 二分上下界開太窄，漏掉真正的答案（例如下界該是 1 卻寫成 0）\n- `mid` 計算用 `(lo + hi) / 2` 在數字很大時可能溢位，一律寫成 `lo + (hi - lo) / 2`\n\n**時間目標**\n\n看到「最小化最大值」就直接開寫二分骨架，把思考時間全部留給 check 函式。",
        },
        {
          id: 10,
          title: "2.3 貪心與交換論證",
          summary:
            "**訊號**\n\n「最少操作次數」「最大總收益」「最多能選幾個」，且每一步的選擇之間沒有複雜的後效性——這一步選什麼，不會回頭改變前面已經定案的價值。排序後從一端掃過去、每步做當下最划算且不反悔的選擇，就是貪心的雛形。貪心是週賽 Q2/Q3 出現率最高的思路之一，先想貪心、不行再升級成 DP，是最省時間的解題順序。\n\n**貪心的三種常見骨架**\n\n1. **排序後取一端**：要最大化就從大的拿、要省成本就從小的拿。「選 K 個數使總和最大」就是排序後取前 K 大。\n2. **排序後雙指標配對**：最大配最小（求最平衡）或能塞就塞（求最少組）。救生艇、頭尾配對都屬此類。\n3. **區間排程**：按右端點排序，能不衝突就選，選了就更新邊界。這是「最多不相交區間 / 最少移除區間」的標準解。\n\n```cpp\n// 區間排程貪心：按右端點排序，盡量多選不重疊的區間\nsort(a.begin(), a.end(),\n     [](auto& x, auto& y) { return x[1] < y[1]; });  // 右端點小的優先\nint cnt = 0, last = INT_MIN;\nfor (auto& it : a) {\n    if (it[0] >= last) {  // 與已選不衝突\n        ++cnt;\n        last = it[1];     // 更新邊界，越早結束越好\n    }\n}\n```\n\n**交換論證：貪心的唯一驗證法**\n\n貪心最危險的地方是「看起來對」。真正的驗證叫交換論證：假設某個最優解在某一步和貪心的選擇不同，把它那一步換成貪心的選擇，論證結果不會變差。既然總能換成貪心的樣子，貪心就也是最優的。以區間排程為例——若最優解選了一個結束更晚的區間，把它換成結束更早的那個，後面能放的空間只會更多，答案不會變差。\n\n**賽場上的實用判斷**\n\n- 直覺有了先花 60 秒構造反例：最小輸入、全部相同、一大一小。造不出反例、時間又緊，週賽裡可以先交——但賽後必須補上證明或反例，否則這題等於沒學會。\n- 第一個貪心策略 WA 了，不要急著加特判補丁。先重新想排序鍵：貪心 WA 大多是「方向整個錯了」，而不是差一個邊界。\n- 附加限制（總和要偶數、最多選 K 個、要配對）用「先貪心拿、再用最小代價修正」處理：例如和是奇數就丟掉已選中最小的奇數。\n\n**常見貪心排序鍵**\n\n截止時間、結束位置（區間）、性價比（價值/重量）、兩數的位差、字典序高位。看到最值題先問自己：把元素依什麼排好之後，從一端掃是不是就對了？\n\n**逐步解法：以「LC 435 Non-overlapping Intervals」為例**\n\n題意：給一組區間，求最少要移除幾個區間，才能讓剩下的區間互不重疊。\n\n- 轉化：「移除最少」等於「保留最多不重疊區間」，答案 = 總數 - 最多可保留數量，回到區間排程的標準骨架\n- 排序鍵：按右端點排序。交換論證——若最優解選了一個結束更晚的區間，把它換成結束更早、且和已選集合不衝突的區間，後面能放的空間只會更多，答案不會變差，因此「優先選右端點小的」一定不劣\n\n```cpp\nint eraseOverlapIntervals(vector<vector<int>>& intervals) {\n    sort(intervals.begin(), intervals.end(),\n         [](auto& a, auto& b) { return a[1] < b[1]; });  // 按右端點排序\n    int kept = 0;\n    long long lastEnd = LLONG_MIN;\n    for (auto& it : intervals) {\n        if (it[0] >= lastEnd) {   // 與已保留的區間不衝突\n            ++kept;\n            lastEnd = it[1];\n        }\n    }\n    return (int)intervals.size() - kept;\n}\n```\n\n- 逐步驗證：對每個區間只問「它的左端點是否 ≥ 目前保留區間的右端點」，一次遍歷 O(n log n)（含排序）\n\n**常見翻車點**\n\n「排序鍵選錯」佔貪心 WA 的大半；tie-break（相等時誰先）沒定義清楚是第二名。還有一種陷阱是「這題其實不是貪心」——當選擇之間互相牽制（選了大的會擋住後面更好的），交換論證會失敗，這時要果斷改用 DP。",
        },
        {
          id: 11,
          title: "2.4 網格與 BFS/DFS",
          summary:
            "**訊號**\n\n輸入是網格或圖、問「最少步數」「能否到達」「連通區域」。等權最短路用 BFS，連通性與計數用 DFS/BFS 都行。\n\n**概念說明：為什麼 BFS 能找到最短路**\n\nBFS 按「離起點的距離」一層一層往外擴散——先把距離為 1 的點全部處理完，再處理距離為 2 的點，以此類推。因為擴散順序嚴格按距離遞增，第一次抵達某個點的那一刻，走過的步數必然是最短的（不可能有更短的路徑还沒被走到，因為更短的路徑所在的那一層早就被處理過了）。這個性質只在「每一步的代價相同（等權圖）」時成立；邊權不同時要換 Dijkstra（見 3.4 節）。\n\nDFS 則是沿著一條路徑一路走到底才回頭，天然適合「窮舉所有路徑」「判斷連通性」「計算連通塊大小」，但不保證第一次抵達就是最短路。\n\n**核心模板**\n\n```cpp\n// 網格 BFS：dist 兼作 visited\nvector<vector<int>> dist(m, vector<int>(n, -1));\nqueue<pair<int, int>> q;\ndist[sx][sy] = 0;\nq.push({sx, sy});\nint dirs[5] = {0, 1, 0, -1, 0};\nwhile (!q.empty()) {\n    auto [x, y] = q.front();\n    q.pop();\n    for (int d = 0; d < 4; ++d) {\n        int nx = x + dirs[d], ny = y + dirs[d + 1];\n        if (0 <= nx && nx < m && 0 <= ny && ny < n &&\n            grid[nx][ny] != WALL && dist[nx][ny] < 0) {\n            dist[nx][ny] = dist[x][y] + 1;\n            q.push({nx, ny});\n        }\n    }\n}\n```\n\n**解題步驟**\n\n1. 先確認邊權是否相等：全部相等用 BFS，不相等用 Dijkstra 或 0-1 BFS（見 3.4 節）\n2. 定義「狀態」：座標本身還是「座標 + 附加資訊」（帶鑰匙、剩餘次數、上一步方向）\n3. 決定單源還是多源：多個起點同時擴散時，把所有起點先塞進佇列，一次 BFS 即可得到「離任一起點的最短距離」\n4. visited／dist 標記時機固定在「入隊時」而不是「出隊時」，否則同一個點會被重複塞進佇列\n\n**思考重點**\n\n- 「狀態」不一定只是座標：帶鑰匙、帶剩餘次數的題，把附加資訊也放進狀態（座標 × 附加狀態一起去重）\n- 多個起點同時擴散（多源 BFS）：把所有起點先塞進佇列即可\n- DFS 在深度大的網格會爆遞迴堆疊，計數連通塊以外的場合優先寫 BFS\n\n**逐步解法：以「LC 994 Rotting Oranges」為例**\n\n題意：網格上每格是空地、新鮮橘子或腐爛橘子，腐爛橘子每分鐘會讓上下左右的新鮮橘子也腐爛，求所有橘子腐爛所需的最少分鐘數（若無法全部腐爛回傳 -1）。\n\n- 訊號：「同時從多個起點擴散」——所有一開始就腐爛的橘子是多源 BFS 的起點\n- 逐步：先把所有腐爛橘子的座標塞進佇列、`dist` 設為 0；跑一般的多源 BFS；答案是所有新鮮橘子被感染的最大 `dist`；BFS 結束後若還有新鮮橘子沒被標記，回傳 -1\n\n```cpp\nint orangesRotting(vector<vector<int>>& grid) {\n    int m = grid.size(), n = grid[0].size();\n    queue<pair<int, int>> q;\n    int fresh = 0;\n    for (int i = 0; i < m; ++i)\n        for (int j = 0; j < n; ++j) {\n            if (grid[i][j] == 2) q.push({i, j});\n            else if (grid[i][j] == 1) ++fresh;\n        }\n    int minutes = 0;\n    int dirs[5] = {0, 1, 0, -1, 0};\n    while (!q.empty() && fresh > 0) {\n        int sz = q.size();\n        for (int k = 0; k < sz; ++k) {\n            auto [x, y] = q.front(); q.pop();\n            for (int d = 0; d < 4; ++d) {\n                int nx = x + dirs[d], ny = y + dirs[d + 1];\n                if (nx >= 0 && nx < m && ny >= 0 && ny < n && grid[nx][ny] == 1) {\n                    grid[nx][ny] = 2;\n                    --fresh;\n                    q.push({nx, ny});\n                }\n            }\n        }\n        ++minutes;\n    }\n    return fresh == 0 ? minutes : -1;\n}\n```\n\n- 這裡用「按層擴散」（每輪處理完當前佇列裡的所有元素才 `++minutes`）取代維護 `dist` 陣列，是多源 BFS 常見的簡化寫法\n\n**常見翻車點**\n\n- visited／dist 標記時機寫成「出隊時才標」，導致同一格被重複塞進佇列，複雜度爆炸\n- 多源 BFS 忘記一次把所有起點都塞進佇列，寫成對每個起點各跑一次 BFS，白白浪費時間\n- 按層擴散時把 `++minutes` 放錯位置（應該在處理完整層之後才加，不是每個元素都加）\n\n**時間目標**\n\n模板要練到閉眼寫對：邊界判斷、visited 標記時機（入隊時標，不是出隊時標）都不假思索。",
        },
        {
          id: 12,
          title: "2.5 線性 DP：從遞迴到轉移方程",
          summary:
            "**訊號**\n\n「方案數」「最大/最小總和」，且決策有後效性——當前選擇影響後面能做什麼（相鄰不能同取、狀態會延續）。貪心給不出理由的最值題，多半是 DP。\n\n**概念說明：DP 成立的兩個條件**\n\n動態規劃能work，靠的是兩個性質同時成立：\n\n1. **重疊子問題**：暴力遞迴會把同一個子問題算很多遍（例如費氏數列 `f(n-1)` 和 `f(n-2)` 底下都會重複算到 `f(n-3)`）。用陣列或雜湊表把算過的子問題存起來（記憶化），就能把指數級的重複計算壓成線性\n2. **最優子結構**：問題的最優解可以由子問題的最優解組合出來，而不需要知道子問題「具體是怎麼達成」的過程。這正是 DP 和貪心的分界——貪心假設「當下最優選擇不會被後面推翻」，DP 則老實地把「每一種可能的子狀態」都保留下來，交給轉移方程去選\n\n遇到最值/計數題先試貪心的原因：貪心是 DP 的特例（狀態數壓到 O(1) 且轉移是確定的），能用貪心解決就不必付 DP 的空間與思考成本；貪心的交換論證失敗，通常就代表某個狀態無法只靠「當下最優」代表，需要把它完整地當一個 DP 維度保留下來。\n\n**思考流程（固定四步）**\n\n1. 定狀態：`dp[i]` 代表「考慮前 i 個元素、且滿足某種結尾條件」的最優值/方案數——狀態語意必須一句話說清\n2. 想轉移：第 i 步有哪幾種選擇，每種選擇從哪個更小的狀態來\n3. 定初值與答案位置：空前綴是什麼、答案在最後一格還是所有格的最值\n4. 檢查順序：轉移只依賴更小的 i 就從左往右填\n\n**核心習慣**\n\n- 想不出狀態時，先寫暴力遞迴再加記憶化，遞迴參數就是狀態\n- 「以 i 結尾」是線性 DP 最萬用的狀態設計，最長遞增子序列、最大子陣列和都是它\n- 狀態機 DP（持有/不持有、開/關）就是把「以 i 結尾」再乘上一個小的模式維度\n\n**逐步解法：以「LC 300 Longest Increasing Subsequence」為例**\n\n題意：求陣列中最長嚴格遞增子序列的長度。\n\n- 定狀態：`dp[i]` = 「以 `a[i]` 結尾的最長遞增子序列長度」——這正是「以 i 結尾」狀態設計的標準範例\n- 想轉移：`dp[i] = max(dp[j] + 1)`，對所有 `j < i` 且 `a[j] < a[i]`；若沒有這樣的 `j`，`dp[i] = 1`\n- 答案位置：所有 `dp[i]` 的最大值，不是 `dp[n-1]`（因為最長遞增子序列不一定以最後一個元素結尾）\n\n```cpp\n// O(n^2) 版本：直接對應轉移方程\nint lengthOfLIS(vector<int>& a) {\n    int n = a.size();\n    vector<int> dp(n, 1);\n    int ans = 1;\n    for (int i = 0; i < n; ++i) {\n        for (int j = 0; j < i; ++j) {\n            if (a[j] < a[i]) dp[i] = max(dp[i], dp[j] + 1);\n        }\n        ans = max(ans, dp[i]);\n    }\n    return ans;\n}\n\n// O(n log n) 優化：維護「長度為 len 的遞增子序列，結尾最小可能值」\nint lengthOfLIS_fast(vector<int>& a) {\n    vector<int> tails; // tails[len-1] = 長度為 len 的子序列的最小結尾值\n    for (int x : a) {\n        auto it = lower_bound(tails.begin(), tails.end(), x);\n        if (it == tails.end()) tails.push_back(x);\n        else *it = x;\n    }\n    return tails.size();\n}\n```\n\n- 從 O(n²) 到 O(n log n) 的優化思路：把「所有可能長度的最優結尾值」維護成一個遞增陣列，每個新元素用二分搜尋找到它能延伸或取代的位置——這是 DP 狀態壓縮＋二分搜尋（見 2.2 節）的經典組合\n\n**常見翻車點**\n\n- 答案位置搞混：以為 `dp[n-1]` 就是答案，忘記要取所有 `dp[i]` 的最值\n- 轉移時把「嚴格遞增」與「非嚴格遞增（可以相等）」的邊界條件搞反，`lower_bound` 與 `upper_bound` 用錯\n- 方案數 DP 忘記取模；記憶化遞迴忘記處理已計算過的狀態直接回傳，退化成指數複雜度\n\n**時間目標**\n\n這一節的題允許 40 分鐘。DP 的速度來自狀態設計的存量，不來自現場的靈感。",
        },
        {
          id: 28,
          title: "2.6 前綴和與差分進階：二維與轉化",
          summary:
            "**這一節在練什麼**\n\n當題目從「一維區間」升級到「二維子矩陣」或「離線批量區間修改」時，前綴和與差分是唯一的 O(1)/O(n) 解法。週賽 Q3 的「子矩陣和 / 矩形區域更新」幾乎一定考二維前綴與差分。\n\n**概念說明：從一維推廣到二維**\n\n二維前綴和是「先對每一列做一維前綴和，再對每一列的結果做一維前綴和」的結果，`pre[i][j]` 代表「從 (0,0) 到 (i-1,j-1) 這個矩形內所有元素的和」。查詢任意子矩陣的和時，用「容斥原理」：整個大矩形的和，減去多算的上方與左方，再加回被減兩次的左上角，就是四項加減的由來。\n\n二維差分則是二維前綴和的逆運算：想對子矩陣 `(r1,c1)~(r2,c2)` 整體加值 `v`，等價於在差分陣列的四個角分別做加減，使得「還原前綴和之後」恰好只有這個矩形範圍被加了 `v`。\n\n**核心模板**\n\n```cpp\n// 二維前綴和：pre[i][j] 為 (0,0) 到 (i-1,j-1) 的總和\nvector<vector<long long>> pre(m + 1, vector<long long>(n + 1));\nfor (int i = 0; i < m; ++i) {\n    for (int j = 0; j < n; ++j) {\n        pre[i+1][j+1] = a[i][j] + pre[i][j+1] + pre[i+1][j] - pre[i][j];\n    }\n}\n// 查詢子矩陣 (r1,c1)~(r2,c2) 的和\nlong long sum = pre[r2+1][c2+1] - pre[r1][c2+1] - pre[r2+1][c1] + pre[r1][c1];\n```\n\n**一維差分：區間加整體求和**\n\n對區間 `[l,r]` 整體加 `v`，只需要 `diff[l] += v`, `diff[r+1] -= v`；最後做一遍前綴和還原。多條區間修改 + 單點查詢時，這是 O(n+m) 的標配。\n\n**解題步驟**\n\n1. 先分辨題目是「查詢」還是「修改」：多次查子矩陣和用二維前綴和；多次對子矩陣整體加值、最後才要結果用二維差分\n2. 二維前綴和/差分都要開 `(m+1) x (n+1)`，下標從 1 開始存，避免邊界判斷\n3. 二維差分的四個角先在紙上畫出來，確認每個角的正負號，再翻成程式碼\n4. 資料量夠大時（`10^5` 級別的子矩陣查詢或修改），二維前綴和/差分仍是最省事的做法；只有「邊查邊改」才需要升級成二維樹狀陣列\n\n**思考重點**\n\n- 二維差分（子矩陣整體加值）需要維護四個角，寫錯一個符號整張圖就錯——先在紙上畫出來，確認每個角的正負號\n- 差分與前綴和互為逆運算：先差分修改、再前綴和還原，是離線處理的核心套路\n- 「子陣列和相關」不要只會暴力枚舉：先算前綴和，把問題轉成「找某個前綴值的出現次數」或「和目標值的差距」\n\n**完整實作範例：LC 2536 Increment Submatrices by One**\n\n題意：多次操作，每次對子矩陣 `(r1,c1)~(r2,c2)` 整體加 1，求最終矩陣。\n\n```cpp\nvector<vector<int>> rangeAddQueries(int n, vector<vector<int>>& queries) {\n    vector<vector<int>> diff(n + 2, vector<int>(n + 2, 0));\n    for (auto& q : queries) {\n        int r1 = q[0], c1 = q[1], r2 = q[2], c2 = q[3];\n        diff[r1][c1]     += 1;\n        diff[r1][c2 + 1] -= 1;\n        diff[r2 + 1][c1] -= 1;\n        diff[r2 + 1][c2 + 1] += 1;\n    }\n    vector<vector<int>> ans(n, vector<int>(n, 0));\n    for (int i = 0; i < n; ++i) {\n        for (int j = 0; j < n; ++j) {\n            int up = i > 0 ? ans[i-1][j] : 0;\n            int left = j > 0 ? ans[i][j-1] : 0;\n            int upleft = (i > 0 && j > 0) ? ans[i-1][j-1] : 0;\n            ans[i][j] = diff[i][j] + up + left - upleft; // 二維前綴和還原\n        }\n    }\n    return ans;\n}\n```\n\n- 逐步：對每次操作只碰差分陣列的四個角（O(1)）；所有操作做完後，跑一次二維前綴和把差分還原成真正的矩陣（O(n²)）；總複雜度 O(q + n²) 遠勝過暴力對每次操作都掃整個子矩陣的 O(q·n²)\n\n**常見翻車點**\n\n- 二維前綴和的邊界：多開一格、查詢時 `r2+1/c2+1` 與 `r1/c1` 的搭配\n- 差分陣列 `diff[r+1]` 的 `+1` 忘了寫，導致後面所有元素都被改到\n- 二維差分還原時忘記加回左上角（容斥的第三項），導致重疊區域算重複",
        },
      ],
    },
    {
      id: 13,
      title: "3. 攻堅關：穩定收下 Q3、偷襲 Q4（1700-2000）",
      summary:
        "這一段決定你能不能從「三題選手」變成「穩定三題半」。1700-2000 的題很少考全新的演算法，考的是組合：排序之後維護一個堆、二分答案套一個 DP check、BFS 的狀態裡多帶一維。核心能力有兩個——資料結構選型（每一步需要查什麼、改什麼，選一個都是 O(log n) 的容器）與 DP 狀態設計。\n\n**練法：**\n- 單題上限 45 分鐘；卡住時先寫下「我每一步需要維護什麼資訊」，這句話通常直接指向正確的資料結構\n- 做完之後問自己：這題是哪兩個基本技巧的組合？把組合方式記下來",
      children: [
        {
          id: 14,
          title: "3.1 堆與有序集合：動態極值",
          summary:
            "**訊號**\n\n「每次取最大/最小的元素處理，處理後可能放回或產生新元素」——堆；「動態插入刪除，還要查前驅後繼」——有序集合（C++ `set`/`multiset`）；「還要按排名查第 k 小」——離散化 + 樹狀陣列（見 4.6 節）或 GNU pbds 的 order-statistics tree。\n\n**概念說明：為什麼堆是「只關心極值」的最佳選擇**\n\n堆（二元堆）用一個陣列模擬完全二元樹，維持「父節點永遠比子節點更極值」的性質，讓插入和取出最大/最小值都只需要 O(log n)——比每次排序 O(n log n) 快得多，也比線性掃描找極值的 O(n) 快。它的限制是「只能看到堆頂，看不到中間的元素」，所以只適合「反覆取極值」的場景，而不適合「需要查詢某個特定值排第幾大」這種場景。\n\n有序集合（平衡二元搜尋樹，如 C++ `set`/`multiset`、Java `TreeMap`/`TreeSet`）維護完整的排序關係，能在 O(log n) 內做到：插入、刪除、查詢前驅（小於等於某值的最大元素）、後繼（大於等於某值的最小元素）。它比堆更強大，但常數更大，且有一個常被誤會的限制：標準庫的 `set` 做不到 O(log n) 查「第 k 小」或「某值的排名」（`std::distance` 對 `set` 迭代器是 O(n)），也不像樹狀陣列/線段樹那樣能做區間統計——需要排名查詢時，改用「離散化 + 樹狀陣列」（見 4.6 節）或 GNU pbds 的 order-statistics tree。\n\n**解題步驟**\n\n1. 先問「我只需要極值，還是也需要查中間元素？」只要極值用堆；要查前驅/後繼/第 k 大用有序集合\n2. 若題目描述「事件按時間發生，某個時刻要處理當前所有可用資源裡最好的一個」，先把事件排序，再用堆維護候選集合——這是「排序 + 堆」雙層結構\n3. 元素會失效（過期、已被使用）時，決定用「惰性刪除」（彈出時檢查是否有效，無效就丟棄再彈下一個）還是「反悔機制」（先接受，超出限制時把堆裡最差的那個吐出來）\n4. C++ `priority_queue` 預設大根堆，需要小根堆時要記得傳入 `greater<>` 或存負數\n\n**思考重點**\n\n- 堆適合「只關心極值」的場景：k 路合併、按時間軸模擬事件、反悔貪心（先拿下，超限時把最差的吐出來）\n- 元素會過期或失效時用惰性刪除：彈出時檢查是否過期，過期就丟掉再彈下一個，不要嘗試在堆裡原地刪\n- 有序集合比堆強在「能查中間」：需要前驅/後繼/範圍計數時果斷換 `set` 或樹狀陣列\n- 按時間軸模擬的題，先把事件排序，再用堆維護「當前可用的候選」——這個「排序 + 堆」的雙層結構是本節一半題目的骨架\n\n**逐步解法：以「LC 1642 Furthest Building You Can Reach」為例**\n\n題意：從第 0 棟樓開始往後走，每往前一棟若下一棟更高，要花「梯子」或「磚頭」補高度差；梯子數量有限但一次能用掉整段高度差，磚頭數量有限但要按高度差的量消耗。求最多能走到第幾棟樓。\n\n- 反悔貪心的直覺：優先用梯子處理「目前遇過的最大高度差」，因為梯子用一次就能解決整段落差，比磚頭划算；當梯子數用完，才把「目前用梯子中最小的那次」換回磚頭（因為换回最小的那次損失最少磚頭）\n- 用小根堆維護「目前已經用梯子解決的高度差」：新的高度差進來先都用梯子（堆裡塞進去）；若梯子數超過上限，就把堆頂（最小的高度差）吐出來改用磚頭\n\n```cpp\nint furthestBuilding(vector<int>& heights, int bricks, int ladders) {\n    priority_queue<int, vector<int>, greater<int>> minHeap; // 小根堆，存已用梯子的高度差\n    for (int i = 0; i + 1 < (int)heights.size(); ++i) {\n        int diff = heights[i + 1] - heights[i];\n        if (diff <= 0) continue;\n        minHeap.push(diff);\n        if ((int)minHeap.size() > ladders) {\n            bricks -= minHeap.top(); // 把最小的落差改用磚頭\n            minHeap.pop();\n            if (bricks < 0) return i; // 磚頭不夠，走不到下一棟\n        }\n    }\n    return heights.size() - 1;\n}\n```\n\n- 這正是「反悔貪心」的典型模板：先假設全部用最省事的資源（梯子），超限時反悔換掉「換回去代價最小」的那一個（堆頂）\n\n**常見翻車點**\n\n比較器方向（C++ `priority_queue` 預設是大根堆）、以及忘了惰性刪除導致取到已失效的極值。",
        },
        {
          id: 15,
          title: "3.2 單調堆疊與單調佇列",
          summary:
            "**訊號**\n\n「每個元素左/右第一個比它大（小）的位置」——單調堆疊；「滑動視窗內的最值」——單調佇列；「每個元素作為最小值能管轄的區間」——單調堆疊算貢獻。\n\n**概念說明：為什麼能做到 O(n)**\n\n單調堆疊/佇列的核心觀察是：如果新來的元素比堆疊/佇列尾端的元素「更好」（例如求「右邊第一個更大元素」時，新元素比堆疊頂端更大），那麼堆疊頂端那個元素就「再也不可能是任何後面元素的答案」了——因為新元素會先擋在它前面。所以可以放心把它彈出，不會漏掉任何解。\n\n因為每個元素最多被彈出一次、被壓入一次，整個演算法的總操作次數是 O(n)，遠比對每個元素都往後掃一次的 O(n²) 暴力法快。單調佇列（用 deque 實作）是同一個想法用在滑動視窗最值上：維護一個「從隊首到隊尾單調遞減（或遞增）」的雙端佇列，隊首永遠是當前視窗內的最值。\n\n**核心模板**\n\n```cpp\n// 每個元素右邊第一個更大元素的下標\nvector<int> nxt(n, -1);\nstack<int> st;  // 存下標，對應值遞減\nfor (int i = 0; i < n; ++i) {\n    while (!st.empty() && a[st.top()] < a[i]) {\n        nxt[st.top()] = i;\n        st.pop();\n    }\n    st.push(i);\n}\n```\n\n**解題步驟**\n\n1. 先確定要找的是「左邊/右邊第一個更大/更小的元素」，還是「滑動視窗內的最值」——前者用單調堆疊，後者用單調佇列（deque）\n2. 想清楚堆疊要維持遞增還是遞減：找「更大」用遞減堆疊（新元素比堆頂大就彈出堆頂）；找「更小」用遞增堆疊\n3. 堆疊/佇列裡存下標而不是值，因為算距離、算管轄範圍都需要位置資訊\n4. 若題目是「所有子陣列的最小值之和」這類全域統計，考慮貢獻法：每個元素乘上它「管轄」的子陣列數量，而管轄範圍正好由左右第一個更小元素決定\n\n**思考重點**\n\n- 單調堆疊的本質：被彈出的元素「再也不會是任何人的答案」，所以每個元素進出各一次，整體 O(n)\n- 貢獻法是它的高階用法：「所有子陣列的最小值之和」= 每個元素 × 它作為最小值管轄的子陣列數，管轄範圍就是左右第一個更小元素之間\n- 邊界用「一邊取嚴格、一邊取非嚴格」處理相等元素，避免重複計數\n\n**逐步解法：以「LC 239 Sliding Window Maximum」為例**\n\n題意：長度為 `k` 的視窗在陣列上滑動，求每個視窗內的最大值。\n\n- 訊號：「滑動視窗內的最值」——單調佇列的標準場景\n- 維護一個 deque，存下標，對應值從隊首到隊尾單調遞減；每次新元素進來前，先把隊尾比它小的元素都彈出（它們不可能再是任何視窗的最大值）；隊首若已經滑出視窗範圍就彈出隊首\n\n```cpp\nvector<int> maxSlidingWindow(vector<int>& a, int k) {\n    deque<int> dq;  // 存下標，值從隊首到隊尾單調遞減\n    vector<int> ans;\n    for (int i = 0; i < (int)a.size(); ++i) {\n        while (!dq.empty() && a[dq.back()] <= a[i]) dq.pop_back();\n        dq.push_back(i);\n        if (dq.front() <= i - k) dq.pop_front(); // 隊首滑出視窗\n        if (i >= k - 1) ans.push_back(a[dq.front()]);\n    }\n    return ans;\n}\n```\n\n- 逐步：每個下標最多進出 deque 各一次，整體 O(n)；隊首永遠是當前視窗內的最大值下標\n\n**常見翻車點**\n\n嚴格與非嚴格不等號的搭配、以及堆疊裡存值還是存下標（幾乎永遠應該存下標）、單調佇列忘記先判斷隊首是否已滑出視窗就直接取值。",
        },
        {
          id: 16,
          title: "3.3 位運算與子集枚舉",
          summary:
            "**訊號**\n\n`n <= 20`（子集狀壓）、「按位與/或/異或」出現在題面、「每個元素選或不選」的小規模組合問題。\n\n**概念說明：用一個整數表示一個集合**\n\n位元運算的核心想法是把「一個大小為 n 的集合的某個子集」編碼成一個 n 位的整數：第 `i` 個 bit 是 1 代表「元素 i 在這個子集裡」。這樣一來，集合運算就變成整數運算：交集是 `&`、聯集是 `|`、對稱差是 `^`、「移除某個元素」是 `mask & ~(1 << i)`、「檢查某元素是否在集合內」是 `(mask >> i) & 1`。這個編碼是後面狀壓 DP（見 4.1 節）的基礎。\n\nAND、OR、XOR 三種位元運算沿著陣列擴展時各有不同的單調性質，是 Q3/Q4 常考的隱藏訊號：AND 只會讓值變小或不變、OR 只會讓值變大或不變（因此「子陣列 AND/OR」不同的取值最多只有 O(log C) 種，C 是數值上限），這個性質常用來把 O(n²) 枚舉子陣列壓到 O(n log C)。XOR 沒有這種單調性，但有「兩個相同的值 XOR 起來變成 0」的抵銷性質，讓「XOR 配對問題」能轉化成前綴異或 + 雜湊表，和前綴和技巧完全同構。\n\n**解題步驟**\n\n1. 看到 `n <= 20` 左右且題目描述「選或不選」「排列順序」，先考慮狀壓，把子集編碼成整數\n2. 需要枚舉某個 mask 的所有子集時，用 `for (sub = mask; sub; sub = (sub - 1) & mask)` 這個標準寫法，別自己想別的枚舉方式\n3. 遇到「子陣列 AND/OR」，先想「不同取值最多 O(log C) 種」這個性質，能不能拿來剪枝或分組計數\n4. 遇到「所有數對的位元運算總和」，逐 bit 分開算：對每個 bit 位置，數一數有幾個數字這一位是 1、幾個是 0，分開處理\n\n**核心模板**\n\n```cpp\n// 枚舉 mask 的所有非空子集\nfor (int sub = mask; sub; sub = (sub - 1) & mask) {\n    // sub 是 mask 的子集\n}\n```\n\n**思考重點**\n\n- AND/OR 沿著子陣列延伸時是單調的（AND 只會變小、OR 只會變大），且不同的值最多 O(log C) 種——「子陣列 AND/OR 等於 k」類的題靠這個性質壓複雜度\n- XOR 的配對問題轉成前綴異或 + 雜湊表，和前綴和完全同構\n- 逐 bit 獨立計算貢獻：涉及所有數對的位運算總和，拆成每個 bit 上有幾個 0、幾個 1\n\n**逐步解法：以「LC 1371 Find the Longest Substring Containing Vowels in Even Counts」為例**\n\n題意：找出最長子字串，使其中每個母音（a,e,i,o,u）出現的次數都是偶數。\n\n- 訊號：「偶數次數」——用一個 5-bit 的 mask 表示「目前每個母音出現次數的奇偶性」，每掃到一個母音就把對應的 bit 做 XOR（奇偶性翻轉）\n- 轉化：兩個位置 `i < j` 之間的子字串滿足條件，等價於 `state[j] == state[i]`（因為兩段前綴的奇偶狀態相同，中間這段的每個母音出現次數變化量都是偶數）——這正是「前綴異或 + 雜湊表」的結構，和前綴和找子陣列和完全同構\n\n```cpp\nint findTheLongestSubstring(string s) {\n    unordered_map<char, int> bit = {{'a',0},{'e',1},{'i',2},{'o',3},{'u',4}};\n    vector<int> firstSeen(32, -2); // state -> 最早出現的位置\n    firstSeen[0] = -1; // 空前綴，state=0\n    int state = 0, ans = 0;\n    for (int i = 0; i < (int)s.size(); ++i) {\n        if (bit.count(s[i])) state ^= (1 << bit[s[i]]);\n        if (firstSeen[state] == -2) firstSeen[state] = i;\n        else ans = max(ans, i - firstSeen[state]);\n    }\n    return ans;\n}\n```\n\n**常見翻車點**\n\n位移超過 31 位沒用 `1LL`、運算子優先級（`&` 比 `==` 低，記得加括號）、以及把「用雜湊表存第一次出現的位置」和「存最近一次出現的位置」搞混（這裡要的是最早出現的位置，因為要找最長子字串）。",
        },
        {
          id: 17,
          title: "3.4 圖論主戰場：最短路、拓撲、並查集",
          summary:
            "**訊號與選型**\n\n- 等權圖最少步數 → BFS；邊權非負 → Dijkstra；邊權只有 0 和 1 → 0-1 BFS（deque）\n- 「先修條件」「依賴順序」→ 拓撲排序，順帶在 DAG 上做 DP\n- 「動態合併集合、查是否連通」→ 並查集；配合「把邊按權重排序後逐條加入」能處理一大類離線連通性問題\n\n**概念說明**\n\n- **Dijkstra**：貪心地每次從「已知最短距離的候選集合」裡取出目前距離最小的節點，確定它的最短距離不會再變，再用它去鬆弛（relax）鄰居。它成立的前提是邊權非負——因為一旦有負邊，「目前距離最小」不代表「最終距離最小」，可能之後繞一條負邊回來會更短\n- **拓撲排序**：對有向無環圖（DAG）按「先修條件」排出一個合法執行順序。做法是不斷取出「入度為 0」的節點（代表沒有未完成的前置條件），常用 BFS（Kahn 演算法）實作；如果排序完還有節點沒被處理，代表圖中存在環\n- **並查集（Union-Find）**：維護一組不相交集合，支援「合併兩個集合」與「查詢兩個元素是否在同一集合」，透過路徑壓縮與按秩合併，均攤複雜度接近 O(1)。凡是「動態地把元素兩兩合併，隨時要問連通性」的題，並查集幾乎是唯一選擇\n\n**核心模板**\n\n```cpp\n// Dijkstra（鄰接表 g[u] = {v, w}）\npriority_queue<pair<long long, int>, vector<pair<long long, int>>, greater<>> pq;\nvector<long long> dist(n, LLONG_MAX);\ndist[s] = 0;\npq.push({0, s});\nwhile (!pq.empty()) {\n    auto [d, u] = pq.top();\n    pq.pop();\n    if (d > dist[u]) continue;  // 惰性刪除過期項\n    for (auto [v, w] : g[u]) {\n        if (d + w < dist[v]) {\n            dist[v] = d + w;\n            pq.push({dist[v], v});\n        }\n    }\n}\n\n// 0-1 BFS：邊權只有 0/1 時用 deque 取代優先佇列，整體 O(V+E)\ndeque<int> dq;\nvector<int> d01(n, INT_MAX);\nd01[s] = 0;\ndq.push_back(s);\nwhile (!dq.empty()) {\n    int u = dq.front();\n    dq.pop_front();\n    for (auto [v, w] : g[u]) {  // w 只會是 0 或 1\n        if (d01[u] + w < d01[v]) {\n            d01[v] = d01[u] + w;\n            if (w == 0) dq.push_front(v);  // 0 權邊推前面，相當於留在同一層\n            else dq.push_back(v);          // 1 權邊推後面，相當於下一層\n        }\n    }\n}\n\n// 並查集\nstruct DSU {\n    vector<int> parent, rank_;\n    DSU(int n) : parent(n), rank_(n, 0) { iota(parent.begin(), parent.end(), 0); }\n    int find(int x) { return parent[x] == x ? x : parent[x] = find(parent[x]); } // 路徑壓縮\n    bool unite(int a, int b) {\n        a = find(a); b = find(b);\n        if (a == b) return false;\n        if (rank_[a] < rank_[b]) swap(a, b);\n        parent[b] = a;\n        if (rank_[a] == rank_[b]) ++rank_[a];\n        return true;\n    }\n};\n```\n\n**解題步驟**\n\n1. 先判斷邊權型態：全部相等用 BFS；非負但不同用 Dijkstra；只有 0/1 用 0-1 BFS（deque，0 權推前面、1 權推後面）\n2. 看到「先修」「依賴」「順序合法性」，先建有向圖，跑拓撲排序；若要在此基礎上求最長/最短路徑，按拓撲序做 DP\n3. 看到「動態合併」「是否連通」「最小生成樹」，先想並查集；「讓所有點連通的最小代價」是 Kruskal（邊按權重排序後用並查集逐條加入，不形成環就留下）\n4. 週賽的圖論題常把「建圖」藏起來：狀態是節點、操作是邊。看到「最少操作次數把 x 變成 y」先想想能不能建圖跑 BFS/Dijkstra\n\n**思考重點**\n\n- 週賽的圖論題常把「建圖」藏起來：狀態是節點、操作是邊。看到「最少操作次數把 x 變成 y」先想想能不能建圖跑 BFS/Dijkstra\n- 最小生成樹（Kruskal = 排序 + 並查集）在「讓所有點連通的最小代價」措辭下出現\n\n**逐步解法：以「LC 743 Network Delay Time」為例**\n\n題意：從節點 k 發送信號，經過帶權有向邊傳到其他節點，求所有節點都收到信號的最短時間（即從 k 出發的單源最短路的最大值）。\n\n```cpp\nint networkDelayTime(vector<vector<int>>& times, int n, int k) {\n    vector<vector<pair<int, int>>> g(n + 1);\n    for (auto& t : times) g[t[0]].push_back({t[1], t[2]});\n    vector<long long> dist(n + 1, LLONG_MAX);\n    dist[k] = 0;\n    priority_queue<pair<long long, int>, vector<pair<long long, int>>, greater<>> pq;\n    pq.push({0, k});\n    while (!pq.empty()) {\n        auto [d, u] = pq.top(); pq.pop();\n        if (d > dist[u]) continue;\n        for (auto [v, w] : g[u]) {\n            if (d + w < dist[v]) {\n                dist[v] = d + w;\n                pq.push({dist[v], v});\n            }\n        }\n    }\n    long long ans = 0;\n    for (int i = 1; i <= n; ++i) {\n        if (dist[i] == LLONG_MAX) return -1; // 有節點收不到信號\n        ans = max(ans, dist[i]);\n    }\n    return (int)ans;\n}\n```\n\n- 逐步：標準 Dijkstra 求出從 `k` 到所有節點的最短距離；答案是這些距離的最大值（因為要等到「最慢收到的那個節點」才算全部收到）；若有節點距離仍是無限大，代表圖不連通，回傳 -1\n\n**常見翻車點**\n\nDijkstra 忘了 `d > dist[u]` 的過期檢查（複雜度退化）、拓撲排序沒檢查是否有環、並查集忘記路徑壓縮或按秩合併導致退化成鏈狀結構、Kruskal 忘記先按權重排序邊。",
        },
        {
          id: 18,
          title: "3.5 DP 進階：背包、區間與狀態機",
          summary:
            "**這一節在練什麼**\n\n線性 DP 之上最常考的三種狀態設計，以及「DP 作為別的技巧的零件」的用法（例如二分答案的 check 是個 DP）。\n\n**三個模型**\n\n- **背包**：「從集合裡選一些，總重不超過 C，最大化價值/湊出目標的方案數」。0/1 背包容量倒序枚舉，完全背包正序枚舉——這一個順序差別要刻進肌肉記憶\n- **區間 DP**：「合併相鄰段的代價」「回文結構」。狀態 `dp[l][r]`，按區間長度從小到大填\n- **狀態機 DP**：「持有/不持有」「上一個選了/沒選」。狀態 = 位置 × 模式，轉移是模式間的合法跳轉\n\n**概念說明：為什麼倒序/正序枚舉決定了 0/1 與完全背包**\n\n0/1 背包裡每個物品只能選一次，若用一維滾動陣列從左往右（正序）更新 `dp[c]`，`dp[c - w[i]]` 有可能已經在「這一輪」被同一個物品更新過，等於物品被重複選取；改成從右往左（倒序）更新，`dp[c - w[i]]` 保證還是「上一輪（考慮前 i-1 個物品）」的值，就不會重複選同一個物品。完全背包裡每個物品可以選無限次，所以正序枚舉、允許用到「這一輪已經更新過」的狀態，剛好就是「同一個物品可以再選一次」的語意。這個正序/倒序的差異，是背包 DP 唯一需要死記的細節。\n\n區間 DP 的狀態 `dp[l][r]` 代表「處理完整個區間 `[l,r]` 的最優值」，轉移時要枚舉一個切割點 `m`，把 `[l,r]` 拆成 `[l,m]` 和 `[m+1,r]` 兩段分別處理再合併——因為轉移依賴「更短的區間」，所以必須按區間長度從小到大填表，不能像線性 DP 一樣單純從左到右。\n\n狀態機 DP 把「額外的一個小維度」（持有股票/沒持有、上一步選了/沒選）當成狀態的一部分，本質上是把原本簡單的線性 DP 狀態「複製」成幾份，並在轉移時定義好這幾份之間的合法跳轉關係。\n\n```cpp\n// 0/1 背包：容量必須倒序\nfor (int i = 0; i < n; ++i) {\n    for (int c = C; c >= w[i]; --c) {\n        dp[c] = max(dp[c], dp[c - w[i]] + v[i]);\n    }\n}\n\n// 完全背包：容量必須正序（同一物品可重複選取）\nfor (int i = 0; i < n; ++i) {\n    for (int c = w[i]; c <= C; ++c) {\n        dp[c] = max(dp[c], dp[c - w[i]] + v[i]);\n    }\n}\n```\n\n**解題步驟**\n\n1. 先判斷是背包、區間 DP 還是狀態機：「選一些東西湊出目標/不超過容量」→ 背包；「合併相鄰段/回文」→ 區間 DP；「有一個持有/不持有之類的額外維度」→ 狀態機\n2. 背包先想清楚「每個物品能選幾次」，決定倒序或正序枚舉容量\n3. 區間 DP 先寫出「拆成兩段的轉移方程」，再確認填表順序是按區間長度遞增\n4. 狀態機先列出所有模式（例如「持有股票」「不持有且在冷卻」「不持有且可交易」），畫出模式間的合法跳轉，再各自寫轉移\n\n**思考重點**\n\n- 方案數 DP 記得取模，且初始值 `dp[0] = 1` 的語意（空集合是一種方案）要想清楚\n- 字串分割類（「分成最少的美麗子字串」）是線性 DP + 可行性判斷的組合：`dp[i] = min(dp[j] + 1)`，j 到 i 這段合法\n\n**逐步解法：以「LC 309 Best Time to Buy and Sell Stock with Cooldown」為例**\n\n題意：可以多次買賣股票，賣出後有一天冷卻期不能買入，求最大獲利。\n\n- 定義三種狀態機模式：`hold`（持有股票）、`sold`（剛賣出，處於冷卻）、`rest`（不持有且可以買入）\n- 轉移：`hold[i] = max(hold[i-1], rest[i-1] - price[i])`（維持持有，或今天買入）；`sold[i] = hold[i-1] + price[i]`（今天賣出）；`rest[i] = max(rest[i-1], sold[i-1])`（維持休息，或昨天賣出後的冷卻結束）\n\n```cpp\nint maxProfit(vector<int>& price) {\n    int n = price.size();\n    long long hold = LLONG_MIN, sold = 0, rest = 0;\n    for (int i = 0; i < n; ++i) {\n        long long newHold = max(hold, rest - price[i]);\n        long long newSold = hold + price[i];\n        long long newRest = max(rest, sold);\n        hold = newHold; sold = newSold; rest = newRest;\n    }\n    return (int)max(sold, rest);\n}\n```\n\n- 這正是「狀態 = 位置 × 模式」的範例：位置是第 i 天，模式是 hold/sold/rest 三種，轉移只在模式之間跳轉\n\n**時間目標**\n\n45 分鐘。這一節做完，Q3 的 DP 應該不再是玄學，而是四步流程的機械執行。",
        },
        {
          id: 29,
          title: "3.6 中位數、分數規劃與掃描線",
          summary:
            "**這一節在練什麼**\n\n1700-2000 分段有幾類「看起來不像標準演算法」的題：涉及中位數性質、區間重疊統計、大範圍離散化後的批量修改。它們的共同點是「轉化觀察」——把原問題換一種量度，就變成前綴和或排序後掃描。\n\n**中位數的關鍵性質**\n\n- 使絕對差之和最小：選中位數當錨點\n- 統計「中位數恰好為 K」的子陣列：把「大於 K」記為 +1、「小於 K」記為 -1，轉成「和為 0（子陣列長度為奇數；偶數長度時依題目的中位數定義，通常是和為 1）且包含 K」的前綴和問題\n\n**概念說明：中位數為什麼最小化絕對差之和**\n\n把元素排序後設中位數為 m。任何候選錨點 x 若偏向較大的一側，靠近它那一側（數值 ≤ 中位數的一半元素）距離變近的量，會被偏離它那一側（另一半元素）距離變遠的量抵銷或更差；因為中位數兩側元素數量最平衡，偏離中位數只會讓總距離變大或持平，這就是「中位數最小化絕對差之和」的直覺。這個性質常出現在「把所有元素移到同一個值，求最少總操作次數」這類題。\n\n**分數規劃（Parametric Search 思維）**\n\n「讓最大子陣列平均值不超過 limit」這類題，先二分 limit，再把每個元素減去 limit 轉成「是否存在和 ≥ 0 的子陣列」——讓平均值問題變成前綴和最值問題。這是二分答案 + 前綴和的經典組合：分數規劃的通用套路是，只要目標函式形如「總收益 / 總代價」且要找它的最大或最小值，就可以二分這個比值 `x`，把「收益 - x × 代價」轉成一個純加法問題，`check(x)` 只需要判斷轉化後的和是否為正。\n\n**掃描線 / 差分離線**\n\n- 大量區間 `[l,r]` 的覆蓋次數：差分陣列 `diff[l]++, diff[r+1]--`，最後掃一遍\n- 「同時被最多區間覆蓋的點」等於差分前綴和的最大值\n- 區間端點排序後掃描：進入事件 +1、離開事件 -1，維護當前計數\n\n**解題步驟**\n\n1. 看到「移動所有元素到同一點，求最小總代價」，先想中位數（絕對差）或平均值（平方差）\n2. 看到「總和 / 總數不超過某個值」「最大化平均值」，先想分數規劃：二分目標比值，把問題轉成純加法的可行性判斷\n3. 看到大量區間 `[l,r]`「同時覆蓋幾次」「最多重疊幾層」，先想差分陣列或事件排序掃描\n4. 掃描線題一定要明確定義「同一個座標點上，離開事件和進入事件誰先處理」，這通常是題目邊界語意（開區間/閉區間）決定的\n\n**逐步解法（中位數）：以「LC 462 Minimum Moves to Equal Array Elements II」為例**\n\n題意：每次操作可將任一元素 +1 或 -1，求把所有元素變成相同值所需的最少總操作次數。\n\n- 訊號：「把所有元素移到同一個值、最小化絕對差之和」——錨點取中位數\n- 逐步：排序後用對撞雙指標把「頭尾配對」的距離一次加完；`a[j] - a[i]` 正好是這一對各自到中位數距離之和，所以連中位數是幾都不必算出來\n\n```cpp\nint minMoves2(vector<int>& a) {\n    sort(a.begin(), a.end());\n    long long ans = 0;\n    int i = 0, j = (int)a.size() - 1;\n    while (i < j) {\n        ans += a[j] - a[i]; // 頭尾一對到中位數的距離和 = 兩者之差\n        --j;\n        ++i;\n    }\n    return (int)ans;\n}\n```\n\n- 為什麼頭尾相減成立：中位數 m 落在 `a[i]` 與 `a[j]` 之間，`(m - a[i]) + (a[j] - m) = a[j] - a[i]`，m 被消掉——這就是「中位數最小化絕對差之和」在程式上的體現\n\n**逐步解法（分數規劃）：以「LC 644 Maximum Average Subarray II」為例**\n\n題意：求長度至少為 k 的連續子陣列裡，最大的平均值。\n\n- 訊號：「最大化平均值 = 總和 / 長度」——二分這個平均值 `x`，把每個元素減去 `x`，問題變成「是否存在長度 ≥ k 的子陣列，其（減去 x 後的）和 ≥ 0」，也就是純加法的可行性判斷\n- check(x)：對減去 x 後的陣列做前綴和，對每個右端點 `i`，答案是 `pre[i]` 減去「下標 ≤ i-k 的最小前綴」——這正是常見翻車點強調的「前綴和要同時維護最小前綴」\n\n```cpp\nbool check(vector<int>& nums, int k, double x) {\n    int n = nums.size();\n    vector<double> pre(n + 1, 0);\n    for (int i = 0; i < n; ++i) pre[i + 1] = pre[i] + nums[i] - x; // 減去 x 後的前綴和\n    double minPre = 0;                        // 下標 <= i-k 的最小前綴\n    for (int i = k; i <= n; ++i) {\n        minPre = min(minPre, pre[i - k]);     // 只允許長度 >= k 的左端點\n        if (pre[i] - minPre >= 0) return true; // 存在一段（長度 >= k）和 >= 0\n    }\n    return false;\n}\ndouble findMaxAverage(vector<int>& nums, int k) {\n    double lo = *min_element(nums.begin(), nums.end());\n    double hi = *max_element(nums.begin(), nums.end());\n    while (hi - lo > 1e-5) {                   // 實數二分，用精度控制收斂\n        double mid = (lo + hi) / 2;\n        if (check(nums, k, mid)) lo = mid; else hi = mid;\n    }\n    return lo;\n}\n```\n\n- 逐步：外層對答案（平均值）做實數二分 O(log((max-min)/eps))，內層 check 是 O(n) 前綴和掃描；把「除法的平均值」轉成「減去 x 後的純加法和」是分數規劃的通用降維手法\n\n**逐步解法（掃描線）：以「LC 253 Meeting Rooms II」為例（求同時進行的最多會議數＝最少需要幾間會議室）**\n\n題意：給一組會議的開始/結束時間，求同一時刻最多有幾場會議同時進行。\n\n- 訊號：「同時被最多區間覆蓋的點」——把每場會議拆成兩個事件：開始時 `+1`、結束時 `-1`，事件按時間排序後掃描，過程中維護的計數最大值就是答案\n- 邊界：若一場會議在某時刻結束、另一場恰好在同一時刻開始，通常視為不衝突（可以共用同一間房），所以同一時間點要先處理「結束事件」再處理「開始事件」\n\n```cpp\nint minMeetingRooms(vector<vector<int>>& intervals) {\n    vector<pair<int, int>> events; // {time, delta}：開始 +1、結束 -1\n    for (auto& it : intervals) {\n        events.push_back({it[0], +1}); // 開始事件\n        events.push_back({it[1], -1}); // 結束事件\n    }\n    // 按 (時間, delta) 排序：同一時間 -1(結束) 排在 +1(開始) 前面，\n    // 正好對應「先處理結束、再處理開始」——端點相接不算衝突\n    sort(events.begin(), events.end());\n    int cur = 0, ans = 0;\n    for (auto& [time, delta] : events) {\n        cur += delta;\n        ans = max(ans, cur);\n    }\n    return ans;\n}\n```\n\n- 逐步：對每個事件依時間排序（同時間時開始事件的優先權需視題目「端點相接算不算衝突」而定，寫之前先想清楚），一次線性掃描維護當前重疊數的最大值\n\n**常見翻車點**\n\n- 中位數轉化後忘記要求「子陣列必須包含 K」這個條件\n- 分數規劃的 check 裡只做前綴和沒有同時維護最小前綴，導致複雜度變高\n- 掃描線事件排序時沒定義「同點先離開後進入」還是反過來，導致邊界計數錯",
        },
      ],
    },
    {
      id: 19,
      title: "4. 天花板：Q4 硬骨頭（2000-2400）",
      summary:
        "Q4 不要求場場會，但每多解出一題就是一次大漲分。2000+ 的題通常有一個「關鍵觀察」：沒看到它，怎麼寫都是超時或錯誤；看到了，剩下的就是攻堅關的技術。所以這一階段的練法和前面不同——允許慢，但必須把每題的關鍵觀察提煉成一句話記下來。\n\n**練法：**\n- 每題最多 90 分鐘；解不出來就精讀題解，然後隔幾天不看題解重寫一遍\n- 每題記錄：關鍵觀察是什麼、什麼訊號能提示它、我當時的思路差在哪一步\n- 一週 3-4 題就夠，貪多嚼不爛",
      children: [
        {
          id: 20,
          title: "4.1 狀壓與高維 DP",
          summary:
            "**訊號**\n\n`n <= 16` 左右的「排列/分組/指派」問題：把元素逐個放進若干組、找最優的處理順序、兩兩配對。指數級的狀態數靠 bitmask 壓進一個整數。\n\n**概念說明：從「排列」到「集合」的降維**\n\n很多題目表面上要求「找出最優的處理順序」，元素數量 n 個看似有 n! 種排列，但仔細分析會發現：某個狀態的最優值，只跟「目前已經處理過哪些元素（一個集合）」有關，不跟「處理的先後順序」有關。既然如此，就不需要枚舉 n! 種排列，只需要對 `2^n` 種子集分別求最優值——這就是狀壓 DP 把指數級的排列問題降維成指數級但小得多的集合問題的核心觀察。`dp[mask]` 代表「已經處理完 mask 這個集合時的最優值」，轉移時窮舉「最後一個處理的元素是誰」，從去掉這個元素的子狀態轉移過來。\n\n**核心模板**\n\n```cpp\n// dp[mask]：已處理集合為 mask 時的最優值\nfor (int mask = 1; mask < (1 << n); ++mask) {\n    int i = __builtin_ctz(mask);       // 常見技巧：固定處理 mask 裡最低位的元素\n    for (int j = 0; j < n; ++j) {\n        if (mask >> j & 1) {\n            dp[mask] = min(dp[mask], dp[mask ^ (1 << j)] + cost(j, mask));\n        }\n    }\n}\n```\n\n**解題步驟**\n\n1. 先確認 n 的範圍是否落在 `2^n * n` 或 `2^n * n^2` 能接受的區間（n ≤ 16-20），這是狀壓 DP 的必要條件\n2. 說服自己「順序不重要，只有集合重要」——寫出這句話，狀態設計就自動出現：`dp[mask]` 或 `dp[mask][extra]`\n3. 想清楚轉移：從 mask 轉移到 mask 需要窮舉「新增/去掉哪個元素」，還是窮舉「mask 的子集」（子集枚舉 DP，見下方複雜度）\n4. 「分成 k 組、每組代價只和組內集合有關」的題，先預處理每個子集的代價，再做子集枚舉 DP\n\n**思考重點**\n\n- 複雜度自查：`2^n * n` 約 10^6（n=16）沒問題；枚舉子集的 `3^n` 在 n=14 左右是極限\n- 「分成 k 組、每組代價只和組內集合有關」的題：先預處理每個子集的代價，再做子集枚舉 DP\n- 對稱性剪枝（第一個元素固定放第一組）常常是卡常與超時的分界\n\n**逐步解法：以「LC 698 Partition to K Equal Sum Subsets」為例**\n\n題意：判斷能否把陣列分成 k 個子集，每個子集的和都相等。\n\n- 訊號：「分組」且 `n` 通常不大——狀壓 DP 的典型場景\n- 狀態設計：`dp[mask]` 表示「用 mask 這個集合的元素，最少能填滿幾個完整的目標和（以及當前這一組還剩多少空間）」。常見寫法是 `dp[mask]` 記錄「這個集合的元素總和 mod target」，如果 `dp[mask]` 已知，加入新元素 `j` 後若「目前這組累積和 + a[j]」不超過 target，就能轉移到 `dp[mask | (1<<j)]`\n\n```cpp\nbool canPartitionKSubsets(vector<int>& nums, int k) {\n    int sum = accumulate(nums.begin(), nums.end(), 0);\n    if (sum % k != 0) return false;\n    int target = sum / k, n = nums.size();\n    vector<int> curSum(1 << n, -1); // curSum[mask] = 這個集合目前這一組累積的和，-1 表示不可達\n    curSum[0] = 0;\n    for (int mask = 0; mask < (1 << n); ++mask) {\n        if (curSum[mask] < 0) continue;\n        for (int j = 0; j < n; ++j) {\n            if (mask >> j & 1) continue;\n            int rem = curSum[mask] % target; // 目前這一組還累積了多少\n            if (rem + nums[j] <= target) {\n                curSum[mask | (1 << j)] = curSum[mask] + nums[j];\n            }\n        }\n    }\n    return curSum[(1 << n) - 1] == sum;\n}\n```\n\n- 逐步：`curSum[mask]` 紀錄用掉 mask 這些元素後累積的總和；透過 `% target` 判斷「目前這一組」還能塞多少，能塞得下才轉移；答案是全集 mask 的累積和恰好等於總和\n\n**關鍵觀察的形態**\n\n這類題的觀察通常是「順序不重要，只有集合重要」——一旦說出這句話，狀壓就是唯一答案。",
        },
        {
          id: 21,
          title: "4.2 樹上思維：樹形 DP 與換根",
          summary:
            "**訊號**\n\n輸入是樹、問「每個子樹的統計量」「選點/選邊滿足約束的最優值」，或「以每個節點為根時的答案」（換根）。\n\n**概念說明：樹形 DP 的本質是後序合併**\n\n樹沒有環，所以「以某節點為根的子樹」是一個天然、不重疊的子問題劃分——這正是 DP 需要的「最優子結構」。樹形 DP 用後序遍歷（先遞迴處理完所有孩子，再處理自己）的順序，把每個孩子子樹算出的統計量合併成當前節點的答案，因此不需要顯式定義「狀態的順序」，遞迴呼叫的順序自然就是正確的轉移順序。\n\n換根 DP 解決的問題是「以每個節點為根時，答案分別是多少」——如果對每個節點各自跑一次樹形 DP 會是 O(n²)。換根 DP 用兩次遍歷把它壓到 O(n)：第一次後序遍歷算出「每個節點為根、只看它子樹內部」的答案（向下的部分）；第二次前序遍歷，把「父節點的答案」扣掉「當前節點對父節點的貢獻」、換算成「父節點以外的部分」，再加給當前節點，得到「當前節點為根時，來自父方向」的貢獻，兩者相加就是完整答案。\n\n**核心框架**\n\n```cpp\n// 樹形 DP：後序遍歷，用孩子的結果合成自己的結果\n// （泛型 lambda 自遞迴寫法，C++14 以上的評測環境都能編譯）\nauto dfs = [&](auto&& dfs, int u, int parent) -> long long {\n    long long res = value[u];\n    for (int v : g[u]) {\n        if (v != parent) {\n            res += max(0LL, dfs(dfs, v, u));  // 視題意合併孩子貢獻\n        }\n    }\n    return res;\n};\n// 呼叫：dfs(dfs, root, -1)\n```\n\n**解題步驟**\n\n1. 先確認題目只問「以固定的根」還是「以每個節點為根」：前者一次樹形 DP 即可，後者要換根\n2. 定義 `dp[u]` 的語意時要包含「只看子樹內部」這個限制詞，這樣才能保證轉移只需要孩子的資訊\n3. 如果一個狀態不夠表達所有需要的資訊（例如同時要「選這個點」和「不選這個點」兩種答案），把狀態擴充成 `dp[u][0/1]`，本質是狀態機 DP 搬到樹上\n4. 換根 DP 動筆前，先寫出「父節點的答案 = 排除當前子節點貢獻後的父方向答案 + 當前子節點自己的向下答案」這條公式，想清楚才開始寫程式\n\n**思考重點**\n\n- 樹形 DP 的狀態常帶一個小模式維：`dp[u][選/不選]`、`dp[u][黑/白]`——本質是狀態機 DP 搬到樹上\n- 換根 DP 兩趟：第一趟後序算「向下」的答案，第二趟前序把「向上」的貢獻推給孩子。推導時把「父親的答案扣掉自己的貢獻」寫成明確的公式再開始寫程式\n- 樹上路徑問題常在「經過每個點的最高路徑」處合併兩個最大孩子鏈\n\n**逐步解法：以「LC 337 House Robber III」為例**\n\n題意：二元樹上每個節點有一個值，相鄰節點（父子）不能同時選，求能選出的最大總和。\n\n- 狀態設計：`dp[u][0]` = 不選 u 時，以 u 為根子樹的最大和；`dp[u][1]` = 選 u 時，以 u 為根子樹的最大和\n- 轉移：`dp[u][1] = value[u] + dp[left][0] + dp[right][0]`（選了 u，孩子都不能選）；`dp[u][0] = max(dp[left][0], dp[left][1]) + max(dp[right][0], dp[right][1])`（不選 u，孩子選不選都可以，取較大者）\n\n```cpp\npair<int,int> dfs(TreeNode* u) { // 回傳 {不選u的最大和, 選u的最大和}\n    if (!u) return {0, 0};\n    auto [lNo, lYes] = dfs(u->left);\n    auto [rNo, rYes] = dfs(u->right);\n    int no = max(lNo, lYes) + max(rNo, rYes);\n    int yes = u->val + lNo + rNo;\n    return {no, yes};\n}\nint rob(TreeNode* root) {\n    auto [no, yes] = dfs(root);\n    return max(no, yes);\n}\n```\n\n- 這正是「狀態機搬到樹上」：每個節點有兩種模式（選/不選），轉移是模式間的合法組合，後序遍歷保證孩子先算完\n\n**關鍵觀察的形態**\n\n「答案可以按子樹分解」。如果某個量無法只靠孩子的資訊合成，考慮多存一個輔助量（最大值+次大值、含根/不含根兩種）。",
        },
        {
          id: 31,
          title: "4.3 圖論進階：基環樹、倍增與 Tarjan",
          summary:
            "**訊號**\n\n「每個節點只有一條出邊」（內向基環樹）、「尋找圖中的關鍵連線或割點」、「樹上任意兩點的路徑查詢或第 K 個祖先」。這些是 2000+ 圖論題最愛套的皮。\n\n**概念說明**\n\n- **基環樹 (Pseudotree)**：N 個點 N 條邊的連通圖，比樹多一條邊，因此恰好包含一個環，環上再掛著一些樹枝。處理套路是先算出每個點的入度（或反過來看出邊），像拓撲排序一樣不斷剝掉「入度為 0」的葉子節點，剝到剝不動為止，剩下的點就是環；樹枝部分可以正常做樹形 DP，環的部分則按「斷環成鏈」或直接繞環枚舉處理\n- **倍增法 (Binary Lifting)**：預處理 `up[k][u]` = 「從 u 往上跳 `2^k` 步會到哪個節點」，任何跳 K 步的查詢都能用 K 的二進位拆解成 O(log K) 次跳躍完成——這是快速冪「用二進位拆解指數」的想法用在圖上的版本。樹上找 LCA（最近公共祖先）與找第 K 個祖先都靠它\n- **Tarjan 演算法**：透過一次 DFS 同時計算每個節點的「發現時間」與「能追溯到的最早祖先」（low-link 值），藉此在線性時間內找出強連通分量（SCC）、橋（刪除後圖不連通的邊）或割點（刪除後圖不連通的點）\n\n**解題步驟**\n\n1. 看到「每個節點恰好一條出邊」「N 個點 N 條邊」，先辨認這是基環樹；先剝離樹枝（拓撲排序剝葉），剩下的環再另外處理\n2. 看到「樹上第 K 個祖先」「兩點的 LCA」「兩點路徑上的最值」且查詢次數很多，先想倍增法預處理\n3. 看到「刪除一條邊/一個點後圖是否還連通」「找出所有的橋/割點」，直接套 Tarjan 模板，記住 low-link 值的定義後不要每次重新推導\n4. 遇到「函式 f(x) 映射到自身」（例如「從 x 出發沿著某規則移動一步到 f(x)」），把它建成一張圖：每個狀態是一個節點，`x -> f(x)` 是一條邊——這就是基環樹的典型建模方式\n\n**核心模板：基環樹剝葉找環**\n\n```cpp\n// g[u] 是 u 的唯一出邊指向的節點；先算入度，剝掉葉子，剩下的是環\nvector<int> indeg(n, 0);\nfor (int u = 0; u < n; ++u) ++indeg[g[u]];\nqueue<int> q;\nvector<bool> onCycle(n, true);\nfor (int u = 0; u < n; ++u) if (indeg[u] == 0) q.push(u);\nwhile (!q.empty()) {\n    int u = q.front(); q.pop();\n    onCycle[u] = false;\n    if (--indeg[g[u]] == 0) q.push(g[u]);\n}\n// onCycle[u] == true 的節點組成環\n```\n\n**核心模板：倍增求第 K 個祖先**\n\n```cpp\n// up[k][u] = u 往上跳 2^k 步到達的節點（不存在為 -1）\n// parent[u] 是 u 的直接父節點，根節點的 parent 為 -1\nint LOG = 1;\nwhile ((1 << LOG) < n) ++LOG;\nvector<vector<int>> up(LOG + 1, vector<int>(n, -1));\nup[0] = parent;\nfor (int k = 1; k <= LOG; ++k) {\n    for (int u = 0; u < n; ++u) {\n        if (up[k - 1][u] != -1) {\n            up[k][u] = up[k - 1][up[k - 1][u]];\n        }\n    }\n}\n// 查詢：把 K 做二進位拆解，對每個是 1 的 bit 跳一次，O(log K)\nint kthAncestor(int u, int K) {\n    for (int k = 0; K > 0 && u != -1; ++k, K >>= 1) {\n        if (K & 1) u = up[k][u];\n    }\n    return u;\n}\n```\n\n求 LCA(u, v) 的流程：先把較深的那個節點用 `kthAncestor` 跳到與另一個同深度；若此時兩點相同即為答案，否則從最大的 k 往小掃，凡是 `up[k][u] != up[k][v]` 就讓兩點一起往上跳 `2^k` 步，結束後兩點的父節點就是 LCA。\n\n**逐步解法：以「LC 2360 Longest Cycle in a Graph」為例**\n\n題意：每個節點最多一條出邊（`edges[i]` 是 i 的下一個節點，可能是 -1 代表沒有出邊），求圖中最長的環長度。\n\n- 這是多個基環樹（或純樹）組成的森林。用上面的剝葉模板先找出所有在環上的節點，再對每個尚未訪問過的環上節點，沿著出邊走一圈算出環長，取最大值\n- 也可以用「時間戳記」DFS：每次從沒訪問過的節點出發，記錄每個節點在這條路徑上被訪問的時間戳；遇到已訪問且時間戳在本次路徑內的節點，環長就是「目前時間戳 - 那個節點的時間戳」\n\n```cpp\nint longestCycle(vector<int>& edges) {\n    int n = edges.size(), ans = -1, timer = 1;\n    vector<int> visitTime(n, 0);      // 0 代表尚未訪問\n    for (int i = 0; i < n; ++i) {\n        if (visitTime[i]) continue;\n        int start = timer;            // 本條路徑的起始時間戳\n        for (int u = i; u != -1; u = edges[u]) {\n            if (visitTime[u]) {                       // 撞到已訪問的節點\n                if (visitTime[u] >= start) {           // 且它在本條路徑上 → 找到環\n                    ans = max(ans, timer - visitTime[u]);\n                }\n                break;                                 // 撞到舊路徑，環必然早已算過\n            }\n            visitTime[u] = timer++;                    // 打上遞增時間戳\n        }\n    }\n    return ans;\n}\n```\n\n- 逐步：沿著每個節點的唯一出邊往前走並打上遞增時間戳；走到已有時間戳的節點 u 時，若 `visitTime[u] >= start` 代表 u 在這一次的路徑上，環長 = 現在的 timer − visitTime[u]；若 u 的時間戳更早，代表接上了先前走過的鏈，該環（若存在）早已算過，直接停。每個節點只被打一次時間戳，整體 O(n)\n\n**關鍵觀察的形態**\n\n「函數 f(x) 映射到自身」本質上就是有向圖的一條邊 x -> f(x)；如果有 N 個狀態，那就形成了一棵基環樹或多個基環樹森林。看透這個等價關係是解題的第一步。",
        },
        {
          id: 22,
          title: "4.4 數學、數論與組合計數",
          summary:
            "**訊號**\n\n「對 10^9+7 取模的方案數」「GCD/因數/質數」「第 k 個滿足條件的數」。這類 Q4 靠的是知識儲備而非現場推導。\n\n**概念說明：為什麼要取模、以及模逆元的由來**\n\n方案數類的答案往往是指數級增長（例如排列數、路徑數），遠超過 64 位整數能表示的範圍，所以題目會要求「對一個質數（常見是 `10^9+7`）取模」。取模運算對加法、減法、乘法都能直接套用（`(a+b) mod p = ((a mod p) + (b mod p)) mod p`，乘法同理），但除法不能直接取模——因為除法在模運算下沒有一般意義的逆元，除非模數是質數。當模數 `p` 是質數時，任何不是 `p` 倍數的 `a` 都存在「模逆元」`a⁻¹`，滿足 `a * a⁻¹ ≡ 1 (mod p)`，由費馬小定理 `a^(p-1) ≡ 1 (mod p)` 可以推出 `a⁻¹ = a^(p-2) mod p`，用快速冪 O(log p) 求出。這就是「除法要轉成乘上模逆元」的原因，也是組合數 `C(n,k) = n! / (k!(n-k)!)` 在模意義下要先預處理階乘、再乘上階乘的逆元的原因。\n\n**必備武器**\n\n- 模運算全家桶：快速冪、模逆元（費馬小定理）、預處理階乘算組合數\n- 因數與質因數：`O(sqrt(n))` 分解、調和級數枚舉倍數（`n/1 + n/2 + ... ≈ n log n`）\n- GCD 沿子陣列延伸只會變小且最多 log 種取值——與 AND/OR 的性質同構\n\n**解題步驟**\n\n1. 看到「對 `10^9+7` 取模的方案數」，先確認需不需要除法：需要就要預處理階乘與階乘的模逆元\n2. 看到「因數」「質因數分解」「約數個數」，先確認資料範圍能否接受 `O(sqrt(n))` 分解，或需要調和級數式的枚舉倍數\n3. 看到「子陣列的 GCD 恰好等於 k」，先想 GCD 沿子陣列延伸的取值只有 O(log C) 種，可以用類似 AND/OR 的技巧壓縮狀態\n4. 看到「第 k 小/第 k 個滿足條件的數」，先想能不能把它轉成「≤ x 的有幾個」再二分 x\n\n```cpp\nlong long power(long long a, long long e, long long mod) {\n    long long r = 1;\n    for (a %= mod; e; e >>= 1, a = a * a % mod) {\n        if (e & 1) {\n            r = r * a % mod;\n        }\n    }\n    return r;\n}\n\n// 預處理階乘與階乘逆元，O(1) 查詢組合數\nconst int MOD = 1e9 + 7, MAXN = 200005;\nvector<long long> fact(MAXN), invFact(MAXN);\nvoid initComb() {\n    fact[0] = 1;\n    for (int i = 1; i < MAXN; ++i) fact[i] = fact[i - 1] * i % MOD;\n    invFact[MAXN - 1] = power(fact[MAXN - 1], MOD - 2, MOD); // 費馬小定理求逆元\n    for (int i = MAXN - 1; i > 0; --i) invFact[i - 1] = invFact[i] * i % MOD;\n}\nlong long C(int n, int k) {\n    if (k < 0 || k > n) return 0;\n    return fact[n] * invFact[k] % MOD * invFact[n - k] % MOD;\n}\n```\n\n**思考重點**\n\n- 計數題的兩板斧：正難則反（總數減不合法）、按「最後一步/最大元素/第一個不同位」分類\n- 「第 k 小」常轉成「≤ x 的有幾個」再二分 x——數學題和二分答案的高頻組合\n\n**補充武器：數位 DP（範圍計數）**\n\n「1 到 N 之間有多少個數字滿足某種數位性質」（不含重複數位、數位和被 k 整除……），當 N 大到 10^9 以上時逐一枚舉必然超時——這是數位 DP 的專屬場景。做法是把數字看成一串數位，從最高位往低位逐位決定：狀態通常是 `(當前位置, 是否仍貼著 N 的上界, 題目要求的性質狀態)`。「貼著上界」表示前面每一位都恰好取到 N 的對應位，因此當前位最多只能填到 N 的這一位；一旦某一位填得比上界小，後面的位就完全自由（0-9 任填），只有這種「自由」狀態才做記憶化。複雜度是 O(位數 × 狀態數 × 10)。它和上面「第 k 小轉成 ≤ x 有幾個再二分」是天生一對：`count(x)` 用數位 DP 實作、外層二分 x；詢問區間 `[L, R]` 內的個數時，用 `count(R) - count(L-1)` 拆成兩次計數。\n\n**逐步解法：以「LC 2447 Number of Subarrays With GCD Equal to K」為例**\n\n題意：求有多少個子陣列，其所有元素的 GCD 恰好等於 k。\n\n- 訊號：GCD 沿子陣列延伸單調不增，且不同取值最多 O(log C) 種——和 AND 的性質同構（見 3.3 節）\n- 逐步：固定右端點 `r`，維護「以 r 結尾的所有子陣列」的 GCD 集合（用 `(gcd值, 出現次數)` 的清單，而非逐一列舉每個左端點），每次右端點右移時，用新元素對前一組清單逐一取 GCD 並合併相同值，清單大小是 O(log C)，因此整體複雜度是 O(n log C)\n\n```cpp\nint subarrayGCD(vector<int>& nums, int k) {\n    int ans = 0;\n    vector<pair<int,int>> cur; // {gcd值, 出現次數}，代表以當前右端點結尾的子陣列\n    for (int x : nums) {\n        vector<pair<int,int>> nxt;\n        nxt.push_back({x, 1});\n        for (auto& [g, c] : cur) {\n            int ng = gcd(g, x);\n            if (!nxt.empty() && nxt.back().first == ng) nxt.back().second += c;\n            else nxt.push_back({ng, c});\n        }\n        cur = nxt;\n        for (auto& [g, c] : cur) if (g == k) ans += c;\n    }\n    return ans;\n}\n```\n\n**常見翻車點**\n\n取模後相減沒加回 `mod`、階乘表開太小、隱含的整數溢位、忘記模逆元只有在模數為質數時才用費馬小定理求。",
        },
        {
          id: 30,
          title: "4.5 字串進階：KMP、Z 演算法與字串雜湊",
          summary:
            "**訊號**\n\n「字串匹配」「子字串出現位置」「前後綴相同」以及「構造/轉換字串」。當字串長度超過 10^4 且需要多次比對時，暴力的 O(n²) 會超時。\n\n**概念說明**\n\n- **KMP 演算法**：核心是 `pi` 陣列（也叫失敗函式），`pi[i]` 代表「模式串前 i+1 個字元」這個子字串，其「最長公共前後綴」的長度。匹配時如果在位置 i 失配，不需要把指標退回去重新比對，而是利用 `pi[i-1]` 直接跳到「已知一定會匹配」的位置繼續比，因為這段前後綴重疊的部分不需要重新驗證——這正是 KMP 能做到 O(n+m) 而不是 O(nm) 的原因\n- **Z 演算法 (Z-function)**：`z[i]` 代表「從位置 i 開始的後綴」與「整個字串的前綴」最長能匹配多長。它用一個滑動的「Z-box」（目前已知匹配到最遠的區間）來重複利用已比對過的資訊，寫法比 KMP 直觀，特別適合「前後綴匹配」「把模式串和文本用分隔符接起來做匹配」\n- **字串雜湊 (Rolling Hash)**：把字串的每個前綴映射成一個大整數（通常用多項式雜湊 `hash[i] = hash[i-1] * base + s[i]`），任意子字串的雜湊值可以用前綴雜湊相減在 O(1) 內算出（和前綴和的想法完全相同），因此能以 O(1) 比較任意兩段子字串是否相等。萬金油但有碰撞風險，建議用雙雜湊（兩組不同的 base/mod）降低碰撞機率\n\n**解題步驟**\n\n1. 先確認題目是不是「在一個長文本裡找一個固定模式串出現的位置」——是的話直接套 KMP 或 Z 演算法模板\n2. 需要「任意兩段子字串是否相等」的多次查詢，用字串雜湊把比較壓到 O(1)\n3. 雜湊碰撞在週賽裡有時會被刻意針對（Anti-Hash 測資），能用確定性演算法（KMP、Z）解決的題優先不要冒雜湊碰撞的風險\n4. 「週期性字串」（找最短循環節）是 KMP 的經典應用：字串長度 n 減去 `pi[n-1]`，如果整除 n，這個差值就是最短循環節長度\n\n**核心模板：KMP**\n\n```cpp\nvector<int> buildPi(const string& p) {\n    int m = p.size();\n    vector<int> pi(m, 0);\n    for (int i = 1, j = 0; i < m; ++i) {\n        while (j > 0 && p[i] != p[j]) j = pi[j - 1]; // 失配時退到次長的公共前後綴\n        if (p[i] == p[j]) ++j;\n        pi[i] = j;\n    }\n    return pi;\n}\nvector<int> kmpSearch(const string& text, const string& pattern) {\n    vector<int> pi = buildPi(pattern), positions;\n    for (int i = 0, j = 0; i < (int)text.size(); ++i) {\n        while (j > 0 && text[i] != pattern[j]) j = pi[j - 1];\n        if (text[i] == pattern[j]) ++j;\n        if (j == (int)pattern.size()) {\n            positions.push_back(i - j + 1); // 找到一次匹配\n            j = pi[j - 1];\n        }\n    }\n    return positions;\n}\n```\n\n**逐步解法：以「LC 28 Find the Index of the First Occurrence in a String」為例**\n\n題意：在文本 `haystack` 中找模式串 `needle` 第一次出現的位置。\n\n- 直接套用上面的 `kmpSearch`，取回傳結果的第一個位置；若沒有結果回傳 -1\n- 複雜度 O(n + m)：建 `pi` 陣列 O(m)，掃描文本 O(n)，指標 `j` 的回退次數被 `pi` 陣列上限住，均攤是常數\n\n**常見翻車點**\n\n- `pi` 陣列建錯（`while` 條件或初始 `j` 寫錯），導致匹配結果整個錯位\n- 字串雜湊沒有用雙雜湊，遇到 Anti-Hash 測資直接 WA\n- 雜湊比較子字串時，前綴雜湊相減忘記乘上對應的 base 冪次，導致長度不同的子字串誤判相等\n\n**關鍵觀察的形態**\n\n「字首和後綴的等價性」或「把問題轉化成尋找特定 pattern」。一旦轉化完成，剩下就是套模板。",
        },
        {
          id: 23,
          title: "4.6 樹狀陣列、線段樹與 Trie",
          summary:
            "**訊號**\n\n「一邊修改一邊查詢」：單點改 + 區間查（樹狀陣列）、區間改或維護區間最值（線段樹）、字首匹配或最大異或（Trie）。逆序對型的「左邊有幾個比它大」也是樹狀陣列的招牌場景。\n\n**概念說明：為什麼需要「能修改的前綴和」**\n\n2.6 節的前綴和只能處理靜態陣列——如果陣列中間的值會不斷變動，每次修改都要重算後面所有前綴和，退化成 O(n)。樹狀陣列（Fenwick Tree）和線段樹就是為了解決「單點/區間修改 + 區間查詢都要 O(log n)」而生的資料結構：\n\n- **樹狀陣列**：利用二進位分解的想法，把「陣列的某個前綴」拆成 O(log n) 段，每段的和只存一次；查詢與修改都只需要沿著這條二進位路徑走，各是 O(log n)。它的限制是通常只支援「單點改、前綴/區間和查詢」，不容易直接做「區間最值」\n- **線段樹**：把陣列遞迴地切成左右兩半建成一棵二元樹，每個節點存它管轄區間的統計量（和、最值皆可）。搭配「懶標記」（lazy propagation）技巧，能把「區間整體修改」也壓到 O(log n)，是三者之中功能最全面但常數也最大的\n- **Trie（字典樹）**：把字串按字元逐層建成樹，樹上每條從根到某節點的路徑代表一個字首。查詢「某字首是否存在」「和某數字 XOR 最大的另一個數字是誰」（用二進位 Trie）都是 O(字串長度或位元數)\n\n**核心模板：樹狀陣列**\n\n```cpp\nstruct Fenwick {\n    int n;\n    vector<long long> t;\n    Fenwick(int n) : n(n), t(n + 1) {}\n    void add(int i, long long d) {\n        for (++i; i <= n; i += i & -i) {\n            t[i] += d;\n        }\n    }\n    long long sum(int i) {  // a[0..i] 之和\n        long long s = 0;\n        for (++i; i > 0; i -= i & -i) {\n            s += t[i];\n        }\n        return s;\n    }\n};\n```\n\n**核心模板：懶標記線段樹（區間加＋區間最大值）**\n\n```cpp\nstruct SegTree {\n    int n;\n    vector<long long> mx, lz;\n    SegTree(int n) : n(n), mx(4 * n, 0), lz(4 * n, 0) {}\n    void apply(int o, long long v) {\n        mx[o] += v;\n        lz[o] += v;\n    }\n    void push(int o) {  // 懶標記下推：把欠孩子的修改補上\n        if (lz[o] != 0) {\n            apply(2 * o, lz[o]);\n            apply(2 * o + 1, lz[o]);\n            lz[o] = 0;\n        }\n    }\n    void add(int o, int l, int r, int ql, int qr, long long v) {\n        if (qr < l || r < ql) return;\n        if (ql <= l && r <= qr) {\n            apply(o, v);\n            return;\n        }\n        push(o);\n        int m = (l + r) / 2;\n        add(2 * o, l, m, ql, qr, v);\n        add(2 * o + 1, m + 1, r, ql, qr, v);\n        mx[o] = max(mx[2 * o], mx[2 * o + 1]);\n    }\n    long long queryMax(int o, int l, int r, int ql, int qr) {\n        if (qr < l || r < ql) return LLONG_MIN;\n        if (ql <= l && r <= qr) return mx[o];\n        push(o);\n        int m = (l + r) / 2;\n        return max(queryMax(2 * o, l, m, ql, qr),\n                   queryMax(2 * o + 1, m + 1, r, ql, qr));\n    }\n    // 對外介面：根節點編號 1、管轄 [0, n-1]\n    void add(int l, int r, long long v) { add(1, 0, n - 1, l, r, v); }\n    long long queryMax(int l, int r) { return queryMax(1, 0, n - 1, l, r); }\n};\n```\n\n**解題步驟**\n\n1. 先寫出暴力解：通常是雙重迴圈，內層做某種統計（數幾個比它大/小、算區間和）\n2. 問「內層那個統計能不能用資料結構維護？」值域小或能離散化就用樹狀陣列；需要區間最值或懶標記式的區間修改就用線段樹；需要字首/位元相關查詢就用 Trie\n3. 值域很大時先離散化（排序去重、座標壓縮），再把「值」當下標丟進樹狀陣列\n4. 「按某維排序、另一維用資料結構查」是高頻雙件套：離線處理查詢，把二維問題降成一維\n\n**思考重點**\n\n- 值域很大時先離散化（排序去重、座標壓縮），再把「值」當下標丟進樹狀陣列\n- 「按某維排序、另一維用資料結構查」是 2000+ 高頻雙件套：離線處理查詢，把二維問題降成一維\n- 線段樹在週賽出現時多半只需要「區間加 + 區間最值/和」的懶標記版本，賽前準備一份自己寫熟的模板\n\n**逐步解法：以「LC 315 Count of Smaller Numbers After Self」為例**\n\n題意：對每個元素，求它右邊有多少個元素比它小。\n\n- 訊號：逆序對型統計——暴力是對每個位置往右掃一次，O(n²)。想把它壓到 O(n log n)，需要「邊掃邊查已出現過的元素裡有幾個比當前值小」\n- 做法：從右往左掃描，把每個元素的值離散化成下標；對樹狀陣列做「單點加 1（代表這個值出現過）」與「查詢比它小的元素個數（前綴和）」\n\n```cpp\nvector<int> countSmaller(vector<int>& nums) {\n    vector<int> sorted(nums);\n    sort(sorted.begin(), sorted.end());\n    sorted.erase(unique(sorted.begin(), sorted.end()), sorted.end()); // 離散化\n    int n = sorted.size();\n    Fenwick fen(n);\n    vector<int> ans(nums.size());\n    for (int i = nums.size() - 1; i >= 0; --i) {\n        int rank = lower_bound(sorted.begin(), sorted.end(), nums[i]) - sorted.begin();\n        ans[i] = rank > 0 ? fen.sum(rank - 1) : 0; // 查比它小的元素個數\n        fen.add(rank, 1); // 把當前值加進樹狀陣列\n    }\n    return ans;\n}\n```\n\n- 逐步：離散化把值域壓到 `[0, n)`；從右往左邊查邊插入，保證查詢時樹狀陣列裡只有「當前元素右邊」已出現過的值；整體 O(n log n)\n\n**常見翻車點**\n\n- 樹狀陣列下標從 1 開始，`add`/`sum` 忘記 `+1` 造成下標錯位\n- 離散化時忘記去重，導致同值元素的 rank 不一致\n- 線段樹懶標記下推（push down）的時機寫錯，導致區間修改沒有正確傳遞到子節點\n\n**關鍵觀察的形態**\n\n「暴力是 O(n²) 的雙重迴圈，內層那個統計可以用資料結構維護」——先寫出暴力，再問內層在查什麼。",
        },
        {
          id: 32,
          title: "4.7 離線查詢與進階區間維護：ST 表與掃描線",
          summary:
            "**訊號**\n\n「給定 Q 個查詢，每次詢問某個區間或某個條件下的最值/數量」，且查詢與查詢之間互相獨立（不強制在線）。\n\n**概念說明：離線的核心是「換一個更方便的順序回答問題」**\n\n「在線」代表每個查詢來了就必須立刻回答，不能參考未來的查詢；「離線」代表可以先把所有查詢都讀進來，選擇一個對演算法最有利的順序，一次處理完再統一回答。離線思維的價值在於：原本每個查詢都要獨立花 O(n) 重新掃描，如果能找到一個順序，讓查詢之間可以「重複利用前一個查詢已經算好的資訊，只需要處理新增的差異部分」，就能把總複雜度從 O(nq) 壓到 O((n+q) log n) 甚至更低。\n\n**核心套路**\n\n- **離線排序**：如果不強制在線回答，第一步永遠是「把所有查詢存起來，按某個維度（如區間長度、上限值）排序」。接著用雙指標或單調堆疊，隨著查詢的推進，逐步把符合條件的元素加入資料結構（如樹狀陣列、線段樹或並查集）中\n- **ST 表 (Sparse Table)**：對於「靜態區間最值查詢 (RMQ)」，ST 表能做到 O(N log N) 預處理、O(1) 查詢，比線段樹的 O(log N) 更適合查詢次數極大的場景。原理是利用「最值運算滿足重疊區間也不影響結果」的性質（`max(a,b)` 就算 a、b 的範圍重疊也不會算錯），把每個區間拆成兩個長度為 2 的冪次、可能重疊的子區間分別查詢再合併\n- **掃描線 (Sweep Line)**：把二維的區間交集或覆蓋問題，降維成沿著時間軸/坐標軸掃描的事件點（進入事件與離開事件）\n\n**解題步驟**\n\n1. 先確認查詢是否「不強制在線」（後面的查詢不依賴前面查詢的即時結果）——是的話才能離線排序\n2. 決定離線排序的維度：常見是按區間右端點排序（配合資料結構逐步加入元素）、按查詢的上限值排序（配合雙指標）\n3. 若查詢是「區間最值，陣列本身不會被修改」，直接上 ST 表，不需要用到線段樹的修改功能\n4. 若是二維幾何覆蓋問題（矩形面積聯集、天際線），先把它轉成掃描線 + 事件點，再想清楚掃描過程中要維護什麼資料結構\n\n**核心模板：ST 表（區間最大值）**\n\n```cpp\nint LOG[100005]; // LOG[i] = floor(log2(i))\nvector<vector<int>> st; // st[k][i] = 從 i 開始，長度 2^k 的區間最大值\nvoid buildSparseTable(vector<int>& a) {\n    int n = a.size();\n    LOG[1] = 0;\n    for (int i = 2; i <= n; ++i) LOG[i] = LOG[i / 2] + 1; // 先建 LOG 表再用\n    int maxK = LOG[n] + 1;\n    st.assign(maxK, vector<int>(n));\n    st[0] = a;\n    for (int k = 1; k < maxK; ++k) {\n        for (int i = 0; i + (1 << k) <= n; ++i) {\n            st[k][i] = max(st[k-1][i], st[k-1][i + (1 << (k-1))]); // 拆成兩個重疊子區間\n        }\n    }\n}\nint queryMax(int l, int r) { // 查詢 [l, r] 的最大值\n    int k = LOG[r - l + 1];\n    return max(st[k][l], st[k][r - (1 << k) + 1]);\n}\n```\n\n**逐步解法：以「LC 218 The Skyline Problem」為例**\n\n題意：給一組矩形建築物 `(left, right, height)`，求它們輪廓線（天際線）的轉折點。\n\n- 訊號：二維覆蓋問題——把每棟建築拆成兩個事件：「左邊界進入，高度變化 +height」與「右邊界離開，高度變化 -height」\n- 逐步：所有事件按 x 座標排序，同一 x 座標有三條 tie-break 規則：進入排在離開之前、多個進入時「高的先進」、多個離開時「矮的先出」——把進入事件的高度取負存起來直接按數值排序，恰好同時滿足三條；用一個「目前所有存活高度」的多重集合（`multiset` 或懶刪除堆）維護當前最大高度；每次事件處理完，若「目前最大高度」和上一個記錄的高度不同，就產生一個新的轉折點\n\n```cpp\nvector<vector<int>> getSkyline(vector<vector<int>>& buildings) {\n    vector<pair<int, int>> events; // {x, h}：h<0 是進入事件（高度 -h），h>0 是離開事件（高度 h）\n    for (auto& b : buildings) {\n        events.push_back({b[0], -b[2]}); // 進入：取負，同 x 時高的先處理、且先於離開\n        events.push_back({b[1], b[2]});  // 離開：為正，同 x 時矮的先處理\n    }\n    sort(events.begin(), events.end());\n    multiset<int> heights = {0};\n    vector<vector<int>> ans;\n    int prevMax = 0;\n    for (auto& [x, h] : events) {\n        if (h < 0) heights.insert(-h);\n        else heights.erase(heights.find(h));\n        int curMax = *heights.rbegin();\n        if (curMax != prevMax) {\n            ans.push_back({x, curMax});\n            prevMax = curMax;\n        }\n    }\n    return ans;\n}\n```\n\n**常見翻車點**\n\n- ST 表只適合靜態陣列（不能修改），陣列若會變動要換線段樹\n- 掃描線事件排序時，同一 x 座標的進入/離開順序沒想清楚，導致高度瞬間重疊時算錯\n- ST 表查詢時兩個子區間重疊是允許的（因為是最值查詢），但如果誤用在「區間和」上會重複計算，這是初學者最常見的誤用\n\n**關鍵觀察的形態**\n\n「如果按這個順序回答查詢，我就可以把原本 O(N) 的搜索，變成『只維護新增部分』的 O(log N)」。離線思維的核心就是降維打擊。",
        },
      ],
    },
    {
      id: 24,
      title: "5. 實戰關：節奏、罰時與復盤",
      summary:
        "技巧練齊了，最後一塊拼圖是「比賽本身」。同樣的實力，賽場執行的好壞可以差出幾百名：開場順序、卡題時的止損、提交前的自查、罰時的控制，都是可以刻意練習的技能。\n\n**賽場執行清單：**\n- 開場先把四題全部掃一遍標題和資料範圍（2 分鐘），對整場的難度分佈有預期\n- 卡題超過 15 分鐘就跳下一題，回頭再看時常常秒懂\n- 提交前 30 秒自查：邊界（空、單元素）、溢位、取模、輸出格式\n- WA 之後先重讀題目而不是重讀程式碼——一半的 WA 是理解錯題意\n- 最後 10 分鐘不開新題，回頭檢查手上做一半的\n\n**復盤模板（賽後 48 小時內完成）：**\n1. 每題實際用時 vs. 理想用時，差在哪個環節（讀題/想法/實作/除錯）\n2. 沒解出的題補完，寫下關鍵觀察與缺的訊號\n3. 每次 WA 歸因到一類（讀題錯/邊界/溢位/演算法錯），維護自己的翻車統計表",
      children: [
        {
          id: 25,
          title: "5.1 模擬實戰：三場 90 分鐘全真模擬",
          summary:
            "**怎麼用這一節**\n\n這裡有三場模擬賽，每場四題按真實週賽的難度曲線配置（Q1≈1200 → Q4≈2100+）。請完全按照真實比賽執行：\n\n- 連續 90 分鐘不中斷，不查資料、不看題解\n- 按 Q1 → Q4 順序開題，執行上一節的賽場清單\n- 記錄每題的開始/提交時間與每次 WA\n\n**概念說明：為什麼要「刻意」模擬而不是隨意刷題**\n\n平時分章節練習時，每一題都已經被貼上標籤（這是滑動視窗、那是 DP），大腦其實省略了「辨認這題屬於哪個模型」這一步——而真實比賽裡，四題混在一起、沒有任何標籤，辨認模型本身就要花時間，甚至可能認錯。全真模擬的意義，是把「不知道會考什麼」這個真實壓力找回來，同時練習在 90 分鐘的資源約束下做取捨（先攻哪題、卡住要不要換題、要不要賭一個沒把握的貪心）。這些能力沒辦法透過分章節練習取得，只能在計時的整場模擬中鍛鍊。\n\n**90 分鐘的資源分配範例**\n\n| 時間段 | 建議動作 |\n| --- | --- |\n| 0:00-0:02 | 掃過四題標題與資料範圍，預估難度分佈 |\n| 0:02-0:12 | Q1，目標零罰時提交 |\n| 0:12-0:25 | Q2，目標零罰時提交 |\n| 0:25-0:60 | Q3：先寫下你認為的模型與訊號，再動手；卡超過 15 分鐘先跳到 Q4 看一眼再回來 |\n| 0:60-0:85 | Q4：先找關鍵觀察，想不到就退回檢查 Q1-Q3 的邊界 |\n| 0:85-0:90 | 不開新題，複查已提交題目的邊界與已寫一半的程式碼 |\n\n**評估基準（依目前水準對照）**\n\n- 目標「穩定三題」：Q1+Q2 合計 15 分鐘內、Q3 在 60 分鐘內拿下\n- 目標「衝擊 AK」：前三題 40 分鐘內收工，留 50 分鐘給 Q4\n- 任何一場罰時超過兩次，回到第一階段補速度與自查清單\n\n**賽後復盤範例**\n\n> Q3 用了 42 分鐘，其中 25 分鐘花在「以為是滑動視窗，寫到一半發現條件不具單調性」。缺的訊號：題目其實允許數值為負，視窗擴大時和不是單調變大——下次看到「允許負數」要先驗證單調性再動手，而不是看到「連續子陣列」就直接寫滑動視窗骨架。\n\n每場都要完成完整復盤。三場打完後對照三份復盤：重複出現的失分環節，就是你下一個訓練週期的主題。",
        },
        {
          id: 26,
          title: "5.2 罰時陷阱：邊界與大數翻車點",
          summary:
            "**這一節在練什麼**\n\n這裡收錄的題共同點是：思路不難，但地雷密佈——邊界情況、整數溢位、細節模擬、「看起來是 A 其實是 B」的偽裝題。它們專門用來訓練「提交前找出自己 bug」的能力。\n\n**練法（與其他節相反）**\n\n- 寫完之後不要立刻提交。先自己構造三個刁鑽測資：最小輸入、全部相同、極端大值\n- 用測資跑過再交，目標是每題零 WA——這一節「一次通過率」比速度重要\n- 提交後如果還是 WA 了，把漏掉的情況補進自己的自查清單\n\n**高頻地雷清單（含具體範例）**\n\n- **乘法溢位**：`int * int` 存進 `long long` 之前就已經溢位了。\n  ```cpp\n  int a = 100000, b = 100000;\n  long long bad = a * b;        // 錯！int*int 先溢位，結果已經錯了才轉型\n  long long good = (long long)a * b; // 對：先轉型再相乘\n  ```\n- **空集合/單元素**：迴圈根本不執行時，初始值是不是答案？例如求最大值卻把初始值設成 0，遇到全負數陣列就會錯。\n- **相等的元素**：嚴格遞增還是非嚴格？`a[i] < a[i+1]` 與 `a[i] <= a[i+1]` 一字之差，結果完全不同；`sort` 預設不保證穩定，需要穩定排序時要用 `stable_sort` 或帶下標排序。\n- **模擬題的終止條件**：狀態會不會迴圈？沒有偵測循環節的模擬題，遇到會無限循環的測資就會超時；判斷方式是把「看過的狀態」存進雜湊集合，重複出現就代表進入循環。\n- **讀題陷阱**：「恰好」與「至少」一字之差，答案的判斷條件完全不同；下標從 0 還是 1 開始，直接影響所有邊界公式；「子陣列（必須連續）」與「子序列（可以不連續）」是兩種完全不同的問題。\n\n**解題步驟：如何在賽場上抓出這類 bug**\n\n1. 寫完後不要看程式碼，重新讀一次題目的每一句話，確認程式碼有沒有遺漏任何一個條件字眼\n2. 手動構造三個測資：最小輸入（空、單一元素）、全部相同的值、貼近資料範圍上限的極端值，逐一跑過\n3. 檢查所有「和」「乘積」「計數」相關的變數型別，凡是可能超過 `2^31` 就一律用 `long long`\n4. 檢查所有 `<` / `<=` 的邊界，問自己：「相等的情況，題目要不要算進去？」\n\n**常見翻車點對照範例**\n\n```cpp\n// 常見錯誤：忘記處理空輸入導致的邊界\nint findMax(vector<int>& a) {\n    int best = 0;              // 錯：如果全是負數，答案會被錯誤地夾在 0\n    for (int x : a) best = max(best, x);\n    return best;\n}\nint findMaxFixed(vector<int>& a) {\n    int best = INT_MIN;        // 對：初始值取型別最小值，或直接用 a[0]\n    for (int x : a) best = max(best, x);\n    return best;\n}\n```",
        },
      ],
    },
  ],
} as TutorialData.Root;
