import type { TutorialData } from "@/types";

/**
 * 週賽 AK 之路：以「一場週賽的解剖」為主軸的原創學習路線。
 * 節點 id 與 public/studyplan/weekly_contest.json 一一對應，
 * 讓題單頁能依 id 取得對應章節的講解。
 */
export const weeklyContest = {
  id: 1,
  title: "週賽 AK 之路",
  src: "",
  last_update: "2026-07-04T00:00:00.000000+00:00",
  summary:
    "這條路線不按演算法教科書的目錄走，而是按「一場週賽的解剖」來設計。每場週賽固定四題：Q1/Q2 考手速與細節、Q3 考建模、Q4 考深度。想拿好名次甚至 AK（All Kill，四題全解），需要的不是把所有演算法學一遍，而是把每個位置需要的能力分開練到位。\n\n**五個階段的分工：**\n- **速度關**：Q1 三分鐘、Q2 八分鐘內一次通過，名次的地板由此決定\n- **建模關**：Q3 最常見的五種模型，練到「看到訊號就想到工具」\n- **攻堅關**：資料結構選型與 DP 狀態設計，把 Q3 變成穩定得分、偶爾偷到 Q4\n- **天花板**：Q4 的高階武器，每解出一題就是一次大漲分\n- **實戰關**：把技巧帶進 90 分鐘的計時賽場，練節奏、控罰時、做復盤\n\n**使用建議：**\n- 每週安排 4-5 天練當前階段的題，週末打一場真實或 Virtual 週賽\n- 每個階段的題目都按難度遞增排列，從第一題開始按順序做\n- 練速度的題（第一階段）要計時；練深度的題（第四階段）允許最多 90 分鐘再看題解\n- 每道卡住的題記下：卡在哪一步、缺什麼訊號、下次怎麼更快想到\n- 打完每場比賽，未解出的題必須在 48 小時內補完，這是漲分最快的一件事",
  children: [
    {
      id: 2,
      title: "1. 速度關：Q1/Q2 十分鐘雙殺（~1400）",
      summary:
        "週賽名次的地板是由 Q1/Q2 的速度決定的：同樣四題全對，快 10 分鐘可能差幾百名；更殘酷的是，一次 WA 罰 5 分鐘，兩次手滑就抵掉一題 Q3 的優勢。這一階段的目標不是「會做」——這些題你本來就會——而是「又快又零罰時」。\n\n**練法：**\n- 每題計時：Q1 等級（<1200）目標 3 分鐘內、Q2 等級（1200-1400）目標 8 分鐘內提交且一次通過\n- 提交前固定過一遍檢查清單：空輸入、單元素、重複值、負數、整數溢位\n- 寫錯一次就記錄原因，統計自己最常見的手滑類型（下標偏移、邊界、讀錯題）\n- 這一階段的題適合當每日熱身，每天 2-3 題維持手感",
      children: [
        {
          id: 3,
          title: "1.1 模擬與枚舉：把題面直譯成程式",
          summary:
            "**這一節在練什麼**\n\n模擬題沒有演算法含量，考的是「讀題 → 程式碼」的直譯速度與準確率。週賽 Q1 幾乎都是這種題，偶爾 Q2 也是。\n\n**核心習慣**\n\n- 讀題時把規則抄成條列（輸入是什麼、每一步做什麼、什麼時候停），寫程式時逐條對照，避免漏規則\n- 枚舉題先估算量級：範圍 `n <= 100` 的雙重迴圈直接寫，不要浪費時間想聰明解法——週賽比的是總時間\n- 字串與下標處理寧可多寫幾個變數，也不要在一行裡塞三個 `+1/-1`\n\n**常見翻車點**\n\n讀錯題是這個等級最大的罰時來源。特別注意「恰好/至少/至多」「子陣列（連續）/子序列（可不連續）」「回傳下標/回傳值」這三組字眼。",
        },
        {
          id: 4,
          title: "1.2 雜湊與計數：O(n) 的第一直覺",
          summary:
            "**這一節在練什麼**\n\n看到「出現次數」「是否存在」「配對」，第一反應就應該是雜湊表或計數陣列，這是 Q1/Q2 最高頻的工具。\n\n**核心習慣**\n\n- 值域小（例如 `0-100` 或小寫字母）用陣列計數，比雜湊表更快也更不容易寫錯\n- 「兩數之和」型的配對問題，邊掃邊查「我需要的另一半是否已出現」，一次遍歷完成\n- 「每個數字的出現次數」統計完之後，常常還要對次數再做一次統計或排序——先想清楚題目要的是哪一層\n\n**常見翻車點**\n\n計數陣列開太小（沒看清值域上限）、忘記處理「自己和自己配對」的情況、以及把「元素」和「次數」兩層搞混。",
        },
        {
          id: 5,
          title: "1.3 排序與雙指標：先排序再思考",
          summary:
            "**這一節在練什麼**\n\n很多題排序之後結構就變得一目了然：最值在兩端、相同元素相鄰、配對可以貪心。排序是 Q2 最常見的第一步。\n\n**核心習慣**\n\n- 拿到陣列題先自問：「排序會不會破壞答案？」不需要保留原順序的題，排序幾乎總是簡化問題\n- 需要保留原下標時，排「(值, 下標)」的配對而不是值本身\n- 排序後的配對/收縮問題用雙指標：一端各放一個指標，根據條件移動其中一個，每步都要說得出「為什麼這樣移動不會漏解」\n\n**常見翻車點**\n\n自訂比較器寫反方向、排序後忘了題目要求輸出原始下標、雙指標移動條件用 `<` 還是 `<=` 沒想清楚導致漏掉相等的情況。",
        },
        {
          id: 6,
          title: "1.4 前綴和與差分：區間統計標配",
          summary:
            "**這一節在練什麼**\n\n「子陣列的和」「區間內的數量」這類措辭出現時，前綴和能把每次區間查詢從 O(n) 壓到 O(1)；反過來「對一段區間整體加值」則是差分陣列的訊號。\n\n**核心模板**\n\n```cpp\n// 前綴和：pre[i] = a[0..i) 之和，區間 [l, r] 的和 = pre[r+1] - pre[l]\nvector<long long> pre(n + 1);\nfor (int i = 0; i < n; ++i) {\n    pre[i + 1] = pre[i] + a[i];\n}\n```\n\n二維版本同理：矩陣前綴和讓任意子矩陣的和變成四項加減。\n\n**核心習慣**\n\n- 前綴和陣列開 `n+1` 格、從 1 開始存，可以避開一堆邊界判斷\n- 和可能超過 `int`，前綴和一律用 64 位整數\n- 「有多少個子陣列滿足…」常常是前綴和 + 雜湊表：邊掃邊查「需要的前綴值出現過幾次」\n\n**常見翻車點**\n\n下標偏移（`pre[r+1] - pre[l]` 寫成 `pre[r] - pre[l]`）與忘記開 long long，是這類題僅有的兩種死法。",
        },
      ],
    },
    {
      id: 7,
      title: "2. 建模關：Q3 的常客套路（1400-1700）",
      summary:
        "Q3 的本質是「把敘述翻譯成已知模型」。題目不會告訴你該用什麼演算法，但訊號都藏在措辭裡：「連續子陣列」指向滑動視窗、「最大化最小值」指向二分答案、「最少操作次數」常是貪心或 BFS。這一階段練五個出現率最高的模型，目標是把「辨認訊號 → 套用模型」的時間壓進 5 分鐘。\n\n**練法：**\n- 每題動手前先寫下：我認為這是什麼模型、依據是哪個訊號\n- 做完對照題解：模型判斷錯的題，把正確訊號補進自己的判斷清單\n- 單題上限 40 分鐘，超時就看提示——這一階段的目的是積累模型，不是硬磕",
      children: [
        {
          id: 8,
          title: "2.1 滑動視窗：連續子陣列的預設解法",
          summary:
            "**訊號**\n\n「最長/最短的連續子陣列（子字串），滿足某條件」，且條件具有單調性：視窗擴大時條件只會朝一個方向變化（例如和變大、種類數變多）。\n\n**核心模板**\n\n```cpp\n// 求最長的滿足條件的視窗\nint left = 0;\nfor (int right = 0; right < n; ++right) {\n    add(a[right]);               // 把右端點納入視窗\n    while (!ok()) {              // 條件被破壞就收縮左端\n        remove(a[left++]);\n    }\n    ans = max(ans, right - left + 1);\n}\n```\n\n**思考重點**\n\n- 先驗證單調性：如果視窗擴大後條件可能「壞了又好」（例如涉及乘積為負、異或），滑動視窗不適用，要改前綴和或其他工具\n- 定長視窗是簡化版：右進左出，維護視窗內統計量即可\n- 「至多 K 種/K 個」型的計數常用「至多 K 減去至多 K-1」的轉化\n\n**時間目標**\n\n模型認出來之後，這類題應該在 15 分鐘內寫完；認不出來才是真正的瓶頸。",
        },
        {
          id: 9,
          title: "2.2 二分搜尋與二分答案",
          summary:
            "**訊號**\n\n「最大化最小值」「最小化最大值」「至少需要多少才能滿足」——答案本身有單調性：如果 x 可行，比 x 更寬鬆的值也可行。此外，有序結構上的查詢（第一個 ≥ x 的位置）是二分的直接形式。\n\n**核心模板**\n\n```cpp\n// 二分答案：找最小的可行值\nlong long lo = minAns, hi = maxAns;\nwhile (lo < hi) {\n    long long mid = lo + (hi - lo) / 2;\n    if (check(mid)) {\n        hi = mid;      // mid 可行，往更小找\n    } else {\n        lo = mid + 1;  // mid 不可行，必須更大\n    }\n}\n// lo 即為答案\n```\n\n**思考重點**\n\n- 把力氣花在 `check` 上：它通常是一個貪心或線性掃描，先在紙上確認它是 O(n) 或 O(n log n)\n- 明確寫下「check(x) 為真代表什麼」，並驗證單調方向，方向想反是最常見的錯\n- 二分邊界用「找最小可行」或「找最大可行」兩個模板之一，不要每次現場推導\n\n**時間目標**\n\n看到「最小化最大值」就直接開寫二分骨架，把思考時間全部留給 check 函式。",
        },
        {
          id: 10,
          title: "2.3 貪心與交換論證",
          summary:
            "**訊號**\n\n「最少操作次數」「最大總收益」，且每一步的選擇之間沒有複雜的後效性。排序後從一端掃過去、每步做局部最優選擇，就是貪心的雛形。\n\n**思考重點**\n\n- 貪心必須有理由。最實用的驗證法是交換論證：假設最優解與貪心解在某一步不同，證明把最優解的那一步換成貪心的選擇不會變差\n- 想不出證明時，花 60 秒構造反例；反例構造不出來、直覺又強，週賽裡可以先交（這是比賽和練習的差別——但賽後要補證明）\n- 常見貪心排序鍵：截止時間、結束位置、性價比、兩數位差\n\n**常見翻車點**\n\n「排序鍵選錯」佔貪心 WA 的大半。如果第一個貪心策略 WA 了，不要急著修補條件，先重新想排序鍵——通常是整個方向錯了。",
        },
        {
          id: 11,
          title: "2.4 網格與 BFS/DFS",
          summary:
            "**訊號**\n\n輸入是網格或圖、問「最少步數」「能否到達」「連通區域」。等權最短路用 BFS，連通性與計數用 DFS/BFS 都行。\n\n**核心模板**\n\n```cpp\n// 網格 BFS：dist 兼作 visited\nvector<vector<int>> dist(m, vector<int>(n, -1));\nqueue<pair<int, int>> q;\ndist[sx][sy] = 0;\nq.push({sx, sy});\nint dirs[5] = {0, 1, 0, -1, 0};\nwhile (!q.empty()) {\n    auto [x, y] = q.front();\n    q.pop();\n    for (int d = 0; d < 4; ++d) {\n        int nx = x + dirs[d], ny = y + dirs[d + 1];\n        if (0 <= nx && nx < m && 0 <= ny && ny < n &&\n            grid[nx][ny] != WALL && dist[nx][ny] < 0) {\n            dist[nx][ny] = dist[x][y] + 1;\n            q.push({nx, ny});\n        }\n    }\n}\n```\n\n**思考重點**\n\n- 「狀態」不一定只是座標：帶鑰匙、帶剩餘次數的題，把附加資訊也放進狀態（座標 × 附加狀態一起去重）\n- 多個起點同時擴散（多源 BFS）：把所有起點先塞進佇列即可\n- DFS 在深度大的網格會爆遞迴堆疊，計數連通塊以外的場合優先寫 BFS\n\n**時間目標**\n\n模板要練到閉眼寫對：邊界判斷、visited 標記時機（入隊時標，不是出隊時標）都不假思索。",
        },
        {
          id: 12,
          title: "2.5 線性 DP：從遞迴到轉移方程",
          summary:
            "**訊號**\n\n「方案數」「最大/最小總和」，且決策有後效性——當前選擇影響後面能做什麼（相鄰不能同取、狀態會延續）。貪心給不出理由的最值題，多半是 DP。\n\n**思考流程（固定四步）**\n\n1. 定狀態：`dp[i]` 代表「考慮前 i 個元素、且滿足某種結尾條件」的最優值/方案數——狀態語意必須一句話說清\n2. 想轉移：第 i 步有哪幾種選擇，每種選擇從哪個更小的狀態來\n3. 定初值與答案位置：空前綴是什麼、答案在最後一格還是所有格的最值\n4. 檢查順序：轉移只依賴更小的 i 就從左往右填\n\n**核心習慣**\n\n- 想不出狀態時，先寫暴力遞迴再加記憶化，遞迴參數就是狀態\n- 「以 i 結尾」是線性 DP 最萬用的狀態設計，最長遞增子序列、最大子陣列和都是它\n- 狀態機 DP（持有/不持有、開/關）就是把「以 i 結尾」再乘上一個小的模式維度\n\n**時間目標**\n\n這一節的題允許 40 分鐘。DP 的速度來自狀態設計的存量，不來自現場的靈感。",
        },
      ],
    },
    {
      id: 13,
      title: "3. 攻堅關：穩定收下 Q3、偷襲 Q4（1700-2000）",
      summary:
        "這一段決定你能不能從「三題選手」變成「穩定三題半」。1700-2000 的題很少考全新的演算法，考的是組合：排序之後維護一個堆、二分答案套一個 DP check、BFS 的狀態裡多帶一維。核心能力有兩個——資料結構選型（每一步需要查什麼、改什麼，選一個都是 O(log n) 的容器）與 DP 狀態設計。\n\n**練法：**\n- 單題上限 45 分鐘；卡住時先寫下「我每一步需要維護什麼資訊」，這句話通常直接指向正確的資料結構\n- 做完之後問自己：這題是哪兩個基本技巧的組合？把組合方式記下來",
      children: [
        {
          id: 14,
          title: "3.1 堆與有序集合：動態極值",
          summary:
            "**訊號**\n\n「每次取最大/最小的元素處理，處理後可能放回或產生新元素」——堆；「動態插入刪除，還要查前驅後繼或第 k 小」——有序集合（C++ `set`/`multiset`）。\n\n**思考重點**\n\n- 堆適合「只關心極值」的場景：k 路合併、按時間軸模擬事件、反悔貪心（先拿下，超限時把最差的吐出來）\n- 元素會過期或失效時用惰性刪除：彈出時檢查是否過期，過期就丟掉再彈下一個，不要嘗試在堆裡原地刪\n- 有序集合比堆強在「能查中間」：需要前驅/後繼/範圍計數時果斷換 `set` 或樹狀陣列\n- 按時間軸模擬的題，先把事件排序，再用堆維護「當前可用的候選」——這個「排序 + 堆」的雙層結構是本節一半題目的骨架\n\n**常見翻車點**\n\n比較器方向（C++ `priority_queue` 預設是大根堆）、以及忘了惰性刪除導致取到已失效的極值。",
        },
        {
          id: 15,
          title: "3.2 單調堆疊與單調佇列",
          summary:
            "**訊號**\n\n「每個元素左/右第一個比它大（小）的位置」——單調堆疊；「滑動視窗內的最值」——單調佇列；「每個元素作為最小值能管轄的區間」——單調堆疊算貢獻。\n\n**核心模板**\n\n```cpp\n// 每個元素右邊第一個更大元素的下標\nvector<int> nxt(n, -1);\nstack<int> st;  // 存下標，對應值遞減\nfor (int i = 0; i < n; ++i) {\n    while (!st.empty() && a[st.top()] < a[i]) {\n        nxt[st.top()] = i;\n        st.pop();\n    }\n    st.push(i);\n}\n```\n\n**思考重點**\n\n- 單調堆疊的本質：被彈出的元素「再也不會是任何人的答案」，所以每個元素進出各一次，整體 O(n)\n- 貢獻法是它的高階用法：「所有子陣列的最小值之和」= 每個元素 × 它作為最小值管轄的子陣列數，管轄範圍就是左右第一個更小元素之間\n- 邊界用「一邊取嚴格、一邊取非嚴格」處理相等元素，避免重複計數\n\n**常見翻車點**\n\n嚴格與非嚴格不等號的搭配、以及堆疊裡存值還是存下標（幾乎永遠應該存下標）。",
        },
        {
          id: 16,
          title: "3.3 位運算與子集枚舉",
          summary:
            "**訊號**\n\n`n <= 20`（子集狀壓）、「按位與/或/異或」出現在題面、「每個元素選或不選」的小規模組合問題。\n\n**核心模板**\n\n```cpp\n// 枚舉 mask 的所有非空子集\nfor (int sub = mask; sub; sub = (sub - 1) & mask) {\n    // sub 是 mask 的子集\n}\n```\n\n**思考重點**\n\n- AND/OR 沿著子陣列延伸時是單調的（AND 只會變小、OR 只會變大），且不同的值最多 O(log C) 種——「子陣列 AND/OR 等於 k」類的題靠這個性質壓複雜度\n- XOR 的配對問題轉成前綴異或 + 雜湊表，和前綴和完全同構\n- 逐 bit 獨立計算貢獻：涉及所有數對的位運算總和，拆成每個 bit 上有幾個 0、幾個 1\n\n**常見翻車點**\n\n位移超過 31 位沒用 `1LL`、運算子優先級（`&` 比 `==` 低，記得加括號）。",
        },
        {
          id: 17,
          title: "3.4 圖論主戰場：最短路、拓撲、並查集",
          summary:
            "**訊號與選型**\n\n- 等權圖最少步數 → BFS；邊權非負 → Dijkstra；邊權只有 0 和 1 → 0-1 BFS（deque）\n- 「先修條件」「依賴順序」→ 拓撲排序，順帶在 DAG 上做 DP\n- 「動態合併集合、查是否連通」→ 並查集；配合「把邊按權重排序後逐條加入」能處理一大類離線連通性問題\n\n**核心模板**\n\n```cpp\n// Dijkstra（鄰接表 g[u] = {v, w}）\npriority_queue<pair<long long, int>, vector<pair<long long, int>>, greater<>> pq;\nvector<long long> dist(n, LLONG_MAX);\ndist[s] = 0;\npq.push({0, s});\nwhile (!pq.empty()) {\n    auto [d, u] = pq.top();\n    pq.pop();\n    if (d > dist[u]) continue;  // 惰性刪除過期項\n    for (auto [v, w] : g[u]) {\n        if (d + w < dist[v]) {\n            dist[v] = d + w;\n            pq.push({dist[v], v});\n        }\n    }\n}\n```\n\n**思考重點**\n\n- 週賽的圖論題常把「建圖」藏起來：狀態是節點、操作是邊。看到「最少操作次數把 x 變成 y」先想想能不能建圖跑 BFS/Dijkstra\n- 最小生成樹（Kruskal = 排序 + 並查集）在「讓所有點連通的最小代價」措辭下出現\n\n**常見翻車點**\n\nDijkstra 忘了 `d > dist[u]` 的過期檢查（複雜度退化）、拓撲排序沒檢查是否有環。",
        },
        {
          id: 18,
          title: "3.5 DP 進階：背包、區間與狀態機",
          summary:
            "**這一節在練什麼**\n\n線性 DP 之上最常考的三種狀態設計，以及「DP 作為別的技巧的零件」的用法（例如二分答案的 check 是個 DP）。\n\n**三個模型**\n\n- **背包**：「從集合裡選一些，總重不超過 C，最大化價值/湊出目標的方案數」。0/1 背包容量倒序枚舉，完全背包正序枚舉——這一個順序差別要刻進肌肉記憶\n- **區間 DP**：「合併相鄰段的代價」「回文結構」。狀態 `dp[l][r]`，按區間長度從小到大填\n- **狀態機 DP**：「持有/不持有」「上一個選了/沒選」。狀態 = 位置 × 模式，轉移是模式間的合法跳轉\n\n```cpp\n// 0/1 背包：容量必須倒序\nfor (int i = 0; i < n; ++i) {\n    for (int c = C; c >= w[i]; --c) {\n        dp[c] = max(dp[c], dp[c - w[i]] + v[i]);\n    }\n}\n```\n\n**思考重點**\n\n- 方案數 DP 記得取模，且初始值 `dp[0] = 1` 的語意（空集合是一種方案）要想清楚\n- 字串分割類（「分成最少的美麗子字串」）是線性 DP + 可行性判斷的組合：`dp[i] = min(dp[j] + 1)`，j 到 i 這段合法\n\n**時間目標**\n\n45 分鐘。這一節做完，Q3 的 DP 應該不再是玄學，而是四步流程的機械執行。",
        },
      ],
    },
    {
      id: 19,
      title: "4. 天花板：Q4 硬骨頭（2000-2400）",
      summary:
        "Q4 不要求場場會，但每多解出一題就是一次大漲分。2000+ 的題通常有一個「關鍵觀察」：沒看到它，怎麼寫都是超時或錯誤；看到了，剩下的就是攻堅關的技術。所以這一階段的練法和前面不同——允許慢，但必須把每題的關鍵觀察提煉成一句話記下來。\n\n**練法：**\n- 每題最多 90 分鐘；解不出來就精讀題解，然後隔幾天不看題解重寫一遍\n- 每題記錄：關鍵觀察是什麼、什麼訊號能提示它、我當時的思路差在哪一步\n- 一週 3-4 題就夠，貪多嚼不爛",
      children: [
        {
          id: 20,
          title: "4.1 狀壓與高維 DP",
          summary:
            "**訊號**\n\n`n <= 16` 左右的「排列/分組/指派」問題：把元素逐個放進若干組、找最優的處理順序、兩兩配對。指數級的狀態數靠 bitmask 壓進一個整數。\n\n**核心模板**\n\n```cpp\n// dp[mask]：已處理集合為 mask 時的最優值\nfor (int mask = 1; mask < (1 << n); ++mask) {\n    int i = __builtin_ctz(mask);       // 常見技巧：固定處理 mask 裡最低位的元素\n    for (int j = 0; j < n; ++j) {\n        if (mask >> j & 1) {\n            dp[mask] = min(dp[mask], dp[mask ^ (1 << j)] + cost(j, mask));\n        }\n    }\n}\n```\n\n**思考重點**\n\n- 複雜度自查：`2^n * n` 約 10^6（n=16）沒問題；枚舉子集的 `3^n` 在 n=14 左右是極限\n- 「分成 k 組、每組代價只和組內集合有關」的題：先預處理每個子集的代價，再做子集枚舉 DP\n- 對稱性剪枝（第一個元素固定放第一組）常常是卡常與超時的分界\n\n**關鍵觀察的形態**\n\n這類題的觀察通常是「順序不重要，只有集合重要」——一旦說出這句話，狀壓就是唯一答案。",
        },
        {
          id: 21,
          title: "4.2 樹上思維：樹形 DP 與換根",
          summary:
            "**訊號**\n\n輸入是樹、問「每個子樹的統計量」「選點/選邊滿足約束的最優值」，或「以每個節點為根時的答案」（換根）。\n\n**核心框架**\n\n```cpp\n// 樹形 DP：後序遍歷，用孩子的結果合成自己的結果\nauto dfs = [&](this auto&& dfs, int u, int parent) -> long long {\n    long long res = value[u];\n    for (int v : g[u]) {\n        if (v != parent) {\n            res += max(0LL, dfs(v, u));  // 視題意合併孩子貢獻\n        }\n    }\n    return res;\n};\n```\n\n**思考重點**\n\n- 樹形 DP 的狀態常帶一個小模式維：`dp[u][選/不選]`、`dp[u][黑/白]`——本質是狀態機 DP 搬到樹上\n- 換根 DP 兩趟：第一趟後序算「向下」的答案，第二趟前序把「向上」的貢獻推給孩子。推導時把「父親的答案扣掉自己的貢獻」寫成明確的公式再開始寫程式\n- 樹上路徑問題常在「經過每個點的最高路徑」處合併兩個最大孩子鏈\n\n**關鍵觀察的形態**\n\n「答案可以按子樹分解」。如果某個量無法只靠孩子的資訊合成，考慮多存一個輔助量（最大值+次大值、含根/不含根兩種）。",
        },
        {
          id: 22,
          title: "4.3 數學、數論與組合計數",
          summary:
            "**訊號**\n\n「對 10^9+7 取模的方案數」「GCD/因數/質數」「第 k 個滿足條件的數」。這類 Q4 靠的是知識儲備而非現場推導。\n\n**必備武器**\n\n- 模運算全家桶：快速冪、模逆元（費馬小定理）、預處理階乘算組合數\n- 因數與質因數：`O(sqrt(n))` 分解、調和級數枚舉倍數（`n/1 + n/2 + ... ≈ n log n`）\n- GCD 沿子陣列延伸只會變小且最多 log 種取值——與 AND/OR 的性質同構\n\n```cpp\nlong long power(long long a, long long e, long long mod) {\n    long long r = 1;\n    for (a %= mod; e; e >>= 1, a = a * a % mod) {\n        if (e & 1) {\n            r = r * a % mod;\n        }\n    }\n    return r;\n}\n```\n\n**思考重點**\n\n- 計數題的兩板斧：正難則反（總數減不合法）、按「最後一步/最大元素/第一個不同位」分類\n- 「第 k 小」常轉成「≤ x 的有幾個」再二分 x——數學題和二分答案的高頻組合\n\n**常見翻車點**\n\n取模後相減沒加回 `mod`、階乘表開太小、隱含的整數溢位。",
        },
        {
          id: 23,
          title: "4.4 樹狀陣列、線段樹與 Trie",
          summary:
            "**訊號**\n\n「一邊修改一邊查詢」：單點改 + 區間查（樹狀陣列）、區間改或維護區間最值（線段樹）、字首匹配或最大異或（Trie）。逆序對型的「左邊有幾個比它大」也是樹狀陣列的招牌場景。\n\n**核心模板**\n\n```cpp\nstruct Fenwick {\n    int n;\n    vector<long long> t;\n    Fenwick(int n) : n(n), t(n + 1) {}\n    void add(int i, long long d) {\n        for (++i; i <= n; i += i & -i) {\n            t[i] += d;\n        }\n    }\n    long long sum(int i) {  // a[0..i] 之和\n        long long s = 0;\n        for (++i; i > 0; i -= i & -i) {\n            s += t[i];\n        }\n        return s;\n    }\n};\n```\n\n**思考重點**\n\n- 值域很大時先離散化（排序去重、座標壓縮），再把「值」當下標丟進樹狀陣列\n- 「按某維排序、另一維用資料結構查」是 2000+ 高頻雙件套：離線處理查詢，把二維問題降成一維\n- 線段樹在週賽出現時多半只需要「區間加 + 區間最值/和」的懶標記版本，賽前準備一份自己寫熟的模板\n\n**關鍵觀察的形態**\n\n「暴力是 O(n²) 的雙重迴圈，內層那個統計可以用資料結構維護」——先寫出暴力，再問內層在查什麼。",
        },
      ],
    },
    {
      id: 24,
      title: "5. 實戰關：節奏、罰時與復盤",
      summary:
        "技巧練齊了，最後一塊拼圖是「比賽本身」。同樣的實力，賽場執行的好壞可以差出幾百名：開場順序、卡題時的止損、提交前的自查、罰時的控制，都是可以刻意練習的技能。\n\n**賽場執行清單：**\n- 開場先把四題全部掃一遍標題和資料範圍（2 分鐘），對整場的難度分佈有預期\n- 卡題超過 15 分鐘就跳下一題，回頭再看時常常秒懂\n- 提交前 30 秒自查：邊界（空、單元素）、溢位、取模、輸出格式\n- WA 之後先重讀題目而不是重讀程式碼——一半的 WA 是理解錯題意\n- 最後 10 分鐘不開新題，回頭檢查手上做一半的\n\n**復盤模板（賽後 48 小時內完成）：**\n1. 每題實際用時 vs. 理想用時，差在哪個環節（讀題/想法/實作/除錯）\n2. 沒解出的題補完，寫下關鍵觀察與缺的訊號\n3. 每次 WA 歸因到一類（讀題錯/邊界/溢位/演算法錯），維護自己的翻車統計表",
      children: [
        {
          id: 25,
          title: "5.1 模擬實戰：三場 90 分鐘全真模擬",
          summary:
            "**怎麼用這一節**\n\n這裡有三場模擬賽，每場四題按真實週賽的難度曲線配置（Q1≈1200 → Q4≈2100+）。請完全按照真實比賽執行：\n\n- 連續 90 分鐘不中斷，不查資料、不看題解\n- 按 Q1 → Q4 順序開題，執行上一節的賽場清單\n- 記錄每題的開始/提交時間與每次 WA\n\n**評估基準（依目前水準對照）**\n\n- 目標「穩定三題」：Q1+Q2 合計 15 分鐘內、Q3 在 60 分鐘內拿下\n- 目標「衝擊 AK」：前三題 40 分鐘內收工，留 50 分鐘給 Q4\n- 任何一場罰時超過兩次，回到第一階段補速度與自查清單\n\n**賽後**\n\n每場都要完成完整復盤。三場打完後對照三份復盤：重複出現的失分環節，就是你下一個訓練週期的主題。",
        },
        {
          id: 26,
          title: "5.2 罰時陷阱：邊界與大數翻車點",
          summary:
            "**這一節在練什麼**\n\n這裡收錄的題共同點是：思路不難，但地雷密佈——邊界情況、整數溢位、細節模擬、「看起來是 A 其實是 B」的偽裝題。它們專門用來訓練「提交前找出自己 bug」的能力。\n\n**練法（與其他節相反）**\n\n- 寫完之後不要立刻提交。先自己構造三個刁鑽測資：最小輸入、全部相同、極端大值\n- 用測資跑過再交，目標是每題零 WA——這一節「一次通過率」比速度重要\n- 提交後如果還是 WA 了，把漏掉的情況補進自己的自查清單\n\n**高頻地雷清單**\n\n- 乘法溢位：`int * int` 存進 `long long` 之前就已經溢位了\n- 空集合/單元素：迴圈根本不執行時，初始值是不是答案？\n- 相等的元素：嚴格遞增還是非嚴格？排序穩不穩定有沒有影響？\n- 模擬題的終止條件：狀態會不會迴圈？要不要偵測循環節？\n- 讀題：「恰好」與「至少」、下標從 0 還是 1 開始",
        },
      ],
    },
  ],
} as TutorialData.Root;
