import type { TutorialData } from "@/types";

export const bitwiseOperations = {
  id: 1,
  title: "位元運算（基礎/性質/拆位/試填/恆等式/思維）",
  src: "https://leetcode.cn/circle/discuss/dHn9Vk/",
  last_update: "2026-02-24T14:03:25.797514+00:00",
  summary:
    "推薦先閱讀：[從集合論到位運算，常見位運算技巧分類總結！](https://leetcode.cn/circle/discuss/CaOJ45/)\n\n**本題單涵蓋**：\n- 基礎位運算操作與性質\n- 異或(XOR)、與或(AND/OR)運算應用\n- 拆位/貢獻法、試填法、恆等式\n- 線性基與思維題型\n\n:::example 常用位運算操作\n**常用位元運算技巧**：\n- `n & (n - 1)`: 移除最後一個 1\n- `n & -n`: 獲取最後一個 1 (lowbit)\n- `1 << i`: 第 i 位為 1\n- `n >> i & 1`: 獲取第 i 位的值\n\n```cpp\n// 常用位運算操作\n// 判斷奇偶：n & 1\n// 取第 i 位：(n >> i) & 1\n// 設第 i 位為 1：n | (1 << i)\n// 清除第 i 位：n & ~(1 << i)\n// 翻轉第 i 位：n ^ (1 << i)\n// 取最低位的 1：n & (-n)（lowbit）\n// 清除最低位的 1：n & (n - 1)\n// 列舉子集：for (int s = m; s > 0; s = (s - 1) & m)\n```\n:::\n",
  children: [
    {
      id: 2,
      title: "1. 基礎題",
      summary:
        "**觀念起點**\n\n位元題的優勢在於每個 bit 通常可獨立討論。對 XOR、OR、AND 這類運算，先判斷單一 bit 在何情況下變成 1，再把每位的貢獻乘回權重。\n\nXOR 某位為 1，代表該位上 1 的數量為奇數，或兩數在該位相異；OR 某位為 1，只要至少一個候選提供該位；AND 則要求所有相關元素皆提供該位。\n\n高位貪心通常自高往低試填，因為高位價值大於所有低位總和。只要能檢查當前高位前綴是否可行，便能逐步固定答案。\n\n枚舉一個遮罩的所有子集可用 `for (int s = m; s; s = (s - 1) & m)`，所有遮罩合計 `O(3^n)`；`bitset` 能把布林轉移壓到 `O(n^2/64)`；按位獨立的計數常與前綴和或 SOS DP 結合。\n\n---\n\n**代表例題：LeetCode 2680 最大或值**\n\n**完整問題**：LeetCode 2680 給定陣列 `nums` 與整數 `k`。可選一個元素將其乘上 `2^k`，最大化整個陣列的 bitwise OR。\n\n本節以 LeetCode 2680「最大或值」 為主例題（rating 1912），並將同一套不變式延伸至本節其他題目。\n\n**題目訊號**\n\n- OR 的每個 bit 只需某個數提供即可。\n- 操作僅能集中於一個元素。\n- 需取得某位置以外所有元素的 OR。\n\n**狀態、不變式與答案更新**\n\n- `prefix_or[i]` 為 i 左側所有元素的 OR。\n- `suffix_or[i]` 為 i 右側所有元素的 OR。\n- 選擇對 i 操作後，候選答案為 `prefix | (nums[i] << k) | suffix`。\n\n**從零建模**\n\n1. 先建立前綴 OR 與後綴 OR。\n2. 枚舉被乘上 `2^k` 的元素 i。\n3. 合併 i 左側 OR、操作後的 nums[i] 與 i 右側 OR。\n4. 取最大值。\n\n**小範例手動模擬**\n\n以 `nums = [1, 2]` 求所有 pair 的 XOR 和：第 0 位上 1 有、2 無 → ones = 1, zeros = 1，貢獻 1 * 1 * 1 = 1；第 1 位上 2 有、1 無 → 1 * 1 * 2 = 2。總和 3（即 1^2）。\n\n**常見模式**\n\n- 拆位 OR 貢獻\n- 前後綴分解\n- 枚舉唯一操作位置\n\n**常見錯誤**\n\n- 左移後可能超過 int，須使用 `long long`。\n- 操作不可分散至多個元素。\n\n**複雜度**：時間 `O(n)`，空間 `O(n)`；亦可採 suffix 壓縮。 逐位掃描，時間 `O(n * B)`（B 為位數）、空間 `O(1)`。\n\n---\n\n**本節題單中的延伸題**\n\n- LeetCode 191 位 1 的個數\n- LeetCode 231 2 的冪\n- LeetCode 338 位元位計數\n- LeetCode 342 4 的冪\n- LeetCode 461 漢明距離\n\n---\n\n**同一模板的套用要點**\n\n把每位的 0/1 計數套到 OR/AND 要換公式（OR：至少一個有；AND：全部都有）；求最大 XOR 用高位試填或線性基。\n\n---\n\n**C++ 實作骨架**\n\n```cpp\nclass Solution {\npublic:\n    long long maximumOr(vector<int>& nums, int k) {\n            int n = nums.size();\n            vector<long long> prefix(n + 1, 0), suffix(n + 1, 0);\n            for (int i = 0; i < n; ++i) {\n                    prefix[i + 1] = prefix[i] | nums[i];\n            }\n            for (int i = n - 1; i >= 0; --i) {\n                    suffix[i] = suffix[i + 1] | nums[i];\n            }\n\n            long long answer = 0;\n            for (int i = 0; i < n; ++i) {\n                    answer = max(answer, prefix[i] | (1LL * nums[i] << k) | suffix[i + 1]);\n            }\n            return answer;\n    }\n};\n```",
      children: [
        {
          id: 16,
          title: "1.1 基礎題",
          summary:
            "推薦先閱讀：[從集合論到位運算，常見位運算技巧分類總結！](https://leetcode.cn/circle/discuss/CaOJ45/)\n\n---\n\n基礎位運算操作與性質\n\n:::example 位運算基礎模板\n**常用操作**：\n- 判斷奇偶：`n & 1`\n- 取第 i 位：`(n >> i) & 1`\n- 設第 i 位為 1：`n | (1 << i)`\n- 設第 i 位為 0：`n & ~(1 << i)`\n\n```cpp\n// 位運算基礎模板\n// 計算二進位中 1 的個數\nint popcount(int n) {\n    int cnt = 0;\n    while (n) {\n        n &= n - 1;  // 清除最低位的 1\n        ++cnt;\n    }\n    return cnt;\n}\n// 也可使用 __builtin_popcount(n)\n```\n:::\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 191 | [位 1 的個數](https://leetcode.cn/problems/number-of-1-bits) |  |\n| 231 | [的冪](https://leetcode.cn/problems/power-of-two) |  |\n| 338 | [位元位計數](https://leetcode.cn/problems/counting-bits) |  |\n| 342 | [的冪](https://leetcode.cn/problems/power-of-four) |  |\n| 461 | [漢明距離](https://leetcode.cn/problems/hamming-distance) |  |\n| 476 | [數字的補數](https://leetcode.cn/problems/number-complement) |  |\n| 面試題 05.01 | [插入](https://leetcode.cn/problems/insert-into-bits-lcci) |  |\n| 693 | [交替位二進位制數](https://leetcode.cn/problems/binary-number-with-alternating-bits) |  |\n| 868 | [二進位制間距](https://leetcode.cn/problems/binary-gap) | 1307 |\n| 1009 | [十進位制整數的反碼](https://leetcode.cn/problems/complement-of-base-10-integer) | 1235 |\n| 1342 | [將數字變成 0 的操作次數](https://leetcode.cn/problems/number-of-steps-to-reduce-a-number-to-zero) | 1164 |\n| 1356 | [根據數字二進位制下 1 的數目排序](https://leetcode.cn/problems/sort-integers-by-the-number-of-1-bits) | 1258 |\n| 2154 | [將找到的值乘以 2](https://leetcode.cn/problems/keep-multiplying-found-values-by-two) | 1236 |\n| 2220 | [轉換數字的最少位翻轉次數](https://leetcode.cn/problems/minimum-bit-flips-to-convert-number) | 1282 |\n| 2595 | [奇偶位數](https://leetcode.cn/problems/number-of-even-and-odd-bits) | 1207 |\n| 2657 | [找到兩個陣列的字首公共陣列](https://leetcode.cn/problems/find-the-prefix-common-array-of-two-arrays) | 1304 |\n| 2917 | [找出陣列中的 K-or 值](https://leetcode.cn/problems/find-the-k-or-of-an-array) | 1389 |\n| 3211 | [生成不含相鄰零的二進位制字串](https://leetcode.cn/problems/generate-binary-strings-without-adjacent-zeros) | 1353 |\n| 3226 | [使兩個整數相等的位更改次數](https://leetcode.cn/problems/number-of-bit-changes-to-make-two-integers-equal) | 1247 |\n| 3370 | [僅含置位位的最小整數](https://leetcode.cn/problems/smallest-number-with-all-set-bits) | 1199 |\n| 3827 | [統計單位元整數](https://leetcode.cn/problems/count-monobit-integers) | 1191 |",
        },
        {
          id: 3,
          title: "1.2 用位運算代替陣列操作",
          summary:
            "## 這個技術解決什麼問題\n\n一組「有／沒有」的旗標，本來要開 `bool` 陣列，每次聯集、交集、比較都得掃一遍 `O(n)`。\n\n當旗標總數不超過整數寬度（32 或 64）時，可以把整個陣列**壓進一個整數**——第 `i` 位代表第 `i` 個旗標。於是「對陣列每格做一樣的事」變成「對整數所有位元一次做完」，每次操作從 `O(n)` 降到 `O(1)`。\n\n26 個英文字母是否出現過、`n <= 20` 的子集列舉、狀態壓縮 DP 的狀態，都是這個技巧的直接應用。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「用位運算代替陣列操作」想：\n\n- **資料是一組「有 / 沒有」的旗標，且總數很小（≤ 32 或 ≤ 64）。** 例如 26 個英文字母是否出現過，正好塞進一個整數。\n- **你需要對整組旗標做聯集、交集、差集，或快速比較兩組是否相同。** 這些用 `| & ^` 一步到位，例如 3690「拆分合並陣列」就需要把整體條件拆成位元逐位判斷或合併。\n- **暴力做法是開一個 `bool` 陣列，每次操作都跑迴圈掃一遍；** 改成 mask 後每次操作降為 `O(1)`。\n\n:::example 嗅到味道的瞬間：你正想開一個小布林陣列又反覆整批處理它\n```cpp\n// 嗅到味道的瞬間：你正想開一個小布林陣列又反覆整批處理它\n// bool used[26];  for (...) used[i] = ...;   // 可以濃縮成一個 int mask\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的「第 `i` 位」代表什麼意思？\n2. 加入 / 移除 / 查詢分別對應哪個位運算？\n3. 元素總數有沒有超過整數寬度（32 / 64）？超過就不該用 mask。\n\n## 核心想法與直覺\n\n本節要解決的場景是：一堆「存在 / 不存在」的旗標，本來你會用一個 `bool` 陣列來記，但其實可以把整個陣列「壓進一個整數」，用位運算一次處理。\n\n:::example 用一個整數當作 32 格的「布林陣列」\n先看一個整數的本質：一個 `int` 有 32 個位元（bit），每個位元只能是 0 或 1。如果我們約定「第 `i` 位是 1」代表「第 `i` 個東西存在」，那麼一個整數就等於一個長度 32 的布林陣列。\n\n```cpp\n// 用一個整數當作 32 格的「布林陣列」\nint mask = 0;                 // 全 0，代表都不存在\nmask |= (1 << 3);             // 把第 3 格設成 1（加入元素 3）\nbool has3 = mask & (1 << 3);  // 查詢第 3 格是不是 1\nmask &= ~(1 << 3);            // 把第 3 格清成 0（移除元素 3）\n```\n:::\n\n\n\n:::example 集合運算濃縮成單一位運算\n這樣做的好處是：原本要跑迴圈才能完成的「整批操作」，現在一個位運算就解決。例如「兩個集合的聯集」用一次 `|`、「交集」用一次 `&`、「對稱差」用一次 `^`，全都是 `O(1)`。\n\n```cpp\n// 集合運算濃縮成單一位運算\nint uni = a | b;   // 聯集：任一邊有就算有\nint both = a & b;  // 交集：兩邊都有才算有\nint diff = a ^ b;  // 對稱差：只有一邊有\n```\n:::\n\n\n\n關鍵觀念：把「對陣列每格做一樣的事」翻譯成「對整數所有位元一次做完」，這正是用位運算代替陣列操作的核心。\n\n## 狀態／資料結構定義\n\n**mask**：一個整數，第 `i` 位為 1 表示「第 `i` 個元素在集合裡」。\n\n動手前先寫下一句話：**「第 `i` 位代表什麼」**。這句沒寫清楚，後面所有位運算都會對不上。\n\n常用操作對照：\n\n| 集合操作 | 位運算 |\n| --- | --- |\n| 加入元素 `i` | `mask \\|= 1 << i` |\n| 移除元素 `i` | `mask &= ~(1 << i)` |\n| 翻轉元素 `i` | `mask ^= 1 << i` |\n| 查詢元素 `i` | `(mask >> i) & 1` |\n| 聯集 | `a \\| b` |\n| 交集 | `a & b` |\n| 差集 `a - b` | `a & ~b` |\n| 元素個數 | `__builtin_popcount(mask)` |\n| 取最低位的 1 | `mask & -mask` |\n| 清掉最低位的 1 | `mask & (mask - 1)` |\n\n寬度超過 31 位時一律寫 `1LL << i` 並用 `long long`。\n\n## 不變量或正確性證明\n\n**表示不變量**：`mask` 的第 `i` 位為 1 ⟺ 元素 `i` 在集合中。所有操作都必須維持這個對應。\n\n**為什麼位運算等價於集合運算**：C++ 的 `& | ^ ~` 都是**逐位獨立**的——結果的第 `i` 位只由兩個運算元的第 `i` 位決定。把每一位看成一個獨立的布林變數，`&` 就是邏輯 AND、`|` 就是 OR、`^` 就是 XOR。因此對整數做一次 `a & b`，等價於對 32 個布林變數同時做 AND，這正是「一次做完整批操作」的來源。\n\n**`mask & -mask` 取出最低位 1 的證明**：補數表示下 `-mask = ~mask + 1`。設 `mask` 的最低位 1 在第 `k` 位，則 `mask` 低於 `k` 的位都是 0。`~mask` 把它們全變成 1、第 `k` 位變成 0；加 1 後低 `k` 位進位歸零、第 `k` 位變回 1，而高於 `k` 的位仍是 `~mask` 的內容（與 `mask` 逐位相反）。因此 `mask & -mask` 只有第 `k` 位存活。\n\n**`mask & (mask - 1)` 清掉最低位 1**：`mask - 1` 把第 `k` 位借位變 0、低 `k` 位全變 1、高位不動。與 `mask` 相 AND 後，第 `k` 位歸零、低位本來就是 0、高位保持——恰好清掉最低位的 1。這也是計算 popcount 的迴圈只跑「1 的個數」次的原因。\n\n## 逐步演算法\n\n:::example 第 i 位 = 元素 i 是否在集合中（i 從 0 算起）\n**1. 設計位元的意義。** 先講清楚「第 `i` 位代表什麼」。通常 `i` 對應某個編號（0~31）或某個狀態。\n\n```cpp\n// 第 i 位 = 元素 i 是否在集合中（i 從 0 算起）\nint set = 0;\n```\n:::\n\n\n\n:::example 加入 / 移除 / 查詢第 i 位\n**2. 三個基本動作：加入、移除、查詢。** 用 `|`、`& ~`、`&` 各對應一個。\n\n```cpp\n// 加入 / 移除 / 查詢第 i 位\nset |= (1 << i);           // 加入\nset &= ~(1 << i);          // 移除\nbool in = (set >> i) & 1;  // 查詢，結果是 0 或 1\n```\n:::\n\n\n\n:::example 用 XOR 找出差異，用 popcount 數 1 的個數\n**3. 整批比較與統計。** 想知道兩個狀態哪些位不同，用 `^`；想數有幾個 1（幾個元素），用內建函式。\n\n```cpp\n// 用 XOR 找出差異，用 popcount 數 1 的個數\nint changed = a ^ b;                  // changed 的 1 = a,b 不同的位\nint count = __builtin_popcount(set);  // set 裡有幾個 1\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- `1 << i` 在 `i >= 31` 時可能溢位，需要 64 位時改寫 `1LL << i` 並用 `long long`。\n- 用 mask 取代陣列只在元素數量小（約 ≤ 32 或 ≤ 64）時划算；超過就回頭用真正的陣列。\n- 每個位運算都是 `O(1)`，把原本 `O(n)` 的整批操作壓成常數，是這個技巧最大的價值。\n\n## C++17 模板\n\n以下用同型延伸示範題 1310. 子陣列異或查詢 展示核心寫法。\n\n**完整題目**：給定陣列與多個 `[l, r]` 查詢，回傳每段子陣列的 XOR 值。\n\n:::example 前綴 XOR：利用 pre[r+1] ^ pre[l] 求區間 [l,r] 的 XOR…\n**線索**：題目出現 XOR/AND/OR、mask 或二進位；先判斷每一位是否能獨立統計或從高位貪心。\n\n```cpp\nclass Solution {\npublic:\n    // 前綴 XOR：利用 pre[r+1] ^ pre[l] 求區間 [l,r] 的 XOR 值\n    vector<int> xorQueries(vector<int>& arr, vector<vector<int>>& queries) {\n            vector<int> pre(arr.size() + 1);  // pre[i] 為 arr[0..i-1] 的 XOR 前綴\n            for (int i = 0; i < (int)arr.size(); ++i) {\n                    pre[i + 1] = pre[i] ^ arr[i];\n            }\n            vector<int> ans;\n            for (auto& q : queries) {\n                    ans.push_back(pre[q[1] + 1] ^ pre[q[0]]);  // 區間 [q[0], q[1]] 的 XOR\n            }\n            return ans;\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- XOR 具有自反性 `x ^ x = 0`，因此區間 XOR 可用前綴 XOR 相消。\n- 程式把整數轉成位元狀態或前綴 XOR，利用位元運算的可逆性/獨立性更新答案。\n- 涉及高位貪心時，迴圈通常從最高位往低位試，因為高位決定主要大小關係。\n\n## 時間與空間複雜度\n\n**每個位運算 `O(1)`**，整批操作從 `O(n)` 壓成常數——這就是本技巧的全部價值。\n\n**空間 `O(1)`**：一個整數取代一條 `bool` 陣列。\n\n**popcount 的成本**：`__builtin_popcount` 在支援 SSE4.2 的機器上編譯成單一 `POPCNT` 指令，是 `O(1)`；手寫的 `while (mask) { mask &= mask - 1; ++cnt; }` 是 `O(popcount)`，最壞 `O(32)`。兩者都遠快過掃陣列。\n\n**適用上限**：元素數超過 64 就沒有現成的整數型別可用。此時要嘛改回真正的陣列，要嘛用 `std::bitset<N>`（它把同樣的技巧推廣到任意寬度，且 `&`、`|` 仍是逐字組批次處理，常數約為 `N / 64`）。\n\n**子集列舉**：`for (int s = mask; s; s = (s - 1) & mask)` 走遍 `mask` 的所有非空子集，總成本對所有 `mask` 加總是 `O(3^n)` 而非 `O(4^n)`——這是狀壓 DP 的關鍵事實。\n\n## 常見錯誤與邊界條件\n\n- 位移用 `1LL << b` 避免溢位。\n- 負數右移與符號位要小心。\n- 按位貢獻要確認每位獨立。\n- `__builtin_popcountll` 對 long long 使用 ll 版本。\n\n## 常見變形\n\n- 前綴 XOR 處理區間異或。\n- 試填最高位處理最大 XOR/最大值。\n- SOS DP 處理子集包含關係。\n\n## 與相似技巧的比較\n\n- **對比 `bool` 陣列 / `vector<bool>`**：`vector<bool>` 其實也做了位壓縮，但沒有一次做整批 `&`、`|` 的介面，聯集交集仍要迴圈。要做集合運算就用整數 mask。\n- **對比 `std::bitset<N>`**：元素數固定且超過 64 時的正解。介面比手寫 mask 清楚（`count()`、`any()`、`test()`），效能同樣是逐字組批次。缺點是大小必須編譯期決定，且不能當 `unordered_map` 的鍵。\n- **對比 `std::set` / `unordered_set`**：支援任意元素、動態大小，但單次操作是 `O(log n)` 或 `O(1)` 帶大常數，且無法一次做聯集。元素數小又要頻繁做集合運算時，mask 快上一兩個數量級。\n- **對比雜湊**：把 mask 當作「狀態的唯一編碼」直接拿來當陣列下標或 map 的鍵，比自訂雜湊函式更快也更不易出錯——這正是狀態壓縮 DP 的做法。\n- **對比排序後比較**：判斷兩組字母是否相同（字母異位詞），排序是 `O(n log n)`，mask 只能判斷「出現過的字母集合相同」而不管次數；要管次數就得用計數陣列。選型時要分清楚是「集合」還是「多重集合」。\n\n## 代表例題\n\n**題號**：3690\n\n要拆成位元看；練習判斷 XOR/AND/OR 的每一位能否獨立統計或貪心決策。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 3690 | [拆分合並陣列](https://leetcode.cn/problems/split-and-merge-array-transformation) | 1982 |\n\n## 本節重點速查\n\n- 旗標數 `<= 32/64` 時，把 `bool` 陣列壓成一個整數：加入 `mask |= 1 << i`、移除 `mask &= ~(1 << i)`、查詢 `(mask >> i) & 1`、聯集 `|`、交集 `&`、差集 `a & ~b`。\n- **動手前先寫下「第 `i` 位代表什麼」**。\n- 位運算逐位獨立，所以一次 `a & b` 等於同時對 32 個布林變數做 AND——這就是把 `O(n)` 壓成 `O(1)` 的原理。\n- 慣用招式：`mask & -mask` 取最低位的 1、`mask & (mask - 1)` 清掉它、`(s - 1) & mask` 列舉子集。\n- `1 << i` 在 `i >= 31` 會溢位，改 `1LL << i`。\n- 超過 64 個元素改用 `std::bitset`。\n",
        },
        {
          id: 4,
          title: "1.3 反轉二進位制數",
          summary:
            "## 這個技術解決什麼問題\n\n把一個整數的二進位表示左右翻轉——第 0 位跑到最高位，第 1 位跑到次高位，依此類推。\n\n直觀做法是轉成字串、反轉、再轉回整數，但那要配置記憶體、還得處理前導零的補齊。逐位搬家用純位運算完成，沒有額外配置，也不會有字串長度不足 32 位的問題。\n\n更值得帶走的是背後的模式：**看到鏡像、回文、反射這種「第 `i` 個對應到第 `n - 1 - i` 個」的關係，就想到逐位搬家**。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「反轉二進位制數」想：\n\n- **題目明說要把某個數 / 字串的位元或字元「顛倒順序」**，如 190「顛倒二進位制位」、3769「二進位制反射排序」。\n- **答案和「第 `i` 個位置對應到第 `n - 1 - i` 個位置」這種對稱關係有關。** 一旦看到鏡像、回文、反射，就想到逐位搬家。\n- **暴力可能是把數字轉成字串再反轉再轉回去；** 直接用位運算逐位搬更乾淨，也不會有轉字串的額外成本。\n\n:::example 嗅到味道的瞬間：你發現「第 i 位要去第 n - 1 - i 位」\n```cpp\n// 嗅到味道的瞬間：你發現「第 i 位要去第 n - 1 - i 位」\n// res |= ((n >> i) & 1) << (n - 1 - i);\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我翻轉的單位是「位元」還是「字元」？\n2. 總寬度是多少？鏡像位置的公式（`寬度 - 1 - i`）對嗎？\n3. 有沒有用 unsigned 避免右移補符號位的坑？\n\n## 核心想法與直覺\n\n本節要解決的場景是：把一個整數的二進位表示「左右翻轉」。例如 8 位數 `0000 1011` 翻轉後變成 `1101 0000`。原本第 0 位的內容跑到最高位，第 1 位跑到次高位，依此類推。\n\n:::example 逐位翻轉：把第 i 位搬到第 31-i 位\n核心想法很單純：**逐位搬家**。我們一位一位地讀出原數的每個位元，再把它放到「鏡像位置」。對 32 位整數來說，第 `i` 位的鏡像位置是第 `31 - i` 位。\n\n```cpp\n// 逐位翻轉：把第 i 位搬到第 31-i 位\nuint32_t reverseBits(uint32_t n) {\n    uint32_t res = 0;\n    for (int i = 0; i < 32; ++i) {\n        res |= ((n >> i) & 1) << (31 - i);  // 取第 i 位，放到鏡像處\n    }\n    return res;\n}\n```\n:::\n\n\n\n`(n >> i) & 1` 是「把第 `i` 位移到最低位再取出來」，得到 0 或 1；再 `<< (31 - i)` 把它送到對稱的位置，用 `|` 累積到答案。這就是 190「顛倒二進位制位」的標準解。\n\n:::example 字串層級的反轉：對稱位置互換\n要特別注意「位元」和「字元」的差別：有些題目翻轉的不是整數的位元，而是字串裡的字元順序（如 3750、3769），但思路一樣——關注「第幾個位置」和「它翻轉後該去哪」。\n\n```cpp\n// 字串層級的反轉：對稱位置互換\nfor (int l = 0, r = s.size() - 1; l < r; l++, r--) {\n    swap(s[l], s[r]);  // 第 l 個和第 r 個交換\n}\n```\n:::\n\n## 狀態／資料結構定義\n\n**輸入**：一律用 `uint32_t`（或 `unsigned`）。用有號 `int` 的話，`n >> i` 會做算術右移、高位補符號位，負數會全盤錯掉。\n\n**寬度**：先確定是幾位。32 位整數的鏡像公式是 `31 - i`，8 位是 `7 - i`。把寬度寫死錯是本節最常見的錯。\n\n**逐位搬家**：`(n >> i) & 1` 取出第 `i` 位（0 或 1），`<< (31 - i)` 送到對稱位置，用 `|=` 累積到答案。\n\n**分治版本**（`O(log 32)`）：先兩兩交換相鄰位、再交換相鄰的 2 位一組、4 位一組、8 位、16 位。每一步用一對遮罩，例如交換奇偶位是 `n = ((n & 0xaaaaaaaa) >> 1) | ((n & 0x55555555) << 1)`。\n\n**字元反轉**則完全是另一回事，用 `std::reverse` 或雙指標 `swap`，別套位運算。\n\n## 不變量或正確性證明\n\n**逐位法的不變量**：處理完前 `k` 位（`i = 0..k-1`）後，答案的最高 `k` 位已經是輸入最低 `k` 位的鏡像，其餘位仍為 0。\n\n每一輪把輸入第 `i` 位放到答案第 `31 - i` 位。因為 `i` 由 0 遞增到 31，`31 - i` 由 31 遞減到 0，兩者是一對一的雙射——每個目標位恰好被寫入一次，不會覆蓋也不會遺漏。跑滿 32 輪後，答案第 `j` 位等於輸入第 `31 - j` 位，正是定義上的翻轉。\n\n**`|=` 為何安全**：答案的第 `31 - i` 位在寫入前必定是 0（由上述雙射，還沒被碰過），所以用 `|` 累積等價於直接賦值。\n\n**分治法的正確性**：把 32 位看成一棵二分樹。交換相鄰 1 位、再交換相鄰 2 位塊、4 位塊……每一層把左右子塊互換。歸納可證：做完前 `t` 層後，每個 `2^t` 大小的區塊內部已完成翻轉。做完 5 層（`2^5 = 32`）整個字組就翻轉完畢。\n\n**為什麼必須用 unsigned**：有號右移在 C++ 中對負數是實作定義（實務上是算術右移，高位補 1）。`(n >> i) & 1` 雖然有 `& 1` 保護不會出錯，但若寫成 `n >>= 1` 的迴圈形式，負數會永遠不歸零而無限迴圈。\n\n## 逐步演算法\n\n**1. 確認翻轉的單位。** 先問清楚是「整數的二進位位元」還是「字串字元」。位元用位運算搬，字元用 `swap`。\n\n:::example 取第 i 位，送到鏡像位置 (width - 1 - i)\n**2. 逐位搬到鏡像位置。** 整數版本的核心就是上面的迴圈：取第 `i` 位、放到第 `n - 1 - i` 位。\n\n```cpp\n// 取第 i 位，送到鏡像位置 (width - 1 - i)\nint bit = (n >> i) & 1;\nres |= bit << (width - 1 - i);\n```\n:::\n\n\n\n:::example 一次交換相鄰兩位（分治翻轉的一步）\n**3. 想加速可用分治翻轉。** 不必逐位，可以先交換相鄰 1 位、再相鄰 2 位、4 位……以 `O(log 寬度)` 完成（進階寫法）。\n\n```cpp\n// 一次交換相鄰兩位（分治翻轉的一步）\nn = ((n & 0x55555555) << 1) | ((n & 0xAAAAAAAA) >> 1);\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 用 `uint32_t`（unsigned）避免右移時補符號位出錯。\n- 確定「寬度」是多少：32 位整數用 31，8 位用 7，別把寬度寫死錯。\n- 逐位法 `O(寬度)`，這裡是 `O(32)` 即常數；分治法 `O(log 32)`。\n\n## C++17 模板\n\n以 190. 顛倒二進位制位 為例。\n\n**完整題目**：顛倒給定的 32 位無號整數的二進位位。\n\n```cpp\n// 190. 顛倒二進位制位：逐位搬家，O(32)\nclass Solution {\npublic:\n    uint32_t reverseBits(uint32_t n) {\n        uint32_t ans = 0;\n        for (int i = 0; i < 32; ++i) {\n            // 取出第 i 位，送到對稱位置 31 - i\n            ans |= ((n >> i) & 1u) << (31 - i);\n        }\n        return ans;\n    }\n};\n```\n\n若同一支程式要呼叫很多次（題目的 follow-up），改用分治版本，`O(log 32) = 5` 步：\n\n```cpp\n// 190 follow-up：分治交換，5 步完成\nuint32_t reverseBitsFast(uint32_t n) {\n    n = ((n & 0xaaaaaaaau) >> 1) | ((n & 0x55555555u) << 1);   // 交換相鄰 1 位\n    n = ((n & 0xccccccccu) >> 2) | ((n & 0x33333333u) << 2);   // 交換相鄰 2 位\n    n = ((n & 0xf0f0f0f0u) >> 4) | ((n & 0x0f0f0f0fu) << 4);   // 交換相鄰 4 位\n    n = ((n & 0xff00ff00u) >> 8) | ((n & 0x00ff00ffu) << 8);   // 交換相鄰 8 位\n    return (n >> 16) | (n << 16);                              // 交換高低 16 位\n}\n```\n\n## 時間與空間複雜度\n\n**逐位法**：`O(寬度)`，32 位就是 32 次迭代——固定次數，實務上視為 `O(1)`。空間 `O(1)`。\n\n**分治法**：`O(log 寬度)` = 5 步，每步兩次遮罩、兩次位移、一次 `|`。常數比逐位法小約 6 倍，且完全沒有分支，是「呼叫很多次」時的正解。空間 `O(1)`。\n\n**對比字串法**：轉字串 `O(32)`、反轉 `O(32)`、轉回 `O(32)`，漸進相同但要動態配置記憶體，實測慢一個數量級，而且要自己補足 32 位的前導零（`bitset<32>(n).to_string()` 可以省掉這步）。\n\n**快取表法**：預先建一張 256 項的位元組翻轉表，每次查 4 次表再拼起來，`O(4)`。適合極高頻呼叫，代價是 256 位元組的表。\n\n## 常見錯誤與邊界條件\n\n- **用有號 `int` 而非 `unsigned`**。右移補符號位，負數（最高位為 1）會出錯；寫成 `while (n) n >>= 1` 更會無限迴圈。一律 `uint32_t`。\n- **寬度寫錯**。32 位用 `31 - i`，8 位用 `7 - i`。從別的題目複製程式時最容易踩到。\n- **`1 << (31 - i)` 溢位**。`1` 是 `int`，`1 << 31` 在有號型別上是未定義行為。寫成 `1u <<` 或讓左運算元本身是 `uint32_t`（如模板中的 `((n >> i) & 1u) << (31 - i)`）。\n- **忘記 `& 1`**。`(n >> i)` 的高位還帶著原本的內容，不遮罩就會把一堆位一起搬過去。\n- **分治法的遮罩打錯一個字元**。`0xaaaaaaaa` 與 `0x55555555` 是互補的（1010… 與 0101…），每一對都要互補。抄錯時症狀是某些輸入對、某些錯，很難查——寫完用 `n = 1` 與 `n = 0x80000000` 各驗一次。\n- **把「翻轉位元」和「翻轉字元」混為一談**。3769 這類題翻的是字元，用 `std::reverse`，別套位運算。\n- 邊界測資：`0`、`1`、`0xffffffff`、`0x80000000` 各驗一次。\n\n## 與相似技巧的比較\n\n- **對比 `std::bitset<32>`**：`bitset` 沒有現成的 `reverse`，仍要自己搬；但 `bitset<32>(n).to_string()` 很適合除錯時把位元印出來看。\n- **對比 `__builtin_bitreverse32`**（Clang 內建）：某些編譯器直接提供，會編成單一指令。可攜性差，競賽環境不保證有。\n- **對比 `std::reverse` 反轉字串**：語意不同——字串反轉的單位是字元，位元反轉的單位是位。看到「二進位制反射」要先確認翻的是哪一種。\n- **對比 `__builtin_popcount` 等其他位技巧**：同屬「逐位處理」家族。popcount 只關心 1 的個數（與位置無關），本節關心的恰恰是位置的鏡像對應。\n- **對比 191 位元 1 的個數**：兩題常一起出現，都是 `O(寬度)` 的逐位掃描；191 可以用 `n & (n - 1)` 只跑 popcount 次，本節則必須跑滿寬度（每一位都要搬，包括 0）。\n\n## 代表例題\n\n**題號**：190、3750、3769\n\n要拆成位元看；練習判斷 XOR/AND/OR 的每一位能否獨立統計或貪心決策。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 190 | [顛倒二進位制位](https://leetcode.cn/problems/reverse-bits) |  |\n| 3750 | [最少反轉次數得到翻轉二進位制字串](https://leetcode.cn/problems/minimum-number-of-flips-to-reverse-binary-string) | 1289 |\n| 3769 | [二進位制反射排序](https://leetcode.cn/problems/sort-integers-by-binary-reflection) | 1364 |\n\n## 本節重點速查\n\n- 把整數的二進位左右翻轉：`ans |= ((n >> i) & 1u) << (31 - i)`，跑滿 32 輪。\n- 因為 `i` 與 `31 - i` 是雙射，每個目標位恰被寫一次，用 `|=` 累積是安全的。\n- **一律用 `uint32_t`**——有號右移補符號位，負數會錯。\n- 寬度別寫死錯（32 位用 31、8 位用 7）。\n- `1 << 31` 在有號型別上是未定義行為，寫 `1u <<`。\n- 高頻呼叫改用分治版（5 步交換 1/2/4/8/16 位塊，遮罩必須成對互補）。\n- 看到「鏡像、回文、反射」的位置對應關係就想到逐位搬家； 但翻的若是**字元**就用 `std::reverse`，別套位運算。\n",
        },
      ],
    },
    {
      id: 5,
      title: "2. 異或（XOR）的性質",
      summary:
        "## 這個技術解決什麼問題\n\nXOR 有三條性質，讓一整類「配對抵消」「區間查詢」「編碼還原」的問題塌縮成線性甚至常數：\n\n1. **自反性** `a ^ a = 0`、**單位元** `a ^ 0 = a`：出現偶數次的數會互相消掉，只留下奇數次的。「找落單的那一個」直接一路 XOR 到底。\n2. **可結合、可交換**：順序無所謂，所以能建**前綴異或**，把區間查詢降到 `O(1)`。\n3. **自身為逆元**：`a ^ b = c` ⟹ `a = b ^ c`。給定「相鄰兩數異或後的編碼」可以直接反推原陣列。\n\n看到 XOR 就先問：我要用的是哪一條。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「異或的性質」想：\n\n- **題目直接出現「異或 / XOR」字樣**，或要你對一段數字做連續異或（1486「陣列異或操作」）。\n- **元素成對出現、只有一個落單**，或要找「出現奇數次」的東西——成對抵消最拿手。\n- **需要反覆查詢「某區間的異或值」**——建前綴異或，查詢降為 `O(1)`（1310）。\n- **題目給的是「相鄰兩數異或後」的編碼，要你還原原陣列**——用自反性反推（1720、1734）。\n\n:::example 嗅到味道的瞬間：你看到「a ^ b = c」想反推 a\n```cpp\n// 嗅到味道的瞬間：你看到「a ^ b = c」想反推 a\n// a = b ^ c;   // XOR 自己就是自己的反運算\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要用的是哪條性質？成對抵消、前綴異或、還是自反反推？\n2. 如果用前綴異或，`pre[0]` 有沒有設成 0、長度有沒有 `n + 1`？\n3. 題目的「重複」是偶數次（會被消掉）還是奇數次（會留下）？\n\n## 核心想法與直覺\n\n本節要解決的場景是：題目裡出現「異或」（XOR，符號 `^`）運算，而我們要利用它幾個特別的性質把問題化簡。\n\n:::example XOR 的核心性質\nXOR 的規則只有一句：**兩個位元相同得 0，不同得 1**。從這條規則可以推出幾個非常好用的性質：\n\n```cpp\n// XOR 的核心性質\nint a = 6;\nint x1 = a ^ 0;  // = a    ：和 0 異或不變\nint x2 = a ^ a;  // = 0    ：和自己異或變 0\n                                  // 還有「可交換、可結合」：a ^ b ^ a == b（成對抵消）\n```\n:::\n\n\n\n最關鍵的是 **「成對抵消」**：如果一串數字做連續 XOR，出現偶數次的數會互相消掉、變成 0，只剩下出現奇數次的。這讓 XOR 很適合「找落單的那一個」或「抵消重複」。\n\n:::example 前綴異或：O(1) 算出任一區間的 XOR\n另一個常用招式是 **前綴異或**。定義 `pre[i] = a[0] ^ a[1] ^ ... ^ a[i - 1]`，那麼任一段 `a[l..r]` 的異或值就是 `pre[r + 1] ^ pre[l]`——因為中間 `a[0..l - 1]` 被算了兩次，剛好抵消。\n\n```cpp\n// 前綴異或：O(1) 算出任一區間的 XOR\n// 區間 [l, r] 的異或 = pre[r+1] ^ pre[l]\nint segXor = pre[r + 1] ^ pre[l];\n```\n:::\n\n\n\n1310「子陣列異或查詢」正是直接套這條公式；1442「異或相等的三元組」也是靠 `pre` 把條件化成相等判斷。\n\n## 狀態／資料結構定義\n\n**落單型**：一個累加變數 `x`，初值 **0**（XOR 的單位元，寫成別的值就錯了），一路 `x ^= v`。\n\n**前綴異或**：`pre[0] = 0`，`pre[i + 1] = pre[i] ^ a[i]`，長度 **`n + 1`**。區間 `[l, r]` 的異或值是 `pre[r + 1] ^ pre[l]`。長度少開一格或忘記 `pre[0] = 0` 是最常見的錯。\n\n**還原型**：已知 `encoded[i] = a[i] ^ a[i + 1]` 與 `a[0]`，則 `a[i + 1] = a[i] ^ encoded[i]`。1720 直接給 `first`；1734 要先用「1..n 的全體 XOR」與「encoded 的奇數位 XOR」推出 `a[0]`。\n\n**計數型**：用 `unordered_map<int,int>` 統計各前綴異或值出現次數，把「子陣列異或為 k」轉成「有幾對前綴滿足 `pre[j] ^ pre[i] = k`」。\n\n## 不變量或正確性證明\n\n**三條性質的證明**都來自「XOR 是逐位獨立的模 2 加法」：第 `k` 位的結果 = 兩運算元第 `k` 位相加後對 2 取餘。\n\n- **自反性**：同一位與自己相加是 `2 mod 2 = 0`。所以 `a ^ a = 0`。\n- **結合律與交換律**：模 2 加法本身滿足，逐位獨立所以整體也滿足。\n- **逆元**：由 `a ^ a = 0` 與結合律，`(a ^ b) ^ b = a ^ (b ^ b) = a ^ 0 = a`。\n\n**落單型的正確性**：把所有元素 XOR 起來，由交換律可任意重排，把相同的數排在一起。每一對消成 0，剩下的就是出現奇數次的那個。若題目保證恰有一個落單，答案唯一。\n\n**前綴異或的正確性**：`pre[r + 1] ^ pre[l] = (a[0] ^ … ^ a[r]) ^ (a[0] ^ … ^ a[l-1])`。前綴共同的部分 `a[0] ^ … ^ a[l-1]` 各出現兩次，由自反性全部消掉，只剩 `a[l] ^ … ^ a[r]`——正是所求。這與前綴**和**用減法的道理相同，只是 XOR 的逆運算就是它自己。\n\n**為什麼 `pre[0]` 必須是 0**：公式在 `l = 0` 時退化成 `pre[r + 1] ^ pre[0]`，只有 `pre[0] = 0` 才等於整段前綴的 XOR。\n\n## 逐步演算法\n\n:::example 找出唯一出現奇數次的數（其餘都成對）\n**1. 看到重複 / 落單，先想成對抵消。** 把所有數 XOR 起來，重複偶數次的自動消失。\n\n```cpp\n// 找出唯一出現奇數次的數（其餘都成對）\nint ans = 0;\nfor (int x : nums) {\n    ans ^= x;  // 成對的抵消，只剩落單的\n}\n```\n:::\n\n\n\n:::example 建前綴異或：pre[i] 是前 i 個數的 XOR\n**2. 看到「區間異或」，建前綴異或陣列。** `pre[0] = 0`，往後逐項累積。\n\n```cpp\n// 建前綴異或：pre[i] 是前 i 個數的 XOR\nvector<int> pre(n + 1, 0);\nfor (int i = 0; i < n; ++i) {\n    pre[i + 1] = pre[i] ^ a[i];\n}\n```\n:::\n\n\n\n:::example encoded[i] = arr[i] ^ arr[i+1] → 反推 arr\n**3. 利用可逆性反推。** XOR 有「自反」特性：`a ^ b = c` 就代表 `a = b ^ c`。編碼題（如 1720「解碼異或後的陣列」）就靠這個逐步還原。\n\n```cpp\n// encoded[i] = arr[i] ^ arr[i+1] → 反推 arr\narr[i + 1] = encoded[i] ^ arr[i];\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 異或不會溢位也沒有進位，但小心初始值要設 `0`（XOR 的單位元）。\n- 前綴陣列長度要 `n + 1`，`pre[0] = 0` 別漏。\n- 建前綴 `O(n)`，每次區間查詢 `O(1)`，整體 `O(n + q)`。\n\n## C++17 模板\n\n以下用本節代表題 1310. 子陣列異或查詢 展示核心寫法。\n\n**完整題目**：給定陣列與多個 `[l, r]` 查詢，回傳每段子陣列的 XOR 值。\n\n:::example 前綴 XOR：利用 pre[r+1] ^ pre[l] 求區間 [l,r] 的 XOR…\n**線索**：題目出現 XOR/AND/OR、mask 或二進位；先判斷每一位是否能獨立統計或從高位貪心。\n\n```cpp\nclass Solution {\npublic:\n    // 前綴 XOR：利用 pre[r+1] ^ pre[l] 求區間 [l,r] 的 XOR 值\n    vector<int> xorQueries(vector<int>& arr, vector<vector<int>>& queries) {\n            vector<int> pre(arr.size() + 1);  // pre[i] 為 arr[0..i-1] 的 XOR 前綴\n            for (int i = 0; i < (int)arr.size(); ++i) {\n                    pre[i + 1] = pre[i] ^ arr[i];\n            }\n            vector<int> ans;\n            for (auto& q : queries) {\n                    ans.push_back(pre[q[1] + 1] ^ pre[q[0]]);  // 區間 [q[0], q[1]] 的 XOR\n            }\n            return ans;\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- XOR 具有自反性 `x ^ x = 0`，因此區間 XOR 可用前綴 XOR 相消。\n- 程式把整數轉成位元狀態或前綴 XOR，利用位元運算的可逆性/獨立性更新答案。\n- 涉及高位貪心時，迴圈通常從最高位往低位試，因為高位決定主要大小關係。\n\n## 時間與空間複雜度\n\n**落單型**：一次掃描 `O(n)`、空間 `O(1)`。對比用雜湊表計數的 `O(n)` 時間 + `O(n)` 空間，XOR 版把空間壓到常數——這正是 136「只出現一次的數字」要求的。\n\n**前綴異或**：建表 `O(n)`、每次查詢 `O(1)`、空間 `O(n)`。`q` 次查詢合計 `O(n + q)`，對比每次現算的 `O(nq)`。\n\n**計數型**（子陣列異或為 k）：一次掃描配雜湊表，`O(n)` 時間、`O(n)` 空間。\n\n**還原型**：`O(n)` 時間、`O(1)` 額外空間。\n\n**XOR 不會溢位也沒有進位**，所以不需要 `long long`，也不必取模——這是它相對於「前綴和」的一個實務優勢。但若題目把 XOR 與加法混用（如 2354），加法那一側仍要防溢位。\n\n## 常見錯誤與邊界條件\n\n- 位移用 `1LL << b` 避免溢位。\n- 負數右移與符號位要小心。\n- 按位貢獻要確認每位獨立。\n- `__builtin_popcountll` 對 long long 使用 ll 版本。\n\n## 常見變形\n\n- 前綴 XOR 處理區間異或。\n- 試填最高位處理最大 XOR/最大值。\n- SOS DP 處理子集包含關係。\n\n## 與相似技巧的比較\n\n- **對比前綴和**：結構完全平行，`pre[r+1] - pre[l]` 對應 `pre[r+1] ^ pre[l]`。差別在 XOR 的逆運算是自己，所以不必擔心溢位與負數；但 XOR 沒有「大小」概念，無法做區間最大值或二分。\n- **對比雜湊表計數**：找落單元素也能用 `unordered_map` 數次數，`O(n)` 空間。XOR 版是 `O(1)` 空間，但只能處理「恰好一個奇數次」的情形。出現兩個落單（260）要先用 `lowbit` 分組，出現三次（137）則要改用逐位模 3 統計——XOR 的自反性只對「偶數次」有效。\n- **對比排序後掃描**：`O(n log n)`，比 XOR 慢且沒有額外好處。\n- **對比 0/1 Trie**：求「與某數 XOR 最大」需要 Trie，`O(n log U)`。本節的性質處理的是抵消與區間，求極值要換工具（見「試填法」與「線性基」）。\n- **對比線性基**：線性基處理「挑子集湊出某個 XOR 值」，是本節性質的線性代數推廣（把整數看成 GF(2) 上的向量）。\n\n## 代表例題\n\n**題號**：1310、1442、1486、1720、1734\n\n要拆成位元看；練習判斷 XOR/AND/OR 的每一位能否獨立統計或貪心決策。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1310 | [子陣列異或查詢](https://leetcode.cn/problems/xor-queries-of-a-subarray) | 1460 |\n| 1442 | [形成兩個異或相等陣列的三元組數目](https://leetcode.cn/problems/count-triplets-that-can-form-two-arrays-of-equal-xor) | 1525 |\n| 1486 | [陣列異或操作](https://leetcode.cn/problems/xor-operation-in-an-array) | 1181 |\n| 1720 | [解碼異或後的陣列](https://leetcode.cn/problems/decode-xored-array) | 1284 |\n| 1734 | [解碼異或後的排列](https://leetcode.cn/problems/decode-xored-permutation) | 2024 |\n| 1803 | [統計異或值在範圍內的數對有多少](https://leetcode.cn/problems/count-pairs-with-xor-in-a-range) | 2479 |\n| 1829 | [每個查詢的最大異或值](https://leetcode.cn/problems/maximum-xor-for-each-query) | 1523 |\n| 2317 | [操作後的最大異或和](https://leetcode.cn/problems/maximum-xor-after-operations) | 1679 |\n| 2429 | [最小異或](https://leetcode.cn/problems/minimize-xor) | 1532 |\n| 2433 | [找出字首異或的原始陣列](https://leetcode.cn/problems/find-the-original-array-of-prefix-xor) | 1367 |\n| 2527 | [查詢陣列異或美麗值](https://leetcode.cn/problems/find-xor-beauty-of-array) | 1550 |\n| 2564 | [子字串異或查詢](https://leetcode.cn/problems/substring-xor-queries) | 1959 |\n| 2588 | [統計美麗子陣列數目](https://leetcode.cn/problems/count-the-number-of-beautiful-subarrays) | 1697 |\n| 2683 | [相鄰值的按位異或](https://leetcode.cn/problems/neighboring-bitwise-xor) | 1518 |\n| 2857 | [統計距離為 k 的點對](https://leetcode.cn/problems/count-pairs-of-points-with-distance-k) | 2082 |\n| 2997 | [使陣列異或和等於 K 的最少操作次數](https://leetcode.cn/problems/minimum-number-of-operations-to-make-array-xor-equal-to-k) | 1525 |\n| 3215 | [用偶數異或設定位計數三元組 II](https://leetcode.cn/problems/count-triplets-with-even-xor-set-bits-ii) |  |\n| 3702 | [按位異或非零的最長子序列](https://leetcode.cn/problems/longest-subsequence-with-non-zero-bitwise-xor) | 1489 |\n\n## 本節重點速查\n\n- XOR 是逐位的模 2 加法，由此得三條性質：**`a ^ a = 0`（偶數次抵消）**、**可結合可交換（能建前綴）**、**自身為逆元（能反推）**。\n- 找落單元素一路 `x ^= v`，初值必須是 **0**，`O(n)` 時間 `O(1)` 空間。\n- 前綴異或 `pre[0] = 0`、長度 `n + 1`，區間查詢 `pre[r+1] ^ pre[l]`——共同前綴各出現兩次被消掉。\n- 還原型用 `a[i+1] = a[i] ^ encoded[i]`。\n- XOR 不溢位、不進位、不必取模。\n- 但自反性只對**偶數次**有效：兩個落單要先用 `lowbit` 分組（260），出現三次要逐位模 3（137）。\n",
    },
    {
      id: 6,
      title: "3. 與或（AND/OR）的性質",
      summary:
        "**觀念起點**\n\n位元題的優勢在於每個 bit 通常可獨立討論。對 XOR、OR、AND 這類運算，先判斷單一 bit 在何情況下變成 1，再把每位的貢獻乘回權重。\n\nXOR 某位為 1，代表該位上 1 的數量為奇數，或兩數在該位相異；OR 某位為 1，只要至少一個候選提供該位；AND 則要求所有相關元素皆提供該位。\n\n高位貪心通常自高往低試填，因為高位價值大於所有低位總和。只要能檢查當前高位前綴是否可行，便能逐步固定答案。\n\n枚舉一個遮罩的所有子集可用 `for (int s = m; s; s = (s - 1) & m)`，所有遮罩合計 `O(3^n)`；`bitset` 能把布林轉移壓到 `O(n^2/64)`；按位獨立的計數常與前綴和或 SOS DP 結合。\n\n---\n\n**代表例題：LeetCode 2680 最大或值**\n\n**完整問題**：LeetCode 2680 給定陣列 `nums` 與整數 `k`。可選一個元素將其乘上 `2^k`，最大化整個陣列的 bitwise OR。\n\n本節以 題單中的 LeetCode 2680「最大或值」 為主例題（rating 1912），並將同一套不變式延伸至本節其他題目。\n\n**題目訊號**\n\n- OR 的每個 bit 只需某個數提供即可。\n- 操作僅能集中於一個元素。\n- 需取得某位置以外所有元素的 OR。\n\n**狀態、不變式與答案更新**\n\n- `prefix_or[i]` 為 i 左側所有元素的 OR。\n- `suffix_or[i]` 為 i 右側所有元素的 OR。\n- 選擇對 i 操作後，候選答案為 `prefix | (nums[i] << k) | suffix`。\n\n**從零建模**\n\n1. 先建立前綴 OR 與後綴 OR。\n2. 枚舉被乘上 `2^k` 的元素 i。\n3. 合併 i 左側 OR、操作後的 nums[i] 與 i 右側 OR。\n4. 取最大值。\n\n**小範例手動模擬**\n\n以 `nums = [1, 2]` 求所有 pair 的 XOR 和：第 0 位上 1 有、2 無 → ones = 1, zeros = 1，貢獻 1 * 1 * 1 = 1；第 1 位上 2 有、1 無 → 1 * 1 * 2 = 2。總和 3（即 1^2）。\n\n**常見模式**\n\n- 拆位 OR 貢獻\n- 前後綴分解\n- 枚舉唯一操作位置\n\n**常見錯誤**\n\n- 左移後可能超過 int，須使用 `long long`。\n- 操作不可分散至多個元素。\n\n**複雜度**：時間 `O(n)`，空間 `O(n)`；亦可採 suffix 壓縮。 逐位掃描，時間 `O(n * B)`（B 為位數）、空間 `O(1)`。\n\n---\n\n**本節題單中的延伸題**\n\n- LeetCode 1318 或運算的最小翻轉次數，rating 1383\n- LeetCode 2401 最長優雅子陣列，rating 1750\n- LeetCode 2419 按位與最大的最長子陣列，rating 1496\n- LeetCode 2680 最大或值，rating 1912\n- LeetCode 2871 將陣列分割成最多數目的子陣列，rating 1750\n\n---\n\n**同一模板的套用要點**\n\n把每位的 0/1 計數套到 OR/AND 要換公式（OR：至少一個有；AND：全部都有）；求最大 XOR 用高位試填或線性基。\n\n---\n\n**C++ 實作骨架**\n\n```cpp\nclass Solution {\npublic:\n    long long maximumOr(vector<int>& nums, int k) {\n            int n = nums.size();\n            vector<long long> prefix(n + 1, 0), suffix(n + 1, 0);\n            for (int i = 0; i < n; ++i) {\n                    prefix[i + 1] = prefix[i] | nums[i];\n            }\n            for (int i = n - 1; i >= 0; --i) {\n                    suffix[i] = suffix[i + 1] | nums[i];\n            }\n\n            long long answer = 0;\n            for (int i = 0; i < n; ++i) {\n                    answer = max(answer, prefix[i] | (1LL * nums[i] << k) | suffix[i + 1]);\n            }\n            return answer;\n    }\n};\n```",
      children: [
        {
          id: 17,
          title: "3.1 與或（AND/OR）的性質",
          summary:
            "AND 的數越多，結果越小。\n\nOR 的數越多，結果越大。\n\n**AND/OR 單調性**：\n- `a & b <= a`, `a | b >= a`\n- 持續 AND 操作只會讓結果變小且 1 的個數減少\n- 持續 OR 操作只會讓結果變大且 1 的個數增加\n\n**常用技巧**：\n- 判斷子陣列 AND/OR 相關問題時，可利用單調性進行雙指標或列舉\n- 對於區間 AND/OR 查詢，可利用 ST 表（稀疏表）預處理，因為這兩種運算滿足結合律和單調性\n\n:::example AND/OR 性質應用模板\n**性質詳解**：\n- AND：某位為 0 則永遠為 0，只有全部為 1 才為 1\n- OR：某位為 1 則永遠為 1，只有全部為 0 才為 0\n- 利用這些性質可以設計貪心策略或進行位分析\n\n```cpp\n// AND/OR 性質應用模板\n// AND 的數越多，結果越小；OR 的數越多，結果越大\n// 利用單調性，常與雙指標/滑動視窗結合\nint and_all = nums[0];\nfor (int i = 1; i < n; ++i) {\n    and_all &= nums[i];  // 持續 AND 只會讓位元從 1 變 0\n}\n// 列舉子陣列的 AND/OR 值：利用「最多 O(log U) 個不同值」的性質\n```\n:::\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1318 | [或運算的最小翻轉次數](https://leetcode.cn/problems/minimum-flips-to-make-a-or-b-equal-to-c) | 1383 |\n| 2401 | [最長優雅子陣列](https://leetcode.cn/problems/longest-nice-subarray) | 1750 |\n| 2419 | [按位與最大的最長子陣列](https://leetcode.cn/problems/longest-subarray-with-maximum-bitwise-and) | 1496 |\n| 2680 | [最大或值](https://leetcode.cn/problems/maximum-or) | 1912 |\n| 2871 | [將陣列分割成最多數目的子陣列](https://leetcode.cn/problems/split-array-into-maximum-number-of-subarrays) | 1750 |\n| 2980 | [檢查按位或是否存在尾隨零](https://leetcode.cn/problems/check-if-bitwise-or-has-trailing-zeros) | 1234 |\n| 3108 | [帶權圖裡旅途的最小代價](https://leetcode.cn/problems/minimum-cost-walk-in-weighted-graph) | 2109 |\n| 3117 | [劃分陣列得到最小的值之和](https://leetcode.cn/problems/minimum-sum-of-values-by-dividing-array) | 2735 |\n| 3125 | [使得按位與結果為 0 的最大數字](https://leetcode.cn/problems/maximum-number-that-makes-result-of-bitwise-and-zero) |  |\n| 3133 | [陣列最後一個元素的最小值](https://leetcode.cn/problems/minimum-array-end) | 1935 |",
        },
        {
          id: 7,
          title: "3.2 AND/OR LogTrick",
          summary:
            "## 這個技術解決什麼問題\n\n要對**所有子陣列**算 AND 或 OR 值並統計——計數、求最值、找最接近 K 的。\n\n子陣列有 `O(n^2)` 個，逐一硬算是 `O(n^2)` 起跳。LogTrick 利用 AND/OR 的一個關鍵單調性：**固定右端點 `r`，當左端點 `l` 往左移時，OR 值只增不減、AND 值只減不增，而且每次變化至少翻掉一個位元**。因此不同的值最多只有 `O(log U)` 種（`U` 是數值上限）。\n\n把「`O(n)` 個左端點」壓成「`O(log U)` 種不同值」，總複雜度降到 `O(n log U)`。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「AND/OR LogTrick」想：\n\n- **要枚舉所有子陣列的 AND 或 OR 值**，並做計數 / 求最值 / 找最接近某數（898、3171「找到按位或最接近 K 的子陣列」）。\n- **暴力是雙層迴圈 `O(n^2)` 甚至 `O(n^2 log)`，會超時；** 你需要利用「不同值只有 `O(log U)` 種」這件事加速。\n- **要找「OR 至少為 K 的最短 / 最長子陣列」**（2411、3097）——固定右端點後，不同 OR 值有序，可配合雙指針或二分。\n\n:::example 嗅到味道的瞬間：你在數所有子陣列的 OR，又怕 O(n^2) 太慢\n```cpp\n// 嗅到味道的瞬間：你在數所有子陣列的 OR，又怕 O(n^2) 太慢\n// 想到「固定右端、左移時 OR 只增且最多變 log 次」就對了\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我固定的是右端點還是左端點？往哪個方向合併？\n2. 我的剪枝條件是什麼？（OR：值不再變大；AND：值不再變小）\n3. 我在每個端點上要對「`O(log U)` 種區間值」做什麼統計？\n\n## 核心想法與直覺\n\n本節要解決的場景是：要對「所有子陣列」算它們的 AND 或 OR 值（例如數一數有多少子陣列的 OR 等於某值）。子陣列有 `O(n^2)` 個，逐一硬算會太慢；LogTrick 利用 AND/OR 的一個特性把它壓下來。\n\n:::example 固定 r，往左擴張時 OR 的值最多變 32 次\n關鍵特性：**固定右端點 `r`，當左端點 `l` 往左移時，`a[l..r]` 的 OR 只會「越來越大或不變」，AND 只會「越來越小或不變」，而且最多只變 `O(log U)` 次**（`U` 是數值上限）。為什麼？因為 OR 只會把某些位從 0 變 1，一個 32 位數最多被「點亮」32 次，所以不同的值最多 32 種。\n\n```cpp\n// 固定 r，往左擴張時 OR 的值最多變 32 次\n// [r,r] 的 OR ⊆ [r-1,r] 的 OR ⊆ [r-2,r] 的 OR ... （位只增不減）\n```\n:::\n\n\n\n於是我們不必為每個 `(l, r)` 都算，只要維護「以 `r` 結尾的所有子陣列，會產生哪幾種不同的 OR 值，各對應多少個左端點」。這份清單最多 `O(log U)` 筆。\n\n:::example 把 a[r] 往左 OR 進去；不再變化就停（這就是 LogTrick）\n實作上有個漂亮的原地寫法：用一個陣列 `a`，掃到新元素 `a[r]` 時，把它 OR 進前面每一個 `a[i]`（`i` 從 `r - 1` 往左），一旦發現 `a[i]` 已經包含 `a[r]`（OR 不再變化）就提早停止。\n\n```cpp\n// 把 a[r] 往左 OR 進去；不再變化就停（這就是 LogTrick）\nfor (int i = r - 1; i >= 0 && (a[i] | a[r]) != a[i]; i--) {\n    a[i] |= a[r];  // a[i] 現在 = 原本 [i..r] 的 OR\n}\n```\n:::\n\n\n\n掃完後 `a[i]` 就代表「子陣列 `[i..r]` 的 OR」，而且因為只更新到「不再變化」為止，總更新次數是 `O(n log U)`。898「子陣列按位或操作」、2411 都靠這招。\n\n## 狀態／資料結構定義\n\n**就地改寫原陣列**是最省事的寫法：掃到右端點 `r` 時，讓 `a[i]` 重新代表「子陣列 `[i..r]` 的 OR（或 AND）」。\n\n處理 `r` 時，從 `i = r - 1` 往左更新 `a[i] |= a[r]`，**一旦 `a[i]` 沒有變化就立刻 break**——因為再往左的值都已包含這個 OR，不會再改變。這個剪枝是複雜度的關鍵。\n\n**清單寫法**（要計數時更方便）：維護 `vector<pair<int,int>>`，每筆是 `(值, 對應的左端點個數)`，最多 `O(log U)` 筆。加入新元素時對每筆做 OR、再把相同值的合併。\n\n**剪枝條件**：OR 用「值不再變大」，AND 用「值不再變小」。方向搞反會退化成 `O(n^2)`。\n\n## 不變量或正確性證明\n\n**單調性不變量**：固定右端點 `r`，定義 `f(l) = a[l] | a[l+1] | … | a[r]`。當 `l` 減小時，OR 的運算元只增不減，所以 `f(l)` 的位元集合只增不減——`f(l-1) ⊇ f(l)`（把整數看成位元集合）。\n\n**「不同值只有 `O(log U)` 種」的證明**：`f` 沿著 `l` 遞減方向的位元集合是一條遞增鏈。每次值真的改變，至少多一個位元；位元總數只有 `log U` 個（32 或 64）。因此鏈長最多 `log U + 1`，不同值最多 `O(log U)` 種。AND 對稱——位元集合只減不增，同樣最多變化 `log U` 次。\n\n**剪枝的正確性**：更新到某個 `i` 時若 `a[i] | a[r] == a[i]`，代表 `a[r]` 的位元已全被 `a[i]` 包含。而由單調性，所有 `j < i` 的 `a[j] ⊇ a[i] ⊇ a[r]`，所以它們 OR 上 `a[r]` 也不會變。**可以安全 break**。\n\n**總更新次數 `O(n log U)`**：這是攤還論證。每個位置 `i` 的值只會在「多一個位元」時被更新，而位元數上限是 `log U`，所以位置 `i` 一生最多被有效更新 `log U` 次。加總得 `O(n log U)`。無效更新（沒變化的那一次）每個右端點只發生一次，共 `O(n)` 次。\n\n## 逐步演算法\n\n**1. 選定方向：固定右端點，狀態存在原陣列。** 把 `a[i]` 重新定義成「目前以 `r` 為右端、左端為 `i` 的區間 AND/OR」。\n\n:::example OR 版：新元素 a[r] 往左傳播，直到某個 a[i] 已包含它\n**2. 加入新元素時，往左合併並提早剪枝。** 這是 LogTrick 的心臟：合併到「值不再變化」就停。\n\n```cpp\n// OR 版：新元素 a[r] 往左傳播，直到某個 a[i] 已包含它\nfor (int i = r; i >= 0; i--) {\n    if (i < r) {\n        if ((a[i] | a[r_orig]) == a[i]) {\n            break;  // 不再變化，停\n        }\n        a[i] |= a[r_orig];\n    }\n}\n```\n:::\n\n\n\n:::example 在右端點 r 統計：例如數有幾個區間 OR == target\n**3. 在每個右端點結算答案。** 此時 `a[0..r]` 列出所有「以 `r` 結尾」的區間 OR 值，掃過去統計（計數、找最接近 `k` 的、找最短長度等）。\n\n```cpp\n// 在右端點 r 統計：例如數有幾個區間 OR == target\nfor (int i = 0; i <= r; ++i) {\n    if (a[i] == target) {\n        ++ans;\n    }\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- AND 版把 `|` 換成 `&`，剪枝條件改成「`a[i] & x == a[i]`（不再變小）」。\n- 想去重相同值時可改維護 `(值, 出現次數)` 的清單，避免重複統計。\n- 每個元素往左傳播最多 `O(log U)` 次，整體 `O(n log U)`，遠快於 `O(n^2)`。\n\n## C++17 模板\n\n以 3171. 找到按位或最接近 K 的子陣列 為例。\n\n**完整題目**：給陣列 `nums` 與整數 `k`，找一個非空子陣列，使它的按位 OR 與 `k` 的絕對差最小，回傳這個最小差值。\n\n**線索**：要看遍所有子陣列的 OR 值，但固定右端點時不同的 OR 值只有 `O(log U)` 種——就地改寫 + 值不變就 break。\n\n```cpp\n// 3171. 找到按位或最接近 K 的子陣列：LogTrick，O(n log U)\nclass Solution {\npublic:\n    int minimumDifference(vector<int>& nums, int k) {\n        int ans = INT_MAX;\n        const int n = nums.size();\n\n        for (int r = 0; r < n; ++r) {\n            ans = min(ans, abs(nums[r] - k));  // 只有 nums[r] 自己的子陣列\n\n            // 往左更新：讓 nums[i] 代表子陣列 [i..r] 的 OR\n            for (int i = r - 1; i >= 0; --i) {\n                if ((nums[i] | nums[r]) == nums[i]) break;  // 沒變化，再往左也不會變\n                nums[i] |= nums[r];\n                ans = min(ans, abs(nums[i] - k));\n            }\n        }\n        return ans;\n    }\n};\n```\n\n改成 AND 只要把 `|` 換成 `&`、剪枝條件換成 `(nums[i] & nums[r]) == nums[i]`。898（子陣列按位或的不同結果數）則把每個右端點產生的值丟進 `unordered_set`。\n\n## 時間與空間複雜度\n\n**時間 `O(n log U)`**：外層掃 `n` 個右端點，內層的有效更新次數由攤還論證界定在 `O(log U)`，加上每個右端點一次無效更新（觸發 break）。`U = 1e9` 時 `log U ≈ 30`，`n = 1e5` 約 `3e6` 次操作。\n\n對比暴力的 `O(n^2)`（`1e10`）差了三個數量級。\n\n**空間 `O(1)`**（就地改寫）或 `O(log U)`（清單寫法）。就地改寫會**破壞輸入**，題目之後還要用原陣列時要先複製一份。\n\n**要計數時**：清單寫法每筆存 `(值, 左端點個數)`，合併相同值後仍是 `O(log U)` 筆，總時間不變、空間 `O(log U)`。\n\n**配合雙指標或二分**（2411 求 OR 至少為 K 的最短子陣列）：固定右端點後，不同 OR 值沿左端點有序，可以在 `O(log log U)` 內二分找分界，但清單只有 `log U` 筆，直接線性掃就夠。\n\n## 常見錯誤與邊界條件\n\n- **剪枝方向搞反**。OR 的剪枝是「值不再變**大**」（`(a[i] | a[r]) == a[i]`），AND 是「值不再變**小**」（`(a[i] & a[r]) == a[i]`）。寫反會提早 break 而漏解，或永不 break 退化成 `O(n^2)`。\n- **忘記處理長度為 1 的子陣列**。內層迴圈從 `r - 1` 開始，`[r..r]` 本身要在進內層之前單獨結算。\n- **就地改寫破壞輸入**。`nums` 被改成前綴 OR 之後就不是原資料了。若後面還要用原值，先 `vector<int> a = nums;`。\n- **沒有 break 而只靠 `if` 包住更新**。少了 `break`，內層仍會跑滿 `O(n)`，攤還論證失效——這是「寫對答案但超時」的典型原因。\n- **計數時忘記合併相同值**。清單裡若有兩筆相同的值沒合併，筆數會超過 `O(log U)`，複雜度退化。\n- **值域用 `int` 但題目到 `1e9` 以上**。改用 `long long` 並注意 `log U` 變成 60。\n- 邊界測資：全部相同、嚴格遞增、只有一個元素、`k` 小於所有值與大於所有值。\n\n## 與相似技巧的比較\n\n- **對比 GCD LogTrick**（下一節）：完全相同的框架，只把 `|` 換成 `gcd`。GCD 每次變化至少除以 2，所以也只有 `O(log U)` 種不同值。認出「這個運算是冪等且單調的」就能套同一招。\n- **對比稀疏表（Sparse Table）**：也能 `O(1)` 回答任意區間的 AND/OR（因為它們冪等），但要 `O(n log n)` 預處理與空間。要回答任意查詢用稀疏表；要枚舉所有子陣列用 LogTrick。\n- **對比滑動視窗**：OR 沒有「移除左端點」的逆運算（不像加法可以減），所以標準滑窗用不上。2411 要用「三指標」或本節的清單來模擬。\n- **對比線段樹**：能處理帶修改的區間 AND/OR，`O(log n)` 一次。本節是靜態且要遍歷所有子陣列的場景，用線段樹反而慢。\n- **對比拆位法**（第 4 節）：拆位是「按位獨立統計」，適合求總和類；LogTrick 保留完整的值，適合求最值或計數特定值。\n\n## 代表例題\n\n**題號**：898、1521、2411、3097、3171\n\n要拆成位元看；練習判斷 XOR/AND/OR 的每一位能否獨立統計或貪心決策。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 898 | [子陣列按位或操作](https://leetcode.cn/problems/bitwise-ors-of-subarrays) | 2133 |\n| 1521 | [找到最接近目標值的函式值](https://leetcode.cn/problems/find-a-value-of-a-mysterious-function-closest-to-target) | 2384 |\n| 2411 | [按位或最大的最小子陣列長度](https://leetcode.cn/problems/smallest-subarrays-with-maximum-bitwise-or) | 1938 |\n| 3097 | [或值至少為 K 的最短子陣列 II](https://leetcode.cn/problems/shortest-subarray-with-or-at-least-k-ii) | 1891 |\n| 3171 | [找到按位或最接近 K 的子陣列](https://leetcode.cn/problems/find-subarray-with-bitwise-or-closest-to-k) | 2163 |\n| 3209 | [子陣列按位與值為 K 的數目](https://leetcode.cn/problems/number-of-subarrays-with-and-value-of-k) | 2050 |\n\n**延伸閱讀**：[LogTrick 入門與兩種維護方式](https://zhuanlan.zhihu.com/p/1933215367158830792)。清單版更新後要原地合併相鄰等值項；不熟悉讀寫指標去重時，先練 [LeetCode 26](https://leetcode.cn/problems/remove-duplicates-from-sorted-array/)，但這裡合併時還要累加計數或保留最左端點，不能只照抄刪重複值。\n\n## 本節重點速查\n\n- 固定右端點時，左端點左移的 OR 值只增不減、AND 值只減不增，且**每次變化至少翻掉一個位元 ⟹ 不同值只有 `O(log U)` 種**。\n- 就地把 `a[i]` 改寫成「子陣列 `[i..r]` 的 OR」，從 `r-1` 往左更新，**值沒變就 `break`**——這個 break 是複雜度的關鍵，少了它會退化成 `O(n^2)`。\n- 剪枝條件：OR 用 `(a[i] | a[r]) == a[i]`，AND 用 `(a[i] & a[r]) == a[i]`，方向別搞反。\n- 長度 1 的子陣列要單獨結算。\n- 就地改寫會破壞輸入。\n- 整體 `O(n log U)`，空間 `O(1)`。\n",
        },
        {
          id: 8,
          title: "3.3 GCD LogTrick",
          summary:
            "## 這個技術解決什麼問題\n\n對**所有子陣列**算 GCD 並統計——計數等於 `k` 的子陣列、求最大分數、最小化某指標。\n\n和上一節是同一個框架，只把 `|` 換成 `gcd`。能套用的理由也一樣：**固定右端點，左端點左移時 GCD 只減不增，而且每次變化至少除以 2**。因此不同的 GCD 值最多 `O(log U)` 種。\n\n暴力是雙層迴圈每段重算 GCD，`O(n^2 log U)`；LogTrick 降到 `O(n log^2 U)`。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「GCD LogTrick」想：\n\n- **要對所有子陣列算 GCD 並統計**——計數（2447）、求最大分數（3574「最大子陣列 GCD 分數」）、或最小化某指標（3605）。\n- **暴力是雙層迴圈每段重算 GCD，`O(n^2 log U)` 會超時；** 利用「不同 GCD 值只有 `O(log U)` 種」加速。\n- **問題形式和 AND/OR 子陣列題長得幾乎一樣**，只是把 `|`、`&` 換成 `gcd`——同一個框架。\n\n:::example 嗅到味道的瞬間：你在枚舉所有子陣列的 gcd，又怕 O(n^2) 太慢\n```cpp\n// 嗅到味道的瞬間：你在枚舉所有子陣列的 gcd，又怕 O(n^2) 太慢\n// 想到「往左擴張時 gcd 只減且最多變 log 次」就對了\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我維護的清單裡，每一筆 `(g, c)` 分別代表什麼？\n2. 加入新元素時，我有沒有「先更新舊筆、再加自己、再合併重複」？\n3. 我的清單長度是否真的維持在 `O(log U)`（有合併重複值）？\n\n## 核心想法與直覺\n\n本節要解決的場景是：對「所有子陣列」算它們的最大公因數（GCD），例如數有幾個子陣列的 GCD 等於 `k`。GCD 和上一節的 AND/OR 共用同一個加速框架，所以這裡用同樣的 LogTrick。\n\n:::example 固定 r，往左擴張時 GCD 只減不增，且最多變 log 次\n關鍵特性：**固定右端點 `r`，當左端點往左移時，`gcd(a[l..r])` 只會「越來越小或不變」，而且最多只變 `O(log U)` 次**。原因是每次 GCD 真的變小時，它至少會「除以 2」（因數變少），一個數最多被砍 `log U` 次就到 1。\n\n```cpp\n// 固定 r，往左擴張時 GCD 只減不增，且最多變 log 次\n// gcd[r,r] ≥ gcd[r-1,r] ≥ gcd[r-2,r] ... 每次變小至少折半\n```\n:::\n\n\n\n:::example cur：以目前右端結尾的 (gcd 值, 個數) 清單，長度 O(log U)\n所以「以 `r` 結尾的所有子陣列」其實只會產生 `O(log U)` 種不同的 GCD 值。我們維護一份清單：每筆是「(某個 GCD 值, 有多少個左端點得到它)」。\n\n```cpp\n// cur：以目前右端結尾的 (gcd 值, 個數) 清單，長度 O(log U)\nvector<pair<int, int>> cur;  // {gcd, count}\n```\n:::\n\n\n\n:::example 用 GCD 更新清單：每筆和 x 取 gcd，再加上 {x,1}\n掃到新元素 `x` 時，把舊清單每筆的值都 `gcd(值, x)`，再加上「只有 `x` 自己」這一筆，然後把相同 GCD 值的合併。2447「最大公因數等於 K 的子陣列數目」就是這個套路。\n\n```cpp\n// 用 GCD 更新清單：每筆和 x 取 gcd，再加上 {x,1}\nfor (auto& [g, c] : cur) {\n    g = gcd(g, x);\n}\ncur.push_back({x, 1});\n```\n:::\n\n## 狀態／資料結構定義\n\n**清單寫法**（GCD 的主流寫法）：對每個右端點 `r` 維護 `vector<pair<long long,int>>`，每筆是 `(GCD 值, 對應的左端點個數)`，依值遞減排列，最多 `O(log U)` 筆。\n\n加入 `a[r]` 時：先把上一輪的清單每筆的值 `g` 換成 `gcd(g, a[r])`，再在最前面加上 `(a[r], 1)`，最後**合併相鄰相同的值**（把個數相加）。合併是維持 `O(log U)` 筆的關鍵。\n\n**就地改寫版**：讓 `a[i]` 代表 `gcd(a[i..r])`，從 `r-1` 往左更新，**值沒變就 break**——與 AND/OR 版一模一樣。\n\n`gcd(0, x) = x`，所以 0 是 GCD 的單位元，初始值要用 0 而不是 1。數值大時用 `long long`（尤其乘法相關題）。C++17 直接用 `std::gcd`（在 `<numeric>`）。\n\n## 不變量或正確性證明\n\n**單調性不變量**：固定 `r`，`g(l) = gcd(a[l], …, a[r])`。左端點左移時多一個運算元，GCD 只會變小或不變，且**新值必定整除舊值**。\n\n**「不同值只有 `O(log U)` 種」的證明**：沿著 `l` 遞減方向，`g` 形成一條整除鏈 `g(l-1) | g(l)`。每次值真的改變時，新值是舊值的真因數，因此**至少減半**。從 `U` 減半到 1 最多 `log2(U)` 次，所以不同值最多 `O(log U)` 種。\n\n這比 AND/OR 的論證更漂亮——AND/OR 靠「至少翻掉一個位元」，GCD 靠「至少除以 2」，兩者都給出 `log U` 的上界。\n\n**剪枝的正確性**：若 `gcd(a[i], a[r]) == a[i]`，代表 `a[i]` 整除 `a[r]`。由整除鏈，所有 `j < i` 的 `a[j]` 都整除 `a[i]`，因而也整除 `a[r]`，它們的 GCD 同樣不變。**可以安全 break**。\n\n**攤還論證**：每個位置的值一生最多有效更新 `log U` 次，總更新次數 `O(n log U)`；每次更新要算一次 GCD，成本 `O(log U)`。合計 `O(n log^2 U)`。\n\n## 逐步演算法\n\n:::example 用 x 更新所有舊區間的 gcd\n**1. 逐個右端點維護清單。** 每到一個新元素，先更新舊清單再加自己。\n\n```cpp\n// 用 x 更新所有舊區間的 gcd\nfor (auto& p : cur) {\n    p.first = gcd(p.first, x);\n}\ncur.push_back({x, 1});\n```\n:::\n\n\n\n:::example 把相同 gcd 的相鄰項合併計數\n**2. 合併相同 GCD 值。** 更新後相鄰可能出現重複值，合併以保持清單短（`O(log U)`）。\n\n```cpp\n// 把相同 gcd 的相鄰項合併計數\nvector<pair<int, int>> merged;\nfor (auto& [g, c] : cur) {\n    if (!merged.empty() && merged.back().first == g) {\n        merged.back().second += c;\n    } else {\n        merged.push_back({g, c});\n    }\n}\ncur = merged;\n```\n:::\n\n\n\n:::example 例如累加 gcd == k 的子陣列數\n**3. 在每個右端點結算答案。** 清單已列出「以 `r` 結尾的所有 GCD 值與個數」，直接統計。\n\n```cpp\n// 例如累加 gcd == k 的子陣列數\nfor (auto& [g, c] : cur) {\n    if (g == k) {\n        ans += c;\n    }\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 用 `std::gcd`（C++17）或自寫輾轉相除；注意 `gcd(0, x) = x`。\n- 數值大時用 `long long`，避免乘法相關題溢位。\n- 每個右端點清單長 `O(log U)`，整體 `O(n log U)`（含 GCD 計算則乘上 `log U`）。\n\n## C++17 模板\n\n以 2447. 最大公因數等於 K 的子陣列數目 為例。\n\n**完整題目**：給陣列 `nums` 與整數 `k`，回傳 GCD 恰好等於 `k` 的子陣列數目。\n\n**線索**：要看遍所有子陣列的 GCD，但固定右端點時不同的 GCD 只有 `O(log U)` 種——維護 `(值, 左端點個數)` 清單。\n\n```cpp\n// 2447. 最大公因數等於 K 的子陣列數目：GCD LogTrick\nclass Solution {\npublic:\n    int subarrayGCD(vector<int>& nums, int k) {\n        int ans = 0;\n        // prev[j] = (以 r-1 結尾的某段 GCD 值, 對應的左端點個數)，依值遞減\n        vector<pair<int, int>> prev;\n\n        for (int x : nums) {\n            vector<pair<int, int>> cur;\n            cur.emplace_back(x, 1);  // 子陣列 [r..r]\n            for (auto& [g, cnt] : prev) {\n                int ng = gcd(g, x);\n                // 與前一筆相同就合併，清單才會維持在 O(log U) 筆\n                if (!cur.empty() && cur.back().first == ng) cur.back().second += cnt;\n                else cur.emplace_back(ng, cnt);\n            }\n            for (auto& [g, cnt] : cur)\n                if (g == k) ans += cnt;\n            prev = move(cur);\n        }\n        return ans;\n    }\n};\n```\n\n3574（最大子陣列 GCD 分數）把最後的統計換成取最值即可，框架不變。\n\n## 時間與空間複雜度\n\n**時間 `O(n log^2 U)`**：`n` 個右端點，每個右端點的清單有 `O(log U)` 筆，每筆算一次 `gcd` 是 `O(log U)`。`U = 1e9`、`n = 1e5` 時約 `1e5 × 30 × 30 = 9e7`——可行但不寬裕，內層迴圈要寫得乾淨。\n\n對比暴力的 `O(n^2 log U)`（`3e11`）。\n\n**空間 `O(log U)`**：兩份清單。就地改寫版是 `O(1)`，但會破壞輸入。\n\n**常數優化**：`std::gcd` 用的是二進位 GCD 或輾轉相除，兩者都是 `O(log U)`。若值域小（例如 `<= 1e5`），可以預先篩出最小質因數來加速，但一般題目不需要。\n\n**與 AND/OR 版的差異**：AND/OR 每筆合併是 `O(1)`（單一位運算），所以總複雜度是 `O(n log U)`；GCD 每筆要 `O(log U)`，所以多一個 `log`。\n\n## 常見錯誤與邊界條件\n\n- **忘記合併相同值**。清單筆數會失控成長，複雜度退化。合併必須在插入時就做（比對 `cur.back().first`）。\n- **GCD 的單位元用錯**。`gcd(0, x) = x`，所以累積 GCD 的初值是 **0** 不是 1。用 1 會讓所有結果變成 1。\n- **就地改寫版忘記 `break`**。與 AND/OR 版同樣的坑——沒有 break 就沒有攤還保證。\n- **溢位**。GCD 本身不會溢位，但配套的乘法（例如「GCD × 長度」的分數）會，用 `long long`。\n- **負數與 0**。`std::gcd` 要求非負（C++17 起對負數是未定義行為的前身版本行為不一），輸入含負數先取絕對值。全為 0 的子陣列 GCD 是 0，特判題目怎麼定義。\n- **長度為 1 的子陣列漏算**。`cur` 一開始就要放進 `(x, 1)`。\n- 邊界測資：全部相同、互質、含 0、只有一個元素、`k` 不整除任何元素。\n\n## 與相似技巧的比較\n\n- **對比 AND/OR LogTrick**：同一個框架，換運算子而已。共通條件是「運算冪等、且結果沿左端點單調」。認出這個共通點，LCM、`min`、`max` 也能套（不過 `min`/`max` 用單調堆疊更好）。\n- **對比稀疏表**：GCD 是冪等的，所以稀疏表能 `O(1)` 回答任意區間 GCD，`O(n log n)` 預處理。要回答任意查詢用稀疏表；要遍歷所有子陣列用 LogTrick。\n- **對比線段樹**：支援單點修改 + 區間 GCD 查詢，`O(log n)`。靜態題用不著。\n- **對比因數分解列舉**：「GCD 等於 k」也能先把所有元素除以 `k`（不整除的視為斷點），再數「GCD 為 1 的子陣列」，但仍需 LogTrick 或類似結構，沒有更簡單。\n- **對比雙指標**：GCD 沿左端點單調不增，看起來像能雙指標，但 GCD 沒有「移除左端點」的逆運算，所以標準滑窗不適用——這與 OR 的處境相同。\n\n## 代表例題\n\n**題號**：2447、2654、2941、3574、3605\n\n要拆成位元看；練習判斷 XOR/AND/OR 的每一位能否獨立統計或貪心決策。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 2447 | [最大公因數等於 K 的子陣列數目](https://leetcode.cn/problems/number-of-subarrays-with-gcd-equal-to-k) | 1603 |\n| 2654 | [使陣列所有元素變成 1 的最少操作次數](https://leetcode.cn/problems/minimum-number-of-operations-to-make-all-array-elements-equal-to-1) | 1929 |\n| 2941 | [子陣列的最大 GCD-Sum](https://leetcode.cn/problems/maximum-gcd-sum-of-a-subarray)（Premium） |  |\n| 3574 | [最大子陣列 GCD 分數](https://leetcode.cn/problems/maximize-subarray-gcd-score) | 2258 |\n| 3605 | [陣列的最小穩定性因子](https://leetcode.cn/problems/minimum-stability-factor-of-array) | 2410 |\n\n## 本節重點速查\n\n- 與 AND/OR LogTrick 同一框架，把 `|` 換成 `gcd`。\n- 固定右端點時 GCD 沿左端點形成**整除鏈**，每次變化至少減半 ⟹ 不同值只有 `O(log U)` 種。\n- 維護 `(GCD 值, 左端點個數)` 清單，插入時**合併相同值**（否則筆數失控）。\n- 就地改寫版一樣是「值沒變就 `break`」。\n- GCD 的單位元是 **0** 不是 1。\n- C++17 用 `std::gcd`（`<numeric>`），輸入含負數先取絕對值。\n- 整體 `O(n log^2 U)`——比 AND/OR 多一個 `log`，因為每次合併要算一次 GCD。\n",
        },
      ],
    },
    {
      id: 9,
      title: "4. 拆位 / 貢獻法",
      summary:
        "**觀念起點**\n\n位元題的優勢在於每個 bit 通常可獨立討論。對 XOR、OR、AND 這類運算，先判斷單一 bit 在何情況下變成 1，再把每位的貢獻乘回權重。\n\nXOR 某位為 1，代表該位上 1 的數量為奇數，或兩數在該位相異；OR 某位為 1，只要至少一個候選提供該位；AND 則要求所有相關元素皆提供該位。\n\n高位貪心通常自高往低試填，因為高位價值大於所有低位總和。只要能檢查當前高位前綴是否可行，便能逐步固定答案。\n\n枚舉一個遮罩的所有子集可用 `for (int s = m; s; s = (s - 1) & m)`，所有遮罩合計 `O(3^n)`；`bitset` 能把布林轉移壓到 `O(n^2/64)`；按位獨立的計數常與前綴和或 SOS DP 結合。\n\n---\n\n**代表例題：LeetCode 2680 最大或值**\n\n**完整問題**：LeetCode 2680 給定陣列 `nums` 與整數 `k`。可選一個元素將其乘上 `2^k`，最大化整個陣列的 bitwise OR。\n\n本節以 LeetCode 2680「最大或值」 為主例題（rating 1912），並將同一套不變式延伸至本節其他題目。\n\n**題目訊號**\n\n- OR 的每個 bit 只需某個數提供即可。\n- 操作僅能集中於一個元素。\n- 需取得某位置以外所有元素的 OR。\n\n**狀態、不變式與答案更新**\n\n- `prefix_or[i]` 為 i 左側所有元素的 OR。\n- `suffix_or[i]` 為 i 右側所有元素的 OR。\n- 選擇對 i 操作後，候選答案為 `prefix | (nums[i] << k) | suffix`。\n\n**從零建模**\n\n1. 先建立前綴 OR 與後綴 OR。\n2. 枚舉被乘上 `2^k` 的元素 i。\n3. 合併 i 左側 OR、操作後的 nums[i] 與 i 右側 OR。\n4. 取最大值。\n\n**小範例手動模擬**\n\n以 `nums = [1, 2]` 求所有 pair 的 XOR 和：第 0 位上 1 有、2 無 → ones = 1, zeros = 1，貢獻 1 * 1 * 1 = 1；第 1 位上 2 有、1 無 → 1 * 1 * 2 = 2。總和 3（即 1^2）。\n\n**常見模式**\n\n- 拆位 OR 貢獻\n- 前後綴分解\n- 枚舉唯一操作位置\n\n**常見錯誤**\n\n- 左移後可能超過 int，須使用 `long long`。\n- 操作不可分散至多個元素。\n\n**複雜度**：時間 `O(n)`，空間 `O(n)`；亦可採 suffix 壓縮。 逐位掃描，時間 `O(n * B)`（B 為位數）、空間 `O(1)`。\n\n---\n\n**本節題單中的延伸題**\n\n- LeetCode 477 漢明距離總和\n- LeetCode 1835 所有數對按位與結果的異或和，rating 1825\n- LeetCode 1863 找出所有子集的異或總和再求和，rating 1372\n- LeetCode 2275 按位與結果大於零的最長組合，rating 1642\n- LeetCode 2425 所有數對的異或和，rating 1622\n\n---\n\n**同一模板的套用要點**\n\n把每位的 0/1 計數套到 OR/AND 要換公式（OR：至少一個有；AND：全部都有）；求最大 XOR 用高位試填或線性基。\n\n---\n\n**C++ 實作骨架**\n\n```cpp\nclass Solution {\npublic:\n    long long maximumOr(vector<int>& nums, int k) {\n            int n = nums.size();\n            vector<long long> prefix(n + 1, 0), suffix(n + 1, 0);\n            for (int i = 0; i < n; ++i) {\n                    prefix[i + 1] = prefix[i] | nums[i];\n            }\n            for (int i = n - 1; i >= 0; --i) {\n                    suffix[i] = suffix[i + 1] | nums[i];\n            }\n\n            long long answer = 0;\n            for (int i = 0; i < n; ++i) {\n                    answer = max(answer, prefix[i] | (1LL * nums[i] << k) | suffix[i + 1]);\n            }\n            return answer;\n    }\n};\n```",
      children: [
        {
          id: 18,
          title: "4.1 拆位 / 貢獻法",
          summary:
            "**位元拆分 (Bit-by-bit)**：\n- 將每個數拆成 30 或 62 個位元，獨立計算每一位的貢獻\n- 常用於解決 XOR/AND/OR 相關的加總問題\n\n:::example 逐位計算每個 bit 的貢獻\n**C++ 模板**：\n\n```cpp\n// 逐位計算每個 bit 的貢獻\nfor (int i = 0; i < 30; ++i) {\n    int count1 = 0;\n    for (int x : nums) {\n        if ((x >> i) & 1) {\n            ++count1;\n        }\n    }\n    // 根據 count1 (1的個數) 和 n - count1 (0的個數) 計算貢獻\n    long long combinations = (long long)count1 * (n - count1);\n    ans += combinations * (1LL << i);\n}\n```\n:::\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 477 | [漢明距離總和](https://leetcode.cn/problems/total-hamming-distance) |  |\n| 1835 | [所有數對按位與結果的異或和](https://leetcode.cn/problems/find-xor-sum-of-all-pairs-bitwise-and) | 1825 |\n| 1863 | [找出所有子集的異或總和再求和](https://leetcode.cn/problems/sum-of-all-subset-xor-totals) | 1372 |\n| 2275 | [按位與結果大於零的最長組合](https://leetcode.cn/problems/largest-combination-with-bitwise-and-greater-than-zero) | 1642 |\n| 2425 | [所有數對的異或和](https://leetcode.cn/problems/bitwise-xor-of-all-pairings) | 1622 |\n| 2505 | [所有子序列和的按位或](https://leetcode.cn/problems/bitwise-or-of-all-subsequence-sums) |  |\n| 3688 | [偶數的按位或運算](https://leetcode.cn/problems/bitwise-or-of-even-numbers-in-an-array) | 1205 |",
        },
        {
          id: 10,
          title: "4.2 進位制拆位",
          summary:
            "## 這個技術解決什麼問題\n\n要對「所有數對」或「所有組合」算某種和，數量是 `O(n^2)` 量級，硬枚舉超時。\n\n突破口是**換軸**：不要問「這一對數差多少」，改問「每一個位上，會貢獻多少」。只要各位之間互不干擾，就能分開算完再相加——把 `O(n^2)` 的兩兩比較，變成「位數 × 一次線性掃描」。\n\n3153（數位不同之和）是最乾淨的例子：兩數在某一位不同才貢獻 1，而某一位的貢獻只取決於「這一位上各數字各有幾個」。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「進位制拆位」想：\n\n- **題目要對「所有數對 / 所有組合」算某種和，數量是 `O(n^2)` 量級，直接枚舉會超時。**\n- **這個「和」可以拆成「每一位的貢獻分別計算，再加起來」**——各位之間互不影響，例如 3153 數位不同之和。\n- **暴力是雙層迴圈兩兩比較；** 拆位後對每一位只要一次線性掃描統計各數字數量。\n\n:::example 嗅到味道的瞬間：你想對每一對數做比較求和，又怕 O(n^2)\n```cpp\n// 嗅到味道的瞬間：你想對每一對數做比較求和，又怕 O(n^2)\n// 試著問：能不能改成「對每一位獨立統計，再加總」？\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我用幾進位拆？每一位的「貢獻」公式是什麼？\n2. 各位之間真的互相獨立、可以分開算再相加嗎？\n3. 計數和乘法會不會溢位，有沒有用 `long long`？\n\n## 核心想法與直覺\n\n本節要解決的場景是：題目要對「所有數對」算某種和（例如所有兩兩之間有幾個位數不同），數對有 `O(n^2)` 個，硬枚舉太慢。進位制拆位的想法是：**把每個數字按「位」拆開，一位一位獨立統計，再把各位的貢獻加總。**\n\n核心觀念叫「**拆貢獻**」。與其問「這一對數差多少」，不如反過來問「每一個位上，會貢獻多少」。因為各位之間互不干擾，可以分開算完再相加。\n\n:::example 拆貢獻：總答案 = 每一位的「不同對數」加總\n以 3153「所有數對中數位不同之和」為例：兩個數在某一位不同，就替答案 +1。那麼總答案 = 把每一位單獨拿出來，數「這一位上有幾對數字不一樣」，再全部加起來。\n\n```cpp\n// 拆貢獻：總答案 = 每一位的「不同對數」加總\n// 對某一位，若有 c0 個數字是 0、c1 個是 1，\n// 則這一位貢獻的「不同對數」= c0 * c1（一個 0 配一個 1 就算一對不同）\nlong long contrib = (long long)c0 * c1;\n```\n:::\n\n\n\n:::example 十進位逐位取數字：個位、十位、百位 ...\n關鍵在於：原本要看 `O(n^2)` 對，現在改成「對每一位，數各種數字各有幾個」，每位只要 `O(n)`，總共位數固定（十進位 10 種數字、二進位 2 種），於是整體降到 `O(n * 位數)`。\n\n```cpp\n// 十進位逐位取數字：個位、十位、百位 ...\nint d = x % 10;  // 取最低位\nx /= 10;         // 去掉最低位，繼續看下一位\n```\n:::\n\n## 狀態／資料結構定義\n\n**進位制與位數**：先決定用幾進位拆。十進位題（3153）用 `% 10` / `/= 10`；二進位題用 `>> k & 1`。位數上限固定（十進位到 `1e9` 是 10 位，二進位是 30 位）。\n\n**每一位的計數**：`cnt[d]` = 這一位上數字為 `d` 的元素個數。十進位是長度 10 的陣列，二進位只需要「1 的個數」一個變數。\n\n**貢獻公式**：以 3153 為例，某一位上若數字 `d` 有 `cnt[d]` 個，則這一位貢獻的「不同對數」是 `C(n, 2) - Σ_d C(cnt[d], 2)`（總對數扣掉相同的對數）。\n\n**長度不一時補齊**：短的數高位視為 0，否則對齊會錯。3153 保證所有數位數相同，但一般題要自己補。\n\n計數與累加一律 `long long`——`C(n, 2)` 在 `n = 1e5` 時是 `5e9`。\n\n## 不變量或正確性證明\n\n**可加性不變量**：答案能寫成 `Σ_位 (該位的貢獻)`，而每一位的貢獻只取決於所有元素在該位的數字分佈。\n\n這一步是整個技巧的地基，必須逐題確認，不能想當然。成立的條件是**目標量對「位」是可加的**：\n\n- 3153「數位不同之和」：兩數的距離定義為「不同位的個數」，而「不同位的個數」= `Σ_位 [兩數在該位不同]`。求和與求和交換次序，得 `Σ_對 Σ_位 = Σ_位 Σ_對`——這正是換軸的合法性。\n- **XOR / AND / OR 的總和**：這些運算逐位獨立，第 `k` 位的結果只由各數第 `k` 位決定，所以也可加（見「恆等式」一節）。\n\n**不可加的反例**：一般的加法**有進位**，第 `k` 位的結果會受低位影響，不能直接拆位獨立統計。看到 `+` 就要先警覺——要嘛先用恆等式 `a + b = (a ^ b) + ((a & b) << 1)` 轉成位運算，要嘛換方法。\n\n**單一位貢獻的正確性**（3153）：該位上總共有 `C(n, 2)` 對；其中數字相同的對數是 `Σ_d C(cnt[d], 2)`。兩者相減就是不同的對數。因為「相同」與「不同」互補且窮盡，這個計數不重不漏。\n\n## 逐步演算法\n\n:::example 二進位拆位\n**1. 決定用幾進位拆。** 看數字字面（十進位）就用 `% 10` / `/= 10`；看二進位特性就用 `& 1` / `>> 1`。\n\n```cpp\n// 二進位拆位\nint bit = (x >> i) & 1;  // 取第 i 位\n```\n:::\n\n\n\n:::example cnt[p][d] = 第 p 位上數字 d 出現幾次（十進位 d 為 0..9）\n**2. 對每一位，統計各種數字出現次數。** 開一個計數表，掃過所有數。\n\n```cpp\n// cnt[p][d] = 第 p 位上數字 d 出現幾次（十進位 d 為 0..9）\nfor (int x : nums) {\n    for (int p = 0; x; p++, x /= 10) {\n        cnt[p][x % 10]++;\n    }\n}\n```\n:::\n\n\n\n:::example 第 p 位的不同對數 = C(n,2) - 各數字內部成對數\n**3. 把每一位的貢獻加總。** 每位「不同對數」= 總對數減去「相同對數」，或直接用各數字數量兩兩相乘。\n\n```cpp\n// 第 p 位的不同對數 = C(n,2) - 各數字內部成對數\nlong long same = 0;\nfor (int d = 0; d < 10; ++d) {\n    same += 1LL * cnt[p][d] * (cnt[p][d] - 1) / 2;\n}\nans += 1LL * n * (n - 1) / 2 - same;\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 位數固定上限（例如十進位最多十幾位），所以複雜度是 `O(n * 位數)`。\n- 乘法可能很大，計數與累加都用 `long long`。\n- 若數字長度不一，短的數高位視為 0（記得補齊）。\n\n## C++17 模板\n\n以 3153. 所有數對中數位差之和 為例。\n\n**完整題目**：給一組**位數相同**的正整數 `nums`，兩數的「數位差」是它們對應位置上不同數字的個數。回傳所有數對的數位差之和。\n\n**線索**：數對有 `O(n^2)` 個，但「數位差」= 各位是否不同的總和，各位獨立 ⟹ 逐位統計。\n\n```cpp\n// 3153. 所有數對中數位差之和：十進位拆位 + 貢獻法\nclass Solution {\npublic:\n    long long sumDigitDifferences(vector<int>& nums) {\n        const long long n = nums.size();\n        const long long totalPairs = n * (n - 1) / 2;\n        long long ans = 0;\n\n        // 逐位處理，直到所有數都被消完\n        for (bool more = true; more; ) {\n            more = false;\n            long long cnt[10] = {};\n            for (int& x : nums) {\n                cnt[x % 10]++;\n                x /= 10;\n                if (x > 0) more = true;\n            }\n            // 這一位的貢獻 = 總對數 − 該位數字相同的對數\n            long long same = 0;\n            for (long long c : cnt) same += c * (c - 1) / 2;\n            ans += totalPairs - same;\n        }\n        return ans;\n    }\n};\n```\n\n二進位版本更短：對每個 `k`，數出第 `k` 位為 1 的個數 `ones`，該位貢獻 `ones * (n - ones)` 對。\n\n## 時間與空間複雜度\n\n**時間 `O(n × 位數)`**：位數是常數（十進位到 `1e9` 是 10 位，二進位 30 位），所以實質是 `O(n)`。`n = 1e5` 時約 `1e6` 次操作。\n\n對比暴力的 `O(n^2 × 位數)`——`n = 1e5` 時是 `1e11`，差了五個數量級。\n\n**空間 `O(1)`**：十進位只需長度 10 的計數陣列，二進位只需一個變數。上面的模板就地修改 `nums`（`x /= 10`），連複製都省了——但這會破壞輸入。\n\n**溢位是本節的主要風險**：`totalPairs = n * (n - 1) / 2` 在 `n = 1e5` 時是 `5e9`，早已超過 `int`；再乘上位數更大。計數變數、對數、累加器一律 `long long`。\n\n**位數不固定時**：取最大值的位數當上限，短的數自動變成 0（`x /= 10` 之後就是 0），計數會落在 `cnt[0]`——這正好等價於「高位補 0」，不必額外處理。\n\n## 常見錯誤與邊界條件\n\n- **對有進位的運算硬拆位**。加法、乘法的第 `k` 位受低位影響，不可加。動手前先問「各位真的獨立嗎」，答不出來就別拆。\n- **溢位**。`n * (n - 1) / 2` 與各位累加都要 `long long`。這是本節最常見的 WA 來源，而且小測資看不出來。\n- **位數不一沒補齊**。若手動枚舉固定位數，短的數高位要當 0。用 `x /= 10` 的寫法會自動處理，但用 `to_string` 對齊時就要自己補。\n- **`C(c, 2)` 在 `c = 0` 或 `1` 時**：`c * (c - 1) / 2` 自然是 0，不必特判；但若寫成別的形式要檢查。\n- **就地修改破壞輸入**。模板裡的 `x /= 10` 會把 `nums` 清成 0，之後不能再用。\n- **迴圈終止條件**。用 `more` 旗標判斷是否還有數字沒處理完；寫成固定 10 圈也可以，但要確認上限夠。\n- 邊界測資：`n = 1`（答案 0）、所有數相同（答案 0）、位數最長的情形。\n\n## 與相似技巧的比較\n\n- **對比恆等式**（第 6 節）：兩者都是「逐位獨立統計」。差別在本節通常是十進位或一般進位制，第 6 節專指 XOR/AND/OR 的位元恆等式。骨架相同，判準都是「各位是否獨立」。\n- **對比單調堆疊的貢獻法**：那裡換的軸是「每個元素當幾次極值」，這裡換的軸是「每一位貢獻多少」。都是「不要枚舉組合，改枚舉貢獻者」的同一思想。\n- **對比前綴和**：前綴和換的是「區間求和」的計算順序，本節換的是「維度」。兩者常一起用——先拆位，再對每一位建前綴和。\n- **對比雜湊表計數**：3153 也能對每一位用 `unordered_map` 統計，但十進位只有 10 種數字，陣列快得多且沒有雜湊開銷。\n- **對比 LogTrick**：LogTrick 保留完整的值、處理的是子陣列；拆位法打散成獨立的位、處理的是數對或子集。看到「所有子陣列」想 LogTrick，看到「所有數對」想拆位。\n\n## 代表例題\n\n**題號**：3153\n\n要拆成位元看；練習判斷 XOR/AND/OR 的每一位能否獨立統計或貪心決策。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 3153 | [所有數對中數位不同之和](https://leetcode.cn/problems/sum-of-digit-differences-of-all-pairs) | 1645 |\n\n## 本節重點速查\n\n- 所有數對的和算不動時，**換軸成「每一位貢獻多少」**：`Σ_對 Σ_位` 交換成 `Σ_位 Σ_對`，`O(n^2)` 降到 `O(n × 位數)`。\n- 合法的前提是**目標量對位可加**——XOR/AND/OR 與「不同位的個數」可以，一般的**加法有進位所以不行**。\n- 十進位貢獻公式：`C(n,2) − Σ_d C(cnt[d],2)`； 二進位更短：`ones * (n - ones)`。\n- 位數不一時短數高位補 0（用 `x /= 10` 會自動處理）。\n- **所有計數與累加用 `long long`**，`n * (n-1) / 2` 在 `n = 1e5` 就有 `5e9`。\n",
        },
      ],
    },
    {
      id: 11,
      title: "5. 試填法",
      summary:
        "## 這個技術解決什麼問題\n\n要建構一個「最大／最小」的整數答案，而答案能**逐位決定**。\n\n直接枚舉所有可能值太大（值域到 `2^30`），枚舉所有配對也太多（`O(n^2)`）。試填法把搜尋空間從「值域」壓成「位數」：從**最高位往低位**，一位一位試著填 1（求最大）或 0（求最小），每一位只需回答一個問題——「這位這樣填，後面還湊得出合法解嗎」。\n\n30 位就是 30 次判定，每次判定通常 `O(n)`，整體 `O(n × 位數)`。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就可以往「試填法」想：\n\n- **要建構一個「最大 / 最小」的整數答案，而答案能逐位決定**（421、2935「找出強數對的最大異或值 II」）。\n- **高位優先有意義**：高位填好就鎖定，不影響後面——典型的逐位貪心（3007、3022）。\n- **暴力是枚舉所有可能值或所有配對，太大；** 改成「逐位試填 + 快速可行性檢查」把空間砍成位數量級。\n- **常和雜湊集合或 0/1 Trie 搭配**，用來快速回答「這位填 1 還湊不湊得出」。\n\n:::example 嗅到味道的瞬間：你想求最大值，又發現「高位先決定、能填就填」說得通\n```cpp\n// 嗅到味道的瞬間：你想求最大值，又發現「高位先決定、能填就填」說得通\n// for (int b = HIGH; b >= 0; b--) if (canSet(b)) ans |= 1 << b;\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我從哪一位開始試填？方向是要 1（求大）還是要 0（求小）？\n2. 「這位填某值是否可行」我怎麼判斷？用什麼資料結構加速？\n3. 鎖定高位後，它真的不會被低位的選擇推翻嗎？\n\n## 核心想法與直覺\n\n本節要解決的場景是：要建構一個「最大 / 最小」的答案，而答案是逐位（bit）決定的——我們從**最高位往低位**，一位一位地「試著填看看」，能填 1（或 0）就填，使結果朝目標方向最佳化。這就是試填法。\n\n:::example 從高位往低位試填，能填 1 就鎖定\n核心直覺：高位的權重遠大於所有低位之和。所以想讓結果最大，就該**優先把高位填 1**；只要某一位填了 1 之後「仍然可行」，就鎖定它，再往下一位試。這是一種貪心。\n\n```cpp\n// 從高位往低位試填，能填 1 就鎖定\nint ans = 0;\nfor (int b = HIGH; b >= 0; b--) {\n    int cand = ans | (1 << b);  // 試著把第 b 位設成 1\n    if (feasible(cand)) {\n        ans = cand;  // 可行就保留，否則維持原樣\n    }\n}\n```\n:::\n\n\n\n`feasible(cand)` 是「假設高位已照 `ans` 決定、這一位填 1，後面還湊得出合法解嗎？」每題的 `feasible` 不同，但框架一樣。\n\n:::example 最大 XOR 試填：這一位想要 1，需存在兩前綴 p,q 使 p^q = 目前目標\n以 421「兩個數的最大異或值」為例：想讓兩數 XOR 最大，就從高位試填——「能不能找到兩個數，使它們在已定的高位之外，這一位異或出 1？」能就鎖定這位是 1。判斷常借助一個雜湊集合存「各數的高位前綴」。\n\n```cpp\n// 最大 XOR 試填：這一位想要 1，需存在兩前綴 p,q 使 p^q = 目前目標\nunordered_set<int> pre;\nfor (int x : nums) {\n    pre.insert(x >> b);\n}\nint want = (ans << 1) | 1;  // 期望的高位前綴（含這位的 1）\nfor (int p : pre) {\n    if (pre.count(p ^ want)) {\n        ans = want;\n        break;\n    }\n}\n```\n:::\n\n## 狀態／資料結構定義\n\n**答案累積器 `ans`**：已經決定好的高位部分。從 0 開始，每確定一位就 `ans |= 1 << b`（求最大時）。\n\n**候選值 `cand = ans | (1 << b)`**：假設這一位填 1 的話，高位加上這一位長什麼樣。\n\n**可行性判定 `feasible(cand)`**：「高位照 `cand` 決定，低位任意，還湊得出合法解嗎？」每題不同，但常見兩種載體——\n\n- **雜湊集合**（421 最大異或對）：把所有數的「高位前綴」放進 `unordered_set`，判斷是否存在 `p` 使 `p ^ cand` 也在集合裡。\n- **0/1 Trie**：把所有數按位插入，查詢時每層盡量往「相反位元」走。\n\n**位數上限**：先確認數值上限對應幾位。`nums[i] <= 2^31 - 1` 用 30（或 31）；到 `1e18` 用 60，且一律 `1LL << b`。上限少算一位，答案會整個偏掉。\n\n## 不變量或正確性證明\n\n**不變量**：處理完第 `b` 位之後，`ans` 的高位部分（第 `b` 位以上）是「所有合法解中，高位能達到的字典序最大（求最大時）」的那一組。\n\n**貪心的正確性**（為什麼高位能填 1 就一定要填）：二進位的高位權重嚴格大於所有低位之和——`2^b > 2^(b-1) + … + 2^0`。所以只要第 `b` 位能填 1，不管低位如何，結果都大於「第 `b` 位填 0 而低位全填 1」的最大值。因此填 1 永遠不吃虧，這是交換論證的一個乾淨特例。\n\n**歸納維持**：假設高位已按不變量決定。第 `b` 位若 `feasible(ans | 1 << b)` 為真，代表存在一個合法解與這些高位一致且第 `b` 位是 1；由上述權重論證，最優解必定屬於這一群，令 `ans |= 1 << b` 保持不變量。若為假，則所有與高位一致的合法解在第 `b` 位都是 0，不填也保持不變量。\n\n**`feasible` 必須是「高位鎖定、低位自由」**。這是最容易寫錯的地方：判定的不是「`cand` 這個完整值可行嗎」，而是「以 `cand` 為前綴的值裡有沒有可行的」。前者會漏掉大量解。\n\n**421 的具體判定**：要找兩數 XOR 的最大值。若 `a ^ b = cand`（只看高位前綴），則 `a = b ^ cand`。所以把所有數的高位前綴丟進集合，只要存在某個前綴 `p` 使 `p ^ cand` 也在集合裡，就代表這一位填 1 可行。\n\n## 逐步演算法\n\n:::example 32 位整數從第 30（或 31）位開始往下\n**1. 找出最高位、決定試填方向。** 求最大就試填 1，求最小就試填 0（或反向判可行）。\n\n```cpp\n// 32 位整數從第 30（或 31）位開始往下\nfor (int b = 30; b >= 0; b--) { /* 試填第 b 位 */\n}\n```\n:::\n\n\n\n:::example 把候選值丟進可行性檢查；可行才接受\n**2. 寫出「假設這位填 X，是否可行」的檢查。** 這是每題最核心、也最不一樣的部分。\n\n```cpp\n// 把候選值丟進可行性檢查；可行才接受\nint cand = ans | (1 << b);\nif (check(cand)) {\n    ans = cand;\n}\n```\n:::\n\n\n\n:::example Trie 走法：求最大 XOR 時，每位優先走「相反位」\n**3. 用資料結構加速可行性判斷。** 常見是雜湊集合（XOR 配對）或 0/1 Trie（逐位走最有利分支）。\n\n```cpp\n// Trie 走法：求最大 XOR 時，每位優先走「相反位」\nint node = root, res = 0;\nfor (int b = HIGH; b >= 0; b--) {\n    int want = !((x >> b) & 1);  // 想走和自己相反的位\n    if (child[node][want]) {\n        res |= 1 << b;\n        node = child[node][want];\n    } else {\n        node = child[node][!want];\n    }\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 最高位選錯（少一位或多一位）會整個偏掉，先確認數值上限對應幾位。\n- 大數值用 `long long` 與 `1LL << b`。\n- 試填位數固定（約 30~60），每位的檢查若 `O(n)`，整體 `O(n * 位數)`。\n\n## C++17 模板\n\n以 421. 陣列中兩個數的最大異或值 為例。\n\n**完整題目**：給整數陣列 `nums`，回傳 `nums[i] ^ nums[j]` 的最大值。\n\n**線索**：答案逐位決定、高位優先，且「這位能不能填 1」可以用雜湊集合 `O(n)` 判定。\n\n```cpp\n// 421. 陣列中兩個數的最大異或值：高位優先試填 + 雜湊集合判定\nclass Solution {\npublic:\n    int findMaximumXOR(vector<int>& nums) {\n        int ans = 0;\n        for (int b = 30; b >= 0; --b) {          // 由最高位往低位試填\n            unordered_set<int> prefixes;\n            for (int x : nums) prefixes.insert(x >> b);  // 只保留高位前綴\n\n            int cand = ans << 1 | 1;             // 假設這一位能填 1\n            bool ok = false;\n            for (int p : prefixes) {\n                // a ^ b == cand  <=>  a == b ^ cand，兩者都要在集合裡\n                if (prefixes.count(p ^ cand)) { ok = true; break; }\n            }\n            ans = ok ? cand : cand - 1;         // 填不了就退回這位為 0\n        }\n        return ans;\n    }\n};\n```\n\n2935（強數對的最大異或值 II）把判定換成 0/1 Trie 加上滑動視窗；3007、3022 則把判定換成「計算某個計數是否超標」，框架不變。\n\n## 時間與空間複雜度\n\n**時間 `O(n × 位數)`**：位數固定 30~60，每位做一次 `O(n)` 的判定。`n = 2e5`、30 位約 `6e6`，很寬裕。\n\n對比暴力枚舉所有配對的 `O(n^2)`（`4e10`）。\n\n**空間 `O(n)`**：每輪的前綴集合。0/1 Trie 版是 `O(n × 位數)` 個節點。\n\n**雜湊集合對 Trie 的取捨**：雜湊版程式短、常數受雜湊表影響；Trie 版沒有雜湊開銷、查詢是穩定的 `O(位數)`，且能自然支援「刪除」（配合滑動視窗，如 2935）。單純求全域最大用雜湊版就好。\n\n**判定成本是瓶頸**：若 `feasible` 寫成 `O(n log n)`，整體變成 `O(n log n × 位數)`。能預先排序、預先建 Trie 的都要移到迴圈外。\n\n## 常見錯誤與邊界條件\n\n- **`feasible` 判成「完整值可行」而非「前綴可行」**。這是本節最根本的錯——判定必須是「以 `cand` 為高位前綴的解存在嗎」，低位是自由的。\n- **最高位選錯**。少算一位答案會被截斷，多算一位在有號型別上會碰到符號位。先確認數值上限對應幾位。\n- **`1 << b` 溢位**。`b >= 31` 時要寫 `1LL << b` 並全程用 `long long`。\n- **求最小時方向沒翻轉**。求最大是「能填 1 就填」，求最小是「能填 0 就填」，`cand` 與退回值都要跟著改。\n- **前綴集合每輪重建的成本被忽略**。模板每輪都重建 `unordered_set`，這是 `O(n)`，可接受；但若寫成每輪重排序就會多一個 `log`。\n- **`ans << 1 | 1` 與 `ans | (1 << b)` 混用**。前者假設 `ans` 只保留已處理的高位（每輪左移一格），後者假設 `ans` 是完整寬度的值。兩種寫法都對，但**不能混用**——模板用的是前者，所以前綴也要用 `x >> b` 對齊。\n- 邊界測資：`n = 1`（無配對）、所有數相同（答案 0）、含 0、最大值。\n\n## 與相似技巧的比較\n\n- **對比二分答案**：兩者都是「猜一個值再驗證」。差別在二分每次砍一半值域（`log W` 次），試填每次確定一個位（同樣 `log W` 次）——複雜度相同，但試填能處理「答案不單調」的情形，因為它靠的是位權重而非單調性。\n- **對比 0/1 Trie**：Trie 是試填法的一種資料結構載體，把「這位能不能走相反方向」變成查一次子節點。求「與某數 XOR 最大」時 Trie 更直接；求「全域最大配對」時雜湊版更短。\n- **對比線性基**（第 7 節）：線性基處理的是「挑**任意子集**做 XOR」，試填處理的是「挑**固定個數**（通常兩個）」。線性基建好之後也用高位貪心求最大值——兩者的貪心論證是同一套。\n- **對比暴力枚舉配對**：`O(n^2)`。試填把「枚舉答案」換成「逐位確定答案」，是典型的降維。\n- **對比 DP**：有些逐位建構題可以寫成數位 DP（狀態是「第幾位 + 是否貼上界」）。答案要求最值且能貪心時試填更短；要計數就得用數位 DP。\n\n## 代表例題\n\n**題號**：421、2935、3007、3022、3145\n\n要拆成位元看；練習判斷 XOR/AND/OR 的每一位能否獨立統計或貪心決策。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 421 | [陣列中兩個數的最大異或值](https://leetcode.cn/problems/maximum-xor-of-two-numbers-in-an-array) |  |\n| 2935 | [找出強數對的最大異或值 II](https://leetcode.cn/problems/maximum-strong-pair-xor-ii) | 2349 |\n| 3007 | [價值和小於等於 K 的最大數字](https://leetcode.cn/problems/maximum-number-that-sum-of-the-prices-is-less-than-or-equal-to-k) | 2258 |\n| 3022 | [給定操作次數內使剩餘元素的或值最小](https://leetcode.cn/problems/minimize-or-of-remaining-elements-using-operations) | 2918 |\n| 3145 | [大陣列元素的乘積](https://leetcode.cn/problems/find-products-of-elements-of-big-array) | 2859 |\n| 3287 | [求出陣列中最大序列值](https://leetcode.cn/problems/find-the-maximum-sequence-value-of-array) | 2545 |\n| 3344 | [最大尺寸陣列](https://leetcode.cn/problems/maximum-sized-array) |  |\n| 3806 | [增加操作後最大按位與的結果](https://leetcode.cn/problems/maximum-bitwise-and-after-increment-operations) | 2259 |\n\n## 本節重點速查\n\n- 答案能逐位決定時，**從最高位往低位試填**：每位問「這位填 1（求最大）的話，後面還湊得出合法解嗎」。\n- 貪心正確性來自位權重——`2^b` 嚴格大於所有低位之和，所以能填 1 就一定要填。\n- **`feasible` 判的是「以 `cand` 為高位前綴的解存在嗎」，不是「`cand` 這個完整值可行嗎」**，判錯這點會漏掉大量解。\n- 421 的判定用 `a ^ b = cand ⟺ a = b ^ cand`，把高位前綴丟進雜湊集合查配對。\n- 位數上限先算清楚，`b >= 31` 要用 `1LL << b`。\n- `ans << 1 | 1` 與 `ans | (1 << b)` 兩種寫法不能混用。\n- 整體 `O(n × 位數)`。\n",
    },
    {
      id: 12,
      title: "6. 恆等式",
      summary:
        "## 這個技術解決什麼問題\n\n題目把一堆數字用 XOR、AND、OR 串起來，要算所有數對或所有子集的總和／計數。硬算是 `O(n^2)`。\n\n核心想法是**把整數拆成一個一個位元，每一位獨立計算，最後再加回去**。之所以可行，是因為 XOR、AND、OR 都是**逐位元運算**——第 `k` 位的結果只跟所有數字的第 `k` 位有關，完全不受其他位影響。一個看似全局的條件，因此被切成 32（或 64）個彼此獨立的小問題。\n\n另一條路是**整數恆等式**，最有用的是 `a + b = (a ^ b) + ((a & b) << 1)`——它把有進位的加法拆成「不進位的部分」加「進位的部分」，讓混有 `+` 與位運算的式子能被改寫成純位運算。\n\n## 辨識題型的訊號\n\n看到這些特徵，就往「拆位 + 恆等式」想：\n\n- **運算子是 XOR / AND / OR，而且要算「所有數對 / 所有子集」的總和或計數。** 全局聚合 + 逐位運算 = 強烈訊號。\n- **暴力是 `O(n^2)` 列舉每一對，明顯超時。** 拆位後每位獨立統計，把它壓成 `O(n log U)`。\n- **式子裡同時出現 `+` 和位運算**，例如要把 `a + b` 換成位運算，這時恆等式 `a + b = (a ^ b) + ((a & b) << 1)` 就派上用場（如 2354「優質數對的數目」用 popcount 把條件轉成可統計的形式）。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的答案能不能「一位一位」算完再加總？各位之間真的互不影響嗎？\n2. 固定某一位時，這一位對答案的貢獻公式是什麼？\n3. 配對數或位移會不會超出 `int`？哪裡該換成 `long long`？\n\n三題都答得出來，就套對了。\n\n## 核心想法與直覺\n\n本節要處理的場景是：題目把一堆數字用 XOR、AND、OR 串起來，要你算總和或判斷某個性質。如果你硬把每一對、每一組都算一遍，常常會超時。「恆等式」的核心想法是：**把整數拆成一個一個位元（bit）來看，每一位獨立計算，最後再把各位的結果加回去。**\n\n為什麼可以這樣拆？因為 XOR、AND、OR 都是「逐位元」運算——第 k 位的結果只跟所有數字的第 k 位有關，完全不受其他位影響。所以一個看似全局的條件，可以被切成 32（或 64）個彼此獨立的小問題。\n\n:::example 加法拆成 XOR（不進位）+ 進位\n舉個最關鍵的恆等式：`a + b = (a XOR b) + 2 * (a AND b)`。意思是「相加」其實等於「不進位的加法（XOR）」再補上「進位的部分（AND 左移一位）」。\n\n```cpp\n// 加法拆成 XOR（不進位）+ 進位\n// a + b == (a ^ b) + ((a & b) << 1)\nint sum = (a ^ b) + ((a & b) << 1);  // 與 a + b 完全相等\n```\n:::\n\n\n\n:::example 第 k 位對「所有數對 XOR 總和」的貢獻\n再看「每一位獨立統計」的威力。假設要算一堆數兩兩 XOR 的總和，只要看第 k 位：設這一位是 1 的數有 `c` 個、是 0 的有 `n - c` 個，那麼第 k 位在所有數對裡產生 1 的次數就是 `c * (n - c)`（一個 1 配一個 0，XOR 才是 1）。\n\n```cpp\n// 第 k 位對「所有數對 XOR 總和」的貢獻\nlong long ones = c;                                  // 第 k 位是 1 的個數\nlong long contrib = ones * (n - ones) * (1LL << k);  // 配對數 × 該位權值\n```\n:::\n\n\n\n把每一位的 `contrib` 加起來就是答案，複雜度只有 `O(n log U)`（U 是數值上限），遠快過 `O(n^2)`。\n\n## 狀態／資料結構定義\n\n**逐位統計**：對每個位 `k`，數出 `ones[k]` = 第 `k` 位為 1 的元素個數，`zeros = n - ones[k]`。\n\n各運算在第 `k` 位的配對數：\n\n| 目標 | 該位貢獻的配對數 |\n| --- | --- |\n| XOR 為 1 的對數 | `ones * zeros` |\n| AND 為 1 的對數 | `C(ones, 2)` |\n| OR 為 1 的對數 | `C(n, 2) - C(zeros, 2)` |\n\n把配對數乘上 `1LL << k` 再加總，就是所有數對的總和。若最終要的是 XOR（如 1835），則看該位的配對數是奇數還是偶數來決定該位是 1 還是 0。\n\n**常用恆等式**：\n\n- `a + b = (a ^ b) + ((a & b) << 1)`\n- `a + b = (a | b) + (a & b)`\n- `a ^ b = (a | b) - (a & b)`\n- `a ^ (a & b) = a & ~b`（差集）\n\n配對數用 `long long`，位移一律 `1LL << k`。\n\n## 不變量或正確性證明\n\n**逐位獨立性**：XOR、AND、OR 都定義為「對每一位分別套用一個布林函數」。因此對任意 `k`，`(a op b)` 的第 `k` 位 = `f(a 的第 k 位, b 的第 k 位)`，與其他位無關。\n\n由此得到**可加性**：若目標量能寫成 `Σ_對 value(對)`，而 `value` 又能寫成 `Σ_k 2^k × [該對在第 k 位為 1]`，則交換求和次序得\n\n`Σ_對 Σ_k 2^k × […] = Σ_k 2^k × (第 k 位為 1 的對數)`\n\n右邊每一項只需一次線性掃描，總共 `O(n log U)`。這個交換次序就是整個技巧的正確性所在。\n\n**`a + b = (a ^ b) + ((a & b) << 1)` 的證明**：把加法拆成「不含進位的逐位相加」與「進位」。`a ^ b` 正是不進位的逐位模 2 加法；`a & b` 標出兩者都是 1 的位，這些位會產生進位，進位要往左移一格，所以是 `(a & b) << 1`。兩者相加恰好還原完整的加法——這也是硬體全加器的結構。\n\n**為什麼加法本身不能逐位獨立統計**：因為進位讓第 `k` 位的結果依賴低位。要處理含 `+` 的式子，就得先用上面的恆等式把它轉成位運算，或改用其他方法。\n\n## 逐步演算法\n\n**1. 先決定「逐位掃描」還是「整數恆等式」。** 若答案是統計量（總和、計數），通常逐位統計；若是化簡式子，套用恆等式直接改寫。\n\n:::example 統計第 k 位上有幾個數是 1\n**2. 逐位統計：對每一位數出 1 的個數。** 固定一個位 k，掃過所有數，看這一位是 0 還是 1。\n\n```cpp\n// 統計第 k 位上有幾個數是 1\nint cnt = 0;\nfor (int x : nums) {\n    if (x >> k & 1) {\n        ++cnt;  // 取出 x 的第 k 位\n    }\n}\n```\n:::\n\n\n\n:::example 把各位貢獻乘權值累加（以某個 per-bit 公式為例）\n**3. 寫出「這一位對答案的公式」，乘上權值。** 不同運算公式不同：XOR 看「1 與 0 配對數」，AND 看「兩邊都是 1」，OR 看「至少一邊是 1」。把公式結果乘上 `1 << k` 再累加。\n\n```cpp\n// 把各位貢獻乘權值累加（以某個 per-bit 公式為例）\nlong long ans = 0;\nfor (int k = 0; k < 30; ++k) {\n    long long c = countOnes(nums, k);\n    ans += perBit(c, nums.size()) * (1LL << k);\n}\n```\n:::\n\n\n\n如 1835「所有數對按位與結果的異或和」就是先把每位的 AND 配對數算出來，再判斷該位在最終 XOR 中是 0 還是 1。\n\n**4. 邊界與複雜度。**\n\n- 位移與相乘可能爆 `int`，配對數用 `long long`，`1LL << k` 別寫成 `1 << k`。\n- 含負數或要碰第 31／63 位時，改用 `unsigned` 或 `long long`，避免符號位作怪。\n- 複雜度通常是 `O(n log U)`：掃 `log U` 個位，每位掃 n 個數。\n\n## C++17 模板\n\n以 1835. 所有數對按位與結果的異或和 為例。\n\n**完整題目**：給兩個陣列 `arr1`、`arr2`，對每一對 `(i, j)` 算 `arr1[i] & arr2[j]`，回傳所有結果的 XOR。\n\n**線索**：逐位獨立 ⟹ 第 `k` 位的答案只取決於「有多少對的 AND 在第 `k` 位是 1」是奇是偶。而該位為 1 的對數 = `arr1` 中該位為 1 的個數 × `arr2` 中該位為 1 的個數。\n\n```cpp\n// 1835. 所有數對按位與結果的異或和：逐位統計配對數的奇偶\nclass Solution {\npublic:\n    int getXORSum(vector<int>& arr1, vector<int>& arr2) {\n        int ans = 0;\n        for (int k = 0; k < 30; ++k) {\n            long long c1 = 0, c2 = 0;\n            for (int x : arr1) c1 += (x >> k) & 1;\n            for (int y : arr2) c2 += (y >> k) & 1;\n            // 該位為 1 的對數是 c1 * c2；XOR 只看奇偶\n            if ((c1 * c2) & 1) ans |= 1 << k;\n        }\n        return ans;\n    }\n};\n```\n\n這題其實還有更漂亮的恆等式解：`(a1 & b1) ^ (a1 & b2) ^ … = (a1 ^ a2 ^ …) & (b1 ^ b2 ^ …)`，因為 AND 對 XOR 有分配律。兩行就解完——值得記住，但逐位統計的框架適用範圍更廣。\n\n## 時間與空間複雜度\n\n**時間 `O(n log U)`**：掃 `log U` 個位（30 或 60），每位掃一次陣列 `O(n)`。`n = 1e5`、30 位約 `3e6`。\n\n對比暴力的 `O(n^2)`。1835 用分配律的恆等式解甚至是 `O(n)`。\n\n**空間 `O(1)`**：只需要每位的計數變數。\n\n**溢位**：`c1 * c2` 在 `n = 1e5` 時可達 `1e10`，必須 `long long`。配對數乘上 `1LL << k` 更大。位移寫成 `1 << k` 在 `k >= 31` 是未定義行為。\n\n**負數與最高位**：題目含負數，或要碰第 31／63 位時，改用 `unsigned` 或 `long long`，避免符號位在右移時作怪。\n\n**先找恆等式再寫迴圈**：像 1835 的分配律、2354 用 popcount 把 `a | b` 的位元數條件轉成可統計的形式——花兩分鐘找恆等式，常常能把 `O(n log U)` 再降到 `O(n)`。\n\n## 常見錯誤與邊界條件\n\n- **對含進位的加法硬拆位**。第 `k` 位受低位影響，不可加。先用 `a + b = (a ^ b) + ((a & b) << 1)` 轉成位運算。\n- **`1 << k` 溢位**。`k >= 31` 要寫 `1LL << k`，且累加器是 `long long`。\n- **配對數溢位**。`ones * zeros`、`c1 * c2` 在 `n = 1e5` 時是 `1e10` 級，必須 `long long`。這是最常見的 WA。\n- **位數上限抓錯**。`nums[i] <= 1e9` 需要 30 位；到 `1e18` 需要 60 位。少掃一位就漏了最高位的貢獻。\n- **求「總和」與求「XOR」混淆**。總和是 `Σ_k 2^k × 配對數`；XOR 是「配對數為奇數則該位為 1」。兩者的收尾完全不同。\n- **`C(c, 2)` 寫成 `c * c / 2`**。應該是 `c * (c - 1) / 2`。\n- **有號右移**。`(x >> k) & 1` 對負數雖有 `& 1` 保護，但若判斷條件寫成 `x >> k` 就會出錯。含負數改用 `unsigned`。\n- 邊界測資：`n = 1`、全 0、全 1、含最大值。\n\n## 與相似技巧的比較\n\n- **對比進位制拆位**（4.2 節）：同一個「換軸」思想，只是這裡的進位制固定是二進位，而且靠的是位運算天生的逐位獨立性。判準都是「各位是否獨立」。\n- **對比 LogTrick**：LogTrick 保留完整的值處理子陣列；本節打散成獨立的位處理數對／子集。看到「所有子陣列」想 LogTrick，看到「所有數對」想拆位。\n- **對比直接找恆等式**：1835 用 AND 對 XOR 的分配律兩行解完，比逐位統計還快。動手前先花兩分鐘想有沒有恆等式——這是本節標題的用意。\n- **對比 popcount 相關技巧**：2354 把「`a | b` 的位元數 >= k」轉成對 popcount 的統計，是恆等式思路的變體——先把條件改寫成可統計的形式，再統計。\n- **對比線性基**：線性基管理「子集 XOR 的可達集合」，是 GF(2) 上的線性代數；本節是逐位的計數統計。前者回答可達性與極值，後者回答總和與計數。\n\n## 代表例題\n\n**題號**：1835、2354\n\n要拆成位元看；練習判斷 XOR/AND/OR 的每一位能否獨立統計或貪心決策。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1835 | [所有數對按位與結果的異或和](https://leetcode.cn/problems/find-xor-sum-of-all-pairs-bitwise-and) | 1825 |\n| 2354 | [優質數對的數目](https://leetcode.cn/problems/number-of-excellent-pairs) | 2076 |\n\n## 本節重點速查\n\n- XOR/AND/OR 都是**逐位元運算**——第 `k` 位的結果只由各數第 `k` 位決定。\n- 因此可以交換求和次序：`Σ_對 Σ_位` ⟹ `Σ_位 Σ_對`，`O(n^2)` 降到 `O(n log U)`。\n- 各位配對數：XOR 為 1 是 `ones × zeros`、AND 為 1 是 `C(ones,2)`、OR 為 1 是 `C(n,2) − C(zeros,2)`。\n- 求**總和**乘 `1LL << k` 加總； 求 **XOR** 看配對數奇偶。\n- **加法有進位所以不可逐位獨立**，先用 `a + b = (a ^ b) + ((a & b) << 1)` 轉換。\n- 動手前先找恆等式——1835 用 AND 對 XOR 的分配律兩行就解完。\n- 配對數與位移一律 `long long` / `1LL << k`。\n",
    },
    {
      id: 13,
      title: "7. 線性基",
      summary:
        "## 這個技術解決什麼問題\n\n從一堆數裡**任意挑子集做 XOR**，問「能湊出的最大值是多少」「某個值能不能被湊出」「有幾種不同的結果」。\n\n列舉所有子集是 `2^n` 種，完全不可行。線性基把「所有子集能湊出的值」壓縮成一個大小最多 `log U` 的陣列——原本 `2^n` 個組合的資訊，全部裝進 30 或 60 格。\n\n之所以能這樣壓縮，是因為 XOR 在數學上就是 **GF(2)（模 2 的體）上的向量加法**：把每個整數看成一個 30 維的 0/1 向量，「挑子集做 XOR」就是「線性組合」。線性基就是這個向量空間的一組基底。\n\n## 辨識題型的訊號\n\n看到這些特徵，就往「線性基」想：\n\n- **運算子是 XOR，而且要從一堆數裡「挑子集」湊出某個目標。** 例如最大 XOR、能否湊出某值、有幾種不同的 XOR 結果。\n- **暴力是列舉 `2^n` 個子集，數量太多。** 線性基把「所有子集能湊出的值」壓縮成一個小陣列。\n- **題目問的是「可達性」或「極值」而非具體哪幾個數**——線性基只在乎能湊出哪些值，正好對味。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要組合的運算是 XOR，而且可以自由挑子集嗎？\n2. 插入一個數時，它被消到 0 還是成為新代表，我判斷得出來嗎？\n3. 求最大值時，為什麼「從高位往低位貪心」一定最優？\n\n三題都答得出來，就套對了。\n\n## 核心想法與直覺\n\n本節要解決的問題是：給你一堆數，從中**任意挑幾個做 XOR**，問「能湊出的最大 XOR 值是多少」或「某個值能不能被湊出來」。直接列舉所有子集是 `2^n` 種，完全不可行。「線性基（linear basis）」是專門對付這類「XOR 子集」問題的工具。\n\n先建立直覺。XOR 有個很像「加法」的性質：一個數重複 XOR 兩次會抵消（`a ^ a = 0`），而且可交換、可結合。所以「挑一些數做 XOR」就很像在做向量的「加減組合」。線性基的目標是：**從原本一堆數裡，提煉出一組「最精簡的代表」，這組代表能 XOR 出的值，跟原本整堆能 XOR 出的值完全一樣。**\n\n:::example 線性基的精髓：每個最高位只留一個代表\n這組代表怎麼存？用一個陣列 `b`，約定 `b[i]` 是一個「最高位剛好在第 i 位」的數。插入一個新數 x 時，從高位往低位看：\n\n```cpp\n// 線性基的精髓：每個最高位只留一個代表\n// 若第 i 位已有代表，就用它消掉 x 的第 i 位\nfor (int i = HIGH; i >= 0; i--) {\n    if (!(x >> i & 1)) {\n        continue;  // x 這一位是 0，跳過\n    }\n    if (b[i]) {\n        x ^= b[i];  // 已有代表 → 消去這一位\n    } else {\n        b[i] = x;\n        break;\n    }  // 沒代表 → x 成為這位的新代表\n}\n```\n:::\n\n\n\n如果 x 一路被消到變成 0，代表它「能被現有代表 XOR 出來」，是多餘的，不必存。最後 `b` 裡非零的元素個數，就是這堆數真正的「自由度」。\n\n## 狀態／資料結構定義\n\n**基底陣列** `basis[b]`（`b = 0 .. BITS-1`）：`basis[b]` 是一個最高位恰為第 `b` 位的代表元，`0` 表示這一位還沒有代表。大小固定 `BITS`（`int` 用 31，`long long` 用 63）。\n\n**插入** `insert(x)`：從高位往低位掃，遇到 `x` 的第 `b` 位是 1 時——若 `basis[b]` 為空就把 `x` 放進去並結束（`x` 成為新代表，基底的秩加一）；否則 `x ^= basis[b]`（用既有代表消去這一位）繼續往下。若 `x` 最後被消成 0，代表它能由既有基底組出，不提供新資訊。\n\n**求最大值**：`ans = 0`，從高位往低位，若 `ans ^ basis[b] > ans` 就 `ans ^= basis[b]`。\n\n**查詢可達性**：把目標值走一次插入流程，被消成 0 就代表可湊出。\n\n**不同結果的個數**：`2^rank`（`rank` 是非空的 `basis[b]` 個數）。若原集合中有元素被消成 0（即存在線性相關），則 0 也可湊出。\n\n位數要涵蓋最大值——超過 `int` 就開 63 位並全程 `long long`。\n\n## 不變量或正確性證明\n\n**基底不變量**：任一時刻，`basis` 中非零元素的**張成空間**（所有子集 XOR 的集合）與已插入的所有元素的張成空間相同；且每個非零 `basis[b]` 的最高位恰為第 `b` 位，各不相同。\n\n**插入維持不變量**：`x ^= basis[b]` 是把 `x` 換成 `x ^ basis[b]`。因為 `basis[b]` 本身在張成空間裡，這個替換不改變「加入 `x` 後的張成空間」（`{x, S}` 與 `{x ^ s, S}` 張成相同，其中 `s ∈ span(S)`）。若 `x` 最後放進空位，最高位互異的性質也保持；若被消成 0，代表 `x ∈ span(basis)`，不加入是正確的。\n\n**最高位互異 ⟹ 線性獨立**：假設某個非空子集 XOR 為 0，取其中最高位最大的那個元素，它的最高位在其他元素中都不存在（因為最高位互異），無法被抵消，XOR 結果的該位必為 1，不可能是 0——矛盾。因此基底線性獨立，`rank` 就是它的大小，不同的子集 XOR 恰有 `2^rank` 個。\n\n**求最大值的貪心正確性**：從高位往低位，若當前 `ans` 的第 `b` 位是 0 而 `basis[b]` 存在，就異或上去把該位變成 1。這一步只會影響第 `b` 位及更低位（`basis[b]` 的最高位就是 `b`），而 `2^b` 大於所有低位之和，所以填 1 永遠不吃虧。這與「試填法」是同一套權重論證。\n\n**可達性判定的正確性**：目標值走完插入流程若被消成 0，表示它是基底的線性組合，即存在一個子集 XOR 等於它。反之若沒被消成 0，說明它在某一位上無法被表示，不可達。\n\n## 逐步演算法\n\n:::example 線性基容器：b[i] 的最高位是第 i 位\n**1. 狀態表示：一個 `b` 陣列當基底。** 長度取決於數值位數，例如 `long long` 開 63。\n\n```cpp\n// 線性基容器：b[i] 的最高位是第 i 位\nlong long b[63] = {0};\n```\n:::\n\n\n\n:::example 把 x 插入線性基（成功放入回傳 true）\n**2. 插入：把每個數逐位消去後嘗試放進基底。** 這就是上面那段迴圈，封裝成函式重複呼叫。\n\n```cpp\n// 把 x 插入線性基（成功放入回傳 true）\nbool insert(long long x) {\n    for (int i = 62; i >= 0; i--) {\n        if (!(x >> i & 1)) {\n            continue;\n        }\n        if (!b[i]) {\n            b[i] = x;\n            return true;\n        }\n        x ^= b[i];\n    }\n    return false;  // x 被完全消去，是多餘的\n}\n```\n:::\n\n\n\n:::example 求能 XOR 出的最大值\n**3. 答案讀取：求最大 XOR 用「高位貪心」。** 從高位到低位看每個代表，若拿它 XOR 能讓目前答案變大，就拿。因為每個最高位只有一個代表，這樣貪心一定最優。\n\n```cpp\n// 求能 XOR 出的最大值\nlong long queryMax() {\n    long long res = 0;\n    for (int i = 62; i >= 0; i--) {\n        if ((res ^ b[i]) > res) {\n            res ^= b[i];\n        }\n    }\n    return res;\n}\n```\n:::\n\n\n\n像 3681「子序列最大 XOR 值」就是把所有數插入線性基，再做一次高位貪心；3630 那種「XOR 與 AND 拆分」的難題，也常用線性基管理 XOR 那一側的可達集合。\n\n**4. 邊界與複雜度。** 每次插入是 `O(位數)`，n 個數共 `O(n × 位數)`；空間只要一個固定大小陣列。注意位數要涵蓋最大值（過 `int` 範圍就開 63 位、用 `long long`）。\n\n## C++17 模板\n\n以 3681. 子序列最大 XOR 值 為例（把所有數插入線性基，再做高位貪心）。\n\n```cpp\n// XOR 線性基：插入、求最大值、查詢可達性\nstruct LinearBasis {\n    static constexpr int BITS = 63;  // long long 用 63，int 用 31\n    long long basis[BITS] = {};\n    int rank = 0;\n\n    // 插入 x；回傳 true 表示它提供了新資訊（成為新的代表元）\n    bool insert(long long x) {\n        for (int b = BITS - 1; b >= 0; --b) {\n            if (!((x >> b) & 1)) continue;  // 這一位是 0，跳過\n            if (!basis[b]) {                // 這一位還沒有代表 → x 成為代表\n                basis[b] = x;\n                ++rank;\n                return true;\n            }\n            x ^= basis[b];                  // 用既有代表消去這一位\n        }\n        return false;  // 被消成 0：x 能由既有基底組出\n    }\n\n    // 所有子集 XOR 的最大值：高位優先貪心\n    long long maxXor() const {\n        long long ans = 0;\n        for (int b = BITS - 1; b >= 0; --b)\n            if ((ans ^ basis[b]) > ans) ans ^= basis[b];\n        return ans;\n    }\n\n    // target 能否由某個子集 XOR 湊出\n    bool canReach(long long target) const {\n        for (int b = BITS - 1; b >= 0; --b) {\n            if (!((target >> b) & 1)) continue;\n            if (!basis[b]) return false;\n            target ^= basis[b];\n        }\n        return target == 0;\n    }\n\n    // 不同的子集 XOR 結果個數（含 0）\n    long long distinctCount() const { return 1LL << rank; }\n};\n```\n\n## 時間與空間複雜度\n\n**插入 `O(BITS)`**：最壞從最高位掃到最低位。`n` 個數共 `O(n × BITS)`——`n = 1e5`、63 位約 `6e6`。\n\n**求最大值 `O(BITS)`**、**查詢可達性 `O(BITS)`**、**計數 `O(1)`**。\n\n**空間 `O(BITS)`**：一個固定大小陣列，與 `n` 無關。這是線性基最漂亮的性質——不論插入多少個數，儲存量都是常數。\n\n對比暴力列舉 `2^n` 個子集：`n = 60` 時是 `1e18`，完全不可行。\n\n**秩的上界是 `BITS`**：插入超過 `BITS` 個線性獨立的數是不可能的，所以第 `BITS + 1` 個之後的插入必定被消成 0（或替換）。這也意味著「不同的子集 XOR 結果」最多 `2^BITS` 個。\n\n**支援刪除嗎**：標準線性基不支援。要處理滑動視窗或帶刪除的情形，需要「帶時間戳的線性基」（每格額外記錄插入時間，插入時盡量保留較晚的元素），複雜度不變但實作較長。\n\n## 常見錯誤與邊界條件\n\n- **`BITS` 開太小**。數值到 `1e18` 卻只開 31 位，高位資訊直接遺失。用 `long long` 就開 63。\n- **位移沒用 `1LL`**。`(x >> b) & 1` 沒問題，但 `1 << b` 在 `b >= 31` 是未定義行為。\n- **把「被消成 0」當成錯誤**。那是正常且重要的情形——代表這個數不提供新資訊。它同時也告訴你「0 可以被非空子集湊出」，某些題目要用到這件事。\n- **求最大值時寫成無條件 `ans ^= basis[b]`**。必須判斷 `(ans ^ basis[b]) > ans`，否則會把已經是 1 的高位異或成 0。\n- **`rank` 沒維護**。計數 `2^rank` 需要它；插入成功時才 `++rank`。\n- **要求「非空子集」時忘記排除 0**。`distinctCount()` 含 0；若題目要求至少選一個且答案不能是 0，要另外判斷。\n- **誤以為線性基能回答「用了哪幾個數」**。它只保留可達集合，不保留組合方式。要還原具體子集得額外記錄每個基底元素由哪些原元素組成。\n- 邊界測資：空集合、單一元素、全部相同、含 0、全部線性相關。\n\n## 與相似技巧的比較\n\n- **對比試填法**（第 5 節）：線性基求最大值用的就是高位貪心，論證完全相同。差別在試填處理「挑固定個數（通常兩個）」，線性基處理「挑任意子集」。\n- **對比 0/1 Trie**：Trie 回答「與某個給定數 XOR 最大」，`O(BITS)`；線性基回答「任意子集 XOR 最大」。兩者常被混淆——問的是「一對數」還是「任意子集」，決定用哪一個。\n- **對比子集 DP**：`dp[mask]` 記錄可達的 XOR 值，`O(2^n)` 或 `O(n × U)`。線性基把空間從 `U` 壓到 `BITS`，是質的差別。\n- **對比高斯消去**：線性基就是 GF(2) 上的高斯消去，只是省去了顯式的矩陣。認出這層關係後，「秩」「線性獨立」「張成空間」這些詞都能直接套用。\n- **對比 XOR 前綴和**（第 2 節）：前綴處理的是「連續區間」的 XOR；線性基處理的是「任意子集」。前者用自反性，後者用線性代數。\n\n## 代表例題\n\n**題號**：3630、3681\n\n要拆成位元看；練習判斷 XOR/AND/OR 的每一位能否獨立統計或貪心決策。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 3630 | [劃分陣列得到最大異或運算和與運算之和](https://leetcode.cn/problems/partition-array-for-maximum-xor-and-and) | 2744 |\n| 3681 | [子序列最大 XOR 值](https://leetcode.cn/problems/maximum-xor-of-subsequences) | 2027 |\n\n**延伸閱讀**：[線性基影片講解](https://www.bilibili.com/video/BV1pm8vzAEXx/)，搭配本節 C++17 模板理解「消去、秩、最高位貪心」三件事。\n\n## 本節重點速查\n\n- XOR 就是 **GF(2) 上的向量加法**，所以「挑子集做 XOR」＝線性組合，而線性基就是那組基底——把 `2^n` 個組合壓成最多 `BITS` 格。\n- `basis[b]` 存「最高位恰為第 `b` 位」的代表元。\n- 插入時從高位掃：該位為 1 且 `basis[b]` 空就佔位（秩 +1），否則 `x ^= basis[b]` 繼續； 被消成 0 代表無新資訊。\n- **最高位互異 ⟹ 線性獨立**，所以不同結果恰有 `2^rank` 個。\n- 求最大值用高位貪心，但要判斷 `(ans ^ basis[b]) > ans` 才異或。\n- 查可達性就走一次插入流程，被消成 0 即可達。\n- `BITS` 要涵蓋值域（`long long` 開 63）。\n- 插入 `O(BITS)`、**空間與 `n` 無關**。\n- 標準線性基不支援刪除。\n",
    },
    {
      id: 14,
      title: "8. 思維題",
      summary:
        "## 這個技術解決什麼問題\n\n題目給的是位運算操作（XOR/AND/OR/翻轉），問「能否達成」或「最少步數」，但資料規模又不像要跑複雜演算法。\n\n這類題沒有模板，價值在於訓練一個習慣：**看到位運算先想性質，別急著模擬**。做法是找出一條**不變式（invariant）**——操作前後不變的量——再用它一步推到答案，常常一兩行就解決。\n\n810「黑板異或遊戲」的關鍵是「全部數的 XOR 是否為 0」在每次操作後的變化規律；2568「最小無法得到的或值」則發現答案只跟「2 的次方是否齊全」有關。\n\n## 辨識題型的訊號\n\n看到這些特徵，就往「位元思維題」想：\n\n- **題目給的是位運算操作（XOR/AND/OR/翻轉），問「能否達成」或「最少步數」**，但資料規模又不像要你跑複雜演算法。\n- **答案疑似是個簡潔結論（true/false、奇偶、某個計數）**，暗示背後有不變式可挖。\n- **暴力模擬狀態爆炸**，但你隱約覺得「某個量好像一直不變」——那就是該找不變式的時候（如 2568「最小無法得到的或值」其實只跟 2 的次方是否齊全有關）。\n\n確認自己有沒有套對，問三個問題：\n\n1. 每次操作後，有沒有某個位元量（XOR 總和、1 的個數、mask）始終不變？\n2. 我能不能用一條式子直接判斷「可行 / 不可行」，而不必模擬全過程？\n3. 邊界值（0、單一元素、全相同）會不會讓我的性質失效？\n\n三題都答得出來，就套對了。\n\n## 核心想法與直覺\n\n本節是位元運算裡的「思維題」：沒有固定模板，重點在於**先觀察題目的位元性質、找出一條不變的規律（invariant），再用它一步推到答案**，常常一兩行就解決。這類題的價值在於訓練你看到位運算時「先想性質，別急著模擬」。\n\n關鍵心法是熟記幾條位運算的小性質，並用它們把題目「看穿」。最常用的兩條：\n\n:::example x & (x-1)：清掉最低位的 1，可用來數 1 的個數\n第一，`x & (x - 1)` 會把 x 「最低位的那個 1」消掉。反覆做就能數出 x 有幾個 1（popcount），也能判斷 x 是不是 2 的次方。\n\n```cpp\n// x & (x-1)：清掉最低位的 1，可用來數 1 的個數\nint popcount(unsigned x) {\n    int c = 0;\n    while (x) {\n        x &= x - 1;\n        ++c;\n    }  // 每次消一個 1\n    return c;\n}\n```\n:::\n\n\n\n第二，OR 與 AND 有「單調」性質：把更多數 OR 起來，結果的 1 只會變多不會變少；AND 則只會變少不會變多。許多思維題就是抓住「某個量單調變化」這條不變式來推結論。\n\n:::example 2546：只看「有沒有 1」這個不變式\n舉 2546「執行逐位運算使字串相等」：操作能把兩個位改成 OR/OR，效果上「只要有一個 1，就能把 0 變 1」。於是不變式是：**有沒有 1 這件事不會改變**。結論立刻浮現——兩字串能變相等，當且僅當「兩者要嘛都全 0，要嘛都至少有一個 1」。\n\n```cpp\n// 2546：只看「有沒有 1」這個不變式\nbool ok(string& s, string& t) {\n    bool a = s.find('1') != string::npos;\n    bool b = t.find('1') != string::npos;\n    return a == b;  // 同時有 1，或同時全 0，才可行\n}\n```\n:::\n\n## 狀態／資料結構定義\n\n本節沒有固定資料結構，要找的是**量**。動手前逐一檢查這幾個候選，看哪個在操作前後不變或單調變化：\n\n- **XOR 總和**：`a[0] ^ a[1] ^ … ^ a[n-1]`。許多「配對消除」「取走兩數放回一數」的操作會保持它不變。\n- **1 的總數的奇偶**（popcount 的奇偶）：翻轉類操作常保持它。\n- **某個 mask 是否改變**：例如「所有數的 AND」「所有數的 OR」。\n- **元素個數的奇偶**：決定先手後手。\n\n**單調量**（不變式的近親）：OR 只增不減、AND 只減不增、popcount 在特定操作下單調——「某個量單調變化」同樣能拿來推結論。\n\n常用內建函式：`__builtin_popcount` / `__builtin_popcountll`、`__builtin_clz`（前導零，**不能對 0 呼叫**）、`__builtin_ctz`（尾隨零，同樣不能對 0 呼叫）。\n\n## 不變量或正確性證明\n\n本節的**全部內容就是找不變式並證明它**，所以這一節是重點而非附錄。標準流程：\n\n1. **寫下一次操作的完整效果**。哪些元素被移除、哪些被加入。\n2. **算候選量的變化**。以「取走兩數 `a`、`b`，放回 `a ^ b`」為例：XOR 總和從 `S` 變成 `S ^ a ^ b ^ (a ^ b) = S`——**不變**。\n3. **檢查終止狀態**。不變式在初始與終止狀態各是什麼值？若兩者矛盾，就證明了「達不到」。\n\n**810 黑板異或遊戲的論證**：先手在全體 XOR 為 0 時直接獲勝。若 XOR 不為 0 且 `n` 為偶數，可以證明先手必勝——反證：假設先手擦掉任何一個數都會讓自己輸（即擦掉後 XOR 為 0），則對所有 `i` 都有 `S ^ a[i] = 0`，即所有 `a[i]` 都等於 `S`。此時全體 XOR 是 `n` 個 `S` 的 XOR，`n` 為偶數時等於 0，與「`S != 0`」矛盾。因此必存在一個能安全擦掉的數。\n\n**2568 的論證**：一個值 `v` 能被某個子集 OR 出來，當且僅當 `v` 的每個位元都被某個元素覆蓋。最小的無法得到的值必是 2 的次方——因為若所有 `2^k`（`k = 0..m`）都能單獨湊出，它們的組合可以湊出所有 `< 2^(m+1)` 的值。所以只要找最小的、不在陣列中出現的 2 的次方。\n\n**單調量的用法**：OR 只增不減意味著「一旦某位變成 1 就回不去」，這常常直接給出「最少步數」的下界。\n\n## 逐步演算法\n\n**1. 先找不變式：哪個位元性質「操作前後不變」？** 試著問：每次操作後，XOR 總和、1 的總數、某個 mask 是否改變？不變的東西就是突破口。例如 810「黑板異或遊戲」的關鍵不變式是「全部數的 XOR 是否為 0」。\n\n:::example 810：先手是否必勝，只看整體 XOR 與個數奇偶\n**2. 用性質改寫條件，別硬模擬。** 把「能不能達成」轉成一條可直接判斷的式子。\n\n```cpp\n// 810：先手是否必勝，只看整體 XOR 與個數奇偶\nbool firstWins(vector<int>& nums) {\n    int x = 0;\n    for (int v : nums) {\n        x ^= v;\n    }\n    return x == 0 || (nums.size() % 2 == 0);\n}\n```\n:::\n\n\n\n:::example 1558：每個數的 1 個數要各自 +1，最大位長決定共享的 ×2 次數\n**3. 善用「最低位 1」與位移技巧。** 很多構造題（如 1558「最少函式呼叫次數」）要數每個數的二進位長度與 1 的個數：加法各自處理（看 1 的個數），乘以 2 是全體共享（看最高位）。\n\n```cpp\n// 1558：每個數的 1 個數要各自 +1，最大位長決定共享的 ×2 次數\nint ops = 0, maxBits = 0;\nfor (int v : nums) {\n    ops += __builtin_popcount(v);  // 各自 +1 的次數\n    maxBits = max(maxBits, 32 - __builtin_clz(v | 1));\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。** 思維題程式碼通常很短，但要小心：popcount 對 0 要正確、位長計算別對 0 取 `clz`（先 `| 1`）；數值大時用 `long long` 與 `__builtin_popcountll`。\n\n## C++17 模板\n\n思維題沒有共用模板，但流程可以固定下來。以 810. 黑板異或遊戲 為例。\n\n**完整題目**：黑板上有一些數，兩人輪流擦掉一個數。若某人擦完後所有剩餘數的 XOR 為 0，該人**輸**；若輪到某人時 XOR 已為 0，該人**贏**。先手是否必勝？\n\n```cpp\n// 810. 黑板異或遊戲：不變式一行解\nclass Solution {\npublic:\n    bool xorGame(vector<int>& nums) {\n        int total = 0;\n        for (int x : nums) total ^= x;\n        // 開局 XOR 已為 0 → 先手直接贏\n        // 否則 n 為偶數時，必存在一個能安全擦掉的數（見證明）→ 先手贏\n        return total == 0 || nums.size() % 2 == 0;\n    }\n};\n```\n\n**找不變式的檢查清單**（每題照跑一遍）：\n\n```cpp\n// 操作前後，下列哪個量不變或單調？\n// 1. XOR 總和            total ^= x\n// 2. popcount 的奇偶     __builtin_popcount(x) & 1\n// 3. 全體 AND / 全體 OR  （OR 只增、AND 只減）\n// 4. 元素個數的奇偶      n & 1\n// 5. 每一位上 1 的個數對 k 取餘\n```\n\n## 時間與空間複雜度\n\n**多半是 `O(n)` 或 `O(位數)`，空間 `O(1)`**。找到不變式之後，程式通常只是掃一遍算出那個量再判斷。\n\n810：`O(n)` 時間、`O(1)` 空間。\n2568：`O(n)` 時間（用一個 mask 記錄哪些 2 的次方出現過）、`O(1)` 空間。\n\n這正是思維題的特徵——**如果你寫出了複雜的搜尋或 DP，多半是還沒找到不變式**。看到題目資料範圍很大（`n <= 1e5`）但操作看起來要模擬指數多種狀態，就該回頭找性質。\n\n**內建函式的成本**：`__builtin_popcount` 在支援 POPCNT 的機器上是單一指令；`__builtin_clz` / `__builtin_ctz` 同樣是單指令，但**對 0 是未定義行為**，呼叫前要先判斷或 `| 1`。\n\n## 常見錯誤與邊界條件\n\n- **急著模擬**。狀態爆炸卻硬寫 BFS/DP，是本節最常見的失敗模式。先花五分鐘檢查不變式清單。\n- **不變式沒證明就用**。「看起來不變」和「證明不變」差很多。一定要寫下一次操作的完整效果再算變化量。\n- **`__builtin_clz(0)` / `__builtin_ctz(0)`**。未定義行為，實務上回傳垃圾值。先特判 0，或寫 `__builtin_clz(x | 1)`。\n- **`__builtin_popcount` 用在 `long long` 上**。要用 `__builtin_popcountll`，否則只算低 32 位。\n- **忽略 `n` 的奇偶**。博弈類不變式題幾乎都要分先後手，`n & 1` 常是答案的一部分。\n- **邊界值 0 與空集合**。popcount(0) = 0 是對的，但「所有數的 AND」在空集合上沒有定義，要看題目怎麼規定。\n- **數值大時型別沒換**。`long long` 配 `1LL <<` 與 `__builtin_popcountll`。\n- 驗證方式：寫一個小暴力（`n <= 10` 窮舉所有操作序列）跟自己的結論對拍，比盯著證明看有效得多。\n\n## 與相似技巧的比較\n\n- **對比其他各節**：本節沒有工具，只有方法。前幾節（LogTrick、拆位、線性基）是「知道招式就能套」；本節是「先看穿題目」。兩者互補——看穿之後往往發現根本不需要招式。\n- **對比博弈論（SG 函數）**：正規的公平組合遊戲用 SG 函數求解，但很多位運算博弈題有更簡單的不變式（如 810）。先找不變式，找不到再上 SG。\n- **對比數學歸納法**：不變式論證本質上就是歸納——證明「初始成立」與「每次操作後仍成立」。\n- **對比對拍驗證**：不變式猜對與否，寫個小暴力對拍最快。這是本節最實用的工具，比反覆推導可靠。\n- **對比 OEIS / 打表找規律**：資料範圍小時可以窮舉前幾項再找規律，往往能直接看出不變式的形式。\n\n## 代表例題\n\n**題號**：810、1558、2509、2546、2568\n\n要拆成位元看；練習判斷 XOR/AND/OR 的每一位能否獨立統計或貪心決策。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 810 | [黑板異或遊戲](https://leetcode.cn/problems/chalkboard-xor-game) |  |\n| 1558 | [得到目標陣列的最少函式呼叫次數](https://leetcode.cn/problems/minimum-numbers-of-function-calls-to-make-target-array) | 1637 |\n| 2509 | [查詢樹中環的長度](https://leetcode.cn/problems/cycle-length-queries-in-a-tree) | 1948 |\n| 2546 | [執行逐位運算使字串相等](https://leetcode.cn/problems/apply-bitwise-operations-to-make-strings-equal) | 1605 |\n| 2568 | [最小無法得到的或值](https://leetcode.cn/problems/minimum-impossible-or) | 1754 |\n| 2571 | [將整數減少到零需要的最少運算元](https://leetcode.cn/problems/minimum-operations-to-reduce-an-integer-to-0) | 1649 |\n| 2749 | [得到整數零需要執行的最少運算元](https://leetcode.cn/problems/minimum-operations-to-make-the-integer-zero) | 2132 |\n| 2835 | [使子序列的和等於目標的最少操作次數](https://leetcode.cn/problems/minimum-operations-to-form-subsequence-with-target-sum) | 2207 |\n| 2897 | [對陣列執行操作使平方和最大](https://leetcode.cn/problems/apply-operations-on-array-to-maximize-sum-of-squares) | 2301 |\n| 2939 | [最大異或乘積](https://leetcode.cn/problems/maximum-xor-product) | 2128 |\n| 3064 | [使用按位查詢猜測數字 I](https://leetcode.cn/problems/guess-the-number-using-bitwise-questions-i) |  |\n| 3094 | [使用按位查詢猜測數字 II](https://leetcode.cn/problems/guess-the-number-using-bitwise-questions-ii) |  |\n| 3315 | [構造最小位運算陣列 II](https://leetcode.cn/problems/construct-the-minimum-bitwise-array-ii) | 1715 |\n| 3644 | [排序排列](https://leetcode.cn/problems/maximum-k-to-sort-a-permutation) | 1775 |\n\n## 本節重點速查\n\n- 位運算思維題沒有模板，只有一個習慣：**先找不變式，別急著模擬**。\n- 流程是「寫下一次操作的完整效果 → 算候選量的變化 → 比對初始與終止狀態」。\n- 候選量清單：**XOR 總和**（取兩數放回其 XOR 時不變）、**popcount 的奇偶**、**全體 AND/OR**（OR 只增、AND 只減）、**元素個數的奇偶**。\n- 810 的結論是 `total == 0 || n % 2 == 0`，靠的是「若每個數都不能安全擦掉，則所有數相等，偶數個 XOR 為 0，矛盾」。\n- 若你寫出了複雜的搜尋，多半是還沒找到不變式。\n- `__builtin_clz` / `ctz` **不能對 0 呼叫**； `long long` 要用 `popcountll`。\n- 猜到不變式後寫小暴力對拍驗證，比盯著證明看有效。\n",
    },
    {
      id: 15,
      title: "9. 其他",
      summary:
        "## 這個技術解決什麼問題\n\n收錄幾道位運算經典題，每一道展示一個值得單獨記住的招式：\n\n- **落單元素**（136、137、260）：出現偶數次的用 XOR 抵消；出現 `k` 次的改成「逐位對 `k` 取餘」。\n- **範圍按位與**（201）：`[m, n]` 全部 AND 的結果，就是 `m` 與 `n` 的**公共二進位前綴**——不必真的逐個運算。\n- **格雷碼**（89）：`i ^ (i >> 1)` 直接生成「相鄰只差一個位元」的序列。\n\n共通心法仍是把整數當成一排位元，靠逐位性質把問題化簡成很小的計算。題目刻意限制 `O(1)` 空間，通常就是在暗示「請用位運算」。\n\n## 辨識題型的訊號\n\n看到這些特徵，就往「位元經典題」想：\n\n- **「除了一個，其餘都出現偶數／k 次，找那個落單的」** → XOR 抵消或逐位對 k 取餘。\n- **「對一段連續區間做 AND / OR」** → 先想公共前綴，而不是真的逐個運算。\n- **「相鄰元素只差一個位元」或「不重複走完所有狀態」** → 格雷碼 `i ^ (i >> 1)`。\n- **暴力用雜湊表計數或逐個列舉也能過，但題目刻意限制 `O(1)` 空間**——這通常是在暗示「請用位運算」。\n\n確認自己有沒有套對，問三個問題：\n\n1. 元素的「出現次數」有沒有規律（偶數、3 次、唯一）？這能不能用 XOR 或逐位取餘表達？\n2. 我是不是在對一個「連續範圍」做位運算？能否改成找前綴？\n3. 牽涉第 31／63 位時，我的型別與迴圈上限正確嗎？\n\n三題都答得出來，就套對了。\n\n## 核心想法與直覺\n\n本節收錄位元運算的幾道「經典題」，它們各自展示一個值得記住的招式。共通心法仍是：**把整數當成一排位元，靠 XOR / AND / OR 的逐位性質，把問題化簡成很小的計算。**\n\n:::example 136：全部 XOR，成對的互相消掉，剩下落單的\n最該記的一招是 XOR 的「成對抵消」：`a ^ a = 0`、`a ^ 0 = a`。所以一串數字全部 XOR 起來，凡是成對出現的都會消失，只剩落單的那個。這就是 136「只出現一次的數字」：\n\n```cpp\n// 136：全部 XOR，成對的互相消掉，剩下落單的\nint singleNumber(vector<int>& nums) {\n    int x = 0;\n    for (int v : nums) {\n        x ^= v;  // a^a=0，最後只剩出現一次的數\n    }\n    return x;\n}\n```\n:::\n\n\n\n:::example 137：每一位的 1 個數對 3 取餘，餘 1 就是答案的該位\n第二招是「逐位統計餘數」對付 137「只出現一次的數字 II」：每個數出現三次、只有一個出現一次。對每一位數出 1 的總數，再對 3 取餘——餘 1 的位就是答案的位。\n\n```cpp\n// 137：每一位的 1 個數對 3 取餘，餘 1 就是答案的該位\nint singleNumber2(vector<int>& nums) {\n    int ans = 0;\n    for (int i = 0; i < 32; ++i) {\n        int cnt = 0;\n        for (int v : nums) {\n            cnt += (v >> i & 1);\n        }\n        if (cnt % 3) {\n            ans |= 1 << i;\n        }\n    }\n    return ans;\n}\n```\n:::\n\n\n\n第三招是「找公共前綴」對付 201「數字範圍按位與」：把 `[m, n]` 全部 AND，只會剩下 m 與 n 相同的二進位高位前綴，低位因為範圍橫跨而被 AND 成 0。\n\n## 狀態／資料結構定義\n\n**XOR 抵消**（136）：一個累加變數，初值 0。\n\n**逐位對 k 取餘**（137，每個數出現三次除了一個）：對每個位 `b`，數出所有元素第 `b` 位為 1 的個數 `cnt`，則答案的第 `b` 位是 `cnt % 3`。用 `int ans` 累積 `((cnt % 3) << b)`。\n\n**分組 XOR**（260，恰有兩個落單）：先全體 XOR 得到 `s = a ^ b`（兩個落單數的 XOR），取 `lowbit = s & -s`（`a`、`b` 在這一位必定不同），再依這一位把所有元素分成兩組各自 XOR，兩組結果就是 `a` 與 `b`。\n\n**範圍 AND**（201）：把 `m`、`n` 同時右移直到相等，記錄移了幾位 `shift`，答案是 `m << shift`。等價寫法是重複 `n &= n - 1` 直到 `n <= m`。\n\n**格雷碼**（89）：`for (int i = 0; i < (1 << n); ++i) res.push_back(i ^ (i >> 1));`\n\n## 不變量或正確性證明\n\n**逐位對 k 取餘的正確性**（137）：設除了一個數出現一次外，其餘都出現三次。對任一位 `b`，所有元素第 `b` 位為 1 的總個數 = `3 × (出現三次且該位為 1 的數的個數) + (落單數該位是否為 1)`。對 3 取餘，前一項消失，剩下的就是落單數的第 `b` 位。逐位還原即得答案。\n\n這個論證對任意 `k` 都成立——這正是「XOR 只能處理偶數次」的推廣：XOR 是 `k = 2` 的特例。\n\n**分組 XOR 的正確性**（260）：設兩個落單數是 `a`、`b`。全體 XOR 後成對的都消掉，得 `s = a ^ b`。因為 `a != b`，`s != 0`，所以 `s` 至少有一位是 1；取最低的那一位 `lowbit`，代表 `a` 與 `b` 在該位不同。依該位分組後，`a` 與 `b` 必落在不同組；而任何成對出現的數，兩份的該位相同，必落在同一組因而互相抵消。所以兩組各自 XOR 的結果就是 `a` 與 `b`。\n\n**範圍 AND 等於公共前綴的證明**（201）：設 `m < n`，且它們的二進位在第 `b` 位首次不同（`m` 是 0、`n` 是 1）。則區間 `[m, n]` 必包含某個數，其第 `b` 位為 1 而更低位全為 0（即 `(m >> b << b) | (1 << b)`），也必包含某個數其第 `b` 位為 1 而更低位全為 1。更關鍵的是：對任何 `j <= b`，區間中同時存在第 `j` 位為 0 與為 1 的數，所以 AND 之後第 `j` 位必為 0。而高於 `b` 的位在 `[m, n]` 中全部相同，AND 後保留。因此結果恰為公共前綴。\n\n**格雷碼的正確性**（89）：要證 `g(i) = i ^ (i >> 1)` 與 `g(i+1)` 恰好差一個位元。設 `i` 的尾隨 1 有 `t` 個（即 `i` 的低 `t` 位是 1，第 `t` 位是 0）。從 `i` 到 `i+1`，低 `t` 位全部由 1 變 0，第 `t` 位由 0 變 1。代入 `g` 展開可驗證只有第 `t` 位改變。另外 `g` 是雙射（逆映射是前綴 XOR），所以 `2^n` 個值互不重複。\n\n## 逐步演算法\n\n**1. 先辨認題目屬於哪種經典套路。** 落單元素 → XOR 抵消；出現 k 次 → 逐位對 k 取餘；範圍 AND → 找公共前綴；相鄰只差一位 → 格雷碼。\n\n:::example 201：找 m 與 n 的公共二進位前綴\n**2. 範圍 AND：把 m、n 一起右移到相等，再移回去。** 不斷砍掉最低位差異，直到兩者相同。\n\n```cpp\n// 201：找 m 與 n 的公共二進位前綴\nint rangeAnd(int m, int n) {\n    int shift = 0;\n    while (m < n) {\n        m >>= 1;\n        n >>= 1;\n        ++shift;\n    }                   // 砍到相等\n    return m << shift;  // 補回低位的 0\n}\n```\n:::\n\n\n\n:::example 89：第 i 個格雷碼，相鄰只差一位\n**3. 格雷碼（89）：用 `i ^ (i >> 1)` 直接生成。** 格雷碼要求相鄰兩數只差一個位元，這條公式天然滿足。\n\n```cpp\n// 89：第 i 個格雷碼，相鄰只差一位\nvector<int> grayCode(int n) {\n    vector<int> res;\n    for (int i = 0; i < (1 << n); ++i) {\n        res.push_back(i ^ (i >> 1));  // 經典恆等式\n    }\n    return res;\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。** 逐位法是 `O(n × 位數)`；XOR 與範圍 AND 是 `O(n)`／`O(位數)`。注意第 31 位（符號位）：累加或位移時用 `int` 通常沒問題，但若數值可能達 `long long`，迴圈上限與型別都要改成 64 位。LCP 81「與非的謎題」這類則要先把 NAND 用 AND/NOT 展開，再逐位處理。\n\n## C++17 模板\n\n**137. 只出現一次的數字 II**（其餘出現三次）——逐位對 3 取餘：\n\n```cpp\n// 137. 只出現一次的數字 II：逐位統計對 3 取餘\nclass Solution {\npublic:\n    int singleNumber(vector<int>& nums) {\n        int ans = 0;\n        for (int b = 0; b < 32; ++b) {\n            int cnt = 0;\n            for (int x : nums) cnt += (x >> b) & 1;\n            if (cnt % 3) ans |= 1 << b;  // 出現三次的都被消掉，只剩落單的那一位\n        }\n        return ans;\n    }\n};\n```\n\n**260. 只出現一次的數字 III**（恰有兩個落單）——lowbit 分組：\n\n```cpp\n// 260. 只出現一次的數字 III：全體 XOR 後用 lowbit 分成兩組\nclass Solution {\npublic:\n    vector<int> singleNumber(vector<int>& nums) {\n        long long s = 0;\n        for (int x : nums) s ^= x;\n        int lowbit = (int)(s & -s);  // a 與 b 在這一位必定不同\n\n        int a = 0, b = 0;\n        for (int x : nums) {\n            if (x & lowbit) a ^= x;\n            else b ^= x;\n        }\n        return {a, b};\n    }\n};\n```\n\n**201. 數字範圍按位與**——找公共前綴：\n\n```cpp\n// 201. 數字範圍按位與：右移到相等，就是公共前綴\nclass Solution {\npublic:\n    int rangeBitwiseAnd(int left, int right) {\n        int shift = 0;\n        while (left < right) { left >>= 1; right >>= 1; ++shift; }\n        return left << shift;\n    }\n};\n```\n\n## 時間與空間複雜度\n\n- **136 落單元素（XOR）**：`O(n)` 時間、**`O(1)` 空間**。這正是題目要求 `O(1)` 空間時的唯一解法。\n- **137 逐位對 k 取餘**：`O(n × 32)` 時間、`O(1)` 空間。也有用兩個狀態變數模擬三進位計數器的 `O(n)` 版本，但可讀性差很多；逐位版的常數在 `n = 3e4` 時完全可以接受。\n- **260 分組 XOR**：`O(n)` 時間、`O(1)` 空間，掃兩遍。\n- **201 範圍 AND**：`O(log n)` 時間、`O(1)` 空間。暴力逐個 AND 是 `O(n - m)`，在 `n - m` 到 `2^31` 時完全不可行。\n- **89 格雷碼**：`O(2^n)` 時間（就是輸出大小）、`O(1)` 額外空間。\n\n**符號位的注意事項**：137 掃到第 31 位時，`cnt % 3` 若為 1 則 `ans |= 1 << 31`，在有號 `int` 上是實作定義行為。實務上多數編譯器會給出正確的負數結果，但嚴謹的寫法是用 `unsigned` 累積再轉回。若數值可能達 `long long`，迴圈上限與型別都要改成 64 位。\n\n## 常見錯誤與邊界條件\n\n- **136 的初值不是 0**。XOR 的單位元是 0，寫成別的值全盤皆錯。\n- **137 沒處理負數**。第 31 位是符號位；用 `unsigned` 累積，或確認編譯器行為。另外 `k` 不是 3 時要改成 `cnt % k`。\n- **260 的 `s & -s` 用 `int` 且 `s = INT_MIN`**。`-INT_MIN` 溢位，模板用 `long long` 存 `s` 再轉回。\n- **201 用暴力迴圈**。`[0, 2^31-1]` 逐個 AND 會跑到天荒地老。用右移法或 `n &= n - 1`。\n- **`__builtin_clz(0)` / `__builtin_ctz(0)`**。未定義行為。先特判或 `| 1`。\n- **`__builtin_popcount` 對 `long long`**。要用 `ll` 版本，否則只算低 32 位。\n- **格雷碼順序方向**。`i ^ (i >> 1)` 給的是標準二進位反射格雷碼；題目若指定不同的起點或方向，要自己調整。\n- **LCP 81「與非的謎題」這類**：先把 NAND 用 `~(a & b)` 展開，再逐位處理，別直接對整數硬湊。\n- 邊界測資：`n = 1`、全部相同、含 0、含負數、`m == n`（範圍 AND）。\n\n## 與相似技巧的比較\n\n- **對比雜湊表計數**：找落單元素用 `unordered_map` 最直觀，`O(n)` 時間但 `O(n)` 空間。題目限制 `O(1)` 空間時就只能用位運算——這個限制本身就是提示。\n- **對比排序後掃描**：`O(n log n)`，比 XOR 慢且要 `O(1)` 空間就得原地排序（破壞輸入）。\n- **對比第 2 節 XOR 性質**：136 是那一節的直接應用；137、260 則展示了自反性的**極限**——只對偶數次有效，其餘情形要換成逐位取餘或分組。\n- **對比第 6 節恆等式**：137 的逐位統計就是恆等式那一節的框架，只是統計目標從「配對數」換成「該位 1 的個數對 k 取餘」。\n- **對比第 8 節思維題**：201 其實也是一道思維題——關鍵在看出「答案是公共前綴」這條性質，而不是套任何模板。兩節的界線本來就模糊。\n\n## 代表例題\n\n**題號**：LCP 81、89、136、137、201\n\n要拆成位元看；練習判斷 XOR/AND/OR 的每一位能否獨立統計或貪心決策。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| LCP 81 | [與非的謎題](https://leetcode.cn/problems/ryfUiz) |  |\n| 89 | [格雷編碼](https://leetcode.cn/problems/gray-code) |  |\n| 136 | [只出現一次的數字](https://leetcode.cn/problems/single-number) |  |\n| 137 | [只出現一次的數字 II](https://leetcode.cn/problems/single-number-ii) |  |\n| 201 | [數字範圍按位與](https://leetcode.cn/problems/bitwise-and-of-numbers-range) |  |\n| 260 | [只出現一次的數字 III](https://leetcode.cn/problems/single-number-iii) |  |\n| 371 | [兩整數之和](https://leetcode.cn/problems/sum-of-two-integers) |  |\n| 390 | [消除遊戲](https://leetcode.cn/problems/elimination-game) |  |\n| 645 | [錯誤的集合](https://leetcode.cn/problems/set-mismatch) |  |\n| 751 | [IP 到 CIDR](https://leetcode.cn/problems/ip-to-cidr) |  |\n| 982 | [按位與為零的三元組](https://leetcode.cn/problems/triples-with-bitwise-and-equal-to-zero) | 2085 |\n| 1238 | [迴圈碼排列](https://leetcode.cn/problems/circular-permutation-in-binary-representation) | 1775 |\n| 1261 | [在受汙染的二叉樹中查詢元素](https://leetcode.cn/problems/find-elements-in-a-contaminated-binary-tree) | 1440 |\n| 1611 | [使整數變為 0 的最少操作次數](https://leetcode.cn/problems/minimum-one-bit-operations-to-make-integers-zero) | 2345 |\n| 1680 | [連線連續二進位制數字](https://leetcode.cn/problems/concatenation-of-consecutive-binary-numbers) | 1630 |\n| 2438 | [二的冪陣列中查詢範圍內的乘積](https://leetcode.cn/problems/range-product-queries-of-powers) | 1610 |\n| 2965 | [找出缺失和重複的數字](https://leetcode.cn/problems/find-missing-and-repeated-values) | 1245 |\n| 3141 | [最大漢明距離](https://leetcode.cn/problems/maximum-hamming-distances) |  |\n| 3307 | [找出第 K 個字元 II](https://leetcode.cn/problems/find-the-k-th-character-in-string-game-ii) | 2232 |\n| 3514 | [不同 XOR 三元組的數目 II](https://leetcode.cn/problems/number-of-unique-xor-triplets-ii) | 1884 |\n| 3595 | [一次或兩次](https://leetcode.cn/problems/once-twice) |  |\n| 3766 | [將數字變成二進位制迴文數的最少操作](https://leetcode.cn/problems/minimum-operations-to-make-binary-palindrome) | 1657 |\n| 3782 | [交替刪除操作後最後剩下的整數](https://leetcode.cn/problems/last-remaining-integer-after-alternating-deletion-operations) | 2074 |\n\n## 本節重點速查\n\n- 四個值得單獨記住的招式。\n- **落單元素**：出現偶數次用 XOR 一路抵消（`O(1)` 空間）； 出現 `k` 次改成「逐位數 1 的個數對 `k` 取餘」（137）； 恰有兩個落單就先全體 XOR 得 `s`，取 `s & -s` 當分組依據，兩組各自 XOR（260）。\n- **範圍按位與**（201）：`[m, n]` 全部 AND ＝ `m` 與 `n` 的**公共二進位前綴**，右移到相等再移回來，`O(log n)`； 因為低位在區間中 0/1 都出現過，AND 後必為 0。\n- **格雷碼**（89）：`i ^ (i >> 1)`，相鄰只差一位。\n- 注意 137 的第 31 位符號問題（用 `unsigned` 累積）、260 的 `s & -s` 在 `INT_MIN` 會溢位（用 `long long`）、`clz`/`ctz` 不能對 0 呼叫。\n- 題目限制 `O(1)` 空間就是在暗示用位運算。\n",
    },
    {
      id: 9005,
      title: "10. 常見面試模式",
      summary:
        "位元運算題在面試中常以「找唯一出現的數」「枚舉子集」「最大異或」等形式出現。關鍵是辨識題目背後的位元結構，善用 `^`、`&`、`|` 與 lowbit 等技巧把暴力做法降到線性或對數。\n\n| 模式 | 訊號 | 核心做法 | 代表題 |\n| --- | --- | --- | --- |\n| XOR 消去 | 其餘元素成對出現，找落單者 | 全部異或，成對者相消只剩答案，`O(n)` | [只出現一次的數字](https://leetcode.cn/problems/single-number) |\n| 缺失數 | `[0, n]` 中缺一個或多一個 | 把下標與值一起異或，缺失值自然浮現 | [丟失的數字](https://leetcode.cn/problems/missing-number) |\n| 子集枚舉 | 集合大小 $n \\le 20$，需遍歷所有子集 | 用位元遮罩 0..2^n-1 代表子集，按位判斷成員 | [子集](https://leetcode.cn/problems/subsets) |\n| 位元 Trie | 求兩數最大異或或異或上界 | 按位高到低建 0/1 字典樹，貪心走相反位 | [數組中兩個數的最大異或值](https://leetcode.cn/problems/maximum-xor-of-two-numbers-in-an-array) |\n| lowbit 技巧 | 需快速取出/清除最低位的 1 | `x & (-x)` 取最低位，`x & (x - 1)` 清最低位 | [位1的個數](https://leetcode.cn/problems/number-of-1-bits) |\n| 狀態壓縮 | 元素有限且需記錄「已選集合」狀態 | 用整數的每一位代表一個元素，配合 DP 轉移 | [優美的排列 II](https://leetcode.cn/problems/beautiful-arrangement-ii) |\n\n**小提醒**\n\n- 異或具交換律與結合律，且 `x ^ x == 0`、`x ^ 0 == x`，這是多數 XOR 題的核心。\n- 用 `x & (-x)` 取 lowbit 時注意以無號或足夠寬的型別運算，避免有號溢位的未定義行為。\n- 子集枚舉的子集（枚舉某遮罩的所有子遮罩）用 `for (int s = m; s; s = (s - 1) & m)`，總複雜度 `O(3^n)`。",
    },
  ],
} as TutorialData.Root;
