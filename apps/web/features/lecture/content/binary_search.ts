import type { TutorialData } from "@/types";

export const binarySearch = {
  id: 1,
  title: "二分搜尋（二分答案/最小化最大值/最大化最小值/第K小）",
  src: "https://leetcode.cn/circle/discuss/SqopEo/",
  last_update: "2026-02-24T14:23:02.465505+00:00",
  summary:
    "![二分題單](https://pic.leetcode.cn/1711713491-SoiQWc-t2.png)\n\n> 圖：閉區間二分迴圈結束時的左右指標位置（查詢第一個 8）\n\n**刷題建議**：初學者建議先刷難度 < 1700 分的題目，進階題目常結合資料結構、圖論等\n\n**本題單涵蓋**：\n- 基礎二分查詢（查詢元素、統計個數）\n- 二分答案（最小化最大值、最大化最小值、第 K 小）\n- 三分法與其他應用\n\n:::example 閉區間 [left, right] 二分模板\n**C++ 模板**：\n\n```cpp\n// 閉區間 [left, right] 二分模板\nint search(vector<int>& nums, int target) {\n    int left = 0, right = nums.size() - 1;  // 搜尋範圍 [left, right]\n    while (left <= right) {\n        int mid = left + (right - left) / 2;  // 防溢位寫法\n        if (nums[mid] == target) {\n            return mid;\n        } else if (nums[mid] < target) {\n            left = mid + 1;\n        } else {\n            right = mid - 1;\n        }\n    }\n    return -1;\n}\n```\n:::\n",
  children: [
    {
      id: 2,
      title: "1. 基礎",
      summary:
        "## 這個技術解決什麼問題\n\n在已排序的資料裡定位：某個值在哪、第一個 `>= target` 的位置在哪、插入點在哪。\n\n線性掃描是 `O(n)`。二分之所以能壓到 `O(log n)`，靠的是「有序」提供的一個強保證：**比較一次中點，就能確定答案不在哪一半**。每次比較丟掉一半候選，`log2(n)` 次之後只剩一個。\n\n真正該內化的不是「找某個值」，而是更一般的形式：**找出 `false false … false true true …` 這條分界線的位置**。查值只是「`nums[i] >= target`」這個條件的特例。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就往二分搜尋想：\n\n- **輸入是「已排序陣列」，或排序後仍保留意義。** LeetCode 704、34、744 都明說陣列有序。\n- **要找「位置」「邊界」「插入點」「第一個／最後一個滿足某條件的元素」。** 例如 744 找「比目標字母大的最小字母」、2529 在排好序的數列裡數正負數的個數，本質都是找分界下標。\n- **暴力是線性掃描 `O(n)`，但題目資料量大、或要呼叫很多次查詢，需要更快。** 二分把每次查詢壓到 `O(log n)`。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的搜尋區間是閉的 `[l, r]` 還是半開的 `[l, r)`？對應的迴圈條件和更新寫對了嗎？\n2. 我要找的是「等於某值」，還是「第一個滿足條件的位置」？判斷條件 `ok(mid)` 是什麼？\n3. 找不到時會回傳什麼？這個回傳值對題目（例如插入位置）有意義嗎？\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n本節要解決的場景是：在一個**已經排序好**的陣列裡，快速找到某個值的位置，或找到「滿足某條件的分界點」。最直覺的暴力做法是從頭一個一個比對，`O(n)`；但只要資料有序，我們就能每次砍掉一半，做到 `O(log n)`。\n\n:::example 最基本的二分：在有序陣列中找 target，找到回傳下標，否則回傳 -1\n核心想法是「猜中點，再決定往左還是往右」。看陣列正中間那個值：如果它已經太大，答案一定在左半邊，右半邊整個丟掉；反之丟掉左半邊。每比一次就排除一半，所以很快。\n\n```cpp\n// 最基本的二分：在有序陣列中找 target，找到回傳下標，否則回傳 -1\nint l = 0, r = (int)a.size() - 1;  // 閉區間 [l, r]，答案可能落在這裡面\nwhile (l <= r) {\n    int mid = l + (r - l) / 2;  // 取中點（這樣寫可避免相加溢位）\n    if (a[mid] == target) {\n        return mid;\n    } else if (a[mid] < target) {\n        l = mid + 1;  // 太小，答案在右半\n    } else {\n        r = mid - 1;  // 太大，答案在左半\n    }\n}\nreturn -1;\n```\n:::\n\n\n\n這裡的關鍵叫「不變式」：迴圈裡永遠保證「若答案存在，它一定還在 `[l, r]` 之內」。每一步我們只丟掉「確定不可能」的那一半，所以不變式不會被破壞，迴圈結束時就能下結論。\n\n:::example 找第一個 >= target 的下標（lower_bound）\n更進階一點，二分常用來找「第一個滿足條件的位置」，例如第一個 `>= target` 的元素（這正是 LeetCode 34、35 的核心）。這時不是找「相等」，而是找「真假分界」。\n\n```cpp\n// 找第一個 >= target 的下標（lower_bound）\nint l = 0, r = (int)a.size();  // 注意：r 取到 size()，半開區間 [l, r)\nwhile (l < r) {\n    int mid = l + (r - l) / 2;\n    if (a[mid] >= target) {\n        r = mid;  // mid 可能就是答案，保留它\n    } else {\n        l = mid + 1;  // mid 太小，答案在更右邊\n    }\n}\nreturn l;  // l 即第一個 >= target 的位置\n```\n:::\n\n## 狀態／資料結構定義\n\n**搜尋區間**：整份程式只圍繞一件事——「答案還可能在哪個區間裡」。兩種寫法，選一種並固定：\n\n- **閉區間 `[l, r]`**：`l = 0`、`r = n - 1`，迴圈條件 `while (l <= r)`，更新用 `l = mid + 1` / `r = mid - 1`。\n- **半開區間 `[l, r)`**：`l = 0`、`r = n`，迴圈條件 `while (l < r)`，更新用 `l = mid + 1` / `r = mid`。\n\n**判定函數** `ok(mid)`：把題目翻譯成一個布林條件，且必須滿足「一旦為真，右邊全為真」。找第一個 `>= target` 時 `ok(i)` 就是 `nums[i] >= target`。\n\n**中點**：`mid = l + (r - l) / 2`。寫成 `(l + r) / 2` 在 `l`、`r` 都接近 `INT_MAX` 時會溢位。\n\n**回傳值**：半開寫法收斂後 `l == r`，就是分界位置；可能等於 `n`，代表沒有任何元素滿足條件——LeetCode 35 求插入位置正是直接用這個值。\n\n## 不變量或正確性證明\n\n**主不變量**：迴圈的每一輪開始時，「若答案存在，它必定落在目前的搜尋區間內」。\n\n以半開區間找「第一個滿足 `ok` 的位置」為例，更精確的不變量是：\n\n- `[0, l)` 之內的位置全部 `ok` 為假；\n- `[r, n)` 之內的位置全部 `ok` 為真（或 `r == n`，代表右邊還沒有已知為真的）。\n\n**維持**：算出 `mid`。若 `ok(mid)` 為真，答案不會在 `mid` 右邊，令 `r = mid`——`[r, n)` 仍全真。若為假，由單調性 `mid` 及其左邊全假，令 `l = mid + 1`——`[0, l)` 仍全假。兩種情形不變量都保住。\n\n**終止**：每輪區間長度至少減 1，且因為 `mid < r` 恆成立（`mid` 向下取整），不會卡住。收斂到 `l == r` 時，由不變量，`l` 左邊全假、`l` 右邊（含 `l`）全真，`l` 正是分界。\n\n**單調性是前提**：`ok` 必須是 `F…F T…T` 的形狀。若真假交錯，「丟掉一半」的推論就不成立，二分會回傳一個無意義的位置。動手前先確認這件事，比背模板重要得多。\n\n## 逐步演算法\n\n**1. 確認資料有序、目標明確。** 二分的前提是「有序」或「有單調的真假分界」。先問自己：我要找的是「某個值」，還是「第一個／最後一個滿足某條件的位置」？\n\n:::example 把判斷條件抽成一句話：mid 是否「達標」\n**2. 決定區間寫法並固定到底。** 兩種常見寫法：閉區間 `[l, r]`（用 `l <= r`、`r = mid - 1`）或半開區間 `[l, r)`（用 `l < r`、`r = mid`）。挑一種、寫熟，不要混用。\n\n```cpp\n// 把判斷條件抽成一句話：mid 是否「達標」\nbool ok(int mid) { return a[mid] >= target; }  // 達標=值夠大\n```\n:::\n\n\n\n:::example 半開區間找分界的骨架\n**3. 中點與收斂。** 中點一律寫 `mid = l + (r - l) / 2` 防溢位；每次依比較結果，把「不可能含答案」的半邊移除，確保區間嚴格變小，迴圈才會結束。\n\n```cpp\n// 半開區間找分界的骨架\nwhile (l < r) {\n    int mid = l + (r - l) / 2;\n    if (ok(mid)) {\n        r = mid;  // 達標 → 答案可能是 mid 或更左\n    } else {\n        l = mid + 1;  // 不達標 → 答案在右邊\n    }\n}\n// 結束時 l == r，即第一個達標的位置\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 「找不到」要處理：lower_bound 可能回傳 `size()`，代表沒有任何元素 `>= target`（LeetCode 35 插入位置就靠這個）。\n- 「第一個 `> target`」減「第一個 `>= target`」就是 target 出現的區間（LeetCode 34 找左右邊界的技巧）。\n- 複雜度固定 `O(log n)`，因為每次區間長度減半。\n\n## C++17 模板\n\n以下用本節代表題 704. 二分查詢 展示核心寫法。\n\n**完整題目**：給定升序整數陣列 `nums` 與目標值 `target`，若 `target` 存在則回傳下標，否則回傳 `-1`。\n\n:::example 標準二分搜尋：在有序陣列中尋找 target，找到回傳下標，否則回傳 -1\n**線索**：題目提供有序資料或明確的真假分界，答案落在第一個/最後一個滿足條件的位置，這是二分的主要線索。\n\n```cpp\nclass Solution {\npublic:\n    // 標準二分搜尋：在有序陣列中尋找 target，找到回傳下標，否則回傳 -1\n    int search(vector<int>& nums, int target) {\n            int l = 0, r = (int)nums.size() - 1;  // 閉區間 [l, r]\n            while (l <= r) {\n                    int mid = l + (r - l) / 2;  // 取中點，避免 l+r 溢位\n                    if (nums[mid] == target) {\n                            return mid;\n                    }\n                    if (nums[mid] < target) {\n                            l = mid + 1;  // 目標在右半段\n                    } else {\n                            r = mid - 1;  // 目標在左半段\n                    }\n            }\n            return -1;\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- 用閉區間維護候選答案；每次比較後刪掉必不可能含答案的一半。若改成二分答案，`target` 會替換成 `check(mid)` 的可行性。\n- `l` 與 `r` 表示仍可能包含答案的範圍，`mid` 只是用來判斷要丟掉哪一半。\n- 每個分支都要能說明被排除的區間為何不可能含答案；這就是二分不變式。\n\n## 時間與空間複雜度\n\n**時間 `O(log n)`**：每輪區間長度減半，最多 `⌈log2(n)⌉` 輪。`n = 1e9` 也只要約 30 次比較。\n\n**空間 `O(1)`**：只用了 `l`、`r`、`mid` 三個變數。遞迴寫法會多 `O(log n)` 的呼叫堆疊，但沒有好處，迭代版更短也更快。\n\n**適用前提的代價**：若資料未排序，先排序是 `O(n log n)`——只查一次的話還不如直接線性掃描的 `O(n)`。二分划算的場景是「排序一次、查詢很多次」，或資料本來就有序。\n\n**34 找左右邊界**：跑兩次 `lower_bound`（第一個 `>= target` 與第一個 `> target`），仍是 `O(log n)`。\n\n## 常見錯誤與邊界條件\n\n- 固定一種區間寫法，不要在同一題混用。\n- `mid = l + (r - l) / 2` 避免溢位。\n- 重複元素題要先決定答案偏左或偏右。\n- 用全小、全大、單元素測資檢查邊界。\n\n## 常見變形\n\n- 找第一個 >= x / 第一個 > x。\n- 在旋轉陣列、山脈陣列或分段單調結構中先判定落在哪一段。\n- 浮點二分與三分法處理連續答案。\n\n## 與相似技巧的比較\n\n- **對比線性掃描**：線性不要求有序，`O(n)`；二分要求有序，`O(log n)`。單次查詢且資料無序時線性反而更好。\n- **對比 `std::lower_bound` / `upper_bound`**：標準庫已經實作了本節的兩個模板，`lower_bound` 是「第一個 `>= target`」，`upper_bound` 是「第一個 `> target`」。實務上優先用它們；手寫的價值在於判定條件不是簡單比大小時（那就是下一節的主題）。\n- **對比雜湊表**：查「某值存不存在」用 `unordered_set` 是 `O(1)`，比二分快。但雜湊表答不出「第一個 `>= target` 是誰」這種順序性問題。\n- **對比有序集合**（`std::set` / `map`）：支援動態插入刪除且仍可查前驅後繼，每次 `O(log n)`。資料會變動就用它；靜態資料用陣列 + 二分，常數小得多。\n- **對比二分答案**：本節在「下標」上二分，後續各節在「答案的值域」上二分。骨架相同，差別只在區間裡裝的是什麼。\n\n## 代表例題\n\n**題號**：34、35、704、744、2529\n\n重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。\n\n- 34 在排序陣列中查詢元素的第一個和最後一個位置：練習在重複元素中找左右邊界；核心不是找到任一 target，而是分別找第一個 `>= target` 與第一個 `> target`。\n- 35 搜尋插入位置：把插入位置視為第一個 `>= target` 的下標，統一處理存在與不存在兩種情況。\n- 704 二分查詢：確認閉區間二分的不變式：`[l, r]` 內仍可能有答案，每次比較後刪掉不可能的一半。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 34 | [在排序陣列中查詢元素的第一個和最後一個位置](https://leetcode.cn/problems/find-first-and-last-position-of-element-in-sorted-array) |  |\n| 35 | [搜尋插入位置](https://leetcode.cn/problems/search-insert-position) |  |\n| 704 | [二分查詢](https://leetcode.cn/problems/binary-search) |  |\n| 744 | [尋找比目標字母大的最小字母](https://leetcode.cn/problems/find-smallest-letter-greater-than-target) |  |\n| 2529 | [正整數和負整數的最大計數](https://leetcode.cn/problems/maximum-count-of-positive-integer-and-negative-integer) | 1196 |\n\n## 本節重點速查\n\n二分的本質是找 `F…F T…T` 的分界，查值只是特例。**先確認單調性**再套模板。閉區間 `[l, r]` 配 `while (l <= r)`、半開 `[l, r)` 配 `while (l < r)`——選一種並在同一題裡固定，混用是最大的錯誤來源。`mid = l + (r - l) / 2` 防溢位。半開寫法收斂後 `l` 就是答案位置，可能等於 `n`（代表無解，LeetCode 35 直接拿它當插入點）。「第一個 `> target`」減「第一個 `>= target`」＝ target 的出現次數（LeetCode 34）。實務上直接用 `lower_bound` / `upper_bound`。用全小、全大、單元素三種測資驗邊界。\n",
    },
    {
      id: 3,
      title: "2. 進階",
      summary:
        "## 這個技術解決什麼問題\n\n題目沒有直接給你一個有序陣列叫你查值，而是要你**自己找出那條單調的真假分界**。\n\n這是從「二分下標」跨到「二分答案」的關鍵一步。基礎二分像查字典——序已經排好，照著查即可；進階二分像偵探——得先把問題翻譯成一句「當 `x` 越大，某件事越容易（或越難）成立」，分界線才會浮現。\n\n另一類用法是把二分當**定位工具**：先用 `lower_bound` 框出一段候選範圍（如 658 找最接近的 k 個數），再用其他手段處理。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就往進階二分想：\n\n- **答案有明顯的「越大越難／越小越難」單調性**，例如 2819「最小相對損失」、LCP 08「劇情觸發時間」——都是設一個門檻再判斷可不可達。\n- **題目要你在有序資料裡「定位一段視窗或一個鄰近區間」**，像 658 找最接近的 k 個、1385 數「距離值」。\n- **暴力是枚舉所有候選 `x`（一次 `O(W)` 或 `O(n)`）太慢；** 換成二分後變 `O(log W)` 次判斷。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我設的候選值 `x` 語意是什麼？`feasible(x)` 怎麼判斷？\n2. 這個可行性真的單調嗎（F...F 之後一路 T...T）？分界在哪？\n3. 我要找的是第一個可行還是最後一個可行？對應的迴圈更新寫對了嗎？\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n本節延續基礎二分，處理稍微「拐個彎」的情境：題目沒有直接叫你找某個值，而是要你**先想清楚「真假分界」在哪裡**，或把二分當成定位工具去框出一段範圍。基礎二分像查字典；這個技巧像偵探，得先把問題翻譯成一條「越往右越成立（或越不成立）」的單調性質，才能套二分。\n\n:::example 進階二分的通用骨架：在 [lo, hi) 找第一個「可行」的 x\n關鍵心法：**把答案想成一個候選值 `x`，並確認「`x` 可不可行」具有單調性**——也就是只要某個 `x` 可行，比它更寬鬆的都可行；反之更嚴格的都不可行。一旦有這條真假分界，就能二分找分界點。\n\n```cpp\n// 進階二分的通用骨架：在 [lo, hi) 找第一個「可行」的 x\nwhile (lo < hi) {\n    int mid = lo + (hi - lo) / 2;\n    if (feasible(mid)) {\n        hi = mid;  // mid 可行 → 答案在左半（含 mid）\n    } else {\n        lo = mid + 1;  // mid 不可行 → 答案在右半\n    }\n}\nreturn lo;  // 第一個可行的 x\n```\n:::\n\n\n\n:::example LeetCode 658：二分視窗左端點 i，使 [i, i+k) 最接近 x\n以 LeetCode 658「找 K 個最接近的元素」為例：要選一段長度 `k` 的連續視窗，使視窗整體最接近 `x`。可以二分「視窗的左端點」——比較 `a[mid]` 與 `a[mid + k]` 哪邊離 `x` 更遠，藉此決定左端點往哪移。這就是用二分去「定位最佳起點」，而不是找某個固定值。\n\n```cpp\n// LeetCode 658：二分視窗左端點 i，使 [i, i+k) 最接近 x\nint lo = 0, hi = (int)a.size() - k;\nwhile (lo < hi) {\n    int mid = lo + (hi - lo) / 2;\n    if (x - a[mid] > a[mid + k] - x) {\n        lo = mid + 1;  // 左邊那個更遠，視窗右移\n    } else {\n        hi = mid;  // 否則往左\n    }\n}\nreturn lo;  // 最佳左端點\n```\n:::\n\n## 狀態／資料結構定義\n\n**候選值 `x`**：不再是下標，而是「答案可能的取值」。先寫清楚它的**單位與語意**——半徑？時間？操作次數？這一句沒寫清楚，後面一定把下標和答案值混在一起。\n\n**可行性判定 `feasible(x)`**：給定 `x`，回答「這樣可不可行」。它通常是一次貪心掃描、一次計數或一次搜尋，成本 `O(n)`。\n\n**二分區間**：`[lo, hi]` 是值域而非下標範圍。`lo` 取「一定不可行」或「最小可能」，`hi` 取「一定可行」的保守上界——寧可設寬，多幾輪 `log` 幾乎不花錢，設窄卻可能回傳錯誤答案。\n\n**定位型的區間**（658、1385）：仍在下標上二分，但上界要設成 `size() - k` 而不是 `size()`，否則視窗會越界。\n\n## 不變量或正確性證明\n\n**核心不變量**與基礎節相同：「答案仍在 `[lo, hi]` 內」。真正需要證明的是**單調性本身**——`feasible` 必須是 `F…F T…T`（或 `T…T F…F`）。\n\n單調性的標準論證長這樣：設 `feasible(x)` 為真，要證 `feasible(x + 1)` 也為真。通常是給出一個「把 `x` 的可行方案改造成 `x + 1` 的可行方案」的構造——多半是恆等改造（放寬限制後原方案照樣合法）。以「最小半徑覆蓋所有房屋」為例：半徑 `x` 能覆蓋全部，那半徑 `x + 1` 的覆蓋範圍是前者的超集，當然也能。\n\n**這一步不能跳過**。看起來單調卻其實不單調的例子很多，例如「恰好用 k 次操作」通常不單調（多做幾次反而不合法），必須改寫成「最多 k 次」才會單調。二分答案寫錯的案例，八成錯在這裡而不是模板。\n\n**分界即答案**：由單調性，`{x : feasible(x)}` 是一段後綴。求最小可行值就是求這段後綴的左端點，正是基礎節「找第一個為真」的模板。\n\n## 逐步演算法\n\n:::example 寫出判斷函式：給定候選 x，回傳是否可行\n**1. 把問題翻成「候選值 + 真假判斷」。** 先問：我能不能設一個值 `x`，然後寫一個 `feasible(x)` 判斷它行不行？例題（如 1385、2819）多半要先想出 `x` 的語意。\n\n```cpp\n// 寫出判斷函式：給定候選 x，回傳是否可行\nbool feasible(int x) { /* 用題意計算，回傳 true/false */\n}\n```\n:::\n\n\n\n**2. 確認單調性，決定找「第一個」還是「最後一個」可行。** 「最小化某量」通常找**第一個** `feasible` 為真的 `x`；「最大化某量」通常找**最後一個**為真的（見下方求最大章節）。先在紙上畫一條 F...F T...T 的分界線。\n\n:::example 找第一個可行 x（最小化方向）\n**3. 設好上下界並二分。** 上下界要涵蓋所有可能答案，但不要過寬以免溢位或變慢。\n\n```cpp\n// 找第一個可行 x（最小化方向）\nint lo = MIN_X, hi = MAX_X;  // 答案的合理範圍\nwhile (lo < hi) {\n    int mid = lo + (hi - lo) / 2;\n    if (feasible(mid)) {\n        hi = mid;\n    } else {\n        lo = mid + 1;\n    }\n}\nreturn lo;\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- `feasible` 一次的成本若是 `O(n)`，整體就是 `O(n log W)`（`W` 是值域寬度），通常足夠快。\n- 若答案是「定位下標」而非「值」（如 658），上界要設成 `size() - k`，避免視窗越界（LeetCode 1287、1385 也要注意索引範圍）。\n\n## C++17 模板\n\n以下用同型延伸示範題 704. 二分查詢 展示核心寫法。\n\n**完整題目**：給定升序整數陣列 `nums` 與目標值 `target`，若 `target` 存在則回傳下標，否則回傳 `-1`。\n\n:::example 標準二分搜尋：在有序陣列中尋找 target，找到回傳下標，否則回傳 -1\n**線索**：題目提供有序資料或明確的真假分界，答案落在第一個/最後一個滿足條件的位置，這是二分的主要線索。\n\n```cpp\nclass Solution {\npublic:\n    // 標準二分搜尋：在有序陣列中尋找 target，找到回傳下標，否則回傳 -1\n    int search(vector<int>& nums, int target) {\n            int l = 0, r = (int)nums.size() - 1;  // 閉區間 [l, r]\n            while (l <= r) {\n                    int mid = l + (r - l) / 2;  // 取中點，避免 l+r 溢位\n                    if (nums[mid] == target) {\n                            return mid;\n                    }\n                    if (nums[mid] < target) {\n                            l = mid + 1;  // 目標在右半段\n                    } else {\n                            r = mid - 1;  // 目標在左半段\n                    }\n            }\n            return -1;\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- 用閉區間維護候選答案；每次比較後刪掉必不可能含答案的一半。若改成二分答案，`target` 會替換成 `check(mid)` 的可行性。\n- `l` 與 `r` 表示仍可能包含答案的範圍，`mid` 只是用來判斷要丟掉哪一半。\n- 每個分支都要能說明被排除的區間為何不可能含答案；這就是二分不變式。\n\n## 時間與空間複雜度\n\n**時間 `O(判定成本 × log W)`**，`W` 是值域寬度。判定通常是 `O(n)`，所以整體多半是 `O(n log W)`。\n\n注意 `log W` 是對**值域**取對數，不是對 `n`。值域到 `1e18` 時 `log W ≈ 60`，仍然很小——這正是二分答案好用的原因：值域再大也只是多幾輪。\n\n**空間**取決於 `feasible` 的實作，二分本身是 `O(1)`。\n\n**判定成本是瓶頸**：若 `feasible` 寫成 `O(n log n)`（例如每次都重新排序），整體變成 `O(n log n log W)`，常會超時。能預先排序、預先算前綴和的，都要移到二分迴圈**外面**。\n\n定位型（658）仍是 `O(log n)` 加上輸出 k 個元素的 `O(k)`。\n\n## 常見錯誤與邊界條件\n\n- 固定一種區間寫法，不要在同一題混用。\n- `mid = l + (r - l) / 2` 避免溢位。\n- 重複元素題要先決定答案偏左或偏右。\n- 用全小、全大、單元素測資檢查邊界。\n\n## 常見變形\n\n- 找第一個 >= x / 第一個 > x。\n- 在旋轉陣列、山脈陣列或分段單調結構中先判定落在哪一段。\n- 浮點二分與三分法處理連續答案。\n\n## 與相似技巧的比較\n\n- **對比基礎二分**：模板一模一樣，差別只在區間裡裝的是「下標」還是「答案值」，以及判定條件是「比大小」還是「跑一次可行性檢查」。\n- **對比直接枚舉答案**：枚舉所有候選 `x` 是 `O(W × 判定成本)`；二分降到 `O(log W × 判定成本)`。代價是必須先證明單調性。\n- **對比貪心直接構造**：有些題能一次貪心求出最優值，那當然更快。二分答案的價值在於「驗證比構造容易」——你答不出最優解是多少，但給你一個數字就能判斷行不行。\n- **對比 DP**：DP 直接算出最優值，通常 `O(n × 狀態)`。狀態爆炸時改用二分答案 + 貪心驗證，往往能降一個量級。\n- **對比三分法**：二分需要單調的真假分界；若函數是單峰（先降後升）而非單調，就沒有分界可找，要改用三分（見第 9 節）。\n\n## 代表例題\n\n**題號**：658、2819、LCP 08、1287、1385\n\n重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 658 | [找到 K 個最接近的元素](https://leetcode.cn/problems/find-k-closest-elements) |  |\n| 2819 | [購買巧克力後的最小相對損失](https://leetcode.cn/problems/minimum-relative-loss-after-buying-chocolates)（Premium） |  |\n| LCP 08 | [劇情觸發時間](https://leetcode.cn/problems/ju-qing-hong-fa-shi-jian) |  |\n| 1287 | [有序陣列中出現次數超過 25% 的元素](https://leetcode.cn/problems/element-appearing-more-than-25-in-sorted-array) | 1179 |\n| 1385 | [兩個陣列間的距離值](https://leetcode.cn/problems/find-the-distance-value-between-two-arrays) | 1235 |\n| 1150 | [檢查一個數是否在陣列中佔絕大多數](https://leetcode.cn/problems/check-if-a-number-is-majority-element-in-a-sorted-array) | 1250 |\n| 2389 | [和有限的最長子序列](https://leetcode.cn/problems/longest-subsequence-with-limited-sum) | 1388 |\n| 1170 | [比較字串最小字母出現頻次](https://leetcode.cn/problems/compare-strings-by-frequency-of-the-smallest-character) | 1432 |\n| 2300 | [咒語和藥水的成功對數](https://leetcode.cn/problems/successful-pairs-of-spells-and-potions) | 1477 |\n| 981 | [基於時間的鍵值儲存](https://leetcode.cn/problems/time-based-key-value-store) | 1575 |\n| 2476 | [二叉搜索樹最近節點查詢](https://leetcode.cn/problems/closest-nodes-queries-in-a-binary-search-tree) | 1597 |\n| 1182 | [與目標顏色間的最短距離](https://leetcode.cn/problems/shortest-distance-to-target-color) | 1627 |\n| 3488 | [距離最小相等元素查詢](https://leetcode.cn/problems/closest-equal-element-queries) | 1699 |\n| 2080 | [區間內查詢數字的頻率](https://leetcode.cn/problems/range-frequency-queries) | 1702 |\n| 2563 | [統計公平數對的數目](https://leetcode.cn/problems/count-the-number-of-fair-pairs) | 1721 |\n| 2070 | [每一個查詢的最大美麗值](https://leetcode.cn/problems/most-beautiful-item-for-each-query) | 1724 |\n| 1146 | [快照陣列](https://leetcode.cn/problems/snapshot-array) | 1771 |\n| 3508 | [設計路由器](https://leetcode.cn/problems/implement-router) | 1851 |\n| 1818 | [絕對差值和](https://leetcode.cn/problems/minimum-absolute-sum-difference) | 1934 |\n| 911 | [線上選舉](https://leetcode.cn/problems/online-election) | 2001 |\n\n## 本節重點速查\n\n進階二分的重點不在模板，在**翻譯**：把題目改寫成一句「`x` 越大，某件事越容易／越難成立」。動手前先證一次單調性——通常是「放寬限制後，原方案照樣合法」的構造論證。**「恰好 k 次」多半不單調，要改寫成「最多 k 次」**。`x` 的單位與上下界要先寫清楚，別把下標和答案值混在一起；上界寧可設寬。判定成本是瓶頸，排序與前綴和都要移到二分迴圈外。整體 `O(n log W)`，`log` 是對值域取的，值域到 `1e18` 也只要約 60 輪。\n",
    },
    {
      id: 5,
      title: "3. 求最小",
      summary:
        "## 這個技術解決什麼問題\n\n「在滿足某條件的前提下，把某個量壓到**最小**」。\n\n這是二分答案最常見的形態。特徵是：直接算出最小值很難，但**給定一個候選值、驗證它可不可行很容易**。半徑、時間、速度、操作次數都是典型的被最小化對象。\n\n能套用的前提是「越大越可行」：把答案放寬，條件只會更容易成立。於是可行集合是一段後綴 `F…F T…T`，求最小可行值就是求分界的左端點。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就往「二分求最小」想：\n\n- **題目出現「最小的…使得…成立」**，例如 475「最小半徑」、2702「使數字變非正的最小操作次數」、3824「滿足條件的最小 K 值」。\n- **把答案放大，條件只會更容易達成（單調）。** 這保證 F...F T...T 的分界存在。\n- **直接求答案很難，但「給定一個答案、驗證可不可行」很容易。** 這正是二分答案的甜蜜點。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的答案是哪個量？它「越大越可行」嗎？\n2. `check(x)` 怎麼寫？給定 x 我如何判斷可不可行？\n3. 上界設到一定可行了嗎？下界會不會漏掉最小答案？\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n本節談「二分答案」中最常見的一種：**求最小**。題目通常長這樣——「在滿足某條件的前提下，把某個量壓到最小」。例如 LeetCode 475 供暖器：要選一個暖爐半徑，讓所有房屋都被覆蓋，問**最小**可行半徑。\n\n:::example 求最小的骨架：在 [lo, hi] 找第一個「可行」的答案\n直接枚舉每個可能的答案太慢，但這類問題有個漂亮的性質：**答案越大越容易滿足條件，答案越小越難**。把半徑放大，本來覆蓋不到的房屋也被涵蓋了；放到夠大一定可行，放太小一定不可行。於是「可行性」沿著答案軸呈現 F...F T...T 的單調分界，我們要找的就是**第一個 T**——最小的可行值。\n\n```cpp\n// 求最小的骨架：在 [lo, hi] 找第一個「可行」的答案\nint lo = MIN, hi = MAX;\nwhile (lo < hi) {\n    int mid = lo + (hi - lo) / 2;\n    if (check(mid)) {\n        hi = mid;  // mid 可行 → 試試更小，但保留 mid\n    } else {\n        lo = mid + 1;  // mid 不可行 → 必須更大\n    }\n}\nreturn lo;  // 第一個可行 = 最小可行答案\n```\n:::\n\n\n\n:::example LeetCode 475：半徑 r 能否覆蓋所有房屋（heaters 已排序）\n這裡的 `check(mid)` 是整個解法的靈魂：給定一個候選答案 `mid`，判斷它「行不行得通」。以 475 為例，`check(r)` 就是「半徑 r 能不能讓每間房屋都被某個暖爐覆蓋」。\n\n```cpp\n// LeetCode 475：半徑 r 能否覆蓋所有房屋（heaters 已排序）\nbool check(int r) {\n    for (int h : houses) {  // 每間房屋\n        auto it = lower_bound(heaters.begin(), heaters.end(), h);\n        bool ok = false;  // 找最近的暖爐，看距離是否 <= r\n        if (it != heaters.end() && *it - h <= r) {\n            ok = true;\n        }\n        if (it != heaters.begin() && h - *prev(it) <= r) {\n            ok = true;\n        }\n        if (!ok) {\n            return false;  // 有房屋蓋不到 → r 太小\n        }\n    }\n    return true;\n}\n```\n:::\n\n## 狀態／資料結構定義\n\n**答案變數 `x`**：要最小化的那個量。先寫下它的單位。\n\n**`check(x)`**：給定 `x`，回傳是否可行。必須滿足 `check(x)` 為真 ⇒ `check(x + 1)` 為真。\n\n**區間**：`lo` 取最小可能值（常是 0、1，或單一元素的最小需求），`hi` 取**一定可行**的保守上界（總和、最大值、值域上限）。\n\n**模板**（找第一個為真，偏左中點）：\n\n```cpp\n// 二分答案求最小：找第一個 check 為真的 x\nlong long lo = 最小可能, hi = 一定可行的上界;\nwhile (lo < hi) {\n    long long mid = lo + (hi - lo) / 2;  // 偏左中點\n    if (check(mid)) hi = mid;            // mid 可行，答案不會更大\n    else lo = mid + 1;                   // mid 不可行，答案更大\n}\n// 迴圈結束時 lo == hi，即最小可行值\n```\n\n答案可能很大（2604 喫穀子的時間、3824 的 K 值），`lo`、`hi`、`mid` 一律用 `long long`。\n\n## 不變量或正確性證明\n\n**不變量**：`[lo, hi]` 始終包含最小可行值。更精確地說——`lo - 1` 及其左邊都不可行，`hi` 及其右邊都可行。\n\n**維持**：`check(mid)` 為真時令 `hi = mid`，「`hi` 右邊全可行」仍成立（`mid` 自己就可行）；為假時令 `lo = mid + 1`，「`lo` 左邊全不可行」仍成立（由單調性，`mid` 及其左邊都不可行）。\n\n**終止與正確性**：偏左中點保證 `mid < hi`，所以 `hi = mid` 一定讓區間縮小，不會死迴圈。收斂到 `lo == hi` 時，由不變量它左邊全不可行、它自己可行，正是最小可行值。\n\n**上界必須真的可行**。若 `hi` 設得太小而其實不可行，`check` 從頭到尾都是假，迴圈會收斂到 `hi` 並回傳一個**不可行**的值——而且程式不會報錯。這是本節最危險的坑，設上界時寧可誇張一點。\n\n**單調性的來源**：多數情況是「資源越多越好辦」。若題目有「恰好」「不多不少」的字眼，先懷疑單調性，並試著改寫成「至多／至少」。\n\n## 逐步演算法\n\n**1. 設答案變數，確認「越大越可行」。** 先講清楚答案是什麼量（半徑、時間、操作次數…），並驗證放大答案只會讓條件更容易成立。\n\n:::example check 模板：給定上限/門檻 x，判斷題目條件能否滿足\n**2. 寫 `check(x)`：給定答案 x，判斷是否可行。** 這是最需要動腦的一步，通常用貪心或一次掃描完成。\n\n```cpp\n// check 模板：給定上限/門檻 x，判斷題目條件能否滿足\nbool check(long long x) { /* 貪心或掃描，回傳 true/false */\n}\n```\n:::\n\n\n\n:::example 求最小：找第一個 check 為真的 x\n**3. 設上下界，二分找第一個可行。** 下界取「絕對不可行或剛好臨界」的值，上界取「一定可行」的值。\n\n```cpp\n// 求最小：找第一個 check 為真的 x\nlong long lo = LOW, hi = HIGH;\nwhile (lo < hi) {\n    long long mid = lo + (hi - lo) / 2;\n    if (check(mid)) {\n        hi = mid;  // 可行就往左壓\n    } else {\n        lo = mid + 1;\n    }\n}\nreturn lo;\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 上界務必設到「保證可行」，否則迴圈可能回傳一個其實不可行的值。\n- `check` 成本 `O(n)` 或 `O(n log n)` 的話，整體是 `O(n log W)`（`W` 為值域）。\n- 答案可能很大，用 `long long` 避免溢位（如 2604 喫穀子的時間、3824 的 K 值）。\n\n## C++17 模板\n\n以下用同型延伸示範題 410. 分割陣列的最大值 展示核心寫法。\n\n**完整題目**：將非負整數陣列 `nums` 分割成 `k` 個非空連續子陣列，最小化這 `k` 段中的最大段和。\n\n:::example 二分答案：最小化最大子陣列和，答案範圍為 [最大單元素, 全部總和]\n**線索**：題目要求最小化上限或最大化下限，且給定候選值後可以快速判斷可不可行，這是二分答案的線索。\n\n```cpp\nclass Solution {\npublic:\n    // 二分答案：最小化最大子陣列和，答案範圍為 [最大單元素, 全部總和]\n    int splitArray(vector<int>& nums, int k) {\n            long long l =\n                            *max_element(nums.begin(), nums.end());  // 下界：每段至少包含最大元素\n            long long r =\n                            accumulate(nums.begin(), nums.end(), 0LL);  // 上界：整個陣列一段\n            // 判斷以 limit 為上限時能否切成 <= k 段\n            auto ok = [&](long long limit) {\n                    int groups = 1;\n                    long long sum = 0;\n                    for (int x : nums) {\n                            if (sum + x > limit) {\n                                    ++groups;  // 超出上限，開新段\n                                    sum = 0;\n                            }\n                            sum += x;\n                    }\n                    return groups <= k;\n            };\n            // 找最小的合法 limit（左偏二分，r 收縮）\n            while (l < r) {\n                    long long mid = l + (r - l) / 2;\n                    if (ok(mid)) {\n                            r = mid;  // mid 合法，嘗試更小\n                    } else {\n                            l = mid + 1;  // mid 不合法，下界提高\n                    }\n            }\n            return (int)l;\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- 答案越大越容易分割成功，因此可二分最小可行值；`ok` 只做貪心掃描，維持每段和不超過 `limit`。\n- `l` 與 `r` 表示仍可能包含答案的範圍，`mid` 只是用來判斷要丟掉哪一半。\n- 每個分支都要能說明被排除的區間為何不可能含答案；這就是二分不變式。\n\n## 時間與空間複雜度\n\n**時間 `O(check 成本 × log W)`**。`check` 通常是一次貪心掃描 `O(n)`，整體 `O(n log W)`。\n\n典型量級：`n = 1e5`、值域 `1e9` 時約 `1e5 × 30 = 3e6` 次基本操作，非常寬裕。\n\n**空間 `O(1)`**（不計 `check` 自身所需）。\n\n**優化 `check` 的常見手段**：\n- 排序只做一次，放在二分之前（475 供暖器要先排序房屋與暖爐）。\n- 前綴和、差分預先算好，讓 `check` 從 `O(n log n)` 降到 `O(n)`。\n- `check` 內部若還要二分（例如對每個房屋二分最近暖爐），整體是 `O(n log n log W)`，通常仍可接受，但能用雙指標取代就更好。\n\n**溢位**：`check` 內部的累加（總和、乘積）也要用 `long long`，不是只有二分變數。\n\n## 常見錯誤與邊界條件\n\n- 先寫出 `x` 的單位與上下界，避免把下標和答案值混在一起。\n- `check` 必須足夠快，否則外層二分會放大成本。\n- 明確單調方向後，再決定使用偏左或偏右中點。\n- 用最小值、最大值、剛好不可行三種測資檢查邊界。\n\n## 常見變形\n\n- 求最小可行值：找第一個 `check(x) = true`。\n- 求最大可行值：找最後一個 `check(x) = true`。\n- 間接值題先把收益/成本轉成可比較的門檻。\n\n## 與相似技巧的比較\n\n- **對比求最大**（第 4 節）：完全鏡像。求最小找「第一個可行」用偏左中點配 `hi = mid`；求最大找「最後一個可行」用偏右中點配 `lo = mid`。中點偏向寫錯就會死迴圈，這是兩節唯一需要分別記憶的地方。\n- **對比最小化最大值**（第 6 節）：那是本節的一個特化——被最小化的量剛好是「某組東西裡的最大者」，`check` 通常是貪心分組。骨架完全相同。\n- **對比貪心直接算**：能一次貪心求出答案當然更快。二分答案適用於「驗證容易、構造難」的情形。\n- **對比 DP**：`dp[i][j]` 之類的狀態設計能直接得到最優值，但狀態數可能爆炸。二分答案把「求最優」換成 `log W` 次「判可行」，常常能降維。\n- **對比優先佇列模擬**：像「最小速度吃香蕉」也能想成模擬，但逐一嘗試速度是 `O(W × n)`，二分把 `W` 降成 `log W`。\n\n## 代表例題\n\n**題號**：475、2604、2702、3807、3824\n\n重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。\n\n- 475 供暖器：把暖爐半徑視為答案門檻；半徑越大越容易覆蓋所有房屋，因此可二分最小可行半徑。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 475 | [供暖器](https://leetcode.cn/problems/heaters) |  |\n| 2604 | [喫掉所有穀子的最短時間](https://leetcode.cn/problems/minimum-time-to-eat-all-grains)（Premium） |  |\n| 2702 | [使數字變為非正數的最小操作次數](https://leetcode.cn/problems/minimum-operations-to-make-numbers-non-positive)（Premium） |  |\n| 3807 | [修復邊以遍歷圖的最小成本](https://leetcode.cn/problems/minimum-cost-to-repair-edges-to-traverse-a-graph)（Premium） |  |\n| 3824 | [減小陣列使其滿足條件的最小 K 值](https://leetcode.cn/problems/minimum-k-to-reduce-array-within-limit) | 1531 |\n| 1283 | [使結果不超過閾值的最小除數](https://leetcode.cn/problems/find-the-smallest-divisor-given-a-threshold) | 1542 |\n| 2187 | [完成旅途的最少時間](https://leetcode.cn/problems/minimum-time-to-complete-trips) | 1641 |\n| 1870 | [準時到達的列車最小時速](https://leetcode.cn/problems/minimum-speed-to-arrive-on-time) | 1676 |\n| 3296 | [移山所需的最少秒數](https://leetcode.cn/problems/minimum-number-of-seconds-to-make-mountain-height-zero) | 1695 |\n| 1011 | [在 D 天內送達包裹的能力](https://leetcode.cn/problems/capacity-to-ship-packages-within-d-days) | 1725 |\n| 3453 | [分割正方形 I](https://leetcode.cn/problems/separate-squares-i) | 1735 |\n| 875 | [愛喫香蕉的珂珂](https://leetcode.cn/problems/koko-eating-bananas) | 1766 |\n| 3639 | [變為活躍狀態的最小時間](https://leetcode.cn/problems/minimum-time-to-activate-string) | 1853 |\n| 2594 | [修車的最少時間](https://leetcode.cn/problems/minimum-time-to-repair-cars) | 1915 |\n| 1482 | [製作 m 束花所需的最少天數](https://leetcode.cn/problems/minimum-number-of-days-to-make-m-bouquets) | 1946 |\n| 3048 | [標記所有下標的最早秒數 I](https://leetcode.cn/problems/earliest-second-to-mark-indices-i) | 2263 |\n\n## 本節重點速查\n\n「求最小可行值」＝找 `F…F T…T` 的第一個真。模板：偏左中點 `mid = lo + (hi - lo) / 2`，可行則 `hi = mid`、不可行則 `lo = mid + 1`，收斂後 `lo` 即答案。**上界一定要設到保證可行**，否則會安靜地回傳一個不可行的值——這是本節最危險的坑。單調性來自「資源越多越好辦」；看到「恰好」就先懷疑，試著改寫成「至多／至少」。排序、前綴和等預處理放在二分迴圈**外**。答案與 `check` 內的累加都用 `long long`。整體 `O(n log W)`。\n",
    },
    {
      id: 7,
      title: "4. 求最大",
      summary:
        "## 這個技術解決什麼問題\n\n「在滿足某條件的前提下，把某個量放到**最大**」——求最小的鏡像。\n\n單調性反過來：把答案放大，條件只會更難達成，所以可行集合是一段**前綴** `T…T F…F`。求最大可行值就是求這段前綴的右端點，也就是「最後一個為真」。\n\n典型對象是 H 指數、字號、能到達的最遠距離、最多能安排的工作數——都是「多要一點就做不到」的量。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就往「二分求最大」想：\n\n- **題目出現「最大的…使得…成立」**，例如 275「最大 H 指數」、1618「適應螢幕的最大字號」、1642「能到達的最遠建築」、LCP 78「城牆防線」。\n- **把答案放大，條件只會更難達成（單調）。** 保證 T...T F...F 的分界存在。\n- **直接求最大很難，但「給定一個值、驗證能否達成」很容易。**\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要最大化的量是什麼？它「越大越難」嗎？\n2. `check(x)` 怎麼判斷給定 x 仍可行？\n3. 我用的是「偏右中點 + `lo = mid`」嗎？會不會死迴圈？\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n本節是「二分答案」的另一面：**求最大**。題目通常是「在滿足某條件的前提下，把某個量盡量放大」。例如 LeetCode 275 H 指數 II：找**最大**的 `h`，使得至少有 `h` 篇論文的引用數不小於 `h`。\n\n:::example 求最大的骨架：在 [lo, hi] 找最後一個「可行」的答案\n和求最小相反，這類問題的單調性是：**答案越小越容易滿足，越大越難**。`h` 訂得低，門檻寬鬆，容易達成；`h` 訂得太高就達不到。於是可行性沿答案軸呈現 T...T F...F，我們要找的是**最後一個 T**——最大的可行值。\n\n```cpp\n// 求最大的骨架：在 [lo, hi] 找最後一個「可行」的答案\nint lo = MIN, hi = MAX;\nwhile (lo < hi) {\n    int mid = lo + (hi - lo + 1) / 2;  // 注意 +1：偏右取中點，避免死迴圈\n    if (check(mid)) {\n        lo = mid;  // mid 可行 → 試更大，但保留 mid\n    } else {\n        hi = mid - 1;  // mid 不可行 → 必須更小\n    }\n}\nreturn lo;  // 最後一個可行 = 最大可行答案\n```\n:::\n\n\n\n:::example LeetCode 275：引用數 citations 已升序，h 篇是否都 >= h ？\n特別注意中點要寫成 `lo + (hi - lo + 1) / 2`（**偏右取整**）。因為更新是 `lo = mid`，若中點偏左，當 `lo` 和 `hi` 只差 1 時 `mid` 會一直等於 `lo`，造成無窮迴圈。求最大用偏右、求最小用偏左，這是固定搭配。\n\n```cpp\n// LeetCode 275：引用數 citations 已升序，h 篇是否都 >= h ？\nbool check(int h) {\n    int n = citations.size();\n    // 後 h 篇（最大的 h 個）中，最小的那個是否 >= h\n    return h == 0 || citations[n - h] >= h;\n}\n```\n:::\n\n## 狀態／資料結構定義\n\n**答案變數 `x`**：要最大化的量。\n\n**`check(x)`**：給定 `x`，回傳能否達成。必須滿足 `check(x)` 為真 ⇒ `check(x - 1)` 為真。\n\n**區間**：`lo` 取**一定可行**的保守下界（常是 0），`hi` 取最大可能值。注意與求最小相反——這裡是**下界**必須保證可行。\n\n**中點必須偏右**：`mid = lo + (hi - lo + 1) / 2`。這是本節唯一需要單獨記憶的細節，寫成偏左會死迴圈（見不變量一節）。\n\n**浮點版本**（644 子陣列最大平均數 II）：不用 `lo < hi`，改成固定跑 100 次迭代，或用 `while (hi - lo > eps)`。整數的 `mid + 1` 技巧在浮點上不適用也不需要。\n\n## 不變量或正確性證明\n\n**不變量**：`lo` 及其左邊都可行，`hi + 1` 及其右邊都不可行。答案落在 `[lo, hi]`。\n\n**維持**：`check(mid)` 為真時令 `lo = mid`（`mid` 可行，答案不會更小）；為假時令 `hi = mid - 1`（由單調性 `mid` 及其右邊都不可行）。\n\n**為什麼中點必須偏右**：假設 `lo = 3`、`hi = 4`。偏左中點算出 `mid = 3`；若 `check(3)` 為真則 `lo = mid = 3`，區間完全沒縮小，下一輪重複同樣的計算——**無限迴圈**。偏右中點算出 `mid = 4`，真則 `lo = 4`（收斂），假則 `hi = 3`（收斂），兩種情形都前進。\n\n一句話記法：**哪一側的更新可能不縮小區間（`lo = mid` 或 `hi = mid`），中點就要偏向哪一側**。求最小用 `hi = mid`，中點偏左；求最大用 `lo = mid`，中點偏右。\n\n**下界必須真的可行**。若 `lo` 設得太高而其實不可行，`check` 全為假，迴圈會收斂到 `lo` 並回傳一個不可行的值。與求最小節的上界坑完全對稱。\n\n## 逐步演算法\n\n**1. 設答案變數，確認「越大越難」。** 講清楚要最大化的量（H 指數、字號、平均數、能到的最遠距離…），並驗證放大答案只會讓條件更難成立。\n\n:::example check 模板：給定下限/規模 x，判斷是否仍能達成\n**2. 寫 `check(x)`：給定答案 x，判斷是否仍可行。** 與求最小相同，多半用貪心或一次掃描。\n\n```cpp\n// check 模板：給定下限/規模 x，判斷是否仍能達成\nbool check(long long x) { /* 貪心或掃描，回傳 true/false */\n}\n```\n:::\n\n\n\n:::example 求最大：找最後一個 check 為真的 x\n**3. 設上下界，用「偏右中點」二分找最後一個可行。**\n\n```cpp\n// 求最大：找最後一個 check 為真的 x\nlong long lo = LOW, hi = HIGH;\nwhile (lo < hi) {\n    long long mid = lo + (hi - lo + 1) / 2;  // 偏右，配合 lo = mid\n    if (check(mid)) {\n        lo = mid;\n    } else {\n        hi = mid - 1;\n    }\n}\nreturn lo;\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 中點偏右是求最大的關鍵，寫成偏左會死迴圈。\n- 下界要設到「保證可行」（例如 0 或最小可能），否則最後可能回傳不可行值。\n- 若答案是浮點（如 644 子陣列最大平均數），改用固定次數的浮點二分或設精度 `eps`。\n- 複雜度 `O(n log W)`。\n\n## C++17 模板\n\n以 275. H 指數 II 為例。\n\n**完整題目**：`citations` 已按升序排列，`citations[i]` 是第 `i` 篇論文的引用次數。求 H 指數——最大的 `h`，使得至少有 `h` 篇論文的引用次數都不小於 `h`。\n\n**線索**：`h` 越大越難滿足（要求更多篇、且每篇門檻更高），是 `T…T F…F`，求最後一個可行的 `h`。\n\n```cpp\n// 275. H 指數 II：二分答案求最大，偏右中點\nclass Solution {\npublic:\n    int hIndex(vector<int>& citations) {\n        const int n = citations.size();\n\n        // check(h)：引用數最高的 h 篇裡，最低的那篇是否 >= h\n        // 陣列升序，最高的 h 篇就是後綴 [n - h, n)，其中最小者是 citations[n - h]\n        auto check = [&](int h) { return h == 0 || citations[n - h] >= h; };\n\n        int lo = 0, hi = n;  // lo = 0 一定可行，是合法的保守下界\n        while (lo < hi) {\n            int mid = lo + (hi - lo + 1) / 2;  // 偏右中點：配 lo = mid 才不會死迴圈\n            if (check(mid)) lo = mid;          // 可行，答案不會更小\n            else hi = mid - 1;                 // 不可行，答案更小\n        }\n        return lo;\n    }\n};\n```\n\n1642（用磚塊與梯子能到達的最遠建築）、1618（最大字號）、2071（最多工作數）都是同一骨架，只是 `check` 換成貪心模擬。\n\n## 時間與空間複雜度\n\n**時間 `O(check 成本 × log W)`**。275 的 `check` 是 `O(1)`，整體 `O(log n)`；`check` 是一次貪心掃描時（1642、2071）整體是 `O(n log W)`。\n\n**空間 `O(1)`**（不計 `check` 自身）。\n\n**浮點二分的輪數**：不用 `eps` 而用固定迭代次數更穩。每輪精度減半，跑 100 輪可把區間壓到 `2^-100`，遠超 `double` 的精度極限，而 100 輪的成本可以忽略。用 `while (hi - lo > eps)` 反而可能因浮點誤差永遠不收斂。\n\n**`check` 內含二分或排序**時整體會多一個 `log`；1898（可移除字元的最大數目）的 `check` 是一次 `O(n)` 子序列比對，整體 `O(n log n)`。\n\n## 常見錯誤與邊界條件\n\n- **中點寫成偏左**。`mid = lo + (hi - lo) / 2` 配 `lo = mid` 會在區間剩兩格時卡死。求最大一律用 `mid = lo + (hi - lo + 1) / 2`。這是本節最常見的錯，而且症狀是超時而非答案錯誤，很容易誤判成演算法不夠快。\n- **下界不可行**。求最大時是**下界**要保證可行（與求最小相反）。設不好會回傳一個達不到的值。\n- **上界設太小**。答案被硬生生截斷，且不會有任何錯誤訊息。\n- **`h == 0` 之類的邊界沒特判**。275 的 `citations[n - h]` 在 `h == 0` 時會越界成 `citations[n]`，要先短路。\n- **把「求最大」的模板套到不單調的題目上**。放大答案不一定更難時（例如中間有一段反而更好做），分界不存在，二分回傳的值毫無意義。\n- **浮點題用整數模板**。`mid + 1` 在浮點上沒有意義，改用固定迭代次數。\n- **累加溢位**。`check` 裡的總和、乘積用 `long long`。\n- 用「答案為 0」「答案為最大可能值」「剛好不可行」三種測資驗邊界。\n\n## 與相似技巧的比較\n\n- **對比求最小**（第 3 節）：完全鏡像。差別只有兩處——單調方向（`T…T F…F` 對 `F…F T…T`）與中點偏向（偏右配 `lo = mid`，偏左配 `hi = mid`）。把兩者並排寫一次，之後就不會記錯。\n- **對比最大化最小值**（第 7 節）：那是本節的特化——被最大化的量剛好是「某組東西裡的最小者」，`check` 通常是貪心放置。骨架與中點偏向完全相同。\n- **對比「取負數轉成求最小」**：把 `x` 換成 `-x` 確實能把求最大變成求最小，但可讀性差且容易在邊界上出錯。直接記住偏右中點更實在。\n- **對比貪心 / 排序直接算**：275 其實也能用計數排序在 `O(n)` 解出 H 指數（H 指數 I 就是那樣）。有更快的直接解法時就別用二分；二分的價值在「驗證比構造容易」。\n- **對比浮點三分**：644 求最大平均數用的是浮點二分（答案單調可判定）；若目標函數是單峰而非單調，就得改用三分法（第 9 節）。\n\n## 代表例題\n\n**題號**：LCP 78、275、644、1618、1642\n\n重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。\n\n- 275 H 指數 II：在已排序引用數中二分 H 指數 `h`：若至少有 `h` 篇論文引用數不小於 `h`，就嘗試更大的 `h`。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| LCP 78 | [城牆防線](https://leetcode.cn/problems/Nsibyl) |  |\n| 275 | [H 指數 II](https://leetcode.cn/problems/h-index-ii) |  |\n| 644 | [子陣列最大平均數 II](https://leetcode.cn/problems/maximum-average-subarray-ii)（Premium） |  |\n| 1618 | [找出適應螢幕的最大字號](https://leetcode.cn/problems/maximum-font-to-fit-a-sentence-in-a-screen)（Premium） |  |\n| 1642 | [可以到達的最遠建築](https://leetcode.cn/problems/furthest-building-you-can-reach) | 1962 |\n| 1802 | [有界陣列中指定下標處的最大值](https://leetcode.cn/problems/maximum-value-at-a-given-index-in-a-bounded-array) | 1929 |\n| 1891 | [割繩子](https://leetcode.cn/problems/cutting-ribbons) |  |\n| 1898 | [可移除字元的最大數目](https://leetcode.cn/problems/maximum-number-of-removable-characters) | 1913 |\n| 2071 | [你可以安排的最多工數目](https://leetcode.cn/problems/maximum-number-of-tasks-you-can-assign) | 2648 |\n| 2137 | [透過倒水操作讓所有的水桶所含水量相等](https://leetcode.cn/problems/pour-water-between-buckets-to-make-water-levels-equal) |  |\n| 2141 | [同時執行 N 臺電腦的最長時間](https://leetcode.cn/problems/maximum-running-time-of-n-computers) | 2265 |\n| 2226 | [每個小孩最多能分到多少糖果](https://leetcode.cn/problems/maximum-candies-allocated-to-k-children) | 1646 |\n| 2258 | [逃離火災](https://leetcode.cn/problems/escape-the-spreading-fire) | 2347 |\n| 2576 | [求出最多標記下標](https://leetcode.cn/problems/find-the-maximum-number-of-marked-indices) | 1843 |\n| 2861 | [最大合金數](https://leetcode.cn/problems/maximum-number-of-alloys) | 1981 |\n| 2982 | [找出出現至少三次的最長特殊子字串 II](https://leetcode.cn/problems/find-longest-special-substring-that-occurs-thrice-ii) | 1773 |\n| 3007 | [價值和小於等於 K 的最大數字](https://leetcode.cn/problems/maximum-number-that-sum-of-the-prices-is-less-than-or-equal-to-k) | 2258 |\n| 3344 | [最大尺寸陣列](https://leetcode.cn/problems/maximum-sized-array) |  |\n\n## 本節重點速查\n\n「求最大可行值」＝找 `T…T F…F` 的最後一個真。**中點必須偏右**：`mid = lo + (hi - lo + 1) / 2`，可行則 `lo = mid`、不可行則 `hi = mid - 1`。寫成偏左會在區間剩兩格時死迴圈——症狀是超時而非答案錯，很容易誤判。記法：哪一側的更新可能不縮小區間，中點就偏向哪一側。與求最小相反，這裡是**下界**要保證可行。浮點題（644）改用固定 100 次迭代，別用 `eps` 當迴圈條件。整體 `O(n log W)`。\n",
    },
    {
      id: 8,
      title: "5. 分間接值",
      summary:
        "## 這個技術解決什麼問題\n\n真正要求的量本身**不單調**，沒辦法直接二分；但存在一個與它相關、且隨答案單調變化的**中間量**。\n\n做法是：先二分那個中間量（門檻、價格層、邊長、半徑），定位到分界之後，再用公式、前綴和或計數**一次**把真正的答案合成出來——而不是逐項模擬。\n\n1648 銷售價值減少的顏色球是最典型的例子：總收益不單調，但「最後會賣到哪個價格層」是單調的；定位到那一層後，用等差級數一次加總，取代一顆一顆賣的 `O(orders)`。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就往「分間接值」想：\n\n- **真正要求的量（收益、座標、計數）本身不單調，無法直接二分**，但存在一個與它相關的單調中間量，例如 1648 的「價格層」、3143「正方形中最多點數」的「正方形邊長」。\n- **逐項模擬會超時**（如一顆一顆賣、一格一格擴張），需要用二分定位門檻後一次合成。\n- **算出間接量後，能用公式／前綴和把答案一次湊出來。**\n\n確認自己有沒有套對，問三個問題：\n\n1. 我真正要的答案是什麼？它本身單調嗎？\n2. 有沒有一個「間接量」隨答案單調變化、可以拿來二分？\n3. 二分定位到間接量後，我怎麼**一次**算出真答案（哪個公式）？邊界零頭處理了嗎？\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n本節處理一種比較隱晦的二分：**分間接值**。意思是——我們真正想要的答案（例如總收益、某個座標、某個計數），不能直接二分；但它和「某個有單調性的中間量」掛勾，於是我們先二分那個**間接量**，再由它一次算出真正的答案。\n\n:::example 核心轉念：不二分「收益」，而二分「最終價格層 p」\n以 LeetCode 1648「銷售價值減少的顏色球」為例：你要賣 `orders` 顆球，每顆球的售價等於該顏色當前剩餘數量，賣完價格就降一格，目標是**最大化總收益**。一顆一顆賣是 `O(orders)`，數量上看可能上億，太慢。觀察到一個單調量：「最後所有顏色都會被賣到某個**價格層 `p`**」——把所有數量 `>= p` 的球削到 `p`。`p` 越低，賣掉的球越多。於是我們二分這個「最終價格層 `p`」，再用等差級數一次加總收益。\n\n```cpp\n// 核心轉念：不二分「收益」，而二分「最終價格層 p」\n// 給定 p：把每堆 cnt 削到 p，賣掉 (cnt - p) 顆，共賣 sold(p) 顆\nlong long sold(int p, vector<int>& a) {\n    long long s = 0;\n    for (int c : a) {\n        if (c > p) {\n            s += c - p;  // 高於 p 的部分都會被賣掉\n        }\n    }\n    return s;  // sold(p) 隨 p 下降而單調遞增\n}\n```\n:::\n\n\n\n:::example 等差級數一次加總：賣價從 hi 降到 lo+1 的總收益\n`sold(p)` 對 `p` 是單調的：`p` 越小賣得越多。我們二分找「剛好能賣到至少 `orders` 顆的最高價格層 `p`」，這是一個標準的單調分界。找到 `p` 後，每堆從 `c` 賣到 `p + 1`（或 `p`）這段的收益是連續整數相加，用等差級數公式 `(首項 + 末項) * 項數 / 2` 一次算完，避免逐顆累加。\n\n```cpp\n// 等差級數一次加總：賣價從 hi 降到 lo+1 的總收益\nlong long sumRange(long long lo, long long hi) {  // 賣價 lo+1, ..., hi\n    return (lo + 1 + hi) * (hi - lo) / 2;\n}\n```\n:::\n\n## 狀態／資料結構定義\n\n**真答案**：題目要的量（總收益、最多點數、某個座標）。\n\n**間接量 `x`**：可二分的中間量。它要滿足兩個條件——(1) `check(x)` 單調；(2) 定位到 `x` 之後，真答案能用 `O(n)` 以內的公式算出。\n\n**`check(x)`**：通常是「以門檻 `x` 執行時，用掉的資源是否不超過額度」。1648 裡就是「所有球都降到價格 `x` 以上，總共要賣幾顆，有沒有超過 `orders`」。\n\n**合成公式**：拿到分界後，把每一段用等差級數 `(首項 + 末項) × 項數 / 2` 或前綴和一次加總。\n\n**零頭**：二分邊界附近常有「剛好賣到一半」的殘量要單獨補算——這是本節 off-by-one 的重災區，實作時要單獨寫一段處理，別想合併進主公式。\n\n## 不變量或正確性證明\n\n**間接量的單調性**：以 1648 為例，`check(p)` = 「把所有顏色的球都賣到價格剩 `p` 為止，需要賣的總顆數 `<= orders`」。`p` 越大代表賣得越少，所以 `check` 是 `T…T F…F`——這一步要獨立論證，因為它就是整個做法成立的地基。\n\n**合成階段的正確性**：設二分定出的分界是 `p`，表示「賣到價格 `p` 是可行的，賣到 `p - 1` 就超額」。此時：\n\n- 每種顏色 `c`，若初始數量 `inventory[c] > p`，就從 `inventory[c]` 一路賣到 `p + 1`，收益是等差級數 `(inventory[c] + p + 1) × (inventory[c] - p) / 2`。\n- 全部加總後若還沒用完 `orders`，剩下的訂單都以單價 `p` 成交（因為所有顏色此時都恰好是 `p`），零頭部分是 `剩餘訂單數 × p`。\n\n這兩段互不重疊、合起來涵蓋所有訂單，因此總收益正確。\n\n**為什麼不能直接二分收益**：收益隨「賣幾顆」增加，但題目給的是固定 `orders`，收益本身沒有可判定的真假分界。這正是要繞道間接量的原因——認出「什麼可以二分、什麼不行」是本節的核心能力。\n\n## 逐步演算法\n\n**1. 找出可二分的「間接量」。** 真答案（收益、計數）若不單調，就找一個與它相關、且**隨著它單調變化**的中間量（價格層、門檻、半徑…）來二分。\n\n:::example 二分最終價格層 p：找最高的 p 使 sold(p) >= orders\n**2. 寫間接量的判斷函式。** 給定間接量 `p`，能否快速算出「對應的數量／可行性」？這一步要 `O(n)` 或更快。\n\n```cpp\n// 二分最終價格層 p：找最高的 p 使 sold(p) >= orders\nint lo = 0, hi = *max_element(a.begin(), a.end());\nwhile (lo < hi) {\n    int mid = lo + (hi - lo) / 2;\n    if (sold(mid, a) >= orders) {\n        lo = mid + 1;  // 還能多賣 → 提高價格層門檻\n    } else {\n        hi = mid;\n    }\n}\n// 結束後 lo = 最低能滿足的下一層；p = lo - 1 之類，依語意微調\n```\n:::\n\n\n\n**3. 由間接量一次回推真答案。** 拿到分界後，**不要再逐項模擬**，用公式（等差級數、前綴和、計數）一次合成最終答案——這正是「分間接值」省時間的地方。\n\n**4. 邊界與複雜度。**\n\n- 二分邊界附近常有「剩餘零頭」要單獨處理（例如 1648 賣到 `p` 那層時可能只賣部分球），務必小心 off-by-one。\n- 收益可能極大，用 `long long`，並記得對大質數取模（1648 要求 mod 1e9+7）。\n- 複雜度 `O(n log W)`，遠優於逐顆 `O(orders)`。\n\n## C++17 模板\n\n以 1648. 銷售價值減少的顏色球 為例。\n\n**完整題目**：`inventory[i]` 是第 `i` 種顏色的球數，賣出一顆的價值等於**當前該顏色剩餘的球數**。總共要賣 `orders` 顆，求最大總價值，答案對 `1e9 + 7` 取模。\n\n**線索**：逐顆貪心賣是 `O(orders)`，而 `orders` 可到 `1e9`。但「最後所有顏色都會降到某個價格層 `p`」是單調的——二分 `p`，再用等差級數一次加總。\n\n```cpp\n// 1648. 銷售價值減少的顏色球：二分價格層 + 等差級數一次合成\nclass Solution {\npublic:\n    int maxProfit(vector<int>& inventory, int orders) {\n        const long long MOD = 1'000'000'007;\n\n        // count(p)：把所有顏色都賣到剩 p 顆，總共要賣幾顆\n        auto count = [&](long long p) {\n            long long total = 0;\n            for (long long v : inventory)\n                if (v > p) total += v - p;\n            return total;\n        };\n\n        // 找最小的 p 使得 count(p) <= orders（p 越大賣得越少，單調）\n        long long lo = 0, hi = *max_element(inventory.begin(), inventory.end());\n        while (lo < hi) {\n            long long mid = lo + (hi - lo) / 2;\n            if (count(mid) <= orders) hi = mid;\n            else lo = mid + 1;\n        }\n        const long long p = lo;\n\n        // 主體：每種顏色從 v 賣到 p + 1，等差級數一次加總\n        long long ans = 0, sold = 0;\n        for (long long v : inventory) {\n            if (v <= p) continue;\n            long long cnt = v - p;\n            sold += cnt;\n            ans = (ans + (v + p + 1) % MOD * (cnt % MOD) / 2) % MOD;\n        }\n\n        // 零頭：剩下的訂單全部以單價 p 成交\n        ans = (ans + (orders - sold) % MOD * p) % MOD;\n        return (int)ans;\n    }\n};\n```\n\n## 時間與空間複雜度\n\n**時間 `O(n log W)`**：二分 `log W` 輪，每輪的 `count` 掃一次 `O(n)`；合成階段再掃一次 `O(n)`。`W` 是值域（最大球數）。\n\n對照逐顆模擬的 `O(orders)`——`orders` 可到 `1e9` 時必然超時，而 `n log W` 只有約 `1e5 × 30`。這個差距就是「分間接值」的全部價值。\n\n**空間 `O(1)`**（不計輸入）。\n\n**溢位是本節的頭號風險**：`count` 的累加可達 `n × 1e9`，等差級數的分子是兩個 `1e9` 級數相乘。二分變數、累加變數、公式中間值一律 `long long`，並在取模前後都檢查一次。\n\n3143（正方形中的最多點數）的間接量是邊長，`check` 是 `O(n)` 掃描，同樣是 `O(n log W)`。\n\n## 常見錯誤與邊界條件\n\n- **零頭沒處理或算重**。二分定出的 `p` 通常會「賣得比 `orders` 少一點」，剩餘訂單要用單價 `p` 補上。忘記補會少算，或把零頭也算進等差級數會多算。這是本節最常見的錯。\n- **等差級數的邊界寫錯**。從 `v` 賣到 `p + 1`（不是 `p`），項數是 `v - p`，首項 `v`、末項 `p + 1`。差一項就整個答案偏掉。\n- **取模與除法混用**。`(首項 + 末項) × 項數 / 2` 在取模運算下不能直接除 2——要嘛先確保分子偶數再除（如上面的寫法，先乘後除且分子必為偶數），要嘛乘以 2 的模逆元。這裡容易寫出「看起來對、大測資錯」的程式。\n- **溢位**。所有中間值用 `long long`；`count` 的累加尤其容易被忽略。\n- **誤以為真答案可以直接二分**。動手前先問「我要的量本身單調嗎」，不單調才需要繞道；繞道時要能說清楚間接量是什麼。\n- **`p = 0` 的邊界**：所有球都賣完的情形，`count(0)` 就是總球數，要確保上下界涵蓋得到。\n\n## 與相似技巧的比較\n\n- **對比一般二分答案**（第 3、4 節）：那兩節二分的就是答案本身；本節要二分的是另一個量，答案得再算一次。判斷依據是「我要的量本身單調嗎」——不單調才需要繞道。\n- **對比純貪心模擬**：1648 逐顆賣球是最直觀的貪心，正確但 `O(orders)`。本節保留同一套貪心邏輯，只是用二分跳過中間過程、用公式一次合成。可以想成「貪心 + 加速」。\n- **對比前綴和 / 差分**：合成階段常用到它們。差別在前綴和是預處理工具，本節是「先定位分界、再用前綴和一次求和」的組合技。\n- **對比數學推導直接解**：1648 若能解出封閉解當然更快，但含取整與零頭，公式極易寫錯。二分定位分界再合成，是「數學推導」與「暴力模擬」之間的務實折衷。\n- **對比第 K 小/大**（第 8 節）：那節也是二分一個值、用計數函數判定，形式非常接近。差別在第 8 節二分的值就是答案；本節二分完還要再合成一次。\n\n## 代表例題\n\n**題號**：1648、3143\n\n把逐顆賣球改成價格層門檻：先二分最後會賣到的價格層，再用等差級數一次加總收益。\n\n- 3143 正方形中的最多點數：重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1648 | [銷售價值減少的顏色球](https://leetcode.cn/problems/sell-diminishing-valued-colored-balls) | 2050 |\n| 3143 | [正方形中的最多點數](https://leetcode.cn/problems/maximum-points-inside-the-square) | 1697 |\n\n## 本節重點速查\n\n真答案不單調時，找一個**隨它單調變化的間接量**來二分（價格層、門檻、邊長），定位到分界後用等差級數或前綴和**一次合成**答案，而不是逐項模擬。1648 靠這招把 `O(orders)`（`orders` 可到 `1e9`）降到 `O(n log W)`。三個必踩的坑：**零頭**要單獨補算（二分後通常還差幾筆，以單價 `p` 成交）、**等差級數的項數與首末項**（從 `v` 賣到 `p + 1`，共 `v - p` 項）、**取模下的除以 2**（先確保分子為偶數再除，或乘模逆元）。所有中間值用 `long long`。\n",
    },
    {
      id: 9,
      title: "6. 最小化最大值",
      summary:
        "## 這個技術解決什麼問題\n\n把東西分組、切割或調度，使得「其中**最大**的那一份盡量小」。\n\n這是「求最小」的一個特化，但因為出現頻率極高、`check` 的形狀又很固定，值得單獨記成一個模型。特徵詞是「使最大的…最小」「最小化…的最大值」——410 分割陣列的最大值、774 加油站最大距離、1631 最小體力消耗路徑都是。\n\n關鍵轉換是：**把答案設成「允許上限 `limit`」**。原本要在所有分法裡找最大值最小的那一種，現在只要問「上限訂在 `limit`，做不做得到」。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就往「最小化最大值」想：\n\n- **題目字面就是「使最大的…最小」**，例如 410「分割陣列的最大值」、774「最小化加油站最大距離」、1631「最小體力消耗路徑」、LCP 12「刷題計劃」。\n- **把允許上限放寬只會更容易（單調）。** 保證 F...F T...T 分界存在。\n- **直接求最小的最大值很難，但「給定上限、判斷可不可行」很容易（貪心或一次搜尋）。**\n\n確認自己有沒有套對，問三個問題：\n\n1. 我把答案設成「上限 limit」了嗎？上限越大越容易達成嗎？\n2. `check(limit)` 怎麼判斷？是貪心分組還是圖上可達？\n3. 我找的是第一個可行上限（偏左二分）嗎？下界會不會讓單一元素就超標？\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n本節是二分答案中非常經典的一型：**最小化最大值**。題目要你把東西分組、安排或調度，使得「其中**最大**的那一份盡量小」。例如 LeetCode 410 分割陣列：把陣列切成 `k` 段，使**最大段和**最小；或 1631 最小體力消耗路徑：找一條路，使路上**最大**的高度差最小。\n\n:::example 最小化最大值的骨架：找第一個「上限 limit 可行」的值\n直接求「最小的最大值」很繞，但有個關鍵轉念：**把答案當成一個「允許上限 `limit`」**。問題就變成「如果規定每一份都不准超過 `limit`，做不做得到？」這個 `check(limit)` 具有單調性——`limit` 放得越寬鬆（越大）越容易做到，越緊（越小）越難。於是可行性沿 `limit` 軸呈 F...F T...T，我們二分找**第一個可行的 `limit`**，那就是答案。\n\n```cpp\n// 最小化最大值的骨架：找第一個「上限 limit 可行」的值\nint lo = LOW, hi = HIGH;\nwhile (lo < hi) {\n    int mid = lo + (hi - lo) / 2;\n    if (check(mid)) {\n        hi = mid;  // 上限 mid 夠用 → 試更小\n    } else {\n        lo = mid + 1;  // mid 太緊 → 放寬\n    }\n}\nreturn lo;  // 最小可行上限 = 最小化後的最大值\n```\n:::\n\n\n\n:::example LeetCode 410：每段和不超過 limit，最少需要幾段 <= k ?\n`check(limit)` 怎麼寫，以 410 為例：規定每段和不超過 `limit`，用貪心一段一段塞，數一數至少需要幾段；若需要的段數 `<= k`，就代表這個 `limit` 可行。\n\n```cpp\n// LeetCode 410：每段和不超過 limit，最少需要幾段 <= k ?\nbool check(long long limit, vector<int>& a, int k) {\n    int seg = 1;\n    long long cur = 0;\n    for (int x : a) {\n        if (x > limit) {\n            return false;  // 單一元素就超上限，不可能\n        }\n        if (cur + x > limit) {\n            ++seg;\n            cur = 0;\n        }  // 開新的一段\n        cur += x;\n    }\n    return seg <= k;  // 段數夠少 → 可行\n}\n```\n:::\n\n## 狀態／資料結構定義\n\n**答案變數 `limit`**：允許的上限（段和上限、距離上限、高度差上限）。\n\n**`check(limit)`**：兩種常見形狀——\n\n- **貪心分組**（410、1011、1891）：從左到右盡量往當前組塞，塞不下就開新的一組，回傳組數是否 `<= k`。\n- **圖上可達**（1631、778）：只走「代價 `<= limit`」的邊，用 BFS/DFS/並查集判斷起點能否到終點。\n\n**區間**：`lo` 取「單一元素的最大值」（任何一組至少要裝得下最大的那個元素，比它小的上限必然不可行），`hi` 取「全部放同一組」的總和或最大可能代價。這組上下界幾乎是模板。\n\n**中點偏左**，配 `hi = mid`——與「求最小」相同。\n\n段和可能很大，用 `long long`。\n\n## 不變量或正確性證明\n\n**單調性**：`check(limit)` 為真 ⇒ `check(limit + 1)` 為真。原本合法的分組方案，在更寬鬆的上限下當然仍然合法——這個構造是恆等的，所以單調性成立。\n\n**貪心 `check` 的正確性**（這是本節真正需要證的部分）：要證「盡量往當前組塞、塞不下才開新組」用的組數是所有合法分法中最少的。\n\n用交換論證。設有一個最優分法 `OPT` 用了 `m` 組，貪心用了 `g` 組。比較兩者的第一個分割點：貪心的第一組是「在不超過 `limit` 的前提下能塞的最長前綴」，所以貪心的第一個分割點不會比 `OPT` 的早。把 `OPT` 的第一個分割點往後移到貪心的位置，第一組仍然合法（沒超過 `limit`），而後面要處理的剩餘序列變短，所需組數不會增加。逐個分割點這樣調整，最後得到貪心的分法，且組數 `<= m`。因此 `g <= m`，貪心最優。\n\n**下界的必要性**：若 `limit` 小於某個單一元素，那個元素永遠塞不進任何一組。`check` 裡要明確擋掉這種情形（或把 `lo` 設成 `max(nums)`），否則貪心會陷入無窮開新組。\n\n**「恰好 k 組」與「至多 k 組」**：多數 minimax 題只要求「不超過 k 組」，此時貪心直接回傳 `組數 <= k` 即可。若題目真的要求恰好 k 組，注意組數可以透過再切一刀增加，所以「至多 k 組可行」通常等價於「恰好 k 組可行」（前提是元素數 `>= k`）——但這一步要自己確認，不能想當然。\n\n## 逐步演算法\n\n**1. 把答案設成「允許上限 limit」。** 確認「上限越大越容易達成」這個單調性，這是套這個模型的前提。\n\n:::example check 模板：每一份/每一步都不超過 limit，能否達成題目要求\n**2. 寫 `check(limit)`：在上限下能否完成。** 多半是貪心（盡量塞滿一份再開新份）或 BFS/DFS（路徑類題目，如 778、1631 判斷在水位/體力 limit 下能否連通起終點）。\n\n```cpp\n// check 模板：每一份/每一步都不超過 limit，能否達成題目要求\nbool check(long long limit) { /* 貪心分組 或 圖上可達性，回傳 true/false */\n}\n```\n:::\n\n\n\n:::example 二分最小可行上限\n**3. 設上下界，二分找第一個可行上限。** 下界常取「單一元素的最大值」（再小一定有元素超標），上界取「全部加起來」或值域最大。\n\n```cpp\n// 二分最小可行上限\nlong long lo = *max_element(a.begin(), a.end());     // 下界：最大單元素\nlong long hi = accumulate(a.begin(), a.end(), 0LL);  // 上界：全部一段\nwhile (lo < hi) {\n    long long mid = lo + (hi - lo) / 2;\n    if (check(mid)) {\n        hi = mid;\n    } else {\n        lo = mid + 1;\n    }\n}\nreturn lo;\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 下界別設太低：若有單一元素就超過 `limit`，那段永遠塞不下，要在 `check` 裡擋掉。\n- 路徑類題目的 `check` 是一次 BFS/DFS `O(n)`，整體 `O(n log W)`。\n- 用 `long long` 防段和溢位。\n\n## C++17 模板\n\n以下用本節代表題 410. 分割陣列的最大值 展示核心寫法。\n\n**完整題目**：將非負整數陣列 `nums` 分割成 `k` 個非空連續子陣列，最小化這 `k` 段中的最大段和。\n\n:::example 二分答案：最小化最大子陣列和，答案範圍為 [最大單元素, 全部總和]\n**線索**：目標是壓低最大代價；看到「最小化最大值」時，通常把答案當成允許上限 `limit`，再檢查是否可行。\n\n```cpp\nclass Solution {\npublic:\n    // 二分答案：最小化最大子陣列和，答案範圍為 [最大單元素, 全部總和]\n    int splitArray(vector<int>& nums, int k) {\n            long long l =\n                            *max_element(nums.begin(), nums.end());  // 下界：每段至少包含最大元素\n            long long r =\n                            accumulate(nums.begin(), nums.end(), 0LL);  // 上界：整個陣列一段\n            // 判斷以 limit 為上限時能否切成 <= k 段\n            auto ok = [&](long long limit) {\n                    int groups = 1;\n                    long long sum = 0;\n                    for (int x : nums) {\n                            if (sum + x > limit) {\n                                    ++groups;  // 超出上限，開新段\n                                    sum = 0;\n                            }\n                            sum += x;\n                    }\n                    return groups <= k;\n            };\n            // 找最小的合法 limit（左偏二分，r 收縮）\n            while (l < r) {\n                    long long mid = l + (r - l) / 2;\n                    if (ok(mid)) {\n                            r = mid;  // mid 合法，嘗試更小\n                    } else {\n                            l = mid + 1;  // mid 不合法，下界提高\n                    }\n            }\n            return (int)l;\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- 答案越大越容易分割成功，因此可二分最小可行值；`ok` 只做貪心掃描，維持每段和不超過 `limit`。\n- `l` 與 `r` 表示仍可能包含答案的範圍，`mid` 只是用來判斷要丟掉哪一半。\n- 每個分支都要能說明被排除的區間為何不可能含答案；這就是二分不變式。\n\n## 時間與空間複雜度\n\n**時間 `O(check 成本 × log W)`**：\n\n- 貪心分組的 `check` 是 `O(n)`，整體 `O(n log W)`，`W` 是「總和 − 最大元素」的區間寬度。\n- 圖上可達的 `check` 是一次 BFS/DFS `O(V + E)`；1631 在 `m × n` 網格上就是 `O(mn log W)`。\n\n**空間**：貪心版 `O(1)`；圖上版要 `O(V)` 的 visited。\n\n**與其他解法的量級對照**：410 用 DP 是 `O(n^2 k)`，二分答案是 `O(n log(sum))`——在 `n = 1000`、`k = 50` 時差了三個數量級。1631 也可以用並查集按邊權排序逐步合併（`O(mn log(mn))`），複雜度相近，看哪個好寫。\n\n段和累加用 `long long`：`n = 1000`、元素到 `1e6` 時總和是 `1e9`，剛好卡在 `int` 邊緣。\n\n## 常見錯誤與邊界條件\n\n- 下界常是單個元素/單條邊的最大值，上界常是總和或最大可能代價。\n- `check` 裡若用貪心，要說明為何局部塞滿不會增加段數。\n- 找第一個 true 時使用偏左中點。\n- 注意題目是否要求恰好 k 組；很多 minimax check 只需不超過 k 組。\n\n## 常見變形\n\n- 分割陣列最大段和：貪心檢查段數。\n- 路徑最大邊權最小：只保留不超過門檻的邊再判斷可達。\n- 工作量分配：判斷容量上限是否足夠。\n\n## 與相似技巧的比較\n\n- **對比求最小**（第 3 節）：本節是它的特化，模板與中點偏向完全相同，只是 `check` 固定為「貪心分組」或「圖上可達」。\n- **對比最大化最小值**（第 7 節）：完全鏡像。最小化最大值用「上限 + 找第一個可行 + 偏左中點」；最大化最小值用「下限 + 找最後一個可行 + 偏右中點」。兩節放在一起對照記最省力。\n- **對比區間 DP**：410 的 DP 解 `dp[i][j]` = 前 `i` 個元素分成 `j` 段的最小最大段和，是 `O(n^2 k)`。二分答案把「求最優」換成 `log` 次「判可行」，降到 `O(n log W)`。看到 minimax 就先想二分。\n- **對比 Kruskal / 並查集**（1631、778）：按邊權由小到大加邊，直到起點與終點連通，第一次連通時的邊權就是答案。複雜度與二分相近，但只需掃一遍排序後的邊，常數更小。\n- **對比 Dijkstra 變體**（1631）：把路徑代價定義成「路上最大邊權」而非「邊權總和」，直接跑一次修改版 Dijkstra 也能解，`O(mn log(mn))`。\n\n## 代表例題\n\n**題號**：LCP 12、410、774、778、1631\n\n把答案當成允許上限；練習設計 `check(limit)`，判斷所有局部成本能否被壓在上限內。\n\n- 410 分割陣列的最大值：把「分成 `k` 段後最大段和最小」改成判斷上限 `limit` 能否在不超過 `k` 段內完成。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| LCP 12 | [小張刷題計劃](https://leetcode.cn/problems/xiao-zhang-shua-ti-ji-hua) |  |\n| 410 | [分割陣列的最大值](https://leetcode.cn/problems/split-array-largest-sum) |  |\n| 774 | [最小化去加油站的最大距離](https://leetcode.cn/problems/minimize-max-distance-to-gas-station)（Premium） |  |\n| 778 | [水位上升的泳池中游泳](https://leetcode.cn/problems/swim-in-rising-water) |  |\n| 1631 | [最小體力消耗路徑](https://leetcode.cn/problems/path-with-minimum-effort) | 1948 |\n| 1760 | [袋子裡最少數目的球](https://leetcode.cn/problems/minimum-limit-of-balls-in-a-bag) | 1940 |\n| 2064 | [分配給商店的最多商品的最小值](https://leetcode.cn/problems/minimized-maximum-of-products-distributed-to-any-store) | 1886 |\n| 2439 | [最小化陣列中的最大值](https://leetcode.cn/problems/minimize-maximum-of-array) | 1965 |\n| 2513 | [最小化兩個陣列中的最大值](https://leetcode.cn/problems/minimize-the-maximum-of-two-arrays) | 2302 |\n| 2560 | [打家劫舍 IV](https://leetcode.cn/problems/house-robber-iv) | 2081 |\n| 2616 | [最小化數對的最大差值](https://leetcode.cn/problems/minimize-the-maximum-difference-of-pairs) | 2155 |\n| 3399 | [字元相同的最短子字串 II](https://leetcode.cn/problems/smallest-substring-with-identical-characters-ii) | 2376 |\n| 3419 | [圖的最大邊權的最小值](https://leetcode.cn/problems/minimize-the-maximum-edge-weight-of-graph) | 2243 |\n| 3605 | [陣列的最小穩定性因子](https://leetcode.cn/problems/minimum-stability-factor-of-array) | 2410 |\n| 3613 | [最小化連通分量的最大成本](https://leetcode.cn/problems/minimize-maximum-component-cost) | 1642 |\n| 3733 | [完成所有送貨任務的最少時間](https://leetcode.cn/problems/minimum-time-to-complete-all-deliveries) | 1973 |\n\n## 本節重點速查\n\n看到「使最大的…最小」，就把答案設成**允許上限 `limit`**，問題變成「上限訂在 `limit`，做不做得到」。上限越大越容易 ⇒ `F…F T…T` ⇒ 找第一個可行、偏左中點配 `hi = mid`。`check` 兩種形狀：**貪心分組**（塞不下才開新組，回傳組數 `<= k`）或**圖上可達**（只走代價 `<= limit` 的邊跑 BFS）。貪心的最優性靠交換論證——分割點往後移不會增加組數。下界取 `max(nums)`（否則單一元素塞不下會無窮開新組），上界取總和。段和用 `long long`。整體 `O(n log W)`，遠優於 DP 的 `O(n^2 k)`。\n",
    },
    {
      id: 10,
      title: "7. 最大化最小值",
      summary:
        "## 這個技術解決什麼問題\n\n安排或挑選，使得「其中**最小**的那一份盡量大」——上一節的鏡像。\n\n特徵詞是「使最小的…最大」「最大化…的最小值」：1552 兩球之間的磁力（相鄰球最小間距最大）、2517 禮盒最大甜蜜度（任兩種糖果的最小差值最大）、1231 分享巧克力（每人拿到的最小甜度最大）。\n\n轉換方式是：**把答案設成「保底下限 `d`」**。原本要在所有安排裡找最小值最大的那一種，現在只要問「保底訂在 `d`，達不達得到」。\n\n## 辨識題型的訊號\n\n看到下面這些特徵，就往「最大化最小值」想：\n\n- **題目字面是「使最小的…最大」**，例如 1552「兩球磁力（最大化最小間距）」、2517「最大甜蜜度」、1231「分享巧克力」、1102「路徑最小值最大」、2528「最大化最小電量」。\n- **把保底下限調低只會更容易（單調）。** 保證 T...T F...F 分界存在。\n- **直接求最大的最小值很難，但「給定下限、判斷達不達得到」很容易（貪心或一次搜尋）。**\n\n確認自己有沒有套對，問三個問題：\n\n1. 我把答案設成「下限 d」了嗎？下限越小越容易達成嗎？\n2. `check(d)` 怎麼判斷？是貪心放置還是圖上可達？\n3. 我用的是「偏右中點 + `lo = mid`」找最後一個可行嗎？會不會死迴圈？\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n本節是上一節的鏡像：**最大化最小值**。題目要你安排或挑選，使得「其中**最小**的那一份盡量大」。例如 LeetCode 1552 兩球之間的磁力：把 `m` 顆球放進不同位置，使**相鄰球最小間距**最大；或 2517 禮盒最大甜蜜度：選若干糖果，使任兩種的**最小差值**最大。\n\n:::example 最大化最小值的骨架：找最後一個「下限 d 可行」的值\n轉念和上一節對稱：**把答案當成一個「保底下限 `d`」**。問題變成「如果要求每一份／每個間距都**至少** `d`，做不做得到？」這個 `check(d)` 的單調性是——`d` 訂得越小越寬鬆、越容易達成；`d` 越大越難。於是可行性沿 `d` 軸呈 T...T F...F，我們二分找**最後一個可行的 `d`**，那就是答案。\n\n```cpp\n// 最大化最小值的骨架：找最後一個「下限 d 可行」的值\nint lo = LOW, hi = HIGH;\nwhile (lo < hi) {\n    int mid = lo + (hi - lo + 1) / 2;  // 偏右取中點（配合 lo = mid，防死迴圈）\n    if (check(mid)) {\n        lo = mid;  // 下限 mid 達得到 → 試更大\n    } else {\n        hi = mid - 1;  // mid 太大 → 退一步\n    }\n}\nreturn lo;  // 最大可行下限 = 最大化後的最小值\n```\n:::\n\n\n\n:::example LeetCode 1552：座標已排序，相鄰間距至少 d，能否放下 m 顆球？\n注意中點要**偏右** `lo + (hi - lo + 1) / 2`，因為更新是 `lo = mid`，偏左會在剩兩個數時死迴圈——這和求最大章節一致。`check(d)` 以 1552 為例：座標排序後貪心放球，每放一顆就跳到下一個距離至少 `d` 的位置，數能放幾顆。\n\n```cpp\n// LeetCode 1552：座標已排序，相鄰間距至少 d，能否放下 m 顆球？\nbool check(int d, vector<int>& pos, int m) {\n    int cnt = 1, last = pos[0];  // 第一顆放最左\n    for (int i = 1; i < (int)pos.size(); ++i) {\n        if (pos[i] - last >= d) {  // 距離夠遠才放下一顆\n            ++cnt;\n            last = pos[i];\n        }\n    }\n    return cnt >= m;  // 放得下 m 顆 → 下限 d 可行\n}\n```\n:::\n\n## 狀態／資料結構定義\n\n**答案變數 `d`**：保底下限（最小間距、最小甜度、最小得分）。\n\n**`check(d)`**：兩種常見形狀——\n\n- **貪心放置**（1552、2517、1231）：排序後從左到右，只要與上一個選中者的距離 `>= d` 就選，回傳選中數量是否 `>= m`。\n- **圖上可達**（1102 得分最高的路徑、2528 最大化城市的最小電量）：只走「值 `>= d`」的格子，判斷起點能否到終點。\n\n**區間**：`lo` 取 0 或最小可能值（一定可行），`hi` 取最大可能下限（座標跨度、值域差、總量）。與上一節相反——這裡是**下界**要保證可行。\n\n**中點必須偏右**：`mid = lo + (hi - lo + 1) / 2`，配 `lo = mid`。\n\n多數題要先排序，排序放在二分迴圈**外**。\n\n## 不變量或正確性證明\n\n**單調性**：`check(d)` 為真 ⇒ `check(d - 1)` 為真。滿足「間距至少 `d`」的方案，當然也滿足「間距至少 `d - 1`」——恆等構造，單調性成立。\n\n**貪心 `check` 的正確性**：要證「排序後，第一個必選，之後只要距離 `>= d` 就立刻選」能選出最多的元素。\n\n交換論證：設最優解選了 `k` 個位置 `p1 < p2 < … < pk`。貪心選的第一個是 `a1`（最左端），而 `p1 >= a1`。把 `p1` 換成 `a1`：間距約束只會更鬆（後面的 `p2` 與 `a1` 的距離 `>= p2 - p1 >= d`），仍然合法。接著同理把 `p2` 換成貪心的 `a2`（貪心的 `a2` 是滿足 `a2 - a1 >= d` 的最左位置，所以 `a2 <= p2`）。逐個替換後得到貪心解，且個數不減。因此貪心選中的數量是最大值。\n\n**「越早選越不吃虧」的不變式**：這是上述論證的直覺版——盡早選中一個位置，會為後面留下更多空間，不可能變差。這個不變式是所有「貪心放置」型 `check` 的共同地基。\n\n**下界必須可行**：`d = 0` 通常平凡可行，可安心當下界。上界若設太小，答案會被截斷。\n\n## 逐步演算法\n\n**1. 把答案設成「保底下限 d」。** 確認「下限越小越容易達成」這個單調性，這是套用前提。\n\n:::example check 模板：要求每一份至少 d，能否湊出題目要的數量\n**2. 寫 `check(d)`：在「每份至少 d」的要求下能否完成。** 多為貪心（能放/能分就盡量放，數夠不夠），如 1231 分巧克力、2517 選糖果都是這個套路。\n\n```cpp\n// check 模板：要求每一份至少 d，能否湊出題目要的數量\nbool check(long long d) { /* 貪心累積，回傳 是否達標 */\n}\n```\n:::\n\n\n\n:::example 二分最大可行下限\n**3. 設上下界，用「偏右中點」二分找最後一個可行下限。**\n\n```cpp\n// 二分最大可行下限\nlong long lo = 0, hi = HIGH;  // 下界 0 一定可行，上界取值域最大\nwhile (lo < hi) {\n    long long mid = lo + (hi - lo + 1) / 2;  // 偏右\n    if (check(mid)) {\n        lo = mid;\n    } else {\n        hi = mid - 1;\n    }\n}\nreturn lo;\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 中點一定要偏右、配 `lo = mid`，否則死迴圈（這是最大化方向的通病）。\n- 上界別設太小，要能涵蓋最大可能下限（例如座標跨度、值域差）。\n- `check` 通常 `O(n)`（排序後一次掃描），整體 `O(n log W)`；圖上題（如 1102 得分最高路徑、2528 最小電量）的 `check` 可能是一次 BFS。\n\n## C++17 模板\n\n以下用本節代表題 1552. 兩球之間的磁力 展示核心寫法。\n\n**完整題目**：給定籃子座標 `position` 與球數 `m`，選出 `m` 個位置放球，使任兩球距離的最小值最大化。\n\n:::example 二分答案：最大化相鄰磁球間的最小距離\n**線索**：目標是拉高最低保障；看到「最大化最小值」時，通常把答案當成下限，判斷是否還能完成安排。\n\n```cpp\nclass Solution {\npublic:\n    // 二分答案：最大化相鄰磁球間的最小距離\n    int maxDistance(vector<int>& position, int m) {\n            sort(position.begin(), position.end());  // 先排序才能貪心放置\n            int l = 1,\n                            r = position.back() - position.front();  // 答案範圍：[1, 最大間距]\n            // 判斷最小間距 >= dist 時能否放下 m 顆磁球\n            auto ok = [&](int dist) {\n                    int cnt = 1, last = position[0];  // 第一顆固定放在最左\n                    for (int x : position) {\n                            if (x - last >= dist) {\n                                    ++cnt;  // 距離夠大，貪心放置\n                                    last = x;\n                            }\n                    }\n                    return cnt >= m;\n            };\n            // 找最大合法 dist（右偏二分，l 收縮）\n            while (l < r) {\n                    int mid = l + (r - l + 1) / 2;  // 上取整，避免 l == mid 死循環\n                    if (ok(mid)) {\n                            l = mid;  // mid 合法，嘗試更大\n                    } else {\n                            r = mid - 1;  // mid 不合法，上界降低\n                    }\n            }\n            return l;\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- 距離下限越小越容易放球，因此 `ok(dist)` 是前半段可行、後半段不可行；用偏右中點尋找最後一個可行距離。\n- `l` 與 `r` 表示仍可能包含答案的範圍，`mid` 只是用來判斷要丟掉哪一半。\n- 每個分支都要能說明被排除的區間為何不可能含答案；這就是二分不變式。\n\n## 時間與空間複雜度\n\n**時間 `O(n log n + check 成本 × log W)`**：排序 `O(n log n)` 做一次；`check` 通常是一次掃描 `O(n)`，二分 `log W` 輪，合計 `O(n log W)`。\n\n1552 的 `position` 到 `1e9`，`log W ≈ 30`；`n = 1e5` 時總共約 `3e6` 次操作。\n\n**圖上版**（1102、2528）：`check` 是一次 BFS `O(mn)`，整體 `O(mn log W)`。\n\n**空間**：排序原地 `O(1)`（或 `O(log n)` 遞迴堆疊）；圖上版要 `O(mn)` 的 visited，且每輪要重置。\n\n**替代解法的量級**：1102 也能用「按值由大到小加點的並查集」在 `O(mn log(mn))` 解出，複雜度相近。2517 的 `check` 需要排序後掃描，排序務必只做一次。\n\n## 常見錯誤與邊界條件\n\n- 上界常是最大座標差、最大格值或總收益。\n- 找最後一個 true 時使用偏右中點，避免死循環。\n- 貪心檢查要維持「越早滿足越不吃虧」的不變式。\n- 若資料未排序，通常先排序或預處理門檻。\n\n## 常見變形\n\n- 放置問題：排序後盡量早放，保留後續空間。\n- 切分收益：累積到至少 `x` 就切一段。\n- 網格安全值：只走安全值不低於門檻的格子。\n\n## 與相似技巧的比較\n\n- **對比最小化最大值**（第 6 節）：完全鏡像。上限對下限、第一個可行對最後一個可行、偏左中點對偏右中點。兩節並排對照著記，比各記一次省力得多。\n- **對比求最大**（第 4 節）：本節是它的特化，`check` 固定為「貪心放置」或「圖上可達」。中點偏向與死迴圈的坑完全相同。\n- **對比直接貪心求最優值**：有些題可以一次貪心算出最大的最小值，但多數情況下「給定 `d` 判可行」比「直接構造最優 `d`」容易得多——這正是二分答案的適用判準。\n- **對比並查集離線處理**（1102、2528）：把格子按值由大到小加入並查集，第一次讓起點與終點連通的那個值就是答案。不需要二分，一次掃描完成，常數更小；但寫起來比二分長。\n- **對比 Dijkstra 變體**：把路徑代價定義成「路上最小值」並取最大化，跑一次修改版 Dijkstra 也能解，適合邊權變化的圖。\n\n## 代表例題\n\n**題號**：1102、1231、1552、2517、2528\n\n把答案當成保底下限；練習證明下限提高後只會更難，並用偏右二分找最後可行值。\n\n- 1552 兩球之間的磁力：排序座標後，判斷能否放下 `m` 顆球且相鄰距離至少為 `d`；`d` 越小越容易，答案要找最後一個可行值。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1102 | [得分最高的路徑](https://leetcode.cn/problems/path-with-maximum-minimum-value)（Premium） | 2011 |\n| 1231 | [分享巧克力](https://leetcode.cn/problems/divide-chocolate)（Premium） | 2029 |\n| 1552 | [兩球之間的磁力](https://leetcode.cn/problems/magnetic-force-between-two-balls) | 1920 |\n| 2517 | [禮盒的最大甜蜜度](https://leetcode.cn/problems/maximum-tastiness-of-candy-basket) | 2021 |\n| 2528 | [最大化城市的最小電量](https://leetcode.cn/problems/maximize-the-minimum-powered-city) | 2236 |\n| 2812 | [找出最安全路徑](https://leetcode.cn/problems/find-the-safest-path-in-a-grid) | 2154 |\n| 3281 | [範圍內整數的最大得分](https://leetcode.cn/problems/maximize-score-of-numbers-in-ranges) | 1768 |\n| 3449 | [最大化遊戲分數的最小值](https://leetcode.cn/problems/maximize-the-minimum-game-score) | 2748 |\n| 3464 | [正方形上的點之間的最大距離](https://leetcode.cn/problems/maximize-the-distance-between-points-on-a-square) | 2806 |\n| 3600 | [升級後最大生成樹穩定性](https://leetcode.cn/problems/maximize-spanning-tree-stability-with-upgrades) | 2301 |\n| 3620 | [恢復網路路徑](https://leetcode.cn/problems/network-recovery-pathways) | 1998 |\n| 3710 | [最大劃分因子](https://leetcode.cn/problems/maximum-partition-factor) | 2135 |\n\n## 本節重點速查\n\n看到「使最小的…最大」，就把答案設成**保底下限 `d`**，問題變成「保底訂在 `d`，達不達得到」。下限越小越容易 ⇒ `T…T F…F` ⇒ 找最後一個可行、**偏右中點** `mid = lo + (hi - lo + 1) / 2` 配 `lo = mid`，寫成偏左會死迴圈。`check` 兩種形狀：**貪心放置**（排序後只要距離 `>= d` 就立刻選，回傳選中數 `>= m`）或**圖上可達**（只走值 `>= d` 的格子）。貪心的正確性靠「越早選越不吃虧」的交換論證。與上一節相反，這裡是**下界**要保證可行。排序放在二分迴圈外。整體 `O(n log W)`。\n",
    },
    {
      id: 11,
      title: "8. 第 K 小/大",
      summary:
        "## 這個技術解決什麼問題\n\n問「排名第 K 的**值**是多少」，而資料量大到不該整個排序，或根本不該攤平（有序矩陣、乘法表、所有數對的距離）。\n\n本節有兩條主線：\n\n1. **二分答案 + 計數**：問「第 K 個值是多少」。訣竅是找到一個比「直接找第 K 個」好算得多的計數問題——給定 `x`，快速算出「不超過 `x` 的個數」。能算 `countLE(x)`，就能二分。\n2. **小根堆逐步展開**：問「前 K 個東西分別是什麼」。多條有序鏈合併時（373 尋找和最小的 K 對數字），每次從堆頂取出最小、再把它的後繼推入。\n\n動手前先分清楚題目要的是哪一種——問「值」走第一條，問「東西」走第二條。\n\n## 辨識題型的訊號\n\n看到下列特徵，往「第 K 小/大」想：\n\n- **問的是「排名第 K 的值」而不是位置**，且資料量大、不適合整個排序。這通常是二分答案的訊號。\n- **有一個比「直接找第 K 個」更好算的計數問題**：給定 `x`，能快速算出「不超過 `x` 的個數」。能算 `countLE`，就能二分。\n- **集合本身就有序或半有序**（有序矩陣、乘法表、排序後的數對距離），讓計數可以用公式或雙指標 `O(n)` 完成，而非 `O(n^2)`。\n- **要「依序吐出前 K 個最小組合」**（多條有序鏈合併），這是小根堆逐步展開的典型場景，如 373。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我是要「第 K 個的值」（二分答案）還是「前 K 個東西」（堆展開）？\n2. 若走二分，我的 `countLE(x)` 怎麼利用題目結構快速算？單調性對嗎？\n3. 若走堆，每彈出一個元素後，要推入哪個「後繼候選」才不漏不重？\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n本節要解決的場景是：一堆數字裡，「排名第 K 小（或第 K 大）的那一個值是多少？」最直覺的做法是全部排序再數到第 K 個，但當資料是「一行行、一列列都已經有序」或「規模大到不該攤平」時，重排序很浪費。這個技巧有兩條主線，常常一起用。\n\n:::example 二分答案的骨架：找最小的 x，使得 <= x 的個數達到 k\n第一條是「二分答案 + 計數」。重點觀念：與其去找「第 K 小的元素在哪」，不如直接去猜「第 K 小的值是多少」。對一個候選值 `x`，我們問一個容易回答的問題：「有多少個元素 `<= x`？」這個計數記為 `count(x)`，它隨 `x` 變大而單調不減，於是可以二分。\n\n```cpp\n// 二分答案的骨架：找最小的 x，使得 <= x 的個數達到 k\nwhile (lo < hi) {\n    long long mid = lo + (hi - lo) / 2;\n    if (countLE(mid) >= k) {\n        hi = mid;  // 達標，答案可能更小\n    } else {\n        lo = mid + 1;  // 不夠，往大找\n    }\n}\n// lo 就是第 k 小的值\n```\n:::\n\n\n\n為什麼答案一定是某個真實存在的元素？因為「使 `count(x) >= k` 的最小 `x`」這個位置，剛好就是第 K 小那個值本身——再小一點計數就掉到 `k` 以下了。像 378（有序矩陣第 K 小）、668（乘法表第 K 小）、719（第 K 小數對距離）、786（第 K 小質數分數）都是這個套路，差別只在 `countLE` 怎麼快速算。\n\n:::example 小根堆：每次取最小，再推入它的後繼候選\n第二條是「小根堆逐步展開」。當題目要「依序產生前 K 個最小組合」（如 373 和最小的 K 對數字），我們用堆只保留「目前已知的最小候選」，每取出一個就把它的「下一個鄰居」推進堆，避免一次生出全部組合。\n\n```cpp\n// 小根堆：每次取最小，再推入它的後繼候選\npriority_queue<pair<int, int>, vector<pair<int, int>>, greater<>> pq;\npq.push({nums1[0] + nums2[0], 0});  // {和, 在 nums2 的下標}，i 固定那條鏈\n```\n:::\n\n## 狀態／資料結構定義\n\n**二分答案路線**：\n\n- **候選值 `x`**：在**值域**上二分，不是在下標上。下界是最小可能值，上界是最大可能值（乘法表是 `n * n`，數對距離是 `max - min`）。\n- **`countLE(x)`**：不超過 `x` 的元素個數。有序矩陣用逐列走階梯 `O(n)`，乘法表用 `Σ min(n, x / i)` `O(n)`，數對距離用雙指標 `O(n)`。這個函數的效率決定整體複雜度。\n- 值域大時 `x` 用 `long long`。\n\n**堆展開路線**：\n\n- **小根堆**存 `(值, 來源座標)`，依值排序。\n- **visited** 防止同一個座標被推入多次（多條路徑可能指向同一格）。\n- 每次彈出堆頂即為下一個最小，推入它的後繼（通常是右邊與下面兩個方向）。\n\n## 不變量或正確性證明\n\n**二分答案路線的核心命題**：**使 `countLE(x) >= k` 的最小 `x`，恰好就是第 K 小的值。**\n\n證明分兩步。設第 K 小的值是 `v`。\n\n- `countLE(v) >= k`：不超過 `v` 的元素至少包含前 K 小的那些，個數 `>= k`。所以 `v` 落在可行集合裡。\n- 對任意 `x < v`，`countLE(x) <= k - 1`：不超過 `x` 的元素都嚴格小於 `v`，而嚴格小於第 K 小值的元素最多 `k - 1` 個。所以 `x` 不可行。\n\n兩者合起來說明 `v` 正是最小的可行值。**單調性**由 `countLE` 對 `x` 不減直接得到。\n\n**為什麼答案一定是集合中真實存在的元素**：由上面第二點，`v - 1` 不可行而 `v` 可行，代表恰好在 `v` 這一點計數跨過門檻——只有 `v` 真的出現在集合裡才會發生跳躍。因此二分回傳的一定是真實元素，不會是「集合中不存在的中間值」。這一點常被懷疑，值得推一次。\n\n**堆展開路線的不變量**：堆中永遠包含「所有已彈出元素的後繼中，尚未彈出的最小候選」。因此第 `i` 次彈出的必是全域第 `i` 小。visited 保證每個座標只被推入一次，否則會重複計數。\n\n## 逐步演算法\n\n**1. 先判斷要走哪條路。** 問「第 K 個值是多少」→ 二分答案；問「前 K 個東西分別是什麼」→ 小根堆展開。\n\n:::example 乘法表：計算 <= x 的元素個數\n**2.（二分答案）寫出 `countLE(x)`。** 利用題目結構讓計數變快。例如乘法表第 `i` 列不超過 `x` 的個數是 `min(n, x / i)`，一列 `O(1)`，全表 `O(n)`。\n\n```cpp\n// 乘法表：計算 <= x 的元素個數\nlong long countLE(int x, int n) {\n    long long cnt = 0;\n    for (int i = 1; i <= n; ++i) {\n        cnt += min(n, x / i);\n    }\n    return cnt;\n}\n```\n:::\n\n\n\n**3.（二分答案）設好二分上下界。** 下界是最小可能值、上界是最大可能值（如 `n * n`），再套上面的二分骨架。\n\n:::example 取出第 k 小：彈出 k 次，每次推入後繼\n**4.（小根堆）每彈一個就補它的後繼。** 用「已彈出第 K 次時的值」當答案；推入前可加 `visited` 防重複。\n\n```cpp\n// 取出第 k 小：彈出 k 次，每次推入後繼\nwhile (k-- > 1) {\n    auto [sum, j] = pq.top();\n    pq.pop();\n    if (j + 1 < n2) {\n        pq.push({/*新和*/, j + 1});  // 同一條鏈往後一格\n    }\n}\n```\n:::\n\n\n\n**5. 邊界與複雜度。** 二分答案是 `O(計數成本 × log值域)`，如乘法表為 `O(n log(n*n))`；堆展開取前 K 個是 `O(k log k)`。注意值域可能很大，二分變數用 `long long` 避免溢位。\n\n## C++17 模板\n\n以下用本節代表題 378. 有序矩陣中第 K 小的元素 展示核心寫法。\n\n**完整題目**：給定每列、每欄皆升序的 `n x n` 矩陣，回傳其中第 `k` 小的元素；重複值要分別計入排名。\n\n:::example 二分值域：在排序矩陣中找第 k 小的數\n**線索**：題目問第 K 小/大，但候選集合太大不能全部列出；能計算 `<= x` 的候選數量，就是二分排名的線索。\n\n```cpp\nclass Solution {\npublic:\n    // 二分值域：在排序矩陣中找第 k 小的數\n    int kthSmallest(vector<vector<int>>& matrix, int k) {\n            int n = matrix.size();\n            int l = matrix[0][0], r = matrix[n - 1][n - 1];  // 值域範圍\n            // 計算矩陣中 <= x 的元素個數（從左下角階梯狀掃描）\n            auto countLE = [&](int x) {\n                    int cnt = 0, row = n - 1, col = 0;  // 從左下角出發\n                    while (row >= 0 && col < n) {\n                            if (matrix[row][col] <= x) {\n                                    cnt += row + 1;  // 同欄 [0..row] 均 <= x\n                                    ++col;\n                            } else {\n                                    row--;  // 當前元素太大，往上走\n                            }\n                    }\n                    return cnt;\n            };\n            // 找最小的 x，使得 <= x 的個數 >= k\n            while (l < r) {\n                    int mid = l + (r - l) / 2;\n                    if (countLE(mid) >= k) {\n                            r = mid;  // mid 可能是答案，繼續縮小\n                    } else {\n                            l = mid + 1;\n                    }\n            }\n            return l;\n    }\n};\n```\n:::\n\n## 程式碼拆解\n\n- 二分的是元素值而不是矩陣下標；`countLE(x)` 利用行列有序在線性時間計算排名，最後找第一個排名至少為 `k` 的值。\n- `l` 與 `r` 表示仍可能包含答案的範圍，`mid` 只是用來判斷要丟掉哪一半。\n- 每個分支都要能說明被排除的區間為何不可能含答案；這就是二分不變式。\n\n## 時間與空間複雜度\n\n**二分答案路線**：`O(countLE 成本 × log W)`。\n\n- 378 有序矩陣第 K 小：`countLE` 走階梯 `O(n)`，值域 `W` 是矩陣值域，整體 `O(n log W)`。\n- 668 乘法表第 K 小：`countLE` 是 `O(n)`，值域是 `n * n`，整體 `O(n log(n^2)) = O(n log n)`。\n- 719 第 K 小數對距離：先排序 `O(n log n)`，`countLE` 用雙指標 `O(n)`，整體 `O(n log n + n log W)`。\n\n空間 `O(1)`（排序除外）。\n\n**堆展開路線**：取前 K 個是 `O(k log k)`，空間 `O(k)`。\n\n**選型判準**：`k` 很小時堆展開更快（`O(k log k)` 與 `n` 無關）；`k` 接近元素總數時二分答案更穩（複雜度與 `k` 無關）。378 在 `k` 到 `n^2` 時，堆展開會退化成 `O(n^2 log n)`，二分答案仍是 `O(n log W)`。\n\n## 常見錯誤與邊界條件\n\n- 二分值域時回傳值，不是回傳下標。\n- `count(<= mid) >= k` 表示答案在左半邊含 mid。\n- 若要回傳實際分數/配對，計數時還要同步維護最接近門檻的候選。\n- 大值域與乘法計數要用 long long 避免溢位。\n\n## 常見變形\n\n- 矩陣第 K 小：用行列有序計數。\n- 乘法表第 K 小：逐列加總 `<= x` 的個數。\n- 數對距離第 K 小：排序後雙指標計數距離不超過 `x` 的數對。\n\n## 與相似技巧的比較\n\n- **對比整體排序**：`O(N log N)`，`N` 是元素總數。有序矩陣的 `N = n^2`，攤平排序是 `O(n^2 log n)`；二分答案是 `O(n log W)`，差一個量級。\n- **對比 quickselect**：平均 `O(N)` 求第 K 小，但需要能隨機存取所有元素、會破壞順序、最壞 `O(N^2)`。元素是「隱式」的（乘法表、所有數對）時根本無法套用——這正是二分答案的主場。\n- **對比大小為 K 的堆**：維護 K 大小的堆求第 K 大是 `O(N log k)`，同樣需要走訪所有元素。\n- **對比第 3 節「求最小」**：本節其實就是求最小的一個實例——`check(x)` 就是 `countLE(x) >= k`。認出這點之後，模板完全共用。\n- **對比分間接值**（第 5 節）：兩者都是二分一個值配計數函數。差別在本節二分的值就是答案，第 5 節二分完還要再合成。\n\n## 代表例題\n\n**題號**：373、378、668、719、786\n\n把每個 `nums1[i]` 搭配 `nums2` 的和視為一條遞增鏈，用小根堆只展開當前最小的下一對；適合對照「產生前 K 個」與「只求第 K 個值」的差別。\n\n- 378 有序矩陣中第 K 小的元素：矩陣行列有序時，不必攤平成 `n^2` 個數；對值 `x` 從左下或右上計算 `<= x` 的數量，二分第一個計數達到 `k` 的值。\n- 668 乘法表中第 K 小的數：乘法表不用真的建表；固定 `x` 後第 `i` 列有 `min(n, x / i)` 個數不超過 `x`，這就是排名判斷。\n- 719 找出第 K 小的數對距離：先排序陣列，固定距離 `x` 後用雙指標統計距離 `<= x` 的數對數，再二分最小可達第 `k` 名的距離。\n- 786 第 K 個最小的質數分數：分數有序但候選是有理數；可二分值並用雙指標計數，同時記錄不超過門檻的最大分數作答案候選。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 373 | [查詢和最小的 K 對數字](https://leetcode.cn/problems/find-k-pairs-with-smallest-sums) |  |\n| 378 | [有序矩陣中第 K 小的元素](https://leetcode.cn/problems/kth-smallest-element-in-a-sorted-matrix) |  |\n| 668 | [乘法表中第 K 小的數](https://leetcode.cn/problems/kth-smallest-number-in-multiplication-table) |  |\n| 719 | [找出第 K 小的數對距離](https://leetcode.cn/problems/find-k-th-smallest-pair-distance) |  |\n| 786 | [第 K 個最小的質數分數](https://leetcode.cn/problems/k-th-smallest-prime-fraction) |  |\n| 793 | [階乘函式後 K 個零](https://leetcode.cn/problems/preimage-size-of-factorial-zeroes-function) |  |\n| 878 | [第 N 個神奇數字](https://leetcode.cn/problems/nth-magical-number) | 1897 |\n| 1201 | [醜數 III](https://leetcode.cn/problems/ugly-number-iii) | 2039 |\n| 1439 | [有序矩陣中的第 k 個最小陣列和](https://leetcode.cn/problems/find-the-kth-smallest-sum-of-a-matrix-with-sorted-rows) | 2134 |\n| 1508 | [子陣列和排序後的區間和](https://leetcode.cn/problems/range-sum-of-sorted-subarray-sums) | 1402 |\n| 1918 | [第 K 小的子陣列和](https://leetcode.cn/problems/kth-smallest-subarray-sum) |  |\n| 2040 | [兩個有序陣列的第 K 小乘積](https://leetcode.cn/problems/kth-smallest-product-of-two-sorted-arrays) | 2518 |\n| 2386 | [找出陣列的第 K 大和](https://leetcode.cn/problems/find-the-k-sum-of-an-array) | 2648 |\n| 3116 | [單面值組合的第 K 小金額](https://leetcode.cn/problems/kth-smallest-amount-with-single-denomination-combination) | 2388 |\n| 3134 | [找出唯一性陣列的中位數](https://leetcode.cn/problems/find-the-median-of-the-uniqueness-array) | 2451 |\n| 3520 | [逆序對計數的最小閾值](https://leetcode.cn/problems/minimum-threshold-for-inversion-pairs-count) |  |\n| 3691 | [最大子陣列總值 II](https://leetcode.cn/problems/maximum-total-subarray-value-ii) | 2469 |\n\n## 本節重點速查\n\n先分清楚問的是「第 K 個**值**」（二分答案）還是「前 K 個**東西**」（小根堆展開）。二分答案的關鍵是找到好算的 `countLE(x)`——有序矩陣走階梯、乘法表用 `Σ min(n, x / i)`、數對距離用雙指標，都是 `O(n)`。核心命題：**使 `countLE(x) >= k` 的最小 `x` 就是第 K 小值**，而且它一定是集合裡真實存在的元素（計數只在真實元素處跨過門檻）。在**值域**上二分，回傳值不是下標。值域大用 `long long`。`k` 很小時堆展開 `O(k log k)` 更快；`k` 大時二分答案 `O(n log W)` 更穩。\n",
    },
    {
      id: 12,
      title: "9. 三分法",
      summary:
        "## 這個技術解決什麼問題\n\n最小化（或最大化）一個對連續參數**先降後升**（或先升後降）的代價函數。\n\n二分法在這裡完全用不上：它需要一條 `F…F T…T` 的真假分界，而單峰函數沒有分界，只有一個極值點。三分法補的正是這個位置——它不比較「可不可行」，而是比較**函數值本身**。\n\n典型場景是幾何最佳化：1515 服務中心的最佳位置要找一個座標，使到所有客戶的距離總和最小。這個總和對 `x`（固定 `y` 時）是凸函數，先降後升。\n\n## 辨識題型的訊號\n\n看到下列特徵，往「三分法」想：\n\n- **要最小化／最大化一個值，而這個值對某個連續參數是「先降後升」或「先升後降」。** 關鍵字像「最佳位置」「最小總代價」「使……最小的座標」。\n- **答案是一個連續座標或實數，不是離散選項**，而且代價函數平滑、單峰。\n- **二分搜尋用不上**：沒有「是/否」的單調分界，只有一個極值點。這正是三分法補位的地方。\n- **可以在常數時間或線性時間內評估 `f(x)`**（給定 `x` 算出代價），否則每輪評估太貴就不划算。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的代價函數對目標參數真的是單峰（凸或凹）嗎？能不能說明為什麼？\n2. 我要找的是最小值還是最大值？比較方向（`<` 還是 `>`）對應對了嗎？\n3. 是一維還是多維？多維時內層要對哪個變數再三分？\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n本節要解決的場景是：有一個函數 `f(x)`，它先「一路下降到最低點、再一路上升」（這種形狀叫**單峰／凸函數**，這裡是「谷形」），我們想找出讓 `f(x)` 最小的那個 `x`。普通二分搜尋在這裡用不了——二分需要「左邊全錯、右邊全對」這種單調分界，而谷形函數兩邊都會升高，沒有這種分界。\n\n:::example 比較兩個內點，判斷最低點落在哪一側\n三分法（ternary search）的核心想法：在區間裡取兩個點 `m1 < m2`，比較 `f(m1)` 和 `f(m2)`，就能砍掉三分之一的區間。\n\n```cpp\n// 比較兩個內點，判斷最低點落在哪一側\ndouble m1 = lo + (hi - lo) / 3;\ndouble m2 = hi - (hi - lo) / 3;\nif (f(m1) < f(m2)) {\n    hi = m2;  // 最低點不在 m2 右邊，丟掉右段\n} else {\n    lo = m1;  // 最低點不在 m1 左邊，丟掉左段\n}\n```\n:::\n\n\n\n為什麼能這樣砍？因為函數是谷形：如果 `f(m1) < f(m2)`，那最低點絕不可能在 `m2` 的右側（右側只會更高），所以 `[m2, hi]` 整段安全捨棄。反之亦然。每一輪區間縮成原來的 `2/3`，重複夠多次就逼近谷底。\n\n像 1515（服務中心最佳位置）就是典型：要找一個點使「到所有客戶的距離總和」最小，這個總和隨座標移動是凸函數，沿 x 與 y 各做一次三分即可。\n\n## 狀態／資料結構定義\n\n**參數區間 `[lo, hi]`**：答案所在的連續範圍，通常由輸入座標的最小／最大值決定。\n\n**代價函數 `f(x)`**：給定參數算出代價。必須是**單峰**的——凸（先降後升，求最小）或凹（先升後降，求最大）。\n\n**兩個內點**：`m1 = lo + (hi - lo) / 3`、`m2 = hi - (hi - lo) / 3`，滿足 `lo < m1 < m2 < hi`。\n\n**收斂方式**：浮點用**固定迭代次數**（例如 100 輪），不要用 `while (hi - lo > eps)`。整數三分則在區間縮到 3 格以內時直接暴力比較，避免兩內點重合卡死。\n\n**巢狀三分**（1515 是二維）：外層對 `x` 三分，內層對每個固定的 `x` 再對 `y` 三分。內層的最小值構成的函數對 `x` 仍是凸的，這是巢狀能成立的理由。\n\n## 不變量或正確性證明\n\n**不變量**：極值點始終落在 `[lo, hi]` 內。\n\n**縮減步驟的正確性**（以求最小、`f` 凸為例）。取 `lo < m1 < m2 < hi`，比較 `f(m1)` 與 `f(m2)`：\n\n- 若 `f(m1) < f(m2)`：極值點不可能在 `(m2, hi]`。反證——設極值點 `x* > m2`，由凸性，`f` 在 `[lo, x*]` 上單調不增，而 `m1 < m2 <= x*`，於是 `f(m1) >= f(m2)`，與假設矛盾。故可令 `hi = m2`。\n- 若 `f(m1) > f(m2)`：對稱地，極值點不在 `[lo, m1)`，令 `lo = m1`。\n- 若相等：凸函數的平坦段必包含極值點，兩側都可以捨，令 `lo = m1`（或 `hi = m2`）皆可。\n\n**收斂速率**：每輪區間長度變成原來的 `2/3`，`t` 輪後是 `(2/3)^t`。要把 `1e9` 的區間壓到 `1e-7`，需要 `t ≈ log(1e16) / log(1.5) ≈ 91` 輪，所以 100 輪是安全的預設值。\n\n**單峰是硬前提**：函數若有多個局部極值，上述反證會失效——`f(m1) < f(m2)` 不再能排除右半邊。動手前一定要能說出「為什麼這個函數是凸的」，例如「若干個凸函數之和仍是凸函數」（1515 的距離和就是這樣得證的）。\n\n## 逐步演算法\n\n**1. 先確認函數是單峰的。** 三分法只對「凸／凹」函數成立。距離總和、拋物線型代價、先減後增的時間成本，通常是凸的；若函數忽上忽下，三分會找錯。\n\n:::example 連續版：固定迭代次數，足夠逼近精度\n**2. 設定區間與停止條件。** 連續值用「區間夠窄就停」（看精度需求），整數則用「區間長度小到可暴力掃」收尾。\n\n```cpp\n// 連續版：固定迭代次數，足夠逼近精度\nfor (int iter = 0; iter < 100; ++iter) {\n    double m1 = lo + (hi - lo) / 3;\n    double m2 = hi - (hi - lo) / 3;\n    if (f(m1) < f(m2)) {\n        hi = m2;\n    } else {\n        lo = m1;\n    }\n}\n```\n:::\n\n\n\n:::example 1515：固定一點，算到所有客戶的距離總和\n**3. 寫出評估函數 `f(x)`。** 這是題目的本體，例如 1515 的 `f(x, y)` 是到所有點的歐式距離和。\n\n```cpp\n// 1515：固定一點，算到所有客戶的距離總和\ndouble cost(double x, double y, vector<vector<int>>& pos) {\n    double s = 0;\n    for (auto& p : pos) {\n        s += hypot(x - p[0], y - p[1]);\n    }\n    return s;\n}\n```\n:::\n\n\n\n:::example 外層對 x 三分，f(x) 內部再對 y 三分求該行最佳值\n**4. 多維就「嵌套三分」。** 二維問題（如平面選點）對 x 做三分，內層再對 y 做三分；外層每次評估都呼叫一次內層三分。\n\n```cpp\n// 外層對 x 三分，f(x) 內部再對 y 三分求該行最佳值\ndouble bestAtX(double x) { /* 對 y 做一次三分，回傳最小 cost */\n}\n```\n:::\n\n\n\n**5. 邊界與複雜度。** 每輪砍掉 `1/3`，迭代 `t` 次精度約 `(2/3)^t`，複雜度 `O(t × 單次評估)`；二維嵌套是 `O(t^2 × 單次評估)`。整數三分要小心邊界，最後幾格直接暴力比較，避免兩內點重合卡死。\n\n## C++17 模板\n\n以 1515. 服務中心的最佳位置 為例。\n\n**完整題目**：給若干客戶的平面座標，找一個服務中心位置，使到所有客戶的**歐氏距離之和**最小，回傳這個最小總距離。\n\n**線索**：距離和對 `(x, y)` 是凸函數（每個點到定點的歐氏距離是凸的，凸函數之和仍凸），沒有真假分界，只有唯一極小值 —— 用巢狀三分。\n\n```cpp\n// 1515. 服務中心的最佳位置：對 x 三分，內層對 y 三分\nclass Solution {\npublic:\n    double getMinDistSum(vector<vector<int>>& positions) {\n        // 固定 x，回傳對 y 最佳化後的最小距離和\n        auto bestForX = [&](double x) {\n            auto cost = [&](double y) {\n                double sum = 0;\n                for (auto& p : positions)\n                    sum += hypot(p[0] - x, p[1] - y);\n                return sum;\n            };\n            double lo = 0, hi = 100;  // 題目座標範圍 0..100\n            for (int it = 0; it < 100; ++it) {  // 固定輪數，別用 eps 當迴圈條件\n                double m1 = lo + (hi - lo) / 3;\n                double m2 = hi - (hi - lo) / 3;\n                if (cost(m1) < cost(m2)) hi = m2;  // 極小值不在 (m2, hi]\n                else lo = m1;                      // 極小值不在 [lo, m1)\n            }\n            return cost(lo);\n        };\n\n        double lo = 0, hi = 100;\n        for (int it = 0; it < 100; ++it) {\n            double m1 = lo + (hi - lo) / 3;\n            double m2 = hi - (hi - lo) / 3;\n            if (bestForX(m1) < bestForX(m2)) hi = m2;\n            else lo = m1;\n        }\n        return bestForX(lo);\n    }\n};\n```\n\n巢狀能成立，是因為「對每個 `x` 取 `y` 的最小值」得到的函數對 `x` 仍是凸的。\n\n## 時間與空間複雜度\n\n**一維三分**：`O(t × 單次評估成本)`，`t` 是迭代輪數。評估 `f(x)` 通常是 `O(n)`，取 `t = 100` 時整體 `O(100n)`。\n\n**二維巢狀三分**：`O(t^2 × 單次評估)`。1515 是 `100 × 100 × 2 × n = 2e4 × n`——`n` 只有 50，所以約 `1e6`，剛好可行。這個 `t^2` 是巢狀三分的實際上限，三維幾乎必然超時。\n\n**空間 `O(1)`**。\n\n**輪數怎麼挑**：每輪乘 `2/3`，100 輪把區間壓到 `(2/3)^100 ≈ 2.5e-18` 倍，遠超 `double` 的有效位數。輪數再多也不會更準，只是浪費時間；再少則可能不夠。100 是安全且慣用的預設。\n\n**注意每輪要算兩次 `f`**，巢狀時內層又各算一輪，成本是相乘的——評估函數若不是 `O(n)` 而是 `O(n log n)`，就要重新估算。\n\n## 常見錯誤與邊界條件\n\n- **函數其實不單峰**。這是最根本的錯，而且不會有任何錯誤訊息——三分會安靜地收斂到某個局部極值。動手前要能說出凸性的理由（常用「凸函數之和仍凸」）。\n- **用 `eps` 當迴圈條件**。`while (hi - lo > eps)` 在浮點誤差下可能永遠不收斂而 TLE。一律改成固定輪數。\n- **求最大時比較方向沒翻轉**。求最小時 `f(m1) < f(m2)` 捨右半；求最大時要改成 `f(m1) > f(m2)` 捨右半。寫反會收斂到另一端的邊界。\n- **整數三分兩內點重合**。區間縮到 2、3 格時 `m1` 可能等於 `m2`，迴圈卡死。整數版要在 `hi - lo <= 2`（或更寬鬆的門檻）時跳出，直接暴力比較剩下的幾格。\n- **巢狀時內層輪數設太少**。內層若不夠準，外層比較的兩個值就帶著雜訊，外層可能往錯誤方向收斂。內外層都用 100 輪。\n- **上下界沒涵蓋最佳解**。1515 的座標範圍是 0..100，設窄了答案會落在邊界上。\n- **誤用二分**。沒有真假分界卻硬套二分，回傳值毫無意義。判準很簡單：問「有沒有一條 `F…F T…T` 的線」，沒有就用三分。\n\n## 與相似技巧的比較\n\n- **對比二分法**：判斷依據完全不同。二分比較的是「可不可行」（需要 `F…F T…T` 的分界），三分比較的是「函數值大小」（需要單峰）。選型判準一句話：**有真假分界用二分，只有唯一極值用三分**。\n- **對比梯度下降**：同樣求連續極值，梯度下降需要導數且對步長敏感、可能收斂到局部極值；三分只要函數值可評估，在單峰前提下保證收斂到全域極值。競賽場合三分更可靠。\n- **對比爬山法／模擬退火**：1515 也常見用模擬退火解，能處理非凸函數，但沒有正確性保證、要調參。函數可證凸時三分更穩。\n- **對比枚舉所有候選**：參數是連續的，根本枚舉不完；即使離散化成 `1e6` 格也比 100 輪三分慢得多。\n- **對比整數二分求極值**：若函數在整數上是單峰，也可以二分「差分的正負號」——`f(x + 1) - f(x)` 由負轉正的位置就是極小點，這其實把三分問題轉回二分問題，在整數情形常更好寫。\n\n## 代表例題\n\n**題號**：1515\n\n重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1515 | [服務中心的最佳位置](https://leetcode.cn/problems/best-position-for-a-service-centre) | 2157 |\n\n## 本節重點速查\n\n**沒有真假分界、只有唯一極值**時用三分。取 `m1 = lo + (hi - lo) / 3`、`m2 = hi - (hi - lo) / 3`，求最小時 `f(m1) < f(m2)` 就令 `hi = m2`，否則 `lo = m1`（求最大要翻轉比較方向）。正確性靠凸性反證：`f(m1) < f(m2)` 保證極值不在 `(m2, hi]`。每輪區間乘 `2/3`，**固定跑 100 輪**，別用 `eps` 當迴圈條件。整數三分要在剩 3 格內跳出暴力比較，否則兩內點重合會卡死。二維巢狀是 `O(t^2 × 評估成本)`，1515 剛好可行，三維幾乎必超時。動手前先說得出「為什麼這函數是凸的」（常用凸函數之和仍凸）。\n",
    },
    {
      id: 13,
      title: "10. 其他",
      summary:
        "## 這個技術解決什麼問題\n\n把二分推廣到「不是一條乾淨的有序陣列」的場合。\n\n本節收的都是同一個道理的變形：**只要能在 `O(1)` 或低成本內判斷「答案在左半還是右半」，就能二分**，資料本身是否全域有序並不重要。\n\n- **旋轉排序陣列**（33、81、153、154）：全域無序，但任一中點必有一側是有序的，據此判斷。\n- **二維矩陣**（74、240）：把矩陣視為攤平後的有序序列，或從右上角走階梯。\n- **在答案值域上二分**（69 平方根、278 第一個錯誤版本）：資料根本不是陣列。\n- **局部性質**（162 尋找峰值）：只靠鄰居比較就能決定往哪一側走。\n- **雙序列分割**（4 兩正序陣列的中位數）：二分的是「切在哪裡」。\n\n## 辨識題型的訊號\n\n看到下列特徵，往「二分」想：\n\n- **資料已排序（或部分有序、或矩陣行列有序）**，且要查找、定位、或求某個臨界值。有序是二分的天然溫床。\n- **答案能改寫成「第一個／最後一個滿足某條件的值或位置」**，而那個條件具單調性（過了門檻就一直成立）。\n- **暴力是線性掃描 `O(n)`，但題目規模或多次查詢逼你做到 `O(log n)`。**\n- **陣列被旋轉、或要在兩個有序序列間找全域排名**——這些是「變形二分」的招牌題，核心仍是找單調分界。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的搜尋空間是「下標」還是「值域」？範圍上下界設對了嗎？\n2. 我要的是「第一個 true」還是「最後一個 true」？`mid` 取法與更新方向配對了嗎？\n3. 我的 `check(mid)` 條件真的單調嗎（前 false 後 true，不會反覆）？\n\n三題都答得出來，模型就套對了。\n\n## 核心想法與直覺\n\n本節是二分搜尋的「綜合場」，收錄各種不完全套在「最小化最大值／第 K 小」模板裡的二分題：旋轉排序陣列（33、81）、x 的平方根（69）、二維矩陣搜尋（74）、兩個有序陣列的中位數（4）。它們表面差很多，但骨子裡都是同一句話：**在一個有「單調分界」的搜尋空間上，用對折的方式快速逼近答案。**\n\n:::example 找第一個讓 check 為 true 的位置（lower_bound 思路）\n二分能用的唯一前提是：把問題改寫成一個「是/否」判斷後，答案空間長成「前面一段全是 false、後面一段全是 true」（或反過來）。我們要找的，就是這條分界線。\n\n```cpp\n// 找第一個讓 check 為 true 的位置（lower_bound 思路）\nwhile (lo < hi) {\n    int mid = lo + (hi - lo) / 2;\n    if (check(mid)) {\n        hi = mid;  // 達標，分界在 mid 或更左\n    } else {\n        lo = mid + 1;  // 不達標，分界在右\n    }\n}\n// lo 即第一個 check 為 true 的位置\n```\n:::\n\n\n\n最直接的應用是 69（x 的平方根）：`check(m) = (m * m <= x)`，前面全 true、後面全 false，找「最後一個平方仍 `<= x` 的數」。74（二維矩陣）則是把 `m×n` 矩陣攤平看成一個有序陣列，用 `idx/n` 和 `idx % n` 還原到 `(row, col)`，普通二分即可。\n\n:::example 33：每次至少有一半有序，判斷目標在不在有序的那半\n旋轉排序陣列（33、81）特別一點：陣列被旋轉後不再整段有序，但**對半切後，至少有一半仍是有序的**，於是每一步先判斷哪半有序、再看目標是否落在那半。\n\n```cpp\n// 33：每次至少有一半有序，判斷目標在不在有序的那半\nif (nums[lo] <= nums[mid]) {  // 左半有序\n    if (nums[lo] <= target && target < nums[mid]) {\n        hi = mid - 1;\n    } else {\n        lo = mid + 1;\n    }\n} else { /* 右半有序，對稱處理 */\n}\n```\n:::\n\n## 狀態／資料結構定義\n\n共通的仍是「搜尋區間 + 判定條件」，只是判定條件變複雜：\n\n- **旋轉陣列**：先比 `nums[lo]` 與 `nums[mid]` 判斷哪一側有序，再判斷 target 是否落在那個有序區間內。81、154 因為有重複元素，`nums[lo] == nums[mid] == nums[hi]` 時無法判斷，只能 `++lo`／`--hi` 退化成線性。\n- **二維矩陣**（74）：每列首尾遞增，可直接把 `(r, c)` 與一維下標 `idx` 互換：`r = idx / n`、`c = idx % n`，然後對 `[0, m * n)` 二分。\n- **尋找峰值**（162）：只需比較 `nums[mid]` 與 `nums[mid + 1]`。\n- **兩陣列中位數**（4）：在較短陣列上二分切點 `i`，另一陣列的切點由 `j = (m + n + 1) / 2 - i` 決定，用四個邊界值 `L1, R1, L2, R2` 判斷切得對不對。\n- **69 平方根**：在 `[0, x]` 上二分，判定是 `mid * mid <= x`，用 `long long` 防溢位。\n\n## 不變量或正確性證明\n\n**旋轉陣列的關鍵引理**：對任意 `mid`，`[lo, mid]` 與 `[mid, hi]` 至少有一側是完全有序的。\n\n理由：旋轉排序陣列最多只有一個「下降點」。若這個下降點在 `[lo, mid]` 內，那 `[mid, hi]` 就不含下降點、因而有序；反之亦然。判斷方式是比較 `nums[lo] <= nums[mid]`——成立則左側有序。\n\n有了有序的一側，就能 `O(1)` 判斷 target 在不在裡面，據此決定捨棄哪一半。不變量「target 若存在必在區間內」因此得以維持。\n\n**重複元素為何會退化**（81、154）：當 `nums[lo] == nums[mid] == nums[hi]` 時，兩側都可能有序也都可能含下降點，引理失效，無法判斷該捨哪一半。唯一安全的動作是 `++lo`（只丟掉一個確定與 `nums[mid]` 相等的元素），最壞情形（全部相同）退化成 `O(n)`。這個最壞界是資訊論下界，無法改善。\n\n**尋找峰值的正確性**（162）：若 `nums[mid] < nums[mid + 1]`，則 `[mid + 1, hi]` 內必有峰值——因為從 `mid + 1` 開始往右，序列要嘛一路上升到 `hi`（`hi` 即峰值，邊界視為負無窮），要嘛中途下降（下降點前即峰值）。這個論證只用到局部比較與邊界假設，不需要全域有序。\n\n**兩陣列中位數的不變量**（4）：切點 `i` 合法的條件是 `L1 <= R2 且 L2 <= R1`。若 `L1 > R2` 表示 `i` 切太右，令 `hi = i - 1`；若 `L2 > R1` 表示切太左，令 `lo = i + 1`。合法時左半的最大值與右半的最小值即可組出中位數。\n\n## 逐步演算法\n\n**1. 把題目翻譯成「單調的是/否問題」。** 先問：我要找的東西，能不能寫成「第一個滿足某條件的值/位置」？能，就能二分。\n\n:::example 69：在值域 [0, x] 上二分平方根\n**2. 選定搜尋空間。** 可能是「下標範圍」（74、旋轉陣列），也可能是「值域範圍」（69 在 `[0, x]` 上找平方根）。\n\n```cpp\n// 69：在值域 [0, x] 上二分平方根\nlong long lo = 0, hi = x;\nwhile (lo < hi) {\n    long long mid = lo + (hi - lo + 1) / 2;  // 取右中點，避免死循環\n    if (mid * mid <= x) {\n        lo = mid;\n    } else {\n        hi = mid - 1;\n    }\n}\n```\n:::\n\n\n\n**3. 寫對 `mid` 與邊界收斂。** 找「第一個 true」用左中點配 `hi = mid`；找「最後一個 true」用右中點配 `lo = mid`。選錯會死循環。\n\n**4. 旋轉陣列：先定有序半，再縮範圍。** 每輪用 `nums[lo] <= nums[mid]` 判斷左半是否有序；81 有重複值時，遇到 `nums[lo] == nums[mid] == nums[hi]` 就 `lo++ 、hi--` 退化處理。\n\n:::example 4：二分短陣列的切割數 i，推出長陣列切割 j，檢查左右交界\n**5. 中位數（4）：在較短陣列上二分切割點。** 把兩陣列各切一刀，使左邊總數固定且「左邊最大 <= 右邊最小」，二分那條切割線即可達 `O(log(min(m,n)))`。\n\n```cpp\n// 4：二分短陣列的切割數 i，推出長陣列切割 j，檢查左右交界\nint i = (lo + hi) / 2;\nint j = (m + n + 1) / 2 - i;  // 保證左半元素總數固定\n```\n:::\n\n\n\n**6. 邊界與複雜度。** 多數是 `O(log n)`；74 是 `O(log(mn))`；4 是 `O(log(min(m,n)))`。乘法、平方注意用 `long long` 防溢位；空陣列、目標不存在、全相同元素都要單獨測。\n\n## C++17 模板\n\n以 33. 搜尋旋轉排序陣列 為例——它最能代表本節「判定條件變複雜、骨架不變」的精神。\n\n**完整題目**：升序陣列在某個未知樞軸上旋轉過（元素互異），給 `target`，回傳它的下標，不存在則回傳 `-1`。要求 `O(log n)`。\n\n**線索**：全域無序，但每個中點必有一側有序——判斷 target 在不在那個有序側，就能決定捨哪一半。\n\n```cpp\n// 33. 搜尋旋轉排序陣列：先判斷哪一側有序，再判斷 target 在不在裡面\nclass Solution {\npublic:\n    int search(vector<int>& nums, int target) {\n        int lo = 0, hi = nums.size() - 1;\n        while (lo <= hi) {\n            int mid = lo + (hi - lo) / 2;\n            if (nums[mid] == target) return mid;\n\n            if (nums[lo] <= nums[mid]) {  // 左半 [lo, mid] 有序\n                if (nums[lo] <= target && target < nums[mid]) hi = mid - 1;\n                else lo = mid + 1;\n            } else {                      // 右半 [mid, hi] 有序\n                if (nums[mid] < target && target <= nums[hi]) lo = mid + 1;\n                else hi = mid - 1;\n            }\n        }\n        return -1;\n    }\n};\n```\n\n81 有重複元素時，在最前面加一段 `if (nums[lo] == nums[mid] && nums[mid] == nums[hi]) { ++lo; --hi; continue; }`，其餘不變。\n\n74 搜尋二維矩陣則把矩陣當成長度 `m * n` 的有序序列：`int r = mid / n, c = mid % n;`，再套第 1 節的基礎模板。\n\n## 時間與空間複雜度\n\n**33、153、162、69、278**：`O(log n)`，空間 `O(1)`。\n\n**74 二維矩陣**：`O(log(m × n))`。240（每列每行分別有序，但列間不連續）不能這樣攤平，要從右上角走階梯，`O(m + n)`。\n\n**81、154（含重複元素）**：平均 `O(log n)`，**最壞 `O(n)`**。全部元素相同時無法判斷該捨哪一半，只能逐一排除。這個最壞界無法改善——若不掃過所有元素，就無法區分「全部相同」與「其中藏了一個不同的」。\n\n**4 兩正序陣列的中位數**：`O(log(min(m, n)))`，在較短的陣列上二分。這是本節複雜度最漂亮的一題，也最容易寫錯。\n\n**222 完全二元樹的節點個數**：`O(log^2 n)`——二分節點編號，每次驗證存在性要走 `O(log n)` 的路徑。\n\n## 常見錯誤與邊界條件\n\n- **旋轉陣列判斷有序側時用 `<` 而非 `<=`**。`lo == mid`（區間只剩一兩格）時 `nums[lo] < nums[mid]` 為假，會誤判成右側有序。要寫 `nums[lo] <= nums[mid]`。\n- **重複元素沒特判**（81、154）。`nums[lo] == nums[mid] == nums[hi]` 時必須收縮邊界，否則會走錯半邊而漏解。\n- **74 攤平時列數行數用錯**。`r = idx / n`、`c = idx % n`，`n` 是**行數**（每列的長度）。用成列數會全盤錯亂。\n- **69 平方根溢位**。`mid * mid` 在 `mid` 接近 `46341` 時就超過 `int`，要轉 `long long` 或改用除法比較 `mid <= x / mid`。\n- **162 尋找峰值誤加邊界特判**。題目保證 `nums[-1]` 與 `nums[n]` 視為負無窮，直接對 `[0, n - 1]` 用「比較 `mid` 與 `mid + 1`」的模板即可，額外的特判反而出錯。\n- **4 的切點公式寫錯**。`j = (m + n + 1) / 2 - i` 的 `+1` 是為了讓奇數長度時左半多一個，去掉會在奇數情形錯。另外務必在**較短**的陣列上二分，否則 `j` 可能為負。\n- **回傳值語意混淆**。有些題要下標（33），有些要值（153），有些要布林（74）。收斂後 `lo` 代表什麼，動手前先寫下來。\n- 一律用「空陣列、單元素、target 在頭／在尾／不存在、全部相同」這組測資掃一遍邊界。\n\n## 與相似技巧的比較\n\n- **對比第 1 節基礎二分**：骨架完全相同，差別只在判定條件。本節的價值就是示範「判定條件可以很複雜，二分照樣成立」——只要它能 `O(1)` 決定捨哪一半。\n- **對比 240 搜尋二維矩陣 II**：74 的每列首尾遞增，可攤平成一維二分 `O(log(mn))`；240 只保證每列每行分別有序，攤平後不是有序序列，必須從右上角走階梯 `O(m + n)`。兩題長得像但性質不同，是常見的陷阱。\n- **對比 4 的合併解法**：兩正序陣列的中位數也能用「合併到第 `(m+n)/2` 個」的雙指標，`O(m + n)`。二分切點降到 `O(log(min(m, n)))`，但邊界條件多得多——面試時先講清楚兩種解法的取捨。\n- **對比 162 的線性掃描**：尋找峰值掃一遍就能找到，`O(n)`。題目要求 `O(log n)` 才需要二分；這題的價值在於示範「不需要全域有序，只要局部性質能決定方向」。\n- **對比 69 的牛頓法**：求平方根用牛頓迭代收斂更快（二次收斂），但二分更好寫也不會有浮點細節問題，整數平方根用二分就夠。\n\n## 代表例題\n\n**題號**：4、33、69、74、81\n\n重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。\n\n- 4 尋找兩個正序陣列的中位數：先把候選集合視為有排名的多重集合；練習寫 `count(<= x)`，再找第一個排名達標的值。\n\n## 例題與分級練習\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 4 | [尋找兩個正序陣列的中位數](https://leetcode.cn/problems/median-of-two-sorted-arrays) |  |\n| 33 | [搜尋旋轉排序陣列](https://leetcode.cn/problems/search-in-rotated-sorted-array) |  |\n| 69 | [x 的平方根](https://leetcode.cn/problems/sqrtx) |  |\n| 74 | [搜尋二維矩陣](https://leetcode.cn/problems/search-a-2d-matrix) |  |\n| 81 | [搜尋旋轉排序陣列 II](https://leetcode.cn/problems/search-in-rotated-sorted-array-ii) |  |\n| 153 | [尋找旋轉排序陣列中的最小值](https://leetcode.cn/problems/find-minimum-in-rotated-sorted-array) |  |\n| 154 | [尋找旋轉排序陣列中的最小值 II](https://leetcode.cn/problems/find-minimum-in-rotated-sorted-array-ii) |  |\n| 162 | [尋找峰值](https://leetcode.cn/problems/find-peak-element) |  |\n| 222 | [完全二叉樹的節點個數](https://leetcode.cn/problems/count-complete-tree-nodes) |  |\n| 278 | [第一個錯誤的版本](https://leetcode.cn/problems/first-bad-version) |  |\n| 302 | [包含全部黑色像素的最小矩形](https://leetcode.cn/problems/smallest-rectangle-enclosing-black-pixels) |  |\n| 374 | [猜數字大小](https://leetcode.cn/problems/guess-number-higher-or-lower) |  |\n| 540 | [有序陣列中的單一元素](https://leetcode.cn/problems/single-element-in-a-sorted-array) |  |\n| 702 | [搜尋長度未知的有序陣列](https://leetcode.cn/problems/search-in-a-sorted-array-of-unknown-size) |  |\n| 852 | [山脈陣列的峰頂索引](https://leetcode.cn/problems/peak-index-in-a-mountain-array) | 1182 |\n| 1060 | [有序陣列中的缺失元素](https://leetcode.cn/problems/missing-element-in-sorted-array) |  |\n| 1064 | [不動點](https://leetcode.cn/problems/fixed-point) | 1308 |\n| 1095 | [山脈陣列中查詢目標值](https://leetcode.cn/problems/find-in-mountain-array) | 1827 |\n| 1198 | [找出所有行中最小公共元素](https://leetcode.cn/problems/find-smallest-common-element-in-all-rows) | 1401 |\n| 1428 | [至少有一個 1 的最左端列](https://leetcode.cn/problems/leftmost-column-with-at-least-a-one) |  |\n| 1533 | [找到最大整數的索引](https://leetcode.cn/problems/find-the-index-of-the-large-integer) |  |\n| 1539 | [第 k 個缺失的正整數](https://leetcode.cn/problems/kth-missing-positive-number) | 1295 |\n| 1901 | [尋找峰值 II](https://leetcode.cn/problems/find-a-peak-element-ii) |  |\n| 2387 | [行排序矩陣的中位數](https://leetcode.cn/problems/median-of-a-row-wise-sorted-matrix) |  |\n| 2936 | [包含相等值數字塊的數量](https://leetcode.cn/problems/number-of-equal-numbers-blocks) |  |\n\n## 本節重點速查\n\n本節的共通道理只有一句：**只要能低成本判斷「答案在左半還是右半」，就能二分，資料不必全域有序**。旋轉陣列（33、153）靠「任一中點必有一側有序」這個引理，判斷時要寫 `nums[lo] <= nums[mid]`（用 `<` 會在剩兩格時誤判）；有重複元素（81、154）時三者相等就無法判斷，只能收縮邊界，最壞退化 `O(n)` 且無法改善。74 把矩陣攤平成 `[0, m*n)` 一維二分（`r = idx / n`、`c = idx % n`），但 240 不能這樣做，要走階梯。69 的 `mid * mid` 會溢位，改用 `mid <= x / mid`。4 在**較短**陣列上二分切點，`j = (m + n + 1) / 2 - i` 的 `+1` 不能省。收斂後 `lo` 代表下標、值還是布林，動手前先寫下來。\n",
    },
    {
      id: 9003,
      title: "11. 常見面試模式",
      summary:
        "二分搜尋在面試中很少是裸題，多半藏在「單調性」或「可行性判定」的背後。下表整理面試最常見的二分變形與其辨識訊號。\n\n| 模式 | 訊號 | 核心做法 | 代表題 |\n| --- | --- | --- | --- |\n| 二分答案 | 最小化最大值、可行性對答案單調 | 對答案區間二分，寫 `check` 判定可行 | [分割數組的最大值](https://leetcode.cn/problems/split-array-largest-sum) |\n| 旋轉陣列搜尋 | 有序陣列被旋轉、找目標或最小值 | 判斷哪半段有序再縮小範圍 | [搜索旋轉排序數組](https://leetcode.cn/problems/search-in-rotated-sorted-array) |\n| 第 K 小 | 在有序矩陣或多序列找第 $k$ 小 | 對值域二分，計數不超過 $mid$ 的個數 | [有序矩陣中第 K 小的元素](https://leetcode.cn/problems/kth-smallest-element-in-a-sorted-matrix) |\n| 實數二分 | 答案為浮點、精度要求 | 固定迭代次數或以 $\\epsilon$ 控制收斂 | [愛吃香蕉的珂珂](https://leetcode.cn/problems/koko-eating-bananas) |\n| 邊界二分 | 找第一個/最後一個滿足條件的位置 | 用 `lower_bound` / `upper_bound` 思路 | [在排序數組中查找元素的第一個和最後一個位置](https://leetcode.cn/problems/find-first-and-last-position-of-element-in-sorted-array) |\n| 峰值二分 | 局部最大、相鄰元素不等 | 依斜率方向走向峰頂 | [尋找峰值](https://leetcode.cn/problems/find-peak-element) |\n\n小提醒：\n\n- 二分答案的核心是寫對 `check`：先確認「答案越大越容易/越難可行」的單調方向。\n- 整數二分務必固定區間語義（閉區間或左閉右開），避免死循環與越界。\n- 實數二分用固定迭代次數（如 100 次）比判 $r-l<\\epsilon$ 更穩，整體 `O(log range)`。",
    },
  ],
} as TutorialData.Root;
